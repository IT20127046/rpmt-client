// modules are defined as an array
// [ module function, map of requires ]
//
// map of requires is short require name -> numeric require
//
// anything defined in a previous bundle is accessed via the
// orig method which is the require for previous bundles

(function (modules, entry, mainEntry, parcelRequireName, globalName) {
  /* eslint-disable no-undef */
  var globalObject =
    typeof globalThis !== 'undefined'
      ? globalThis
      : typeof self !== 'undefined'
      ? self
      : typeof window !== 'undefined'
      ? window
      : typeof global !== 'undefined'
      ? global
      : {};
  /* eslint-enable no-undef */

  // Save the require from previous bundle to this closure if any
  var previousRequire =
    typeof globalObject[parcelRequireName] === 'function' &&
    globalObject[parcelRequireName];

  var cache = previousRequire.cache || {};
  // Do not use `require` to prevent Webpack from trying to bundle this call
  var nodeRequire =
    typeof module !== 'undefined' &&
    typeof module.require === 'function' &&
    module.require.bind(module);

  function newRequire(name, jumped) {
    if (!cache[name]) {
      if (!modules[name]) {
        // if we cannot find the module within our internal map or
        // cache jump to the current global require ie. the last bundle
        // that was added to the page.
        var currentRequire =
          typeof globalObject[parcelRequireName] === 'function' &&
          globalObject[parcelRequireName];
        if (!jumped && currentRequire) {
          return currentRequire(name, true);
        }

        // If there are other bundles on this page the require from the
        // previous one is saved to 'previousRequire'. Repeat this as
        // many times as there are bundles until the module is found or
        // we exhaust the require chain.
        if (previousRequire) {
          return previousRequire(name, true);
        }

        // Try the node require function if it exists.
        if (nodeRequire && typeof name === 'string') {
          return nodeRequire(name);
        }

        var err = new Error("Cannot find module '" + name + "'");
        err.code = 'MODULE_NOT_FOUND';
        throw err;
      }

      localRequire.resolve = resolve;
      localRequire.cache = {};

      var module = (cache[name] = new newRequire.Module(name));

      modules[name][0].call(
        module.exports,
        localRequire,
        module,
        module.exports,
        this
      );
    }

    return cache[name].exports;

    function localRequire(x) {
      var res = localRequire.resolve(x);
      return res === false ? {} : newRequire(res);
    }

    function resolve(x) {
      var id = modules[name][1][x];
      return id != null ? id : x;
    }
  }

  function Module(moduleName) {
    this.id = moduleName;
    this.bundle = newRequire;
    this.exports = {};
  }

  newRequire.isParcelRequire = true;
  newRequire.Module = Module;
  newRequire.modules = modules;
  newRequire.cache = cache;
  newRequire.parent = previousRequire;
  newRequire.register = function (id, exports) {
    modules[id] = [
      function (require, module) {
        module.exports = exports;
      },
      {},
    ];
  };

  Object.defineProperty(newRequire, 'root', {
    get: function () {
      return globalObject[parcelRequireName];
    },
  });

  globalObject[parcelRequireName] = newRequire;

  for (var i = 0; i < entry.length; i++) {
    newRequire(entry[i]);
  }

  if (mainEntry) {
    // Expose entry point to Node, AMD or browser globals
    // Based on https://github.com/ForbesLindesay/umd/blob/master/template.js
    var mainExports = newRequire(mainEntry);

    // CommonJS
    if (typeof exports === 'object' && typeof module !== 'undefined') {
      module.exports = mainExports;

      // RequireJS
    } else if (typeof define === 'function' && define.amd) {
      define(function () {
        return mainExports;
      });

      // <script>
    } else if (globalName) {
      this[globalName] = mainExports;
    }
  }
})({"kn9T2":[function(require,module,exports) {
var Refresh = require('react-refresh/runtime');
Refresh.injectIntoGlobalHook(window);
window.$RefreshReg$ = function() {};
window.$RefreshSig$ = function() {
    return function(type) {
        return type;
    };
};

},{"react-refresh/runtime":"786KC"}],"786KC":[function(require,module,exports) {
'use strict';
module.exports = require('./cjs/react-refresh-runtime.development.js');

},{"./cjs/react-refresh-runtime.development.js":"hdge7"}],"hdge7":[function(require,module,exports) {
/** @license React v0.9.0
 * react-refresh-runtime.development.js
 *
 * Copyright (c) Facebook, Inc. and its affiliates.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE file in the root directory of this source tree.
 */ 'use strict';
(function() {
    // ATTENTION
    // When adding new symbols to this file,
    // Please consider also adding to 'react-devtools-shared/src/backend/ReactSymbols'
    // The Symbol used to tag the ReactElement-like types. If there is no native Symbol
    // nor polyfill, then a plain number is used for performance.
    var REACT_ELEMENT_TYPE = 0xeac7;
    var REACT_PORTAL_TYPE = 0xeaca;
    var REACT_FRAGMENT_TYPE = 0xeacb;
    var REACT_STRICT_MODE_TYPE = 0xeacc;
    var REACT_PROFILER_TYPE = 0xead2;
    var REACT_PROVIDER_TYPE = 0xeacd;
    var REACT_CONTEXT_TYPE = 0xeace;
    var REACT_FORWARD_REF_TYPE = 0xead0;
    var REACT_SUSPENSE_TYPE = 0xead1;
    var REACT_SUSPENSE_LIST_TYPE = 0xead8;
    var REACT_MEMO_TYPE = 0xead3;
    var REACT_LAZY_TYPE = 0xead4;
    var REACT_BLOCK_TYPE = 0xead9;
    var REACT_SERVER_BLOCK_TYPE = 0xeada;
    var REACT_FUNDAMENTAL_TYPE = 0xead5;
    var REACT_SCOPE_TYPE = 0xead7;
    var REACT_OPAQUE_ID_TYPE = 0xeae0;
    var REACT_DEBUG_TRACING_MODE_TYPE = 0xeae1;
    var REACT_OFFSCREEN_TYPE = 0xeae2;
    var REACT_LEGACY_HIDDEN_TYPE = 0xeae3;
    if (typeof Symbol === 'function' && Symbol.for) {
        var symbolFor = Symbol.for;
        REACT_ELEMENT_TYPE = symbolFor('react.element');
        REACT_PORTAL_TYPE = symbolFor('react.portal');
        REACT_FRAGMENT_TYPE = symbolFor('react.fragment');
        REACT_STRICT_MODE_TYPE = symbolFor('react.strict_mode');
        REACT_PROFILER_TYPE = symbolFor('react.profiler');
        REACT_PROVIDER_TYPE = symbolFor('react.provider');
        REACT_CONTEXT_TYPE = symbolFor('react.context');
        REACT_FORWARD_REF_TYPE = symbolFor('react.forward_ref');
        REACT_SUSPENSE_TYPE = symbolFor('react.suspense');
        REACT_SUSPENSE_LIST_TYPE = symbolFor('react.suspense_list');
        REACT_MEMO_TYPE = symbolFor('react.memo');
        REACT_LAZY_TYPE = symbolFor('react.lazy');
        REACT_BLOCK_TYPE = symbolFor('react.block');
        REACT_SERVER_BLOCK_TYPE = symbolFor('react.server.block');
        REACT_FUNDAMENTAL_TYPE = symbolFor('react.fundamental');
        REACT_SCOPE_TYPE = symbolFor('react.scope');
        REACT_OPAQUE_ID_TYPE = symbolFor('react.opaque.id');
        REACT_DEBUG_TRACING_MODE_TYPE = symbolFor('react.debug_trace_mode');
        REACT_OFFSCREEN_TYPE = symbolFor('react.offscreen');
        REACT_LEGACY_HIDDEN_TYPE = symbolFor('react.legacy_hidden');
    }
    var PossiblyWeakMap = typeof WeakMap === 'function' ? WeakMap : Map; // We never remove these associations.
    // It's OK to reference families, but use WeakMap/Set for types.
    var allFamiliesByID = new Map();
    var allFamiliesByType = new PossiblyWeakMap();
    var allSignaturesByType = new PossiblyWeakMap(); // This WeakMap is read by React, so we only put families
    // that have actually been edited here. This keeps checks fast.
    // $FlowIssue
    var updatedFamiliesByType = new PossiblyWeakMap(); // This is cleared on every performReactRefresh() call.
    // It is an array of [Family, NextType] tuples.
    var pendingUpdates = []; // This is injected by the renderer via DevTools global hook.
    var helpersByRendererID = new Map();
    var helpersByRoot = new Map(); // We keep track of mounted roots so we can schedule updates.
    var mountedRoots = new Set(); // If a root captures an error, we remember it so we can retry on edit.
    var failedRoots = new Set(); // In environments that support WeakMap, we also remember the last element for every root.
    // It needs to be weak because we do this even for roots that failed to mount.
    // If there is no WeakMap, we won't attempt to do retrying.
    // $FlowIssue
    var rootElements = typeof WeakMap === 'function' ? new WeakMap() : null;
    var isPerformingRefresh = false;
    function computeFullKey(signature) {
        if (signature.fullKey !== null) return signature.fullKey;
        var fullKey = signature.ownKey;
        var hooks;
        try {
            hooks = signature.getCustomHooks();
        } catch (err) {
            // This can happen in an edge case, e.g. if expression like Foo.useSomething
            // depends on Foo which is lazily initialized during rendering.
            // In that case just assume we'll have to remount.
            signature.forceReset = true;
            signature.fullKey = fullKey;
            return fullKey;
        }
        for(var i = 0; i < hooks.length; i++){
            var hook = hooks[i];
            if (typeof hook !== 'function') {
                // Something's wrong. Assume we need to remount.
                signature.forceReset = true;
                signature.fullKey = fullKey;
                return fullKey;
            }
            var nestedHookSignature = allSignaturesByType.get(hook);
            if (nestedHookSignature === undefined) continue;
            var nestedHookKey = computeFullKey(nestedHookSignature);
            if (nestedHookSignature.forceReset) signature.forceReset = true;
            fullKey += '\n---\n' + nestedHookKey;
        }
        signature.fullKey = fullKey;
        return fullKey;
    }
    function haveEqualSignatures(prevType, nextType) {
        var prevSignature = allSignaturesByType.get(prevType);
        var nextSignature = allSignaturesByType.get(nextType);
        if (prevSignature === undefined && nextSignature === undefined) return true;
        if (prevSignature === undefined || nextSignature === undefined) return false;
        if (computeFullKey(prevSignature) !== computeFullKey(nextSignature)) return false;
        if (nextSignature.forceReset) return false;
        return true;
    }
    function isReactClass(type) {
        return type.prototype && type.prototype.isReactComponent;
    }
    function canPreserveStateBetween(prevType, nextType) {
        if (isReactClass(prevType) || isReactClass(nextType)) return false;
        if (haveEqualSignatures(prevType, nextType)) return true;
        return false;
    }
    function resolveFamily(type) {
        // Only check updated types to keep lookups fast.
        return updatedFamiliesByType.get(type);
    } // If we didn't care about IE11, we could use new Map/Set(iterable).
    function cloneMap(map) {
        var clone = new Map();
        map.forEach(function(value, key) {
            clone.set(key, value);
        });
        return clone;
    }
    function cloneSet(set) {
        var clone = new Set();
        set.forEach(function(value) {
            clone.add(value);
        });
        return clone;
    }
    function performReactRefresh() {
        if (pendingUpdates.length === 0) return null;
        if (isPerformingRefresh) return null;
        isPerformingRefresh = true;
        try {
            var staleFamilies = new Set();
            var updatedFamilies = new Set();
            var updates = pendingUpdates;
            pendingUpdates = [];
            updates.forEach(function(_ref) {
                var family = _ref[0], nextType = _ref[1];
                // Now that we got a real edit, we can create associations
                // that will be read by the React reconciler.
                var prevType = family.current;
                updatedFamiliesByType.set(prevType, family);
                updatedFamiliesByType.set(nextType, family);
                family.current = nextType; // Determine whether this should be a re-render or a re-mount.
                if (canPreserveStateBetween(prevType, nextType)) updatedFamilies.add(family);
                else staleFamilies.add(family);
            }); // TODO: rename these fields to something more meaningful.
            var update = {
                updatedFamilies: updatedFamilies,
                // Families that will re-render preserving state
                staleFamilies: staleFamilies // Families that will be remounted
            };
            helpersByRendererID.forEach(function(helpers) {
                // Even if there are no roots, set the handler on first update.
                // This ensures that if *new* roots are mounted, they'll use the resolve handler.
                helpers.setRefreshHandler(resolveFamily);
            });
            var didError = false;
            var firstError = null; // We snapshot maps and sets that are mutated during commits.
            // If we don't do this, there is a risk they will be mutated while
            // we iterate over them. For example, trying to recover a failed root
            // may cause another root to be added to the failed list -- an infinite loop.
            var failedRootsSnapshot = cloneSet(failedRoots);
            var mountedRootsSnapshot = cloneSet(mountedRoots);
            var helpersByRootSnapshot = cloneMap(helpersByRoot);
            failedRootsSnapshot.forEach(function(root) {
                var helpers = helpersByRootSnapshot.get(root);
                if (helpers === undefined) throw new Error('Could not find helpers for a root. This is a bug in React Refresh.');
                failedRoots.has(root);
                if (rootElements === null) return;
                if (!rootElements.has(root)) return;
                var element = rootElements.get(root);
                try {
                    helpers.scheduleRoot(root, element);
                } catch (err) {
                    if (!didError) {
                        didError = true;
                        firstError = err;
                    } // Keep trying other roots.
                }
            });
            mountedRootsSnapshot.forEach(function(root) {
                var helpers = helpersByRootSnapshot.get(root);
                if (helpers === undefined) throw new Error('Could not find helpers for a root. This is a bug in React Refresh.');
                mountedRoots.has(root);
                try {
                    helpers.scheduleRefresh(root, update);
                } catch (err) {
                    if (!didError) {
                        didError = true;
                        firstError = err;
                    } // Keep trying other roots.
                }
            });
            if (didError) throw firstError;
            return update;
        } finally{
            isPerformingRefresh = false;
        }
    }
    function register(type, id) {
        if (type === null) return;
        if (typeof type !== 'function' && typeof type !== 'object') return;
         // This can happen in an edge case, e.g. if we register
        // return value of a HOC but it returns a cached component.
        // Ignore anything but the first registration for each type.
        if (allFamiliesByType.has(type)) return;
         // Create family or remember to update it.
        // None of this bookkeeping affects reconciliation
        // until the first performReactRefresh() call above.
        var family = allFamiliesByID.get(id);
        if (family === undefined) {
            family = {
                current: type
            };
            allFamiliesByID.set(id, family);
        } else pendingUpdates.push([
            family,
            type
        ]);
        allFamiliesByType.set(type, family); // Visit inner types because we might not have registered them.
        if (typeof type === 'object' && type !== null) switch(type.$$typeof){
            case REACT_FORWARD_REF_TYPE:
                register(type.render, id + '$render');
                break;
            case REACT_MEMO_TYPE:
                register(type.type, id + '$type');
                break;
        }
    }
    function setSignature(type, key) {
        var forceReset = arguments.length > 2 && arguments[2] !== undefined ? arguments[2] : false;
        var getCustomHooks = arguments.length > 3 ? arguments[3] : undefined;
        allSignaturesByType.set(type, {
            forceReset: forceReset,
            ownKey: key,
            fullKey: null,
            getCustomHooks: getCustomHooks || function() {
                return [];
            }
        });
    } // This is lazily called during first render for a type.
    // It captures Hook list at that time so inline requires don't break comparisons.
    function collectCustomHooksForSignature(type) {
        var signature = allSignaturesByType.get(type);
        if (signature !== undefined) computeFullKey(signature);
    }
    function getFamilyByID(id) {
        return allFamiliesByID.get(id);
    }
    function getFamilyByType(type) {
        return allFamiliesByType.get(type);
    }
    function findAffectedHostInstances(families) {
        var affectedInstances = new Set();
        mountedRoots.forEach(function(root) {
            var helpers = helpersByRoot.get(root);
            if (helpers === undefined) throw new Error('Could not find helpers for a root. This is a bug in React Refresh.');
            var instancesForRoot = helpers.findHostInstancesForRefresh(root, families);
            instancesForRoot.forEach(function(inst) {
                affectedInstances.add(inst);
            });
        });
        return affectedInstances;
    }
    function injectIntoGlobalHook(globalObject) {
        // For React Native, the global hook will be set up by require('react-devtools-core').
        // That code will run before us. So we need to monkeypatch functions on existing hook.
        // For React Web, the global hook will be set up by the extension.
        // This will also run before us.
        var hook = globalObject.__REACT_DEVTOOLS_GLOBAL_HOOK__;
        if (hook === undefined) {
            // However, if there is no DevTools extension, we'll need to set up the global hook ourselves.
            // Note that in this case it's important that renderer code runs *after* this method call.
            // Otherwise, the renderer will think that there is no global hook, and won't do the injection.
            var nextID = 0;
            globalObject.__REACT_DEVTOOLS_GLOBAL_HOOK__ = hook = {
                renderers: new Map(),
                supportsFiber: true,
                inject: function(injected) {
                    return nextID++;
                },
                onScheduleFiberRoot: function(id, root, children) {},
                onCommitFiberRoot: function(id, root, maybePriorityLevel, didError) {},
                onCommitFiberUnmount: function() {}
            };
        } // Here, we just want to get a reference to scheduleRefresh.
        var oldInject = hook.inject;
        hook.inject = function(injected) {
            var id = oldInject.apply(this, arguments);
            if (typeof injected.scheduleRefresh === 'function' && typeof injected.setRefreshHandler === 'function') // This version supports React Refresh.
            helpersByRendererID.set(id, injected);
            return id;
        }; // Do the same for any already injected roots.
        // This is useful if ReactDOM has already been initialized.
        // https://github.com/facebook/react/issues/17626
        hook.renderers.forEach(function(injected, id) {
            if (typeof injected.scheduleRefresh === 'function' && typeof injected.setRefreshHandler === 'function') // This version supports React Refresh.
            helpersByRendererID.set(id, injected);
        }); // We also want to track currently mounted roots.
        var oldOnCommitFiberRoot = hook.onCommitFiberRoot;
        var oldOnScheduleFiberRoot = hook.onScheduleFiberRoot || function() {};
        hook.onScheduleFiberRoot = function(id, root, children) {
            if (!isPerformingRefresh) {
                // If it was intentionally scheduled, don't attempt to restore.
                // This includes intentionally scheduled unmounts.
                failedRoots.delete(root);
                if (rootElements !== null) rootElements.set(root, children);
            }
            return oldOnScheduleFiberRoot.apply(this, arguments);
        };
        hook.onCommitFiberRoot = function(id, root, maybePriorityLevel, didError) {
            var helpers = helpersByRendererID.get(id);
            if (helpers === undefined) return;
            helpersByRoot.set(root, helpers);
            var current = root.current;
            var alternate = current.alternate; // We need to determine whether this root has just (un)mounted.
            // This logic is copy-pasted from similar logic in the DevTools backend.
            // If this breaks with some refactoring, you'll want to update DevTools too.
            if (alternate !== null) {
                var wasMounted = alternate.memoizedState != null && alternate.memoizedState.element != null;
                var isMounted = current.memoizedState != null && current.memoizedState.element != null;
                if (!wasMounted && isMounted) {
                    // Mount a new root.
                    mountedRoots.add(root);
                    failedRoots.delete(root);
                } else if (wasMounted && isMounted) ;
                else if (wasMounted && !isMounted) {
                    // Unmount an existing root.
                    mountedRoots.delete(root);
                    if (didError) // We'll remount it on future edits.
                    failedRoots.add(root);
                    else helpersByRoot.delete(root);
                } else if (!wasMounted && !isMounted) {
                    if (didError) // We'll remount it on future edits.
                    failedRoots.add(root);
                }
            } else // Mount a new root.
            mountedRoots.add(root);
            return oldOnCommitFiberRoot.apply(this, arguments);
        };
    }
    function hasUnrecoverableErrors() {
        // TODO: delete this after removing dependency in RN.
        return false;
    } // Exposed for testing.
    function _getMountedRootCount() {
        return mountedRoots.size;
    } // This is a wrapper over more primitive functions for setting signature.
    // Signatures let us decide whether the Hook order has changed on refresh.
    //
    // This function is intended to be used as a transform target, e.g.:
    // var _s = createSignatureFunctionForTransform()
    //
    // function Hello() {
    //   const [foo, setFoo] = useState(0);
    //   const value = useCustomHook();
    //   _s(); /* Second call triggers collecting the custom Hook list.
    //          * This doesn't happen during the module evaluation because we
    //          * don't want to change the module order with inline requires.
    //          * Next calls are noops. */
    //   return <h1>Hi</h1>;
    // }
    //
    // /* First call specifies the signature: */
    // _s(
    //   Hello,
    //   'useState{[foo, setFoo]}(0)',
    //   () => [useCustomHook], /* Lazy to avoid triggering inline requires */
    // );
    function createSignatureFunctionForTransform() {
        // We'll fill in the signature in two steps.
        // First, we'll know the signature itself. This happens outside the component.
        // Then, we'll know the references to custom Hooks. This happens inside the component.
        // After that, the returned function will be a fast path no-op.
        var status = 'needsSignature';
        var savedType;
        var hasCustomHooks;
        return function(type, key, forceReset, getCustomHooks) {
            switch(status){
                case 'needsSignature':
                    if (type !== undefined) {
                        // If we received an argument, this is the initial registration call.
                        savedType = type;
                        hasCustomHooks = typeof getCustomHooks === 'function';
                        setSignature(type, key, forceReset, getCustomHooks); // The next call we expect is from inside a function, to fill in the custom Hooks.
                        status = 'needsCustomHooks';
                    }
                    break;
                case 'needsCustomHooks':
                    if (hasCustomHooks) collectCustomHooksForSignature(savedType);
                    status = 'resolved';
                    break;
            }
            return type;
        };
    }
    function isLikelyComponentType(type) {
        switch(typeof type){
            case 'function':
                // First, deal with classes.
                if (type.prototype != null) {
                    if (type.prototype.isReactComponent) // React class.
                    return true;
                    var ownNames = Object.getOwnPropertyNames(type.prototype);
                    if (ownNames.length > 1 || ownNames[0] !== 'constructor') // This looks like a class.
                    return false;
                     // eslint-disable-next-line no-proto
                    if (type.prototype.__proto__ !== Object.prototype) // It has a superclass.
                    return false;
                     // Pass through.
                // This looks like a regular function with empty prototype.
                } // For plain functions and arrows, use name as a heuristic.
                var name = type.name || type.displayName;
                return typeof name === 'string' && /^[A-Z]/.test(name);
            case 'object':
                if (type != null) switch(type.$$typeof){
                    case REACT_FORWARD_REF_TYPE:
                    case REACT_MEMO_TYPE:
                        // Definitely React components.
                        return true;
                    default:
                        return false;
                }
                return false;
            default:
                return false;
        }
    }
    exports._getMountedRootCount = _getMountedRootCount;
    exports.collectCustomHooksForSignature = collectCustomHooksForSignature;
    exports.createSignatureFunctionForTransform = createSignatureFunctionForTransform;
    exports.findAffectedHostInstances = findAffectedHostInstances;
    exports.getFamilyByID = getFamilyByID;
    exports.getFamilyByType = getFamilyByType;
    exports.hasUnrecoverableErrors = hasUnrecoverableErrors;
    exports.injectIntoGlobalHook = injectIntoGlobalHook;
    exports.isLikelyComponentType = isLikelyComponentType;
    exports.performReactRefresh = performReactRefresh;
    exports.register = register;
    exports.setSignature = setSignature;
})();

},{}],"7fmqN":[function(require,module,exports) {
"use strict";
var HMR_HOST = null;
var HMR_PORT = null;
var HMR_SECURE = false;
var HMR_ENV_HASH = "d6ea1d42532a7575";
module.bundle.HMR_BUNDLE_ID = "890e741a975ef6c8";
function _toConsumableArray(arr) {
    return _arrayWithoutHoles(arr) || _iterableToArray(arr) || _unsupportedIterableToArray(arr) || _nonIterableSpread();
}
function _nonIterableSpread() {
    throw new TypeError("Invalid attempt to spread non-iterable instance.\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method.");
}
function _iterableToArray(iter) {
    if (typeof Symbol !== "undefined" && iter[Symbol.iterator] != null || iter["@@iterator"] != null) return Array.from(iter);
}
function _arrayWithoutHoles(arr) {
    if (Array.isArray(arr)) return _arrayLikeToArray(arr);
}
function _createForOfIteratorHelper(o, allowArrayLike) {
    var it = typeof Symbol !== "undefined" && o[Symbol.iterator] || o["@@iterator"];
    if (!it) {
        if (Array.isArray(o) || (it = _unsupportedIterableToArray(o)) || allowArrayLike && o && typeof o.length === "number") {
            if (it) o = it;
            var i = 0;
            var F = function F() {};
            return {
                s: F,
                n: function n() {
                    if (i >= o.length) return {
                        done: true
                    };
                    return {
                        done: false,
                        value: o[i++]
                    };
                },
                e: function e(_e) {
                    throw _e;
                },
                f: F
            };
        }
        throw new TypeError("Invalid attempt to iterate non-iterable instance.\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method.");
    }
    var normalCompletion = true, didErr = false, err;
    return {
        s: function s() {
            it = it.call(o);
        },
        n: function n() {
            var step = it.next();
            normalCompletion = step.done;
            return step;
        },
        e: function e(_e2) {
            didErr = true;
            err = _e2;
        },
        f: function f() {
            try {
                if (!normalCompletion && it.return != null) it.return();
            } finally{
                if (didErr) throw err;
            }
        }
    };
}
function _unsupportedIterableToArray(o, minLen) {
    if (!o) return;
    if (typeof o === "string") return _arrayLikeToArray(o, minLen);
    var n = Object.prototype.toString.call(o).slice(8, -1);
    if (n === "Object" && o.constructor) n = o.constructor.name;
    if (n === "Map" || n === "Set") return Array.from(o);
    if (n === "Arguments" || /^(?:Ui|I)nt(?:8|16|32)(?:Clamped)?Array$/.test(n)) return _arrayLikeToArray(o, minLen);
}
function _arrayLikeToArray(arr, len) {
    if (len == null || len > arr.length) len = arr.length;
    for(var i = 0, arr2 = new Array(len); i < len; i++)arr2[i] = arr[i];
    return arr2;
}
/* global HMR_HOST, HMR_PORT, HMR_ENV_HASH, HMR_SECURE, chrome, browser */ /*::
import type {
  HMRAsset,
  HMRMessage,
} from '@parcel/reporter-dev-server/src/HMRServer.js';
interface ParcelRequire {
  (string): mixed;
  cache: {|[string]: ParcelModule|};
  hotData: mixed;
  Module: any;
  parent: ?ParcelRequire;
  isParcelRequire: true;
  modules: {|[string]: [Function, {|[string]: string|}]|};
  HMR_BUNDLE_ID: string;
  root: ParcelRequire;
}
interface ParcelModule {
  hot: {|
    data: mixed,
    accept(cb: (Function) => void): void,
    dispose(cb: (mixed) => void): void,
    // accept(deps: Array<string> | string, cb: (Function) => void): void,
    // decline(): void,
    _acceptCallbacks: Array<(Function) => void>,
    _disposeCallbacks: Array<(mixed) => void>,
  |};
}
interface ExtensionContext {
  runtime: {|
    reload(): void,
  |};
}
declare var module: {bundle: ParcelRequire, ...};
declare var HMR_HOST: string;
declare var HMR_PORT: string;
declare var HMR_ENV_HASH: string;
declare var HMR_SECURE: boolean;
declare var chrome: ExtensionContext;
declare var browser: ExtensionContext;
*/ var OVERLAY_ID = '__parcel__error__overlay__';
var OldModule = module.bundle.Module;
function Module(moduleName) {
    OldModule.call(this, moduleName);
    this.hot = {
        data: module.bundle.hotData,
        _acceptCallbacks: [],
        _disposeCallbacks: [],
        accept: function accept(fn) {
            this._acceptCallbacks.push(fn || function() {});
        },
        dispose: function dispose(fn) {
            this._disposeCallbacks.push(fn);
        }
    };
    module.bundle.hotData = undefined;
}
module.bundle.Module = Module;
var checkedAssets, acceptedAssets, assetsToAccept /*: Array<[ParcelRequire, string]> */ ;
function getHostname() {
    return HMR_HOST || (location.protocol.indexOf('http') === 0 ? location.hostname : 'localhost');
}
function getPort() {
    return HMR_PORT || location.port;
} // eslint-disable-next-line no-redeclare
var parent = module.bundle.parent;
if ((!parent || !parent.isParcelRequire) && typeof WebSocket !== 'undefined') {
    var hostname = getHostname();
    var port = getPort();
    var protocol = HMR_SECURE || location.protocol == 'https:' && !/localhost|127.0.0.1|0.0.0.0/.test(hostname) ? 'wss' : 'ws';
    var ws = new WebSocket(protocol + '://' + hostname + (port ? ':' + port : '') + '/'); // $FlowFixMe
    ws.onmessage = function(event) {
        checkedAssets = {} /*: {|[string]: boolean|} */ ;
        acceptedAssets = {} /*: {|[string]: boolean|} */ ;
        assetsToAccept = [];
        var data = JSON.parse(event.data);
        if (data.type === 'update') {
            // Remove error overlay if there is one
            if (typeof document !== 'undefined') removeErrorOverlay();
            var assets = data.assets.filter(function(asset) {
                return asset.envHash === HMR_ENV_HASH;
            }); // Handle HMR Update
            var handled = assets.every(function(asset) {
                return asset.type === 'css' || asset.type === 'js' && hmrAcceptCheck(module.bundle.root, asset.id, asset.depsByBundle);
            });
            if (handled) {
                console.clear();
                assets.forEach(function(asset) {
                    hmrApply(module.bundle.root, asset);
                });
                for(var i = 0; i < assetsToAccept.length; i++){
                    var id = assetsToAccept[i][1];
                    if (!acceptedAssets[id]) hmrAcceptRun(assetsToAccept[i][0], id);
                }
            } else if ('reload' in location) location.reload();
            else {
                // Web extension context
                var ext = typeof chrome === 'undefined' ? typeof browser === 'undefined' ? null : browser : chrome;
                if (ext && ext.runtime && ext.runtime.reload) ext.runtime.reload();
            }
        }
        if (data.type === 'error') {
            // Log parcel errors to console
            var _iterator = _createForOfIteratorHelper(data.diagnostics.ansi), _step;
            try {
                for(_iterator.s(); !(_step = _iterator.n()).done;){
                    var ansiDiagnostic = _step.value;
                    var stack = ansiDiagnostic.codeframe ? ansiDiagnostic.codeframe : ansiDiagnostic.stack;
                    console.error('🚨 [parcel]: ' + ansiDiagnostic.message + '\n' + stack + '\n\n' + ansiDiagnostic.hints.join('\n'));
                }
            } catch (err) {
                _iterator.e(err);
            } finally{
                _iterator.f();
            }
            if (typeof document !== 'undefined') {
                // Render the fancy html overlay
                removeErrorOverlay();
                var overlay = createErrorOverlay(data.diagnostics.html); // $FlowFixMe
                document.body.appendChild(overlay);
            }
        }
    };
    ws.onerror = function(e) {
        console.error(e.message);
    };
    ws.onclose = function() {
        console.warn('[parcel] 🚨 Connection to the HMR server was lost');
    };
}
function removeErrorOverlay() {
    var overlay = document.getElementById(OVERLAY_ID);
    if (overlay) {
        overlay.remove();
        console.log('[parcel] ✨ Error resolved');
    }
}
function createErrorOverlay(diagnostics) {
    var overlay = document.createElement('div');
    overlay.id = OVERLAY_ID;
    var errorHTML = '<div style="background: black; opacity: 0.85; font-size: 16px; color: white; position: fixed; height: 100%; width: 100%; top: 0px; left: 0px; padding: 30px; font-family: Menlo, Consolas, monospace; z-index: 9999;">';
    var _iterator2 = _createForOfIteratorHelper(diagnostics), _step2;
    try {
        for(_iterator2.s(); !(_step2 = _iterator2.n()).done;){
            var diagnostic = _step2.value;
            var stack = diagnostic.codeframe ? diagnostic.codeframe : diagnostic.stack;
            errorHTML += "\n      <div>\n        <div style=\"font-size: 18px; font-weight: bold; margin-top: 20px;\">\n          \uD83D\uDEA8 ".concat(diagnostic.message, "\n        </div>\n        <pre>").concat(stack, "</pre>\n        <div>\n          ").concat(diagnostic.hints.map(function(hint) {
                return '<div>💡 ' + hint + '</div>';
            }).join(''), "\n        </div>\n        ").concat(diagnostic.documentation ? "<div>\uD83D\uDCDD <a style=\"color: violet\" href=\"".concat(diagnostic.documentation, "\" target=\"_blank\">Learn more</a></div>") : '', "\n      </div>\n    ");
        }
    } catch (err) {
        _iterator2.e(err);
    } finally{
        _iterator2.f();
    }
    errorHTML += '</div>';
    overlay.innerHTML = errorHTML;
    return overlay;
}
function getParents(bundle, id) /*: Array<[ParcelRequire, string]> */ {
    var modules = bundle.modules;
    if (!modules) return [];
    var parents = [];
    var k, d, dep;
    for(k in modules)for(d in modules[k][1]){
        dep = modules[k][1][d];
        if (dep === id || Array.isArray(dep) && dep[dep.length - 1] === id) parents.push([
            bundle,
            k
        ]);
    }
    if (bundle.parent) parents = parents.concat(getParents(bundle.parent, id));
    return parents;
}
function updateLink(link) {
    var newLink = link.cloneNode();
    newLink.onload = function() {
        if (link.parentNode !== null) // $FlowFixMe
        link.parentNode.removeChild(link);
    };
    newLink.setAttribute('href', link.getAttribute('href').split('?')[0] + '?' + Date.now()); // $FlowFixMe
    link.parentNode.insertBefore(newLink, link.nextSibling);
}
var cssTimeout = null;
function reloadCSS() {
    if (cssTimeout) return;
    cssTimeout = setTimeout(function() {
        var links = document.querySelectorAll('link[rel="stylesheet"]');
        for(var i = 0; i < links.length; i++){
            // $FlowFixMe[incompatible-type]
            var href = links[i].getAttribute('href');
            var hostname = getHostname();
            var servedFromHMRServer = hostname === 'localhost' ? new RegExp('^(https?:\\/\\/(0.0.0.0|127.0.0.1)|localhost):' + getPort()).test(href) : href.indexOf(hostname + ':' + getPort());
            var absolute = /^https?:\/\//i.test(href) && href.indexOf(location.origin) !== 0 && !servedFromHMRServer;
            if (!absolute) updateLink(links[i]);
        }
        cssTimeout = null;
    }, 50);
}
function hmrApply(bundle, asset) {
    var modules = bundle.modules;
    if (!modules) return;
    if (asset.type === 'css') reloadCSS();
    else if (asset.type === 'js') {
        var deps = asset.depsByBundle[bundle.HMR_BUNDLE_ID];
        if (deps) {
            if (modules[asset.id]) {
                // Remove dependencies that are removed and will become orphaned.
                // This is necessary so that if the asset is added back again, the cache is gone, and we prevent a full page reload.
                var oldDeps = modules[asset.id][1];
                for(var dep in oldDeps)if (!deps[dep] || deps[dep] !== oldDeps[dep]) {
                    var id = oldDeps[dep];
                    var parents = getParents(module.bundle.root, id);
                    if (parents.length === 1) hmrDelete(module.bundle.root, id);
                }
            }
            var fn = new Function('require', 'module', 'exports', asset.output);
            modules[asset.id] = [
                fn,
                deps
            ];
        } else if (bundle.parent) hmrApply(bundle.parent, asset);
    }
}
function hmrDelete(bundle, id1) {
    var modules = bundle.modules;
    if (!modules) return;
    if (modules[id1]) {
        // Collect dependencies that will become orphaned when this module is deleted.
        var deps = modules[id1][1];
        var orphans = [];
        for(var dep in deps){
            var parents = getParents(module.bundle.root, deps[dep]);
            if (parents.length === 1) orphans.push(deps[dep]);
        } // Delete the module. This must be done before deleting dependencies in case of circular dependencies.
        delete modules[id1];
        delete bundle.cache[id1]; // Now delete the orphans.
        orphans.forEach(function(id) {
            hmrDelete(module.bundle.root, id);
        });
    } else if (bundle.parent) hmrDelete(bundle.parent, id1);
}
function hmrAcceptCheck(bundle, id, depsByBundle) {
    if (hmrAcceptCheckOne(bundle, id, depsByBundle)) return true;
     // Traverse parents breadth first. All possible ancestries must accept the HMR update, or we'll reload.
    var parents = getParents(module.bundle.root, id);
    var accepted = false;
    while(parents.length > 0){
        var v = parents.shift();
        var a = hmrAcceptCheckOne(v[0], v[1], null);
        if (a) // If this parent accepts, stop traversing upward, but still consider siblings.
        accepted = true;
        else {
            // Otherwise, queue the parents in the next level upward.
            var p = getParents(module.bundle.root, v[1]);
            if (p.length === 0) {
                // If there are no parents, then we've reached an entry without accepting. Reload.
                accepted = false;
                break;
            }
            parents.push.apply(parents, _toConsumableArray(p));
        }
    }
    return accepted;
}
function hmrAcceptCheckOne(bundle, id, depsByBundle) {
    var modules = bundle.modules;
    if (!modules) return;
    if (depsByBundle && !depsByBundle[bundle.HMR_BUNDLE_ID]) {
        // If we reached the root bundle without finding where the asset should go,
        // there's nothing to do. Mark as "accepted" so we don't reload the page.
        if (!bundle.parent) return true;
        return hmrAcceptCheck(bundle.parent, id, depsByBundle);
    }
    if (checkedAssets[id]) return true;
    checkedAssets[id] = true;
    var cached = bundle.cache[id];
    assetsToAccept.push([
        bundle,
        id
    ]);
    if (!cached || cached.hot && cached.hot._acceptCallbacks.length) return true;
}
function hmrAcceptRun(bundle, id) {
    var cached = bundle.cache[id];
    bundle.hotData = {};
    if (cached && cached.hot) cached.hot.data = bundle.hotData;
    if (cached && cached.hot && cached.hot._disposeCallbacks.length) cached.hot._disposeCallbacks.forEach(function(cb) {
        cb(bundle.hotData);
    });
    delete bundle.cache[id];
    bundle(id);
    cached = bundle.cache[id];
    if (cached && cached.hot && cached.hot._acceptCallbacks.length) cached.hot._acceptCallbacks.forEach(function(cb) {
        var assetsToAlsoAccept = cb(function() {
            return getParents(module.bundle.root, id);
        });
        if (assetsToAlsoAccept && assetsToAccept.length) // $FlowFixMe[method-unbinding]
        assetsToAccept.push.apply(assetsToAccept, assetsToAlsoAccept);
    });
    acceptedAssets[id] = true;
}

},{}],"8lqZg":[function(require,module,exports) {
var $parcel$ReactRefreshHelpers$20e5 = require("@parcel/transformer-react-refresh-wrap/lib/helpers/helpers.js");
var prevRefreshReg = window.$RefreshReg$;
var prevRefreshSig = window.$RefreshSig$;
$parcel$ReactRefreshHelpers$20e5.prelude(module);

try {
var parcelHelpers = require("@parcel/transformer-js/src/esmodule-helpers.js");
var _jsxDevRuntime = require("react/jsx-dev-runtime");
var _reactDom = require("react-dom");
var _reactDomDefault = parcelHelpers.interopDefault(_reactDom);
var _app = require("./App");
var _appDefault = parcelHelpers.interopDefault(_app);
_reactDomDefault.default.render(/*#__PURE__*/ _jsxDevRuntime.jsxDEV(_appDefault.default, {}, void 0, false, {
    fileName: "src/index.js",
    lineNumber: 4,
    columnNumber: 17
}, undefined), document.getElementById('root'));

  $parcel$ReactRefreshHelpers$20e5.postlude(module);
} finally {
  window.$RefreshReg$ = prevRefreshReg;
  window.$RefreshSig$ = prevRefreshSig;
}
},{"react/jsx-dev-runtime":"iTorj","react-dom":"j6uA9","./App":"2kQhy","@parcel/transformer-js/src/esmodule-helpers.js":"gkKU3","@parcel/transformer-react-refresh-wrap/lib/helpers/helpers.js":"km3Ru"}],"iTorj":[function(require,module,exports) {
'use strict';
module.exports = require('./cjs/react-jsx-dev-runtime.development.js');

},{"./cjs/react-jsx-dev-runtime.development.js":"48uCM"}],"48uCM":[function(require,module,exports) {
/**
 * @license React
 * react-jsx-dev-runtime.development.js
 *
 * Copyright (c) Facebook, Inc. and its affiliates.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE file in the root directory of this source tree.
 */ 'use strict';
(function() {
    var React = require('react');
    // -----------------------------------------------------------------------------
    var enableScopeAPI = false; // Experimental Create Event Handle API.
    var enableCacheElement = false;
    var enableTransitionTracing = false; // No known bugs, but needs performance testing
    var enableLegacyHidden = false; // Enables unstable_avoidThisFallback feature in Fiber
    // stuff. Intended to enable React core members to more easily debug scheduling
    // issues in DEV builds.
    var enableDebugTracing = false; // Track which Fiber(s) schedule render work.
    // ATTENTION
    var REACT_ELEMENT_TYPE = Symbol.for('react.element');
    var REACT_PORTAL_TYPE = Symbol.for('react.portal');
    var REACT_FRAGMENT_TYPE = Symbol.for('react.fragment');
    var REACT_STRICT_MODE_TYPE = Symbol.for('react.strict_mode');
    var REACT_PROFILER_TYPE = Symbol.for('react.profiler');
    var REACT_PROVIDER_TYPE = Symbol.for('react.provider');
    var REACT_CONTEXT_TYPE = Symbol.for('react.context');
    var REACT_FORWARD_REF_TYPE = Symbol.for('react.forward_ref');
    var REACT_SUSPENSE_TYPE = Symbol.for('react.suspense');
    var REACT_SUSPENSE_LIST_TYPE = Symbol.for('react.suspense_list');
    var REACT_MEMO_TYPE = Symbol.for('react.memo');
    var REACT_LAZY_TYPE = Symbol.for('react.lazy');
    var REACT_OFFSCREEN_TYPE = Symbol.for('react.offscreen');
    var MAYBE_ITERATOR_SYMBOL = Symbol.iterator;
    var FAUX_ITERATOR_SYMBOL = '@@iterator';
    function getIteratorFn(maybeIterable) {
        if (maybeIterable === null || typeof maybeIterable !== 'object') return null;
        var maybeIterator = MAYBE_ITERATOR_SYMBOL && maybeIterable[MAYBE_ITERATOR_SYMBOL] || maybeIterable[FAUX_ITERATOR_SYMBOL];
        if (typeof maybeIterator === 'function') return maybeIterator;
        return null;
    }
    var ReactSharedInternals = React.__SECRET_INTERNALS_DO_NOT_USE_OR_YOU_WILL_BE_FIRED;
    function error(format) {
        for(var _len2 = arguments.length, args = new Array(_len2 > 1 ? _len2 - 1 : 0), _key2 = 1; _key2 < _len2; _key2++)args[_key2 - 1] = arguments[_key2];
        printWarning('error', format, args);
    }
    function printWarning(level, format, args) {
        var ReactDebugCurrentFrame = ReactSharedInternals.ReactDebugCurrentFrame;
        var stack = ReactDebugCurrentFrame.getStackAddendum();
        if (stack !== '') {
            format += '%s';
            args = args.concat([
                stack
            ]);
        } // eslint-disable-next-line react-internal/safe-string-coercion
        var argsWithFormat = args.map(function(item) {
            return String(item);
        }); // Careful: RN currently depends on this prefix
        argsWithFormat.unshift('Warning: ' + format); // We intentionally don't use spread (or .apply) directly because it
        // breaks IE9: https://github.com/facebook/react/issues/13610
        // eslint-disable-next-line react-internal/no-production-logging
        Function.prototype.apply.call(console[level], console, argsWithFormat);
    }
    var REACT_MODULE_REFERENCE;
    REACT_MODULE_REFERENCE = Symbol.for('react.module.reference');
    function isValidElementType(type) {
        if (typeof type === 'string' || typeof type === 'function') return true;
         // Note: typeof might be other than 'symbol' or 'number' (e.g. if it's a polyfill).
        if (type === REACT_FRAGMENT_TYPE || type === REACT_PROFILER_TYPE || enableDebugTracing || type === REACT_STRICT_MODE_TYPE || type === REACT_SUSPENSE_TYPE || type === REACT_SUSPENSE_LIST_TYPE || enableLegacyHidden || type === REACT_OFFSCREEN_TYPE || enableScopeAPI || enableCacheElement || enableTransitionTracing) return true;
        if (typeof type === 'object' && type !== null) {
            if (type.$$typeof === REACT_LAZY_TYPE || type.$$typeof === REACT_MEMO_TYPE || type.$$typeof === REACT_PROVIDER_TYPE || type.$$typeof === REACT_CONTEXT_TYPE || type.$$typeof === REACT_FORWARD_REF_TYPE || // types supported by any Flight configuration anywhere since
            // we don't know which Flight build this will end up being used
            // with.
            type.$$typeof === REACT_MODULE_REFERENCE || type.getModuleId !== undefined) return true;
        }
        return false;
    }
    function getWrappedName(outerType, innerType, wrapperName) {
        var displayName = outerType.displayName;
        if (displayName) return displayName;
        var functionName = innerType.displayName || innerType.name || '';
        return functionName !== '' ? wrapperName + "(" + functionName + ")" : wrapperName;
    } // Keep in sync with react-reconciler/getComponentNameFromFiber
    function getContextName(type) {
        return type.displayName || 'Context';
    } // Note that the reconciler package should generally prefer to use getComponentNameFromFiber() instead.
    function getComponentNameFromType(type) {
        if (type == null) // Host root, text node or just invalid type.
        return null;
        if (typeof type.tag === 'number') error("Received an unexpected object in getComponentNameFromType(). This is likely a bug in React. Please file an issue.");
        if (typeof type === 'function') return type.displayName || type.name || null;
        if (typeof type === 'string') return type;
        switch(type){
            case REACT_FRAGMENT_TYPE:
                return 'Fragment';
            case REACT_PORTAL_TYPE:
                return 'Portal';
            case REACT_PROFILER_TYPE:
                return 'Profiler';
            case REACT_STRICT_MODE_TYPE:
                return 'StrictMode';
            case REACT_SUSPENSE_TYPE:
                return 'Suspense';
            case REACT_SUSPENSE_LIST_TYPE:
                return 'SuspenseList';
        }
        if (typeof type === 'object') switch(type.$$typeof){
            case REACT_CONTEXT_TYPE:
                var context = type;
                return getContextName(context) + '.Consumer';
            case REACT_PROVIDER_TYPE:
                var provider = type;
                return getContextName(provider._context) + '.Provider';
            case REACT_FORWARD_REF_TYPE:
                return getWrappedName(type, type.render, 'ForwardRef');
            case REACT_MEMO_TYPE:
                var outerName = type.displayName || null;
                if (outerName !== null) return outerName;
                return getComponentNameFromType(type.type) || 'Memo';
            case REACT_LAZY_TYPE:
                var lazyComponent = type;
                var payload = lazyComponent._payload;
                var init = lazyComponent._init;
                try {
                    return getComponentNameFromType(init(payload));
                } catch (x) {
                    return null;
                }
        }
        return null;
    }
    var assign = Object.assign;
    // Helpers to patch console.logs to avoid logging during side-effect free
    // replaying on render function. This currently only patches the object
    // lazily which won't cover if the log function was extracted eagerly.
    // We could also eagerly patch the method.
    var disabledDepth = 0;
    var prevLog;
    var prevInfo;
    var prevWarn;
    var prevError;
    var prevGroup;
    var prevGroupCollapsed;
    var prevGroupEnd;
    function disabledLog() {}
    disabledLog.__reactDisabledLog = true;
    function disableLogs() {
        if (disabledDepth === 0) {
            /* eslint-disable react-internal/no-production-logging */ prevLog = console.log;
            prevInfo = console.info;
            prevWarn = console.warn;
            prevError = console.error;
            prevGroup = console.group;
            prevGroupCollapsed = console.groupCollapsed;
            prevGroupEnd = console.groupEnd; // https://github.com/facebook/react/issues/19099
            var props = {
                configurable: true,
                enumerable: true,
                value: disabledLog,
                writable: true
            }; // $FlowFixMe Flow thinks console is immutable.
            Object.defineProperties(console, {
                info: props,
                log: props,
                warn: props,
                error: props,
                group: props,
                groupCollapsed: props,
                groupEnd: props
            });
        /* eslint-enable react-internal/no-production-logging */ }
        disabledDepth++;
    }
    function reenableLogs() {
        disabledDepth--;
        if (disabledDepth === 0) {
            /* eslint-disable react-internal/no-production-logging */ var props = {
                configurable: true,
                enumerable: true,
                writable: true
            }; // $FlowFixMe Flow thinks console is immutable.
            Object.defineProperties(console, {
                log: assign({}, props, {
                    value: prevLog
                }),
                info: assign({}, props, {
                    value: prevInfo
                }),
                warn: assign({}, props, {
                    value: prevWarn
                }),
                error: assign({}, props, {
                    value: prevError
                }),
                group: assign({}, props, {
                    value: prevGroup
                }),
                groupCollapsed: assign({}, props, {
                    value: prevGroupCollapsed
                }),
                groupEnd: assign({}, props, {
                    value: prevGroupEnd
                })
            });
        /* eslint-enable react-internal/no-production-logging */ }
        if (disabledDepth < 0) error("disabledDepth fell below zero. This is a bug in React. Please file an issue.");
    }
    var ReactCurrentDispatcher = ReactSharedInternals.ReactCurrentDispatcher;
    var prefix;
    function describeBuiltInComponentFrame(name, source, ownerFn) {
        if (prefix === undefined) // Extract the VM specific prefix used by each line.
        try {
            throw Error();
        } catch (x) {
            var match = x.stack.trim().match(/\n( *(at )?)/);
            prefix = match && match[1] || '';
        }
         // We use the prefix to ensure our stacks line up with native stack frames.
        return '\n' + prefix + name;
    }
    var reentry = false;
    var componentFrameCache;
    var PossiblyWeakMap = typeof WeakMap === 'function' ? WeakMap : Map;
    componentFrameCache = new PossiblyWeakMap();
    function describeNativeComponentFrame(fn, construct) {
        // If something asked for a stack inside a fake render, it should get ignored.
        if (!fn || reentry) return '';
        var frame = componentFrameCache.get(fn);
        if (frame !== undefined) return frame;
        var control;
        reentry = true;
        var previousPrepareStackTrace = Error.prepareStackTrace; // $FlowFixMe It does accept undefined.
        Error.prepareStackTrace = undefined;
        var previousDispatcher;
        previousDispatcher = ReactCurrentDispatcher.current; // Set the dispatcher in DEV because this might be call in the render function
        // for warnings.
        ReactCurrentDispatcher.current = null;
        disableLogs();
        try {
            // This should throw.
            if (construct) {
                // Something should be setting the props in the constructor.
                var Fake = function() {
                    throw Error();
                }; // $FlowFixMe
                Object.defineProperty(Fake.prototype, 'props', {
                    set: function() {
                        // We use a throwing setter instead of frozen or non-writable props
                        // because that won't throw in a non-strict mode function.
                        throw Error();
                    }
                });
                if (typeof Reflect === 'object' && Reflect.construct) {
                    // We construct a different control for this case to include any extra
                    // frames added by the construct call.
                    try {
                        Reflect.construct(Fake, []);
                    } catch (x) {
                        control = x;
                    }
                    Reflect.construct(fn, [], Fake);
                } else {
                    try {
                        Fake.call();
                    } catch (x) {
                        control = x;
                    }
                    fn.call(Fake.prototype);
                }
            } else {
                try {
                    throw Error();
                } catch (x) {
                    control = x;
                }
                fn();
            }
        } catch (sample) {
            // This is inlined manually because closure doesn't do it for us.
            if (sample && control && typeof sample.stack === 'string') {
                // This extracts the first frame from the sample that isn't also in the control.
                // Skipping one frame that we assume is the frame that calls the two.
                var sampleLines = sample.stack.split('\n');
                var controlLines = control.stack.split('\n');
                var s = sampleLines.length - 1;
                var c = controlLines.length - 1;
                while(s >= 1 && c >= 0 && sampleLines[s] !== controlLines[c])// We expect at least one stack frame to be shared.
                // Typically this will be the root most one. However, stack frames may be
                // cut off due to maximum stack limits. In this case, one maybe cut off
                // earlier than the other. We assume that the sample is longer or the same
                // and there for cut off earlier. So we should find the root most frame in
                // the sample somewhere in the control.
                c--;
                for(; s >= 1 && c >= 0; s--, c--)// Next we find the first one that isn't the same which should be the
                // frame that called our sample function and the control.
                if (sampleLines[s] !== controlLines[c]) {
                    // In V8, the first line is describing the message but other VMs don't.
                    // If we're about to return the first line, and the control is also on the same
                    // line, that's a pretty good indicator that our sample threw at same line as
                    // the control. I.e. before we entered the sample frame. So we ignore this result.
                    // This can happen if you passed a class to function component, or non-function.
                    if (s !== 1 || c !== 1) do {
                        s--;
                        c--; // We may still have similar intermediate frames from the construct call.
                        // The next one that isn't the same should be our match though.
                        if (c < 0 || sampleLines[s] !== controlLines[c]) {
                            // V8 adds a "new" prefix for native classes. Let's remove it to make it prettier.
                            var _frame = '\n' + sampleLines[s].replace(' at new ', ' at '); // If our component frame is labeled "<anonymous>"
                            // but we have a user-provided "displayName"
                            // splice it in to make the stack more readable.
                            if (fn.displayName && _frame.includes('<anonymous>')) _frame = _frame.replace('<anonymous>', fn.displayName);
                            if (typeof fn === 'function') componentFrameCache.set(fn, _frame);
                            return _frame;
                        }
                    }while (s >= 1 && c >= 0)
                    break;
                }
            }
        } finally{
            reentry = false;
            ReactCurrentDispatcher.current = previousDispatcher;
            reenableLogs();
            Error.prepareStackTrace = previousPrepareStackTrace;
        } // Fallback to just using the name if we couldn't make it throw.
        var name = fn ? fn.displayName || fn.name : '';
        var syntheticFrame = name ? describeBuiltInComponentFrame(name) : '';
        if (typeof fn === 'function') componentFrameCache.set(fn, syntheticFrame);
        return syntheticFrame;
    }
    function describeFunctionComponentFrame(fn, source, ownerFn) {
        return describeNativeComponentFrame(fn, false);
    }
    function shouldConstruct(Component) {
        var prototype = Component.prototype;
        return !!(prototype && prototype.isReactComponent);
    }
    function describeUnknownElementTypeFrameInDEV(type, source, ownerFn) {
        if (type == null) return '';
        if (typeof type === 'function') return describeNativeComponentFrame(type, shouldConstruct(type));
        if (typeof type === 'string') return describeBuiltInComponentFrame(type);
        switch(type){
            case REACT_SUSPENSE_TYPE:
                return describeBuiltInComponentFrame('Suspense');
            case REACT_SUSPENSE_LIST_TYPE:
                return describeBuiltInComponentFrame('SuspenseList');
        }
        if (typeof type === 'object') switch(type.$$typeof){
            case REACT_FORWARD_REF_TYPE:
                return describeFunctionComponentFrame(type.render);
            case REACT_MEMO_TYPE:
                // Memo may contain any component type so we recursively resolve it.
                return describeUnknownElementTypeFrameInDEV(type.type, source, ownerFn);
            case REACT_LAZY_TYPE:
                var lazyComponent = type;
                var payload = lazyComponent._payload;
                var init = lazyComponent._init;
                try {
                    // Lazy may contain any component type so we recursively resolve it.
                    return describeUnknownElementTypeFrameInDEV(init(payload), source, ownerFn);
                } catch (x) {}
        }
        return '';
    }
    var hasOwnProperty = Object.prototype.hasOwnProperty;
    var loggedTypeFailures = {};
    var ReactDebugCurrentFrame1 = ReactSharedInternals.ReactDebugCurrentFrame;
    function setCurrentlyValidatingElement(element) {
        if (element) {
            var owner = element._owner;
            var stack = describeUnknownElementTypeFrameInDEV(element.type, element._source, owner ? owner.type : null);
            ReactDebugCurrentFrame1.setExtraStackFrame(stack);
        } else ReactDebugCurrentFrame1.setExtraStackFrame(null);
    }
    function checkPropTypes(typeSpecs, values, location, componentName, element) {
        // $FlowFixMe This is okay but Flow doesn't know it.
        var has = Function.call.bind(hasOwnProperty);
        for(var typeSpecName in typeSpecs)if (has(typeSpecs, typeSpecName)) {
            var error$1 = void 0; // Prop type validation may throw. In case they do, we don't want to
            // fail the render phase where it didn't fail before. So we log it.
            // After these have been cleaned up, we'll let them throw.
            try {
                // This is intentionally an invariant that gets caught. It's the same
                // behavior as without this statement except with a better message.
                if (typeof typeSpecs[typeSpecName] !== 'function') {
                    // eslint-disable-next-line react-internal/prod-error-codes
                    var err = Error((componentName || 'React class') + ': ' + location + ' type `' + typeSpecName + '` is invalid; ' + 'it must be a function, usually from the `prop-types` package, but received `' + typeof typeSpecs[typeSpecName] + '`.' + 'This often happens because of typos such as `PropTypes.function` instead of `PropTypes.func`.');
                    err.name = 'Invariant Violation';
                    throw err;
                }
                error$1 = typeSpecs[typeSpecName](values, typeSpecName, componentName, location, null, 'SECRET_DO_NOT_PASS_THIS_OR_YOU_WILL_BE_FIRED');
            } catch (ex) {
                error$1 = ex;
            }
            if (error$1 && !(error$1 instanceof Error)) {
                setCurrentlyValidatingElement(element);
                error("%s: type specification of %s `%s` is invalid; the type checker function must return `null` or an `Error` but returned a %s. You may have forgotten to pass an argument to the type checker creator (arrayOf, instanceOf, objectOf, oneOf, oneOfType, and shape all require an argument).", componentName || 'React class', location, typeSpecName, typeof error$1);
                setCurrentlyValidatingElement(null);
            }
            if (error$1 instanceof Error && !(error$1.message in loggedTypeFailures)) {
                // Only monitor this failure once because there tends to be a lot of the
                // same error.
                loggedTypeFailures[error$1.message] = true;
                setCurrentlyValidatingElement(element);
                error('Failed %s type: %s', location, error$1.message);
                setCurrentlyValidatingElement(null);
            }
        }
    }
    var isArrayImpl = Array.isArray; // eslint-disable-next-line no-redeclare
    function isArray(a) {
        return isArrayImpl(a);
    }
    /*
 * The `'' + value` pattern (used in in perf-sensitive code) throws for Symbol
 * and Temporal.* types. See https://github.com/facebook/react/pull/22064.
 *
 * The functions in this module will throw an easier-to-understand,
 * easier-to-debug exception with a clear errors message message explaining the
 * problem. (Instead of a confusing exception thrown inside the implementation
 * of the `value` object).
 */ // $FlowFixMe only called in DEV, so void return is not possible.
    function typeName(value) {
        // toStringTag is needed for namespaced types like Temporal.Instant
        var hasToStringTag = typeof Symbol === 'function' && Symbol.toStringTag;
        var type = hasToStringTag && value[Symbol.toStringTag] || value.constructor.name || 'Object';
        return type;
    } // $FlowFixMe only called in DEV, so void return is not possible.
    function willCoercionThrow(value) {
        try {
            testStringCoercion(value);
            return false;
        } catch (e) {
            return true;
        }
    }
    function testStringCoercion(value) {
        // If you ended up here by following an exception call stack, here's what's
        // happened: you supplied an object or symbol value to React (as a prop, key,
        // DOM attribute, CSS property, string ref, etc.) and when React tried to
        // coerce it to a string using `'' + value`, an exception was thrown.
        //
        // The most common types that will cause this exception are `Symbol` instances
        // and Temporal objects like `Temporal.Instant`. But any object that has a
        // `valueOf` or `[Symbol.toPrimitive]` method that throws will also cause this
        // exception. (Library authors do this to prevent users from using built-in
        // numeric operators like `+` or comparison operators like `>=` because custom
        // methods are needed to perform accurate arithmetic or comparison.)
        //
        // To fix the problem, coerce this object or symbol value to a string before
        // passing it to React. The most reliable way is usually `String(value)`.
        //
        // To find which value is throwing, check the browser or debugger console.
        // Before this exception was thrown, there should be `console.error` output
        // that shows the type (Symbol, Temporal.PlainDate, etc.) that caused the
        // problem and how that type was used: key, atrribute, input value prop, etc.
        // In most cases, this console output also shows the component and its
        // ancestor components where the exception happened.
        //
        // eslint-disable-next-line react-internal/safe-string-coercion
        return '' + value;
    }
    function checkKeyStringCoercion(value) {
        if (willCoercionThrow(value)) {
            error("The provided key is an unsupported type %s. This value must be coerced to a string before before using it here.", typeName(value));
            return testStringCoercion(value); // throw (to help callers find troubleshooting comments)
        }
    }
    var ReactCurrentOwner = ReactSharedInternals.ReactCurrentOwner;
    var RESERVED_PROPS = {
        key: true,
        ref: true,
        __self: true,
        __source: true
    };
    var specialPropKeyWarningShown;
    var specialPropRefWarningShown;
    var didWarnAboutStringRefs;
    didWarnAboutStringRefs = {};
    function hasValidRef(config) {
        if (hasOwnProperty.call(config, 'ref')) {
            var getter = Object.getOwnPropertyDescriptor(config, 'ref').get;
            if (getter && getter.isReactWarning) return false;
        }
        return config.ref !== undefined;
    }
    function hasValidKey(config) {
        if (hasOwnProperty.call(config, 'key')) {
            var getter = Object.getOwnPropertyDescriptor(config, 'key').get;
            if (getter && getter.isReactWarning) return false;
        }
        return config.key !== undefined;
    }
    function warnIfStringRefCannotBeAutoConverted(config, self) {
        if (typeof config.ref === 'string' && ReactCurrentOwner.current && self && ReactCurrentOwner.current.stateNode !== self) {
            var componentName = getComponentNameFromType(ReactCurrentOwner.current.type);
            if (!didWarnAboutStringRefs[componentName]) {
                error('Component "%s" contains the string ref "%s". Support for string refs will be removed in a future major release. This case cannot be automatically converted to an arrow function. We ask you to manually fix this case by using useRef() or createRef() instead. Learn more about using refs safely here: https://reactjs.org/link/strict-mode-string-ref', getComponentNameFromType(ReactCurrentOwner.current.type), config.ref);
                didWarnAboutStringRefs[componentName] = true;
            }
        }
    }
    function defineKeyPropWarningGetter(props, displayName) {
        var warnAboutAccessingKey = function() {
            if (!specialPropKeyWarningShown) {
                specialPropKeyWarningShown = true;
                error("%s: `key` is not a prop. Trying to access it will result in `undefined` being returned. If you need to access the same value within the child component, you should pass it as a different prop. (https://reactjs.org/link/special-props)", displayName);
            }
        };
        warnAboutAccessingKey.isReactWarning = true;
        Object.defineProperty(props, 'key', {
            get: warnAboutAccessingKey,
            configurable: true
        });
    }
    function defineRefPropWarningGetter(props, displayName) {
        var warnAboutAccessingRef = function() {
            if (!specialPropRefWarningShown) {
                specialPropRefWarningShown = true;
                error("%s: `ref` is not a prop. Trying to access it will result in `undefined` being returned. If you need to access the same value within the child component, you should pass it as a different prop. (https://reactjs.org/link/special-props)", displayName);
            }
        };
        warnAboutAccessingRef.isReactWarning = true;
        Object.defineProperty(props, 'ref', {
            get: warnAboutAccessingRef,
            configurable: true
        });
    }
    /**
 * Factory method to create a new React element. This no longer adheres to
 * the class pattern, so do not use new to call it. Also, instanceof check
 * will not work. Instead test $$typeof field against Symbol.for('react.element') to check
 * if something is a React Element.
 *
 * @param {*} type
 * @param {*} props
 * @param {*} key
 * @param {string|object} ref
 * @param {*} owner
 * @param {*} self A *temporary* helper to detect places where `this` is
 * different from the `owner` when React.createElement is called, so that we
 * can warn. We want to get rid of owner and replace string `ref`s with arrow
 * functions, and as long as `this` and owner are the same, there will be no
 * change in behavior.
 * @param {*} source An annotation object (added by a transpiler or otherwise)
 * indicating filename, line number, and/or other information.
 * @internal
 */ var ReactElement = function(type, key, ref, self, source, owner, props) {
        var element = {
            // This tag allows us to uniquely identify this as a React Element
            $$typeof: REACT_ELEMENT_TYPE,
            // Built-in properties that belong on the element
            type: type,
            key: key,
            ref: ref,
            props: props,
            // Record the component responsible for creating this element.
            _owner: owner
        };
        // The validation flag is currently mutative. We put it on
        // an external backing store so that we can freeze the whole object.
        // This can be replaced with a WeakMap once they are implemented in
        // commonly used development environments.
        element._store = {}; // To make comparing ReactElements easier for testing purposes, we make
        // the validation flag non-enumerable (where possible, which should
        // include every environment we run tests in), so the test framework
        // ignores it.
        Object.defineProperty(element._store, 'validated', {
            configurable: false,
            enumerable: false,
            writable: true,
            value: false
        }); // self and source are DEV only properties.
        Object.defineProperty(element, '_self', {
            configurable: false,
            enumerable: false,
            writable: false,
            value: self
        }); // Two elements created in two different places should be considered
        // equal for testing purposes and therefore we hide it from enumeration.
        Object.defineProperty(element, '_source', {
            configurable: false,
            enumerable: false,
            writable: false,
            value: source
        });
        if (Object.freeze) {
            Object.freeze(element.props);
            Object.freeze(element);
        }
        return element;
    };
    /**
 * https://github.com/reactjs/rfcs/pull/107
 * @param {*} type
 * @param {object} props
 * @param {string} key
 */ function jsxDEV(type, config, maybeKey, source, self) {
        var propName; // Reserved names are extracted
        var props = {};
        var key = null;
        var ref = null; // Currently, key can be spread in as a prop. This causes a potential
        // issue if key is also explicitly declared (ie. <div {...props} key="Hi" />
        // or <div key="Hi" {...props} /> ). We want to deprecate key spread,
        // but as an intermediary step, we will use jsxDEV for everything except
        // <div {...props} key="Hi" />, because we aren't currently able to tell if
        // key is explicitly declared to be undefined or not.
        if (maybeKey !== undefined) {
            checkKeyStringCoercion(maybeKey);
            key = '' + maybeKey;
        }
        if (hasValidKey(config)) {
            checkKeyStringCoercion(config.key);
            key = '' + config.key;
        }
        if (hasValidRef(config)) {
            ref = config.ref;
            warnIfStringRefCannotBeAutoConverted(config, self);
        } // Remaining properties are added to a new props object
        for(propName in config)if (hasOwnProperty.call(config, propName) && !RESERVED_PROPS.hasOwnProperty(propName)) props[propName] = config[propName];
         // Resolve default props
        if (type && type.defaultProps) {
            var defaultProps = type.defaultProps;
            for(propName in defaultProps)if (props[propName] === undefined) props[propName] = defaultProps[propName];
        }
        if (key || ref) {
            var displayName = typeof type === 'function' ? type.displayName || type.name || 'Unknown' : type;
            if (key) defineKeyPropWarningGetter(props, displayName);
            if (ref) defineRefPropWarningGetter(props, displayName);
        }
        return ReactElement(type, key, ref, self, source, ReactCurrentOwner.current, props);
    }
    var ReactCurrentOwner$1 = ReactSharedInternals.ReactCurrentOwner;
    var ReactDebugCurrentFrame$1 = ReactSharedInternals.ReactDebugCurrentFrame;
    function setCurrentlyValidatingElement$1(element) {
        if (element) {
            var owner = element._owner;
            var stack = describeUnknownElementTypeFrameInDEV(element.type, element._source, owner ? owner.type : null);
            ReactDebugCurrentFrame$1.setExtraStackFrame(stack);
        } else ReactDebugCurrentFrame$1.setExtraStackFrame(null);
    }
    var propTypesMisspellWarningShown;
    propTypesMisspellWarningShown = false;
    /**
 * Verifies the object is a ReactElement.
 * See https://reactjs.org/docs/react-api.html#isvalidelement
 * @param {?object} object
 * @return {boolean} True if `object` is a ReactElement.
 * @final
 */ function isValidElement(object) {
        return typeof object === 'object' && object !== null && object.$$typeof === REACT_ELEMENT_TYPE;
    }
    function getDeclarationErrorAddendum() {
        if (ReactCurrentOwner$1.current) {
            var name = getComponentNameFromType(ReactCurrentOwner$1.current.type);
            if (name) return '\n\nCheck the render method of `' + name + '`.';
        }
        return '';
    }
    function getSourceInfoErrorAddendum(source) {
        if (source !== undefined) {
            var fileName = source.fileName.replace(/^.*[\\\/]/, '');
            var lineNumber = source.lineNumber;
            return '\n\nCheck your code at ' + fileName + ':' + lineNumber + '.';
        }
        return '';
    }
    /**
 * Warn if there's no key explicitly set on dynamic arrays of children or
 * object keys are not valid. This allows us to keep track of children between
 * updates.
 */ var ownerHasKeyUseWarning = {};
    function getCurrentComponentErrorInfo(parentType) {
        var info = getDeclarationErrorAddendum();
        if (!info) {
            var parentName = typeof parentType === 'string' ? parentType : parentType.displayName || parentType.name;
            if (parentName) info = "\n\nCheck the top-level render call using <" + parentName + ">.";
        }
        return info;
    }
    /**
 * Warn if the element doesn't have an explicit key assigned to it.
 * This element is in an array. The array could grow and shrink or be
 * reordered. All children that haven't already been validated are required to
 * have a "key" property assigned to it. Error statuses are cached so a warning
 * will only be shown once.
 *
 * @internal
 * @param {ReactElement} element Element that requires a key.
 * @param {*} parentType element's parent's type.
 */ function validateExplicitKey(element, parentType) {
        if (!element._store || element._store.validated || element.key != null) return;
        element._store.validated = true;
        var currentComponentErrorInfo = getCurrentComponentErrorInfo(parentType);
        if (ownerHasKeyUseWarning[currentComponentErrorInfo]) return;
        ownerHasKeyUseWarning[currentComponentErrorInfo] = true; // Usually the current owner is the offender, but if it accepts children as a
        // property, it may be the creator of the child that's responsible for
        // assigning it a key.
        var childOwner = '';
        if (element && element._owner && element._owner !== ReactCurrentOwner$1.current) // Give the component that originally created this child.
        childOwner = " It was passed a child from " + getComponentNameFromType(element._owner.type) + ".";
        setCurrentlyValidatingElement$1(element);
        error('Each child in a list should have a unique "key" prop.%s%s See https://reactjs.org/link/warning-keys for more information.', currentComponentErrorInfo, childOwner);
        setCurrentlyValidatingElement$1(null);
    }
    /**
 * Ensure that every element either is passed in a static location, in an
 * array with an explicit keys property defined, or in an object literal
 * with valid key property.
 *
 * @internal
 * @param {ReactNode} node Statically passed child of any type.
 * @param {*} parentType node's parent's type.
 */ function validateChildKeys(node, parentType) {
        if (typeof node !== 'object') return;
        if (isArray(node)) for(var i = 0; i < node.length; i++){
            var child = node[i];
            if (isValidElement(child)) validateExplicitKey(child, parentType);
        }
        else if (isValidElement(node)) // This element was passed in a valid location.
        {
            if (node._store) node._store.validated = true;
        } else if (node) {
            var iteratorFn = getIteratorFn(node);
            if (typeof iteratorFn === 'function') // Entry iterators used to provide implicit keys,
            // but now we print a separate warning for them later.
            {
                if (iteratorFn !== node.entries) {
                    var iterator = iteratorFn.call(node);
                    var step;
                    while(!(step = iterator.next()).done)if (isValidElement(step.value)) validateExplicitKey(step.value, parentType);
                }
            }
        }
    }
    /**
 * Given an element, validate that its props follow the propTypes definition,
 * provided by the type.
 *
 * @param {ReactElement} element
 */ function validatePropTypes(element) {
        var type = element.type;
        if (type === null || type === undefined || typeof type === 'string') return;
        var propTypes;
        if (typeof type === 'function') propTypes = type.propTypes;
        else if (typeof type === 'object' && (type.$$typeof === REACT_FORWARD_REF_TYPE || // Inner props are checked in the reconciler.
        type.$$typeof === REACT_MEMO_TYPE)) propTypes = type.propTypes;
        else return;
        if (propTypes) {
            // Intentionally inside to avoid triggering lazy initializers:
            var name = getComponentNameFromType(type);
            checkPropTypes(propTypes, element.props, 'prop', name, element);
        } else if (type.PropTypes !== undefined && !propTypesMisspellWarningShown) {
            propTypesMisspellWarningShown = true; // Intentionally inside to avoid triggering lazy initializers:
            var _name = getComponentNameFromType(type);
            error('Component %s declared `PropTypes` instead of `propTypes`. Did you misspell the property assignment?', _name || 'Unknown');
        }
        if (typeof type.getDefaultProps === 'function' && !type.getDefaultProps.isReactClassApproved) error("getDefaultProps is only used on classic React.createClass definitions. Use a static property named `defaultProps` instead.");
    }
    /**
 * Given a fragment, validate that it can only be provided with fragment props
 * @param {ReactElement} fragment
 */ function validateFragmentProps(fragment) {
        var keys = Object.keys(fragment.props);
        for(var i = 0; i < keys.length; i++){
            var key = keys[i];
            if (key !== 'children' && key !== 'key') {
                setCurrentlyValidatingElement$1(fragment);
                error("Invalid prop `%s` supplied to `React.Fragment`. React.Fragment can only have `key` and `children` props.", key);
                setCurrentlyValidatingElement$1(null);
                break;
            }
        }
        if (fragment.ref !== null) {
            setCurrentlyValidatingElement$1(fragment);
            error('Invalid attribute `ref` supplied to `React.Fragment`.');
            setCurrentlyValidatingElement$1(null);
        }
    }
    function jsxWithValidation(type, props, key, isStaticChildren, source, self) {
        var validType = isValidElementType(type); // We warn in this case but don't throw. We expect the element creation to
        // succeed and there will likely be errors in render.
        if (!validType) {
            var info = '';
            if (type === undefined || typeof type === 'object' && type !== null && Object.keys(type).length === 0) info += " You likely forgot to export your component from the file it's defined in, or you might have mixed up default and named imports.";
            var sourceInfo = getSourceInfoErrorAddendum(source);
            if (sourceInfo) info += sourceInfo;
            else info += getDeclarationErrorAddendum();
            var typeString;
            if (type === null) typeString = 'null';
            else if (isArray(type)) typeString = 'array';
            else if (type !== undefined && type.$$typeof === REACT_ELEMENT_TYPE) {
                typeString = "<" + (getComponentNameFromType(type.type) || 'Unknown') + " />";
                info = ' Did you accidentally export a JSX literal instead of a component?';
            } else typeString = typeof type;
            error("React.jsx: type is invalid -- expected a string (for built-in components) or a class/function (for composite components) but got: %s.%s", typeString, info);
        }
        var element = jsxDEV(type, props, key, source, self); // The result can be nullish if a mock or a custom function is used.
        // TODO: Drop this when these are no longer allowed as the type argument.
        if (element == null) return element;
         // Skip key warning if the type isn't valid since our key validation logic
        // doesn't expect a non-string/function type and can throw confusing errors.
        // We don't want exception behavior to differ between dev and prod.
        // (Rendering will throw with a helpful message and as soon as the type is
        // fixed, the key warnings will appear.)
        if (validType) {
            var children = props.children;
            if (children !== undefined) {
                if (isStaticChildren) {
                    if (isArray(children)) {
                        for(var i = 0; i < children.length; i++)validateChildKeys(children[i], type);
                        if (Object.freeze) Object.freeze(children);
                    } else error("React.jsx: Static children should always be an array. You are likely explicitly calling React.jsxs or React.jsxDEV. Use the Babel transform instead.");
                } else validateChildKeys(children, type);
            }
        }
        if (type === REACT_FRAGMENT_TYPE) validateFragmentProps(element);
        else validatePropTypes(element);
        return element;
    } // These two functions exist to still get child warnings in dev
    var jsxDEV$1 = jsxWithValidation;
    exports.Fragment = REACT_FRAGMENT_TYPE;
    exports.jsxDEV = jsxDEV$1;
})();

},{"react":"21dqq"}],"21dqq":[function(require,module,exports) {
'use strict';
module.exports = require('./cjs/react.development.js');

},{"./cjs/react.development.js":"6YvXz"}],"6YvXz":[function(require,module,exports) {
/**
 * @license React
 * react.development.js
 *
 * Copyright (c) Facebook, Inc. and its affiliates.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE file in the root directory of this source tree.
 */ 'use strict';
(function() {
    /* global __REACT_DEVTOOLS_GLOBAL_HOOK__ */ if (typeof __REACT_DEVTOOLS_GLOBAL_HOOK__ !== 'undefined' && typeof __REACT_DEVTOOLS_GLOBAL_HOOK__.registerInternalModuleStart === 'function') __REACT_DEVTOOLS_GLOBAL_HOOK__.registerInternalModuleStart(new Error());
    var ReactVersion = '18.1.0';
    // -----------------------------------------------------------------------------
    var enableScopeAPI = false; // Experimental Create Event Handle API.
    var enableCacheElement = false;
    var enableTransitionTracing = false; // No known bugs, but needs performance testing
    var enableLegacyHidden = false; // Enables unstable_avoidThisFallback feature in Fiber
    // stuff. Intended to enable React core members to more easily debug scheduling
    // issues in DEV builds.
    var enableDebugTracing = false; // Track which Fiber(s) schedule render work.
    // ATTENTION
    var REACT_ELEMENT_TYPE = Symbol.for('react.element');
    var REACT_PORTAL_TYPE = Symbol.for('react.portal');
    var REACT_FRAGMENT_TYPE = Symbol.for('react.fragment');
    var REACT_STRICT_MODE_TYPE = Symbol.for('react.strict_mode');
    var REACT_PROFILER_TYPE = Symbol.for('react.profiler');
    var REACT_PROVIDER_TYPE = Symbol.for('react.provider');
    var REACT_CONTEXT_TYPE = Symbol.for('react.context');
    var REACT_FORWARD_REF_TYPE = Symbol.for('react.forward_ref');
    var REACT_SUSPENSE_TYPE = Symbol.for('react.suspense');
    var REACT_SUSPENSE_LIST_TYPE = Symbol.for('react.suspense_list');
    var REACT_MEMO_TYPE = Symbol.for('react.memo');
    var REACT_LAZY_TYPE = Symbol.for('react.lazy');
    var REACT_OFFSCREEN_TYPE = Symbol.for('react.offscreen');
    var MAYBE_ITERATOR_SYMBOL = Symbol.iterator;
    var FAUX_ITERATOR_SYMBOL = '@@iterator';
    function getIteratorFn(maybeIterable) {
        if (maybeIterable === null || typeof maybeIterable !== 'object') return null;
        var maybeIterator = MAYBE_ITERATOR_SYMBOL && maybeIterable[MAYBE_ITERATOR_SYMBOL] || maybeIterable[FAUX_ITERATOR_SYMBOL];
        if (typeof maybeIterator === 'function') return maybeIterator;
        return null;
    }
    /**
 * Keeps track of the current dispatcher.
 */ var ReactCurrentDispatcher = {
        /**
   * @internal
   * @type {ReactComponent}
   */ current: null
    };
    /**
 * Keeps track of the current batch's configuration such as how long an update
 * should suspend for if it needs to.
 */ var ReactCurrentBatchConfig = {
        transition: null
    };
    var ReactCurrentActQueue = {
        current: null,
        // Used to reproduce behavior of `batchedUpdates` in legacy mode.
        isBatchingLegacy: false,
        didScheduleLegacyUpdate: false
    };
    /**
 * Keeps track of the current owner.
 *
 * The current owner is the component who should own any components that are
 * currently being constructed.
 */ var ReactCurrentOwner = {
        /**
   * @internal
   * @type {ReactComponent}
   */ current: null
    };
    var ReactDebugCurrentFrame1 = {};
    var currentExtraStackFrame = null;
    function setExtraStackFrame(stack) {
        currentExtraStackFrame = stack;
    }
    ReactDebugCurrentFrame1.setExtraStackFrame = function(stack) {
        currentExtraStackFrame = stack;
    }; // Stack implementation injected by the current renderer.
    ReactDebugCurrentFrame1.getCurrentStack = null;
    ReactDebugCurrentFrame1.getStackAddendum = function() {
        var stack = ''; // Add an extra top frame while an element is being validated
        if (currentExtraStackFrame) stack += currentExtraStackFrame;
         // Delegate to the injected renderer-specific implementation
        var impl = ReactDebugCurrentFrame1.getCurrentStack;
        if (impl) stack += impl() || '';
        return stack;
    };
    var ReactSharedInternals = {
        ReactCurrentDispatcher: ReactCurrentDispatcher,
        ReactCurrentBatchConfig: ReactCurrentBatchConfig,
        ReactCurrentOwner: ReactCurrentOwner
    };
    ReactSharedInternals.ReactDebugCurrentFrame = ReactDebugCurrentFrame1;
    ReactSharedInternals.ReactCurrentActQueue = ReactCurrentActQueue;
    // by calls to these methods by a Babel plugin.
    //
    // In PROD (or in packages without access to React internals),
    // they are left as they are instead.
    function warn(format) {
        for(var _len = arguments.length, args = new Array(_len > 1 ? _len - 1 : 0), _key = 1; _key < _len; _key++)args[_key - 1] = arguments[_key];
        printWarning('warn', format, args);
    }
    function error1(format) {
        for(var _len2 = arguments.length, args = new Array(_len2 > 1 ? _len2 - 1 : 0), _key2 = 1; _key2 < _len2; _key2++)args[_key2 - 1] = arguments[_key2];
        printWarning('error', format, args);
    }
    function printWarning(level, format, args) {
        var ReactDebugCurrentFrame = ReactSharedInternals.ReactDebugCurrentFrame;
        var stack = ReactDebugCurrentFrame.getStackAddendum();
        if (stack !== '') {
            format += '%s';
            args = args.concat([
                stack
            ]);
        } // eslint-disable-next-line react-internal/safe-string-coercion
        var argsWithFormat = args.map(function(item) {
            return String(item);
        }); // Careful: RN currently depends on this prefix
        argsWithFormat.unshift('Warning: ' + format); // We intentionally don't use spread (or .apply) directly because it
        // breaks IE9: https://github.com/facebook/react/issues/13610
        // eslint-disable-next-line react-internal/no-production-logging
        Function.prototype.apply.call(console[level], console, argsWithFormat);
    }
    var didWarnStateUpdateForUnmountedComponent = {};
    function warnNoop(publicInstance, callerName) {
        var _constructor = publicInstance.constructor;
        var componentName = _constructor && (_constructor.displayName || _constructor.name) || 'ReactClass';
        var warningKey = componentName + "." + callerName;
        if (didWarnStateUpdateForUnmountedComponent[warningKey]) return;
        error1("Can't call %s on a component that is not yet mounted. This is a no-op, but it might indicate a bug in your application. Instead, assign to `this.state` directly or define a `state = {};` class property with the desired state in the %s component.", callerName, componentName);
        didWarnStateUpdateForUnmountedComponent[warningKey] = true;
    }
    /**
 * This is the abstract API for an update queue.
 */ var ReactNoopUpdateQueue = {
        /**
   * Checks whether or not this composite component is mounted.
   * @param {ReactClass} publicInstance The instance we want to test.
   * @return {boolean} True if mounted, false otherwise.
   * @protected
   * @final
   */ isMounted: function(publicInstance) {
            return false;
        },
        /**
   * Forces an update. This should only be invoked when it is known with
   * certainty that we are **not** in a DOM transaction.
   *
   * You may want to call this when you know that some deeper aspect of the
   * component's state has changed but `setState` was not called.
   *
   * This will not invoke `shouldComponentUpdate`, but it will invoke
   * `componentWillUpdate` and `componentDidUpdate`.
   *
   * @param {ReactClass} publicInstance The instance that should rerender.
   * @param {?function} callback Called after component is updated.
   * @param {?string} callerName name of the calling function in the public API.
   * @internal
   */ enqueueForceUpdate: function(publicInstance, callback, callerName) {
            warnNoop(publicInstance, 'forceUpdate');
        },
        /**
   * Replaces all of the state. Always use this or `setState` to mutate state.
   * You should treat `this.state` as immutable.
   *
   * There is no guarantee that `this.state` will be immediately updated, so
   * accessing `this.state` after calling this method may return the old value.
   *
   * @param {ReactClass} publicInstance The instance that should rerender.
   * @param {object} completeState Next state.
   * @param {?function} callback Called after component is updated.
   * @param {?string} callerName name of the calling function in the public API.
   * @internal
   */ enqueueReplaceState: function(publicInstance, completeState, callback, callerName) {
            warnNoop(publicInstance, 'replaceState');
        },
        /**
   * Sets a subset of the state. This only exists because _pendingState is
   * internal. This provides a merging strategy that is not available to deep
   * properties which is confusing. TODO: Expose pendingState or don't use it
   * during the merge.
   *
   * @param {ReactClass} publicInstance The instance that should rerender.
   * @param {object} partialState Next partial state to be merged with state.
   * @param {?function} callback Called after component is updated.
   * @param {?string} Name of the calling function in the public API.
   * @internal
   */ enqueueSetState: function(publicInstance, partialState, callback, callerName) {
            warnNoop(publicInstance, 'setState');
        }
    };
    var assign = Object.assign;
    var emptyObject = {};
    Object.freeze(emptyObject);
    /**
 * Base class helpers for the updating state of a component.
 */ function Component1(props, context, updater) {
        this.props = props;
        this.context = context; // If a component has string refs, we will assign a different object later.
        this.refs = emptyObject; // We initialize the default updater but the real one gets injected by the
        // renderer.
        this.updater = updater || ReactNoopUpdateQueue;
    }
    Component1.prototype.isReactComponent = {};
    /**
 * Sets a subset of the state. Always use this to mutate
 * state. You should treat `this.state` as immutable.
 *
 * There is no guarantee that `this.state` will be immediately updated, so
 * accessing `this.state` after calling this method may return the old value.
 *
 * There is no guarantee that calls to `setState` will run synchronously,
 * as they may eventually be batched together.  You can provide an optional
 * callback that will be executed when the call to setState is actually
 * completed.
 *
 * When a function is provided to setState, it will be called at some point in
 * the future (not synchronously). It will be called with the up to date
 * component arguments (state, props, context). These values can be different
 * from this.* because your function may be called after receiveProps but before
 * shouldComponentUpdate, and this new state, props, and context will not yet be
 * assigned to this.
 *
 * @param {object|function} partialState Next partial state or function to
 *        produce next partial state to be merged with current state.
 * @param {?function} callback Called after state is updated.
 * @final
 * @protected
 */ Component1.prototype.setState = function(partialState, callback) {
        if (typeof partialState !== 'object' && typeof partialState !== 'function' && partialState != null) throw new Error("setState(...): takes an object of state variables to update or a function which returns an object of state variables.");
        this.updater.enqueueSetState(this, partialState, callback, 'setState');
    };
    /**
 * Forces an update. This should only be invoked when it is known with
 * certainty that we are **not** in a DOM transaction.
 *
 * You may want to call this when you know that some deeper aspect of the
 * component's state has changed but `setState` was not called.
 *
 * This will not invoke `shouldComponentUpdate`, but it will invoke
 * `componentWillUpdate` and `componentDidUpdate`.
 *
 * @param {?function} callback Called after update is complete.
 * @final
 * @protected
 */ Component1.prototype.forceUpdate = function(callback) {
        this.updater.enqueueForceUpdate(this, callback, 'forceUpdate');
    };
    var deprecatedAPIs = {
        isMounted: [
            'isMounted',
            "Instead, make sure to clean up subscriptions and pending requests in componentWillUnmount to prevent memory leaks."
        ],
        replaceState: [
            'replaceState',
            "Refactor your code to use setState instead (see https://github.com/facebook/react/issues/3236)."
        ]
    };
    var defineDeprecationWarning = function(methodName, info) {
        Object.defineProperty(Component1.prototype, methodName, {
            get: function() {
                warn('%s(...) is deprecated in plain JavaScript React classes. %s', info[0], info[1]);
                return undefined;
            }
        });
    };
    for(var fnName in deprecatedAPIs)if (deprecatedAPIs.hasOwnProperty(fnName)) defineDeprecationWarning(fnName, deprecatedAPIs[fnName]);
    function ComponentDummy() {}
    ComponentDummy.prototype = Component1.prototype;
    /**
 * Convenience component with default shallow equality check for sCU.
 */ function PureComponent(props, context, updater) {
        this.props = props;
        this.context = context; // If a component has string refs, we will assign a different object later.
        this.refs = emptyObject;
        this.updater = updater || ReactNoopUpdateQueue;
    }
    var pureComponentPrototype = PureComponent.prototype = new ComponentDummy();
    pureComponentPrototype.constructor = PureComponent; // Avoid an extra prototype jump for these methods.
    assign(pureComponentPrototype, Component1.prototype);
    pureComponentPrototype.isPureReactComponent = true;
    // an immutable object with a single mutable value
    function createRef() {
        var refObject = {
            current: null
        };
        Object.seal(refObject);
        return refObject;
    }
    var isArrayImpl = Array.isArray; // eslint-disable-next-line no-redeclare
    function isArray(a) {
        return isArrayImpl(a);
    }
    /*
 * The `'' + value` pattern (used in in perf-sensitive code) throws for Symbol
 * and Temporal.* types. See https://github.com/facebook/react/pull/22064.
 *
 * The functions in this module will throw an easier-to-understand,
 * easier-to-debug exception with a clear errors message message explaining the
 * problem. (Instead of a confusing exception thrown inside the implementation
 * of the `value` object).
 */ // $FlowFixMe only called in DEV, so void return is not possible.
    function typeName(value) {
        // toStringTag is needed for namespaced types like Temporal.Instant
        var hasToStringTag = typeof Symbol === 'function' && Symbol.toStringTag;
        var type = hasToStringTag && value[Symbol.toStringTag] || value.constructor.name || 'Object';
        return type;
    } // $FlowFixMe only called in DEV, so void return is not possible.
    function willCoercionThrow(value) {
        try {
            testStringCoercion(value);
            return false;
        } catch (e) {
            return true;
        }
    }
    function testStringCoercion(value) {
        // If you ended up here by following an exception call stack, here's what's
        // happened: you supplied an object or symbol value to React (as a prop, key,
        // DOM attribute, CSS property, string ref, etc.) and when React tried to
        // coerce it to a string using `'' + value`, an exception was thrown.
        //
        // The most common types that will cause this exception are `Symbol` instances
        // and Temporal objects like `Temporal.Instant`. But any object that has a
        // `valueOf` or `[Symbol.toPrimitive]` method that throws will also cause this
        // exception. (Library authors do this to prevent users from using built-in
        // numeric operators like `+` or comparison operators like `>=` because custom
        // methods are needed to perform accurate arithmetic or comparison.)
        //
        // To fix the problem, coerce this object or symbol value to a string before
        // passing it to React. The most reliable way is usually `String(value)`.
        //
        // To find which value is throwing, check the browser or debugger console.
        // Before this exception was thrown, there should be `console.error` output
        // that shows the type (Symbol, Temporal.PlainDate, etc.) that caused the
        // problem and how that type was used: key, atrribute, input value prop, etc.
        // In most cases, this console output also shows the component and its
        // ancestor components where the exception happened.
        //
        // eslint-disable-next-line react-internal/safe-string-coercion
        return '' + value;
    }
    function checkKeyStringCoercion(value) {
        if (willCoercionThrow(value)) {
            error1("The provided key is an unsupported type %s. This value must be coerced to a string before before using it here.", typeName(value));
            return testStringCoercion(value); // throw (to help callers find troubleshooting comments)
        }
    }
    function getWrappedName(outerType, innerType, wrapperName) {
        var displayName = outerType.displayName;
        if (displayName) return displayName;
        var functionName = innerType.displayName || innerType.name || '';
        return functionName !== '' ? wrapperName + "(" + functionName + ")" : wrapperName;
    } // Keep in sync with react-reconciler/getComponentNameFromFiber
    function getContextName(type) {
        return type.displayName || 'Context';
    } // Note that the reconciler package should generally prefer to use getComponentNameFromFiber() instead.
    function getComponentNameFromType(type) {
        if (type == null) // Host root, text node or just invalid type.
        return null;
        if (typeof type.tag === 'number') error1("Received an unexpected object in getComponentNameFromType(). This is likely a bug in React. Please file an issue.");
        if (typeof type === 'function') return type.displayName || type.name || null;
        if (typeof type === 'string') return type;
        switch(type){
            case REACT_FRAGMENT_TYPE:
                return 'Fragment';
            case REACT_PORTAL_TYPE:
                return 'Portal';
            case REACT_PROFILER_TYPE:
                return 'Profiler';
            case REACT_STRICT_MODE_TYPE:
                return 'StrictMode';
            case REACT_SUSPENSE_TYPE:
                return 'Suspense';
            case REACT_SUSPENSE_LIST_TYPE:
                return 'SuspenseList';
        }
        if (typeof type === 'object') switch(type.$$typeof){
            case REACT_CONTEXT_TYPE:
                var context = type;
                return getContextName(context) + '.Consumer';
            case REACT_PROVIDER_TYPE:
                var provider = type;
                return getContextName(provider._context) + '.Provider';
            case REACT_FORWARD_REF_TYPE:
                return getWrappedName(type, type.render, 'ForwardRef');
            case REACT_MEMO_TYPE:
                var outerName = type.displayName || null;
                if (outerName !== null) return outerName;
                return getComponentNameFromType(type.type) || 'Memo';
            case REACT_LAZY_TYPE:
                var lazyComponent = type;
                var payload = lazyComponent._payload;
                var init = lazyComponent._init;
                try {
                    return getComponentNameFromType(init(payload));
                } catch (x) {
                    return null;
                }
        }
        return null;
    }
    var hasOwnProperty = Object.prototype.hasOwnProperty;
    var RESERVED_PROPS = {
        key: true,
        ref: true,
        __self: true,
        __source: true
    };
    var specialPropKeyWarningShown, specialPropRefWarningShown, didWarnAboutStringRefs;
    didWarnAboutStringRefs = {};
    function hasValidRef(config) {
        if (hasOwnProperty.call(config, 'ref')) {
            var getter = Object.getOwnPropertyDescriptor(config, 'ref').get;
            if (getter && getter.isReactWarning) return false;
        }
        return config.ref !== undefined;
    }
    function hasValidKey(config) {
        if (hasOwnProperty.call(config, 'key')) {
            var getter = Object.getOwnPropertyDescriptor(config, 'key').get;
            if (getter && getter.isReactWarning) return false;
        }
        return config.key !== undefined;
    }
    function defineKeyPropWarningGetter(props, displayName) {
        var warnAboutAccessingKey = function() {
            if (!specialPropKeyWarningShown) {
                specialPropKeyWarningShown = true;
                error1("%s: `key` is not a prop. Trying to access it will result in `undefined` being returned. If you need to access the same value within the child component, you should pass it as a different prop. (https://reactjs.org/link/special-props)", displayName);
            }
        };
        warnAboutAccessingKey.isReactWarning = true;
        Object.defineProperty(props, 'key', {
            get: warnAboutAccessingKey,
            configurable: true
        });
    }
    function defineRefPropWarningGetter(props, displayName) {
        var warnAboutAccessingRef = function() {
            if (!specialPropRefWarningShown) {
                specialPropRefWarningShown = true;
                error1("%s: `ref` is not a prop. Trying to access it will result in `undefined` being returned. If you need to access the same value within the child component, you should pass it as a different prop. (https://reactjs.org/link/special-props)", displayName);
            }
        };
        warnAboutAccessingRef.isReactWarning = true;
        Object.defineProperty(props, 'ref', {
            get: warnAboutAccessingRef,
            configurable: true
        });
    }
    function warnIfStringRefCannotBeAutoConverted(config) {
        if (typeof config.ref === 'string' && ReactCurrentOwner.current && config.__self && ReactCurrentOwner.current.stateNode !== config.__self) {
            var componentName = getComponentNameFromType(ReactCurrentOwner.current.type);
            if (!didWarnAboutStringRefs[componentName]) {
                error1('Component "%s" contains the string ref "%s". Support for string refs will be removed in a future major release. This case cannot be automatically converted to an arrow function. We ask you to manually fix this case by using useRef() or createRef() instead. Learn more about using refs safely here: https://reactjs.org/link/strict-mode-string-ref', componentName, config.ref);
                didWarnAboutStringRefs[componentName] = true;
            }
        }
    }
    /**
 * Factory method to create a new React element. This no longer adheres to
 * the class pattern, so do not use new to call it. Also, instanceof check
 * will not work. Instead test $$typeof field against Symbol.for('react.element') to check
 * if something is a React Element.
 *
 * @param {*} type
 * @param {*} props
 * @param {*} key
 * @param {string|object} ref
 * @param {*} owner
 * @param {*} self A *temporary* helper to detect places where `this` is
 * different from the `owner` when React.createElement is called, so that we
 * can warn. We want to get rid of owner and replace string `ref`s with arrow
 * functions, and as long as `this` and owner are the same, there will be no
 * change in behavior.
 * @param {*} source An annotation object (added by a transpiler or otherwise)
 * indicating filename, line number, and/or other information.
 * @internal
 */ var ReactElement = function(type, key, ref, self, source, owner, props) {
        var element = {
            // This tag allows us to uniquely identify this as a React Element
            $$typeof: REACT_ELEMENT_TYPE,
            // Built-in properties that belong on the element
            type: type,
            key: key,
            ref: ref,
            props: props,
            // Record the component responsible for creating this element.
            _owner: owner
        };
        // The validation flag is currently mutative. We put it on
        // an external backing store so that we can freeze the whole object.
        // This can be replaced with a WeakMap once they are implemented in
        // commonly used development environments.
        element._store = {}; // To make comparing ReactElements easier for testing purposes, we make
        // the validation flag non-enumerable (where possible, which should
        // include every environment we run tests in), so the test framework
        // ignores it.
        Object.defineProperty(element._store, 'validated', {
            configurable: false,
            enumerable: false,
            writable: true,
            value: false
        }); // self and source are DEV only properties.
        Object.defineProperty(element, '_self', {
            configurable: false,
            enumerable: false,
            writable: false,
            value: self
        }); // Two elements created in two different places should be considered
        // equal for testing purposes and therefore we hide it from enumeration.
        Object.defineProperty(element, '_source', {
            configurable: false,
            enumerable: false,
            writable: false,
            value: source
        });
        if (Object.freeze) {
            Object.freeze(element.props);
            Object.freeze(element);
        }
        return element;
    };
    /**
 * Create and return a new ReactElement of the given type.
 * See https://reactjs.org/docs/react-api.html#createelement
 */ function createElement(type, config, children) {
        var propName; // Reserved names are extracted
        var props = {};
        var key = null;
        var ref = null;
        var self = null;
        var source = null;
        if (config != null) {
            if (hasValidRef(config)) {
                ref = config.ref;
                warnIfStringRefCannotBeAutoConverted(config);
            }
            if (hasValidKey(config)) {
                checkKeyStringCoercion(config.key);
                key = '' + config.key;
            }
            self = config.__self === undefined ? null : config.__self;
            source = config.__source === undefined ? null : config.__source; // Remaining properties are added to a new props object
            for(propName in config)if (hasOwnProperty.call(config, propName) && !RESERVED_PROPS.hasOwnProperty(propName)) props[propName] = config[propName];
        } // Children can be more than one argument, and those are transferred onto
        // the newly allocated props object.
        var childrenLength = arguments.length - 2;
        if (childrenLength === 1) props.children = children;
        else if (childrenLength > 1) {
            var childArray = Array(childrenLength);
            for(var i = 0; i < childrenLength; i++)childArray[i] = arguments[i + 2];
            if (Object.freeze) Object.freeze(childArray);
            props.children = childArray;
        } // Resolve default props
        if (type && type.defaultProps) {
            var defaultProps = type.defaultProps;
            for(propName in defaultProps)if (props[propName] === undefined) props[propName] = defaultProps[propName];
        }
        if (key || ref) {
            var displayName = typeof type === 'function' ? type.displayName || type.name || 'Unknown' : type;
            if (key) defineKeyPropWarningGetter(props, displayName);
            if (ref) defineRefPropWarningGetter(props, displayName);
        }
        return ReactElement(type, key, ref, self, source, ReactCurrentOwner.current, props);
    }
    function cloneAndReplaceKey(oldElement, newKey) {
        var newElement = ReactElement(oldElement.type, newKey, oldElement.ref, oldElement._self, oldElement._source, oldElement._owner, oldElement.props);
        return newElement;
    }
    /**
 * Clone and return a new ReactElement using element as the starting point.
 * See https://reactjs.org/docs/react-api.html#cloneelement
 */ function cloneElement(element, config, children) {
        if (element === null || element === undefined) throw new Error("React.cloneElement(...): The argument must be a React element, but you passed " + element + ".");
        var propName; // Original props are copied
        var props = assign({}, element.props); // Reserved names are extracted
        var key = element.key;
        var ref = element.ref; // Self is preserved since the owner is preserved.
        var self = element._self; // Source is preserved since cloneElement is unlikely to be targeted by a
        // transpiler, and the original source is probably a better indicator of the
        // true owner.
        var source = element._source; // Owner will be preserved, unless ref is overridden
        var owner = element._owner;
        if (config != null) {
            if (hasValidRef(config)) {
                // Silently steal the ref from the parent.
                ref = config.ref;
                owner = ReactCurrentOwner.current;
            }
            if (hasValidKey(config)) {
                checkKeyStringCoercion(config.key);
                key = '' + config.key;
            } // Remaining properties override existing props
            var defaultProps;
            if (element.type && element.type.defaultProps) defaultProps = element.type.defaultProps;
            for(propName in config)if (hasOwnProperty.call(config, propName) && !RESERVED_PROPS.hasOwnProperty(propName)) {
                if (config[propName] === undefined && defaultProps !== undefined) // Resolve default props
                props[propName] = defaultProps[propName];
                else props[propName] = config[propName];
            }
        } // Children can be more than one argument, and those are transferred onto
        // the newly allocated props object.
        var childrenLength = arguments.length - 2;
        if (childrenLength === 1) props.children = children;
        else if (childrenLength > 1) {
            var childArray = Array(childrenLength);
            for(var i = 0; i < childrenLength; i++)childArray[i] = arguments[i + 2];
            props.children = childArray;
        }
        return ReactElement(element.type, key, ref, self, source, owner, props);
    }
    /**
 * Verifies the object is a ReactElement.
 * See https://reactjs.org/docs/react-api.html#isvalidelement
 * @param {?object} object
 * @return {boolean} True if `object` is a ReactElement.
 * @final
 */ function isValidElement(object) {
        return typeof object === 'object' && object !== null && object.$$typeof === REACT_ELEMENT_TYPE;
    }
    var SEPARATOR = '.';
    var SUBSEPARATOR = ':';
    /**
 * Escape and wrap key so it is safe to use as a reactid
 *
 * @param {string} key to be escaped.
 * @return {string} the escaped key.
 */ function escape(key) {
        var escapeRegex = /[=:]/g;
        var escaperLookup = {
            '=': '=0',
            ':': '=2'
        };
        var escapedString = key.replace(escapeRegex, function(match) {
            return escaperLookup[match];
        });
        return '$' + escapedString;
    }
    /**
 * TODO: Test that a single child and an array with one item have the same key
 * pattern.
 */ var didWarnAboutMaps = false;
    var userProvidedKeyEscapeRegex = /\/+/g;
    function escapeUserProvidedKey(text) {
        return text.replace(userProvidedKeyEscapeRegex, '$&/');
    }
    /**
 * Generate a key string that identifies a element within a set.
 *
 * @param {*} element A element that could contain a manual key.
 * @param {number} index Index that is used if a manual key is not provided.
 * @return {string}
 */ function getElementKey(element, index) {
        // Do some typechecking here since we call this blindly. We want to ensure
        // that we don't block potential future ES APIs.
        if (typeof element === 'object' && element !== null && element.key != null) {
            checkKeyStringCoercion(element.key);
            return escape('' + element.key);
        } // Implicit key determined by the index in the set
        return index.toString(36);
    }
    function mapIntoArray(children, array, escapedPrefix, nameSoFar, callback) {
        var type = typeof children;
        if (type === 'undefined' || type === 'boolean') // All of the above are perceived as null.
        children = null;
        var invokeCallback = false;
        if (children === null) invokeCallback = true;
        else switch(type){
            case 'string':
            case 'number':
                invokeCallback = true;
                break;
            case 'object':
                switch(children.$$typeof){
                    case REACT_ELEMENT_TYPE:
                    case REACT_PORTAL_TYPE:
                        invokeCallback = true;
                }
        }
        if (invokeCallback) {
            var _child = children;
            var mappedChild = callback(_child); // If it's the only child, treat the name as if it was wrapped in an array
            // so that it's consistent if the number of children grows:
            var childKey = nameSoFar === '' ? SEPARATOR + getElementKey(_child, 0) : nameSoFar;
            if (isArray(mappedChild)) {
                var escapedChildKey = '';
                if (childKey != null) escapedChildKey = escapeUserProvidedKey(childKey) + '/';
                mapIntoArray(mappedChild, array, escapedChildKey, '', function(c) {
                    return c;
                });
            } else if (mappedChild != null) {
                if (isValidElement(mappedChild)) {
                    // The `if` statement here prevents auto-disabling of the safe
                    // coercion ESLint rule, so we must manually disable it below.
                    // $FlowFixMe Flow incorrectly thinks React.Portal doesn't have a key
                    if (mappedChild.key && (!_child || _child.key !== mappedChild.key)) checkKeyStringCoercion(mappedChild.key);
                    mappedChild = cloneAndReplaceKey(mappedChild, // traverseAllChildren used to do for objects as children
                    escapedPrefix + (mappedChild.key && (!_child || _child.key !== mappedChild.key) ? // eslint-disable-next-line react-internal/safe-string-coercion
                    escapeUserProvidedKey('' + mappedChild.key) + '/' : '') + childKey);
                }
                array.push(mappedChild);
            }
            return 1;
        }
        var child;
        var nextName;
        var subtreeCount = 0; // Count of children found in the current subtree.
        var nextNamePrefix = nameSoFar === '' ? SEPARATOR : nameSoFar + SUBSEPARATOR;
        if (isArray(children)) for(var i = 0; i < children.length; i++){
            child = children[i];
            nextName = nextNamePrefix + getElementKey(child, i);
            subtreeCount += mapIntoArray(child, array, escapedPrefix, nextName, callback);
        }
        else {
            var iteratorFn = getIteratorFn(children);
            if (typeof iteratorFn === 'function') {
                var iterableChildren = children;
                // Warn about using Maps as children
                if (iteratorFn === iterableChildren.entries) {
                    if (!didWarnAboutMaps) warn("Using Maps as children is not supported. Use an array of keyed ReactElements instead.");
                    didWarnAboutMaps = true;
                }
                var iterator = iteratorFn.call(iterableChildren);
                var step;
                var ii = 0;
                while(!(step = iterator.next()).done){
                    child = step.value;
                    nextName = nextNamePrefix + getElementKey(child, ii++);
                    subtreeCount += mapIntoArray(child, array, escapedPrefix, nextName, callback);
                }
            } else if (type === 'object') {
                // eslint-disable-next-line react-internal/safe-string-coercion
                var childrenString = String(children);
                throw new Error("Objects are not valid as a React child (found: " + (childrenString === '[object Object]' ? 'object with keys {' + Object.keys(children).join(', ') + '}' : childrenString) + "). " + 'If you meant to render a collection of children, use an array ' + 'instead.');
            }
        }
        return subtreeCount;
    }
    /**
 * Maps children that are typically specified as `props.children`.
 *
 * See https://reactjs.org/docs/react-api.html#reactchildrenmap
 *
 * The provided mapFunction(child, index) will be called for each
 * leaf child.
 *
 * @param {?*} children Children tree container.
 * @param {function(*, int)} func The map function.
 * @param {*} context Context for mapFunction.
 * @return {object} Object containing the ordered map of results.
 */ function mapChildren(children, func, context) {
        if (children == null) return children;
        var result = [];
        var count = 0;
        mapIntoArray(children, result, '', '', function(child) {
            return func.call(context, child, count++);
        });
        return result;
    }
    /**
 * Count the number of children that are typically specified as
 * `props.children`.
 *
 * See https://reactjs.org/docs/react-api.html#reactchildrencount
 *
 * @param {?*} children Children tree container.
 * @return {number} The number of children.
 */ function countChildren(children) {
        var n = 0;
        mapChildren(children, function() {
            n++; // Don't return anything
        });
        return n;
    }
    /**
 * Iterates through children that are typically specified as `props.children`.
 *
 * See https://reactjs.org/docs/react-api.html#reactchildrenforeach
 *
 * The provided forEachFunc(child, index) will be called for each
 * leaf child.
 *
 * @param {?*} children Children tree container.
 * @param {function(*, int)} forEachFunc
 * @param {*} forEachContext Context for forEachContext.
 */ function forEachChildren(children, forEachFunc, forEachContext) {
        mapChildren(children, function() {
            forEachFunc.apply(this, arguments); // Don't return anything.
        }, forEachContext);
    }
    /**
 * Flatten a children object (typically specified as `props.children`) and
 * return an array with appropriately re-keyed children.
 *
 * See https://reactjs.org/docs/react-api.html#reactchildrentoarray
 */ function toArray(children) {
        return mapChildren(children, function(child) {
            return child;
        }) || [];
    }
    /**
 * Returns the first child in a collection of children and verifies that there
 * is only one child in the collection.
 *
 * See https://reactjs.org/docs/react-api.html#reactchildrenonly
 *
 * The current implementation of this function assumes that a single child gets
 * passed without a wrapper, but the purpose of this helper function is to
 * abstract away the particular structure of children.
 *
 * @param {?object} children Child collection structure.
 * @return {ReactElement} The first and only `ReactElement` contained in the
 * structure.
 */ function onlyChild(children) {
        if (!isValidElement(children)) throw new Error('React.Children.only expected to receive a single React element child.');
        return children;
    }
    function createContext(defaultValue) {
        // TODO: Second argument used to be an optional `calculateChangedBits`
        // function. Warn to reserve for future use?
        var context = {
            $$typeof: REACT_CONTEXT_TYPE,
            // As a workaround to support multiple concurrent renderers, we categorize
            // some renderers as primary and others as secondary. We only expect
            // there to be two concurrent renderers at most: React Native (primary) and
            // Fabric (secondary); React DOM (primary) and React ART (secondary).
            // Secondary renderers store their context values on separate fields.
            _currentValue: defaultValue,
            _currentValue2: defaultValue,
            // Used to track how many concurrent renderers this context currently
            // supports within in a single renderer. Such as parallel server rendering.
            _threadCount: 0,
            // These are circular
            Provider: null,
            Consumer: null,
            // Add these to use same hidden class in VM as ServerContext
            _defaultValue: null,
            _globalName: null
        };
        context.Provider = {
            $$typeof: REACT_PROVIDER_TYPE,
            _context: context
        };
        var hasWarnedAboutUsingNestedContextConsumers = false;
        var hasWarnedAboutUsingConsumerProvider = false;
        var hasWarnedAboutDisplayNameOnConsumer = false;
        // A separate object, but proxies back to the original context object for
        // backwards compatibility. It has a different $$typeof, so we can properly
        // warn for the incorrect usage of Context as a Consumer.
        var Consumer = {
            $$typeof: REACT_CONTEXT_TYPE,
            _context: context
        }; // $FlowFixMe: Flow complains about not setting a value, which is intentional here
        Object.defineProperties(Consumer, {
            Provider: {
                get: function() {
                    if (!hasWarnedAboutUsingConsumerProvider) {
                        hasWarnedAboutUsingConsumerProvider = true;
                        error1("Rendering <Context.Consumer.Provider> is not supported and will be removed in a future major release. Did you mean to render <Context.Provider> instead?");
                    }
                    return context.Provider;
                },
                set: function(_Provider) {
                    context.Provider = _Provider;
                }
            },
            _currentValue: {
                get: function() {
                    return context._currentValue;
                },
                set: function(_currentValue) {
                    context._currentValue = _currentValue;
                }
            },
            _currentValue2: {
                get: function() {
                    return context._currentValue2;
                },
                set: function(_currentValue2) {
                    context._currentValue2 = _currentValue2;
                }
            },
            _threadCount: {
                get: function() {
                    return context._threadCount;
                },
                set: function(_threadCount) {
                    context._threadCount = _threadCount;
                }
            },
            Consumer: {
                get: function() {
                    if (!hasWarnedAboutUsingNestedContextConsumers) {
                        hasWarnedAboutUsingNestedContextConsumers = true;
                        error1("Rendering <Context.Consumer.Consumer> is not supported and will be removed in a future major release. Did you mean to render <Context.Consumer> instead?");
                    }
                    return context.Consumer;
                }
            },
            displayName: {
                get: function() {
                    return context.displayName;
                },
                set: function(displayName) {
                    if (!hasWarnedAboutDisplayNameOnConsumer) {
                        warn("Setting `displayName` on Context.Consumer has no effect. You should set it directly on the context with Context.displayName = '%s'.", displayName);
                        hasWarnedAboutDisplayNameOnConsumer = true;
                    }
                }
            }
        }); // $FlowFixMe: Flow complains about missing properties because it doesn't understand defineProperty
        context.Consumer = Consumer;
        context._currentRenderer = null;
        context._currentRenderer2 = null;
        return context;
    }
    var Uninitialized = -1;
    var Pending = 0;
    var Resolved = 1;
    var Rejected = 2;
    function lazyInitializer(payload) {
        if (payload._status === Uninitialized) {
            var ctor = payload._result;
            var thenable = ctor(); // Transition to the next state.
            // This might throw either because it's missing or throws. If so, we treat it
            // as still uninitialized and try again next time. Which is the same as what
            // happens if the ctor or any wrappers processing the ctor throws. This might
            // end up fixing it if the resolution was a concurrency bug.
            thenable.then(function(moduleObject) {
                if (payload._status === Pending || payload._status === Uninitialized) {
                    // Transition to the next state.
                    var resolved = payload;
                    resolved._status = Resolved;
                    resolved._result = moduleObject;
                }
            }, function(error) {
                if (payload._status === Pending || payload._status === Uninitialized) {
                    // Transition to the next state.
                    var rejected = payload;
                    rejected._status = Rejected;
                    rejected._result = error;
                }
            });
            if (payload._status === Uninitialized) {
                // In case, we're still uninitialized, then we're waiting for the thenable
                // to resolve. Set it as pending in the meantime.
                var pending = payload;
                pending._status = Pending;
                pending._result = thenable;
            }
        }
        if (payload._status === Resolved) {
            var moduleObject1 = payload._result;
            if (moduleObject1 === undefined) error1("lazy: Expected the result of a dynamic import() call. Instead received: %s\n\nYour code should look like: \n  const MyComponent = lazy(() => import('./MyComponent'))\n\nDid you accidentally put curly braces around the import?", moduleObject1);
            if (!('default' in moduleObject1)) error1("lazy: Expected the result of a dynamic import() call. Instead received: %s\n\nYour code should look like: \n  const MyComponent = lazy(() => import('./MyComponent'))", moduleObject1);
            return moduleObject1.default;
        } else throw payload._result;
    }
    function lazy(ctor) {
        var payload = {
            // We use these fields to store the result.
            _status: Uninitialized,
            _result: ctor
        };
        var lazyType = {
            $$typeof: REACT_LAZY_TYPE,
            _payload: payload,
            _init: lazyInitializer
        };
        // In production, this would just set it on the object.
        var defaultProps;
        var propTypes; // $FlowFixMe
        Object.defineProperties(lazyType, {
            defaultProps: {
                configurable: true,
                get: function() {
                    return defaultProps;
                },
                set: function(newDefaultProps) {
                    error1("React.lazy(...): It is not supported to assign `defaultProps` to a lazy component import. Either specify them where the component is defined, or create a wrapping component around it.");
                    defaultProps = newDefaultProps; // Match production behavior more closely:
                    // $FlowFixMe
                    Object.defineProperty(lazyType, 'defaultProps', {
                        enumerable: true
                    });
                }
            },
            propTypes: {
                configurable: true,
                get: function() {
                    return propTypes;
                },
                set: function(newPropTypes) {
                    error1("React.lazy(...): It is not supported to assign `propTypes` to a lazy component import. Either specify them where the component is defined, or create a wrapping component around it.");
                    propTypes = newPropTypes; // Match production behavior more closely:
                    // $FlowFixMe
                    Object.defineProperty(lazyType, 'propTypes', {
                        enumerable: true
                    });
                }
            }
        });
        return lazyType;
    }
    function forwardRef(render) {
        if (render != null && render.$$typeof === REACT_MEMO_TYPE) error1("forwardRef requires a render function but received a `memo` component. Instead of forwardRef(memo(...)), use memo(forwardRef(...)).");
        else if (typeof render !== 'function') error1('forwardRef requires a render function but was given %s.', render === null ? 'null' : typeof render);
        else if (render.length !== 0 && render.length !== 2) error1('forwardRef render functions accept exactly two parameters: props and ref. %s', render.length === 1 ? 'Did you forget to use the ref parameter?' : 'Any additional parameter will be undefined.');
        if (render != null) {
            if (render.defaultProps != null || render.propTypes != null) error1("forwardRef render functions do not support propTypes or defaultProps. Did you accidentally pass a React component?");
        }
        var elementType = {
            $$typeof: REACT_FORWARD_REF_TYPE,
            render: render
        };
        var ownName;
        Object.defineProperty(elementType, 'displayName', {
            enumerable: false,
            configurable: true,
            get: function() {
                return ownName;
            },
            set: function(name) {
                ownName = name; // The inner component shouldn't inherit this display name in most cases,
                // because the component may be used elsewhere.
                // But it's nice for anonymous functions to inherit the name,
                // so that our component-stack generation logic will display their frames.
                // An anonymous function generally suggests a pattern like:
                //   React.forwardRef((props, ref) => {...});
                // This kind of inner function is not used elsewhere so the side effect is okay.
                if (!render.name && !render.displayName) render.displayName = name;
            }
        });
        return elementType;
    }
    var REACT_MODULE_REFERENCE;
    REACT_MODULE_REFERENCE = Symbol.for('react.module.reference');
    function isValidElementType(type) {
        if (typeof type === 'string' || typeof type === 'function') return true;
         // Note: typeof might be other than 'symbol' or 'number' (e.g. if it's a polyfill).
        if (type === REACT_FRAGMENT_TYPE || type === REACT_PROFILER_TYPE || enableDebugTracing || type === REACT_STRICT_MODE_TYPE || type === REACT_SUSPENSE_TYPE || type === REACT_SUSPENSE_LIST_TYPE || enableLegacyHidden || type === REACT_OFFSCREEN_TYPE || enableScopeAPI || enableCacheElement || enableTransitionTracing) return true;
        if (typeof type === 'object' && type !== null) {
            if (type.$$typeof === REACT_LAZY_TYPE || type.$$typeof === REACT_MEMO_TYPE || type.$$typeof === REACT_PROVIDER_TYPE || type.$$typeof === REACT_CONTEXT_TYPE || type.$$typeof === REACT_FORWARD_REF_TYPE || // types supported by any Flight configuration anywhere since
            // we don't know which Flight build this will end up being used
            // with.
            type.$$typeof === REACT_MODULE_REFERENCE || type.getModuleId !== undefined) return true;
        }
        return false;
    }
    function memo(type, compare) {
        if (!isValidElementType(type)) error1("memo: The first argument must be a component. Instead received: %s", type === null ? 'null' : typeof type);
        var elementType = {
            $$typeof: REACT_MEMO_TYPE,
            type: type,
            compare: compare === undefined ? null : compare
        };
        var ownName;
        Object.defineProperty(elementType, 'displayName', {
            enumerable: false,
            configurable: true,
            get: function() {
                return ownName;
            },
            set: function(name) {
                ownName = name; // The inner component shouldn't inherit this display name in most cases,
                // because the component may be used elsewhere.
                // But it's nice for anonymous functions to inherit the name,
                // so that our component-stack generation logic will display their frames.
                // An anonymous function generally suggests a pattern like:
                //   React.memo((props) => {...});
                // This kind of inner function is not used elsewhere so the side effect is okay.
                if (!type.name && !type.displayName) type.displayName = name;
            }
        });
        return elementType;
    }
    function resolveDispatcher() {
        var dispatcher = ReactCurrentDispatcher.current;
        if (dispatcher === null) error1("Invalid hook call. Hooks can only be called inside of the body of a function component. This could happen for one of the following reasons:\n1. You might have mismatching versions of React and the renderer (such as React DOM)\n2. You might be breaking the Rules of Hooks\n3. You might have more than one copy of React in the same app\nSee https://reactjs.org/link/invalid-hook-call for tips about how to debug and fix this problem.");
        // intentionally don't throw our own error because this is in a hot path.
        // Also helps ensure this is inlined.
        return dispatcher;
    }
    function useContext(Context) {
        var dispatcher = resolveDispatcher();
        // TODO: add a more generic warning for invalid values.
        if (Context._context !== undefined) {
            var realContext = Context._context; // Don't deduplicate because this legitimately causes bugs
            // and nobody should be using this in existing code.
            if (realContext.Consumer === Context) error1("Calling useContext(Context.Consumer) is not supported, may cause bugs, and will be removed in a future major release. Did you mean to call useContext(Context) instead?");
            else if (realContext.Provider === Context) error1("Calling useContext(Context.Provider) is not supported. Did you mean to call useContext(Context) instead?");
        }
        return dispatcher.useContext(Context);
    }
    function useState(initialState) {
        var dispatcher = resolveDispatcher();
        return dispatcher.useState(initialState);
    }
    function useReducer(reducer, initialArg, init) {
        var dispatcher = resolveDispatcher();
        return dispatcher.useReducer(reducer, initialArg, init);
    }
    function useRef(initialValue) {
        var dispatcher = resolveDispatcher();
        return dispatcher.useRef(initialValue);
    }
    function useEffect(create, deps) {
        var dispatcher = resolveDispatcher();
        return dispatcher.useEffect(create, deps);
    }
    function useInsertionEffect(create, deps) {
        var dispatcher = resolveDispatcher();
        return dispatcher.useInsertionEffect(create, deps);
    }
    function useLayoutEffect(create, deps) {
        var dispatcher = resolveDispatcher();
        return dispatcher.useLayoutEffect(create, deps);
    }
    function useCallback(callback, deps) {
        var dispatcher = resolveDispatcher();
        return dispatcher.useCallback(callback, deps);
    }
    function useMemo(create, deps) {
        var dispatcher = resolveDispatcher();
        return dispatcher.useMemo(create, deps);
    }
    function useImperativeHandle(ref, create, deps) {
        var dispatcher = resolveDispatcher();
        return dispatcher.useImperativeHandle(ref, create, deps);
    }
    function useDebugValue(value, formatterFn) {
        var dispatcher = resolveDispatcher();
        return dispatcher.useDebugValue(value, formatterFn);
    }
    function useTransition() {
        var dispatcher = resolveDispatcher();
        return dispatcher.useTransition();
    }
    function useDeferredValue(value) {
        var dispatcher = resolveDispatcher();
        return dispatcher.useDeferredValue(value);
    }
    function useId() {
        var dispatcher = resolveDispatcher();
        return dispatcher.useId();
    }
    function useSyncExternalStore(subscribe, getSnapshot, getServerSnapshot) {
        var dispatcher = resolveDispatcher();
        return dispatcher.useSyncExternalStore(subscribe, getSnapshot, getServerSnapshot);
    }
    // Helpers to patch console.logs to avoid logging during side-effect free
    // replaying on render function. This currently only patches the object
    // lazily which won't cover if the log function was extracted eagerly.
    // We could also eagerly patch the method.
    var disabledDepth = 0;
    var prevLog;
    var prevInfo;
    var prevWarn;
    var prevError;
    var prevGroup;
    var prevGroupCollapsed;
    var prevGroupEnd;
    function disabledLog() {}
    disabledLog.__reactDisabledLog = true;
    function disableLogs() {
        if (disabledDepth === 0) {
            /* eslint-disable react-internal/no-production-logging */ prevLog = console.log;
            prevInfo = console.info;
            prevWarn = console.warn;
            prevError = console.error;
            prevGroup = console.group;
            prevGroupCollapsed = console.groupCollapsed;
            prevGroupEnd = console.groupEnd; // https://github.com/facebook/react/issues/19099
            var props = {
                configurable: true,
                enumerable: true,
                value: disabledLog,
                writable: true
            }; // $FlowFixMe Flow thinks console is immutable.
            Object.defineProperties(console, {
                info: props,
                log: props,
                warn: props,
                error: props,
                group: props,
                groupCollapsed: props,
                groupEnd: props
            });
        /* eslint-enable react-internal/no-production-logging */ }
        disabledDepth++;
    }
    function reenableLogs() {
        disabledDepth--;
        if (disabledDepth === 0) {
            /* eslint-disable react-internal/no-production-logging */ var props = {
                configurable: true,
                enumerable: true,
                writable: true
            }; // $FlowFixMe Flow thinks console is immutable.
            Object.defineProperties(console, {
                log: assign({}, props, {
                    value: prevLog
                }),
                info: assign({}, props, {
                    value: prevInfo
                }),
                warn: assign({}, props, {
                    value: prevWarn
                }),
                error: assign({}, props, {
                    value: prevError
                }),
                group: assign({}, props, {
                    value: prevGroup
                }),
                groupCollapsed: assign({}, props, {
                    value: prevGroupCollapsed
                }),
                groupEnd: assign({}, props, {
                    value: prevGroupEnd
                })
            });
        /* eslint-enable react-internal/no-production-logging */ }
        if (disabledDepth < 0) error1("disabledDepth fell below zero. This is a bug in React. Please file an issue.");
    }
    var ReactCurrentDispatcher$1 = ReactSharedInternals.ReactCurrentDispatcher;
    var prefix;
    function describeBuiltInComponentFrame(name, source, ownerFn) {
        if (prefix === undefined) // Extract the VM specific prefix used by each line.
        try {
            throw Error();
        } catch (x) {
            var match = x.stack.trim().match(/\n( *(at )?)/);
            prefix = match && match[1] || '';
        }
         // We use the prefix to ensure our stacks line up with native stack frames.
        return '\n' + prefix + name;
    }
    var reentry = false;
    var componentFrameCache;
    var PossiblyWeakMap = typeof WeakMap === 'function' ? WeakMap : Map;
    componentFrameCache = new PossiblyWeakMap();
    function describeNativeComponentFrame(fn, construct) {
        // If something asked for a stack inside a fake render, it should get ignored.
        if (!fn || reentry) return '';
        var frame = componentFrameCache.get(fn);
        if (frame !== undefined) return frame;
        var control;
        reentry = true;
        var previousPrepareStackTrace = Error.prepareStackTrace; // $FlowFixMe It does accept undefined.
        Error.prepareStackTrace = undefined;
        var previousDispatcher;
        previousDispatcher = ReactCurrentDispatcher$1.current; // Set the dispatcher in DEV because this might be call in the render function
        // for warnings.
        ReactCurrentDispatcher$1.current = null;
        disableLogs();
        try {
            // This should throw.
            if (construct) {
                // Something should be setting the props in the constructor.
                var Fake = function() {
                    throw Error();
                }; // $FlowFixMe
                Object.defineProperty(Fake.prototype, 'props', {
                    set: function() {
                        // We use a throwing setter instead of frozen or non-writable props
                        // because that won't throw in a non-strict mode function.
                        throw Error();
                    }
                });
                if (typeof Reflect === 'object' && Reflect.construct) {
                    // We construct a different control for this case to include any extra
                    // frames added by the construct call.
                    try {
                        Reflect.construct(Fake, []);
                    } catch (x) {
                        control = x;
                    }
                    Reflect.construct(fn, [], Fake);
                } else {
                    try {
                        Fake.call();
                    } catch (x) {
                        control = x;
                    }
                    fn.call(Fake.prototype);
                }
            } else {
                try {
                    throw Error();
                } catch (x) {
                    control = x;
                }
                fn();
            }
        } catch (sample) {
            // This is inlined manually because closure doesn't do it for us.
            if (sample && control && typeof sample.stack === 'string') {
                // This extracts the first frame from the sample that isn't also in the control.
                // Skipping one frame that we assume is the frame that calls the two.
                var sampleLines = sample.stack.split('\n');
                var controlLines = control.stack.split('\n');
                var s = sampleLines.length - 1;
                var c = controlLines.length - 1;
                while(s >= 1 && c >= 0 && sampleLines[s] !== controlLines[c])// We expect at least one stack frame to be shared.
                // Typically this will be the root most one. However, stack frames may be
                // cut off due to maximum stack limits. In this case, one maybe cut off
                // earlier than the other. We assume that the sample is longer or the same
                // and there for cut off earlier. So we should find the root most frame in
                // the sample somewhere in the control.
                c--;
                for(; s >= 1 && c >= 0; s--, c--)// Next we find the first one that isn't the same which should be the
                // frame that called our sample function and the control.
                if (sampleLines[s] !== controlLines[c]) {
                    // In V8, the first line is describing the message but other VMs don't.
                    // If we're about to return the first line, and the control is also on the same
                    // line, that's a pretty good indicator that our sample threw at same line as
                    // the control. I.e. before we entered the sample frame. So we ignore this result.
                    // This can happen if you passed a class to function component, or non-function.
                    if (s !== 1 || c !== 1) do {
                        s--;
                        c--; // We may still have similar intermediate frames from the construct call.
                        // The next one that isn't the same should be our match though.
                        if (c < 0 || sampleLines[s] !== controlLines[c]) {
                            // V8 adds a "new" prefix for native classes. Let's remove it to make it prettier.
                            var _frame = '\n' + sampleLines[s].replace(' at new ', ' at '); // If our component frame is labeled "<anonymous>"
                            // but we have a user-provided "displayName"
                            // splice it in to make the stack more readable.
                            if (fn.displayName && _frame.includes('<anonymous>')) _frame = _frame.replace('<anonymous>', fn.displayName);
                            if (typeof fn === 'function') componentFrameCache.set(fn, _frame);
                            return _frame;
                        }
                    }while (s >= 1 && c >= 0)
                    break;
                }
            }
        } finally{
            reentry = false;
            ReactCurrentDispatcher$1.current = previousDispatcher;
            reenableLogs();
            Error.prepareStackTrace = previousPrepareStackTrace;
        } // Fallback to just using the name if we couldn't make it throw.
        var name = fn ? fn.displayName || fn.name : '';
        var syntheticFrame = name ? describeBuiltInComponentFrame(name) : '';
        if (typeof fn === 'function') componentFrameCache.set(fn, syntheticFrame);
        return syntheticFrame;
    }
    function describeFunctionComponentFrame(fn, source, ownerFn) {
        return describeNativeComponentFrame(fn, false);
    }
    function shouldConstruct(Component) {
        var prototype = Component.prototype;
        return !!(prototype && prototype.isReactComponent);
    }
    function describeUnknownElementTypeFrameInDEV(type, source, ownerFn) {
        if (type == null) return '';
        if (typeof type === 'function') return describeNativeComponentFrame(type, shouldConstruct(type));
        if (typeof type === 'string') return describeBuiltInComponentFrame(type);
        switch(type){
            case REACT_SUSPENSE_TYPE:
                return describeBuiltInComponentFrame('Suspense');
            case REACT_SUSPENSE_LIST_TYPE:
                return describeBuiltInComponentFrame('SuspenseList');
        }
        if (typeof type === 'object') switch(type.$$typeof){
            case REACT_FORWARD_REF_TYPE:
                return describeFunctionComponentFrame(type.render);
            case REACT_MEMO_TYPE:
                // Memo may contain any component type so we recursively resolve it.
                return describeUnknownElementTypeFrameInDEV(type.type, source, ownerFn);
            case REACT_LAZY_TYPE:
                var lazyComponent = type;
                var payload = lazyComponent._payload;
                var init = lazyComponent._init;
                try {
                    // Lazy may contain any component type so we recursively resolve it.
                    return describeUnknownElementTypeFrameInDEV(init(payload), source, ownerFn);
                } catch (x) {}
        }
        return '';
    }
    var loggedTypeFailures = {};
    var ReactDebugCurrentFrame$1 = ReactSharedInternals.ReactDebugCurrentFrame;
    function setCurrentlyValidatingElement(element) {
        if (element) {
            var owner = element._owner;
            var stack = describeUnknownElementTypeFrameInDEV(element.type, element._source, owner ? owner.type : null);
            ReactDebugCurrentFrame$1.setExtraStackFrame(stack);
        } else ReactDebugCurrentFrame$1.setExtraStackFrame(null);
    }
    function checkPropTypes(typeSpecs, values, location, componentName, element) {
        // $FlowFixMe This is okay but Flow doesn't know it.
        var has = Function.call.bind(hasOwnProperty);
        for(var typeSpecName in typeSpecs)if (has(typeSpecs, typeSpecName)) {
            var error$1 = void 0; // Prop type validation may throw. In case they do, we don't want to
            // fail the render phase where it didn't fail before. So we log it.
            // After these have been cleaned up, we'll let them throw.
            try {
                // This is intentionally an invariant that gets caught. It's the same
                // behavior as without this statement except with a better message.
                if (typeof typeSpecs[typeSpecName] !== 'function') {
                    // eslint-disable-next-line react-internal/prod-error-codes
                    var err = Error((componentName || 'React class') + ': ' + location + ' type `' + typeSpecName + '` is invalid; ' + 'it must be a function, usually from the `prop-types` package, but received `' + typeof typeSpecs[typeSpecName] + '`.' + 'This often happens because of typos such as `PropTypes.function` instead of `PropTypes.func`.');
                    err.name = 'Invariant Violation';
                    throw err;
                }
                error$1 = typeSpecs[typeSpecName](values, typeSpecName, componentName, location, null, 'SECRET_DO_NOT_PASS_THIS_OR_YOU_WILL_BE_FIRED');
            } catch (ex) {
                error$1 = ex;
            }
            if (error$1 && !(error$1 instanceof Error)) {
                setCurrentlyValidatingElement(element);
                error1("%s: type specification of %s `%s` is invalid; the type checker function must return `null` or an `Error` but returned a %s. You may have forgotten to pass an argument to the type checker creator (arrayOf, instanceOf, objectOf, oneOf, oneOfType, and shape all require an argument).", componentName || 'React class', location, typeSpecName, typeof error$1);
                setCurrentlyValidatingElement(null);
            }
            if (error$1 instanceof Error && !(error$1.message in loggedTypeFailures)) {
                // Only monitor this failure once because there tends to be a lot of the
                // same error.
                loggedTypeFailures[error$1.message] = true;
                setCurrentlyValidatingElement(element);
                error1('Failed %s type: %s', location, error$1.message);
                setCurrentlyValidatingElement(null);
            }
        }
    }
    function setCurrentlyValidatingElement$1(element) {
        if (element) {
            var owner = element._owner;
            var stack = describeUnknownElementTypeFrameInDEV(element.type, element._source, owner ? owner.type : null);
            setExtraStackFrame(stack);
        } else setExtraStackFrame(null);
    }
    var propTypesMisspellWarningShown;
    propTypesMisspellWarningShown = false;
    function getDeclarationErrorAddendum() {
        if (ReactCurrentOwner.current) {
            var name = getComponentNameFromType(ReactCurrentOwner.current.type);
            if (name) return '\n\nCheck the render method of `' + name + '`.';
        }
        return '';
    }
    function getSourceInfoErrorAddendum(source) {
        if (source !== undefined) {
            var fileName = source.fileName.replace(/^.*[\\\/]/, '');
            var lineNumber = source.lineNumber;
            return '\n\nCheck your code at ' + fileName + ':' + lineNumber + '.';
        }
        return '';
    }
    function getSourceInfoErrorAddendumForProps(elementProps) {
        if (elementProps !== null && elementProps !== undefined) return getSourceInfoErrorAddendum(elementProps.__source);
        return '';
    }
    /**
 * Warn if there's no key explicitly set on dynamic arrays of children or
 * object keys are not valid. This allows us to keep track of children between
 * updates.
 */ var ownerHasKeyUseWarning = {};
    function getCurrentComponentErrorInfo(parentType) {
        var info = getDeclarationErrorAddendum();
        if (!info) {
            var parentName = typeof parentType === 'string' ? parentType : parentType.displayName || parentType.name;
            if (parentName) info = "\n\nCheck the top-level render call using <" + parentName + ">.";
        }
        return info;
    }
    /**
 * Warn if the element doesn't have an explicit key assigned to it.
 * This element is in an array. The array could grow and shrink or be
 * reordered. All children that haven't already been validated are required to
 * have a "key" property assigned to it. Error statuses are cached so a warning
 * will only be shown once.
 *
 * @internal
 * @param {ReactElement} element Element that requires a key.
 * @param {*} parentType element's parent's type.
 */ function validateExplicitKey(element, parentType) {
        if (!element._store || element._store.validated || element.key != null) return;
        element._store.validated = true;
        var currentComponentErrorInfo = getCurrentComponentErrorInfo(parentType);
        if (ownerHasKeyUseWarning[currentComponentErrorInfo]) return;
        ownerHasKeyUseWarning[currentComponentErrorInfo] = true; // Usually the current owner is the offender, but if it accepts children as a
        // property, it may be the creator of the child that's responsible for
        // assigning it a key.
        var childOwner = '';
        if (element && element._owner && element._owner !== ReactCurrentOwner.current) // Give the component that originally created this child.
        childOwner = " It was passed a child from " + getComponentNameFromType(element._owner.type) + ".";
        setCurrentlyValidatingElement$1(element);
        error1('Each child in a list should have a unique "key" prop.%s%s See https://reactjs.org/link/warning-keys for more information.', currentComponentErrorInfo, childOwner);
        setCurrentlyValidatingElement$1(null);
    }
    /**
 * Ensure that every element either is passed in a static location, in an
 * array with an explicit keys property defined, or in an object literal
 * with valid key property.
 *
 * @internal
 * @param {ReactNode} node Statically passed child of any type.
 * @param {*} parentType node's parent's type.
 */ function validateChildKeys(node, parentType) {
        if (typeof node !== 'object') return;
        if (isArray(node)) for(var i = 0; i < node.length; i++){
            var child = node[i];
            if (isValidElement(child)) validateExplicitKey(child, parentType);
        }
        else if (isValidElement(node)) // This element was passed in a valid location.
        {
            if (node._store) node._store.validated = true;
        } else if (node) {
            var iteratorFn = getIteratorFn(node);
            if (typeof iteratorFn === 'function') // Entry iterators used to provide implicit keys,
            // but now we print a separate warning for them later.
            {
                if (iteratorFn !== node.entries) {
                    var iterator = iteratorFn.call(node);
                    var step;
                    while(!(step = iterator.next()).done)if (isValidElement(step.value)) validateExplicitKey(step.value, parentType);
                }
            }
        }
    }
    /**
 * Given an element, validate that its props follow the propTypes definition,
 * provided by the type.
 *
 * @param {ReactElement} element
 */ function validatePropTypes(element) {
        var type = element.type;
        if (type === null || type === undefined || typeof type === 'string') return;
        var propTypes;
        if (typeof type === 'function') propTypes = type.propTypes;
        else if (typeof type === 'object' && (type.$$typeof === REACT_FORWARD_REF_TYPE || // Inner props are checked in the reconciler.
        type.$$typeof === REACT_MEMO_TYPE)) propTypes = type.propTypes;
        else return;
        if (propTypes) {
            // Intentionally inside to avoid triggering lazy initializers:
            var name = getComponentNameFromType(type);
            checkPropTypes(propTypes, element.props, 'prop', name, element);
        } else if (type.PropTypes !== undefined && !propTypesMisspellWarningShown) {
            propTypesMisspellWarningShown = true; // Intentionally inside to avoid triggering lazy initializers:
            var _name = getComponentNameFromType(type);
            error1('Component %s declared `PropTypes` instead of `propTypes`. Did you misspell the property assignment?', _name || 'Unknown');
        }
        if (typeof type.getDefaultProps === 'function' && !type.getDefaultProps.isReactClassApproved) error1("getDefaultProps is only used on classic React.createClass definitions. Use a static property named `defaultProps` instead.");
    }
    /**
 * Given a fragment, validate that it can only be provided with fragment props
 * @param {ReactElement} fragment
 */ function validateFragmentProps(fragment) {
        var keys = Object.keys(fragment.props);
        for(var i = 0; i < keys.length; i++){
            var key = keys[i];
            if (key !== 'children' && key !== 'key') {
                setCurrentlyValidatingElement$1(fragment);
                error1("Invalid prop `%s` supplied to `React.Fragment`. React.Fragment can only have `key` and `children` props.", key);
                setCurrentlyValidatingElement$1(null);
                break;
            }
        }
        if (fragment.ref !== null) {
            setCurrentlyValidatingElement$1(fragment);
            error1('Invalid attribute `ref` supplied to `React.Fragment`.');
            setCurrentlyValidatingElement$1(null);
        }
    }
    function createElementWithValidation(type, props, children) {
        var validType = isValidElementType(type); // We warn in this case but don't throw. We expect the element creation to
        // succeed and there will likely be errors in render.
        if (!validType) {
            var info = '';
            if (type === undefined || typeof type === 'object' && type !== null && Object.keys(type).length === 0) info += " You likely forgot to export your component from the file it's defined in, or you might have mixed up default and named imports.";
            var sourceInfo = getSourceInfoErrorAddendumForProps(props);
            if (sourceInfo) info += sourceInfo;
            else info += getDeclarationErrorAddendum();
            var typeString;
            if (type === null) typeString = 'null';
            else if (isArray(type)) typeString = 'array';
            else if (type !== undefined && type.$$typeof === REACT_ELEMENT_TYPE) {
                typeString = "<" + (getComponentNameFromType(type.type) || 'Unknown') + " />";
                info = ' Did you accidentally export a JSX literal instead of a component?';
            } else typeString = typeof type;
            error1("React.createElement: type is invalid -- expected a string (for built-in components) or a class/function (for composite components) but got: %s.%s", typeString, info);
        }
        var element = createElement.apply(this, arguments); // The result can be nullish if a mock or a custom function is used.
        // TODO: Drop this when these are no longer allowed as the type argument.
        if (element == null) return element;
         // Skip key warning if the type isn't valid since our key validation logic
        // doesn't expect a non-string/function type and can throw confusing errors.
        // We don't want exception behavior to differ between dev and prod.
        // (Rendering will throw with a helpful message and as soon as the type is
        // fixed, the key warnings will appear.)
        if (validType) for(var i = 2; i < arguments.length; i++)validateChildKeys(arguments[i], type);
        if (type === REACT_FRAGMENT_TYPE) validateFragmentProps(element);
        else validatePropTypes(element);
        return element;
    }
    var didWarnAboutDeprecatedCreateFactory = false;
    function createFactoryWithValidation(type) {
        var validatedFactory = createElementWithValidation.bind(null, type);
        validatedFactory.type = type;
        if (!didWarnAboutDeprecatedCreateFactory) {
            didWarnAboutDeprecatedCreateFactory = true;
            warn("React.createFactory() is deprecated and will be removed in a future major release. Consider using JSX or use React.createElement() directly instead.");
        } // Legacy hook: remove it
        Object.defineProperty(validatedFactory, 'type', {
            enumerable: false,
            get: function() {
                warn("Factory.type is deprecated. Access the class directly before passing it to createFactory.");
                Object.defineProperty(this, 'type', {
                    value: type
                });
                return type;
            }
        });
        return validatedFactory;
    }
    function cloneElementWithValidation(element, props, children) {
        var newElement = cloneElement.apply(this, arguments);
        for(var i = 2; i < arguments.length; i++)validateChildKeys(arguments[i], newElement.type);
        validatePropTypes(newElement);
        return newElement;
    }
    function startTransition(scope, options) {
        var prevTransition = ReactCurrentBatchConfig.transition;
        ReactCurrentBatchConfig.transition = {};
        var currentTransition = ReactCurrentBatchConfig.transition;
        ReactCurrentBatchConfig.transition._updatedFibers = new Set();
        try {
            scope();
        } finally{
            ReactCurrentBatchConfig.transition = prevTransition;
            if (prevTransition === null && currentTransition._updatedFibers) {
                var updatedFibersCount = currentTransition._updatedFibers.size;
                if (updatedFibersCount > 10) warn("Detected a large number of updates inside startTransition. If this is due to a subscription please re-write it to use React provided hooks. Otherwise concurrent mode guarantees are off the table.");
                currentTransition._updatedFibers.clear();
            }
        }
    }
    var didWarnAboutMessageChannel = false;
    var enqueueTaskImpl = null;
    function enqueueTask(task) {
        if (enqueueTaskImpl === null) try {
            // read require off the module object to get around the bundlers.
            // we don't want them to detect a require and bundle a Node polyfill.
            var requireString = ('require' + Math.random()).slice(0, 7);
            var nodeRequire = module && module[requireString]; // assuming we're in node, let's try to get node's
            // version of setImmediate, bypassing fake timers if any.
            enqueueTaskImpl = nodeRequire.call(module, 'timers').setImmediate;
        } catch (_err) {
            // we're in a browser
            // we can't use regular timers because they may still be faked
            // so we try MessageChannel+postMessage instead
            enqueueTaskImpl = function(callback) {
                if (didWarnAboutMessageChannel === false) {
                    didWarnAboutMessageChannel = true;
                    if (typeof MessageChannel === 'undefined') error1("This browser does not have a MessageChannel implementation, so enqueuing tasks via await act(async () => ...) will fail. Please file an issue at https://github.com/facebook/react/issues if you encounter this warning.");
                }
                var channel = new MessageChannel();
                channel.port1.onmessage = callback;
                channel.port2.postMessage(undefined);
            };
        }
        return enqueueTaskImpl(task);
    }
    var actScopeDepth = 0;
    var didWarnNoAwaitAct = false;
    function act(callback) {
        // `act` calls can be nested, so we track the depth. This represents the
        // number of `act` scopes on the stack.
        var prevActScopeDepth = actScopeDepth;
        actScopeDepth++;
        if (ReactCurrentActQueue.current === null) // This is the outermost `act` scope. Initialize the queue. The reconciler
        // will detect the queue and use it instead of Scheduler.
        ReactCurrentActQueue.current = [];
        var prevIsBatchingLegacy = ReactCurrentActQueue.isBatchingLegacy;
        var result;
        try {
            // Used to reproduce behavior of `batchedUpdates` in legacy mode. Only
            // set to `true` while the given callback is executed, not for updates
            // triggered during an async event, because this is how the legacy
            // implementation of `act` behaved.
            ReactCurrentActQueue.isBatchingLegacy = true;
            result = callback(); // Replicate behavior of original `act` implementation in legacy mode,
            // which flushed updates immediately after the scope function exits, even
            // if it's an async function.
            if (!prevIsBatchingLegacy && ReactCurrentActQueue.didScheduleLegacyUpdate) {
                var queue = ReactCurrentActQueue.current;
                if (queue !== null) {
                    ReactCurrentActQueue.didScheduleLegacyUpdate = false;
                    flushActQueue(queue);
                }
            }
        } catch (error2) {
            popActScope(prevActScopeDepth);
            throw error2;
        } finally{
            ReactCurrentActQueue.isBatchingLegacy = prevIsBatchingLegacy;
        }
        if (result !== null && typeof result === 'object' && typeof result.then === 'function') {
            var thenableResult = result; // The callback is an async function (i.e. returned a promise). Wait
            // for it to resolve before exiting the current scope.
            var wasAwaited = false;
            var thenable = {
                then: function(resolve, reject) {
                    wasAwaited = true;
                    thenableResult.then(function(returnValue) {
                        popActScope(prevActScopeDepth);
                        if (actScopeDepth === 0) // We've exited the outermost act scope. Recursively flush the
                        // queue until there's no remaining work.
                        recursivelyFlushAsyncActWork(returnValue, resolve, reject);
                        else resolve(returnValue);
                    }, function(error) {
                        // The callback threw an error.
                        popActScope(prevActScopeDepth);
                        reject(error);
                    });
                }
            };
            if (!didWarnNoAwaitAct && typeof Promise !== 'undefined') // eslint-disable-next-line no-undef
            Promise.resolve().then(function() {}).then(function() {
                if (!wasAwaited) {
                    didWarnNoAwaitAct = true;
                    error1("You called act(async () => ...) without await. This could lead to unexpected testing behaviour, interleaving multiple act calls and mixing their scopes. You should - await act(async () => ...);");
                }
            });
            return thenable;
        } else {
            var returnValue1 = result; // The callback is not an async function. Exit the current scope
            // immediately, without awaiting.
            popActScope(prevActScopeDepth);
            if (actScopeDepth === 0) {
                // Exiting the outermost act scope. Flush the queue.
                var _queue = ReactCurrentActQueue.current;
                if (_queue !== null) {
                    flushActQueue(_queue);
                    ReactCurrentActQueue.current = null;
                } // Return a thenable. If the user awaits it, we'll flush again in
                // case additional work was scheduled by a microtask.
                var _thenable = {
                    then: function(resolve, reject) {
                        // Confirm we haven't re-entered another `act` scope, in case
                        // the user does something weird like await the thenable
                        // multiple times.
                        if (ReactCurrentActQueue.current === null) {
                            // Recursively flush the queue until there's no remaining work.
                            ReactCurrentActQueue.current = [];
                            recursivelyFlushAsyncActWork(returnValue1, resolve, reject);
                        } else resolve(returnValue1);
                    }
                };
                return _thenable;
            } else {
                // Since we're inside a nested `act` scope, the returned thenable
                // immediately resolves. The outer scope will flush the queue.
                var _thenable2 = {
                    then: function(resolve, reject) {
                        resolve(returnValue1);
                    }
                };
                return _thenable2;
            }
        }
    }
    function popActScope(prevActScopeDepth) {
        if (prevActScopeDepth !== actScopeDepth - 1) error1("You seem to have overlapping act() calls, this is not supported. Be sure to await previous act() calls before making a new one. ");
        actScopeDepth = prevActScopeDepth;
    }
    function recursivelyFlushAsyncActWork(returnValue, resolve, reject) {
        var queue = ReactCurrentActQueue.current;
        if (queue !== null) try {
            flushActQueue(queue);
            enqueueTask(function() {
                if (queue.length === 0) {
                    // No additional work was scheduled. Finish.
                    ReactCurrentActQueue.current = null;
                    resolve(returnValue);
                } else // Keep flushing work until there's none left.
                recursivelyFlushAsyncActWork(returnValue, resolve, reject);
            });
        } catch (error) {
            reject(error);
        }
        else resolve(returnValue);
    }
    var isFlushing = false;
    function flushActQueue(queue) {
        if (!isFlushing) {
            // Prevent re-entrance.
            isFlushing = true;
            var i = 0;
            try {
                for(; i < queue.length; i++){
                    var callback = queue[i];
                    do callback = callback(true);
                    while (callback !== null)
                }
                queue.length = 0;
            } catch (error) {
                // If something throws, leave the remaining callbacks on the queue.
                queue = queue.slice(i + 1);
                throw error;
            } finally{
                isFlushing = false;
            }
        }
    }
    var createElement$1 = createElementWithValidation;
    var cloneElement$1 = cloneElementWithValidation;
    var createFactory = createFactoryWithValidation;
    var Children = {
        map: mapChildren,
        forEach: forEachChildren,
        count: countChildren,
        toArray: toArray,
        only: onlyChild
    };
    exports.Children = Children;
    exports.Component = Component1;
    exports.Fragment = REACT_FRAGMENT_TYPE;
    exports.Profiler = REACT_PROFILER_TYPE;
    exports.PureComponent = PureComponent;
    exports.StrictMode = REACT_STRICT_MODE_TYPE;
    exports.Suspense = REACT_SUSPENSE_TYPE;
    exports.__SECRET_INTERNALS_DO_NOT_USE_OR_YOU_WILL_BE_FIRED = ReactSharedInternals;
    exports.cloneElement = cloneElement$1;
    exports.createContext = createContext;
    exports.createElement = createElement$1;
    exports.createFactory = createFactory;
    exports.createRef = createRef;
    exports.forwardRef = forwardRef;
    exports.isValidElement = isValidElement;
    exports.lazy = lazy;
    exports.memo = memo;
    exports.startTransition = startTransition;
    exports.unstable_act = act;
    exports.useCallback = useCallback;
    exports.useContext = useContext;
    exports.useDebugValue = useDebugValue;
    exports.useDeferredValue = useDeferredValue;
    exports.useEffect = useEffect;
    exports.useId = useId;
    exports.useImperativeHandle = useImperativeHandle;
    exports.useInsertionEffect = useInsertionEffect;
    exports.useLayoutEffect = useLayoutEffect;
    exports.useMemo = useMemo;
    exports.useReducer = useReducer;
    exports.useRef = useRef;
    exports.useState = useState;
    exports.useSyncExternalStore = useSyncExternalStore;
    exports.useTransition = useTransition;
    exports.version = ReactVersion;
    /* global __REACT_DEVTOOLS_GLOBAL_HOOK__ */ if (typeof __REACT_DEVTOOLS_GLOBAL_HOOK__ !== 'undefined' && typeof __REACT_DEVTOOLS_GLOBAL_HOOK__.registerInternalModuleStop === 'function') __REACT_DEVTOOLS_GLOBAL_HOOK__.registerInternalModuleStop(new Error());
})();

},{}],"j6uA9":[function(require,module,exports) {
'use strict';
function checkDCE() {
    /* global __REACT_DEVTOOLS_GLOBAL_HOOK__ */ if (typeof __REACT_DEVTOOLS_GLOBAL_HOOK__ === 'undefined' || typeof __REACT_DEVTOOLS_GLOBAL_HOOK__.checkDCE !== 'function') return;
    var err;
    // This branch is unreachable because this function is only called
    // in production, but the condition is true only in development.
    // Therefore if the branch is still here, dead code elimination wasn't
    // properly applied.
    // Don't change the message. React DevTools relies on it. Also make sure
    // this message doesn't occur elsewhere in this function, or it will cause
    // a false positive.
    throw new Error('^_^');
}
module.exports = require('./cjs/react-dom.development.js');

},{"./cjs/react-dom.development.js":"3iA9v"}],"3iA9v":[function(require,module,exports) {
/**
 * @license React
 * react-dom.development.js
 *
 * Copyright (c) Facebook, Inc. and its affiliates.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE file in the root directory of this source tree.
 */ 'use strict';
(function() {
    /* global __REACT_DEVTOOLS_GLOBAL_HOOK__ */ if (typeof __REACT_DEVTOOLS_GLOBAL_HOOK__ !== 'undefined' && typeof __REACT_DEVTOOLS_GLOBAL_HOOK__.registerInternalModuleStart === 'function') __REACT_DEVTOOLS_GLOBAL_HOOK__.registerInternalModuleStart(new Error());
    var React = require('react');
    var Scheduler = require('scheduler');
    var ReactSharedInternals = React.__SECRET_INTERNALS_DO_NOT_USE_OR_YOU_WILL_BE_FIRED;
    var suppressWarning = false;
    function setSuppressWarning(newSuppressWarning) {
        suppressWarning = newSuppressWarning;
    } // In DEV, calls to console.warn and console.error get replaced
    // by calls to these methods by a Babel plugin.
    //
    // In PROD (or in packages without access to React internals),
    // they are left as they are instead.
    function warn(format) {
        if (!suppressWarning) {
            for(var _len = arguments.length, args = new Array(_len > 1 ? _len - 1 : 0), _key = 1; _key < _len; _key++)args[_key - 1] = arguments[_key];
            printWarning('warn', format, args);
        }
    }
    function error1(format) {
        if (!suppressWarning) {
            for(var _len2 = arguments.length, args = new Array(_len2 > 1 ? _len2 - 1 : 0), _key2 = 1; _key2 < _len2; _key2++)args[_key2 - 1] = arguments[_key2];
            printWarning('error', format, args);
        }
    }
    function printWarning(level, format, args) {
        var ReactDebugCurrentFrame = ReactSharedInternals.ReactDebugCurrentFrame;
        var stack = ReactDebugCurrentFrame.getStackAddendum();
        if (stack !== '') {
            format += '%s';
            args = args.concat([
                stack
            ]);
        } // eslint-disable-next-line react-internal/safe-string-coercion
        var argsWithFormat = args.map(function(item) {
            return String(item);
        }); // Careful: RN currently depends on this prefix
        argsWithFormat.unshift('Warning: ' + format); // We intentionally don't use spread (or .apply) directly because it
        // breaks IE9: https://github.com/facebook/react/issues/13610
        // eslint-disable-next-line react-internal/no-production-logging
        Function.prototype.apply.call(console[level], console, argsWithFormat);
    }
    var FunctionComponent = 0;
    var ClassComponent = 1;
    var IndeterminateComponent = 2; // Before we know whether it is function or class
    var HostRoot = 3; // Root of a host tree. Could be nested inside another node.
    var HostPortal = 4; // A subtree. Could be an entry point to a different renderer.
    var HostComponent = 5;
    var HostText = 6;
    var Fragment = 7;
    var Mode = 8;
    var ContextConsumer = 9;
    var ContextProvider = 10;
    var ForwardRef = 11;
    var Profiler = 12;
    var SuspenseComponent = 13;
    var MemoComponent = 14;
    var SimpleMemoComponent = 15;
    var LazyComponent = 16;
    var IncompleteClassComponent = 17;
    var DehydratedFragment = 18;
    var SuspenseListComponent = 19;
    var ScopeComponent = 21;
    var OffscreenComponent = 22;
    var LegacyHiddenComponent = 23;
    var CacheComponent = 24;
    var TracingMarkerComponent = 25;
    // -----------------------------------------------------------------------------
    var enableClientRenderFallbackOnTextMismatch = true; // TODO: Need to review this code one more time before landing
    // the react-reconciler package.
    var enableNewReconciler = false; // Support legacy Primer support on internal FB www
    var enableLazyContextPropagation = false; // FB-only usage. The new API has different semantics.
    var enableLegacyHidden = false; // Enables unstable_avoidThisFallback feature in Fiber
    var enableSuspenseAvoidThisFallback = false; // Enables unstable_avoidThisFallback feature in Fizz
    // React DOM Chopping Block
    //
    // Similar to main Chopping Block but only flags related to React DOM. These are
    // grouped because we will likely batch all of them into a single major release.
    // -----------------------------------------------------------------------------
    // Disable support for comment nodes as React DOM containers. Already disabled
    // in open source, but www codebase still relies on it. Need to remove.
    var disableCommentsAsDOMContainers = true; // Disable javascript: URL strings in href for XSS protection.
    // and client rendering, mostly to allow JSX attributes to apply to the custom
    // element's object properties instead of only HTML attributes.
    // https://github.com/facebook/react/issues/11347
    var enableCustomElementPropertySupport = false; // Disables children for <textarea> elements
    var warnAboutStringRefs = false; // -----------------------------------------------------------------------------
    // Debugging and DevTools
    // -----------------------------------------------------------------------------
    // Adds user timing marks for e.g. state updates, suspense, and work loop stuff,
    // for an experimental timeline tool.
    var enableSchedulingProfiler = true; // Helps identify side effects in render-phase lifecycle hooks and setState
    var enableProfilerTimer = true; // Record durations for commit and passive effects phases.
    var enableProfilerCommitHooks = true; // Phase param passed to onRender callback differentiates between an "update" and a "cascading-update".
    var allNativeEvents = new Set();
    /**
 * Mapping from registration name to event name
 */ var registrationNameDependencies1 = {};
    /**
 * Mapping from lowercase registration names to the properly cased version,
 * used to warn in the case of missing event handlers. Available
 * only in true.
 * @type {Object}
 */ var possibleRegistrationNames1 = {}; // Trust the developer to only use possibleRegistrationNames in true
    function registerTwoPhaseEvent(registrationName, dependencies) {
        registerDirectEvent(registrationName, dependencies);
        registerDirectEvent(registrationName + 'Capture', dependencies);
    }
    function registerDirectEvent(registrationName, dependencies) {
        if (registrationNameDependencies1[registrationName]) error1("EventRegistry: More than one plugin attempted to publish the same registration name, `%s`.", registrationName);
        registrationNameDependencies1[registrationName] = dependencies;
        var lowerCasedName = registrationName.toLowerCase();
        possibleRegistrationNames1[lowerCasedName] = registrationName;
        if (registrationName === 'onDoubleClick') possibleRegistrationNames1.ondblclick = registrationName;
        for(var i = 0; i < dependencies.length; i++)allNativeEvents.add(dependencies[i]);
    }
    var canUseDOM = !!(typeof window !== 'undefined' && typeof window.document !== 'undefined' && typeof window.document.createElement !== 'undefined');
    var hasOwnProperty = Object.prototype.hasOwnProperty;
    /*
 * The `'' + value` pattern (used in in perf-sensitive code) throws for Symbol
 * and Temporal.* types. See https://github.com/facebook/react/pull/22064.
 *
 * The functions in this module will throw an easier-to-understand,
 * easier-to-debug exception with a clear errors message message explaining the
 * problem. (Instead of a confusing exception thrown inside the implementation
 * of the `value` object).
 */ // $FlowFixMe only called in DEV, so void return is not possible.
    function typeName(value) {
        // toStringTag is needed for namespaced types like Temporal.Instant
        var hasToStringTag = typeof Symbol === 'function' && Symbol.toStringTag;
        var type = hasToStringTag && value[Symbol.toStringTag] || value.constructor.name || 'Object';
        return type;
    } // $FlowFixMe only called in DEV, so void return is not possible.
    function willCoercionThrow(value) {
        try {
            testStringCoercion(value);
            return false;
        } catch (e) {
            return true;
        }
    }
    function testStringCoercion(value) {
        // If you ended up here by following an exception call stack, here's what's
        // happened: you supplied an object or symbol value to React (as a prop, key,
        // DOM attribute, CSS property, string ref, etc.) and when React tried to
        // coerce it to a string using `'' + value`, an exception was thrown.
        //
        // The most common types that will cause this exception are `Symbol` instances
        // and Temporal objects like `Temporal.Instant`. But any object that has a
        // `valueOf` or `[Symbol.toPrimitive]` method that throws will also cause this
        // exception. (Library authors do this to prevent users from using built-in
        // numeric operators like `+` or comparison operators like `>=` because custom
        // methods are needed to perform accurate arithmetic or comparison.)
        //
        // To fix the problem, coerce this object or symbol value to a string before
        // passing it to React. The most reliable way is usually `String(value)`.
        //
        // To find which value is throwing, check the browser or debugger console.
        // Before this exception was thrown, there should be `console.error` output
        // that shows the type (Symbol, Temporal.PlainDate, etc.) that caused the
        // problem and how that type was used: key, atrribute, input value prop, etc.
        // In most cases, this console output also shows the component and its
        // ancestor components where the exception happened.
        //
        // eslint-disable-next-line react-internal/safe-string-coercion
        return '' + value;
    }
    function checkAttributeStringCoercion(value, attributeName) {
        if (willCoercionThrow(value)) {
            error1("The provided `%s` attribute is an unsupported type %s. This value must be coerced to a string before before using it here.", attributeName, typeName(value));
            return testStringCoercion(value); // throw (to help callers find troubleshooting comments)
        }
    }
    function checkKeyStringCoercion(value) {
        if (willCoercionThrow(value)) {
            error1("The provided key is an unsupported type %s. This value must be coerced to a string before before using it here.", typeName(value));
            return testStringCoercion(value); // throw (to help callers find troubleshooting comments)
        }
    }
    function checkPropStringCoercion(value, propName) {
        if (willCoercionThrow(value)) {
            error1("The provided `%s` prop is an unsupported type %s. This value must be coerced to a string before before using it here.", propName, typeName(value));
            return testStringCoercion(value); // throw (to help callers find troubleshooting comments)
        }
    }
    function checkCSSPropertyStringCoercion(value, propName) {
        if (willCoercionThrow(value)) {
            error1("The provided `%s` CSS property is an unsupported type %s. This value must be coerced to a string before before using it here.", propName, typeName(value));
            return testStringCoercion(value); // throw (to help callers find troubleshooting comments)
        }
    }
    function checkHtmlStringCoercion(value) {
        if (willCoercionThrow(value)) {
            error1("The provided HTML markup uses a value of unsupported type %s. This value must be coerced to a string before before using it here.", typeName(value));
            return testStringCoercion(value); // throw (to help callers find troubleshooting comments)
        }
    }
    function checkFormFieldValueStringCoercion(value) {
        if (willCoercionThrow(value)) {
            error1("Form field values (value, checked, defaultValue, or defaultChecked props) must be strings, not %s. This value must be coerced to a string before before using it here.", typeName(value));
            return testStringCoercion(value); // throw (to help callers find troubleshooting comments)
        }
    }
    // A reserved attribute.
    // It is handled by React separately and shouldn't be written to the DOM.
    var RESERVED = 0; // A simple string attribute.
    // Attributes that aren't in the filter are presumed to have this type.
    var STRING = 1; // A string attribute that accepts booleans in React. In HTML, these are called
    // "enumerated" attributes with "true" and "false" as possible values.
    // When true, it should be set to a "true" string.
    // When false, it should be set to a "false" string.
    var BOOLEANISH_STRING = 2; // A real boolean attribute.
    // When true, it should be present (set either to an empty string or its name).
    // When false, it should be omitted.
    var BOOLEAN = 3; // An attribute that can be used as a flag as well as with a value.
    // When true, it should be present (set either to an empty string or its name).
    // When false, it should be omitted.
    // For any other value, should be present with that value.
    var OVERLOADED_BOOLEAN = 4; // An attribute that must be numeric or parse as a numeric.
    // When falsy, it should be removed.
    var NUMERIC = 5; // An attribute that must be positive numeric or parse as a positive numeric.
    // When falsy, it should be removed.
    var POSITIVE_NUMERIC = 6;
    /* eslint-disable max-len */ var ATTRIBUTE_NAME_START_CHAR = ":A-Z_a-z\\u00C0-\\u00D6\\u00D8-\\u00F6\\u00F8-\\u02FF\\u0370-\\u037D\\u037F-\\u1FFF\\u200C-\\u200D\\u2070-\\u218F\\u2C00-\\u2FEF\\u3001-\\uD7FF\\uF900-\\uFDCF\\uFDF0-\\uFFFD";
    /* eslint-enable max-len */ var ATTRIBUTE_NAME_CHAR = ATTRIBUTE_NAME_START_CHAR + "\\-.0-9\\u00B7\\u0300-\\u036F\\u203F-\\u2040";
    var VALID_ATTRIBUTE_NAME_REGEX = new RegExp('^[' + ATTRIBUTE_NAME_START_CHAR + '][' + ATTRIBUTE_NAME_CHAR + ']*$');
    var illegalAttributeNameCache = {};
    var validatedAttributeNameCache = {};
    function isAttributeNameSafe(attributeName) {
        if (hasOwnProperty.call(validatedAttributeNameCache, attributeName)) return true;
        if (hasOwnProperty.call(illegalAttributeNameCache, attributeName)) return false;
        if (VALID_ATTRIBUTE_NAME_REGEX.test(attributeName)) {
            validatedAttributeNameCache[attributeName] = true;
            return true;
        }
        illegalAttributeNameCache[attributeName] = true;
        error1('Invalid attribute name: `%s`', attributeName);
        return false;
    }
    function shouldIgnoreAttribute(name, propertyInfo, isCustomComponentTag) {
        if (propertyInfo !== null) return propertyInfo.type === RESERVED;
        if (isCustomComponentTag) return false;
        if (name.length > 2 && (name[0] === 'o' || name[0] === 'O') && (name[1] === 'n' || name[1] === 'N')) return true;
        return false;
    }
    function shouldRemoveAttributeWithWarning(name, value, propertyInfo, isCustomComponentTag) {
        if (propertyInfo !== null && propertyInfo.type === RESERVED) return false;
        switch(typeof value){
            case 'function':
            case 'symbol':
                // eslint-disable-line
                return true;
            case 'boolean':
                if (isCustomComponentTag) return false;
                if (propertyInfo !== null) return !propertyInfo.acceptsBooleans;
                else {
                    var prefix = name.toLowerCase().slice(0, 5);
                    return prefix !== 'data-' && prefix !== 'aria-';
                }
            default:
                return false;
        }
    }
    function shouldRemoveAttribute(name, value, propertyInfo, isCustomComponentTag) {
        if (value === null || typeof value === 'undefined') return true;
        if (shouldRemoveAttributeWithWarning(name, value, propertyInfo, isCustomComponentTag)) return true;
        if (isCustomComponentTag) return false;
        if (propertyInfo !== null) switch(propertyInfo.type){
            case BOOLEAN:
                return !value;
            case OVERLOADED_BOOLEAN:
                return value === false;
            case NUMERIC:
                return isNaN(value);
            case POSITIVE_NUMERIC:
                return isNaN(value) || value < 1;
        }
        return false;
    }
    function getPropertyInfo(name) {
        return properties.hasOwnProperty(name) ? properties[name] : null;
    }
    function PropertyInfoRecord(name, type, mustUseProperty, attributeName, attributeNamespace, sanitizeURL, removeEmptyString) {
        this.acceptsBooleans = type === BOOLEANISH_STRING || type === BOOLEAN || type === OVERLOADED_BOOLEAN;
        this.attributeName = attributeName;
        this.attributeNamespace = attributeNamespace;
        this.mustUseProperty = mustUseProperty;
        this.propertyName = name;
        this.type = type;
        this.sanitizeURL = sanitizeURL;
        this.removeEmptyString = removeEmptyString;
    } // When adding attributes to this list, be sure to also add them to
    // the `possibleStandardNames` module to ensure casing and incorrect
    // name warnings.
    var properties = {}; // These props are reserved by React. They shouldn't be written to the DOM.
    var reservedProps = [
        'children',
        'dangerouslySetInnerHTML',
        // elements (not just inputs). Now that ReactDOMInput assigns to the
        // defaultValue property -- do we need this?
        'defaultValue',
        'defaultChecked',
        'innerHTML',
        'suppressContentEditableWarning',
        'suppressHydrationWarning',
        'style'
    ];
    reservedProps.forEach(function(name) {
        properties[name] = new PropertyInfoRecord(name, RESERVED, false, name, null, false, false);
    }); // A few React string attributes have a different name.
    // This is a mapping from React prop names to the attribute names.
    [
        [
            'acceptCharset',
            'accept-charset'
        ],
        [
            'className',
            'class'
        ],
        [
            'htmlFor',
            'for'
        ],
        [
            'httpEquiv',
            'http-equiv'
        ]
    ].forEach(function(_ref) {
        var name = _ref[0], attributeName = _ref[1];
        properties[name] = new PropertyInfoRecord(name, STRING, false, attributeName, null, false, false);
    }); // These are "enumerated" HTML attributes that accept "true" and "false".
    // In React, we let users pass `true` and `false` even though technically
    // these aren't boolean attributes (they are coerced to strings).
    [
        'contentEditable',
        'draggable',
        'spellCheck',
        'value'
    ].forEach(function(name) {
        properties[name] = new PropertyInfoRecord(name, BOOLEANISH_STRING, false, name.toLowerCase(), null, false, false);
    }); // These are "enumerated" SVG attributes that accept "true" and "false".
    // In React, we let users pass `true` and `false` even though technically
    // these aren't boolean attributes (they are coerced to strings).
    // Since these are SVG attributes, their attribute names are case-sensitive.
    [
        'autoReverse',
        'externalResourcesRequired',
        'focusable',
        'preserveAlpha'
    ].forEach(function(name) {
        properties[name] = new PropertyInfoRecord(name, BOOLEANISH_STRING, false, name, null, false, false);
    }); // These are HTML boolean attributes.
    [
        'allowFullScreen',
        'async',
        // on the client side because the browsers are inconsistent. Instead we call focus().
        'autoFocus',
        'autoPlay',
        'controls',
        'default',
        'defer',
        'disabled',
        'disablePictureInPicture',
        'disableRemotePlayback',
        'formNoValidate',
        'hidden',
        'loop',
        'noModule',
        'noValidate',
        'open',
        'playsInline',
        'readOnly',
        'required',
        'reversed',
        'scoped',
        'seamless',
        'itemScope'
    ].forEach(function(name) {
        properties[name] = new PropertyInfoRecord(name, BOOLEAN, false, name.toLowerCase(), null, false, false);
    }); // These are the few React props that we set as DOM properties
    // rather than attributes. These are all booleans.
    [
        'checked',
        // disabled with `removeAttribute`. We have special logic for handling this.
        'multiple',
        'muted',
        'selected' // NOTE: if you add a camelCased prop to this list,
    ].forEach(function(name) {
        properties[name] = new PropertyInfoRecord(name, BOOLEAN, true, name, null, false, false);
    }); // These are HTML attributes that are "overloaded booleans": they behave like
    // booleans, but can also accept a string value.
    [
        'capture',
        'download' // NOTE: if you add a camelCased prop to this list,
    ].forEach(function(name) {
        properties[name] = new PropertyInfoRecord(name, OVERLOADED_BOOLEAN, false, name, null, false, false);
    }); // These are HTML attributes that must be positive numbers.
    [
        'cols',
        'rows',
        'size',
        'span' // NOTE: if you add a camelCased prop to this list,
    ].forEach(function(name) {
        properties[name] = new PropertyInfoRecord(name, POSITIVE_NUMERIC, false, name, null, false, false);
    }); // These are HTML attributes that must be numbers.
    [
        'rowSpan',
        'start'
    ].forEach(function(name) {
        properties[name] = new PropertyInfoRecord(name, NUMERIC, false, name.toLowerCase(), null, false, false);
    });
    var CAMELIZE = /[\-\:]([a-z])/g;
    var capitalize = function(token) {
        return token[1].toUpperCase();
    }; // This is a list of all SVG attributes that need special casing, namespacing,
    // or boolean value assignment. Regular attributes that just accept strings
    // and have the same names are omitted, just like in the HTML attribute filter.
    // Some of these attributes can be hard to find. This list was created by
    // scraping the MDN documentation.
    [
        'accent-height',
        'alignment-baseline',
        'arabic-form',
        'baseline-shift',
        'cap-height',
        'clip-path',
        'clip-rule',
        'color-interpolation',
        'color-interpolation-filters',
        'color-profile',
        'color-rendering',
        'dominant-baseline',
        'enable-background',
        'fill-opacity',
        'fill-rule',
        'flood-color',
        'flood-opacity',
        'font-family',
        'font-size',
        'font-size-adjust',
        'font-stretch',
        'font-style',
        'font-variant',
        'font-weight',
        'glyph-name',
        'glyph-orientation-horizontal',
        'glyph-orientation-vertical',
        'horiz-adv-x',
        'horiz-origin-x',
        'image-rendering',
        'letter-spacing',
        'lighting-color',
        'marker-end',
        'marker-mid',
        'marker-start',
        'overline-position',
        'overline-thickness',
        'paint-order',
        'panose-1',
        'pointer-events',
        'rendering-intent',
        'shape-rendering',
        'stop-color',
        'stop-opacity',
        'strikethrough-position',
        'strikethrough-thickness',
        'stroke-dasharray',
        'stroke-dashoffset',
        'stroke-linecap',
        'stroke-linejoin',
        'stroke-miterlimit',
        'stroke-opacity',
        'stroke-width',
        'text-anchor',
        'text-decoration',
        'text-rendering',
        'underline-position',
        'underline-thickness',
        'unicode-bidi',
        'unicode-range',
        'units-per-em',
        'v-alphabetic',
        'v-hanging',
        'v-ideographic',
        'v-mathematical',
        'vector-effect',
        'vert-adv-y',
        'vert-origin-x',
        'vert-origin-y',
        'word-spacing',
        'writing-mode',
        'xmlns:xlink',
        'x-height' // NOTE: if you add a camelCased prop to this list,
    ].forEach(function(attributeName) {
        var name = attributeName.replace(CAMELIZE, capitalize);
        properties[name] = new PropertyInfoRecord(name, STRING, false, attributeName, null, false, false);
    }); // String SVG attributes with the xlink namespace.
    [
        'xlink:actuate',
        'xlink:arcrole',
        'xlink:role',
        'xlink:show',
        'xlink:title',
        'xlink:type' // NOTE: if you add a camelCased prop to this list,
    ].forEach(function(attributeName) {
        var name = attributeName.replace(CAMELIZE, capitalize);
        properties[name] = new PropertyInfoRecord(name, STRING, false, attributeName, 'http://www.w3.org/1999/xlink', false, false);
    }); // String SVG attributes with the xml namespace.
    [
        'xml:base',
        'xml:lang',
        'xml:space' // NOTE: if you add a camelCased prop to this list,
    ].forEach(function(attributeName) {
        var name = attributeName.replace(CAMELIZE, capitalize);
        properties[name] = new PropertyInfoRecord(name, STRING, false, attributeName, 'http://www.w3.org/XML/1998/namespace', false, false);
    }); // These attribute exists both in HTML and SVG.
    // The attribute name is case-sensitive in SVG so we can't just use
    // the React name like we do for attributes that exist only in HTML.
    [
        'tabIndex',
        'crossOrigin'
    ].forEach(function(attributeName) {
        properties[attributeName] = new PropertyInfoRecord(attributeName, STRING, false, attributeName.toLowerCase(), null, false, false);
    }); // These attributes accept URLs. These must not allow javascript: URLS.
    // These will also need to accept Trusted Types object in the future.
    var xlinkHref = 'xlinkHref';
    properties[xlinkHref] = new PropertyInfoRecord('xlinkHref', STRING, false, 'xlink:href', 'http://www.w3.org/1999/xlink', true, false);
    [
        'src',
        'href',
        'action',
        'formAction'
    ].forEach(function(attributeName) {
        properties[attributeName] = new PropertyInfoRecord(attributeName, STRING, false, attributeName.toLowerCase(), null, true, true);
    });
    // and any newline or tab are filtered out as if they're not part of the URL.
    // https://url.spec.whatwg.org/#url-parsing
    // Tab or newline are defined as \r\n\t:
    // https://infra.spec.whatwg.org/#ascii-tab-or-newline
    // A C0 control is a code point in the range \u0000 NULL to \u001F
    // INFORMATION SEPARATOR ONE, inclusive:
    // https://infra.spec.whatwg.org/#c0-control-or-space
    /* eslint-disable max-len */ var isJavaScriptProtocol = /^[\u0000-\u001F ]*j[\r\n\t]*a[\r\n\t]*v[\r\n\t]*a[\r\n\t]*s[\r\n\t]*c[\r\n\t]*r[\r\n\t]*i[\r\n\t]*p[\r\n\t]*t[\r\n\t]*\:/i;
    var didWarn = false;
    function sanitizeURL1(url) {
        if (!didWarn && isJavaScriptProtocol.test(url)) {
            didWarn = true;
            error1("A future version of React will block javascript: URLs as a security precaution. Use event handlers instead if you can. If you need to generate unsafe HTML try using dangerouslySetInnerHTML instead. React was passed %s.", JSON.stringify(url));
        }
    }
    /**
 * Get the value for a property on a node. Only used in DEV for SSR validation.
 * The "expected" argument is used as a hint of what the expected value is.
 * Some properties have multiple equivalent values.
 */ function getValueForProperty(node, name, expected, propertyInfo) {
        if (propertyInfo.mustUseProperty) {
            var propertyName = propertyInfo.propertyName;
            return node[propertyName];
        } else {
            checkAttributeStringCoercion(expected, name);
            if (propertyInfo.sanitizeURL) // If we haven't fully disabled javascript: URLs, and if
            // the hydration is successful of a javascript: URL, we
            // still want to warn on the client.
            // eslint-disable-next-line react-internal/safe-string-coercion
            sanitizeURL1('' + expected);
            var attributeName = propertyInfo.attributeName;
            var stringValue = null;
            if (propertyInfo.type === OVERLOADED_BOOLEAN) {
                if (node.hasAttribute(attributeName)) {
                    var value = node.getAttribute(attributeName);
                    if (value === '') return true;
                    if (shouldRemoveAttribute(name, expected, propertyInfo, false)) return value;
                     // eslint-disable-next-line react-internal/safe-string-coercion
                    if (value === '' + expected) return expected;
                    return value;
                }
            } else if (node.hasAttribute(attributeName)) {
                if (shouldRemoveAttribute(name, expected, propertyInfo, false)) // We had an attribute but shouldn't have had one, so read it
                // for the error message.
                return node.getAttribute(attributeName);
                if (propertyInfo.type === BOOLEAN) // If this was a boolean, it doesn't matter what the value is
                // the fact that we have it is the same as the expected.
                return expected;
                 // Even if this property uses a namespace we use getAttribute
                // because we assume its namespaced name is the same as our config.
                // To use getAttributeNS we need the local name which we don't have
                // in our config atm.
                stringValue = node.getAttribute(attributeName);
            }
            if (shouldRemoveAttribute(name, expected, propertyInfo, false)) return stringValue === null ? expected : stringValue; // eslint-disable-next-line react-internal/safe-string-coercion
            else if (stringValue === '' + expected) return expected;
            else return stringValue;
        }
    }
    /**
 * Get the value for a attribute on a node. Only used in DEV for SSR validation.
 * The third argument is used as a hint of what the expected value is. Some
 * attributes have multiple equivalent values.
 */ function getValueForAttribute(node, name, expected) {
        if (!isAttributeNameSafe(name)) return;
        if (!node.hasAttribute(name)) return expected === undefined ? undefined : null;
        var value = node.getAttribute(name);
        checkAttributeStringCoercion(expected, name);
        if (value === '' + expected) return expected;
        return value;
    }
    /**
 * Sets the value for a property on a node.
 *
 * @param {DOMElement} node
 * @param {string} name
 * @param {*} value
 */ function setValueForProperty(node, name, value, isCustomComponentTag) {
        var propertyInfo = getPropertyInfo(name);
        if (shouldIgnoreAttribute(name, propertyInfo, isCustomComponentTag)) return;
        if (shouldRemoveAttribute(name, value, propertyInfo, isCustomComponentTag)) value = null;
         // If the prop isn't in the special list, treat it as a simple attribute.
        if (isCustomComponentTag || propertyInfo === null) {
            if (isAttributeNameSafe(name)) {
                var _attributeName = name;
                if (value === null) node.removeAttribute(_attributeName);
                else {
                    checkAttributeStringCoercion(value, name);
                    node.setAttribute(_attributeName, '' + value);
                }
            }
            return;
        }
        var mustUseProperty = propertyInfo.mustUseProperty;
        if (mustUseProperty) {
            var propertyName = propertyInfo.propertyName;
            if (value === null) {
                var type = propertyInfo.type;
                node[propertyName] = type === BOOLEAN ? false : '';
            } else // Contrary to `setAttribute`, object properties are properly
            // `toString`ed by IE8/9.
            node[propertyName] = value;
            return;
        } // The rest are treated as attributes with special cases.
        var attributeName = propertyInfo.attributeName, attributeNamespace = propertyInfo.attributeNamespace;
        if (value === null) node.removeAttribute(attributeName);
        else {
            var _type = propertyInfo.type;
            var attributeValue;
            if (_type === BOOLEAN || _type === OVERLOADED_BOOLEAN && value === true) // If attribute type is boolean, we know for sure it won't be an execution sink
            // and we won't require Trusted Type here.
            attributeValue = '';
            else {
                checkAttributeStringCoercion(value, attributeName);
                attributeValue = '' + value;
                if (propertyInfo.sanitizeURL) sanitizeURL1(attributeValue.toString());
            }
            if (attributeNamespace) node.setAttributeNS(attributeNamespace, attributeName, attributeValue);
            else node.setAttribute(attributeName, attributeValue);
        }
    }
    // ATTENTION
    var REACT_ELEMENT_TYPE = Symbol.for('react.element');
    var REACT_PORTAL_TYPE = Symbol.for('react.portal');
    var REACT_FRAGMENT_TYPE = Symbol.for('react.fragment');
    var REACT_STRICT_MODE_TYPE = Symbol.for('react.strict_mode');
    var REACT_PROFILER_TYPE = Symbol.for('react.profiler');
    var REACT_PROVIDER_TYPE = Symbol.for('react.provider');
    var REACT_CONTEXT_TYPE = Symbol.for('react.context');
    var REACT_FORWARD_REF_TYPE = Symbol.for('react.forward_ref');
    var REACT_SUSPENSE_TYPE = Symbol.for('react.suspense');
    var REACT_SUSPENSE_LIST_TYPE = Symbol.for('react.suspense_list');
    var REACT_MEMO_TYPE = Symbol.for('react.memo');
    var REACT_LAZY_TYPE = Symbol.for('react.lazy');
    var REACT_SCOPE_TYPE = Symbol.for('react.scope');
    var REACT_DEBUG_TRACING_MODE_TYPE = Symbol.for('react.debug_trace_mode');
    var REACT_OFFSCREEN_TYPE = Symbol.for('react.offscreen');
    var REACT_LEGACY_HIDDEN_TYPE = Symbol.for('react.legacy_hidden');
    var REACT_CACHE_TYPE = Symbol.for('react.cache');
    var REACT_TRACING_MARKER_TYPE = Symbol.for('react.tracing_marker');
    var MAYBE_ITERATOR_SYMBOL = Symbol.iterator;
    var FAUX_ITERATOR_SYMBOL = '@@iterator';
    function getIteratorFn(maybeIterable) {
        if (maybeIterable === null || typeof maybeIterable !== 'object') return null;
        var maybeIterator = MAYBE_ITERATOR_SYMBOL && maybeIterable[MAYBE_ITERATOR_SYMBOL] || maybeIterable[FAUX_ITERATOR_SYMBOL];
        if (typeof maybeIterator === 'function') return maybeIterator;
        return null;
    }
    var assign = Object.assign;
    // Helpers to patch console.logs to avoid logging during side-effect free
    // replaying on render function. This currently only patches the object
    // lazily which won't cover if the log function was extracted eagerly.
    // We could also eagerly patch the method.
    var disabledDepth = 0;
    var prevLog;
    var prevInfo;
    var prevWarn;
    var prevError;
    var prevGroup;
    var prevGroupCollapsed;
    var prevGroupEnd;
    function disabledLog() {}
    disabledLog.__reactDisabledLog = true;
    function disableLogs() {
        if (disabledDepth === 0) {
            /* eslint-disable react-internal/no-production-logging */ prevLog = console.log;
            prevInfo = console.info;
            prevWarn = console.warn;
            prevError = console.error;
            prevGroup = console.group;
            prevGroupCollapsed = console.groupCollapsed;
            prevGroupEnd = console.groupEnd; // https://github.com/facebook/react/issues/19099
            var props = {
                configurable: true,
                enumerable: true,
                value: disabledLog,
                writable: true
            }; // $FlowFixMe Flow thinks console is immutable.
            Object.defineProperties(console, {
                info: props,
                log: props,
                warn: props,
                error: props,
                group: props,
                groupCollapsed: props,
                groupEnd: props
            });
        /* eslint-enable react-internal/no-production-logging */ }
        disabledDepth++;
    }
    function reenableLogs() {
        disabledDepth--;
        if (disabledDepth === 0) {
            /* eslint-disable react-internal/no-production-logging */ var props = {
                configurable: true,
                enumerable: true,
                writable: true
            }; // $FlowFixMe Flow thinks console is immutable.
            Object.defineProperties(console, {
                log: assign({}, props, {
                    value: prevLog
                }),
                info: assign({}, props, {
                    value: prevInfo
                }),
                warn: assign({}, props, {
                    value: prevWarn
                }),
                error: assign({}, props, {
                    value: prevError
                }),
                group: assign({}, props, {
                    value: prevGroup
                }),
                groupCollapsed: assign({}, props, {
                    value: prevGroupCollapsed
                }),
                groupEnd: assign({}, props, {
                    value: prevGroupEnd
                })
            });
        /* eslint-enable react-internal/no-production-logging */ }
        if (disabledDepth < 0) error1("disabledDepth fell below zero. This is a bug in React. Please file an issue.");
    }
    var ReactCurrentDispatcher1 = ReactSharedInternals.ReactCurrentDispatcher;
    var prefix1;
    function describeBuiltInComponentFrame(name, source, ownerFn) {
        if (prefix1 === undefined) // Extract the VM specific prefix used by each line.
        try {
            throw Error();
        } catch (x) {
            var match = x.stack.trim().match(/\n( *(at )?)/);
            prefix1 = match && match[1] || '';
        }
         // We use the prefix to ensure our stacks line up with native stack frames.
        return '\n' + prefix1 + name;
    }
    var reentry = false;
    var componentFrameCache;
    var PossiblyWeakMap = typeof WeakMap === 'function' ? WeakMap : Map;
    componentFrameCache = new PossiblyWeakMap();
    function describeNativeComponentFrame(fn, construct) {
        // If something asked for a stack inside a fake render, it should get ignored.
        if (!fn || reentry) return '';
        var frame = componentFrameCache.get(fn);
        if (frame !== undefined) return frame;
        var control;
        reentry = true;
        var previousPrepareStackTrace = Error.prepareStackTrace; // $FlowFixMe It does accept undefined.
        Error.prepareStackTrace = undefined;
        var previousDispatcher;
        previousDispatcher = ReactCurrentDispatcher1.current; // Set the dispatcher in DEV because this might be call in the render function
        // for warnings.
        ReactCurrentDispatcher1.current = null;
        disableLogs();
        try {
            // This should throw.
            if (construct) {
                // Something should be setting the props in the constructor.
                var Fake = function() {
                    throw Error();
                }; // $FlowFixMe
                Object.defineProperty(Fake.prototype, 'props', {
                    set: function() {
                        // We use a throwing setter instead of frozen or non-writable props
                        // because that won't throw in a non-strict mode function.
                        throw Error();
                    }
                });
                if (typeof Reflect === 'object' && Reflect.construct) {
                    // We construct a different control for this case to include any extra
                    // frames added by the construct call.
                    try {
                        Reflect.construct(Fake, []);
                    } catch (x) {
                        control = x;
                    }
                    Reflect.construct(fn, [], Fake);
                } else {
                    try {
                        Fake.call();
                    } catch (x) {
                        control = x;
                    }
                    fn.call(Fake.prototype);
                }
            } else {
                try {
                    throw Error();
                } catch (x) {
                    control = x;
                }
                fn();
            }
        } catch (sample) {
            // This is inlined manually because closure doesn't do it for us.
            if (sample && control && typeof sample.stack === 'string') {
                // This extracts the first frame from the sample that isn't also in the control.
                // Skipping one frame that we assume is the frame that calls the two.
                var sampleLines = sample.stack.split('\n');
                var controlLines = control.stack.split('\n');
                var s = sampleLines.length - 1;
                var c = controlLines.length - 1;
                while(s >= 1 && c >= 0 && sampleLines[s] !== controlLines[c])// We expect at least one stack frame to be shared.
                // Typically this will be the root most one. However, stack frames may be
                // cut off due to maximum stack limits. In this case, one maybe cut off
                // earlier than the other. We assume that the sample is longer or the same
                // and there for cut off earlier. So we should find the root most frame in
                // the sample somewhere in the control.
                c--;
                for(; s >= 1 && c >= 0; s--, c--)// Next we find the first one that isn't the same which should be the
                // frame that called our sample function and the control.
                if (sampleLines[s] !== controlLines[c]) {
                    // In V8, the first line is describing the message but other VMs don't.
                    // If we're about to return the first line, and the control is also on the same
                    // line, that's a pretty good indicator that our sample threw at same line as
                    // the control. I.e. before we entered the sample frame. So we ignore this result.
                    // This can happen if you passed a class to function component, or non-function.
                    if (s !== 1 || c !== 1) do {
                        s--;
                        c--; // We may still have similar intermediate frames from the construct call.
                        // The next one that isn't the same should be our match though.
                        if (c < 0 || sampleLines[s] !== controlLines[c]) {
                            // V8 adds a "new" prefix for native classes. Let's remove it to make it prettier.
                            var _frame = '\n' + sampleLines[s].replace(' at new ', ' at '); // If our component frame is labeled "<anonymous>"
                            // but we have a user-provided "displayName"
                            // splice it in to make the stack more readable.
                            if (fn.displayName && _frame.includes('<anonymous>')) _frame = _frame.replace('<anonymous>', fn.displayName);
                            if (typeof fn === 'function') componentFrameCache.set(fn, _frame);
                            return _frame;
                        }
                    }while (s >= 1 && c >= 0)
                    break;
                }
            }
        } finally{
            reentry = false;
            ReactCurrentDispatcher1.current = previousDispatcher;
            reenableLogs();
            Error.prepareStackTrace = previousPrepareStackTrace;
        } // Fallback to just using the name if we couldn't make it throw.
        var name = fn ? fn.displayName || fn.name : '';
        var syntheticFrame = name ? describeBuiltInComponentFrame(name) : '';
        if (typeof fn === 'function') componentFrameCache.set(fn, syntheticFrame);
        return syntheticFrame;
    }
    function describeClassComponentFrame(ctor, source, ownerFn) {
        return describeNativeComponentFrame(ctor, true);
    }
    function describeFunctionComponentFrame(fn, source, ownerFn) {
        return describeNativeComponentFrame(fn, false);
    }
    function shouldConstruct(Component) {
        var prototype = Component.prototype;
        return !!(prototype && prototype.isReactComponent);
    }
    function describeUnknownElementTypeFrameInDEV(type, source, ownerFn) {
        if (type == null) return '';
        if (typeof type === 'function') return describeNativeComponentFrame(type, shouldConstruct(type));
        if (typeof type === 'string') return describeBuiltInComponentFrame(type);
        switch(type){
            case REACT_SUSPENSE_TYPE:
                return describeBuiltInComponentFrame('Suspense');
            case REACT_SUSPENSE_LIST_TYPE:
                return describeBuiltInComponentFrame('SuspenseList');
        }
        if (typeof type === 'object') switch(type.$$typeof){
            case REACT_FORWARD_REF_TYPE:
                return describeFunctionComponentFrame(type.render);
            case REACT_MEMO_TYPE:
                // Memo may contain any component type so we recursively resolve it.
                return describeUnknownElementTypeFrameInDEV(type.type, source, ownerFn);
            case REACT_LAZY_TYPE:
                var lazyComponent = type;
                var payload = lazyComponent._payload;
                var init = lazyComponent._init;
                try {
                    // Lazy may contain any component type so we recursively resolve it.
                    return describeUnknownElementTypeFrameInDEV(init(payload), source, ownerFn);
                } catch (x) {}
        }
        return '';
    }
    function describeFiber(fiber) {
        var owner = fiber._debugOwner ? fiber._debugOwner.type : null;
        var source = fiber._debugSource;
        switch(fiber.tag){
            case HostComponent:
                return describeBuiltInComponentFrame(fiber.type);
            case LazyComponent:
                return describeBuiltInComponentFrame('Lazy');
            case SuspenseComponent:
                return describeBuiltInComponentFrame('Suspense');
            case SuspenseListComponent:
                return describeBuiltInComponentFrame('SuspenseList');
            case FunctionComponent:
            case IndeterminateComponent:
            case SimpleMemoComponent:
                return describeFunctionComponentFrame(fiber.type);
            case ForwardRef:
                return describeFunctionComponentFrame(fiber.type.render);
            case ClassComponent:
                return describeClassComponentFrame(fiber.type);
            default:
                return '';
        }
    }
    function getStackByFiberInDevAndProd(workInProgress) {
        try {
            var info = '';
            var node = workInProgress;
            do {
                info += describeFiber(node);
                node = node.return;
            }while (node)
            return info;
        } catch (x) {
            return '\nError generating stack: ' + x.message + '\n' + x.stack;
        }
    }
    function getWrappedName(outerType, innerType, wrapperName) {
        var displayName = outerType.displayName;
        if (displayName) return displayName;
        var functionName = innerType.displayName || innerType.name || '';
        return functionName !== '' ? wrapperName + "(" + functionName + ")" : wrapperName;
    } // Keep in sync with react-reconciler/getComponentNameFromFiber
    function getContextName(type) {
        return type.displayName || 'Context';
    } // Note that the reconciler package should generally prefer to use getComponentNameFromFiber() instead.
    function getComponentNameFromType(type) {
        if (type == null) // Host root, text node or just invalid type.
        return null;
        if (typeof type.tag === 'number') error1("Received an unexpected object in getComponentNameFromType(). This is likely a bug in React. Please file an issue.");
        if (typeof type === 'function') return type.displayName || type.name || null;
        if (typeof type === 'string') return type;
        switch(type){
            case REACT_FRAGMENT_TYPE:
                return 'Fragment';
            case REACT_PORTAL_TYPE:
                return 'Portal';
            case REACT_PROFILER_TYPE:
                return 'Profiler';
            case REACT_STRICT_MODE_TYPE:
                return 'StrictMode';
            case REACT_SUSPENSE_TYPE:
                return 'Suspense';
            case REACT_SUSPENSE_LIST_TYPE:
                return 'SuspenseList';
        }
        if (typeof type === 'object') switch(type.$$typeof){
            case REACT_CONTEXT_TYPE:
                var context = type;
                return getContextName(context) + '.Consumer';
            case REACT_PROVIDER_TYPE:
                var provider = type;
                return getContextName(provider._context) + '.Provider';
            case REACT_FORWARD_REF_TYPE:
                return getWrappedName(type, type.render, 'ForwardRef');
            case REACT_MEMO_TYPE:
                var outerName = type.displayName || null;
                if (outerName !== null) return outerName;
                return getComponentNameFromType(type.type) || 'Memo';
            case REACT_LAZY_TYPE:
                var lazyComponent = type;
                var payload = lazyComponent._payload;
                var init = lazyComponent._init;
                try {
                    return getComponentNameFromType(init(payload));
                } catch (x) {
                    return null;
                }
        }
        return null;
    }
    function getWrappedName$1(outerType, innerType, wrapperName) {
        var functionName = innerType.displayName || innerType.name || '';
        return outerType.displayName || (functionName !== '' ? wrapperName + "(" + functionName + ")" : wrapperName);
    } // Keep in sync with shared/getComponentNameFromType
    function getContextName$1(type) {
        return type.displayName || 'Context';
    }
    function getComponentNameFromFiber(fiber) {
        var tag = fiber.tag, type = fiber.type;
        switch(tag){
            case CacheComponent:
                return 'Cache';
            case ContextConsumer:
                var context = type;
                return getContextName$1(context) + '.Consumer';
            case ContextProvider:
                var provider = type;
                return getContextName$1(provider._context) + '.Provider';
            case DehydratedFragment:
                return 'DehydratedFragment';
            case ForwardRef:
                return getWrappedName$1(type, type.render, 'ForwardRef');
            case Fragment:
                return 'Fragment';
            case HostComponent:
                // Host component type is the display name (e.g. "div", "View")
                return type;
            case HostPortal:
                return 'Portal';
            case HostRoot:
                return 'Root';
            case HostText:
                return 'Text';
            case LazyComponent:
                // Name comes from the type in this case; we don't have a tag.
                return getComponentNameFromType(type);
            case Mode:
                if (type === REACT_STRICT_MODE_TYPE) // Don't be less specific than shared/getComponentNameFromType
                return 'StrictMode';
                return 'Mode';
            case OffscreenComponent:
                return 'Offscreen';
            case Profiler:
                return 'Profiler';
            case ScopeComponent:
                return 'Scope';
            case SuspenseComponent:
                return 'Suspense';
            case SuspenseListComponent:
                return 'SuspenseList';
            case TracingMarkerComponent:
                return 'TracingMarker';
            // The display name for this tags come from the user-provided type:
            case ClassComponent:
            case FunctionComponent:
            case IncompleteClassComponent:
            case IndeterminateComponent:
            case MemoComponent:
            case SimpleMemoComponent:
                if (typeof type === 'function') return type.displayName || type.name || null;
                if (typeof type === 'string') return type;
                break;
        }
        return null;
    }
    var ReactDebugCurrentFrame1 = ReactSharedInternals.ReactDebugCurrentFrame;
    var current1 = null;
    var isRendering = false;
    function getCurrentFiberOwnerNameInDevOrNull() {
        if (current1 === null) return null;
        var owner = current1._debugOwner;
        if (owner !== null && typeof owner !== 'undefined') return getComponentNameFromFiber(owner);
        return null;
    }
    function getCurrentFiberStackInDev() {
        if (current1 === null) return '';
         // Safe because if current fiber exists, we are reconciling,
        // and it is guaranteed to be the work-in-progress version.
        return getStackByFiberInDevAndProd(current1);
    }
    function resetCurrentFiber() {
        ReactDebugCurrentFrame1.getCurrentStack = null;
        current1 = null;
        isRendering = false;
    }
    function setCurrentFiber(fiber) {
        ReactDebugCurrentFrame1.getCurrentStack = fiber === null ? null : getCurrentFiberStackInDev;
        current1 = fiber;
        isRendering = false;
    }
    function getCurrentFiber() {
        return current1;
    }
    function setIsRendering(rendering) {
        isRendering = rendering;
    }
    // Flow does not allow string concatenation of most non-string types. To work
    // around this limitation, we use an opaque type that can only be obtained by
    // passing the value through getToStringValue first.
    function toString(value) {
        // The coercion safety check is performed in getToStringValue().
        // eslint-disable-next-line react-internal/safe-string-coercion
        return '' + value;
    }
    function getToStringValue(value) {
        switch(typeof value){
            case 'boolean':
            case 'number':
            case 'string':
            case 'undefined':
                return value;
            case 'object':
                checkFormFieldValueStringCoercion(value);
                return value;
            default:
                // function, symbol are assigned as empty strings
                return '';
        }
    }
    var hasReadOnlyValue = {
        button: true,
        checkbox: true,
        image: true,
        hidden: true,
        radio: true,
        reset: true,
        submit: true
    };
    function checkControlledValueProps(tagName, props) {
        if (!(hasReadOnlyValue[props.type] || props.onChange || props.onInput || props.readOnly || props.disabled || props.value == null)) error1("You provided a `value` prop to a form field without an `onChange` handler. This will render a read-only field. If the field should be mutable use `defaultValue`. Otherwise, set either `onChange` or `readOnly`.");
        if (!(props.onChange || props.readOnly || props.disabled || props.checked == null)) error1("You provided a `checked` prop to a form field without an `onChange` handler. This will render a read-only field. If the field should be mutable use `defaultChecked`. Otherwise, set either `onChange` or `readOnly`.");
    }
    function isCheckable(elem) {
        var type = elem.type;
        var nodeName = elem.nodeName;
        return nodeName && nodeName.toLowerCase() === 'input' && (type === 'checkbox' || type === 'radio');
    }
    function getTracker(node) {
        return node._valueTracker;
    }
    function detachTracker(node) {
        node._valueTracker = null;
    }
    function getValueFromNode(node) {
        var value = '';
        if (!node) return value;
        if (isCheckable(node)) value = node.checked ? 'true' : 'false';
        else value = node.value;
        return value;
    }
    function trackValueOnNode(node) {
        var valueField = isCheckable(node) ? 'checked' : 'value';
        var descriptor = Object.getOwnPropertyDescriptor(node.constructor.prototype, valueField);
        checkFormFieldValueStringCoercion(node[valueField]);
        var currentValue = '' + node[valueField]; // if someone has already defined a value or Safari, then bail
        // and don't track value will cause over reporting of changes,
        // but it's better then a hard failure
        // (needed for certain tests that spyOn input values and Safari)
        if (node.hasOwnProperty(valueField) || typeof descriptor === 'undefined' || typeof descriptor.get !== 'function' || typeof descriptor.set !== 'function') return;
        var get = descriptor.get, set = descriptor.set;
        Object.defineProperty(node, valueField, {
            configurable: true,
            get: function() {
                return get.call(this);
            },
            set: function(value) {
                checkFormFieldValueStringCoercion(value);
                currentValue = '' + value;
                set.call(this, value);
            }
        }); // We could've passed this the first time
        // but it triggers a bug in IE11 and Edge 14/15.
        // Calling defineProperty() again should be equivalent.
        // https://github.com/facebook/react/issues/11768
        Object.defineProperty(node, valueField, {
            enumerable: descriptor.enumerable
        });
        var tracker = {
            getValue: function() {
                return currentValue;
            },
            setValue: function(value) {
                checkFormFieldValueStringCoercion(value);
                currentValue = '' + value;
            },
            stopTracking: function() {
                detachTracker(node);
                delete node[valueField];
            }
        };
        return tracker;
    }
    function track(node) {
        if (getTracker(node)) return;
         // TODO: Once it's just Fiber we can move this to node._wrapperState
        node._valueTracker = trackValueOnNode(node);
    }
    function updateValueIfChanged(node) {
        if (!node) return false;
        var tracker = getTracker(node); // if there is no tracker at this point it's unlikely
        // that trying again will succeed
        if (!tracker) return true;
        var lastValue = tracker.getValue();
        var nextValue = getValueFromNode(node);
        if (nextValue !== lastValue) {
            tracker.setValue(nextValue);
            return true;
        }
        return false;
    }
    function getActiveElement(doc) {
        doc = doc || (typeof document !== 'undefined' ? document : undefined);
        if (typeof doc === 'undefined') return null;
        try {
            return doc.activeElement || doc.body;
        } catch (e) {
            return doc.body;
        }
    }
    var didWarnValueDefaultValue = false;
    var didWarnCheckedDefaultChecked = false;
    var didWarnControlledToUncontrolled = false;
    var didWarnUncontrolledToControlled = false;
    function isControlled(props) {
        var usesChecked = props.type === 'checkbox' || props.type === 'radio';
        return usesChecked ? props.checked != null : props.value != null;
    }
    /**
 * Implements an <input> host component that allows setting these optional
 * props: `checked`, `value`, `defaultChecked`, and `defaultValue`.
 *
 * If `checked` or `value` are not supplied (or null/undefined), user actions
 * that affect the checked state or value will trigger updates to the element.
 *
 * If they are supplied (and not null/undefined), the rendered element will not
 * trigger updates to the element. Instead, the props must change in order for
 * the rendered element to be updated.
 *
 * The rendered element will be initialized as unchecked (or `defaultChecked`)
 * with an empty value (or `defaultValue`).
 *
 * See http://www.w3.org/TR/2012/WD-html5-20121025/the-input-element.html
 */ function getHostProps(element, props) {
        var node = element;
        var checked = props.checked;
        var hostProps = assign({}, props, {
            defaultChecked: undefined,
            defaultValue: undefined,
            value: undefined,
            checked: checked != null ? checked : node._wrapperState.initialChecked
        });
        return hostProps;
    }
    function initWrapperState(element, props) {
        checkControlledValueProps('input', props);
        if (props.checked !== undefined && props.defaultChecked !== undefined && !didWarnCheckedDefaultChecked) {
            error1("%s contains an input of type %s with both checked and defaultChecked props. Input elements must be either controlled or uncontrolled (specify either the checked prop, or the defaultChecked prop, but not both). Decide between using a controlled or uncontrolled input element and remove one of these props. More info: https://reactjs.org/link/controlled-components", getCurrentFiberOwnerNameInDevOrNull() || 'A component', props.type);
            didWarnCheckedDefaultChecked = true;
        }
        if (props.value !== undefined && props.defaultValue !== undefined && !didWarnValueDefaultValue) {
            error1("%s contains an input of type %s with both value and defaultValue props. Input elements must be either controlled or uncontrolled (specify either the value prop, or the defaultValue prop, but not both). Decide between using a controlled or uncontrolled input element and remove one of these props. More info: https://reactjs.org/link/controlled-components", getCurrentFiberOwnerNameInDevOrNull() || 'A component', props.type);
            didWarnValueDefaultValue = true;
        }
        var node = element;
        var defaultValue = props.defaultValue == null ? '' : props.defaultValue;
        node._wrapperState = {
            initialChecked: props.checked != null ? props.checked : props.defaultChecked,
            initialValue: getToStringValue(props.value != null ? props.value : defaultValue),
            controlled: isControlled(props)
        };
    }
    function updateChecked(element, props) {
        var node = element;
        var checked = props.checked;
        if (checked != null) setValueForProperty(node, 'checked', checked, false);
    }
    function updateWrapper(element, props) {
        var node = element;
        var controlled = isControlled(props);
        if (!node._wrapperState.controlled && controlled && !didWarnUncontrolledToControlled) {
            error1("A component is changing an uncontrolled input to be controlled. This is likely caused by the value changing from undefined to a defined value, which should not happen. Decide between using a controlled or uncontrolled input element for the lifetime of the component. More info: https://reactjs.org/link/controlled-components");
            didWarnUncontrolledToControlled = true;
        }
        if (node._wrapperState.controlled && !controlled && !didWarnControlledToUncontrolled) {
            error1("A component is changing a controlled input to be uncontrolled. This is likely caused by the value changing from a defined to undefined, which should not happen. Decide between using a controlled or uncontrolled input element for the lifetime of the component. More info: https://reactjs.org/link/controlled-components");
            didWarnControlledToUncontrolled = true;
        }
        updateChecked(element, props);
        var value = getToStringValue(props.value);
        var type = props.type;
        if (value != null) {
            if (type === 'number') {
                if (value === 0 && node.value === '' || // eslint-disable-next-line
                node.value != value) node.value = toString(value);
            } else if (node.value !== toString(value)) node.value = toString(value);
        } else if (type === 'submit' || type === 'reset') {
            // Submit/reset inputs need the attribute removed completely to avoid
            // blank-text buttons.
            node.removeAttribute('value');
            return;
        }
        // When syncing the value attribute, the value comes from a cascade of
        // properties:
        //  1. The value React property
        //  2. The defaultValue React property
        //  3. Otherwise there should be no change
        if (props.hasOwnProperty('value')) setDefaultValue(node, props.type, value);
        else if (props.hasOwnProperty('defaultValue')) setDefaultValue(node, props.type, getToStringValue(props.defaultValue));
        // When syncing the checked attribute, it only changes when it needs
        // to be removed, such as transitioning from a checkbox into a text input
        if (props.checked == null && props.defaultChecked != null) node.defaultChecked = !!props.defaultChecked;
    }
    function postMountWrapper(element, props, isHydrating) {
        var node = element; // Do not assign value if it is already set. This prevents user text input
        // from being lost during SSR hydration.
        if (props.hasOwnProperty('value') || props.hasOwnProperty('defaultValue')) {
            var type = props.type;
            var isButton = type === 'submit' || type === 'reset'; // Avoid setting value attribute on submit/reset inputs as it overrides the
            // default value provided by the browser. See: #12872
            if (isButton && (props.value === undefined || props.value === null)) return;
            var initialValue = toString(node._wrapperState.initialValue); // Do not assign value if it is already set. This prevents user text input
            // from being lost during SSR hydration.
            if (!isHydrating) // When syncing the value attribute, the value property should use
            // the wrapperState._initialValue property. This uses:
            //
            //   1. The value React property when present
            //   2. The defaultValue React property when present
            //   3. An empty string
            {
                if (initialValue !== node.value) node.value = initialValue;
            }
            // Otherwise, the value attribute is synchronized to the property,
            // so we assign defaultValue to the same thing as the value property
            // assignment step above.
            node.defaultValue = initialValue;
        } // Normally, we'd just do `node.checked = node.checked` upon initial mount, less this bug
        // this is needed to work around a chrome bug where setting defaultChecked
        // will sometimes influence the value of checked (even after detachment).
        // Reference: https://bugs.chromium.org/p/chromium/issues/detail?id=608416
        // We need to temporarily unset name to avoid disrupting radio button groups.
        var name = node.name;
        if (name !== '') node.name = '';
        // When syncing the checked attribute, both the checked property and
        // attribute are assigned at the same time using defaultChecked. This uses:
        //
        //   1. The checked React property when present
        //   2. The defaultChecked React property when present
        //   3. Otherwise, false
        node.defaultChecked = !node.defaultChecked;
        node.defaultChecked = !!node._wrapperState.initialChecked;
        if (name !== '') node.name = name;
    }
    function restoreControlledState(element, props) {
        var node = element;
        updateWrapper(node, props);
        updateNamedCousins(node, props);
    }
    function updateNamedCousins(rootNode, props) {
        var name = props.name;
        if (props.type === 'radio' && name != null) {
            var queryRoot = rootNode;
            while(queryRoot.parentNode)queryRoot = queryRoot.parentNode;
             // If `rootNode.form` was non-null, then we could try `form.elements`,
            checkAttributeStringCoercion(name, 'name');
            var group = queryRoot.querySelectorAll('input[name=' + JSON.stringify('' + name) + '][type="radio"]');
            for(var i = 0; i < group.length; i++){
                var otherNode = group[i];
                if (otherNode === rootNode || otherNode.form !== rootNode.form) continue;
                 // This will throw if radio buttons rendered by different copies of React
                // and the same name are rendered into the same form (same as #1939).
                // That's probably okay; we don't support it just as we don't support
                // mixing React radio buttons with non-React ones.
                var otherProps = getFiberCurrentPropsFromNode(otherNode);
                if (!otherProps) throw new Error("ReactDOMInput: Mixing React and non-React radio inputs with the same `name` is not supported.");
                 // We need update the tracked value on the named cousin since the value
                // was changed but the input saw no event or value set
                updateValueIfChanged(otherNode); // If this is a controlled radio button group, forcing the input that
                // was previously checked to update will cause it to be come re-checked
                // as appropriate.
                updateWrapper(otherNode, otherProps);
            }
        }
    } // In Chrome, assigning defaultValue to certain input types triggers input validation.
    // For number inputs, the display value loses trailing decimal points. For email inputs,
    // Chrome raises "The specified value <x> is not a valid email address".
    //
    // Here we check to see if the defaultValue has actually changed, avoiding these problems
    // when the user is inputting text
    //
    // https://github.com/facebook/react/issues/7253
    function setDefaultValue(node, type, value) {
        if (type !== 'number' || getActiveElement(node.ownerDocument) !== node) {
            if (value == null) node.defaultValue = toString(node._wrapperState.initialValue);
            else if (node.defaultValue !== toString(value)) node.defaultValue = toString(value);
        }
    }
    var didWarnSelectedSetOnOption = false;
    var didWarnInvalidChild = false;
    var didWarnInvalidInnerHTML = false;
    /**
 * Implements an <option> host component that warns when `selected` is set.
 */ function validateProps(element, props) {
        // If a value is not provided, then the children must be simple.
        if (props.value == null) {
            if (typeof props.children === 'object' && props.children !== null) React.Children.forEach(props.children, function(child) {
                if (child == null) return;
                if (typeof child === 'string' || typeof child === 'number') return;
                if (!didWarnInvalidChild) {
                    didWarnInvalidChild = true;
                    error1("Cannot infer the option value of complex children. Pass a `value` prop or use a plain string as children to <option>.");
                }
            });
            else if (props.dangerouslySetInnerHTML != null) {
                if (!didWarnInvalidInnerHTML) {
                    didWarnInvalidInnerHTML = true;
                    error1("Pass a `value` prop if you set dangerouslyInnerHTML so React knows which value should be selected.");
                }
            }
        } // TODO: Remove support for `selected` in <option>.
        if (props.selected != null && !didWarnSelectedSetOnOption) {
            error1("Use the `defaultValue` or `value` props on <select> instead of setting `selected` on <option>.");
            didWarnSelectedSetOnOption = true;
        }
    }
    function postMountWrapper$1(element, props) {
        // value="" should make a value attribute (#6219)
        if (props.value != null) element.setAttribute('value', toString(getToStringValue(props.value)));
    }
    var isArrayImpl = Array.isArray; // eslint-disable-next-line no-redeclare
    function isArray(a) {
        return isArrayImpl(a);
    }
    var didWarnValueDefaultValue$1;
    didWarnValueDefaultValue$1 = false;
    function getDeclarationErrorAddendum() {
        var ownerName = getCurrentFiberOwnerNameInDevOrNull();
        if (ownerName) return '\n\nCheck the render method of `' + ownerName + '`.';
        return '';
    }
    var valuePropNames = [
        'value',
        'defaultValue'
    ];
    /**
 * Validation function for `value` and `defaultValue`.
 */ function checkSelectPropTypes(props) {
        checkControlledValueProps('select', props);
        for(var i = 0; i < valuePropNames.length; i++){
            var propName = valuePropNames[i];
            if (props[propName] == null) continue;
            var propNameIsArray = isArray(props[propName]);
            if (props.multiple && !propNameIsArray) error1("The `%s` prop supplied to <select> must be an array if `multiple` is true.%s", propName, getDeclarationErrorAddendum());
            else if (!props.multiple && propNameIsArray) error1("The `%s` prop supplied to <select> must be a scalar value if `multiple` is false.%s", propName, getDeclarationErrorAddendum());
        }
    }
    function updateOptions(node, multiple, propValue, setDefaultSelected) {
        var options = node.options;
        if (multiple) {
            var selectedValues = propValue;
            var selectedValue = {};
            for(var i = 0; i < selectedValues.length; i++)// Prefix to avoid chaos with special keys.
            selectedValue['$' + selectedValues[i]] = true;
            for(var _i = 0; _i < options.length; _i++){
                var selected = selectedValue.hasOwnProperty('$' + options[_i].value);
                if (options[_i].selected !== selected) options[_i].selected = selected;
                if (selected && setDefaultSelected) options[_i].defaultSelected = true;
            }
        } else {
            // Do not set `select.value` as exact behavior isn't consistent across all
            // browsers for all cases.
            var _selectedValue = toString(getToStringValue(propValue));
            var defaultSelected = null;
            for(var _i2 = 0; _i2 < options.length; _i2++){
                if (options[_i2].value === _selectedValue) {
                    options[_i2].selected = true;
                    if (setDefaultSelected) options[_i2].defaultSelected = true;
                    return;
                }
                if (defaultSelected === null && !options[_i2].disabled) defaultSelected = options[_i2];
            }
            if (defaultSelected !== null) defaultSelected.selected = true;
        }
    }
    /**
 * Implements a <select> host component that allows optionally setting the
 * props `value` and `defaultValue`. If `multiple` is false, the prop must be a
 * stringable. If `multiple` is true, the prop must be an array of stringables.
 *
 * If `value` is not supplied (or null/undefined), user actions that change the
 * selected option will trigger updates to the rendered options.
 *
 * If it is supplied (and not null/undefined), the rendered options will not
 * update in response to user actions. Instead, the `value` prop must change in
 * order for the rendered options to update.
 *
 * If `defaultValue` is provided, any options with the supplied values will be
 * selected.
 */ function getHostProps$1(element, props) {
        return assign({}, props, {
            value: undefined
        });
    }
    function initWrapperState$1(element, props) {
        var node = element;
        checkSelectPropTypes(props);
        node._wrapperState = {
            wasMultiple: !!props.multiple
        };
        if (props.value !== undefined && props.defaultValue !== undefined && !didWarnValueDefaultValue$1) {
            error1("Select elements must be either controlled or uncontrolled (specify either the value prop, or the defaultValue prop, but not both). Decide between using a controlled or uncontrolled select element and remove one of these props. More info: https://reactjs.org/link/controlled-components");
            didWarnValueDefaultValue$1 = true;
        }
    }
    function postMountWrapper$2(element, props) {
        var node = element;
        node.multiple = !!props.multiple;
        var value = props.value;
        if (value != null) updateOptions(node, !!props.multiple, value, false);
        else if (props.defaultValue != null) updateOptions(node, !!props.multiple, props.defaultValue, true);
    }
    function postUpdateWrapper(element, props) {
        var node = element;
        var wasMultiple = node._wrapperState.wasMultiple;
        node._wrapperState.wasMultiple = !!props.multiple;
        var value = props.value;
        if (value != null) updateOptions(node, !!props.multiple, value, false);
        else if (wasMultiple !== !!props.multiple) {
            // For simplicity, reapply `defaultValue` if `multiple` is toggled.
            if (props.defaultValue != null) updateOptions(node, !!props.multiple, props.defaultValue, true);
            else // Revert the select back to its default unselected state.
            updateOptions(node, !!props.multiple, props.multiple ? [] : '', false);
        }
    }
    function restoreControlledState$1(element, props) {
        var node = element;
        var value = props.value;
        if (value != null) updateOptions(node, !!props.multiple, value, false);
    }
    var didWarnValDefaultVal = false;
    /**
 * Implements a <textarea> host component that allows setting `value`, and
 * `defaultValue`. This differs from the traditional DOM API because value is
 * usually set as PCDATA children.
 *
 * If `value` is not supplied (or null/undefined), user actions that affect the
 * value will trigger updates to the element.
 *
 * If `value` is supplied (and not null/undefined), the rendered element will
 * not trigger updates to the element. Instead, the `value` prop must change in
 * order for the rendered element to be updated.
 *
 * The rendered element will be initialized with an empty value, the prop
 * `defaultValue` if specified, or the children content (deprecated).
 */ function getHostProps$2(element, props) {
        var node = element;
        if (props.dangerouslySetInnerHTML != null) throw new Error('`dangerouslySetInnerHTML` does not make sense on <textarea>.');
         // Always set children to the same thing. In IE9, the selection range will
        // get reset if `textContent` is mutated.  We could add a check in setTextContent
        // to only set the value if/when the value differs from the node value (which would
        // completely solve this IE9 bug), but Sebastian+Sophie seemed to like this
        // solution. The value can be a boolean or object so that's why it's forced
        // to be a string.
        var hostProps = assign({}, props, {
            value: undefined,
            defaultValue: undefined,
            children: toString(node._wrapperState.initialValue)
        });
        return hostProps;
    }
    function initWrapperState$2(element, props) {
        var node = element;
        checkControlledValueProps('textarea', props);
        if (props.value !== undefined && props.defaultValue !== undefined && !didWarnValDefaultVal) {
            error1("%s contains a textarea with both value and defaultValue props. Textarea elements must be either controlled or uncontrolled (specify either the value prop, or the defaultValue prop, but not both). Decide between using a controlled or uncontrolled textarea and remove one of these props. More info: https://reactjs.org/link/controlled-components", getCurrentFiberOwnerNameInDevOrNull() || 'A component');
            didWarnValDefaultVal = true;
        }
        var initialValue = props.value; // Only bother fetching default value if we're going to use it
        if (initialValue == null) {
            var children = props.children, defaultValue = props.defaultValue;
            if (children != null) {
                error1("Use the `defaultValue` or `value` props instead of setting children on <textarea>.");
                if (defaultValue != null) throw new Error('If you supply `defaultValue` on a <textarea>, do not pass children.');
                if (isArray(children)) {
                    if (children.length > 1) throw new Error('<textarea> can only have at most one child.');
                    children = children[0];
                }
                defaultValue = children;
            }
            if (defaultValue == null) defaultValue = '';
            initialValue = defaultValue;
        }
        node._wrapperState = {
            initialValue: getToStringValue(initialValue)
        };
    }
    function updateWrapper$1(element, props) {
        var node = element;
        var value = getToStringValue(props.value);
        var defaultValue = getToStringValue(props.defaultValue);
        if (value != null) {
            // Cast `value` to a string to ensure the value is set correctly. While
            // browsers typically do this as necessary, jsdom doesn't.
            var newValue = toString(value); // To avoid side effects (such as losing text selection), only set value if changed
            if (newValue !== node.value) node.value = newValue;
            if (props.defaultValue == null && node.defaultValue !== newValue) node.defaultValue = newValue;
        }
        if (defaultValue != null) node.defaultValue = toString(defaultValue);
    }
    function postMountWrapper$3(element, props) {
        var node = element; // This is in postMount because we need access to the DOM node, which is not
        // available until after the component has mounted.
        var textContent = node.textContent; // Only set node.value if textContent is equal to the expected
        // initial value. In IE10/IE11 there is a bug where the placeholder attribute
        // will populate textContent as well.
        // https://developer.microsoft.com/microsoft-edge/platform/issues/101525/
        if (textContent === node._wrapperState.initialValue) {
            if (textContent !== '' && textContent !== null) node.value = textContent;
        }
    }
    function restoreControlledState$2(element, props) {
        // DOM component is still mounted; update
        updateWrapper$1(element, props);
    }
    var HTML_NAMESPACE = 'http://www.w3.org/1999/xhtml';
    var MATH_NAMESPACE = 'http://www.w3.org/1998/Math/MathML';
    var SVG_NAMESPACE = 'http://www.w3.org/2000/svg'; // Assumes there is no parent namespace.
    function getIntrinsicNamespace(type) {
        switch(type){
            case 'svg':
                return SVG_NAMESPACE;
            case 'math':
                return MATH_NAMESPACE;
            default:
                return HTML_NAMESPACE;
        }
    }
    function getChildNamespace(parentNamespace, type) {
        if (parentNamespace == null || parentNamespace === HTML_NAMESPACE) // No (or default) parent namespace: potential entry point.
        return getIntrinsicNamespace(type);
        if (parentNamespace === SVG_NAMESPACE && type === 'foreignObject') // We're leaving SVG.
        return HTML_NAMESPACE;
         // By default, pass namespace below.
        return parentNamespace;
    }
    /* globals MSApp */ /**
 * Create a function which has 'unsafe' privileges (required by windows8 apps)
 */ var createMicrosoftUnsafeLocalFunction = function(func) {
        if (typeof MSApp !== 'undefined' && MSApp.execUnsafeLocalFunction) return function(arg0, arg1, arg2, arg3) {
            MSApp.execUnsafeLocalFunction(function() {
                return func(arg0, arg1, arg2, arg3);
            });
        };
        else return func;
    };
    var reusableSVGContainer;
    /**
 * Set the innerHTML property of a node
 *
 * @param {DOMElement} node
 * @param {string} html
 * @internal
 */ var setInnerHTML = createMicrosoftUnsafeLocalFunction(function(node, html) {
        if (node.namespaceURI === SVG_NAMESPACE) {
            if (!('innerHTML' in node)) {
                // IE does not have innerHTML for SVG nodes, so instead we inject the
                // new markup in a temp node and then move the child nodes across into
                // the target node
                reusableSVGContainer = reusableSVGContainer || document.createElement('div');
                reusableSVGContainer.innerHTML = '<svg>' + html.valueOf().toString() + '</svg>';
                var svgNode = reusableSVGContainer.firstChild;
                while(node.firstChild)node.removeChild(node.firstChild);
                while(svgNode.firstChild)node.appendChild(svgNode.firstChild);
                return;
            }
        }
        node.innerHTML = html;
    });
    /**
 * HTML nodeType values that represent the type of the node
 */ var ELEMENT_NODE = 1;
    var TEXT_NODE = 3;
    var COMMENT_NODE = 8;
    var DOCUMENT_NODE = 9;
    var DOCUMENT_FRAGMENT_NODE = 11;
    /**
 * Set the textContent property of a node. For text updates, it's faster
 * to set the `nodeValue` of the Text node directly instead of using
 * `.textContent` which will remove the existing node and create a new one.
 *
 * @param {DOMElement} node
 * @param {string} text
 * @internal
 */ var setTextContent = function(node, text) {
        if (text) {
            var firstChild = node.firstChild;
            if (firstChild && firstChild === node.lastChild && firstChild.nodeType === TEXT_NODE) {
                firstChild.nodeValue = text;
                return;
            }
        }
        node.textContent = text;
    };
    // List derived from Gecko source code:
    // https://github.com/mozilla/gecko-dev/blob/4e638efc71/layout/style/test/property_database.js
    var shorthandToLonghand = {
        animation: [
            'animationDelay',
            'animationDirection',
            'animationDuration',
            'animationFillMode',
            'animationIterationCount',
            'animationName',
            'animationPlayState',
            'animationTimingFunction'
        ],
        background: [
            'backgroundAttachment',
            'backgroundClip',
            'backgroundColor',
            'backgroundImage',
            'backgroundOrigin',
            'backgroundPositionX',
            'backgroundPositionY',
            'backgroundRepeat',
            'backgroundSize'
        ],
        backgroundPosition: [
            'backgroundPositionX',
            'backgroundPositionY'
        ],
        border: [
            'borderBottomColor',
            'borderBottomStyle',
            'borderBottomWidth',
            'borderImageOutset',
            'borderImageRepeat',
            'borderImageSlice',
            'borderImageSource',
            'borderImageWidth',
            'borderLeftColor',
            'borderLeftStyle',
            'borderLeftWidth',
            'borderRightColor',
            'borderRightStyle',
            'borderRightWidth',
            'borderTopColor',
            'borderTopStyle',
            'borderTopWidth'
        ],
        borderBlockEnd: [
            'borderBlockEndColor',
            'borderBlockEndStyle',
            'borderBlockEndWidth'
        ],
        borderBlockStart: [
            'borderBlockStartColor',
            'borderBlockStartStyle',
            'borderBlockStartWidth'
        ],
        borderBottom: [
            'borderBottomColor',
            'borderBottomStyle',
            'borderBottomWidth'
        ],
        borderColor: [
            'borderBottomColor',
            'borderLeftColor',
            'borderRightColor',
            'borderTopColor'
        ],
        borderImage: [
            'borderImageOutset',
            'borderImageRepeat',
            'borderImageSlice',
            'borderImageSource',
            'borderImageWidth'
        ],
        borderInlineEnd: [
            'borderInlineEndColor',
            'borderInlineEndStyle',
            'borderInlineEndWidth'
        ],
        borderInlineStart: [
            'borderInlineStartColor',
            'borderInlineStartStyle',
            'borderInlineStartWidth'
        ],
        borderLeft: [
            'borderLeftColor',
            'borderLeftStyle',
            'borderLeftWidth'
        ],
        borderRadius: [
            'borderBottomLeftRadius',
            'borderBottomRightRadius',
            'borderTopLeftRadius',
            'borderTopRightRadius'
        ],
        borderRight: [
            'borderRightColor',
            'borderRightStyle',
            'borderRightWidth'
        ],
        borderStyle: [
            'borderBottomStyle',
            'borderLeftStyle',
            'borderRightStyle',
            'borderTopStyle'
        ],
        borderTop: [
            'borderTopColor',
            'borderTopStyle',
            'borderTopWidth'
        ],
        borderWidth: [
            'borderBottomWidth',
            'borderLeftWidth',
            'borderRightWidth',
            'borderTopWidth'
        ],
        columnRule: [
            'columnRuleColor',
            'columnRuleStyle',
            'columnRuleWidth'
        ],
        columns: [
            'columnCount',
            'columnWidth'
        ],
        flex: [
            'flexBasis',
            'flexGrow',
            'flexShrink'
        ],
        flexFlow: [
            'flexDirection',
            'flexWrap'
        ],
        font: [
            'fontFamily',
            'fontFeatureSettings',
            'fontKerning',
            'fontLanguageOverride',
            'fontSize',
            'fontSizeAdjust',
            'fontStretch',
            'fontStyle',
            'fontVariant',
            'fontVariantAlternates',
            'fontVariantCaps',
            'fontVariantEastAsian',
            'fontVariantLigatures',
            'fontVariantNumeric',
            'fontVariantPosition',
            'fontWeight',
            'lineHeight'
        ],
        fontVariant: [
            'fontVariantAlternates',
            'fontVariantCaps',
            'fontVariantEastAsian',
            'fontVariantLigatures',
            'fontVariantNumeric',
            'fontVariantPosition'
        ],
        gap: [
            'columnGap',
            'rowGap'
        ],
        grid: [
            'gridAutoColumns',
            'gridAutoFlow',
            'gridAutoRows',
            'gridTemplateAreas',
            'gridTemplateColumns',
            'gridTemplateRows'
        ],
        gridArea: [
            'gridColumnEnd',
            'gridColumnStart',
            'gridRowEnd',
            'gridRowStart'
        ],
        gridColumn: [
            'gridColumnEnd',
            'gridColumnStart'
        ],
        gridColumnGap: [
            'columnGap'
        ],
        gridGap: [
            'columnGap',
            'rowGap'
        ],
        gridRow: [
            'gridRowEnd',
            'gridRowStart'
        ],
        gridRowGap: [
            'rowGap'
        ],
        gridTemplate: [
            'gridTemplateAreas',
            'gridTemplateColumns',
            'gridTemplateRows'
        ],
        listStyle: [
            'listStyleImage',
            'listStylePosition',
            'listStyleType'
        ],
        margin: [
            'marginBottom',
            'marginLeft',
            'marginRight',
            'marginTop'
        ],
        marker: [
            'markerEnd',
            'markerMid',
            'markerStart'
        ],
        mask: [
            'maskClip',
            'maskComposite',
            'maskImage',
            'maskMode',
            'maskOrigin',
            'maskPositionX',
            'maskPositionY',
            'maskRepeat',
            'maskSize'
        ],
        maskPosition: [
            'maskPositionX',
            'maskPositionY'
        ],
        outline: [
            'outlineColor',
            'outlineStyle',
            'outlineWidth'
        ],
        overflow: [
            'overflowX',
            'overflowY'
        ],
        padding: [
            'paddingBottom',
            'paddingLeft',
            'paddingRight',
            'paddingTop'
        ],
        placeContent: [
            'alignContent',
            'justifyContent'
        ],
        placeItems: [
            'alignItems',
            'justifyItems'
        ],
        placeSelf: [
            'alignSelf',
            'justifySelf'
        ],
        textDecoration: [
            'textDecorationColor',
            'textDecorationLine',
            'textDecorationStyle'
        ],
        textEmphasis: [
            'textEmphasisColor',
            'textEmphasisStyle'
        ],
        transition: [
            'transitionDelay',
            'transitionDuration',
            'transitionProperty',
            'transitionTimingFunction'
        ],
        wordWrap: [
            'overflowWrap'
        ]
    };
    /**
 * CSS properties which accept numbers but are not in units of "px".
 */ var isUnitlessNumber = {
        animationIterationCount: true,
        aspectRatio: true,
        borderImageOutset: true,
        borderImageSlice: true,
        borderImageWidth: true,
        boxFlex: true,
        boxFlexGroup: true,
        boxOrdinalGroup: true,
        columnCount: true,
        columns: true,
        flex: true,
        flexGrow: true,
        flexPositive: true,
        flexShrink: true,
        flexNegative: true,
        flexOrder: true,
        gridArea: true,
        gridRow: true,
        gridRowEnd: true,
        gridRowSpan: true,
        gridRowStart: true,
        gridColumn: true,
        gridColumnEnd: true,
        gridColumnSpan: true,
        gridColumnStart: true,
        fontWeight: true,
        lineClamp: true,
        lineHeight: true,
        opacity: true,
        order: true,
        orphans: true,
        tabSize: true,
        widows: true,
        zIndex: true,
        zoom: true,
        // SVG-related properties
        fillOpacity: true,
        floodOpacity: true,
        stopOpacity: true,
        strokeDasharray: true,
        strokeDashoffset: true,
        strokeMiterlimit: true,
        strokeOpacity: true,
        strokeWidth: true
    };
    /**
 * @param {string} prefix vendor-specific prefix, eg: Webkit
 * @param {string} key style name, eg: transitionDuration
 * @return {string} style name prefixed with `prefix`, properly camelCased, eg:
 * WebkitTransitionDuration
 */ function prefixKey(prefix, key) {
        return prefix + key.charAt(0).toUpperCase() + key.substring(1);
    }
    /**
 * Support style names that may come passed in prefixed by adding permutations
 * of vendor prefixes.
 */ var prefixes1 = [
        'Webkit',
        'ms',
        'Moz',
        'O'
    ]; // Using Object.keys here, or else the vanilla for-in loop makes IE8 go into an
    // infinite loop, because it iterates over the newly added props too.
    Object.keys(isUnitlessNumber).forEach(function(prop) {
        prefixes1.forEach(function(prefix) {
            isUnitlessNumber[prefixKey(prefix, prop)] = isUnitlessNumber[prop];
        });
    });
    /**
 * Convert a value into the proper css writable value. The style name `name`
 * should be logical (no hyphens), as specified
 * in `CSSProperty.isUnitlessNumber`.
 *
 * @param {string} name CSS property name such as `topMargin`.
 * @param {*} value CSS property value such as `10px`.
 * @return {string} Normalized style value with dimensions applied.
 */ function dangerousStyleValue(name, value, isCustomProperty) {
        // Note that we've removed escapeTextForBrowser() calls here since the
        // whole string will be escaped when the attribute is injected into
        // the markup. If you provide unsafe user data here they can inject
        // arbitrary CSS which may be problematic (I couldn't repro this):
        // https://www.owasp.org/index.php/XSS_Filter_Evasion_Cheat_Sheet
        // http://www.thespanner.co.uk/2007/11/26/ultimate-xss-css-injection/
        // This is not an XSS hole but instead a potential CSS injection issue
        // which has lead to a greater discussion about how we're going to
        // trust URLs moving forward. See #2115901
        var isEmpty = value == null || typeof value === 'boolean' || value === '';
        if (isEmpty) return '';
        if (!isCustomProperty && typeof value === 'number' && value !== 0 && !(isUnitlessNumber.hasOwnProperty(name) && isUnitlessNumber[name])) return value + 'px'; // Presumes implicit 'px' suffix for unitless numbers
        checkCSSPropertyStringCoercion(value, name);
        return ('' + value).trim();
    }
    var uppercasePattern = /([A-Z])/g;
    var msPattern = /^ms-/;
    /**
 * Hyphenates a camelcased CSS property name, for example:
 *
 *   > hyphenateStyleName('backgroundColor')
 *   < "background-color"
 *   > hyphenateStyleName('MozTransition')
 *   < "-moz-transition"
 *   > hyphenateStyleName('msTransition')
 *   < "-ms-transition"
 *
 * As Modernizr suggests (http://modernizr.com/docs/#prefixed), an `ms` prefix
 * is converted to `-ms-`.
 */ function hyphenateStyleName(name) {
        return name.replace(uppercasePattern, '-$1').toLowerCase().replace(msPattern, '-ms-');
    }
    var warnValidStyle = function() {};
    // 'msTransform' is correct, but the other prefixes should be capitalized
    var badVendoredStyleNamePattern = /^(?:webkit|moz|o)[A-Z]/;
    var msPattern$1 = /^-ms-/;
    var hyphenPattern = /-(.)/g; // style values shouldn't contain a semicolon
    var badStyleValueWithSemicolonPattern = /;\s*$/;
    var warnedStyleNames = {};
    var warnedStyleValues = {};
    var warnedForNaNValue = false;
    var warnedForInfinityValue = false;
    var camelize = function(string) {
        return string.replace(hyphenPattern, function(_, character) {
            return character.toUpperCase();
        });
    };
    var warnHyphenatedStyleName = function(name) {
        if (warnedStyleNames.hasOwnProperty(name) && warnedStyleNames[name]) return;
        warnedStyleNames[name] = true;
        error1('Unsupported style property %s. Did you mean %s?', name, // (http://www.andismith.com/blog/2012/02/modernizr-prefixed/), an `-ms` prefix
        // is converted to lowercase `ms`.
        camelize(name.replace(msPattern$1, 'ms-')));
    };
    var warnBadVendoredStyleName = function(name) {
        if (warnedStyleNames.hasOwnProperty(name) && warnedStyleNames[name]) return;
        warnedStyleNames[name] = true;
        error1('Unsupported vendor-prefixed style property %s. Did you mean %s?', name, name.charAt(0).toUpperCase() + name.slice(1));
    };
    var warnStyleValueWithSemicolon = function(name, value) {
        if (warnedStyleValues.hasOwnProperty(value) && warnedStyleValues[value]) return;
        warnedStyleValues[value] = true;
        error1('Style property values shouldn\'t contain a semicolon. Try "%s: %s" instead.', name, value.replace(badStyleValueWithSemicolonPattern, ''));
    };
    var warnStyleValueIsNaN = function(name, value) {
        if (warnedForNaNValue) return;
        warnedForNaNValue = true;
        error1('`NaN` is an invalid value for the `%s` css style property.', name);
    };
    var warnStyleValueIsInfinity = function(name, value) {
        if (warnedForInfinityValue) return;
        warnedForInfinityValue = true;
        error1('`Infinity` is an invalid value for the `%s` css style property.', name);
    };
    warnValidStyle = function(name, value) {
        if (name.indexOf('-') > -1) warnHyphenatedStyleName(name);
        else if (badVendoredStyleNamePattern.test(name)) warnBadVendoredStyleName(name);
        else if (badStyleValueWithSemicolonPattern.test(value)) warnStyleValueWithSemicolon(name, value);
        if (typeof value === 'number') {
            if (isNaN(value)) warnStyleValueIsNaN(name, value);
            else if (!isFinite(value)) warnStyleValueIsInfinity(name, value);
        }
    };
    var warnValidStyle$1 = warnValidStyle;
    /**
 * Operations for dealing with CSS properties.
 */ /**
 * This creates a string that is expected to be equivalent to the style
 * attribute generated by server-side rendering. It by-passes warnings and
 * security checks so it's not safe to use this value for anything other than
 * comparison. It is only used in DEV for SSR validation.
 */ function createDangerousStringForStyles(styles) {
        var serialized = '';
        var delimiter = '';
        for(var styleName in styles){
            if (!styles.hasOwnProperty(styleName)) continue;
            var styleValue = styles[styleName];
            if (styleValue != null) {
                var isCustomProperty = styleName.indexOf('--') === 0;
                serialized += delimiter + (isCustomProperty ? styleName : hyphenateStyleName(styleName)) + ':';
                serialized += dangerousStyleValue(styleName, styleValue, isCustomProperty);
                delimiter = ';';
            }
        }
        return serialized || null;
    }
    /**
 * Sets the value for multiple styles on a node.  If a value is specified as
 * '' (empty string), the corresponding style property will be unset.
 *
 * @param {DOMElement} node
 * @param {object} styles
 */ function setValueForStyles(node, styles) {
        var style = node.style;
        for(var styleName in styles){
            if (!styles.hasOwnProperty(styleName)) continue;
            var isCustomProperty = styleName.indexOf('--') === 0;
            if (!isCustomProperty) warnValidStyle$1(styleName, styles[styleName]);
            var styleValue = dangerousStyleValue(styleName, styles[styleName], isCustomProperty);
            if (styleName === 'float') styleName = 'cssFloat';
            if (isCustomProperty) style.setProperty(styleName, styleValue);
            else style[styleName] = styleValue;
        }
    }
    function isValueEmpty(value) {
        return value == null || typeof value === 'boolean' || value === '';
    }
    /**
 * Given {color: 'red', overflow: 'hidden'} returns {
 *   color: 'color',
 *   overflowX: 'overflow',
 *   overflowY: 'overflow',
 * }. This can be read as "the overflowY property was set by the overflow
 * shorthand". That is, the values are the property that each was derived from.
 */ function expandShorthandMap(styles) {
        var expanded = {};
        for(var key in styles){
            var longhands = shorthandToLonghand[key] || [
                key
            ];
            for(var i = 0; i < longhands.length; i++)expanded[longhands[i]] = key;
        }
        return expanded;
    }
    /**
 * When mixing shorthand and longhand property names, we warn during updates if
 * we expect an incorrect result to occur. In particular, we warn for:
 *
 * Updating a shorthand property (longhand gets overwritten):
 *   {font: 'foo', fontVariant: 'bar'} -> {font: 'baz', fontVariant: 'bar'}
 *   becomes .style.font = 'baz'
 * Removing a shorthand property (longhand gets lost too):
 *   {font: 'foo', fontVariant: 'bar'} -> {fontVariant: 'bar'}
 *   becomes .style.font = ''
 * Removing a longhand property (should revert to shorthand; doesn't):
 *   {font: 'foo', fontVariant: 'bar'} -> {font: 'foo'}
 *   becomes .style.fontVariant = ''
 */ function validateShorthandPropertyCollisionInDev(styleUpdates, nextStyles) {
        if (!nextStyles) return;
        var expandedUpdates = expandShorthandMap(styleUpdates);
        var expandedStyles = expandShorthandMap(nextStyles);
        var warnedAbout = {};
        for(var key in expandedUpdates){
            var originalKey = expandedUpdates[key];
            var correctOriginalKey = expandedStyles[key];
            if (correctOriginalKey && originalKey !== correctOriginalKey) {
                var warningKey = originalKey + ',' + correctOriginalKey;
                if (warnedAbout[warningKey]) continue;
                warnedAbout[warningKey] = true;
                error1("%s a style property during rerender (%s) when a conflicting property is set (%s) can lead to styling bugs. To avoid this, don't mix shorthand and non-shorthand properties for the same value; instead, replace the shorthand with separate values.", isValueEmpty(styleUpdates[originalKey]) ? 'Removing' : 'Updating', originalKey, correctOriginalKey);
            }
        }
    }
    // For HTML, certain tags should omit their close tag. We keep a list for
    // those special-case tags.
    var omittedCloseTags = {
        area: true,
        base: true,
        br: true,
        col: true,
        embed: true,
        hr: true,
        img: true,
        input: true,
        keygen: true,
        link: true,
        meta: true,
        param: true,
        source: true,
        track: true,
        wbr: true // NOTE: menuitem's close tag should be omitted, but that causes problems.
    };
    // `omittedCloseTags` except that `menuitem` should still have its closing tag.
    var voidElementTags = assign({
        menuitem: true
    }, omittedCloseTags);
    var HTML = '__html';
    function assertValidProps(tag, props) {
        if (!props) return;
         // Note the use of `==` which checks for null or undefined.
        if (voidElementTags[tag]) {
            if (props.children != null || props.dangerouslySetInnerHTML != null) throw new Error(tag + " is a void element tag and must neither have `children` nor " + 'use `dangerouslySetInnerHTML`.');
        }
        if (props.dangerouslySetInnerHTML != null) {
            if (props.children != null) throw new Error('Can only set one of `children` or `props.dangerouslySetInnerHTML`.');
            if (typeof props.dangerouslySetInnerHTML !== 'object' || !(HTML in props.dangerouslySetInnerHTML)) throw new Error("`props.dangerouslySetInnerHTML` must be in the form `{__html: ...}`. Please visit https://reactjs.org/link/dangerously-set-inner-html for more information.");
        }
        if (!props.suppressContentEditableWarning && props.contentEditable && props.children != null) error1("A component is `contentEditable` and contains `children` managed by React. It is now your responsibility to guarantee that none of those nodes are unexpectedly modified or duplicated. This is probably not intentional.");
        if (props.style != null && typeof props.style !== 'object') throw new Error("The `style` prop expects a mapping from style properties to values, not a string. For example, style={{marginRight: spacing + 'em'}} when using JSX.");
    }
    function isCustomComponent(tagName, props) {
        if (tagName.indexOf('-') === -1) return typeof props.is === 'string';
        switch(tagName){
            // These are reserved SVG and MathML elements.
            // We don't mind this list too much because we expect it to never grow.
            // The alternative is to track the namespace in a few places which is convoluted.
            // https://w3c.github.io/webcomponents/spec/custom/#custom-elements-core-concepts
            case 'annotation-xml':
            case 'color-profile':
            case 'font-face':
            case 'font-face-src':
            case 'font-face-uri':
            case 'font-face-format':
            case 'font-face-name':
            case 'missing-glyph':
                return false;
            default:
                return true;
        }
    }
    // When adding attributes to the HTML or SVG allowed attribute list, be sure to
    // also add them to this module to ensure casing and incorrect name
    // warnings.
    var possibleStandardNames = {
        // HTML
        accept: 'accept',
        acceptcharset: 'acceptCharset',
        'accept-charset': 'acceptCharset',
        accesskey: 'accessKey',
        action: 'action',
        allowfullscreen: 'allowFullScreen',
        alt: 'alt',
        as: 'as',
        async: 'async',
        autocapitalize: 'autoCapitalize',
        autocomplete: 'autoComplete',
        autocorrect: 'autoCorrect',
        autofocus: 'autoFocus',
        autoplay: 'autoPlay',
        autosave: 'autoSave',
        capture: 'capture',
        cellpadding: 'cellPadding',
        cellspacing: 'cellSpacing',
        challenge: 'challenge',
        charset: 'charSet',
        checked: 'checked',
        children: 'children',
        cite: 'cite',
        class: 'className',
        classid: 'classID',
        classname: 'className',
        cols: 'cols',
        colspan: 'colSpan',
        content: 'content',
        contenteditable: 'contentEditable',
        contextmenu: 'contextMenu',
        controls: 'controls',
        controlslist: 'controlsList',
        coords: 'coords',
        crossorigin: 'crossOrigin',
        dangerouslysetinnerhtml: 'dangerouslySetInnerHTML',
        data: 'data',
        datetime: 'dateTime',
        default: 'default',
        defaultchecked: 'defaultChecked',
        defaultvalue: 'defaultValue',
        defer: 'defer',
        dir: 'dir',
        disabled: 'disabled',
        disablepictureinpicture: 'disablePictureInPicture',
        disableremoteplayback: 'disableRemotePlayback',
        download: 'download',
        draggable: 'draggable',
        enctype: 'encType',
        enterkeyhint: 'enterKeyHint',
        for: 'htmlFor',
        form: 'form',
        formmethod: 'formMethod',
        formaction: 'formAction',
        formenctype: 'formEncType',
        formnovalidate: 'formNoValidate',
        formtarget: 'formTarget',
        frameborder: 'frameBorder',
        headers: 'headers',
        height: 'height',
        hidden: 'hidden',
        high: 'high',
        href: 'href',
        hreflang: 'hrefLang',
        htmlfor: 'htmlFor',
        httpequiv: 'httpEquiv',
        'http-equiv': 'httpEquiv',
        icon: 'icon',
        id: 'id',
        imagesizes: 'imageSizes',
        imagesrcset: 'imageSrcSet',
        innerhtml: 'innerHTML',
        inputmode: 'inputMode',
        integrity: 'integrity',
        is: 'is',
        itemid: 'itemID',
        itemprop: 'itemProp',
        itemref: 'itemRef',
        itemscope: 'itemScope',
        itemtype: 'itemType',
        keyparams: 'keyParams',
        keytype: 'keyType',
        kind: 'kind',
        label: 'label',
        lang: 'lang',
        list: 'list',
        loop: 'loop',
        low: 'low',
        manifest: 'manifest',
        marginwidth: 'marginWidth',
        marginheight: 'marginHeight',
        max: 'max',
        maxlength: 'maxLength',
        media: 'media',
        mediagroup: 'mediaGroup',
        method: 'method',
        min: 'min',
        minlength: 'minLength',
        multiple: 'multiple',
        muted: 'muted',
        name: 'name',
        nomodule: 'noModule',
        nonce: 'nonce',
        novalidate: 'noValidate',
        open: 'open',
        optimum: 'optimum',
        pattern: 'pattern',
        placeholder: 'placeholder',
        playsinline: 'playsInline',
        poster: 'poster',
        preload: 'preload',
        profile: 'profile',
        radiogroup: 'radioGroup',
        readonly: 'readOnly',
        referrerpolicy: 'referrerPolicy',
        rel: 'rel',
        required: 'required',
        reversed: 'reversed',
        role: 'role',
        rows: 'rows',
        rowspan: 'rowSpan',
        sandbox: 'sandbox',
        scope: 'scope',
        scoped: 'scoped',
        scrolling: 'scrolling',
        seamless: 'seamless',
        selected: 'selected',
        shape: 'shape',
        size: 'size',
        sizes: 'sizes',
        span: 'span',
        spellcheck: 'spellCheck',
        src: 'src',
        srcdoc: 'srcDoc',
        srclang: 'srcLang',
        srcset: 'srcSet',
        start: 'start',
        step: 'step',
        style: 'style',
        summary: 'summary',
        tabindex: 'tabIndex',
        target: 'target',
        title: 'title',
        type: 'type',
        usemap: 'useMap',
        value: 'value',
        width: 'width',
        wmode: 'wmode',
        wrap: 'wrap',
        // SVG
        about: 'about',
        accentheight: 'accentHeight',
        'accent-height': 'accentHeight',
        accumulate: 'accumulate',
        additive: 'additive',
        alignmentbaseline: 'alignmentBaseline',
        'alignment-baseline': 'alignmentBaseline',
        allowreorder: 'allowReorder',
        alphabetic: 'alphabetic',
        amplitude: 'amplitude',
        arabicform: 'arabicForm',
        'arabic-form': 'arabicForm',
        ascent: 'ascent',
        attributename: 'attributeName',
        attributetype: 'attributeType',
        autoreverse: 'autoReverse',
        azimuth: 'azimuth',
        basefrequency: 'baseFrequency',
        baselineshift: 'baselineShift',
        'baseline-shift': 'baselineShift',
        baseprofile: 'baseProfile',
        bbox: 'bbox',
        begin: 'begin',
        bias: 'bias',
        by: 'by',
        calcmode: 'calcMode',
        capheight: 'capHeight',
        'cap-height': 'capHeight',
        clip: 'clip',
        clippath: 'clipPath',
        'clip-path': 'clipPath',
        clippathunits: 'clipPathUnits',
        cliprule: 'clipRule',
        'clip-rule': 'clipRule',
        color: 'color',
        colorinterpolation: 'colorInterpolation',
        'color-interpolation': 'colorInterpolation',
        colorinterpolationfilters: 'colorInterpolationFilters',
        'color-interpolation-filters': 'colorInterpolationFilters',
        colorprofile: 'colorProfile',
        'color-profile': 'colorProfile',
        colorrendering: 'colorRendering',
        'color-rendering': 'colorRendering',
        contentscripttype: 'contentScriptType',
        contentstyletype: 'contentStyleType',
        cursor: 'cursor',
        cx: 'cx',
        cy: 'cy',
        d: 'd',
        datatype: 'datatype',
        decelerate: 'decelerate',
        descent: 'descent',
        diffuseconstant: 'diffuseConstant',
        direction: 'direction',
        display: 'display',
        divisor: 'divisor',
        dominantbaseline: 'dominantBaseline',
        'dominant-baseline': 'dominantBaseline',
        dur: 'dur',
        dx: 'dx',
        dy: 'dy',
        edgemode: 'edgeMode',
        elevation: 'elevation',
        enablebackground: 'enableBackground',
        'enable-background': 'enableBackground',
        end: 'end',
        exponent: 'exponent',
        externalresourcesrequired: 'externalResourcesRequired',
        fill: 'fill',
        fillopacity: 'fillOpacity',
        'fill-opacity': 'fillOpacity',
        fillrule: 'fillRule',
        'fill-rule': 'fillRule',
        filter: 'filter',
        filterres: 'filterRes',
        filterunits: 'filterUnits',
        floodopacity: 'floodOpacity',
        'flood-opacity': 'floodOpacity',
        floodcolor: 'floodColor',
        'flood-color': 'floodColor',
        focusable: 'focusable',
        fontfamily: 'fontFamily',
        'font-family': 'fontFamily',
        fontsize: 'fontSize',
        'font-size': 'fontSize',
        fontsizeadjust: 'fontSizeAdjust',
        'font-size-adjust': 'fontSizeAdjust',
        fontstretch: 'fontStretch',
        'font-stretch': 'fontStretch',
        fontstyle: 'fontStyle',
        'font-style': 'fontStyle',
        fontvariant: 'fontVariant',
        'font-variant': 'fontVariant',
        fontweight: 'fontWeight',
        'font-weight': 'fontWeight',
        format: 'format',
        from: 'from',
        fx: 'fx',
        fy: 'fy',
        g1: 'g1',
        g2: 'g2',
        glyphname: 'glyphName',
        'glyph-name': 'glyphName',
        glyphorientationhorizontal: 'glyphOrientationHorizontal',
        'glyph-orientation-horizontal': 'glyphOrientationHorizontal',
        glyphorientationvertical: 'glyphOrientationVertical',
        'glyph-orientation-vertical': 'glyphOrientationVertical',
        glyphref: 'glyphRef',
        gradienttransform: 'gradientTransform',
        gradientunits: 'gradientUnits',
        hanging: 'hanging',
        horizadvx: 'horizAdvX',
        'horiz-adv-x': 'horizAdvX',
        horizoriginx: 'horizOriginX',
        'horiz-origin-x': 'horizOriginX',
        ideographic: 'ideographic',
        imagerendering: 'imageRendering',
        'image-rendering': 'imageRendering',
        in2: 'in2',
        in: 'in',
        inlist: 'inlist',
        intercept: 'intercept',
        k1: 'k1',
        k2: 'k2',
        k3: 'k3',
        k4: 'k4',
        k: 'k',
        kernelmatrix: 'kernelMatrix',
        kernelunitlength: 'kernelUnitLength',
        kerning: 'kerning',
        keypoints: 'keyPoints',
        keysplines: 'keySplines',
        keytimes: 'keyTimes',
        lengthadjust: 'lengthAdjust',
        letterspacing: 'letterSpacing',
        'letter-spacing': 'letterSpacing',
        lightingcolor: 'lightingColor',
        'lighting-color': 'lightingColor',
        limitingconeangle: 'limitingConeAngle',
        local: 'local',
        markerend: 'markerEnd',
        'marker-end': 'markerEnd',
        markerheight: 'markerHeight',
        markermid: 'markerMid',
        'marker-mid': 'markerMid',
        markerstart: 'markerStart',
        'marker-start': 'markerStart',
        markerunits: 'markerUnits',
        markerwidth: 'markerWidth',
        mask: 'mask',
        maskcontentunits: 'maskContentUnits',
        maskunits: 'maskUnits',
        mathematical: 'mathematical',
        mode: 'mode',
        numoctaves: 'numOctaves',
        offset: 'offset',
        opacity: 'opacity',
        operator: 'operator',
        order: 'order',
        orient: 'orient',
        orientation: 'orientation',
        origin: 'origin',
        overflow: 'overflow',
        overlineposition: 'overlinePosition',
        'overline-position': 'overlinePosition',
        overlinethickness: 'overlineThickness',
        'overline-thickness': 'overlineThickness',
        paintorder: 'paintOrder',
        'paint-order': 'paintOrder',
        panose1: 'panose1',
        'panose-1': 'panose1',
        pathlength: 'pathLength',
        patterncontentunits: 'patternContentUnits',
        patterntransform: 'patternTransform',
        patternunits: 'patternUnits',
        pointerevents: 'pointerEvents',
        'pointer-events': 'pointerEvents',
        points: 'points',
        pointsatx: 'pointsAtX',
        pointsaty: 'pointsAtY',
        pointsatz: 'pointsAtZ',
        prefix: 'prefix',
        preservealpha: 'preserveAlpha',
        preserveaspectratio: 'preserveAspectRatio',
        primitiveunits: 'primitiveUnits',
        property: 'property',
        r: 'r',
        radius: 'radius',
        refx: 'refX',
        refy: 'refY',
        renderingintent: 'renderingIntent',
        'rendering-intent': 'renderingIntent',
        repeatcount: 'repeatCount',
        repeatdur: 'repeatDur',
        requiredextensions: 'requiredExtensions',
        requiredfeatures: 'requiredFeatures',
        resource: 'resource',
        restart: 'restart',
        result: 'result',
        results: 'results',
        rotate: 'rotate',
        rx: 'rx',
        ry: 'ry',
        scale: 'scale',
        security: 'security',
        seed: 'seed',
        shaperendering: 'shapeRendering',
        'shape-rendering': 'shapeRendering',
        slope: 'slope',
        spacing: 'spacing',
        specularconstant: 'specularConstant',
        specularexponent: 'specularExponent',
        speed: 'speed',
        spreadmethod: 'spreadMethod',
        startoffset: 'startOffset',
        stddeviation: 'stdDeviation',
        stemh: 'stemh',
        stemv: 'stemv',
        stitchtiles: 'stitchTiles',
        stopcolor: 'stopColor',
        'stop-color': 'stopColor',
        stopopacity: 'stopOpacity',
        'stop-opacity': 'stopOpacity',
        strikethroughposition: 'strikethroughPosition',
        'strikethrough-position': 'strikethroughPosition',
        strikethroughthickness: 'strikethroughThickness',
        'strikethrough-thickness': 'strikethroughThickness',
        string: 'string',
        stroke: 'stroke',
        strokedasharray: 'strokeDasharray',
        'stroke-dasharray': 'strokeDasharray',
        strokedashoffset: 'strokeDashoffset',
        'stroke-dashoffset': 'strokeDashoffset',
        strokelinecap: 'strokeLinecap',
        'stroke-linecap': 'strokeLinecap',
        strokelinejoin: 'strokeLinejoin',
        'stroke-linejoin': 'strokeLinejoin',
        strokemiterlimit: 'strokeMiterlimit',
        'stroke-miterlimit': 'strokeMiterlimit',
        strokewidth: 'strokeWidth',
        'stroke-width': 'strokeWidth',
        strokeopacity: 'strokeOpacity',
        'stroke-opacity': 'strokeOpacity',
        suppresscontenteditablewarning: 'suppressContentEditableWarning',
        suppresshydrationwarning: 'suppressHydrationWarning',
        surfacescale: 'surfaceScale',
        systemlanguage: 'systemLanguage',
        tablevalues: 'tableValues',
        targetx: 'targetX',
        targety: 'targetY',
        textanchor: 'textAnchor',
        'text-anchor': 'textAnchor',
        textdecoration: 'textDecoration',
        'text-decoration': 'textDecoration',
        textlength: 'textLength',
        textrendering: 'textRendering',
        'text-rendering': 'textRendering',
        to: 'to',
        transform: 'transform',
        typeof: 'typeof',
        u1: 'u1',
        u2: 'u2',
        underlineposition: 'underlinePosition',
        'underline-position': 'underlinePosition',
        underlinethickness: 'underlineThickness',
        'underline-thickness': 'underlineThickness',
        unicode: 'unicode',
        unicodebidi: 'unicodeBidi',
        'unicode-bidi': 'unicodeBidi',
        unicoderange: 'unicodeRange',
        'unicode-range': 'unicodeRange',
        unitsperem: 'unitsPerEm',
        'units-per-em': 'unitsPerEm',
        unselectable: 'unselectable',
        valphabetic: 'vAlphabetic',
        'v-alphabetic': 'vAlphabetic',
        values: 'values',
        vectoreffect: 'vectorEffect',
        'vector-effect': 'vectorEffect',
        version: 'version',
        vertadvy: 'vertAdvY',
        'vert-adv-y': 'vertAdvY',
        vertoriginx: 'vertOriginX',
        'vert-origin-x': 'vertOriginX',
        vertoriginy: 'vertOriginY',
        'vert-origin-y': 'vertOriginY',
        vhanging: 'vHanging',
        'v-hanging': 'vHanging',
        videographic: 'vIdeographic',
        'v-ideographic': 'vIdeographic',
        viewbox: 'viewBox',
        viewtarget: 'viewTarget',
        visibility: 'visibility',
        vmathematical: 'vMathematical',
        'v-mathematical': 'vMathematical',
        vocab: 'vocab',
        widths: 'widths',
        wordspacing: 'wordSpacing',
        'word-spacing': 'wordSpacing',
        writingmode: 'writingMode',
        'writing-mode': 'writingMode',
        x1: 'x1',
        x2: 'x2',
        x: 'x',
        xchannelselector: 'xChannelSelector',
        xheight: 'xHeight',
        'x-height': 'xHeight',
        xlinkactuate: 'xlinkActuate',
        'xlink:actuate': 'xlinkActuate',
        xlinkarcrole: 'xlinkArcrole',
        'xlink:arcrole': 'xlinkArcrole',
        xlinkhref: 'xlinkHref',
        'xlink:href': 'xlinkHref',
        xlinkrole: 'xlinkRole',
        'xlink:role': 'xlinkRole',
        xlinkshow: 'xlinkShow',
        'xlink:show': 'xlinkShow',
        xlinktitle: 'xlinkTitle',
        'xlink:title': 'xlinkTitle',
        xlinktype: 'xlinkType',
        'xlink:type': 'xlinkType',
        xmlbase: 'xmlBase',
        'xml:base': 'xmlBase',
        xmllang: 'xmlLang',
        'xml:lang': 'xmlLang',
        xmlns: 'xmlns',
        'xml:space': 'xmlSpace',
        xmlnsxlink: 'xmlnsXlink',
        'xmlns:xlink': 'xmlnsXlink',
        xmlspace: 'xmlSpace',
        y1: 'y1',
        y2: 'y2',
        y: 'y',
        ychannelselector: 'yChannelSelector',
        z: 'z',
        zoomandpan: 'zoomAndPan'
    };
    var ariaProperties = {
        'aria-current': 0,
        // state
        'aria-description': 0,
        'aria-details': 0,
        'aria-disabled': 0,
        // state
        'aria-hidden': 0,
        // state
        'aria-invalid': 0,
        // state
        'aria-keyshortcuts': 0,
        'aria-label': 0,
        'aria-roledescription': 0,
        // Widget Attributes
        'aria-autocomplete': 0,
        'aria-checked': 0,
        'aria-expanded': 0,
        'aria-haspopup': 0,
        'aria-level': 0,
        'aria-modal': 0,
        'aria-multiline': 0,
        'aria-multiselectable': 0,
        'aria-orientation': 0,
        'aria-placeholder': 0,
        'aria-pressed': 0,
        'aria-readonly': 0,
        'aria-required': 0,
        'aria-selected': 0,
        'aria-sort': 0,
        'aria-valuemax': 0,
        'aria-valuemin': 0,
        'aria-valuenow': 0,
        'aria-valuetext': 0,
        // Live Region Attributes
        'aria-atomic': 0,
        'aria-busy': 0,
        'aria-live': 0,
        'aria-relevant': 0,
        // Drag-and-Drop Attributes
        'aria-dropeffect': 0,
        'aria-grabbed': 0,
        // Relationship Attributes
        'aria-activedescendant': 0,
        'aria-colcount': 0,
        'aria-colindex': 0,
        'aria-colspan': 0,
        'aria-controls': 0,
        'aria-describedby': 0,
        'aria-errormessage': 0,
        'aria-flowto': 0,
        'aria-labelledby': 0,
        'aria-owns': 0,
        'aria-posinset': 0,
        'aria-rowcount': 0,
        'aria-rowindex': 0,
        'aria-rowspan': 0,
        'aria-setsize': 0
    };
    var warnedProperties = {};
    var rARIA = new RegExp('^(aria)-[' + ATTRIBUTE_NAME_CHAR + ']*$');
    var rARIACamel = new RegExp('^(aria)[A-Z][' + ATTRIBUTE_NAME_CHAR + ']*$');
    function validateProperty(tagName, name) {
        if (hasOwnProperty.call(warnedProperties, name) && warnedProperties[name]) return true;
        if (rARIACamel.test(name)) {
            var ariaName = 'aria-' + name.slice(4).toLowerCase();
            var correctName = ariaProperties.hasOwnProperty(ariaName) ? ariaName : null; // If this is an aria-* attribute, but is not listed in the known DOM
            // DOM properties, then it is an invalid aria-* attribute.
            if (correctName == null) {
                error1('Invalid ARIA attribute `%s`. ARIA attributes follow the pattern aria-* and must be lowercase.', name);
                warnedProperties[name] = true;
                return true;
            } // aria-* attributes should be lowercase; suggest the lowercase version.
            if (name !== correctName) {
                error1('Invalid ARIA attribute `%s`. Did you mean `%s`?', name, correctName);
                warnedProperties[name] = true;
                return true;
            }
        }
        if (rARIA.test(name)) {
            var lowerCasedName = name.toLowerCase();
            var standardName = ariaProperties.hasOwnProperty(lowerCasedName) ? lowerCasedName : null; // If this is an aria-* attribute, but is not listed in the known DOM
            // DOM properties, then it is an invalid aria-* attribute.
            if (standardName == null) {
                warnedProperties[name] = true;
                return false;
            } // aria-* attributes should be lowercase; suggest the lowercase version.
            if (name !== standardName) {
                error1('Unknown ARIA attribute `%s`. Did you mean `%s`?', name, standardName);
                warnedProperties[name] = true;
                return true;
            }
        }
        return true;
    }
    function warnInvalidARIAProps(type, props) {
        var invalidProps = [];
        for(var key in props){
            var isValid = validateProperty(type, key);
            if (!isValid) invalidProps.push(key);
        }
        var unknownPropString = invalidProps.map(function(prop) {
            return '`' + prop + '`';
        }).join(', ');
        if (invalidProps.length === 1) error1("Invalid aria prop %s on <%s> tag. For details, see https://reactjs.org/link/invalid-aria-props", unknownPropString, type);
        else if (invalidProps.length > 1) error1("Invalid aria props %s on <%s> tag. For details, see https://reactjs.org/link/invalid-aria-props", unknownPropString, type);
    }
    function validateProperties(type, props) {
        if (isCustomComponent(type, props)) return;
        warnInvalidARIAProps(type, props);
    }
    var didWarnValueNull = false;
    function validateProperties$1(type, props) {
        if (type !== 'input' && type !== 'textarea' && type !== 'select') return;
        if (props != null && props.value === null && !didWarnValueNull) {
            didWarnValueNull = true;
            if (type === 'select' && props.multiple) error1("`value` prop on `%s` should not be null. Consider using an empty array when `multiple` is set to `true` to clear the component or `undefined` for uncontrolled components.", type);
            else error1("`value` prop on `%s` should not be null. Consider using an empty string to clear the component or `undefined` for uncontrolled components.", type);
        }
    }
    var validateProperty$1 = function() {};
    var warnedProperties$1 = {};
    var EVENT_NAME_REGEX = /^on./;
    var INVALID_EVENT_NAME_REGEX = /^on[^A-Z]/;
    var rARIA$1 = new RegExp('^(aria)-[' + ATTRIBUTE_NAME_CHAR + ']*$');
    var rARIACamel$1 = new RegExp('^(aria)[A-Z][' + ATTRIBUTE_NAME_CHAR + ']*$');
    validateProperty$1 = function(tagName, name, value, eventRegistry) {
        if (hasOwnProperty.call(warnedProperties$1, name) && warnedProperties$1[name]) return true;
        var lowerCasedName = name.toLowerCase();
        if (lowerCasedName === 'onfocusin' || lowerCasedName === 'onfocusout') {
            error1("React uses onFocus and onBlur instead of onFocusIn and onFocusOut. All React events are normalized to bubble, so onFocusIn and onFocusOut are not needed/supported by React.");
            warnedProperties$1[name] = true;
            return true;
        } // We can't rely on the event system being injected on the server.
        if (eventRegistry != null) {
            var registrationNameDependencies = eventRegistry.registrationNameDependencies, possibleRegistrationNames = eventRegistry.possibleRegistrationNames;
            if (registrationNameDependencies.hasOwnProperty(name)) return true;
            var registrationName = possibleRegistrationNames.hasOwnProperty(lowerCasedName) ? possibleRegistrationNames[lowerCasedName] : null;
            if (registrationName != null) {
                error1('Invalid event handler property `%s`. Did you mean `%s`?', name, registrationName);
                warnedProperties$1[name] = true;
                return true;
            }
            if (EVENT_NAME_REGEX.test(name)) {
                error1('Unknown event handler property `%s`. It will be ignored.', name);
                warnedProperties$1[name] = true;
                return true;
            }
        } else if (EVENT_NAME_REGEX.test(name)) {
            // If no event plugins have been injected, we are in a server environment.
            // So we can't tell if the event name is correct for sure, but we can filter
            // out known bad ones like `onclick`. We can't suggest a specific replacement though.
            if (INVALID_EVENT_NAME_REGEX.test(name)) error1("Invalid event handler property `%s`. React events use the camelCase naming convention, for example `onClick`.", name);
            warnedProperties$1[name] = true;
            return true;
        } // Let the ARIA attribute hook validate ARIA attributes
        if (rARIA$1.test(name) || rARIACamel$1.test(name)) return true;
        if (lowerCasedName === 'innerhtml') {
            error1("Directly setting property `innerHTML` is not permitted. For more information, lookup documentation on `dangerouslySetInnerHTML`.");
            warnedProperties$1[name] = true;
            return true;
        }
        if (lowerCasedName === 'aria') {
            error1("The `aria` attribute is reserved for future use in React. Pass individual `aria-` attributes instead.");
            warnedProperties$1[name] = true;
            return true;
        }
        if (lowerCasedName === 'is' && value !== null && value !== undefined && typeof value !== 'string') {
            error1("Received a `%s` for a string attribute `is`. If this is expected, cast the value to a string.", typeof value);
            warnedProperties$1[name] = true;
            return true;
        }
        if (typeof value === 'number' && isNaN(value)) {
            error1("Received NaN for the `%s` attribute. If this is expected, cast the value to a string.", name);
            warnedProperties$1[name] = true;
            return true;
        }
        var propertyInfo = getPropertyInfo(name);
        var isReserved = propertyInfo !== null && propertyInfo.type === RESERVED; // Known attributes should match the casing specified in the property config.
        if (possibleStandardNames.hasOwnProperty(lowerCasedName)) {
            var standardName = possibleStandardNames[lowerCasedName];
            if (standardName !== name) {
                error1('Invalid DOM property `%s`. Did you mean `%s`?', name, standardName);
                warnedProperties$1[name] = true;
                return true;
            }
        } else if (!isReserved && name !== lowerCasedName) {
            // Unknown attributes should have lowercase casing since that's how they
            // will be cased anyway with server rendering.
            error1("React does not recognize the `%s` prop on a DOM element. If you intentionally want it to appear in the DOM as a custom attribute, spell it as lowercase `%s` instead. If you accidentally passed it from a parent component, remove it from the DOM element.", name, lowerCasedName);
            warnedProperties$1[name] = true;
            return true;
        }
        if (typeof value === 'boolean' && shouldRemoveAttributeWithWarning(name, value, propertyInfo, false)) {
            if (value) error1('Received `%s` for a non-boolean attribute `%s`.\n\nIf you want to write it to the DOM, pass a string instead: %s="%s" or %s={value.toString()}.', value, name, name, value, name);
            else error1('Received `%s` for a non-boolean attribute `%s`.\n\nIf you want to write it to the DOM, pass a string instead: %s="%s" or %s={value.toString()}.\n\nIf you used to conditionally omit it with %s={condition && value}, pass %s={condition ? value : undefined} instead.', value, name, name, value, name, name, name);
            warnedProperties$1[name] = true;
            return true;
        } // Now that we've validated casing, do not validate
        // data types for reserved props
        if (isReserved) return true;
         // Warn when a known attribute is a bad type
        if (shouldRemoveAttributeWithWarning(name, value, propertyInfo, false)) {
            warnedProperties$1[name] = true;
            return false;
        } // Warn when passing the strings 'false' or 'true' into a boolean prop
        if ((value === 'false' || value === 'true') && propertyInfo !== null && propertyInfo.type === BOOLEAN) {
            error1("Received the string `%s` for the boolean attribute `%s`. %s Did you mean %s={%s}?", value, name, value === 'false' ? 'The browser will interpret it as a truthy value.' : 'Although this works, it will not work as expected if you pass the string "false".', name, value);
            warnedProperties$1[name] = true;
            return true;
        }
        return true;
    };
    var warnUnknownProperties = function(type, props, eventRegistry) {
        var unknownProps = [];
        for(var key in props){
            var isValid = validateProperty$1(type, key, props[key], eventRegistry);
            if (!isValid) unknownProps.push(key);
        }
        var unknownPropString = unknownProps.map(function(prop) {
            return '`' + prop + '`';
        }).join(', ');
        if (unknownProps.length === 1) error1("Invalid value for prop %s on <%s> tag. Either remove it from the element, or pass a string or number value to keep it in the DOM. For details, see https://reactjs.org/link/attribute-behavior ", unknownPropString, type);
        else if (unknownProps.length > 1) error1("Invalid values for props %s on <%s> tag. Either remove them from the element, or pass a string or number value to keep them in the DOM. For details, see https://reactjs.org/link/attribute-behavior ", unknownPropString, type);
    };
    function validateProperties$2(type, props, eventRegistry) {
        if (isCustomComponent(type, props)) return;
        warnUnknownProperties(type, props, eventRegistry);
    }
    var IS_EVENT_HANDLE_NON_MANAGED_NODE = 1;
    var IS_NON_DELEGATED = 2;
    var IS_CAPTURE_PHASE = 4;
    // set to LEGACY_FB_SUPPORT. LEGACY_FB_SUPPORT only gets set when
    // we call willDeferLaterForLegacyFBSupport, thus not bailing out
    // will result in endless cycles like an infinite loop.
    // We also don't want to defer during event replaying.
    var SHOULD_NOT_PROCESS_POLYFILL_EVENT_PLUGINS = IS_EVENT_HANDLE_NON_MANAGED_NODE | IS_NON_DELEGATED | IS_CAPTURE_PHASE;
    // This exists to avoid circular dependency between ReactDOMEventReplaying
    // and DOMPluginEventSystem.
    var currentReplayingEvent = null;
    function setReplayingEvent(event) {
        if (currentReplayingEvent !== null) error1("Expected currently replaying event to be null. This error is likely caused by a bug in React. Please file an issue.");
        currentReplayingEvent = event;
    }
    function resetReplayingEvent() {
        if (currentReplayingEvent === null) error1("Expected currently replaying event to not be null. This error is likely caused by a bug in React. Please file an issue.");
        currentReplayingEvent = null;
    }
    function isReplayingEvent(event) {
        return event === currentReplayingEvent;
    }
    /**
 * Gets the target node from a native browser event by accounting for
 * inconsistencies in browser DOM APIs.
 *
 * @param {object} nativeEvent Native browser event.
 * @return {DOMEventTarget} Target node.
 */ function getEventTarget(nativeEvent) {
        // Fallback to nativeEvent.srcElement for IE9
        // https://github.com/facebook/react/issues/12506
        var target = nativeEvent.target || nativeEvent.srcElement || window; // Normalize SVG <use> element events #4963
        if (target.correspondingUseElement) target = target.correspondingUseElement;
         // Safari may fire events on text nodes (Node.TEXT_NODE is 3).
        // @see http://www.quirksmode.org/js/events_properties.html
        return target.nodeType === TEXT_NODE ? target.parentNode : target;
    }
    var restoreImpl = null;
    var restoreTarget = null;
    var restoreQueue = null;
    function restoreStateOfTarget(target) {
        // We perform this translation at the end of the event loop so that we
        // always receive the correct fiber here
        var internalInstance = getInstanceFromNode(target);
        if (!internalInstance) // Unmounted
        return;
        if (typeof restoreImpl !== 'function') throw new Error("setRestoreImplementation() needs to be called to handle a target for controlled events. This error is likely caused by a bug in React. Please file an issue.");
        var stateNode = internalInstance.stateNode; // Guard against Fiber being unmounted.
        if (stateNode) {
            var _props = getFiberCurrentPropsFromNode(stateNode);
            restoreImpl(internalInstance.stateNode, internalInstance.type, _props);
        }
    }
    function setRestoreImplementation(impl) {
        restoreImpl = impl;
    }
    function enqueueStateRestore(target) {
        if (restoreTarget) {
            if (restoreQueue) restoreQueue.push(target);
            else restoreQueue = [
                target
            ];
        } else restoreTarget = target;
    }
    function needsStateRestore() {
        return restoreTarget !== null || restoreQueue !== null;
    }
    function restoreStateIfNeeded() {
        if (!restoreTarget) return;
        var target = restoreTarget;
        var queuedTargets = restoreQueue;
        restoreTarget = null;
        restoreQueue = null;
        restoreStateOfTarget(target);
        if (queuedTargets) for(var i = 0; i < queuedTargets.length; i++)restoreStateOfTarget(queuedTargets[i]);
    }
    // the renderer. Such as when we're dispatching events or if third party
    // libraries need to call batchedUpdates. Eventually, this API will go away when
    // everything is batched by default. We'll then have a similar API to opt-out of
    // scheduled work and instead do synchronous work.
    // Defaults
    var batchedUpdatesImpl = function(fn, bookkeeping) {
        return fn(bookkeeping);
    };
    var flushSyncImpl = function() {};
    var isInsideEventHandler = false;
    function finishEventHandler() {
        // Here we wait until all updates have propagated, which is important
        // when using controlled components within layers:
        // https://github.com/facebook/react/issues/1698
        // Then we restore state of any controlled component.
        var controlledComponentsHavePendingUpdates = needsStateRestore();
        if (controlledComponentsHavePendingUpdates) {
            // If a controlled event was fired, we may need to restore the state of
            // the DOM node back to the controlled value. This is necessary when React
            // bails out of the update without touching the DOM.
            // TODO: Restore state in the microtask, after the discrete updates flush,
            // instead of early flushing them here.
            flushSyncImpl();
            restoreStateIfNeeded();
        }
    }
    function batchedUpdates(fn, a, b) {
        if (isInsideEventHandler) // If we are currently inside another batch, we need to wait until it
        // fully completes before restoring state.
        return fn(a, b);
        isInsideEventHandler = true;
        try {
            return batchedUpdatesImpl(fn, a, b);
        } finally{
            isInsideEventHandler = false;
            finishEventHandler();
        }
    } // TODO: Replace with flushSync
    function setBatchingImplementation(_batchedUpdatesImpl, _discreteUpdatesImpl, _flushSyncImpl) {
        batchedUpdatesImpl = _batchedUpdatesImpl;
        flushSyncImpl = _flushSyncImpl;
    }
    function isInteractive(tag) {
        return tag === 'button' || tag === 'input' || tag === 'select' || tag === 'textarea';
    }
    function shouldPreventMouseEvent(name, type, props) {
        switch(name){
            case 'onClick':
            case 'onClickCapture':
            case 'onDoubleClick':
            case 'onDoubleClickCapture':
            case 'onMouseDown':
            case 'onMouseDownCapture':
            case 'onMouseMove':
            case 'onMouseMoveCapture':
            case 'onMouseUp':
            case 'onMouseUpCapture':
            case 'onMouseEnter':
                return !!(props.disabled && isInteractive(type));
            default:
                return false;
        }
    }
    /**
 * @param {object} inst The instance, which is the source of events.
 * @param {string} registrationName Name of listener (e.g. `onClick`).
 * @return {?function} The stored callback.
 */ function getListener(inst, registrationName) {
        var stateNode = inst.stateNode;
        if (stateNode === null) // Work in progress (ex: onload events in incremental mode).
        return null;
        var props = getFiberCurrentPropsFromNode(stateNode);
        if (props === null) // Work in progress.
        return null;
        var listener = props[registrationName];
        if (shouldPreventMouseEvent(registrationName, inst.type, props)) return null;
        if (listener && typeof listener !== 'function') throw new Error("Expected `" + registrationName + "` listener to be a function, instead got a value of `" + typeof listener + "` type.");
        return listener;
    }
    var passiveBrowserEventsSupported = false; // Check if browser support events with passive listeners
    // https://developer.mozilla.org/en-US/docs/Web/API/EventTarget/addEventListener#Safely_detecting_option_support
    if (canUseDOM) try {
        var options1 = {}; // $FlowFixMe: Ignore Flow complaining about needing a value
        Object.defineProperty(options1, 'passive', {
            get: function() {
                passiveBrowserEventsSupported = true;
            }
        });
        window.addEventListener('test', options1, options1);
        window.removeEventListener('test', options1, options1);
    } catch (e1) {
        passiveBrowserEventsSupported = false;
    }
    function invokeGuardedCallbackProd(name, func, context, a, b, c, d, e, f) {
        var funcArgs = Array.prototype.slice.call(arguments, 3);
        try {
            func.apply(context, funcArgs);
        } catch (error) {
            this.onError(error);
        }
    }
    var invokeGuardedCallbackImpl = invokeGuardedCallbackProd;
    // In DEV mode, we swap out invokeGuardedCallback for a special version
    // that plays more nicely with the browser's DevTools. The idea is to preserve
    // "Pause on exceptions" behavior. Because React wraps all user-provided
    // functions in invokeGuardedCallback, and the production version of
    // invokeGuardedCallback uses a try-catch, all user exceptions are treated
    // like caught exceptions, and the DevTools won't pause unless the developer
    // takes the extra step of enabling pause on caught exceptions. This is
    // unintuitive, though, because even though React has caught the error, from
    // the developer's perspective, the error is uncaught.
    //
    // To preserve the expected "Pause on exceptions" behavior, we don't use a
    // try-catch in DEV. Instead, we synchronously dispatch a fake event to a fake
    // DOM node, and call the user-provided callback from inside an event handler
    // for that fake event. If the callback throws, the error is "captured" using
    // a global event handler. But because the error happens in a different
    // event loop context, it does not interrupt the normal program flow.
    // Effectively, this gives us try-catch behavior without actually using
    // try-catch. Neat!
    // Check that the browser supports the APIs we need to implement our special
    // DEV version of invokeGuardedCallback
    if (typeof window !== 'undefined' && typeof window.dispatchEvent === 'function' && typeof document !== 'undefined' && typeof document.createEvent === 'function') {
        var fakeNode = document.createElement('react');
        invokeGuardedCallbackImpl = function invokeGuardedCallbackDev(name, func, context, a, b, c, d, e, f) {
            // If document doesn't exist we know for sure we will crash in this method
            // when we call document.createEvent(). However this can cause confusing
            // errors: https://github.com/facebook/create-react-app/issues/3482
            // So we preemptively throw with a better message instead.
            if (typeof document === 'undefined' || document === null) throw new Error("The `document` global was defined when React was initialized, but is not defined anymore. This can happen in a test environment if a component schedules an update from an asynchronous callback, but the test has already finished running. To solve this, you can either unmount the component at the end of your test (and ensure that any asynchronous operations get canceled in `componentWillUnmount`), or you can change the test itself to be asynchronous.");
            var evt = document.createEvent('Event');
            var didCall = false; // Keeps track of whether the user-provided callback threw an error. We
            // set this to true at the beginning, then set it to false right after
            // calling the function. If the function errors, `didError` will never be
            // set to false. This strategy works even if the browser is flaky and
            // fails to call our global error handler, because it doesn't rely on
            // the error event at all.
            var didError = true; // Keeps track of the value of window.event so that we can reset it
            // during the callback to let user code access window.event in the
            // browsers that support it.
            var windowEvent = window.event; // Keeps track of the descriptor of window.event to restore it after event
            // dispatching: https://github.com/facebook/react/issues/13688
            var windowEventDescriptor = Object.getOwnPropertyDescriptor(window, 'event');
            function restoreAfterDispatch() {
                // We immediately remove the callback from event listeners so that
                // nested `invokeGuardedCallback` calls do not clash. Otherwise, a
                // nested call would trigger the fake event handlers of any call higher
                // in the stack.
                fakeNode.removeEventListener(evtType, callCallback, false); // We check for window.hasOwnProperty('event') to prevent the
                // window.event assignment in both IE <= 10 as they throw an error
                // "Member not found" in strict mode, and in Firefox which does not
                // support window.event.
                if (typeof window.event !== 'undefined' && window.hasOwnProperty('event')) window.event = windowEvent;
            } // Create an event handler for our fake event. We will synchronously
            // dispatch our fake event using `dispatchEvent`. Inside the handler, we
            // call the user-provided callback.
            var funcArgs = Array.prototype.slice.call(arguments, 3);
            function callCallback() {
                didCall = true;
                restoreAfterDispatch();
                func.apply(context, funcArgs);
                didError = false;
            } // Create a global error event handler. We use this to capture the value
            // that was thrown. It's possible that this error handler will fire more
            // than once; for example, if non-React code also calls `dispatchEvent`
            // and a handler for that event throws. We should be resilient to most of
            // those cases. Even if our error event handler fires more than once, the
            // last error event is always used. If the callback actually does error,
            // we know that the last error event is the correct one, because it's not
            // possible for anything else to have happened in between our callback
            // erroring and the code that follows the `dispatchEvent` call below. If
            // the callback doesn't error, but the error event was fired, we know to
            // ignore it because `didError` will be false, as described above.
            var error; // Use this to track whether the error event is ever called.
            var didSetError = false;
            var isCrossOriginError = false;
            function handleWindowError(event) {
                error = event.error;
                didSetError = true;
                if (error === null && event.colno === 0 && event.lineno === 0) isCrossOriginError = true;
                if (event.defaultPrevented) {
                    // Some other error handler has prevented default.
                    // Browsers silence the error report if this happens.
                    // We'll remember this to later decide whether to log it or not.
                    if (error != null && typeof error === 'object') try {
                        error._suppressLogging = true;
                    } catch (inner) {}
                }
            } // Create a fake event type.
            var evtType = "react-" + (name ? name : 'invokeguardedcallback'); // Attach our event handlers
            window.addEventListener('error', handleWindowError);
            fakeNode.addEventListener(evtType, callCallback, false); // Synchronously dispatch our fake event. If the user-provided function
            // errors, it will trigger our global error handler.
            evt.initEvent(evtType, false, false);
            fakeNode.dispatchEvent(evt);
            if (windowEventDescriptor) Object.defineProperty(window, 'event', windowEventDescriptor);
            if (didCall && didError) {
                if (!didSetError) // The callback errored, but the error event never fired.
                // eslint-disable-next-line react-internal/prod-error-codes
                error = new Error("An error was thrown inside one of your components, but React doesn't know what it was. This is likely due to browser flakiness. React does its best to preserve the \"Pause on exceptions\" behavior of the DevTools, which requires some DEV-mode only tricks. It's possible that these don't work in your browser. Try triggering the error in production mode, or switching to a modern browser. If you suspect that this is actually an issue with React, please file an issue.");
                else if (isCrossOriginError) // eslint-disable-next-line react-internal/prod-error-codes
                error = new Error("A cross-origin error was thrown. React doesn't have access to the actual error object in development. See https://reactjs.org/link/crossorigin-error for more information.");
                this.onError(error);
            } // Remove our event listeners
            window.removeEventListener('error', handleWindowError);
            if (!didCall) {
                // Something went really wrong, and our event was not dispatched.
                // https://github.com/facebook/react/issues/16734
                // https://github.com/facebook/react/issues/16585
                // Fall back to the production implementation.
                restoreAfterDispatch();
                return invokeGuardedCallbackProd.apply(this, arguments);
            }
        };
    }
    var invokeGuardedCallbackImpl$1 = invokeGuardedCallbackImpl;
    var hasError = false;
    var caughtError = null; // Used by event system to capture/rethrow the first error.
    var hasRethrowError = false;
    var rethrowError = null;
    var reporter = {
        onError: function(error) {
            hasError = true;
            caughtError = error;
        }
    };
    /**
 * Call a function while guarding against errors that happens within it.
 * Returns an error if it throws, otherwise null.
 *
 * In production, this is implemented using a try-catch. The reason we don't
 * use a try-catch directly is so that we can swap out a different
 * implementation in DEV mode.
 *
 * @param {String} name of the guard to use for logging or debugging
 * @param {Function} func The function to invoke
 * @param {*} context The context to use when calling the function
 * @param {...*} args Arguments for function
 */ function invokeGuardedCallback(name, func, context, a, b, c, d, e, f) {
        hasError = false;
        caughtError = null;
        invokeGuardedCallbackImpl$1.apply(reporter, arguments);
    }
    /**
 * Same as invokeGuardedCallback, but instead of returning an error, it stores
 * it in a global so it can be rethrown by `rethrowCaughtError` later.
 * TODO: See if caughtError and rethrowError can be unified.
 *
 * @param {String} name of the guard to use for logging or debugging
 * @param {Function} func The function to invoke
 * @param {*} context The context to use when calling the function
 * @param {...*} args Arguments for function
 */ function invokeGuardedCallbackAndCatchFirstError(name, func, context, a, b, c, d, e, f) {
        invokeGuardedCallback.apply(this, arguments);
        if (hasError) {
            var error = clearCaughtError();
            if (!hasRethrowError) {
                hasRethrowError = true;
                rethrowError = error;
            }
        }
    }
    /**
 * During execution of guarded functions we will capture the first error which
 * we will rethrow to be handled by the top level error handler.
 */ function rethrowCaughtError() {
        if (hasRethrowError) {
            var error = rethrowError;
            hasRethrowError = false;
            rethrowError = null;
            throw error;
        }
    }
    function hasCaughtError() {
        return hasError;
    }
    function clearCaughtError() {
        if (hasError) {
            var error = caughtError;
            hasError = false;
            caughtError = null;
            return error;
        } else throw new Error("clearCaughtError was called but no error was captured. This error is likely caused by a bug in React. Please file an issue.");
    }
    /**
 * `ReactInstanceMap` maintains a mapping from a public facing stateful
 * instance (key) and the internal representation (value). This allows public
 * methods to accept the user facing instance as an argument and map them back
 * to internal methods.
 *
 * Note that this module is currently shared and assumed to be stateless.
 * If this becomes an actual Map, that will break.
 */ function get1(key) {
        return key._reactInternals;
    }
    function has1(key) {
        return key._reactInternals !== undefined;
    }
    function set1(key, value) {
        key._reactInternals = value;
    }
    // Don't change these two values. They're used by React Dev Tools.
    var NoFlags = /*                      */ 0;
    var PerformedWork = /*                */ 1; // You can change the rest (and add more).
    var Placement = /*                    */ 2;
    var Update = /*                       */ 4;
    var ChildDeletion = /*                */ 16;
    var ContentReset = /*                 */ 32;
    var Callback = /*                     */ 64;
    var DidCapture = /*                   */ 128;
    var ForceClientRender = /*            */ 256;
    var Ref = /*                          */ 512;
    var Snapshot = /*                     */ 1024;
    var Passive = /*                      */ 2048;
    var Hydrating = /*                    */ 4096;
    var Visibility = /*                   */ 8192;
    var StoreConsistency = /*             */ 16384;
    var LifecycleEffectMask = Passive | Update | Callback | Ref | Snapshot | StoreConsistency; // Union of all commit flags (flags with the lifetime of a particular commit)
    var HostEffectMask = /*               */ 32767; // These are not really side effects, but we still reuse this field.
    var Incomplete = /*                   */ 32768;
    var ShouldCapture = /*                */ 65536;
    var ForceUpdateForLegacySuspense = /* */ 131072;
    var Forked = /*                       */ 1048576; // Static tags describe aspects of a fiber that are not specific to a render,
    // e.g. a fiber uses a passive effect (even if there are no updates on this particular render).
    // This enables us to defer more work in the unmount case,
    // since we can defer traversing the tree during layout to look for Passive effects,
    // and instead rely on the static flag as a signal that there may be cleanup work.
    var RefStatic = /*                    */ 2097152;
    var LayoutStatic = /*                 */ 4194304;
    var PassiveStatic = /*                */ 8388608; // These flags allow us to traverse to fibers that have effects on mount
    // without traversing the entire tree after every commit for
    // double invoking
    var MountLayoutDev = /*               */ 16777216;
    var MountPassiveDev = /*              */ 33554432; // Groups of flags that are used in the commit phase to skip over trees that
    // don't contain effects, by checking subtreeFlags.
    var BeforeMutationMask = // flag logic (see #20043)
    Update | Snapshot | 0;
    var MutationMask = Placement | Update | ChildDeletion | ContentReset | Ref | Hydrating | Visibility;
    var LayoutMask = Update | Callback | Ref | Visibility; // TODO: Split into PassiveMountMask and PassiveUnmountMask
    var PassiveMask = Passive | ChildDeletion; // Union of tags that don't get reset on clones.
    // This allows certain concepts to persist without recalculating them,
    // e.g. whether a subtree contains passive effects or portals.
    var StaticMask = LayoutStatic | PassiveStatic | RefStatic;
    var ReactCurrentOwner = ReactSharedInternals.ReactCurrentOwner;
    function getNearestMountedFiber(fiber) {
        var node = fiber;
        var nearestMounted = fiber;
        if (!fiber.alternate) {
            // If there is no alternate, this might be a new tree that isn't inserted
            // yet. If it is, then it will have a pending insertion effect on it.
            var nextNode = node;
            do {
                node = nextNode;
                if ((node.flags & (Placement | Hydrating)) !== NoFlags) // This is an insertion or in-progress hydration. The nearest possible
                // mounted fiber is the parent but we need to continue to figure out
                // if that one is still mounted.
                nearestMounted = node.return;
                nextNode = node.return;
            }while (nextNode)
        } else while(node.return)node = node.return;
        if (node.tag === HostRoot) // TODO: Check if this was a nested HostRoot when used with
        // renderContainerIntoSubtree.
        return nearestMounted;
         // If we didn't hit the root, that means that we're in an disconnected tree
        // that has been unmounted.
        return null;
    }
    function getSuspenseInstanceFromFiber(fiber) {
        if (fiber.tag === SuspenseComponent) {
            var suspenseState = fiber.memoizedState;
            if (suspenseState === null) {
                var current = fiber.alternate;
                if (current !== null) suspenseState = current.memoizedState;
            }
            if (suspenseState !== null) return suspenseState.dehydrated;
        }
        return null;
    }
    function getContainerFromFiber(fiber) {
        return fiber.tag === HostRoot ? fiber.stateNode.containerInfo : null;
    }
    function isFiberMounted(fiber) {
        return getNearestMountedFiber(fiber) === fiber;
    }
    function isMounted(component) {
        var owner = ReactCurrentOwner.current;
        if (owner !== null && owner.tag === ClassComponent) {
            var ownerFiber = owner;
            var instance = ownerFiber.stateNode;
            if (!instance._warnedAboutRefsInRender) error1("%s is accessing isMounted inside its render() function. render() should be a pure function of props and state. It should never access something that requires stale data from the previous render, such as refs. Move this logic to componentDidMount and componentDidUpdate instead.", getComponentNameFromFiber(ownerFiber) || 'A component');
            instance._warnedAboutRefsInRender = true;
        }
        var fiber = get1(component);
        if (!fiber) return false;
        return getNearestMountedFiber(fiber) === fiber;
    }
    function assertIsMounted(fiber) {
        if (getNearestMountedFiber(fiber) !== fiber) throw new Error('Unable to find node on an unmounted component.');
    }
    function findCurrentFiberUsingSlowPath(fiber) {
        var alternate = fiber.alternate;
        if (!alternate) {
            // If there is no alternate, then we only need to check if it is mounted.
            var nearestMounted = getNearestMountedFiber(fiber);
            if (nearestMounted === null) throw new Error('Unable to find node on an unmounted component.');
            if (nearestMounted !== fiber) return null;
            return fiber;
        } // If we have two possible branches, we'll walk backwards up to the root
        // to see what path the root points to. On the way we may hit one of the
        // special cases and we'll deal with them.
        var a = fiber;
        var b = alternate;
        while(true){
            var parentA = a.return;
            if (parentA === null) break;
            var parentB = parentA.alternate;
            if (parentB === null) {
                // There is no alternate. This is an unusual case. Currently, it only
                // happens when a Suspense component is hidden. An extra fragment fiber
                // is inserted in between the Suspense fiber and its children. Skip
                // over this extra fragment fiber and proceed to the next parent.
                var nextParent = parentA.return;
                if (nextParent !== null) {
                    a = b = nextParent;
                    continue;
                } // If there's no parent, we're at the root.
                break;
            } // If both copies of the parent fiber point to the same child, we can
            // assume that the child is current. This happens when we bailout on low
            // priority: the bailed out fiber's child reuses the current child.
            if (parentA.child === parentB.child) {
                var child = parentA.child;
                while(child){
                    if (child === a) {
                        // We've determined that A is the current branch.
                        assertIsMounted(parentA);
                        return fiber;
                    }
                    if (child === b) {
                        // We've determined that B is the current branch.
                        assertIsMounted(parentA);
                        return alternate;
                    }
                    child = child.sibling;
                } // We should never have an alternate for any mounting node. So the only
                // way this could possibly happen is if this was unmounted, if at all.
                throw new Error('Unable to find node on an unmounted component.');
            }
            if (a.return !== b.return) {
                // The return pointer of A and the return pointer of B point to different
                // fibers. We assume that return pointers never criss-cross, so A must
                // belong to the child set of A.return, and B must belong to the child
                // set of B.return.
                a = parentA;
                b = parentB;
            } else {
                // The return pointers point to the same fiber. We'll have to use the
                // default, slow path: scan the child sets of each parent alternate to see
                // which child belongs to which set.
                //
                // Search parent A's child set
                var didFindChild = false;
                var _child = parentA.child;
                while(_child){
                    if (_child === a) {
                        didFindChild = true;
                        a = parentA;
                        b = parentB;
                        break;
                    }
                    if (_child === b) {
                        didFindChild = true;
                        b = parentA;
                        a = parentB;
                        break;
                    }
                    _child = _child.sibling;
                }
                if (!didFindChild) {
                    // Search parent B's child set
                    _child = parentB.child;
                    while(_child){
                        if (_child === a) {
                            didFindChild = true;
                            a = parentB;
                            b = parentA;
                            break;
                        }
                        if (_child === b) {
                            didFindChild = true;
                            b = parentB;
                            a = parentA;
                            break;
                        }
                        _child = _child.sibling;
                    }
                    if (!didFindChild) throw new Error("Child was not found in either parent set. This indicates a bug in React related to the return pointer. Please file an issue.");
                }
            }
            if (a.alternate !== b) throw new Error("Return fibers should always be each others' alternates. This error is likely caused by a bug in React. Please file an issue.");
        } // If the root is not a host container, we're in a disconnected tree. I.e.
        // unmounted.
        if (a.tag !== HostRoot) throw new Error('Unable to find node on an unmounted component.');
        if (a.stateNode.current === a) // We've determined that A is the current branch.
        return fiber;
         // Otherwise B has to be current branch.
        return alternate;
    }
    function findCurrentHostFiber(parent) {
        var currentParent = findCurrentFiberUsingSlowPath(parent);
        return currentParent !== null ? findCurrentHostFiberImpl(currentParent) : null;
    }
    function findCurrentHostFiberImpl(node) {
        // Next we'll drill down this component to find the first HostComponent/Text.
        if (node.tag === HostComponent || node.tag === HostText) return node;
        var child = node.child;
        while(child !== null){
            var match = findCurrentHostFiberImpl(child);
            if (match !== null) return match;
            child = child.sibling;
        }
        return null;
    }
    function findCurrentHostFiberWithNoPortals(parent) {
        var currentParent = findCurrentFiberUsingSlowPath(parent);
        return currentParent !== null ? findCurrentHostFiberWithNoPortalsImpl(currentParent) : null;
    }
    function findCurrentHostFiberWithNoPortalsImpl(node) {
        // Next we'll drill down this component to find the first HostComponent/Text.
        if (node.tag === HostComponent || node.tag === HostText) return node;
        var child = node.child;
        while(child !== null){
            if (child.tag !== HostPortal) {
                var match = findCurrentHostFiberWithNoPortalsImpl(child);
                if (match !== null) return match;
            }
            child = child.sibling;
        }
        return null;
    }
    // This module only exists as an ESM wrapper around the external CommonJS
    var scheduleCallback = Scheduler.unstable_scheduleCallback;
    var cancelCallback = Scheduler.unstable_cancelCallback;
    var shouldYield = Scheduler.unstable_shouldYield;
    var requestPaint = Scheduler.unstable_requestPaint;
    var now = Scheduler.unstable_now;
    var getCurrentPriorityLevel = Scheduler.unstable_getCurrentPriorityLevel;
    var ImmediatePriority = Scheduler.unstable_ImmediatePriority;
    var UserBlockingPriority = Scheduler.unstable_UserBlockingPriority;
    var NormalPriority = Scheduler.unstable_NormalPriority;
    var LowPriority = Scheduler.unstable_LowPriority;
    var IdlePriority = Scheduler.unstable_IdlePriority;
    // this doesn't actually exist on the scheduler, but it *does*
    // on scheduler/unstable_mock, which we'll need for internal testing
    var unstable_yieldValue = Scheduler.unstable_yieldValue;
    var unstable_setDisableYieldValue = Scheduler.unstable_setDisableYieldValue;
    var rendererID = null;
    var injectedHook = null;
    var injectedProfilingHooks = null;
    var hasLoggedError = false;
    var isDevToolsPresent = typeof __REACT_DEVTOOLS_GLOBAL_HOOK__ !== 'undefined';
    function injectInternals(internals) {
        if (typeof __REACT_DEVTOOLS_GLOBAL_HOOK__ === 'undefined') // No DevTools
        return false;
        var hook = __REACT_DEVTOOLS_GLOBAL_HOOK__;
        if (hook.isDisabled) // This isn't a real property on the hook, but it can be set to opt out
        // of DevTools integration and associated warnings and logs.
        // https://github.com/facebook/react/issues/3877
        return true;
        if (!hook.supportsFiber) {
            error1("The installed version of React DevTools is too old and will not work with the current version of React. Please update React DevTools. https://reactjs.org/link/react-devtools");
            return true;
        }
        try {
            if (enableSchedulingProfiler) // Conditionally inject these hooks only if Timeline profiler is supported by this build.
            // This gives DevTools a way to feature detect that isn't tied to version number
            // (since profiling and timeline are controlled by different feature flags).
            internals = assign({}, internals, {
                getLaneLabelMap: getLaneLabelMap,
                injectProfilingHooks: injectProfilingHooks
            });
            rendererID = hook.inject(internals); // We have successfully injected, so now it is safe to set up hooks.
            injectedHook = hook;
        } catch (err) {
            error1('React instrumentation encountered an error: %s.', err);
        }
        if (hook.checkDCE) // This is the real DevTools.
        return true;
        else // This is likely a hook installed by Fast Refresh runtime.
        return false;
    }
    function onScheduleRoot(root, children) {
        if (injectedHook && typeof injectedHook.onScheduleFiberRoot === 'function') try {
            injectedHook.onScheduleFiberRoot(rendererID, root, children);
        } catch (err) {
            if (!hasLoggedError) {
                hasLoggedError = true;
                error1('React instrumentation encountered an error: %s', err);
            }
        }
    }
    function onCommitRoot(root, eventPriority) {
        if (injectedHook && typeof injectedHook.onCommitFiberRoot === 'function') try {
            var didError = (root.current.flags & DidCapture) === DidCapture;
            if (enableProfilerTimer) {
                var schedulerPriority;
                switch(eventPriority){
                    case DiscreteEventPriority:
                        schedulerPriority = ImmediatePriority;
                        break;
                    case ContinuousEventPriority:
                        schedulerPriority = UserBlockingPriority;
                        break;
                    case DefaultEventPriority:
                        schedulerPriority = NormalPriority;
                        break;
                    case IdleEventPriority:
                        schedulerPriority = IdlePriority;
                        break;
                    default:
                        schedulerPriority = NormalPriority;
                        break;
                }
                injectedHook.onCommitFiberRoot(rendererID, root, schedulerPriority, didError);
            } else injectedHook.onCommitFiberRoot(rendererID, root, undefined, didError);
        } catch (err) {
            if (!hasLoggedError) {
                hasLoggedError = true;
                error1('React instrumentation encountered an error: %s', err);
            }
        }
    }
    function onPostCommitRoot(root) {
        if (injectedHook && typeof injectedHook.onPostCommitFiberRoot === 'function') try {
            injectedHook.onPostCommitFiberRoot(rendererID, root);
        } catch (err) {
            if (!hasLoggedError) {
                hasLoggedError = true;
                error1('React instrumentation encountered an error: %s', err);
            }
        }
    }
    function onCommitUnmount(fiber) {
        if (injectedHook && typeof injectedHook.onCommitFiberUnmount === 'function') try {
            injectedHook.onCommitFiberUnmount(rendererID, fiber);
        } catch (err) {
            if (!hasLoggedError) {
                hasLoggedError = true;
                error1('React instrumentation encountered an error: %s', err);
            }
        }
    }
    function setIsStrictModeForDevtools(newIsStrictMode) {
        if (typeof unstable_yieldValue === 'function') {
            // We're in a test because Scheduler.unstable_yieldValue only exists
            // in SchedulerMock. To reduce the noise in strict mode tests,
            // suppress warnings and disable scheduler yielding during the double render
            unstable_setDisableYieldValue(newIsStrictMode);
            setSuppressWarning(newIsStrictMode);
        }
        if (injectedHook && typeof injectedHook.setStrictMode === 'function') try {
            injectedHook.setStrictMode(rendererID, newIsStrictMode);
        } catch (err) {
            if (!hasLoggedError) {
                hasLoggedError = true;
                error1('React instrumentation encountered an error: %s', err);
            }
        }
    } // Profiler API hooks
    function injectProfilingHooks(profilingHooks) {
        injectedProfilingHooks = profilingHooks;
    }
    function getLaneLabelMap() {
        var map = new Map();
        var lane = 1;
        for(var index = 0; index < TotalLanes; index++){
            var label = getLabelForLane(lane);
            map.set(lane, label);
            lane *= 2;
        }
        return map;
    }
    function markCommitStarted(lanes) {
        if (injectedProfilingHooks !== null && typeof injectedProfilingHooks.markCommitStarted === 'function') injectedProfilingHooks.markCommitStarted(lanes);
    }
    function markCommitStopped() {
        if (injectedProfilingHooks !== null && typeof injectedProfilingHooks.markCommitStopped === 'function') injectedProfilingHooks.markCommitStopped();
    }
    function markComponentRenderStarted(fiber) {
        if (injectedProfilingHooks !== null && typeof injectedProfilingHooks.markComponentRenderStarted === 'function') injectedProfilingHooks.markComponentRenderStarted(fiber);
    }
    function markComponentRenderStopped() {
        if (injectedProfilingHooks !== null && typeof injectedProfilingHooks.markComponentRenderStopped === 'function') injectedProfilingHooks.markComponentRenderStopped();
    }
    function markComponentPassiveEffectMountStarted(fiber) {
        if (injectedProfilingHooks !== null && typeof injectedProfilingHooks.markComponentPassiveEffectMountStarted === 'function') injectedProfilingHooks.markComponentPassiveEffectMountStarted(fiber);
    }
    function markComponentPassiveEffectMountStopped() {
        if (injectedProfilingHooks !== null && typeof injectedProfilingHooks.markComponentPassiveEffectMountStopped === 'function') injectedProfilingHooks.markComponentPassiveEffectMountStopped();
    }
    function markComponentPassiveEffectUnmountStarted(fiber) {
        if (injectedProfilingHooks !== null && typeof injectedProfilingHooks.markComponentPassiveEffectUnmountStarted === 'function') injectedProfilingHooks.markComponentPassiveEffectUnmountStarted(fiber);
    }
    function markComponentPassiveEffectUnmountStopped() {
        if (injectedProfilingHooks !== null && typeof injectedProfilingHooks.markComponentPassiveEffectUnmountStopped === 'function') injectedProfilingHooks.markComponentPassiveEffectUnmountStopped();
    }
    function markComponentLayoutEffectMountStarted(fiber) {
        if (injectedProfilingHooks !== null && typeof injectedProfilingHooks.markComponentLayoutEffectMountStarted === 'function') injectedProfilingHooks.markComponentLayoutEffectMountStarted(fiber);
    }
    function markComponentLayoutEffectMountStopped() {
        if (injectedProfilingHooks !== null && typeof injectedProfilingHooks.markComponentLayoutEffectMountStopped === 'function') injectedProfilingHooks.markComponentLayoutEffectMountStopped();
    }
    function markComponentLayoutEffectUnmountStarted(fiber) {
        if (injectedProfilingHooks !== null && typeof injectedProfilingHooks.markComponentLayoutEffectUnmountStarted === 'function') injectedProfilingHooks.markComponentLayoutEffectUnmountStarted(fiber);
    }
    function markComponentLayoutEffectUnmountStopped() {
        if (injectedProfilingHooks !== null && typeof injectedProfilingHooks.markComponentLayoutEffectUnmountStopped === 'function') injectedProfilingHooks.markComponentLayoutEffectUnmountStopped();
    }
    function markComponentErrored(fiber, thrownValue, lanes) {
        if (injectedProfilingHooks !== null && typeof injectedProfilingHooks.markComponentErrored === 'function') injectedProfilingHooks.markComponentErrored(fiber, thrownValue, lanes);
    }
    function markComponentSuspended(fiber, wakeable, lanes) {
        if (injectedProfilingHooks !== null && typeof injectedProfilingHooks.markComponentSuspended === 'function') injectedProfilingHooks.markComponentSuspended(fiber, wakeable, lanes);
    }
    function markLayoutEffectsStarted(lanes) {
        if (injectedProfilingHooks !== null && typeof injectedProfilingHooks.markLayoutEffectsStarted === 'function') injectedProfilingHooks.markLayoutEffectsStarted(lanes);
    }
    function markLayoutEffectsStopped() {
        if (injectedProfilingHooks !== null && typeof injectedProfilingHooks.markLayoutEffectsStopped === 'function') injectedProfilingHooks.markLayoutEffectsStopped();
    }
    function markPassiveEffectsStarted(lanes) {
        if (injectedProfilingHooks !== null && typeof injectedProfilingHooks.markPassiveEffectsStarted === 'function') injectedProfilingHooks.markPassiveEffectsStarted(lanes);
    }
    function markPassiveEffectsStopped() {
        if (injectedProfilingHooks !== null && typeof injectedProfilingHooks.markPassiveEffectsStopped === 'function') injectedProfilingHooks.markPassiveEffectsStopped();
    }
    function markRenderStarted(lanes) {
        if (injectedProfilingHooks !== null && typeof injectedProfilingHooks.markRenderStarted === 'function') injectedProfilingHooks.markRenderStarted(lanes);
    }
    function markRenderYielded() {
        if (injectedProfilingHooks !== null && typeof injectedProfilingHooks.markRenderYielded === 'function') injectedProfilingHooks.markRenderYielded();
    }
    function markRenderStopped() {
        if (injectedProfilingHooks !== null && typeof injectedProfilingHooks.markRenderStopped === 'function') injectedProfilingHooks.markRenderStopped();
    }
    function markRenderScheduled(lane) {
        if (injectedProfilingHooks !== null && typeof injectedProfilingHooks.markRenderScheduled === 'function') injectedProfilingHooks.markRenderScheduled(lane);
    }
    function markForceUpdateScheduled(fiber, lane) {
        if (injectedProfilingHooks !== null && typeof injectedProfilingHooks.markForceUpdateScheduled === 'function') injectedProfilingHooks.markForceUpdateScheduled(fiber, lane);
    }
    function markStateUpdateScheduled(fiber, lane) {
        if (injectedProfilingHooks !== null && typeof injectedProfilingHooks.markStateUpdateScheduled === 'function') injectedProfilingHooks.markStateUpdateScheduled(fiber, lane);
    }
    var NoMode = /*                         */ 0; // TODO: Remove ConcurrentMode by reading from the root tag instead
    var ConcurrentMode = /*                 */ 1;
    var ProfileMode = /*                    */ 2;
    var StrictLegacyMode = /*               */ 8;
    var StrictEffectsMode = /*              */ 16;
    // TODO: This is pretty well supported by browsers. Maybe we can drop it.
    var clz32 = Math.clz32 ? Math.clz32 : clz32Fallback; // Count leading zeros.
    // Based on:
    // https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Math/clz32
    var log = Math.log;
    var LN2 = Math.LN2;
    function clz32Fallback(x) {
        var asUint = x >>> 0;
        if (asUint === 0) return 32;
        return 31 - (log(asUint) / LN2 | 0) | 0;
    }
    // If those values are changed that package should be rebuilt and redeployed.
    var TotalLanes = 31;
    var NoLanes = /*                        */ 0;
    var NoLane = /*                          */ 0;
    var SyncLane = /*                        */ 1;
    var InputContinuousHydrationLane = /*    */ 2;
    var InputContinuousLane = /*             */ 4;
    var DefaultHydrationLane = /*            */ 8;
    var DefaultLane = /*                     */ 16;
    var TransitionHydrationLane = /*                */ 32;
    var TransitionLanes = /*                       */ 4194240;
    var TransitionLane1 = /*                        */ 64;
    var TransitionLane2 = /*                        */ 128;
    var TransitionLane3 = /*                        */ 256;
    var TransitionLane4 = /*                        */ 512;
    var TransitionLane5 = /*                        */ 1024;
    var TransitionLane6 = /*                        */ 2048;
    var TransitionLane7 = /*                        */ 4096;
    var TransitionLane8 = /*                        */ 8192;
    var TransitionLane9 = /*                        */ 16384;
    var TransitionLane10 = /*                       */ 32768;
    var TransitionLane11 = /*                       */ 65536;
    var TransitionLane12 = /*                       */ 131072;
    var TransitionLane13 = /*                       */ 262144;
    var TransitionLane14 = /*                       */ 524288;
    var TransitionLane15 = /*                       */ 1048576;
    var TransitionLane16 = /*                       */ 2097152;
    var RetryLanes = /*                            */ 130023424;
    var RetryLane1 = /*                             */ 4194304;
    var RetryLane2 = /*                             */ 8388608;
    var RetryLane3 = /*                             */ 16777216;
    var RetryLane4 = /*                             */ 33554432;
    var RetryLane5 = /*                             */ 67108864;
    var SomeRetryLane = RetryLane1;
    var SelectiveHydrationLane = /*          */ 134217728;
    var NonIdleLanes = /*                          */ 268435455;
    var IdleHydrationLane = /*               */ 268435456;
    var IdleLane = /*                        */ 536870912;
    var OffscreenLane = /*                   */ 1073741824; // This function is used for the experimental timeline (react-devtools-timeline)
    // It should be kept in sync with the Lanes values above.
    function getLabelForLane(lane) {
        if (lane & SyncLane) return 'Sync';
        if (lane & InputContinuousHydrationLane) return 'InputContinuousHydration';
        if (lane & InputContinuousLane) return 'InputContinuous';
        if (lane & DefaultHydrationLane) return 'DefaultHydration';
        if (lane & DefaultLane) return 'Default';
        if (lane & TransitionHydrationLane) return 'TransitionHydration';
        if (lane & TransitionLanes) return 'Transition';
        if (lane & RetryLanes) return 'Retry';
        if (lane & SelectiveHydrationLane) return 'SelectiveHydration';
        if (lane & IdleHydrationLane) return 'IdleHydration';
        if (lane & IdleLane) return 'Idle';
        if (lane & OffscreenLane) return 'Offscreen';
    }
    var NoTimestamp = -1;
    var nextTransitionLane = TransitionLane1;
    var nextRetryLane = RetryLane1;
    function getHighestPriorityLanes(lanes) {
        switch(getHighestPriorityLane(lanes)){
            case SyncLane:
                return SyncLane;
            case InputContinuousHydrationLane:
                return InputContinuousHydrationLane;
            case InputContinuousLane:
                return InputContinuousLane;
            case DefaultHydrationLane:
                return DefaultHydrationLane;
            case DefaultLane:
                return DefaultLane;
            case TransitionHydrationLane:
                return TransitionHydrationLane;
            case TransitionLane1:
            case TransitionLane2:
            case TransitionLane3:
            case TransitionLane4:
            case TransitionLane5:
            case TransitionLane6:
            case TransitionLane7:
            case TransitionLane8:
            case TransitionLane9:
            case TransitionLane10:
            case TransitionLane11:
            case TransitionLane12:
            case TransitionLane13:
            case TransitionLane14:
            case TransitionLane15:
            case TransitionLane16:
                return lanes & TransitionLanes;
            case RetryLane1:
            case RetryLane2:
            case RetryLane3:
            case RetryLane4:
            case RetryLane5:
                return lanes & RetryLanes;
            case SelectiveHydrationLane:
                return SelectiveHydrationLane;
            case IdleHydrationLane:
                return IdleHydrationLane;
            case IdleLane:
                return IdleLane;
            case OffscreenLane:
                return OffscreenLane;
            default:
                error1('Should have found matching lanes. This is a bug in React.');
                return lanes;
        }
    }
    function getNextLanes(root, wipLanes) {
        // Early bailout if there's no pending work left.
        var pendingLanes = root.pendingLanes;
        if (pendingLanes === NoLanes) return NoLanes;
        var nextLanes = NoLanes;
        var suspendedLanes = root.suspendedLanes;
        var pingedLanes = root.pingedLanes; // Do not work on any idle work until all the non-idle work has finished,
        // even if the work is suspended.
        var nonIdlePendingLanes = pendingLanes & NonIdleLanes;
        if (nonIdlePendingLanes !== NoLanes) {
            var nonIdleUnblockedLanes = nonIdlePendingLanes & ~suspendedLanes;
            if (nonIdleUnblockedLanes !== NoLanes) nextLanes = getHighestPriorityLanes(nonIdleUnblockedLanes);
            else {
                var nonIdlePingedLanes = nonIdlePendingLanes & pingedLanes;
                if (nonIdlePingedLanes !== NoLanes) nextLanes = getHighestPriorityLanes(nonIdlePingedLanes);
            }
        } else {
            // The only remaining work is Idle.
            var unblockedLanes = pendingLanes & ~suspendedLanes;
            if (unblockedLanes !== NoLanes) nextLanes = getHighestPriorityLanes(unblockedLanes);
            else if (pingedLanes !== NoLanes) nextLanes = getHighestPriorityLanes(pingedLanes);
        }
        if (nextLanes === NoLanes) // This should only be reachable if we're suspended
        // TODO: Consider warning in this path if a fallback timer is not scheduled.
        return NoLanes;
         // If we're already in the middle of a render, switching lanes will interrupt
        // it and we'll lose our progress. We should only do this if the new lanes are
        // higher priority.
        if (wipLanes !== NoLanes && wipLanes !== nextLanes && // bother waiting until the root is complete.
        (wipLanes & suspendedLanes) === NoLanes) {
            var nextLane = getHighestPriorityLane(nextLanes);
            var wipLane = getHighestPriorityLane(wipLanes);
            if (// one. This works because the bits decrease in priority as you go left.
            nextLane >= wipLane || // only difference between default updates and transition updates is that
            // default updates do not support refresh transitions.
            nextLane === DefaultLane && (wipLane & TransitionLanes) !== NoLanes) // Keep working on the existing in-progress tree. Do not interrupt.
            return wipLanes;
        }
        if ((nextLanes & InputContinuousLane) !== NoLanes) // When updates are sync by default, we entangle continuous priority updates
        // and default updates, so they render in the same batch. The only reason
        // they use separate lanes is because continuous updates should interrupt
        // transitions, but default updates should not.
        nextLanes |= pendingLanes & DefaultLane;
         // Check for entangled lanes and add them to the batch.
        //
        // A lane is said to be entangled with another when it's not allowed to render
        // in a batch that does not also include the other lane. Typically we do this
        // when multiple updates have the same source, and we only want to respond to
        // the most recent event from that source.
        //
        // Note that we apply entanglements *after* checking for partial work above.
        // This means that if a lane is entangled during an interleaved event while
        // it's already rendering, we won't interrupt it. This is intentional, since
        // entanglement is usually "best effort": we'll try our best to render the
        // lanes in the same batch, but it's not worth throwing out partially
        // completed work in order to do it.
        // TODO: Reconsider this. The counter-argument is that the partial work
        // represents an intermediate state, which we don't want to show to the user.
        // And by spending extra time finishing it, we're increasing the amount of
        // time it takes to show the final state, which is what they are actually
        // waiting for.
        //
        // For those exceptions where entanglement is semantically important, like
        // useMutableSource, we should ensure that there is no partial work at the
        // time we apply the entanglement.
        var entangledLanes = root.entangledLanes;
        if (entangledLanes !== NoLanes) {
            var entanglements = root.entanglements;
            var lanes = nextLanes & entangledLanes;
            while(lanes > 0){
                var index = pickArbitraryLaneIndex(lanes);
                var lane = 1 << index;
                nextLanes |= entanglements[index];
                lanes &= ~lane;
            }
        }
        return nextLanes;
    }
    function getMostRecentEventTime(root, lanes) {
        var eventTimes = root.eventTimes;
        var mostRecentEventTime = NoTimestamp;
        while(lanes > 0){
            var index = pickArbitraryLaneIndex(lanes);
            var lane = 1 << index;
            var eventTime = eventTimes[index];
            if (eventTime > mostRecentEventTime) mostRecentEventTime = eventTime;
            lanes &= ~lane;
        }
        return mostRecentEventTime;
    }
    function computeExpirationTime(lane, currentTime) {
        switch(lane){
            case SyncLane:
            case InputContinuousHydrationLane:
            case InputContinuousLane:
                // User interactions should expire slightly more quickly.
                //
                // NOTE: This is set to the corresponding constant as in Scheduler.js.
                // When we made it larger, a product metric in www regressed, suggesting
                // there's a user interaction that's being starved by a series of
                // synchronous updates. If that theory is correct, the proper solution is
                // to fix the starvation. However, this scenario supports the idea that
                // expiration times are an important safeguard when starvation
                // does happen.
                return currentTime + 250;
            case DefaultHydrationLane:
            case DefaultLane:
            case TransitionHydrationLane:
            case TransitionLane1:
            case TransitionLane2:
            case TransitionLane3:
            case TransitionLane4:
            case TransitionLane5:
            case TransitionLane6:
            case TransitionLane7:
            case TransitionLane8:
            case TransitionLane9:
            case TransitionLane10:
            case TransitionLane11:
            case TransitionLane12:
            case TransitionLane13:
            case TransitionLane14:
            case TransitionLane15:
            case TransitionLane16:
                return currentTime + 5000;
            case RetryLane1:
            case RetryLane2:
            case RetryLane3:
            case RetryLane4:
            case RetryLane5:
                // TODO: Retries should be allowed to expire if they are CPU bound for
                // too long, but when I made this change it caused a spike in browser
                // crashes. There must be some other underlying bug; not super urgent but
                // ideally should figure out why and fix it. Unfortunately we don't have
                // a repro for the crashes, only detected via production metrics.
                return NoTimestamp;
            case SelectiveHydrationLane:
            case IdleHydrationLane:
            case IdleLane:
            case OffscreenLane:
                // Anything idle priority or lower should never expire.
                return NoTimestamp;
            default:
                error1('Should have found matching lanes. This is a bug in React.');
                return NoTimestamp;
        }
    }
    function markStarvedLanesAsExpired(root, currentTime) {
        // TODO: This gets called every time we yield. We can optimize by storing
        // the earliest expiration time on the root. Then use that to quickly bail out
        // of this function.
        var pendingLanes = root.pendingLanes;
        var suspendedLanes = root.suspendedLanes;
        var pingedLanes = root.pingedLanes;
        var expirationTimes = root.expirationTimes; // Iterate through the pending lanes and check if we've reached their
        // expiration time. If so, we'll assume the update is being starved and mark
        // it as expired to force it to finish.
        var lanes = pendingLanes;
        while(lanes > 0){
            var index = pickArbitraryLaneIndex(lanes);
            var lane = 1 << index;
            var expirationTime = expirationTimes[index];
            if (expirationTime === NoTimestamp) // Found a pending lane with no expiration time. If it's not suspended, or
            // if it's pinged, assume it's CPU-bound. Compute a new expiration time
            // using the current time.
            {
                if ((lane & suspendedLanes) === NoLanes || (lane & pingedLanes) !== NoLanes) // Assumes timestamps are monotonically increasing.
                expirationTimes[index] = computeExpirationTime(lane, currentTime);
            } else if (expirationTime <= currentTime) // This lane expired
            root.expiredLanes |= lane;
            lanes &= ~lane;
        }
    } // This returns the highest priority pending lanes regardless of whether they
    // are suspended.
    function getHighestPriorityPendingLanes(root) {
        return getHighestPriorityLanes(root.pendingLanes);
    }
    function getLanesToRetrySynchronouslyOnError(root) {
        var everythingButOffscreen = root.pendingLanes & ~OffscreenLane;
        if (everythingButOffscreen !== NoLanes) return everythingButOffscreen;
        if (everythingButOffscreen & OffscreenLane) return OffscreenLane;
        return NoLanes;
    }
    function includesSyncLane(lanes) {
        return (lanes & SyncLane) !== NoLanes;
    }
    function includesNonIdleWork(lanes) {
        return (lanes & NonIdleLanes) !== NoLanes;
    }
    function includesOnlyRetries(lanes) {
        return (lanes & RetryLanes) === lanes;
    }
    function includesOnlyNonUrgentLanes(lanes) {
        var UrgentLanes = SyncLane | InputContinuousLane | DefaultLane;
        return (lanes & UrgentLanes) === NoLanes;
    }
    function includesOnlyTransitions(lanes) {
        return (lanes & TransitionLanes) === lanes;
    }
    function includesBlockingLane(root, lanes) {
        var SyncDefaultLanes = InputContinuousHydrationLane | InputContinuousLane | DefaultHydrationLane | DefaultLane;
        return (lanes & SyncDefaultLanes) !== NoLanes;
    }
    function includesExpiredLane(root, lanes) {
        // This is a separate check from includesBlockingLane because a lane can
        // expire after a render has already started.
        return (lanes & root.expiredLanes) !== NoLanes;
    }
    function isTransitionLane(lane) {
        return (lane & TransitionLanes) !== NoLanes;
    }
    function claimNextTransitionLane() {
        // Cycle through the lanes, assigning each new transition to the next lane.
        // In most cases, this means every transition gets its own lane, until we
        // run out of lanes and cycle back to the beginning.
        var lane = nextTransitionLane;
        nextTransitionLane <<= 1;
        if ((nextTransitionLane & TransitionLanes) === NoLanes) nextTransitionLane = TransitionLane1;
        return lane;
    }
    function claimNextRetryLane() {
        var lane = nextRetryLane;
        nextRetryLane <<= 1;
        if ((nextRetryLane & RetryLanes) === NoLanes) nextRetryLane = RetryLane1;
        return lane;
    }
    function getHighestPriorityLane(lanes) {
        return lanes & -lanes;
    }
    function pickArbitraryLane(lanes) {
        // This wrapper function gets inlined. Only exists so to communicate that it
        // doesn't matter which bit is selected; you can pick any bit without
        // affecting the algorithms where its used. Here I'm using
        // getHighestPriorityLane because it requires the fewest operations.
        return getHighestPriorityLane(lanes);
    }
    function pickArbitraryLaneIndex(lanes) {
        return 31 - clz32(lanes);
    }
    function laneToIndex(lane) {
        return pickArbitraryLaneIndex(lane);
    }
    function includesSomeLane(a, b) {
        return (a & b) !== NoLanes;
    }
    function isSubsetOfLanes(set, subset) {
        return (set & subset) === subset;
    }
    function mergeLanes(a, b) {
        return a | b;
    }
    function removeLanes(set, subset) {
        return set & ~subset;
    }
    function intersectLanes(a, b) {
        return a & b;
    } // Seems redundant, but it changes the type from a single lane (used for
    // updates) to a group of lanes (used for flushing work).
    function laneToLanes(lane) {
        return lane;
    }
    function higherPriorityLane(a, b) {
        // This works because the bit ranges decrease in priority as you go left.
        return a !== NoLane && a < b ? a : b;
    }
    function createLaneMap(initial) {
        // Intentionally pushing one by one.
        // https://v8.dev/blog/elements-kinds#avoid-creating-holes
        var laneMap = [];
        for(var i = 0; i < TotalLanes; i++)laneMap.push(initial);
        return laneMap;
    }
    function markRootUpdated(root, updateLane, eventTime) {
        root.pendingLanes |= updateLane; // If there are any suspended transitions, it's possible this new update
        // could unblock them. Clear the suspended lanes so that we can try rendering
        // them again.
        //
        // TODO: We really only need to unsuspend only lanes that are in the
        // `subtreeLanes` of the updated fiber, or the update lanes of the return
        // path. This would exclude suspended updates in an unrelated sibling tree,
        // since there's no way for this update to unblock it.
        //
        // We don't do this if the incoming update is idle, because we never process
        // idle updates until after all the regular updates have finished; there's no
        // way it could unblock a transition.
        if (updateLane !== IdleLane) {
            root.suspendedLanes = NoLanes;
            root.pingedLanes = NoLanes;
        }
        var eventTimes = root.eventTimes;
        var index = laneToIndex(updateLane); // We can always overwrite an existing timestamp because we prefer the most
        // recent event, and we assume time is monotonically increasing.
        eventTimes[index] = eventTime;
    }
    function markRootSuspended(root, suspendedLanes) {
        root.suspendedLanes |= suspendedLanes;
        root.pingedLanes &= ~suspendedLanes; // The suspended lanes are no longer CPU-bound. Clear their expiration times.
        var expirationTimes = root.expirationTimes;
        var lanes = suspendedLanes;
        while(lanes > 0){
            var index = pickArbitraryLaneIndex(lanes);
            var lane = 1 << index;
            expirationTimes[index] = NoTimestamp;
            lanes &= ~lane;
        }
    }
    function markRootPinged(root, pingedLanes, eventTime) {
        root.pingedLanes |= root.suspendedLanes & pingedLanes;
    }
    function markRootFinished(root, remainingLanes) {
        var noLongerPendingLanes = root.pendingLanes & ~remainingLanes;
        root.pendingLanes = remainingLanes; // Let's try everything again
        root.suspendedLanes = NoLanes;
        root.pingedLanes = NoLanes;
        root.expiredLanes &= remainingLanes;
        root.mutableReadLanes &= remainingLanes;
        root.entangledLanes &= remainingLanes;
        var entanglements = root.entanglements;
        var eventTimes = root.eventTimes;
        var expirationTimes = root.expirationTimes; // Clear the lanes that no longer have pending work
        var lanes = noLongerPendingLanes;
        while(lanes > 0){
            var index = pickArbitraryLaneIndex(lanes);
            var lane = 1 << index;
            entanglements[index] = NoLanes;
            eventTimes[index] = NoTimestamp;
            expirationTimes[index] = NoTimestamp;
            lanes &= ~lane;
        }
    }
    function markRootEntangled(root, entangledLanes) {
        // In addition to entangling each of the given lanes with each other, we also
        // have to consider _transitive_ entanglements. For each lane that is already
        // entangled with *any* of the given lanes, that lane is now transitively
        // entangled with *all* the given lanes.
        //
        // Translated: If C is entangled with A, then entangling A with B also
        // entangles C with B.
        //
        // If this is hard to grasp, it might help to intentionally break this
        // function and look at the tests that fail in ReactTransition-test.js. Try
        // commenting out one of the conditions below.
        var rootEntangledLanes = root.entangledLanes |= entangledLanes;
        var entanglements = root.entanglements;
        var lanes = rootEntangledLanes;
        while(lanes){
            var index = pickArbitraryLaneIndex(lanes);
            var lane = 1 << index;
            if (lane & entangledLanes | entanglements[index] & entangledLanes) entanglements[index] |= entangledLanes;
            lanes &= ~lane;
        }
    }
    function getBumpedLaneForHydration(root, renderLanes) {
        var renderLane = getHighestPriorityLane(renderLanes);
        var lane;
        switch(renderLane){
            case InputContinuousLane:
                lane = InputContinuousHydrationLane;
                break;
            case DefaultLane:
                lane = DefaultHydrationLane;
                break;
            case TransitionLane1:
            case TransitionLane2:
            case TransitionLane3:
            case TransitionLane4:
            case TransitionLane5:
            case TransitionLane6:
            case TransitionLane7:
            case TransitionLane8:
            case TransitionLane9:
            case TransitionLane10:
            case TransitionLane11:
            case TransitionLane12:
            case TransitionLane13:
            case TransitionLane14:
            case TransitionLane15:
            case TransitionLane16:
            case RetryLane1:
            case RetryLane2:
            case RetryLane3:
            case RetryLane4:
            case RetryLane5:
                lane = TransitionHydrationLane;
                break;
            case IdleLane:
                lane = IdleHydrationLane;
                break;
            default:
                // Everything else is already either a hydration lane, or shouldn't
                // be retried at a hydration lane.
                lane = NoLane;
                break;
        } // Check if the lane we chose is suspended. If so, that indicates that we
        // already attempted and failed to hydrate at that level. Also check if we're
        // already rendering that lane, which is rare but could happen.
        if ((lane & (root.suspendedLanes | renderLanes)) !== NoLane) // Give up trying to hydrate and fall back to client render.
        return NoLane;
        return lane;
    }
    function addFiberToLanesMap(root, fiber, lanes) {
        if (!isDevToolsPresent) return;
        var pendingUpdatersLaneMap = root.pendingUpdatersLaneMap;
        while(lanes > 0){
            var index = laneToIndex(lanes);
            var lane = 1 << index;
            var updaters = pendingUpdatersLaneMap[index];
            updaters.add(fiber);
            lanes &= ~lane;
        }
    }
    function movePendingFibersToMemoized(root, lanes) {
        if (!isDevToolsPresent) return;
        var pendingUpdatersLaneMap = root.pendingUpdatersLaneMap;
        var memoizedUpdaters = root.memoizedUpdaters;
        while(lanes > 0){
            var index = laneToIndex(lanes);
            var lane = 1 << index;
            var updaters = pendingUpdatersLaneMap[index];
            if (updaters.size > 0) {
                updaters.forEach(function(fiber) {
                    var alternate = fiber.alternate;
                    if (alternate === null || !memoizedUpdaters.has(alternate)) memoizedUpdaters.add(fiber);
                });
                updaters.clear();
            }
            lanes &= ~lane;
        }
    }
    function getTransitionsForLanes(root, lanes) {
        return null;
    }
    var DiscreteEventPriority = SyncLane;
    var ContinuousEventPriority = InputContinuousLane;
    var DefaultEventPriority = DefaultLane;
    var IdleEventPriority = IdleLane;
    var currentUpdatePriority = NoLane;
    function getCurrentUpdatePriority() {
        return currentUpdatePriority;
    }
    function setCurrentUpdatePriority(newPriority) {
        currentUpdatePriority = newPriority;
    }
    function runWithPriority(priority, fn) {
        var previousPriority = currentUpdatePriority;
        try {
            currentUpdatePriority = priority;
            return fn();
        } finally{
            currentUpdatePriority = previousPriority;
        }
    }
    function higherEventPriority(a, b) {
        return a !== 0 && a < b ? a : b;
    }
    function lowerEventPriority(a, b) {
        return a === 0 || a > b ? a : b;
    }
    function isHigherEventPriority(a, b) {
        return a !== 0 && a < b;
    }
    function lanesToEventPriority(lanes) {
        var lane = getHighestPriorityLane(lanes);
        if (!isHigherEventPriority(DiscreteEventPriority, lane)) return DiscreteEventPriority;
        if (!isHigherEventPriority(ContinuousEventPriority, lane)) return ContinuousEventPriority;
        if (includesNonIdleWork(lane)) return DefaultEventPriority;
        return IdleEventPriority;
    }
    // This is imported by the event replaying implementation in React DOM. It's
    // in a separate file to break a circular dependency between the renderer and
    // the reconciler.
    function isRootDehydrated(root) {
        var currentState = root.current.memoizedState;
        return currentState.isDehydrated;
    }
    var _attemptSynchronousHydration;
    function setAttemptSynchronousHydration(fn) {
        _attemptSynchronousHydration = fn;
    }
    function attemptSynchronousHydration(fiber) {
        _attemptSynchronousHydration(fiber);
    }
    var attemptContinuousHydration;
    function setAttemptContinuousHydration(fn) {
        attemptContinuousHydration = fn;
    }
    var attemptHydrationAtCurrentPriority;
    function setAttemptHydrationAtCurrentPriority(fn) {
        attemptHydrationAtCurrentPriority = fn;
    }
    var getCurrentUpdatePriority$1;
    function setGetCurrentUpdatePriority(fn) {
        getCurrentUpdatePriority$1 = fn;
    }
    var attemptHydrationAtPriority;
    function setAttemptHydrationAtPriority(fn) {
        attemptHydrationAtPriority = fn;
    } // TODO: Upgrade this definition once we're on a newer version of Flow that
    // has this definition built-in.
    var hasScheduledReplayAttempt = false; // The queue of discrete events to be replayed.
    var queuedDiscreteEvents = []; // Indicates if any continuous event targets are non-null for early bailout.
    // if the last target was dehydrated.
    var queuedFocus = null;
    var queuedDrag = null;
    var queuedMouse = null; // For pointer events there can be one latest event per pointerId.
    var queuedPointers = new Map();
    var queuedPointerCaptures = new Map(); // We could consider replaying selectionchange and touchmoves too.
    var queuedExplicitHydrationTargets = [];
    var discreteReplayableEvents = [
        'mousedown',
        'mouseup',
        'touchcancel',
        'touchend',
        'touchstart',
        'auxclick',
        'dblclick',
        'pointercancel',
        'pointerdown',
        'pointerup',
        'dragend',
        'dragstart',
        'drop',
        'compositionend',
        'compositionstart',
        'keydown',
        'keypress',
        'keyup',
        'input',
        'textInput',
        'copy',
        'cut',
        'paste',
        'click',
        'change',
        'contextmenu',
        'reset',
        'submit'
    ];
    function isDiscreteEventThatRequiresHydration(eventType) {
        return discreteReplayableEvents.indexOf(eventType) > -1;
    }
    function createQueuedReplayableEvent(blockedOn, domEventName, eventSystemFlags, targetContainer, nativeEvent) {
        return {
            blockedOn: blockedOn,
            domEventName: domEventName,
            eventSystemFlags: eventSystemFlags,
            nativeEvent: nativeEvent,
            targetContainers: [
                targetContainer
            ]
        };
    }
    function clearIfContinuousEvent(domEventName, nativeEvent) {
        switch(domEventName){
            case 'focusin':
            case 'focusout':
                queuedFocus = null;
                break;
            case 'dragenter':
            case 'dragleave':
                queuedDrag = null;
                break;
            case 'mouseover':
            case 'mouseout':
                queuedMouse = null;
                break;
            case 'pointerover':
            case 'pointerout':
                var pointerId = nativeEvent.pointerId;
                queuedPointers.delete(pointerId);
                break;
            case 'gotpointercapture':
            case 'lostpointercapture':
                var _pointerId = nativeEvent.pointerId;
                queuedPointerCaptures.delete(_pointerId);
                break;
        }
    }
    function accumulateOrCreateContinuousQueuedReplayableEvent(existingQueuedEvent, blockedOn, domEventName, eventSystemFlags, targetContainer, nativeEvent) {
        if (existingQueuedEvent === null || existingQueuedEvent.nativeEvent !== nativeEvent) {
            var queuedEvent = createQueuedReplayableEvent(blockedOn, domEventName, eventSystemFlags, targetContainer, nativeEvent);
            if (blockedOn !== null) {
                var _fiber2 = getInstanceFromNode(blockedOn);
                if (_fiber2 !== null) // Attempt to increase the priority of this target.
                attemptContinuousHydration(_fiber2);
            }
            return queuedEvent;
        } // If we have already queued this exact event, then it's because
        // the different event systems have different DOM event listeners.
        // We can accumulate the flags, and the targetContainers, and
        // store a single event to be replayed.
        existingQueuedEvent.eventSystemFlags |= eventSystemFlags;
        var targetContainers = existingQueuedEvent.targetContainers;
        if (targetContainer !== null && targetContainers.indexOf(targetContainer) === -1) targetContainers.push(targetContainer);
        return existingQueuedEvent;
    }
    function queueIfContinuousEvent(blockedOn, domEventName, eventSystemFlags, targetContainer, nativeEvent) {
        // These set relatedTarget to null because the replayed event will be treated as if we
        // moved from outside the window (no target) onto the target once it hydrates.
        // Instead of mutating we could clone the event.
        switch(domEventName){
            case 'focusin':
                var focusEvent = nativeEvent;
                queuedFocus = accumulateOrCreateContinuousQueuedReplayableEvent(queuedFocus, blockedOn, domEventName, eventSystemFlags, targetContainer, focusEvent);
                return true;
            case 'dragenter':
                var dragEvent = nativeEvent;
                queuedDrag = accumulateOrCreateContinuousQueuedReplayableEvent(queuedDrag, blockedOn, domEventName, eventSystemFlags, targetContainer, dragEvent);
                return true;
            case 'mouseover':
                var mouseEvent = nativeEvent;
                queuedMouse = accumulateOrCreateContinuousQueuedReplayableEvent(queuedMouse, blockedOn, domEventName, eventSystemFlags, targetContainer, mouseEvent);
                return true;
            case 'pointerover':
                var pointerEvent = nativeEvent;
                var pointerId = pointerEvent.pointerId;
                queuedPointers.set(pointerId, accumulateOrCreateContinuousQueuedReplayableEvent(queuedPointers.get(pointerId) || null, blockedOn, domEventName, eventSystemFlags, targetContainer, pointerEvent));
                return true;
            case 'gotpointercapture':
                var _pointerEvent = nativeEvent;
                var _pointerId2 = _pointerEvent.pointerId;
                queuedPointerCaptures.set(_pointerId2, accumulateOrCreateContinuousQueuedReplayableEvent(queuedPointerCaptures.get(_pointerId2) || null, blockedOn, domEventName, eventSystemFlags, targetContainer, _pointerEvent));
                return true;
        }
        return false;
    } // Check if this target is unblocked. Returns true if it's unblocked.
    function attemptExplicitHydrationTarget(queuedTarget) {
        // TODO: This function shares a lot of logic with findInstanceBlockingEvent.
        // Try to unify them. It's a bit tricky since it would require two return
        // values.
        var targetInst = getClosestInstanceFromNode(queuedTarget.target);
        if (targetInst !== null) {
            var nearestMounted = getNearestMountedFiber(targetInst);
            if (nearestMounted !== null) {
                var tag = nearestMounted.tag;
                if (tag === SuspenseComponent) {
                    var instance = getSuspenseInstanceFromFiber(nearestMounted);
                    if (instance !== null) {
                        // We're blocked on hydrating this boundary.
                        // Increase its priority.
                        queuedTarget.blockedOn = instance;
                        attemptHydrationAtPriority(queuedTarget.priority, function() {
                            attemptHydrationAtCurrentPriority(nearestMounted);
                        });
                        return;
                    }
                } else if (tag === HostRoot) {
                    var root = nearestMounted.stateNode;
                    if (isRootDehydrated(root)) {
                        queuedTarget.blockedOn = getContainerFromFiber(nearestMounted); // We don't currently have a way to increase the priority of
                        // a root other than sync.
                        return;
                    }
                }
            }
        }
        queuedTarget.blockedOn = null;
    }
    function queueExplicitHydrationTarget(target) {
        // TODO: This will read the priority if it's dispatched by the React
        // event system but not native events. Should read window.event.type, like
        // we do for updates (getCurrentEventPriority).
        var updatePriority = getCurrentUpdatePriority$1();
        var queuedTarget = {
            blockedOn: null,
            target: target,
            priority: updatePriority
        };
        var i = 0;
        for(; i < queuedExplicitHydrationTargets.length; i++){
            // Stop once we hit the first target with lower priority than
            if (!isHigherEventPriority(updatePriority, queuedExplicitHydrationTargets[i].priority)) break;
        }
        queuedExplicitHydrationTargets.splice(i, 0, queuedTarget);
        if (i === 0) attemptExplicitHydrationTarget(queuedTarget);
    }
    function attemptReplayContinuousQueuedEvent(queuedEvent) {
        if (queuedEvent.blockedOn !== null) return false;
        var targetContainers = queuedEvent.targetContainers;
        while(targetContainers.length > 0){
            var targetContainer = targetContainers[0];
            var nextBlockedOn = findInstanceBlockingEvent(queuedEvent.domEventName, queuedEvent.eventSystemFlags, targetContainer, queuedEvent.nativeEvent);
            if (nextBlockedOn === null) {
                var nativeEvent = queuedEvent.nativeEvent;
                var nativeEventClone = new nativeEvent.constructor(nativeEvent.type, nativeEvent);
                setReplayingEvent(nativeEventClone);
                nativeEvent.target.dispatchEvent(nativeEventClone);
                resetReplayingEvent();
            } else {
                // We're still blocked. Try again later.
                var _fiber3 = getInstanceFromNode(nextBlockedOn);
                if (_fiber3 !== null) attemptContinuousHydration(_fiber3);
                queuedEvent.blockedOn = nextBlockedOn;
                return false;
            } // This target container was successfully dispatched. Try the next.
            targetContainers.shift();
        }
        return true;
    }
    function attemptReplayContinuousQueuedEventInMap(queuedEvent, key, map) {
        if (attemptReplayContinuousQueuedEvent(queuedEvent)) map.delete(key);
    }
    function replayUnblockedEvents() {
        hasScheduledReplayAttempt = false;
        if (queuedFocus !== null && attemptReplayContinuousQueuedEvent(queuedFocus)) queuedFocus = null;
        if (queuedDrag !== null && attemptReplayContinuousQueuedEvent(queuedDrag)) queuedDrag = null;
        if (queuedMouse !== null && attemptReplayContinuousQueuedEvent(queuedMouse)) queuedMouse = null;
        queuedPointers.forEach(attemptReplayContinuousQueuedEventInMap);
        queuedPointerCaptures.forEach(attemptReplayContinuousQueuedEventInMap);
    }
    function scheduleCallbackIfUnblocked(queuedEvent, unblocked) {
        if (queuedEvent.blockedOn === unblocked) {
            queuedEvent.blockedOn = null;
            if (!hasScheduledReplayAttempt) {
                hasScheduledReplayAttempt = true; // Schedule a callback to attempt replaying as many events as are
                // now unblocked. This first might not actually be unblocked yet.
                // We could check it early to avoid scheduling an unnecessary callback.
                Scheduler.unstable_scheduleCallback(Scheduler.unstable_NormalPriority, replayUnblockedEvents);
            }
        }
    }
    function retryIfBlockedOn(unblocked) {
        // Mark anything that was blocked on this as no longer blocked
        // and eligible for a replay.
        if (queuedDiscreteEvents.length > 0) {
            scheduleCallbackIfUnblocked(queuedDiscreteEvents[0], unblocked); // This is a exponential search for each boundary that commits. I think it's
            // worth it because we expect very few discrete events to queue up and once
            // we are actually fully unblocked it will be fast to replay them.
            for(var i = 1; i < queuedDiscreteEvents.length; i++){
                var queuedEvent = queuedDiscreteEvents[i];
                if (queuedEvent.blockedOn === unblocked) queuedEvent.blockedOn = null;
            }
        }
        if (queuedFocus !== null) scheduleCallbackIfUnblocked(queuedFocus, unblocked);
        if (queuedDrag !== null) scheduleCallbackIfUnblocked(queuedDrag, unblocked);
        if (queuedMouse !== null) scheduleCallbackIfUnblocked(queuedMouse, unblocked);
        var unblock = function(queuedEvent) {
            return scheduleCallbackIfUnblocked(queuedEvent, unblocked);
        };
        queuedPointers.forEach(unblock);
        queuedPointerCaptures.forEach(unblock);
        for(var _i = 0; _i < queuedExplicitHydrationTargets.length; _i++){
            var queuedTarget = queuedExplicitHydrationTargets[_i];
            if (queuedTarget.blockedOn === unblocked) queuedTarget.blockedOn = null;
        }
        while(queuedExplicitHydrationTargets.length > 0){
            var nextExplicitTarget = queuedExplicitHydrationTargets[0];
            if (nextExplicitTarget.blockedOn !== null) break;
            else {
                attemptExplicitHydrationTarget(nextExplicitTarget);
                if (nextExplicitTarget.blockedOn === null) // We're unblocked.
                queuedExplicitHydrationTargets.shift();
            }
        }
    }
    var ReactCurrentBatchConfig = ReactSharedInternals.ReactCurrentBatchConfig; // TODO: can we stop exporting these?
    var _enabled = true; // This is exported in FB builds for use by legacy FB layer infra.
    // We'd like to remove this but it's not clear if this is safe.
    function setEnabled(enabled) {
        _enabled = !!enabled;
    }
    function isEnabled() {
        return _enabled;
    }
    function createEventListenerWrapperWithPriority(targetContainer, domEventName, eventSystemFlags) {
        var eventPriority = getEventPriority(domEventName);
        var listenerWrapper;
        switch(eventPriority){
            case DiscreteEventPriority:
                listenerWrapper = dispatchDiscreteEvent;
                break;
            case ContinuousEventPriority:
                listenerWrapper = dispatchContinuousEvent;
                break;
            case DefaultEventPriority:
            default:
                listenerWrapper = dispatchEvent;
                break;
        }
        return listenerWrapper.bind(null, domEventName, eventSystemFlags, targetContainer);
    }
    function dispatchDiscreteEvent(domEventName, eventSystemFlags, container, nativeEvent) {
        var previousPriority = getCurrentUpdatePriority();
        var prevTransition = ReactCurrentBatchConfig.transition;
        ReactCurrentBatchConfig.transition = null;
        try {
            setCurrentUpdatePriority(DiscreteEventPriority);
            dispatchEvent(domEventName, eventSystemFlags, container, nativeEvent);
        } finally{
            setCurrentUpdatePriority(previousPriority);
            ReactCurrentBatchConfig.transition = prevTransition;
        }
    }
    function dispatchContinuousEvent(domEventName, eventSystemFlags, container, nativeEvent) {
        var previousPriority = getCurrentUpdatePriority();
        var prevTransition = ReactCurrentBatchConfig.transition;
        ReactCurrentBatchConfig.transition = null;
        try {
            setCurrentUpdatePriority(ContinuousEventPriority);
            dispatchEvent(domEventName, eventSystemFlags, container, nativeEvent);
        } finally{
            setCurrentUpdatePriority(previousPriority);
            ReactCurrentBatchConfig.transition = prevTransition;
        }
    }
    function dispatchEvent(domEventName, eventSystemFlags, targetContainer, nativeEvent) {
        if (!_enabled) return;
        dispatchEventWithEnableCapturePhaseSelectiveHydrationWithoutDiscreteEventReplay(domEventName, eventSystemFlags, targetContainer, nativeEvent);
    }
    function dispatchEventWithEnableCapturePhaseSelectiveHydrationWithoutDiscreteEventReplay(domEventName, eventSystemFlags, targetContainer, nativeEvent) {
        var blockedOn = findInstanceBlockingEvent(domEventName, eventSystemFlags, targetContainer, nativeEvent);
        if (blockedOn === null) {
            dispatchEventForPluginEventSystem(domEventName, eventSystemFlags, nativeEvent, return_targetInst, targetContainer);
            clearIfContinuousEvent(domEventName, nativeEvent);
            return;
        }
        if (queueIfContinuousEvent(blockedOn, domEventName, eventSystemFlags, targetContainer, nativeEvent)) {
            nativeEvent.stopPropagation();
            return;
        } // We need to clear only if we didn't queue because
        // queueing is accumulative.
        clearIfContinuousEvent(domEventName, nativeEvent);
        if (eventSystemFlags & IS_CAPTURE_PHASE && isDiscreteEventThatRequiresHydration(domEventName)) {
            while(blockedOn !== null){
                var fiber = getInstanceFromNode(blockedOn);
                if (fiber !== null) attemptSynchronousHydration(fiber);
                var nextBlockedOn = findInstanceBlockingEvent(domEventName, eventSystemFlags, targetContainer, nativeEvent);
                if (nextBlockedOn === null) dispatchEventForPluginEventSystem(domEventName, eventSystemFlags, nativeEvent, return_targetInst, targetContainer);
                if (nextBlockedOn === blockedOn) break;
                blockedOn = nextBlockedOn;
            }
            if (blockedOn !== null) nativeEvent.stopPropagation();
            return;
        } // This is not replayable so we'll invoke it but without a target,
        // in case the event system needs to trace it.
        dispatchEventForPluginEventSystem(domEventName, eventSystemFlags, nativeEvent, null, targetContainer);
    }
    var return_targetInst = null; // Returns a SuspenseInstance or Container if it's blocked.
    // The return_targetInst field above is conceptually part of the return value.
    function findInstanceBlockingEvent(domEventName, eventSystemFlags, targetContainer, nativeEvent) {
        // TODO: Warn if _enabled is false.
        return_targetInst = null;
        var nativeEventTarget = getEventTarget(nativeEvent);
        var targetInst = getClosestInstanceFromNode(nativeEventTarget);
        if (targetInst !== null) {
            var nearestMounted = getNearestMountedFiber(targetInst);
            if (nearestMounted === null) // This tree has been unmounted already. Dispatch without a target.
            targetInst = null;
            else {
                var tag = nearestMounted.tag;
                if (tag === SuspenseComponent) {
                    var instance = getSuspenseInstanceFromFiber(nearestMounted);
                    if (instance !== null) // Queue the event to be replayed later. Abort dispatching since we
                    // don't want this event dispatched twice through the event system.
                    // TODO: If this is the first discrete event in the queue. Schedule an increased
                    // priority for this boundary.
                    return instance;
                     // This shouldn't happen, something went wrong but to avoid blocking
                    // the whole system, dispatch the event without a target.
                    // TODO: Warn.
                    targetInst = null;
                } else if (tag === HostRoot) {
                    var root = nearestMounted.stateNode;
                    if (isRootDehydrated(root)) // If this happens during a replay something went wrong and it might block
                    // the whole system.
                    return getContainerFromFiber(nearestMounted);
                    targetInst = null;
                } else if (nearestMounted !== targetInst) // If we get an event (ex: img onload) before committing that
                // component's mount, ignore it for now (that is, treat it as if it was an
                // event on a non-React tree). We might also consider queueing events and
                // dispatching them after the mount.
                targetInst = null;
            }
        }
        return_targetInst = targetInst; // We're not blocked on anything.
        return null;
    }
    function getEventPriority(domEventName) {
        switch(domEventName){
            // Used by SimpleEventPlugin:
            case 'cancel':
            case 'click':
            case 'close':
            case 'contextmenu':
            case 'copy':
            case 'cut':
            case 'auxclick':
            case 'dblclick':
            case 'dragend':
            case 'dragstart':
            case 'drop':
            case 'focusin':
            case 'focusout':
            case 'input':
            case 'invalid':
            case 'keydown':
            case 'keypress':
            case 'keyup':
            case 'mousedown':
            case 'mouseup':
            case 'paste':
            case 'pause':
            case 'play':
            case 'pointercancel':
            case 'pointerdown':
            case 'pointerup':
            case 'ratechange':
            case 'reset':
            case 'resize':
            case 'seeked':
            case 'submit':
            case 'touchcancel':
            case 'touchend':
            case 'touchstart':
            case 'volumechange':
            // eslint-disable-next-line no-fallthrough
            case 'change':
            case 'selectionchange':
            case 'textInput':
            case 'compositionstart':
            case 'compositionend':
            case 'compositionupdate':
            // eslint-disable-next-line no-fallthrough
            case 'beforeblur':
            case 'afterblur':
            // eslint-disable-next-line no-fallthrough
            case 'beforeinput':
            case 'blur':
            case 'fullscreenchange':
            case 'focus':
            case 'hashchange':
            case 'popstate':
            case 'select':
            case 'selectstart':
                return DiscreteEventPriority;
            case 'drag':
            case 'dragenter':
            case 'dragexit':
            case 'dragleave':
            case 'dragover':
            case 'mousemove':
            case 'mouseout':
            case 'mouseover':
            case 'pointermove':
            case 'pointerout':
            case 'pointerover':
            case 'scroll':
            case 'toggle':
            case 'touchmove':
            case 'wheel':
            // eslint-disable-next-line no-fallthrough
            case 'mouseenter':
            case 'mouseleave':
            case 'pointerenter':
            case 'pointerleave':
                return ContinuousEventPriority;
            case 'message':
                // We might be in the Scheduler callback.
                // Eventually this mechanism will be replaced by a check
                // of the current priority on the native scheduler.
                var schedulerPriority = getCurrentPriorityLevel();
                switch(schedulerPriority){
                    case ImmediatePriority:
                        return DiscreteEventPriority;
                    case UserBlockingPriority:
                        return ContinuousEventPriority;
                    case NormalPriority:
                    case LowPriority:
                        // TODO: Handle LowSchedulerPriority, somehow. Maybe the same lane as hydration.
                        return DefaultEventPriority;
                    case IdlePriority:
                        return IdleEventPriority;
                    default:
                        return DefaultEventPriority;
                }
            default:
                return DefaultEventPriority;
        }
    }
    function addEventBubbleListener(target, eventType, listener) {
        target.addEventListener(eventType, listener, false);
        return listener;
    }
    function addEventCaptureListener(target, eventType, listener) {
        target.addEventListener(eventType, listener, true);
        return listener;
    }
    function addEventCaptureListenerWithPassiveFlag(target, eventType, listener, passive) {
        target.addEventListener(eventType, listener, {
            capture: true,
            passive: passive
        });
        return listener;
    }
    function addEventBubbleListenerWithPassiveFlag(target, eventType, listener, passive) {
        target.addEventListener(eventType, listener, {
            passive: passive
        });
        return listener;
    }
    /**
 * These variables store information about text content of a target node,
 * allowing comparison of content before and after a given event.
 *
 * Identify the node where selection currently begins, then observe
 * both its text content and its current position in the DOM. Since the
 * browser may natively replace the target node during composition, we can
 * use its position to find its replacement.
 *
 *
 */ var root1 = null;
    var startText = null;
    var fallbackText = null;
    function initialize(nativeEventTarget) {
        root1 = nativeEventTarget;
        startText = getText();
        return true;
    }
    function reset() {
        root1 = null;
        startText = null;
        fallbackText = null;
    }
    function getData() {
        if (fallbackText) return fallbackText;
        var start;
        var startValue = startText;
        var startLength = startValue.length;
        var end;
        var endValue = getText();
        var endLength = endValue.length;
        for(start = 0; start < startLength; start++){
            if (startValue[start] !== endValue[start]) break;
        }
        var minEnd = startLength - start;
        for(end = 1; end <= minEnd; end++){
            if (startValue[startLength - end] !== endValue[endLength - end]) break;
        }
        var sliceTail = end > 1 ? 1 - end : undefined;
        fallbackText = endValue.slice(start, sliceTail);
        return fallbackText;
    }
    function getText() {
        if ('value' in root1) return root1.value;
        return root1.textContent;
    }
    /**
 * `charCode` represents the actual "character code" and is safe to use with
 * `String.fromCharCode`. As such, only keys that correspond to printable
 * characters produce a valid `charCode`, the only exception to this is Enter.
 * The Tab-key is considered non-printable and does not have a `charCode`,
 * presumably because it does not produce a tab-character in browsers.
 *
 * @param {object} nativeEvent Native browser event.
 * @return {number} Normalized `charCode` property.
 */ function getEventCharCode(nativeEvent) {
        var charCode;
        var keyCode = nativeEvent.keyCode;
        if ('charCode' in nativeEvent) {
            charCode = nativeEvent.charCode; // FF does not set `charCode` for the Enter-key, check against `keyCode`.
            if (charCode === 0 && keyCode === 13) charCode = 13;
        } else // IE8 does not implement `charCode`, but `keyCode` has the correct value.
        charCode = keyCode;
         // IE and Edge (on Windows) and Chrome / Safari (on Windows and Linux)
        // report Enter as charCode 10 when ctrl is pressed.
        if (charCode === 10) charCode = 13;
         // Some non-printable keys are reported in `charCode`/`keyCode`, discard them.
        // Must not discard the (non-)printable Enter-key.
        if (charCode >= 32 || charCode === 13) return charCode;
        return 0;
    }
    function functionThatReturnsTrue() {
        return true;
    }
    function functionThatReturnsFalse() {
        return false;
    } // This is intentionally a factory so that we have different returned constructors.
    // If we had a single constructor, it would be megamorphic and engines would deopt.
    function createSyntheticEvent(Interface) {
        /**
   * Synthetic events are dispatched by event plugins, typically in response to a
   * top-level event delegation handler.
   *
   * These systems should generally use pooling to reduce the frequency of garbage
   * collection. The system should check `isPersistent` to determine whether the
   * event should be released into the pool after being dispatched. Users that
   * need a persisted event should invoke `persist`.
   *
   * Synthetic events (and subclasses) implement the DOM Level 3 Events API by
   * normalizing browser quirks. Subclasses do not necessarily have to implement a
   * DOM interface; custom application-specific events can also subclass this.
   */ function SyntheticBaseEvent(reactName, reactEventType, targetInst, nativeEvent, nativeEventTarget) {
            this._reactName = reactName;
            this._targetInst = targetInst;
            this.type = reactEventType;
            this.nativeEvent = nativeEvent;
            this.target = nativeEventTarget;
            this.currentTarget = null;
            for(var _propName in Interface){
                if (!Interface.hasOwnProperty(_propName)) continue;
                var normalize = Interface[_propName];
                if (normalize) this[_propName] = normalize(nativeEvent);
                else this[_propName] = nativeEvent[_propName];
            }
            var defaultPrevented = nativeEvent.defaultPrevented != null ? nativeEvent.defaultPrevented : nativeEvent.returnValue === false;
            if (defaultPrevented) this.isDefaultPrevented = functionThatReturnsTrue;
            else this.isDefaultPrevented = functionThatReturnsFalse;
            this.isPropagationStopped = functionThatReturnsFalse;
            return this;
        }
        assign(SyntheticBaseEvent.prototype, {
            preventDefault: function() {
                this.defaultPrevented = true;
                var event = this.nativeEvent;
                if (!event) return;
                if (event.preventDefault) event.preventDefault(); // $FlowFixMe - flow is not aware of `unknown` in IE
                else if (typeof event.returnValue !== 'unknown') event.returnValue = false;
                this.isDefaultPrevented = functionThatReturnsTrue;
            },
            stopPropagation: function() {
                var event = this.nativeEvent;
                if (!event) return;
                if (event.stopPropagation) event.stopPropagation(); // $FlowFixMe - flow is not aware of `unknown` in IE
                else if (typeof event.cancelBubble !== 'unknown') // The ChangeEventPlugin registers a "propertychange" event for
                // IE. This event does not support bubbling or cancelling, and
                // any references to cancelBubble throw "Member not found".  A
                // typeof check of "unknown" circumvents this issue (and is also
                // IE specific).
                event.cancelBubble = true;
                this.isPropagationStopped = functionThatReturnsTrue;
            },
            /**
     * We release all dispatched `SyntheticEvent`s after each event loop, adding
     * them back into the pool. This allows a way to hold onto a reference that
     * won't be added back into the pool.
     */ persist: function() {},
            /**
     * Checks if this event should be released back into the pool.
     *
     * @return {boolean} True if this should not be released, false otherwise.
     */ isPersistent: functionThatReturnsTrue
        });
        return SyntheticBaseEvent;
    }
    /**
 * @interface Event
 * @see http://www.w3.org/TR/DOM-Level-3-Events/
 */ var EventInterface = {
        eventPhase: 0,
        bubbles: 0,
        cancelable: 0,
        timeStamp: function(event) {
            return event.timeStamp || Date.now();
        },
        defaultPrevented: 0,
        isTrusted: 0
    };
    var SyntheticEvent = createSyntheticEvent(EventInterface);
    var UIEventInterface = assign({}, EventInterface, {
        view: 0,
        detail: 0
    });
    var SyntheticUIEvent = createSyntheticEvent(UIEventInterface);
    var lastMovementX;
    var lastMovementY;
    var lastMouseEvent;
    function updateMouseMovementPolyfillState(event) {
        if (event !== lastMouseEvent) {
            if (lastMouseEvent && event.type === 'mousemove') {
                lastMovementX = event.screenX - lastMouseEvent.screenX;
                lastMovementY = event.screenY - lastMouseEvent.screenY;
            } else {
                lastMovementX = 0;
                lastMovementY = 0;
            }
            lastMouseEvent = event;
        }
    }
    /**
 * @interface MouseEvent
 * @see http://www.w3.org/TR/DOM-Level-3-Events/
 */ var MouseEventInterface = assign({}, UIEventInterface, {
        screenX: 0,
        screenY: 0,
        clientX: 0,
        clientY: 0,
        pageX: 0,
        pageY: 0,
        ctrlKey: 0,
        shiftKey: 0,
        altKey: 0,
        metaKey: 0,
        getModifierState: getEventModifierState,
        button: 0,
        buttons: 0,
        relatedTarget: function(event) {
            if (event.relatedTarget === undefined) return event.fromElement === event.srcElement ? event.toElement : event.fromElement;
            return event.relatedTarget;
        },
        movementX: function(event) {
            if ('movementX' in event) return event.movementX;
            updateMouseMovementPolyfillState(event);
            return lastMovementX;
        },
        movementY: function(event) {
            if ('movementY' in event) return event.movementY;
             // Don't need to call updateMouseMovementPolyfillState() here
            // because it's guaranteed to have already run when movementX
            // was copied.
            return lastMovementY;
        }
    });
    var SyntheticMouseEvent = createSyntheticEvent(MouseEventInterface);
    /**
 * @interface DragEvent
 * @see http://www.w3.org/TR/DOM-Level-3-Events/
 */ var DragEventInterface = assign({}, MouseEventInterface, {
        dataTransfer: 0
    });
    var SyntheticDragEvent = createSyntheticEvent(DragEventInterface);
    /**
 * @interface FocusEvent
 * @see http://www.w3.org/TR/DOM-Level-3-Events/
 */ var FocusEventInterface = assign({}, UIEventInterface, {
        relatedTarget: 0
    });
    var SyntheticFocusEvent = createSyntheticEvent(FocusEventInterface);
    /**
 * @interface Event
 * @see http://www.w3.org/TR/css3-animations/#AnimationEvent-interface
 * @see https://developer.mozilla.org/en-US/docs/Web/API/AnimationEvent
 */ var AnimationEventInterface = assign({}, EventInterface, {
        animationName: 0,
        elapsedTime: 0,
        pseudoElement: 0
    });
    var SyntheticAnimationEvent = createSyntheticEvent(AnimationEventInterface);
    /**
 * @interface Event
 * @see http://www.w3.org/TR/clipboard-apis/
 */ var ClipboardEventInterface = assign({}, EventInterface, {
        clipboardData: function(event) {
            return 'clipboardData' in event ? event.clipboardData : window.clipboardData;
        }
    });
    var SyntheticClipboardEvent = createSyntheticEvent(ClipboardEventInterface);
    /**
 * @interface Event
 * @see http://www.w3.org/TR/DOM-Level-3-Events/#events-compositionevents
 */ var CompositionEventInterface = assign({}, EventInterface, {
        data: 0
    });
    var SyntheticCompositionEvent = createSyntheticEvent(CompositionEventInterface);
    /**
 * @interface Event
 * @see http://www.w3.org/TR/2013/WD-DOM-Level-3-Events-20131105
 *      /#events-inputevents
 */ // Happens to share the same list for now.
    var SyntheticInputEvent = SyntheticCompositionEvent;
    /**
 * Normalization of deprecated HTML5 `key` values
 * @see https://developer.mozilla.org/en-US/docs/Web/API/KeyboardEvent#Key_names
 */ var normalizeKey = {
        Esc: 'Escape',
        Spacebar: ' ',
        Left: 'ArrowLeft',
        Up: 'ArrowUp',
        Right: 'ArrowRight',
        Down: 'ArrowDown',
        Del: 'Delete',
        Win: 'OS',
        Menu: 'ContextMenu',
        Apps: 'ContextMenu',
        Scroll: 'ScrollLock',
        MozPrintableKey: 'Unidentified'
    };
    /**
 * Translation from legacy `keyCode` to HTML5 `key`
 * Only special keys supported, all others depend on keyboard layout or browser
 * @see https://developer.mozilla.org/en-US/docs/Web/API/KeyboardEvent#Key_names
 */ var translateToKey = {
        '8': 'Backspace',
        '9': 'Tab',
        '12': 'Clear',
        '13': 'Enter',
        '16': 'Shift',
        '17': 'Control',
        '18': 'Alt',
        '19': 'Pause',
        '20': 'CapsLock',
        '27': 'Escape',
        '32': ' ',
        '33': 'PageUp',
        '34': 'PageDown',
        '35': 'End',
        '36': 'Home',
        '37': 'ArrowLeft',
        '38': 'ArrowUp',
        '39': 'ArrowRight',
        '40': 'ArrowDown',
        '45': 'Insert',
        '46': 'Delete',
        '112': 'F1',
        '113': 'F2',
        '114': 'F3',
        '115': 'F4',
        '116': 'F5',
        '117': 'F6',
        '118': 'F7',
        '119': 'F8',
        '120': 'F9',
        '121': 'F10',
        '122': 'F11',
        '123': 'F12',
        '144': 'NumLock',
        '145': 'ScrollLock',
        '224': 'Meta'
    };
    /**
 * @param {object} nativeEvent Native browser event.
 * @return {string} Normalized `key` property.
 */ function getEventKey(nativeEvent) {
        if (nativeEvent.key) {
            // Normalize inconsistent values reported by browsers due to
            // implementations of a working draft specification.
            // FireFox implements `key` but returns `MozPrintableKey` for all
            // printable characters (normalized to `Unidentified`), ignore it.
            var key = normalizeKey[nativeEvent.key] || nativeEvent.key;
            if (key !== 'Unidentified') return key;
        } // Browser does not implement `key`, polyfill as much of it as we can.
        if (nativeEvent.type === 'keypress') {
            var charCode = getEventCharCode(nativeEvent); // The enter-key is technically both printable and non-printable and can
            // thus be captured by `keypress`, no other non-printable key should.
            return charCode === 13 ? 'Enter' : String.fromCharCode(charCode);
        }
        if (nativeEvent.type === 'keydown' || nativeEvent.type === 'keyup') // While user keyboard layout determines the actual meaning of each
        // `keyCode` value, almost all function keys have a universal value.
        return translateToKey[nativeEvent.keyCode] || 'Unidentified';
        return '';
    }
    /**
 * Translation from modifier key to the associated property in the event.
 * @see http://www.w3.org/TR/DOM-Level-3-Events/#keys-Modifiers
 */ var modifierKeyToProp = {
        Alt: 'altKey',
        Control: 'ctrlKey',
        Meta: 'metaKey',
        Shift: 'shiftKey'
    }; // Older browsers (Safari <= 10, iOS Safari <= 10.2) do not support
    // getModifierState. If getModifierState is not supported, we map it to a set of
    // modifier keys exposed by the event. In this case, Lock-keys are not supported.
    function modifierStateGetter(keyArg) {
        var syntheticEvent = this;
        var nativeEvent = syntheticEvent.nativeEvent;
        if (nativeEvent.getModifierState) return nativeEvent.getModifierState(keyArg);
        var keyProp = modifierKeyToProp[keyArg];
        return keyProp ? !!nativeEvent[keyProp] : false;
    }
    function getEventModifierState(nativeEvent) {
        return modifierStateGetter;
    }
    /**
 * @interface KeyboardEvent
 * @see http://www.w3.org/TR/DOM-Level-3-Events/
 */ var KeyboardEventInterface = assign({}, UIEventInterface, {
        key: getEventKey,
        code: 0,
        location: 0,
        ctrlKey: 0,
        shiftKey: 0,
        altKey: 0,
        metaKey: 0,
        repeat: 0,
        locale: 0,
        getModifierState: getEventModifierState,
        // Legacy Interface
        charCode: function(event) {
            // `charCode` is the result of a KeyPress event and represents the value of
            // the actual printable character.
            // KeyPress is deprecated, but its replacement is not yet final and not
            // implemented in any major browser. Only KeyPress has charCode.
            if (event.type === 'keypress') return getEventCharCode(event);
            return 0;
        },
        keyCode: function(event) {
            // `keyCode` is the result of a KeyDown/Up event and represents the value of
            // physical keyboard key.
            // The actual meaning of the value depends on the users' keyboard layout
            // which cannot be detected. Assuming that it is a US keyboard layout
            // provides a surprisingly accurate mapping for US and European users.
            // Due to this, it is left to the user to implement at this time.
            if (event.type === 'keydown' || event.type === 'keyup') return event.keyCode;
            return 0;
        },
        which: function(event) {
            // `which` is an alias for either `keyCode` or `charCode` depending on the
            // type of the event.
            if (event.type === 'keypress') return getEventCharCode(event);
            if (event.type === 'keydown' || event.type === 'keyup') return event.keyCode;
            return 0;
        }
    });
    var SyntheticKeyboardEvent = createSyntheticEvent(KeyboardEventInterface);
    /**
 * @interface PointerEvent
 * @see http://www.w3.org/TR/pointerevents/
 */ var PointerEventInterface = assign({}, MouseEventInterface, {
        pointerId: 0,
        width: 0,
        height: 0,
        pressure: 0,
        tangentialPressure: 0,
        tiltX: 0,
        tiltY: 0,
        twist: 0,
        pointerType: 0,
        isPrimary: 0
    });
    var SyntheticPointerEvent = createSyntheticEvent(PointerEventInterface);
    /**
 * @interface TouchEvent
 * @see http://www.w3.org/TR/touch-events/
 */ var TouchEventInterface = assign({}, UIEventInterface, {
        touches: 0,
        targetTouches: 0,
        changedTouches: 0,
        altKey: 0,
        metaKey: 0,
        ctrlKey: 0,
        shiftKey: 0,
        getModifierState: getEventModifierState
    });
    var SyntheticTouchEvent = createSyntheticEvent(TouchEventInterface);
    /**
 * @interface Event
 * @see http://www.w3.org/TR/2009/WD-css3-transitions-20090320/#transition-events-
 * @see https://developer.mozilla.org/en-US/docs/Web/API/TransitionEvent
 */ var TransitionEventInterface = assign({}, EventInterface, {
        propertyName: 0,
        elapsedTime: 0,
        pseudoElement: 0
    });
    var SyntheticTransitionEvent = createSyntheticEvent(TransitionEventInterface);
    /**
 * @interface WheelEvent
 * @see http://www.w3.org/TR/DOM-Level-3-Events/
 */ var WheelEventInterface = assign({}, MouseEventInterface, {
        deltaX: function(event) {
            return 'deltaX' in event ? event.deltaX : 'wheelDeltaX' in event ? -event.wheelDeltaX : 0;
        },
        deltaY: function(event) {
            return 'deltaY' in event ? event.deltaY : 'wheelDeltaY' in event ? -event.wheelDeltaY : 'wheelDelta' in event ? -event.wheelDelta : 0;
        },
        deltaZ: 0,
        // Browsers without "deltaMode" is reporting in raw wheel delta where one
        // notch on the scroll is always +/- 120, roughly equivalent to pixels.
        // A good approximation of DOM_DELTA_LINE (1) is 5% of viewport size or
        // ~40 pixels, for DOM_DELTA_SCREEN (2) it is 87.5% of viewport size.
        deltaMode: 0
    });
    var SyntheticWheelEvent = createSyntheticEvent(WheelEventInterface);
    var END_KEYCODES = [
        9,
        13,
        27,
        32
    ]; // Tab, Return, Esc, Space
    var START_KEYCODE = 229;
    var canUseCompositionEvent = canUseDOM && 'CompositionEvent' in window;
    var documentMode = null;
    if (canUseDOM && 'documentMode' in document) documentMode = document.documentMode;
     // Webkit offers a very useful `textInput` event that can be used to
    // directly represent `beforeInput`. The IE `textinput` event is not as
    // useful, so we don't use it.
    var canUseTextInputEvent = canUseDOM && 'TextEvent' in window && !documentMode; // In IE9+, we have access to composition events, but the data supplied
    // by the native compositionend event may be incorrect. Japanese ideographic
    // spaces, for instance (\u3000) are not recorded correctly.
    var useFallbackCompositionData = canUseDOM && (!canUseCompositionEvent || documentMode && documentMode > 8 && documentMode <= 11);
    var SPACEBAR_CODE = 32;
    var SPACEBAR_CHAR = String.fromCharCode(SPACEBAR_CODE);
    function registerEvents() {
        registerTwoPhaseEvent('onBeforeInput', [
            'compositionend',
            'keypress',
            'textInput',
            'paste'
        ]);
        registerTwoPhaseEvent('onCompositionEnd', [
            'compositionend',
            'focusout',
            'keydown',
            'keypress',
            'keyup',
            'mousedown'
        ]);
        registerTwoPhaseEvent('onCompositionStart', [
            'compositionstart',
            'focusout',
            'keydown',
            'keypress',
            'keyup',
            'mousedown'
        ]);
        registerTwoPhaseEvent('onCompositionUpdate', [
            'compositionupdate',
            'focusout',
            'keydown',
            'keypress',
            'keyup',
            'mousedown'
        ]);
    } // Track whether we've ever handled a keypress on the space key.
    var hasSpaceKeypress = false;
    /**
 * Return whether a native keypress event is assumed to be a command.
 * This is required because Firefox fires `keypress` events for key commands
 * (cut, copy, select-all, etc.) even though no character is inserted.
 */ function isKeypressCommand(nativeEvent) {
        return (nativeEvent.ctrlKey || nativeEvent.altKey || nativeEvent.metaKey) && !(nativeEvent.ctrlKey && nativeEvent.altKey);
    }
    /**
 * Translate native top level events into event types.
 */ function getCompositionEventType(domEventName) {
        switch(domEventName){
            case 'compositionstart':
                return 'onCompositionStart';
            case 'compositionend':
                return 'onCompositionEnd';
            case 'compositionupdate':
                return 'onCompositionUpdate';
        }
    }
    /**
 * Does our fallback best-guess model think this event signifies that
 * composition has begun?
 */ function isFallbackCompositionStart(domEventName, nativeEvent) {
        return domEventName === 'keydown' && nativeEvent.keyCode === START_KEYCODE;
    }
    /**
 * Does our fallback mode think that this event is the end of composition?
 */ function isFallbackCompositionEnd(domEventName, nativeEvent) {
        switch(domEventName){
            case 'keyup':
                // Command keys insert or clear IME input.
                return END_KEYCODES.indexOf(nativeEvent.keyCode) !== -1;
            case 'keydown':
                // Expect IME keyCode on each keydown. If we get any other
                // code we must have exited earlier.
                return nativeEvent.keyCode !== START_KEYCODE;
            case 'keypress':
            case 'mousedown':
            case 'focusout':
                // Events are not possible without cancelling IME.
                return true;
            default:
                return false;
        }
    }
    /**
 * Google Input Tools provides composition data via a CustomEvent,
 * with the `data` property populated in the `detail` object. If this
 * is available on the event object, use it. If not, this is a plain
 * composition event and we have nothing special to extract.
 *
 * @param {object} nativeEvent
 * @return {?string}
 */ function getDataFromCustomEvent(nativeEvent) {
        var detail = nativeEvent.detail;
        if (typeof detail === 'object' && 'data' in detail) return detail.data;
        return null;
    }
    /**
 * Check if a composition event was triggered by Korean IME.
 * Our fallback mode does not work well with IE's Korean IME,
 * so just use native composition events when Korean IME is used.
 * Although CompositionEvent.locale property is deprecated,
 * it is available in IE, where our fallback mode is enabled.
 *
 * @param {object} nativeEvent
 * @return {boolean}
 */ function isUsingKoreanIME(nativeEvent) {
        return nativeEvent.locale === 'ko';
    } // Track the current IME composition status, if any.
    var isComposing = false;
    /**
 * @return {?object} A SyntheticCompositionEvent.
 */ function extractCompositionEvent(dispatchQueue, domEventName, targetInst, nativeEvent, nativeEventTarget) {
        var eventType;
        var fallbackData;
        if (canUseCompositionEvent) eventType = getCompositionEventType(domEventName);
        else if (!isComposing) {
            if (isFallbackCompositionStart(domEventName, nativeEvent)) eventType = 'onCompositionStart';
        } else if (isFallbackCompositionEnd(domEventName, nativeEvent)) eventType = 'onCompositionEnd';
        if (!eventType) return null;
        if (useFallbackCompositionData && !isUsingKoreanIME(nativeEvent)) {
            // The current composition is stored statically and must not be
            // overwritten while composition continues.
            if (!isComposing && eventType === 'onCompositionStart') isComposing = initialize(nativeEventTarget);
            else if (eventType === 'onCompositionEnd') {
                if (isComposing) fallbackData = getData();
            }
        }
        var listeners = accumulateTwoPhaseListeners(targetInst, eventType);
        if (listeners.length > 0) {
            var event = new SyntheticCompositionEvent(eventType, domEventName, null, nativeEvent, nativeEventTarget);
            dispatchQueue.push({
                event: event,
                listeners: listeners
            });
            if (fallbackData) // Inject data generated from fallback path into the synthetic event.
            // This matches the property of native CompositionEventInterface.
            event.data = fallbackData;
            else {
                var customData = getDataFromCustomEvent(nativeEvent);
                if (customData !== null) event.data = customData;
            }
        }
    }
    function getNativeBeforeInputChars(domEventName, nativeEvent) {
        switch(domEventName){
            case 'compositionend':
                return getDataFromCustomEvent(nativeEvent);
            case 'keypress':
                /**
       * If native `textInput` events are available, our goal is to make
       * use of them. However, there is a special case: the spacebar key.
       * In Webkit, preventing default on a spacebar `textInput` event
       * cancels character insertion, but it *also* causes the browser
       * to fall back to its default spacebar behavior of scrolling the
       * page.
       *
       * Tracking at:
       * https://code.google.com/p/chromium/issues/detail?id=355103
       *
       * To avoid this issue, use the keypress event as if no `textInput`
       * event is available.
       */ var which = nativeEvent.which;
                if (which !== SPACEBAR_CODE) return null;
                hasSpaceKeypress = true;
                return SPACEBAR_CHAR;
            case 'textInput':
                // Record the characters to be added to the DOM.
                var chars = nativeEvent.data; // If it's a spacebar character, assume that we have already handled
                // it at the keypress level and bail immediately. Android Chrome
                // doesn't give us keycodes, so we need to ignore it.
                if (chars === SPACEBAR_CHAR && hasSpaceKeypress) return null;
                return chars;
            default:
                // For other native event types, do nothing.
                return null;
        }
    }
    /**
 * For browsers that do not provide the `textInput` event, extract the
 * appropriate string to use for SyntheticInputEvent.
 */ function getFallbackBeforeInputChars(domEventName, nativeEvent) {
        // If we are currently composing (IME) and using a fallback to do so,
        // try to extract the composed characters from the fallback object.
        // If composition event is available, we extract a string only at
        // compositionevent, otherwise extract it at fallback events.
        if (isComposing) {
            if (domEventName === 'compositionend' || !canUseCompositionEvent && isFallbackCompositionEnd(domEventName, nativeEvent)) {
                var chars = getData();
                reset();
                isComposing = false;
                return chars;
            }
            return null;
        }
        switch(domEventName){
            case 'paste':
                // If a paste event occurs after a keypress, throw out the input
                // chars. Paste events should not lead to BeforeInput events.
                return null;
            case 'keypress':
                /**
       * As of v27, Firefox may fire keypress events even when no character
       * will be inserted. A few possibilities:
       *
       * - `which` is `0`. Arrow keys, Esc key, etc.
       *
       * - `which` is the pressed key code, but no char is available.
       *   Ex: 'AltGr + d` in Polish. There is no modified character for
       *   this key combination and no character is inserted into the
       *   document, but FF fires the keypress for char code `100` anyway.
       *   No `input` event will occur.
       *
       * - `which` is the pressed key code, but a command combination is
       *   being used. Ex: `Cmd+C`. No character is inserted, and no
       *   `input` event will occur.
       */ if (!isKeypressCommand(nativeEvent)) {
                    // IE fires the `keypress` event when a user types an emoji via
                    // Touch keyboard of Windows.  In such a case, the `char` property
                    // holds an emoji character like `\uD83D\uDE0A`.  Because its length
                    // is 2, the property `which` does not represent an emoji correctly.
                    // In such a case, we directly return the `char` property instead of
                    // using `which`.
                    if (nativeEvent.char && nativeEvent.char.length > 1) return nativeEvent.char;
                    else if (nativeEvent.which) return String.fromCharCode(nativeEvent.which);
                }
                return null;
            case 'compositionend':
                return useFallbackCompositionData && !isUsingKoreanIME(nativeEvent) ? null : nativeEvent.data;
            default:
                return null;
        }
    }
    /**
 * Extract a SyntheticInputEvent for `beforeInput`, based on either native
 * `textInput` or fallback behavior.
 *
 * @return {?object} A SyntheticInputEvent.
 */ function extractBeforeInputEvent(dispatchQueue, domEventName, targetInst, nativeEvent, nativeEventTarget) {
        var chars;
        if (canUseTextInputEvent) chars = getNativeBeforeInputChars(domEventName, nativeEvent);
        else chars = getFallbackBeforeInputChars(domEventName, nativeEvent);
         // If no characters are being inserted, no BeforeInput event should
        // be fired.
        if (!chars) return null;
        var listeners = accumulateTwoPhaseListeners(targetInst, 'onBeforeInput');
        if (listeners.length > 0) {
            var event = new SyntheticInputEvent('onBeforeInput', 'beforeinput', null, nativeEvent, nativeEventTarget);
            dispatchQueue.push({
                event: event,
                listeners: listeners
            });
            event.data = chars;
        }
    }
    /**
 * Create an `onBeforeInput` event to match
 * http://www.w3.org/TR/2013/WD-DOM-Level-3-Events-20131105/#events-inputevents.
 *
 * This event plugin is based on the native `textInput` event
 * available in Chrome, Safari, Opera, and IE. This event fires after
 * `onKeyPress` and `onCompositionEnd`, but before `onInput`.
 *
 * `beforeInput` is spec'd but not implemented in any browsers, and
 * the `input` event does not provide any useful information about what has
 * actually been added, contrary to the spec. Thus, `textInput` is the best
 * available event to identify the characters that have actually been inserted
 * into the target node.
 *
 * This plugin is also responsible for emitting `composition` events, thus
 * allowing us to share composition fallback code for both `beforeInput` and
 * `composition` event types.
 */ function extractEvents(dispatchQueue, domEventName, targetInst, nativeEvent, nativeEventTarget, eventSystemFlags, targetContainer) {
        extractCompositionEvent(dispatchQueue, domEventName, targetInst, nativeEvent, nativeEventTarget);
        extractBeforeInputEvent(dispatchQueue, domEventName, targetInst, nativeEvent, nativeEventTarget);
    }
    /**
 * @see http://www.whatwg.org/specs/web-apps/current-work/multipage/the-input-element.html#input-type-attr-summary
 */ var supportedInputTypes = {
        color: true,
        date: true,
        datetime: true,
        'datetime-local': true,
        email: true,
        month: true,
        number: true,
        password: true,
        range: true,
        search: true,
        tel: true,
        text: true,
        time: true,
        url: true,
        week: true
    };
    function isTextInputElement(elem) {
        var nodeName = elem && elem.nodeName && elem.nodeName.toLowerCase();
        if (nodeName === 'input') return !!supportedInputTypes[elem.type];
        if (nodeName === 'textarea') return true;
        return false;
    }
    /**
 * Checks if an event is supported in the current execution environment.
 *
 * NOTE: This will not work correctly for non-generic events such as `change`,
 * `reset`, `load`, `error`, and `select`.
 *
 * Borrows from Modernizr.
 *
 * @param {string} eventNameSuffix Event name, e.g. "click".
 * @return {boolean} True if the event is supported.
 * @internal
 * @license Modernizr 3.0.0pre (Custom Build) | MIT
 */ function isEventSupported(eventNameSuffix) {
        if (!canUseDOM) return false;
        var eventName = 'on' + eventNameSuffix;
        var isSupported = eventName in document;
        if (!isSupported) {
            var element = document.createElement('div');
            element.setAttribute(eventName, 'return;');
            isSupported = typeof element[eventName] === 'function';
        }
        return isSupported;
    }
    function registerEvents$1() {
        registerTwoPhaseEvent('onChange', [
            'change',
            'click',
            'focusin',
            'focusout',
            'input',
            'keydown',
            'keyup',
            'selectionchange'
        ]);
    }
    function createAndAccumulateChangeEvent(dispatchQueue, inst, nativeEvent, target) {
        // Flag this event loop as needing state restore.
        enqueueStateRestore(target);
        var listeners = accumulateTwoPhaseListeners(inst, 'onChange');
        if (listeners.length > 0) {
            var event = new SyntheticEvent('onChange', 'change', null, nativeEvent, target);
            dispatchQueue.push({
                event: event,
                listeners: listeners
            });
        }
    }
    /**
 * For IE shims
 */ var activeElement = null;
    var activeElementInst = null;
    /**
 * SECTION: handle `change` event
 */ function shouldUseChangeEvent(elem) {
        var nodeName = elem.nodeName && elem.nodeName.toLowerCase();
        return nodeName === 'select' || nodeName === 'input' && elem.type === 'file';
    }
    function manualDispatchChangeEvent(nativeEvent) {
        var dispatchQueue = [];
        createAndAccumulateChangeEvent(dispatchQueue, activeElementInst, nativeEvent, getEventTarget(nativeEvent)); // If change and propertychange bubbled, we'd just bind to it like all the
        // other events and have it go through ReactBrowserEventEmitter. Since it
        // doesn't, we manually listen for the events and so we have to enqueue and
        // process the abstract event manually.
        //
        // Batching is necessary here in order to ensure that all event handlers run
        // before the next rerender (including event handlers attached to ancestor
        // elements instead of directly on the input). Without this, controlled
        // components don't work properly in conjunction with event bubbling because
        // the component is rerendered and the value reverted before all the event
        // handlers can run. See https://github.com/facebook/react/issues/708.
        batchedUpdates(runEventInBatch, dispatchQueue);
    }
    function runEventInBatch(dispatchQueue) {
        processDispatchQueue(dispatchQueue, 0);
    }
    function getInstIfValueChanged(targetInst) {
        var targetNode = getNodeFromInstance(targetInst);
        if (updateValueIfChanged(targetNode)) return targetInst;
    }
    function getTargetInstForChangeEvent(domEventName, targetInst) {
        if (domEventName === 'change') return targetInst;
    }
    /**
 * SECTION: handle `input` event
 */ var isInputEventSupported = false;
    if (canUseDOM) // IE9 claims to support the input event but fails to trigger it when
    // deleting text, so we ignore its input events.
    isInputEventSupported = isEventSupported('input') && (!document.documentMode || document.documentMode > 9);
    /**
 * (For IE <=9) Starts tracking propertychange events on the passed-in element
 * and override the value property so that we can distinguish user events from
 * value changes in JS.
 */ function startWatchingForValueChange(target, targetInst) {
        activeElement = target;
        activeElementInst = targetInst;
        activeElement.attachEvent('onpropertychange', handlePropertyChange);
    }
    /**
 * (For IE <=9) Removes the event listeners from the currently-tracked element,
 * if any exists.
 */ function stopWatchingForValueChange() {
        if (!activeElement) return;
        activeElement.detachEvent('onpropertychange', handlePropertyChange);
        activeElement = null;
        activeElementInst = null;
    }
    /**
 * (For IE <=9) Handles a propertychange event, sending a `change` event if
 * the value of the active element has changed.
 */ function handlePropertyChange(nativeEvent) {
        if (nativeEvent.propertyName !== 'value') return;
        if (getInstIfValueChanged(activeElementInst)) manualDispatchChangeEvent(nativeEvent);
    }
    function handleEventsForInputEventPolyfill(domEventName, target, targetInst) {
        if (domEventName === 'focusin') {
            // In IE9, propertychange fires for most input events but is buggy and
            // doesn't fire when text is deleted, but conveniently, selectionchange
            // appears to fire in all of the remaining cases so we catch those and
            // forward the event if the value has changed
            // In either case, we don't want to call the event handler if the value
            // is changed from JS so we redefine a setter for `.value` that updates
            // our activeElementValue variable, allowing us to ignore those changes
            //
            // stopWatching() should be a noop here but we call it just in case we
            // missed a blur event somehow.
            stopWatchingForValueChange();
            startWatchingForValueChange(target, targetInst);
        } else if (domEventName === 'focusout') stopWatchingForValueChange();
    } // For IE8 and IE9.
    function getTargetInstForInputEventPolyfill(domEventName, targetInst) {
        if (domEventName === 'selectionchange' || domEventName === 'keyup' || domEventName === 'keydown') // On the selectionchange event, the target is just document which isn't
        // helpful for us so just check activeElement instead.
        //
        // 99% of the time, keydown and keyup aren't necessary. IE8 fails to fire
        // propertychange on the first input event after setting `value` from a
        // script and fires only keydown, keypress, keyup. Catching keyup usually
        // gets it and catching keydown lets us fire an event for the first
        // keystroke if user does a key repeat (it'll be a little delayed: right
        // before the second keystroke). Other input methods (e.g., paste) seem to
        // fire selectionchange normally.
        return getInstIfValueChanged(activeElementInst);
    }
    /**
 * SECTION: handle `click` event
 */ function shouldUseClickEvent(elem) {
        // Use the `click` event to detect changes to checkbox and radio inputs.
        // This approach works across all browsers, whereas `change` does not fire
        // until `blur` in IE8.
        var nodeName = elem.nodeName;
        return nodeName && nodeName.toLowerCase() === 'input' && (elem.type === 'checkbox' || elem.type === 'radio');
    }
    function getTargetInstForClickEvent(domEventName, targetInst) {
        if (domEventName === 'click') return getInstIfValueChanged(targetInst);
    }
    function getTargetInstForInputOrChangeEvent(domEventName, targetInst) {
        if (domEventName === 'input' || domEventName === 'change') return getInstIfValueChanged(targetInst);
    }
    function handleControlledInputBlur(node) {
        var state = node._wrapperState;
        if (!state || !state.controlled || node.type !== 'number') return;
        // If controlled, assign the value attribute to the current value on blur
        setDefaultValue(node, 'number', node.value);
    }
    /**
 * This plugin creates an `onChange` event that normalizes change events
 * across form elements. This event fires at a time when it's possible to
 * change the element's value without seeing a flicker.
 *
 * Supported elements are:
 * - input (see `isTextInputElement`)
 * - textarea
 * - select
 */ function extractEvents$1(dispatchQueue, domEventName, targetInst, nativeEvent, nativeEventTarget, eventSystemFlags, targetContainer) {
        var targetNode = targetInst ? getNodeFromInstance(targetInst) : window;
        var getTargetInstFunc, handleEventFunc;
        if (shouldUseChangeEvent(targetNode)) getTargetInstFunc = getTargetInstForChangeEvent;
        else if (isTextInputElement(targetNode)) {
            if (isInputEventSupported) getTargetInstFunc = getTargetInstForInputOrChangeEvent;
            else {
                getTargetInstFunc = getTargetInstForInputEventPolyfill;
                handleEventFunc = handleEventsForInputEventPolyfill;
            }
        } else if (shouldUseClickEvent(targetNode)) getTargetInstFunc = getTargetInstForClickEvent;
        if (getTargetInstFunc) {
            var inst = getTargetInstFunc(domEventName, targetInst);
            if (inst) {
                createAndAccumulateChangeEvent(dispatchQueue, inst, nativeEvent, nativeEventTarget);
                return;
            }
        }
        if (handleEventFunc) handleEventFunc(domEventName, targetNode, targetInst);
         // When blurring, set the value attribute for number inputs
        if (domEventName === 'focusout') handleControlledInputBlur(targetNode);
    }
    function registerEvents$2() {
        registerDirectEvent('onMouseEnter', [
            'mouseout',
            'mouseover'
        ]);
        registerDirectEvent('onMouseLeave', [
            'mouseout',
            'mouseover'
        ]);
        registerDirectEvent('onPointerEnter', [
            'pointerout',
            'pointerover'
        ]);
        registerDirectEvent('onPointerLeave', [
            'pointerout',
            'pointerover'
        ]);
    }
    /**
 * For almost every interaction we care about, there will be both a top-level
 * `mouseover` and `mouseout` event that occurs. Only use `mouseout` so that
 * we do not extract duplicate events. However, moving the mouse into the
 * browser from outside will not fire a `mouseout` event. In this case, we use
 * the `mouseover` top-level event.
 */ function extractEvents$2(dispatchQueue, domEventName, targetInst, nativeEvent, nativeEventTarget, eventSystemFlags, targetContainer) {
        var isOverEvent = domEventName === 'mouseover' || domEventName === 'pointerover';
        var isOutEvent = domEventName === 'mouseout' || domEventName === 'pointerout';
        if (isOverEvent && !isReplayingEvent(nativeEvent)) {
            // If this is an over event with a target, we might have already dispatched
            // the event in the out event of the other target. If this is replayed,
            // then it's because we couldn't dispatch against this target previously
            // so we have to do it now instead.
            var related = nativeEvent.relatedTarget || nativeEvent.fromElement;
            if (related) {
                // If the related node is managed by React, we can assume that we have
                // already dispatched the corresponding events during its mouseout.
                if (getClosestInstanceFromNode(related) || isContainerMarkedAsRoot(related)) return;
            }
        }
        if (!isOutEvent && !isOverEvent) // Must not be a mouse or pointer in or out - ignoring.
        return;
        var win; // TODO: why is this nullable in the types but we read from it?
        if (nativeEventTarget.window === nativeEventTarget) // `nativeEventTarget` is probably a window object.
        win = nativeEventTarget;
        else {
            // TODO: Figure out why `ownerDocument` is sometimes undefined in IE8.
            var doc = nativeEventTarget.ownerDocument;
            if (doc) win = doc.defaultView || doc.parentWindow;
            else win = window;
        }
        var from;
        var to;
        if (isOutEvent) {
            var _related = nativeEvent.relatedTarget || nativeEvent.toElement;
            from = targetInst;
            to = _related ? getClosestInstanceFromNode(_related) : null;
            if (to !== null) {
                var nearestMounted = getNearestMountedFiber(to);
                if (to !== nearestMounted || to.tag !== HostComponent && to.tag !== HostText) to = null;
            }
        } else {
            // Moving to a node from outside the window.
            from = null;
            to = targetInst;
        }
        if (from === to) // Nothing pertains to our managed components.
        return;
        var SyntheticEventCtor = SyntheticMouseEvent;
        var leaveEventType = 'onMouseLeave';
        var enterEventType = 'onMouseEnter';
        var eventTypePrefix = 'mouse';
        if (domEventName === 'pointerout' || domEventName === 'pointerover') {
            SyntheticEventCtor = SyntheticPointerEvent;
            leaveEventType = 'onPointerLeave';
            enterEventType = 'onPointerEnter';
            eventTypePrefix = 'pointer';
        }
        var fromNode = from == null ? win : getNodeFromInstance(from);
        var toNode = to == null ? win : getNodeFromInstance(to);
        var leave = new SyntheticEventCtor(leaveEventType, eventTypePrefix + 'leave', from, nativeEvent, nativeEventTarget);
        leave.target = fromNode;
        leave.relatedTarget = toNode;
        var enter = null; // We should only process this nativeEvent if we are processing
        // the first ancestor. Next time, we will ignore the event.
        var nativeTargetInst = getClosestInstanceFromNode(nativeEventTarget);
        if (nativeTargetInst === targetInst) {
            var enterEvent = new SyntheticEventCtor(enterEventType, eventTypePrefix + 'enter', to, nativeEvent, nativeEventTarget);
            enterEvent.target = toNode;
            enterEvent.relatedTarget = fromNode;
            enter = enterEvent;
        }
        accumulateEnterLeaveTwoPhaseListeners(dispatchQueue, leave, enter, from, to);
    }
    /**
 * inlined Object.is polyfill to avoid requiring consumers ship their own
 * https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Object/is
 */ function is(x, y) {
        return x === y && (x !== 0 || 1 / x === 1 / y) || x !== x && y !== y // eslint-disable-line no-self-compare
        ;
    }
    var objectIs = typeof Object.is === 'function' ? Object.is : is;
    /**
 * Performs equality by iterating through keys on an object and returning false
 * when any key has values which are not strictly equal between the arguments.
 * Returns true when the values of all keys are strictly equal.
 */ function shallowEqual(objA, objB) {
        if (objectIs(objA, objB)) return true;
        if (typeof objA !== 'object' || objA === null || typeof objB !== 'object' || objB === null) return false;
        var keysA = Object.keys(objA);
        var keysB = Object.keys(objB);
        if (keysA.length !== keysB.length) return false;
         // Test for A's keys different from B.
        for(var i = 0; i < keysA.length; i++){
            var currentKey = keysA[i];
            if (!hasOwnProperty.call(objB, currentKey) || !objectIs(objA[currentKey], objB[currentKey])) return false;
        }
        return true;
    }
    /**
 * Given any node return the first leaf node without children.
 *
 * @param {DOMElement|DOMTextNode} node
 * @return {DOMElement|DOMTextNode}
 */ function getLeafNode(node) {
        while(node && node.firstChild)node = node.firstChild;
        return node;
    }
    /**
 * Get the next sibling within a container. This will walk up the
 * DOM if a node's siblings have been exhausted.
 *
 * @param {DOMElement|DOMTextNode} node
 * @return {?DOMElement|DOMTextNode}
 */ function getSiblingNode(node) {
        while(node){
            if (node.nextSibling) return node.nextSibling;
            node = node.parentNode;
        }
    }
    /**
 * Get object describing the nodes which contain characters at offset.
 *
 * @param {DOMElement|DOMTextNode} root
 * @param {number} offset
 * @return {?object}
 */ function getNodeForCharacterOffset(root, offset) {
        var node = getLeafNode(root);
        var nodeStart = 0;
        var nodeEnd = 0;
        while(node){
            if (node.nodeType === TEXT_NODE) {
                nodeEnd = nodeStart + node.textContent.length;
                if (nodeStart <= offset && nodeEnd >= offset) return {
                    node: node,
                    offset: offset - nodeStart
                };
                nodeStart = nodeEnd;
            }
            node = getLeafNode(getSiblingNode(node));
        }
    }
    /**
 * @param {DOMElement} outerNode
 * @return {?object}
 */ function getOffsets(outerNode) {
        var ownerDocument = outerNode.ownerDocument;
        var win = ownerDocument && ownerDocument.defaultView || window;
        var selection = win.getSelection && win.getSelection();
        if (!selection || selection.rangeCount === 0) return null;
        var anchorNode = selection.anchorNode, anchorOffset = selection.anchorOffset, focusNode = selection.focusNode, focusOffset = selection.focusOffset; // In Firefox, anchorNode and focusNode can be "anonymous divs", e.g. the
        // up/down buttons on an <input type="number">. Anonymous divs do not seem to
        // expose properties, triggering a "Permission denied error" if any of its
        // properties are accessed. The only seemingly possible way to avoid erroring
        // is to access a property that typically works for non-anonymous divs and
        // catch any error that may otherwise arise. See
        // https://bugzilla.mozilla.org/show_bug.cgi?id=208427
        try {
            /* eslint-disable no-unused-expressions */ anchorNode.nodeType;
            focusNode.nodeType;
        /* eslint-enable no-unused-expressions */ } catch (e) {
            return null;
        }
        return getModernOffsetsFromPoints(outerNode, anchorNode, anchorOffset, focusNode, focusOffset);
    }
    /**
 * Returns {start, end} where `start` is the character/codepoint index of
 * (anchorNode, anchorOffset) within the textContent of `outerNode`, and
 * `end` is the index of (focusNode, focusOffset).
 *
 * Returns null if you pass in garbage input but we should probably just crash.
 *
 * Exported only for testing.
 */ function getModernOffsetsFromPoints(outerNode, anchorNode, anchorOffset, focusNode, focusOffset) {
        var length = 0;
        var start = -1;
        var end = -1;
        var indexWithinAnchor = 0;
        var indexWithinFocus = 0;
        var node = outerNode;
        var parentNode = null;
        outer: while(true){
            var next = null;
            while(true){
                if (node === anchorNode && (anchorOffset === 0 || node.nodeType === TEXT_NODE)) start = length + anchorOffset;
                if (node === focusNode && (focusOffset === 0 || node.nodeType === TEXT_NODE)) end = length + focusOffset;
                if (node.nodeType === TEXT_NODE) length += node.nodeValue.length;
                if ((next = node.firstChild) === null) break;
                 // Moving from `node` to its first child `next`.
                parentNode = node;
                node = next;
            }
            while(true){
                if (node === outerNode) break outer;
                if (parentNode === anchorNode && ++indexWithinAnchor === anchorOffset) start = length;
                if (parentNode === focusNode && ++indexWithinFocus === focusOffset) end = length;
                if ((next = node.nextSibling) !== null) break;
                node = parentNode;
                parentNode = node.parentNode;
            } // Moving from `node` to its next sibling `next`.
            node = next;
        }
        if (start === -1 || end === -1) // This should never happen. (Would happen if the anchor/focus nodes aren't
        // actually inside the passed-in node.)
        return null;
        return {
            start: start,
            end: end
        };
    }
    /**
 * In modern non-IE browsers, we can support both forward and backward
 * selections.
 *
 * Note: IE10+ supports the Selection object, but it does not support
 * the `extend` method, which means that even in modern IE, it's not possible
 * to programmatically create a backward selection. Thus, for all IE
 * versions, we use the old IE API to create our selections.
 *
 * @param {DOMElement|DOMTextNode} node
 * @param {object} offsets
 */ function setOffsets(node, offsets) {
        var doc = node.ownerDocument || document;
        var win = doc && doc.defaultView || window; // Edge fails with "Object expected" in some scenarios.
        // (For instance: TinyMCE editor used in a list component that supports pasting to add more,
        // fails when pasting 100+ items)
        if (!win.getSelection) return;
        var selection = win.getSelection();
        var length = node.textContent.length;
        var start = Math.min(offsets.start, length);
        var end = offsets.end === undefined ? start : Math.min(offsets.end, length); // IE 11 uses modern selection, but doesn't support the extend method.
        // Flip backward selections, so we can set with a single range.
        if (!selection.extend && start > end) {
            var temp = end;
            end = start;
            start = temp;
        }
        var startMarker = getNodeForCharacterOffset(node, start);
        var endMarker = getNodeForCharacterOffset(node, end);
        if (startMarker && endMarker) {
            if (selection.rangeCount === 1 && selection.anchorNode === startMarker.node && selection.anchorOffset === startMarker.offset && selection.focusNode === endMarker.node && selection.focusOffset === endMarker.offset) return;
            var range = doc.createRange();
            range.setStart(startMarker.node, startMarker.offset);
            selection.removeAllRanges();
            if (start > end) {
                selection.addRange(range);
                selection.extend(endMarker.node, endMarker.offset);
            } else {
                range.setEnd(endMarker.node, endMarker.offset);
                selection.addRange(range);
            }
        }
    }
    function isTextNode(node) {
        return node && node.nodeType === TEXT_NODE;
    }
    function containsNode(outerNode, innerNode) {
        if (!outerNode || !innerNode) return false;
        else if (outerNode === innerNode) return true;
        else if (isTextNode(outerNode)) return false;
        else if (isTextNode(innerNode)) return containsNode(outerNode, innerNode.parentNode);
        else if ('contains' in outerNode) return outerNode.contains(innerNode);
        else if (outerNode.compareDocumentPosition) return !!(outerNode.compareDocumentPosition(innerNode) & 16);
        else return false;
    }
    function isInDocument(node) {
        return node && node.ownerDocument && containsNode(node.ownerDocument.documentElement, node);
    }
    function isSameOriginFrame(iframe) {
        try {
            // Accessing the contentDocument of a HTMLIframeElement can cause the browser
            // to throw, e.g. if it has a cross-origin src attribute.
            // Safari will show an error in the console when the access results in "Blocked a frame with origin". e.g:
            // iframe.contentDocument.defaultView;
            // A safety way is to access one of the cross origin properties: Window or Location
            // Which might result in "SecurityError" DOM Exception and it is compatible to Safari.
            // https://html.spec.whatwg.org/multipage/browsers.html#integration-with-idl
            return typeof iframe.contentWindow.location.href === 'string';
        } catch (err) {
            return false;
        }
    }
    function getActiveElementDeep() {
        var win = window;
        var element = getActiveElement();
        while(element instanceof win.HTMLIFrameElement){
            if (isSameOriginFrame(element)) win = element.contentWindow;
            else return element;
            element = getActiveElement(win.document);
        }
        return element;
    }
    /**
 * @ReactInputSelection: React input selection module. Based on Selection.js,
 * but modified to be suitable for react and has a couple of bug fixes (doesn't
 * assume buttons have range selections allowed).
 * Input selection module for React.
 */ /**
 * @hasSelectionCapabilities: we get the element types that support selection
 * from https://html.spec.whatwg.org/#do-not-apply, looking at `selectionStart`
 * and `selectionEnd` rows.
 */ function hasSelectionCapabilities(elem) {
        var nodeName = elem && elem.nodeName && elem.nodeName.toLowerCase();
        return nodeName && (nodeName === 'input' && (elem.type === 'text' || elem.type === 'search' || elem.type === 'tel' || elem.type === 'url' || elem.type === 'password') || nodeName === 'textarea' || elem.contentEditable === 'true');
    }
    function getSelectionInformation() {
        var focusedElem = getActiveElementDeep();
        return {
            focusedElem: focusedElem,
            selectionRange: hasSelectionCapabilities(focusedElem) ? getSelection(focusedElem) : null
        };
    }
    /**
 * @restoreSelection: If any selection information was potentially lost,
 * restore it. This is useful when performing operations that could remove dom
 * nodes and place them back in, resulting in focus being lost.
 */ function restoreSelection(priorSelectionInformation) {
        var curFocusedElem = getActiveElementDeep();
        var priorFocusedElem = priorSelectionInformation.focusedElem;
        var priorSelectionRange = priorSelectionInformation.selectionRange;
        if (curFocusedElem !== priorFocusedElem && isInDocument(priorFocusedElem)) {
            if (priorSelectionRange !== null && hasSelectionCapabilities(priorFocusedElem)) setSelection(priorFocusedElem, priorSelectionRange);
             // Focusing a node can change the scroll position, which is undesirable
            var ancestors = [];
            var ancestor = priorFocusedElem;
            while(ancestor = ancestor.parentNode)if (ancestor.nodeType === ELEMENT_NODE) ancestors.push({
                element: ancestor,
                left: ancestor.scrollLeft,
                top: ancestor.scrollTop
            });
            if (typeof priorFocusedElem.focus === 'function') priorFocusedElem.focus();
            for(var i = 0; i < ancestors.length; i++){
                var info = ancestors[i];
                info.element.scrollLeft = info.left;
                info.element.scrollTop = info.top;
            }
        }
    }
    /**
 * @getSelection: Gets the selection bounds of a focused textarea, input or
 * contentEditable node.
 * -@input: Look up selection bounds of this input
 * -@return {start: selectionStart, end: selectionEnd}
 */ function getSelection(input) {
        var selection;
        if ('selectionStart' in input) // Modern browser with input or textarea.
        selection = {
            start: input.selectionStart,
            end: input.selectionEnd
        };
        else // Content editable or old IE textarea.
        selection = getOffsets(input);
        return selection || {
            start: 0,
            end: 0
        };
    }
    /**
 * @setSelection: Sets the selection bounds of a textarea or input and focuses
 * the input.
 * -@input     Set selection bounds of this input or textarea
 * -@offsets   Object of same form that is returned from get*
 */ function setSelection(input, offsets) {
        var start = offsets.start;
        var end = offsets.end;
        if (end === undefined) end = start;
        if ('selectionStart' in input) {
            input.selectionStart = start;
            input.selectionEnd = Math.min(end, input.value.length);
        } else setOffsets(input, offsets);
    }
    var skipSelectionChangeEvent = canUseDOM && 'documentMode' in document && document.documentMode <= 11;
    function registerEvents$3() {
        registerTwoPhaseEvent('onSelect', [
            'focusout',
            'contextmenu',
            'dragend',
            'focusin',
            'keydown',
            'keyup',
            'mousedown',
            'mouseup',
            'selectionchange'
        ]);
    }
    var activeElement$1 = null;
    var activeElementInst$1 = null;
    var lastSelection = null;
    var mouseDown = false;
    /**
 * Get an object which is a unique representation of the current selection.
 *
 * The return value will not be consistent across nodes or browsers, but
 * two identical selections on the same node will return identical objects.
 */ function getSelection$1(node) {
        if ('selectionStart' in node && hasSelectionCapabilities(node)) return {
            start: node.selectionStart,
            end: node.selectionEnd
        };
        else {
            var win = node.ownerDocument && node.ownerDocument.defaultView || window;
            var selection = win.getSelection();
            return {
                anchorNode: selection.anchorNode,
                anchorOffset: selection.anchorOffset,
                focusNode: selection.focusNode,
                focusOffset: selection.focusOffset
            };
        }
    }
    /**
 * Get document associated with the event target.
 */ function getEventTargetDocument(eventTarget) {
        return eventTarget.window === eventTarget ? eventTarget.document : eventTarget.nodeType === DOCUMENT_NODE ? eventTarget : eventTarget.ownerDocument;
    }
    /**
 * Poll selection to see whether it's changed.
 *
 * @param {object} nativeEvent
 * @param {object} nativeEventTarget
 * @return {?SyntheticEvent}
 */ function constructSelectEvent(dispatchQueue, nativeEvent, nativeEventTarget) {
        // Ensure we have the right element, and that the user is not dragging a
        // selection (this matches native `select` event behavior). In HTML5, select
        // fires only on input and textarea thus if there's no focused element we
        // won't dispatch.
        var doc = getEventTargetDocument(nativeEventTarget);
        if (mouseDown || activeElement$1 == null || activeElement$1 !== getActiveElement(doc)) return;
         // Only fire when selection has actually changed.
        var currentSelection = getSelection$1(activeElement$1);
        if (!lastSelection || !shallowEqual(lastSelection, currentSelection)) {
            lastSelection = currentSelection;
            var listeners = accumulateTwoPhaseListeners(activeElementInst$1, 'onSelect');
            if (listeners.length > 0) {
                var event = new SyntheticEvent('onSelect', 'select', null, nativeEvent, nativeEventTarget);
                dispatchQueue.push({
                    event: event,
                    listeners: listeners
                });
                event.target = activeElement$1;
            }
        }
    }
    /**
 * This plugin creates an `onSelect` event that normalizes select events
 * across form elements.
 *
 * Supported elements are:
 * - input (see `isTextInputElement`)
 * - textarea
 * - contentEditable
 *
 * This differs from native browser implementations in the following ways:
 * - Fires on contentEditable fields as well as inputs.
 * - Fires for collapsed selection.
 * - Fires after user input.
 */ function extractEvents$3(dispatchQueue, domEventName, targetInst, nativeEvent, nativeEventTarget, eventSystemFlags, targetContainer) {
        var targetNode = targetInst ? getNodeFromInstance(targetInst) : window;
        switch(domEventName){
            // Track the input node that has focus.
            case 'focusin':
                if (isTextInputElement(targetNode) || targetNode.contentEditable === 'true') {
                    activeElement$1 = targetNode;
                    activeElementInst$1 = targetInst;
                    lastSelection = null;
                }
                break;
            case 'focusout':
                activeElement$1 = null;
                activeElementInst$1 = null;
                lastSelection = null;
                break;
            // Don't fire the event while the user is dragging. This matches the
            // semantics of the native select event.
            case 'mousedown':
                mouseDown = true;
                break;
            case 'contextmenu':
            case 'mouseup':
            case 'dragend':
                mouseDown = false;
                constructSelectEvent(dispatchQueue, nativeEvent, nativeEventTarget);
                break;
            // Chrome and IE fire non-standard event when selection is changed (and
            // sometimes when it hasn't). IE's event fires out of order with respect
            // to key and input events on deletion, so we discard it.
            //
            // Firefox doesn't support selectionchange, so check selection status
            // after each key entry. The selection changes after keydown and before
            // keyup, but we check on keydown as well in the case of holding down a
            // key, when multiple keydown events are fired but only one keyup is.
            // This is also our approach for IE handling, for the reason above.
            case 'selectionchange':
                if (skipSelectionChangeEvent) break;
            // falls through
            case 'keydown':
            case 'keyup':
                constructSelectEvent(dispatchQueue, nativeEvent, nativeEventTarget);
        }
    }
    /**
 * Generate a mapping of standard vendor prefixes using the defined style property and event name.
 *
 * @param {string} styleProp
 * @param {string} eventName
 * @returns {object}
 */ function makePrefixMap(styleProp, eventName) {
        var prefixes = {};
        prefixes[styleProp.toLowerCase()] = eventName.toLowerCase();
        prefixes['Webkit' + styleProp] = 'webkit' + eventName;
        prefixes['Moz' + styleProp] = 'moz' + eventName;
        return prefixes;
    }
    /**
 * A list of event names to a configurable list of vendor prefixes.
 */ var vendorPrefixes = {
        animationend: makePrefixMap('Animation', 'AnimationEnd'),
        animationiteration: makePrefixMap('Animation', 'AnimationIteration'),
        animationstart: makePrefixMap('Animation', 'AnimationStart'),
        transitionend: makePrefixMap('Transition', 'TransitionEnd')
    };
    /**
 * Event names that have already been detected and prefixed (if applicable).
 */ var prefixedEventNames = {};
    /**
 * Element to check for prefixes on.
 */ var style1 = {};
    /**
 * Bootstrap if a DOM exists.
 */ if (canUseDOM) {
        style1 = document.createElement('div').style; // On some platforms, in particular some releases of Android 4.x,
        // the un-prefixed "animation" and "transition" properties are defined on the
        // style object but the events that fire will still be prefixed, so we need
        // to check if the un-prefixed events are usable, and if not remove them from the map.
        if (!('AnimationEvent' in window)) {
            delete vendorPrefixes.animationend.animation;
            delete vendorPrefixes.animationiteration.animation;
            delete vendorPrefixes.animationstart.animation;
        } // Same as above
        if (!('TransitionEvent' in window)) delete vendorPrefixes.transitionend.transition;
    }
    /**
 * Attempts to determine the correct vendor prefixed event name.
 *
 * @param {string} eventName
 * @returns {string}
 */ function getVendorPrefixedEventName(eventName) {
        if (prefixedEventNames[eventName]) return prefixedEventNames[eventName];
        else if (!vendorPrefixes[eventName]) return eventName;
        var prefixMap = vendorPrefixes[eventName];
        for(var styleProp in prefixMap){
            if (prefixMap.hasOwnProperty(styleProp) && styleProp in style1) return prefixedEventNames[eventName] = prefixMap[styleProp];
        }
        return eventName;
    }
    var ANIMATION_END = getVendorPrefixedEventName('animationend');
    var ANIMATION_ITERATION = getVendorPrefixedEventName('animationiteration');
    var ANIMATION_START = getVendorPrefixedEventName('animationstart');
    var TRANSITION_END = getVendorPrefixedEventName('transitionend');
    var topLevelEventsToReactNames = new Map(); // NOTE: Capitalization is important in this list!
    //
    // E.g. it needs "pointerDown", not "pointerdown".
    // This is because we derive both React name ("onPointerDown")
    // and DOM name ("pointerdown") from the same list.
    //
    // Exceptions that don't match this convention are listed separately.
    //
    // prettier-ignore
    var simpleEventPluginEvents = [
        'abort',
        'auxClick',
        'cancel',
        'canPlay',
        'canPlayThrough',
        'click',
        'close',
        'contextMenu',
        'copy',
        'cut',
        'drag',
        'dragEnd',
        'dragEnter',
        'dragExit',
        'dragLeave',
        'dragOver',
        'dragStart',
        'drop',
        'durationChange',
        'emptied',
        'encrypted',
        'ended',
        'error',
        'gotPointerCapture',
        'input',
        'invalid',
        'keyDown',
        'keyPress',
        'keyUp',
        'load',
        'loadedData',
        'loadedMetadata',
        'loadStart',
        'lostPointerCapture',
        'mouseDown',
        'mouseMove',
        'mouseOut',
        'mouseOver',
        'mouseUp',
        'paste',
        'pause',
        'play',
        'playing',
        'pointerCancel',
        'pointerDown',
        'pointerMove',
        'pointerOut',
        'pointerOver',
        'pointerUp',
        'progress',
        'rateChange',
        'reset',
        'resize',
        'seeked',
        'seeking',
        'stalled',
        'submit',
        'suspend',
        'timeUpdate',
        'touchCancel',
        'touchEnd',
        'touchStart',
        'volumeChange',
        'scroll',
        'toggle',
        'touchMove',
        'waiting',
        'wheel'
    ];
    function registerSimpleEvent(domEventName, reactName) {
        topLevelEventsToReactNames.set(domEventName, reactName);
        registerTwoPhaseEvent(reactName, [
            domEventName
        ]);
    }
    function registerSimpleEvents() {
        for(var i = 0; i < simpleEventPluginEvents.length; i++){
            var eventName = simpleEventPluginEvents[i];
            var domEventName = eventName.toLowerCase();
            var capitalizedEvent = eventName[0].toUpperCase() + eventName.slice(1);
            registerSimpleEvent(domEventName, 'on' + capitalizedEvent);
        } // Special cases where event names don't match.
        registerSimpleEvent(ANIMATION_END, 'onAnimationEnd');
        registerSimpleEvent(ANIMATION_ITERATION, 'onAnimationIteration');
        registerSimpleEvent(ANIMATION_START, 'onAnimationStart');
        registerSimpleEvent('dblclick', 'onDoubleClick');
        registerSimpleEvent('focusin', 'onFocus');
        registerSimpleEvent('focusout', 'onBlur');
        registerSimpleEvent(TRANSITION_END, 'onTransitionEnd');
    }
    function extractEvents$4(dispatchQueue, domEventName, targetInst, nativeEvent, nativeEventTarget, eventSystemFlags, targetContainer) {
        var reactName = topLevelEventsToReactNames.get(domEventName);
        if (reactName === undefined) return;
        var SyntheticEventCtor = SyntheticEvent;
        var reactEventType = domEventName;
        switch(domEventName){
            case 'keypress':
                // Firefox creates a keypress event for function keys too. This removes
                // the unwanted keypress events. Enter is however both printable and
                // non-printable. One would expect Tab to be as well (but it isn't).
                if (getEventCharCode(nativeEvent) === 0) return;
            /* falls through */ case 'keydown':
            case 'keyup':
                SyntheticEventCtor = SyntheticKeyboardEvent;
                break;
            case 'focusin':
                reactEventType = 'focus';
                SyntheticEventCtor = SyntheticFocusEvent;
                break;
            case 'focusout':
                reactEventType = 'blur';
                SyntheticEventCtor = SyntheticFocusEvent;
                break;
            case 'beforeblur':
            case 'afterblur':
                SyntheticEventCtor = SyntheticFocusEvent;
                break;
            case 'click':
                // Firefox creates a click event on right mouse clicks. This removes the
                // unwanted click events.
                if (nativeEvent.button === 2) return;
            /* falls through */ case 'auxclick':
            case 'dblclick':
            case 'mousedown':
            case 'mousemove':
            case 'mouseup':
            /* falls through */ case 'mouseout':
            case 'mouseover':
            case 'contextmenu':
                SyntheticEventCtor = SyntheticMouseEvent;
                break;
            case 'drag':
            case 'dragend':
            case 'dragenter':
            case 'dragexit':
            case 'dragleave':
            case 'dragover':
            case 'dragstart':
            case 'drop':
                SyntheticEventCtor = SyntheticDragEvent;
                break;
            case 'touchcancel':
            case 'touchend':
            case 'touchmove':
            case 'touchstart':
                SyntheticEventCtor = SyntheticTouchEvent;
                break;
            case ANIMATION_END:
            case ANIMATION_ITERATION:
            case ANIMATION_START:
                SyntheticEventCtor = SyntheticAnimationEvent;
                break;
            case TRANSITION_END:
                SyntheticEventCtor = SyntheticTransitionEvent;
                break;
            case 'scroll':
                SyntheticEventCtor = SyntheticUIEvent;
                break;
            case 'wheel':
                SyntheticEventCtor = SyntheticWheelEvent;
                break;
            case 'copy':
            case 'cut':
            case 'paste':
                SyntheticEventCtor = SyntheticClipboardEvent;
                break;
            case 'gotpointercapture':
            case 'lostpointercapture':
            case 'pointercancel':
            case 'pointerdown':
            case 'pointermove':
            case 'pointerout':
            case 'pointerover':
            case 'pointerup':
                SyntheticEventCtor = SyntheticPointerEvent;
                break;
        }
        var inCapturePhase = (eventSystemFlags & IS_CAPTURE_PHASE) !== 0;
        // Some events don't bubble in the browser.
        // In the past, React has always bubbled them, but this can be surprising.
        // We're going to try aligning closer to the browser behavior by not bubbling
        // them in React either. We'll start by not bubbling onScroll, and then expand.
        var accumulateTargetOnly = !inCapturePhase && // nonDelegatedEvents list in DOMPluginEventSystem.
        // Then we can remove this special list.
        // This is a breaking change that can wait until React 18.
        domEventName === 'scroll';
        var _listeners = accumulateSinglePhaseListeners(targetInst, reactName, nativeEvent.type, inCapturePhase, accumulateTargetOnly);
        if (_listeners.length > 0) {
            // Intentionally create event lazily.
            var _event = new SyntheticEventCtor(reactName, reactEventType, null, nativeEvent, nativeEventTarget);
            dispatchQueue.push({
                event: _event,
                listeners: _listeners
            });
        }
    }
    // TODO: remove top-level side effect.
    registerSimpleEvents();
    registerEvents$2();
    registerEvents$1();
    registerEvents$3();
    registerEvents();
    function extractEvents$5(dispatchQueue, domEventName, targetInst, nativeEvent, nativeEventTarget, eventSystemFlags, targetContainer) {
        // TODO: we should remove the concept of a "SimpleEventPlugin".
        // This is the basic functionality of the event system. All
        // the other plugins are essentially polyfills. So the plugin
        // should probably be inlined somewhere and have its logic
        // be core the to event system. This would potentially allow
        // us to ship builds of React without the polyfilled plugins below.
        extractEvents$4(dispatchQueue, domEventName, targetInst, nativeEvent, nativeEventTarget, eventSystemFlags);
        var shouldProcessPolyfillPlugins = (eventSystemFlags & SHOULD_NOT_PROCESS_POLYFILL_EVENT_PLUGINS) === 0; // We don't process these events unless we are in the
        // event's native "bubble" phase, which means that we're
        // not in the capture phase. That's because we emulate
        // the capture phase here still. This is a trade-off,
        // because in an ideal world we would not emulate and use
        // the phases properly, like we do with the SimpleEvent
        // plugin. However, the plugins below either expect
        // emulation (EnterLeave) or use state localized to that
        // plugin (BeforeInput, Change, Select). The state in
        // these modules complicates things, as you'll essentially
        // get the case where the capture phase event might change
        // state, only for the following bubble event to come in
        // later and not trigger anything as the state now
        // invalidates the heuristics of the event plugin. We
        // could alter all these plugins to work in such ways, but
        // that might cause other unknown side-effects that we
        // can't foresee right now.
        if (shouldProcessPolyfillPlugins) {
            extractEvents$2(dispatchQueue, domEventName, targetInst, nativeEvent, nativeEventTarget);
            extractEvents$1(dispatchQueue, domEventName, targetInst, nativeEvent, nativeEventTarget);
            extractEvents$3(dispatchQueue, domEventName, targetInst, nativeEvent, nativeEventTarget);
            extractEvents(dispatchQueue, domEventName, targetInst, nativeEvent, nativeEventTarget);
        }
    } // List of events that need to be individually attached to media elements.
    var mediaEventTypes = [
        'abort',
        'canplay',
        'canplaythrough',
        'durationchange',
        'emptied',
        'encrypted',
        'ended',
        'error',
        'loadeddata',
        'loadedmetadata',
        'loadstart',
        'pause',
        'play',
        'playing',
        'progress',
        'ratechange',
        'resize',
        'seeked',
        'seeking',
        'stalled',
        'suspend',
        'timeupdate',
        'volumechange',
        'waiting'
    ]; // We should not delegate these events to the container, but rather
    // set them on the actual target element itself. This is primarily
    // because these events do not consistently bubble in the DOM.
    var nonDelegatedEvents = new Set([
        'cancel',
        'close',
        'invalid',
        'load',
        'scroll',
        'toggle'
    ].concat(mediaEventTypes));
    function executeDispatch(event, listener, currentTarget) {
        var type = event.type || 'unknown-event';
        event.currentTarget = currentTarget;
        invokeGuardedCallbackAndCatchFirstError(type, listener, undefined, event);
        event.currentTarget = null;
    }
    function processDispatchQueueItemsInOrder(event, dispatchListeners, inCapturePhase) {
        var previousInstance;
        if (inCapturePhase) for(var i = dispatchListeners.length - 1; i >= 0; i--){
            var _dispatchListeners$i = dispatchListeners[i], instance = _dispatchListeners$i.instance, currentTarget = _dispatchListeners$i.currentTarget, listener = _dispatchListeners$i.listener;
            if (instance !== previousInstance && event.isPropagationStopped()) return;
            executeDispatch(event, listener, currentTarget);
            previousInstance = instance;
        }
        else for(var _i = 0; _i < dispatchListeners.length; _i++){
            var _dispatchListeners$_i = dispatchListeners[_i], _instance = _dispatchListeners$_i.instance, _currentTarget = _dispatchListeners$_i.currentTarget, _listener = _dispatchListeners$_i.listener;
            if (_instance !== previousInstance && event.isPropagationStopped()) return;
            executeDispatch(event, _listener, _currentTarget);
            previousInstance = _instance;
        }
    }
    function processDispatchQueue(dispatchQueue, eventSystemFlags) {
        var inCapturePhase = (eventSystemFlags & IS_CAPTURE_PHASE) !== 0;
        for(var i = 0; i < dispatchQueue.length; i++){
            var _dispatchQueue$i = dispatchQueue[i], event = _dispatchQueue$i.event, listeners = _dispatchQueue$i.listeners;
            processDispatchQueueItemsInOrder(event, listeners, inCapturePhase); //  event system doesn't use pooling.
        } // This would be a good time to rethrow if any of the event handlers threw.
        rethrowCaughtError();
    }
    function dispatchEventsForPlugins(domEventName, eventSystemFlags, nativeEvent, targetInst, targetContainer) {
        var nativeEventTarget = getEventTarget(nativeEvent);
        var dispatchQueue = [];
        extractEvents$5(dispatchQueue, domEventName, targetInst, nativeEvent, nativeEventTarget, eventSystemFlags);
        processDispatchQueue(dispatchQueue, eventSystemFlags);
    }
    function listenToNonDelegatedEvent(domEventName, targetElement) {
        if (!nonDelegatedEvents.has(domEventName)) error1('Did not expect a listenToNonDelegatedEvent() call for "%s". This is a bug in React. Please file an issue.', domEventName);
        var isCapturePhaseListener = false;
        var listenerSet = getEventListenerSet(targetElement);
        var listenerSetKey = getListenerSetKey(domEventName, isCapturePhaseListener);
        if (!listenerSet.has(listenerSetKey)) {
            addTrappedEventListener(targetElement, domEventName, IS_NON_DELEGATED, isCapturePhaseListener);
            listenerSet.add(listenerSetKey);
        }
    }
    function listenToNativeEvent(domEventName, isCapturePhaseListener, target) {
        if (nonDelegatedEvents.has(domEventName) && !isCapturePhaseListener) error1('Did not expect a listenToNativeEvent() call for "%s" in the bubble phase. This is a bug in React. Please file an issue.', domEventName);
        var eventSystemFlags = 0;
        if (isCapturePhaseListener) eventSystemFlags |= IS_CAPTURE_PHASE;
        addTrappedEventListener(target, domEventName, eventSystemFlags, isCapturePhaseListener);
    } // This is only used by createEventHandle when the
    var listeningMarker = '_reactListening' + Math.random().toString(36).slice(2);
    function listenToAllSupportedEvents(rootContainerElement) {
        if (!rootContainerElement[listeningMarker]) {
            rootContainerElement[listeningMarker] = true;
            allNativeEvents.forEach(function(domEventName) {
                // We handle selectionchange separately because it
                // doesn't bubble and needs to be on the document.
                if (domEventName !== 'selectionchange') {
                    if (!nonDelegatedEvents.has(domEventName)) listenToNativeEvent(domEventName, false, rootContainerElement);
                    listenToNativeEvent(domEventName, true, rootContainerElement);
                }
            });
            var ownerDocument = rootContainerElement.nodeType === DOCUMENT_NODE ? rootContainerElement : rootContainerElement.ownerDocument;
            if (ownerDocument !== null) // The selectionchange event also needs deduplication
            // but it is attached to the document.
            {
                if (!ownerDocument[listeningMarker]) {
                    ownerDocument[listeningMarker] = true;
                    listenToNativeEvent('selectionchange', false, ownerDocument);
                }
            }
        }
    }
    function addTrappedEventListener(targetContainer, domEventName, eventSystemFlags, isCapturePhaseListener, isDeferredListenerForLegacyFBSupport) {
        var listener = createEventListenerWrapperWithPriority(targetContainer, domEventName, eventSystemFlags); // If passive option is not supported, then the event will be
        // active and not passive.
        var isPassiveListener = undefined;
        if (passiveBrowserEventsSupported) // Browsers introduced an intervention, making these events
        // passive by default on document. React doesn't bind them
        // to document anymore, but changing this now would undo
        // the performance wins from the change. So we emulate
        // the existing behavior manually on the roots now.
        // https://github.com/facebook/react/issues/19651
        {
            if (domEventName === 'touchstart' || domEventName === 'touchmove' || domEventName === 'wheel') isPassiveListener = true;
        }
        var unsubscribeListener; // When legacyFBSupport is enabled, it's for when we
        if (isCapturePhaseListener) {
            if (isPassiveListener !== undefined) unsubscribeListener = addEventCaptureListenerWithPassiveFlag(targetContainer, domEventName, listener, isPassiveListener);
            else unsubscribeListener = addEventCaptureListener(targetContainer, domEventName, listener);
        } else if (isPassiveListener !== undefined) unsubscribeListener = addEventBubbleListenerWithPassiveFlag(targetContainer, domEventName, listener, isPassiveListener);
        else unsubscribeListener = addEventBubbleListener(targetContainer, domEventName, listener);
    }
    function isMatchingRootContainer(grandContainer, targetContainer) {
        return grandContainer === targetContainer || grandContainer.nodeType === COMMENT_NODE && grandContainer.parentNode === targetContainer;
    }
    function dispatchEventForPluginEventSystem(domEventName, eventSystemFlags, nativeEvent, targetInst, targetContainer) {
        var ancestorInst = targetInst;
        if ((eventSystemFlags & IS_EVENT_HANDLE_NON_MANAGED_NODE) === 0 && (eventSystemFlags & IS_NON_DELEGATED) === 0) {
            var targetContainerNode = targetContainer; // If we are using the legacy FB support flag, we
            if (targetInst !== null) {
                // The below logic attempts to work out if we need to change
                // the target fiber to a different ancestor. We had similar logic
                // in the legacy event system, except the big difference between
                // systems is that the modern event system now has an event listener
                // attached to each React Root and React Portal Root. Together,
                // the DOM nodes representing these roots are the "rootContainer".
                // To figure out which ancestor instance we should use, we traverse
                // up the fiber tree from the target instance and attempt to find
                // root boundaries that match that of our current "rootContainer".
                // If we find that "rootContainer", we find the parent fiber
                // sub-tree for that root and make that our ancestor instance.
                var node = targetInst;
                mainLoop: while(true){
                    if (node === null) return;
                    var nodeTag = node.tag;
                    if (nodeTag === HostRoot || nodeTag === HostPortal) {
                        var container = node.stateNode.containerInfo;
                        if (isMatchingRootContainer(container, targetContainerNode)) break;
                        if (nodeTag === HostPortal) {
                            // The target is a portal, but it's not the rootContainer we're looking for.
                            // Normally portals handle their own events all the way down to the root.
                            // So we should be able to stop now. However, we don't know if this portal
                            // was part of *our* root.
                            var grandNode = node.return;
                            while(grandNode !== null){
                                var grandTag = grandNode.tag;
                                if (grandTag === HostRoot || grandTag === HostPortal) {
                                    var grandContainer = grandNode.stateNode.containerInfo;
                                    if (isMatchingRootContainer(grandContainer, targetContainerNode)) // This is the rootContainer we're looking for and we found it as
                                    // a parent of the Portal. That means we can ignore it because the
                                    // Portal will bubble through to us.
                                    return;
                                }
                                grandNode = grandNode.return;
                            }
                        } // Now we need to find it's corresponding host fiber in the other
                        // tree. To do this we can use getClosestInstanceFromNode, but we
                        // need to validate that the fiber is a host instance, otherwise
                        // we need to traverse up through the DOM till we find the correct
                        // node that is from the other tree.
                        while(container !== null){
                            var parentNode = getClosestInstanceFromNode(container);
                            if (parentNode === null) return;
                            var parentTag = parentNode.tag;
                            if (parentTag === HostComponent || parentTag === HostText) {
                                node = ancestorInst = parentNode;
                                continue mainLoop;
                            }
                            container = container.parentNode;
                        }
                    }
                    node = node.return;
                }
            }
        }
        batchedUpdates(function() {
            return dispatchEventsForPlugins(domEventName, eventSystemFlags, nativeEvent, ancestorInst);
        });
    }
    function createDispatchListener(instance, listener, currentTarget) {
        return {
            instance: instance,
            listener: listener,
            currentTarget: currentTarget
        };
    }
    function accumulateSinglePhaseListeners(targetFiber, reactName, nativeEventType, inCapturePhase, accumulateTargetOnly, nativeEvent) {
        var captureName = reactName !== null ? reactName + 'Capture' : null;
        var reactEventName = inCapturePhase ? captureName : reactName;
        var listeners = [];
        var instance = targetFiber;
        var lastHostComponent = null; // Accumulate all instances and listeners via the target -> root path.
        while(instance !== null){
            var _instance2 = instance, stateNode = _instance2.stateNode, tag = _instance2.tag; // Handle listeners that are on HostComponents (i.e. <div>)
            if (tag === HostComponent && stateNode !== null) {
                lastHostComponent = stateNode; // createEventHandle listeners
                if (reactEventName !== null) {
                    var listener = getListener(instance, reactEventName);
                    if (listener != null) listeners.push(createDispatchListener(instance, listener, lastHostComponent));
                }
            } // If we are only accumulating events for the target, then we don't
            // continue to propagate through the React fiber tree to find other
            // listeners.
            if (accumulateTargetOnly) break;
             // If we are processing the onBeforeBlur event, then we need to take
            instance = instance.return;
        }
        return listeners;
    } // We should only use this function for:
    // - BeforeInputEventPlugin
    // - ChangeEventPlugin
    // - SelectEventPlugin
    // This is because we only process these plugins
    // in the bubble phase, so we need to accumulate two
    // phase event listeners (via emulation).
    function accumulateTwoPhaseListeners(targetFiber, reactName) {
        var captureName = reactName + 'Capture';
        var listeners = [];
        var instance = targetFiber; // Accumulate all instances and listeners via the target -> root path.
        while(instance !== null){
            var _instance3 = instance, stateNode = _instance3.stateNode, tag = _instance3.tag; // Handle listeners that are on HostComponents (i.e. <div>)
            if (tag === HostComponent && stateNode !== null) {
                var currentTarget = stateNode;
                var captureListener = getListener(instance, captureName);
                if (captureListener != null) listeners.unshift(createDispatchListener(instance, captureListener, currentTarget));
                var bubbleListener = getListener(instance, reactName);
                if (bubbleListener != null) listeners.push(createDispatchListener(instance, bubbleListener, currentTarget));
            }
            instance = instance.return;
        }
        return listeners;
    }
    function getParent(inst) {
        if (inst === null) return null;
        do inst = inst.return; // TODO: If this is a HostRoot we might want to bail out.
        while (inst && inst.tag !== HostComponent)
        if (inst) return inst;
        return null;
    }
    /**
 * Return the lowest common ancestor of A and B, or null if they are in
 * different trees.
 */ function getLowestCommonAncestor(instA, instB) {
        var nodeA = instA;
        var nodeB = instB;
        var depthA = 0;
        for(var tempA = nodeA; tempA; tempA = getParent(tempA))depthA++;
        var depthB = 0;
        for(var tempB = nodeB; tempB; tempB = getParent(tempB))depthB++;
         // If A is deeper, crawl up.
        while(depthA - depthB > 0){
            nodeA = getParent(nodeA);
            depthA--;
        } // If B is deeper, crawl up.
        while(depthB - depthA > 0){
            nodeB = getParent(nodeB);
            depthB--;
        } // Walk in lockstep until we find a match.
        var depth = depthA;
        while(depth--){
            if (nodeA === nodeB || nodeB !== null && nodeA === nodeB.alternate) return nodeA;
            nodeA = getParent(nodeA);
            nodeB = getParent(nodeB);
        }
        return null;
    }
    function accumulateEnterLeaveListenersForEvent(dispatchQueue, event, target, common, inCapturePhase) {
        var registrationName = event._reactName;
        var listeners = [];
        var instance = target;
        while(instance !== null){
            if (instance === common) break;
            var _instance4 = instance, alternate = _instance4.alternate, stateNode = _instance4.stateNode, tag = _instance4.tag;
            if (alternate !== null && alternate === common) break;
            if (tag === HostComponent && stateNode !== null) {
                var currentTarget = stateNode;
                if (inCapturePhase) {
                    var captureListener = getListener(instance, registrationName);
                    if (captureListener != null) listeners.unshift(createDispatchListener(instance, captureListener, currentTarget));
                } else if (!inCapturePhase) {
                    var bubbleListener = getListener(instance, registrationName);
                    if (bubbleListener != null) listeners.push(createDispatchListener(instance, bubbleListener, currentTarget));
                }
            }
            instance = instance.return;
        }
        if (listeners.length !== 0) dispatchQueue.push({
            event: event,
            listeners: listeners
        });
    } // We should only use this function for:
    // - EnterLeaveEventPlugin
    // This is because we only process this plugin
    // in the bubble phase, so we need to accumulate two
    // phase event listeners.
    function accumulateEnterLeaveTwoPhaseListeners(dispatchQueue, leaveEvent, enterEvent, from, to) {
        var common = from && to ? getLowestCommonAncestor(from, to) : null;
        if (from !== null) accumulateEnterLeaveListenersForEvent(dispatchQueue, leaveEvent, from, common, false);
        if (to !== null && enterEvent !== null) accumulateEnterLeaveListenersForEvent(dispatchQueue, enterEvent, to, common, true);
    }
    function getListenerSetKey(domEventName, capture) {
        return domEventName + "__" + (capture ? 'capture' : 'bubble');
    }
    var didWarnInvalidHydration = false;
    var DANGEROUSLY_SET_INNER_HTML = 'dangerouslySetInnerHTML';
    var SUPPRESS_CONTENT_EDITABLE_WARNING = 'suppressContentEditableWarning';
    var SUPPRESS_HYDRATION_WARNING = 'suppressHydrationWarning';
    var AUTOFOCUS = 'autoFocus';
    var CHILDREN = 'children';
    var STYLE = 'style';
    var HTML$1 = '__html';
    var warnedUnknownTags;
    var validatePropertiesInDevelopment;
    var warnForPropDifference;
    var warnForExtraAttributes;
    var warnForInvalidEventListener;
    var canDiffStyleForHydrationWarning;
    var normalizeHTML;
    warnedUnknownTags = {
        // There are working polyfills for <dialog>. Let people use it.
        dialog: true,
        // Electron ships a custom <webview> tag to display external web content in
        // an isolated frame and process.
        // This tag is not present in non Electron environments such as JSDom which
        // is often used for testing purposes.
        // @see https://electronjs.org/docs/api/webview-tag
        webview: true
    };
    validatePropertiesInDevelopment = function(type, props) {
        validateProperties(type, props);
        validateProperties$1(type, props);
        validateProperties$2(type, props, {
            registrationNameDependencies: registrationNameDependencies1,
            possibleRegistrationNames: possibleRegistrationNames1
        });
    }; // IE 11 parses & normalizes the style attribute as opposed to other
    // browsers. It adds spaces and sorts the properties in some
    // non-alphabetical order. Handling that would require sorting CSS
    // properties in the client & server versions or applying
    // `expectedStyle` to a temporary DOM node to read its `style` attribute
    // normalized. Since it only affects IE, we're skipping style warnings
    // in that browser completely in favor of doing all that work.
    // See https://github.com/facebook/react/issues/11807
    canDiffStyleForHydrationWarning = canUseDOM && !document.documentMode;
    warnForPropDifference = function(propName, serverValue, clientValue) {
        if (didWarnInvalidHydration) return;
        var normalizedClientValue = normalizeMarkupForTextOrAttribute(clientValue);
        var normalizedServerValue = normalizeMarkupForTextOrAttribute(serverValue);
        if (normalizedServerValue === normalizedClientValue) return;
        didWarnInvalidHydration = true;
        error1('Prop `%s` did not match. Server: %s Client: %s', propName, JSON.stringify(normalizedServerValue), JSON.stringify(normalizedClientValue));
    };
    warnForExtraAttributes = function(attributeNames) {
        if (didWarnInvalidHydration) return;
        didWarnInvalidHydration = true;
        var names = [];
        attributeNames.forEach(function(name) {
            names.push(name);
        });
        error1('Extra attributes from the server: %s', names);
    };
    warnForInvalidEventListener = function(registrationName, listener) {
        if (listener === false) error1("Expected `%s` listener to be a function, instead got `false`.\n\nIf you used to conditionally omit it with %s={condition && value}, pass %s={condition ? value : undefined} instead.", registrationName, registrationName, registrationName);
        else error1('Expected `%s` listener to be a function, instead got a value of `%s` type.', registrationName, typeof listener);
    }; // Parse the HTML and read it back to normalize the HTML string so that it
    // can be used for comparison.
    normalizeHTML = function(parent, html) {
        // We could have created a separate document here to avoid
        // re-initializing custom elements if they exist. But this breaks
        // how <noscript> is being handled. So we use the same document.
        // See the discussion in https://github.com/facebook/react/pull/11157.
        var testElement = parent.namespaceURI === HTML_NAMESPACE ? parent.ownerDocument.createElement(parent.tagName) : parent.ownerDocument.createElementNS(parent.namespaceURI, parent.tagName);
        testElement.innerHTML = html;
        return testElement.innerHTML;
    };
    // It also can turn \u0000 into \uFFFD inside attributes.
    // https://www.w3.org/TR/html5/single-page.html#preprocessing-the-input-stream
    // If we have a mismatch, it might be caused by that.
    // We will still patch up in this case but not fire the warning.
    var NORMALIZE_NEWLINES_REGEX = /\r\n?/g;
    var NORMALIZE_NULL_AND_REPLACEMENT_REGEX = /\u0000|\uFFFD/g;
    function normalizeMarkupForTextOrAttribute(markup) {
        checkHtmlStringCoercion(markup);
        var markupString = typeof markup === 'string' ? markup : '' + markup;
        return markupString.replace(NORMALIZE_NEWLINES_REGEX, '\n').replace(NORMALIZE_NULL_AND_REPLACEMENT_REGEX, '');
    }
    function checkForUnmatchedText(serverText, clientText, isConcurrentMode, shouldWarnDev) {
        var normalizedClientText = normalizeMarkupForTextOrAttribute(clientText);
        var normalizedServerText = normalizeMarkupForTextOrAttribute(serverText);
        if (normalizedServerText === normalizedClientText) return;
        if (shouldWarnDev) {
            if (!didWarnInvalidHydration) {
                didWarnInvalidHydration = true;
                error1('Text content did not match. Server: "%s" Client: "%s"', normalizedServerText, normalizedClientText);
            }
        }
        if (isConcurrentMode && enableClientRenderFallbackOnTextMismatch) // In concurrent roots, we throw when there's a text mismatch and revert to
        // client rendering, up to the nearest Suspense boundary.
        throw new Error('Text content does not match server-rendered HTML.');
    }
    function getOwnerDocumentFromRootContainer(rootContainerElement) {
        return rootContainerElement.nodeType === DOCUMENT_NODE ? rootContainerElement : rootContainerElement.ownerDocument;
    }
    function noop() {}
    function trapClickOnNonInteractiveElement(node) {
        // Mobile Safari does not fire properly bubble click events on
        // non-interactive elements, which means delegated click listeners do not
        // fire. The workaround for this bug involves attaching an empty click
        // listener on the target node.
        // https://www.quirksmode.org/blog/archives/2010/09/click_event_del.html
        // Just set it using the onclick property so that we don't have to manage any
        // bookkeeping for it. Not sure if we need to clear it when the listener is
        // removed.
        // TODO: Only do this for the relevant Safaris maybe?
        node.onclick = noop;
    }
    function setInitialDOMProperties(tag, domElement, rootContainerElement, nextProps, isCustomComponentTag) {
        for(var propKey in nextProps){
            if (!nextProps.hasOwnProperty(propKey)) continue;
            var nextProp = nextProps[propKey];
            if (propKey === STYLE) {
                if (nextProp) // Freeze the next style object so that we can assume it won't be
                // mutated. We have already warned for this in the past.
                Object.freeze(nextProp);
                setValueForStyles(domElement, nextProp);
            } else if (propKey === DANGEROUSLY_SET_INNER_HTML) {
                var nextHtml = nextProp ? nextProp[HTML$1] : undefined;
                if (nextHtml != null) setInnerHTML(domElement, nextHtml);
            } else if (propKey === CHILDREN) {
                if (typeof nextProp === 'string') {
                    // Avoid setting initial textContent when the text is empty. In IE11 setting
                    // textContent on a <textarea> will cause the placeholder to not
                    // show within the <textarea> until it has been focused and blurred again.
                    // https://github.com/facebook/react/issues/6731#issuecomment-254874553
                    var canSetTextContent = tag !== 'textarea' || nextProp !== '';
                    if (canSetTextContent) setTextContent(domElement, nextProp);
                } else if (typeof nextProp === 'number') setTextContent(domElement, '' + nextProp);
            } else if (propKey === SUPPRESS_CONTENT_EDITABLE_WARNING || propKey === SUPPRESS_HYDRATION_WARNING) ;
            else if (propKey === AUTOFOCUS) ;
            else if (registrationNameDependencies1.hasOwnProperty(propKey)) {
                if (nextProp != null) {
                    if (typeof nextProp !== 'function') warnForInvalidEventListener(propKey, nextProp);
                    if (propKey === 'onScroll') listenToNonDelegatedEvent('scroll', domElement);
                }
            } else if (nextProp != null) setValueForProperty(domElement, propKey, nextProp, isCustomComponentTag);
        }
    }
    function updateDOMProperties(domElement, updatePayload, wasCustomComponentTag, isCustomComponentTag) {
        // TODO: Handle wasCustomComponentTag
        for(var i = 0; i < updatePayload.length; i += 2){
            var propKey = updatePayload[i];
            var propValue = updatePayload[i + 1];
            if (propKey === STYLE) setValueForStyles(domElement, propValue);
            else if (propKey === DANGEROUSLY_SET_INNER_HTML) setInnerHTML(domElement, propValue);
            else if (propKey === CHILDREN) setTextContent(domElement, propValue);
            else setValueForProperty(domElement, propKey, propValue, isCustomComponentTag);
        }
    }
    function createElement(type, props, rootContainerElement, parentNamespace) {
        var isCustomComponentTag; // We create tags in the namespace of their parent container, except HTML
        // tags get no namespace.
        var ownerDocument = getOwnerDocumentFromRootContainer(rootContainerElement);
        var domElement;
        var namespaceURI = parentNamespace;
        if (namespaceURI === HTML_NAMESPACE) namespaceURI = getIntrinsicNamespace(type);
        if (namespaceURI === HTML_NAMESPACE) {
            isCustomComponentTag = isCustomComponent(type, props); // Should this check be gated by parent namespace? Not sure we want to
            // allow <SVG> or <mATH>.
            if (!isCustomComponentTag && type !== type.toLowerCase()) error1("<%s /> is using incorrect casing. Use PascalCase for React components, or lowercase for HTML elements.", type);
            if (type === 'script') {
                // Create the script via .innerHTML so its "parser-inserted" flag is
                // set to true and it does not execute
                var div = ownerDocument.createElement('div');
                div.innerHTML = "<script></script>"; // eslint-disable-line
                // This is guaranteed to yield a script element.
                var firstChild = div.firstChild;
                domElement = div.removeChild(firstChild);
            } else if (typeof props.is === 'string') // $FlowIssue `createElement` should be updated for Web Components
            domElement = ownerDocument.createElement(type, {
                is: props.is
            });
            else {
                // Separate else branch instead of using `props.is || undefined` above because of a Firefox bug.
                // See discussion in https://github.com/facebook/react/pull/6896
                // and discussion in https://bugzilla.mozilla.org/show_bug.cgi?id=1276240
                domElement = ownerDocument.createElement(type); // Normally attributes are assigned in `setInitialDOMProperties`, however the `multiple` and `size`
                // attributes on `select`s needs to be added before `option`s are inserted.
                // This prevents:
                // - a bug where the `select` does not scroll to the correct option because singular
                //  `select` elements automatically pick the first item #13222
                // - a bug where the `select` set the first item as selected despite the `size` attribute #14239
                // See https://github.com/facebook/react/issues/13222
                // and https://github.com/facebook/react/issues/14239
                if (type === 'select') {
                    var node = domElement;
                    if (props.multiple) node.multiple = true;
                    else if (props.size) // Setting a size greater than 1 causes a select to behave like `multiple=true`, where
                    // it is possible that no option is selected.
                    //
                    // This is only necessary when a select in "single selection mode".
                    node.size = props.size;
                }
            }
        } else domElement = ownerDocument.createElementNS(namespaceURI, type);
        if (namespaceURI === HTML_NAMESPACE) {
            if (!isCustomComponentTag && Object.prototype.toString.call(domElement) === '[object HTMLUnknownElement]' && !hasOwnProperty.call(warnedUnknownTags, type)) {
                warnedUnknownTags[type] = true;
                error1("The tag <%s> is unrecognized in this browser. If you meant to render a React component, start its name with an uppercase letter.", type);
            }
        }
        return domElement;
    }
    function createTextNode(text, rootContainerElement) {
        return getOwnerDocumentFromRootContainer(rootContainerElement).createTextNode(text);
    }
    function setInitialProperties(domElement, tag, rawProps, rootContainerElement) {
        var isCustomComponentTag = isCustomComponent(tag, rawProps);
        validatePropertiesInDevelopment(tag, rawProps);
        var props;
        switch(tag){
            case 'dialog':
                listenToNonDelegatedEvent('cancel', domElement);
                listenToNonDelegatedEvent('close', domElement);
                props = rawProps;
                break;
            case 'iframe':
            case 'object':
            case 'embed':
                // We listen to this event in case to ensure emulated bubble
                // listeners still fire for the load event.
                listenToNonDelegatedEvent('load', domElement);
                props = rawProps;
                break;
            case 'video':
            case 'audio':
                // We listen to these events in case to ensure emulated bubble
                // listeners still fire for all the media events.
                for(var i = 0; i < mediaEventTypes.length; i++)listenToNonDelegatedEvent(mediaEventTypes[i], domElement);
                props = rawProps;
                break;
            case 'source':
                // We listen to this event in case to ensure emulated bubble
                // listeners still fire for the error event.
                listenToNonDelegatedEvent('error', domElement);
                props = rawProps;
                break;
            case 'img':
            case 'image':
            case 'link':
                // We listen to these events in case to ensure emulated bubble
                // listeners still fire for error and load events.
                listenToNonDelegatedEvent('error', domElement);
                listenToNonDelegatedEvent('load', domElement);
                props = rawProps;
                break;
            case 'details':
                // We listen to this event in case to ensure emulated bubble
                // listeners still fire for the toggle event.
                listenToNonDelegatedEvent('toggle', domElement);
                props = rawProps;
                break;
            case 'input':
                initWrapperState(domElement, rawProps);
                props = getHostProps(domElement, rawProps); // We listen to this event in case to ensure emulated bubble
                // listeners still fire for the invalid event.
                listenToNonDelegatedEvent('invalid', domElement);
                break;
            case 'option':
                validateProps(domElement, rawProps);
                props = rawProps;
                break;
            case 'select':
                initWrapperState$1(domElement, rawProps);
                props = getHostProps$1(domElement, rawProps); // We listen to this event in case to ensure emulated bubble
                // listeners still fire for the invalid event.
                listenToNonDelegatedEvent('invalid', domElement);
                break;
            case 'textarea':
                initWrapperState$2(domElement, rawProps);
                props = getHostProps$2(domElement, rawProps); // We listen to this event in case to ensure emulated bubble
                // listeners still fire for the invalid event.
                listenToNonDelegatedEvent('invalid', domElement);
                break;
            default:
                props = rawProps;
        }
        assertValidProps(tag, props);
        setInitialDOMProperties(tag, domElement, rootContainerElement, props, isCustomComponentTag);
        switch(tag){
            case 'input':
                // TODO: Make sure we check if this is still unmounted or do any clean
                // up necessary since we never stop tracking anymore.
                track(domElement);
                postMountWrapper(domElement, rawProps, false);
                break;
            case 'textarea':
                // TODO: Make sure we check if this is still unmounted or do any clean
                // up necessary since we never stop tracking anymore.
                track(domElement);
                postMountWrapper$3(domElement);
                break;
            case 'option':
                postMountWrapper$1(domElement, rawProps);
                break;
            case 'select':
                postMountWrapper$2(domElement, rawProps);
                break;
            default:
                if (typeof props.onClick === 'function') // TODO: This cast may not be sound for SVG, MathML or custom elements.
                trapClickOnNonInteractiveElement(domElement);
                break;
        }
    } // Calculate the diff between the two objects.
    function diffProperties(domElement, tag, lastRawProps, nextRawProps, rootContainerElement) {
        validatePropertiesInDevelopment(tag, nextRawProps);
        var updatePayload = null;
        var lastProps;
        var nextProps;
        switch(tag){
            case 'input':
                lastProps = getHostProps(domElement, lastRawProps);
                nextProps = getHostProps(domElement, nextRawProps);
                updatePayload = [];
                break;
            case 'select':
                lastProps = getHostProps$1(domElement, lastRawProps);
                nextProps = getHostProps$1(domElement, nextRawProps);
                updatePayload = [];
                break;
            case 'textarea':
                lastProps = getHostProps$2(domElement, lastRawProps);
                nextProps = getHostProps$2(domElement, nextRawProps);
                updatePayload = [];
                break;
            default:
                lastProps = lastRawProps;
                nextProps = nextRawProps;
                if (typeof lastProps.onClick !== 'function' && typeof nextProps.onClick === 'function') // TODO: This cast may not be sound for SVG, MathML or custom elements.
                trapClickOnNonInteractiveElement(domElement);
                break;
        }
        assertValidProps(tag, nextProps);
        var propKey;
        var styleName;
        var styleUpdates = null;
        for(propKey in lastProps){
            if (nextProps.hasOwnProperty(propKey) || !lastProps.hasOwnProperty(propKey) || lastProps[propKey] == null) continue;
            if (propKey === STYLE) {
                var lastStyle = lastProps[propKey];
                for(styleName in lastStyle)if (lastStyle.hasOwnProperty(styleName)) {
                    if (!styleUpdates) styleUpdates = {};
                    styleUpdates[styleName] = '';
                }
            } else if (propKey === DANGEROUSLY_SET_INNER_HTML || propKey === CHILDREN) ;
            else if (propKey === SUPPRESS_CONTENT_EDITABLE_WARNING || propKey === SUPPRESS_HYDRATION_WARNING) ;
            else if (propKey === AUTOFOCUS) ;
            else if (registrationNameDependencies1.hasOwnProperty(propKey)) // This is a special case. If any listener updates we need to ensure
            // that the "current" fiber pointer gets updated so we need a commit
            // to update this element.
            {
                if (!updatePayload) updatePayload = [];
            } else // For all other deleted properties we add it to the queue. We use
            // the allowed property list in the commit phase instead.
            (updatePayload = updatePayload || []).push(propKey, null);
        }
        for(propKey in nextProps){
            var nextProp = nextProps[propKey];
            var lastProp = lastProps != null ? lastProps[propKey] : undefined;
            if (!nextProps.hasOwnProperty(propKey) || nextProp === lastProp || nextProp == null && lastProp == null) continue;
            if (propKey === STYLE) {
                if (nextProp) // Freeze the next style object so that we can assume it won't be
                // mutated. We have already warned for this in the past.
                Object.freeze(nextProp);
                if (lastProp) {
                    // Unset styles on `lastProp` but not on `nextProp`.
                    for(styleName in lastProp)if (lastProp.hasOwnProperty(styleName) && (!nextProp || !nextProp.hasOwnProperty(styleName))) {
                        if (!styleUpdates) styleUpdates = {};
                        styleUpdates[styleName] = '';
                    }
                     // Update styles that changed since `lastProp`.
                    for(styleName in nextProp)if (nextProp.hasOwnProperty(styleName) && lastProp[styleName] !== nextProp[styleName]) {
                        if (!styleUpdates) styleUpdates = {};
                        styleUpdates[styleName] = nextProp[styleName];
                    }
                } else {
                    // Relies on `updateStylesByID` not mutating `styleUpdates`.
                    if (!styleUpdates) {
                        if (!updatePayload) updatePayload = [];
                        updatePayload.push(propKey, styleUpdates);
                    }
                    styleUpdates = nextProp;
                }
            } else if (propKey === DANGEROUSLY_SET_INNER_HTML) {
                var nextHtml = nextProp ? nextProp[HTML$1] : undefined;
                var lastHtml = lastProp ? lastProp[HTML$1] : undefined;
                if (nextHtml != null) {
                    if (lastHtml !== nextHtml) (updatePayload = updatePayload || []).push(propKey, nextHtml);
                }
            } else if (propKey === CHILDREN) {
                if (typeof nextProp === 'string' || typeof nextProp === 'number') (updatePayload = updatePayload || []).push(propKey, '' + nextProp);
            } else if (propKey === SUPPRESS_CONTENT_EDITABLE_WARNING || propKey === SUPPRESS_HYDRATION_WARNING) ;
            else if (registrationNameDependencies1.hasOwnProperty(propKey)) {
                if (nextProp != null) {
                    // We eagerly listen to this even though we haven't committed yet.
                    if (typeof nextProp !== 'function') warnForInvalidEventListener(propKey, nextProp);
                    if (propKey === 'onScroll') listenToNonDelegatedEvent('scroll', domElement);
                }
                if (!updatePayload && lastProp !== nextProp) // This is a special case. If any listener updates we need to ensure
                // that the "current" props pointer gets updated so we need a commit
                // to update this element.
                updatePayload = [];
            } else // For any other property we always add it to the queue and then we
            // filter it out using the allowed property list during the commit.
            (updatePayload = updatePayload || []).push(propKey, nextProp);
        }
        if (styleUpdates) {
            validateShorthandPropertyCollisionInDev(styleUpdates, nextProps[STYLE]);
            (updatePayload = updatePayload || []).push(STYLE, styleUpdates);
        }
        return updatePayload;
    } // Apply the diff.
    function updateProperties(domElement, updatePayload, tag, lastRawProps, nextRawProps) {
        // Update checked *before* name.
        // In the middle of an update, it is possible to have multiple checked.
        // When a checked radio tries to change name, browser makes another radio's checked false.
        if (tag === 'input' && nextRawProps.type === 'radio' && nextRawProps.name != null) updateChecked(domElement, nextRawProps);
        var wasCustomComponentTag = isCustomComponent(tag, lastRawProps);
        var isCustomComponentTag = isCustomComponent(tag, nextRawProps); // Apply the diff.
        updateDOMProperties(domElement, updatePayload, wasCustomComponentTag, isCustomComponentTag); // TODO: Ensure that an update gets scheduled if any of the special props
        // changed.
        switch(tag){
            case 'input':
                // Update the wrapper around inputs *after* updating props. This has to
                // happen after `updateDOMProperties`. Otherwise HTML5 input validations
                // raise warnings and prevent the new value from being assigned.
                updateWrapper(domElement, nextRawProps);
                break;
            case 'textarea':
                updateWrapper$1(domElement, nextRawProps);
                break;
            case 'select':
                // <select> value update needs to occur after <option> children
                // reconciliation
                postUpdateWrapper(domElement, nextRawProps);
                break;
        }
    }
    function getPossibleStandardName(propName) {
        var lowerCasedName = propName.toLowerCase();
        if (!possibleStandardNames.hasOwnProperty(lowerCasedName)) return null;
        return possibleStandardNames[lowerCasedName] || null;
    }
    function diffHydratedProperties(domElement, tag, rawProps, parentNamespace, rootContainerElement, isConcurrentMode, shouldWarnDev) {
        var isCustomComponentTag;
        var extraAttributeNames;
        isCustomComponentTag = isCustomComponent(tag, rawProps);
        validatePropertiesInDevelopment(tag, rawProps);
        switch(tag){
            case 'dialog':
                listenToNonDelegatedEvent('cancel', domElement);
                listenToNonDelegatedEvent('close', domElement);
                break;
            case 'iframe':
            case 'object':
            case 'embed':
                // We listen to this event in case to ensure emulated bubble
                // listeners still fire for the load event.
                listenToNonDelegatedEvent('load', domElement);
                break;
            case 'video':
            case 'audio':
                // We listen to these events in case to ensure emulated bubble
                // listeners still fire for all the media events.
                for(var i = 0; i < mediaEventTypes.length; i++)listenToNonDelegatedEvent(mediaEventTypes[i], domElement);
                break;
            case 'source':
                // We listen to this event in case to ensure emulated bubble
                // listeners still fire for the error event.
                listenToNonDelegatedEvent('error', domElement);
                break;
            case 'img':
            case 'image':
            case 'link':
                // We listen to these events in case to ensure emulated bubble
                // listeners still fire for error and load events.
                listenToNonDelegatedEvent('error', domElement);
                listenToNonDelegatedEvent('load', domElement);
                break;
            case 'details':
                // We listen to this event in case to ensure emulated bubble
                // listeners still fire for the toggle event.
                listenToNonDelegatedEvent('toggle', domElement);
                break;
            case 'input':
                initWrapperState(domElement, rawProps); // We listen to this event in case to ensure emulated bubble
                // listeners still fire for the invalid event.
                listenToNonDelegatedEvent('invalid', domElement);
                break;
            case 'option':
                validateProps(domElement, rawProps);
                break;
            case 'select':
                initWrapperState$1(domElement, rawProps); // We listen to this event in case to ensure emulated bubble
                // listeners still fire for the invalid event.
                listenToNonDelegatedEvent('invalid', domElement);
                break;
            case 'textarea':
                initWrapperState$2(domElement, rawProps); // We listen to this event in case to ensure emulated bubble
                // listeners still fire for the invalid event.
                listenToNonDelegatedEvent('invalid', domElement);
                break;
        }
        assertValidProps(tag, rawProps);
        extraAttributeNames = new Set();
        var attributes = domElement.attributes;
        for(var _i = 0; _i < attributes.length; _i++){
            var name = attributes[_i].name.toLowerCase();
            switch(name){
                // Controlled attributes are not validated
                // TODO: Only ignore them on controlled tags.
                case 'value':
                    break;
                case 'checked':
                    break;
                case 'selected':
                    break;
                default:
                    // Intentionally use the original name.
                    // See discussion in https://github.com/facebook/react/pull/10676.
                    extraAttributeNames.add(attributes[_i].name);
            }
        }
        var updatePayload = null;
        for(var propKey in rawProps){
            if (!rawProps.hasOwnProperty(propKey)) continue;
            var nextProp = rawProps[propKey];
            if (propKey === CHILDREN) {
                // For text content children we compare against textContent. This
                // might match additional HTML that is hidden when we read it using
                // textContent. E.g. "foo" will match "f<span>oo</span>" but that still
                // satisfies our requirement. Our requirement is not to produce perfect
                // HTML and attributes. Ideally we should preserve structure but it's
                // ok not to if the visible content is still enough to indicate what
                // even listeners these nodes might be wired up to.
                // TODO: Warn if there is more than a single textNode as a child.
                // TODO: Should we use domElement.firstChild.nodeValue to compare?
                if (typeof nextProp === 'string') {
                    if (domElement.textContent !== nextProp) {
                        if (rawProps[SUPPRESS_HYDRATION_WARNING] !== true) checkForUnmatchedText(domElement.textContent, nextProp, isConcurrentMode, shouldWarnDev);
                        updatePayload = [
                            CHILDREN,
                            nextProp
                        ];
                    }
                } else if (typeof nextProp === 'number') {
                    if (domElement.textContent !== '' + nextProp) {
                        if (rawProps[SUPPRESS_HYDRATION_WARNING] !== true) checkForUnmatchedText(domElement.textContent, nextProp, isConcurrentMode, shouldWarnDev);
                        updatePayload = [
                            CHILDREN,
                            '' + nextProp
                        ];
                    }
                }
            } else if (registrationNameDependencies1.hasOwnProperty(propKey)) {
                if (nextProp != null) {
                    if (typeof nextProp !== 'function') warnForInvalidEventListener(propKey, nextProp);
                    if (propKey === 'onScroll') listenToNonDelegatedEvent('scroll', domElement);
                }
            } else if (shouldWarnDev && true && typeof isCustomComponentTag === 'boolean') {
                // Validate that the properties correspond to their expected values.
                var serverValue = void 0;
                var propertyInfo = isCustomComponentTag && enableCustomElementPropertySupport ? null : getPropertyInfo(propKey);
                if (rawProps[SUPPRESS_HYDRATION_WARNING] === true) ;
                else if (propKey === SUPPRESS_CONTENT_EDITABLE_WARNING || propKey === SUPPRESS_HYDRATION_WARNING || // TODO: Only ignore them on controlled tags.
                propKey === 'value' || propKey === 'checked' || propKey === 'selected') ;
                else if (propKey === DANGEROUSLY_SET_INNER_HTML) {
                    var serverHTML = domElement.innerHTML;
                    var nextHtml = nextProp ? nextProp[HTML$1] : undefined;
                    if (nextHtml != null) {
                        var expectedHTML = normalizeHTML(domElement, nextHtml);
                        if (expectedHTML !== serverHTML) warnForPropDifference(propKey, serverHTML, expectedHTML);
                    }
                } else if (propKey === STYLE) {
                    // $FlowFixMe - Should be inferred as not undefined.
                    extraAttributeNames.delete(propKey);
                    if (canDiffStyleForHydrationWarning) {
                        var expectedStyle = createDangerousStringForStyles(nextProp);
                        serverValue = domElement.getAttribute('style');
                        if (expectedStyle !== serverValue) warnForPropDifference(propKey, serverValue, expectedStyle);
                    }
                } else if (isCustomComponentTag && !enableCustomElementPropertySupport) {
                    // $FlowFixMe - Should be inferred as not undefined.
                    extraAttributeNames.delete(propKey.toLowerCase());
                    serverValue = getValueForAttribute(domElement, propKey, nextProp);
                    if (nextProp !== serverValue) warnForPropDifference(propKey, serverValue, nextProp);
                } else if (!shouldIgnoreAttribute(propKey, propertyInfo, isCustomComponentTag) && !shouldRemoveAttribute(propKey, nextProp, propertyInfo, isCustomComponentTag)) {
                    var isMismatchDueToBadCasing = false;
                    if (propertyInfo !== null) {
                        // $FlowFixMe - Should be inferred as not undefined.
                        extraAttributeNames.delete(propertyInfo.attributeName);
                        serverValue = getValueForProperty(domElement, propKey, nextProp, propertyInfo);
                    } else {
                        var ownNamespace = parentNamespace;
                        if (ownNamespace === HTML_NAMESPACE) ownNamespace = getIntrinsicNamespace(tag);
                        if (ownNamespace === HTML_NAMESPACE) // $FlowFixMe - Should be inferred as not undefined.
                        extraAttributeNames.delete(propKey.toLowerCase());
                        else {
                            var standardName = getPossibleStandardName(propKey);
                            if (standardName !== null && standardName !== propKey) {
                                // If an SVG prop is supplied with bad casing, it will
                                // be successfully parsed from HTML, but will produce a mismatch
                                // (and would be incorrectly rendered on the client).
                                // However, we already warn about bad casing elsewhere.
                                // So we'll skip the misleading extra mismatch warning in this case.
                                isMismatchDueToBadCasing = true; // $FlowFixMe - Should be inferred as not undefined.
                                extraAttributeNames.delete(standardName);
                            } // $FlowFixMe - Should be inferred as not undefined.
                            extraAttributeNames.delete(propKey);
                        }
                        serverValue = getValueForAttribute(domElement, propKey, nextProp);
                    }
                    var dontWarnCustomElement = enableCustomElementPropertySupport;
                    if (!dontWarnCustomElement && nextProp !== serverValue && !isMismatchDueToBadCasing) warnForPropDifference(propKey, serverValue, nextProp);
                }
            }
        }
        if (shouldWarnDev) {
            if (extraAttributeNames.size > 0 && rawProps[SUPPRESS_HYDRATION_WARNING] !== true) // $FlowFixMe - Should be inferred as not undefined.
            warnForExtraAttributes(extraAttributeNames);
        }
        switch(tag){
            case 'input':
                // TODO: Make sure we check if this is still unmounted or do any clean
                // up necessary since we never stop tracking anymore.
                track(domElement);
                postMountWrapper(domElement, rawProps, true);
                break;
            case 'textarea':
                // TODO: Make sure we check if this is still unmounted or do any clean
                // up necessary since we never stop tracking anymore.
                track(domElement);
                postMountWrapper$3(domElement);
                break;
            case 'select':
            case 'option':
                break;
            default:
                if (typeof rawProps.onClick === 'function') // TODO: This cast may not be sound for SVG, MathML or custom elements.
                trapClickOnNonInteractiveElement(domElement);
                break;
        }
        return updatePayload;
    }
    function diffHydratedText(textNode, text, isConcurrentMode) {
        var isDifferent = textNode.nodeValue !== text;
        return isDifferent;
    }
    function warnForDeletedHydratableElement(parentNode, child) {
        if (didWarnInvalidHydration) return;
        didWarnInvalidHydration = true;
        error1('Did not expect server HTML to contain a <%s> in <%s>.', child.nodeName.toLowerCase(), parentNode.nodeName.toLowerCase());
    }
    function warnForDeletedHydratableText(parentNode, child) {
        if (didWarnInvalidHydration) return;
        didWarnInvalidHydration = true;
        error1('Did not expect server HTML to contain the text node "%s" in <%s>.', child.nodeValue, parentNode.nodeName.toLowerCase());
    }
    function warnForInsertedHydratedElement(parentNode, tag, props) {
        if (didWarnInvalidHydration) return;
        didWarnInvalidHydration = true;
        error1('Expected server HTML to contain a matching <%s> in <%s>.', tag, parentNode.nodeName.toLowerCase());
    }
    function warnForInsertedHydratedText(parentNode, text) {
        if (text === '') // We expect to insert empty text nodes since they're not represented in
        // the HTML.
        // TODO: Remove this special case if we can just avoid inserting empty
        // text nodes.
        return;
        if (didWarnInvalidHydration) return;
        didWarnInvalidHydration = true;
        error1('Expected server HTML to contain a matching text node for "%s" in <%s>.', text, parentNode.nodeName.toLowerCase());
    }
    function restoreControlledState$3(domElement, tag, props) {
        switch(tag){
            case 'input':
                restoreControlledState(domElement, props);
                return;
            case 'textarea':
                restoreControlledState$2(domElement, props);
                return;
            case 'select':
                restoreControlledState$1(domElement, props);
                return;
        }
    }
    var validateDOMNesting = function() {};
    var updatedAncestorInfo = function() {};
    // This validation code was written based on the HTML5 parsing spec:
    // https://html.spec.whatwg.org/multipage/syntax.html#has-an-element-in-scope
    //
    // Note: this does not catch all invalid nesting, nor does it try to (as it's
    // not clear what practical benefit doing so provides); instead, we warn only
    // for cases where the parser will give a parse tree differing from what React
    // intended. For example, <b><div></div></b> is invalid but we don't warn
    // because it still parses correctly; we do warn for other cases like nested
    // <p> tags where the beginning of the second element implicitly closes the
    // first, causing a confusing mess.
    // https://html.spec.whatwg.org/multipage/syntax.html#special
    var specialTags = [
        'address',
        'applet',
        'area',
        'article',
        'aside',
        'base',
        'basefont',
        'bgsound',
        'blockquote',
        'body',
        'br',
        'button',
        'caption',
        'center',
        'col',
        'colgroup',
        'dd',
        'details',
        'dir',
        'div',
        'dl',
        'dt',
        'embed',
        'fieldset',
        'figcaption',
        'figure',
        'footer',
        'form',
        'frame',
        'frameset',
        'h1',
        'h2',
        'h3',
        'h4',
        'h5',
        'h6',
        'head',
        'header',
        'hgroup',
        'hr',
        'html',
        'iframe',
        'img',
        'input',
        'isindex',
        'li',
        'link',
        'listing',
        'main',
        'marquee',
        'menu',
        'menuitem',
        'meta',
        'nav',
        'noembed',
        'noframes',
        'noscript',
        'object',
        'ol',
        'p',
        'param',
        'plaintext',
        'pre',
        'script',
        'section',
        'select',
        'source',
        'style',
        'summary',
        'table',
        'tbody',
        'td',
        'template',
        'textarea',
        'tfoot',
        'th',
        'thead',
        'title',
        'tr',
        'track',
        'ul',
        'wbr',
        'xmp'
    ]; // https://html.spec.whatwg.org/multipage/syntax.html#has-an-element-in-scope
    var inScopeTags = [
        'applet',
        'caption',
        'html',
        'table',
        'td',
        'th',
        'marquee',
        'object',
        'template',
        // TODO: Distinguish by namespace here -- for <title>, including it here
        // errs on the side of fewer warnings
        'foreignObject',
        'desc',
        'title'
    ]; // https://html.spec.whatwg.org/multipage/syntax.html#has-an-element-in-button-scope
    var buttonScopeTags = inScopeTags.concat([
        'button'
    ]); // https://html.spec.whatwg.org/multipage/syntax.html#generate-implied-end-tags
    var impliedEndTags = [
        'dd',
        'dt',
        'li',
        'option',
        'optgroup',
        'p',
        'rp',
        'rt'
    ];
    var emptyAncestorInfo = {
        current: null,
        formTag: null,
        aTagInScope: null,
        buttonTagInScope: null,
        nobrTagInScope: null,
        pTagInButtonScope: null,
        listItemTagAutoclosing: null,
        dlItemTagAutoclosing: null
    };
    updatedAncestorInfo = function(oldInfo, tag) {
        var ancestorInfo = assign({}, oldInfo || emptyAncestorInfo);
        var info = {
            tag: tag
        };
        if (inScopeTags.indexOf(tag) !== -1) {
            ancestorInfo.aTagInScope = null;
            ancestorInfo.buttonTagInScope = null;
            ancestorInfo.nobrTagInScope = null;
        }
        if (buttonScopeTags.indexOf(tag) !== -1) ancestorInfo.pTagInButtonScope = null;
         // See rules for 'li', 'dd', 'dt' start tags in
        // https://html.spec.whatwg.org/multipage/syntax.html#parsing-main-inbody
        if (specialTags.indexOf(tag) !== -1 && tag !== 'address' && tag !== 'div' && tag !== 'p') {
            ancestorInfo.listItemTagAutoclosing = null;
            ancestorInfo.dlItemTagAutoclosing = null;
        }
        ancestorInfo.current = info;
        if (tag === 'form') ancestorInfo.formTag = info;
        if (tag === 'a') ancestorInfo.aTagInScope = info;
        if (tag === 'button') ancestorInfo.buttonTagInScope = info;
        if (tag === 'nobr') ancestorInfo.nobrTagInScope = info;
        if (tag === 'p') ancestorInfo.pTagInButtonScope = info;
        if (tag === 'li') ancestorInfo.listItemTagAutoclosing = info;
        if (tag === 'dd' || tag === 'dt') ancestorInfo.dlItemTagAutoclosing = info;
        return ancestorInfo;
    };
    /**
   * Returns whether
   */ var isTagValidWithParent = function(tag, parentTag) {
        // First, let's check if we're in an unusual parsing mode...
        switch(parentTag){
            // https://html.spec.whatwg.org/multipage/syntax.html#parsing-main-inselect
            case 'select':
                return tag === 'option' || tag === 'optgroup' || tag === '#text';
            case 'optgroup':
                return tag === 'option' || tag === '#text';
            // Strictly speaking, seeing an <option> doesn't mean we're in a <select>
            // but
            case 'option':
                return tag === '#text';
            // https://html.spec.whatwg.org/multipage/syntax.html#parsing-main-intd
            // https://html.spec.whatwg.org/multipage/syntax.html#parsing-main-incaption
            // No special behavior since these rules fall back to "in body" mode for
            // all except special table nodes which cause bad parsing behavior anyway.
            // https://html.spec.whatwg.org/multipage/syntax.html#parsing-main-intr
            case 'tr':
                return tag === 'th' || tag === 'td' || tag === 'style' || tag === 'script' || tag === 'template';
            // https://html.spec.whatwg.org/multipage/syntax.html#parsing-main-intbody
            case 'tbody':
            case 'thead':
            case 'tfoot':
                return tag === 'tr' || tag === 'style' || tag === 'script' || tag === 'template';
            // https://html.spec.whatwg.org/multipage/syntax.html#parsing-main-incolgroup
            case 'colgroup':
                return tag === 'col' || tag === 'template';
            // https://html.spec.whatwg.org/multipage/syntax.html#parsing-main-intable
            case 'table':
                return tag === 'caption' || tag === 'colgroup' || tag === 'tbody' || tag === 'tfoot' || tag === 'thead' || tag === 'style' || tag === 'script' || tag === 'template';
            // https://html.spec.whatwg.org/multipage/syntax.html#parsing-main-inhead
            case 'head':
                return tag === 'base' || tag === 'basefont' || tag === 'bgsound' || tag === 'link' || tag === 'meta' || tag === 'title' || tag === 'noscript' || tag === 'noframes' || tag === 'style' || tag === 'script' || tag === 'template';
            // https://html.spec.whatwg.org/multipage/semantics.html#the-html-element
            case 'html':
                return tag === 'head' || tag === 'body' || tag === 'frameset';
            case 'frameset':
                return tag === 'frame';
            case '#document':
                return tag === 'html';
        } // Probably in the "in body" parsing mode, so we outlaw only tag combos
        // where the parsing rules cause implicit opens or closes to be added.
        // https://html.spec.whatwg.org/multipage/syntax.html#parsing-main-inbody
        switch(tag){
            case 'h1':
            case 'h2':
            case 'h3':
            case 'h4':
            case 'h5':
            case 'h6':
                return parentTag !== 'h1' && parentTag !== 'h2' && parentTag !== 'h3' && parentTag !== 'h4' && parentTag !== 'h5' && parentTag !== 'h6';
            case 'rp':
            case 'rt':
                return impliedEndTags.indexOf(parentTag) === -1;
            case 'body':
            case 'caption':
            case 'col':
            case 'colgroup':
            case 'frameset':
            case 'frame':
            case 'head':
            case 'html':
            case 'tbody':
            case 'td':
            case 'tfoot':
            case 'th':
            case 'thead':
            case 'tr':
                // These tags are only valid with a few parents that have special child
                // parsing rules -- if we're down here, then none of those matched and
                // so we allow it only if we don't know what the parent is, as all other
                // cases are invalid.
                return parentTag == null;
        }
        return true;
    };
    /**
   * Returns whether
   */ var findInvalidAncestorForTag = function(tag, ancestorInfo) {
        switch(tag){
            case 'address':
            case 'article':
            case 'aside':
            case 'blockquote':
            case 'center':
            case 'details':
            case 'dialog':
            case 'dir':
            case 'div':
            case 'dl':
            case 'fieldset':
            case 'figcaption':
            case 'figure':
            case 'footer':
            case 'header':
            case 'hgroup':
            case 'main':
            case 'menu':
            case 'nav':
            case 'ol':
            case 'p':
            case 'section':
            case 'summary':
            case 'ul':
            case 'pre':
            case 'listing':
            case 'table':
            case 'hr':
            case 'xmp':
            case 'h1':
            case 'h2':
            case 'h3':
            case 'h4':
            case 'h5':
            case 'h6':
                return ancestorInfo.pTagInButtonScope;
            case 'form':
                return ancestorInfo.formTag || ancestorInfo.pTagInButtonScope;
            case 'li':
                return ancestorInfo.listItemTagAutoclosing;
            case 'dd':
            case 'dt':
                return ancestorInfo.dlItemTagAutoclosing;
            case 'button':
                return ancestorInfo.buttonTagInScope;
            case 'a':
                // Spec says something about storing a list of markers, but it sounds
                // equivalent to this check.
                return ancestorInfo.aTagInScope;
            case 'nobr':
                return ancestorInfo.nobrTagInScope;
        }
        return null;
    };
    var didWarn$1 = {};
    validateDOMNesting = function(childTag, childText, ancestorInfo) {
        ancestorInfo = ancestorInfo || emptyAncestorInfo;
        var parentInfo = ancestorInfo.current;
        var parentTag = parentInfo && parentInfo.tag;
        if (childText != null) {
            if (childTag != null) error1('validateDOMNesting: when childText is passed, childTag should be null');
            childTag = '#text';
        }
        var invalidParent = isTagValidWithParent(childTag, parentTag) ? null : parentInfo;
        var invalidAncestor = invalidParent ? null : findInvalidAncestorForTag(childTag, ancestorInfo);
        var invalidParentOrAncestor = invalidParent || invalidAncestor;
        if (!invalidParentOrAncestor) return;
        var ancestorTag = invalidParentOrAncestor.tag;
        var warnKey = !!invalidParent + '|' + childTag + '|' + ancestorTag;
        if (didWarn$1[warnKey]) return;
        didWarn$1[warnKey] = true;
        var tagDisplayName = childTag;
        var whitespaceInfo = '';
        if (childTag === '#text') {
            if (/\S/.test(childText)) tagDisplayName = 'Text nodes';
            else {
                tagDisplayName = 'Whitespace text nodes';
                whitespaceInfo = " Make sure you don't have any extra whitespace between tags on each line of your source code.";
            }
        } else tagDisplayName = '<' + childTag + '>';
        if (invalidParent) {
            var info = '';
            if (ancestorTag === 'table' && childTag === 'tr') info += " Add a <tbody>, <thead> or <tfoot> to your code to match the DOM tree generated by the browser.";
            error1('validateDOMNesting(...): %s cannot appear as a child of <%s>.%s%s', tagDisplayName, ancestorTag, whitespaceInfo, info);
        } else error1("validateDOMNesting(...): %s cannot appear as a descendant of <%s>.", tagDisplayName, ancestorTag);
    };
    var SUPPRESS_HYDRATION_WARNING$1 = 'suppressHydrationWarning';
    var SUSPENSE_START_DATA = '$';
    var SUSPENSE_END_DATA = '/$';
    var SUSPENSE_PENDING_START_DATA = '$?';
    var SUSPENSE_FALLBACK_START_DATA = '$!';
    var STYLE$1 = 'style';
    var eventsEnabled = null;
    var selectionInformation = null;
    function getRootHostContext(rootContainerInstance) {
        var type;
        var namespace;
        var nodeType = rootContainerInstance.nodeType;
        switch(nodeType){
            case DOCUMENT_NODE:
            case DOCUMENT_FRAGMENT_NODE:
                type = nodeType === DOCUMENT_NODE ? '#document' : '#fragment';
                var root = rootContainerInstance.documentElement;
                namespace = root ? root.namespaceURI : getChildNamespace(null, '');
                break;
            default:
                var container = nodeType === COMMENT_NODE ? rootContainerInstance.parentNode : rootContainerInstance;
                var ownNamespace = container.namespaceURI || null;
                type = container.tagName;
                namespace = getChildNamespace(ownNamespace, type);
                break;
        }
        var validatedTag = type.toLowerCase();
        var ancestorInfo = updatedAncestorInfo(null, validatedTag);
        return {
            namespace: namespace,
            ancestorInfo: ancestorInfo
        };
    }
    function getChildHostContext(parentHostContext, type, rootContainerInstance) {
        var parentHostContextDev = parentHostContext;
        var namespace = getChildNamespace(parentHostContextDev.namespace, type);
        var ancestorInfo = updatedAncestorInfo(parentHostContextDev.ancestorInfo, type);
        return {
            namespace: namespace,
            ancestorInfo: ancestorInfo
        };
    }
    function getPublicInstance(instance) {
        return instance;
    }
    function prepareForCommit(containerInfo) {
        eventsEnabled = isEnabled();
        selectionInformation = getSelectionInformation();
        var activeInstance = null;
        setEnabled(false);
        return activeInstance;
    }
    function resetAfterCommit(containerInfo) {
        restoreSelection(selectionInformation);
        setEnabled(eventsEnabled);
        eventsEnabled = null;
        selectionInformation = null;
    }
    function createInstance(type, props, rootContainerInstance, hostContext, internalInstanceHandle) {
        var parentNamespace;
        // TODO: take namespace into account when validating.
        var hostContextDev = hostContext;
        validateDOMNesting(type, null, hostContextDev.ancestorInfo);
        if (typeof props.children === 'string' || typeof props.children === 'number') {
            var string = '' + props.children;
            var ownAncestorInfo = updatedAncestorInfo(hostContextDev.ancestorInfo, type);
            validateDOMNesting(null, string, ownAncestorInfo);
        }
        parentNamespace = hostContextDev.namespace;
        var domElement = createElement(type, props, rootContainerInstance, parentNamespace);
        precacheFiberNode(internalInstanceHandle, domElement);
        updateFiberProps(domElement, props);
        return domElement;
    }
    function appendInitialChild(parentInstance, child) {
        parentInstance.appendChild(child);
    }
    function finalizeInitialChildren(domElement, type, props, rootContainerInstance, hostContext) {
        setInitialProperties(domElement, type, props, rootContainerInstance);
        switch(type){
            case 'button':
            case 'input':
            case 'select':
            case 'textarea':
                return !!props.autoFocus;
            case 'img':
                return true;
            default:
                return false;
        }
    }
    function prepareUpdate(domElement, type, oldProps, newProps, rootContainerInstance, hostContext) {
        var hostContextDev = hostContext;
        if (typeof newProps.children !== typeof oldProps.children && (typeof newProps.children === 'string' || typeof newProps.children === 'number')) {
            var string = '' + newProps.children;
            var ownAncestorInfo = updatedAncestorInfo(hostContextDev.ancestorInfo, type);
            validateDOMNesting(null, string, ownAncestorInfo);
        }
        return diffProperties(domElement, type, oldProps, newProps);
    }
    function shouldSetTextContent(type, props) {
        return type === 'textarea' || type === 'noscript' || typeof props.children === 'string' || typeof props.children === 'number' || typeof props.dangerouslySetInnerHTML === 'object' && props.dangerouslySetInnerHTML !== null && props.dangerouslySetInnerHTML.__html != null;
    }
    function createTextInstance(text, rootContainerInstance, hostContext, internalInstanceHandle) {
        var hostContextDev = hostContext;
        validateDOMNesting(null, text, hostContextDev.ancestorInfo);
        var textNode = createTextNode(text, rootContainerInstance);
        precacheFiberNode(internalInstanceHandle, textNode);
        return textNode;
    }
    function getCurrentEventPriority() {
        var currentEvent = window.event;
        if (currentEvent === undefined) return DefaultEventPriority;
        return getEventPriority(currentEvent.type);
    }
    // if a component just imports ReactDOM (e.g. for findDOMNode).
    // Some environments might not have setTimeout or clearTimeout.
    var scheduleTimeout = typeof setTimeout === 'function' ? setTimeout : undefined;
    var cancelTimeout = typeof clearTimeout === 'function' ? clearTimeout : undefined;
    var noTimeout = -1;
    var localPromise = typeof Promise === 'function' ? Promise : undefined; // -------------------
    var scheduleMicrotask = typeof queueMicrotask === 'function' ? queueMicrotask : typeof localPromise !== 'undefined' ? function(callback) {
        return localPromise.resolve(null).then(callback).catch(handleErrorInNextTick);
    } : scheduleTimeout; // TODO: Determine the best fallback here.
    function handleErrorInNextTick(error) {
        setTimeout(function() {
            throw error;
        });
    } // -------------------
    function commitMount(domElement, type, newProps, internalInstanceHandle) {
        // Despite the naming that might imply otherwise, this method only
        // fires if there is an `Update` effect scheduled during mounting.
        // This happens if `finalizeInitialChildren` returns `true` (which it
        // does to implement the `autoFocus` attribute on the client). But
        // there are also other cases when this might happen (such as patching
        // up text content during hydration mismatch). So we'll check this again.
        switch(type){
            case 'button':
            case 'input':
            case 'select':
            case 'textarea':
                if (newProps.autoFocus) domElement.focus();
                return;
            case 'img':
                if (newProps.src) domElement.src = newProps.src;
                return;
        }
    }
    function commitUpdate(domElement, updatePayload, type, oldProps, newProps, internalInstanceHandle) {
        // Apply the diff to the DOM node.
        updateProperties(domElement, updatePayload, type, oldProps, newProps); // Update the props handle so that we know which props are the ones with
        // with current event handlers.
        updateFiberProps(domElement, newProps);
    }
    function resetTextContent(domElement) {
        setTextContent(domElement, '');
    }
    function commitTextUpdate(textInstance, oldText, newText) {
        textInstance.nodeValue = newText;
    }
    function appendChild(parentInstance, child) {
        parentInstance.appendChild(child);
    }
    function appendChildToContainer(container, child) {
        var parentNode;
        if (container.nodeType === COMMENT_NODE) {
            parentNode = container.parentNode;
            parentNode.insertBefore(child, container);
        } else {
            parentNode = container;
            parentNode.appendChild(child);
        } // This container might be used for a portal.
        // If something inside a portal is clicked, that click should bubble
        // through the React tree. However, on Mobile Safari the click would
        // never bubble through the *DOM* tree unless an ancestor with onclick
        // event exists. So we wouldn't see it and dispatch it.
        // This is why we ensure that non React root containers have inline onclick
        // defined.
        // https://github.com/facebook/react/issues/11918
        var reactRootContainer = container._reactRootContainer;
        if ((reactRootContainer === null || reactRootContainer === undefined) && parentNode.onclick === null) // TODO: This cast may not be sound for SVG, MathML or custom elements.
        trapClickOnNonInteractiveElement(parentNode);
    }
    function insertBefore(parentInstance, child, beforeChild) {
        parentInstance.insertBefore(child, beforeChild);
    }
    function insertInContainerBefore(container, child, beforeChild) {
        if (container.nodeType === COMMENT_NODE) container.parentNode.insertBefore(child, beforeChild);
        else container.insertBefore(child, beforeChild);
    }
    function removeChild(parentInstance, child) {
        parentInstance.removeChild(child);
    }
    function removeChildFromContainer(container, child) {
        if (container.nodeType === COMMENT_NODE) container.parentNode.removeChild(child);
        else container.removeChild(child);
    }
    function clearSuspenseBoundary(parentInstance, suspenseInstance) {
        var node = suspenseInstance; // Delete all nodes within this suspense boundary.
        // There might be nested nodes so we need to keep track of how
        // deep we are and only break out when we're back on top.
        var depth = 0;
        do {
            var nextNode = node.nextSibling;
            parentInstance.removeChild(node);
            if (nextNode && nextNode.nodeType === COMMENT_NODE) {
                var data = nextNode.data;
                if (data === SUSPENSE_END_DATA) {
                    if (depth === 0) {
                        parentInstance.removeChild(nextNode); // Retry if any event replaying was blocked on this.
                        retryIfBlockedOn(suspenseInstance);
                        return;
                    } else depth--;
                } else if (data === SUSPENSE_START_DATA || data === SUSPENSE_PENDING_START_DATA || data === SUSPENSE_FALLBACK_START_DATA) depth++;
            }
            node = nextNode;
        }while (node) // TODO: Warn, we didn't find the end comment boundary.
        // Retry if any event replaying was blocked on this.
        retryIfBlockedOn(suspenseInstance);
    }
    function clearSuspenseBoundaryFromContainer(container, suspenseInstance) {
        if (container.nodeType === COMMENT_NODE) clearSuspenseBoundary(container.parentNode, suspenseInstance);
        else if (container.nodeType === ELEMENT_NODE) clearSuspenseBoundary(container, suspenseInstance);
         // Retry if any event replaying was blocked on this.
        retryIfBlockedOn(container);
    }
    function hideInstance(instance) {
        var style = instance.style;
        if (typeof style.setProperty === 'function') style.setProperty('display', 'none', 'important');
        else style.display = 'none';
    }
    function hideTextInstance(textInstance) {
        textInstance.nodeValue = '';
    }
    function unhideInstance(instance, props) {
        var styleProp = props[STYLE$1];
        var display = styleProp !== undefined && styleProp !== null && styleProp.hasOwnProperty('display') ? styleProp.display : null;
        instance.style.display = dangerousStyleValue('display', display);
    }
    function unhideTextInstance(textInstance, text) {
        textInstance.nodeValue = text;
    }
    function clearContainer(container) {
        if (container.nodeType === ELEMENT_NODE) container.textContent = '';
        else if (container.nodeType === DOCUMENT_NODE) {
            var body = container.body;
            if (body != null) body.textContent = '';
        }
    } // -------------------
    function canHydrateInstance(instance, type, props) {
        if (instance.nodeType !== ELEMENT_NODE || type.toLowerCase() !== instance.nodeName.toLowerCase()) return null;
         // This has now been refined to an element node.
        return instance;
    }
    function canHydrateTextInstance(instance, text) {
        if (text === '' || instance.nodeType !== TEXT_NODE) // Empty strings are not parsed by HTML so there won't be a correct match here.
        return null;
         // This has now been refined to a text node.
        return instance;
    }
    function canHydrateSuspenseInstance(instance) {
        if (instance.nodeType !== COMMENT_NODE) // Empty strings are not parsed by HTML so there won't be a correct match here.
        return null;
         // This has now been refined to a suspense node.
        return instance;
    }
    function isSuspenseInstancePending(instance) {
        return instance.data === SUSPENSE_PENDING_START_DATA;
    }
    function isSuspenseInstanceFallback(instance) {
        return instance.data === SUSPENSE_FALLBACK_START_DATA;
    }
    function registerSuspenseInstanceRetry(instance, callback) {
        instance._reactRetry = callback;
    }
    function getNextHydratable(node) {
        // Skip non-hydratable nodes.
        for(; node != null; node = node.nextSibling){
            var nodeType = node.nodeType;
            if (nodeType === ELEMENT_NODE || nodeType === TEXT_NODE) break;
            if (nodeType === COMMENT_NODE) {
                var nodeData = node.data;
                if (nodeData === SUSPENSE_START_DATA || nodeData === SUSPENSE_FALLBACK_START_DATA || nodeData === SUSPENSE_PENDING_START_DATA) break;
                if (nodeData === SUSPENSE_END_DATA) return null;
            }
        }
        return node;
    }
    function getNextHydratableSibling(instance) {
        return getNextHydratable(instance.nextSibling);
    }
    function getFirstHydratableChild(parentInstance) {
        return getNextHydratable(parentInstance.firstChild);
    }
    function getFirstHydratableChildWithinContainer(parentContainer) {
        return getNextHydratable(parentContainer.firstChild);
    }
    function getFirstHydratableChildWithinSuspenseInstance(parentInstance) {
        return getNextHydratable(parentInstance.nextSibling);
    }
    function hydrateInstance(instance, type, props, rootContainerInstance, hostContext, internalInstanceHandle, shouldWarnDev) {
        precacheFiberNode(internalInstanceHandle, instance); // TODO: Possibly defer this until the commit phase where all the events
        // get attached.
        updateFiberProps(instance, props);
        var parentNamespace;
        var hostContextDev = hostContext;
        parentNamespace = hostContextDev.namespace;
        // when the legacy root API is removed.
        var isConcurrentMode = (internalInstanceHandle.mode & ConcurrentMode) !== NoMode;
        return diffHydratedProperties(instance, type, props, parentNamespace, rootContainerInstance, isConcurrentMode, shouldWarnDev);
    }
    function hydrateTextInstance(textInstance, text, internalInstanceHandle, shouldWarnDev) {
        precacheFiberNode(internalInstanceHandle, textInstance); // TODO: Temporary hack to check if we're in a concurrent root. We can delete
        // when the legacy root API is removed.
        var isConcurrentMode = (internalInstanceHandle.mode & ConcurrentMode) !== NoMode;
        return diffHydratedText(textInstance, text);
    }
    function hydrateSuspenseInstance(suspenseInstance, internalInstanceHandle) {
        precacheFiberNode(internalInstanceHandle, suspenseInstance);
    }
    function getNextHydratableInstanceAfterSuspenseInstance(suspenseInstance) {
        var node = suspenseInstance.nextSibling; // Skip past all nodes within this suspense boundary.
        // There might be nested nodes so we need to keep track of how
        // deep we are and only break out when we're back on top.
        var depth = 0;
        while(node){
            if (node.nodeType === COMMENT_NODE) {
                var data = node.data;
                if (data === SUSPENSE_END_DATA) {
                    if (depth === 0) return getNextHydratableSibling(node);
                    else depth--;
                } else if (data === SUSPENSE_START_DATA || data === SUSPENSE_FALLBACK_START_DATA || data === SUSPENSE_PENDING_START_DATA) depth++;
            }
            node = node.nextSibling;
        } // TODO: Warn, we didn't find the end comment boundary.
        return null;
    } // Returns the SuspenseInstance if this node is a direct child of a
    // SuspenseInstance. I.e. if its previous sibling is a Comment with
    // SUSPENSE_x_START_DATA. Otherwise, null.
    function getParentSuspenseInstance(targetInstance) {
        var node = targetInstance.previousSibling; // Skip past all nodes within this suspense boundary.
        // There might be nested nodes so we need to keep track of how
        // deep we are and only break out when we're back on top.
        var depth = 0;
        while(node){
            if (node.nodeType === COMMENT_NODE) {
                var data = node.data;
                if (data === SUSPENSE_START_DATA || data === SUSPENSE_FALLBACK_START_DATA || data === SUSPENSE_PENDING_START_DATA) {
                    if (depth === 0) return node;
                    else depth--;
                } else if (data === SUSPENSE_END_DATA) depth++;
            }
            node = node.previousSibling;
        }
        return null;
    }
    function commitHydratedContainer(container) {
        // Retry if any event replaying was blocked on this.
        retryIfBlockedOn(container);
    }
    function commitHydratedSuspenseInstance(suspenseInstance) {
        // Retry if any event replaying was blocked on this.
        retryIfBlockedOn(suspenseInstance);
    }
    function shouldDeleteUnhydratedTailInstances(parentType) {
        return parentType !== 'head' && parentType !== 'body';
    }
    function didNotMatchHydratedContainerTextInstance(parentContainer, textInstance, text, isConcurrentMode) {
        var shouldWarnDev = true;
        checkForUnmatchedText(textInstance.nodeValue, text, isConcurrentMode, shouldWarnDev);
    }
    function didNotMatchHydratedTextInstance(parentType, parentProps, parentInstance, textInstance, text, isConcurrentMode) {
        if (parentProps[SUPPRESS_HYDRATION_WARNING$1] !== true) {
            var shouldWarnDev = true;
            checkForUnmatchedText(textInstance.nodeValue, text, isConcurrentMode, shouldWarnDev);
        }
    }
    function didNotHydrateInstanceWithinContainer(parentContainer, instance) {
        if (instance.nodeType === ELEMENT_NODE) warnForDeletedHydratableElement(parentContainer, instance);
        else if (instance.nodeType === COMMENT_NODE) ;
        else warnForDeletedHydratableText(parentContainer, instance);
    }
    function didNotHydrateInstanceWithinSuspenseInstance(parentInstance, instance) {
        // $FlowFixMe: Only Element or Document can be parent nodes.
        var parentNode = parentInstance.parentNode;
        if (parentNode !== null) {
            if (instance.nodeType === ELEMENT_NODE) warnForDeletedHydratableElement(parentNode, instance);
            else if (instance.nodeType === COMMENT_NODE) ;
            else warnForDeletedHydratableText(parentNode, instance);
        }
    }
    function didNotHydrateInstance(parentType, parentProps, parentInstance, instance, isConcurrentMode) {
        if (isConcurrentMode || parentProps[SUPPRESS_HYDRATION_WARNING$1] !== true) {
            if (instance.nodeType === ELEMENT_NODE) warnForDeletedHydratableElement(parentInstance, instance);
            else if (instance.nodeType === COMMENT_NODE) ;
            else warnForDeletedHydratableText(parentInstance, instance);
        }
    }
    function didNotFindHydratableInstanceWithinContainer(parentContainer, type, props) {
        warnForInsertedHydratedElement(parentContainer, type);
    }
    function didNotFindHydratableTextInstanceWithinContainer(parentContainer, text) {
        warnForInsertedHydratedText(parentContainer, text);
    }
    function didNotFindHydratableInstanceWithinSuspenseInstance(parentInstance, type, props) {
        // $FlowFixMe: Only Element or Document can be parent nodes.
        var parentNode = parentInstance.parentNode;
        if (parentNode !== null) warnForInsertedHydratedElement(parentNode, type);
    }
    function didNotFindHydratableTextInstanceWithinSuspenseInstance(parentInstance, text) {
        // $FlowFixMe: Only Element or Document can be parent nodes.
        var parentNode = parentInstance.parentNode;
        if (parentNode !== null) warnForInsertedHydratedText(parentNode, text);
    }
    function didNotFindHydratableInstance(parentType, parentProps, parentInstance, type, props, isConcurrentMode) {
        if (isConcurrentMode || parentProps[SUPPRESS_HYDRATION_WARNING$1] !== true) warnForInsertedHydratedElement(parentInstance, type);
    }
    function didNotFindHydratableTextInstance(parentType, parentProps, parentInstance, text, isConcurrentMode) {
        if (isConcurrentMode || parentProps[SUPPRESS_HYDRATION_WARNING$1] !== true) warnForInsertedHydratedText(parentInstance, text);
    }
    function errorHydratingContainer(parentContainer) {
        // TODO: This gets logged by onRecoverableError, too, so we should be
        // able to remove it.
        error1('An error occurred during hydration. The server HTML was replaced with client content in <%s>.', parentContainer.nodeName.toLowerCase());
    }
    function preparePortalMount(portalInstance) {
        listenToAllSupportedEvents(portalInstance);
    }
    var randomKey = Math.random().toString(36).slice(2);
    var internalInstanceKey = '__reactFiber$' + randomKey;
    var internalPropsKey = '__reactProps$' + randomKey;
    var internalContainerInstanceKey = '__reactContainer$' + randomKey;
    var internalEventHandlersKey = '__reactEvents$' + randomKey;
    var internalEventHandlerListenersKey = '__reactListeners$' + randomKey;
    var internalEventHandlesSetKey = '__reactHandles$' + randomKey;
    function detachDeletedInstance(node) {
        // TODO: This function is only called on host components. I don't think all of
        // these fields are relevant.
        delete node[internalInstanceKey];
        delete node[internalPropsKey];
        delete node[internalEventHandlersKey];
        delete node[internalEventHandlerListenersKey];
        delete node[internalEventHandlesSetKey];
    }
    function precacheFiberNode(hostInst, node) {
        node[internalInstanceKey] = hostInst;
    }
    function markContainerAsRoot(hostRoot, node) {
        node[internalContainerInstanceKey] = hostRoot;
    }
    function unmarkContainerAsRoot(node) {
        node[internalContainerInstanceKey] = null;
    }
    function isContainerMarkedAsRoot(node) {
        return !!node[internalContainerInstanceKey];
    } // Given a DOM node, return the closest HostComponent or HostText fiber ancestor.
    // If the target node is part of a hydrated or not yet rendered subtree, then
    // this may also return a SuspenseComponent or HostRoot to indicate that.
    // Conceptually the HostRoot fiber is a child of the Container node. So if you
    // pass the Container node as the targetNode, you will not actually get the
    // HostRoot back. To get to the HostRoot, you need to pass a child of it.
    // The same thing applies to Suspense boundaries.
    function getClosestInstanceFromNode(targetNode) {
        var targetInst = targetNode[internalInstanceKey];
        if (targetInst) // Don't return HostRoot or SuspenseComponent here.
        return targetInst;
         // If the direct event target isn't a React owned DOM node, we need to look
        // to see if one of its parents is a React owned DOM node.
        var parentNode = targetNode.parentNode;
        while(parentNode){
            // We'll check if this is a container root that could include
            // React nodes in the future. We need to check this first because
            // if we're a child of a dehydrated container, we need to first
            // find that inner container before moving on to finding the parent
            // instance. Note that we don't check this field on  the targetNode
            // itself because the fibers are conceptually between the container
            // node and the first child. It isn't surrounding the container node.
            // If it's not a container, we check if it's an instance.
            targetInst = parentNode[internalContainerInstanceKey] || parentNode[internalInstanceKey];
            if (targetInst) {
                // Since this wasn't the direct target of the event, we might have
                // stepped past dehydrated DOM nodes to get here. However they could
                // also have been non-React nodes. We need to answer which one.
                // If we the instance doesn't have any children, then there can't be
                // a nested suspense boundary within it. So we can use this as a fast
                // bailout. Most of the time, when people add non-React children to
                // the tree, it is using a ref to a child-less DOM node.
                // Normally we'd only need to check one of the fibers because if it
                // has ever gone from having children to deleting them or vice versa
                // it would have deleted the dehydrated boundary nested inside already.
                // However, since the HostRoot starts out with an alternate it might
                // have one on the alternate so we need to check in case this was a
                // root.
                var alternate = targetInst.alternate;
                if (targetInst.child !== null || alternate !== null && alternate.child !== null) {
                    // Next we need to figure out if the node that skipped past is
                    // nested within a dehydrated boundary and if so, which one.
                    var suspenseInstance = getParentSuspenseInstance(targetNode);
                    while(suspenseInstance !== null){
                        // We found a suspense instance. That means that we haven't
                        // hydrated it yet. Even though we leave the comments in the
                        // DOM after hydrating, and there are boundaries in the DOM
                        // that could already be hydrated, we wouldn't have found them
                        // through this pass since if the target is hydrated it would
                        // have had an internalInstanceKey on it.
                        // Let's get the fiber associated with the SuspenseComponent
                        // as the deepest instance.
                        var targetSuspenseInst = suspenseInstance[internalInstanceKey];
                        if (targetSuspenseInst) return targetSuspenseInst;
                         // If we don't find a Fiber on the comment, it might be because
                        // we haven't gotten to hydrate it yet. There might still be a
                        // parent boundary that hasn't above this one so we need to find
                        // the outer most that is known.
                        suspenseInstance = getParentSuspenseInstance(suspenseInstance); // If we don't find one, then that should mean that the parent
                    // host component also hasn't hydrated yet. We can return it
                    // below since it will bail out on the isMounted check later.
                    }
                }
                return targetInst;
            }
            targetNode = parentNode;
            parentNode = targetNode.parentNode;
        }
        return null;
    }
    /**
 * Given a DOM node, return the ReactDOMComponent or ReactDOMTextComponent
 * instance, or null if the node was not rendered by this React.
 */ function getInstanceFromNode(node) {
        var inst = node[internalInstanceKey] || node[internalContainerInstanceKey];
        if (inst) {
            if (inst.tag === HostComponent || inst.tag === HostText || inst.tag === SuspenseComponent || inst.tag === HostRoot) return inst;
            else return null;
        }
        return null;
    }
    /**
 * Given a ReactDOMComponent or ReactDOMTextComponent, return the corresponding
 * DOM node.
 */ function getNodeFromInstance(inst) {
        if (inst.tag === HostComponent || inst.tag === HostText) // In Fiber this, is just the state node right now. We assume it will be
        // a host component or host text.
        return inst.stateNode;
         // Without this first invariant, passing a non-DOM-component triggers the next
        // invariant for a missing parent, which is super confusing.
        throw new Error('getNodeFromInstance: Invalid argument.');
    }
    function getFiberCurrentPropsFromNode(node) {
        return node[internalPropsKey] || null;
    }
    function updateFiberProps(node, props) {
        node[internalPropsKey] = props;
    }
    function getEventListenerSet(node) {
        var elementListenerSet = node[internalEventHandlersKey];
        if (elementListenerSet === undefined) elementListenerSet = node[internalEventHandlersKey] = new Set();
        return elementListenerSet;
    }
    var loggedTypeFailures = {};
    var ReactDebugCurrentFrame$1 = ReactSharedInternals.ReactDebugCurrentFrame;
    function setCurrentlyValidatingElement(element) {
        if (element) {
            var owner = element._owner;
            var stack = describeUnknownElementTypeFrameInDEV(element.type, element._source, owner ? owner.type : null);
            ReactDebugCurrentFrame$1.setExtraStackFrame(stack);
        } else ReactDebugCurrentFrame$1.setExtraStackFrame(null);
    }
    function checkPropTypes(typeSpecs, values, location, componentName, element) {
        // $FlowFixMe This is okay but Flow doesn't know it.
        var has = Function.call.bind(hasOwnProperty);
        for(var typeSpecName in typeSpecs)if (has(typeSpecs, typeSpecName)) {
            var error$1 = void 0; // Prop type validation may throw. In case they do, we don't want to
            // fail the render phase where it didn't fail before. So we log it.
            // After these have been cleaned up, we'll let them throw.
            try {
                // This is intentionally an invariant that gets caught. It's the same
                // behavior as without this statement except with a better message.
                if (typeof typeSpecs[typeSpecName] !== 'function') {
                    // eslint-disable-next-line react-internal/prod-error-codes
                    var err = Error((componentName || 'React class') + ': ' + location + ' type `' + typeSpecName + '` is invalid; ' + 'it must be a function, usually from the `prop-types` package, but received `' + typeof typeSpecs[typeSpecName] + '`.' + 'This often happens because of typos such as `PropTypes.function` instead of `PropTypes.func`.');
                    err.name = 'Invariant Violation';
                    throw err;
                }
                error$1 = typeSpecs[typeSpecName](values, typeSpecName, componentName, location, null, 'SECRET_DO_NOT_PASS_THIS_OR_YOU_WILL_BE_FIRED');
            } catch (ex) {
                error$1 = ex;
            }
            if (error$1 && !(error$1 instanceof Error)) {
                setCurrentlyValidatingElement(element);
                error1("%s: type specification of %s `%s` is invalid; the type checker function must return `null` or an `Error` but returned a %s. You may have forgotten to pass an argument to the type checker creator (arrayOf, instanceOf, objectOf, oneOf, oneOfType, and shape all require an argument).", componentName || 'React class', location, typeSpecName, typeof error$1);
                setCurrentlyValidatingElement(null);
            }
            if (error$1 instanceof Error && !(error$1.message in loggedTypeFailures)) {
                // Only monitor this failure once because there tends to be a lot of the
                // same error.
                loggedTypeFailures[error$1.message] = true;
                setCurrentlyValidatingElement(element);
                error1('Failed %s type: %s', location, error$1.message);
                setCurrentlyValidatingElement(null);
            }
        }
    }
    var valueStack = [];
    var fiberStack;
    fiberStack = [];
    var index1 = -1;
    function createCursor(defaultValue) {
        return {
            current: defaultValue
        };
    }
    function pop(cursor, fiber) {
        if (index1 < 0) {
            error1('Unexpected pop.');
            return;
        }
        if (fiber !== fiberStack[index1]) error1('Unexpected Fiber popped.');
        cursor.current = valueStack[index1];
        valueStack[index1] = null;
        fiberStack[index1] = null;
        index1--;
    }
    function push(cursor, value, fiber) {
        index1++;
        valueStack[index1] = cursor.current;
        fiberStack[index1] = fiber;
        cursor.current = value;
    }
    var warnedAboutMissingGetChildContext;
    warnedAboutMissingGetChildContext = {};
    var emptyContextObject = {};
    Object.freeze(emptyContextObject);
    var contextStackCursor = createCursor(emptyContextObject); // A cursor to a boolean indicating whether the context has changed.
    var didPerformWorkStackCursor = createCursor(false); // Keep track of the previous context object that was on the stack.
    // We use this to get access to the parent context after we have already
    // pushed the next context provider, and now need to merge their contexts.
    var previousContext = emptyContextObject;
    function getUnmaskedContext(workInProgress, Component, didPushOwnContextIfProvider) {
        if (didPushOwnContextIfProvider && isContextProvider(Component)) // If the fiber is a context provider itself, when we read its context
        // we may have already pushed its own child context on the stack. A context
        // provider should not "see" its own child context. Therefore we read the
        // previous (parent) context instead for a context provider.
        return previousContext;
        return contextStackCursor.current;
    }
    function cacheContext(workInProgress, unmaskedContext, maskedContext) {
        var instance = workInProgress.stateNode;
        instance.__reactInternalMemoizedUnmaskedChildContext = unmaskedContext;
        instance.__reactInternalMemoizedMaskedChildContext = maskedContext;
    }
    function getMaskedContext(workInProgress, unmaskedContext) {
        var type = workInProgress.type;
        var contextTypes = type.contextTypes;
        if (!contextTypes) return emptyContextObject;
         // Avoid recreating masked context unless unmasked context has changed.
        // Failing to do this will result in unnecessary calls to componentWillReceiveProps.
        // This may trigger infinite loops if componentWillReceiveProps calls setState.
        var instance = workInProgress.stateNode;
        if (instance && instance.__reactInternalMemoizedUnmaskedChildContext === unmaskedContext) return instance.__reactInternalMemoizedMaskedChildContext;
        var context = {};
        for(var key in contextTypes)context[key] = unmaskedContext[key];
        var name = getComponentNameFromFiber(workInProgress) || 'Unknown';
        checkPropTypes(contextTypes, context, 'context', name);
        // Context is created before the class component is instantiated so check for instance.
        if (instance) cacheContext(workInProgress, unmaskedContext, context);
        return context;
    }
    function hasContextChanged1() {
        return didPerformWorkStackCursor.current;
    }
    function isContextProvider(type) {
        var childContextTypes = type.childContextTypes;
        return childContextTypes !== null && childContextTypes !== undefined;
    }
    function popContext(fiber) {
        pop(didPerformWorkStackCursor, fiber);
        pop(contextStackCursor, fiber);
    }
    function popTopLevelContextObject(fiber) {
        pop(didPerformWorkStackCursor, fiber);
        pop(contextStackCursor, fiber);
    }
    function pushTopLevelContextObject(fiber, context, didChange) {
        if (contextStackCursor.current !== emptyContextObject) throw new Error("Unexpected context found on stack. This error is likely caused by a bug in React. Please file an issue.");
        push(contextStackCursor, context, fiber);
        push(didPerformWorkStackCursor, didChange, fiber);
    }
    function processChildContext(fiber, type, parentContext) {
        var instance = fiber.stateNode;
        var childContextTypes = type.childContextTypes; // TODO (bvaughn) Replace this behavior with an invariant() in the future.
        // It has only been added in Fiber to match the (unintentional) behavior in Stack.
        if (typeof instance.getChildContext !== 'function') {
            var componentName = getComponentNameFromFiber(fiber) || 'Unknown';
            if (!warnedAboutMissingGetChildContext[componentName]) {
                warnedAboutMissingGetChildContext[componentName] = true;
                error1("%s.childContextTypes is specified but there is no getChildContext() method on the instance. You can either define getChildContext() on %s or remove childContextTypes from it.", componentName, componentName);
            }
            return parentContext;
        }
        var childContext = instance.getChildContext();
        for(var contextKey in childContext){
            if (!(contextKey in childContextTypes)) throw new Error((getComponentNameFromFiber(fiber) || 'Unknown') + ".getChildContext(): key \"" + contextKey + "\" is not defined in childContextTypes.");
        }
        var name = getComponentNameFromFiber(fiber) || 'Unknown';
        checkPropTypes(childContextTypes, childContext, 'child context', name);
        return assign({}, parentContext, childContext);
    }
    function pushContextProvider(workInProgress) {
        var instance = workInProgress.stateNode; // We push the context as early as possible to ensure stack integrity.
        // If the instance does not exist yet, we will push null at first,
        // and replace it on the stack later when invalidating the context.
        var memoizedMergedChildContext = instance && instance.__reactInternalMemoizedMergedChildContext || emptyContextObject; // Remember the parent context so we can merge with it later.
        // Inherit the parent's did-perform-work value to avoid inadvertently blocking updates.
        previousContext = contextStackCursor.current;
        push(contextStackCursor, memoizedMergedChildContext, workInProgress);
        push(didPerformWorkStackCursor, didPerformWorkStackCursor.current, workInProgress);
        return true;
    }
    function invalidateContextProvider(workInProgress, type, didChange) {
        var instance = workInProgress.stateNode;
        if (!instance) throw new Error("Expected to have an instance by this point. This error is likely caused by a bug in React. Please file an issue.");
        if (didChange) {
            // Merge parent and own context.
            // Skip this if we're not updating due to sCU.
            // This avoids unnecessarily recomputing memoized values.
            var mergedContext = processChildContext(workInProgress, type, previousContext);
            instance.__reactInternalMemoizedMergedChildContext = mergedContext; // Replace the old (or empty) context with the new one.
            // It is important to unwind the context in the reverse order.
            pop(didPerformWorkStackCursor, workInProgress);
            pop(contextStackCursor, workInProgress); // Now push the new context and mark that it has changed.
            push(contextStackCursor, mergedContext, workInProgress);
            push(didPerformWorkStackCursor, didChange, workInProgress);
        } else {
            pop(didPerformWorkStackCursor, workInProgress);
            push(didPerformWorkStackCursor, didChange, workInProgress);
        }
    }
    function findCurrentUnmaskedContext(fiber) {
        // Currently this is only used with renderSubtreeIntoContainer; not sure if it
        // makes sense elsewhere
        if (!isFiberMounted(fiber) || fiber.tag !== ClassComponent) throw new Error("Expected subtree parent to be a mounted class component. This error is likely caused by a bug in React. Please file an issue.");
        var node = fiber;
        do {
            switch(node.tag){
                case HostRoot:
                    return node.stateNode.context;
                case ClassComponent:
                    var Component = node.type;
                    if (isContextProvider(Component)) return node.stateNode.__reactInternalMemoizedMergedChildContext;
                    break;
            }
            node = node.return;
        }while (node !== null)
        throw new Error("Found unexpected detached subtree parent. This error is likely caused by a bug in React. Please file an issue.");
    }
    var LegacyRoot = 0;
    var ConcurrentRoot = 1;
    var syncQueue = null;
    var includesLegacySyncCallbacks = false;
    var isFlushingSyncQueue = false;
    function scheduleSyncCallback(callback) {
        // Push this callback into an internal queue. We'll flush these either in
        // the next tick, or earlier if something calls `flushSyncCallbackQueue`.
        if (syncQueue === null) syncQueue = [
            callback
        ];
        else // Push onto existing queue. Don't need to schedule a callback because
        // we already scheduled one when we created the queue.
        syncQueue.push(callback);
    }
    function scheduleLegacySyncCallback(callback) {
        includesLegacySyncCallbacks = true;
        scheduleSyncCallback(callback);
    }
    function flushSyncCallbacksOnlyInLegacyMode() {
        // Only flushes the queue if there's a legacy sync callback scheduled.
        // TODO: There's only a single type of callback: performSyncOnWorkOnRoot. So
        // it might make more sense for the queue to be a list of roots instead of a
        // list of generic callbacks. Then we can have two: one for legacy roots, one
        // for concurrent roots. And this method would only flush the legacy ones.
        if (includesLegacySyncCallbacks) flushSyncCallbacks();
    }
    function flushSyncCallbacks() {
        if (!isFlushingSyncQueue && syncQueue !== null) {
            // Prevent re-entrance.
            isFlushingSyncQueue = true;
            var i = 0;
            var previousUpdatePriority = getCurrentUpdatePriority();
            try {
                var isSync = true;
                var queue = syncQueue; // TODO: Is this necessary anymore? The only user code that runs in this
                // queue is in the render or commit phases.
                setCurrentUpdatePriority(DiscreteEventPriority);
                for(; i < queue.length; i++){
                    var callback = queue[i];
                    do callback = callback(isSync);
                    while (callback !== null)
                }
                syncQueue = null;
                includesLegacySyncCallbacks = false;
            } catch (error) {
                // If something throws, leave the remaining callbacks on the queue.
                if (syncQueue !== null) syncQueue = syncQueue.slice(i + 1);
                 // Resume flushing in the next tick
                scheduleCallback(ImmediatePriority, flushSyncCallbacks);
                throw error;
            } finally{
                setCurrentUpdatePriority(previousUpdatePriority);
                isFlushingSyncQueue = false;
            }
        }
        return null;
    }
    var ReactCurrentBatchConfig$1 = ReactSharedInternals.ReactCurrentBatchConfig;
    var NoTransition = null;
    function requestCurrentTransition() {
        return ReactCurrentBatchConfig$1.transition;
    }
    var ReactStrictModeWarnings = {
        recordUnsafeLifecycleWarnings: function(fiber, instance) {},
        flushPendingUnsafeLifecycleWarnings: function() {},
        recordLegacyContextWarning: function(fiber, instance) {},
        flushLegacyContextWarning: function() {},
        discardPendingWarnings: function() {}
    };
    var findStrictRoot = function(fiber) {
        var maybeStrictRoot = null;
        var node = fiber;
        while(node !== null){
            if (node.mode & StrictLegacyMode) maybeStrictRoot = node;
            node = node.return;
        }
        return maybeStrictRoot;
    };
    var setToSortedString = function(set) {
        var array = [];
        set.forEach(function(value) {
            array.push(value);
        });
        return array.sort().join(', ');
    };
    var pendingComponentWillMountWarnings = [];
    var pendingUNSAFE_ComponentWillMountWarnings = [];
    var pendingComponentWillReceivePropsWarnings = [];
    var pendingUNSAFE_ComponentWillReceivePropsWarnings = [];
    var pendingComponentWillUpdateWarnings = [];
    var pendingUNSAFE_ComponentWillUpdateWarnings = []; // Tracks components we have already warned about.
    var didWarnAboutUnsafeLifecycles = new Set();
    ReactStrictModeWarnings.recordUnsafeLifecycleWarnings = function(fiber, instance) {
        // Dedupe strategy: Warn once per component.
        if (didWarnAboutUnsafeLifecycles.has(fiber.type)) return;
        if (typeof instance.componentWillMount === 'function' && instance.componentWillMount.__suppressDeprecationWarning !== true) pendingComponentWillMountWarnings.push(fiber);
        if (fiber.mode & StrictLegacyMode && typeof instance.UNSAFE_componentWillMount === 'function') pendingUNSAFE_ComponentWillMountWarnings.push(fiber);
        if (typeof instance.componentWillReceiveProps === 'function' && instance.componentWillReceiveProps.__suppressDeprecationWarning !== true) pendingComponentWillReceivePropsWarnings.push(fiber);
        if (fiber.mode & StrictLegacyMode && typeof instance.UNSAFE_componentWillReceiveProps === 'function') pendingUNSAFE_ComponentWillReceivePropsWarnings.push(fiber);
        if (typeof instance.componentWillUpdate === 'function' && instance.componentWillUpdate.__suppressDeprecationWarning !== true) pendingComponentWillUpdateWarnings.push(fiber);
        if (fiber.mode & StrictLegacyMode && typeof instance.UNSAFE_componentWillUpdate === 'function') pendingUNSAFE_ComponentWillUpdateWarnings.push(fiber);
    };
    ReactStrictModeWarnings.flushPendingUnsafeLifecycleWarnings = function() {
        // We do an initial pass to gather component names
        var componentWillMountUniqueNames = new Set();
        if (pendingComponentWillMountWarnings.length > 0) {
            pendingComponentWillMountWarnings.forEach(function(fiber) {
                componentWillMountUniqueNames.add(getComponentNameFromFiber(fiber) || 'Component');
                didWarnAboutUnsafeLifecycles.add(fiber.type);
            });
            pendingComponentWillMountWarnings = [];
        }
        var UNSAFE_componentWillMountUniqueNames = new Set();
        if (pendingUNSAFE_ComponentWillMountWarnings.length > 0) {
            pendingUNSAFE_ComponentWillMountWarnings.forEach(function(fiber) {
                UNSAFE_componentWillMountUniqueNames.add(getComponentNameFromFiber(fiber) || 'Component');
                didWarnAboutUnsafeLifecycles.add(fiber.type);
            });
            pendingUNSAFE_ComponentWillMountWarnings = [];
        }
        var componentWillReceivePropsUniqueNames = new Set();
        if (pendingComponentWillReceivePropsWarnings.length > 0) {
            pendingComponentWillReceivePropsWarnings.forEach(function(fiber) {
                componentWillReceivePropsUniqueNames.add(getComponentNameFromFiber(fiber) || 'Component');
                didWarnAboutUnsafeLifecycles.add(fiber.type);
            });
            pendingComponentWillReceivePropsWarnings = [];
        }
        var UNSAFE_componentWillReceivePropsUniqueNames = new Set();
        if (pendingUNSAFE_ComponentWillReceivePropsWarnings.length > 0) {
            pendingUNSAFE_ComponentWillReceivePropsWarnings.forEach(function(fiber) {
                UNSAFE_componentWillReceivePropsUniqueNames.add(getComponentNameFromFiber(fiber) || 'Component');
                didWarnAboutUnsafeLifecycles.add(fiber.type);
            });
            pendingUNSAFE_ComponentWillReceivePropsWarnings = [];
        }
        var componentWillUpdateUniqueNames = new Set();
        if (pendingComponentWillUpdateWarnings.length > 0) {
            pendingComponentWillUpdateWarnings.forEach(function(fiber) {
                componentWillUpdateUniqueNames.add(getComponentNameFromFiber(fiber) || 'Component');
                didWarnAboutUnsafeLifecycles.add(fiber.type);
            });
            pendingComponentWillUpdateWarnings = [];
        }
        var UNSAFE_componentWillUpdateUniqueNames = new Set();
        if (pendingUNSAFE_ComponentWillUpdateWarnings.length > 0) {
            pendingUNSAFE_ComponentWillUpdateWarnings.forEach(function(fiber) {
                UNSAFE_componentWillUpdateUniqueNames.add(getComponentNameFromFiber(fiber) || 'Component');
                didWarnAboutUnsafeLifecycles.add(fiber.type);
            });
            pendingUNSAFE_ComponentWillUpdateWarnings = [];
        } // Finally, we flush all the warnings
        // UNSAFE_ ones before the deprecated ones, since they'll be 'louder'
        if (UNSAFE_componentWillMountUniqueNames.size > 0) {
            var sortedNames = setToSortedString(UNSAFE_componentWillMountUniqueNames);
            error1("Using UNSAFE_componentWillMount in strict mode is not recommended and may indicate bugs in your code. See https://reactjs.org/link/unsafe-component-lifecycles for details.\n\n* Move code with side effects to componentDidMount, and set initial state in the constructor.\n\nPlease update the following components: %s", sortedNames);
        }
        if (UNSAFE_componentWillReceivePropsUniqueNames.size > 0) {
            var _sortedNames = setToSortedString(UNSAFE_componentWillReceivePropsUniqueNames);
            error1("Using UNSAFE_componentWillReceiveProps in strict mode is not recommended and may indicate bugs in your code. See https://reactjs.org/link/unsafe-component-lifecycles for details.\n\n* Move data fetching code or side effects to componentDidUpdate.\n* If you're updating state whenever props change, refactor your code to use memoization techniques or move it to static getDerivedStateFromProps. Learn more at: https://reactjs.org/link/derived-state\n\nPlease update the following components: %s", _sortedNames);
        }
        if (UNSAFE_componentWillUpdateUniqueNames.size > 0) {
            var _sortedNames2 = setToSortedString(UNSAFE_componentWillUpdateUniqueNames);
            error1("Using UNSAFE_componentWillUpdate in strict mode is not recommended and may indicate bugs in your code. See https://reactjs.org/link/unsafe-component-lifecycles for details.\n\n* Move data fetching code or side effects to componentDidUpdate.\n\nPlease update the following components: %s", _sortedNames2);
        }
        if (componentWillMountUniqueNames.size > 0) {
            var _sortedNames3 = setToSortedString(componentWillMountUniqueNames);
            warn("componentWillMount has been renamed, and is not recommended for use. See https://reactjs.org/link/unsafe-component-lifecycles for details.\n\n* Move code with side effects to componentDidMount, and set initial state in the constructor.\n* Rename componentWillMount to UNSAFE_componentWillMount to suppress this warning in non-strict mode. In React 18.x, only the UNSAFE_ name will work. To rename all deprecated lifecycles to their new names, you can run `npx react-codemod rename-unsafe-lifecycles` in your project source folder.\n\nPlease update the following components: %s", _sortedNames3);
        }
        if (componentWillReceivePropsUniqueNames.size > 0) {
            var _sortedNames4 = setToSortedString(componentWillReceivePropsUniqueNames);
            warn("componentWillReceiveProps has been renamed, and is not recommended for use. See https://reactjs.org/link/unsafe-component-lifecycles for details.\n\n* Move data fetching code or side effects to componentDidUpdate.\n* If you're updating state whenever props change, refactor your code to use memoization techniques or move it to static getDerivedStateFromProps. Learn more at: https://reactjs.org/link/derived-state\n* Rename componentWillReceiveProps to UNSAFE_componentWillReceiveProps to suppress this warning in non-strict mode. In React 18.x, only the UNSAFE_ name will work. To rename all deprecated lifecycles to their new names, you can run `npx react-codemod rename-unsafe-lifecycles` in your project source folder.\n\nPlease update the following components: %s", _sortedNames4);
        }
        if (componentWillUpdateUniqueNames.size > 0) {
            var _sortedNames5 = setToSortedString(componentWillUpdateUniqueNames);
            warn("componentWillUpdate has been renamed, and is not recommended for use. See https://reactjs.org/link/unsafe-component-lifecycles for details.\n\n* Move data fetching code or side effects to componentDidUpdate.\n* Rename componentWillUpdate to UNSAFE_componentWillUpdate to suppress this warning in non-strict mode. In React 18.x, only the UNSAFE_ name will work. To rename all deprecated lifecycles to their new names, you can run `npx react-codemod rename-unsafe-lifecycles` in your project source folder.\n\nPlease update the following components: %s", _sortedNames5);
        }
    };
    var pendingLegacyContextWarning = new Map(); // Tracks components we have already warned about.
    var didWarnAboutLegacyContext = new Set();
    ReactStrictModeWarnings.recordLegacyContextWarning = function(fiber, instance) {
        var strictRoot = findStrictRoot(fiber);
        if (strictRoot === null) {
            error1("Expected to find a StrictMode component in a strict mode tree. This error is likely caused by a bug in React. Please file an issue.");
            return;
        } // Dedup strategy: Warn once per component.
        if (didWarnAboutLegacyContext.has(fiber.type)) return;
        var warningsForRoot = pendingLegacyContextWarning.get(strictRoot);
        if (fiber.type.contextTypes != null || fiber.type.childContextTypes != null || instance !== null && typeof instance.getChildContext === 'function') {
            if (warningsForRoot === undefined) {
                warningsForRoot = [];
                pendingLegacyContextWarning.set(strictRoot, warningsForRoot);
            }
            warningsForRoot.push(fiber);
        }
    };
    ReactStrictModeWarnings.flushLegacyContextWarning = function() {
        pendingLegacyContextWarning.forEach(function(fiberArray, strictRoot) {
            if (fiberArray.length === 0) return;
            var firstFiber = fiberArray[0];
            var uniqueNames = new Set();
            fiberArray.forEach(function(fiber) {
                uniqueNames.add(getComponentNameFromFiber(fiber) || 'Component');
                didWarnAboutLegacyContext.add(fiber.type);
            });
            var sortedNames = setToSortedString(uniqueNames);
            try {
                setCurrentFiber(firstFiber);
                error1("Legacy context API has been detected within a strict-mode tree.\n\nThe old API will be supported in all 16.x releases, but applications using it should migrate to the new version.\n\nPlease update the following components: %s\n\nLearn more about this warning here: https://reactjs.org/link/legacy-context", sortedNames);
            } finally{
                resetCurrentFiber();
            }
        });
    };
    ReactStrictModeWarnings.discardPendingWarnings = function() {
        pendingComponentWillMountWarnings = [];
        pendingUNSAFE_ComponentWillMountWarnings = [];
        pendingComponentWillReceivePropsWarnings = [];
        pendingUNSAFE_ComponentWillReceivePropsWarnings = [];
        pendingComponentWillUpdateWarnings = [];
        pendingUNSAFE_ComponentWillUpdateWarnings = [];
        pendingLegacyContextWarning = new Map();
    };
    function resolveDefaultProps(Component, baseProps) {
        if (Component && Component.defaultProps) {
            // Resolve default props. Taken from ReactElement
            var props = assign({}, baseProps);
            var defaultProps = Component.defaultProps;
            for(var propName in defaultProps)if (props[propName] === undefined) props[propName] = defaultProps[propName];
            return props;
        }
        return baseProps;
    }
    var valueCursor = createCursor(null);
    var rendererSigil;
    // Use this to detect multiple renderers using the same context
    rendererSigil = {};
    var currentlyRenderingFiber = null;
    var lastContextDependency = null;
    var lastFullyObservedContext = null;
    var isDisallowedContextReadInDEV = false;
    function resetContextDependencies() {
        // This is called right before React yields execution, to ensure `readContext`
        // cannot be called outside the render phase.
        currentlyRenderingFiber = null;
        lastContextDependency = null;
        lastFullyObservedContext = null;
        isDisallowedContextReadInDEV = false;
    }
    function enterDisallowedContextReadInDEV() {
        isDisallowedContextReadInDEV = true;
    }
    function exitDisallowedContextReadInDEV() {
        isDisallowedContextReadInDEV = false;
    }
    function pushProvider(providerFiber, context, nextValue) {
        push(valueCursor, context._currentValue, providerFiber);
        context._currentValue = nextValue;
        if (context._currentRenderer !== undefined && context._currentRenderer !== null && context._currentRenderer !== rendererSigil) error1("Detected multiple renderers concurrently rendering the same context provider. This is currently unsupported.");
        context._currentRenderer = rendererSigil;
    }
    function popProvider(context, providerFiber) {
        var currentValue = valueCursor.current;
        pop(valueCursor, providerFiber);
        context._currentValue = currentValue;
    }
    function scheduleContextWorkOnParentPath(parent, renderLanes, propagationRoot) {
        // Update the child lanes of all the ancestors, including the alternates.
        var node = parent;
        while(node !== null){
            var alternate = node.alternate;
            if (!isSubsetOfLanes(node.childLanes, renderLanes)) {
                node.childLanes = mergeLanes(node.childLanes, renderLanes);
                if (alternate !== null) alternate.childLanes = mergeLanes(alternate.childLanes, renderLanes);
            } else if (alternate !== null && !isSubsetOfLanes(alternate.childLanes, renderLanes)) alternate.childLanes = mergeLanes(alternate.childLanes, renderLanes);
            if (node === propagationRoot) break;
            node = node.return;
        }
        if (node !== propagationRoot) error1("Expected to find the propagation root when scheduling context work. This error is likely caused by a bug in React. Please file an issue.");
    }
    function propagateContextChange(workInProgress, context, renderLanes) {
        propagateContextChange_eager(workInProgress, context, renderLanes);
    }
    function propagateContextChange_eager(workInProgress, context, renderLanes) {
        var fiber = workInProgress.child;
        if (fiber !== null) // Set the return pointer of the child to the work-in-progress fiber.
        fiber.return = workInProgress;
        while(fiber !== null){
            var nextFiber = void 0; // Visit this fiber.
            var list = fiber.dependencies;
            if (list !== null) {
                nextFiber = fiber.child;
                var dependency = list.firstContext;
                while(dependency !== null){
                    // Check if the context matches.
                    if (dependency.context === context) {
                        // Match! Schedule an update on this fiber.
                        if (fiber.tag === ClassComponent) {
                            // Schedule a force update on the work-in-progress.
                            var lane = pickArbitraryLane(renderLanes);
                            var update = createUpdate(NoTimestamp, lane);
                            update.tag = ForceUpdate; // TODO: Because we don't have a work-in-progress, this will add the
                            // update to the current fiber, too, which means it will persist even if
                            // this render is thrown away. Since it's a race condition, not sure it's
                            // worth fixing.
                            // Inlined `enqueueUpdate` to remove interleaved update check
                            var updateQueue = fiber.updateQueue;
                            if (updateQueue === null) ;
                            else {
                                var sharedQueue = updateQueue.shared;
                                var pending = sharedQueue.pending;
                                if (pending === null) // This is the first update. Create a circular list.
                                update.next = update;
                                else {
                                    update.next = pending.next;
                                    pending.next = update;
                                }
                                sharedQueue.pending = update;
                            }
                        }
                        fiber.lanes = mergeLanes(fiber.lanes, renderLanes);
                        var alternate = fiber.alternate;
                        if (alternate !== null) alternate.lanes = mergeLanes(alternate.lanes, renderLanes);
                        scheduleContextWorkOnParentPath(fiber.return, renderLanes, workInProgress); // Mark the updated lanes on the list, too.
                        list.lanes = mergeLanes(list.lanes, renderLanes); // Since we already found a match, we can stop traversing the
                        break;
                    }
                    dependency = dependency.next;
                }
            } else if (fiber.tag === ContextProvider) // Don't scan deeper if this is a matching provider
            nextFiber = fiber.type === workInProgress.type ? null : fiber.child;
            else if (fiber.tag === DehydratedFragment) {
                // If a dehydrated suspense boundary is in this subtree, we don't know
                // if it will have any context consumers in it. The best we can do is
                // mark it as having updates.
                var parentSuspense = fiber.return;
                if (parentSuspense === null) throw new Error('We just came from a parent so we must have had a parent. This is a bug in React.');
                parentSuspense.lanes = mergeLanes(parentSuspense.lanes, renderLanes);
                var _alternate = parentSuspense.alternate;
                if (_alternate !== null) _alternate.lanes = mergeLanes(_alternate.lanes, renderLanes);
                 // This is intentionally passing this fiber as the parent
                // because we want to schedule this fiber as having work
                // on its children. We'll use the childLanes on
                // this fiber to indicate that a context has changed.
                scheduleContextWorkOnParentPath(parentSuspense, renderLanes, workInProgress);
                nextFiber = fiber.sibling;
            } else // Traverse down.
            nextFiber = fiber.child;
            if (nextFiber !== null) // Set the return pointer of the child to the work-in-progress fiber.
            nextFiber.return = fiber;
            else {
                // No child. Traverse to next sibling.
                nextFiber = fiber;
                while(nextFiber !== null){
                    if (nextFiber === workInProgress) {
                        // We're back to the root of this subtree. Exit.
                        nextFiber = null;
                        break;
                    }
                    var sibling = nextFiber.sibling;
                    if (sibling !== null) {
                        // Set the return pointer of the sibling to the work-in-progress fiber.
                        sibling.return = nextFiber.return;
                        nextFiber = sibling;
                        break;
                    } // No more siblings. Traverse up.
                    nextFiber = nextFiber.return;
                }
            }
            fiber = nextFiber;
        }
    }
    function prepareToReadContext(workInProgress, renderLanes) {
        currentlyRenderingFiber = workInProgress;
        lastContextDependency = null;
        lastFullyObservedContext = null;
        var dependencies = workInProgress.dependencies;
        if (dependencies !== null) {
            var firstContext = dependencies.firstContext;
            if (firstContext !== null) {
                if (includesSomeLane(dependencies.lanes, renderLanes)) // Context list has a pending update. Mark that this fiber performed work.
                markWorkInProgressReceivedUpdate();
                 // Reset the work-in-progress list
                dependencies.firstContext = null;
            }
        }
    }
    function readContext(context) {
        // This warning would fire if you read context inside a Hook like useMemo.
        // Unlike the class check below, it's not enforced in production for perf.
        if (isDisallowedContextReadInDEV) error1("Context can only be read while React is rendering. In classes, you can read it in the render method or getDerivedStateFromProps. In function components, you can read it directly in the function body, but not inside Hooks like useReducer() or useMemo().");
        var value = context._currentValue;
        if (lastFullyObservedContext === context) ;
        else {
            var contextItem = {
                context: context,
                memoizedValue: value,
                next: null
            };
            if (lastContextDependency === null) {
                if (currentlyRenderingFiber === null) throw new Error("Context can only be read while React is rendering. In classes, you can read it in the render method or getDerivedStateFromProps. In function components, you can read it directly in the function body, but not inside Hooks like useReducer() or useMemo().");
                 // This is the first dependency for this component. Create a new list.
                lastContextDependency = contextItem;
                currentlyRenderingFiber.dependencies = {
                    lanes: NoLanes,
                    firstContext: contextItem
                };
            } else // Append a new context item.
            lastContextDependency = lastContextDependency.next = contextItem;
        }
        return value;
    }
    // An array of all update queues that received updates during the current
    // render. When this render exits, either because it finishes or because it is
    // interrupted, the interleaved updates will be transferred onto the main part
    // of the queue.
    var interleavedQueues = null;
    function pushInterleavedQueue(queue) {
        if (interleavedQueues === null) interleavedQueues = [
            queue
        ];
        else interleavedQueues.push(queue);
    }
    function hasInterleavedUpdates() {
        return interleavedQueues !== null;
    }
    function enqueueInterleavedUpdates() {
        // Transfer the interleaved updates onto the main queue. Each queue has a
        // `pending` field and an `interleaved` field. When they are not null, they
        // point to the last node in a circular linked list. We need to append the
        // interleaved list to the end of the pending list by joining them into a
        // single, circular list.
        if (interleavedQueues !== null) {
            for(var i = 0; i < interleavedQueues.length; i++){
                var queue = interleavedQueues[i];
                var lastInterleavedUpdate = queue.interleaved;
                if (lastInterleavedUpdate !== null) {
                    queue.interleaved = null;
                    var firstInterleavedUpdate = lastInterleavedUpdate.next;
                    var lastPendingUpdate = queue.pending;
                    if (lastPendingUpdate !== null) {
                        var firstPendingUpdate = lastPendingUpdate.next;
                        lastPendingUpdate.next = firstInterleavedUpdate;
                        lastInterleavedUpdate.next = firstPendingUpdate;
                    }
                    queue.pending = lastInterleavedUpdate;
                }
            }
            interleavedQueues = null;
        }
    }
    var UpdateState = 0;
    var ReplaceState = 1;
    var ForceUpdate = 2;
    var CaptureUpdate = 3; // Global state that is reset at the beginning of calling `processUpdateQueue`.
    // It should only be read right after calling `processUpdateQueue`, via
    // `checkHasForceUpdateAfterProcessing`.
    var hasForceUpdate = false;
    var didWarnUpdateInsideUpdate;
    var currentlyProcessingQueue;
    didWarnUpdateInsideUpdate = false;
    currentlyProcessingQueue = null;
    function initializeUpdateQueue(fiber) {
        var queue = {
            baseState: fiber.memoizedState,
            firstBaseUpdate: null,
            lastBaseUpdate: null,
            shared: {
                pending: null,
                interleaved: null,
                lanes: NoLanes
            },
            effects: null
        };
        fiber.updateQueue = queue;
    }
    function cloneUpdateQueue(current, workInProgress) {
        // Clone the update queue from current. Unless it's already a clone.
        var queue = workInProgress.updateQueue;
        var currentQueue = current.updateQueue;
        if (queue === currentQueue) {
            var clone = {
                baseState: currentQueue.baseState,
                firstBaseUpdate: currentQueue.firstBaseUpdate,
                lastBaseUpdate: currentQueue.lastBaseUpdate,
                shared: currentQueue.shared,
                effects: currentQueue.effects
            };
            workInProgress.updateQueue = clone;
        }
    }
    function createUpdate(eventTime, lane) {
        var update = {
            eventTime: eventTime,
            lane: lane,
            tag: UpdateState,
            payload: null,
            callback: null,
            next: null
        };
        return update;
    }
    function enqueueUpdate(fiber, update, lane) {
        var updateQueue = fiber.updateQueue;
        if (updateQueue === null) // Only occurs if the fiber has been unmounted.
        return;
        var sharedQueue = updateQueue.shared;
        if (isInterleavedUpdate(fiber)) {
            var interleaved = sharedQueue.interleaved;
            if (interleaved === null) {
                // This is the first update. Create a circular list.
                update.next = update; // At the end of the current render, this queue's interleaved updates will
                // be transferred to the pending queue.
                pushInterleavedQueue(sharedQueue);
            } else {
                update.next = interleaved.next;
                interleaved.next = update;
            }
            sharedQueue.interleaved = update;
        } else {
            var pending = sharedQueue.pending;
            if (pending === null) // This is the first update. Create a circular list.
            update.next = update;
            else {
                update.next = pending.next;
                pending.next = update;
            }
            sharedQueue.pending = update;
        }
        if (currentlyProcessingQueue === sharedQueue && !didWarnUpdateInsideUpdate) {
            error1("An update (setState, replaceState, or forceUpdate) was scheduled from inside an update function. Update functions should be pure, with zero side-effects. Consider using componentDidUpdate or a callback.");
            didWarnUpdateInsideUpdate = true;
        }
    }
    function entangleTransitions(root, fiber, lane) {
        var updateQueue = fiber.updateQueue;
        if (updateQueue === null) // Only occurs if the fiber has been unmounted.
        return;
        var sharedQueue = updateQueue.shared;
        if (isTransitionLane(lane)) {
            var queueLanes = sharedQueue.lanes; // If any entangled lanes are no longer pending on the root, then they must
            // have finished. We can remove them from the shared queue, which represents
            // a superset of the actually pending lanes. In some cases we may entangle
            // more than we need to, but that's OK. In fact it's worse if we *don't*
            // entangle when we should.
            queueLanes = intersectLanes(queueLanes, root.pendingLanes); // Entangle the new transition lane with the other transition lanes.
            var newQueueLanes = mergeLanes(queueLanes, lane);
            sharedQueue.lanes = newQueueLanes; // Even if queue.lanes already include lane, we don't know for certain if
            // the lane finished since the last time we entangled it. So we need to
            // entangle it again, just to be sure.
            markRootEntangled(root, newQueueLanes);
        }
    }
    function enqueueCapturedUpdate(workInProgress, capturedUpdate) {
        // Captured updates are updates that are thrown by a child during the render
        // phase. They should be discarded if the render is aborted. Therefore,
        // we should only put them on the work-in-progress queue, not the current one.
        var queue = workInProgress.updateQueue; // Check if the work-in-progress queue is a clone.
        var current = workInProgress.alternate;
        if (current !== null) {
            var currentQueue = current.updateQueue;
            if (queue === currentQueue) {
                // The work-in-progress queue is the same as current. This happens when
                // we bail out on a parent fiber that then captures an error thrown by
                // a child. Since we want to append the update only to the work-in
                // -progress queue, we need to clone the updates. We usually clone during
                // processUpdateQueue, but that didn't happen in this case because we
                // skipped over the parent when we bailed out.
                var newFirst = null;
                var newLast = null;
                var firstBaseUpdate = queue.firstBaseUpdate;
                if (firstBaseUpdate !== null) {
                    // Loop through the updates and clone them.
                    var update = firstBaseUpdate;
                    do {
                        var clone = {
                            eventTime: update.eventTime,
                            lane: update.lane,
                            tag: update.tag,
                            payload: update.payload,
                            callback: update.callback,
                            next: null
                        };
                        if (newLast === null) newFirst = newLast = clone;
                        else {
                            newLast.next = clone;
                            newLast = clone;
                        }
                        update = update.next;
                    }while (update !== null) // Append the captured update the end of the cloned list.
                    if (newLast === null) newFirst = newLast = capturedUpdate;
                    else {
                        newLast.next = capturedUpdate;
                        newLast = capturedUpdate;
                    }
                } else // There are no base updates.
                newFirst = newLast = capturedUpdate;
                queue = {
                    baseState: currentQueue.baseState,
                    firstBaseUpdate: newFirst,
                    lastBaseUpdate: newLast,
                    shared: currentQueue.shared,
                    effects: currentQueue.effects
                };
                workInProgress.updateQueue = queue;
                return;
            }
        } // Append the update to the end of the list.
        var lastBaseUpdate = queue.lastBaseUpdate;
        if (lastBaseUpdate === null) queue.firstBaseUpdate = capturedUpdate;
        else lastBaseUpdate.next = capturedUpdate;
        queue.lastBaseUpdate = capturedUpdate;
    }
    function getStateFromUpdate(workInProgress, queue, update, prevState, nextProps, instance) {
        switch(update.tag){
            case ReplaceState:
                var payload = update.payload;
                if (typeof payload === 'function') {
                    enterDisallowedContextReadInDEV();
                    var nextState = payload.call(instance, prevState, nextProps);
                    if (workInProgress.mode & StrictLegacyMode) {
                        setIsStrictModeForDevtools(true);
                        try {
                            payload.call(instance, prevState, nextProps);
                        } finally{
                            setIsStrictModeForDevtools(false);
                        }
                    }
                    exitDisallowedContextReadInDEV();
                    return nextState;
                } // State object
                return payload;
            case CaptureUpdate:
                workInProgress.flags = workInProgress.flags & ~ShouldCapture | DidCapture;
            // Intentional fallthrough
            case UpdateState:
                var _payload = update.payload;
                var partialState;
                if (typeof _payload === 'function') {
                    enterDisallowedContextReadInDEV();
                    partialState = _payload.call(instance, prevState, nextProps);
                    if (workInProgress.mode & StrictLegacyMode) {
                        setIsStrictModeForDevtools(true);
                        try {
                            _payload.call(instance, prevState, nextProps);
                        } finally{
                            setIsStrictModeForDevtools(false);
                        }
                    }
                    exitDisallowedContextReadInDEV();
                } else // Partial state object
                partialState = _payload;
                if (partialState === null || partialState === undefined) // Null and undefined are treated as no-ops.
                return prevState;
                 // Merge the partial state and the previous state.
                return assign({}, prevState, partialState);
            case ForceUpdate:
                hasForceUpdate = true;
                return prevState;
        }
        return prevState;
    }
    function processUpdateQueue(workInProgress, props, instance, renderLanes) {
        // This is always non-null on a ClassComponent or HostRoot
        var queue = workInProgress.updateQueue;
        hasForceUpdate = false;
        currentlyProcessingQueue = queue.shared;
        var firstBaseUpdate = queue.firstBaseUpdate;
        var lastBaseUpdate = queue.lastBaseUpdate; // Check if there are pending updates. If so, transfer them to the base queue.
        var pendingQueue = queue.shared.pending;
        if (pendingQueue !== null) {
            queue.shared.pending = null; // The pending queue is circular. Disconnect the pointer between first
            // and last so that it's non-circular.
            var lastPendingUpdate = pendingQueue;
            var firstPendingUpdate = lastPendingUpdate.next;
            lastPendingUpdate.next = null; // Append pending updates to base queue
            if (lastBaseUpdate === null) firstBaseUpdate = firstPendingUpdate;
            else lastBaseUpdate.next = firstPendingUpdate;
            lastBaseUpdate = lastPendingUpdate; // If there's a current queue, and it's different from the base queue, then
            // we need to transfer the updates to that queue, too. Because the base
            // queue is a singly-linked list with no cycles, we can append to both
            // lists and take advantage of structural sharing.
            // TODO: Pass `current` as argument
            var current = workInProgress.alternate;
            if (current !== null) {
                // This is always non-null on a ClassComponent or HostRoot
                var currentQueue = current.updateQueue;
                var currentLastBaseUpdate = currentQueue.lastBaseUpdate;
                if (currentLastBaseUpdate !== lastBaseUpdate) {
                    if (currentLastBaseUpdate === null) currentQueue.firstBaseUpdate = firstPendingUpdate;
                    else currentLastBaseUpdate.next = firstPendingUpdate;
                    currentQueue.lastBaseUpdate = lastPendingUpdate;
                }
            }
        } // These values may change as we process the queue.
        if (firstBaseUpdate !== null) {
            // Iterate through the list of updates to compute the result.
            var newState = queue.baseState; // TODO: Don't need to accumulate this. Instead, we can remove renderLanes
            // from the original lanes.
            var newLanes = NoLanes;
            var newBaseState = null;
            var newFirstBaseUpdate = null;
            var newLastBaseUpdate = null;
            var update = firstBaseUpdate;
            do {
                var updateLane = update.lane;
                var updateEventTime = update.eventTime;
                if (!isSubsetOfLanes(renderLanes, updateLane)) {
                    // Priority is insufficient. Skip this update. If this is the first
                    // skipped update, the previous update/state is the new base
                    // update/state.
                    var clone = {
                        eventTime: updateEventTime,
                        lane: updateLane,
                        tag: update.tag,
                        payload: update.payload,
                        callback: update.callback,
                        next: null
                    };
                    if (newLastBaseUpdate === null) {
                        newFirstBaseUpdate = newLastBaseUpdate = clone;
                        newBaseState = newState;
                    } else newLastBaseUpdate = newLastBaseUpdate.next = clone;
                     // Update the remaining priority in the queue.
                    newLanes = mergeLanes(newLanes, updateLane);
                } else {
                    // This update does have sufficient priority.
                    if (newLastBaseUpdate !== null) {
                        var _clone = {
                            eventTime: updateEventTime,
                            // This update is going to be committed so we never want uncommit
                            // it. Using NoLane works because 0 is a subset of all bitmasks, so
                            // this will never be skipped by the check above.
                            lane: NoLane,
                            tag: update.tag,
                            payload: update.payload,
                            callback: update.callback,
                            next: null
                        };
                        newLastBaseUpdate = newLastBaseUpdate.next = _clone;
                    } // Process this update.
                    newState = getStateFromUpdate(workInProgress, queue, update, newState, props, instance);
                    var callback = update.callback;
                    if (callback !== null && // callback again.
                    update.lane !== NoLane) {
                        workInProgress.flags |= Callback;
                        var effects = queue.effects;
                        if (effects === null) queue.effects = [
                            update
                        ];
                        else effects.push(update);
                    }
                }
                update = update.next;
                if (update === null) {
                    pendingQueue = queue.shared.pending;
                    if (pendingQueue === null) break;
                    else {
                        // An update was scheduled from inside a reducer. Add the new
                        // pending updates to the end of the list and keep processing.
                        var _lastPendingUpdate = pendingQueue; // Intentionally unsound. Pending updates form a circular list, but we
                        // unravel them when transferring them to the base queue.
                        var _firstPendingUpdate = _lastPendingUpdate.next;
                        _lastPendingUpdate.next = null;
                        update = _firstPendingUpdate;
                        queue.lastBaseUpdate = _lastPendingUpdate;
                        queue.shared.pending = null;
                    }
                }
            }while (true)
            if (newLastBaseUpdate === null) newBaseState = newState;
            queue.baseState = newBaseState;
            queue.firstBaseUpdate = newFirstBaseUpdate;
            queue.lastBaseUpdate = newLastBaseUpdate; // Interleaved updates are stored on a separate queue. We aren't going to
            // process them during this render, but we do need to track which lanes
            // are remaining.
            var lastInterleaved = queue.shared.interleaved;
            if (lastInterleaved !== null) {
                var interleaved = lastInterleaved;
                do {
                    newLanes = mergeLanes(newLanes, interleaved.lane);
                    interleaved = interleaved.next;
                }while (interleaved !== lastInterleaved)
            } else if (firstBaseUpdate === null) // `queue.lanes` is used for entangling transitions. We can set it back to
            // zero once the queue is empty.
            queue.shared.lanes = NoLanes;
             // Set the remaining expiration time to be whatever is remaining in the queue.
            // This should be fine because the only two other things that contribute to
            // expiration time are props and context. We're already in the middle of the
            // begin phase by the time we start processing the queue, so we've already
            // dealt with the props. Context in components that specify
            // shouldComponentUpdate is tricky; but we'll have to account for
            // that regardless.
            markSkippedUpdateLanes(newLanes);
            workInProgress.lanes = newLanes;
            workInProgress.memoizedState = newState;
        }
        currentlyProcessingQueue = null;
    }
    function callCallback1(callback, context) {
        if (typeof callback !== 'function') throw new Error('Invalid argument passed as callback. Expected a function. Instead ' + ("received: " + callback));
        callback.call(context);
    }
    function resetHasForceUpdateBeforeProcessing() {
        hasForceUpdate = false;
    }
    function checkHasForceUpdateAfterProcessing() {
        return hasForceUpdate;
    }
    function commitUpdateQueue(finishedWork, finishedQueue, instance) {
        // Commit the effects
        var effects = finishedQueue.effects;
        finishedQueue.effects = null;
        if (effects !== null) for(var i = 0; i < effects.length; i++){
            var effect = effects[i];
            var callback = effect.callback;
            if (callback !== null) {
                effect.callback = null;
                callCallback1(callback, instance);
            }
        }
    }
    var fakeInternalInstance = {}; // React.Component uses a shared frozen object by default.
    // We'll use it to determine whether we need to initialize legacy refs.
    var emptyRefsObject = new React.Component().refs;
    var didWarnAboutStateAssignmentForComponent;
    var didWarnAboutUninitializedState;
    var didWarnAboutGetSnapshotBeforeUpdateWithoutDidUpdate;
    var didWarnAboutLegacyLifecyclesAndDerivedState;
    var didWarnAboutUndefinedDerivedState;
    var warnOnUndefinedDerivedState;
    var warnOnInvalidCallback;
    var didWarnAboutDirectlyAssigningPropsToState;
    var didWarnAboutContextTypeAndContextTypes;
    var didWarnAboutInvalidateContextType;
    didWarnAboutStateAssignmentForComponent = new Set();
    didWarnAboutUninitializedState = new Set();
    didWarnAboutGetSnapshotBeforeUpdateWithoutDidUpdate = new Set();
    didWarnAboutLegacyLifecyclesAndDerivedState = new Set();
    didWarnAboutDirectlyAssigningPropsToState = new Set();
    didWarnAboutUndefinedDerivedState = new Set();
    didWarnAboutContextTypeAndContextTypes = new Set();
    didWarnAboutInvalidateContextType = new Set();
    var didWarnOnInvalidCallback = new Set();
    warnOnInvalidCallback = function(callback, callerName) {
        if (callback === null || typeof callback === 'function') return;
        var key = callerName + '_' + callback;
        if (!didWarnOnInvalidCallback.has(key)) {
            didWarnOnInvalidCallback.add(key);
            error1("%s(...): Expected the last optional `callback` argument to be a function. Instead received: %s.", callerName, callback);
        }
    };
    warnOnUndefinedDerivedState = function(type, partialState) {
        if (partialState === undefined) {
            var componentName = getComponentNameFromType(type) || 'Component';
            if (!didWarnAboutUndefinedDerivedState.has(componentName)) {
                didWarnAboutUndefinedDerivedState.add(componentName);
                error1("%s.getDerivedStateFromProps(): A valid state object (or null) must be returned. You have returned undefined.", componentName);
            }
        }
    }; // This is so gross but it's at least non-critical and can be removed if
    // it causes problems. This is meant to give a nicer error message for
    // ReactDOM15.unstable_renderSubtreeIntoContainer(reactDOM16Component,
    // ...)) which otherwise throws a "_processChildContext is not a function"
    // exception.
    Object.defineProperty(fakeInternalInstance, '_processChildContext', {
        enumerable: false,
        value: function() {
            throw new Error("_processChildContext is not available in React 16+. This likely means you have multiple copies of React and are attempting to nest a React 15 tree inside a React 16 tree using unstable_renderSubtreeIntoContainer, which isn't supported. Try to make sure you have only one copy of React (and ideally, switch to ReactDOM.createPortal).");
        }
    });
    Object.freeze(fakeInternalInstance);
    function applyDerivedStateFromProps(workInProgress, ctor, getDerivedStateFromProps, nextProps) {
        var prevState = workInProgress.memoizedState;
        var partialState = getDerivedStateFromProps(nextProps, prevState);
        if (workInProgress.mode & StrictLegacyMode) {
            setIsStrictModeForDevtools(true);
            try {
                // Invoke the function an extra time to help detect side-effects.
                partialState = getDerivedStateFromProps(nextProps, prevState);
            } finally{
                setIsStrictModeForDevtools(false);
            }
        }
        warnOnUndefinedDerivedState(ctor, partialState);
        var memoizedState = partialState === null || partialState === undefined ? prevState : assign({}, prevState, partialState);
        workInProgress.memoizedState = memoizedState; // Once the update queue is empty, persist the derived state onto the
        // base state.
        if (workInProgress.lanes === NoLanes) {
            // Queue is always non-null for classes
            var updateQueue = workInProgress.updateQueue;
            updateQueue.baseState = memoizedState;
        }
    }
    var classComponentUpdater = {
        isMounted: isMounted,
        enqueueSetState: function(inst, payload, callback) {
            var fiber = get1(inst);
            var eventTime = requestEventTime();
            var lane = requestUpdateLane(fiber);
            var update = createUpdate(eventTime, lane);
            update.payload = payload;
            if (callback !== undefined && callback !== null) {
                warnOnInvalidCallback(callback, 'setState');
                update.callback = callback;
            }
            enqueueUpdate(fiber, update);
            var root = scheduleUpdateOnFiber(fiber, lane, eventTime);
            if (root !== null) entangleTransitions(root, fiber, lane);
            markStateUpdateScheduled(fiber, lane);
        },
        enqueueReplaceState: function(inst, payload, callback) {
            var fiber = get1(inst);
            var eventTime = requestEventTime();
            var lane = requestUpdateLane(fiber);
            var update = createUpdate(eventTime, lane);
            update.tag = ReplaceState;
            update.payload = payload;
            if (callback !== undefined && callback !== null) {
                warnOnInvalidCallback(callback, 'replaceState');
                update.callback = callback;
            }
            enqueueUpdate(fiber, update);
            var root = scheduleUpdateOnFiber(fiber, lane, eventTime);
            if (root !== null) entangleTransitions(root, fiber, lane);
            markStateUpdateScheduled(fiber, lane);
        },
        enqueueForceUpdate: function(inst, callback) {
            var fiber = get1(inst);
            var eventTime = requestEventTime();
            var lane = requestUpdateLane(fiber);
            var update = createUpdate(eventTime, lane);
            update.tag = ForceUpdate;
            if (callback !== undefined && callback !== null) {
                warnOnInvalidCallback(callback, 'forceUpdate');
                update.callback = callback;
            }
            enqueueUpdate(fiber, update);
            var root = scheduleUpdateOnFiber(fiber, lane, eventTime);
            if (root !== null) entangleTransitions(root, fiber, lane);
            markForceUpdateScheduled(fiber, lane);
        }
    };
    function checkShouldComponentUpdate(workInProgress, ctor, oldProps, newProps, oldState, newState, nextContext) {
        var instance = workInProgress.stateNode;
        if (typeof instance.shouldComponentUpdate === 'function') {
            var shouldUpdate = instance.shouldComponentUpdate(newProps, newState, nextContext);
            if (workInProgress.mode & StrictLegacyMode) {
                setIsStrictModeForDevtools(true);
                try {
                    // Invoke the function an extra time to help detect side-effects.
                    shouldUpdate = instance.shouldComponentUpdate(newProps, newState, nextContext);
                } finally{
                    setIsStrictModeForDevtools(false);
                }
            }
            if (shouldUpdate === undefined) error1("%s.shouldComponentUpdate(): Returned undefined instead of a boolean value. Make sure to return true or false.", getComponentNameFromType(ctor) || 'Component');
            return shouldUpdate;
        }
        if (ctor.prototype && ctor.prototype.isPureReactComponent) return !shallowEqual(oldProps, newProps) || !shallowEqual(oldState, newState);
        return true;
    }
    function checkClassInstance(workInProgress, ctor, newProps) {
        var instance = workInProgress.stateNode;
        var name = getComponentNameFromType(ctor) || 'Component';
        var renderPresent = instance.render;
        if (!renderPresent) {
            if (ctor.prototype && typeof ctor.prototype.render === 'function') error1("%s(...): No `render` method found on the returned component instance: did you accidentally return an object from the constructor?", name);
            else error1("%s(...): No `render` method found on the returned component instance: you may have forgotten to define `render`.", name);
        }
        if (instance.getInitialState && !instance.getInitialState.isReactClassApproved && !instance.state) error1("getInitialState was defined on %s, a plain JavaScript class. This is only supported for classes created using React.createClass. Did you mean to define a state property instead?", name);
        if (instance.getDefaultProps && !instance.getDefaultProps.isReactClassApproved) error1("getDefaultProps was defined on %s, a plain JavaScript class. This is only supported for classes created using React.createClass. Use a static property to define defaultProps instead.", name);
        if (instance.propTypes) error1("propTypes was defined as an instance property on %s. Use a static property to define propTypes instead.", name);
        if (instance.contextType) error1("contextType was defined as an instance property on %s. Use a static property to define contextType instead.", name);
        if (instance.contextTypes) error1("contextTypes was defined as an instance property on %s. Use a static property to define contextTypes instead.", name);
        if (ctor.contextType && ctor.contextTypes && !didWarnAboutContextTypeAndContextTypes.has(ctor)) {
            didWarnAboutContextTypeAndContextTypes.add(ctor);
            error1("%s declares both contextTypes and contextType static properties. The legacy contextTypes property will be ignored.", name);
        }
        if (typeof instance.componentShouldUpdate === 'function') error1("%s has a method called componentShouldUpdate(). Did you mean shouldComponentUpdate()? The name is phrased as a question because the function is expected to return a value.", name);
        if (ctor.prototype && ctor.prototype.isPureReactComponent && typeof instance.shouldComponentUpdate !== 'undefined') error1("%s has a method called shouldComponentUpdate(). shouldComponentUpdate should not be used when extending React.PureComponent. Please extend React.Component if shouldComponentUpdate is used.", getComponentNameFromType(ctor) || 'A pure component');
        if (typeof instance.componentDidUnmount === 'function') error1("%s has a method called componentDidUnmount(). But there is no such lifecycle method. Did you mean componentWillUnmount()?", name);
        if (typeof instance.componentDidReceiveProps === 'function') error1("%s has a method called componentDidReceiveProps(). But there is no such lifecycle method. If you meant to update the state in response to changing props, use componentWillReceiveProps(). If you meant to fetch data or run side-effects or mutations after React has updated the UI, use componentDidUpdate().", name);
        if (typeof instance.componentWillRecieveProps === 'function') error1("%s has a method called componentWillRecieveProps(). Did you mean componentWillReceiveProps()?", name);
        if (typeof instance.UNSAFE_componentWillRecieveProps === 'function') error1("%s has a method called UNSAFE_componentWillRecieveProps(). Did you mean UNSAFE_componentWillReceiveProps()?", name);
        var hasMutatedProps = instance.props !== newProps;
        if (instance.props !== undefined && hasMutatedProps) error1("%s(...): When calling super() in `%s`, make sure to pass up the same props that your component's constructor was passed.", name, name);
        if (instance.defaultProps) error1("Setting defaultProps as an instance property on %s is not supported and will be ignored. Instead, define defaultProps as a static property on %s.", name, name);
        if (typeof instance.getSnapshotBeforeUpdate === 'function' && typeof instance.componentDidUpdate !== 'function' && !didWarnAboutGetSnapshotBeforeUpdateWithoutDidUpdate.has(ctor)) {
            didWarnAboutGetSnapshotBeforeUpdateWithoutDidUpdate.add(ctor);
            error1("%s: getSnapshotBeforeUpdate() should be used with componentDidUpdate(). This component defines getSnapshotBeforeUpdate() only.", getComponentNameFromType(ctor));
        }
        if (typeof instance.getDerivedStateFromProps === 'function') error1("%s: getDerivedStateFromProps() is defined as an instance method and will be ignored. Instead, declare it as a static method.", name);
        if (typeof instance.getDerivedStateFromError === 'function') error1("%s: getDerivedStateFromError() is defined as an instance method and will be ignored. Instead, declare it as a static method.", name);
        if (typeof ctor.getSnapshotBeforeUpdate === 'function') error1("%s: getSnapshotBeforeUpdate() is defined as a static method and will be ignored. Instead, declare it as an instance method.", name);
        var _state = instance.state;
        if (_state && (typeof _state !== 'object' || isArray(_state))) error1('%s.state: must be set to an object or null', name);
        if (typeof instance.getChildContext === 'function' && typeof ctor.childContextTypes !== 'object') error1("%s.getChildContext(): childContextTypes must be defined in order to use getChildContext().", name);
    }
    function adoptClassInstance(workInProgress, instance) {
        instance.updater = classComponentUpdater;
        workInProgress.stateNode = instance; // The instance needs access to the fiber so that it can schedule updates
        set1(instance, workInProgress);
        instance._reactInternalInstance = fakeInternalInstance;
    }
    function constructClassInstance(workInProgress, ctor, props) {
        var isLegacyContextConsumer = false;
        var unmaskedContext = emptyContextObject;
        var context = emptyContextObject;
        var contextType = ctor.contextType;
        if ('contextType' in ctor) {
            var isValid = contextType === null || contextType !== undefined && contextType.$$typeof === REACT_CONTEXT_TYPE && contextType._context === undefined; // Not a <Context.Consumer>
            if (!isValid && !didWarnAboutInvalidateContextType.has(ctor)) {
                didWarnAboutInvalidateContextType.add(ctor);
                var addendum = '';
                if (contextType === undefined) addendum = " However, it is set to undefined. This can be caused by a typo or by mixing up named and default imports. This can also happen due to a circular dependency, so try moving the createContext() call to a separate file.";
                else if (typeof contextType !== 'object') addendum = ' However, it is set to a ' + typeof contextType + '.';
                else if (contextType.$$typeof === REACT_PROVIDER_TYPE) addendum = ' Did you accidentally pass the Context.Provider instead?';
                else if (contextType._context !== undefined) // <Context.Consumer>
                addendum = ' Did you accidentally pass the Context.Consumer instead?';
                else addendum = ' However, it is set to an object with keys {' + Object.keys(contextType).join(', ') + '}.';
                error1("%s defines an invalid contextType. contextType should point to the Context object returned by React.createContext().%s", getComponentNameFromType(ctor) || 'Component', addendum);
            }
        }
        if (typeof contextType === 'object' && contextType !== null) context = readContext(contextType);
        else {
            unmaskedContext = getUnmaskedContext(workInProgress, ctor, true);
            var contextTypes = ctor.contextTypes;
            isLegacyContextConsumer = contextTypes !== null && contextTypes !== undefined;
            context = isLegacyContextConsumer ? getMaskedContext(workInProgress, unmaskedContext) : emptyContextObject;
        }
        var instance = new ctor(props, context); // Instantiate twice to help detect side-effects.
        if (workInProgress.mode & StrictLegacyMode) {
            setIsStrictModeForDevtools(true);
            try {
                instance = new ctor(props, context); // eslint-disable-line no-new
            } finally{
                setIsStrictModeForDevtools(false);
            }
        }
        var state = workInProgress.memoizedState = instance.state !== null && instance.state !== undefined ? instance.state : null;
        adoptClassInstance(workInProgress, instance);
        if (typeof ctor.getDerivedStateFromProps === 'function' && state === null) {
            var componentName = getComponentNameFromType(ctor) || 'Component';
            if (!didWarnAboutUninitializedState.has(componentName)) {
                didWarnAboutUninitializedState.add(componentName);
                error1("`%s` uses `getDerivedStateFromProps` but its initial state is %s. This is not recommended. Instead, define the initial state by assigning an object to `this.state` in the constructor of `%s`. This ensures that `getDerivedStateFromProps` arguments have a consistent shape.", componentName, instance.state === null ? 'null' : 'undefined', componentName);
            }
        } // If new component APIs are defined, "unsafe" lifecycles won't be called.
        // Warn about these lifecycles if they are present.
        // Don't warn about react-lifecycles-compat polyfilled methods though.
        if (typeof ctor.getDerivedStateFromProps === 'function' || typeof instance.getSnapshotBeforeUpdate === 'function') {
            var foundWillMountName = null;
            var foundWillReceivePropsName = null;
            var foundWillUpdateName = null;
            if (typeof instance.componentWillMount === 'function' && instance.componentWillMount.__suppressDeprecationWarning !== true) foundWillMountName = 'componentWillMount';
            else if (typeof instance.UNSAFE_componentWillMount === 'function') foundWillMountName = 'UNSAFE_componentWillMount';
            if (typeof instance.componentWillReceiveProps === 'function' && instance.componentWillReceiveProps.__suppressDeprecationWarning !== true) foundWillReceivePropsName = 'componentWillReceiveProps';
            else if (typeof instance.UNSAFE_componentWillReceiveProps === 'function') foundWillReceivePropsName = 'UNSAFE_componentWillReceiveProps';
            if (typeof instance.componentWillUpdate === 'function' && instance.componentWillUpdate.__suppressDeprecationWarning !== true) foundWillUpdateName = 'componentWillUpdate';
            else if (typeof instance.UNSAFE_componentWillUpdate === 'function') foundWillUpdateName = 'UNSAFE_componentWillUpdate';
            if (foundWillMountName !== null || foundWillReceivePropsName !== null || foundWillUpdateName !== null) {
                var _componentName = getComponentNameFromType(ctor) || 'Component';
                var newApiName = typeof ctor.getDerivedStateFromProps === 'function' ? 'getDerivedStateFromProps()' : 'getSnapshotBeforeUpdate()';
                if (!didWarnAboutLegacyLifecyclesAndDerivedState.has(_componentName)) {
                    didWarnAboutLegacyLifecyclesAndDerivedState.add(_componentName);
                    error1("Unsafe legacy lifecycles will not be called for components using new component APIs.\n\n%s uses %s but also contains the following legacy lifecycles:%s%s%s\n\nThe above lifecycles should be removed. Learn more about this warning here:\nhttps://reactjs.org/link/unsafe-component-lifecycles", _componentName, newApiName, foundWillMountName !== null ? "\n  " + foundWillMountName : '', foundWillReceivePropsName !== null ? "\n  " + foundWillReceivePropsName : '', foundWillUpdateName !== null ? "\n  " + foundWillUpdateName : '');
                }
            }
        }
        // ReactFiberContext usually updates this cache but can't for newly-created instances.
        if (isLegacyContextConsumer) cacheContext(workInProgress, unmaskedContext, context);
        return instance;
    }
    function callComponentWillMount(workInProgress, instance) {
        var oldState = instance.state;
        if (typeof instance.componentWillMount === 'function') instance.componentWillMount();
        if (typeof instance.UNSAFE_componentWillMount === 'function') instance.UNSAFE_componentWillMount();
        if (oldState !== instance.state) {
            error1("%s.componentWillMount(): Assigning directly to this.state is deprecated (except inside a component's constructor). Use setState instead.", getComponentNameFromFiber(workInProgress) || 'Component');
            classComponentUpdater.enqueueReplaceState(instance, instance.state, null);
        }
    }
    function callComponentWillReceiveProps(workInProgress, instance, newProps, nextContext) {
        var oldState = instance.state;
        if (typeof instance.componentWillReceiveProps === 'function') instance.componentWillReceiveProps(newProps, nextContext);
        if (typeof instance.UNSAFE_componentWillReceiveProps === 'function') instance.UNSAFE_componentWillReceiveProps(newProps, nextContext);
        if (instance.state !== oldState) {
            var componentName = getComponentNameFromFiber(workInProgress) || 'Component';
            if (!didWarnAboutStateAssignmentForComponent.has(componentName)) {
                didWarnAboutStateAssignmentForComponent.add(componentName);
                error1("%s.componentWillReceiveProps(): Assigning directly to this.state is deprecated (except inside a component's constructor). Use setState instead.", componentName);
            }
            classComponentUpdater.enqueueReplaceState(instance, instance.state, null);
        }
    } // Invokes the mount life-cycles on a previously never rendered instance.
    function mountClassInstance(workInProgress, ctor, newProps, renderLanes) {
        checkClassInstance(workInProgress, ctor, newProps);
        var instance = workInProgress.stateNode;
        instance.props = newProps;
        instance.state = workInProgress.memoizedState;
        instance.refs = emptyRefsObject;
        initializeUpdateQueue(workInProgress);
        var contextType = ctor.contextType;
        if (typeof contextType === 'object' && contextType !== null) instance.context = readContext(contextType);
        else {
            var unmaskedContext = getUnmaskedContext(workInProgress, ctor, true);
            instance.context = getMaskedContext(workInProgress, unmaskedContext);
        }
        if (instance.state === newProps) {
            var componentName = getComponentNameFromType(ctor) || 'Component';
            if (!didWarnAboutDirectlyAssigningPropsToState.has(componentName)) {
                didWarnAboutDirectlyAssigningPropsToState.add(componentName);
                error1("%s: It is not recommended to assign props directly to state because updates to props won't be reflected in state. In most cases, it is better to use props directly.", componentName);
            }
        }
        if (workInProgress.mode & StrictLegacyMode) ReactStrictModeWarnings.recordLegacyContextWarning(workInProgress, instance);
        ReactStrictModeWarnings.recordUnsafeLifecycleWarnings(workInProgress, instance);
        instance.state = workInProgress.memoizedState;
        var getDerivedStateFromProps = ctor.getDerivedStateFromProps;
        if (typeof getDerivedStateFromProps === 'function') {
            applyDerivedStateFromProps(workInProgress, ctor, getDerivedStateFromProps, newProps);
            instance.state = workInProgress.memoizedState;
        } // In order to support react-lifecycles-compat polyfilled components,
        // Unsafe lifecycles should not be invoked for components using the new APIs.
        if (typeof ctor.getDerivedStateFromProps !== 'function' && typeof instance.getSnapshotBeforeUpdate !== 'function' && (typeof instance.UNSAFE_componentWillMount === 'function' || typeof instance.componentWillMount === 'function')) {
            callComponentWillMount(workInProgress, instance); // If we had additional state updates during this life-cycle, let's
            // process them now.
            processUpdateQueue(workInProgress, newProps, instance, renderLanes);
            instance.state = workInProgress.memoizedState;
        }
        if (typeof instance.componentDidMount === 'function') {
            var fiberFlags = Update;
            fiberFlags |= LayoutStatic;
            if ((workInProgress.mode & StrictEffectsMode) !== NoMode) fiberFlags |= MountLayoutDev;
            workInProgress.flags |= fiberFlags;
        }
    }
    function resumeMountClassInstance(workInProgress, ctor, newProps, renderLanes) {
        var instance = workInProgress.stateNode;
        var oldProps = workInProgress.memoizedProps;
        instance.props = oldProps;
        var oldContext = instance.context;
        var contextType = ctor.contextType;
        var nextContext = emptyContextObject;
        if (typeof contextType === 'object' && contextType !== null) nextContext = readContext(contextType);
        else {
            var nextLegacyUnmaskedContext = getUnmaskedContext(workInProgress, ctor, true);
            nextContext = getMaskedContext(workInProgress, nextLegacyUnmaskedContext);
        }
        var getDerivedStateFromProps = ctor.getDerivedStateFromProps;
        var hasNewLifecycles = typeof getDerivedStateFromProps === 'function' || typeof instance.getSnapshotBeforeUpdate === 'function'; // Note: During these life-cycles, instance.props/instance.state are what
        // ever the previously attempted to render - not the "current". However,
        // during componentDidUpdate we pass the "current" props.
        // In order to support react-lifecycles-compat polyfilled components,
        // Unsafe lifecycles should not be invoked for components using the new APIs.
        if (!hasNewLifecycles && (typeof instance.UNSAFE_componentWillReceiveProps === 'function' || typeof instance.componentWillReceiveProps === 'function')) {
            if (oldProps !== newProps || oldContext !== nextContext) callComponentWillReceiveProps(workInProgress, instance, newProps, nextContext);
        }
        resetHasForceUpdateBeforeProcessing();
        var oldState = workInProgress.memoizedState;
        var newState = instance.state = oldState;
        processUpdateQueue(workInProgress, newProps, instance, renderLanes);
        newState = workInProgress.memoizedState;
        if (oldProps === newProps && oldState === newState && !hasContextChanged1() && !checkHasForceUpdateAfterProcessing()) {
            // If an update was already in progress, we should schedule an Update
            // effect even though we're bailing out, so that cWU/cDU are called.
            if (typeof instance.componentDidMount === 'function') {
                var fiberFlags = Update;
                fiberFlags |= LayoutStatic;
                if ((workInProgress.mode & StrictEffectsMode) !== NoMode) fiberFlags |= MountLayoutDev;
                workInProgress.flags |= fiberFlags;
            }
            return false;
        }
        if (typeof getDerivedStateFromProps === 'function') {
            applyDerivedStateFromProps(workInProgress, ctor, getDerivedStateFromProps, newProps);
            newState = workInProgress.memoizedState;
        }
        var shouldUpdate = checkHasForceUpdateAfterProcessing() || checkShouldComponentUpdate(workInProgress, ctor, oldProps, newProps, oldState, newState, nextContext);
        if (shouldUpdate) {
            // In order to support react-lifecycles-compat polyfilled components,
            // Unsafe lifecycles should not be invoked for components using the new APIs.
            if (!hasNewLifecycles && (typeof instance.UNSAFE_componentWillMount === 'function' || typeof instance.componentWillMount === 'function')) {
                if (typeof instance.componentWillMount === 'function') instance.componentWillMount();
                if (typeof instance.UNSAFE_componentWillMount === 'function') instance.UNSAFE_componentWillMount();
            }
            if (typeof instance.componentDidMount === 'function') {
                var _fiberFlags = Update;
                _fiberFlags |= LayoutStatic;
                if ((workInProgress.mode & StrictEffectsMode) !== NoMode) _fiberFlags |= MountLayoutDev;
                workInProgress.flags |= _fiberFlags;
            }
        } else {
            // If an update was already in progress, we should schedule an Update
            // effect even though we're bailing out, so that cWU/cDU are called.
            if (typeof instance.componentDidMount === 'function') {
                var _fiberFlags2 = Update;
                _fiberFlags2 |= LayoutStatic;
                if ((workInProgress.mode & StrictEffectsMode) !== NoMode) _fiberFlags2 |= MountLayoutDev;
                workInProgress.flags |= _fiberFlags2;
            } // If shouldComponentUpdate returned false, we should still update the
            // memoized state to indicate that this work can be reused.
            workInProgress.memoizedProps = newProps;
            workInProgress.memoizedState = newState;
        } // Update the existing instance's state, props, and context pointers even
        // if shouldComponentUpdate returns false.
        instance.props = newProps;
        instance.state = newState;
        instance.context = nextContext;
        return shouldUpdate;
    } // Invokes the update life-cycles and returns false if it shouldn't rerender.
    function updateClassInstance(current, workInProgress, ctor, newProps, renderLanes) {
        var instance = workInProgress.stateNode;
        cloneUpdateQueue(current, workInProgress);
        var unresolvedOldProps = workInProgress.memoizedProps;
        var oldProps = workInProgress.type === workInProgress.elementType ? unresolvedOldProps : resolveDefaultProps(workInProgress.type, unresolvedOldProps);
        instance.props = oldProps;
        var unresolvedNewProps = workInProgress.pendingProps;
        var oldContext = instance.context;
        var contextType = ctor.contextType;
        var nextContext = emptyContextObject;
        if (typeof contextType === 'object' && contextType !== null) nextContext = readContext(contextType);
        else {
            var nextUnmaskedContext = getUnmaskedContext(workInProgress, ctor, true);
            nextContext = getMaskedContext(workInProgress, nextUnmaskedContext);
        }
        var getDerivedStateFromProps = ctor.getDerivedStateFromProps;
        var hasNewLifecycles = typeof getDerivedStateFromProps === 'function' || typeof instance.getSnapshotBeforeUpdate === 'function'; // Note: During these life-cycles, instance.props/instance.state are what
        // ever the previously attempted to render - not the "current". However,
        // during componentDidUpdate we pass the "current" props.
        // In order to support react-lifecycles-compat polyfilled components,
        // Unsafe lifecycles should not be invoked for components using the new APIs.
        if (!hasNewLifecycles && (typeof instance.UNSAFE_componentWillReceiveProps === 'function' || typeof instance.componentWillReceiveProps === 'function')) {
            if (unresolvedOldProps !== unresolvedNewProps || oldContext !== nextContext) callComponentWillReceiveProps(workInProgress, instance, newProps, nextContext);
        }
        resetHasForceUpdateBeforeProcessing();
        var oldState = workInProgress.memoizedState;
        var newState = instance.state = oldState;
        processUpdateQueue(workInProgress, newProps, instance, renderLanes);
        newState = workInProgress.memoizedState;
        if (unresolvedOldProps === unresolvedNewProps && oldState === newState && !hasContextChanged1() && !checkHasForceUpdateAfterProcessing() && !enableLazyContextPropagation) {
            // If an update was already in progress, we should schedule an Update
            // effect even though we're bailing out, so that cWU/cDU are called.
            if (typeof instance.componentDidUpdate === 'function') {
                if (unresolvedOldProps !== current.memoizedProps || oldState !== current.memoizedState) workInProgress.flags |= Update;
            }
            if (typeof instance.getSnapshotBeforeUpdate === 'function') {
                if (unresolvedOldProps !== current.memoizedProps || oldState !== current.memoizedState) workInProgress.flags |= Snapshot;
            }
            return false;
        }
        if (typeof getDerivedStateFromProps === 'function') {
            applyDerivedStateFromProps(workInProgress, ctor, getDerivedStateFromProps, newProps);
            newState = workInProgress.memoizedState;
        }
        var shouldUpdate = checkHasForceUpdateAfterProcessing() || checkShouldComponentUpdate(workInProgress, ctor, oldProps, newProps, oldState, newState, nextContext) || // both before and after `shouldComponentUpdate` has been called. Not ideal,
        // but I'm loath to refactor this function. This only happens for memoized
        // components so it's not that common.
        enableLazyContextPropagation;
        if (shouldUpdate) {
            // In order to support react-lifecycles-compat polyfilled components,
            // Unsafe lifecycles should not be invoked for components using the new APIs.
            if (!hasNewLifecycles && (typeof instance.UNSAFE_componentWillUpdate === 'function' || typeof instance.componentWillUpdate === 'function')) {
                if (typeof instance.componentWillUpdate === 'function') instance.componentWillUpdate(newProps, newState, nextContext);
                if (typeof instance.UNSAFE_componentWillUpdate === 'function') instance.UNSAFE_componentWillUpdate(newProps, newState, nextContext);
            }
            if (typeof instance.componentDidUpdate === 'function') workInProgress.flags |= Update;
            if (typeof instance.getSnapshotBeforeUpdate === 'function') workInProgress.flags |= Snapshot;
        } else {
            // If an update was already in progress, we should schedule an Update
            // effect even though we're bailing out, so that cWU/cDU are called.
            if (typeof instance.componentDidUpdate === 'function') {
                if (unresolvedOldProps !== current.memoizedProps || oldState !== current.memoizedState) workInProgress.flags |= Update;
            }
            if (typeof instance.getSnapshotBeforeUpdate === 'function') {
                if (unresolvedOldProps !== current.memoizedProps || oldState !== current.memoizedState) workInProgress.flags |= Snapshot;
            } // If shouldComponentUpdate returned false, we should still update the
            // memoized props/state to indicate that this work can be reused.
            workInProgress.memoizedProps = newProps;
            workInProgress.memoizedState = newState;
        } // Update the existing instance's state, props, and context pointers even
        // if shouldComponentUpdate returns false.
        instance.props = newProps;
        instance.state = newState;
        instance.context = nextContext;
        return shouldUpdate;
    }
    // TODO: Use the unified fiber stack module instead of this local one?
    // Intentionally not using it yet to derisk the initial implementation, because
    // the way we push/pop these values is a bit unusual. If there's a mistake, I'd
    // rather the ids be wrong than crash the whole reconciler.
    var forkStack = [];
    var forkStackIndex = 0;
    var treeForkProvider = null;
    var treeForkCount = 0;
    var idStack = [];
    var idStackIndex = 0;
    var treeContextProvider = null;
    var treeContextId = 1;
    var treeContextOverflow = '';
    function isForkedChild(workInProgress) {
        warnIfNotHydrating();
        return (workInProgress.flags & Forked) !== NoFlags;
    }
    function getForksAtLevel(workInProgress) {
        warnIfNotHydrating();
        return treeForkCount;
    }
    function getTreeId() {
        var overflow = treeContextOverflow;
        var idWithLeadingBit = treeContextId;
        var id = idWithLeadingBit & ~getLeadingBit(idWithLeadingBit);
        return id.toString(32) + overflow;
    }
    function pushTreeFork(workInProgress, totalChildren) {
        // This is called right after we reconcile an array (or iterator) of child
        // fibers, because that's the only place where we know how many children in
        // the whole set without doing extra work later, or storing addtional
        // information on the fiber.
        //
        // That's why this function is separate from pushTreeId — it's called during
        // the render phase of the fork parent, not the child, which is where we push
        // the other context values.
        //
        // In the Fizz implementation this is much simpler because the child is
        // rendered in the same callstack as the parent.
        //
        // It might be better to just add a `forks` field to the Fiber type. It would
        // make this module simpler.
        warnIfNotHydrating();
        forkStack[forkStackIndex++] = treeForkCount;
        forkStack[forkStackIndex++] = treeForkProvider;
        treeForkProvider = workInProgress;
        treeForkCount = totalChildren;
    }
    function pushTreeId(workInProgress, totalChildren, index) {
        warnIfNotHydrating();
        idStack[idStackIndex++] = treeContextId;
        idStack[idStackIndex++] = treeContextOverflow;
        idStack[idStackIndex++] = treeContextProvider;
        treeContextProvider = workInProgress;
        var baseIdWithLeadingBit = treeContextId;
        var baseOverflow = treeContextOverflow; // The leftmost 1 marks the end of the sequence, non-inclusive. It's not part
        // of the id; we use it to account for leading 0s.
        var baseLength = getBitLength(baseIdWithLeadingBit) - 1;
        var baseId = baseIdWithLeadingBit & ~(1 << baseLength);
        var slot = index + 1;
        var length = getBitLength(totalChildren) + baseLength; // 30 is the max length we can store without overflowing, taking into
        // consideration the leading 1 we use to mark the end of the sequence.
        if (length > 30) {
            // We overflowed the bitwise-safe range. Fall back to slower algorithm.
            // This branch assumes the length of the base id is greater than 5; it won't
            // work for smaller ids, because you need 5 bits per character.
            //
            // We encode the id in multiple steps: first the base id, then the
            // remaining digits.
            //
            // Each 5 bit sequence corresponds to a single base 32 character. So for
            // example, if the current id is 23 bits long, we can convert 20 of those
            // bits into a string of 4 characters, with 3 bits left over.
            //
            // First calculate how many bits in the base id represent a complete
            // sequence of characters.
            var numberOfOverflowBits = baseLength - baseLength % 5; // Then create a bitmask that selects only those bits.
            var newOverflowBits = (1 << numberOfOverflowBits) - 1; // Select the bits, and convert them to a base 32 string.
            var newOverflow = (baseId & newOverflowBits).toString(32); // Now we can remove those bits from the base id.
            var restOfBaseId = baseId >> numberOfOverflowBits;
            var restOfBaseLength = baseLength - numberOfOverflowBits; // Finally, encode the rest of the bits using the normal algorithm. Because
            // we made more room, this time it won't overflow.
            var restOfLength = getBitLength(totalChildren) + restOfBaseLength;
            var restOfNewBits = slot << restOfBaseLength;
            var id = restOfNewBits | restOfBaseId;
            var overflow = newOverflow + baseOverflow;
            treeContextId = 1 << restOfLength | id;
            treeContextOverflow = overflow;
        } else {
            // Normal path
            var newBits = slot << baseLength;
            var _id = newBits | baseId;
            var _overflow = baseOverflow;
            treeContextId = 1 << length | _id;
            treeContextOverflow = _overflow;
        }
    }
    function pushMaterializedTreeId(workInProgress) {
        warnIfNotHydrating(); // This component materialized an id. This will affect any ids that appear
        // in its children.
        var returnFiber = workInProgress.return;
        if (returnFiber !== null) {
            var numberOfForks = 1;
            var slotIndex = 0;
            pushTreeFork(workInProgress, numberOfForks);
            pushTreeId(workInProgress, numberOfForks, slotIndex);
        }
    }
    function getBitLength(number) {
        return 32 - clz32(number);
    }
    function getLeadingBit(id) {
        return 1 << getBitLength(id) - 1;
    }
    function popTreeContext(workInProgress) {
        // Restore the previous values.
        // This is a bit more complicated than other context-like modules in Fiber
        // because the same Fiber may appear on the stack multiple times and for
        // different reasons. We have to keep popping until the work-in-progress is
        // no longer at the top of the stack.
        while(workInProgress === treeForkProvider){
            treeForkProvider = forkStack[--forkStackIndex];
            forkStack[forkStackIndex] = null;
            treeForkCount = forkStack[--forkStackIndex];
            forkStack[forkStackIndex] = null;
        }
        while(workInProgress === treeContextProvider){
            treeContextProvider = idStack[--idStackIndex];
            idStack[idStackIndex] = null;
            treeContextOverflow = idStack[--idStackIndex];
            idStack[idStackIndex] = null;
            treeContextId = idStack[--idStackIndex];
            idStack[idStackIndex] = null;
        }
    }
    function getSuspendedTreeContext() {
        warnIfNotHydrating();
        if (treeContextProvider !== null) return {
            id: treeContextId,
            overflow: treeContextOverflow
        };
        else return null;
    }
    function restoreSuspendedTreeContext(workInProgress, suspendedContext) {
        warnIfNotHydrating();
        idStack[idStackIndex++] = treeContextId;
        idStack[idStackIndex++] = treeContextOverflow;
        idStack[idStackIndex++] = treeContextProvider;
        treeContextId = suspendedContext.id;
        treeContextOverflow = suspendedContext.overflow;
        treeContextProvider = workInProgress;
    }
    function warnIfNotHydrating() {
        if (!getIsHydrating()) error1("Expected to be hydrating. This is a bug in React. Please file an issue.");
    }
    // This may have been an insertion or a hydration.
    var hydrationParentFiber = null;
    var nextHydratableInstance = null;
    var isHydrating1 = false; // This flag allows for warning supression when we expect there to be mismatches
    // due to earlier mismatches or a suspended fiber.
    var didSuspendOrErrorDEV = false; // Hydration errors that were thrown inside this boundary
    var hydrationErrors = null;
    function warnIfHydrating() {
        if (isHydrating1) error1('We should not be hydrating here. This is a bug in React. Please file a bug.');
    }
    function markDidThrowWhileHydratingDEV() {
        didSuspendOrErrorDEV = true;
    }
    function enterHydrationState(fiber) {
        var parentInstance = fiber.stateNode.containerInfo;
        nextHydratableInstance = getFirstHydratableChildWithinContainer(parentInstance);
        hydrationParentFiber = fiber;
        isHydrating1 = true;
        hydrationErrors = null;
        didSuspendOrErrorDEV = false;
        return true;
    }
    function reenterHydrationStateFromDehydratedSuspenseInstance(fiber, suspenseInstance, treeContext) {
        nextHydratableInstance = getFirstHydratableChildWithinSuspenseInstance(suspenseInstance);
        hydrationParentFiber = fiber;
        isHydrating1 = true;
        hydrationErrors = null;
        didSuspendOrErrorDEV = false;
        if (treeContext !== null) restoreSuspendedTreeContext(fiber, treeContext);
        return true;
    }
    function warnUnhydratedInstance(returnFiber, instance) {
        switch(returnFiber.tag){
            case HostRoot:
                didNotHydrateInstanceWithinContainer(returnFiber.stateNode.containerInfo, instance);
                break;
            case HostComponent:
                var isConcurrentMode = (returnFiber.mode & ConcurrentMode) !== NoMode;
                didNotHydrateInstance(returnFiber.type, returnFiber.memoizedProps, returnFiber.stateNode, instance, isConcurrentMode);
                break;
            case SuspenseComponent:
                var suspenseState = returnFiber.memoizedState;
                if (suspenseState.dehydrated !== null) didNotHydrateInstanceWithinSuspenseInstance(suspenseState.dehydrated, instance);
                break;
        }
    }
    function deleteHydratableInstance(returnFiber, instance) {
        warnUnhydratedInstance(returnFiber, instance);
        var childToDelete = createFiberFromHostInstanceForDeletion();
        childToDelete.stateNode = instance;
        childToDelete.return = returnFiber;
        var deletions = returnFiber.deletions;
        if (deletions === null) {
            returnFiber.deletions = [
                childToDelete
            ];
            returnFiber.flags |= ChildDeletion;
        } else deletions.push(childToDelete);
    }
    function warnNonhydratedInstance(returnFiber, fiber) {
        if (didSuspendOrErrorDEV) // Inside a boundary that already suspended. We're currently rendering the
        // siblings of a suspended node. The mismatch may be due to the missing
        // data, so it's probably a false positive.
        return;
        switch(returnFiber.tag){
            case HostRoot:
                var parentContainer = returnFiber.stateNode.containerInfo;
                switch(fiber.tag){
                    case HostComponent:
                        var type = fiber.type;
                        var props = fiber.pendingProps;
                        didNotFindHydratableInstanceWithinContainer(parentContainer, type);
                        break;
                    case HostText:
                        var text = fiber.pendingProps;
                        didNotFindHydratableTextInstanceWithinContainer(parentContainer, text);
                        break;
                }
                break;
            case HostComponent:
                var parentType = returnFiber.type;
                var parentProps = returnFiber.memoizedProps;
                var parentInstance = returnFiber.stateNode;
                switch(fiber.tag){
                    case HostComponent:
                        var _type = fiber.type;
                        var _props = fiber.pendingProps;
                        var isConcurrentMode = (returnFiber.mode & ConcurrentMode) !== NoMode;
                        didNotFindHydratableInstance(parentType, parentProps, parentInstance, _type, _props, isConcurrentMode);
                        break;
                    case HostText:
                        var _text = fiber.pendingProps;
                        var _isConcurrentMode = (returnFiber.mode & ConcurrentMode) !== NoMode;
                        didNotFindHydratableTextInstance(parentType, parentProps, parentInstance, _text, _isConcurrentMode);
                        break;
                }
                break;
            case SuspenseComponent:
                var suspenseState = returnFiber.memoizedState;
                var _parentInstance = suspenseState.dehydrated;
                if (_parentInstance !== null) switch(fiber.tag){
                    case HostComponent:
                        var _type2 = fiber.type;
                        var _props2 = fiber.pendingProps;
                        didNotFindHydratableInstanceWithinSuspenseInstance(_parentInstance, _type2);
                        break;
                    case HostText:
                        var _text2 = fiber.pendingProps;
                        didNotFindHydratableTextInstanceWithinSuspenseInstance(_parentInstance, _text2);
                        break;
                }
                break;
            default:
                return;
        }
    }
    function insertNonHydratedInstance(returnFiber, fiber) {
        fiber.flags = fiber.flags & ~Hydrating | Placement;
        warnNonhydratedInstance(returnFiber, fiber);
    }
    function tryHydrate(fiber, nextInstance) {
        switch(fiber.tag){
            case HostComponent:
                var type = fiber.type;
                var props = fiber.pendingProps;
                var instance = canHydrateInstance(nextInstance, type);
                if (instance !== null) {
                    fiber.stateNode = instance;
                    hydrationParentFiber = fiber;
                    nextHydratableInstance = getFirstHydratableChild(instance);
                    return true;
                }
                return false;
            case HostText:
                var text = fiber.pendingProps;
                var textInstance = canHydrateTextInstance(nextInstance, text);
                if (textInstance !== null) {
                    fiber.stateNode = textInstance;
                    hydrationParentFiber = fiber; // Text Instances don't have children so there's nothing to hydrate.
                    nextHydratableInstance = null;
                    return true;
                }
                return false;
            case SuspenseComponent:
                var suspenseInstance = canHydrateSuspenseInstance(nextInstance);
                if (suspenseInstance !== null) {
                    var suspenseState = {
                        dehydrated: suspenseInstance,
                        treeContext: getSuspendedTreeContext(),
                        retryLane: OffscreenLane
                    };
                    fiber.memoizedState = suspenseState; // Store the dehydrated fragment as a child fiber.
                    // This simplifies the code for getHostSibling and deleting nodes,
                    // since it doesn't have to consider all Suspense boundaries and
                    // check if they're dehydrated ones or not.
                    var dehydratedFragment = createFiberFromDehydratedFragment(suspenseInstance);
                    dehydratedFragment.return = fiber;
                    fiber.child = dehydratedFragment;
                    hydrationParentFiber = fiber; // While a Suspense Instance does have children, we won't step into
                    // it during the first pass. Instead, we'll reenter it later.
                    nextHydratableInstance = null;
                    return true;
                }
                return false;
            default:
                return false;
        }
    }
    function shouldClientRenderOnMismatch(fiber) {
        return (fiber.mode & ConcurrentMode) !== NoMode && (fiber.flags & DidCapture) === NoFlags;
    }
    function throwOnHydrationMismatch(fiber) {
        throw new Error("Hydration failed because the initial UI does not match what was rendered on the server.");
    }
    function tryToClaimNextHydratableInstance(fiber) {
        if (!isHydrating1) return;
        var nextInstance = nextHydratableInstance;
        if (!nextInstance) {
            if (shouldClientRenderOnMismatch(fiber)) {
                warnNonhydratedInstance(hydrationParentFiber, fiber);
                throwOnHydrationMismatch();
            } // Nothing to hydrate. Make it an insertion.
            insertNonHydratedInstance(hydrationParentFiber, fiber);
            isHydrating1 = false;
            hydrationParentFiber = fiber;
            return;
        }
        var firstAttemptedInstance = nextInstance;
        if (!tryHydrate(fiber, nextInstance)) {
            if (shouldClientRenderOnMismatch(fiber)) {
                warnNonhydratedInstance(hydrationParentFiber, fiber);
                throwOnHydrationMismatch();
            } // If we can't hydrate this instance let's try the next one.
            // We use this as a heuristic. It's based on intuition and not data so it
            // might be flawed or unnecessary.
            nextInstance = getNextHydratableSibling(firstAttemptedInstance);
            var prevHydrationParentFiber = hydrationParentFiber;
            if (!nextInstance || !tryHydrate(fiber, nextInstance)) {
                // Nothing to hydrate. Make it an insertion.
                insertNonHydratedInstance(hydrationParentFiber, fiber);
                isHydrating1 = false;
                hydrationParentFiber = fiber;
                return;
            } // We matched the next one, we'll now assume that the first one was
            // superfluous and we'll delete it. Since we can't eagerly delete it
            // we'll have to schedule a deletion. To do that, this node needs a dummy
            // fiber associated with it.
            deleteHydratableInstance(prevHydrationParentFiber, firstAttemptedInstance);
        }
    }
    function prepareToHydrateHostInstance(fiber, rootContainerInstance, hostContext) {
        var instance = fiber.stateNode;
        var shouldWarnIfMismatchDev = !didSuspendOrErrorDEV;
        var updatePayload = hydrateInstance(instance, fiber.type, fiber.memoizedProps, rootContainerInstance, hostContext, fiber, shouldWarnIfMismatchDev); // TODO: Type this specific to this type of component.
        fiber.updateQueue = updatePayload; // If the update payload indicates that there is a change or if there
        // is a new ref we mark this as an update.
        if (updatePayload !== null) return true;
        return false;
    }
    function prepareToHydrateHostTextInstance(fiber) {
        var textInstance = fiber.stateNode;
        var textContent = fiber.memoizedProps;
        var shouldUpdate = hydrateTextInstance(textInstance, textContent, fiber);
        if (shouldUpdate) {
            // We assume that prepareToHydrateHostTextInstance is called in a context where the
            // hydration parent is the parent host component of this host text.
            var returnFiber = hydrationParentFiber;
            if (returnFiber !== null) switch(returnFiber.tag){
                case HostRoot:
                    var parentContainer = returnFiber.stateNode.containerInfo;
                    var isConcurrentMode = (returnFiber.mode & ConcurrentMode) !== NoMode;
                    didNotMatchHydratedContainerTextInstance(parentContainer, textInstance, textContent, isConcurrentMode);
                    break;
                case HostComponent:
                    var parentType = returnFiber.type;
                    var parentProps = returnFiber.memoizedProps;
                    var parentInstance = returnFiber.stateNode;
                    var _isConcurrentMode2 = (returnFiber.mode & ConcurrentMode) !== NoMode;
                    didNotMatchHydratedTextInstance(parentType, parentProps, parentInstance, textInstance, textContent, _isConcurrentMode2);
                    break;
            }
        }
        return shouldUpdate;
    }
    function prepareToHydrateHostSuspenseInstance(fiber) {
        var suspenseState = fiber.memoizedState;
        var suspenseInstance = suspenseState !== null ? suspenseState.dehydrated : null;
        if (!suspenseInstance) throw new Error("Expected to have a hydrated suspense instance. This error is likely caused by a bug in React. Please file an issue.");
        hydrateSuspenseInstance(suspenseInstance, fiber);
    }
    function skipPastDehydratedSuspenseInstance(fiber) {
        var suspenseState = fiber.memoizedState;
        var suspenseInstance = suspenseState !== null ? suspenseState.dehydrated : null;
        if (!suspenseInstance) throw new Error("Expected to have a hydrated suspense instance. This error is likely caused by a bug in React. Please file an issue.");
        return getNextHydratableInstanceAfterSuspenseInstance(suspenseInstance);
    }
    function popToNextHostParent(fiber) {
        var parent = fiber.return;
        while(parent !== null && parent.tag !== HostComponent && parent.tag !== HostRoot && parent.tag !== SuspenseComponent)parent = parent.return;
        hydrationParentFiber = parent;
    }
    function popHydrationState(fiber) {
        if (fiber !== hydrationParentFiber) // We're deeper than the current hydration context, inside an inserted
        // tree.
        return false;
        if (!isHydrating1) {
            // If we're not currently hydrating but we're in a hydration context, then
            // we were an insertion and now need to pop up reenter hydration of our
            // siblings.
            popToNextHostParent(fiber);
            isHydrating1 = true;
            return false;
        } // If we have any remaining hydratable nodes, we need to delete them now.
        // We only do this deeper than head and body since they tend to have random
        // other nodes in them. We also ignore components with pure text content in
        // side of them. We also don't delete anything inside the root container.
        if (fiber.tag !== HostRoot && (fiber.tag !== HostComponent || shouldDeleteUnhydratedTailInstances(fiber.type) && !shouldSetTextContent(fiber.type, fiber.memoizedProps))) {
            var nextInstance = nextHydratableInstance;
            if (nextInstance) {
                if (shouldClientRenderOnMismatch(fiber)) {
                    warnIfUnhydratedTailNodes(fiber);
                    throwOnHydrationMismatch();
                } else while(nextInstance){
                    deleteHydratableInstance(fiber, nextInstance);
                    nextInstance = getNextHydratableSibling(nextInstance);
                }
            }
        }
        popToNextHostParent(fiber);
        if (fiber.tag === SuspenseComponent) nextHydratableInstance = skipPastDehydratedSuspenseInstance(fiber);
        else nextHydratableInstance = hydrationParentFiber ? getNextHydratableSibling(fiber.stateNode) : null;
        return true;
    }
    function hasUnhydratedTailNodes() {
        return isHydrating1 && nextHydratableInstance !== null;
    }
    function warnIfUnhydratedTailNodes(fiber) {
        var nextInstance = nextHydratableInstance;
        while(nextInstance){
            warnUnhydratedInstance(fiber, nextInstance);
            nextInstance = getNextHydratableSibling(nextInstance);
        }
    }
    function resetHydrationState() {
        hydrationParentFiber = null;
        nextHydratableInstance = null;
        isHydrating1 = false;
        didSuspendOrErrorDEV = false;
    }
    function upgradeHydrationErrorsToRecoverable() {
        if (hydrationErrors !== null) {
            // Successfully completed a forced client render. The errors that occurred
            // during the hydration attempt are now recovered. We will log them in
            // commit phase, once the entire tree has finished.
            queueRecoverableErrors(hydrationErrors);
            hydrationErrors = null;
        }
    }
    function getIsHydrating() {
        return isHydrating1;
    }
    function queueHydrationError(error) {
        if (hydrationErrors === null) hydrationErrors = [
            error
        ];
        else hydrationErrors.push(error);
    }
    var didWarnAboutMaps;
    var didWarnAboutGenerators;
    var didWarnAboutStringRefs;
    var ownerHasKeyUseWarning;
    var ownerHasFunctionTypeWarning;
    var warnForMissingKey = function(child, returnFiber) {};
    didWarnAboutMaps = false;
    didWarnAboutGenerators = false;
    didWarnAboutStringRefs = {};
    /**
   * Warn if there's no key explicitly set on dynamic arrays of children or
   * object keys are not valid. This allows us to keep track of children between
   * updates.
   */ ownerHasKeyUseWarning = {};
    ownerHasFunctionTypeWarning = {};
    warnForMissingKey = function(child, returnFiber) {
        if (child === null || typeof child !== 'object') return;
        if (!child._store || child._store.validated || child.key != null) return;
        if (typeof child._store !== 'object') throw new Error("React Component in warnForMissingKey should have a _store. This error is likely caused by a bug in React. Please file an issue.");
        child._store.validated = true;
        var componentName = getComponentNameFromFiber(returnFiber) || 'Component';
        if (ownerHasKeyUseWarning[componentName]) return;
        ownerHasKeyUseWarning[componentName] = true;
        error1('Each child in a list should have a unique "key" prop. See https://reactjs.org/link/warning-keys for more information.');
    };
    function coerceRef(returnFiber, current, element) {
        var mixedRef = element.ref;
        if (mixedRef !== null && typeof mixedRef !== 'function' && typeof mixedRef !== 'object') {
            // TODO: Clean this up once we turn on the string ref warning for
            // everyone, because the strict mode case will no longer be relevant
            if ((returnFiber.mode & StrictLegacyMode || warnAboutStringRefs) && // because these cannot be automatically converted to an arrow function
            // using a codemod. Therefore, we don't have to warn about string refs again.
            !(element._owner && element._self && element._owner.stateNode !== element._self)) {
                var componentName = getComponentNameFromFiber(returnFiber) || 'Component';
                if (!didWarnAboutStringRefs[componentName]) {
                    error1('A string ref, "%s", has been found within a strict mode tree. String refs are a source of potential bugs and should be avoided. We recommend using useRef() or createRef() instead. Learn more about using refs safely here: https://reactjs.org/link/strict-mode-string-ref', mixedRef);
                    didWarnAboutStringRefs[componentName] = true;
                }
            }
            if (element._owner) {
                var owner = element._owner;
                var inst;
                if (owner) {
                    var ownerFiber = owner;
                    if (ownerFiber.tag !== ClassComponent) throw new Error("Function components cannot have string refs. We recommend using useRef() instead. Learn more about using refs safely here: https://reactjs.org/link/strict-mode-string-ref");
                    inst = ownerFiber.stateNode;
                }
                if (!inst) throw new Error("Missing owner for string ref " + mixedRef + ". This error is likely caused by a " + 'bug in React. Please file an issue.');
                 // Assigning this to a const so Flow knows it won't change in the closure
                var resolvedInst = inst;
                checkPropStringCoercion(mixedRef, 'ref');
                var stringRef = '' + mixedRef; // Check if previous string ref matches new string ref
                if (current !== null && current.ref !== null && typeof current.ref === 'function' && current.ref._stringRef === stringRef) return current.ref;
                var ref = function(value) {
                    var refs = resolvedInst.refs;
                    if (refs === emptyRefsObject) // This is a lazy pooled frozen object, so we need to initialize.
                    refs = resolvedInst.refs = {};
                    if (value === null) delete refs[stringRef];
                    else refs[stringRef] = value;
                };
                ref._stringRef = stringRef;
                return ref;
            } else {
                if (typeof mixedRef !== 'string') throw new Error('Expected ref to be a function, a string, an object returned by React.createRef(), or null.');
                if (!element._owner) throw new Error("Element ref was specified as a string (" + mixedRef + ") but no owner was set. This could happen for one of" + ' the following reasons:\n' + '1. You may be adding a ref to a function component\n' + "2. You may be adding a ref to a component that was not created inside a component's render method\n" + '3. You have multiple copies of React loaded\n' + 'See https://reactjs.org/link/refs-must-have-owner for more information.');
            }
        }
        return mixedRef;
    }
    function throwOnInvalidObjectType(returnFiber, newChild) {
        var childString = Object.prototype.toString.call(newChild);
        throw new Error("Objects are not valid as a React child (found: " + (childString === '[object Object]' ? 'object with keys {' + Object.keys(newChild).join(', ') + '}' : childString) + "). " + 'If you meant to render a collection of children, use an array ' + 'instead.');
    }
    function warnOnFunctionType(returnFiber) {
        var componentName = getComponentNameFromFiber(returnFiber) || 'Component';
        if (ownerHasFunctionTypeWarning[componentName]) return;
        ownerHasFunctionTypeWarning[componentName] = true;
        error1("Functions are not valid as a React child. This may happen if you return a Component instead of <Component /> from render. Or maybe you meant to call this function rather than return it.");
    }
    function resolveLazy(lazyType) {
        var payload = lazyType._payload;
        var init = lazyType._init;
        return init(payload);
    } // This wrapper function exists because I expect to clone the code in each path
    // to be able to optimize each path individually by branching early. This needs
    // a compiler or we can do it manually. Helpers that don't need this branching
    // live outside of this function.
    function ChildReconciler(shouldTrackSideEffects) {
        function deleteChild(returnFiber, childToDelete) {
            if (!shouldTrackSideEffects) // Noop.
            return;
            var deletions = returnFiber.deletions;
            if (deletions === null) {
                returnFiber.deletions = [
                    childToDelete
                ];
                returnFiber.flags |= ChildDeletion;
            } else deletions.push(childToDelete);
        }
        function deleteRemainingChildren(returnFiber, currentFirstChild) {
            if (!shouldTrackSideEffects) // Noop.
            return null;
             // TODO: For the shouldClone case, this could be micro-optimized a bit by
            // assuming that after the first child we've already added everything.
            var childToDelete = currentFirstChild;
            while(childToDelete !== null){
                deleteChild(returnFiber, childToDelete);
                childToDelete = childToDelete.sibling;
            }
            return null;
        }
        function mapRemainingChildren(returnFiber, currentFirstChild) {
            // Add the remaining children to a temporary map so that we can find them by
            // keys quickly. Implicit (null) keys get added to this set with their index
            // instead.
            var existingChildren = new Map();
            var existingChild = currentFirstChild;
            while(existingChild !== null){
                if (existingChild.key !== null) existingChildren.set(existingChild.key, existingChild);
                else existingChildren.set(existingChild.index, existingChild);
                existingChild = existingChild.sibling;
            }
            return existingChildren;
        }
        function useFiber(fiber, pendingProps) {
            // We currently set sibling to null and index to 0 here because it is easy
            // to forget to do before returning it. E.g. for the single child case.
            var clone = createWorkInProgress(fiber, pendingProps);
            clone.index = 0;
            clone.sibling = null;
            return clone;
        }
        function placeChild(newFiber, lastPlacedIndex, newIndex) {
            newFiber.index = newIndex;
            if (!shouldTrackSideEffects) {
                // During hydration, the useId algorithm needs to know which fibers are
                // part of a list of children (arrays, iterators).
                newFiber.flags |= Forked;
                return lastPlacedIndex;
            }
            var current = newFiber.alternate;
            if (current !== null) {
                var oldIndex = current.index;
                if (oldIndex < lastPlacedIndex) {
                    // This is a move.
                    newFiber.flags |= Placement;
                    return lastPlacedIndex;
                } else // This item can stay in place.
                return oldIndex;
            } else {
                // This is an insertion.
                newFiber.flags |= Placement;
                return lastPlacedIndex;
            }
        }
        function placeSingleChild(newFiber) {
            // This is simpler for the single child case. We only need to do a
            // placement for inserting new children.
            if (shouldTrackSideEffects && newFiber.alternate === null) newFiber.flags |= Placement;
            return newFiber;
        }
        function updateTextNode(returnFiber, current, textContent, lanes) {
            if (current === null || current.tag !== HostText) {
                // Insert
                var created = createFiberFromText(textContent, returnFiber.mode, lanes);
                created.return = returnFiber;
                return created;
            } else {
                // Update
                var existing = useFiber(current, textContent);
                existing.return = returnFiber;
                return existing;
            }
        }
        function updateElement(returnFiber, current, element, lanes) {
            var elementType = element.type;
            if (elementType === REACT_FRAGMENT_TYPE) return updateFragment(returnFiber, current, element.props.children, lanes, element.key);
            if (current !== null) {
                if (current.elementType === elementType || isCompatibleFamilyForHotReloading(current, element) || // We need to do this after the Hot Reloading check above,
                // because hot reloading has different semantics than prod because
                // it doesn't resuspend. So we can't let the call below suspend.
                typeof elementType === 'object' && elementType !== null && elementType.$$typeof === REACT_LAZY_TYPE && resolveLazy(elementType) === current.type) {
                    // Move based on index
                    var existing = useFiber(current, element.props);
                    existing.ref = coerceRef(returnFiber, current, element);
                    existing.return = returnFiber;
                    existing._debugSource = element._source;
                    existing._debugOwner = element._owner;
                    return existing;
                }
            } // Insert
            var created = createFiberFromElement(element, returnFiber.mode, lanes);
            created.ref = coerceRef(returnFiber, current, element);
            created.return = returnFiber;
            return created;
        }
        function updatePortal(returnFiber, current, portal, lanes) {
            if (current === null || current.tag !== HostPortal || current.stateNode.containerInfo !== portal.containerInfo || current.stateNode.implementation !== portal.implementation) {
                // Insert
                var created = createFiberFromPortal(portal, returnFiber.mode, lanes);
                created.return = returnFiber;
                return created;
            } else {
                // Update
                var existing = useFiber(current, portal.children || []);
                existing.return = returnFiber;
                return existing;
            }
        }
        function updateFragment(returnFiber, current, fragment, lanes, key) {
            if (current === null || current.tag !== Fragment) {
                // Insert
                var created = createFiberFromFragment(fragment, returnFiber.mode, lanes, key);
                created.return = returnFiber;
                return created;
            } else {
                // Update
                var existing = useFiber(current, fragment);
                existing.return = returnFiber;
                return existing;
            }
        }
        function createChild(returnFiber, newChild, lanes) {
            if (typeof newChild === 'string' && newChild !== '' || typeof newChild === 'number') {
                // Text nodes don't have keys. If the previous node is implicitly keyed
                // we can continue to replace it without aborting even if it is not a text
                // node.
                var created = createFiberFromText('' + newChild, returnFiber.mode, lanes);
                created.return = returnFiber;
                return created;
            }
            if (typeof newChild === 'object' && newChild !== null) {
                switch(newChild.$$typeof){
                    case REACT_ELEMENT_TYPE:
                        var _created = createFiberFromElement(newChild, returnFiber.mode, lanes);
                        _created.ref = coerceRef(returnFiber, null, newChild);
                        _created.return = returnFiber;
                        return _created;
                    case REACT_PORTAL_TYPE:
                        var _created2 = createFiberFromPortal(newChild, returnFiber.mode, lanes);
                        _created2.return = returnFiber;
                        return _created2;
                    case REACT_LAZY_TYPE:
                        var payload = newChild._payload;
                        var init = newChild._init;
                        return createChild(returnFiber, init(payload), lanes);
                }
                if (isArray(newChild) || getIteratorFn(newChild)) {
                    var _created3 = createFiberFromFragment(newChild, returnFiber.mode, lanes, null);
                    _created3.return = returnFiber;
                    return _created3;
                }
                throwOnInvalidObjectType(returnFiber, newChild);
            }
            if (typeof newChild === 'function') warnOnFunctionType(returnFiber);
            return null;
        }
        function updateSlot(returnFiber, oldFiber, newChild, lanes) {
            // Update the fiber if the keys match, otherwise return null.
            var key = oldFiber !== null ? oldFiber.key : null;
            if (typeof newChild === 'string' && newChild !== '' || typeof newChild === 'number') {
                // Text nodes don't have keys. If the previous node is implicitly keyed
                // we can continue to replace it without aborting even if it is not a text
                // node.
                if (key !== null) return null;
                return updateTextNode(returnFiber, oldFiber, '' + newChild, lanes);
            }
            if (typeof newChild === 'object' && newChild !== null) {
                switch(newChild.$$typeof){
                    case REACT_ELEMENT_TYPE:
                        if (newChild.key === key) return updateElement(returnFiber, oldFiber, newChild, lanes);
                        else return null;
                    case REACT_PORTAL_TYPE:
                        if (newChild.key === key) return updatePortal(returnFiber, oldFiber, newChild, lanes);
                        else return null;
                    case REACT_LAZY_TYPE:
                        var payload = newChild._payload;
                        var init = newChild._init;
                        return updateSlot(returnFiber, oldFiber, init(payload), lanes);
                }
                if (isArray(newChild) || getIteratorFn(newChild)) {
                    if (key !== null) return null;
                    return updateFragment(returnFiber, oldFiber, newChild, lanes, null);
                }
                throwOnInvalidObjectType(returnFiber, newChild);
            }
            if (typeof newChild === 'function') warnOnFunctionType(returnFiber);
            return null;
        }
        function updateFromMap(existingChildren, returnFiber, newIdx, newChild, lanes) {
            if (typeof newChild === 'string' && newChild !== '' || typeof newChild === 'number') {
                // Text nodes don't have keys, so we neither have to check the old nor
                // new node for the key. If both are text nodes, they match.
                var matchedFiber = existingChildren.get(newIdx) || null;
                return updateTextNode(returnFiber, matchedFiber, '' + newChild, lanes);
            }
            if (typeof newChild === 'object' && newChild !== null) {
                switch(newChild.$$typeof){
                    case REACT_ELEMENT_TYPE:
                        var _matchedFiber = existingChildren.get(newChild.key === null ? newIdx : newChild.key) || null;
                        return updateElement(returnFiber, _matchedFiber, newChild, lanes);
                    case REACT_PORTAL_TYPE:
                        var _matchedFiber2 = existingChildren.get(newChild.key === null ? newIdx : newChild.key) || null;
                        return updatePortal(returnFiber, _matchedFiber2, newChild, lanes);
                    case REACT_LAZY_TYPE:
                        var payload = newChild._payload;
                        var init = newChild._init;
                        return updateFromMap(existingChildren, returnFiber, newIdx, init(payload), lanes);
                }
                if (isArray(newChild) || getIteratorFn(newChild)) {
                    var _matchedFiber3 = existingChildren.get(newIdx) || null;
                    return updateFragment(returnFiber, _matchedFiber3, newChild, lanes, null);
                }
                throwOnInvalidObjectType(returnFiber, newChild);
            }
            if (typeof newChild === 'function') warnOnFunctionType(returnFiber);
            return null;
        }
        /**
   * Warns if there is a duplicate or missing key
   */ function warnOnInvalidKey(child, knownKeys, returnFiber) {
            if (typeof child !== 'object' || child === null) return knownKeys;
            switch(child.$$typeof){
                case REACT_ELEMENT_TYPE:
                case REACT_PORTAL_TYPE:
                    warnForMissingKey(child, returnFiber);
                    var key = child.key;
                    if (typeof key !== 'string') break;
                    if (knownKeys === null) {
                        knownKeys = new Set();
                        knownKeys.add(key);
                        break;
                    }
                    if (!knownKeys.has(key)) {
                        knownKeys.add(key);
                        break;
                    }
                    error1("Encountered two children with the same key, `%s`. Keys should be unique so that components maintain their identity across updates. Non-unique keys may cause children to be duplicated and/or omitted \u2014 the behavior is unsupported and could change in a future version.", key);
                    break;
                case REACT_LAZY_TYPE:
                    var payload = child._payload;
                    var init = child._init;
                    warnOnInvalidKey(init(payload), knownKeys, returnFiber);
                    break;
            }
            return knownKeys;
        }
        function reconcileChildrenArray(returnFiber, currentFirstChild, newChildren, lanes) {
            // First, validate keys.
            var knownKeys = null;
            for(var i = 0; i < newChildren.length; i++){
                var child = newChildren[i];
                knownKeys = warnOnInvalidKey(child, knownKeys, returnFiber);
            }
            var resultingFirstChild = null;
            var previousNewFiber = null;
            var oldFiber = currentFirstChild;
            var lastPlacedIndex = 0;
            var newIdx = 0;
            var nextOldFiber = null;
            for(; oldFiber !== null && newIdx < newChildren.length; newIdx++){
                if (oldFiber.index > newIdx) {
                    nextOldFiber = oldFiber;
                    oldFiber = null;
                } else nextOldFiber = oldFiber.sibling;
                var newFiber = updateSlot(returnFiber, oldFiber, newChildren[newIdx], lanes);
                if (newFiber === null) {
                    // TODO: This breaks on empty slots like null children. That's
                    // unfortunate because it triggers the slow path all the time. We need
                    // a better way to communicate whether this was a miss or null,
                    // boolean, undefined, etc.
                    if (oldFiber === null) oldFiber = nextOldFiber;
                    break;
                }
                if (shouldTrackSideEffects) {
                    if (oldFiber && newFiber.alternate === null) // We matched the slot, but we didn't reuse the existing fiber, so we
                    // need to delete the existing child.
                    deleteChild(returnFiber, oldFiber);
                }
                lastPlacedIndex = placeChild(newFiber, lastPlacedIndex, newIdx);
                if (previousNewFiber === null) // TODO: Move out of the loop. This only happens for the first run.
                resultingFirstChild = newFiber;
                else // TODO: Defer siblings if we're not at the right index for this slot.
                // I.e. if we had null values before, then we want to defer this
                // for each null value. However, we also don't want to call updateSlot
                // with the previous one.
                previousNewFiber.sibling = newFiber;
                previousNewFiber = newFiber;
                oldFiber = nextOldFiber;
            }
            if (newIdx === newChildren.length) {
                // We've reached the end of the new children. We can delete the rest.
                deleteRemainingChildren(returnFiber, oldFiber);
                if (getIsHydrating()) {
                    var numberOfForks = newIdx;
                    pushTreeFork(returnFiber, numberOfForks);
                }
                return resultingFirstChild;
            }
            if (oldFiber === null) {
                // If we don't have any more existing children we can choose a fast path
                // since the rest will all be insertions.
                for(; newIdx < newChildren.length; newIdx++){
                    var _newFiber = createChild(returnFiber, newChildren[newIdx], lanes);
                    if (_newFiber === null) continue;
                    lastPlacedIndex = placeChild(_newFiber, lastPlacedIndex, newIdx);
                    if (previousNewFiber === null) // TODO: Move out of the loop. This only happens for the first run.
                    resultingFirstChild = _newFiber;
                    else previousNewFiber.sibling = _newFiber;
                    previousNewFiber = _newFiber;
                }
                if (getIsHydrating()) {
                    var _numberOfForks = newIdx;
                    pushTreeFork(returnFiber, _numberOfForks);
                }
                return resultingFirstChild;
            } // Add all children to a key map for quick lookups.
            var existingChildren = mapRemainingChildren(returnFiber, oldFiber); // Keep scanning and use the map to restore deleted items as moves.
            for(; newIdx < newChildren.length; newIdx++){
                var _newFiber2 = updateFromMap(existingChildren, returnFiber, newIdx, newChildren[newIdx], lanes);
                if (_newFiber2 !== null) {
                    if (shouldTrackSideEffects) {
                        if (_newFiber2.alternate !== null) // The new fiber is a work in progress, but if there exists a
                        // current, that means that we reused the fiber. We need to delete
                        // it from the child list so that we don't add it to the deletion
                        // list.
                        existingChildren.delete(_newFiber2.key === null ? newIdx : _newFiber2.key);
                    }
                    lastPlacedIndex = placeChild(_newFiber2, lastPlacedIndex, newIdx);
                    if (previousNewFiber === null) resultingFirstChild = _newFiber2;
                    else previousNewFiber.sibling = _newFiber2;
                    previousNewFiber = _newFiber2;
                }
            }
            if (shouldTrackSideEffects) // Any existing children that weren't consumed above were deleted. We need
            // to add them to the deletion list.
            existingChildren.forEach(function(child) {
                return deleteChild(returnFiber, child);
            });
            if (getIsHydrating()) {
                var _numberOfForks2 = newIdx;
                pushTreeFork(returnFiber, _numberOfForks2);
            }
            return resultingFirstChild;
        }
        function reconcileChildrenIterator(returnFiber, currentFirstChild, newChildrenIterable, lanes) {
            // This is the same implementation as reconcileChildrenArray(),
            // but using the iterator instead.
            var iteratorFn = getIteratorFn(newChildrenIterable);
            if (typeof iteratorFn !== 'function') throw new Error("An object is not an iterable. This error is likely caused by a bug in React. Please file an issue.");
            // We don't support rendering Generators because it's a mutation.
            // See https://github.com/facebook/react/issues/12995
            if (typeof Symbol === 'function' && newChildrenIterable[Symbol.toStringTag] === 'Generator') {
                if (!didWarnAboutGenerators) error1("Using Generators as children is unsupported and will likely yield unexpected results because enumerating a generator mutates it. You may convert it to an array with `Array.from()` or the `[...spread]` operator before rendering. Keep in mind you might need to polyfill these features for older browsers.");
                didWarnAboutGenerators = true;
            } // Warn about using Maps as children
            if (newChildrenIterable.entries === iteratorFn) {
                if (!didWarnAboutMaps) error1("Using Maps as children is not supported. Use an array of keyed ReactElements instead.");
                didWarnAboutMaps = true;
            } // First, validate keys.
            // We'll get a different iterator later for the main pass.
            var _newChildren = iteratorFn.call(newChildrenIterable);
            if (_newChildren) {
                var knownKeys = null;
                var _step = _newChildren.next();
                for(; !_step.done; _step = _newChildren.next()){
                    var child = _step.value;
                    knownKeys = warnOnInvalidKey(child, knownKeys, returnFiber);
                }
            }
            var newChildren = iteratorFn.call(newChildrenIterable);
            if (newChildren == null) throw new Error('An iterable object provided no iterator.');
            var resultingFirstChild = null;
            var previousNewFiber = null;
            var oldFiber = currentFirstChild;
            var lastPlacedIndex = 0;
            var newIdx = 0;
            var nextOldFiber = null;
            var step = newChildren.next();
            for(; oldFiber !== null && !step.done; newIdx++, step = newChildren.next()){
                if (oldFiber.index > newIdx) {
                    nextOldFiber = oldFiber;
                    oldFiber = null;
                } else nextOldFiber = oldFiber.sibling;
                var newFiber = updateSlot(returnFiber, oldFiber, step.value, lanes);
                if (newFiber === null) {
                    // TODO: This breaks on empty slots like null children. That's
                    // unfortunate because it triggers the slow path all the time. We need
                    // a better way to communicate whether this was a miss or null,
                    // boolean, undefined, etc.
                    if (oldFiber === null) oldFiber = nextOldFiber;
                    break;
                }
                if (shouldTrackSideEffects) {
                    if (oldFiber && newFiber.alternate === null) // We matched the slot, but we didn't reuse the existing fiber, so we
                    // need to delete the existing child.
                    deleteChild(returnFiber, oldFiber);
                }
                lastPlacedIndex = placeChild(newFiber, lastPlacedIndex, newIdx);
                if (previousNewFiber === null) // TODO: Move out of the loop. This only happens for the first run.
                resultingFirstChild = newFiber;
                else // TODO: Defer siblings if we're not at the right index for this slot.
                // I.e. if we had null values before, then we want to defer this
                // for each null value. However, we also don't want to call updateSlot
                // with the previous one.
                previousNewFiber.sibling = newFiber;
                previousNewFiber = newFiber;
                oldFiber = nextOldFiber;
            }
            if (step.done) {
                // We've reached the end of the new children. We can delete the rest.
                deleteRemainingChildren(returnFiber, oldFiber);
                if (getIsHydrating()) {
                    var numberOfForks = newIdx;
                    pushTreeFork(returnFiber, numberOfForks);
                }
                return resultingFirstChild;
            }
            if (oldFiber === null) {
                // If we don't have any more existing children we can choose a fast path
                // since the rest will all be insertions.
                for(; !step.done; newIdx++, step = newChildren.next()){
                    var _newFiber3 = createChild(returnFiber, step.value, lanes);
                    if (_newFiber3 === null) continue;
                    lastPlacedIndex = placeChild(_newFiber3, lastPlacedIndex, newIdx);
                    if (previousNewFiber === null) // TODO: Move out of the loop. This only happens for the first run.
                    resultingFirstChild = _newFiber3;
                    else previousNewFiber.sibling = _newFiber3;
                    previousNewFiber = _newFiber3;
                }
                if (getIsHydrating()) {
                    var _numberOfForks3 = newIdx;
                    pushTreeFork(returnFiber, _numberOfForks3);
                }
                return resultingFirstChild;
            } // Add all children to a key map for quick lookups.
            var existingChildren = mapRemainingChildren(returnFiber, oldFiber); // Keep scanning and use the map to restore deleted items as moves.
            for(; !step.done; newIdx++, step = newChildren.next()){
                var _newFiber4 = updateFromMap(existingChildren, returnFiber, newIdx, step.value, lanes);
                if (_newFiber4 !== null) {
                    if (shouldTrackSideEffects) {
                        if (_newFiber4.alternate !== null) // The new fiber is a work in progress, but if there exists a
                        // current, that means that we reused the fiber. We need to delete
                        // it from the child list so that we don't add it to the deletion
                        // list.
                        existingChildren.delete(_newFiber4.key === null ? newIdx : _newFiber4.key);
                    }
                    lastPlacedIndex = placeChild(_newFiber4, lastPlacedIndex, newIdx);
                    if (previousNewFiber === null) resultingFirstChild = _newFiber4;
                    else previousNewFiber.sibling = _newFiber4;
                    previousNewFiber = _newFiber4;
                }
            }
            if (shouldTrackSideEffects) // Any existing children that weren't consumed above were deleted. We need
            // to add them to the deletion list.
            existingChildren.forEach(function(child) {
                return deleteChild(returnFiber, child);
            });
            if (getIsHydrating()) {
                var _numberOfForks4 = newIdx;
                pushTreeFork(returnFiber, _numberOfForks4);
            }
            return resultingFirstChild;
        }
        function reconcileSingleTextNode(returnFiber, currentFirstChild, textContent, lanes) {
            // There's no need to check for keys on text nodes since we don't have a
            // way to define them.
            if (currentFirstChild !== null && currentFirstChild.tag === HostText) {
                // We already have an existing node so let's just update it and delete
                // the rest.
                deleteRemainingChildren(returnFiber, currentFirstChild.sibling);
                var existing = useFiber(currentFirstChild, textContent);
                existing.return = returnFiber;
                return existing;
            } // The existing first child is not a text node so we need to create one
            // and delete the existing ones.
            deleteRemainingChildren(returnFiber, currentFirstChild);
            var created = createFiberFromText(textContent, returnFiber.mode, lanes);
            created.return = returnFiber;
            return created;
        }
        function reconcileSingleElement(returnFiber, currentFirstChild, element, lanes) {
            var key = element.key;
            var child = currentFirstChild;
            while(child !== null){
                // TODO: If key === null and child.key === null, then this only applies to
                // the first item in the list.
                if (child.key === key) {
                    var elementType = element.type;
                    if (elementType === REACT_FRAGMENT_TYPE) {
                        if (child.tag === Fragment) {
                            deleteRemainingChildren(returnFiber, child.sibling);
                            var existing = useFiber(child, element.props.children);
                            existing.return = returnFiber;
                            existing._debugSource = element._source;
                            existing._debugOwner = element._owner;
                            return existing;
                        }
                    } else if (child.elementType === elementType || isCompatibleFamilyForHotReloading(child, element) || // We need to do this after the Hot Reloading check above,
                    // because hot reloading has different semantics than prod because
                    // it doesn't resuspend. So we can't let the call below suspend.
                    typeof elementType === 'object' && elementType !== null && elementType.$$typeof === REACT_LAZY_TYPE && resolveLazy(elementType) === child.type) {
                        deleteRemainingChildren(returnFiber, child.sibling);
                        var _existing = useFiber(child, element.props);
                        _existing.ref = coerceRef(returnFiber, child, element);
                        _existing.return = returnFiber;
                        _existing._debugSource = element._source;
                        _existing._debugOwner = element._owner;
                        return _existing;
                    }
                     // Didn't match.
                    deleteRemainingChildren(returnFiber, child);
                    break;
                } else deleteChild(returnFiber, child);
                child = child.sibling;
            }
            if (element.type === REACT_FRAGMENT_TYPE) {
                var created = createFiberFromFragment(element.props.children, returnFiber.mode, lanes, element.key);
                created.return = returnFiber;
                return created;
            } else {
                var _created4 = createFiberFromElement(element, returnFiber.mode, lanes);
                _created4.ref = coerceRef(returnFiber, currentFirstChild, element);
                _created4.return = returnFiber;
                return _created4;
            }
        }
        function reconcileSinglePortal(returnFiber, currentFirstChild, portal, lanes) {
            var key = portal.key;
            var child = currentFirstChild;
            while(child !== null){
                // TODO: If key === null and child.key === null, then this only applies to
                // the first item in the list.
                if (child.key === key) {
                    if (child.tag === HostPortal && child.stateNode.containerInfo === portal.containerInfo && child.stateNode.implementation === portal.implementation) {
                        deleteRemainingChildren(returnFiber, child.sibling);
                        var existing = useFiber(child, portal.children || []);
                        existing.return = returnFiber;
                        return existing;
                    } else {
                        deleteRemainingChildren(returnFiber, child);
                        break;
                    }
                } else deleteChild(returnFiber, child);
                child = child.sibling;
            }
            var created = createFiberFromPortal(portal, returnFiber.mode, lanes);
            created.return = returnFiber;
            return created;
        } // This API will tag the children with the side-effect of the reconciliation
        // itself. They will be added to the side-effect list as we pass through the
        // children and the parent.
        function reconcileChildFibers(returnFiber, currentFirstChild, newChild, lanes) {
            // This function is not recursive.
            // If the top level item is an array, we treat it as a set of children,
            // not as a fragment. Nested arrays on the other hand will be treated as
            // fragment nodes. Recursion happens at the normal flow.
            // Handle top level unkeyed fragments as if they were arrays.
            // This leads to an ambiguity between <>{[...]}</> and <>...</>.
            // We treat the ambiguous cases above the same.
            var isUnkeyedTopLevelFragment = typeof newChild === 'object' && newChild !== null && newChild.type === REACT_FRAGMENT_TYPE && newChild.key === null;
            if (isUnkeyedTopLevelFragment) newChild = newChild.props.children;
             // Handle object types
            if (typeof newChild === 'object' && newChild !== null) {
                switch(newChild.$$typeof){
                    case REACT_ELEMENT_TYPE:
                        return placeSingleChild(reconcileSingleElement(returnFiber, currentFirstChild, newChild, lanes));
                    case REACT_PORTAL_TYPE:
                        return placeSingleChild(reconcileSinglePortal(returnFiber, currentFirstChild, newChild, lanes));
                    case REACT_LAZY_TYPE:
                        var payload = newChild._payload;
                        var init = newChild._init; // TODO: This function is supposed to be non-recursive.
                        return reconcileChildFibers(returnFiber, currentFirstChild, init(payload), lanes);
                }
                if (isArray(newChild)) return reconcileChildrenArray(returnFiber, currentFirstChild, newChild, lanes);
                if (getIteratorFn(newChild)) return reconcileChildrenIterator(returnFiber, currentFirstChild, newChild, lanes);
                throwOnInvalidObjectType(returnFiber, newChild);
            }
            if (typeof newChild === 'string' && newChild !== '' || typeof newChild === 'number') return placeSingleChild(reconcileSingleTextNode(returnFiber, currentFirstChild, '' + newChild, lanes));
            if (typeof newChild === 'function') warnOnFunctionType(returnFiber);
            return deleteRemainingChildren(returnFiber, currentFirstChild);
        }
        return reconcileChildFibers;
    }
    var reconcileChildFibers1 = ChildReconciler(true);
    var mountChildFibers = ChildReconciler(false);
    function cloneChildFibers(current, workInProgress) {
        if (current !== null && workInProgress.child !== current.child) throw new Error('Resuming work not yet implemented.');
        if (workInProgress.child === null) return;
        var currentChild = workInProgress.child;
        var newChild = createWorkInProgress(currentChild, currentChild.pendingProps);
        workInProgress.child = newChild;
        newChild.return = workInProgress;
        while(currentChild.sibling !== null){
            currentChild = currentChild.sibling;
            newChild = newChild.sibling = createWorkInProgress(currentChild, currentChild.pendingProps);
            newChild.return = workInProgress;
        }
        newChild.sibling = null;
    } // Reset a workInProgress child set to prepare it for a second pass.
    function resetChildFibers(workInProgress, lanes) {
        var child = workInProgress.child;
        while(child !== null){
            resetWorkInProgress(child, lanes);
            child = child.sibling;
        }
    }
    var NO_CONTEXT = {};
    var contextStackCursor$1 = createCursor(NO_CONTEXT);
    var contextFiberStackCursor = createCursor(NO_CONTEXT);
    var rootInstanceStackCursor = createCursor(NO_CONTEXT);
    function requiredContext(c) {
        if (c === NO_CONTEXT) throw new Error("Expected host context to exist. This error is likely caused by a bug in React. Please file an issue.");
        return c;
    }
    function getRootHostContainer() {
        var rootInstance = requiredContext(rootInstanceStackCursor.current);
        return rootInstance;
    }
    function pushHostContainer(fiber, nextRootInstance) {
        // Push current root instance onto the stack;
        // This allows us to reset root when portals are popped.
        push(rootInstanceStackCursor, nextRootInstance, fiber); // Track the context and the Fiber that provided it.
        // This enables us to pop only Fibers that provide unique contexts.
        push(contextFiberStackCursor, fiber, fiber); // Finally, we need to push the host context to the stack.
        // However, we can't just call getRootHostContext() and push it because
        // we'd have a different number of entries on the stack depending on
        // whether getRootHostContext() throws somewhere in renderer code or not.
        // So we push an empty value first. This lets us safely unwind on errors.
        push(contextStackCursor$1, NO_CONTEXT, fiber);
        var nextRootContext = getRootHostContext(nextRootInstance); // Now that we know this function doesn't throw, replace it.
        pop(contextStackCursor$1, fiber);
        push(contextStackCursor$1, nextRootContext, fiber);
    }
    function popHostContainer(fiber) {
        pop(contextStackCursor$1, fiber);
        pop(contextFiberStackCursor, fiber);
        pop(rootInstanceStackCursor, fiber);
    }
    function getHostContext() {
        var context = requiredContext(contextStackCursor$1.current);
        return context;
    }
    function pushHostContext(fiber) {
        var rootInstance = requiredContext(rootInstanceStackCursor.current);
        var context = requiredContext(contextStackCursor$1.current);
        var nextContext = getChildHostContext(context, fiber.type); // Don't push this Fiber's context unless it's unique.
        if (context === nextContext) return;
         // Track the context and the Fiber that provided it.
        // This enables us to pop only Fibers that provide unique contexts.
        push(contextFiberStackCursor, fiber, fiber);
        push(contextStackCursor$1, nextContext, fiber);
    }
    function popHostContext(fiber) {
        // Do not pop unless this Fiber provided the current context.
        // pushHostContext() only pushes Fibers that provide unique contexts.
        if (contextFiberStackCursor.current !== fiber) return;
        pop(contextStackCursor$1, fiber);
        pop(contextFiberStackCursor, fiber);
    }
    var DefaultSuspenseContext = 0; // The Suspense Context is split into two parts. The lower bits is
    // inherited deeply down the subtree. The upper bits only affect
    // this immediate suspense boundary and gets reset each new
    // boundary or suspense list.
    var SubtreeSuspenseContextMask = 1; // Subtree Flags:
    // InvisibleParentSuspenseContext indicates that one of our parent Suspense
    // boundaries is not currently showing visible main content.
    // Either because it is already showing a fallback or is not mounted at all.
    // We can use this to determine if it is desirable to trigger a fallback at
    // the parent. If not, then we might need to trigger undesirable boundaries
    // and/or suspend the commit to avoid hiding the parent content.
    var InvisibleParentSuspenseContext = 1; // Shallow Flags:
    // ForceSuspenseFallback can be used by SuspenseList to force newly added
    // items into their fallback state during one of the render passes.
    var ForceSuspenseFallback = 2;
    var suspenseStackCursor = createCursor(DefaultSuspenseContext);
    function hasSuspenseContext(parentContext, flag) {
        return (parentContext & flag) !== 0;
    }
    function setDefaultShallowSuspenseContext(parentContext) {
        return parentContext & SubtreeSuspenseContextMask;
    }
    function setShallowSuspenseContext(parentContext, shallowContext) {
        return parentContext & SubtreeSuspenseContextMask | shallowContext;
    }
    function addSubtreeSuspenseContext(parentContext, subtreeContext) {
        return parentContext | subtreeContext;
    }
    function pushSuspenseContext(fiber, newContext) {
        push(suspenseStackCursor, newContext, fiber);
    }
    function popSuspenseContext(fiber) {
        pop(suspenseStackCursor, fiber);
    }
    function shouldCaptureSuspense(workInProgress, hasInvisibleParent) {
        // If it was the primary children that just suspended, capture and render the
        // fallback. Otherwise, don't capture and bubble to the next boundary.
        var nextState = workInProgress.memoizedState;
        if (nextState !== null) {
            if (nextState.dehydrated !== null) // A dehydrated boundary always captures.
            return true;
            return false;
        }
        var props = workInProgress.memoizedProps; // Regular boundaries always capture.
        return true;
    }
    function findFirstSuspended(row) {
        var node = row;
        while(node !== null){
            if (node.tag === SuspenseComponent) {
                var state = node.memoizedState;
                if (state !== null) {
                    var dehydrated = state.dehydrated;
                    if (dehydrated === null || isSuspenseInstancePending(dehydrated) || isSuspenseInstanceFallback(dehydrated)) return node;
                }
            } else if (node.tag === SuspenseListComponent && // keep track of whether it suspended or not.
            node.memoizedProps.revealOrder !== undefined) {
                var didSuspend = (node.flags & DidCapture) !== NoFlags;
                if (didSuspend) return node;
            } else if (node.child !== null) {
                node.child.return = node;
                node = node.child;
                continue;
            }
            if (node === row) return null;
            while(node.sibling === null){
                if (node.return === null || node.return === row) return null;
                node = node.return;
            }
            node.sibling.return = node.return;
            node = node.sibling;
        }
        return null;
    }
    var NoFlags$1 = /*   */ 0; // Represents whether effect should fire.
    var HasEffect = /* */ 1; // Represents the phase in which the effect (not the clean-up) fires.
    var Insertion = /*  */ 2;
    var Layout = /*    */ 4;
    var Passive$1 = /*   */ 8;
    // and should be reset before starting a new render.
    // This tracks which mutable sources need to be reset after a render.
    var workInProgressSources = [];
    function resetWorkInProgressVersions() {
        for(var i = 0; i < workInProgressSources.length; i++){
            var mutableSource = workInProgressSources[i];
            mutableSource._workInProgressVersionPrimary = null;
        }
        workInProgressSources.length = 0;
    }
    // This ensures that the version used for server rendering matches the one
    // that is eventually read during hydration.
    // If they don't match there's a potential tear and a full deopt render is required.
    function registerMutableSourceForHydration(root, mutableSource) {
        var getVersion = mutableSource._getVersion;
        var version = getVersion(mutableSource._source); // TODO Clear this data once all pending hydration work is finished.
        // Retaining it forever may interfere with GC.
        if (root.mutableSourceEagerHydrationData == null) root.mutableSourceEagerHydrationData = [
            mutableSource,
            version
        ];
        else root.mutableSourceEagerHydrationData.push(mutableSource, version);
    }
    var ReactCurrentDispatcher$1 = ReactSharedInternals.ReactCurrentDispatcher, ReactCurrentBatchConfig$2 = ReactSharedInternals.ReactCurrentBatchConfig;
    var didWarnAboutMismatchedHooksForComponent;
    var didWarnUncachedGetSnapshot;
    didWarnAboutMismatchedHooksForComponent = new Set();
    // These are set right before calling the component.
    var renderLanes1 = NoLanes; // The work-in-progress fiber. I've named it differently to distinguish it from
    // the work-in-progress hook.
    var currentlyRenderingFiber$1 = null; // Hooks are stored as a linked list on the fiber's memoizedState field. The
    // current hook list is the list that belongs to the current fiber. The
    // work-in-progress hook list is a new list that will be added to the
    // work-in-progress fiber.
    var currentHook1 = null;
    var workInProgressHook = null; // Whether an update was scheduled at any point during the render phase. This
    // does not get reset if we do another render pass; only when we're completely
    // finished evaluating this component. This is an optimization so we know
    // whether we need to clear render phase updates after a throw.
    var didScheduleRenderPhaseUpdate = false; // Where an update was scheduled only during the current render pass. This
    // gets reset after each attempt.
    // TODO: Maybe there's some way to consolidate this with
    // `didScheduleRenderPhaseUpdate`. Or with `numberOfReRenders`.
    var didScheduleRenderPhaseUpdateDuringThisPass = false; // Counts the number of useId hooks in this component.
    var localIdCounter = 0; // Used for ids that are generated completely client-side (i.e. not during
    // hydration). This counter is global, so client ids are not stable across
    // render attempts.
    var globalClientIdCounter = 0;
    var RE_RENDER_LIMIT = 25; // In DEV, this is the name of the currently executing primitive hook
    var currentHookNameInDev = null; // In DEV, this list ensures that hooks are called in the same order between renders.
    // The list stores the order of hooks used during the initial render (mount).
    // Subsequent renders (updates) reference this list.
    var hookTypesDev = null;
    var hookTypesUpdateIndexDev = -1; // In DEV, this tracks whether currently rendering component needs to ignore
    // the dependencies for Hooks that need them (e.g. useEffect or useMemo).
    // When true, such Hooks will always be "remounted". Only used during hot reload.
    var ignorePreviousDependencies = false;
    function mountHookTypesDev() {
        var hookName = currentHookNameInDev;
        if (hookTypesDev === null) hookTypesDev = [
            hookName
        ];
        else hookTypesDev.push(hookName);
    }
    function updateHookTypesDev() {
        var hookName = currentHookNameInDev;
        if (hookTypesDev !== null) {
            hookTypesUpdateIndexDev++;
            if (hookTypesDev[hookTypesUpdateIndexDev] !== hookName) warnOnHookMismatchInDev(hookName);
        }
    }
    function checkDepsAreArrayDev(deps) {
        if (deps !== undefined && deps !== null && !isArray(deps)) // Verify deps, but only on mount to avoid extra checks.
        // It's unlikely their type would change as usually you define them inline.
        error1("%s received a final argument that is not an array (instead, received `%s`). When specified, the final argument must be an array.", currentHookNameInDev, typeof deps);
    }
    function warnOnHookMismatchInDev(currentHookName) {
        var componentName = getComponentNameFromFiber(currentlyRenderingFiber$1);
        if (!didWarnAboutMismatchedHooksForComponent.has(componentName)) {
            didWarnAboutMismatchedHooksForComponent.add(componentName);
            if (hookTypesDev !== null) {
                var table = '';
                var secondColumnStart = 30;
                for(var i = 0; i <= hookTypesUpdateIndexDev; i++){
                    var oldHookName = hookTypesDev[i];
                    var newHookName = i === hookTypesUpdateIndexDev ? currentHookName : oldHookName;
                    var row = i + 1 + ". " + oldHookName; // Extra space so second column lines up
                    // lol @ IE not supporting String#repeat
                    while(row.length < secondColumnStart)row += ' ';
                    row += newHookName + '\n';
                    table += row;
                }
                error1("React has detected a change in the order of Hooks called by %s. This will lead to bugs and errors if not fixed. For more information, read the Rules of Hooks: https://reactjs.org/link/rules-of-hooks\n\n   Previous render            Next render\n   ------------------------------------------------------\n%s   ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^\n", componentName, table);
            }
        }
    }
    function throwInvalidHookError() {
        throw new Error("Invalid hook call. Hooks can only be called inside of the body of a function component. This could happen for one of the following reasons:\n1. You might have mismatching versions of React and the renderer (such as React DOM)\n2. You might be breaking the Rules of Hooks\n3. You might have more than one copy of React in the same app\nSee https://reactjs.org/link/invalid-hook-call for tips about how to debug and fix this problem.");
    }
    function areHookInputsEqual(nextDeps, prevDeps) {
        if (ignorePreviousDependencies) // Only true when this component is being hot reloaded.
        return false;
        if (prevDeps === null) {
            error1("%s received a final argument during this render, but not during the previous render. Even though the final argument is optional, its type cannot change between renders.", currentHookNameInDev);
            return false;
        }
        // Don't bother comparing lengths in prod because these arrays should be
        // passed inline.
        if (nextDeps.length !== prevDeps.length) error1("The final argument passed to %s changed size between renders. The order and size of this array must remain constant.\n\nPrevious: %s\nIncoming: %s", currentHookNameInDev, "[" + prevDeps.join(', ') + "]", "[" + nextDeps.join(', ') + "]");
        for(var i = 0; i < prevDeps.length && i < nextDeps.length; i++){
            if (objectIs(nextDeps[i], prevDeps[i])) continue;
            return false;
        }
        return true;
    }
    function renderWithHooks(current, workInProgress, Component, props, secondArg, nextRenderLanes) {
        renderLanes1 = nextRenderLanes;
        currentlyRenderingFiber$1 = workInProgress;
        hookTypesDev = current !== null ? current._debugHookTypes : null;
        hookTypesUpdateIndexDev = -1; // Used for hot reloading:
        ignorePreviousDependencies = current !== null && current.type !== workInProgress.type;
        workInProgress.memoizedState = null;
        workInProgress.updateQueue = null;
        workInProgress.lanes = NoLanes; // The following should have already been reset
        if (current !== null && current.memoizedState !== null) ReactCurrentDispatcher$1.current = HooksDispatcherOnUpdateInDEV;
        else if (hookTypesDev !== null) // This dispatcher handles an edge case where a component is updating,
        // but no stateful hooks have been used.
        // We want to match the production code behavior (which will use HooksDispatcherOnMount),
        // but with the extra DEV validation to ensure hooks ordering hasn't changed.
        // This dispatcher does that.
        ReactCurrentDispatcher$1.current = HooksDispatcherOnMountWithHookTypesInDEV;
        else ReactCurrentDispatcher$1.current = HooksDispatcherOnMountInDEV;
        var children = Component(props, secondArg); // Check if there was a render phase update
        if (didScheduleRenderPhaseUpdateDuringThisPass) {
            // Keep rendering in a loop for as long as render phase updates continue to
            // be scheduled. Use a counter to prevent infinite loops.
            var numberOfReRenders = 0;
            do {
                didScheduleRenderPhaseUpdateDuringThisPass = false;
                localIdCounter = 0;
                if (numberOfReRenders >= RE_RENDER_LIMIT) throw new Error("Too many re-renders. React limits the number of renders to prevent an infinite loop.");
                numberOfReRenders += 1;
                // Even when hot reloading, allow dependencies to stabilize
                // after first render to prevent infinite render phase updates.
                ignorePreviousDependencies = false;
                currentHook1 = null;
                workInProgressHook = null;
                workInProgress.updateQueue = null;
                // Also validate hook order for cascading updates.
                hookTypesUpdateIndexDev = -1;
                ReactCurrentDispatcher$1.current = HooksDispatcherOnRerenderInDEV;
                children = Component(props, secondArg);
            }while (didScheduleRenderPhaseUpdateDuringThisPass)
        } // We can assume the previous dispatcher is always this one, since we set it
        // at the beginning of the render phase and there's no re-entrance.
        ReactCurrentDispatcher$1.current = ContextOnlyDispatcher;
        workInProgress._debugHookTypes = hookTypesDev;
        // hookTypesDev could catch more cases (e.g. context) but only in DEV bundles.
        var didRenderTooFewHooks = currentHook1 !== null && currentHook1.next !== null;
        renderLanes1 = NoLanes;
        currentlyRenderingFiber$1 = null;
        currentHook1 = null;
        workInProgressHook = null;
        currentHookNameInDev = null;
        hookTypesDev = null;
        hookTypesUpdateIndexDev = -1; // Confirm that a static flag was not added or removed since the last
        // render. If this fires, it suggests that we incorrectly reset the static
        // flags in some other part of the codebase. This has happened before, for
        // example, in the SuspenseList implementation.
        if (current !== null && (current.flags & StaticMask) !== (workInProgress.flags & StaticMask) && // and creates false positives. To make this work in legacy mode, we'd
        // need to mark fibers that commit in an incomplete state, somehow. For
        // now I'll disable the warning that most of the bugs that would trigger
        // it are either exclusive to concurrent mode or exist in both.
        (current.mode & ConcurrentMode) !== NoMode) error1("Internal React error: Expected static flag was missing. Please notify the React team.");
        didScheduleRenderPhaseUpdate = false; // This is reset by checkDidRenderIdHook
        // localIdCounter = 0;
        if (didRenderTooFewHooks) throw new Error("Rendered fewer hooks than expected. This may be caused by an accidental early return statement.");
        return children;
    }
    function checkDidRenderIdHook() {
        // This should be called immediately after every renderWithHooks call.
        // Conceptually, it's part of the return value of renderWithHooks; it's only a
        // separate function to avoid using an array tuple.
        var didRenderIdHook = localIdCounter !== 0;
        localIdCounter = 0;
        return didRenderIdHook;
    }
    function bailoutHooks(current, workInProgress, lanes) {
        workInProgress.updateQueue = current.updateQueue; // TODO: Don't need to reset the flags here, because they're reset in the
        // complete phase (bubbleProperties).
        if ((workInProgress.mode & StrictEffectsMode) !== NoMode) workInProgress.flags &= ~(MountPassiveDev | MountLayoutDev | Passive | Update);
        else workInProgress.flags &= ~(Passive | Update);
        current.lanes = removeLanes(current.lanes, lanes);
    }
    function resetHooksAfterThrow() {
        // We can assume the previous dispatcher is always this one, since we set it
        // at the beginning of the render phase and there's no re-entrance.
        ReactCurrentDispatcher$1.current = ContextOnlyDispatcher;
        if (didScheduleRenderPhaseUpdate) {
            // There were render phase updates. These are only valid for this render
            // phase, which we are now aborting. Remove the updates from the queues so
            // they do not persist to the next render. Do not remove updates from hooks
            // that weren't processed.
            //
            // Only reset the updates from the queue if it has a clone. If it does
            // not have a clone, that means it wasn't processed, and the updates were
            // scheduled before we entered the render phase.
            var hook = currentlyRenderingFiber$1.memoizedState;
            while(hook !== null){
                var queue = hook.queue;
                if (queue !== null) queue.pending = null;
                hook = hook.next;
            }
            didScheduleRenderPhaseUpdate = false;
        }
        renderLanes1 = NoLanes;
        currentlyRenderingFiber$1 = null;
        currentHook1 = null;
        workInProgressHook = null;
        hookTypesDev = null;
        hookTypesUpdateIndexDev = -1;
        currentHookNameInDev = null;
        isUpdatingOpaqueValueInRenderPhase = false;
        didScheduleRenderPhaseUpdateDuringThisPass = false;
        localIdCounter = 0;
    }
    function mountWorkInProgressHook() {
        var hook = {
            memoizedState: null,
            baseState: null,
            baseQueue: null,
            queue: null,
            next: null
        };
        if (workInProgressHook === null) // This is the first hook in the list
        currentlyRenderingFiber$1.memoizedState = workInProgressHook = hook;
        else // Append to the end of the list
        workInProgressHook = workInProgressHook.next = hook;
        return workInProgressHook;
    }
    function updateWorkInProgressHook() {
        // This function is used both for updates and for re-renders triggered by a
        // render phase update. It assumes there is either a current hook we can
        // clone, or a work-in-progress hook from a previous render pass that we can
        // use as a base. When we reach the end of the base list, we must switch to
        // the dispatcher used for mounts.
        var nextCurrentHook;
        if (currentHook1 === null) {
            var current = currentlyRenderingFiber$1.alternate;
            if (current !== null) nextCurrentHook = current.memoizedState;
            else nextCurrentHook = null;
        } else nextCurrentHook = currentHook1.next;
        var nextWorkInProgressHook;
        if (workInProgressHook === null) nextWorkInProgressHook = currentlyRenderingFiber$1.memoizedState;
        else nextWorkInProgressHook = workInProgressHook.next;
        if (nextWorkInProgressHook !== null) {
            // There's already a work-in-progress. Reuse it.
            workInProgressHook = nextWorkInProgressHook;
            nextWorkInProgressHook = workInProgressHook.next;
            currentHook1 = nextCurrentHook;
        } else {
            // Clone from the current hook.
            if (nextCurrentHook === null) throw new Error('Rendered more hooks than during the previous render.');
            currentHook1 = nextCurrentHook;
            var newHook = {
                memoizedState: currentHook1.memoizedState,
                baseState: currentHook1.baseState,
                baseQueue: currentHook1.baseQueue,
                queue: currentHook1.queue,
                next: null
            };
            if (workInProgressHook === null) // This is the first hook in the list.
            currentlyRenderingFiber$1.memoizedState = workInProgressHook = newHook;
            else // Append to the end of the list.
            workInProgressHook = workInProgressHook.next = newHook;
        }
        return workInProgressHook;
    }
    function createFunctionComponentUpdateQueue() {
        return {
            lastEffect: null,
            stores: null
        };
    }
    function basicStateReducer(state, action) {
        // $FlowFixMe: Flow doesn't like mixed types
        return typeof action === 'function' ? action(state) : action;
    }
    function mountReducer(reducer, initialArg, init) {
        var hook = mountWorkInProgressHook();
        var initialState;
        if (init !== undefined) initialState = init(initialArg);
        else initialState = initialArg;
        hook.memoizedState = hook.baseState = initialState;
        var queue = {
            pending: null,
            interleaved: null,
            lanes: NoLanes,
            dispatch: null,
            lastRenderedReducer: reducer,
            lastRenderedState: initialState
        };
        hook.queue = queue;
        var dispatch = queue.dispatch = dispatchReducerAction.bind(null, currentlyRenderingFiber$1, queue);
        return [
            hook.memoizedState,
            dispatch
        ];
    }
    function updateReducer(reducer, initialArg, init) {
        var hook = updateWorkInProgressHook();
        var queue = hook.queue;
        if (queue === null) throw new Error('Should have a queue. This is likely a bug in React. Please file an issue.');
        queue.lastRenderedReducer = reducer;
        var current = currentHook1; // The last rebase update that is NOT part of the base state.
        var baseQueue = current.baseQueue; // The last pending update that hasn't been processed yet.
        var pendingQueue = queue.pending;
        if (pendingQueue !== null) {
            // We have new updates that haven't been processed yet.
            // We'll add them to the base queue.
            if (baseQueue !== null) {
                // Merge the pending queue and the base queue.
                var baseFirst = baseQueue.next;
                var pendingFirst = pendingQueue.next;
                baseQueue.next = pendingFirst;
                pendingQueue.next = baseFirst;
            }
            if (current.baseQueue !== baseQueue) // Internal invariant that should never happen, but feasibly could in
            // the future if we implement resuming, or some form of that.
            error1("Internal error: Expected work-in-progress queue to be a clone. This is a bug in React.");
            current.baseQueue = baseQueue = pendingQueue;
            queue.pending = null;
        }
        if (baseQueue !== null) {
            // We have a queue to process.
            var first = baseQueue.next;
            var newState = current.baseState;
            var newBaseState = null;
            var newBaseQueueFirst = null;
            var newBaseQueueLast = null;
            var update = first;
            do {
                var updateLane = update.lane;
                if (!isSubsetOfLanes(renderLanes1, updateLane)) {
                    // Priority is insufficient. Skip this update. If this is the first
                    // skipped update, the previous update/state is the new base
                    // update/state.
                    var clone = {
                        lane: updateLane,
                        action: update.action,
                        hasEagerState: update.hasEagerState,
                        eagerState: update.eagerState,
                        next: null
                    };
                    if (newBaseQueueLast === null) {
                        newBaseQueueFirst = newBaseQueueLast = clone;
                        newBaseState = newState;
                    } else newBaseQueueLast = newBaseQueueLast.next = clone;
                     // Update the remaining priority in the queue.
                    // TODO: Don't need to accumulate this. Instead, we can remove
                    // renderLanes from the original lanes.
                    currentlyRenderingFiber$1.lanes = mergeLanes(currentlyRenderingFiber$1.lanes, updateLane);
                    markSkippedUpdateLanes(updateLane);
                } else {
                    // This update does have sufficient priority.
                    if (newBaseQueueLast !== null) {
                        var _clone = {
                            // This update is going to be committed so we never want uncommit
                            // it. Using NoLane works because 0 is a subset of all bitmasks, so
                            // this will never be skipped by the check above.
                            lane: NoLane,
                            action: update.action,
                            hasEagerState: update.hasEagerState,
                            eagerState: update.eagerState,
                            next: null
                        };
                        newBaseQueueLast = newBaseQueueLast.next = _clone;
                    } // Process this update.
                    if (update.hasEagerState) // If this update is a state update (not a reducer) and was processed eagerly,
                    // we can use the eagerly computed state
                    newState = update.eagerState;
                    else {
                        var action = update.action;
                        newState = reducer(newState, action);
                    }
                }
                update = update.next;
            }while (update !== null && update !== first)
            if (newBaseQueueLast === null) newBaseState = newState;
            else newBaseQueueLast.next = newBaseQueueFirst;
             // Mark that the fiber performed work, but only if the new state is
            // different from the current state.
            if (!objectIs(newState, hook.memoizedState)) markWorkInProgressReceivedUpdate();
            hook.memoizedState = newState;
            hook.baseState = newBaseState;
            hook.baseQueue = newBaseQueueLast;
            queue.lastRenderedState = newState;
        } // Interleaved updates are stored on a separate queue. We aren't going to
        // process them during this render, but we do need to track which lanes
        // are remaining.
        var lastInterleaved = queue.interleaved;
        if (lastInterleaved !== null) {
            var interleaved = lastInterleaved;
            do {
                var interleavedLane = interleaved.lane;
                currentlyRenderingFiber$1.lanes = mergeLanes(currentlyRenderingFiber$1.lanes, interleavedLane);
                markSkippedUpdateLanes(interleavedLane);
                interleaved = interleaved.next;
            }while (interleaved !== lastInterleaved)
        } else if (baseQueue === null) // `queue.lanes` is used for entangling transitions. We can set it back to
        // zero once the queue is empty.
        queue.lanes = NoLanes;
        var dispatch = queue.dispatch;
        return [
            hook.memoizedState,
            dispatch
        ];
    }
    function rerenderReducer(reducer, initialArg, init) {
        var hook = updateWorkInProgressHook();
        var queue = hook.queue;
        if (queue === null) throw new Error('Should have a queue. This is likely a bug in React. Please file an issue.');
        queue.lastRenderedReducer = reducer; // This is a re-render. Apply the new render phase updates to the previous
        // work-in-progress hook.
        var dispatch = queue.dispatch;
        var lastRenderPhaseUpdate = queue.pending;
        var newState = hook.memoizedState;
        if (lastRenderPhaseUpdate !== null) {
            // The queue doesn't persist past this render pass.
            queue.pending = null;
            var firstRenderPhaseUpdate = lastRenderPhaseUpdate.next;
            var update = firstRenderPhaseUpdate;
            do {
                // Process this render phase update. We don't have to check the
                // priority because it will always be the same as the current
                // render's.
                var action = update.action;
                newState = reducer(newState, action);
                update = update.next;
            }while (update !== firstRenderPhaseUpdate) // Mark that the fiber performed work, but only if the new state is
            // different from the current state.
            if (!objectIs(newState, hook.memoizedState)) markWorkInProgressReceivedUpdate();
            hook.memoizedState = newState; // Don't persist the state accumulated from the render phase updates to
            // the base state unless the queue is empty.
            // TODO: Not sure if this is the desired semantics, but it's what we
            // do for gDSFP. I can't remember why.
            if (hook.baseQueue === null) hook.baseState = newState;
            queue.lastRenderedState = newState;
        }
        return [
            newState,
            dispatch
        ];
    }
    function mountMutableSource(source, getSnapshot, subscribe) {
        return undefined;
    }
    function updateMutableSource(source, getSnapshot, subscribe) {
        return undefined;
    }
    function mountSyncExternalStore(subscribe, getSnapshot, getServerSnapshot) {
        var fiber = currentlyRenderingFiber$1;
        var hook = mountWorkInProgressHook();
        var nextSnapshot;
        var isHydrating = getIsHydrating();
        if (isHydrating) {
            if (getServerSnapshot === undefined) throw new Error("Missing getServerSnapshot, which is required for server-rendered content. Will revert to client rendering.");
            nextSnapshot = getServerSnapshot();
            if (!didWarnUncachedGetSnapshot) {
                if (nextSnapshot !== getServerSnapshot()) {
                    error1('The result of getServerSnapshot should be cached to avoid an infinite loop');
                    didWarnUncachedGetSnapshot = true;
                }
            }
        } else {
            nextSnapshot = getSnapshot();
            if (!didWarnUncachedGetSnapshot) {
                var cachedSnapshot = getSnapshot();
                if (!objectIs(nextSnapshot, cachedSnapshot)) {
                    error1('The result of getSnapshot should be cached to avoid an infinite loop');
                    didWarnUncachedGetSnapshot = true;
                }
            }
            // Right before committing, we will walk the tree and check if any of the
            // stores were mutated.
            //
            // We won't do this if we're hydrating server-rendered content, because if
            // the content is stale, it's already visible anyway. Instead we'll patch
            // it up in a passive effect.
            var root = getWorkInProgressRoot();
            if (root === null) throw new Error('Expected a work-in-progress root. This is a bug in React. Please file an issue.');
            if (!includesBlockingLane(root, renderLanes1)) pushStoreConsistencyCheck(fiber, getSnapshot, nextSnapshot);
        } // Read the current snapshot from the store on every render. This breaks the
        // normal rules of React, and only works because store updates are
        // always synchronous.
        hook.memoizedState = nextSnapshot;
        var inst = {
            value: nextSnapshot,
            getSnapshot: getSnapshot
        };
        hook.queue = inst; // Schedule an effect to subscribe to the store.
        mountEffect(subscribeToStore.bind(null, fiber, inst, subscribe), [
            subscribe
        ]); // Schedule an effect to update the mutable instance fields. We will update
        // this whenever subscribe, getSnapshot, or value changes. Because there's no
        // clean-up function, and we track the deps correctly, we can call pushEffect
        // directly, without storing any additional state. For the same reason, we
        // don't need to set a static flag, either.
        // TODO: We can move this to the passive phase once we add a pre-commit
        // consistency check. See the next comment.
        fiber.flags |= Passive;
        pushEffect(HasEffect | Passive$1, updateStoreInstance.bind(null, fiber, inst, nextSnapshot, getSnapshot), undefined, null);
        return nextSnapshot;
    }
    function updateSyncExternalStore(subscribe, getSnapshot, getServerSnapshot) {
        var fiber = currentlyRenderingFiber$1;
        var hook = updateWorkInProgressHook(); // Read the current snapshot from the store on every render. This breaks the
        // normal rules of React, and only works because store updates are
        // always synchronous.
        var nextSnapshot = getSnapshot();
        if (!didWarnUncachedGetSnapshot) {
            var cachedSnapshot = getSnapshot();
            if (!objectIs(nextSnapshot, cachedSnapshot)) {
                error1('The result of getSnapshot should be cached to avoid an infinite loop');
                didWarnUncachedGetSnapshot = true;
            }
        }
        var prevSnapshot = hook.memoizedState;
        var snapshotChanged = !objectIs(prevSnapshot, nextSnapshot);
        if (snapshotChanged) {
            hook.memoizedState = nextSnapshot;
            markWorkInProgressReceivedUpdate();
        }
        var inst = hook.queue;
        updateEffect(subscribeToStore.bind(null, fiber, inst, subscribe), [
            subscribe
        ]); // Whenever getSnapshot or subscribe changes, we need to check in the
        // commit phase if there was an interleaved mutation. In concurrent mode
        // this can happen all the time, but even in synchronous mode, an earlier
        // effect may have mutated the store.
        if (inst.getSnapshot !== getSnapshot || snapshotChanged || // checking whether we scheduled a subscription effect above.
        workInProgressHook !== null && workInProgressHook.memoizedState.tag & HasEffect) {
            fiber.flags |= Passive;
            pushEffect(HasEffect | Passive$1, updateStoreInstance.bind(null, fiber, inst, nextSnapshot, getSnapshot), undefined, null); // Unless we're rendering a blocking lane, schedule a consistency check.
            // Right before committing, we will walk the tree and check if any of the
            // stores were mutated.
            var root = getWorkInProgressRoot();
            if (root === null) throw new Error('Expected a work-in-progress root. This is a bug in React. Please file an issue.');
            if (!includesBlockingLane(root, renderLanes1)) pushStoreConsistencyCheck(fiber, getSnapshot, nextSnapshot);
        }
        return nextSnapshot;
    }
    function pushStoreConsistencyCheck(fiber, getSnapshot, renderedSnapshot) {
        fiber.flags |= StoreConsistency;
        var check = {
            getSnapshot: getSnapshot,
            value: renderedSnapshot
        };
        var componentUpdateQueue = currentlyRenderingFiber$1.updateQueue;
        if (componentUpdateQueue === null) {
            componentUpdateQueue = createFunctionComponentUpdateQueue();
            currentlyRenderingFiber$1.updateQueue = componentUpdateQueue;
            componentUpdateQueue.stores = [
                check
            ];
        } else {
            var stores = componentUpdateQueue.stores;
            if (stores === null) componentUpdateQueue.stores = [
                check
            ];
            else stores.push(check);
        }
    }
    function updateStoreInstance(fiber, inst, nextSnapshot, getSnapshot) {
        // These are updated in the passive phase
        inst.value = nextSnapshot;
        inst.getSnapshot = getSnapshot; // Something may have been mutated in between render and commit. This could
        // have been in an event that fired before the passive effects, or it could
        // have been in a layout effect. In that case, we would have used the old
        // snapsho and getSnapshot values to bail out. We need to check one more time.
        if (checkIfSnapshotChanged(inst)) // Force a re-render.
        forceStoreRerender(fiber);
    }
    function subscribeToStore(fiber, inst, subscribe) {
        var handleStoreChange = function() {
            // The store changed. Check if the snapshot changed since the last time we
            // read from the store.
            if (checkIfSnapshotChanged(inst)) // Force a re-render.
            forceStoreRerender(fiber);
        }; // Subscribe to the store and return a clean-up function.
        return subscribe(handleStoreChange);
    }
    function checkIfSnapshotChanged(inst) {
        var latestGetSnapshot = inst.getSnapshot;
        var prevValue = inst.value;
        try {
            var nextValue = latestGetSnapshot();
            return !objectIs(prevValue, nextValue);
        } catch (error) {
            return true;
        }
    }
    function forceStoreRerender(fiber) {
        scheduleUpdateOnFiber(fiber, SyncLane, NoTimestamp);
    }
    function mountState(initialState) {
        var hook = mountWorkInProgressHook();
        if (typeof initialState === 'function') // $FlowFixMe: Flow doesn't like mixed types
        initialState = initialState();
        hook.memoizedState = hook.baseState = initialState;
        var queue = {
            pending: null,
            interleaved: null,
            lanes: NoLanes,
            dispatch: null,
            lastRenderedReducer: basicStateReducer,
            lastRenderedState: initialState
        };
        hook.queue = queue;
        var dispatch = queue.dispatch = dispatchSetState.bind(null, currentlyRenderingFiber$1, queue);
        return [
            hook.memoizedState,
            dispatch
        ];
    }
    function updateState(initialState) {
        return updateReducer(basicStateReducer);
    }
    function rerenderState(initialState) {
        return rerenderReducer(basicStateReducer);
    }
    function pushEffect(tag, create, destroy, deps) {
        var effect = {
            tag: tag,
            create: create,
            destroy: destroy,
            deps: deps,
            // Circular
            next: null
        };
        var componentUpdateQueue = currentlyRenderingFiber$1.updateQueue;
        if (componentUpdateQueue === null) {
            componentUpdateQueue = createFunctionComponentUpdateQueue();
            currentlyRenderingFiber$1.updateQueue = componentUpdateQueue;
            componentUpdateQueue.lastEffect = effect.next = effect;
        } else {
            var lastEffect = componentUpdateQueue.lastEffect;
            if (lastEffect === null) componentUpdateQueue.lastEffect = effect.next = effect;
            else {
                var firstEffect = lastEffect.next;
                lastEffect.next = effect;
                effect.next = firstEffect;
                componentUpdateQueue.lastEffect = effect;
            }
        }
        return effect;
    }
    function mountRef(initialValue) {
        var hook = mountWorkInProgressHook();
        var _ref2 = {
            current: initialValue
        };
        hook.memoizedState = _ref2;
        return _ref2;
    }
    function updateRef(initialValue) {
        var hook = updateWorkInProgressHook();
        return hook.memoizedState;
    }
    function mountEffectImpl(fiberFlags, hookFlags, create, deps) {
        var hook = mountWorkInProgressHook();
        var nextDeps = deps === undefined ? null : deps;
        currentlyRenderingFiber$1.flags |= fiberFlags;
        hook.memoizedState = pushEffect(HasEffect | hookFlags, create, undefined, nextDeps);
    }
    function updateEffectImpl(fiberFlags, hookFlags, create, deps) {
        var hook = updateWorkInProgressHook();
        var nextDeps = deps === undefined ? null : deps;
        var destroy = undefined;
        if (currentHook1 !== null) {
            var prevEffect = currentHook1.memoizedState;
            destroy = prevEffect.destroy;
            if (nextDeps !== null) {
                var prevDeps = prevEffect.deps;
                if (areHookInputsEqual(nextDeps, prevDeps)) {
                    hook.memoizedState = pushEffect(hookFlags, create, destroy, nextDeps);
                    return;
                }
            }
        }
        currentlyRenderingFiber$1.flags |= fiberFlags;
        hook.memoizedState = pushEffect(HasEffect | hookFlags, create, destroy, nextDeps);
    }
    function mountEffect(create, deps) {
        if ((currentlyRenderingFiber$1.mode & StrictEffectsMode) !== NoMode) return mountEffectImpl(MountPassiveDev | Passive | PassiveStatic, Passive$1, create, deps);
        else return mountEffectImpl(Passive | PassiveStatic, Passive$1, create, deps);
    }
    function updateEffect(create, deps) {
        return updateEffectImpl(Passive, Passive$1, create, deps);
    }
    function mountInsertionEffect(create, deps) {
        return mountEffectImpl(Update, Insertion, create, deps);
    }
    function updateInsertionEffect(create, deps) {
        return updateEffectImpl(Update, Insertion, create, deps);
    }
    function mountLayoutEffect(create, deps) {
        var fiberFlags = Update;
        fiberFlags |= LayoutStatic;
        if ((currentlyRenderingFiber$1.mode & StrictEffectsMode) !== NoMode) fiberFlags |= MountLayoutDev;
        return mountEffectImpl(fiberFlags, Layout, create, deps);
    }
    function updateLayoutEffect(create, deps) {
        return updateEffectImpl(Update, Layout, create, deps);
    }
    function imperativeHandleEffect(create, ref) {
        if (typeof ref === 'function') {
            var refCallback = ref;
            var _inst = create();
            refCallback(_inst);
            return function() {
                refCallback(null);
            };
        } else if (ref !== null && ref !== undefined) {
            var refObject = ref;
            if (!refObject.hasOwnProperty('current')) error1("Expected useImperativeHandle() first argument to either be a ref callback or React.createRef() object. Instead received: %s.", 'an object with keys {' + Object.keys(refObject).join(', ') + '}');
            var _inst2 = create();
            refObject.current = _inst2;
            return function() {
                refObject.current = null;
            };
        }
    }
    function mountImperativeHandle(ref, create, deps) {
        if (typeof create !== 'function') error1("Expected useImperativeHandle() second argument to be a function that creates a handle. Instead received: %s.", create !== null ? typeof create : 'null');
        var effectDeps = deps !== null && deps !== undefined ? deps.concat([
            ref
        ]) : null;
        var fiberFlags = Update;
        fiberFlags |= LayoutStatic;
        if ((currentlyRenderingFiber$1.mode & StrictEffectsMode) !== NoMode) fiberFlags |= MountLayoutDev;
        return mountEffectImpl(fiberFlags, Layout, imperativeHandleEffect.bind(null, create, ref), effectDeps);
    }
    function updateImperativeHandle(ref, create, deps) {
        if (typeof create !== 'function') error1("Expected useImperativeHandle() second argument to be a function that creates a handle. Instead received: %s.", create !== null ? typeof create : 'null');
        var effectDeps = deps !== null && deps !== undefined ? deps.concat([
            ref
        ]) : null;
        return updateEffectImpl(Update, Layout, imperativeHandleEffect.bind(null, create, ref), effectDeps);
    }
    function mountDebugValue(value, formatterFn) {
    // The react-debug-hooks package injects its own implementation
    // so that e.g. DevTools can display custom hook values.
    }
    var updateDebugValue = mountDebugValue;
    function mountCallback(callback, deps) {
        var hook = mountWorkInProgressHook();
        var nextDeps = deps === undefined ? null : deps;
        hook.memoizedState = [
            callback,
            nextDeps
        ];
        return callback;
    }
    function updateCallback(callback, deps) {
        var hook = updateWorkInProgressHook();
        var nextDeps = deps === undefined ? null : deps;
        var prevState = hook.memoizedState;
        if (prevState !== null) {
            if (nextDeps !== null) {
                var prevDeps = prevState[1];
                if (areHookInputsEqual(nextDeps, prevDeps)) return prevState[0];
            }
        }
        hook.memoizedState = [
            callback,
            nextDeps
        ];
        return callback;
    }
    function mountMemo(nextCreate, deps) {
        var hook = mountWorkInProgressHook();
        var nextDeps = deps === undefined ? null : deps;
        var nextValue = nextCreate();
        hook.memoizedState = [
            nextValue,
            nextDeps
        ];
        return nextValue;
    }
    function updateMemo(nextCreate, deps) {
        var hook = updateWorkInProgressHook();
        var nextDeps = deps === undefined ? null : deps;
        var prevState = hook.memoizedState;
        if (prevState !== null) // Assume these are defined. If they're not, areHookInputsEqual will warn.
        {
            if (nextDeps !== null) {
                var prevDeps = prevState[1];
                if (areHookInputsEqual(nextDeps, prevDeps)) return prevState[0];
            }
        }
        var nextValue = nextCreate();
        hook.memoizedState = [
            nextValue,
            nextDeps
        ];
        return nextValue;
    }
    function mountDeferredValue(value) {
        var hook = mountWorkInProgressHook();
        hook.memoizedState = value;
        return value;
    }
    function updateDeferredValue(value) {
        var hook = updateWorkInProgressHook();
        var resolvedCurrentHook = currentHook1;
        var prevValue = resolvedCurrentHook.memoizedState;
        return updateDeferredValueImpl(hook, prevValue, value);
    }
    function rerenderDeferredValue(value) {
        var hook = updateWorkInProgressHook();
        if (currentHook1 === null) {
            // This is a rerender during a mount.
            hook.memoizedState = value;
            return value;
        } else {
            // This is a rerender during an update.
            var prevValue = currentHook1.memoizedState;
            return updateDeferredValueImpl(hook, prevValue, value);
        }
    }
    function updateDeferredValueImpl(hook, prevValue, value) {
        var shouldDeferValue = !includesOnlyNonUrgentLanes(renderLanes1);
        if (shouldDeferValue) {
            // This is an urgent update. If the value has changed, keep using the
            // previous value and spawn a deferred render to update it later.
            if (!objectIs(value, prevValue)) {
                // Schedule a deferred render
                var deferredLane = claimNextTransitionLane();
                currentlyRenderingFiber$1.lanes = mergeLanes(currentlyRenderingFiber$1.lanes, deferredLane);
                markSkippedUpdateLanes(deferredLane); // Set this to true to indicate that the rendered value is inconsistent
                // from the latest value. The name "baseState" doesn't really match how we
                // use it because we're reusing a state hook field instead of creating a
                // new one.
                hook.baseState = true;
            } // Reuse the previous value
            return prevValue;
        } else {
            // This is not an urgent update, so we can use the latest value regardless
            // of what it is. No need to defer it.
            // However, if we're currently inside a spawned render, then we need to mark
            // this as an update to prevent the fiber from bailing out.
            //
            // `baseState` is true when the current value is different from the rendered
            // value. The name doesn't really match how we use it because we're reusing
            // a state hook field instead of creating a new one.
            if (hook.baseState) {
                // Flip this back to false.
                hook.baseState = false;
                markWorkInProgressReceivedUpdate();
            }
            hook.memoizedState = value;
            return value;
        }
    }
    function startTransition(setPending, callback, options) {
        var previousPriority = getCurrentUpdatePriority();
        setCurrentUpdatePriority(higherEventPriority(previousPriority, ContinuousEventPriority));
        setPending(true);
        var prevTransition = ReactCurrentBatchConfig$2.transition;
        ReactCurrentBatchConfig$2.transition = {};
        var currentTransition = ReactCurrentBatchConfig$2.transition;
        ReactCurrentBatchConfig$2.transition._updatedFibers = new Set();
        try {
            setPending(false);
            callback();
        } finally{
            setCurrentUpdatePriority(previousPriority);
            ReactCurrentBatchConfig$2.transition = prevTransition;
            if (prevTransition === null && currentTransition._updatedFibers) {
                var updatedFibersCount = currentTransition._updatedFibers.size;
                if (updatedFibersCount > 10) warn("Detected a large number of updates inside startTransition. If this is due to a subscription please re-write it to use React provided hooks. Otherwise concurrent mode guarantees are off the table.");
                currentTransition._updatedFibers.clear();
            }
        }
    }
    function mountTransition() {
        var _mountState = mountState(false), isPending = _mountState[0], setPending = _mountState[1]; // The `start` method never changes.
        var start = startTransition.bind(null, setPending);
        var hook = mountWorkInProgressHook();
        hook.memoizedState = start;
        return [
            isPending,
            start
        ];
    }
    function updateTransition() {
        var _updateState = updateState(), isPending = _updateState[0];
        var hook = updateWorkInProgressHook();
        var start = hook.memoizedState;
        return [
            isPending,
            start
        ];
    }
    function rerenderTransition() {
        var _rerenderState = rerenderState(), isPending = _rerenderState[0];
        var hook = updateWorkInProgressHook();
        var start = hook.memoizedState;
        return [
            isPending,
            start
        ];
    }
    var isUpdatingOpaqueValueInRenderPhase = false;
    function getIsUpdatingOpaqueValueInRenderPhaseInDEV() {
        return isUpdatingOpaqueValueInRenderPhase;
    }
    function mountId() {
        var hook = mountWorkInProgressHook();
        var root = getWorkInProgressRoot(); // TODO: In Fizz, id generation is specific to each server config. Maybe we
        // should do this in Fiber, too? Deferring this decision for now because
        // there's no other place to store the prefix except for an internal field on
        // the public createRoot object, which the fiber tree does not currently have
        // a reference to.
        var identifierPrefix = root.identifierPrefix;
        var id;
        if (getIsHydrating()) {
            var treeId = getTreeId(); // Use a captial R prefix for server-generated ids.
            id = ':' + identifierPrefix + 'R' + treeId; // Unless this is the first id at this level, append a number at the end
            // that represents the position of this useId hook among all the useId
            // hooks for this fiber.
            var localId = localIdCounter++;
            if (localId > 0) id += 'H' + localId.toString(32);
            id += ':';
        } else {
            // Use a lowercase r prefix for client-generated ids.
            var globalClientId = globalClientIdCounter++;
            id = ':' + identifierPrefix + 'r' + globalClientId.toString(32) + ':';
        }
        hook.memoizedState = id;
        return id;
    }
    function updateId() {
        var hook = updateWorkInProgressHook();
        var id = hook.memoizedState;
        return id;
    }
    function dispatchReducerAction(fiber, queue, action) {
        if (typeof arguments[3] === 'function') error1("State updates from the useState() and useReducer() Hooks don't support the second callback argument. To execute a side effect after rendering, declare it in the component body with useEffect().");
        var lane = requestUpdateLane(fiber);
        var update = {
            lane: lane,
            action: action,
            hasEagerState: false,
            eagerState: null,
            next: null
        };
        if (isRenderPhaseUpdate(fiber)) enqueueRenderPhaseUpdate(queue, update);
        else {
            enqueueUpdate$1(fiber, queue, update);
            var eventTime = requestEventTime();
            var root = scheduleUpdateOnFiber(fiber, lane, eventTime);
            if (root !== null) entangleTransitionUpdate(root, queue, lane);
        }
        markUpdateInDevTools(fiber, lane);
    }
    function dispatchSetState(fiber, queue, action) {
        if (typeof arguments[3] === 'function') error1("State updates from the useState() and useReducer() Hooks don't support the second callback argument. To execute a side effect after rendering, declare it in the component body with useEffect().");
        var lane = requestUpdateLane(fiber);
        var update = {
            lane: lane,
            action: action,
            hasEagerState: false,
            eagerState: null,
            next: null
        };
        if (isRenderPhaseUpdate(fiber)) enqueueRenderPhaseUpdate(queue, update);
        else {
            enqueueUpdate$1(fiber, queue, update);
            var alternate = fiber.alternate;
            if (fiber.lanes === NoLanes && (alternate === null || alternate.lanes === NoLanes)) {
                // The queue is currently empty, which means we can eagerly compute the
                // next state before entering the render phase. If the new state is the
                // same as the current state, we may be able to bail out entirely.
                var lastRenderedReducer = queue.lastRenderedReducer;
                if (lastRenderedReducer !== null) {
                    var prevDispatcher;
                    prevDispatcher = ReactCurrentDispatcher$1.current;
                    ReactCurrentDispatcher$1.current = InvalidNestedHooksDispatcherOnUpdateInDEV;
                    try {
                        var currentState = queue.lastRenderedState;
                        var eagerState = lastRenderedReducer(currentState, action); // Stash the eagerly computed state, and the reducer used to compute
                        // it, on the update object. If the reducer hasn't changed by the
                        // time we enter the render phase, then the eager state can be used
                        // without calling the reducer again.
                        update.hasEagerState = true;
                        update.eagerState = eagerState;
                        if (objectIs(eagerState, currentState)) // Fast path. We can bail out without scheduling React to re-render.
                        // It's still possible that we'll need to rebase this update later,
                        // if the component re-renders for a different reason and by that
                        // time the reducer has changed.
                        return;
                    } catch (error) {} finally{
                        ReactCurrentDispatcher$1.current = prevDispatcher;
                    }
                }
            }
            var eventTime = requestEventTime();
            var root = scheduleUpdateOnFiber(fiber, lane, eventTime);
            if (root !== null) entangleTransitionUpdate(root, queue, lane);
        }
        markUpdateInDevTools(fiber, lane);
    }
    function isRenderPhaseUpdate(fiber) {
        var alternate = fiber.alternate;
        return fiber === currentlyRenderingFiber$1 || alternate !== null && alternate === currentlyRenderingFiber$1;
    }
    function enqueueRenderPhaseUpdate(queue, update) {
        // This is a render phase update. Stash it in a lazily-created map of
        // queue -> linked list of updates. After this render pass, we'll restart
        // and apply the stashed updates on top of the work-in-progress hook.
        didScheduleRenderPhaseUpdateDuringThisPass = didScheduleRenderPhaseUpdate = true;
        var pending = queue.pending;
        if (pending === null) // This is the first update. Create a circular list.
        update.next = update;
        else {
            update.next = pending.next;
            pending.next = update;
        }
        queue.pending = update;
    }
    function enqueueUpdate$1(fiber, queue, update, lane) {
        if (isInterleavedUpdate(fiber)) {
            var interleaved = queue.interleaved;
            if (interleaved === null) {
                // This is the first update. Create a circular list.
                update.next = update; // At the end of the current render, this queue's interleaved updates will
                // be transferred to the pending queue.
                pushInterleavedQueue(queue);
            } else {
                update.next = interleaved.next;
                interleaved.next = update;
            }
            queue.interleaved = update;
        } else {
            var pending = queue.pending;
            if (pending === null) // This is the first update. Create a circular list.
            update.next = update;
            else {
                update.next = pending.next;
                pending.next = update;
            }
            queue.pending = update;
        }
    }
    function entangleTransitionUpdate(root, queue, lane) {
        if (isTransitionLane(lane)) {
            var queueLanes = queue.lanes; // If any entangled lanes are no longer pending on the root, then they
            // must have finished. We can remove them from the shared queue, which
            // represents a superset of the actually pending lanes. In some cases we
            // may entangle more than we need to, but that's OK. In fact it's worse if
            // we *don't* entangle when we should.
            queueLanes = intersectLanes(queueLanes, root.pendingLanes); // Entangle the new transition lane with the other transition lanes.
            var newQueueLanes = mergeLanes(queueLanes, lane);
            queue.lanes = newQueueLanes; // Even if queue.lanes already include lane, we don't know for certain if
            // the lane finished since the last time we entangled it. So we need to
            // entangle it again, just to be sure.
            markRootEntangled(root, newQueueLanes);
        }
    }
    function markUpdateInDevTools(fiber, lane, action) {
        markStateUpdateScheduled(fiber, lane);
    }
    var ContextOnlyDispatcher = {
        readContext: readContext,
        useCallback: throwInvalidHookError,
        useContext: throwInvalidHookError,
        useEffect: throwInvalidHookError,
        useImperativeHandle: throwInvalidHookError,
        useInsertionEffect: throwInvalidHookError,
        useLayoutEffect: throwInvalidHookError,
        useMemo: throwInvalidHookError,
        useReducer: throwInvalidHookError,
        useRef: throwInvalidHookError,
        useState: throwInvalidHookError,
        useDebugValue: throwInvalidHookError,
        useDeferredValue: throwInvalidHookError,
        useTransition: throwInvalidHookError,
        useMutableSource: throwInvalidHookError,
        useSyncExternalStore: throwInvalidHookError,
        useId: throwInvalidHookError,
        unstable_isNewReconciler: enableNewReconciler
    };
    var HooksDispatcherOnMountInDEV = null;
    var HooksDispatcherOnMountWithHookTypesInDEV = null;
    var HooksDispatcherOnUpdateInDEV = null;
    var HooksDispatcherOnRerenderInDEV = null;
    var InvalidNestedHooksDispatcherOnMountInDEV = null;
    var InvalidNestedHooksDispatcherOnUpdateInDEV = null;
    var InvalidNestedHooksDispatcherOnRerenderInDEV = null;
    var warnInvalidContextAccess = function() {
        error1("Context can only be read while React is rendering. In classes, you can read it in the render method or getDerivedStateFromProps. In function components, you can read it directly in the function body, but not inside Hooks like useReducer() or useMemo().");
    };
    var warnInvalidHookAccess = function() {
        error1("Do not call Hooks inside useEffect(...), useMemo(...), or other built-in Hooks. You can only call Hooks at the top level of your React function. For more information, see https://reactjs.org/link/rules-of-hooks");
    };
    HooksDispatcherOnMountInDEV = {
        readContext: function(context) {
            return readContext(context);
        },
        useCallback: function(callback, deps) {
            currentHookNameInDev = 'useCallback';
            mountHookTypesDev();
            checkDepsAreArrayDev(deps);
            return mountCallback(callback, deps);
        },
        useContext: function(context) {
            currentHookNameInDev = 'useContext';
            mountHookTypesDev();
            return readContext(context);
        },
        useEffect: function(create, deps) {
            currentHookNameInDev = 'useEffect';
            mountHookTypesDev();
            checkDepsAreArrayDev(deps);
            return mountEffect(create, deps);
        },
        useImperativeHandle: function(ref, create, deps) {
            currentHookNameInDev = 'useImperativeHandle';
            mountHookTypesDev();
            checkDepsAreArrayDev(deps);
            return mountImperativeHandle(ref, create, deps);
        },
        useInsertionEffect: function(create, deps) {
            currentHookNameInDev = 'useInsertionEffect';
            mountHookTypesDev();
            checkDepsAreArrayDev(deps);
            return mountInsertionEffect(create, deps);
        },
        useLayoutEffect: function(create, deps) {
            currentHookNameInDev = 'useLayoutEffect';
            mountHookTypesDev();
            checkDepsAreArrayDev(deps);
            return mountLayoutEffect(create, deps);
        },
        useMemo: function(create, deps) {
            currentHookNameInDev = 'useMemo';
            mountHookTypesDev();
            checkDepsAreArrayDev(deps);
            var prevDispatcher = ReactCurrentDispatcher$1.current;
            ReactCurrentDispatcher$1.current = InvalidNestedHooksDispatcherOnMountInDEV;
            try {
                return mountMemo(create, deps);
            } finally{
                ReactCurrentDispatcher$1.current = prevDispatcher;
            }
        },
        useReducer: function(reducer, initialArg, init) {
            currentHookNameInDev = 'useReducer';
            mountHookTypesDev();
            var prevDispatcher = ReactCurrentDispatcher$1.current;
            ReactCurrentDispatcher$1.current = InvalidNestedHooksDispatcherOnMountInDEV;
            try {
                return mountReducer(reducer, initialArg, init);
            } finally{
                ReactCurrentDispatcher$1.current = prevDispatcher;
            }
        },
        useRef: function(initialValue) {
            currentHookNameInDev = 'useRef';
            mountHookTypesDev();
            return mountRef(initialValue);
        },
        useState: function(initialState) {
            currentHookNameInDev = 'useState';
            mountHookTypesDev();
            var prevDispatcher = ReactCurrentDispatcher$1.current;
            ReactCurrentDispatcher$1.current = InvalidNestedHooksDispatcherOnMountInDEV;
            try {
                return mountState(initialState);
            } finally{
                ReactCurrentDispatcher$1.current = prevDispatcher;
            }
        },
        useDebugValue: function(value, formatterFn) {
            currentHookNameInDev = 'useDebugValue';
            mountHookTypesDev();
            return mountDebugValue();
        },
        useDeferredValue: function(value) {
            currentHookNameInDev = 'useDeferredValue';
            mountHookTypesDev();
            return mountDeferredValue(value);
        },
        useTransition: function() {
            currentHookNameInDev = 'useTransition';
            mountHookTypesDev();
            return mountTransition();
        },
        useMutableSource: function(source, getSnapshot, subscribe) {
            currentHookNameInDev = 'useMutableSource';
            mountHookTypesDev();
            return mountMutableSource();
        },
        useSyncExternalStore: function(subscribe, getSnapshot, getServerSnapshot) {
            currentHookNameInDev = 'useSyncExternalStore';
            mountHookTypesDev();
            return mountSyncExternalStore(subscribe, getSnapshot, getServerSnapshot);
        },
        useId: function() {
            currentHookNameInDev = 'useId';
            mountHookTypesDev();
            return mountId();
        },
        unstable_isNewReconciler: enableNewReconciler
    };
    HooksDispatcherOnMountWithHookTypesInDEV = {
        readContext: function(context) {
            return readContext(context);
        },
        useCallback: function(callback, deps) {
            currentHookNameInDev = 'useCallback';
            updateHookTypesDev();
            return mountCallback(callback, deps);
        },
        useContext: function(context) {
            currentHookNameInDev = 'useContext';
            updateHookTypesDev();
            return readContext(context);
        },
        useEffect: function(create, deps) {
            currentHookNameInDev = 'useEffect';
            updateHookTypesDev();
            return mountEffect(create, deps);
        },
        useImperativeHandle: function(ref, create, deps) {
            currentHookNameInDev = 'useImperativeHandle';
            updateHookTypesDev();
            return mountImperativeHandle(ref, create, deps);
        },
        useInsertionEffect: function(create, deps) {
            currentHookNameInDev = 'useInsertionEffect';
            updateHookTypesDev();
            return mountInsertionEffect(create, deps);
        },
        useLayoutEffect: function(create, deps) {
            currentHookNameInDev = 'useLayoutEffect';
            updateHookTypesDev();
            return mountLayoutEffect(create, deps);
        },
        useMemo: function(create, deps) {
            currentHookNameInDev = 'useMemo';
            updateHookTypesDev();
            var prevDispatcher = ReactCurrentDispatcher$1.current;
            ReactCurrentDispatcher$1.current = InvalidNestedHooksDispatcherOnMountInDEV;
            try {
                return mountMemo(create, deps);
            } finally{
                ReactCurrentDispatcher$1.current = prevDispatcher;
            }
        },
        useReducer: function(reducer, initialArg, init) {
            currentHookNameInDev = 'useReducer';
            updateHookTypesDev();
            var prevDispatcher = ReactCurrentDispatcher$1.current;
            ReactCurrentDispatcher$1.current = InvalidNestedHooksDispatcherOnMountInDEV;
            try {
                return mountReducer(reducer, initialArg, init);
            } finally{
                ReactCurrentDispatcher$1.current = prevDispatcher;
            }
        },
        useRef: function(initialValue) {
            currentHookNameInDev = 'useRef';
            updateHookTypesDev();
            return mountRef(initialValue);
        },
        useState: function(initialState) {
            currentHookNameInDev = 'useState';
            updateHookTypesDev();
            var prevDispatcher = ReactCurrentDispatcher$1.current;
            ReactCurrentDispatcher$1.current = InvalidNestedHooksDispatcherOnMountInDEV;
            try {
                return mountState(initialState);
            } finally{
                ReactCurrentDispatcher$1.current = prevDispatcher;
            }
        },
        useDebugValue: function(value, formatterFn) {
            currentHookNameInDev = 'useDebugValue';
            updateHookTypesDev();
            return mountDebugValue();
        },
        useDeferredValue: function(value) {
            currentHookNameInDev = 'useDeferredValue';
            updateHookTypesDev();
            return mountDeferredValue(value);
        },
        useTransition: function() {
            currentHookNameInDev = 'useTransition';
            updateHookTypesDev();
            return mountTransition();
        },
        useMutableSource: function(source, getSnapshot, subscribe) {
            currentHookNameInDev = 'useMutableSource';
            updateHookTypesDev();
            return mountMutableSource();
        },
        useSyncExternalStore: function(subscribe, getSnapshot, getServerSnapshot) {
            currentHookNameInDev = 'useSyncExternalStore';
            updateHookTypesDev();
            return mountSyncExternalStore(subscribe, getSnapshot, getServerSnapshot);
        },
        useId: function() {
            currentHookNameInDev = 'useId';
            updateHookTypesDev();
            return mountId();
        },
        unstable_isNewReconciler: enableNewReconciler
    };
    HooksDispatcherOnUpdateInDEV = {
        readContext: function(context) {
            return readContext(context);
        },
        useCallback: function(callback, deps) {
            currentHookNameInDev = 'useCallback';
            updateHookTypesDev();
            return updateCallback(callback, deps);
        },
        useContext: function(context) {
            currentHookNameInDev = 'useContext';
            updateHookTypesDev();
            return readContext(context);
        },
        useEffect: function(create, deps) {
            currentHookNameInDev = 'useEffect';
            updateHookTypesDev();
            return updateEffect(create, deps);
        },
        useImperativeHandle: function(ref, create, deps) {
            currentHookNameInDev = 'useImperativeHandle';
            updateHookTypesDev();
            return updateImperativeHandle(ref, create, deps);
        },
        useInsertionEffect: function(create, deps) {
            currentHookNameInDev = 'useInsertionEffect';
            updateHookTypesDev();
            return updateInsertionEffect(create, deps);
        },
        useLayoutEffect: function(create, deps) {
            currentHookNameInDev = 'useLayoutEffect';
            updateHookTypesDev();
            return updateLayoutEffect(create, deps);
        },
        useMemo: function(create, deps) {
            currentHookNameInDev = 'useMemo';
            updateHookTypesDev();
            var prevDispatcher = ReactCurrentDispatcher$1.current;
            ReactCurrentDispatcher$1.current = InvalidNestedHooksDispatcherOnUpdateInDEV;
            try {
                return updateMemo(create, deps);
            } finally{
                ReactCurrentDispatcher$1.current = prevDispatcher;
            }
        },
        useReducer: function(reducer, initialArg, init) {
            currentHookNameInDev = 'useReducer';
            updateHookTypesDev();
            var prevDispatcher = ReactCurrentDispatcher$1.current;
            ReactCurrentDispatcher$1.current = InvalidNestedHooksDispatcherOnUpdateInDEV;
            try {
                return updateReducer(reducer, initialArg, init);
            } finally{
                ReactCurrentDispatcher$1.current = prevDispatcher;
            }
        },
        useRef: function(initialValue) {
            currentHookNameInDev = 'useRef';
            updateHookTypesDev();
            return updateRef();
        },
        useState: function(initialState) {
            currentHookNameInDev = 'useState';
            updateHookTypesDev();
            var prevDispatcher = ReactCurrentDispatcher$1.current;
            ReactCurrentDispatcher$1.current = InvalidNestedHooksDispatcherOnUpdateInDEV;
            try {
                return updateState(initialState);
            } finally{
                ReactCurrentDispatcher$1.current = prevDispatcher;
            }
        },
        useDebugValue: function(value, formatterFn) {
            currentHookNameInDev = 'useDebugValue';
            updateHookTypesDev();
            return updateDebugValue();
        },
        useDeferredValue: function(value) {
            currentHookNameInDev = 'useDeferredValue';
            updateHookTypesDev();
            return updateDeferredValue(value);
        },
        useTransition: function() {
            currentHookNameInDev = 'useTransition';
            updateHookTypesDev();
            return updateTransition();
        },
        useMutableSource: function(source, getSnapshot, subscribe) {
            currentHookNameInDev = 'useMutableSource';
            updateHookTypesDev();
            return updateMutableSource();
        },
        useSyncExternalStore: function(subscribe, getSnapshot, getServerSnapshot) {
            currentHookNameInDev = 'useSyncExternalStore';
            updateHookTypesDev();
            return updateSyncExternalStore(subscribe, getSnapshot);
        },
        useId: function() {
            currentHookNameInDev = 'useId';
            updateHookTypesDev();
            return updateId();
        },
        unstable_isNewReconciler: enableNewReconciler
    };
    HooksDispatcherOnRerenderInDEV = {
        readContext: function(context) {
            return readContext(context);
        },
        useCallback: function(callback, deps) {
            currentHookNameInDev = 'useCallback';
            updateHookTypesDev();
            return updateCallback(callback, deps);
        },
        useContext: function(context) {
            currentHookNameInDev = 'useContext';
            updateHookTypesDev();
            return readContext(context);
        },
        useEffect: function(create, deps) {
            currentHookNameInDev = 'useEffect';
            updateHookTypesDev();
            return updateEffect(create, deps);
        },
        useImperativeHandle: function(ref, create, deps) {
            currentHookNameInDev = 'useImperativeHandle';
            updateHookTypesDev();
            return updateImperativeHandle(ref, create, deps);
        },
        useInsertionEffect: function(create, deps) {
            currentHookNameInDev = 'useInsertionEffect';
            updateHookTypesDev();
            return updateInsertionEffect(create, deps);
        },
        useLayoutEffect: function(create, deps) {
            currentHookNameInDev = 'useLayoutEffect';
            updateHookTypesDev();
            return updateLayoutEffect(create, deps);
        },
        useMemo: function(create, deps) {
            currentHookNameInDev = 'useMemo';
            updateHookTypesDev();
            var prevDispatcher = ReactCurrentDispatcher$1.current;
            ReactCurrentDispatcher$1.current = InvalidNestedHooksDispatcherOnRerenderInDEV;
            try {
                return updateMemo(create, deps);
            } finally{
                ReactCurrentDispatcher$1.current = prevDispatcher;
            }
        },
        useReducer: function(reducer, initialArg, init) {
            currentHookNameInDev = 'useReducer';
            updateHookTypesDev();
            var prevDispatcher = ReactCurrentDispatcher$1.current;
            ReactCurrentDispatcher$1.current = InvalidNestedHooksDispatcherOnRerenderInDEV;
            try {
                return rerenderReducer(reducer, initialArg, init);
            } finally{
                ReactCurrentDispatcher$1.current = prevDispatcher;
            }
        },
        useRef: function(initialValue) {
            currentHookNameInDev = 'useRef';
            updateHookTypesDev();
            return updateRef();
        },
        useState: function(initialState) {
            currentHookNameInDev = 'useState';
            updateHookTypesDev();
            var prevDispatcher = ReactCurrentDispatcher$1.current;
            ReactCurrentDispatcher$1.current = InvalidNestedHooksDispatcherOnRerenderInDEV;
            try {
                return rerenderState(initialState);
            } finally{
                ReactCurrentDispatcher$1.current = prevDispatcher;
            }
        },
        useDebugValue: function(value, formatterFn) {
            currentHookNameInDev = 'useDebugValue';
            updateHookTypesDev();
            return updateDebugValue();
        },
        useDeferredValue: function(value) {
            currentHookNameInDev = 'useDeferredValue';
            updateHookTypesDev();
            return rerenderDeferredValue(value);
        },
        useTransition: function() {
            currentHookNameInDev = 'useTransition';
            updateHookTypesDev();
            return rerenderTransition();
        },
        useMutableSource: function(source, getSnapshot, subscribe) {
            currentHookNameInDev = 'useMutableSource';
            updateHookTypesDev();
            return updateMutableSource();
        },
        useSyncExternalStore: function(subscribe, getSnapshot, getServerSnapshot) {
            currentHookNameInDev = 'useSyncExternalStore';
            updateHookTypesDev();
            return updateSyncExternalStore(subscribe, getSnapshot);
        },
        useId: function() {
            currentHookNameInDev = 'useId';
            updateHookTypesDev();
            return updateId();
        },
        unstable_isNewReconciler: enableNewReconciler
    };
    InvalidNestedHooksDispatcherOnMountInDEV = {
        readContext: function(context) {
            warnInvalidContextAccess();
            return readContext(context);
        },
        useCallback: function(callback, deps) {
            currentHookNameInDev = 'useCallback';
            warnInvalidHookAccess();
            mountHookTypesDev();
            return mountCallback(callback, deps);
        },
        useContext: function(context) {
            currentHookNameInDev = 'useContext';
            warnInvalidHookAccess();
            mountHookTypesDev();
            return readContext(context);
        },
        useEffect: function(create, deps) {
            currentHookNameInDev = 'useEffect';
            warnInvalidHookAccess();
            mountHookTypesDev();
            return mountEffect(create, deps);
        },
        useImperativeHandle: function(ref, create, deps) {
            currentHookNameInDev = 'useImperativeHandle';
            warnInvalidHookAccess();
            mountHookTypesDev();
            return mountImperativeHandle(ref, create, deps);
        },
        useInsertionEffect: function(create, deps) {
            currentHookNameInDev = 'useInsertionEffect';
            warnInvalidHookAccess();
            mountHookTypesDev();
            return mountInsertionEffect(create, deps);
        },
        useLayoutEffect: function(create, deps) {
            currentHookNameInDev = 'useLayoutEffect';
            warnInvalidHookAccess();
            mountHookTypesDev();
            return mountLayoutEffect(create, deps);
        },
        useMemo: function(create, deps) {
            currentHookNameInDev = 'useMemo';
            warnInvalidHookAccess();
            mountHookTypesDev();
            var prevDispatcher = ReactCurrentDispatcher$1.current;
            ReactCurrentDispatcher$1.current = InvalidNestedHooksDispatcherOnMountInDEV;
            try {
                return mountMemo(create, deps);
            } finally{
                ReactCurrentDispatcher$1.current = prevDispatcher;
            }
        },
        useReducer: function(reducer, initialArg, init) {
            currentHookNameInDev = 'useReducer';
            warnInvalidHookAccess();
            mountHookTypesDev();
            var prevDispatcher = ReactCurrentDispatcher$1.current;
            ReactCurrentDispatcher$1.current = InvalidNestedHooksDispatcherOnMountInDEV;
            try {
                return mountReducer(reducer, initialArg, init);
            } finally{
                ReactCurrentDispatcher$1.current = prevDispatcher;
            }
        },
        useRef: function(initialValue) {
            currentHookNameInDev = 'useRef';
            warnInvalidHookAccess();
            mountHookTypesDev();
            return mountRef(initialValue);
        },
        useState: function(initialState) {
            currentHookNameInDev = 'useState';
            warnInvalidHookAccess();
            mountHookTypesDev();
            var prevDispatcher = ReactCurrentDispatcher$1.current;
            ReactCurrentDispatcher$1.current = InvalidNestedHooksDispatcherOnMountInDEV;
            try {
                return mountState(initialState);
            } finally{
                ReactCurrentDispatcher$1.current = prevDispatcher;
            }
        },
        useDebugValue: function(value, formatterFn) {
            currentHookNameInDev = 'useDebugValue';
            warnInvalidHookAccess();
            mountHookTypesDev();
            return mountDebugValue();
        },
        useDeferredValue: function(value) {
            currentHookNameInDev = 'useDeferredValue';
            warnInvalidHookAccess();
            mountHookTypesDev();
            return mountDeferredValue(value);
        },
        useTransition: function() {
            currentHookNameInDev = 'useTransition';
            warnInvalidHookAccess();
            mountHookTypesDev();
            return mountTransition();
        },
        useMutableSource: function(source, getSnapshot, subscribe) {
            currentHookNameInDev = 'useMutableSource';
            warnInvalidHookAccess();
            mountHookTypesDev();
            return mountMutableSource();
        },
        useSyncExternalStore: function(subscribe, getSnapshot, getServerSnapshot) {
            currentHookNameInDev = 'useSyncExternalStore';
            warnInvalidHookAccess();
            mountHookTypesDev();
            return mountSyncExternalStore(subscribe, getSnapshot, getServerSnapshot);
        },
        useId: function() {
            currentHookNameInDev = 'useId';
            warnInvalidHookAccess();
            mountHookTypesDev();
            return mountId();
        },
        unstable_isNewReconciler: enableNewReconciler
    };
    InvalidNestedHooksDispatcherOnUpdateInDEV = {
        readContext: function(context) {
            warnInvalidContextAccess();
            return readContext(context);
        },
        useCallback: function(callback, deps) {
            currentHookNameInDev = 'useCallback';
            warnInvalidHookAccess();
            updateHookTypesDev();
            return updateCallback(callback, deps);
        },
        useContext: function(context) {
            currentHookNameInDev = 'useContext';
            warnInvalidHookAccess();
            updateHookTypesDev();
            return readContext(context);
        },
        useEffect: function(create, deps) {
            currentHookNameInDev = 'useEffect';
            warnInvalidHookAccess();
            updateHookTypesDev();
            return updateEffect(create, deps);
        },
        useImperativeHandle: function(ref, create, deps) {
            currentHookNameInDev = 'useImperativeHandle';
            warnInvalidHookAccess();
            updateHookTypesDev();
            return updateImperativeHandle(ref, create, deps);
        },
        useInsertionEffect: function(create, deps) {
            currentHookNameInDev = 'useInsertionEffect';
            warnInvalidHookAccess();
            updateHookTypesDev();
            return updateInsertionEffect(create, deps);
        },
        useLayoutEffect: function(create, deps) {
            currentHookNameInDev = 'useLayoutEffect';
            warnInvalidHookAccess();
            updateHookTypesDev();
            return updateLayoutEffect(create, deps);
        },
        useMemo: function(create, deps) {
            currentHookNameInDev = 'useMemo';
            warnInvalidHookAccess();
            updateHookTypesDev();
            var prevDispatcher = ReactCurrentDispatcher$1.current;
            ReactCurrentDispatcher$1.current = InvalidNestedHooksDispatcherOnUpdateInDEV;
            try {
                return updateMemo(create, deps);
            } finally{
                ReactCurrentDispatcher$1.current = prevDispatcher;
            }
        },
        useReducer: function(reducer, initialArg, init) {
            currentHookNameInDev = 'useReducer';
            warnInvalidHookAccess();
            updateHookTypesDev();
            var prevDispatcher = ReactCurrentDispatcher$1.current;
            ReactCurrentDispatcher$1.current = InvalidNestedHooksDispatcherOnUpdateInDEV;
            try {
                return updateReducer(reducer, initialArg, init);
            } finally{
                ReactCurrentDispatcher$1.current = prevDispatcher;
            }
        },
        useRef: function(initialValue) {
            currentHookNameInDev = 'useRef';
            warnInvalidHookAccess();
            updateHookTypesDev();
            return updateRef();
        },
        useState: function(initialState) {
            currentHookNameInDev = 'useState';
            warnInvalidHookAccess();
            updateHookTypesDev();
            var prevDispatcher = ReactCurrentDispatcher$1.current;
            ReactCurrentDispatcher$1.current = InvalidNestedHooksDispatcherOnUpdateInDEV;
            try {
                return updateState(initialState);
            } finally{
                ReactCurrentDispatcher$1.current = prevDispatcher;
            }
        },
        useDebugValue: function(value, formatterFn) {
            currentHookNameInDev = 'useDebugValue';
            warnInvalidHookAccess();
            updateHookTypesDev();
            return updateDebugValue();
        },
        useDeferredValue: function(value) {
            currentHookNameInDev = 'useDeferredValue';
            warnInvalidHookAccess();
            updateHookTypesDev();
            return updateDeferredValue(value);
        },
        useTransition: function() {
            currentHookNameInDev = 'useTransition';
            warnInvalidHookAccess();
            updateHookTypesDev();
            return updateTransition();
        },
        useMutableSource: function(source, getSnapshot, subscribe) {
            currentHookNameInDev = 'useMutableSource';
            warnInvalidHookAccess();
            updateHookTypesDev();
            return updateMutableSource();
        },
        useSyncExternalStore: function(subscribe, getSnapshot, getServerSnapshot) {
            currentHookNameInDev = 'useSyncExternalStore';
            warnInvalidHookAccess();
            updateHookTypesDev();
            return updateSyncExternalStore(subscribe, getSnapshot);
        },
        useId: function() {
            currentHookNameInDev = 'useId';
            warnInvalidHookAccess();
            updateHookTypesDev();
            return updateId();
        },
        unstable_isNewReconciler: enableNewReconciler
    };
    InvalidNestedHooksDispatcherOnRerenderInDEV = {
        readContext: function(context) {
            warnInvalidContextAccess();
            return readContext(context);
        },
        useCallback: function(callback, deps) {
            currentHookNameInDev = 'useCallback';
            warnInvalidHookAccess();
            updateHookTypesDev();
            return updateCallback(callback, deps);
        },
        useContext: function(context) {
            currentHookNameInDev = 'useContext';
            warnInvalidHookAccess();
            updateHookTypesDev();
            return readContext(context);
        },
        useEffect: function(create, deps) {
            currentHookNameInDev = 'useEffect';
            warnInvalidHookAccess();
            updateHookTypesDev();
            return updateEffect(create, deps);
        },
        useImperativeHandle: function(ref, create, deps) {
            currentHookNameInDev = 'useImperativeHandle';
            warnInvalidHookAccess();
            updateHookTypesDev();
            return updateImperativeHandle(ref, create, deps);
        },
        useInsertionEffect: function(create, deps) {
            currentHookNameInDev = 'useInsertionEffect';
            warnInvalidHookAccess();
            updateHookTypesDev();
            return updateInsertionEffect(create, deps);
        },
        useLayoutEffect: function(create, deps) {
            currentHookNameInDev = 'useLayoutEffect';
            warnInvalidHookAccess();
            updateHookTypesDev();
            return updateLayoutEffect(create, deps);
        },
        useMemo: function(create, deps) {
            currentHookNameInDev = 'useMemo';
            warnInvalidHookAccess();
            updateHookTypesDev();
            var prevDispatcher = ReactCurrentDispatcher$1.current;
            ReactCurrentDispatcher$1.current = InvalidNestedHooksDispatcherOnUpdateInDEV;
            try {
                return updateMemo(create, deps);
            } finally{
                ReactCurrentDispatcher$1.current = prevDispatcher;
            }
        },
        useReducer: function(reducer, initialArg, init) {
            currentHookNameInDev = 'useReducer';
            warnInvalidHookAccess();
            updateHookTypesDev();
            var prevDispatcher = ReactCurrentDispatcher$1.current;
            ReactCurrentDispatcher$1.current = InvalidNestedHooksDispatcherOnUpdateInDEV;
            try {
                return rerenderReducer(reducer, initialArg, init);
            } finally{
                ReactCurrentDispatcher$1.current = prevDispatcher;
            }
        },
        useRef: function(initialValue) {
            currentHookNameInDev = 'useRef';
            warnInvalidHookAccess();
            updateHookTypesDev();
            return updateRef();
        },
        useState: function(initialState) {
            currentHookNameInDev = 'useState';
            warnInvalidHookAccess();
            updateHookTypesDev();
            var prevDispatcher = ReactCurrentDispatcher$1.current;
            ReactCurrentDispatcher$1.current = InvalidNestedHooksDispatcherOnUpdateInDEV;
            try {
                return rerenderState(initialState);
            } finally{
                ReactCurrentDispatcher$1.current = prevDispatcher;
            }
        },
        useDebugValue: function(value, formatterFn) {
            currentHookNameInDev = 'useDebugValue';
            warnInvalidHookAccess();
            updateHookTypesDev();
            return updateDebugValue();
        },
        useDeferredValue: function(value) {
            currentHookNameInDev = 'useDeferredValue';
            warnInvalidHookAccess();
            updateHookTypesDev();
            return rerenderDeferredValue(value);
        },
        useTransition: function() {
            currentHookNameInDev = 'useTransition';
            warnInvalidHookAccess();
            updateHookTypesDev();
            return rerenderTransition();
        },
        useMutableSource: function(source, getSnapshot, subscribe) {
            currentHookNameInDev = 'useMutableSource';
            warnInvalidHookAccess();
            updateHookTypesDev();
            return updateMutableSource();
        },
        useSyncExternalStore: function(subscribe, getSnapshot, getServerSnapshot) {
            currentHookNameInDev = 'useSyncExternalStore';
            warnInvalidHookAccess();
            updateHookTypesDev();
            return updateSyncExternalStore(subscribe, getSnapshot);
        },
        useId: function() {
            currentHookNameInDev = 'useId';
            warnInvalidHookAccess();
            updateHookTypesDev();
            return updateId();
        },
        unstable_isNewReconciler: enableNewReconciler
    };
    var now$1 = Scheduler.unstable_now;
    var commitTime1 = 0;
    var layoutEffectStartTime = -1;
    var profilerStartTime = -1;
    var passiveEffectStartTime = -1;
    /**
 * Tracks whether the current update was a nested/cascading update (scheduled from a layout effect).
 *
 * The overall sequence is:
 *   1. render
 *   2. commit (and call `onRender`, `onCommit`)
 *   3. check for nested updates
 *   4. flush passive effects (and call `onPostCommit`)
 *
 * Nested updates are identified in step 3 above,
 * but step 4 still applies to the work that was just committed.
 * We use two flags to track nested updates then:
 * one tracks whether the upcoming update is a nested update,
 * and the other tracks whether the current update was a nested update.
 * The first value gets synced to the second at the start of the render phase.
 */ var currentUpdateIsNested = false;
    var nestedUpdateScheduled = false;
    function isCurrentUpdateNested() {
        return currentUpdateIsNested;
    }
    function markNestedUpdateScheduled() {
        nestedUpdateScheduled = true;
    }
    function resetNestedUpdateFlag() {
        currentUpdateIsNested = false;
        nestedUpdateScheduled = false;
    }
    function syncNestedUpdateFlag() {
        currentUpdateIsNested = nestedUpdateScheduled;
        nestedUpdateScheduled = false;
    }
    function getCommitTime() {
        return commitTime1;
    }
    function recordCommitTime() {
        commitTime1 = now$1();
    }
    function startProfilerTimer(fiber) {
        profilerStartTime = now$1();
        if (fiber.actualStartTime < 0) fiber.actualStartTime = now$1();
    }
    function stopProfilerTimerIfRunning(fiber) {
        profilerStartTime = -1;
    }
    function stopProfilerTimerIfRunningAndRecordDelta(fiber, overrideBaseTime) {
        if (profilerStartTime >= 0) {
            var elapsedTime = now$1() - profilerStartTime;
            fiber.actualDuration += elapsedTime;
            if (overrideBaseTime) fiber.selfBaseDuration = elapsedTime;
            profilerStartTime = -1;
        }
    }
    function recordLayoutEffectDuration(fiber) {
        if (layoutEffectStartTime >= 0) {
            var elapsedTime = now$1() - layoutEffectStartTime;
            layoutEffectStartTime = -1; // Store duration on the next nearest Profiler ancestor
            // Or the root (for the DevTools Profiler to read)
            var parentFiber = fiber.return;
            while(parentFiber !== null){
                switch(parentFiber.tag){
                    case HostRoot:
                        var root = parentFiber.stateNode;
                        root.effectDuration += elapsedTime;
                        return;
                    case Profiler:
                        var parentStateNode = parentFiber.stateNode;
                        parentStateNode.effectDuration += elapsedTime;
                        return;
                }
                parentFiber = parentFiber.return;
            }
        }
    }
    function recordPassiveEffectDuration(fiber) {
        if (passiveEffectStartTime >= 0) {
            var elapsedTime = now$1() - passiveEffectStartTime;
            passiveEffectStartTime = -1; // Store duration on the next nearest Profiler ancestor
            // Or the root (for the DevTools Profiler to read)
            var parentFiber = fiber.return;
            while(parentFiber !== null){
                switch(parentFiber.tag){
                    case HostRoot:
                        var root = parentFiber.stateNode;
                        if (root !== null) root.passiveEffectDuration += elapsedTime;
                        return;
                    case Profiler:
                        var parentStateNode = parentFiber.stateNode;
                        if (parentStateNode !== null) // Detached fibers have their state node cleared out.
                        // In this case, the return pointer is also cleared out,
                        // so we won't be able to report the time spent in this Profiler's subtree.
                        parentStateNode.passiveEffectDuration += elapsedTime;
                        return;
                }
                parentFiber = parentFiber.return;
            }
        }
    }
    function startLayoutEffectTimer() {
        layoutEffectStartTime = now$1();
    }
    function startPassiveEffectTimer() {
        passiveEffectStartTime = now$1();
    }
    function transferActualDuration(fiber) {
        // Transfer time spent rendering these children so we don't lose it
        // after we rerender. This is used as a helper in special cases
        // where we should count the work of multiple passes.
        var child = fiber.child;
        while(child){
            fiber.actualDuration += child.actualDuration;
            child = child.sibling;
        }
    }
    function createCapturedValue(value, source) {
        // If the value is an error, call this function immediately after it is thrown
        // so the stack is accurate.
        return {
            value: value,
            source: source,
            stack: getStackByFiberInDevAndProd(source)
        };
    }
    // This module is forked in different environments.
    // By default, return `true` to log errors to the console.
    // Forks can return `false` if this isn't desirable.
    function showErrorDialog(boundary, errorInfo) {
        return true;
    }
    function logCapturedError(boundary, errorInfo) {
        try {
            var logError = showErrorDialog(boundary, errorInfo); // Allow injected showErrorDialog() to prevent default console.error logging.
            // This enables renderers like ReactNative to better manage redbox behavior.
            if (logError === false) return;
            var error = errorInfo.value;
            var source = errorInfo.source;
            var stack = errorInfo.stack;
            var componentStack = stack !== null ? stack : ''; // Browsers support silencing uncaught errors by calling
            // `preventDefault()` in window `error` handler.
            // We record this information as an expando on the error.
            if (error != null && error._suppressLogging) {
                if (boundary.tag === ClassComponent) // The error is recoverable and was silenced.
                // Ignore it and don't print the stack addendum.
                // This is handy for testing error boundaries without noise.
                return;
                 // The error is fatal. Since the silencing might have
                // been accidental, we'll surface it anyway.
                // However, the browser would have silenced the original error
                // so we'll print it first, and then print the stack addendum.
                console['error'](error); // Don't transform to our wrapper
            // For a more detailed description of this block, see:
            // https://github.com/facebook/react/pull/13384
            }
            var componentName = source ? getComponentNameFromFiber(source) : null;
            var componentNameMessage = componentName ? "The above error occurred in the <" + componentName + "> component:" : 'The above error occurred in one of your React components:';
            var errorBoundaryMessage;
            if (boundary.tag === HostRoot) errorBoundaryMessage = "Consider adding an error boundary to your tree to customize error handling behavior.\nVisit https://reactjs.org/link/error-boundaries to learn more about error boundaries.";
            else {
                var errorBoundaryName = getComponentNameFromFiber(boundary) || 'Anonymous';
                errorBoundaryMessage = "React will try to recreate this component tree from scratch " + ("using the error boundary you provided, " + errorBoundaryName + ".");
            }
            var combinedMessage = componentNameMessage + "\n" + componentStack + "\n\n" + ("" + errorBoundaryMessage); // In development, we provide our own message with just the component stack.
            // We don't include the original error message and JS stack because the browser
            // has already printed it. Even if the application swallows the error, it is still
            // displayed by the browser thanks to the DEV-only fake event trick in ReactErrorUtils.
            console['error'](combinedMessage); // Don't transform to our wrapper
        } catch (e) {
            // This method must not throw, or React internal state will get messed up.
            // If console.error is overridden, or logCapturedError() shows a dialog that throws,
            // we want to report this error outside of the normal stack as a last resort.
            // https://github.com/facebook/react/issues/13188
            setTimeout(function() {
                throw e;
            });
        }
    }
    var PossiblyWeakMap$1 = typeof WeakMap === 'function' ? WeakMap : Map;
    function createRootErrorUpdate(fiber, errorInfo, lane) {
        var update = createUpdate(NoTimestamp, lane); // Unmount the root by rendering null.
        update.tag = CaptureUpdate; // Caution: React DevTools currently depends on this property
        // being called "element".
        update.payload = {
            element: null
        };
        var error = errorInfo.value;
        update.callback = function() {
            onUncaughtError(error);
            logCapturedError(fiber, errorInfo);
        };
        return update;
    }
    function createClassErrorUpdate(fiber, errorInfo, lane) {
        var update = createUpdate(NoTimestamp, lane);
        update.tag = CaptureUpdate;
        var getDerivedStateFromError = fiber.type.getDerivedStateFromError;
        if (typeof getDerivedStateFromError === 'function') {
            var error$1 = errorInfo.value;
            update.payload = function() {
                return getDerivedStateFromError(error$1);
            };
            update.callback = function() {
                markFailedErrorBoundaryForHotReloading(fiber);
                logCapturedError(fiber, errorInfo);
            };
        }
        var inst = fiber.stateNode;
        if (inst !== null && typeof inst.componentDidCatch === 'function') update.callback = function callback() {
            markFailedErrorBoundaryForHotReloading(fiber);
            logCapturedError(fiber, errorInfo);
            if (typeof getDerivedStateFromError !== 'function') // To preserve the preexisting retry behavior of error boundaries,
            // we keep track of which ones already failed during this batch.
            // This gets reset before we yield back to the browser.
            // TODO: Warn in strict mode if getDerivedStateFromError is
            // not defined.
            markLegacyErrorBoundaryAsFailed(this);
            var error$1 = errorInfo.value;
            var stack = errorInfo.stack;
            this.componentDidCatch(error$1, {
                componentStack: stack !== null ? stack : ''
            });
            if (typeof getDerivedStateFromError !== 'function') // If componentDidCatch is the only error boundary method defined,
            // then it needs to call setState to recover from errors.
            // If no state update is scheduled then the boundary will swallow the error.
            {
                if (!includesSomeLane(fiber.lanes, SyncLane)) error1("%s: Error boundaries should implement getDerivedStateFromError(). In that method, return a state update to display an error message or fallback UI.", getComponentNameFromFiber(fiber) || 'Unknown');
            }
        };
        return update;
    }
    function attachPingListener(root, wakeable, lanes) {
        // Attach a ping listener
        //
        // The data might resolve before we have a chance to commit the fallback. Or,
        // in the case of a refresh, we'll never commit a fallback. So we need to
        // attach a listener now. When it resolves ("pings"), we can decide whether to
        // try rendering the tree again.
        //
        // Only attach a listener if one does not already exist for the lanes
        // we're currently rendering (which acts like a "thread ID" here).
        //
        // We only need to do this in concurrent mode. Legacy Suspense always
        // commits fallbacks synchronously, so there are no pings.
        var pingCache = root.pingCache;
        var threadIDs;
        if (pingCache === null) {
            pingCache = root.pingCache = new PossiblyWeakMap$1();
            threadIDs = new Set();
            pingCache.set(wakeable, threadIDs);
        } else {
            threadIDs = pingCache.get(wakeable);
            if (threadIDs === undefined) {
                threadIDs = new Set();
                pingCache.set(wakeable, threadIDs);
            }
        }
        if (!threadIDs.has(lanes)) {
            // Memoize using the thread ID to prevent redundant listeners.
            threadIDs.add(lanes);
            var ping = pingSuspendedRoot.bind(null, root, wakeable, lanes);
            if (isDevToolsPresent) // If we have pending work still, restore the original updaters
            restorePendingUpdaters(root, lanes);
            wakeable.then(ping, ping);
        }
    }
    function attachRetryListener(suspenseBoundary, root, wakeable, lanes) {
        // Retry listener
        //
        // If the fallback does commit, we need to attach a different type of
        // listener. This one schedules an update on the Suspense boundary to turn
        // the fallback state off.
        //
        // Stash the wakeable on the boundary fiber so we can access it in the
        // commit phase.
        //
        // When the wakeable resolves, we'll attempt to render the boundary
        // again ("retry").
        var wakeables = suspenseBoundary.updateQueue;
        if (wakeables === null) {
            var updateQueue = new Set();
            updateQueue.add(wakeable);
            suspenseBoundary.updateQueue = updateQueue;
        } else wakeables.add(wakeable);
    }
    function resetSuspendedComponent(sourceFiber, rootRenderLanes) {
        // A legacy mode Suspense quirk, only relevant to hook components.
        var tag = sourceFiber.tag;
        if ((sourceFiber.mode & ConcurrentMode) === NoMode && (tag === FunctionComponent || tag === ForwardRef || tag === SimpleMemoComponent)) {
            var currentSource = sourceFiber.alternate;
            if (currentSource) {
                sourceFiber.updateQueue = currentSource.updateQueue;
                sourceFiber.memoizedState = currentSource.memoizedState;
                sourceFiber.lanes = currentSource.lanes;
            } else {
                sourceFiber.updateQueue = null;
                sourceFiber.memoizedState = null;
            }
        }
    }
    function getNearestSuspenseBoundaryToCapture(returnFiber) {
        var node = returnFiber;
        do {
            if (node.tag === SuspenseComponent && shouldCaptureSuspense(node)) return node;
             // This boundary already captured during this render. Continue to the next
            // boundary.
            node = node.return;
        }while (node !== null)
        return null;
    }
    function markSuspenseBoundaryShouldCapture(suspenseBoundary, returnFiber, sourceFiber, root, rootRenderLanes) {
        // This marks a Suspense boundary so that when we're unwinding the stack,
        // it captures the suspended "exception" and does a second (fallback) pass.
        if ((suspenseBoundary.mode & ConcurrentMode) === NoMode) {
            // Legacy Mode Suspense
            //
            // If the boundary is in legacy mode, we should *not*
            // suspend the commit. Pretend as if the suspended component rendered
            // null and keep rendering. When the Suspense boundary completes,
            // we'll do a second pass to render the fallback.
            if (suspenseBoundary === returnFiber) // Special case where we suspended while reconciling the children of
            // a Suspense boundary's inner Offscreen wrapper fiber. This happens
            // when a React.lazy component is a direct child of a
            // Suspense boundary.
            //
            // Suspense boundaries are implemented as multiple fibers, but they
            // are a single conceptual unit. The legacy mode behavior where we
            // pretend the suspended fiber committed as `null` won't work,
            // because in this case the "suspended" fiber is the inner
            // Offscreen wrapper.
            //
            // Because the contents of the boundary haven't started rendering
            // yet (i.e. nothing in the tree has partially rendered) we can
            // switch to the regular, concurrent mode behavior: mark the
            // boundary with ShouldCapture and enter the unwind phase.
            suspenseBoundary.flags |= ShouldCapture;
            else {
                suspenseBoundary.flags |= DidCapture;
                sourceFiber.flags |= ForceUpdateForLegacySuspense; // We're going to commit this fiber even though it didn't complete.
                // But we shouldn't call any lifecycle methods or callbacks. Remove
                // all lifecycle effect tags.
                sourceFiber.flags &= ~(LifecycleEffectMask | Incomplete);
                if (sourceFiber.tag === ClassComponent) {
                    var currentSourceFiber = sourceFiber.alternate;
                    if (currentSourceFiber === null) // This is a new mount. Change the tag so it's not mistaken for a
                    // completed class component. For example, we should not call
                    // componentWillUnmount if it is deleted.
                    sourceFiber.tag = IncompleteClassComponent;
                    else {
                        // When we try rendering again, we should not reuse the current fiber,
                        // since it's known to be in an inconsistent state. Use a force update to
                        // prevent a bail out.
                        var update = createUpdate(NoTimestamp, SyncLane);
                        update.tag = ForceUpdate;
                        enqueueUpdate(sourceFiber, update);
                    }
                } // The source fiber did not complete. Mark it with Sync priority to
                // indicate that it still has pending work.
                sourceFiber.lanes = mergeLanes(sourceFiber.lanes, SyncLane);
            }
            return suspenseBoundary;
        } // Confirmed that the boundary is in a concurrent mode tree. Continue
        // with the normal suspend path.
        //
        // After this we'll use a set of heuristics to determine whether this
        // render pass will run to completion or restart or "suspend" the commit.
        // The actual logic for this is spread out in different places.
        //
        // This first principle is that if we're going to suspend when we complete
        // a root, then we should also restart if we get an update or ping that
        // might unsuspend it, and vice versa. The only reason to suspend is
        // because you think you might want to restart before committing. However,
        // it doesn't make sense to restart only while in the period we're suspended.
        //
        // Restarting too aggressively is also not good because it starves out any
        // intermediate loading state. So we use heuristics to determine when.
        // Suspense Heuristics
        //
        // If nothing threw a Promise or all the same fallbacks are already showing,
        // then don't suspend/restart.
        //
        // If this is an initial render of a new tree of Suspense boundaries and
        // those trigger a fallback, then don't suspend/restart. We want to ensure
        // that we can show the initial loading state as quickly as possible.
        //
        // If we hit a "Delayed" case, such as when we'd switch from content back into
        // a fallback, then we should always suspend/restart. Transitions apply
        // to this case. If none is defined, JND is used instead.
        //
        // If we're already showing a fallback and it gets "retried", allowing us to show
        // another level, but there's still an inner boundary that would show a fallback,
        // then we suspend/restart for 500ms since the last time we showed a fallback
        // anywhere in the tree. This effectively throttles progressive loading into a
        // consistent train of commits. This also gives us an opportunity to restart to
        // get to the completed state slightly earlier.
        //
        // If there's ambiguity due to batching it's resolved in preference of:
        // 1) "delayed", 2) "initial render", 3) "retry".
        //
        // We want to ensure that a "busy" state doesn't get force committed. We want to
        // ensure that new initial loading states can commit as soon as possible.
        suspenseBoundary.flags |= ShouldCapture; // TODO: I think we can remove this, since we now use `DidCapture` in
        // the begin phase to prevent an early bailout.
        suspenseBoundary.lanes = rootRenderLanes;
        return suspenseBoundary;
    }
    function throwException(root, returnFiber, sourceFiber, value, rootRenderLanes) {
        // The source fiber did not complete.
        sourceFiber.flags |= Incomplete;
        if (isDevToolsPresent) // If we have pending work still, restore the original updaters
        restorePendingUpdaters(root, rootRenderLanes);
        if (value !== null && typeof value === 'object' && typeof value.then === 'function') {
            // This is a wakeable. The component suspended.
            var wakeable = value;
            resetSuspendedComponent(sourceFiber);
            if (getIsHydrating() && sourceFiber.mode & ConcurrentMode) markDidThrowWhileHydratingDEV();
            var suspenseBoundary = getNearestSuspenseBoundaryToCapture(returnFiber);
            if (suspenseBoundary !== null) {
                suspenseBoundary.flags &= ~ForceClientRender;
                markSuspenseBoundaryShouldCapture(suspenseBoundary, returnFiber, sourceFiber, root, rootRenderLanes); // We only attach ping listeners in concurrent mode. Legacy Suspense always
                // commits fallbacks synchronously, so there are no pings.
                if (suspenseBoundary.mode & ConcurrentMode) attachPingListener(root, wakeable, rootRenderLanes);
                attachRetryListener(suspenseBoundary, root, wakeable);
                return;
            } else {
                // No boundary was found. Unless this is a sync update, this is OK.
                // We can suspend and wait for more data to arrive.
                if (!includesSyncLane(rootRenderLanes)) {
                    // This is not a sync update. Suspend. Since we're not activating a
                    // Suspense boundary, this will unwind all the way to the root without
                    // performing a second pass to render a fallback. (This is arguably how
                    // refresh transitions should work, too, since we're not going to commit
                    // the fallbacks anyway.)
                    //
                    // This case also applies to initial hydration.
                    attachPingListener(root, wakeable, rootRenderLanes);
                    renderDidSuspendDelayIfPossible();
                    return;
                } // This is a sync/discrete update. We treat this case like an error
                // because discrete renders are expected to produce a complete tree
                // synchronously to maintain consistency with external state.
                var uncaughtSuspenseError = new Error("A component suspended while responding to synchronous input. This will cause the UI to be replaced with a loading indicator. To fix, updates that suspend should be wrapped with startTransition."); // If we're outside a transition, fall through to the regular error path.
                // The error will be caught by the nearest suspense boundary.
                value = uncaughtSuspenseError;
            }
        } else // This is a regular error, not a Suspense wakeable.
        if (getIsHydrating() && sourceFiber.mode & ConcurrentMode) {
            markDidThrowWhileHydratingDEV();
            var _suspenseBoundary = getNearestSuspenseBoundaryToCapture(returnFiber); // If the error was thrown during hydration, we may be able to recover by
            // discarding the dehydrated content and switching to a client render.
            // Instead of surfacing the error, find the nearest Suspense boundary
            // and render it again without hydration.
            if (_suspenseBoundary !== null) {
                if ((_suspenseBoundary.flags & ShouldCapture) === NoFlags) // Set a flag to indicate that we should try rendering the normal
                // children again, not the fallback.
                _suspenseBoundary.flags |= ForceClientRender;
                markSuspenseBoundaryShouldCapture(_suspenseBoundary, returnFiber, sourceFiber, root, rootRenderLanes); // Even though the user may not be affected by this error, we should
                // still log it so it can be fixed.
                queueHydrationError(value);
                return;
            }
        }
         // We didn't find a boundary that could handle this type of exception. Start
        // over and traverse parent path again, this time treating the exception
        // as an error.
        renderDidError(value);
        value = createCapturedValue(value, sourceFiber);
        var workInProgress = returnFiber;
        do {
            switch(workInProgress.tag){
                case HostRoot:
                    var _errorInfo = value;
                    workInProgress.flags |= ShouldCapture;
                    var lane = pickArbitraryLane(rootRenderLanes);
                    workInProgress.lanes = mergeLanes(workInProgress.lanes, lane);
                    var update = createRootErrorUpdate(workInProgress, _errorInfo, lane);
                    enqueueCapturedUpdate(workInProgress, update);
                    return;
                case ClassComponent:
                    // Capture and retry
                    var errorInfo = value;
                    var ctor = workInProgress.type;
                    var instance = workInProgress.stateNode;
                    if ((workInProgress.flags & DidCapture) === NoFlags && (typeof ctor.getDerivedStateFromError === 'function' || instance !== null && typeof instance.componentDidCatch === 'function' && !isAlreadyFailedLegacyErrorBoundary(instance))) {
                        workInProgress.flags |= ShouldCapture;
                        var _lane = pickArbitraryLane(rootRenderLanes);
                        workInProgress.lanes = mergeLanes(workInProgress.lanes, _lane); // Schedule the error boundary to re-render using updated state
                        var _update = createClassErrorUpdate(workInProgress, errorInfo, _lane);
                        enqueueCapturedUpdate(workInProgress, _update);
                        return;
                    }
                    break;
            }
            workInProgress = workInProgress.return;
        }while (workInProgress !== null)
    }
    function getSuspendedCache() {
        return null;
    }
    function markUpdate(workInProgress) {
        // Tag the fiber with an update effect. This turns a Placement into
        // a PlacementAndUpdate.
        workInProgress.flags |= Update;
    }
    function markRef(workInProgress) {
        workInProgress.flags |= Ref;
        workInProgress.flags |= RefStatic;
    }
    var appendAllChildren;
    var updateHostContainer;
    var updateHostComponent;
    var updateHostText;
    // Mutation mode
    appendAllChildren = function(parent, workInProgress, needsVisibilityToggle, isHidden) {
        // We only have the top Fiber that was created but we need recurse down its
        // children to find all the terminal nodes.
        var node = workInProgress.child;
        while(node !== null){
            if (node.tag === HostComponent || node.tag === HostText) appendInitialChild(parent, node.stateNode);
            else if (node.tag === HostPortal) ;
            else if (node.child !== null) {
                node.child.return = node;
                node = node.child;
                continue;
            }
            if (node === workInProgress) return;
            while(node.sibling === null){
                if (node.return === null || node.return === workInProgress) return;
                node = node.return;
            }
            node.sibling.return = node.return;
            node = node.sibling;
        }
    };
    updateHostContainer = function(current, workInProgress) {};
    updateHostComponent = function(current, workInProgress, type, newProps, rootContainerInstance) {
        // If we have an alternate, that means this is an update and we need to
        // schedule a side-effect to do the updates.
        var oldProps = current.memoizedProps;
        if (oldProps === newProps) // In mutation mode, this is sufficient for a bailout because
        // we won't touch this node even if children changed.
        return;
         // If we get updated because one of our children updated, we don't
        // have newProps so we'll have to reuse them.
        // TODO: Split the update API as separate for the props vs. children.
        // Even better would be if children weren't special cased at all tho.
        var instance = workInProgress.stateNode;
        var currentHostContext = getHostContext(); // TODO: Experiencing an error where oldProps is null. Suggests a host
        // component is hitting the resume path. Figure out why. Possibly
        // related to `hidden`.
        var updatePayload = prepareUpdate(instance, type, oldProps, newProps, rootContainerInstance, currentHostContext); // TODO: Type this specific to this type of component.
        workInProgress.updateQueue = updatePayload; // If the update payload indicates that there is a change or if there
        // is a new ref we mark this as an update. All the work is done in commitWork.
        if (updatePayload) markUpdate(workInProgress);
    };
    updateHostText = function(current, workInProgress, oldText, newText) {
        // If the text differs, mark it as an update. All the work in done in commitWork.
        if (oldText !== newText) markUpdate(workInProgress);
    };
    function cutOffTailIfNeeded(renderState, hasRenderedATailFallback) {
        if (getIsHydrating()) // If we're hydrating, we should consume as many items as we can
        // so we don't leave any behind.
        return;
        switch(renderState.tailMode){
            case 'hidden':
                // Any insertions at the end of the tail list after this point
                // should be invisible. If there are already mounted boundaries
                // anything before them are not considered for collapsing.
                // Therefore we need to go through the whole tail to find if
                // there are any.
                var tailNode = renderState.tail;
                var lastTailNode = null;
                while(tailNode !== null){
                    if (tailNode.alternate !== null) lastTailNode = tailNode;
                    tailNode = tailNode.sibling;
                } // Next we're simply going to delete all insertions after the
                // last rendered item.
                if (lastTailNode === null) // All remaining items in the tail are insertions.
                renderState.tail = null;
                else // Detach the insertion after the last node that was already
                // inserted.
                lastTailNode.sibling = null;
                break;
            case 'collapsed':
                // Any insertions at the end of the tail list after this point
                // should be invisible. If there are already mounted boundaries
                // anything before them are not considered for collapsing.
                // Therefore we need to go through the whole tail to find if
                // there are any.
                var _tailNode = renderState.tail;
                var _lastTailNode = null;
                while(_tailNode !== null){
                    if (_tailNode.alternate !== null) _lastTailNode = _tailNode;
                    _tailNode = _tailNode.sibling;
                } // Next we're simply going to delete all insertions after the
                // last rendered item.
                if (_lastTailNode === null) {
                    // All remaining items in the tail are insertions.
                    if (!hasRenderedATailFallback && renderState.tail !== null) // We suspended during the head. We want to show at least one
                    // row at the tail. So we'll keep on and cut off the rest.
                    renderState.tail.sibling = null;
                    else renderState.tail = null;
                } else // Detach the insertion after the last node that was already
                // inserted.
                _lastTailNode.sibling = null;
                break;
        }
    }
    function bubbleProperties(completedWork) {
        var didBailout = completedWork.alternate !== null && completedWork.alternate.child === completedWork.child;
        var newChildLanes = NoLanes;
        var subtreeFlags = NoFlags;
        if (!didBailout) {
            // Bubble up the earliest expiration time.
            if ((completedWork.mode & ProfileMode) !== NoMode) {
                // In profiling mode, resetChildExpirationTime is also used to reset
                // profiler durations.
                var actualDuration = completedWork.actualDuration;
                var treeBaseDuration = completedWork.selfBaseDuration;
                var child = completedWork.child;
                while(child !== null){
                    newChildLanes = mergeLanes(newChildLanes, mergeLanes(child.lanes, child.childLanes));
                    subtreeFlags |= child.subtreeFlags;
                    subtreeFlags |= child.flags; // When a fiber is cloned, its actualDuration is reset to 0. This value will
                    // only be updated if work is done on the fiber (i.e. it doesn't bailout).
                    // When work is done, it should bubble to the parent's actualDuration. If
                    // the fiber has not been cloned though, (meaning no work was done), then
                    // this value will reflect the amount of time spent working on a previous
                    // render. In that case it should not bubble. We determine whether it was
                    // cloned by comparing the child pointer.
                    actualDuration += child.actualDuration;
                    treeBaseDuration += child.treeBaseDuration;
                    child = child.sibling;
                }
                completedWork.actualDuration = actualDuration;
                completedWork.treeBaseDuration = treeBaseDuration;
            } else {
                var _child = completedWork.child;
                while(_child !== null){
                    newChildLanes = mergeLanes(newChildLanes, mergeLanes(_child.lanes, _child.childLanes));
                    subtreeFlags |= _child.subtreeFlags;
                    subtreeFlags |= _child.flags; // Update the return pointer so the tree is consistent. This is a code
                    // smell because it assumes the commit phase is never concurrent with
                    // the render phase. Will address during refactor to alternate model.
                    _child.return = completedWork;
                    _child = _child.sibling;
                }
            }
            completedWork.subtreeFlags |= subtreeFlags;
        } else {
            // Bubble up the earliest expiration time.
            if ((completedWork.mode & ProfileMode) !== NoMode) {
                // In profiling mode, resetChildExpirationTime is also used to reset
                // profiler durations.
                var _treeBaseDuration = completedWork.selfBaseDuration;
                var _child2 = completedWork.child;
                while(_child2 !== null){
                    newChildLanes = mergeLanes(newChildLanes, mergeLanes(_child2.lanes, _child2.childLanes)); // "Static" flags share the lifetime of the fiber/hook they belong to,
                    // so we should bubble those up even during a bailout. All the other
                    // flags have a lifetime only of a single render + commit, so we should
                    // ignore them.
                    subtreeFlags |= _child2.subtreeFlags & StaticMask;
                    subtreeFlags |= _child2.flags & StaticMask;
                    _treeBaseDuration += _child2.treeBaseDuration;
                    _child2 = _child2.sibling;
                }
                completedWork.treeBaseDuration = _treeBaseDuration;
            } else {
                var _child3 = completedWork.child;
                while(_child3 !== null){
                    newChildLanes = mergeLanes(newChildLanes, mergeLanes(_child3.lanes, _child3.childLanes)); // "Static" flags share the lifetime of the fiber/hook they belong to,
                    // so we should bubble those up even during a bailout. All the other
                    // flags have a lifetime only of a single render + commit, so we should
                    // ignore them.
                    subtreeFlags |= _child3.subtreeFlags & StaticMask;
                    subtreeFlags |= _child3.flags & StaticMask; // Update the return pointer so the tree is consistent. This is a code
                    // smell because it assumes the commit phase is never concurrent with
                    // the render phase. Will address during refactor to alternate model.
                    _child3.return = completedWork;
                    _child3 = _child3.sibling;
                }
            }
            completedWork.subtreeFlags |= subtreeFlags;
        }
        completedWork.childLanes = newChildLanes;
        return didBailout;
    }
    function completeWork(current, workInProgress, renderLanes) {
        var newProps = workInProgress.pendingProps; // Note: This intentionally doesn't check if we're hydrating because comparing
        // to the current tree provider fiber is just as fast and less error-prone.
        // Ideally we would have a special version of the work loop only
        // for hydration.
        popTreeContext(workInProgress);
        switch(workInProgress.tag){
            case IndeterminateComponent:
            case LazyComponent:
            case SimpleMemoComponent:
            case FunctionComponent:
            case ForwardRef:
            case Fragment:
            case Mode:
            case Profiler:
            case ContextConsumer:
            case MemoComponent:
                bubbleProperties(workInProgress);
                return null;
            case ClassComponent:
                var Component = workInProgress.type;
                if (isContextProvider(Component)) popContext(workInProgress);
                bubbleProperties(workInProgress);
                return null;
            case HostRoot:
                var fiberRoot = workInProgress.stateNode;
                popHostContainer(workInProgress);
                popTopLevelContextObject(workInProgress);
                resetWorkInProgressVersions();
                if (fiberRoot.pendingContext) {
                    fiberRoot.context = fiberRoot.pendingContext;
                    fiberRoot.pendingContext = null;
                }
                if (current === null || current.child === null) {
                    // If we hydrated, pop so that we can delete any remaining children
                    // that weren't hydrated.
                    var wasHydrated = popHydrationState(workInProgress);
                    if (wasHydrated) // If we hydrated, then we'll need to schedule an update for
                    // the commit side-effects on the root.
                    markUpdate(workInProgress);
                    else if (current !== null) {
                        var prevState = current.memoizedState;
                        if (!prevState.isDehydrated || (workInProgress.flags & ForceClientRender) !== NoFlags) {
                            // Schedule an effect to clear this container at the start of the
                            // next commit. This handles the case of React rendering into a
                            // container with previous children. It's also safe to do for
                            // updates too, because current.child would only be null if the
                            // previous render was null (so the container would already
                            // be empty).
                            workInProgress.flags |= Snapshot; // If this was a forced client render, there may have been
                            // recoverable errors during first hydration attempt. If so, add
                            // them to a queue so we can log them in the commit phase.
                            upgradeHydrationErrorsToRecoverable();
                        }
                    }
                }
                updateHostContainer(current, workInProgress);
                bubbleProperties(workInProgress);
                return null;
            case HostComponent:
                popHostContext(workInProgress);
                var rootContainerInstance = getRootHostContainer();
                var type = workInProgress.type;
                if (current !== null && workInProgress.stateNode != null) {
                    updateHostComponent(current, workInProgress, type, newProps, rootContainerInstance);
                    if (current.ref !== workInProgress.ref) markRef(workInProgress);
                } else {
                    if (!newProps) {
                        if (workInProgress.stateNode === null) throw new Error("We must have new props for new mounts. This error is likely caused by a bug in React. Please file an issue.");
                         // This can happen when we abort work.
                        bubbleProperties(workInProgress);
                        return null;
                    }
                    var currentHostContext = getHostContext(); // TODO: Move createInstance to beginWork and keep it on a context
                    // "stack" as the parent. Then append children as we go in beginWork
                    // or completeWork depending on whether we want to add them top->down or
                    // bottom->up. Top->down is faster in IE11.
                    var _wasHydrated = popHydrationState(workInProgress);
                    if (_wasHydrated) // TODO: Move this and createInstance step into the beginPhase
                    // to consolidate.
                    {
                        if (prepareToHydrateHostInstance(workInProgress, rootContainerInstance, currentHostContext)) // If changes to the hydrated node need to be applied at the
                        // commit-phase we mark this as such.
                        markUpdate(workInProgress);
                    } else {
                        var instance = createInstance(type, newProps, rootContainerInstance, currentHostContext, workInProgress);
                        appendAllChildren(instance, workInProgress, false, false);
                        workInProgress.stateNode = instance; // Certain renderers require commit-time effects for initial mount.
                        // (eg DOM renderer supports auto-focus for certain elements).
                        // Make sure such renderers get scheduled for later work.
                        if (finalizeInitialChildren(instance, type, newProps, rootContainerInstance)) markUpdate(workInProgress);
                    }
                    if (workInProgress.ref !== null) // If there is a ref on a host node we need to schedule a callback
                    markRef(workInProgress);
                }
                bubbleProperties(workInProgress);
                return null;
            case HostText:
                var newText = newProps;
                if (current && workInProgress.stateNode != null) {
                    var oldText = current.memoizedProps; // If we have an alternate, that means this is an update and we need
                    // to schedule a side-effect to do the updates.
                    updateHostText(current, workInProgress, oldText, newText);
                } else {
                    if (typeof newText !== 'string') {
                        if (workInProgress.stateNode === null) throw new Error("We must have new props for new mounts. This error is likely caused by a bug in React. Please file an issue.");
                         // This can happen when we abort work.
                    }
                    var _rootContainerInstance = getRootHostContainer();
                    var _currentHostContext = getHostContext();
                    var _wasHydrated2 = popHydrationState(workInProgress);
                    if (_wasHydrated2) {
                        if (prepareToHydrateHostTextInstance(workInProgress)) markUpdate(workInProgress);
                    } else workInProgress.stateNode = createTextInstance(newText, _rootContainerInstance, _currentHostContext, workInProgress);
                }
                bubbleProperties(workInProgress);
                return null;
            case SuspenseComponent:
                popSuspenseContext(workInProgress);
                var nextState = workInProgress.memoizedState;
                if (hasUnhydratedTailNodes() && (workInProgress.mode & ConcurrentMode) !== NoMode && (workInProgress.flags & DidCapture) === NoFlags) {
                    warnIfUnhydratedTailNodes(workInProgress);
                    resetHydrationState();
                    workInProgress.flags |= ForceClientRender | Incomplete | ShouldCapture;
                    return workInProgress;
                }
                if (nextState !== null && nextState.dehydrated !== null) {
                    // We might be inside a hydration state the first time we're picking up this
                    // Suspense boundary, and also after we've reentered it for further hydration.
                    var _wasHydrated3 = popHydrationState(workInProgress);
                    if (current === null) {
                        if (!_wasHydrated3) throw new Error("A dehydrated suspense component was completed without a hydrated node. This is probably a bug in React.");
                        prepareToHydrateHostSuspenseInstance(workInProgress);
                        bubbleProperties(workInProgress);
                        if ((workInProgress.mode & ProfileMode) !== NoMode) {
                            var isTimedOutSuspense = nextState !== null;
                            if (isTimedOutSuspense) {
                                // Don't count time spent in a timed out Suspense subtree as part of the base duration.
                                var primaryChildFragment = workInProgress.child;
                                if (primaryChildFragment !== null) // $FlowFixMe Flow doesn't support type casting in combination with the -= operator
                                workInProgress.treeBaseDuration -= primaryChildFragment.treeBaseDuration;
                            }
                        }
                        return null;
                    } else {
                        // We might have reentered this boundary to hydrate it. If so, we need to reset the hydration
                        // state since we're now exiting out of it. popHydrationState doesn't do that for us.
                        resetHydrationState();
                        if ((workInProgress.flags & DidCapture) === NoFlags) // This boundary did not suspend so it's now hydrated and unsuspended.
                        workInProgress.memoizedState = null;
                         // If nothing suspended, we need to schedule an effect to mark this boundary
                        // as having hydrated so events know that they're free to be invoked.
                        // It's also a signal to replay events and the suspense callback.
                        // If something suspended, schedule an effect to attach retry listeners.
                        // So we might as well always mark this.
                        workInProgress.flags |= Update;
                        bubbleProperties(workInProgress);
                        if ((workInProgress.mode & ProfileMode) !== NoMode) {
                            var _isTimedOutSuspense = nextState !== null;
                            if (_isTimedOutSuspense) {
                                // Don't count time spent in a timed out Suspense subtree as part of the base duration.
                                var _primaryChildFragment = workInProgress.child;
                                if (_primaryChildFragment !== null) // $FlowFixMe Flow doesn't support type casting in combination with the -= operator
                                workInProgress.treeBaseDuration -= _primaryChildFragment.treeBaseDuration;
                            }
                        }
                        return null;
                    }
                } // Successfully completed this tree. If this was a forced client render,
                // there may have been recoverable errors during first hydration
                // attempt. If so, add them to a queue so we can log them in the
                // commit phase.
                upgradeHydrationErrorsToRecoverable();
                if ((workInProgress.flags & DidCapture) !== NoFlags) {
                    // Something suspended. Re-render with the fallback children.
                    workInProgress.lanes = renderLanes; // Do not reset the effect list.
                    if ((workInProgress.mode & ProfileMode) !== NoMode) transferActualDuration(workInProgress);
                     // Don't bubble properties in this case.
                    return workInProgress;
                }
                var nextDidTimeout = nextState !== null;
                var prevDidTimeout = false;
                if (current === null) popHydrationState(workInProgress);
                else {
                    var _prevState = current.memoizedState;
                    prevDidTimeout = _prevState !== null;
                }
                // a passive effect, which is when we process the transitions
                if (nextDidTimeout !== prevDidTimeout) // an effect to toggle the subtree's visibility. When we switch from
                // fallback -> primary, the inner Offscreen fiber schedules this effect
                // as part of its normal complete phase. But when we switch from
                // primary -> fallback, the inner Offscreen fiber does not have a complete
                // phase. So we need to schedule its effect here.
                //
                // We also use this flag to connect/disconnect the effects, but the same
                // logic applies: when re-connecting, the Offscreen fiber's complete
                // phase will handle scheduling the effect. It's only when the fallback
                // is active that we have to do anything special.
                {
                    if (nextDidTimeout) {
                        var _offscreenFiber2 = workInProgress.child;
                        _offscreenFiber2.flags |= Visibility; // TODO: This will still suspend a synchronous tree if anything
                        // in the concurrent tree already suspended during this render.
                        // This is a known bug.
                        if ((workInProgress.mode & ConcurrentMode) !== NoMode) {
                            // TODO: Move this back to throwException because this is too late
                            // if this is a large tree which is common for initial loads. We
                            // don't know if we should restart a render or not until we get
                            // this marker, and this is too late.
                            // If this render already had a ping or lower pri updates,
                            // and this is the first time we know we're going to suspend we
                            // should be able to immediately restart from within throwException.
                            var hasInvisibleChildContext = current === null && (workInProgress.memoizedProps.unstable_avoidThisFallback !== true || !enableSuspenseAvoidThisFallback);
                            if (hasInvisibleChildContext || hasSuspenseContext(suspenseStackCursor.current, InvisibleParentSuspenseContext)) // If this was in an invisible tree or a new render, then showing
                            // this boundary is ok.
                            renderDidSuspend();
                            else // Otherwise, we're going to have to hide content so we should
                            // suspend for longer if possible.
                            renderDidSuspendDelayIfPossible();
                        }
                    }
                }
                var wakeables = workInProgress.updateQueue;
                if (wakeables !== null) // Schedule an effect to attach a retry listener to the promise.
                // TODO: Move to passive phase
                workInProgress.flags |= Update;
                bubbleProperties(workInProgress);
                if ((workInProgress.mode & ProfileMode) !== NoMode) {
                    if (nextDidTimeout) {
                        // Don't count time spent in a timed out Suspense subtree as part of the base duration.
                        var _primaryChildFragment2 = workInProgress.child;
                        if (_primaryChildFragment2 !== null) // $FlowFixMe Flow doesn't support type casting in combination with the -= operator
                        workInProgress.treeBaseDuration -= _primaryChildFragment2.treeBaseDuration;
                    }
                }
                return null;
            case HostPortal:
                popHostContainer(workInProgress);
                updateHostContainer(current, workInProgress);
                if (current === null) preparePortalMount(workInProgress.stateNode.containerInfo);
                bubbleProperties(workInProgress);
                return null;
            case ContextProvider:
                // Pop provider fiber
                var context = workInProgress.type._context;
                popProvider(context, workInProgress);
                bubbleProperties(workInProgress);
                return null;
            case IncompleteClassComponent:
                // Same as class component case. I put it down here so that the tags are
                // sequential to ensure this switch is compiled to a jump table.
                var _Component = workInProgress.type;
                if (isContextProvider(_Component)) popContext(workInProgress);
                bubbleProperties(workInProgress);
                return null;
            case SuspenseListComponent:
                popSuspenseContext(workInProgress);
                var renderState = workInProgress.memoizedState;
                if (renderState === null) {
                    // We're running in the default, "independent" mode.
                    // We don't do anything in this mode.
                    bubbleProperties(workInProgress);
                    return null;
                }
                var didSuspendAlready = (workInProgress.flags & DidCapture) !== NoFlags;
                var renderedTail = renderState.rendering;
                if (renderedTail === null) {
                    // We just rendered the head.
                    if (!didSuspendAlready) {
                        // This is the first pass. We need to figure out if anything is still
                        // suspended in the rendered set.
                        // If new content unsuspended, but there's still some content that
                        // didn't. Then we need to do a second pass that forces everything
                        // to keep showing their fallbacks.
                        // We might be suspended if something in this render pass suspended, or
                        // something in the previous committed pass suspended. Otherwise,
                        // there's no chance so we can skip the expensive call to
                        // findFirstSuspended.
                        var cannotBeSuspended = renderHasNotSuspendedYet() && (current === null || (current.flags & DidCapture) === NoFlags);
                        if (!cannotBeSuspended) {
                            var row = workInProgress.child;
                            while(row !== null){
                                var suspended = findFirstSuspended(row);
                                if (suspended !== null) {
                                    didSuspendAlready = true;
                                    workInProgress.flags |= DidCapture;
                                    cutOffTailIfNeeded(renderState, false); // If this is a newly suspended tree, it might not get committed as
                                    // part of the second pass. In that case nothing will subscribe to
                                    // its thenables. Instead, we'll transfer its thenables to the
                                    // SuspenseList so that it can retry if they resolve.
                                    // There might be multiple of these in the list but since we're
                                    // going to wait for all of them anyway, it doesn't really matter
                                    // which ones gets to ping. In theory we could get clever and keep
                                    // track of how many dependencies remain but it gets tricky because
                                    // in the meantime, we can add/remove/change items and dependencies.
                                    // We might bail out of the loop before finding any but that
                                    // doesn't matter since that means that the other boundaries that
                                    // we did find already has their listeners attached.
                                    var newThenables = suspended.updateQueue;
                                    if (newThenables !== null) {
                                        workInProgress.updateQueue = newThenables;
                                        workInProgress.flags |= Update;
                                    } // Rerender the whole list, but this time, we'll force fallbacks
                                    // to stay in place.
                                    // Reset the effect flags before doing the second pass since that's now invalid.
                                    // Reset the child fibers to their original state.
                                    workInProgress.subtreeFlags = NoFlags;
                                    resetChildFibers(workInProgress, renderLanes); // Set up the Suspense Context to force suspense and immediately
                                    // rerender the children.
                                    pushSuspenseContext(workInProgress, setShallowSuspenseContext(suspenseStackCursor.current, ForceSuspenseFallback)); // Don't bubble properties in this case.
                                    return workInProgress.child;
                                }
                                row = row.sibling;
                            }
                        }
                        if (renderState.tail !== null && now() > getRenderTargetTime()) {
                            // We have already passed our CPU deadline but we still have rows
                            // left in the tail. We'll just give up further attempts to render
                            // the main content and only render fallbacks.
                            workInProgress.flags |= DidCapture;
                            didSuspendAlready = true;
                            cutOffTailIfNeeded(renderState, false); // Since nothing actually suspended, there will nothing to ping this
                            // to get it started back up to attempt the next item. While in terms
                            // of priority this work has the same priority as this current render,
                            // it's not part of the same transition once the transition has
                            // committed. If it's sync, we still want to yield so that it can be
                            // painted. Conceptually, this is really the same as pinging.
                            // We can use any RetryLane even if it's the one currently rendering
                            // since we're leaving it behind on this node.
                            workInProgress.lanes = SomeRetryLane;
                        }
                    } else cutOffTailIfNeeded(renderState, false);
                     // Next we're going to render the tail.
                } else {
                    // Append the rendered row to the child list.
                    if (!didSuspendAlready) {
                        var _suspended = findFirstSuspended(renderedTail);
                        if (_suspended !== null) {
                            workInProgress.flags |= DidCapture;
                            didSuspendAlready = true; // Ensure we transfer the update queue to the parent so that it doesn't
                            // get lost if this row ends up dropped during a second pass.
                            var _newThenables = _suspended.updateQueue;
                            if (_newThenables !== null) {
                                workInProgress.updateQueue = _newThenables;
                                workInProgress.flags |= Update;
                            }
                            cutOffTailIfNeeded(renderState, true); // This might have been modified.
                            if (renderState.tail === null && renderState.tailMode === 'hidden' && !renderedTail.alternate && !getIsHydrating() // We don't cut it if we're hydrating.
                            ) {
                                // We're done.
                                bubbleProperties(workInProgress);
                                return null;
                            }
                        } else if (// time we have to render. So rendering one more row would likely
                        // exceed it.
                        now() * 2 - renderState.renderingStartTime > getRenderTargetTime() && renderLanes !== OffscreenLane) {
                            // We have now passed our CPU deadline and we'll just give up further
                            // attempts to render the main content and only render fallbacks.
                            // The assumption is that this is usually faster.
                            workInProgress.flags |= DidCapture;
                            didSuspendAlready = true;
                            cutOffTailIfNeeded(renderState, false); // Since nothing actually suspended, there will nothing to ping this
                            // to get it started back up to attempt the next item. While in terms
                            // of priority this work has the same priority as this current render,
                            // it's not part of the same transition once the transition has
                            // committed. If it's sync, we still want to yield so that it can be
                            // painted. Conceptually, this is really the same as pinging.
                            // We can use any RetryLane even if it's the one currently rendering
                            // since we're leaving it behind on this node.
                            workInProgress.lanes = SomeRetryLane;
                        }
                    }
                    if (renderState.isBackwards) {
                        // The effect list of the backwards tail will have been added
                        // to the end. This breaks the guarantee that life-cycles fire in
                        // sibling order but that isn't a strong guarantee promised by React.
                        // Especially since these might also just pop in during future commits.
                        // Append to the beginning of the list.
                        renderedTail.sibling = workInProgress.child;
                        workInProgress.child = renderedTail;
                    } else {
                        var previousSibling = renderState.last;
                        if (previousSibling !== null) previousSibling.sibling = renderedTail;
                        else workInProgress.child = renderedTail;
                        renderState.last = renderedTail;
                    }
                }
                if (renderState.tail !== null) {
                    // We still have tail rows to render.
                    // Pop a row.
                    var next = renderState.tail;
                    renderState.rendering = next;
                    renderState.tail = next.sibling;
                    renderState.renderingStartTime = now();
                    next.sibling = null; // Restore the context.
                    // TODO: We can probably just avoid popping it instead and only
                    // setting it the first time we go from not suspended to suspended.
                    var suspenseContext = suspenseStackCursor.current;
                    if (didSuspendAlready) suspenseContext = setShallowSuspenseContext(suspenseContext, ForceSuspenseFallback);
                    else suspenseContext = setDefaultShallowSuspenseContext(suspenseContext);
                    pushSuspenseContext(workInProgress, suspenseContext); // Do a pass over the next row.
                    // Don't bubble properties in this case.
                    return next;
                }
                bubbleProperties(workInProgress);
                return null;
            case ScopeComponent:
                break;
            case OffscreenComponent:
            case LegacyHiddenComponent:
                popRenderLanes(workInProgress);
                var _nextState = workInProgress.memoizedState;
                var nextIsHidden = _nextState !== null;
                if (current !== null) {
                    var _prevState2 = current.memoizedState;
                    var prevIsHidden = _prevState2 !== null;
                    if (prevIsHidden !== nextIsHidden && !enableLegacyHidden) workInProgress.flags |= Visibility;
                }
                if (!nextIsHidden || (workInProgress.mode & ConcurrentMode) === NoMode) bubbleProperties(workInProgress);
                else // Don't bubble properties for hidden children unless we're rendering
                // at offscreen priority.
                if (includesSomeLane(subtreeRenderLanes1, OffscreenLane)) {
                    bubbleProperties(workInProgress);
                    // Check if there was an insertion or update in the hidden subtree.
                    // If so, we need to hide those nodes in the commit phase, so
                    // schedule a visibility effect.
                    if (workInProgress.subtreeFlags & (Placement | Update)) workInProgress.flags |= Visibility;
                }
                return null;
            case CacheComponent:
                return null;
            case TracingMarkerComponent:
                return null;
        }
        throw new Error("Unknown unit of work tag (" + workInProgress.tag + "). This error is likely caused by a bug in " + 'React. Please file an issue.');
    }
    var ReactCurrentOwner$1 = ReactSharedInternals.ReactCurrentOwner;
    var didReceiveUpdate = false;
    var didWarnAboutBadClass;
    var didWarnAboutModulePatternComponent;
    var didWarnAboutContextTypeOnFunctionComponent;
    var didWarnAboutGetDerivedStateOnFunctionComponent;
    var didWarnAboutFunctionRefs;
    var didWarnAboutReassigningProps;
    var didWarnAboutRevealOrder;
    var didWarnAboutTailOptions;
    didWarnAboutBadClass = {};
    didWarnAboutModulePatternComponent = {};
    didWarnAboutContextTypeOnFunctionComponent = {};
    didWarnAboutGetDerivedStateOnFunctionComponent = {};
    didWarnAboutFunctionRefs = {};
    didWarnAboutReassigningProps = false;
    didWarnAboutRevealOrder = {};
    didWarnAboutTailOptions = {};
    function reconcileChildren(current, workInProgress, nextChildren, renderLanes) {
        if (current === null) // If this is a fresh new component that hasn't been rendered yet, we
        // won't update its child set by applying minimal side-effects. Instead,
        // we will add them all to the child before it gets rendered. That means
        // we can optimize this reconciliation pass by not tracking side-effects.
        workInProgress.child = mountChildFibers(workInProgress, null, nextChildren, renderLanes);
        else // If the current child is the same as the work in progress, it means that
        // we haven't yet started any work on these children. Therefore, we use
        // the clone algorithm to create a copy of all the current children.
        // If we had any progressed work already, that is invalid at this point so
        // let's throw it out.
        workInProgress.child = reconcileChildFibers1(workInProgress, current.child, nextChildren, renderLanes);
    }
    function forceUnmountCurrentAndReconcile(current, workInProgress, nextChildren, renderLanes) {
        // This function is fork of reconcileChildren. It's used in cases where we
        // want to reconcile without matching against the existing set. This has the
        // effect of all current children being unmounted; even if the type and key
        // are the same, the old child is unmounted and a new child is created.
        //
        // To do this, we're going to go through the reconcile algorithm twice. In
        // the first pass, we schedule a deletion for all the current children by
        // passing null.
        workInProgress.child = reconcileChildFibers1(workInProgress, current.child, null, renderLanes); // In the second pass, we mount the new children. The trick here is that we
        // pass null in place of where we usually pass the current child set. This has
        // the effect of remounting all children regardless of whether their
        // identities match.
        workInProgress.child = reconcileChildFibers1(workInProgress, null, nextChildren, renderLanes);
    }
    function updateForwardRef(current, workInProgress, Component, nextProps, renderLanes) {
        if (workInProgress.type !== workInProgress.elementType) {
            // Lazy component props can't be validated in createElement
            // because they're only guaranteed to be resolved here.
            var innerPropTypes = Component.propTypes;
            if (innerPropTypes) checkPropTypes(innerPropTypes, nextProps, 'prop', getComponentNameFromType(Component));
        }
        var render = Component.render;
        var ref = workInProgress.ref; // The rest is a fork of updateFunctionComponent
        var nextChildren;
        var hasId;
        prepareToReadContext(workInProgress, renderLanes);
        markComponentRenderStarted(workInProgress);
        ReactCurrentOwner$1.current = workInProgress;
        setIsRendering(true);
        nextChildren = renderWithHooks(current, workInProgress, render, nextProps, ref, renderLanes);
        hasId = checkDidRenderIdHook();
        if (workInProgress.mode & StrictLegacyMode) {
            setIsStrictModeForDevtools(true);
            try {
                nextChildren = renderWithHooks(current, workInProgress, render, nextProps, ref, renderLanes);
                hasId = checkDidRenderIdHook();
            } finally{
                setIsStrictModeForDevtools(false);
            }
        }
        setIsRendering(false);
        markComponentRenderStopped();
        if (current !== null && !didReceiveUpdate) {
            bailoutHooks(current, workInProgress, renderLanes);
            return bailoutOnAlreadyFinishedWork(current, workInProgress, renderLanes);
        }
        if (getIsHydrating() && hasId) pushMaterializedTreeId(workInProgress);
         // React DevTools reads this flag.
        workInProgress.flags |= PerformedWork;
        reconcileChildren(current, workInProgress, nextChildren, renderLanes);
        return workInProgress.child;
    }
    function updateMemoComponent(current, workInProgress, Component, nextProps, renderLanes) {
        if (current === null) {
            var type = Component.type;
            if (isSimpleFunctionComponent(type) && Component.compare === null && Component.defaultProps === undefined) {
                var resolvedType = type;
                resolvedType = resolveFunctionForHotReloading(type);
                // and with only the default shallow comparison, we upgrade it
                // to a SimpleMemoComponent to allow fast path updates.
                workInProgress.tag = SimpleMemoComponent;
                workInProgress.type = resolvedType;
                validateFunctionComponentInDev(workInProgress, type);
                return updateSimpleMemoComponent(current, workInProgress, resolvedType, nextProps, renderLanes);
            }
            var innerPropTypes = type.propTypes;
            if (innerPropTypes) // Inner memo component props aren't currently validated in createElement.
            // We could move it there, but we'd still need this for lazy code path.
            checkPropTypes(innerPropTypes, nextProps, 'prop', getComponentNameFromType(type));
            var child = createFiberFromTypeAndProps(Component.type, null, nextProps, workInProgress, workInProgress.mode, renderLanes);
            child.ref = workInProgress.ref;
            child.return = workInProgress;
            workInProgress.child = child;
            return child;
        }
        var _type = Component.type;
        var _innerPropTypes = _type.propTypes;
        if (_innerPropTypes) // Inner memo component props aren't currently validated in createElement.
        // We could move it there, but we'd still need this for lazy code path.
        checkPropTypes(_innerPropTypes, nextProps, 'prop', getComponentNameFromType(_type));
        var currentChild = current.child; // This is always exactly one child
        var hasScheduledUpdateOrContext = checkScheduledUpdateOrContext(current, renderLanes);
        if (!hasScheduledUpdateOrContext) {
            // This will be the props with resolved defaultProps,
            // unlike current.memoizedProps which will be the unresolved ones.
            var prevProps = currentChild.memoizedProps; // Default to shallow comparison
            var compare = Component.compare;
            compare = compare !== null ? compare : shallowEqual;
            if (compare(prevProps, nextProps) && current.ref === workInProgress.ref) return bailoutOnAlreadyFinishedWork(current, workInProgress, renderLanes);
        } // React DevTools reads this flag.
        workInProgress.flags |= PerformedWork;
        var newChild = createWorkInProgress(currentChild, nextProps);
        newChild.ref = workInProgress.ref;
        newChild.return = workInProgress;
        workInProgress.child = newChild;
        return newChild;
    }
    function updateSimpleMemoComponent(current, workInProgress, Component, nextProps, renderLanes) {
        if (workInProgress.type !== workInProgress.elementType) {
            // Lazy component props can't be validated in createElement
            // because they're only guaranteed to be resolved here.
            var outerMemoType = workInProgress.elementType;
            if (outerMemoType.$$typeof === REACT_LAZY_TYPE) {
                // We warn when you define propTypes on lazy()
                // so let's just skip over it to find memo() outer wrapper.
                // Inner props for memo are validated later.
                var lazyComponent = outerMemoType;
                var payload = lazyComponent._payload;
                var init = lazyComponent._init;
                try {
                    outerMemoType = init(payload);
                } catch (x) {
                    outerMemoType = null;
                } // Inner propTypes will be validated in the function component path.
                var outerPropTypes = outerMemoType && outerMemoType.propTypes;
                if (outerPropTypes) checkPropTypes(outerPropTypes, nextProps, 'prop', getComponentNameFromType(outerMemoType));
            }
        }
        if (current !== null) {
            var prevProps = current.memoizedProps;
            if (shallowEqual(prevProps, nextProps) && current.ref === workInProgress.ref && workInProgress.type === current.type) {
                didReceiveUpdate = false; // The props are shallowly equal. Reuse the previous props object, like we
                // would during a normal fiber bailout.
                //
                // We don't have strong guarantees that the props object is referentially
                // equal during updates where we can't bail out anyway — like if the props
                // are shallowly equal, but there's a local state or context update in the
                // same batch.
                //
                // However, as a principle, we should aim to make the behavior consistent
                // across different ways of memoizing a component. For example, React.memo
                // has a different internal Fiber layout if you pass a normal function
                // component (SimpleMemoComponent) versus if you pass a different type
                // like forwardRef (MemoComponent). But this is an implementation detail.
                // Wrapping a component in forwardRef (or React.lazy, etc) shouldn't
                // affect whether the props object is reused during a bailout.
                workInProgress.pendingProps = nextProps = prevProps;
                if (!checkScheduledUpdateOrContext(current, renderLanes)) {
                    // The pending lanes were cleared at the beginning of beginWork. We're
                    // about to bail out, but there might be other lanes that weren't
                    // included in the current render. Usually, the priority level of the
                    // remaining updates is accumulated during the evaluation of the
                    // component (i.e. when processing the update queue). But since since
                    // we're bailing out early *without* evaluating the component, we need
                    // to account for it here, too. Reset to the value of the current fiber.
                    // NOTE: This only applies to SimpleMemoComponent, not MemoComponent,
                    // because a MemoComponent fiber does not have hooks or an update queue;
                    // rather, it wraps around an inner component, which may or may not
                    // contains hooks.
                    // TODO: Move the reset at in beginWork out of the common path so that
                    // this is no longer necessary.
                    workInProgress.lanes = current.lanes;
                    return bailoutOnAlreadyFinishedWork(current, workInProgress, renderLanes);
                } else if ((current.flags & ForceUpdateForLegacySuspense) !== NoFlags) // This is a special case that only exists for legacy mode.
                // See https://github.com/facebook/react/pull/19216.
                didReceiveUpdate = true;
            }
        }
        return updateFunctionComponent(current, workInProgress, Component, nextProps, renderLanes);
    }
    function updateOffscreenComponent(current, workInProgress, renderLanes) {
        var nextProps = workInProgress.pendingProps;
        var nextChildren = nextProps.children;
        var prevState = current !== null ? current.memoizedState : null;
        if (nextProps.mode === 'hidden' || enableLegacyHidden) {
            // Rendering a hidden tree.
            if ((workInProgress.mode & ConcurrentMode) === NoMode) {
                // In legacy sync mode, don't defer the subtree. Render it now.
                // TODO: Consider how Offscreen should work with transitions in the future
                var nextState = {
                    baseLanes: NoLanes,
                    cachePool: null,
                    transitions: null
                };
                workInProgress.memoizedState = nextState;
                pushRenderLanes(workInProgress, renderLanes);
            } else if (!includesSomeLane(renderLanes, OffscreenLane)) {
                var spawnedCachePool = null; // We're hidden, and we're not rendering at Offscreen. We will bail out
                // and resume this tree later.
                var nextBaseLanes;
                if (prevState !== null) {
                    var prevBaseLanes = prevState.baseLanes;
                    nextBaseLanes = mergeLanes(prevBaseLanes, renderLanes);
                } else nextBaseLanes = renderLanes;
                 // Schedule this fiber to re-render at offscreen priority. Then bailout.
                workInProgress.lanes = workInProgress.childLanes = laneToLanes(OffscreenLane);
                var _nextState = {
                    baseLanes: nextBaseLanes,
                    cachePool: spawnedCachePool,
                    transitions: null
                };
                workInProgress.memoizedState = _nextState;
                workInProgress.updateQueue = null;
                // to avoid a push/pop misalignment.
                pushRenderLanes(workInProgress, nextBaseLanes);
                return null;
            } else {
                // This is the second render. The surrounding visible content has already
                // committed. Now we resume rendering the hidden tree.
                // Rendering at offscreen, so we can clear the base lanes.
                var _nextState2 = {
                    baseLanes: NoLanes,
                    cachePool: null,
                    transitions: null
                };
                workInProgress.memoizedState = _nextState2; // Push the lanes that were skipped when we bailed out.
                var subtreeRenderLanes = prevState !== null ? prevState.baseLanes : renderLanes;
                pushRenderLanes(workInProgress, subtreeRenderLanes);
            }
        } else {
            // Rendering a visible tree.
            var _subtreeRenderLanes;
            if (prevState !== null) {
                // We're going from hidden -> visible.
                _subtreeRenderLanes = mergeLanes(prevState.baseLanes, renderLanes);
                workInProgress.memoizedState = null;
            } else // We weren't previously hidden, and we still aren't, so there's nothing
            // special to do. Need to push to the stack regardless, though, to avoid
            // a push/pop misalignment.
            _subtreeRenderLanes = renderLanes;
            pushRenderLanes(workInProgress, _subtreeRenderLanes);
        }
        reconcileChildren(current, workInProgress, nextChildren, renderLanes);
        return workInProgress.child;
    }
    function updateFragment1(current, workInProgress, renderLanes) {
        var nextChildren = workInProgress.pendingProps;
        reconcileChildren(current, workInProgress, nextChildren, renderLanes);
        return workInProgress.child;
    }
    function updateMode(current, workInProgress, renderLanes) {
        var nextChildren = workInProgress.pendingProps.children;
        reconcileChildren(current, workInProgress, nextChildren, renderLanes);
        return workInProgress.child;
    }
    function updateProfiler(current, workInProgress, renderLanes) {
        workInProgress.flags |= Update;
        // Reset effect durations for the next eventual effect phase.
        // These are reset during render to allow the DevTools commit hook a chance to read them,
        var stateNode = workInProgress.stateNode;
        stateNode.effectDuration = 0;
        stateNode.passiveEffectDuration = 0;
        var nextProps = workInProgress.pendingProps;
        var nextChildren = nextProps.children;
        reconcileChildren(current, workInProgress, nextChildren, renderLanes);
        return workInProgress.child;
    }
    function markRef$1(current, workInProgress) {
        var ref = workInProgress.ref;
        if (current === null && ref !== null || current !== null && current.ref !== ref) {
            // Schedule a Ref effect
            workInProgress.flags |= Ref;
            workInProgress.flags |= RefStatic;
        }
    }
    function updateFunctionComponent(current, workInProgress, Component, nextProps, renderLanes) {
        if (workInProgress.type !== workInProgress.elementType) {
            // Lazy component props can't be validated in createElement
            // because they're only guaranteed to be resolved here.
            var innerPropTypes = Component.propTypes;
            if (innerPropTypes) checkPropTypes(innerPropTypes, nextProps, 'prop', getComponentNameFromType(Component));
        }
        var context;
        var unmaskedContext = getUnmaskedContext(workInProgress, Component, true);
        context = getMaskedContext(workInProgress, unmaskedContext);
        var nextChildren;
        var hasId;
        prepareToReadContext(workInProgress, renderLanes);
        markComponentRenderStarted(workInProgress);
        ReactCurrentOwner$1.current = workInProgress;
        setIsRendering(true);
        nextChildren = renderWithHooks(current, workInProgress, Component, nextProps, context, renderLanes);
        hasId = checkDidRenderIdHook();
        if (workInProgress.mode & StrictLegacyMode) {
            setIsStrictModeForDevtools(true);
            try {
                nextChildren = renderWithHooks(current, workInProgress, Component, nextProps, context, renderLanes);
                hasId = checkDidRenderIdHook();
            } finally{
                setIsStrictModeForDevtools(false);
            }
        }
        setIsRendering(false);
        markComponentRenderStopped();
        if (current !== null && !didReceiveUpdate) {
            bailoutHooks(current, workInProgress, renderLanes);
            return bailoutOnAlreadyFinishedWork(current, workInProgress, renderLanes);
        }
        if (getIsHydrating() && hasId) pushMaterializedTreeId(workInProgress);
         // React DevTools reads this flag.
        workInProgress.flags |= PerformedWork;
        reconcileChildren(current, workInProgress, nextChildren, renderLanes);
        return workInProgress.child;
    }
    function updateClassComponent(current, workInProgress, Component, nextProps, renderLanes) {
        // This is used by DevTools to force a boundary to error.
        switch(shouldError(workInProgress)){
            case false:
                var _instance = workInProgress.stateNode;
                var ctor = workInProgress.type; // TODO This way of resetting the error boundary state is a hack.
                // Is there a better way to do this?
                var tempInstance = new ctor(workInProgress.memoizedProps, _instance.context);
                var state = tempInstance.state;
                _instance.updater.enqueueSetState(_instance, state, null);
                break;
            case true:
                workInProgress.flags |= DidCapture;
                workInProgress.flags |= ShouldCapture; // eslint-disable-next-line react-internal/prod-error-codes
                var error$1 = new Error('Simulated error coming from DevTools');
                var lane = pickArbitraryLane(renderLanes);
                workInProgress.lanes = mergeLanes(workInProgress.lanes, lane); // Schedule the error boundary to re-render using updated state
                var update = createClassErrorUpdate(workInProgress, createCapturedValue(error$1, workInProgress), lane);
                enqueueCapturedUpdate(workInProgress, update);
                break;
        }
        if (workInProgress.type !== workInProgress.elementType) {
            // Lazy component props can't be validated in createElement
            // because they're only guaranteed to be resolved here.
            var innerPropTypes = Component.propTypes;
            if (innerPropTypes) checkPropTypes(innerPropTypes, nextProps, 'prop', getComponentNameFromType(Component));
        }
        // During mounting we don't know the child context yet as the instance doesn't exist.
        // We will invalidate the child context in finishClassComponent() right after rendering.
        var hasContext;
        if (isContextProvider(Component)) {
            hasContext = true;
            pushContextProvider(workInProgress);
        } else hasContext = false;
        prepareToReadContext(workInProgress, renderLanes);
        var instance = workInProgress.stateNode;
        var shouldUpdate;
        if (instance === null) {
            if (current !== null) {
                // A class component without an instance only mounts if it suspended
                // inside a non-concurrent tree, in an inconsistent state. We want to
                // treat it like a new mount, even though an empty version of it already
                // committed. Disconnect the alternate pointers.
                current.alternate = null;
                workInProgress.alternate = null; // Since this is conceptually a new fiber, schedule a Placement effect
                workInProgress.flags |= Placement;
            } // In the initial pass we might need to construct the instance.
            constructClassInstance(workInProgress, Component, nextProps);
            mountClassInstance(workInProgress, Component, nextProps, renderLanes);
            shouldUpdate = true;
        } else if (current === null) // In a resume, we'll already have an instance we can reuse.
        shouldUpdate = resumeMountClassInstance(workInProgress, Component, nextProps, renderLanes);
        else shouldUpdate = updateClassInstance(current, workInProgress, Component, nextProps, renderLanes);
        var nextUnitOfWork = finishClassComponent(current, workInProgress, Component, shouldUpdate, hasContext, renderLanes);
        var inst = workInProgress.stateNode;
        if (shouldUpdate && inst.props !== nextProps) {
            if (!didWarnAboutReassigningProps) error1("It looks like %s is reassigning its own `this.props` while rendering. This is not supported and can lead to confusing bugs.", getComponentNameFromFiber(workInProgress) || 'a component');
            didWarnAboutReassigningProps = true;
        }
        return nextUnitOfWork;
    }
    function finishClassComponent(current, workInProgress, Component, shouldUpdate, hasContext, renderLanes) {
        // Refs should update even if shouldComponentUpdate returns false
        markRef$1(current, workInProgress);
        var didCaptureError = (workInProgress.flags & DidCapture) !== NoFlags;
        if (!shouldUpdate && !didCaptureError) {
            // Context providers should defer to sCU for rendering
            if (hasContext) invalidateContextProvider(workInProgress, Component, false);
            return bailoutOnAlreadyFinishedWork(current, workInProgress, renderLanes);
        }
        var instance = workInProgress.stateNode; // Rerender
        ReactCurrentOwner$1.current = workInProgress;
        var nextChildren;
        if (didCaptureError && typeof Component.getDerivedStateFromError !== 'function') {
            // If we captured an error, but getDerivedStateFromError is not defined,
            // unmount all the children. componentDidCatch will schedule an update to
            // re-render a fallback. This is temporary until we migrate everyone to
            // the new API.
            // TODO: Warn in a future release.
            nextChildren = null;
            stopProfilerTimerIfRunning();
        } else {
            markComponentRenderStarted(workInProgress);
            setIsRendering(true);
            nextChildren = instance.render();
            if (workInProgress.mode & StrictLegacyMode) {
                setIsStrictModeForDevtools(true);
                try {
                    instance.render();
                } finally{
                    setIsStrictModeForDevtools(false);
                }
            }
            setIsRendering(false);
            markComponentRenderStopped();
        } // React DevTools reads this flag.
        workInProgress.flags |= PerformedWork;
        if (current !== null && didCaptureError) // If we're recovering from an error, reconcile without reusing any of
        // the existing children. Conceptually, the normal children and the children
        // that are shown on error are two different sets, so we shouldn't reuse
        // normal children even if their identities match.
        forceUnmountCurrentAndReconcile(current, workInProgress, nextChildren, renderLanes);
        else reconcileChildren(current, workInProgress, nextChildren, renderLanes);
         // Memoize state using the values we just used to render.
        // TODO: Restructure so we never read values from the instance.
        workInProgress.memoizedState = instance.state; // The context might have changed so we need to recalculate it.
        if (hasContext) invalidateContextProvider(workInProgress, Component, true);
        return workInProgress.child;
    }
    function pushHostRootContext(workInProgress) {
        var root = workInProgress.stateNode;
        if (root.pendingContext) pushTopLevelContextObject(workInProgress, root.pendingContext, root.pendingContext !== root.context);
        else if (root.context) // Should always be set
        pushTopLevelContextObject(workInProgress, root.context, false);
        pushHostContainer(workInProgress, root.containerInfo);
    }
    function updateHostRoot(current, workInProgress, renderLanes) {
        pushHostRootContext(workInProgress);
        if (current === null) throw new Error('Should have a current fiber. This is a bug in React.');
        var nextProps = workInProgress.pendingProps;
        var prevState = workInProgress.memoizedState;
        var prevChildren = prevState.element;
        cloneUpdateQueue(current, workInProgress);
        processUpdateQueue(workInProgress, nextProps, null, renderLanes);
        var nextState = workInProgress.memoizedState;
        var root = workInProgress.stateNode;
        // being called "element".
        var nextChildren = nextState.element;
        if (prevState.isDehydrated) {
            // This is a hydration root whose shell has not yet hydrated. We should
            // attempt to hydrate.
            // Flip isDehydrated to false to indicate that when this render
            // finishes, the root will no longer be dehydrated.
            var overrideState = {
                element: nextChildren,
                isDehydrated: false,
                cache: nextState.cache,
                pendingSuspenseBoundaries: nextState.pendingSuspenseBoundaries,
                transitions: nextState.transitions
            };
            var updateQueue = workInProgress.updateQueue; // `baseState` can always be the last state because the root doesn't
            // have reducer functions so it doesn't need rebasing.
            updateQueue.baseState = overrideState;
            workInProgress.memoizedState = overrideState;
            if (workInProgress.flags & ForceClientRender) {
                // Something errored during a previous attempt to hydrate the shell, so we
                // forced a client render.
                var recoverableError = new Error("There was an error while hydrating. Because the error happened outside of a Suspense boundary, the entire root will switch to client rendering.");
                return mountHostRootWithoutHydrating(current, workInProgress, nextChildren, renderLanes, recoverableError);
            } else if (nextChildren !== prevChildren) {
                var _recoverableError = new Error("This root received an early update, before anything was able hydrate. Switched the entire root to client rendering.");
                return mountHostRootWithoutHydrating(current, workInProgress, nextChildren, renderLanes, _recoverableError);
            } else {
                // The outermost shell has not hydrated yet. Start hydrating.
                enterHydrationState(workInProgress);
                var child = mountChildFibers(workInProgress, null, nextChildren, renderLanes);
                workInProgress.child = child;
                var node = child;
                while(node){
                    // Mark each child as hydrating. This is a fast path to know whether this
                    // tree is part of a hydrating tree. This is used to determine if a child
                    // node has fully mounted yet, and for scheduling event replaying.
                    // Conceptually this is similar to Placement in that a new subtree is
                    // inserted into the React tree here. It just happens to not need DOM
                    // mutations because it already exists.
                    node.flags = node.flags & ~Placement | Hydrating;
                    node = node.sibling;
                }
            }
        } else {
            // Root is not dehydrated. Either this is a client-only root, or it
            // already hydrated.
            resetHydrationState();
            if (nextChildren === prevChildren) return bailoutOnAlreadyFinishedWork(current, workInProgress, renderLanes);
            reconcileChildren(current, workInProgress, nextChildren, renderLanes);
        }
        return workInProgress.child;
    }
    function mountHostRootWithoutHydrating(current, workInProgress, nextChildren, renderLanes, recoverableError) {
        // Revert to client rendering.
        resetHydrationState();
        queueHydrationError(recoverableError);
        workInProgress.flags |= ForceClientRender;
        reconcileChildren(current, workInProgress, nextChildren, renderLanes);
        return workInProgress.child;
    }
    function updateHostComponent$1(current, workInProgress, renderLanes) {
        pushHostContext(workInProgress);
        if (current === null) tryToClaimNextHydratableInstance(workInProgress);
        var type = workInProgress.type;
        var nextProps = workInProgress.pendingProps;
        var prevProps = current !== null ? current.memoizedProps : null;
        var nextChildren = nextProps.children;
        var isDirectTextChild = shouldSetTextContent(type, nextProps);
        if (isDirectTextChild) // We special case a direct text child of a host node. This is a common
        // case. We won't handle it as a reified child. We will instead handle
        // this in the host environment that also has access to this prop. That
        // avoids allocating another HostText fiber and traversing it.
        nextChildren = null;
        else if (prevProps !== null && shouldSetTextContent(type, prevProps)) // If we're switching from a direct text child to a normal child, or to
        // empty, we need to schedule the text content to be reset.
        workInProgress.flags |= ContentReset;
        markRef$1(current, workInProgress);
        reconcileChildren(current, workInProgress, nextChildren, renderLanes);
        return workInProgress.child;
    }
    function updateHostText$1(current, workInProgress) {
        if (current === null) tryToClaimNextHydratableInstance(workInProgress);
         // Nothing to do here. This is terminal. We'll do the completion step
        // immediately after.
        return null;
    }
    function mountLazyComponent(_current, workInProgress, elementType, renderLanes) {
        if (_current !== null) {
            // A lazy component only mounts if it suspended inside a non-
            // concurrent tree, in an inconsistent state. We want to treat it like
            // a new mount, even though an empty version of it already committed.
            // Disconnect the alternate pointers.
            _current.alternate = null;
            workInProgress.alternate = null; // Since this is conceptually a new fiber, schedule a Placement effect
            workInProgress.flags |= Placement;
        }
        var props = workInProgress.pendingProps;
        var lazyComponent = elementType;
        var payload = lazyComponent._payload;
        var init = lazyComponent._init;
        var Component = init(payload); // Store the unwrapped component in the type.
        workInProgress.type = Component;
        var resolvedTag = workInProgress.tag = resolveLazyComponentTag(Component);
        var resolvedProps = resolveDefaultProps(Component, props);
        var child;
        switch(resolvedTag){
            case FunctionComponent:
                validateFunctionComponentInDev(workInProgress, Component);
                workInProgress.type = Component = resolveFunctionForHotReloading(Component);
                child = updateFunctionComponent(null, workInProgress, Component, resolvedProps, renderLanes);
                return child;
            case ClassComponent:
                workInProgress.type = Component = resolveClassForHotReloading(Component);
                child = updateClassComponent(null, workInProgress, Component, resolvedProps, renderLanes);
                return child;
            case ForwardRef:
                workInProgress.type = Component = resolveForwardRefForHotReloading(Component);
                child = updateForwardRef(null, workInProgress, Component, resolvedProps, renderLanes);
                return child;
            case MemoComponent:
                if (workInProgress.type !== workInProgress.elementType) {
                    var outerPropTypes = Component.propTypes;
                    if (outerPropTypes) checkPropTypes(outerPropTypes, resolvedProps, 'prop', getComponentNameFromType(Component));
                }
                child = updateMemoComponent(null, workInProgress, Component, resolveDefaultProps(Component.type, resolvedProps), renderLanes);
                return child;
        }
        var hint = '';
        if (Component !== null && typeof Component === 'object' && Component.$$typeof === REACT_LAZY_TYPE) hint = ' Did you wrap a component in React.lazy() more than once?';
        // because the fact that it's a separate type of work is an
        // implementation detail.
        throw new Error("Element type is invalid. Received a promise that resolves to: " + Component + ". " + ("Lazy element type must resolve to a class or function." + hint));
    }
    function mountIncompleteClassComponent(_current, workInProgress, Component, nextProps, renderLanes) {
        if (_current !== null) {
            // An incomplete component only mounts if it suspended inside a non-
            // concurrent tree, in an inconsistent state. We want to treat it like
            // a new mount, even though an empty version of it already committed.
            // Disconnect the alternate pointers.
            _current.alternate = null;
            workInProgress.alternate = null; // Since this is conceptually a new fiber, schedule a Placement effect
            workInProgress.flags |= Placement;
        } // Promote the fiber to a class and try rendering again.
        workInProgress.tag = ClassComponent; // The rest of this function is a fork of `updateClassComponent`
        // Push context providers early to prevent context stack mismatches.
        // During mounting we don't know the child context yet as the instance doesn't exist.
        // We will invalidate the child context in finishClassComponent() right after rendering.
        var hasContext;
        if (isContextProvider(Component)) {
            hasContext = true;
            pushContextProvider(workInProgress);
        } else hasContext = false;
        prepareToReadContext(workInProgress, renderLanes);
        constructClassInstance(workInProgress, Component, nextProps);
        mountClassInstance(workInProgress, Component, nextProps, renderLanes);
        return finishClassComponent(null, workInProgress, Component, true, hasContext, renderLanes);
    }
    function mountIndeterminateComponent(_current, workInProgress, Component, renderLanes) {
        if (_current !== null) {
            // An indeterminate component only mounts if it suspended inside a non-
            // concurrent tree, in an inconsistent state. We want to treat it like
            // a new mount, even though an empty version of it already committed.
            // Disconnect the alternate pointers.
            _current.alternate = null;
            workInProgress.alternate = null; // Since this is conceptually a new fiber, schedule a Placement effect
            workInProgress.flags |= Placement;
        }
        var props = workInProgress.pendingProps;
        var context;
        var unmaskedContext = getUnmaskedContext(workInProgress, Component, false);
        context = getMaskedContext(workInProgress, unmaskedContext);
        prepareToReadContext(workInProgress, renderLanes);
        var value;
        var hasId;
        markComponentRenderStarted(workInProgress);
        if (Component.prototype && typeof Component.prototype.render === 'function') {
            var componentName = getComponentNameFromType(Component) || 'Unknown';
            if (!didWarnAboutBadClass[componentName]) {
                error1("The <%s /> component appears to have a render method, but doesn't extend React.Component. This is likely to cause errors. Change %s to extend React.Component instead.", componentName, componentName);
                didWarnAboutBadClass[componentName] = true;
            }
        }
        if (workInProgress.mode & StrictLegacyMode) ReactStrictModeWarnings.recordLegacyContextWarning(workInProgress, null);
        setIsRendering(true);
        ReactCurrentOwner$1.current = workInProgress;
        value = renderWithHooks(null, workInProgress, Component, props, context, renderLanes);
        hasId = checkDidRenderIdHook();
        setIsRendering(false);
        markComponentRenderStopped();
        workInProgress.flags |= PerformedWork;
        // Support for module components is deprecated and is removed behind a flag.
        // Whether or not it would crash later, we want to show a good message in DEV first.
        if (typeof value === 'object' && value !== null && typeof value.render === 'function' && value.$$typeof === undefined) {
            var _componentName = getComponentNameFromType(Component) || 'Unknown';
            if (!didWarnAboutModulePatternComponent[_componentName]) {
                error1("The <%s /> component appears to be a function component that returns a class instance. Change %s to a class that extends React.Component instead. If you can't use a class try assigning the prototype on the function as a workaround. `%s.prototype = React.Component.prototype`. Don't use an arrow function since it cannot be called with `new` by React.", _componentName, _componentName, _componentName);
                didWarnAboutModulePatternComponent[_componentName] = true;
            }
        }
        if (// Eventually we'll delete this branch altogether.
        typeof value === 'object' && value !== null && typeof value.render === 'function' && value.$$typeof === undefined) {
            var _componentName2 = getComponentNameFromType(Component) || 'Unknown';
            if (!didWarnAboutModulePatternComponent[_componentName2]) {
                error1("The <%s /> component appears to be a function component that returns a class instance. Change %s to a class that extends React.Component instead. If you can't use a class try assigning the prototype on the function as a workaround. `%s.prototype = React.Component.prototype`. Don't use an arrow function since it cannot be called with `new` by React.", _componentName2, _componentName2, _componentName2);
                didWarnAboutModulePatternComponent[_componentName2] = true;
            }
            workInProgress.tag = ClassComponent; // Throw out any hooks that were used.
            workInProgress.memoizedState = null;
            workInProgress.updateQueue = null; // Push context providers early to prevent context stack mismatches.
            // During mounting we don't know the child context yet as the instance doesn't exist.
            // We will invalidate the child context in finishClassComponent() right after rendering.
            var hasContext = false;
            if (isContextProvider(Component)) {
                hasContext = true;
                pushContextProvider(workInProgress);
            } else hasContext = false;
            workInProgress.memoizedState = value.state !== null && value.state !== undefined ? value.state : null;
            initializeUpdateQueue(workInProgress);
            adoptClassInstance(workInProgress, value);
            mountClassInstance(workInProgress, Component, props, renderLanes);
            return finishClassComponent(null, workInProgress, Component, true, hasContext, renderLanes);
        } else {
            // Proceed under the assumption that this is a function component
            workInProgress.tag = FunctionComponent;
            if (workInProgress.mode & StrictLegacyMode) {
                setIsStrictModeForDevtools(true);
                try {
                    value = renderWithHooks(null, workInProgress, Component, props, context, renderLanes);
                    hasId = checkDidRenderIdHook();
                } finally{
                    setIsStrictModeForDevtools(false);
                }
            }
            if (getIsHydrating() && hasId) pushMaterializedTreeId(workInProgress);
            reconcileChildren(null, workInProgress, value, renderLanes);
            validateFunctionComponentInDev(workInProgress, Component);
            return workInProgress.child;
        }
    }
    function validateFunctionComponentInDev(workInProgress, Component) {
        if (Component) {
            if (Component.childContextTypes) error1('%s(...): childContextTypes cannot be defined on a function component.', Component.displayName || Component.name || 'Component');
        }
        if (workInProgress.ref !== null) {
            var info = '';
            var ownerName = getCurrentFiberOwnerNameInDevOrNull();
            if (ownerName) info += '\n\nCheck the render method of `' + ownerName + '`.';
            var warningKey = ownerName || '';
            var debugSource = workInProgress._debugSource;
            if (debugSource) warningKey = debugSource.fileName + ':' + debugSource.lineNumber;
            if (!didWarnAboutFunctionRefs[warningKey]) {
                didWarnAboutFunctionRefs[warningKey] = true;
                error1("Function components cannot be given refs. Attempts to access this ref will fail. Did you mean to use React.forwardRef()?%s", info);
            }
        }
        if (typeof Component.getDerivedStateFromProps === 'function') {
            var _componentName3 = getComponentNameFromType(Component) || 'Unknown';
            if (!didWarnAboutGetDerivedStateOnFunctionComponent[_componentName3]) {
                error1('%s: Function components do not support getDerivedStateFromProps.', _componentName3);
                didWarnAboutGetDerivedStateOnFunctionComponent[_componentName3] = true;
            }
        }
        if (typeof Component.contextType === 'object' && Component.contextType !== null) {
            var _componentName4 = getComponentNameFromType(Component) || 'Unknown';
            if (!didWarnAboutContextTypeOnFunctionComponent[_componentName4]) {
                error1('%s: Function components do not support contextType.', _componentName4);
                didWarnAboutContextTypeOnFunctionComponent[_componentName4] = true;
            }
        }
    }
    var SUSPENDED_MARKER = {
        dehydrated: null,
        treeContext: null,
        retryLane: NoLane
    };
    function mountSuspenseOffscreenState(renderLanes) {
        return {
            baseLanes: renderLanes,
            cachePool: getSuspendedCache(),
            transitions: null
        };
    }
    function updateSuspenseOffscreenState(prevOffscreenState, renderLanes) {
        var cachePool = null;
        return {
            baseLanes: mergeLanes(prevOffscreenState.baseLanes, renderLanes),
            cachePool: cachePool,
            transitions: prevOffscreenState.transitions
        };
    } // TODO: Probably should inline this back
    function shouldRemainOnFallback(suspenseContext, current, workInProgress, renderLanes) {
        // If we're already showing a fallback, there are cases where we need to
        // remain on that fallback regardless of whether the content has resolved.
        // For example, SuspenseList coordinates when nested content appears.
        if (current !== null) {
            var suspenseState = current.memoizedState;
            if (suspenseState === null) // Currently showing content. Don't hide it, even if ForceSuspenseFallback
            // is true. More precise name might be "ForceRemainSuspenseFallback".
            // Note: This is a factoring smell. Can't remain on a fallback if there's
            // no fallback to remain on.
            return false;
        } // Not currently showing content. Consult the Suspense context.
        return hasSuspenseContext(suspenseContext, ForceSuspenseFallback);
    }
    function getRemainingWorkInPrimaryTree(current, renderLanes) {
        // TODO: Should not remove render lanes that were pinged during this render
        return removeLanes(current.childLanes, renderLanes);
    }
    function updateSuspenseComponent(current, workInProgress, renderLanes) {
        var nextProps = workInProgress.pendingProps; // This is used by DevTools to force a boundary to suspend.
        if (shouldSuspend(workInProgress)) workInProgress.flags |= DidCapture;
        var suspenseContext = suspenseStackCursor.current;
        var showFallback = false;
        var didSuspend = (workInProgress.flags & DidCapture) !== NoFlags;
        if (didSuspend || shouldRemainOnFallback(suspenseContext, current)) {
            // Something in this boundary's subtree already suspended. Switch to
            // rendering the fallback children.
            showFallback = true;
            workInProgress.flags &= ~DidCapture;
        } else // Attempting the main content
        if (current === null || current.memoizedState !== null) suspenseContext = addSubtreeSuspenseContext(suspenseContext, InvisibleParentSuspenseContext);
        suspenseContext = setDefaultShallowSuspenseContext(suspenseContext);
        pushSuspenseContext(workInProgress, suspenseContext); // OK, the next part is confusing. We're about to reconcile the Suspense
        // boundary's children. This involves some custom reconciliation logic. Two
        // main reasons this is so complicated.
        //
        // First, Legacy Mode has different semantics for backwards compatibility. The
        // primary tree will commit in an inconsistent state, so when we do the
        // second pass to render the fallback, we do some exceedingly, uh, clever
        // hacks to make that not totally break. Like transferring effects and
        // deletions from hidden tree. In Concurrent Mode, it's much simpler,
        // because we bailout on the primary tree completely and leave it in its old
        // state, no effects. Same as what we do for Offscreen (except that
        // Offscreen doesn't have the first render pass).
        //
        // Second is hydration. During hydration, the Suspense fiber has a slightly
        // different layout, where the child points to a dehydrated fragment, which
        // contains the DOM rendered by the server.
        //
        // Third, even if you set all that aside, Suspense is like error boundaries in
        // that we first we try to render one tree, and if that fails, we render again
        // and switch to a different tree. Like a try/catch block. So we have to track
        // which branch we're currently rendering. Ideally we would model this using
        // a stack.
        if (current === null) {
            // Initial mount
            // If we're currently hydrating, try to hydrate this boundary.
            tryToClaimNextHydratableInstance(workInProgress); // This could've been a dehydrated suspense component.
            var suspenseState = workInProgress.memoizedState;
            if (suspenseState !== null) {
                var dehydrated = suspenseState.dehydrated;
                if (dehydrated !== null) return mountDehydratedSuspenseComponent(workInProgress, dehydrated);
            }
            var nextPrimaryChildren = nextProps.children;
            var nextFallbackChildren = nextProps.fallback;
            if (showFallback) {
                var fallbackFragment = mountSuspenseFallbackChildren(workInProgress, nextPrimaryChildren, nextFallbackChildren, renderLanes);
                var primaryChildFragment = workInProgress.child;
                primaryChildFragment.memoizedState = mountSuspenseOffscreenState(renderLanes);
                workInProgress.memoizedState = SUSPENDED_MARKER;
                return fallbackFragment;
            } else return mountSuspensePrimaryChildren(workInProgress, nextPrimaryChildren);
        } else {
            // This is an update.
            // If the current fiber has a SuspenseState, that means it's already showing
            // a fallback.
            var prevState = current.memoizedState;
            if (prevState !== null) {
                // The current tree is already showing a fallback
                // Special path for hydration
                var _dehydrated = prevState.dehydrated;
                if (_dehydrated !== null) {
                    if (!didSuspend) return updateDehydratedSuspenseComponent(current, workInProgress, _dehydrated, prevState, renderLanes);
                    else if (workInProgress.flags & ForceClientRender) {
                        // Something errored during hydration. Try again without hydrating.
                        workInProgress.flags &= ~ForceClientRender;
                        return retrySuspenseComponentWithoutHydrating(current, workInProgress, renderLanes, new Error("There was an error while hydrating this Suspense boundary. Switched to client rendering."));
                    } else if (workInProgress.memoizedState !== null) {
                        // Something suspended and we should still be in dehydrated mode.
                        // Leave the existing child in place.
                        workInProgress.child = current.child; // The dehydrated completion pass expects this flag to be there
                        // but the normal suspense pass doesn't.
                        workInProgress.flags |= DidCapture;
                        return null;
                    } else {
                        // Suspended but we should no longer be in dehydrated mode.
                        // Therefore we now have to render the fallback.
                        var _nextPrimaryChildren = nextProps.children;
                        var _nextFallbackChildren = nextProps.fallback;
                        var fallbackChildFragment = mountSuspenseFallbackAfterRetryWithoutHydrating(current, workInProgress, _nextPrimaryChildren, _nextFallbackChildren, renderLanes);
                        var _primaryChildFragment2 = workInProgress.child;
                        _primaryChildFragment2.memoizedState = mountSuspenseOffscreenState(renderLanes);
                        workInProgress.memoizedState = SUSPENDED_MARKER;
                        return fallbackChildFragment;
                    }
                }
                if (showFallback) {
                    var _nextFallbackChildren2 = nextProps.fallback;
                    var _nextPrimaryChildren2 = nextProps.children;
                    var _fallbackChildFragment = updateSuspenseFallbackChildren(current, workInProgress, _nextPrimaryChildren2, _nextFallbackChildren2, renderLanes);
                    var _primaryChildFragment3 = workInProgress.child;
                    var prevOffscreenState = current.child.memoizedState;
                    _primaryChildFragment3.memoizedState = prevOffscreenState === null ? mountSuspenseOffscreenState(renderLanes) : updateSuspenseOffscreenState(prevOffscreenState, renderLanes);
                    _primaryChildFragment3.childLanes = getRemainingWorkInPrimaryTree(current, renderLanes);
                    workInProgress.memoizedState = SUSPENDED_MARKER;
                    return _fallbackChildFragment;
                } else {
                    var _nextPrimaryChildren3 = nextProps.children;
                    var _primaryChildFragment4 = updateSuspensePrimaryChildren(current, workInProgress, _nextPrimaryChildren3, renderLanes);
                    workInProgress.memoizedState = null;
                    return _primaryChildFragment4;
                }
            } else // The current tree is not already showing a fallback.
            if (showFallback) {
                // Timed out.
                var _nextFallbackChildren3 = nextProps.fallback;
                var _nextPrimaryChildren4 = nextProps.children;
                var _fallbackChildFragment2 = updateSuspenseFallbackChildren(current, workInProgress, _nextPrimaryChildren4, _nextFallbackChildren3, renderLanes);
                var _primaryChildFragment5 = workInProgress.child;
                var _prevOffscreenState = current.child.memoizedState;
                _primaryChildFragment5.memoizedState = _prevOffscreenState === null ? mountSuspenseOffscreenState(renderLanes) : updateSuspenseOffscreenState(_prevOffscreenState, renderLanes);
                _primaryChildFragment5.childLanes = getRemainingWorkInPrimaryTree(current, renderLanes);
                // fallback children.
                workInProgress.memoizedState = SUSPENDED_MARKER;
                return _fallbackChildFragment2;
            } else {
                // Still haven't timed out. Continue rendering the children, like we
                // normally do.
                var _nextPrimaryChildren5 = nextProps.children;
                var _primaryChildFragment6 = updateSuspensePrimaryChildren(current, workInProgress, _nextPrimaryChildren5, renderLanes);
                workInProgress.memoizedState = null;
                return _primaryChildFragment6;
            }
        }
    }
    function mountSuspensePrimaryChildren(workInProgress, primaryChildren, renderLanes) {
        var mode = workInProgress.mode;
        var primaryChildProps = {
            mode: 'visible',
            children: primaryChildren
        };
        var primaryChildFragment = mountWorkInProgressOffscreenFiber(primaryChildProps, mode);
        primaryChildFragment.return = workInProgress;
        workInProgress.child = primaryChildFragment;
        return primaryChildFragment;
    }
    function mountSuspenseFallbackChildren(workInProgress, primaryChildren, fallbackChildren, renderLanes) {
        var mode = workInProgress.mode;
        var progressedPrimaryFragment = workInProgress.child;
        var primaryChildProps = {
            mode: 'hidden',
            children: primaryChildren
        };
        var primaryChildFragment;
        var fallbackChildFragment;
        if ((mode & ConcurrentMode) === NoMode && progressedPrimaryFragment !== null) {
            // In legacy mode, we commit the primary tree as if it successfully
            // completed, even though it's in an inconsistent state.
            primaryChildFragment = progressedPrimaryFragment;
            primaryChildFragment.childLanes = NoLanes;
            primaryChildFragment.pendingProps = primaryChildProps;
            if (workInProgress.mode & ProfileMode) {
                // Reset the durations from the first pass so they aren't included in the
                // final amounts. This seems counterintuitive, since we're intentionally
                // not measuring part of the render phase, but this makes it match what we
                // do in Concurrent Mode.
                primaryChildFragment.actualDuration = 0;
                primaryChildFragment.actualStartTime = -1;
                primaryChildFragment.selfBaseDuration = 0;
                primaryChildFragment.treeBaseDuration = 0;
            }
            fallbackChildFragment = createFiberFromFragment(fallbackChildren, mode, renderLanes, null);
        } else {
            primaryChildFragment = mountWorkInProgressOffscreenFiber(primaryChildProps, mode);
            fallbackChildFragment = createFiberFromFragment(fallbackChildren, mode, renderLanes, null);
        }
        primaryChildFragment.return = workInProgress;
        fallbackChildFragment.return = workInProgress;
        primaryChildFragment.sibling = fallbackChildFragment;
        workInProgress.child = primaryChildFragment;
        return fallbackChildFragment;
    }
    function mountWorkInProgressOffscreenFiber(offscreenProps, mode, renderLanes) {
        // The props argument to `createFiberFromOffscreen` is `any` typed, so we use
        // this wrapper function to constrain it.
        return createFiberFromOffscreen(offscreenProps, mode, NoLanes, null);
    }
    function updateWorkInProgressOffscreenFiber(current, offscreenProps) {
        // The props argument to `createWorkInProgress` is `any` typed, so we use this
        // wrapper function to constrain it.
        return createWorkInProgress(current, offscreenProps);
    }
    function updateSuspensePrimaryChildren(current, workInProgress, primaryChildren, renderLanes) {
        var currentPrimaryChildFragment = current.child;
        var currentFallbackChildFragment = currentPrimaryChildFragment.sibling;
        var primaryChildFragment = updateWorkInProgressOffscreenFiber(currentPrimaryChildFragment, {
            mode: 'visible',
            children: primaryChildren
        });
        if ((workInProgress.mode & ConcurrentMode) === NoMode) primaryChildFragment.lanes = renderLanes;
        primaryChildFragment.return = workInProgress;
        primaryChildFragment.sibling = null;
        if (currentFallbackChildFragment !== null) {
            // Delete the fallback child fragment
            var deletions = workInProgress.deletions;
            if (deletions === null) {
                workInProgress.deletions = [
                    currentFallbackChildFragment
                ];
                workInProgress.flags |= ChildDeletion;
            } else deletions.push(currentFallbackChildFragment);
        }
        workInProgress.child = primaryChildFragment;
        return primaryChildFragment;
    }
    function updateSuspenseFallbackChildren(current, workInProgress, primaryChildren, fallbackChildren, renderLanes) {
        var mode = workInProgress.mode;
        var currentPrimaryChildFragment = current.child;
        var currentFallbackChildFragment = currentPrimaryChildFragment.sibling;
        var primaryChildProps = {
            mode: 'hidden',
            children: primaryChildren
        };
        var primaryChildFragment;
        if (// completed, even though it's in an inconsistent state.
        (mode & ConcurrentMode) === NoMode && // already cloned. In legacy mode, the only case where this isn't true is
        // when DevTools forces us to display a fallback; we skip the first render
        // pass entirely and go straight to rendering the fallback. (In Concurrent
        // Mode, SuspenseList can also trigger this scenario, but this is a legacy-
        // only codepath.)
        workInProgress.child !== currentPrimaryChildFragment) {
            var progressedPrimaryFragment = workInProgress.child;
            primaryChildFragment = progressedPrimaryFragment;
            primaryChildFragment.childLanes = NoLanes;
            primaryChildFragment.pendingProps = primaryChildProps;
            if (workInProgress.mode & ProfileMode) {
                // Reset the durations from the first pass so they aren't included in the
                // final amounts. This seems counterintuitive, since we're intentionally
                // not measuring part of the render phase, but this makes it match what we
                // do in Concurrent Mode.
                primaryChildFragment.actualDuration = 0;
                primaryChildFragment.actualStartTime = -1;
                primaryChildFragment.selfBaseDuration = currentPrimaryChildFragment.selfBaseDuration;
                primaryChildFragment.treeBaseDuration = currentPrimaryChildFragment.treeBaseDuration;
            }
            // However, since we're going to remain on the fallback, we no longer want
            // to delete it.
            workInProgress.deletions = null;
        } else {
            primaryChildFragment = updateWorkInProgressOffscreenFiber(currentPrimaryChildFragment, primaryChildProps);
            // (We don't do this in legacy mode, because in legacy mode we don't re-use
            // the current tree; see previous branch.)
            primaryChildFragment.subtreeFlags = currentPrimaryChildFragment.subtreeFlags & StaticMask;
        }
        var fallbackChildFragment;
        if (currentFallbackChildFragment !== null) fallbackChildFragment = createWorkInProgress(currentFallbackChildFragment, fallbackChildren);
        else {
            fallbackChildFragment = createFiberFromFragment(fallbackChildren, mode, renderLanes, null); // Needs a placement effect because the parent (the Suspense boundary) already
            // mounted but this is a new fiber.
            fallbackChildFragment.flags |= Placement;
        }
        fallbackChildFragment.return = workInProgress;
        primaryChildFragment.return = workInProgress;
        primaryChildFragment.sibling = fallbackChildFragment;
        workInProgress.child = primaryChildFragment;
        return fallbackChildFragment;
    }
    function retrySuspenseComponentWithoutHydrating(current, workInProgress, renderLanes, recoverableError) {
        // Falling back to client rendering. Because this has performance
        // implications, it's considered a recoverable error, even though the user
        // likely won't observe anything wrong with the UI.
        //
        // The error is passed in as an argument to enforce that every caller provide
        // a custom message, or explicitly opt out (currently the only path that opts
        // out is legacy mode; every concurrent path provides an error).
        if (recoverableError !== null) queueHydrationError(recoverableError);
         // This will add the old fiber to the deletion list
        reconcileChildFibers1(workInProgress, current.child, null, renderLanes); // We're now not suspended nor dehydrated.
        var nextProps = workInProgress.pendingProps;
        var primaryChildren = nextProps.children;
        var primaryChildFragment = mountSuspensePrimaryChildren(workInProgress, primaryChildren); // Needs a placement effect because the parent (the Suspense boundary) already
        // mounted but this is a new fiber.
        primaryChildFragment.flags |= Placement;
        workInProgress.memoizedState = null;
        return primaryChildFragment;
    }
    function mountSuspenseFallbackAfterRetryWithoutHydrating(current, workInProgress, primaryChildren, fallbackChildren, renderLanes) {
        var fiberMode = workInProgress.mode;
        var primaryChildProps = {
            mode: 'visible',
            children: primaryChildren
        };
        var primaryChildFragment = mountWorkInProgressOffscreenFiber(primaryChildProps, fiberMode);
        var fallbackChildFragment = createFiberFromFragment(fallbackChildren, fiberMode, renderLanes, null); // Needs a placement effect because the parent (the Suspense
        // boundary) already mounted but this is a new fiber.
        fallbackChildFragment.flags |= Placement;
        primaryChildFragment.return = workInProgress;
        fallbackChildFragment.return = workInProgress;
        primaryChildFragment.sibling = fallbackChildFragment;
        workInProgress.child = primaryChildFragment;
        if ((workInProgress.mode & ConcurrentMode) !== NoMode) // We will have dropped the effect list which contains the
        // deletion. We need to reconcile to delete the current child.
        reconcileChildFibers1(workInProgress, current.child, null, renderLanes);
        return fallbackChildFragment;
    }
    function mountDehydratedSuspenseComponent(workInProgress, suspenseInstance, renderLanes) {
        // During the first pass, we'll bail out and not drill into the children.
        // Instead, we'll leave the content in place and try to hydrate it later.
        if ((workInProgress.mode & ConcurrentMode) === NoMode) {
            error1("Cannot hydrate Suspense in legacy mode. Switch from ReactDOM.hydrate(element, container) to ReactDOMClient.hydrateRoot(container, <App />).render(element) or remove the Suspense components from the server rendered components.");
            workInProgress.lanes = laneToLanes(SyncLane);
        } else if (isSuspenseInstanceFallback(suspenseInstance)) // This is a client-only boundary. Since we won't get any content from the server
        // for this, we need to schedule that at a higher priority based on when it would
        // have timed out. In theory we could render it in this pass but it would have the
        // wrong priority associated with it and will prevent hydration of parent path.
        // Instead, we'll leave work left on it to render it in a separate commit.
        // TODO This time should be the time at which the server rendered response that is
        // a parent to this boundary was displayed. However, since we currently don't have
        // a protocol to transfer that time, we'll just estimate it by using the current
        // time. This will mean that Suspense timeouts are slightly shifted to later than
        // they should be.
        // Schedule a normal pri update to render this content.
        workInProgress.lanes = laneToLanes(DefaultHydrationLane);
        else // We'll continue hydrating the rest at offscreen priority since we'll already
        // be showing the right content coming from the server, it is no rush.
        workInProgress.lanes = laneToLanes(OffscreenLane);
        return null;
    }
    function updateDehydratedSuspenseComponent(current, workInProgress, suspenseInstance, suspenseState, renderLanes) {
        // We should never be hydrating at this point because it is the first pass,
        // but after we've already committed once.
        warnIfHydrating();
        if ((workInProgress.mode & ConcurrentMode) === NoMode) return retrySuspenseComponentWithoutHydrating(current, workInProgress, renderLanes, // required — every concurrent mode path that causes hydration to
        // de-opt to client rendering should have an error message.
        null);
        if (isSuspenseInstanceFallback(suspenseInstance)) // This boundary is in a permanent fallback state. In this case, we'll never
        // get an update and we'll never be able to hydrate the final content. Let's just try the
        // client side render instead.
        return retrySuspenseComponentWithoutHydrating(current, workInProgress, renderLanes, // here on the client. Or, in production, a hash/id that corresponds to
        // the error.
        new Error("The server could not finish this Suspense boundary, likely due to an error during server rendering. Switched to client rendering."));
        // any context has changed, we need to treat is as if the input might have changed.
        var hasContextChanged = includesSomeLane(renderLanes, current.childLanes);
        if (didReceiveUpdate || hasContextChanged) {
            // This boundary has changed since the first render. This means that we are now unable to
            // hydrate it. We might still be able to hydrate it using a higher priority lane.
            var root = getWorkInProgressRoot();
            if (root !== null) {
                var attemptHydrationAtLane = getBumpedLaneForHydration(root, renderLanes);
                if (attemptHydrationAtLane !== NoLane && attemptHydrationAtLane !== suspenseState.retryLane) {
                    // Intentionally mutating since this render will get interrupted. This
                    // is one of the very rare times where we mutate the current tree
                    // during the render phase.
                    suspenseState.retryLane = attemptHydrationAtLane; // TODO: Ideally this would inherit the event time of the current render
                    var eventTime = NoTimestamp;
                    scheduleUpdateOnFiber(current, attemptHydrationAtLane, eventTime);
                }
            } // If we have scheduled higher pri work above, this will probably just abort the render
            // since we now have higher priority work, but in case it doesn't, we need to prepare to
            // render something, if we time out. Even if that requires us to delete everything and
            // skip hydration.
            // Delay having to do this as long as the suspense timeout allows us.
            renderDidSuspendDelayIfPossible();
            return retrySuspenseComponentWithoutHydrating(current, workInProgress, renderLanes, new Error("This Suspense boundary received an update before it finished hydrating. This caused the boundary to switch to client rendering. The usual way to fix this is to wrap the original update in startTransition."));
        } else if (isSuspenseInstancePending(suspenseInstance)) {
            // This component is still pending more data from the server, so we can't hydrate its
            // content. We treat it as if this component suspended itself. It might seem as if
            // we could just try to render it client-side instead. However, this will perform a
            // lot of unnecessary work and is unlikely to complete since it often will suspend
            // on missing data anyway. Additionally, the server might be able to render more
            // than we can on the client yet. In that case we'd end up with more fallback states
            // on the client than if we just leave it alone. If the server times out or errors
            // these should update this boundary to the permanent Fallback state instead.
            // Mark it as having captured (i.e. suspended).
            workInProgress.flags |= DidCapture; // Leave the child in place. I.e. the dehydrated fragment.
            workInProgress.child = current.child; // Register a callback to retry this boundary once the server has sent the result.
            var retry = retryDehydratedSuspenseBoundary.bind(null, current);
            registerSuspenseInstanceRetry(suspenseInstance, retry);
            return null;
        } else {
            // This is the first attempt.
            reenterHydrationStateFromDehydratedSuspenseInstance(workInProgress, suspenseInstance, suspenseState.treeContext);
            var nextProps = workInProgress.pendingProps;
            var primaryChildren = nextProps.children;
            var primaryChildFragment = mountSuspensePrimaryChildren(workInProgress, primaryChildren); // Mark the children as hydrating. This is a fast path to know whether this
            // tree is part of a hydrating tree. This is used to determine if a child
            // node has fully mounted yet, and for scheduling event replaying.
            // Conceptually this is similar to Placement in that a new subtree is
            // inserted into the React tree here. It just happens to not need DOM
            // mutations because it already exists.
            primaryChildFragment.flags |= Hydrating;
            return primaryChildFragment;
        }
    }
    function scheduleSuspenseWorkOnFiber(fiber, renderLanes, propagationRoot) {
        fiber.lanes = mergeLanes(fiber.lanes, renderLanes);
        var alternate = fiber.alternate;
        if (alternate !== null) alternate.lanes = mergeLanes(alternate.lanes, renderLanes);
        scheduleContextWorkOnParentPath(fiber.return, renderLanes, propagationRoot);
    }
    function propagateSuspenseContextChange(workInProgress, firstChild, renderLanes) {
        // Mark any Suspense boundaries with fallbacks as having work to do.
        // If they were previously forced into fallbacks, they may now be able
        // to unblock.
        var node = firstChild;
        while(node !== null){
            if (node.tag === SuspenseComponent) {
                var state = node.memoizedState;
                if (state !== null) scheduleSuspenseWorkOnFiber(node, renderLanes, workInProgress);
            } else if (node.tag === SuspenseListComponent) // If the tail is hidden there might not be an Suspense boundaries
            // to schedule work on. In this case we have to schedule it on the
            // list itself.
            // We don't have to traverse to the children of the list since
            // the list will propagate the change when it rerenders.
            scheduleSuspenseWorkOnFiber(node, renderLanes, workInProgress);
            else if (node.child !== null) {
                node.child.return = node;
                node = node.child;
                continue;
            }
            if (node === workInProgress) return;
            while(node.sibling === null){
                if (node.return === null || node.return === workInProgress) return;
                node = node.return;
            }
            node.sibling.return = node.return;
            node = node.sibling;
        }
    }
    function findLastContentRow(firstChild) {
        // This is going to find the last row among these children that is already
        // showing content on the screen, as opposed to being in fallback state or
        // new. If a row has multiple Suspense boundaries, any of them being in the
        // fallback state, counts as the whole row being in a fallback state.
        // Note that the "rows" will be workInProgress, but any nested children
        // will still be current since we haven't rendered them yet. The mounted
        // order may not be the same as the new order. We use the new order.
        var row = firstChild;
        var lastContentRow = null;
        while(row !== null){
            var currentRow = row.alternate; // New rows can't be content rows.
            if (currentRow !== null && findFirstSuspended(currentRow) === null) lastContentRow = row;
            row = row.sibling;
        }
        return lastContentRow;
    }
    function validateRevealOrder(revealOrder) {
        if (revealOrder !== undefined && revealOrder !== 'forwards' && revealOrder !== 'backwards' && revealOrder !== 'together' && !didWarnAboutRevealOrder[revealOrder]) {
            didWarnAboutRevealOrder[revealOrder] = true;
            if (typeof revealOrder === 'string') switch(revealOrder.toLowerCase()){
                case 'together':
                case 'forwards':
                case 'backwards':
                    error1('"%s" is not a valid value for revealOrder on <SuspenseList />. Use lowercase "%s" instead.', revealOrder, revealOrder.toLowerCase());
                    break;
                case 'forward':
                case 'backward':
                    error1('"%s" is not a valid value for revealOrder on <SuspenseList />. React uses the -s suffix in the spelling. Use "%ss" instead.', revealOrder, revealOrder.toLowerCase());
                    break;
                default:
                    error1('"%s" is not a supported revealOrder on <SuspenseList />. Did you mean "together", "forwards" or "backwards"?', revealOrder);
                    break;
            }
            else error1('%s is not a supported value for revealOrder on <SuspenseList />. Did you mean "together", "forwards" or "backwards"?', revealOrder);
        }
    }
    function validateTailOptions(tailMode, revealOrder) {
        if (tailMode !== undefined && !didWarnAboutTailOptions[tailMode]) {
            if (tailMode !== 'collapsed' && tailMode !== 'hidden') {
                didWarnAboutTailOptions[tailMode] = true;
                error1('"%s" is not a supported value for tail on <SuspenseList />. Did you mean "collapsed" or "hidden"?', tailMode);
            } else if (revealOrder !== 'forwards' && revealOrder !== 'backwards') {
                didWarnAboutTailOptions[tailMode] = true;
                error1('<SuspenseList tail="%s" /> is only valid if revealOrder is "forwards" or "backwards". Did you mean to specify revealOrder="forwards"?', tailMode);
            }
        }
    }
    function validateSuspenseListNestedChild(childSlot, index) {
        var isAnArray = isArray(childSlot);
        var isIterable = !isAnArray && typeof getIteratorFn(childSlot) === 'function';
        if (isAnArray || isIterable) {
            var type = isAnArray ? 'array' : 'iterable';
            error1("A nested %s was passed to row #%s in <SuspenseList />. Wrap it in an additional SuspenseList to configure its revealOrder: <SuspenseList revealOrder=...> ... <SuspenseList revealOrder=...>{%s}</SuspenseList> ... </SuspenseList>", type, index, type);
            return false;
        }
        return true;
    }
    function validateSuspenseListChildren(children, revealOrder) {
        if ((revealOrder === 'forwards' || revealOrder === 'backwards') && children !== undefined && children !== null && children !== false) {
            if (isArray(children)) for(var i = 0; i < children.length; i++){
                if (!validateSuspenseListNestedChild(children[i], i)) return;
            }
            else {
                var iteratorFn = getIteratorFn(children);
                if (typeof iteratorFn === 'function') {
                    var childrenIterator = iteratorFn.call(children);
                    if (childrenIterator) {
                        var step = childrenIterator.next();
                        var _i = 0;
                        for(; !step.done; step = childrenIterator.next()){
                            if (!validateSuspenseListNestedChild(step.value, _i)) return;
                            _i++;
                        }
                    }
                } else error1('A single row was passed to a <SuspenseList revealOrder="%s" />. This is not useful since it needs multiple rows. Did you mean to pass multiple children or an array?', revealOrder);
            }
        }
    }
    function initSuspenseListRenderState(workInProgress, isBackwards, tail, lastContentRow, tailMode) {
        var renderState = workInProgress.memoizedState;
        if (renderState === null) workInProgress.memoizedState = {
            isBackwards: isBackwards,
            rendering: null,
            renderingStartTime: 0,
            last: lastContentRow,
            tail: tail,
            tailMode: tailMode
        };
        else {
            // We can reuse the existing object from previous renders.
            renderState.isBackwards = isBackwards;
            renderState.rendering = null;
            renderState.renderingStartTime = 0;
            renderState.last = lastContentRow;
            renderState.tail = tail;
            renderState.tailMode = tailMode;
        }
    } // This can end up rendering this component multiple passes.
    // The first pass splits the children fibers into two sets. A head and tail.
    // We first render the head. If anything is in fallback state, we do another
    // pass through beginWork to rerender all children (including the tail) with
    // the force suspend context. If the first render didn't have anything in
    // in fallback state. Then we render each row in the tail one-by-one.
    // That happens in the completeWork phase without going back to beginWork.
    function updateSuspenseListComponent(current, workInProgress, renderLanes) {
        var nextProps = workInProgress.pendingProps;
        var revealOrder = nextProps.revealOrder;
        var tailMode = nextProps.tail;
        var newChildren = nextProps.children;
        validateRevealOrder(revealOrder);
        validateTailOptions(tailMode, revealOrder);
        validateSuspenseListChildren(newChildren, revealOrder);
        reconcileChildren(current, workInProgress, newChildren, renderLanes);
        var suspenseContext = suspenseStackCursor.current;
        var shouldForceFallback = hasSuspenseContext(suspenseContext, ForceSuspenseFallback);
        if (shouldForceFallback) {
            suspenseContext = setShallowSuspenseContext(suspenseContext, ForceSuspenseFallback);
            workInProgress.flags |= DidCapture;
        } else {
            var didSuspendBefore = current !== null && (current.flags & DidCapture) !== NoFlags;
            if (didSuspendBefore) // If we previously forced a fallback, we need to schedule work
            // on any nested boundaries to let them know to try to render
            // again. This is the same as context updating.
            propagateSuspenseContextChange(workInProgress, workInProgress.child, renderLanes);
            suspenseContext = setDefaultShallowSuspenseContext(suspenseContext);
        }
        pushSuspenseContext(workInProgress, suspenseContext);
        if ((workInProgress.mode & ConcurrentMode) === NoMode) // In legacy mode, SuspenseList doesn't work so we just
        // use make it a noop by treating it as the default revealOrder.
        workInProgress.memoizedState = null;
        else switch(revealOrder){
            case 'forwards':
                var lastContentRow = findLastContentRow(workInProgress.child);
                var tail;
                if (lastContentRow === null) {
                    // The whole list is part of the tail.
                    // TODO: We could fast path by just rendering the tail now.
                    tail = workInProgress.child;
                    workInProgress.child = null;
                } else {
                    // Disconnect the tail rows after the content row.
                    // We're going to render them separately later.
                    tail = lastContentRow.sibling;
                    lastContentRow.sibling = null;
                }
                initSuspenseListRenderState(workInProgress, false, tail, lastContentRow, tailMode);
                break;
            case 'backwards':
                // We're going to find the first row that has existing content.
                // At the same time we're going to reverse the list of everything
                // we pass in the meantime. That's going to be our tail in reverse
                // order.
                var _tail = null;
                var row = workInProgress.child;
                workInProgress.child = null;
                while(row !== null){
                    var currentRow = row.alternate; // New rows can't be content rows.
                    if (currentRow !== null && findFirstSuspended(currentRow) === null) {
                        // This is the beginning of the main content.
                        workInProgress.child = row;
                        break;
                    }
                    var nextRow = row.sibling;
                    row.sibling = _tail;
                    _tail = row;
                    row = nextRow;
                } // TODO: If workInProgress.child is null, we can continue on the tail immediately.
                initSuspenseListRenderState(workInProgress, true, _tail, null, tailMode);
                break;
            case 'together':
                initSuspenseListRenderState(workInProgress, false, null, null, undefined);
                break;
            default:
                // The default reveal order is the same as not having
                // a boundary.
                workInProgress.memoizedState = null;
        }
        return workInProgress.child;
    }
    function updatePortalComponent(current, workInProgress, renderLanes) {
        pushHostContainer(workInProgress, workInProgress.stateNode.containerInfo);
        var nextChildren = workInProgress.pendingProps;
        if (current === null) // Portals are special because we don't append the children during mount
        // but at commit. Therefore we need to track insertions which the normal
        // flow doesn't do during mount. This doesn't happen at the root because
        // the root always starts with a "current" with a null child.
        // TODO: Consider unifying this with how the root works.
        workInProgress.child = reconcileChildFibers1(workInProgress, null, nextChildren, renderLanes);
        else reconcileChildren(current, workInProgress, nextChildren, renderLanes);
        return workInProgress.child;
    }
    var hasWarnedAboutUsingNoValuePropOnContextProvider = false;
    function updateContextProvider(current, workInProgress, renderLanes) {
        var providerType = workInProgress.type;
        var context = providerType._context;
        var newProps = workInProgress.pendingProps;
        var oldProps = workInProgress.memoizedProps;
        var newValue = newProps.value;
        if (!('value' in newProps)) {
            if (!hasWarnedAboutUsingNoValuePropOnContextProvider) {
                hasWarnedAboutUsingNoValuePropOnContextProvider = true;
                error1('The `value` prop is required for the `<Context.Provider>`. Did you misspell it or forget to pass it?');
            }
        }
        var providerPropTypes = workInProgress.type.propTypes;
        if (providerPropTypes) checkPropTypes(providerPropTypes, newProps, 'prop', 'Context.Provider');
        pushProvider(workInProgress, context, newValue);
        if (oldProps !== null) {
            var oldValue = oldProps.value;
            if (objectIs(oldValue, newValue)) {
                // No change. Bailout early if children are the same.
                if (oldProps.children === newProps.children && !hasContextChanged1()) return bailoutOnAlreadyFinishedWork(current, workInProgress, renderLanes);
            } else // The context value changed. Search for matching consumers and schedule
            // them to update.
            propagateContextChange(workInProgress, context, renderLanes);
        }
        var newChildren = newProps.children;
        reconcileChildren(current, workInProgress, newChildren, renderLanes);
        return workInProgress.child;
    }
    var hasWarnedAboutUsingContextAsConsumer = false;
    function updateContextConsumer(current, workInProgress, renderLanes) {
        var context = workInProgress.type; // The logic below for Context differs depending on PROD or DEV mode. In
        if (context._context === undefined) {
            // This may be because it's a Context (rather than a Consumer).
            // Or it may be because it's older React where they're the same thing.
            // We only want to warn if we're sure it's a new React.
            if (context !== context.Consumer) {
                if (!hasWarnedAboutUsingContextAsConsumer) {
                    hasWarnedAboutUsingContextAsConsumer = true;
                    error1("Rendering <Context> directly is not supported and will be removed in a future major release. Did you mean to render <Context.Consumer> instead?");
                }
            }
        } else context = context._context;
        var newProps = workInProgress.pendingProps;
        var render = newProps.children;
        if (typeof render !== 'function') error1("A context consumer was rendered with multiple children, or a child that isn't a function. A context consumer expects a single child that is a function. If you did pass a function, make sure there is no trailing or leading whitespace around it.");
        prepareToReadContext(workInProgress, renderLanes);
        var newValue = readContext(context);
        markComponentRenderStarted(workInProgress);
        var newChildren;
        ReactCurrentOwner$1.current = workInProgress;
        setIsRendering(true);
        newChildren = render(newValue);
        setIsRendering(false);
        markComponentRenderStopped();
        workInProgress.flags |= PerformedWork;
        reconcileChildren(current, workInProgress, newChildren, renderLanes);
        return workInProgress.child;
    }
    function markWorkInProgressReceivedUpdate() {
        didReceiveUpdate = true;
    }
    function bailoutOnAlreadyFinishedWork(current, workInProgress, renderLanes) {
        if (current !== null) // Reuse previous dependencies
        workInProgress.dependencies = current.dependencies;
        // Don't update "base" render times for bailouts.
        stopProfilerTimerIfRunning();
        markSkippedUpdateLanes(workInProgress.lanes); // Check if the children have any pending work.
        if (!includesSomeLane(renderLanes, workInProgress.childLanes)) return null;
         // This fiber doesn't have work, but its subtree does. Clone the child
        // fibers and continue.
        cloneChildFibers(current, workInProgress);
        return workInProgress.child;
    }
    function remountFiber(current, oldWorkInProgress, newWorkInProgress) {
        var returnFiber = oldWorkInProgress.return;
        if (returnFiber === null) // eslint-disable-next-line react-internal/prod-error-codes
        throw new Error('Cannot swap the root fiber.');
         // Disconnect from the old current.
        // It will get deleted.
        current.alternate = null;
        oldWorkInProgress.alternate = null; // Connect to the new tree.
        newWorkInProgress.index = oldWorkInProgress.index;
        newWorkInProgress.sibling = oldWorkInProgress.sibling;
        newWorkInProgress.return = oldWorkInProgress.return;
        newWorkInProgress.ref = oldWorkInProgress.ref; // Replace the child/sibling pointers above it.
        if (oldWorkInProgress === returnFiber.child) returnFiber.child = newWorkInProgress;
        else {
            var prevSibling = returnFiber.child;
            if (prevSibling === null) // eslint-disable-next-line react-internal/prod-error-codes
            throw new Error('Expected parent to have a child.');
            while(prevSibling.sibling !== oldWorkInProgress){
                prevSibling = prevSibling.sibling;
                if (prevSibling === null) // eslint-disable-next-line react-internal/prod-error-codes
                throw new Error('Expected to find the previous sibling.');
            }
            prevSibling.sibling = newWorkInProgress;
        } // Delete the old fiber and place the new one.
        // Since the old fiber is disconnected, we have to schedule it manually.
        var deletions = returnFiber.deletions;
        if (deletions === null) {
            returnFiber.deletions = [
                current
            ];
            returnFiber.flags |= ChildDeletion;
        } else deletions.push(current);
        newWorkInProgress.flags |= Placement; // Restart work from the new fiber.
        return newWorkInProgress;
    }
    function checkScheduledUpdateOrContext(current, renderLanes) {
        // Before performing an early bailout, we must check if there are pending
        // updates or context.
        var updateLanes = current.lanes;
        if (includesSomeLane(updateLanes, renderLanes)) return true;
         // No pending update, but because context is propagated lazily, we need
        return false;
    }
    function attemptEarlyBailoutIfNoScheduledUpdate(current, workInProgress, renderLanes) {
        // This fiber does not have any pending work. Bailout without entering
        // the begin phase. There's still some bookkeeping we that needs to be done
        // in this optimized path, mostly pushing stuff onto the stack.
        switch(workInProgress.tag){
            case HostRoot:
                pushHostRootContext(workInProgress);
                var root = workInProgress.stateNode;
                resetHydrationState();
                break;
            case HostComponent:
                pushHostContext(workInProgress);
                break;
            case ClassComponent:
                var Component = workInProgress.type;
                if (isContextProvider(Component)) pushContextProvider(workInProgress);
                break;
            case HostPortal:
                pushHostContainer(workInProgress, workInProgress.stateNode.containerInfo);
                break;
            case ContextProvider:
                var newValue = workInProgress.memoizedProps.value;
                var context = workInProgress.type._context;
                pushProvider(workInProgress, context, newValue);
                break;
            case Profiler:
                // Profiler should only call onRender when one of its descendants actually rendered.
                var hasChildWork = includesSomeLane(renderLanes, workInProgress.childLanes);
                if (hasChildWork) workInProgress.flags |= Update;
                // Reset effect durations for the next eventual effect phase.
                // These are reset during render to allow the DevTools commit hook a chance to read them,
                var stateNode = workInProgress.stateNode;
                stateNode.effectDuration = 0;
                stateNode.passiveEffectDuration = 0;
                break;
            case SuspenseComponent:
                var state = workInProgress.memoizedState;
                if (state !== null) {
                    if (state.dehydrated !== null) {
                        pushSuspenseContext(workInProgress, setDefaultShallowSuspenseContext(suspenseStackCursor.current)); // We know that this component will suspend again because if it has
                        // been unsuspended it has committed as a resolved Suspense component.
                        // If it needs to be retried, it should have work scheduled on it.
                        workInProgress.flags |= DidCapture; // We should never render the children of a dehydrated boundary until we
                        // upgrade it. We return null instead of bailoutOnAlreadyFinishedWork.
                        return null;
                    } // If this boundary is currently timed out, we need to decide
                    // whether to retry the primary children, or to skip over it and
                    // go straight to the fallback. Check the priority of the primary
                    // child fragment.
                    var primaryChildFragment = workInProgress.child;
                    var primaryChildLanes = primaryChildFragment.childLanes;
                    if (includesSomeLane(renderLanes, primaryChildLanes)) // The primary children have pending work. Use the normal path
                    // to attempt to render the primary children again.
                    return updateSuspenseComponent(current, workInProgress, renderLanes);
                    else {
                        // The primary child fragment does not have pending work marked
                        // on it
                        pushSuspenseContext(workInProgress, setDefaultShallowSuspenseContext(suspenseStackCursor.current)); // The primary children do not have pending work with sufficient
                        // priority. Bailout.
                        var child = bailoutOnAlreadyFinishedWork(current, workInProgress, renderLanes);
                        if (child !== null) // The fallback children have pending work. Skip over the
                        // primary children and work on the fallback.
                        return child.sibling;
                        else // Note: We can return `null` here because we already checked
                        // whether there were nested context consumers, via the call to
                        // `bailoutOnAlreadyFinishedWork` above.
                        return null;
                    }
                } else pushSuspenseContext(workInProgress, setDefaultShallowSuspenseContext(suspenseStackCursor.current));
                break;
            case SuspenseListComponent:
                var didSuspendBefore = (current.flags & DidCapture) !== NoFlags;
                var _hasChildWork = includesSomeLane(renderLanes, workInProgress.childLanes);
                if (didSuspendBefore) {
                    if (_hasChildWork) // If something was in fallback state last time, and we have all the
                    // same children then we're still in progressive loading state.
                    // Something might get unblocked by state updates or retries in the
                    // tree which will affect the tail. So we need to use the normal
                    // path to compute the correct tail.
                    return updateSuspenseListComponent(current, workInProgress, renderLanes);
                     // If none of the children had any work, that means that none of
                    // them got retried so they'll still be blocked in the same way
                    // as before. We can fast bail out.
                    workInProgress.flags |= DidCapture;
                } // If nothing suspended before and we're rendering the same children,
                // then the tail doesn't matter. Anything new that suspends will work
                // in the "together" mode, so we can continue from the state we had.
                var renderState = workInProgress.memoizedState;
                if (renderState !== null) {
                    // Reset to the "together" mode in case we've started a different
                    // update in the past but didn't complete it.
                    renderState.rendering = null;
                    renderState.tail = null;
                    renderState.lastEffect = null;
                }
                pushSuspenseContext(workInProgress, suspenseStackCursor.current);
                if (_hasChildWork) break;
                else // If none of the children had any work, that means that none of
                // them got retried so they'll still be blocked in the same way
                // as before. We can fast bail out.
                return null;
            case OffscreenComponent:
            case LegacyHiddenComponent:
                // Need to check if the tree still needs to be deferred. This is
                // almost identical to the logic used in the normal update path,
                // so we'll just enter that. The only difference is we'll bail out
                // at the next level instead of this one, because the child props
                // have not changed. Which is fine.
                // TODO: Probably should refactor `beginWork` to split the bailout
                // path from the normal path. I'm tempted to do a labeled break here
                // but I won't :)
                workInProgress.lanes = NoLanes;
                return updateOffscreenComponent(current, workInProgress, renderLanes);
        }
        return bailoutOnAlreadyFinishedWork(current, workInProgress, renderLanes);
    }
    function beginWork(current, workInProgress, renderLanes) {
        if (workInProgress._debugNeedsRemount && current !== null) // This will restart the begin phase with a new fiber.
        return remountFiber(current, workInProgress, createFiberFromTypeAndProps(workInProgress.type, workInProgress.key, workInProgress.pendingProps, workInProgress._debugOwner || null, workInProgress.mode, workInProgress.lanes));
        if (current !== null) {
            var oldProps = current.memoizedProps;
            var newProps = workInProgress.pendingProps;
            if (oldProps !== newProps || hasContextChanged1() || workInProgress.type !== current.type) // If props or context changed, mark the fiber as having performed work.
            // This may be unset if the props are determined to be equal later (memo).
            didReceiveUpdate = true;
            else {
                // Neither props nor legacy context changes. Check if there's a pending
                // update or context change.
                var hasScheduledUpdateOrContext = checkScheduledUpdateOrContext(current, renderLanes);
                if (!hasScheduledUpdateOrContext && // may not be work scheduled on `current`, so we check for this flag.
                (workInProgress.flags & DidCapture) === NoFlags) {
                    // No pending updates or context. Bail out now.
                    didReceiveUpdate = false;
                    return attemptEarlyBailoutIfNoScheduledUpdate(current, workInProgress, renderLanes);
                }
                if ((current.flags & ForceUpdateForLegacySuspense) !== NoFlags) // This is a special case that only exists for legacy mode.
                // See https://github.com/facebook/react/pull/19216.
                didReceiveUpdate = true;
                else // An update was scheduled on this fiber, but there are no new props
                // nor legacy context. Set this to false. If an update queue or context
                // consumer produces a changed value, it will set this to true. Otherwise,
                // the component will assume the children have not changed and bail out.
                didReceiveUpdate = false;
            }
        } else {
            didReceiveUpdate = false;
            if (getIsHydrating() && isForkedChild(workInProgress)) {
                // Check if this child belongs to a list of muliple children in
                // its parent.
                //
                // In a true multi-threaded implementation, we would render children on
                // parallel threads. This would represent the beginning of a new render
                // thread for this subtree.
                //
                // We only use this for id generation during hydration, which is why the
                // logic is located in this special branch.
                var slotIndex = workInProgress.index;
                var numberOfForks = getForksAtLevel();
                pushTreeId(workInProgress, numberOfForks, slotIndex);
            }
        } // Before entering the begin phase, clear pending update priority.
        // TODO: This assumes that we're about to evaluate the component and process
        // the update queue. However, there's an exception: SimpleMemoComponent
        // sometimes bails out later in the begin phase. This indicates that we should
        // move this assignment out of the common path and into each branch.
        workInProgress.lanes = NoLanes;
        switch(workInProgress.tag){
            case IndeterminateComponent:
                return mountIndeterminateComponent(current, workInProgress, workInProgress.type, renderLanes);
            case LazyComponent:
                var elementType = workInProgress.elementType;
                return mountLazyComponent(current, workInProgress, elementType, renderLanes);
            case FunctionComponent:
                var Component = workInProgress.type;
                var unresolvedProps = workInProgress.pendingProps;
                var resolvedProps = workInProgress.elementType === Component ? unresolvedProps : resolveDefaultProps(Component, unresolvedProps);
                return updateFunctionComponent(current, workInProgress, Component, resolvedProps, renderLanes);
            case ClassComponent:
                var _Component = workInProgress.type;
                var _unresolvedProps = workInProgress.pendingProps;
                var _resolvedProps = workInProgress.elementType === _Component ? _unresolvedProps : resolveDefaultProps(_Component, _unresolvedProps);
                return updateClassComponent(current, workInProgress, _Component, _resolvedProps, renderLanes);
            case HostRoot:
                return updateHostRoot(current, workInProgress, renderLanes);
            case HostComponent:
                return updateHostComponent$1(current, workInProgress, renderLanes);
            case HostText:
                return updateHostText$1(current, workInProgress);
            case SuspenseComponent:
                return updateSuspenseComponent(current, workInProgress, renderLanes);
            case HostPortal:
                return updatePortalComponent(current, workInProgress, renderLanes);
            case ForwardRef:
                var type = workInProgress.type;
                var _unresolvedProps2 = workInProgress.pendingProps;
                var _resolvedProps2 = workInProgress.elementType === type ? _unresolvedProps2 : resolveDefaultProps(type, _unresolvedProps2);
                return updateForwardRef(current, workInProgress, type, _resolvedProps2, renderLanes);
            case Fragment:
                return updateFragment1(current, workInProgress, renderLanes);
            case Mode:
                return updateMode(current, workInProgress, renderLanes);
            case Profiler:
                return updateProfiler(current, workInProgress, renderLanes);
            case ContextProvider:
                return updateContextProvider(current, workInProgress, renderLanes);
            case ContextConsumer:
                return updateContextConsumer(current, workInProgress, renderLanes);
            case MemoComponent:
                var _type2 = workInProgress.type;
                var _unresolvedProps3 = workInProgress.pendingProps; // Resolve outer props first, then resolve inner props.
                var _resolvedProps3 = resolveDefaultProps(_type2, _unresolvedProps3);
                if (workInProgress.type !== workInProgress.elementType) {
                    var outerPropTypes = _type2.propTypes;
                    if (outerPropTypes) checkPropTypes(outerPropTypes, _resolvedProps3, 'prop', getComponentNameFromType(_type2));
                }
                _resolvedProps3 = resolveDefaultProps(_type2.type, _resolvedProps3);
                return updateMemoComponent(current, workInProgress, _type2, _resolvedProps3, renderLanes);
            case SimpleMemoComponent:
                return updateSimpleMemoComponent(current, workInProgress, workInProgress.type, workInProgress.pendingProps, renderLanes);
            case IncompleteClassComponent:
                var _Component2 = workInProgress.type;
                var _unresolvedProps4 = workInProgress.pendingProps;
                var _resolvedProps4 = workInProgress.elementType === _Component2 ? _unresolvedProps4 : resolveDefaultProps(_Component2, _unresolvedProps4);
                return mountIncompleteClassComponent(current, workInProgress, _Component2, _resolvedProps4, renderLanes);
            case SuspenseListComponent:
                return updateSuspenseListComponent(current, workInProgress, renderLanes);
            case ScopeComponent:
                break;
            case OffscreenComponent:
                return updateOffscreenComponent(current, workInProgress, renderLanes);
        }
        throw new Error("Unknown unit of work tag (" + workInProgress.tag + "). This error is likely caused by a bug in " + 'React. Please file an issue.');
    }
    function unwindWork(current, workInProgress, renderLanes) {
        // Note: This intentionally doesn't check if we're hydrating because comparing
        // to the current tree provider fiber is just as fast and less error-prone.
        // Ideally we would have a special version of the work loop only
        // for hydration.
        popTreeContext(workInProgress);
        switch(workInProgress.tag){
            case ClassComponent:
                var Component = workInProgress.type;
                if (isContextProvider(Component)) popContext(workInProgress);
                var flags = workInProgress.flags;
                if (flags & ShouldCapture) {
                    workInProgress.flags = flags & ~ShouldCapture | DidCapture;
                    if ((workInProgress.mode & ProfileMode) !== NoMode) transferActualDuration(workInProgress);
                    return workInProgress;
                }
                return null;
            case HostRoot:
                var root = workInProgress.stateNode;
                popHostContainer(workInProgress);
                popTopLevelContextObject(workInProgress);
                resetWorkInProgressVersions();
                var _flags = workInProgress.flags;
                if ((_flags & ShouldCapture) !== NoFlags && (_flags & DidCapture) === NoFlags) {
                    // There was an error during render that wasn't captured by a suspense
                    // boundary. Do a second pass on the root to unmount the children.
                    workInProgress.flags = _flags & ~ShouldCapture | DidCapture;
                    return workInProgress;
                } // We unwound to the root without completing it. Exit.
                return null;
            case HostComponent:
                // TODO: popHydrationState
                popHostContext(workInProgress);
                return null;
            case SuspenseComponent:
                popSuspenseContext(workInProgress);
                var suspenseState = workInProgress.memoizedState;
                if (suspenseState !== null && suspenseState.dehydrated !== null) {
                    if (workInProgress.alternate === null) throw new Error("Threw in newly mounted dehydrated component. This is likely a bug in React. Please file an issue.");
                    resetHydrationState();
                }
                var _flags2 = workInProgress.flags;
                if (_flags2 & ShouldCapture) {
                    workInProgress.flags = _flags2 & ~ShouldCapture | DidCapture; // Captured a suspense effect. Re-render the boundary.
                    if ((workInProgress.mode & ProfileMode) !== NoMode) transferActualDuration(workInProgress);
                    return workInProgress;
                }
                return null;
            case SuspenseListComponent:
                popSuspenseContext(workInProgress); // SuspenseList doesn't actually catch anything. It should've been
                // caught by a nested boundary. If not, it should bubble through.
                return null;
            case HostPortal:
                popHostContainer(workInProgress);
                return null;
            case ContextProvider:
                var context = workInProgress.type._context;
                popProvider(context, workInProgress);
                return null;
            case OffscreenComponent:
            case LegacyHiddenComponent:
                popRenderLanes(workInProgress);
                return null;
            case CacheComponent:
                return null;
            default:
                return null;
        }
    }
    function unwindInterruptedWork(current, interruptedWork, renderLanes) {
        // Note: This intentionally doesn't check if we're hydrating because comparing
        // to the current tree provider fiber is just as fast and less error-prone.
        // Ideally we would have a special version of the work loop only
        // for hydration.
        popTreeContext(interruptedWork);
        switch(interruptedWork.tag){
            case ClassComponent:
                var childContextTypes = interruptedWork.type.childContextTypes;
                if (childContextTypes !== null && childContextTypes !== undefined) popContext(interruptedWork);
                break;
            case HostRoot:
                var root = interruptedWork.stateNode;
                popHostContainer(interruptedWork);
                popTopLevelContextObject(interruptedWork);
                resetWorkInProgressVersions();
                break;
            case HostComponent:
                popHostContext(interruptedWork);
                break;
            case HostPortal:
                popHostContainer(interruptedWork);
                break;
            case SuspenseComponent:
                popSuspenseContext(interruptedWork);
                break;
            case SuspenseListComponent:
                popSuspenseContext(interruptedWork);
                break;
            case ContextProvider:
                var context = interruptedWork.type._context;
                popProvider(context, interruptedWork);
                break;
            case OffscreenComponent:
            case LegacyHiddenComponent:
                popRenderLanes(interruptedWork);
                break;
        }
    }
    var didWarnAboutUndefinedSnapshotBeforeUpdate = null;
    didWarnAboutUndefinedSnapshotBeforeUpdate = new Set();
    // Allows us to avoid traversing the return path to find the nearest Offscreen ancestor.
    // Only used when enableSuspenseLayoutEffectSemantics is enabled.
    var offscreenSubtreeIsHidden = false;
    var offscreenSubtreeWasHidden = false;
    var PossiblyWeakSet = typeof WeakSet === 'function' ? WeakSet : Set;
    var nextEffect = null; // Used for Profiling builds to track updaters.
    var inProgressLanes = null;
    var inProgressRoot = null;
    function reportUncaughtErrorInDEV(error) {
        invokeGuardedCallback(null, function() {
            throw error;
        });
        clearCaughtError();
    }
    var callComponentWillUnmountWithTimer = function(current, instance) {
        instance.props = current.memoizedProps;
        instance.state = current.memoizedState;
        if (current.mode & ProfileMode) try {
            startLayoutEffectTimer();
            instance.componentWillUnmount();
        } finally{
            recordLayoutEffectDuration(current);
        }
        else instance.componentWillUnmount();
    }; // Capture errors so they don't interrupt mounting.
    function safelyCallCommitHookLayoutEffectListMount(current, nearestMountedAncestor) {
        try {
            commitHookEffectListMount(Layout, current);
        } catch (error) {
            captureCommitPhaseError(current, nearestMountedAncestor, error);
        }
    } // Capture errors so they don't interrupt unmounting.
    function safelyCallComponentWillUnmount(current, nearestMountedAncestor, instance) {
        try {
            callComponentWillUnmountWithTimer(current, instance);
        } catch (error) {
            captureCommitPhaseError(current, nearestMountedAncestor, error);
        }
    } // Capture errors so they don't interrupt mounting.
    function safelyCallComponentDidMount(current, nearestMountedAncestor, instance) {
        try {
            instance.componentDidMount();
        } catch (error) {
            captureCommitPhaseError(current, nearestMountedAncestor, error);
        }
    } // Capture errors so they don't interrupt mounting.
    function safelyAttachRef(current, nearestMountedAncestor) {
        try {
            commitAttachRef(current);
        } catch (error) {
            captureCommitPhaseError(current, nearestMountedAncestor, error);
        }
    }
    function safelyDetachRef(current, nearestMountedAncestor) {
        var ref = current.ref;
        if (ref !== null) {
            if (typeof ref === 'function') {
                var retVal;
                try {
                    if (enableProfilerTimer && enableProfilerCommitHooks && current.mode & ProfileMode) try {
                        startLayoutEffectTimer();
                        retVal = ref(null);
                    } finally{
                        recordLayoutEffectDuration(current);
                    }
                    else retVal = ref(null);
                } catch (error) {
                    captureCommitPhaseError(current, nearestMountedAncestor, error);
                }
                if (typeof retVal === 'function') error1("Unexpected return value from a callback ref in %s. A callback ref should not return a function.", getComponentNameFromFiber(current));
            } else ref.current = null;
        }
    }
    function safelyCallDestroy(current, nearestMountedAncestor, destroy) {
        try {
            destroy();
        } catch (error) {
            captureCommitPhaseError(current, nearestMountedAncestor, error);
        }
    }
    var focusedInstanceHandle = null;
    var shouldFireAfterActiveInstanceBlur = false;
    function commitBeforeMutationEffects(root, firstChild) {
        focusedInstanceHandle = prepareForCommit(root.containerInfo);
        nextEffect = firstChild;
        commitBeforeMutationEffects_begin(); // We no longer need to track the active instance fiber
        var shouldFire = shouldFireAfterActiveInstanceBlur;
        shouldFireAfterActiveInstanceBlur = false;
        focusedInstanceHandle = null;
        return shouldFire;
    }
    function commitBeforeMutationEffects_begin() {
        while(nextEffect !== null){
            var fiber = nextEffect; // This phase is only used for beforeActiveInstanceBlur.
            var child = fiber.child;
            if ((fiber.subtreeFlags & BeforeMutationMask) !== NoFlags && child !== null) {
                child.return = fiber;
                nextEffect = child;
            } else commitBeforeMutationEffects_complete();
        }
    }
    function commitBeforeMutationEffects_complete() {
        while(nextEffect !== null){
            var fiber = nextEffect;
            setCurrentFiber(fiber);
            try {
                commitBeforeMutationEffectsOnFiber(fiber);
            } catch (error) {
                captureCommitPhaseError(fiber, fiber.return, error);
            }
            resetCurrentFiber();
            var sibling = fiber.sibling;
            if (sibling !== null) {
                sibling.return = fiber.return;
                nextEffect = sibling;
                return;
            }
            nextEffect = fiber.return;
        }
    }
    function commitBeforeMutationEffectsOnFiber(finishedWork) {
        var current = finishedWork.alternate;
        var flags = finishedWork.flags;
        if ((flags & Snapshot) !== NoFlags) {
            setCurrentFiber(finishedWork);
            switch(finishedWork.tag){
                case FunctionComponent:
                case ForwardRef:
                case SimpleMemoComponent:
                    break;
                case ClassComponent:
                    if (current !== null) {
                        var prevProps = current.memoizedProps;
                        var prevState = current.memoizedState;
                        var instance = finishedWork.stateNode; // We could update instance props and state here,
                        if (finishedWork.type === finishedWork.elementType && !didWarnAboutReassigningProps) {
                            if (instance.props !== finishedWork.memoizedProps) error1("Expected %s props to match memoized props before getSnapshotBeforeUpdate. This might either be because of a bug in React, or because a component reassigns its own `this.props`. Please file an issue.", getComponentNameFromFiber(finishedWork) || 'instance');
                            if (instance.state !== finishedWork.memoizedState) error1("Expected %s state to match memoized state before getSnapshotBeforeUpdate. This might either be because of a bug in React, or because a component reassigns its own `this.state`. Please file an issue.", getComponentNameFromFiber(finishedWork) || 'instance');
                        }
                        var snapshot = instance.getSnapshotBeforeUpdate(finishedWork.elementType === finishedWork.type ? prevProps : resolveDefaultProps(finishedWork.type, prevProps), prevState);
                        var didWarnSet = didWarnAboutUndefinedSnapshotBeforeUpdate;
                        if (snapshot === undefined && !didWarnSet.has(finishedWork.type)) {
                            didWarnSet.add(finishedWork.type);
                            error1("%s.getSnapshotBeforeUpdate(): A snapshot value (or null) must be returned. You have returned undefined.", getComponentNameFromFiber(finishedWork));
                        }
                        instance.__reactInternalSnapshotBeforeUpdate = snapshot;
                    }
                    break;
                case HostRoot:
                    var root = finishedWork.stateNode;
                    clearContainer(root.containerInfo);
                    break;
                case HostComponent:
                case HostText:
                case HostPortal:
                case IncompleteClassComponent:
                    break;
                default:
                    throw new Error("This unit of work tag should not have side-effects. This error is likely caused by a bug in React. Please file an issue.");
            }
            resetCurrentFiber();
        }
    }
    function commitHookEffectListUnmount(flags, finishedWork, nearestMountedAncestor) {
        var updateQueue = finishedWork.updateQueue;
        var lastEffect = updateQueue !== null ? updateQueue.lastEffect : null;
        if (lastEffect !== null) {
            var firstEffect = lastEffect.next;
            var effect = firstEffect;
            do {
                if ((effect.tag & flags) === flags) {
                    // Unmount
                    var destroy = effect.destroy;
                    effect.destroy = undefined;
                    if (destroy !== undefined) {
                        if ((flags & Passive$1) !== NoFlags$1) markComponentPassiveEffectUnmountStarted(finishedWork);
                        else if ((flags & Layout) !== NoFlags$1) markComponentLayoutEffectUnmountStarted(finishedWork);
                        if ((flags & Insertion) !== NoFlags$1) setIsRunningInsertionEffect(true);
                        safelyCallDestroy(finishedWork, nearestMountedAncestor, destroy);
                        if ((flags & Insertion) !== NoFlags$1) setIsRunningInsertionEffect(false);
                        if ((flags & Passive$1) !== NoFlags$1) markComponentPassiveEffectUnmountStopped();
                        else if ((flags & Layout) !== NoFlags$1) markComponentLayoutEffectUnmountStopped();
                    }
                }
                effect = effect.next;
            }while (effect !== firstEffect)
        }
    }
    function commitHookEffectListMount(flags, finishedWork) {
        var updateQueue = finishedWork.updateQueue;
        var lastEffect = updateQueue !== null ? updateQueue.lastEffect : null;
        if (lastEffect !== null) {
            var firstEffect = lastEffect.next;
            var effect = firstEffect;
            do {
                if ((effect.tag & flags) === flags) {
                    if ((flags & Passive$1) !== NoFlags$1) markComponentPassiveEffectMountStarted(finishedWork);
                    else if ((flags & Layout) !== NoFlags$1) markComponentLayoutEffectMountStarted(finishedWork);
                    var create = effect.create;
                    if ((flags & Insertion) !== NoFlags$1) setIsRunningInsertionEffect(true);
                    effect.destroy = create();
                    if ((flags & Insertion) !== NoFlags$1) setIsRunningInsertionEffect(false);
                    if ((flags & Passive$1) !== NoFlags$1) markComponentPassiveEffectMountStopped();
                    else if ((flags & Layout) !== NoFlags$1) markComponentLayoutEffectMountStopped();
                    var destroy = effect.destroy;
                    if (destroy !== undefined && typeof destroy !== 'function') {
                        var hookName = void 0;
                        if ((effect.tag & Layout) !== NoFlags) hookName = 'useLayoutEffect';
                        else if ((effect.tag & Insertion) !== NoFlags) hookName = 'useInsertionEffect';
                        else hookName = 'useEffect';
                        var addendum = void 0;
                        if (destroy === null) addendum = " You returned null. If your effect does not require clean up, return undefined (or nothing).";
                        else if (typeof destroy.then === 'function') addendum = '\n\nIt looks like you wrote ' + hookName + '(async () => ...) or returned a Promise. ' + 'Instead, write the async function inside your effect ' + 'and call it immediately:\n\n' + hookName + '(() => {\n' + '  async function fetchData() {\n' + '    // You can await here\n' + '    const response = await MyAPI.getData(someId);\n' + '    // ...\n' + '  }\n' + '  fetchData();\n' + "}, [someId]); // Or [] if effect doesn't need props or state\n\n" + 'Learn more about data fetching with Hooks: https://reactjs.org/link/hooks-data-fetching';
                        else addendum = ' You returned: ' + destroy;
                        error1("%s must not return anything besides a function, which is used for clean-up.%s", hookName, addendum);
                    }
                }
                effect = effect.next;
            }while (effect !== firstEffect)
        }
    }
    function commitPassiveEffectDurations(finishedRoot, finishedWork) {
        // Only Profilers with work in their subtree will have an Update effect scheduled.
        if ((finishedWork.flags & Update) !== NoFlags) switch(finishedWork.tag){
            case Profiler:
                var passiveEffectDuration = finishedWork.stateNode.passiveEffectDuration;
                var _finishedWork$memoize = finishedWork.memoizedProps, id = _finishedWork$memoize.id, onPostCommit = _finishedWork$memoize.onPostCommit; // This value will still reflect the previous commit phase.
                // It does not get reset until the start of the next commit phase.
                var commitTime = getCommitTime();
                var phase = finishedWork.alternate === null ? 'mount' : 'update';
                if (isCurrentUpdateNested()) phase = 'nested-update';
                if (typeof onPostCommit === 'function') onPostCommit(id, phase, passiveEffectDuration, commitTime);
                 // Bubble times to the next nearest ancestor Profiler.
                // After we process that Profiler, we'll bubble further up.
                var parentFiber = finishedWork.return;
                outer: while(parentFiber !== null){
                    switch(parentFiber.tag){
                        case HostRoot:
                            var root = parentFiber.stateNode;
                            root.passiveEffectDuration += passiveEffectDuration;
                            break outer;
                        case Profiler:
                            var parentStateNode = parentFiber.stateNode;
                            parentStateNode.passiveEffectDuration += passiveEffectDuration;
                            break outer;
                    }
                    parentFiber = parentFiber.return;
                }
                break;
        }
    }
    function commitLayoutEffectOnFiber(finishedRoot, current, finishedWork, committedLanes) {
        if ((finishedWork.flags & LayoutMask) !== NoFlags) switch(finishedWork.tag){
            case FunctionComponent:
            case ForwardRef:
            case SimpleMemoComponent:
                if (!offscreenSubtreeWasHidden) {
                    // At this point layout effects have already been destroyed (during mutation phase).
                    // This is done to prevent sibling component effects from interfering with each other,
                    // e.g. a destroy function in one component should never override a ref set
                    // by a create function in another component during the same commit.
                    if (finishedWork.mode & ProfileMode) try {
                        startLayoutEffectTimer();
                        commitHookEffectListMount(Layout | HasEffect, finishedWork);
                    } finally{
                        recordLayoutEffectDuration(finishedWork);
                    }
                    else commitHookEffectListMount(Layout | HasEffect, finishedWork);
                }
                break;
            case ClassComponent:
                var instance = finishedWork.stateNode;
                if (finishedWork.flags & Update) {
                    if (!offscreenSubtreeWasHidden) {
                        if (current === null) {
                            if (finishedWork.type === finishedWork.elementType && !didWarnAboutReassigningProps) {
                                if (instance.props !== finishedWork.memoizedProps) error1("Expected %s props to match memoized props before componentDidMount. This might either be because of a bug in React, or because a component reassigns its own `this.props`. Please file an issue.", getComponentNameFromFiber(finishedWork) || 'instance');
                                if (instance.state !== finishedWork.memoizedState) error1("Expected %s state to match memoized state before componentDidMount. This might either be because of a bug in React, or because a component reassigns its own `this.state`. Please file an issue.", getComponentNameFromFiber(finishedWork) || 'instance');
                            }
                            if (finishedWork.mode & ProfileMode) try {
                                startLayoutEffectTimer();
                                instance.componentDidMount();
                            } finally{
                                recordLayoutEffectDuration(finishedWork);
                            }
                            else instance.componentDidMount();
                        } else {
                            var prevProps = finishedWork.elementType === finishedWork.type ? current.memoizedProps : resolveDefaultProps(finishedWork.type, current.memoizedProps);
                            var prevState = current.memoizedState; // We could update instance props and state here,
                            if (finishedWork.type === finishedWork.elementType && !didWarnAboutReassigningProps) {
                                if (instance.props !== finishedWork.memoizedProps) error1("Expected %s props to match memoized props before componentDidUpdate. This might either be because of a bug in React, or because a component reassigns its own `this.props`. Please file an issue.", getComponentNameFromFiber(finishedWork) || 'instance');
                                if (instance.state !== finishedWork.memoizedState) error1("Expected %s state to match memoized state before componentDidUpdate. This might either be because of a bug in React, or because a component reassigns its own `this.state`. Please file an issue.", getComponentNameFromFiber(finishedWork) || 'instance');
                            }
                            if (finishedWork.mode & ProfileMode) try {
                                startLayoutEffectTimer();
                                instance.componentDidUpdate(prevProps, prevState, instance.__reactInternalSnapshotBeforeUpdate);
                            } finally{
                                recordLayoutEffectDuration(finishedWork);
                            }
                            else instance.componentDidUpdate(prevProps, prevState, instance.__reactInternalSnapshotBeforeUpdate);
                        }
                    }
                } // TODO: I think this is now always non-null by the time it reaches the
                // commit phase. Consider removing the type check.
                var updateQueue = finishedWork.updateQueue;
                if (updateQueue !== null) {
                    if (finishedWork.type === finishedWork.elementType && !didWarnAboutReassigningProps) {
                        if (instance.props !== finishedWork.memoizedProps) error1("Expected %s props to match memoized props before processing the update queue. This might either be because of a bug in React, or because a component reassigns its own `this.props`. Please file an issue.", getComponentNameFromFiber(finishedWork) || 'instance');
                        if (instance.state !== finishedWork.memoizedState) error1("Expected %s state to match memoized state before processing the update queue. This might either be because of a bug in React, or because a component reassigns its own `this.state`. Please file an issue.", getComponentNameFromFiber(finishedWork) || 'instance');
                    }
                    // but instead we rely on them being set during last render.
                    // TODO: revisit this when we implement resuming.
                    commitUpdateQueue(finishedWork, updateQueue, instance);
                }
                break;
            case HostRoot:
                // TODO: I think this is now always non-null by the time it reaches the
                // commit phase. Consider removing the type check.
                var _updateQueue = finishedWork.updateQueue;
                if (_updateQueue !== null) {
                    var _instance = null;
                    if (finishedWork.child !== null) switch(finishedWork.child.tag){
                        case HostComponent:
                            _instance = getPublicInstance(finishedWork.child.stateNode);
                            break;
                        case ClassComponent:
                            _instance = finishedWork.child.stateNode;
                            break;
                    }
                    commitUpdateQueue(finishedWork, _updateQueue, _instance);
                }
                break;
            case HostComponent:
                var _instance2 = finishedWork.stateNode; // Renderers may schedule work to be done after host components are mounted
                // (eg DOM renderer may schedule auto-focus for inputs and form controls).
                // These effects should only be committed when components are first mounted,
                // aka when there is no current/alternate.
                if (current === null && finishedWork.flags & Update) {
                    var type = finishedWork.type;
                    var props = finishedWork.memoizedProps;
                    commitMount(_instance2, type, props);
                }
                break;
            case HostText:
                break;
            case HostPortal:
                break;
            case Profiler:
                var _finishedWork$memoize2 = finishedWork.memoizedProps, onCommit = _finishedWork$memoize2.onCommit, onRender = _finishedWork$memoize2.onRender;
                var effectDuration = finishedWork.stateNode.effectDuration;
                var commitTime = getCommitTime();
                var phase = current === null ? 'mount' : 'update';
                if (isCurrentUpdateNested()) phase = 'nested-update';
                if (typeof onRender === 'function') onRender(finishedWork.memoizedProps.id, phase, finishedWork.actualDuration, finishedWork.treeBaseDuration, finishedWork.actualStartTime, commitTime);
                if (typeof onCommit === 'function') onCommit(finishedWork.memoizedProps.id, phase, effectDuration, commitTime);
                 // Schedule a passive effect for this Profiler to call onPostCommit hooks.
                // This effect should be scheduled even if there is no onPostCommit callback for this Profiler,
                // because the effect is also where times bubble to parent Profilers.
                enqueuePendingPassiveProfilerEffect(finishedWork); // Propagate layout effect durations to the next nearest Profiler ancestor.
                // Do not reset these values until the next render so DevTools has a chance to read them first.
                var parentFiber = finishedWork.return;
                outer: while(parentFiber !== null){
                    switch(parentFiber.tag){
                        case HostRoot:
                            var root = parentFiber.stateNode;
                            root.effectDuration += effectDuration;
                            break outer;
                        case Profiler:
                            var parentStateNode = parentFiber.stateNode;
                            parentStateNode.effectDuration += effectDuration;
                            break outer;
                    }
                    parentFiber = parentFiber.return;
                }
                break;
            case SuspenseComponent:
                commitSuspenseHydrationCallbacks(finishedRoot, finishedWork);
                break;
            case SuspenseListComponent:
            case IncompleteClassComponent:
            case ScopeComponent:
            case OffscreenComponent:
            case LegacyHiddenComponent:
                break;
            default:
                throw new Error("This unit of work tag should not have side-effects. This error is likely caused by a bug in React. Please file an issue.");
        }
        if (!offscreenSubtreeWasHidden) {
            if (finishedWork.flags & Ref) commitAttachRef(finishedWork);
        }
    }
    function reappearLayoutEffectsOnFiber(node) {
        // Turn on layout effects in a tree that previously disappeared.
        // TODO (Offscreen) Check: flags & LayoutStatic
        switch(node.tag){
            case FunctionComponent:
            case ForwardRef:
            case SimpleMemoComponent:
                if (node.mode & ProfileMode) try {
                    startLayoutEffectTimer();
                    safelyCallCommitHookLayoutEffectListMount(node, node.return);
                } finally{
                    recordLayoutEffectDuration(node);
                }
                else safelyCallCommitHookLayoutEffectListMount(node, node.return);
                break;
            case ClassComponent:
                var instance = node.stateNode;
                if (typeof instance.componentDidMount === 'function') safelyCallComponentDidMount(node, node.return, instance);
                safelyAttachRef(node, node.return);
                break;
            case HostComponent:
                safelyAttachRef(node, node.return);
                break;
        }
    }
    function hideOrUnhideAllChildren(finishedWork, isHidden) {
        // Only hide or unhide the top-most host nodes.
        var hostSubtreeRoot = null;
        // We only have the top Fiber that was inserted but we need to recurse down its
        // children to find all the terminal nodes.
        var node = finishedWork;
        while(true){
            if (node.tag === HostComponent) {
                if (hostSubtreeRoot === null) {
                    hostSubtreeRoot = node;
                    try {
                        var instance = node.stateNode;
                        if (isHidden) hideInstance(instance);
                        else unhideInstance(node.stateNode, node.memoizedProps);
                    } catch (error) {
                        captureCommitPhaseError(finishedWork, finishedWork.return, error);
                    }
                }
            } else if (node.tag === HostText) {
                if (hostSubtreeRoot === null) try {
                    var _instance3 = node.stateNode;
                    if (isHidden) hideTextInstance(_instance3);
                    else unhideTextInstance(_instance3, node.memoizedProps);
                } catch (error) {
                    captureCommitPhaseError(finishedWork, finishedWork.return, error);
                }
            } else if ((node.tag === OffscreenComponent || node.tag === LegacyHiddenComponent) && node.memoizedState !== null && node !== finishedWork) ;
            else if (node.child !== null) {
                node.child.return = node;
                node = node.child;
                continue;
            }
            if (node === finishedWork) return;
            while(node.sibling === null){
                if (node.return === null || node.return === finishedWork) return;
                if (hostSubtreeRoot === node) hostSubtreeRoot = null;
                node = node.return;
            }
            if (hostSubtreeRoot === node) hostSubtreeRoot = null;
            node.sibling.return = node.return;
            node = node.sibling;
        }
    }
    function commitAttachRef(finishedWork) {
        var ref = finishedWork.ref;
        if (ref !== null) {
            var instance = finishedWork.stateNode;
            var instanceToUse;
            switch(finishedWork.tag){
                case HostComponent:
                    instanceToUse = getPublicInstance(instance);
                    break;
                default:
                    instanceToUse = instance;
            } // Moved outside to ensure DCE works with this flag
            if (typeof ref === 'function') {
                var retVal;
                if (finishedWork.mode & ProfileMode) try {
                    startLayoutEffectTimer();
                    retVal = ref(instanceToUse);
                } finally{
                    recordLayoutEffectDuration(finishedWork);
                }
                else retVal = ref(instanceToUse);
                if (typeof retVal === 'function') error1("Unexpected return value from a callback ref in %s. A callback ref should not return a function.", getComponentNameFromFiber(finishedWork));
            } else {
                if (!ref.hasOwnProperty('current')) error1("Unexpected ref object provided for %s. Use either a ref-setter function or React.createRef().", getComponentNameFromFiber(finishedWork));
                ref.current = instanceToUse;
            }
        }
    }
    function detachFiberMutation(fiber) {
        // Cut off the return pointer to disconnect it from the tree.
        // This enables us to detect and warn against state updates on an unmounted component.
        // It also prevents events from bubbling from within disconnected components.
        //
        // Ideally, we should also clear the child pointer of the parent alternate to let this
        // get GC:ed but we don't know which for sure which parent is the current
        // one so we'll settle for GC:ing the subtree of this child.
        // This child itself will be GC:ed when the parent updates the next time.
        //
        // Note that we can't clear child or sibling pointers yet.
        // They're needed for passive effects and for findDOMNode.
        // We defer those fields, and all other cleanup, to the passive phase (see detachFiberAfterEffects).
        //
        // Don't reset the alternate yet, either. We need that so we can detach the
        // alternate's fields in the passive phase. Clearing the return pointer is
        // sufficient for findDOMNode semantics.
        var alternate = fiber.alternate;
        if (alternate !== null) alternate.return = null;
        fiber.return = null;
    }
    function detachFiberAfterEffects(fiber) {
        var alternate = fiber.alternate;
        if (alternate !== null) {
            fiber.alternate = null;
            detachFiberAfterEffects(alternate);
        } // Note: Defensively using negation instead of < in case
        // Clear cyclical Fiber fields. This level alone is designed to roughly
        // approximate the planned Fiber refactor. In that world, `setState` will be
        // bound to a special "instance" object instead of a Fiber. The Instance
        // object will not have any of these fields. It will only be connected to
        // the fiber tree via a single link at the root. So if this level alone is
        // sufficient to fix memory issues, that bodes well for our plans.
        fiber.child = null;
        fiber.deletions = null;
        fiber.sibling = null; // The `stateNode` is cyclical because on host nodes it points to the host
        // tree, which has its own pointers to children, parents, and siblings.
        // The other host nodes also point back to fibers, so we should detach that
        // one, too.
        if (fiber.tag === HostComponent) {
            var hostInstance = fiber.stateNode;
            if (hostInstance !== null) detachDeletedInstance(hostInstance);
        }
        fiber.stateNode = null; // I'm intentionally not clearing the `return` field in this level. We
        fiber._debugOwner = null;
        // Theoretically, nothing in here should be necessary, because we already
        // disconnected the fiber from the tree. So even if something leaks this
        // particular fiber, it won't leak anything else
        //
        // The purpose of this branch is to be super aggressive so we can measure
        // if there's any difference in memory impact. If there is, that could
        // indicate a React leak we don't know about.
        fiber.return = null;
        fiber.dependencies = null;
        fiber.memoizedProps = null;
        fiber.memoizedState = null;
        fiber.pendingProps = null;
        fiber.stateNode = null; // TODO: Move to `commitPassiveUnmountInsideDeletedTreeOnFiber` instead.
        fiber.updateQueue = null;
    }
    function getHostParentFiber(fiber) {
        var parent = fiber.return;
        while(parent !== null){
            if (isHostParent(parent)) return parent;
            parent = parent.return;
        }
        throw new Error("Expected to find a host parent. This error is likely caused by a bug in React. Please file an issue.");
    }
    function isHostParent(fiber) {
        return fiber.tag === HostComponent || fiber.tag === HostRoot || fiber.tag === HostPortal;
    }
    function getHostSibling(fiber) {
        // We're going to search forward into the tree until we find a sibling host
        // node. Unfortunately, if multiple insertions are done in a row we have to
        // search past them. This leads to exponential search for the next sibling.
        // TODO: Find a more efficient way to do this.
        var node = fiber;
        siblings: while(true){
            // If we didn't find anything, let's try the next sibling.
            while(node.sibling === null){
                if (node.return === null || isHostParent(node.return)) // If we pop out of the root or hit the parent the fiber we are the
                // last sibling.
                return null;
                node = node.return;
            }
            node.sibling.return = node.return;
            node = node.sibling;
            while(node.tag !== HostComponent && node.tag !== HostText && node.tag !== DehydratedFragment){
                // If it is not host node and, we might have a host node inside it.
                // Try to search down until we find one.
                if (node.flags & Placement) continue siblings;
                 // If we don't have a child, try the siblings instead.
                // We also skip portals because they are not part of this host tree.
                if (node.child === null || node.tag === HostPortal) continue siblings;
                else {
                    node.child.return = node;
                    node = node.child;
                }
            } // Check if this host node is stable or about to be placed.
            if (!(node.flags & Placement)) // Found it!
            return node.stateNode;
        }
    }
    function commitPlacement(finishedWork) {
        var parentFiber = getHostParentFiber(finishedWork); // Note: these two variables *must* always be updated together.
        switch(parentFiber.tag){
            case HostComponent:
                var parent = parentFiber.stateNode;
                if (parentFiber.flags & ContentReset) {
                    // Reset the text content of the parent before doing any insertions
                    resetTextContent(parent); // Clear ContentReset from the effect tag
                    parentFiber.flags &= ~ContentReset;
                }
                var before = getHostSibling(finishedWork); // We only have the top Fiber that was inserted but we need to recurse down its
                // children to find all the terminal nodes.
                insertOrAppendPlacementNode(finishedWork, before, parent);
                break;
            case HostRoot:
            case HostPortal:
                var _parent = parentFiber.stateNode.containerInfo;
                var _before = getHostSibling(finishedWork);
                insertOrAppendPlacementNodeIntoContainer(finishedWork, _before, _parent);
                break;
            // eslint-disable-next-line-no-fallthrough
            default:
                throw new Error("Invalid host parent fiber. This error is likely caused by a bug in React. Please file an issue.");
        }
    }
    function insertOrAppendPlacementNodeIntoContainer(node, before, parent) {
        var tag = node.tag;
        var isHost = tag === HostComponent || tag === HostText;
        if (isHost) {
            var stateNode = node.stateNode;
            if (before) insertInContainerBefore(parent, stateNode, before);
            else appendChildToContainer(parent, stateNode);
        } else if (tag === HostPortal) ;
        else {
            var child = node.child;
            if (child !== null) {
                insertOrAppendPlacementNodeIntoContainer(child, before, parent);
                var sibling = child.sibling;
                while(sibling !== null){
                    insertOrAppendPlacementNodeIntoContainer(sibling, before, parent);
                    sibling = sibling.sibling;
                }
            }
        }
    }
    function insertOrAppendPlacementNode(node, before, parent) {
        var tag = node.tag;
        var isHost = tag === HostComponent || tag === HostText;
        if (isHost) {
            var stateNode = node.stateNode;
            if (before) insertBefore(parent, stateNode, before);
            else appendChild(parent, stateNode);
        } else if (tag === HostPortal) ;
        else {
            var child = node.child;
            if (child !== null) {
                insertOrAppendPlacementNode(child, before, parent);
                var sibling = child.sibling;
                while(sibling !== null){
                    insertOrAppendPlacementNode(sibling, before, parent);
                    sibling = sibling.sibling;
                }
            }
        }
    } // These are tracked on the stack as we recursively traverse a
    // deleted subtree.
    // TODO: Update these during the whole mutation phase, not just during
    // a deletion.
    var hostParent = null;
    var hostParentIsContainer = false;
    function commitDeletionEffects(root, returnFiber, deletedFiber) {
        // We only have the top Fiber that was deleted but we need to recurse down its
        // children to find all the terminal nodes.
        // Recursively delete all host nodes from the parent, detach refs, clean
        // up mounted layout effects, and call componentWillUnmount.
        // We only need to remove the topmost host child in each branch. But then we
        // still need to keep traversing to unmount effects, refs, and cWU. TODO: We
        // could split this into two separate traversals functions, where the second
        // one doesn't include any removeChild logic. This is maybe the same
        // function as "disappearLayoutEffects" (or whatever that turns into after
        // the layout phase is refactored to use recursion).
        // Before starting, find the nearest host parent on the stack so we know
        // which instance/container to remove the children from.
        // TODO: Instead of searching up the fiber return path on every deletion, we
        // can track the nearest host component on the JS stack as we traverse the
        // tree during the commit phase. This would make insertions faster, too.
        var parent = returnFiber;
        findParent: while(parent !== null){
            switch(parent.tag){
                case HostComponent:
                    hostParent = parent.stateNode;
                    hostParentIsContainer = false;
                    break findParent;
                case HostRoot:
                    hostParent = parent.stateNode.containerInfo;
                    hostParentIsContainer = true;
                    break findParent;
                case HostPortal:
                    hostParent = parent.stateNode.containerInfo;
                    hostParentIsContainer = true;
                    break findParent;
            }
            parent = parent.return;
        }
        if (hostParent === null) throw new Error("Expected to find a host parent. This error is likely caused by a bug in React. Please file an issue.");
        commitDeletionEffectsOnFiber(root, returnFiber, deletedFiber);
        hostParent = null;
        hostParentIsContainer = false;
        detachFiberMutation(deletedFiber);
    }
    function recursivelyTraverseDeletionEffects(finishedRoot, nearestMountedAncestor, parent) {
        // TODO: Use a static flag to skip trees that don't have unmount effects
        var child = parent.child;
        while(child !== null){
            commitDeletionEffectsOnFiber(finishedRoot, nearestMountedAncestor, child);
            child = child.sibling;
        }
    }
    function commitDeletionEffectsOnFiber(finishedRoot, nearestMountedAncestor, deletedFiber) {
        onCommitUnmount(deletedFiber); // The cases in this outer switch modify the stack before they traverse
        // into their subtree. There are simpler cases in the inner switch
        // that don't modify the stack.
        switch(deletedFiber.tag){
            case HostComponent:
                if (!offscreenSubtreeWasHidden) safelyDetachRef(deletedFiber, nearestMountedAncestor);
                 // Intentional fallthrough to next branch
            // eslint-disable-next-line-no-fallthrough
            case HostText:
                var prevHostParent = hostParent;
                var prevHostParentIsContainer = hostParentIsContainer;
                hostParent = null;
                recursivelyTraverseDeletionEffects(finishedRoot, nearestMountedAncestor, deletedFiber);
                hostParent = prevHostParent;
                hostParentIsContainer = prevHostParentIsContainer;
                if (hostParent !== null) {
                    // Now that all the child effects have unmounted, we can remove the
                    // node from the tree.
                    if (hostParentIsContainer) removeChildFromContainer(hostParent, deletedFiber.stateNode);
                    else removeChild(hostParent, deletedFiber.stateNode);
                }
                return;
            case DehydratedFragment:
                if (hostParent !== null) {
                    if (hostParentIsContainer) clearSuspenseBoundaryFromContainer(hostParent, deletedFiber.stateNode);
                    else clearSuspenseBoundary(hostParent, deletedFiber.stateNode);
                }
                return;
            case HostPortal:
                // When we go into a portal, it becomes the parent to remove from.
                var _prevHostParent = hostParent;
                var _prevHostParentIsContainer = hostParentIsContainer;
                hostParent = deletedFiber.stateNode.containerInfo;
                hostParentIsContainer = true;
                recursivelyTraverseDeletionEffects(finishedRoot, nearestMountedAncestor, deletedFiber);
                hostParent = _prevHostParent;
                hostParentIsContainer = _prevHostParentIsContainer;
                return;
            case FunctionComponent:
            case ForwardRef:
            case MemoComponent:
            case SimpleMemoComponent:
                if (!offscreenSubtreeWasHidden) {
                    var updateQueue = deletedFiber.updateQueue;
                    if (updateQueue !== null) {
                        var lastEffect = updateQueue.lastEffect;
                        if (lastEffect !== null) {
                            var firstEffect = lastEffect.next;
                            var effect = firstEffect;
                            do {
                                var _effect = effect, destroy = _effect.destroy, tag = _effect.tag;
                                if (destroy !== undefined) {
                                    if ((tag & Insertion) !== NoFlags$1) safelyCallDestroy(deletedFiber, nearestMountedAncestor, destroy);
                                    else if ((tag & Layout) !== NoFlags$1) {
                                        markComponentLayoutEffectUnmountStarted(deletedFiber);
                                        if (deletedFiber.mode & ProfileMode) {
                                            startLayoutEffectTimer();
                                            safelyCallDestroy(deletedFiber, nearestMountedAncestor, destroy);
                                            recordLayoutEffectDuration(deletedFiber);
                                        } else safelyCallDestroy(deletedFiber, nearestMountedAncestor, destroy);
                                        markComponentLayoutEffectUnmountStopped();
                                    }
                                }
                                effect = effect.next;
                            }while (effect !== firstEffect)
                        }
                    }
                }
                recursivelyTraverseDeletionEffects(finishedRoot, nearestMountedAncestor, deletedFiber);
                return;
            case ClassComponent:
                if (!offscreenSubtreeWasHidden) {
                    safelyDetachRef(deletedFiber, nearestMountedAncestor);
                    var instance = deletedFiber.stateNode;
                    if (typeof instance.componentWillUnmount === 'function') safelyCallComponentWillUnmount(deletedFiber, nearestMountedAncestor, instance);
                }
                recursivelyTraverseDeletionEffects(finishedRoot, nearestMountedAncestor, deletedFiber);
                return;
            case ScopeComponent:
                recursivelyTraverseDeletionEffects(finishedRoot, nearestMountedAncestor, deletedFiber);
                return;
            case OffscreenComponent:
                if (deletedFiber.mode & ConcurrentMode) {
                    // If this offscreen component is hidden, we already unmounted it. Before
                    // deleting the children, track that it's already unmounted so that we
                    // don't attempt to unmount the effects again.
                    // TODO: If the tree is hidden, in most cases we should be able to skip
                    // over the nested children entirely. An exception is we haven't yet found
                    // the topmost host node to delete, which we already track on the stack.
                    // But the other case is portals, which need to be detached no matter how
                    // deeply they are nested. We should use a subtree flag to track whether a
                    // subtree includes a nested portal.
                    var prevOffscreenSubtreeWasHidden = offscreenSubtreeWasHidden;
                    offscreenSubtreeWasHidden = prevOffscreenSubtreeWasHidden || deletedFiber.memoizedState !== null;
                    recursivelyTraverseDeletionEffects(finishedRoot, nearestMountedAncestor, deletedFiber);
                    offscreenSubtreeWasHidden = prevOffscreenSubtreeWasHidden;
                } else recursivelyTraverseDeletionEffects(finishedRoot, nearestMountedAncestor, deletedFiber);
                break;
            default:
                recursivelyTraverseDeletionEffects(finishedRoot, nearestMountedAncestor, deletedFiber);
                return;
        }
    }
    function commitSuspenseCallback(finishedWork) {
        // TODO: Move this to passive phase
        var newState = finishedWork.memoizedState;
    }
    function commitSuspenseHydrationCallbacks(finishedRoot, finishedWork) {
        var newState = finishedWork.memoizedState;
        if (newState === null) {
            var current = finishedWork.alternate;
            if (current !== null) {
                var prevState = current.memoizedState;
                if (prevState !== null) {
                    var suspenseInstance = prevState.dehydrated;
                    if (suspenseInstance !== null) commitHydratedSuspenseInstance(suspenseInstance);
                }
            }
        }
    }
    function attachSuspenseRetryListeners(finishedWork) {
        // If this boundary just timed out, then it will have a set of wakeables.
        // For each wakeable, attach a listener so that when it resolves, React
        // attempts to re-render the boundary in the primary (pre-timeout) state.
        var wakeables = finishedWork.updateQueue;
        if (wakeables !== null) {
            finishedWork.updateQueue = null;
            var retryCache = finishedWork.stateNode;
            if (retryCache === null) retryCache = finishedWork.stateNode = new PossiblyWeakSet();
            wakeables.forEach(function(wakeable) {
                // Memoize using the boundary fiber to prevent redundant listeners.
                var retry = resolveRetryWakeable.bind(null, finishedWork, wakeable);
                if (!retryCache.has(wakeable)) {
                    retryCache.add(wakeable);
                    if (isDevToolsPresent) {
                        if (inProgressLanes !== null && inProgressRoot !== null) // If we have pending work still, associate the original updaters with it.
                        restorePendingUpdaters(inProgressRoot, inProgressLanes);
                        else throw Error('Expected finished root and lanes to be set. This is a bug in React.');
                    }
                    wakeable.then(retry, retry);
                }
            });
        }
    } // This function detects when a Suspense boundary goes from visible to hidden.
    function commitMutationEffects(root, finishedWork, committedLanes) {
        inProgressLanes = committedLanes;
        inProgressRoot = root;
        setCurrentFiber(finishedWork);
        commitMutationEffectsOnFiber(finishedWork, root);
        setCurrentFiber(finishedWork);
        inProgressLanes = null;
        inProgressRoot = null;
    }
    function recursivelyTraverseMutationEffects(root, parentFiber, lanes) {
        // Deletions effects can be scheduled on any fiber type. They need to happen
        // before the children effects hae fired.
        var deletions = parentFiber.deletions;
        if (deletions !== null) for(var i = 0; i < deletions.length; i++){
            var childToDelete = deletions[i];
            try {
                commitDeletionEffects(root, parentFiber, childToDelete);
            } catch (error) {
                captureCommitPhaseError(childToDelete, parentFiber, error);
            }
        }
        var prevDebugFiber = getCurrentFiber();
        if (parentFiber.subtreeFlags & MutationMask) {
            var child = parentFiber.child;
            while(child !== null){
                setCurrentFiber(child);
                commitMutationEffectsOnFiber(child, root);
                child = child.sibling;
            }
        }
        setCurrentFiber(prevDebugFiber);
    }
    function commitMutationEffectsOnFiber(finishedWork, root, lanes) {
        var current = finishedWork.alternate;
        var flags = finishedWork.flags; // The effect flag should be checked *after* we refine the type of fiber,
        // because the fiber tag is more specific. An exception is any flag related
        // to reconcilation, because those can be set on all fiber types.
        switch(finishedWork.tag){
            case FunctionComponent:
            case ForwardRef:
            case MemoComponent:
            case SimpleMemoComponent:
                recursivelyTraverseMutationEffects(root, finishedWork);
                commitReconciliationEffects(finishedWork);
                if (flags & Update) {
                    try {
                        commitHookEffectListUnmount(Insertion | HasEffect, finishedWork, finishedWork.return);
                        commitHookEffectListMount(Insertion | HasEffect, finishedWork);
                    } catch (error) {
                        captureCommitPhaseError(finishedWork, finishedWork.return, error);
                    } // Layout effects are destroyed during the mutation phase so that all
                    // destroy functions for all fibers are called before any create functions.
                    // This prevents sibling component effects from interfering with each other,
                    // e.g. a destroy function in one component should never override a ref set
                    // by a create function in another component during the same commit.
                    if (finishedWork.mode & ProfileMode) {
                        try {
                            startLayoutEffectTimer();
                            commitHookEffectListUnmount(Layout | HasEffect, finishedWork, finishedWork.return);
                        } catch (error) {
                            captureCommitPhaseError(finishedWork, finishedWork.return, error);
                        }
                        recordLayoutEffectDuration(finishedWork);
                    } else try {
                        commitHookEffectListUnmount(Layout | HasEffect, finishedWork, finishedWork.return);
                    } catch (error2) {
                        captureCommitPhaseError(finishedWork, finishedWork.return, error2);
                    }
                }
                return;
            case ClassComponent:
                recursivelyTraverseMutationEffects(root, finishedWork);
                commitReconciliationEffects(finishedWork);
                if (flags & Ref) {
                    if (current !== null) safelyDetachRef(current, current.return);
                }
                return;
            case HostComponent:
                recursivelyTraverseMutationEffects(root, finishedWork);
                commitReconciliationEffects(finishedWork);
                if (flags & Ref) {
                    if (current !== null) safelyDetachRef(current, current.return);
                }
                // TODO: ContentReset gets cleared by the children during the commit
                // phase. This is a refactor hazard because it means we must read
                // flags the flags after `commitReconciliationEffects` has already run;
                // the order matters. We should refactor so that ContentReset does not
                // rely on mutating the flag during commit. Like by setting a flag
                // during the render phase instead.
                if (finishedWork.flags & ContentReset) {
                    var instance = finishedWork.stateNode;
                    try {
                        resetTextContent(instance);
                    } catch (error) {
                        captureCommitPhaseError(finishedWork, finishedWork.return, error);
                    }
                }
                if (flags & Update) {
                    var _instance4 = finishedWork.stateNode;
                    if (_instance4 != null) {
                        // Commit the work prepared earlier.
                        var newProps = finishedWork.memoizedProps; // For hydration we reuse the update path but we treat the oldProps
                        // as the newProps. The updatePayload will contain the real change in
                        // this case.
                        var oldProps = current !== null ? current.memoizedProps : newProps;
                        var type = finishedWork.type; // TODO: Type the updateQueue to be specific to host components.
                        var updatePayload = finishedWork.updateQueue;
                        finishedWork.updateQueue = null;
                        if (updatePayload !== null) try {
                            commitUpdate(_instance4, updatePayload, type, oldProps, newProps, finishedWork);
                        } catch (error) {
                            captureCommitPhaseError(finishedWork, finishedWork.return, error);
                        }
                    }
                }
                return;
            case HostText:
                recursivelyTraverseMutationEffects(root, finishedWork);
                commitReconciliationEffects(finishedWork);
                if (flags & Update) {
                    if (finishedWork.stateNode === null) throw new Error("This should have a text node initialized. This error is likely caused by a bug in React. Please file an issue.");
                    var textInstance = finishedWork.stateNode;
                    var newText = finishedWork.memoizedProps; // For hydration we reuse the update path but we treat the oldProps
                    // as the newProps. The updatePayload will contain the real change in
                    // this case.
                    var oldText = current !== null ? current.memoizedProps : newText;
                    try {
                        commitTextUpdate(textInstance, oldText, newText);
                    } catch (error) {
                        captureCommitPhaseError(finishedWork, finishedWork.return, error);
                    }
                }
                return;
            case HostRoot:
                recursivelyTraverseMutationEffects(root, finishedWork);
                commitReconciliationEffects(finishedWork);
                if (flags & Update) {
                    if (current !== null) {
                        var prevRootState = current.memoizedState;
                        if (prevRootState.isDehydrated) try {
                            commitHydratedContainer(root.containerInfo);
                        } catch (error) {
                            captureCommitPhaseError(finishedWork, finishedWork.return, error);
                        }
                    }
                }
                return;
            case HostPortal:
                recursivelyTraverseMutationEffects(root, finishedWork);
                commitReconciliationEffects(finishedWork);
                return;
            case SuspenseComponent:
                recursivelyTraverseMutationEffects(root, finishedWork);
                commitReconciliationEffects(finishedWork);
                var offscreenFiber = finishedWork.child;
                if (offscreenFiber.flags & Visibility) {
                    var newState = offscreenFiber.memoizedState;
                    var isHidden = newState !== null;
                    if (isHidden) {
                        var wasHidden = offscreenFiber.alternate !== null && offscreenFiber.alternate.memoizedState !== null;
                        if (!wasHidden) // TODO: Move to passive phase
                        markCommitTimeOfFallback();
                    }
                }
                if (flags & Update) {
                    try {
                        commitSuspenseCallback(finishedWork);
                    } catch (error) {
                        captureCommitPhaseError(finishedWork, finishedWork.return, error);
                    }
                    attachSuspenseRetryListeners(finishedWork);
                }
                return;
            case OffscreenComponent:
                var _wasHidden = current !== null && current.memoizedState !== null;
                if (finishedWork.mode & ConcurrentMode) {
                    // Before committing the children, track on the stack whether this
                    // offscreen subtree was already hidden, so that we don't unmount the
                    // effects again.
                    var prevOffscreenSubtreeWasHidden = offscreenSubtreeWasHidden;
                    offscreenSubtreeWasHidden = prevOffscreenSubtreeWasHidden || _wasHidden;
                    recursivelyTraverseMutationEffects(root, finishedWork);
                    offscreenSubtreeWasHidden = prevOffscreenSubtreeWasHidden;
                } else recursivelyTraverseMutationEffects(root, finishedWork);
                commitReconciliationEffects(finishedWork);
                if (flags & Visibility) {
                    var _newState = finishedWork.memoizedState;
                    var _isHidden = _newState !== null;
                    var offscreenBoundary = finishedWork;
                    // TODO: This needs to run whenever there's an insertion or update
                    // inside a hidden Offscreen tree.
                    hideOrUnhideAllChildren(offscreenBoundary, _isHidden);
                    if (_isHidden) {
                        if (!_wasHidden) {
                            if ((offscreenBoundary.mode & ConcurrentMode) !== NoMode) {
                                nextEffect = offscreenBoundary;
                                var offscreenChild = offscreenBoundary.child;
                                while(offscreenChild !== null){
                                    nextEffect = offscreenChild;
                                    disappearLayoutEffects_begin(offscreenChild);
                                    offscreenChild = offscreenChild.sibling;
                                }
                            }
                        }
                    }
                }
                return;
            case SuspenseListComponent:
                recursivelyTraverseMutationEffects(root, finishedWork);
                commitReconciliationEffects(finishedWork);
                if (flags & Update) attachSuspenseRetryListeners(finishedWork);
                return;
            case ScopeComponent:
                return;
            default:
                recursivelyTraverseMutationEffects(root, finishedWork);
                commitReconciliationEffects(finishedWork);
                return;
        }
    }
    function commitReconciliationEffects(finishedWork) {
        // Placement effects (insertions, reorders) can be scheduled on any fiber
        // type. They needs to happen after the children effects have fired, but
        // before the effects on this fiber have fired.
        var flags = finishedWork.flags;
        if (flags & Placement) {
            try {
                commitPlacement(finishedWork);
            } catch (error) {
                captureCommitPhaseError(finishedWork, finishedWork.return, error);
            } // Clear the "placement" from effect tag so that we know that this is
            // inserted, before any life-cycles like componentDidMount gets called.
            // TODO: findDOMNode doesn't rely on this any more but isMounted does
            // and isMounted is deprecated anyway so we should be able to kill this.
            finishedWork.flags &= ~Placement;
        }
        if (flags & Hydrating) finishedWork.flags &= ~Hydrating;
    }
    function commitLayoutEffects(finishedWork, root, committedLanes) {
        inProgressLanes = committedLanes;
        inProgressRoot = root;
        nextEffect = finishedWork;
        commitLayoutEffects_begin(finishedWork, root, committedLanes);
        inProgressLanes = null;
        inProgressRoot = null;
    }
    function commitLayoutEffects_begin(subtreeRoot, root, committedLanes) {
        // Suspense layout effects semantics don't change for legacy roots.
        var isModernRoot = (subtreeRoot.mode & ConcurrentMode) !== NoMode;
        while(nextEffect !== null){
            var fiber = nextEffect;
            var firstChild = fiber.child;
            if (fiber.tag === OffscreenComponent && isModernRoot) {
                // Keep track of the current Offscreen stack's state.
                var isHidden = fiber.memoizedState !== null;
                var newOffscreenSubtreeIsHidden = isHidden || offscreenSubtreeIsHidden;
                if (newOffscreenSubtreeIsHidden) {
                    // The Offscreen tree is hidden. Skip over its layout effects.
                    commitLayoutMountEffects_complete(subtreeRoot, root, committedLanes);
                    continue;
                } else {
                    // TODO (Offscreen) Also check: subtreeFlags & LayoutMask
                    var current = fiber.alternate;
                    var wasHidden = current !== null && current.memoizedState !== null;
                    var newOffscreenSubtreeWasHidden = wasHidden || offscreenSubtreeWasHidden;
                    var prevOffscreenSubtreeIsHidden = offscreenSubtreeIsHidden;
                    var prevOffscreenSubtreeWasHidden = offscreenSubtreeWasHidden; // Traverse the Offscreen subtree with the current Offscreen as the root.
                    offscreenSubtreeIsHidden = newOffscreenSubtreeIsHidden;
                    offscreenSubtreeWasHidden = newOffscreenSubtreeWasHidden;
                    if (offscreenSubtreeWasHidden && !prevOffscreenSubtreeWasHidden) {
                        // This is the root of a reappearing boundary. Turn its layout effects
                        // back on.
                        nextEffect = fiber;
                        reappearLayoutEffects_begin(fiber);
                    }
                    var child = firstChild;
                    while(child !== null){
                        nextEffect = child;
                        commitLayoutEffects_begin(child, root, committedLanes);
                        child = child.sibling;
                    } // Restore Offscreen state and resume in our-progress traversal.
                    nextEffect = fiber;
                    offscreenSubtreeIsHidden = prevOffscreenSubtreeIsHidden;
                    offscreenSubtreeWasHidden = prevOffscreenSubtreeWasHidden;
                    commitLayoutMountEffects_complete(subtreeRoot, root, committedLanes);
                    continue;
                }
            }
            if ((fiber.subtreeFlags & LayoutMask) !== NoFlags && firstChild !== null) {
                firstChild.return = fiber;
                nextEffect = firstChild;
            } else commitLayoutMountEffects_complete(subtreeRoot, root, committedLanes);
        }
    }
    function commitLayoutMountEffects_complete(subtreeRoot, root, committedLanes) {
        while(nextEffect !== null){
            var fiber = nextEffect;
            if ((fiber.flags & LayoutMask) !== NoFlags) {
                var current = fiber.alternate;
                setCurrentFiber(fiber);
                try {
                    commitLayoutEffectOnFiber(root, current, fiber, committedLanes);
                } catch (error) {
                    captureCommitPhaseError(fiber, fiber.return, error);
                }
                resetCurrentFiber();
            }
            if (fiber === subtreeRoot) {
                nextEffect = null;
                return;
            }
            var sibling = fiber.sibling;
            if (sibling !== null) {
                sibling.return = fiber.return;
                nextEffect = sibling;
                return;
            }
            nextEffect = fiber.return;
        }
    }
    function disappearLayoutEffects_begin(subtreeRoot) {
        while(nextEffect !== null){
            var fiber = nextEffect;
            var firstChild = fiber.child; // TODO (Offscreen) Check: flags & (RefStatic | LayoutStatic)
            switch(fiber.tag){
                case FunctionComponent:
                case ForwardRef:
                case MemoComponent:
                case SimpleMemoComponent:
                    if (fiber.mode & ProfileMode) try {
                        startLayoutEffectTimer();
                        commitHookEffectListUnmount(Layout, fiber, fiber.return);
                    } finally{
                        recordLayoutEffectDuration(fiber);
                    }
                    else commitHookEffectListUnmount(Layout, fiber, fiber.return);
                    break;
                case ClassComponent:
                    // TODO (Offscreen) Check: flags & RefStatic
                    safelyDetachRef(fiber, fiber.return);
                    var instance = fiber.stateNode;
                    if (typeof instance.componentWillUnmount === 'function') safelyCallComponentWillUnmount(fiber, fiber.return, instance);
                    break;
                case HostComponent:
                    safelyDetachRef(fiber, fiber.return);
                    break;
                case OffscreenComponent:
                    // Check if this is a
                    var isHidden = fiber.memoizedState !== null;
                    if (isHidden) {
                        // Nested Offscreen tree is already hidden. Don't disappear
                        // its effects.
                        disappearLayoutEffects_complete(subtreeRoot);
                        continue;
                    }
                    break;
            } // TODO (Offscreen) Check: subtreeFlags & LayoutStatic
            if (firstChild !== null) {
                firstChild.return = fiber;
                nextEffect = firstChild;
            } else disappearLayoutEffects_complete(subtreeRoot);
        }
    }
    function disappearLayoutEffects_complete(subtreeRoot) {
        while(nextEffect !== null){
            var fiber = nextEffect;
            if (fiber === subtreeRoot) {
                nextEffect = null;
                return;
            }
            var sibling = fiber.sibling;
            if (sibling !== null) {
                sibling.return = fiber.return;
                nextEffect = sibling;
                return;
            }
            nextEffect = fiber.return;
        }
    }
    function reappearLayoutEffects_begin(subtreeRoot) {
        while(nextEffect !== null){
            var fiber = nextEffect;
            var firstChild = fiber.child;
            if (fiber.tag === OffscreenComponent) {
                var isHidden = fiber.memoizedState !== null;
                if (isHidden) {
                    // Nested Offscreen tree is still hidden. Don't re-appear its effects.
                    reappearLayoutEffects_complete(subtreeRoot);
                    continue;
                }
            } // TODO (Offscreen) Check: subtreeFlags & LayoutStatic
            if (firstChild !== null) {
                // This node may have been reused from a previous render, so we can't
                // assume its return pointer is correct.
                firstChild.return = fiber;
                nextEffect = firstChild;
            } else reappearLayoutEffects_complete(subtreeRoot);
        }
    }
    function reappearLayoutEffects_complete(subtreeRoot) {
        while(nextEffect !== null){
            var fiber = nextEffect; // TODO (Offscreen) Check: flags & LayoutStatic
            setCurrentFiber(fiber);
            try {
                reappearLayoutEffectsOnFiber(fiber);
            } catch (error) {
                captureCommitPhaseError(fiber, fiber.return, error);
            }
            resetCurrentFiber();
            if (fiber === subtreeRoot) {
                nextEffect = null;
                return;
            }
            var sibling = fiber.sibling;
            if (sibling !== null) {
                // This node may have been reused from a previous render, so we can't
                // assume its return pointer is correct.
                sibling.return = fiber.return;
                nextEffect = sibling;
                return;
            }
            nextEffect = fiber.return;
        }
    }
    function commitPassiveMountEffects(root, finishedWork, committedLanes, committedTransitions) {
        nextEffect = finishedWork;
        commitPassiveMountEffects_begin(finishedWork, root, committedLanes, committedTransitions);
    }
    function commitPassiveMountEffects_begin(subtreeRoot, root, committedLanes, committedTransitions) {
        while(nextEffect !== null){
            var fiber = nextEffect;
            var firstChild = fiber.child;
            if ((fiber.subtreeFlags & PassiveMask) !== NoFlags && firstChild !== null) {
                firstChild.return = fiber;
                nextEffect = firstChild;
            } else commitPassiveMountEffects_complete(subtreeRoot, root, committedLanes, committedTransitions);
        }
    }
    function commitPassiveMountEffects_complete(subtreeRoot, root, committedLanes, committedTransitions) {
        while(nextEffect !== null){
            var fiber = nextEffect;
            if ((fiber.flags & Passive) !== NoFlags) {
                setCurrentFiber(fiber);
                try {
                    commitPassiveMountOnFiber(root, fiber, committedLanes, committedTransitions);
                } catch (error) {
                    captureCommitPhaseError(fiber, fiber.return, error);
                }
                resetCurrentFiber();
            }
            if (fiber === subtreeRoot) {
                nextEffect = null;
                return;
            }
            var sibling = fiber.sibling;
            if (sibling !== null) {
                sibling.return = fiber.return;
                nextEffect = sibling;
                return;
            }
            nextEffect = fiber.return;
        }
    }
    function commitPassiveMountOnFiber(finishedRoot, finishedWork, committedLanes, committedTransitions) {
        switch(finishedWork.tag){
            case FunctionComponent:
            case ForwardRef:
            case SimpleMemoComponent:
                if (finishedWork.mode & ProfileMode) {
                    startPassiveEffectTimer();
                    try {
                        commitHookEffectListMount(Passive$1 | HasEffect, finishedWork);
                    } finally{
                        recordPassiveEffectDuration(finishedWork);
                    }
                } else commitHookEffectListMount(Passive$1 | HasEffect, finishedWork);
                break;
        }
    }
    function commitPassiveUnmountEffects(firstChild) {
        nextEffect = firstChild;
        commitPassiveUnmountEffects_begin();
    }
    function commitPassiveUnmountEffects_begin() {
        while(nextEffect !== null){
            var fiber = nextEffect;
            var child = fiber.child;
            if ((nextEffect.flags & ChildDeletion) !== NoFlags) {
                var deletions = fiber.deletions;
                if (deletions !== null) {
                    for(var i = 0; i < deletions.length; i++){
                        var fiberToDelete = deletions[i];
                        nextEffect = fiberToDelete;
                        commitPassiveUnmountEffectsInsideOfDeletedTree_begin(fiberToDelete, fiber);
                    }
                    // A fiber was deleted from this parent fiber, but it's still part of
                    // the previous (alternate) parent fiber's list of children. Because
                    // children are a linked list, an earlier sibling that's still alive
                    // will be connected to the deleted fiber via its `alternate`:
                    //
                    //   live fiber
                    //   --alternate--> previous live fiber
                    //   --sibling--> deleted fiber
                    //
                    // We can't disconnect `alternate` on nodes that haven't been deleted
                    // yet, but we can disconnect the `sibling` and `child` pointers.
                    var previousFiber = fiber.alternate;
                    if (previousFiber !== null) {
                        var detachedChild = previousFiber.child;
                        if (detachedChild !== null) {
                            previousFiber.child = null;
                            do {
                                var detachedSibling = detachedChild.sibling;
                                detachedChild.sibling = null;
                                detachedChild = detachedSibling;
                            }while (detachedChild !== null)
                        }
                    }
                    nextEffect = fiber;
                }
            }
            if ((fiber.subtreeFlags & PassiveMask) !== NoFlags && child !== null) {
                child.return = fiber;
                nextEffect = child;
            } else commitPassiveUnmountEffects_complete();
        }
    }
    function commitPassiveUnmountEffects_complete() {
        while(nextEffect !== null){
            var fiber = nextEffect;
            if ((fiber.flags & Passive) !== NoFlags) {
                setCurrentFiber(fiber);
                commitPassiveUnmountOnFiber(fiber);
                resetCurrentFiber();
            }
            var sibling = fiber.sibling;
            if (sibling !== null) {
                sibling.return = fiber.return;
                nextEffect = sibling;
                return;
            }
            nextEffect = fiber.return;
        }
    }
    function commitPassiveUnmountOnFiber(finishedWork) {
        switch(finishedWork.tag){
            case FunctionComponent:
            case ForwardRef:
            case SimpleMemoComponent:
                if (finishedWork.mode & ProfileMode) {
                    startPassiveEffectTimer();
                    commitHookEffectListUnmount(Passive$1 | HasEffect, finishedWork, finishedWork.return);
                    recordPassiveEffectDuration(finishedWork);
                } else commitHookEffectListUnmount(Passive$1 | HasEffect, finishedWork, finishedWork.return);
                break;
        }
    }
    function commitPassiveUnmountEffectsInsideOfDeletedTree_begin(deletedSubtreeRoot, nearestMountedAncestor) {
        while(nextEffect !== null){
            var fiber = nextEffect; // Deletion effects fire in parent -> child order
            // TODO: Check if fiber has a PassiveStatic flag
            setCurrentFiber(fiber);
            commitPassiveUnmountInsideDeletedTreeOnFiber(fiber, nearestMountedAncestor);
            resetCurrentFiber();
            var child = fiber.child; // TODO: Only traverse subtree if it has a PassiveStatic flag. (But, if we
            // do this, still need to handle `deletedTreeCleanUpLevel` correctly.)
            if (child !== null) {
                child.return = fiber;
                nextEffect = child;
            } else commitPassiveUnmountEffectsInsideOfDeletedTree_complete(deletedSubtreeRoot);
        }
    }
    function commitPassiveUnmountEffectsInsideOfDeletedTree_complete(deletedSubtreeRoot) {
        while(nextEffect !== null){
            var fiber = nextEffect;
            var sibling = fiber.sibling;
            var returnFiber = fiber.return;
            // Recursively traverse the entire deleted tree and clean up fiber fields.
            // This is more aggressive than ideal, and the long term goal is to only
            // have to detach the deleted tree at the root.
            detachFiberAfterEffects(fiber);
            if (fiber === deletedSubtreeRoot) {
                nextEffect = null;
                return;
            }
            if (sibling !== null) {
                sibling.return = returnFiber;
                nextEffect = sibling;
                return;
            }
            nextEffect = returnFiber;
        }
    }
    function commitPassiveUnmountInsideDeletedTreeOnFiber(current, nearestMountedAncestor) {
        switch(current.tag){
            case FunctionComponent:
            case ForwardRef:
            case SimpleMemoComponent:
                if (current.mode & ProfileMode) {
                    startPassiveEffectTimer();
                    commitHookEffectListUnmount(Passive$1, current, nearestMountedAncestor);
                    recordPassiveEffectDuration(current);
                } else commitHookEffectListUnmount(Passive$1, current, nearestMountedAncestor);
                break;
        }
    } // TODO: Reuse reappearLayoutEffects traversal here?
    function invokeLayoutEffectMountInDEV(fiber) {
        // We don't need to re-check StrictEffectsMode here.
        // This function is only called if that check has already passed.
        switch(fiber.tag){
            case FunctionComponent:
            case ForwardRef:
            case SimpleMemoComponent:
                try {
                    commitHookEffectListMount(Layout | HasEffect, fiber);
                } catch (error) {
                    captureCommitPhaseError(fiber, fiber.return, error);
                }
                break;
            case ClassComponent:
                var instance = fiber.stateNode;
                try {
                    instance.componentDidMount();
                } catch (error3) {
                    captureCommitPhaseError(fiber, fiber.return, error3);
                }
                break;
        }
    }
    function invokePassiveEffectMountInDEV(fiber) {
        // We don't need to re-check StrictEffectsMode here.
        // This function is only called if that check has already passed.
        switch(fiber.tag){
            case FunctionComponent:
            case ForwardRef:
            case SimpleMemoComponent:
                try {
                    commitHookEffectListMount(Passive$1 | HasEffect, fiber);
                } catch (error) {
                    captureCommitPhaseError(fiber, fiber.return, error);
                }
                break;
        }
    }
    function invokeLayoutEffectUnmountInDEV(fiber) {
        // We don't need to re-check StrictEffectsMode here.
        // This function is only called if that check has already passed.
        switch(fiber.tag){
            case FunctionComponent:
            case ForwardRef:
            case SimpleMemoComponent:
                try {
                    commitHookEffectListUnmount(Layout | HasEffect, fiber, fiber.return);
                } catch (error) {
                    captureCommitPhaseError(fiber, fiber.return, error);
                }
                break;
            case ClassComponent:
                var instance = fiber.stateNode;
                if (typeof instance.componentWillUnmount === 'function') safelyCallComponentWillUnmount(fiber, fiber.return, instance);
                break;
        }
    }
    function invokePassiveEffectUnmountInDEV(fiber) {
        // We don't need to re-check StrictEffectsMode here.
        // This function is only called if that check has already passed.
        switch(fiber.tag){
            case FunctionComponent:
            case ForwardRef:
            case SimpleMemoComponent:
                try {
                    commitHookEffectListUnmount(Passive$1 | HasEffect, fiber, fiber.return);
                } catch (error) {
                    captureCommitPhaseError(fiber, fiber.return, error);
                }
        }
    }
    var COMPONENT_TYPE = 0;
    var HAS_PSEUDO_CLASS_TYPE = 1;
    var ROLE_TYPE = 2;
    var TEST_NAME_TYPE = 3;
    var TEXT_TYPE = 4;
    if (typeof Symbol === 'function' && Symbol.for) {
        var symbolFor = Symbol.for;
        COMPONENT_TYPE = symbolFor('selector.component');
        HAS_PSEUDO_CLASS_TYPE = symbolFor('selector.has_pseudo_class');
        ROLE_TYPE = symbolFor('selector.role');
        TEST_NAME_TYPE = symbolFor('selector.test_id');
        TEXT_TYPE = symbolFor('selector.text');
    }
    var commitHooks = [];
    function onCommitRoot$1() {
        commitHooks.forEach(function(commitHook) {
            return commitHook();
        });
    }
    var ReactCurrentActQueue = ReactSharedInternals.ReactCurrentActQueue;
    function isLegacyActEnvironment(fiber) {
        // Legacy mode. We preserve the behavior of React 17's act. It assumes an
        // act environment whenever `jest` is defined, but you can still turn off
        // spurious warnings by setting IS_REACT_ACT_ENVIRONMENT explicitly
        // to false.
        var isReactActEnvironmentGlobal = typeof IS_REACT_ACT_ENVIRONMENT !== 'undefined' ? IS_REACT_ACT_ENVIRONMENT : undefined; // $FlowExpectedError - Flow doesn't know about jest
        var jestIsDefined = typeof jest !== 'undefined';
        return jestIsDefined && isReactActEnvironmentGlobal !== false;
    }
    function isConcurrentActEnvironment() {
        var isReactActEnvironmentGlobal = typeof IS_REACT_ACT_ENVIRONMENT !== 'undefined' ? IS_REACT_ACT_ENVIRONMENT : undefined;
        if (!isReactActEnvironmentGlobal && ReactCurrentActQueue.current !== null) // TODO: Include link to relevant documentation page.
        error1("The current testing environment is not configured to support act(...)");
        return isReactActEnvironmentGlobal;
    }
    var ceil = Math.ceil;
    var ReactCurrentDispatcher$2 = ReactSharedInternals.ReactCurrentDispatcher, ReactCurrentOwner$2 = ReactSharedInternals.ReactCurrentOwner, ReactCurrentBatchConfig$3 = ReactSharedInternals.ReactCurrentBatchConfig, ReactCurrentActQueue$1 = ReactSharedInternals.ReactCurrentActQueue;
    var NoContext = /*             */ 0;
    var BatchedContext = /*               */ 1;
    var RenderContext = /*                */ 2;
    var CommitContext = /*                */ 4;
    var RootInProgress = 0;
    var RootFatalErrored = 1;
    var RootErrored = 2;
    var RootSuspended = 3;
    var RootSuspendedWithDelay = 4;
    var RootCompleted = 5;
    var RootDidNotComplete = 6; // Describes where we are in the React execution stack
    var executionContext = NoContext; // The root we're working on
    var workInProgressRoot = null; // The fiber we're working on
    var workInProgress1 = null; // The lanes we're rendering
    var workInProgressRootRenderLanes = NoLanes; // Stack that allows components to change the render lanes for its subtree
    // This is a superset of the lanes we started working on at the root. The only
    // case where it's different from `workInProgressRootRenderLanes` is when we
    // enter a subtree that is hidden and needs to be unhidden: Suspense and
    // Offscreen component.
    //
    // Most things in the work loop should deal with workInProgressRootRenderLanes.
    // Most things in begin/complete phases should deal with subtreeRenderLanes.
    var subtreeRenderLanes1 = NoLanes;
    var subtreeRenderLanesCursor = createCursor(NoLanes); // Whether to root completed, errored, suspended, etc.
    var workInProgressRootExitStatus = RootInProgress; // A fatal error, if one is thrown
    var workInProgressRootFatalError = null; // "Included" lanes refer to lanes that were worked on during this render. It's
    // slightly different than `renderLanes` because `renderLanes` can change as you
    // enter and exit an Offscreen tree. This value is the combination of all render
    // lanes for the entire render phase.
    var workInProgressRootIncludedLanes = NoLanes; // The work left over by components that were visited during this render. Only
    // includes unprocessed updates, not work in bailed out children.
    var workInProgressRootSkippedLanes = NoLanes; // Lanes that were updated (in an interleaved event) during this render.
    var workInProgressRootInterleavedUpdatedLanes = NoLanes; // Lanes that were updated during the render phase (*not* an interleaved event).
    var workInProgressRootPingedLanes = NoLanes; // Errors that are thrown during the render phase.
    var workInProgressRootConcurrentErrors = null; // These are errors that we recovered from without surfacing them to the UI.
    // We will log them once the tree commits.
    var workInProgressRootRecoverableErrors = null; // The most recent time we committed a fallback. This lets us ensure a train
    // model where we don't commit new loading states in too quick succession.
    var globalMostRecentFallbackTime = 0;
    var FALLBACK_THROTTLE_MS = 500; // The absolute time for when we should start giving up on rendering
    // more and prefer CPU suspense heuristics instead.
    var workInProgressRootRenderTargetTime = Infinity; // How long a render is supposed to take before we start following CPU
    // suspense heuristics and opt out of rendering more content.
    var RENDER_TIMEOUT_MS = 500;
    var workInProgressTransitions = null;
    function resetRenderTimer() {
        workInProgressRootRenderTargetTime = now() + RENDER_TIMEOUT_MS;
    }
    function getRenderTargetTime() {
        return workInProgressRootRenderTargetTime;
    }
    var hasUncaughtError = false;
    var firstUncaughtError = null;
    var legacyErrorBoundariesThatAlreadyFailed = null; // Only used when enableProfilerNestedUpdateScheduledHook is true;
    var rootDoesHavePassiveEffects = false;
    var rootWithPendingPassiveEffects = null;
    var pendingPassiveEffectsLanes = NoLanes;
    var pendingPassiveProfilerEffects = [];
    var pendingPassiveTransitions = null; // Use these to prevent an infinite loop of nested updates
    var NESTED_UPDATE_LIMIT = 50;
    var nestedUpdateCount = 0;
    var rootWithNestedUpdates = null;
    var isFlushingPassiveEffects = false;
    var didScheduleUpdateDuringPassiveEffects = false;
    var NESTED_PASSIVE_UPDATE_LIMIT = 50;
    var nestedPassiveUpdateCount = 0;
    var rootWithPassiveNestedUpdates = null; // If two updates are scheduled within the same event, we should treat their
    // event times as simultaneous, even if the actual clock time has advanced
    // between the first and second call.
    var currentEventTime = NoTimestamp;
    var currentEventTransitionLane = NoLanes;
    var isRunningInsertionEffect = false;
    function getWorkInProgressRoot() {
        return workInProgressRoot;
    }
    function requestEventTime() {
        if ((executionContext & (RenderContext | CommitContext)) !== NoContext) // We're inside React, so it's fine to read the actual time.
        return now();
         // We're not inside React, so we may be in the middle of a browser event.
        if (currentEventTime !== NoTimestamp) // Use the same start time for all updates until we enter React again.
        return currentEventTime;
         // This is the first update since React yielded. Compute a new start time.
        currentEventTime = now();
        return currentEventTime;
    }
    function requestUpdateLane(fiber) {
        // Special cases
        var mode = fiber.mode;
        if ((mode & ConcurrentMode) === NoMode) return SyncLane;
        else if ((executionContext & RenderContext) !== NoContext && workInProgressRootRenderLanes !== NoLanes) // This is a render phase update. These are not officially supported. The
        // old behavior is to give this the same "thread" (lanes) as
        // whatever is currently rendering. So if you call `setState` on a component
        // that happens later in the same render, it will flush. Ideally, we want to
        // remove the special case and treat them as if they came from an
        // interleaved event. Regardless, this pattern is not officially supported.
        // This behavior is only a fallback. The flag only exists until we can roll
        // out the setState warning, since existing code might accidentally rely on
        // the current behavior.
        return pickArbitraryLane(workInProgressRootRenderLanes);
        var isTransition = requestCurrentTransition() !== NoTransition;
        if (isTransition) {
            if (ReactCurrentBatchConfig$3.transition !== null) {
                var transition = ReactCurrentBatchConfig$3.transition;
                if (!transition._updatedFibers) transition._updatedFibers = new Set();
                transition._updatedFibers.add(fiber);
            } // The algorithm for assigning an update to a lane should be stable for all
            // updates at the same priority within the same event. To do this, the
            // inputs to the algorithm must be the same.
            //
            // The trick we use is to cache the first of each of these inputs within an
            // event. Then reset the cached values once we can be sure the event is
            // over. Our heuristic for that is whenever we enter a concurrent work loop.
            if (currentEventTransitionLane === NoLane) // All transitions within the same event are assigned the same lane.
            currentEventTransitionLane = claimNextTransitionLane();
            return currentEventTransitionLane;
        } // Updates originating inside certain React methods, like flushSync, have
        // their priority set by tracking it with a context variable.
        //
        // The opaque type returned by the host config is internally a lane, so we can
        // use that directly.
        // TODO: Move this type conversion to the event priority module.
        var updateLane = getCurrentUpdatePriority();
        if (updateLane !== NoLane) return updateLane;
         // This update originated outside React. Ask the host environment for an
        // appropriate priority, based on the type of event.
        //
        // The opaque type returned by the host config is internally a lane, so we can
        // use that directly.
        // TODO: Move this type conversion to the event priority module.
        var eventLane = getCurrentEventPriority();
        return eventLane;
    }
    function requestRetryLane(fiber) {
        // This is a fork of `requestUpdateLane` designed specifically for Suspense
        // "retries" — a special update that attempts to flip a Suspense boundary
        // from its placeholder state to its primary/resolved state.
        // Special cases
        var mode = fiber.mode;
        if ((mode & ConcurrentMode) === NoMode) return SyncLane;
        return claimNextRetryLane();
    }
    function scheduleUpdateOnFiber(fiber, lane, eventTime) {
        checkForNestedUpdates();
        if (isRunningInsertionEffect) error1('useInsertionEffect must not schedule updates.');
        var root = markUpdateLaneFromFiberToRoot(fiber, lane);
        if (root === null) return null;
        if (isFlushingPassiveEffects) didScheduleUpdateDuringPassiveEffects = true;
        markRootUpdated(root, lane, eventTime);
        if ((executionContext & RenderContext) !== NoLanes && root === workInProgressRoot) // This update was dispatched during the render phase. This is a mistake
        // if the update originates from user space (with the exception of local
        // hook updates, which are handled differently and don't reach this
        // function), but there are some internal React features that use this as
        // an implementation detail, like selective hydration.
        warnAboutRenderPhaseUpdatesInDEV(fiber); // Track lanes that were updated during the render phase
        else {
            if (isDevToolsPresent) addFiberToLanesMap(root, fiber, lane);
            warnIfUpdatesNotWrappedWithActDEV(fiber);
            if (root === workInProgressRoot) {
                // TODO: Consolidate with `isInterleavedUpdate` check
                // Received an update to a tree that's in the middle of rendering. Mark
                // that there was an interleaved update work on this root. Unless the
                // `deferRenderPhaseUpdateToNextBatch` flag is off and this is a render
                // phase update. In that case, we don't treat render phase updates as if
                // they were interleaved, for backwards compat reasons.
                if ((executionContext & RenderContext) === NoContext) workInProgressRootInterleavedUpdatedLanes = mergeLanes(workInProgressRootInterleavedUpdatedLanes, lane);
                if (workInProgressRootExitStatus === RootSuspendedWithDelay) // The root already suspended with a delay, which means this render
                // definitely won't finish. Since we have a new update, let's mark it as
                // suspended now, right before marking the incoming update. This has the
                // effect of interrupting the current render and switching to the update.
                // TODO: Make sure this doesn't override pings that happen while we've
                // already started rendering.
                markRootSuspended$1(root, workInProgressRootRenderLanes);
            }
            ensureRootIsScheduled(root, eventTime);
            if (lane === SyncLane && executionContext === NoContext && (fiber.mode & ConcurrentMode) === NoMode && !ReactCurrentActQueue$1.isBatchingLegacy) {
                // Flush the synchronous work now, unless we're already working or inside
                // a batch. This is intentionally inside scheduleUpdateOnFiber instead of
                // scheduleCallbackForFiber to preserve the ability to schedule a callback
                // without immediately flushing it. We only do this for user-initiated
                // updates, to preserve historical behavior of legacy mode.
                resetRenderTimer();
                flushSyncCallbacksOnlyInLegacyMode();
            }
        }
        return root;
    }
    function scheduleInitialHydrationOnRoot(root, lane, eventTime) {
        // This is a special fork of scheduleUpdateOnFiber that is only used to
        // schedule the initial hydration of a root that has just been created. Most
        // of the stuff in scheduleUpdateOnFiber can be skipped.
        //
        // The main reason for this separate path, though, is to distinguish the
        // initial children from subsequent updates. In fully client-rendered roots
        // (createRoot instead of hydrateRoot), all top-level renders are modeled as
        // updates, but hydration roots are special because the initial render must
        // match what was rendered on the server.
        var current = root.current;
        current.lanes = lane;
        markRootUpdated(root, lane, eventTime);
        ensureRootIsScheduled(root, eventTime);
    } // This is split into a separate function so we can mark a fiber with pending
    // work without treating it as a typical update that originates from an event;
    // e.g. retrying a Suspense boundary isn't an update, but it does schedule work
    // on a fiber.
    function markUpdateLaneFromFiberToRoot(sourceFiber, lane) {
        // Update the source fiber's lanes
        sourceFiber.lanes = mergeLanes(sourceFiber.lanes, lane);
        var alternate = sourceFiber.alternate;
        if (alternate !== null) alternate.lanes = mergeLanes(alternate.lanes, lane);
        if (alternate === null && (sourceFiber.flags & (Placement | Hydrating)) !== NoFlags) warnAboutUpdateOnNotYetMountedFiberInDEV(sourceFiber);
        var node = sourceFiber;
        var parent = sourceFiber.return;
        while(parent !== null){
            parent.childLanes = mergeLanes(parent.childLanes, lane);
            alternate = parent.alternate;
            if (alternate !== null) alternate.childLanes = mergeLanes(alternate.childLanes, lane);
            else if ((parent.flags & (Placement | Hydrating)) !== NoFlags) warnAboutUpdateOnNotYetMountedFiberInDEV(sourceFiber);
            node = parent;
            parent = parent.return;
        }
        if (node.tag === HostRoot) {
            var root = node.stateNode;
            return root;
        } else return null;
    }
    function isInterleavedUpdate(fiber, lane) {
        return(// Requires some refactoring. Not a big deal though since it's rare for
        // concurrent apps to have more than a single root.
        (workInProgressRoot !== null || // we should treat this as an interleaved update, too. This is also a
        // defensive coding measure in case a new update comes in between when
        // rendering has finished and when the interleaved updates are transferred
        // to the main queue.
        hasInterleavedUpdates()) && (fiber.mode & ConcurrentMode) !== NoMode && // then don't treat this as an interleaved update. This pattern is
        // accompanied by a warning but we haven't fully deprecated it yet. We can
        // remove once the deferRenderPhaseUpdateToNextBatch flag is enabled.
        (executionContext & RenderContext) === NoContext);
    } // Use this function to schedule a task for a root. There's only one task per
    // root; if a task was already scheduled, we'll check to make sure the priority
    // of the existing task is the same as the priority of the next level that the
    // root has work on. This function is called on every update, and right before
    // exiting a task.
    function ensureRootIsScheduled(root, currentTime) {
        var existingCallbackNode = root.callbackNode; // Check if any lanes are being starved by other work. If so, mark them as
        // expired so we know to work on those next.
        markStarvedLanesAsExpired(root, currentTime); // Determine the next lanes to work on, and their priority.
        var nextLanes = getNextLanes(root, root === workInProgressRoot ? workInProgressRootRenderLanes : NoLanes);
        if (nextLanes === NoLanes) {
            // Special case: There's nothing to work on.
            if (existingCallbackNode !== null) cancelCallback$1(existingCallbackNode);
            root.callbackNode = null;
            root.callbackPriority = NoLane;
            return;
        } // We use the highest priority lane to represent the priority of the callback.
        var newCallbackPriority = getHighestPriorityLane(nextLanes); // Check if there's an existing task. We may be able to reuse it.
        var existingCallbackPriority = root.callbackPriority;
        if (existingCallbackPriority === newCallbackPriority && // Scheduler task, rather than an `act` task, cancel it and re-scheduled
        // on the `act` queue.
        !(ReactCurrentActQueue$1.current !== null && existingCallbackNode !== fakeActCallbackNode)) {
            // If we're going to re-use an existing task, it needs to exist.
            // Assume that discrete update microtasks are non-cancellable and null.
            // TODO: Temporary until we confirm this warning is not fired.
            if (existingCallbackNode == null && existingCallbackPriority !== SyncLane) error1('Expected scheduled callback to exist. This error is likely caused by a bug in React. Please file an issue.');
            return;
        }
        if (existingCallbackNode != null) // Cancel the existing callback. We'll schedule a new one below.
        cancelCallback$1(existingCallbackNode);
         // Schedule a new callback.
        var newCallbackNode;
        if (newCallbackPriority === SyncLane) {
            // Special case: Sync React callbacks are scheduled on a special
            // internal queue
            if (root.tag === LegacyRoot) {
                if (ReactCurrentActQueue$1.isBatchingLegacy !== null) ReactCurrentActQueue$1.didScheduleLegacyUpdate = true;
                scheduleLegacySyncCallback(performSyncWorkOnRoot.bind(null, root));
            } else scheduleSyncCallback(performSyncWorkOnRoot.bind(null, root));
            // Flush the queue in a microtask.
            if (ReactCurrentActQueue$1.current !== null) // Inside `act`, use our internal `act` queue so that these get flushed
            // at the end of the current scope even when using the sync version
            // of `act`.
            ReactCurrentActQueue$1.current.push(flushSyncCallbacks);
            else scheduleMicrotask(function() {
                // In Safari, appending an iframe forces microtasks to run.
                // https://github.com/facebook/react/issues/22459
                // We don't support running callbacks in the middle of render
                // or commit so we need to check against that.
                if (executionContext === NoContext) // It's only safe to do this conditionally because we always
                // check for pending work before we exit the task.
                flushSyncCallbacks();
            });
            newCallbackNode = null;
        } else {
            var schedulerPriorityLevel;
            switch(lanesToEventPriority(nextLanes)){
                case DiscreteEventPriority:
                    schedulerPriorityLevel = ImmediatePriority;
                    break;
                case ContinuousEventPriority:
                    schedulerPriorityLevel = UserBlockingPriority;
                    break;
                case DefaultEventPriority:
                    schedulerPriorityLevel = NormalPriority;
                    break;
                case IdleEventPriority:
                    schedulerPriorityLevel = IdlePriority;
                    break;
                default:
                    schedulerPriorityLevel = NormalPriority;
                    break;
            }
            newCallbackNode = scheduleCallback$1(schedulerPriorityLevel, performConcurrentWorkOnRoot.bind(null, root));
        }
        root.callbackPriority = newCallbackPriority;
        root.callbackNode = newCallbackNode;
    } // This is the entry point for every concurrent task, i.e. anything that
    // goes through Scheduler.
    function performConcurrentWorkOnRoot(root, didTimeout) {
        resetNestedUpdateFlag();
        // event time. The next update will compute a new event time.
        currentEventTime = NoTimestamp;
        currentEventTransitionLane = NoLanes;
        if ((executionContext & (RenderContext | CommitContext)) !== NoContext) throw new Error('Should not already be working.');
         // Flush any pending passive effects before deciding which lanes to work on,
        // in case they schedule additional work.
        var originalCallbackNode = root.callbackNode;
        var didFlushPassiveEffects = flushPassiveEffects();
        if (didFlushPassiveEffects) {
            // Something in the passive effect phase may have canceled the current task.
            // Check if the task node for this root was changed.
            if (root.callbackNode !== originalCallbackNode) // The current task was canceled. Exit. We don't need to call
            // `ensureRootIsScheduled` because the check above implies either that
            // there's a new task, or that there's no remaining work on this root.
            return null;
        } // Determine the next lanes to work on, using the fields stored
        // on the root.
        var lanes = getNextLanes(root, root === workInProgressRoot ? workInProgressRootRenderLanes : NoLanes);
        if (lanes === NoLanes) // Defensive coding. This is never expected to happen.
        return null;
         // We disable time-slicing in some cases: if the work has been CPU-bound
        // for too long ("expired" work, to prevent starvation), or we're in
        // sync-updates-by-default mode.
        // TODO: We only check `didTimeout` defensively, to account for a Scheduler
        // bug we're still investigating. Once the bug in Scheduler is fixed,
        // we can remove this, since we track expiration ourselves.
        var shouldTimeSlice = !includesBlockingLane(root, lanes) && !includesExpiredLane(root, lanes) && !didTimeout;
        var exitStatus = shouldTimeSlice ? renderRootConcurrent(root, lanes) : renderRootSync(root, lanes);
        if (exitStatus !== RootInProgress) {
            if (exitStatus === RootErrored) {
                // If something threw an error, try rendering one more time. We'll
                // render synchronously to block concurrent data mutations, and we'll
                // includes all pending updates are included. If it still fails after
                // the second attempt, we'll give up and commit the resulting tree.
                var errorRetryLanes = getLanesToRetrySynchronouslyOnError(root);
                if (errorRetryLanes !== NoLanes) {
                    lanes = errorRetryLanes;
                    exitStatus = recoverFromConcurrentError(root, errorRetryLanes);
                }
            }
            if (exitStatus === RootFatalErrored) {
                var fatalError = workInProgressRootFatalError;
                prepareFreshStack(root, NoLanes);
                markRootSuspended$1(root, lanes);
                ensureRootIsScheduled(root, now());
                throw fatalError;
            }
            if (exitStatus === RootDidNotComplete) // The render unwound without completing the tree. This happens in special
            // cases where need to exit the current render without producing a
            // consistent tree or committing.
            //
            // This should only happen during a concurrent render, not a discrete or
            // synchronous update. We should have already checked for this when we
            // unwound the stack.
            markRootSuspended$1(root, lanes);
            else {
                // The render completed.
                // Check if this render may have yielded to a concurrent event, and if so,
                // confirm that any newly rendered stores are consistent.
                // TODO: It's possible that even a concurrent render may never have yielded
                // to the main thread, if it was fast enough, or if it expired. We could
                // skip the consistency check in that case, too.
                var renderWasConcurrent = !includesBlockingLane(root, lanes);
                var finishedWork = root.current.alternate;
                if (renderWasConcurrent && !isRenderConsistentWithExternalStores(finishedWork)) {
                    // A store was mutated in an interleaved event. Render again,
                    // synchronously, to block further mutations.
                    exitStatus = renderRootSync(root, lanes); // We need to check again if something threw
                    if (exitStatus === RootErrored) {
                        var _errorRetryLanes = getLanesToRetrySynchronouslyOnError(root);
                        if (_errorRetryLanes !== NoLanes) {
                            lanes = _errorRetryLanes;
                            exitStatus = recoverFromConcurrentError(root, _errorRetryLanes); // We assume the tree is now consistent because we didn't yield to any
                        // concurrent events.
                        }
                    }
                    if (exitStatus === RootFatalErrored) {
                        var _fatalError = workInProgressRootFatalError;
                        prepareFreshStack(root, NoLanes);
                        markRootSuspended$1(root, lanes);
                        ensureRootIsScheduled(root, now());
                        throw _fatalError;
                    }
                } // We now have a consistent tree. The next step is either to commit it,
                // or, if something suspended, wait to commit it after a timeout.
                root.finishedWork = finishedWork;
                root.finishedLanes = lanes;
                finishConcurrentRender(root, exitStatus, lanes);
            }
        }
        ensureRootIsScheduled(root, now());
        if (root.callbackNode === originalCallbackNode) // The task node scheduled for this root is the same one that's
        // currently executed. Need to return a continuation.
        return performConcurrentWorkOnRoot.bind(null, root);
        return null;
    }
    function recoverFromConcurrentError(root, errorRetryLanes) {
        // If an error occurred during hydration, discard server response and fall
        // back to client side render.
        // Before rendering again, save the errors from the previous attempt.
        var errorsFromFirstAttempt = workInProgressRootConcurrentErrors;
        if (isRootDehydrated(root)) {
            // The shell failed to hydrate. Set a flag to force a client rendering
            // during the next attempt. To do this, we call prepareFreshStack now
            // to create the root work-in-progress fiber. This is a bit weird in terms
            // of factoring, because it relies on renderRootSync not calling
            // prepareFreshStack again in the call below, which happens because the
            // root and lanes haven't changed.
            //
            // TODO: I think what we should do is set ForceClientRender inside
            // throwException, like we do for nested Suspense boundaries. The reason
            // it's here instead is so we can switch to the synchronous work loop, too.
            // Something to consider for a future refactor.
            var rootWorkInProgress = prepareFreshStack(root, errorRetryLanes);
            rootWorkInProgress.flags |= ForceClientRender;
            errorHydratingContainer(root.containerInfo);
        }
        var exitStatus = renderRootSync(root, errorRetryLanes);
        if (exitStatus !== RootErrored) {
            // Successfully finished rendering on retry
            // The errors from the failed first attempt have been recovered. Add
            // them to the collection of recoverable errors. We'll log them in the
            // commit phase.
            var errorsFromSecondAttempt = workInProgressRootRecoverableErrors;
            workInProgressRootRecoverableErrors = errorsFromFirstAttempt; // The errors from the second attempt should be queued after the errors
            // from the first attempt, to preserve the causal sequence.
            if (errorsFromSecondAttempt !== null) queueRecoverableErrors(errorsFromSecondAttempt);
        }
        return exitStatus;
    }
    function queueRecoverableErrors(errors) {
        if (workInProgressRootRecoverableErrors === null) workInProgressRootRecoverableErrors = errors;
        else workInProgressRootRecoverableErrors.push.apply(workInProgressRootRecoverableErrors, errors);
    }
    function finishConcurrentRender(root, exitStatus, lanes) {
        switch(exitStatus){
            case RootInProgress:
            case RootFatalErrored:
                throw new Error('Root did not complete. This is a bug in React.');
            // Flow knows about invariant, so it complains if I add a break
            // statement, but eslint doesn't know about invariant, so it complains
            // if I do. eslint-disable-next-line no-fallthrough
            case RootErrored:
                // We should have already attempted to retry this tree. If we reached
                // this point, it errored again. Commit it.
                commitRoot(root, workInProgressRootRecoverableErrors, workInProgressTransitions);
                break;
            case RootSuspended:
                markRootSuspended$1(root, lanes); // We have an acceptable loading state. We need to figure out if we
                // should immediately commit it or wait a bit.
                if (includesOnlyRetries(lanes) && !shouldForceFlushFallbacksInDEV()) {
                    // This render only included retries, no updates. Throttle committing
                    // retries so that we don't show too many loading states too quickly.
                    var msUntilTimeout = globalMostRecentFallbackTime + FALLBACK_THROTTLE_MS - now(); // Don't bother with a very short suspense time.
                    if (msUntilTimeout > 10) {
                        var nextLanes = getNextLanes(root, NoLanes);
                        if (nextLanes !== NoLanes) break;
                        var suspendedLanes = root.suspendedLanes;
                        if (!isSubsetOfLanes(suspendedLanes, lanes)) {
                            // We should prefer to render the fallback of at the last
                            // suspended level. Ping the last suspended level to try
                            // rendering it again.
                            // FIXME: What if the suspended lanes are Idle? Should not restart.
                            var eventTime = requestEventTime();
                            markRootPinged(root, suspendedLanes);
                            break;
                        } // The render is suspended, it hasn't timed out, and there's no
                        // lower priority work to do. Instead of committing the fallback
                        // immediately, wait for more data to arrive.
                        root.timeoutHandle = scheduleTimeout(commitRoot.bind(null, root, workInProgressRootRecoverableErrors, workInProgressTransitions), msUntilTimeout);
                        break;
                    }
                } // The work expired. Commit immediately.
                commitRoot(root, workInProgressRootRecoverableErrors, workInProgressTransitions);
                break;
            case RootSuspendedWithDelay:
                markRootSuspended$1(root, lanes);
                if (includesOnlyTransitions(lanes)) break;
                if (!shouldForceFlushFallbacksInDEV()) {
                    // This is not a transition, but we did trigger an avoided state.
                    // Schedule a placeholder to display after a short delay, using the Just
                    // Noticeable Difference.
                    // TODO: Is the JND optimization worth the added complexity? If this is
                    // the only reason we track the event time, then probably not.
                    // Consider removing.
                    var mostRecentEventTime = getMostRecentEventTime(root, lanes);
                    var eventTimeMs = mostRecentEventTime;
                    var timeElapsedMs = now() - eventTimeMs;
                    var _msUntilTimeout = jnd(timeElapsedMs) - timeElapsedMs; // Don't bother with a very short suspense time.
                    if (_msUntilTimeout > 10) {
                        // Instead of committing the fallback immediately, wait for more data
                        // to arrive.
                        root.timeoutHandle = scheduleTimeout(commitRoot.bind(null, root, workInProgressRootRecoverableErrors, workInProgressTransitions), _msUntilTimeout);
                        break;
                    }
                } // Commit the placeholder.
                commitRoot(root, workInProgressRootRecoverableErrors, workInProgressTransitions);
                break;
            case RootCompleted:
                // The work completed. Ready to commit.
                commitRoot(root, workInProgressRootRecoverableErrors, workInProgressTransitions);
                break;
            default:
                throw new Error('Unknown root exit status.');
        }
    }
    function isRenderConsistentWithExternalStores(finishedWork) {
        // Search the rendered tree for external store reads, and check whether the
        // stores were mutated in a concurrent event. Intentionally using an iterative
        // loop instead of recursion so we can exit early.
        var node = finishedWork;
        while(true){
            if (node.flags & StoreConsistency) {
                var updateQueue = node.updateQueue;
                if (updateQueue !== null) {
                    var checks = updateQueue.stores;
                    if (checks !== null) for(var i = 0; i < checks.length; i++){
                        var check = checks[i];
                        var getSnapshot = check.getSnapshot;
                        var renderedValue = check.value;
                        try {
                            if (!objectIs(getSnapshot(), renderedValue)) // Found an inconsistent store.
                            return false;
                        } catch (error) {
                            // If `getSnapshot` throws, return `false`. This will schedule
                            // a re-render, and the error will be rethrown during render.
                            return false;
                        }
                    }
                }
            }
            var child = node.child;
            if (node.subtreeFlags & StoreConsistency && child !== null) {
                child.return = node;
                node = child;
                continue;
            }
            if (node === finishedWork) return true;
            while(node.sibling === null){
                if (node.return === null || node.return === finishedWork) return true;
                node = node.return;
            }
            node.sibling.return = node.return;
            node = node.sibling;
        } // Flow doesn't know this is unreachable, but eslint does
        // eslint-disable-next-line no-unreachable
        return true;
    }
    function markRootSuspended$1(root, suspendedLanes) {
        // When suspending, we should always exclude lanes that were pinged or (more
        // rarely, since we try to avoid it) updated during the render phase.
        // TODO: Lol maybe there's a better way to factor this besides this
        // obnoxiously named function :)
        suspendedLanes = removeLanes(suspendedLanes, workInProgressRootPingedLanes);
        suspendedLanes = removeLanes(suspendedLanes, workInProgressRootInterleavedUpdatedLanes);
        markRootSuspended(root, suspendedLanes);
    } // This is the entry point for synchronous tasks that don't go
    // through Scheduler
    function performSyncWorkOnRoot(root) {
        syncNestedUpdateFlag();
        if ((executionContext & (RenderContext | CommitContext)) !== NoContext) throw new Error('Should not already be working.');
        flushPassiveEffects();
        var lanes = getNextLanes(root, NoLanes);
        if (!includesSomeLane(lanes, SyncLane)) {
            // There's no remaining sync work left.
            ensureRootIsScheduled(root, now());
            return null;
        }
        var exitStatus = renderRootSync(root, lanes);
        if (root.tag !== LegacyRoot && exitStatus === RootErrored) {
            // If something threw an error, try rendering one more time. We'll render
            // synchronously to block concurrent data mutations, and we'll includes
            // all pending updates are included. If it still fails after the second
            // attempt, we'll give up and commit the resulting tree.
            var errorRetryLanes = getLanesToRetrySynchronouslyOnError(root);
            if (errorRetryLanes !== NoLanes) {
                lanes = errorRetryLanes;
                exitStatus = recoverFromConcurrentError(root, errorRetryLanes);
            }
        }
        if (exitStatus === RootFatalErrored) {
            var fatalError = workInProgressRootFatalError;
            prepareFreshStack(root, NoLanes);
            markRootSuspended$1(root, lanes);
            ensureRootIsScheduled(root, now());
            throw fatalError;
        }
        if (exitStatus === RootDidNotComplete) throw new Error('Root did not complete. This is a bug in React.');
         // We now have a consistent tree. Because this is a sync render, we
        // will commit it even if something suspended.
        var finishedWork = root.current.alternate;
        root.finishedWork = finishedWork;
        root.finishedLanes = lanes;
        commitRoot(root, workInProgressRootRecoverableErrors, workInProgressTransitions); // Before exiting, make sure there's a callback scheduled for the next
        // pending level.
        ensureRootIsScheduled(root, now());
        return null;
    }
    function flushRoot(root, lanes) {
        if (lanes !== NoLanes) {
            markRootEntangled(root, mergeLanes(lanes, SyncLane));
            ensureRootIsScheduled(root, now());
            if ((executionContext & (RenderContext | CommitContext)) === NoContext) {
                resetRenderTimer();
                flushSyncCallbacks();
            }
        }
    }
    function batchedUpdates$1(fn, a) {
        var prevExecutionContext = executionContext;
        executionContext |= BatchedContext;
        try {
            return fn(a);
        } finally{
            executionContext = prevExecutionContext; // If there were legacy sync updates, flush them at the end of the outer
            // most batchedUpdates-like method.
            if (executionContext === NoContext && !ReactCurrentActQueue$1.isBatchingLegacy) {
                resetRenderTimer();
                flushSyncCallbacksOnlyInLegacyMode();
            }
        }
    }
    function discreteUpdates(fn, a, b, c, d) {
        var previousPriority = getCurrentUpdatePriority();
        var prevTransition = ReactCurrentBatchConfig$3.transition;
        try {
            ReactCurrentBatchConfig$3.transition = null;
            setCurrentUpdatePriority(DiscreteEventPriority);
            return fn(a, b, c, d);
        } finally{
            setCurrentUpdatePriority(previousPriority);
            ReactCurrentBatchConfig$3.transition = prevTransition;
            if (executionContext === NoContext) resetRenderTimer();
        }
    } // Overload the definition to the two valid signatures.
    // Warning, this opts-out of checking the function body.
    // eslint-disable-next-line no-redeclare
    function flushSync(fn) {
        // In legacy mode, we flush pending passive effects at the beginning of the
        // next event, not at the end of the previous one.
        if (rootWithPendingPassiveEffects !== null && rootWithPendingPassiveEffects.tag === LegacyRoot && (executionContext & (RenderContext | CommitContext)) === NoContext) flushPassiveEffects();
        var prevExecutionContext = executionContext;
        executionContext |= BatchedContext;
        var prevTransition = ReactCurrentBatchConfig$3.transition;
        var previousPriority = getCurrentUpdatePriority();
        try {
            ReactCurrentBatchConfig$3.transition = null;
            setCurrentUpdatePriority(DiscreteEventPriority);
            if (fn) return fn();
            else return undefined;
        } finally{
            setCurrentUpdatePriority(previousPriority);
            ReactCurrentBatchConfig$3.transition = prevTransition;
            executionContext = prevExecutionContext; // Flush the immediate callbacks that were scheduled during this batch.
            // Note that this will happen even if batchedUpdates is higher up
            // the stack.
            if ((executionContext & (RenderContext | CommitContext)) === NoContext) flushSyncCallbacks();
        }
    }
    function isAlreadyRendering() {
        // Used by the renderer to print a warning if certain APIs are called from
        // the wrong context.
        return (executionContext & (RenderContext | CommitContext)) !== NoContext;
    }
    function pushRenderLanes(fiber, lanes) {
        push(subtreeRenderLanesCursor, subtreeRenderLanes1, fiber);
        subtreeRenderLanes1 = mergeLanes(subtreeRenderLanes1, lanes);
        workInProgressRootIncludedLanes = mergeLanes(workInProgressRootIncludedLanes, lanes);
    }
    function popRenderLanes(fiber) {
        subtreeRenderLanes1 = subtreeRenderLanesCursor.current;
        pop(subtreeRenderLanesCursor, fiber);
    }
    function prepareFreshStack(root, lanes) {
        root.finishedWork = null;
        root.finishedLanes = NoLanes;
        var timeoutHandle = root.timeoutHandle;
        if (timeoutHandle !== noTimeout) {
            // The root previous suspended and scheduled a timeout to commit a fallback
            // state. Now that we have additional work, cancel the timeout.
            root.timeoutHandle = noTimeout; // $FlowFixMe Complains noTimeout is not a TimeoutID, despite the check above
            cancelTimeout(timeoutHandle);
        }
        if (workInProgress1 !== null) {
            var interruptedWork = workInProgress1.return;
            while(interruptedWork !== null){
                var current = interruptedWork.alternate;
                unwindInterruptedWork(current, interruptedWork);
                interruptedWork = interruptedWork.return;
            }
        }
        workInProgressRoot = root;
        var rootWorkInProgress = createWorkInProgress(root.current, null);
        workInProgress1 = rootWorkInProgress;
        workInProgressRootRenderLanes = subtreeRenderLanes1 = workInProgressRootIncludedLanes = lanes;
        workInProgressRootExitStatus = RootInProgress;
        workInProgressRootFatalError = null;
        workInProgressRootSkippedLanes = NoLanes;
        workInProgressRootInterleavedUpdatedLanes = NoLanes;
        workInProgressRootPingedLanes = NoLanes;
        workInProgressRootConcurrentErrors = null;
        workInProgressRootRecoverableErrors = null;
        enqueueInterleavedUpdates();
        ReactStrictModeWarnings.discardPendingWarnings();
        return rootWorkInProgress;
    }
    function handleError(root, thrownValue) {
        do {
            var erroredWork = workInProgress1;
            try {
                // Reset module-level state that was set during the render phase.
                resetContextDependencies();
                resetHooksAfterThrow();
                resetCurrentFiber(); // TODO: I found and added this missing line while investigating a
                // separate issue. Write a regression test using string refs.
                ReactCurrentOwner$2.current = null;
                if (erroredWork === null || erroredWork.return === null) {
                    // Expected to be working on a non-root fiber. This is a fatal error
                    // because there's no ancestor that can handle it; the root is
                    // supposed to capture all errors that weren't caught by an error
                    // boundary.
                    workInProgressRootExitStatus = RootFatalErrored;
                    workInProgressRootFatalError = thrownValue; // Set `workInProgress` to null. This represents advancing to the next
                    // sibling, or the parent if there are no siblings. But since the root
                    // has no siblings nor a parent, we set it to null. Usually this is
                    // handled by `completeUnitOfWork` or `unwindWork`, but since we're
                    // intentionally not calling those, we need set it here.
                    // TODO: Consider calling `unwindWork` to pop the contexts.
                    workInProgress1 = null;
                    return;
                }
                if (enableProfilerTimer && erroredWork.mode & ProfileMode) // Record the time spent rendering before an error was thrown. This
                // avoids inaccurate Profiler durations in the case of a
                // suspended render.
                stopProfilerTimerIfRunningAndRecordDelta(erroredWork, true);
                if (enableSchedulingProfiler) {
                    markComponentRenderStopped();
                    if (thrownValue !== null && typeof thrownValue === 'object' && typeof thrownValue.then === 'function') {
                        var wakeable = thrownValue;
                        markComponentSuspended(erroredWork, wakeable, workInProgressRootRenderLanes);
                    } else markComponentErrored(erroredWork, thrownValue, workInProgressRootRenderLanes);
                }
                throwException(root, erroredWork.return, erroredWork, thrownValue, workInProgressRootRenderLanes);
                completeUnitOfWork(erroredWork);
            } catch (yetAnotherThrownValue) {
                // Something in the return path also threw.
                thrownValue = yetAnotherThrownValue;
                if (workInProgress1 === erroredWork && erroredWork !== null) {
                    // If this boundary has already errored, then we had trouble processing
                    // the error. Bubble it to the next boundary.
                    erroredWork = erroredWork.return;
                    workInProgress1 = erroredWork;
                } else erroredWork = workInProgress1;
                continue;
            } // Return to the normal work loop.
            return;
        }while (true)
    }
    function pushDispatcher() {
        var prevDispatcher = ReactCurrentDispatcher$2.current;
        ReactCurrentDispatcher$2.current = ContextOnlyDispatcher;
        if (prevDispatcher === null) // The React isomorphic package does not include a default dispatcher.
        // Instead the first renderer will lazily attach one, in order to give
        // nicer error messages.
        return ContextOnlyDispatcher;
        else return prevDispatcher;
    }
    function popDispatcher(prevDispatcher) {
        ReactCurrentDispatcher$2.current = prevDispatcher;
    }
    function markCommitTimeOfFallback() {
        globalMostRecentFallbackTime = now();
    }
    function markSkippedUpdateLanes(lane) {
        workInProgressRootSkippedLanes = mergeLanes(lane, workInProgressRootSkippedLanes);
    }
    function renderDidSuspend() {
        if (workInProgressRootExitStatus === RootInProgress) workInProgressRootExitStatus = RootSuspended;
    }
    function renderDidSuspendDelayIfPossible() {
        if (workInProgressRootExitStatus === RootInProgress || workInProgressRootExitStatus === RootSuspended || workInProgressRootExitStatus === RootErrored) workInProgressRootExitStatus = RootSuspendedWithDelay;
         // Check if there are updates that we skipped tree that might have unblocked
        // this render.
        if (workInProgressRoot !== null && (includesNonIdleWork(workInProgressRootSkippedLanes) || includesNonIdleWork(workInProgressRootInterleavedUpdatedLanes))) // Mark the current render as suspended so that we switch to working on
        // the updates that were skipped. Usually we only suspend at the end of
        // the render phase.
        // TODO: We should probably always mark the root as suspended immediately
        // (inside this function), since by suspending at the end of the render
        // phase introduces a potential mistake where we suspend lanes that were
        // pinged or updated while we were rendering.
        markRootSuspended$1(workInProgressRoot, workInProgressRootRenderLanes);
    }
    function renderDidError(error) {
        if (workInProgressRootExitStatus !== RootSuspendedWithDelay) workInProgressRootExitStatus = RootErrored;
        if (workInProgressRootConcurrentErrors === null) workInProgressRootConcurrentErrors = [
            error
        ];
        else workInProgressRootConcurrentErrors.push(error);
    } // Called during render to determine if anything has suspended.
    // Returns false if we're not sure.
    function renderHasNotSuspendedYet() {
        // If something errored or completed, we can't really be sure,
        // so those are false.
        return workInProgressRootExitStatus === RootInProgress;
    }
    function renderRootSync(root, lanes) {
        var prevExecutionContext = executionContext;
        executionContext |= RenderContext;
        var prevDispatcher = pushDispatcher(); // If the root or lanes have changed, throw out the existing stack
        // and prepare a fresh one. Otherwise we'll continue where we left off.
        if (workInProgressRoot !== root || workInProgressRootRenderLanes !== lanes) {
            if (isDevToolsPresent) {
                var memoizedUpdaters = root.memoizedUpdaters;
                if (memoizedUpdaters.size > 0) {
                    restorePendingUpdaters(root, workInProgressRootRenderLanes);
                    memoizedUpdaters.clear();
                } // At this point, move Fibers that scheduled the upcoming work from the Map to the Set.
                // If we bailout on this work, we'll move them back (like above).
                // It's important to move them now in case the work spawns more work at the same priority with different updaters.
                // That way we can keep the current update and future updates separate.
                movePendingFibersToMemoized(root, lanes);
            }
            workInProgressTransitions = getTransitionsForLanes();
            prepareFreshStack(root, lanes);
        }
        markRenderStarted(lanes);
        for(;;)try {
            workLoopSync();
            break;
        } catch (thrownValue) {
            handleError(root, thrownValue);
        }
        resetContextDependencies();
        executionContext = prevExecutionContext;
        popDispatcher(prevDispatcher);
        if (workInProgress1 !== null) // This is a sync render, so we should have finished the whole tree.
        throw new Error("Cannot commit an incomplete root. This error is likely caused by a bug in React. Please file an issue.");
        markRenderStopped();
        workInProgressRoot = null;
        workInProgressRootRenderLanes = NoLanes;
        return workInProgressRootExitStatus;
    } // The work loop is an extremely hot path. Tell Closure not to inline it.
    /** @noinline */ function workLoopSync() {
        // Already timed out, so perform work without checking if we need to yield.
        while(workInProgress1 !== null)performUnitOfWork(workInProgress1);
    }
    function renderRootConcurrent(root, lanes) {
        var prevExecutionContext = executionContext;
        executionContext |= RenderContext;
        var prevDispatcher = pushDispatcher(); // If the root or lanes have changed, throw out the existing stack
        // and prepare a fresh one. Otherwise we'll continue where we left off.
        if (workInProgressRoot !== root || workInProgressRootRenderLanes !== lanes) {
            if (isDevToolsPresent) {
                var memoizedUpdaters = root.memoizedUpdaters;
                if (memoizedUpdaters.size > 0) {
                    restorePendingUpdaters(root, workInProgressRootRenderLanes);
                    memoizedUpdaters.clear();
                } // At this point, move Fibers that scheduled the upcoming work from the Map to the Set.
                // If we bailout on this work, we'll move them back (like above).
                // It's important to move them now in case the work spawns more work at the same priority with different updaters.
                // That way we can keep the current update and future updates separate.
                movePendingFibersToMemoized(root, lanes);
            }
            workInProgressTransitions = getTransitionsForLanes();
            resetRenderTimer();
            prepareFreshStack(root, lanes);
        }
        markRenderStarted(lanes);
        for(;;)try {
            workLoopConcurrent();
            break;
        } catch (thrownValue) {
            handleError(root, thrownValue);
        }
        resetContextDependencies();
        popDispatcher(prevDispatcher);
        executionContext = prevExecutionContext;
        if (workInProgress1 !== null) {
            markRenderYielded();
            return RootInProgress;
        } else {
            markRenderStopped();
            workInProgressRoot = null;
            workInProgressRootRenderLanes = NoLanes; // Return the final exit status.
            return workInProgressRootExitStatus;
        }
    }
    /** @noinline */ function workLoopConcurrent() {
        // Perform work until Scheduler asks us to yield
        while(workInProgress1 !== null && !shouldYield())performUnitOfWork(workInProgress1);
    }
    function performUnitOfWork(unitOfWork) {
        // The current, flushed, state of this fiber is the alternate. Ideally
        // nothing should rely on this, but relying on it here means that we don't
        // need an additional field on the work in progress.
        var current = unitOfWork.alternate;
        setCurrentFiber(unitOfWork);
        var next;
        if ((unitOfWork.mode & ProfileMode) !== NoMode) {
            startProfilerTimer(unitOfWork);
            next = beginWork$1(current, unitOfWork, subtreeRenderLanes1);
            stopProfilerTimerIfRunningAndRecordDelta(unitOfWork, true);
        } else next = beginWork$1(current, unitOfWork, subtreeRenderLanes1);
        resetCurrentFiber();
        unitOfWork.memoizedProps = unitOfWork.pendingProps;
        if (next === null) // If this doesn't spawn new work, complete the current work.
        completeUnitOfWork(unitOfWork);
        else workInProgress1 = next;
        ReactCurrentOwner$2.current = null;
    }
    function completeUnitOfWork(unitOfWork) {
        // Attempt to complete the current unit of work, then move to the next
        // sibling. If there are no more siblings, return to the parent fiber.
        var completedWork = unitOfWork;
        do {
            // The current, flushed, state of this fiber is the alternate. Ideally
            // nothing should rely on this, but relying on it here means that we don't
            // need an additional field on the work in progress.
            var current = completedWork.alternate;
            var returnFiber = completedWork.return; // Check if the work completed or if something threw.
            if ((completedWork.flags & Incomplete) === NoFlags) {
                setCurrentFiber(completedWork);
                var next = void 0;
                if ((completedWork.mode & ProfileMode) === NoMode) next = completeWork(current, completedWork, subtreeRenderLanes1);
                else {
                    startProfilerTimer(completedWork);
                    next = completeWork(current, completedWork, subtreeRenderLanes1); // Update render duration assuming we didn't error.
                    stopProfilerTimerIfRunningAndRecordDelta(completedWork, false);
                }
                resetCurrentFiber();
                if (next !== null) {
                    // Completing this fiber spawned new work. Work on that next.
                    workInProgress1 = next;
                    return;
                }
            } else {
                // This fiber did not complete because something threw. Pop values off
                // the stack without entering the complete phase. If this is a boundary,
                // capture values if possible.
                var _next = unwindWork(current, completedWork); // Because this fiber did not complete, don't reset its lanes.
                if (_next !== null) {
                    // If completing this work spawned new work, do that next. We'll come
                    // back here again.
                    // Since we're restarting, remove anything that is not a host effect
                    // from the effect tag.
                    _next.flags &= HostEffectMask;
                    workInProgress1 = _next;
                    return;
                }
                if ((completedWork.mode & ProfileMode) !== NoMode) {
                    // Record the render duration for the fiber that errored.
                    stopProfilerTimerIfRunningAndRecordDelta(completedWork, false); // Include the time spent working on failed children before continuing.
                    var actualDuration = completedWork.actualDuration;
                    var child = completedWork.child;
                    while(child !== null){
                        actualDuration += child.actualDuration;
                        child = child.sibling;
                    }
                    completedWork.actualDuration = actualDuration;
                }
                if (returnFiber !== null) {
                    // Mark the parent fiber as incomplete and clear its subtree flags.
                    returnFiber.flags |= Incomplete;
                    returnFiber.subtreeFlags = NoFlags;
                    returnFiber.deletions = null;
                } else {
                    // We've unwound all the way to the root.
                    workInProgressRootExitStatus = RootDidNotComplete;
                    workInProgress1 = null;
                    return;
                }
            }
            var siblingFiber = completedWork.sibling;
            if (siblingFiber !== null) {
                // If there is more work to do in this returnFiber, do that next.
                workInProgress1 = siblingFiber;
                return;
            } // Otherwise, return to the parent
            completedWork = returnFiber; // Update the next thing we're working on in case something throws.
            workInProgress1 = completedWork;
        }while (completedWork !== null) // We've reached the root.
        if (workInProgressRootExitStatus === RootInProgress) workInProgressRootExitStatus = RootCompleted;
    }
    function commitRoot(root, recoverableErrors, transitions) {
        // TODO: This no longer makes any sense. We already wrap the mutation and
        // layout phases. Should be able to remove.
        var previousUpdateLanePriority = getCurrentUpdatePriority();
        var prevTransition = ReactCurrentBatchConfig$3.transition;
        try {
            ReactCurrentBatchConfig$3.transition = null;
            setCurrentUpdatePriority(DiscreteEventPriority);
            commitRootImpl(root, recoverableErrors, transitions, previousUpdateLanePriority);
        } finally{
            ReactCurrentBatchConfig$3.transition = prevTransition;
            setCurrentUpdatePriority(previousUpdateLanePriority);
        }
        return null;
    }
    function commitRootImpl(root, recoverableErrors, transitions, renderPriorityLevel) {
        do // `flushPassiveEffects` will call `flushSyncUpdateQueue` at the end, which
        // means `flushPassiveEffects` will sometimes result in additional
        // passive effects. So we need to keep flushing in a loop until there are
        // no more pending effects.
        // TODO: Might be better if `flushPassiveEffects` did not automatically
        // flush synchronous work at the end, to avoid factoring hazards like this.
        flushPassiveEffects();
        while (rootWithPendingPassiveEffects !== null)
        flushRenderPhaseStrictModeWarningsInDEV();
        if ((executionContext & (RenderContext | CommitContext)) !== NoContext) throw new Error('Should not already be working.');
        var finishedWork = root.finishedWork;
        var lanes = root.finishedLanes;
        markCommitStarted(lanes);
        if (finishedWork === null) {
            markCommitStopped();
            return null;
        } else if (lanes === NoLanes) error1("root.finishedLanes should not be empty during a commit. This is a bug in React.");
        root.finishedWork = null;
        root.finishedLanes = NoLanes;
        if (finishedWork === root.current) throw new Error("Cannot commit the same tree as before. This error is likely caused by a bug in React. Please file an issue.");
         // commitRoot never returns a continuation; it always finishes synchronously.
        // So we can clear these now to allow a new callback to be scheduled.
        root.callbackNode = null;
        root.callbackPriority = NoLane; // Update the first and last pending times on this root. The new first
        // pending time is whatever is left on the root fiber.
        var remainingLanes = mergeLanes(finishedWork.lanes, finishedWork.childLanes);
        markRootFinished(root, remainingLanes);
        if (root === workInProgressRoot) {
            // We can reset these now that they are finished.
            workInProgressRoot = null;
            workInProgress1 = null;
            workInProgressRootRenderLanes = NoLanes;
        } // If there are pending passive effects, schedule a callback to process them.
        // Do this as early as possible, so it is queued before anything else that
        // might get scheduled in the commit phase. (See #16714.)
        // TODO: Delete all other places that schedule the passive effect callback
        // They're redundant.
        if ((finishedWork.subtreeFlags & PassiveMask) !== NoFlags || (finishedWork.flags & PassiveMask) !== NoFlags) {
            if (!rootDoesHavePassiveEffects) {
                rootDoesHavePassiveEffects = true;
                // to store it in pendingPassiveTransitions until they get processed
                // We need to pass this through as an argument to commitRoot
                // because workInProgressTransitions might have changed between
                // the previous render and commit if we throttle the commit
                // with setTimeout
                pendingPassiveTransitions = transitions;
                scheduleCallback$1(NormalPriority, function() {
                    flushPassiveEffects(); // This render triggered passive effects: release the root cache pool
                    // *after* passive effects fire to avoid freeing a cache pool that may
                    // be referenced by a node in the tree (HostRoot, Cache boundary etc)
                    return null;
                });
            }
        } // Check if there are any effects in the whole tree.
        // TODO: This is left over from the effect list implementation, where we had
        // to check for the existence of `firstEffect` to satisfy Flow. I think the
        // only other reason this optimization exists is because it affects profiling.
        // Reconsider whether this is necessary.
        var subtreeHasEffects = (finishedWork.subtreeFlags & (BeforeMutationMask | MutationMask | LayoutMask | PassiveMask)) !== NoFlags;
        var rootHasEffect = (finishedWork.flags & (BeforeMutationMask | MutationMask | LayoutMask | PassiveMask)) !== NoFlags;
        if (subtreeHasEffects || rootHasEffect) {
            var prevTransition = ReactCurrentBatchConfig$3.transition;
            ReactCurrentBatchConfig$3.transition = null;
            var previousPriority = getCurrentUpdatePriority();
            setCurrentUpdatePriority(DiscreteEventPriority);
            var prevExecutionContext = executionContext;
            executionContext |= CommitContext; // Reset this to null before calling lifecycles
            ReactCurrentOwner$2.current = null; // The commit phase is broken into several sub-phases. We do a separate pass
            // of the effect list for each phase: all mutation effects come before all
            // layout effects, and so on.
            // The first phase a "before mutation" phase. We use this phase to read the
            // state of the host tree right before we mutate it. This is where
            // getSnapshotBeforeUpdate is called.
            var shouldFireAfterActiveInstanceBlur = commitBeforeMutationEffects(root, finishedWork);
            // Mark the current commit time to be shared by all Profilers in this
            // batch. This enables them to be grouped later.
            recordCommitTime();
            commitMutationEffects(root, finishedWork, lanes);
            resetAfterCommit(root.containerInfo); // The work-in-progress tree is now the current tree. This must come after
            // the mutation phase, so that the previous tree is still current during
            // componentWillUnmount, but before the layout phase, so that the finished
            // work is current during componentDidMount/Update.
            root.current = finishedWork; // The next phase is the layout phase, where we call effects that read
            markLayoutEffectsStarted(lanes);
            commitLayoutEffects(finishedWork, root, lanes);
            markLayoutEffectsStopped();
            // opportunity to paint.
            requestPaint();
            executionContext = prevExecutionContext; // Reset the priority to the previous non-sync value.
            setCurrentUpdatePriority(previousPriority);
            ReactCurrentBatchConfig$3.transition = prevTransition;
        } else {
            // No effects.
            root.current = finishedWork; // Measure these anyway so the flamegraph explicitly shows that there were
            recordCommitTime();
        }
        var rootDidHavePassiveEffects = rootDoesHavePassiveEffects;
        if (rootDoesHavePassiveEffects) {
            // This commit has passive effects. Stash a reference to them. But don't
            // schedule a callback until after flushing layout work.
            rootDoesHavePassiveEffects = false;
            rootWithPendingPassiveEffects = root;
            pendingPassiveEffectsLanes = lanes;
        } else {
            nestedPassiveUpdateCount = 0;
            rootWithPassiveNestedUpdates = null;
        } // Read this again, since an effect might have updated it
        remainingLanes = root.pendingLanes; // Check if there's remaining work on this root
        // TODO: This is part of the `componentDidCatch` implementation. Its purpose
        // is to detect whether something might have called setState inside
        // `componentDidCatch`. The mechanism is known to be flawed because `setState`
        // inside `componentDidCatch` is itself flawed — that's why we recommend
        // `getDerivedStateFromError` instead. However, it could be improved by
        // checking if remainingLanes includes Sync work, instead of whether there's
        // any work remaining at all (which would also include stuff like Suspense
        // retries or transitions). It's been like this for a while, though, so fixing
        // it probably isn't that urgent.
        if (remainingLanes === NoLanes) // If there's no remaining work, we can clear the set of already failed
        // error boundaries.
        legacyErrorBoundariesThatAlreadyFailed = null;
        if (!rootDidHavePassiveEffects) commitDoubleInvokeEffectsInDEV(root.current, false);
        onCommitRoot(finishedWork.stateNode, renderPriorityLevel);
        if (isDevToolsPresent) root.memoizedUpdaters.clear();
        onCommitRoot$1();
        // additional work on this root is scheduled.
        ensureRootIsScheduled(root, now());
        if (recoverableErrors !== null) {
            // There were errors during this render, but recovered from them without
            // needing to surface it to the UI. We log them here.
            var onRecoverableError = root.onRecoverableError;
            for(var i = 0; i < recoverableErrors.length; i++){
                var recoverableError = recoverableErrors[i];
                onRecoverableError(recoverableError);
            }
        }
        if (hasUncaughtError) {
            hasUncaughtError = false;
            var error$1 = firstUncaughtError;
            firstUncaughtError = null;
            throw error$1;
        } // If the passive effects are the result of a discrete render, flush them
        // synchronously at the end of the current task so that the result is
        // immediately observable. Otherwise, we assume that they are not
        // order-dependent and do not need to be observed by external systems, so we
        // can wait until after paint.
        // TODO: We can optimize this by not scheduling the callback earlier. Since we
        // currently schedule the callback in multiple places, will wait until those
        // are consolidated.
        if (includesSomeLane(pendingPassiveEffectsLanes, SyncLane) && root.tag !== LegacyRoot) flushPassiveEffects();
         // Read this again, since a passive effect might have updated it
        remainingLanes = root.pendingLanes;
        if (includesSomeLane(remainingLanes, SyncLane)) {
            markNestedUpdateScheduled();
            // finishing. If there are too many, it indicates an infinite update loop.
            if (root === rootWithNestedUpdates) nestedUpdateCount++;
            else {
                nestedUpdateCount = 0;
                rootWithNestedUpdates = root;
            }
        } else nestedUpdateCount = 0;
         // If layout work was scheduled, flush it now.
        flushSyncCallbacks();
        markCommitStopped();
        return null;
    }
    function flushPassiveEffects() {
        // Returns whether passive effects were flushed.
        // TODO: Combine this check with the one in flushPassiveEFfectsImpl. We should
        // probably just combine the two functions. I believe they were only separate
        // in the first place because we used to wrap it with
        // `Scheduler.runWithPriority`, which accepts a function. But now we track the
        // priority within React itself, so we can mutate the variable directly.
        if (rootWithPendingPassiveEffects !== null) {
            var renderPriority = lanesToEventPriority(pendingPassiveEffectsLanes);
            var priority = lowerEventPriority(DefaultEventPriority, renderPriority);
            var prevTransition = ReactCurrentBatchConfig$3.transition;
            var previousPriority = getCurrentUpdatePriority();
            try {
                ReactCurrentBatchConfig$3.transition = null;
                setCurrentUpdatePriority(priority);
                return flushPassiveEffectsImpl();
            } finally{
                setCurrentUpdatePriority(previousPriority);
                ReactCurrentBatchConfig$3.transition = prevTransition; // Once passive effects have run for the tree - giving components a
            }
        }
        return false;
    }
    function enqueuePendingPassiveProfilerEffect(fiber) {
        pendingPassiveProfilerEffects.push(fiber);
        if (!rootDoesHavePassiveEffects) {
            rootDoesHavePassiveEffects = true;
            scheduleCallback$1(NormalPriority, function() {
                flushPassiveEffects();
                return null;
            });
        }
    }
    function flushPassiveEffectsImpl() {
        if (rootWithPendingPassiveEffects === null) return false;
         // Cache and clear the transitions flag
        var transitions = pendingPassiveTransitions;
        pendingPassiveTransitions = null;
        var root = rootWithPendingPassiveEffects;
        var lanes = pendingPassiveEffectsLanes;
        rootWithPendingPassiveEffects = null; // TODO: This is sometimes out of sync with rootWithPendingPassiveEffects.
        // Figure out why and fix it. It's not causing any known issues (probably
        // because it's only used for profiling), but it's a refactor hazard.
        pendingPassiveEffectsLanes = NoLanes;
        if ((executionContext & (RenderContext | CommitContext)) !== NoContext) throw new Error('Cannot flush passive effects while already rendering.');
        isFlushingPassiveEffects = true;
        didScheduleUpdateDuringPassiveEffects = false;
        markPassiveEffectsStarted(lanes);
        var prevExecutionContext = executionContext;
        executionContext |= CommitContext;
        commitPassiveUnmountEffects(root.current);
        commitPassiveMountEffects(root, root.current, lanes, transitions); // TODO: Move to commitPassiveMountEffects
        var profilerEffects = pendingPassiveProfilerEffects;
        pendingPassiveProfilerEffects = [];
        for(var i = 0; i < profilerEffects.length; i++){
            var _fiber = profilerEffects[i];
            commitPassiveEffectDurations(root, _fiber);
        }
        markPassiveEffectsStopped();
        commitDoubleInvokeEffectsInDEV(root.current, true);
        executionContext = prevExecutionContext;
        flushSyncCallbacks();
        // If additional passive effects were scheduled, increment a counter. If this
        // exceeds the limit, we'll fire a warning.
        if (didScheduleUpdateDuringPassiveEffects) {
            if (root === rootWithPassiveNestedUpdates) nestedPassiveUpdateCount++;
            else {
                nestedPassiveUpdateCount = 0;
                rootWithPassiveNestedUpdates = root;
            }
        } else nestedPassiveUpdateCount = 0;
        isFlushingPassiveEffects = false;
        didScheduleUpdateDuringPassiveEffects = false;
        onPostCommitRoot(root);
        var stateNode = root.current.stateNode;
        stateNode.effectDuration = 0;
        stateNode.passiveEffectDuration = 0;
        return true;
    }
    function isAlreadyFailedLegacyErrorBoundary(instance) {
        return legacyErrorBoundariesThatAlreadyFailed !== null && legacyErrorBoundariesThatAlreadyFailed.has(instance);
    }
    function markLegacyErrorBoundaryAsFailed(instance) {
        if (legacyErrorBoundariesThatAlreadyFailed === null) legacyErrorBoundariesThatAlreadyFailed = new Set([
            instance
        ]);
        else legacyErrorBoundariesThatAlreadyFailed.add(instance);
    }
    function prepareToThrowUncaughtError(error) {
        if (!hasUncaughtError) {
            hasUncaughtError = true;
            firstUncaughtError = error;
        }
    }
    var onUncaughtError = prepareToThrowUncaughtError;
    function captureCommitPhaseErrorOnRoot(rootFiber, sourceFiber, error) {
        var errorInfo = createCapturedValue(error, sourceFiber);
        var update = createRootErrorUpdate(rootFiber, errorInfo, SyncLane);
        enqueueUpdate(rootFiber, update);
        var eventTime = requestEventTime();
        var root = markUpdateLaneFromFiberToRoot(rootFiber, SyncLane);
        if (root !== null) {
            markRootUpdated(root, SyncLane, eventTime);
            ensureRootIsScheduled(root, eventTime);
        }
    }
    function captureCommitPhaseError(sourceFiber, nearestMountedAncestor, error$1) {
        reportUncaughtErrorInDEV(error$1);
        setIsRunningInsertionEffect(false);
        if (sourceFiber.tag === HostRoot) {
            // Error was thrown at the root. There is no parent, so the root
            // itself should capture it.
            captureCommitPhaseErrorOnRoot(sourceFiber, sourceFiber, error$1);
            return;
        }
        var fiber = null;
        fiber = nearestMountedAncestor;
        while(fiber !== null){
            if (fiber.tag === HostRoot) {
                captureCommitPhaseErrorOnRoot(fiber, sourceFiber, error$1);
                return;
            } else if (fiber.tag === ClassComponent) {
                var ctor = fiber.type;
                var instance = fiber.stateNode;
                if (typeof ctor.getDerivedStateFromError === 'function' || typeof instance.componentDidCatch === 'function' && !isAlreadyFailedLegacyErrorBoundary(instance)) {
                    var errorInfo = createCapturedValue(error$1, sourceFiber);
                    var update = createClassErrorUpdate(fiber, errorInfo, SyncLane);
                    enqueueUpdate(fiber, update);
                    var eventTime = requestEventTime();
                    var root = markUpdateLaneFromFiberToRoot(fiber, SyncLane);
                    if (root !== null) {
                        markRootUpdated(root, SyncLane, eventTime);
                        ensureRootIsScheduled(root, eventTime);
                    }
                    return;
                }
            }
            fiber = fiber.return;
        }
        // TODO: Until we re-land skipUnmountedBoundaries (see #20147), this warning
        // will fire for errors that are thrown by destroy functions inside deleted
        // trees. What it should instead do is propagate the error to the parent of
        // the deleted tree. In the meantime, do not add this warning to the
        // allowlist; this is only for our internal use.
        error1("Internal React error: Attempted to capture a commit phase error inside a detached tree. This indicates a bug in React. Likely causes include deleting the same fiber more than once, committing an already-finished tree, or an inconsistent return pointer.\n\nError message:\n\n%s", error$1);
    }
    function pingSuspendedRoot(root, wakeable, pingedLanes) {
        var pingCache = root.pingCache;
        if (pingCache !== null) // The wakeable resolved, so we no longer need to memoize, because it will
        // never be thrown again.
        pingCache.delete(wakeable);
        var eventTime = requestEventTime();
        markRootPinged(root, pingedLanes);
        warnIfSuspenseResolutionNotWrappedWithActDEV(root);
        if (workInProgressRoot === root && isSubsetOfLanes(workInProgressRootRenderLanes, pingedLanes)) {
            // Received a ping at the same priority level at which we're currently
            // rendering. We might want to restart this render. This should mirror
            // the logic of whether or not a root suspends once it completes.
            // TODO: If we're rendering sync either due to Sync, Batched or expired,
            // we should probably never restart.
            // If we're suspended with delay, or if it's a retry, we'll always suspend
            // so we can always restart.
            if (workInProgressRootExitStatus === RootSuspendedWithDelay || workInProgressRootExitStatus === RootSuspended && includesOnlyRetries(workInProgressRootRenderLanes) && now() - globalMostRecentFallbackTime < FALLBACK_THROTTLE_MS) // Restart from the root.
            prepareFreshStack(root, NoLanes);
            else // Even though we can't restart right now, we might get an
            // opportunity later. So we mark this render as having a ping.
            workInProgressRootPingedLanes = mergeLanes(workInProgressRootPingedLanes, pingedLanes);
        }
        ensureRootIsScheduled(root, eventTime);
    }
    function retryTimedOutBoundary(boundaryFiber, retryLane) {
        // The boundary fiber (a Suspense component or SuspenseList component)
        // previously was rendered in its fallback state. One of the promises that
        // suspended it has resolved, which means at least part of the tree was
        // likely unblocked. Try rendering again, at a new lanes.
        if (retryLane === NoLane) // TODO: Assign this to `suspenseState.retryLane`? to avoid
        // unnecessary entanglement?
        retryLane = requestRetryLane(boundaryFiber);
         // TODO: Special case idle priority?
        var eventTime = requestEventTime();
        var root = markUpdateLaneFromFiberToRoot(boundaryFiber, retryLane);
        if (root !== null) {
            markRootUpdated(root, retryLane, eventTime);
            ensureRootIsScheduled(root, eventTime);
        }
    }
    function retryDehydratedSuspenseBoundary(boundaryFiber) {
        var suspenseState = boundaryFiber.memoizedState;
        var retryLane = NoLane;
        if (suspenseState !== null) retryLane = suspenseState.retryLane;
        retryTimedOutBoundary(boundaryFiber, retryLane);
    }
    function resolveRetryWakeable(boundaryFiber, wakeable) {
        var retryLane = NoLane; // Default
        var retryCache;
        switch(boundaryFiber.tag){
            case SuspenseComponent:
                retryCache = boundaryFiber.stateNode;
                var suspenseState = boundaryFiber.memoizedState;
                if (suspenseState !== null) retryLane = suspenseState.retryLane;
                break;
            case SuspenseListComponent:
                retryCache = boundaryFiber.stateNode;
                break;
            default:
                throw new Error("Pinged unknown suspense boundary type. This is probably a bug in React.");
        }
        if (retryCache !== null) // The wakeable resolved, so we no longer need to memoize, because it will
        // never be thrown again.
        retryCache.delete(wakeable);
        retryTimedOutBoundary(boundaryFiber, retryLane);
    } // Computes the next Just Noticeable Difference (JND) boundary.
    // The theory is that a person can't tell the difference between small differences in time.
    // Therefore, if we wait a bit longer than necessary that won't translate to a noticeable
    // difference in the experience. However, waiting for longer might mean that we can avoid
    // showing an intermediate loading state. The longer we have already waited, the harder it
    // is to tell small differences in time. Therefore, the longer we've already waited,
    // the longer we can wait additionally. At some point we have to give up though.
    // We pick a train model where the next boundary commits at a consistent schedule.
    // These particular numbers are vague estimates. We expect to adjust them based on research.
    function jnd(timeElapsed) {
        return timeElapsed < 120 ? 120 : timeElapsed < 480 ? 480 : timeElapsed < 1080 ? 1080 : timeElapsed < 1920 ? 1920 : timeElapsed < 3000 ? 3000 : timeElapsed < 4320 ? 4320 : ceil(timeElapsed / 1960) * 1960;
    }
    function checkForNestedUpdates() {
        if (nestedUpdateCount > NESTED_UPDATE_LIMIT) {
            nestedUpdateCount = 0;
            rootWithNestedUpdates = null;
            throw new Error("Maximum update depth exceeded. This can happen when a component repeatedly calls setState inside componentWillUpdate or componentDidUpdate. React limits the number of nested updates to prevent infinite loops.");
        }
        if (nestedPassiveUpdateCount > NESTED_PASSIVE_UPDATE_LIMIT) {
            nestedPassiveUpdateCount = 0;
            rootWithPassiveNestedUpdates = null;
            error1("Maximum update depth exceeded. This can happen when a component calls setState inside useEffect, but useEffect either doesn't have a dependency array, or one of the dependencies changes on every render.");
        }
    }
    function flushRenderPhaseStrictModeWarningsInDEV() {
        ReactStrictModeWarnings.flushLegacyContextWarning();
        ReactStrictModeWarnings.flushPendingUnsafeLifecycleWarnings();
    }
    function commitDoubleInvokeEffectsInDEV(fiber, hasPassiveEffects) {
        // TODO (StrictEffects) Should we set a marker on the root if it contains strict effects
        // so we don't traverse unnecessarily? similar to subtreeFlags but just at the root level.
        // Maybe not a big deal since this is DEV only behavior.
        setCurrentFiber(fiber);
        invokeEffectsInDev(fiber, MountLayoutDev, invokeLayoutEffectUnmountInDEV);
        if (hasPassiveEffects) invokeEffectsInDev(fiber, MountPassiveDev, invokePassiveEffectUnmountInDEV);
        invokeEffectsInDev(fiber, MountLayoutDev, invokeLayoutEffectMountInDEV);
        if (hasPassiveEffects) invokeEffectsInDev(fiber, MountPassiveDev, invokePassiveEffectMountInDEV);
        resetCurrentFiber();
    }
    function invokeEffectsInDev(firstChild, fiberFlags, invokeEffectFn) {
        // We don't need to re-check StrictEffectsMode here.
        // This function is only called if that check has already passed.
        var current = firstChild;
        var subtreeRoot = null;
        while(current !== null){
            var primarySubtreeFlag = current.subtreeFlags & fiberFlags;
            if (current !== subtreeRoot && current.child !== null && primarySubtreeFlag !== NoFlags) current = current.child;
            else {
                if ((current.flags & fiberFlags) !== NoFlags) invokeEffectFn(current);
                if (current.sibling !== null) current = current.sibling;
                else current = subtreeRoot = current.return;
            }
        }
    }
    var didWarnStateUpdateForNotYetMountedComponent = null;
    function warnAboutUpdateOnNotYetMountedFiberInDEV(fiber) {
        if ((executionContext & RenderContext) !== NoContext) // We let the other warning about render phase updates deal with this one.
        return;
        if (!(fiber.mode & ConcurrentMode)) return;
        var tag = fiber.tag;
        if (tag !== IndeterminateComponent && tag !== HostRoot && tag !== ClassComponent && tag !== FunctionComponent && tag !== ForwardRef && tag !== MemoComponent && tag !== SimpleMemoComponent) // Only warn for user-defined components, not internal ones like Suspense.
        return;
         // We show the whole stack but dedupe on the top component's name because
        // the problematic code almost always lies inside that component.
        var componentName = getComponentNameFromFiber(fiber) || 'ReactComponent';
        if (didWarnStateUpdateForNotYetMountedComponent !== null) {
            if (didWarnStateUpdateForNotYetMountedComponent.has(componentName)) return;
            didWarnStateUpdateForNotYetMountedComponent.add(componentName);
        } else didWarnStateUpdateForNotYetMountedComponent = new Set([
            componentName
        ]);
        var previousFiber = current1;
        try {
            setCurrentFiber(fiber);
            error1("Can't perform a React state update on a component that hasn't mounted yet. This indicates that you have a side-effect in your render function that asynchronously later calls tries to update the component. Move this work to useEffect instead.");
        } finally{
            if (previousFiber) setCurrentFiber(fiber);
            else resetCurrentFiber();
        }
    }
    var beginWork$1;
    var dummyFiber = null;
    beginWork$1 = function(current, unitOfWork, lanes) {
        // If a component throws an error, we replay it again in a synchronously
        // dispatched event, so that the debugger will treat it as an uncaught
        // error See ReactErrorUtils for more information.
        // Before entering the begin phase, copy the work-in-progress onto a dummy
        // fiber. If beginWork throws, we'll use this to reset the state.
        var originalWorkInProgressCopy = assignFiberPropertiesInDEV(dummyFiber, unitOfWork);
        try {
            return beginWork(current, unitOfWork, lanes);
        } catch (originalError) {
            if (originalError !== null && typeof originalError === 'object' && typeof originalError.then === 'function') // Don't replay promises. Treat everything else like an error.
            throw originalError;
             // Keep this code in sync with handleError; any changes here must have
            // corresponding changes there.
            resetContextDependencies();
            resetHooksAfterThrow(); // Don't reset current debug fiber, since we're about to work on the
            // same fiber again.
            // Unwind the failed stack frame
            unwindInterruptedWork(current, unitOfWork); // Restore the original properties of the fiber.
            assignFiberPropertiesInDEV(unitOfWork, originalWorkInProgressCopy);
            if (unitOfWork.mode & ProfileMode) // Reset the profiler timer.
            startProfilerTimer(unitOfWork);
             // Run beginWork again.
            invokeGuardedCallback(null, beginWork, null, current, unitOfWork, lanes);
            if (hasCaughtError()) {
                var replayError = clearCaughtError();
                if (typeof replayError === 'object' && replayError !== null && replayError._suppressLogging && typeof originalError === 'object' && originalError !== null && !originalError._suppressLogging) // If suppressed, let the flag carry over to the original error which is the one we'll rethrow.
                originalError._suppressLogging = true;
            } // We always throw the original error in case the second render pass is not idempotent.
            // This can happen if a memoized function or CommonJS module doesn't throw after first invocation.
            throw originalError;
        }
    };
    var didWarnAboutUpdateInRender = false;
    var didWarnAboutUpdateInRenderForAnotherComponent;
    didWarnAboutUpdateInRenderForAnotherComponent = new Set();
    function warnAboutRenderPhaseUpdatesInDEV(fiber) {
        if (isRendering && !getIsUpdatingOpaqueValueInRenderPhaseInDEV()) switch(fiber.tag){
            case FunctionComponent:
            case ForwardRef:
            case SimpleMemoComponent:
                var renderingComponentName = workInProgress1 && getComponentNameFromFiber(workInProgress1) || 'Unknown'; // Dedupe by the rendering component because it's the one that needs to be fixed.
                var dedupeKey = renderingComponentName;
                if (!didWarnAboutUpdateInRenderForAnotherComponent.has(dedupeKey)) {
                    didWarnAboutUpdateInRenderForAnotherComponent.add(dedupeKey);
                    var setStateComponentName = getComponentNameFromFiber(fiber) || 'Unknown';
                    error1("Cannot update a component (`%s`) while rendering a different component (`%s`). To locate the bad setState() call inside `%s`, follow the stack trace as described in https://reactjs.org/link/setstate-in-render", setStateComponentName, renderingComponentName, renderingComponentName);
                }
                break;
            case ClassComponent:
                if (!didWarnAboutUpdateInRender) {
                    error1("Cannot update during an existing state transition (such as within `render`). Render methods should be a pure function of props and state.");
                    didWarnAboutUpdateInRender = true;
                }
                break;
        }
    }
    function restorePendingUpdaters(root, lanes) {
        if (isDevToolsPresent) {
            var memoizedUpdaters = root.memoizedUpdaters;
            memoizedUpdaters.forEach(function(schedulingFiber) {
                addFiberToLanesMap(root, schedulingFiber, lanes);
            }); // This function intentionally does not clear memoized updaters.
        // Those may still be relevant to the current commit
        // and a future one (e.g. Suspense).
        }
    }
    var fakeActCallbackNode = {};
    function scheduleCallback$1(priorityLevel, callback) {
        // If we're currently inside an `act` scope, bypass Scheduler and push to
        // the `act` queue instead.
        var actQueue = ReactCurrentActQueue$1.current;
        if (actQueue !== null) {
            actQueue.push(callback);
            return fakeActCallbackNode;
        } else return scheduleCallback(priorityLevel, callback);
    }
    function cancelCallback$1(callbackNode) {
        if (callbackNode === fakeActCallbackNode) return;
         // In production, always call Scheduler. This function will be stripped out.
        return cancelCallback(callbackNode);
    }
    function shouldForceFlushFallbacksInDEV() {
        // Never force flush in production. This function should get stripped out.
        return ReactCurrentActQueue$1.current !== null;
    }
    function warnIfUpdatesNotWrappedWithActDEV(fiber) {
        if (fiber.mode & ConcurrentMode) {
            if (!isConcurrentActEnvironment()) // Not in an act environment. No need to warn.
            return;
        } else {
            // Legacy mode has additional cases where we suppress a warning.
            if (!isLegacyActEnvironment()) // Not in an act environment. No need to warn.
            return;
            if (executionContext !== NoContext) // Legacy mode doesn't warn if the update is batched, i.e.
            // batchedUpdates or flushSync.
            return;
            if (fiber.tag !== FunctionComponent && fiber.tag !== ForwardRef && fiber.tag !== SimpleMemoComponent) // For backwards compatibility with pre-hooks code, legacy mode only
            // warns for updates that originate from a hook.
            return;
        }
        if (ReactCurrentActQueue$1.current === null) {
            var previousFiber = current1;
            try {
                setCurrentFiber(fiber);
                error1("An update to %s inside a test was not wrapped in act(...).\n\nWhen testing, code that causes React state updates should be wrapped into act(...):\n\nact(() => {\n  /* fire events that update state */\n});\n/* assert on the output */\n\nThis ensures that you're testing the behavior the user would see in the browser. Learn more at https://reactjs.org/link/wrap-tests-with-act", getComponentNameFromFiber(fiber));
            } finally{
                if (previousFiber) setCurrentFiber(fiber);
                else resetCurrentFiber();
            }
        }
    }
    function warnIfSuspenseResolutionNotWrappedWithActDEV(root) {
        if (root.tag !== LegacyRoot && isConcurrentActEnvironment() && ReactCurrentActQueue$1.current === null) error1("A suspended resource finished loading inside a test, but the event was not wrapped in act(...).\n\nWhen testing, code that resolves suspended data should be wrapped into act(...):\n\nact(() => {\n  /* finish loading suspended data */\n});\n/* assert on the output */\n\nThis ensures that you're testing the behavior the user would see in the browser. Learn more at https://reactjs.org/link/wrap-tests-with-act");
    }
    function setIsRunningInsertionEffect(isRunning) {
        isRunningInsertionEffect = isRunning;
    }
    /* eslint-disable react-internal/prod-error-codes */ var resolveFamily = null; // $FlowFixMe Flow gets confused by a WeakSet feature check below.
    var failedBoundaries = null;
    var setRefreshHandler = function(handler) {
        resolveFamily = handler;
    };
    function resolveFunctionForHotReloading(type) {
        if (resolveFamily === null) // Hot reloading is disabled.
        return type;
        var family = resolveFamily(type);
        if (family === undefined) return type;
         // Use the latest known implementation.
        return family.current;
    }
    function resolveClassForHotReloading(type) {
        // No implementation differences.
        return resolveFunctionForHotReloading(type);
    }
    function resolveForwardRefForHotReloading(type) {
        if (resolveFamily === null) // Hot reloading is disabled.
        return type;
        var family = resolveFamily(type);
        if (family === undefined) {
            // Check if we're dealing with a real forwardRef. Don't want to crash early.
            if (type !== null && type !== undefined && typeof type.render === 'function') {
                // ForwardRef is special because its resolved .type is an object,
                // but it's possible that we only have its inner render function in the map.
                // If that inner render function is different, we'll build a new forwardRef type.
                var currentRender = resolveFunctionForHotReloading(type.render);
                if (type.render !== currentRender) {
                    var syntheticType = {
                        $$typeof: REACT_FORWARD_REF_TYPE,
                        render: currentRender
                    };
                    if (type.displayName !== undefined) syntheticType.displayName = type.displayName;
                    return syntheticType;
                }
            }
            return type;
        } // Use the latest known implementation.
        return family.current;
    }
    function isCompatibleFamilyForHotReloading(fiber, element) {
        if (resolveFamily === null) // Hot reloading is disabled.
        return false;
        var prevType = fiber.elementType;
        var nextType = element.type; // If we got here, we know types aren't === equal.
        var needsCompareFamilies = false;
        var $$typeofNextType = typeof nextType === 'object' && nextType !== null ? nextType.$$typeof : null;
        switch(fiber.tag){
            case ClassComponent:
                if (typeof nextType === 'function') needsCompareFamilies = true;
                break;
            case FunctionComponent:
                if (typeof nextType === 'function') needsCompareFamilies = true;
                else if ($$typeofNextType === REACT_LAZY_TYPE) // We don't know the inner type yet.
                // We're going to assume that the lazy inner type is stable,
                // and so it is sufficient to avoid reconciling it away.
                // We're not going to unwrap or actually use the new lazy type.
                needsCompareFamilies = true;
                break;
            case ForwardRef:
                if ($$typeofNextType === REACT_FORWARD_REF_TYPE) needsCompareFamilies = true;
                else if ($$typeofNextType === REACT_LAZY_TYPE) needsCompareFamilies = true;
                break;
            case MemoComponent:
            case SimpleMemoComponent:
                if ($$typeofNextType === REACT_MEMO_TYPE) // TODO: if it was but can no longer be simple,
                // we shouldn't set this.
                needsCompareFamilies = true;
                else if ($$typeofNextType === REACT_LAZY_TYPE) needsCompareFamilies = true;
                break;
            default:
                return false;
        } // Check if both types have a family and it's the same one.
        if (needsCompareFamilies) {
            // Note: memo() and forwardRef() we'll compare outer rather than inner type.
            // This means both of them need to be registered to preserve state.
            // If we unwrapped and compared the inner types for wrappers instead,
            // then we would risk falsely saying two separate memo(Foo)
            // calls are equivalent because they wrap the same Foo function.
            var prevFamily = resolveFamily(prevType);
            if (prevFamily !== undefined && prevFamily === resolveFamily(nextType)) return true;
        }
        return false;
    }
    function markFailedErrorBoundaryForHotReloading(fiber) {
        if (resolveFamily === null) // Hot reloading is disabled.
        return;
        if (typeof WeakSet !== 'function') return;
        if (failedBoundaries === null) failedBoundaries = new WeakSet();
        failedBoundaries.add(fiber);
    }
    var scheduleRefresh = function(root, update) {
        if (resolveFamily === null) // Hot reloading is disabled.
        return;
        var staleFamilies = update.staleFamilies, updatedFamilies = update.updatedFamilies;
        flushPassiveEffects();
        flushSync(function() {
            scheduleFibersWithFamiliesRecursively(root.current, updatedFamilies, staleFamilies);
        });
    };
    var scheduleRoot = function(root, element) {
        if (root.context !== emptyContextObject) // Super edge case: root has a legacy _renderSubtree context
        // but we don't know the parentComponent so we can't pass it.
        // Just ignore. We'll delete this with _renderSubtree code path later.
        return;
        flushPassiveEffects();
        flushSync(function() {
            updateContainer(element, root, null, null);
        });
    };
    function scheduleFibersWithFamiliesRecursively(fiber, updatedFamilies, staleFamilies) {
        var alternate = fiber.alternate, child = fiber.child, sibling = fiber.sibling, tag = fiber.tag, type = fiber.type;
        var candidateType = null;
        switch(tag){
            case FunctionComponent:
            case SimpleMemoComponent:
            case ClassComponent:
                candidateType = type;
                break;
            case ForwardRef:
                candidateType = type.render;
                break;
        }
        if (resolveFamily === null) throw new Error('Expected resolveFamily to be set during hot reload.');
        var needsRender = false;
        var needsRemount = false;
        if (candidateType !== null) {
            var family = resolveFamily(candidateType);
            if (family !== undefined) {
                if (staleFamilies.has(family)) needsRemount = true;
                else if (updatedFamilies.has(family)) {
                    if (tag === ClassComponent) needsRemount = true;
                    else needsRender = true;
                }
            }
        }
        if (failedBoundaries !== null) {
            if (failedBoundaries.has(fiber) || alternate !== null && failedBoundaries.has(alternate)) needsRemount = true;
        }
        if (needsRemount) fiber._debugNeedsRemount = true;
        if (needsRemount || needsRender) scheduleUpdateOnFiber(fiber, SyncLane, NoTimestamp);
        if (child !== null && !needsRemount) scheduleFibersWithFamiliesRecursively(child, updatedFamilies, staleFamilies);
        if (sibling !== null) scheduleFibersWithFamiliesRecursively(sibling, updatedFamilies, staleFamilies);
    }
    var findHostInstancesForRefresh = function(root, families) {
        var hostInstances = new Set();
        var types = new Set(families.map(function(family) {
            return family.current;
        }));
        findHostInstancesForMatchingFibersRecursively(root.current, types, hostInstances);
        return hostInstances;
    };
    function findHostInstancesForMatchingFibersRecursively(fiber, types, hostInstances) {
        var child = fiber.child, sibling = fiber.sibling, tag = fiber.tag, type = fiber.type;
        var candidateType = null;
        switch(tag){
            case FunctionComponent:
            case SimpleMemoComponent:
            case ClassComponent:
                candidateType = type;
                break;
            case ForwardRef:
                candidateType = type.render;
                break;
        }
        var didMatch = false;
        if (candidateType !== null) {
            if (types.has(candidateType)) didMatch = true;
        }
        if (didMatch) // We have a match. This only drills down to the closest host components.
        // There's no need to search deeper because for the purpose of giving
        // visual feedback, "flashing" outermost parent rectangles is sufficient.
        findHostInstancesForFiberShallowly(fiber, hostInstances);
        else // If there's no match, maybe there will be one further down in the child tree.
        if (child !== null) findHostInstancesForMatchingFibersRecursively(child, types, hostInstances);
        if (sibling !== null) findHostInstancesForMatchingFibersRecursively(sibling, types, hostInstances);
    }
    function findHostInstancesForFiberShallowly(fiber, hostInstances) {
        var foundHostInstances = findChildHostInstancesForFiberShallowly(fiber, hostInstances);
        if (foundHostInstances) return;
         // If we didn't find any host children, fallback to closest host parent.
        var node = fiber;
        while(true){
            switch(node.tag){
                case HostComponent:
                    hostInstances.add(node.stateNode);
                    return;
                case HostPortal:
                    hostInstances.add(node.stateNode.containerInfo);
                    return;
                case HostRoot:
                    hostInstances.add(node.stateNode.containerInfo);
                    return;
            }
            if (node.return === null) throw new Error('Expected to reach root first.');
            node = node.return;
        }
    }
    function findChildHostInstancesForFiberShallowly(fiber, hostInstances) {
        var node = fiber;
        var foundHostInstances = false;
        while(true){
            if (node.tag === HostComponent) {
                // We got a match.
                foundHostInstances = true;
                hostInstances.add(node.stateNode); // There may still be more, so keep searching.
            } else if (node.child !== null) {
                node.child.return = node;
                node = node.child;
                continue;
            }
            if (node === fiber) return foundHostInstances;
            while(node.sibling === null){
                if (node.return === null || node.return === fiber) return foundHostInstances;
                node = node.return;
            }
            node.sibling.return = node.return;
            node = node.sibling;
        }
        return false;
    }
    var hasBadMapPolyfill;
    hasBadMapPolyfill = false;
    try {
        var nonExtensibleObject = Object.preventExtensions({});
        /* eslint-disable no-new */ new Map([
            [
                nonExtensibleObject,
                null
            ]
        ]);
        new Set([
            nonExtensibleObject
        ]);
    /* eslint-enable no-new */ } catch (e) {
        // TODO: Consider warning about bad polyfills
        hasBadMapPolyfill = true;
    }
    function FiberNode(tag, pendingProps, key, mode) {
        // Instance
        this.tag = tag;
        this.key = key;
        this.elementType = null;
        this.type = null;
        this.stateNode = null; // Fiber
        this.return = null;
        this.child = null;
        this.sibling = null;
        this.index = 0;
        this.ref = null;
        this.pendingProps = pendingProps;
        this.memoizedProps = null;
        this.updateQueue = null;
        this.memoizedState = null;
        this.dependencies = null;
        this.mode = mode; // Effects
        this.flags = NoFlags;
        this.subtreeFlags = NoFlags;
        this.deletions = null;
        this.lanes = NoLanes;
        this.childLanes = NoLanes;
        this.alternate = null;
        // Note: The following is done to avoid a v8 performance cliff.
        //
        // Initializing the fields below to smis and later updating them with
        // double values will cause Fibers to end up having separate shapes.
        // This behavior/bug has something to do with Object.preventExtension().
        // Fortunately this only impacts DEV builds.
        // Unfortunately it makes React unusably slow for some applications.
        // To work around this, initialize the fields below with doubles.
        //
        // Learn more about this here:
        // https://github.com/facebook/react/issues/14365
        // https://bugs.chromium.org/p/v8/issues/detail?id=8538
        this.actualDuration = Number.NaN;
        this.actualStartTime = Number.NaN;
        this.selfBaseDuration = Number.NaN;
        this.treeBaseDuration = Number.NaN; // It's okay to replace the initial doubles with smis after initialization.
        // This won't trigger the performance cliff mentioned above,
        // and it simplifies other profiler code (including DevTools).
        this.actualDuration = 0;
        this.actualStartTime = -1;
        this.selfBaseDuration = 0;
        this.treeBaseDuration = 0;
        // This isn't directly used but is handy for debugging internals:
        this._debugSource = null;
        this._debugOwner = null;
        this._debugNeedsRemount = false;
        this._debugHookTypes = null;
        if (!hasBadMapPolyfill && typeof Object.preventExtensions === 'function') Object.preventExtensions(this);
    } // This is a constructor function, rather than a POJO constructor, still
    // please ensure we do the following:
    // 1) Nobody should add any instance methods on this. Instance methods can be
    //    more difficult to predict when they get optimized and they are almost
    //    never inlined properly in static compilers.
    // 2) Nobody should rely on `instanceof Fiber` for type testing. We should
    //    always know when it is a fiber.
    // 3) We might want to experiment with using numeric keys since they are easier
    //    to optimize in a non-JIT environment.
    // 4) We can easily go from a constructor to a createFiber object literal if that
    //    is faster.
    // 5) It should be easy to port this to a C struct and keep a C implementation
    //    compatible.
    var createFiber = function(tag, pendingProps, key, mode) {
        // $FlowFixMe: the shapes are exact here but Flow doesn't like constructors
        return new FiberNode(tag, pendingProps, key, mode);
    };
    function shouldConstruct$1(Component) {
        var prototype = Component.prototype;
        return !!(prototype && prototype.isReactComponent);
    }
    function isSimpleFunctionComponent(type) {
        return typeof type === 'function' && !shouldConstruct$1(type) && type.defaultProps === undefined;
    }
    function resolveLazyComponentTag(Component) {
        if (typeof Component === 'function') return shouldConstruct$1(Component) ? ClassComponent : FunctionComponent;
        else if (Component !== undefined && Component !== null) {
            var $$typeof = Component.$$typeof;
            if ($$typeof === REACT_FORWARD_REF_TYPE) return ForwardRef;
            if ($$typeof === REACT_MEMO_TYPE) return MemoComponent;
        }
        return IndeterminateComponent;
    } // This is used to create an alternate fiber to do work on.
    function createWorkInProgress(current, pendingProps) {
        var workInProgress = current.alternate;
        if (workInProgress === null) {
            // We use a double buffering pooling technique because we know that we'll
            // only ever need at most two versions of a tree. We pool the "other" unused
            // node that we're free to reuse. This is lazily created to avoid allocating
            // extra objects for things that are never updated. It also allow us to
            // reclaim the extra memory if needed.
            workInProgress = createFiber(current.tag, pendingProps, current.key, current.mode);
            workInProgress.elementType = current.elementType;
            workInProgress.type = current.type;
            workInProgress.stateNode = current.stateNode;
            // DEV-only fields
            workInProgress._debugSource = current._debugSource;
            workInProgress._debugOwner = current._debugOwner;
            workInProgress._debugHookTypes = current._debugHookTypes;
            workInProgress.alternate = current;
            current.alternate = workInProgress;
        } else {
            workInProgress.pendingProps = pendingProps; // Needed because Blocks store data on type.
            workInProgress.type = current.type; // We already have an alternate.
            // Reset the effect tag.
            workInProgress.flags = NoFlags; // The effects are no longer valid.
            workInProgress.subtreeFlags = NoFlags;
            workInProgress.deletions = null;
            // We intentionally reset, rather than copy, actualDuration & actualStartTime.
            // This prevents time from endlessly accumulating in new commits.
            // This has the downside of resetting values for different priority renders,
            // But works for yielding (the common case) and should support resuming.
            workInProgress.actualDuration = 0;
            workInProgress.actualStartTime = -1;
        } // Reset all effects except static ones.
        // Static effects are not specific to a render.
        workInProgress.flags = current.flags & StaticMask;
        workInProgress.childLanes = current.childLanes;
        workInProgress.lanes = current.lanes;
        workInProgress.child = current.child;
        workInProgress.memoizedProps = current.memoizedProps;
        workInProgress.memoizedState = current.memoizedState;
        workInProgress.updateQueue = current.updateQueue; // Clone the dependencies object. This is mutated during the render phase, so
        // it cannot be shared with the current fiber.
        var currentDependencies = current.dependencies;
        workInProgress.dependencies = currentDependencies === null ? null : {
            lanes: currentDependencies.lanes,
            firstContext: currentDependencies.firstContext
        }; // These will be overridden during the parent's reconciliation
        workInProgress.sibling = current.sibling;
        workInProgress.index = current.index;
        workInProgress.ref = current.ref;
        workInProgress.selfBaseDuration = current.selfBaseDuration;
        workInProgress.treeBaseDuration = current.treeBaseDuration;
        workInProgress._debugNeedsRemount = current._debugNeedsRemount;
        switch(workInProgress.tag){
            case IndeterminateComponent:
            case FunctionComponent:
            case SimpleMemoComponent:
                workInProgress.type = resolveFunctionForHotReloading(current.type);
                break;
            case ClassComponent:
                workInProgress.type = resolveClassForHotReloading(current.type);
                break;
            case ForwardRef:
                workInProgress.type = resolveForwardRefForHotReloading(current.type);
                break;
        }
        return workInProgress;
    } // Used to reuse a Fiber for a second pass.
    function resetWorkInProgress(workInProgress, renderLanes) {
        // This resets the Fiber to what createFiber or createWorkInProgress would
        // have set the values to before during the first pass. Ideally this wouldn't
        // be necessary but unfortunately many code paths reads from the workInProgress
        // when they should be reading from current and writing to workInProgress.
        // We assume pendingProps, index, key, ref, return are still untouched to
        // avoid doing another reconciliation.
        // Reset the effect flags but keep any Placement tags, since that's something
        // that child fiber is setting, not the reconciliation.
        workInProgress.flags &= StaticMask | Placement; // The effects are no longer valid.
        var current = workInProgress.alternate;
        if (current === null) {
            // Reset to createFiber's initial values.
            workInProgress.childLanes = NoLanes;
            workInProgress.lanes = renderLanes;
            workInProgress.child = null;
            workInProgress.subtreeFlags = NoFlags;
            workInProgress.memoizedProps = null;
            workInProgress.memoizedState = null;
            workInProgress.updateQueue = null;
            workInProgress.dependencies = null;
            workInProgress.stateNode = null;
            // Note: We don't reset the actualTime counts. It's useful to accumulate
            // actual time across multiple render passes.
            workInProgress.selfBaseDuration = 0;
            workInProgress.treeBaseDuration = 0;
        } else {
            // Reset to the cloned values that createWorkInProgress would've.
            workInProgress.childLanes = current.childLanes;
            workInProgress.lanes = current.lanes;
            workInProgress.child = current.child;
            workInProgress.subtreeFlags = NoFlags;
            workInProgress.deletions = null;
            workInProgress.memoizedProps = current.memoizedProps;
            workInProgress.memoizedState = current.memoizedState;
            workInProgress.updateQueue = current.updateQueue; // Needed because Blocks store data on type.
            workInProgress.type = current.type; // Clone the dependencies object. This is mutated during the render phase, so
            // it cannot be shared with the current fiber.
            var currentDependencies = current.dependencies;
            workInProgress.dependencies = currentDependencies === null ? null : {
                lanes: currentDependencies.lanes,
                firstContext: currentDependencies.firstContext
            };
            // Note: We don't reset the actualTime counts. It's useful to accumulate
            // actual time across multiple render passes.
            workInProgress.selfBaseDuration = current.selfBaseDuration;
            workInProgress.treeBaseDuration = current.treeBaseDuration;
        }
        return workInProgress;
    }
    function createHostRootFiber(tag, isStrictMode, concurrentUpdatesByDefaultOverride) {
        var mode;
        if (tag === ConcurrentRoot) {
            mode = ConcurrentMode;
            if (isStrictMode === true) {
                mode |= StrictLegacyMode;
                mode |= StrictEffectsMode;
            }
        } else mode = NoMode;
        if (isDevToolsPresent) // Always collect profile timings when DevTools are present.
        // This enables DevTools to start capturing timing at any point–
        // Without some nodes in the tree having empty base times.
        mode |= ProfileMode;
        return createFiber(HostRoot, null, null, mode);
    }
    function createFiberFromTypeAndProps(type, key, pendingProps, owner, mode, lanes) {
        var fiberTag = IndeterminateComponent; // The resolved type is set if we know what the final type will be. I.e. it's not lazy.
        var resolvedType = type;
        if (typeof type === 'function') {
            if (shouldConstruct$1(type)) {
                fiberTag = ClassComponent;
                resolvedType = resolveClassForHotReloading(resolvedType);
            } else resolvedType = resolveFunctionForHotReloading(resolvedType);
        } else if (typeof type === 'string') fiberTag = HostComponent;
        else getTag: switch(type){
            case REACT_FRAGMENT_TYPE:
                return createFiberFromFragment(pendingProps.children, mode, lanes, key);
            case REACT_STRICT_MODE_TYPE:
                fiberTag = Mode;
                mode |= StrictLegacyMode;
                if ((mode & ConcurrentMode) !== NoMode) // Strict effects should never run on legacy roots
                mode |= StrictEffectsMode;
                break;
            case REACT_PROFILER_TYPE:
                return createFiberFromProfiler(pendingProps, mode, lanes, key);
            case REACT_SUSPENSE_TYPE:
                return createFiberFromSuspense(pendingProps, mode, lanes, key);
            case REACT_SUSPENSE_LIST_TYPE:
                return createFiberFromSuspenseList(pendingProps, mode, lanes, key);
            case REACT_OFFSCREEN_TYPE:
                return createFiberFromOffscreen(pendingProps, mode, lanes, key);
            case REACT_LEGACY_HIDDEN_TYPE:
            // eslint-disable-next-line no-fallthrough
            case REACT_SCOPE_TYPE:
            // eslint-disable-next-line no-fallthrough
            case REACT_CACHE_TYPE:
            // eslint-disable-next-line no-fallthrough
            case REACT_TRACING_MARKER_TYPE:
            // eslint-disable-next-line no-fallthrough
            case REACT_DEBUG_TRACING_MODE_TYPE:
            // eslint-disable-next-line no-fallthrough
            default:
                if (typeof type === 'object' && type !== null) switch(type.$$typeof){
                    case REACT_PROVIDER_TYPE:
                        fiberTag = ContextProvider;
                        break getTag;
                    case REACT_CONTEXT_TYPE:
                        // This is a consumer
                        fiberTag = ContextConsumer;
                        break getTag;
                    case REACT_FORWARD_REF_TYPE:
                        fiberTag = ForwardRef;
                        resolvedType = resolveForwardRefForHotReloading(resolvedType);
                        break getTag;
                    case REACT_MEMO_TYPE:
                        fiberTag = MemoComponent;
                        break getTag;
                    case REACT_LAZY_TYPE:
                        fiberTag = LazyComponent;
                        resolvedType = null;
                        break getTag;
                }
                var info = '';
                if (type === undefined || typeof type === 'object' && type !== null && Object.keys(type).length === 0) info += " You likely forgot to export your component from the file it's defined in, or you might have mixed up default and named imports.";
                var ownerName = owner ? getComponentNameFromFiber(owner) : null;
                if (ownerName) info += '\n\nCheck the render method of `' + ownerName + '`.';
                throw new Error("Element type is invalid: expected a string (for built-in components) or a class/function (for composite components) " + ("but got: " + (type == null ? type : typeof type) + "." + info));
        }
        var fiber = createFiber(fiberTag, pendingProps, key, mode);
        fiber.elementType = type;
        fiber.type = resolvedType;
        fiber.lanes = lanes;
        fiber._debugOwner = owner;
        return fiber;
    }
    function createFiberFromElement(element, mode, lanes) {
        var owner = null;
        owner = element._owner;
        var type = element.type;
        var key = element.key;
        var pendingProps = element.props;
        var fiber = createFiberFromTypeAndProps(type, key, pendingProps, owner, mode, lanes);
        fiber._debugSource = element._source;
        fiber._debugOwner = element._owner;
        return fiber;
    }
    function createFiberFromFragment(elements, mode, lanes, key) {
        var fiber = createFiber(Fragment, elements, key, mode);
        fiber.lanes = lanes;
        return fiber;
    }
    function createFiberFromProfiler(pendingProps, mode, lanes, key) {
        if (typeof pendingProps.id !== 'string') error1('Profiler must specify an "id" of type `string` as a prop. Received the type `%s` instead.', typeof pendingProps.id);
        var fiber = createFiber(Profiler, pendingProps, key, mode | ProfileMode);
        fiber.elementType = REACT_PROFILER_TYPE;
        fiber.lanes = lanes;
        fiber.stateNode = {
            effectDuration: 0,
            passiveEffectDuration: 0
        };
        return fiber;
    }
    function createFiberFromSuspense(pendingProps, mode, lanes, key) {
        var fiber = createFiber(SuspenseComponent, pendingProps, key, mode);
        fiber.elementType = REACT_SUSPENSE_TYPE;
        fiber.lanes = lanes;
        return fiber;
    }
    function createFiberFromSuspenseList(pendingProps, mode, lanes, key) {
        var fiber = createFiber(SuspenseListComponent, pendingProps, key, mode);
        fiber.elementType = REACT_SUSPENSE_LIST_TYPE;
        fiber.lanes = lanes;
        return fiber;
    }
    function createFiberFromOffscreen(pendingProps, mode, lanes, key) {
        var fiber = createFiber(OffscreenComponent, pendingProps, key, mode);
        fiber.elementType = REACT_OFFSCREEN_TYPE;
        fiber.lanes = lanes;
        var primaryChildInstance = {};
        fiber.stateNode = primaryChildInstance;
        return fiber;
    }
    function createFiberFromText(content, mode, lanes) {
        var fiber = createFiber(HostText, content, null, mode);
        fiber.lanes = lanes;
        return fiber;
    }
    function createFiberFromHostInstanceForDeletion() {
        var fiber = createFiber(HostComponent, null, null, NoMode);
        fiber.elementType = 'DELETED';
        return fiber;
    }
    function createFiberFromDehydratedFragment(dehydratedNode) {
        var fiber = createFiber(DehydratedFragment, null, null, NoMode);
        fiber.stateNode = dehydratedNode;
        return fiber;
    }
    function createFiberFromPortal(portal, mode, lanes) {
        var pendingProps = portal.children !== null ? portal.children : [];
        var fiber = createFiber(HostPortal, pendingProps, portal.key, mode);
        fiber.lanes = lanes;
        fiber.stateNode = {
            containerInfo: portal.containerInfo,
            pendingChildren: null,
            // Used by persistent updates
            implementation: portal.implementation
        };
        return fiber;
    } // Used for stashing WIP properties to replay failed work in DEV.
    function assignFiberPropertiesInDEV(target, source) {
        if (target === null) // This Fiber's initial properties will always be overwritten.
        // We only use a Fiber to ensure the same hidden class so DEV isn't slow.
        target = createFiber(IndeterminateComponent, null, null, NoMode);
         // This is intentionally written as a list of all properties.
        // We tried to use Object.assign() instead but this is called in
        // the hottest path, and Object.assign() was too slow:
        // https://github.com/facebook/react/issues/12502
        // This code is DEV-only so size is not a concern.
        target.tag = source.tag;
        target.key = source.key;
        target.elementType = source.elementType;
        target.type = source.type;
        target.stateNode = source.stateNode;
        target.return = source.return;
        target.child = source.child;
        target.sibling = source.sibling;
        target.index = source.index;
        target.ref = source.ref;
        target.pendingProps = source.pendingProps;
        target.memoizedProps = source.memoizedProps;
        target.updateQueue = source.updateQueue;
        target.memoizedState = source.memoizedState;
        target.dependencies = source.dependencies;
        target.mode = source.mode;
        target.flags = source.flags;
        target.subtreeFlags = source.subtreeFlags;
        target.deletions = source.deletions;
        target.lanes = source.lanes;
        target.childLanes = source.childLanes;
        target.alternate = source.alternate;
        target.actualDuration = source.actualDuration;
        target.actualStartTime = source.actualStartTime;
        target.selfBaseDuration = source.selfBaseDuration;
        target.treeBaseDuration = source.treeBaseDuration;
        target._debugSource = source._debugSource;
        target._debugOwner = source._debugOwner;
        target._debugNeedsRemount = source._debugNeedsRemount;
        target._debugHookTypes = source._debugHookTypes;
        return target;
    }
    function FiberRootNode(containerInfo, tag, hydrate, identifierPrefix, onRecoverableError) {
        this.tag = tag;
        this.containerInfo = containerInfo;
        this.pendingChildren = null;
        this.current = null;
        this.pingCache = null;
        this.finishedWork = null;
        this.timeoutHandle = noTimeout;
        this.context = null;
        this.pendingContext = null;
        this.callbackNode = null;
        this.callbackPriority = NoLane;
        this.eventTimes = createLaneMap(NoLanes);
        this.expirationTimes = createLaneMap(NoTimestamp);
        this.pendingLanes = NoLanes;
        this.suspendedLanes = NoLanes;
        this.pingedLanes = NoLanes;
        this.expiredLanes = NoLanes;
        this.mutableReadLanes = NoLanes;
        this.finishedLanes = NoLanes;
        this.entangledLanes = NoLanes;
        this.entanglements = createLaneMap(NoLanes);
        this.identifierPrefix = identifierPrefix;
        this.onRecoverableError = onRecoverableError;
        this.mutableSourceEagerHydrationData = null;
        this.effectDuration = 0;
        this.passiveEffectDuration = 0;
        this.memoizedUpdaters = new Set();
        var pendingUpdatersLaneMap = this.pendingUpdatersLaneMap = [];
        for(var _i = 0; _i < TotalLanes; _i++)pendingUpdatersLaneMap.push(new Set());
        switch(tag){
            case ConcurrentRoot:
                this._debugRootType = hydrate ? 'hydrateRoot()' : 'createRoot()';
                break;
            case LegacyRoot:
                this._debugRootType = hydrate ? 'hydrate()' : 'render()';
                break;
        }
    }
    function createFiberRoot(containerInfo, tag, hydrate, initialChildren, hydrationCallbacks, isStrictMode, concurrentUpdatesByDefaultOverride, // host config, but because they are passed in at runtime, we have to thread
    // them through the root constructor. Perhaps we should put them all into a
    // single type, like a DynamicHostConfig that is defined by the renderer.
    identifierPrefix, onRecoverableError, transitionCallbacks) {
        var root = new FiberRootNode(containerInfo, tag, hydrate, identifierPrefix, onRecoverableError);
        // stateNode is any.
        var uninitializedFiber = createHostRootFiber(tag, isStrictMode);
        root.current = uninitializedFiber;
        uninitializedFiber.stateNode = root;
        var _initialState = {
            element: initialChildren,
            isDehydrated: hydrate,
            cache: null,
            // not enabled yet
            transitions: null,
            pendingSuspenseBoundaries: null
        };
        uninitializedFiber.memoizedState = _initialState;
        initializeUpdateQueue(uninitializedFiber);
        return root;
    }
    var ReactVersion = '18.1.0';
    function createPortal(children, containerInfo, implementation) {
        var key = arguments.length > 3 && arguments[3] !== undefined ? arguments[3] : null;
        checkKeyStringCoercion(key);
        return {
            // This tag allow us to uniquely identify this as a React Portal
            $$typeof: REACT_PORTAL_TYPE,
            key: key == null ? null : '' + key,
            children: children,
            containerInfo: containerInfo,
            implementation: implementation
        };
    }
    var didWarnAboutNestedUpdates;
    var didWarnAboutFindNodeInStrictMode;
    didWarnAboutNestedUpdates = false;
    didWarnAboutFindNodeInStrictMode = {};
    function getContextForSubtree(parentComponent) {
        if (!parentComponent) return emptyContextObject;
        var fiber = get1(parentComponent);
        var parentContext = findCurrentUnmaskedContext(fiber);
        if (fiber.tag === ClassComponent) {
            var Component = fiber.type;
            if (isContextProvider(Component)) return processChildContext(fiber, Component, parentContext);
        }
        return parentContext;
    }
    function findHostInstanceWithWarning(component, methodName) {
        var fiber = get1(component);
        if (fiber === undefined) {
            if (typeof component.render === 'function') throw new Error('Unable to find node on an unmounted component.');
            else {
                var keys = Object.keys(component).join(',');
                throw new Error("Argument appears to not be a ReactComponent. Keys: " + keys);
            }
        }
        var hostFiber = findCurrentHostFiber(fiber);
        if (hostFiber === null) return null;
        if (hostFiber.mode & StrictLegacyMode) {
            var componentName = getComponentNameFromFiber(fiber) || 'Component';
            if (!didWarnAboutFindNodeInStrictMode[componentName]) {
                didWarnAboutFindNodeInStrictMode[componentName] = true;
                var previousFiber = current1;
                try {
                    setCurrentFiber(hostFiber);
                    if (fiber.mode & StrictLegacyMode) error1("%s is deprecated in StrictMode. %s was passed an instance of %s which is inside StrictMode. Instead, add a ref directly to the element you want to reference. Learn more about using refs safely here: https://reactjs.org/link/strict-mode-find-node", methodName, methodName, componentName);
                    else error1("%s is deprecated in StrictMode. %s was passed an instance of %s which renders StrictMode children. Instead, add a ref directly to the element you want to reference. Learn more about using refs safely here: https://reactjs.org/link/strict-mode-find-node", methodName, methodName, componentName);
                } finally{
                    // Ideally this should reset to previous but this shouldn't be called in
                    // render and there's another warning for that anyway.
                    if (previousFiber) setCurrentFiber(previousFiber);
                    else resetCurrentFiber();
                }
            }
        }
        return hostFiber.stateNode;
    }
    function createContainer(containerInfo, tag, hydrationCallbacks, isStrictMode, concurrentUpdatesByDefaultOverride, identifierPrefix, onRecoverableError, transitionCallbacks) {
        var hydrate = false;
        var initialChildren = null;
        return createFiberRoot(containerInfo, tag, hydrate, initialChildren, hydrationCallbacks, isStrictMode, concurrentUpdatesByDefaultOverride, identifierPrefix, onRecoverableError);
    }
    function createHydrationContainer(initialChildren, callback, containerInfo, tag, hydrationCallbacks, isStrictMode, concurrentUpdatesByDefaultOverride, identifierPrefix, onRecoverableError, transitionCallbacks) {
        var hydrate = true;
        var root = createFiberRoot(containerInfo, tag, hydrate, initialChildren, hydrationCallbacks, isStrictMode, concurrentUpdatesByDefaultOverride, identifierPrefix, onRecoverableError); // TODO: Move this to FiberRoot constructor
        root.context = getContextForSubtree(null); // Schedule the initial render. In a hydration root, this is different from
        // a regular update because the initial render must match was was rendered
        // on the server.
        // NOTE: This update intentionally doesn't have a payload. We're only using
        // the update to schedule work on the root fiber (and, for legacy roots, to
        // enqueue the callback if one is provided).
        var current = root.current;
        var eventTime = requestEventTime();
        var lane = requestUpdateLane(current);
        var update = createUpdate(eventTime, lane);
        update.callback = callback !== undefined && callback !== null ? callback : null;
        enqueueUpdate(current, update);
        scheduleInitialHydrationOnRoot(root, lane, eventTime);
        return root;
    }
    function updateContainer(element, container, parentComponent, callback) {
        onScheduleRoot(container, element);
        var current$1 = container.current;
        var eventTime = requestEventTime();
        var lane = requestUpdateLane(current$1);
        markRenderScheduled(lane);
        var context = getContextForSubtree(parentComponent);
        if (container.context === null) container.context = context;
        else container.pendingContext = context;
        if (isRendering && current1 !== null && !didWarnAboutNestedUpdates) {
            didWarnAboutNestedUpdates = true;
            error1("Render methods should be a pure function of props and state; triggering nested component updates from render is not allowed. If necessary, trigger nested updates in componentDidUpdate.\n\nCheck the render method of %s.", getComponentNameFromFiber(current1) || 'Unknown');
        }
        var update = createUpdate(eventTime, lane); // Caution: React DevTools currently depends on this property
        // being called "element".
        update.payload = {
            element: element
        };
        callback = callback === undefined ? null : callback;
        if (callback !== null) {
            if (typeof callback !== 'function') error1("render(...): Expected the last optional `callback` argument to be a function. Instead received: %s.", callback);
            update.callback = callback;
        }
        enqueueUpdate(current$1, update);
        var root = scheduleUpdateOnFiber(current$1, lane, eventTime);
        if (root !== null) entangleTransitions(root, current$1, lane);
        return lane;
    }
    function getPublicRootInstance(container) {
        var containerFiber = container.current;
        if (!containerFiber.child) return null;
        switch(containerFiber.child.tag){
            case HostComponent:
                return getPublicInstance(containerFiber.child.stateNode);
            default:
                return containerFiber.child.stateNode;
        }
    }
    function attemptSynchronousHydration$1(fiber) {
        switch(fiber.tag){
            case HostRoot:
                var root = fiber.stateNode;
                if (isRootDehydrated(root)) {
                    // Flush the first scheduled "update".
                    var lanes = getHighestPriorityPendingLanes(root);
                    flushRoot(root, lanes);
                }
                break;
            case SuspenseComponent:
                var eventTime = requestEventTime();
                flushSync(function() {
                    return scheduleUpdateOnFiber(fiber, SyncLane, eventTime);
                }); // If we're still blocked after this, we need to increase
                // the priority of any promises resolving within this
                // boundary so that they next attempt also has higher pri.
                var retryLane = SyncLane;
                markRetryLaneIfNotHydrated(fiber, retryLane);
                break;
        }
    }
    function markRetryLaneImpl(fiber, retryLane) {
        var suspenseState = fiber.memoizedState;
        if (suspenseState !== null && suspenseState.dehydrated !== null) suspenseState.retryLane = higherPriorityLane(suspenseState.retryLane, retryLane);
    } // Increases the priority of thenables when they resolve within this boundary.
    function markRetryLaneIfNotHydrated(fiber, retryLane) {
        markRetryLaneImpl(fiber, retryLane);
        var alternate = fiber.alternate;
        if (alternate) markRetryLaneImpl(alternate, retryLane);
    }
    function attemptContinuousHydration$1(fiber) {
        if (fiber.tag !== SuspenseComponent) // We ignore HostRoots here because we can't increase
        // their priority and they should not suspend on I/O,
        // since you have to wrap anything that might suspend in
        // Suspense.
        return;
        var eventTime = requestEventTime();
        var lane = SelectiveHydrationLane;
        scheduleUpdateOnFiber(fiber, lane, eventTime);
        markRetryLaneIfNotHydrated(fiber, lane);
    }
    function attemptHydrationAtCurrentPriority$1(fiber) {
        if (fiber.tag !== SuspenseComponent) // We ignore HostRoots here because we can't increase
        // their priority other than synchronously flush it.
        return;
        var eventTime = requestEventTime();
        var lane = requestUpdateLane(fiber);
        scheduleUpdateOnFiber(fiber, lane, eventTime);
        markRetryLaneIfNotHydrated(fiber, lane);
    }
    function findHostInstanceWithNoPortals(fiber) {
        var hostFiber = findCurrentHostFiberWithNoPortals(fiber);
        if (hostFiber === null) return null;
        return hostFiber.stateNode;
    }
    var shouldErrorImpl = function(fiber) {
        return null;
    };
    function shouldError(fiber) {
        return shouldErrorImpl(fiber);
    }
    var shouldSuspendImpl = function(fiber) {
        return false;
    };
    function shouldSuspend(fiber) {
        return shouldSuspendImpl(fiber);
    }
    var overrideHookState = null;
    var overrideHookStateDeletePath = null;
    var overrideHookStateRenamePath = null;
    var overrideProps = null;
    var overridePropsDeletePath = null;
    var overridePropsRenamePath = null;
    var scheduleUpdate = null;
    var setErrorHandler = null;
    var setSuspenseHandler = null;
    var copyWithDeleteImpl = function(obj, path, index) {
        var key = path[index];
        var updated = isArray(obj) ? obj.slice() : assign({}, obj);
        if (index + 1 === path.length) {
            if (isArray(updated)) updated.splice(key, 1);
            else delete updated[key];
            return updated;
        } // $FlowFixMe number or string is fine here
        updated[key] = copyWithDeleteImpl(obj[key], path, index + 1);
        return updated;
    };
    var copyWithDelete = function(obj, path) {
        return copyWithDeleteImpl(obj, path, 0);
    };
    var copyWithRenameImpl = function(obj, oldPath, newPath, index) {
        var oldKey = oldPath[index];
        var updated = isArray(obj) ? obj.slice() : assign({}, obj);
        if (index + 1 === oldPath.length) {
            var newKey = newPath[index]; // $FlowFixMe number or string is fine here
            updated[newKey] = updated[oldKey];
            if (isArray(updated)) updated.splice(oldKey, 1);
            else delete updated[oldKey];
        } else // $FlowFixMe number or string is fine here
        updated[oldKey] = copyWithRenameImpl(obj[oldKey], oldPath, newPath, index + 1);
        return updated;
    };
    var copyWithRename = function(obj, oldPath, newPath) {
        if (oldPath.length !== newPath.length) {
            warn('copyWithRename() expects paths of the same length');
            return;
        } else {
            for(var i = 0; i < newPath.length - 1; i++)if (oldPath[i] !== newPath[i]) {
                warn('copyWithRename() expects paths to be the same except for the deepest key');
                return;
            }
        }
        return copyWithRenameImpl(obj, oldPath, newPath, 0);
    };
    var copyWithSetImpl = function(obj, path, index, value) {
        if (index >= path.length) return value;
        var key = path[index];
        var updated = isArray(obj) ? obj.slice() : assign({}, obj); // $FlowFixMe number or string is fine here
        updated[key] = copyWithSetImpl(obj[key], path, index + 1, value);
        return updated;
    };
    var copyWithSet = function(obj, path, value) {
        return copyWithSetImpl(obj, path, 0, value);
    };
    var findHook = function(fiber, id) {
        // For now, the "id" of stateful hooks is just the stateful hook index.
        // This may change in the future with e.g. nested hooks.
        var currentHook = fiber.memoizedState;
        while(currentHook !== null && id > 0){
            currentHook = currentHook.next;
            id--;
        }
        return currentHook;
    }; // Support DevTools editable values for useState and useReducer.
    overrideHookState = function(fiber, id, path, value) {
        var hook = findHook(fiber, id);
        if (hook !== null) {
            var newState = copyWithSet(hook.memoizedState, path, value);
            hook.memoizedState = newState;
            hook.baseState = newState; // We aren't actually adding an update to the queue,
            // because there is no update we can add for useReducer hooks that won't trigger an error.
            // (There's no appropriate action type for DevTools overrides.)
            // As a result though, React will see the scheduled update as a noop and bailout.
            // Shallow cloning props works as a workaround for now to bypass the bailout check.
            fiber.memoizedProps = assign({}, fiber.memoizedProps);
            scheduleUpdateOnFiber(fiber, SyncLane, NoTimestamp);
        }
    };
    overrideHookStateDeletePath = function(fiber, id, path) {
        var hook = findHook(fiber, id);
        if (hook !== null) {
            var newState = copyWithDelete(hook.memoizedState, path);
            hook.memoizedState = newState;
            hook.baseState = newState; // We aren't actually adding an update to the queue,
            // because there is no update we can add for useReducer hooks that won't trigger an error.
            // (There's no appropriate action type for DevTools overrides.)
            // As a result though, React will see the scheduled update as a noop and bailout.
            // Shallow cloning props works as a workaround for now to bypass the bailout check.
            fiber.memoizedProps = assign({}, fiber.memoizedProps);
            scheduleUpdateOnFiber(fiber, SyncLane, NoTimestamp);
        }
    };
    overrideHookStateRenamePath = function(fiber, id, oldPath, newPath) {
        var hook = findHook(fiber, id);
        if (hook !== null) {
            var newState = copyWithRename(hook.memoizedState, oldPath, newPath);
            hook.memoizedState = newState;
            hook.baseState = newState; // We aren't actually adding an update to the queue,
            // because there is no update we can add for useReducer hooks that won't trigger an error.
            // (There's no appropriate action type for DevTools overrides.)
            // As a result though, React will see the scheduled update as a noop and bailout.
            // Shallow cloning props works as a workaround for now to bypass the bailout check.
            fiber.memoizedProps = assign({}, fiber.memoizedProps);
            scheduleUpdateOnFiber(fiber, SyncLane, NoTimestamp);
        }
    }; // Support DevTools props for function components, forwardRef, memo, host components, etc.
    overrideProps = function(fiber, path, value) {
        fiber.pendingProps = copyWithSet(fiber.memoizedProps, path, value);
        if (fiber.alternate) fiber.alternate.pendingProps = fiber.pendingProps;
        scheduleUpdateOnFiber(fiber, SyncLane, NoTimestamp);
    };
    overridePropsDeletePath = function(fiber, path) {
        fiber.pendingProps = copyWithDelete(fiber.memoizedProps, path);
        if (fiber.alternate) fiber.alternate.pendingProps = fiber.pendingProps;
        scheduleUpdateOnFiber(fiber, SyncLane, NoTimestamp);
    };
    overridePropsRenamePath = function(fiber, oldPath, newPath) {
        fiber.pendingProps = copyWithRename(fiber.memoizedProps, oldPath, newPath);
        if (fiber.alternate) fiber.alternate.pendingProps = fiber.pendingProps;
        scheduleUpdateOnFiber(fiber, SyncLane, NoTimestamp);
    };
    scheduleUpdate = function(fiber) {
        scheduleUpdateOnFiber(fiber, SyncLane, NoTimestamp);
    };
    setErrorHandler = function(newShouldErrorImpl) {
        shouldErrorImpl = newShouldErrorImpl;
    };
    setSuspenseHandler = function(newShouldSuspendImpl) {
        shouldSuspendImpl = newShouldSuspendImpl;
    };
    function findHostInstanceByFiber(fiber) {
        var hostFiber = findCurrentHostFiber(fiber);
        if (hostFiber === null) return null;
        return hostFiber.stateNode;
    }
    function emptyFindFiberByHostInstance(instance) {
        return null;
    }
    function getCurrentFiberForDevTools() {
        return current1;
    }
    function injectIntoDevTools(devToolsConfig) {
        var findFiberByHostInstance = devToolsConfig.findFiberByHostInstance;
        var ReactCurrentDispatcher = ReactSharedInternals.ReactCurrentDispatcher;
        return injectInternals({
            bundleType: devToolsConfig.bundleType,
            version: devToolsConfig.version,
            rendererPackageName: devToolsConfig.rendererPackageName,
            rendererConfig: devToolsConfig.rendererConfig,
            overrideHookState: overrideHookState,
            overrideHookStateDeletePath: overrideHookStateDeletePath,
            overrideHookStateRenamePath: overrideHookStateRenamePath,
            overrideProps: overrideProps,
            overridePropsDeletePath: overridePropsDeletePath,
            overridePropsRenamePath: overridePropsRenamePath,
            setErrorHandler: setErrorHandler,
            setSuspenseHandler: setSuspenseHandler,
            scheduleUpdate: scheduleUpdate,
            currentDispatcherRef: ReactCurrentDispatcher,
            findHostInstanceByFiber: findHostInstanceByFiber,
            findFiberByHostInstance: findFiberByHostInstance || emptyFindFiberByHostInstance,
            // React Refresh
            findHostInstancesForRefresh: findHostInstancesForRefresh,
            scheduleRefresh: scheduleRefresh,
            scheduleRoot: scheduleRoot,
            setRefreshHandler: setRefreshHandler,
            // Enables DevTools to append owner stacks to error messages in DEV mode.
            getCurrentFiber: getCurrentFiberForDevTools,
            // Enables DevTools to detect reconciler version rather than renderer version
            // which may not match for third party renderers.
            reconcilerVersion: ReactVersion
        });
    }
    /* global reportError */ var defaultOnRecoverableError = typeof reportError === 'function' ? // emulating an uncaught JavaScript error.
    reportError : function(error) {
        // In older browsers and test environments, fallback to console.error.
        // eslint-disable-next-line react-internal/no-production-logging
        console['error'](error);
    };
    function ReactDOMRoot(internalRoot) {
        this._internalRoot = internalRoot;
    }
    ReactDOMHydrationRoot.prototype.render = ReactDOMRoot.prototype.render = function(children) {
        var root = this._internalRoot;
        if (root === null) throw new Error('Cannot update an unmounted root.');
        if (typeof arguments[1] === 'function') error1("render(...): does not support the second callback argument. To execute a side effect after rendering, declare it in a component body with useEffect().");
        else if (isValidContainer(arguments[1])) error1("You passed a container to the second argument of root.render(...). You don't need to pass it again since you already passed it to create the root.");
        else if (typeof arguments[1] !== 'undefined') error1("You passed a second argument to root.render(...) but it only accepts one argument.");
        var container = root.containerInfo;
        if (container.nodeType !== COMMENT_NODE) {
            var hostInstance = findHostInstanceWithNoPortals(root.current);
            if (hostInstance) {
                if (hostInstance.parentNode !== container) error1("render(...): It looks like the React-rendered content of the root container was removed without using React. This is not supported and will cause errors. Instead, call root.unmount() to empty a root's container.");
            }
        }
        updateContainer(children, root, null, null);
    };
    ReactDOMHydrationRoot.prototype.unmount = ReactDOMRoot.prototype.unmount = function() {
        if (typeof arguments[0] === 'function') error1("unmount(...): does not support a callback argument. To execute a side effect after rendering, declare it in a component body with useEffect().");
        var root = this._internalRoot;
        if (root !== null) {
            this._internalRoot = null;
            var container = root.containerInfo;
            if (isAlreadyRendering()) error1("Attempted to synchronously unmount a root while React was already rendering. React cannot finish unmounting the root until the current render has completed, which may lead to a race condition.");
            flushSync(function() {
                updateContainer(null, root, null, null);
            });
            unmarkContainerAsRoot(container);
        }
    };
    function createRoot(container, options) {
        if (!isValidContainer(container)) throw new Error('createRoot(...): Target container is not a DOM element.');
        warnIfReactDOMContainerInDEV(container);
        var isStrictMode = false;
        var concurrentUpdatesByDefaultOverride = false;
        var identifierPrefix = '';
        var onRecoverableError = defaultOnRecoverableError;
        var transitionCallbacks = null;
        if (options !== null && options !== undefined) {
            if (options.hydrate) warn('hydrate through createRoot is deprecated. Use ReactDOMClient.hydrateRoot(container, <App />) instead.');
            else if (typeof options === 'object' && options !== null && options.$$typeof === REACT_ELEMENT_TYPE) error1("You passed a JSX element to createRoot. You probably meant to call root.render instead. Example usage:\n\n  let root = createRoot(domContainer);\n  root.render(<App />);");
            if (options.unstable_strictMode === true) isStrictMode = true;
            if (options.identifierPrefix !== undefined) identifierPrefix = options.identifierPrefix;
            if (options.onRecoverableError !== undefined) onRecoverableError = options.onRecoverableError;
            if (options.transitionCallbacks !== undefined) transitionCallbacks = options.transitionCallbacks;
        }
        var root = createContainer(container, ConcurrentRoot, null, isStrictMode, concurrentUpdatesByDefaultOverride, identifierPrefix, onRecoverableError);
        markContainerAsRoot(root.current, container);
        var rootContainerElement = container.nodeType === COMMENT_NODE ? container.parentNode : container;
        listenToAllSupportedEvents(rootContainerElement);
        return new ReactDOMRoot(root);
    }
    function ReactDOMHydrationRoot(internalRoot) {
        this._internalRoot = internalRoot;
    }
    function scheduleHydration(target) {
        if (target) queueExplicitHydrationTarget(target);
    }
    ReactDOMHydrationRoot.prototype.unstable_scheduleHydration = scheduleHydration;
    function hydrateRoot(container, initialChildren, options) {
        if (!isValidContainer(container)) throw new Error('hydrateRoot(...): Target container is not a DOM element.');
        warnIfReactDOMContainerInDEV(container);
        if (initialChildren === undefined) error1("Must provide initial children as second argument to hydrateRoot. Example usage: hydrateRoot(domContainer, <App />)");
        // the hydration callbacks.
        var hydrationCallbacks = options != null ? options : null; // TODO: Delete this option
        var mutableSources = options != null && options.hydratedSources || null;
        var isStrictMode = false;
        var concurrentUpdatesByDefaultOverride = false;
        var identifierPrefix = '';
        var onRecoverableError = defaultOnRecoverableError;
        if (options !== null && options !== undefined) {
            if (options.unstable_strictMode === true) isStrictMode = true;
            if (options.identifierPrefix !== undefined) identifierPrefix = options.identifierPrefix;
            if (options.onRecoverableError !== undefined) onRecoverableError = options.onRecoverableError;
        }
        var root = createHydrationContainer(initialChildren, null, container, ConcurrentRoot, hydrationCallbacks, isStrictMode, concurrentUpdatesByDefaultOverride, identifierPrefix, onRecoverableError);
        markContainerAsRoot(root.current, container); // This can't be a comment node since hydration doesn't work on comment nodes anyway.
        listenToAllSupportedEvents(container);
        if (mutableSources) for(var i = 0; i < mutableSources.length; i++){
            var mutableSource = mutableSources[i];
            registerMutableSourceForHydration(root, mutableSource);
        }
        return new ReactDOMHydrationRoot(root);
    }
    function isValidContainer(node) {
        return !!(node && (node.nodeType === ELEMENT_NODE || node.nodeType === DOCUMENT_NODE || node.nodeType === DOCUMENT_FRAGMENT_NODE || !disableCommentsAsDOMContainers));
    } // TODO: Remove this function which also includes comment nodes.
    // We only use it in places that are currently more relaxed.
    function isValidContainerLegacy(node) {
        return !!(node && (node.nodeType === ELEMENT_NODE || node.nodeType === DOCUMENT_NODE || node.nodeType === DOCUMENT_FRAGMENT_NODE || node.nodeType === COMMENT_NODE && node.nodeValue === ' react-mount-point-unstable '));
    }
    function warnIfReactDOMContainerInDEV(container) {
        if (container.nodeType === ELEMENT_NODE && container.tagName && container.tagName.toUpperCase() === 'BODY') error1("createRoot(): Creating roots directly with document.body is discouraged, since its children are often manipulated by third-party scripts and browser extensions. This may lead to subtle reconciliation issues. Try using a container element created for your app.");
        if (isContainerMarkedAsRoot(container)) {
            if (container._reactRootContainer) error1("You are calling ReactDOMClient.createRoot() on a container that was previously passed to ReactDOM.render(). This is not supported.");
            else error1("You are calling ReactDOMClient.createRoot() on a container that has already been passed to createRoot() before. Instead, call root.render() on the existing root instead if you want to update it.");
        }
    }
    var ReactCurrentOwner$3 = ReactSharedInternals.ReactCurrentOwner;
    var topLevelUpdateWarnings;
    topLevelUpdateWarnings = function(container) {
        if (container._reactRootContainer && container.nodeType !== COMMENT_NODE) {
            var hostInstance = findHostInstanceWithNoPortals(container._reactRootContainer.current);
            if (hostInstance) {
                if (hostInstance.parentNode !== container) error1("render(...): It looks like the React-rendered content of this container was removed without using React. This is not supported and will cause errors. Instead, call ReactDOM.unmountComponentAtNode to empty a container.");
            }
        }
        var isRootRenderedBySomeReact = !!container._reactRootContainer;
        var rootEl = getReactRootElementInContainer(container);
        var hasNonRootReactChild = !!(rootEl && getInstanceFromNode(rootEl));
        if (hasNonRootReactChild && !isRootRenderedBySomeReact) error1("render(...): Replacing React-rendered children with a new root component. If you intended to update the children of this node, you should instead have the existing children update their state and render the new components instead of calling ReactDOM.render.");
        if (container.nodeType === ELEMENT_NODE && container.tagName && container.tagName.toUpperCase() === 'BODY') error1("render(): Rendering components directly into document.body is discouraged, since its children are often manipulated by third-party scripts and browser extensions. This may lead to subtle reconciliation issues. Try rendering into a container element created for your app.");
    };
    function getReactRootElementInContainer(container) {
        if (!container) return null;
        if (container.nodeType === DOCUMENT_NODE) return container.documentElement;
        else return container.firstChild;
    }
    function noopOnRecoverableError() {
    // legacy API.
    }
    function legacyCreateRootFromDOMContainer(container, initialChildren, parentComponent, callback, isHydrationContainer) {
        if (isHydrationContainer) {
            if (typeof callback === 'function') {
                var originalCallback = callback;
                callback = function() {
                    var instance = getPublicRootInstance(root);
                    originalCallback.call(instance);
                };
            }
            var root = createHydrationContainer(initialChildren, callback, container, LegacyRoot, null, false, false, '', noopOnRecoverableError);
            container._reactRootContainer = root;
            markContainerAsRoot(root.current, container);
            var rootContainerElement = container.nodeType === COMMENT_NODE ? container.parentNode : container;
            listenToAllSupportedEvents(rootContainerElement);
            flushSync();
            return root;
        } else {
            // First clear any existing content.
            var rootSibling;
            while(rootSibling = container.lastChild)container.removeChild(rootSibling);
            if (typeof callback === 'function') {
                var _originalCallback = callback;
                callback = function() {
                    var instance = getPublicRootInstance(_root);
                    _originalCallback.call(instance);
                };
            }
            var _root = createContainer(container, LegacyRoot, null, false, false, '', noopOnRecoverableError);
            container._reactRootContainer = _root;
            markContainerAsRoot(_root.current, container);
            var _rootContainerElement = container.nodeType === COMMENT_NODE ? container.parentNode : container;
            listenToAllSupportedEvents(_rootContainerElement); // Initial mount should not be batched.
            flushSync(function() {
                updateContainer(initialChildren, _root, parentComponent, callback);
            });
            return _root;
        }
    }
    function warnOnInvalidCallback$1(callback, callerName) {
        if (callback !== null && typeof callback !== 'function') error1("%s(...): Expected the last optional `callback` argument to be a function. Instead received: %s.", callerName, callback);
    }
    function legacyRenderSubtreeIntoContainer(parentComponent, children, container, forceHydrate, callback) {
        topLevelUpdateWarnings(container);
        warnOnInvalidCallback$1(callback === undefined ? null : callback, 'render');
        var maybeRoot = container._reactRootContainer;
        var root;
        if (!maybeRoot) // Initial mount
        root = legacyCreateRootFromDOMContainer(container, children, parentComponent, callback, forceHydrate);
        else {
            root = maybeRoot;
            if (typeof callback === 'function') {
                var originalCallback = callback;
                callback = function() {
                    var instance = getPublicRootInstance(root);
                    originalCallback.call(instance);
                };
            } // Update
            updateContainer(children, root, parentComponent, callback);
        }
        return getPublicRootInstance(root);
    }
    function findDOMNode(componentOrElement) {
        var owner = ReactCurrentOwner$3.current;
        if (owner !== null && owner.stateNode !== null) {
            var warnedAboutRefsInRender = owner.stateNode._warnedAboutRefsInRender;
            if (!warnedAboutRefsInRender) error1("%s is accessing findDOMNode inside its render(). render() should be a pure function of props and state. It should never access something that requires stale data from the previous render, such as refs. Move this logic to componentDidMount and componentDidUpdate instead.", getComponentNameFromType(owner.type) || 'A component');
            owner.stateNode._warnedAboutRefsInRender = true;
        }
        if (componentOrElement == null) return null;
        if (componentOrElement.nodeType === ELEMENT_NODE) return componentOrElement;
        return findHostInstanceWithWarning(componentOrElement, 'findDOMNode');
    }
    function hydrate1(element, container, callback) {
        error1("ReactDOM.hydrate is no longer supported in React 18. Use hydrateRoot instead. Until you switch to the new API, your app will behave as if it's running React 17. Learn more: https://reactjs.org/link/switch-to-createroot");
        if (!isValidContainerLegacy(container)) throw new Error('Target container is not a DOM element.');
        var isModernRoot = isContainerMarkedAsRoot(container) && container._reactRootContainer === undefined;
        if (isModernRoot) error1("You are calling ReactDOM.hydrate() on a container that was previously passed to ReactDOMClient.createRoot(). This is not supported. Did you mean to call hydrateRoot(container, element)?");
        return legacyRenderSubtreeIntoContainer(null, element, container, true, callback);
    }
    function render1(element, container, callback) {
        error1("ReactDOM.render is no longer supported in React 18. Use createRoot instead. Until you switch to the new API, your app will behave as if it's running React 17. Learn more: https://reactjs.org/link/switch-to-createroot");
        if (!isValidContainerLegacy(container)) throw new Error('Target container is not a DOM element.');
        var isModernRoot = isContainerMarkedAsRoot(container) && container._reactRootContainer === undefined;
        if (isModernRoot) error1("You are calling ReactDOM.render() on a container that was previously passed to ReactDOMClient.createRoot(). This is not supported. Did you mean to call root.render(element)?");
        return legacyRenderSubtreeIntoContainer(null, element, container, false, callback);
    }
    function unstable_renderSubtreeIntoContainer(parentComponent, element, containerNode, callback) {
        error1("ReactDOM.unstable_renderSubtreeIntoContainer() is no longer supported in React 18. Consider using a portal instead. Until you switch to the createRoot API, your app will behave as if it's running React 17. Learn more: https://reactjs.org/link/switch-to-createroot");
        if (!isValidContainerLegacy(containerNode)) throw new Error('Target container is not a DOM element.');
        if (parentComponent == null || !has1(parentComponent)) throw new Error('parentComponent must be a valid React Component');
        return legacyRenderSubtreeIntoContainer(parentComponent, element, containerNode, false, callback);
    }
    function unmountComponentAtNode(container) {
        if (!isValidContainerLegacy(container)) throw new Error('unmountComponentAtNode(...): Target container is not a DOM element.');
        var isModernRoot = isContainerMarkedAsRoot(container) && container._reactRootContainer === undefined;
        if (isModernRoot) error1("You are calling ReactDOM.unmountComponentAtNode() on a container that was previously passed to ReactDOMClient.createRoot(). This is not supported. Did you mean to call root.unmount()?");
        if (container._reactRootContainer) {
            var rootEl = getReactRootElementInContainer(container);
            var renderedByDifferentReact = rootEl && !getInstanceFromNode(rootEl);
            if (renderedByDifferentReact) error1("unmountComponentAtNode(): The node you're attempting to unmount was rendered by another copy of React.");
            flushSync(function() {
                legacyRenderSubtreeIntoContainer(null, null, container, false, function() {
                    // $FlowFixMe This should probably use `delete container._reactRootContainer`
                    container._reactRootContainer = null;
                    unmarkContainerAsRoot(container);
                });
            }); // If you call unmountComponentAtNode twice in quick succession, you'll
            // get `true` twice. That's probably fine?
            return true;
        } else {
            var _rootEl = getReactRootElementInContainer(container);
            var hasNonRootReactChild = !!(_rootEl && getInstanceFromNode(_rootEl)); // Check if the container itself is a React root node.
            var isContainerReactRoot = container.nodeType === ELEMENT_NODE && isValidContainerLegacy(container.parentNode) && !!container.parentNode._reactRootContainer;
            if (hasNonRootReactChild) error1("unmountComponentAtNode(): The node you're attempting to unmount was rendered by React and is not a top-level container. %s", isContainerReactRoot ? "You may have accidentally passed in a React root node instead of its container." : "Instead, have the parent component update its state and rerender in order to remove this component.");
            return false;
        }
    }
    setAttemptSynchronousHydration(attemptSynchronousHydration$1);
    setAttemptContinuousHydration(attemptContinuousHydration$1);
    setAttemptHydrationAtCurrentPriority(attemptHydrationAtCurrentPriority$1);
    setGetCurrentUpdatePriority(getCurrentUpdatePriority);
    setAttemptHydrationAtPriority(runWithPriority);
    if (typeof Map !== 'function' || Map.prototype == null || typeof Map.prototype.forEach !== 'function' || typeof Set !== 'function' || Set.prototype == null || typeof Set.prototype.clear !== 'function' || typeof Set.prototype.forEach !== 'function') error1("React depends on Map and Set built-in types. Make sure that you load a polyfill in older browsers. https://reactjs.org/link/react-polyfills");
    setRestoreImplementation(restoreControlledState$3);
    setBatchingImplementation(batchedUpdates$1, discreteUpdates, flushSync);
    function createPortal$1(children, container) {
        var key = arguments.length > 2 && arguments[2] !== undefined ? arguments[2] : null;
        if (!isValidContainer(container)) throw new Error('Target container is not a DOM element.');
         // TODO: pass ReactDOM portal implementation as third argument
        // $FlowFixMe The Flow type is opaque but there's no way to actually create it.
        return createPortal(children, container, null, key);
    }
    function renderSubtreeIntoContainer(parentComponent, element, containerNode, callback) {
        return unstable_renderSubtreeIntoContainer(parentComponent, element, containerNode, callback);
    }
    var Internals = {
        usingClientEntryPoint: false,
        // Keep in sync with ReactTestUtils.js.
        // This is an array for better minification.
        Events: [
            getInstanceFromNode,
            getNodeFromInstance,
            getFiberCurrentPropsFromNode,
            enqueueStateRestore,
            restoreStateIfNeeded,
            batchedUpdates$1
        ]
    };
    function createRoot$1(container, options) {
        if (!Internals.usingClientEntryPoint && true) error1('You are importing createRoot from "react-dom" which is not supported. You should instead import it from "react-dom/client".');
        return createRoot(container, options);
    }
    function hydrateRoot$1(container, initialChildren, options) {
        if (!Internals.usingClientEntryPoint && true) error1('You are importing hydrateRoot from "react-dom" which is not supported. You should instead import it from "react-dom/client".');
        return hydrateRoot(container, initialChildren, options);
    } // Overload the definition to the two valid signatures.
    // Warning, this opts-out of checking the function body.
    // eslint-disable-next-line no-redeclare
    function flushSync$1(fn) {
        if (isAlreadyRendering()) error1("flushSync was called from inside a lifecycle method. React cannot flush when React is already rendering. Consider moving this call to a scheduler task or micro task.");
        return flushSync(fn);
    }
    var foundDevTools = injectIntoDevTools({
        findFiberByHostInstance: getClosestInstanceFromNode,
        bundleType: 1,
        version: ReactVersion,
        rendererPackageName: 'react-dom'
    });
    if (!foundDevTools && canUseDOM && window.top === window.self) // If we're in Chrome or Firefox, provide a download link if not installed.
    {
        if (navigator.userAgent.indexOf('Chrome') > -1 && navigator.userAgent.indexOf('Edge') === -1 || navigator.userAgent.indexOf('Firefox') > -1) {
            var protocol = window.location.protocol; // Don't warn in exotic cases like chrome-extension://.
            if (/^(https?|file):$/.test(protocol)) // eslint-disable-next-line react-internal/no-production-logging
            console.info("%cDownload the React DevTools for a better development experience: https://reactjs.org/link/react-devtools" + (protocol === 'file:' ? "\nYou might need to use a local HTTP server (instead of file://): https://reactjs.org/link/react-devtools-faq" : ''), 'font-weight:bold');
        }
    }
    exports.__SECRET_INTERNALS_DO_NOT_USE_OR_YOU_WILL_BE_FIRED = Internals;
    exports.createPortal = createPortal$1;
    exports.createRoot = createRoot$1;
    exports.findDOMNode = findDOMNode;
    exports.flushSync = flushSync$1;
    exports.hydrate = hydrate1;
    exports.hydrateRoot = hydrateRoot$1;
    exports.render = render1;
    exports.unmountComponentAtNode = unmountComponentAtNode;
    exports.unstable_batchedUpdates = batchedUpdates$1;
    exports.unstable_renderSubtreeIntoContainer = renderSubtreeIntoContainer;
    exports.version = ReactVersion;
    /* global __REACT_DEVTOOLS_GLOBAL_HOOK__ */ if (typeof __REACT_DEVTOOLS_GLOBAL_HOOK__ !== 'undefined' && typeof __REACT_DEVTOOLS_GLOBAL_HOOK__.registerInternalModuleStop === 'function') __REACT_DEVTOOLS_GLOBAL_HOOK__.registerInternalModuleStop(new Error());
})();

},{"react":"21dqq","scheduler":"juvHo"}],"juvHo":[function(require,module,exports) {
'use strict';
module.exports = require('./cjs/scheduler.development.js');

},{"./cjs/scheduler.development.js":"RqdIf"}],"RqdIf":[function(require,module,exports) {
/**
 * @license React
 * scheduler.development.js
 *
 * Copyright (c) Facebook, Inc. and its affiliates.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE file in the root directory of this source tree.
 */ 'use strict';
(function() {
    /* global __REACT_DEVTOOLS_GLOBAL_HOOK__ */ if (typeof __REACT_DEVTOOLS_GLOBAL_HOOK__ !== 'undefined' && typeof __REACT_DEVTOOLS_GLOBAL_HOOK__.registerInternalModuleStart === 'function') __REACT_DEVTOOLS_GLOBAL_HOOK__.registerInternalModuleStart(new Error());
    var enableSchedulerDebugging = false;
    var enableProfiling = false;
    var frameYieldMs = 5;
    function push(heap, node) {
        var index = heap.length;
        heap.push(node);
        siftUp(heap, node, index);
    }
    function peek(heap) {
        return heap.length === 0 ? null : heap[0];
    }
    function pop(heap) {
        if (heap.length === 0) return null;
        var first = heap[0];
        var last = heap.pop();
        if (last !== first) {
            heap[0] = last;
            siftDown(heap, last, 0);
        }
        return first;
    }
    function siftUp(heap, node, i) {
        var index = i;
        while(index > 0){
            var parentIndex = index - 1 >>> 1;
            var parent = heap[parentIndex];
            if (compare(parent, node) > 0) {
                // The parent is larger. Swap positions.
                heap[parentIndex] = node;
                heap[index] = parent;
                index = parentIndex;
            } else // The parent is smaller. Exit.
            return;
        }
    }
    function siftDown(heap, node, i) {
        var index = i;
        var length = heap.length;
        var halfLength = length >>> 1;
        while(index < halfLength){
            var leftIndex = (index + 1) * 2 - 1;
            var left = heap[leftIndex];
            var rightIndex = leftIndex + 1;
            var right = heap[rightIndex]; // If the left or right node is smaller, swap with the smaller of those.
            if (compare(left, node) < 0) {
                if (rightIndex < length && compare(right, left) < 0) {
                    heap[index] = right;
                    heap[rightIndex] = node;
                    index = rightIndex;
                } else {
                    heap[index] = left;
                    heap[leftIndex] = node;
                    index = leftIndex;
                }
            } else if (rightIndex < length && compare(right, node) < 0) {
                heap[index] = right;
                heap[rightIndex] = node;
                index = rightIndex;
            } else // Neither child is smaller. Exit.
            return;
        }
    }
    function compare(a, b) {
        // Compare sort index first, then task id.
        var diff = a.sortIndex - b.sortIndex;
        return diff !== 0 ? diff : a.id - b.id;
    }
    // TODO: Use symbols?
    var ImmediatePriority = 1;
    var UserBlockingPriority = 2;
    var NormalPriority = 3;
    var LowPriority = 4;
    var IdlePriority = 5;
    function markTaskErrored(task, ms) {}
    /* eslint-disable no-var */ var hasPerformanceNow = typeof performance === 'object' && typeof performance.now === 'function';
    if (hasPerformanceNow) {
        var localPerformance = performance;
        exports.unstable_now = function() {
            return localPerformance.now();
        };
    } else {
        var localDate = Date;
        var initialTime = localDate.now();
        exports.unstable_now = function() {
            return localDate.now() - initialTime;
        };
    } // Max 31 bit integer. The max integer size in V8 for 32-bit systems.
    // Math.pow(2, 30) - 1
    // 0b111111111111111111111111111111
    var maxSigned31BitInt = 1073741823; // Times out immediately
    var IMMEDIATE_PRIORITY_TIMEOUT = -1; // Eventually times out
    var USER_BLOCKING_PRIORITY_TIMEOUT = 250;
    var NORMAL_PRIORITY_TIMEOUT = 5000;
    var LOW_PRIORITY_TIMEOUT = 10000; // Never times out
    var IDLE_PRIORITY_TIMEOUT = maxSigned31BitInt; // Tasks are stored on a min heap
    var taskQueue = [];
    var timerQueue = []; // Incrementing id counter. Used to maintain insertion order.
    var taskIdCounter = 1; // Pausing the scheduler is useful for debugging.
    var currentTask = null;
    var currentPriorityLevel = NormalPriority; // This is set while performing work, to prevent re-entrance.
    var isPerformingWork = false;
    var isHostCallbackScheduled = false;
    var isHostTimeoutScheduled = false; // Capture local references to native APIs, in case a polyfill overrides them.
    var localSetTimeout = typeof setTimeout === 'function' ? setTimeout : null;
    var localClearTimeout = typeof clearTimeout === 'function' ? clearTimeout : null;
    var localSetImmediate = typeof setImmediate !== 'undefined' ? setImmediate : null; // IE and Node.js + jsdom
    var isInputPending = typeof navigator !== 'undefined' && navigator.scheduling !== undefined && navigator.scheduling.isInputPending !== undefined ? navigator.scheduling.isInputPending.bind(navigator.scheduling) : null;
    function advanceTimers(currentTime) {
        // Check for tasks that are no longer delayed and add them to the queue.
        var timer = peek(timerQueue);
        while(timer !== null){
            if (timer.callback === null) // Timer was cancelled.
            pop(timerQueue);
            else if (timer.startTime <= currentTime) {
                // Timer fired. Transfer to the task queue.
                pop(timerQueue);
                timer.sortIndex = timer.expirationTime;
                push(taskQueue, timer);
            } else // Remaining timers are pending.
            return;
            timer = peek(timerQueue);
        }
    }
    function handleTimeout(currentTime) {
        isHostTimeoutScheduled = false;
        advanceTimers(currentTime);
        if (!isHostCallbackScheduled) {
            if (peek(taskQueue) !== null) {
                isHostCallbackScheduled = true;
                requestHostCallback(flushWork);
            } else {
                var firstTimer = peek(timerQueue);
                if (firstTimer !== null) requestHostTimeout(handleTimeout, firstTimer.startTime - currentTime);
            }
        }
    }
    function flushWork(hasTimeRemaining, initialTime) {
        isHostCallbackScheduled = false;
        if (isHostTimeoutScheduled) {
            // We scheduled a timeout but it's no longer needed. Cancel it.
            isHostTimeoutScheduled = false;
            cancelHostTimeout();
        }
        isPerformingWork = true;
        var previousPriorityLevel = currentPriorityLevel;
        try {
            if (enableProfiling) try {
                return workLoop(hasTimeRemaining, initialTime);
            } catch (error) {
                if (currentTask !== null) {
                    var currentTime = exports.unstable_now();
                    markTaskErrored(currentTask, currentTime);
                    currentTask.isQueued = false;
                }
                throw error;
            }
            else // No catch in prod code path.
            return workLoop(hasTimeRemaining, initialTime);
        } finally{
            currentTask = null;
            currentPriorityLevel = previousPriorityLevel;
            isPerformingWork = false;
        }
    }
    function workLoop(hasTimeRemaining, initialTime) {
        var currentTime = initialTime;
        advanceTimers(currentTime);
        currentTask = peek(taskQueue);
        while(currentTask !== null && !enableSchedulerDebugging){
            if (currentTask.expirationTime > currentTime && (!hasTimeRemaining || shouldYieldToHost())) break;
            var callback = currentTask.callback;
            if (typeof callback === 'function') {
                currentTask.callback = null;
                currentPriorityLevel = currentTask.priorityLevel;
                var didUserCallbackTimeout = currentTask.expirationTime <= currentTime;
                var continuationCallback = callback(didUserCallbackTimeout);
                currentTime = exports.unstable_now();
                if (typeof continuationCallback === 'function') currentTask.callback = continuationCallback;
                else if (currentTask === peek(taskQueue)) pop(taskQueue);
                advanceTimers(currentTime);
            } else pop(taskQueue);
            currentTask = peek(taskQueue);
        } // Return whether there's additional work
        if (currentTask !== null) return true;
        else {
            var firstTimer = peek(timerQueue);
            if (firstTimer !== null) requestHostTimeout(handleTimeout, firstTimer.startTime - currentTime);
            return false;
        }
    }
    function unstable_runWithPriority(priorityLevel, eventHandler) {
        switch(priorityLevel){
            case ImmediatePriority:
            case UserBlockingPriority:
            case NormalPriority:
            case LowPriority:
            case IdlePriority:
                break;
            default:
                priorityLevel = NormalPriority;
        }
        var previousPriorityLevel = currentPriorityLevel;
        currentPriorityLevel = priorityLevel;
        try {
            return eventHandler();
        } finally{
            currentPriorityLevel = previousPriorityLevel;
        }
    }
    function unstable_next(eventHandler) {
        var priorityLevel;
        switch(currentPriorityLevel){
            case ImmediatePriority:
            case UserBlockingPriority:
            case NormalPriority:
                // Shift down to normal priority
                priorityLevel = NormalPriority;
                break;
            default:
                // Anything lower than normal priority should remain at the current level.
                priorityLevel = currentPriorityLevel;
                break;
        }
        var previousPriorityLevel = currentPriorityLevel;
        currentPriorityLevel = priorityLevel;
        try {
            return eventHandler();
        } finally{
            currentPriorityLevel = previousPriorityLevel;
        }
    }
    function unstable_wrapCallback(callback) {
        var parentPriorityLevel = currentPriorityLevel;
        return function() {
            // This is a fork of runWithPriority, inlined for performance.
            var previousPriorityLevel = currentPriorityLevel;
            currentPriorityLevel = parentPriorityLevel;
            try {
                return callback.apply(this, arguments);
            } finally{
                currentPriorityLevel = previousPriorityLevel;
            }
        };
    }
    function unstable_scheduleCallback(priorityLevel, callback, options) {
        var currentTime = exports.unstable_now();
        var startTime;
        if (typeof options === 'object' && options !== null) {
            var delay = options.delay;
            if (typeof delay === 'number' && delay > 0) startTime = currentTime + delay;
            else startTime = currentTime;
        } else startTime = currentTime;
        var timeout;
        switch(priorityLevel){
            case ImmediatePriority:
                timeout = IMMEDIATE_PRIORITY_TIMEOUT;
                break;
            case UserBlockingPriority:
                timeout = USER_BLOCKING_PRIORITY_TIMEOUT;
                break;
            case IdlePriority:
                timeout = IDLE_PRIORITY_TIMEOUT;
                break;
            case LowPriority:
                timeout = LOW_PRIORITY_TIMEOUT;
                break;
            case NormalPriority:
            default:
                timeout = NORMAL_PRIORITY_TIMEOUT;
                break;
        }
        var expirationTime = startTime + timeout;
        var newTask = {
            id: taskIdCounter++,
            callback: callback,
            priorityLevel: priorityLevel,
            startTime: startTime,
            expirationTime: expirationTime,
            sortIndex: -1
        };
        if (startTime > currentTime) {
            // This is a delayed task.
            newTask.sortIndex = startTime;
            push(timerQueue, newTask);
            if (peek(taskQueue) === null && newTask === peek(timerQueue)) {
                // All tasks are delayed, and this is the task with the earliest delay.
                if (isHostTimeoutScheduled) // Cancel an existing timeout.
                cancelHostTimeout();
                else isHostTimeoutScheduled = true;
                 // Schedule a timeout.
                requestHostTimeout(handleTimeout, startTime - currentTime);
            }
        } else {
            newTask.sortIndex = expirationTime;
            push(taskQueue, newTask);
            // wait until the next time we yield.
            if (!isHostCallbackScheduled && !isPerformingWork) {
                isHostCallbackScheduled = true;
                requestHostCallback(flushWork);
            }
        }
        return newTask;
    }
    function unstable_pauseExecution() {}
    function unstable_continueExecution() {
        if (!isHostCallbackScheduled && !isPerformingWork) {
            isHostCallbackScheduled = true;
            requestHostCallback(flushWork);
        }
    }
    function unstable_getFirstCallbackNode() {
        return peek(taskQueue);
    }
    function unstable_cancelCallback(task) {
        // remove from the queue because you can't remove arbitrary nodes from an
        // array based heap, only the first one.)
        task.callback = null;
    }
    function unstable_getCurrentPriorityLevel() {
        return currentPriorityLevel;
    }
    var isMessageLoopRunning = false;
    var scheduledHostCallback = null;
    var taskTimeoutID = -1; // Scheduler periodically yields in case there is other work on the main
    // thread, like user events. By default, it yields multiple times per frame.
    // It does not attempt to align with frame boundaries, since most tasks don't
    // need to be frame aligned; for those that do, use requestAnimationFrame.
    var frameInterval = frameYieldMs;
    var startTime1 = -1;
    function shouldYieldToHost() {
        var timeElapsed = exports.unstable_now() - startTime1;
        if (timeElapsed < frameInterval) // The main thread has only been blocked for a really short amount of time;
        // smaller than a single frame. Don't yield yet.
        return false;
         // The main thread has been blocked for a non-negligible amount of time. We
        return true;
    }
    function requestPaint() {}
    function forceFrameRate(fps) {
        if (fps < 0 || fps > 125) {
            // Using console['error'] to evade Babel and ESLint
            console['error']("forceFrameRate takes a positive int between 0 and 125, forcing frame rates higher than 125 fps is not supported");
            return;
        }
        if (fps > 0) frameInterval = Math.floor(1000 / fps);
        else // reset the framerate
        frameInterval = frameYieldMs;
    }
    var performWorkUntilDeadline = function() {
        if (scheduledHostCallback !== null) {
            var currentTime = exports.unstable_now(); // Keep track of the start time so we can measure how long the main thread
            // has been blocked.
            startTime1 = currentTime;
            var hasTimeRemaining = true; // If a scheduler task throws, exit the current browser task so the
            // error can be observed.
            //
            // Intentionally not using a try-catch, since that makes some debugging
            // techniques harder. Instead, if `scheduledHostCallback` errors, then
            // `hasMoreWork` will remain true, and we'll continue the work loop.
            var hasMoreWork = true;
            try {
                hasMoreWork = scheduledHostCallback(hasTimeRemaining, currentTime);
            } finally{
                if (hasMoreWork) // If there's more work, schedule the next message event at the end
                // of the preceding one.
                schedulePerformWorkUntilDeadline();
                else {
                    isMessageLoopRunning = false;
                    scheduledHostCallback = null;
                }
            }
        } else isMessageLoopRunning = false;
         // Yielding to the browser will give it a chance to paint, so we can
    };
    var schedulePerformWorkUntilDeadline;
    if (typeof localSetImmediate === 'function') // Node.js and old IE.
    // There's a few reasons for why we prefer setImmediate.
    //
    // Unlike MessageChannel, it doesn't prevent a Node.js process from exiting.
    // (Even though this is a DOM fork of the Scheduler, you could get here
    // with a mix of Node.js 15+, which has a MessageChannel, and jsdom.)
    // https://github.com/facebook/react/issues/20756
    //
    // But also, it runs earlier which is the semantic we want.
    // If other browsers ever implement it, it's better to use it.
    // Although both of these would be inferior to native scheduling.
    schedulePerformWorkUntilDeadline = function() {
        localSetImmediate(performWorkUntilDeadline);
    };
    else if (typeof MessageChannel !== 'undefined') {
        // DOM and Worker environments.
        // We prefer MessageChannel because of the 4ms setTimeout clamping.
        var channel = new MessageChannel();
        var port = channel.port2;
        channel.port1.onmessage = performWorkUntilDeadline;
        schedulePerformWorkUntilDeadline = function() {
            port.postMessage(null);
        };
    } else // We should only fallback here in non-browser environments.
    schedulePerformWorkUntilDeadline = function() {
        localSetTimeout(performWorkUntilDeadline, 0);
    };
    function requestHostCallback(callback) {
        scheduledHostCallback = callback;
        if (!isMessageLoopRunning) {
            isMessageLoopRunning = true;
            schedulePerformWorkUntilDeadline();
        }
    }
    function requestHostTimeout(callback, ms) {
        taskTimeoutID = localSetTimeout(function() {
            callback(exports.unstable_now());
        }, ms);
    }
    function cancelHostTimeout() {
        localClearTimeout(taskTimeoutID);
        taskTimeoutID = -1;
    }
    var unstable_requestPaint = requestPaint;
    var unstable_Profiling = null;
    exports.unstable_IdlePriority = IdlePriority;
    exports.unstable_ImmediatePriority = ImmediatePriority;
    exports.unstable_LowPriority = LowPriority;
    exports.unstable_NormalPriority = NormalPriority;
    exports.unstable_Profiling = unstable_Profiling;
    exports.unstable_UserBlockingPriority = UserBlockingPriority;
    exports.unstable_cancelCallback = unstable_cancelCallback;
    exports.unstable_continueExecution = unstable_continueExecution;
    exports.unstable_forceFrameRate = forceFrameRate;
    exports.unstable_getCurrentPriorityLevel = unstable_getCurrentPriorityLevel;
    exports.unstable_getFirstCallbackNode = unstable_getFirstCallbackNode;
    exports.unstable_next = unstable_next;
    exports.unstable_pauseExecution = unstable_pauseExecution;
    exports.unstable_requestPaint = unstable_requestPaint;
    exports.unstable_runWithPriority = unstable_runWithPriority;
    exports.unstable_scheduleCallback = unstable_scheduleCallback;
    exports.unstable_shouldYield = shouldYieldToHost;
    exports.unstable_wrapCallback = unstable_wrapCallback;
    /* global __REACT_DEVTOOLS_GLOBAL_HOOK__ */ if (typeof __REACT_DEVTOOLS_GLOBAL_HOOK__ !== 'undefined' && typeof __REACT_DEVTOOLS_GLOBAL_HOOK__.registerInternalModuleStop === 'function') __REACT_DEVTOOLS_GLOBAL_HOOK__.registerInternalModuleStop(new Error());
})();

},{}],"2kQhy":[function(require,module,exports) {
var $parcel$ReactRefreshHelpers$f00f = require("@parcel/transformer-react-refresh-wrap/lib/helpers/helpers.js");
var prevRefreshReg = window.$RefreshReg$;
var prevRefreshSig = window.$RefreshSig$;
$parcel$ReactRefreshHelpers$f00f.prelude(module);

try {
var parcelHelpers = require("@parcel/transformer-js/src/esmodule-helpers.js");
parcelHelpers.defineInteropFlag(exports);
var _jsxDevRuntime = require("react/jsx-dev-runtime");
var _react = require("react");
var _reactDefault = parcelHelpers.interopDefault(_react);
var _reactRouterDom = require("react-router-dom");
//user
var _createUser = require("./components/IT20125202/user/CreateUser");
var _createUserDefault = parcelHelpers.interopDefault(_createUser);
var _userProfile = require("./components/IT20125202/user/UserProfile");
var _userProfileDefault = parcelHelpers.interopDefault(_userProfile);
var _home = require("./components/IT20127046/Home");
var _homeDefault = parcelHelpers.interopDefault(_home);
var _userLogin = require("./components/IT20125202/user/UserLogin");
var _userLoginDefault = parcelHelpers.interopDefault(_userLogin);
var _navBar = require("./components/IT20125202/NavBar");
var _navBarDefault = parcelHelpers.interopDefault(_navBar);
var _landingPage = require("./components/IT20125202/LandingPage");
var _landingPageDefault = parcelHelpers.interopDefault(_landingPage);
//student
var _registerTopic = require("./components/IT20125202/topic/RegisterTopic");
var _registerTopicDefault = parcelHelpers.interopDefault(_registerTopic);
var _viewSubmissions = require("./components/IT20125202/topic/ViewSubmissions");
var _viewSubmissionsDefault = parcelHelpers.interopDefault(_viewSubmissions);
var _groupChatStudent = require("./components/IT20127046/user/GroupChat_Student");
var _groupChatStudentDefault = parcelHelpers.interopDefault(_groupChatStudent);
var _sviewEvaluation = require("./components/IT20128036/user/SviewEvaluation");
var _sviewEvaluationDefault = parcelHelpers.interopDefault(_sviewEvaluation);
var _sviewSubmitionType = require("./components/IT20128036/user/SviewSubmitionType");
var _sviewSubmitionTypeDefault = parcelHelpers.interopDefault(_sviewSubmitionType);
var _submitions = require("./components/IT20128036/user/Submitions");
var _submitionsDefault = parcelHelpers.interopDefault(_submitions);
var _viewSubmitionsp = require("./components/IT20128036/user/viewSubmitionsp");
var _viewSubmitionspDefault = parcelHelpers.interopDefault(_viewSubmitionsp);
var _sviewMarks = require("./components/IT20128036/user/sviewMarks");
var _sviewMarksDefault = parcelHelpers.interopDefault(_sviewMarks);
var _editSubmition = require("./components/IT20128036/user/editSubmition");
var _editSubmitionDefault = parcelHelpers.interopDefault(_editSubmition);
var _exportPDF = require("./components/IT20128036/user/exportPDF");
var _studentViewSpData = require("./components/IT20128036/user/StudentViewSpData");
var _studentViewSpDataDefault = parcelHelpers.interopDefault(_studentViewSpData);
var _createStudentGroup = require("../src/components/IT20131456/user/CreateStudentGroup");
var _createStudentGroupDefault = parcelHelpers.interopDefault(_createStudentGroup);
var _viewStudentGroup = require("../src/components/IT20131456/user/ViewStudentGroup");
var _viewStudentGroupDefault = parcelHelpers.interopDefault(_viewStudentGroup);
var _requestSupervisor = require("./components/IT20131456/user/RequestSupervisor");
var _requestSupervisorDefault = parcelHelpers.interopDefault(_requestSupervisor);
var _createRequest = require("./components/IT20131456/user/CreateRequest");
var _createRequestDefault = parcelHelpers.interopDefault(_createRequest);
var _requestSummary = require("./components/IT20131456/user/RequestSummary");
var _requestSummaryDefault = parcelHelpers.interopDefault(_requestSummary);
//admin
var _editUser = require("./components/IT20125202/user/EditUser");
var _editUserDefault = parcelHelpers.interopDefault(_editUser);
var _userDetails = require("./components/IT20125202/user/UserDetails");
var _userDetailsDefault = parcelHelpers.interopDefault(_userDetails);
var _userRoles = require("./components/IT20125202/user/UserRoles");
var _userRolesDefault = parcelHelpers.interopDefault(_userRoles);
var _adminHome = require("./components/IT20125202/admin/AdminHome");
var _adminHomeDefault = parcelHelpers.interopDefault(_adminHome);
var _viewListAdmin = require("./components/IT20125202/topic/ViewListAdmin");
var _viewListAdminDefault = parcelHelpers.interopDefault(_viewListAdmin);
var _adminLogin = require("./components/IT20125202/admin/AdminLogin");
var _adminLoginDefault = parcelHelpers.interopDefault(_adminLogin);
var _documentTempAdmin = require("./components/IT20127046/admin/DocumentTemp_Admin");
var _documentTempAdminDefault = parcelHelpers.interopDefault(_documentTempAdmin);
var _documentTempCreate = require("./components/IT20127046/admin/DocumentTempCreate");
var _documentTempCreateDefault = parcelHelpers.interopDefault(_documentTempCreate);
var _documentTempUpdate = require("./components/IT20127046/admin/DocumentTempUpdate");
var _documentTempUpdateDefault = parcelHelpers.interopDefault(_documentTempUpdate);
var _userRolesPanel = require("./components/IT20125202/user/UserRoles_Panel");
var _userRolesPanelDefault = parcelHelpers.interopDefault(_userRolesPanel);
var _userRolesStudents = require("./components/IT20125202/user/UserRoles_Students");
var _userRolesStudentsDefault = parcelHelpers.interopDefault(_userRolesStudents);
var _userRolesSupervisor = require("./components/IT20125202/user/UserRoles_Supervisor");
var _userRolesSupervisorDefault = parcelHelpers.interopDefault(_userRolesSupervisor);
var _addSubmitionType = require("./components/IT20128036/admin/addSubmitionType");
var _addSubmitionTypeDefault = parcelHelpers.interopDefault(_addSubmitionType);
var _submitionTypeList = require("./components/IT20128036/admin/SubmitionTypeList");
var _submitionTypeListDefault = parcelHelpers.interopDefault(_submitionTypeList);
var _editSubmitionType = require("./components/IT20128036/admin/editSubmitionType");
var _editSubmitionTypeDefault = parcelHelpers.interopDefault(_editSubmitionType);
var _studentGroup = require("../src/components/IT20131456/admin/StudentGroup");
var _studentGroupDefault = parcelHelpers.interopDefault(_studentGroup);
var _viewStudentGroupAdmin = require("../src/components/IT20131456/admin/ViewStudentGroupAdmin");
var _viewStudentGroupAdminDefault = parcelHelpers.interopDefault(_viewStudentGroupAdmin);
var _editStudentGroup = require("./components/IT20131456/admin/EditStudentGroup");
var _editStudentGroupDefault = parcelHelpers.interopDefault(_editStudentGroup);
var _createSupervisorDetails = require("./components/IT20131456/admin/CreateSupervisorDetails");
var _createSupervisorDetailsDefault = parcelHelpers.interopDefault(_createSupervisorDetails);
var _editSupervisorDetails = require("./components/IT20131456/admin/EditSupervisorDetails");
var _editSupervisorDetailsDefault = parcelHelpers.interopDefault(_editSupervisorDetails);
var _supervisorDetails = require("./components/IT20131456/admin/SupervisorDetails");
var _supervisorDetailsDefault = parcelHelpers.interopDefault(_supervisorDetails);
//panel
var _viewTopicList = require("./components/IT20125202/topic/ViewTopicList");
var _viewTopicListDefault = parcelHelpers.interopDefault(_viewTopicList);
var _viewTopic = require("./components/IT20125202/topic/ViewTopic");
var _viewTopicDefault = parcelHelpers.interopDefault(_viewTopic);
var _updateTopic = require("./components/IT20125202/topic/UpdateTopic");
var _updateTopicDefault = parcelHelpers.interopDefault(_updateTopic);
//import AddEvaluation from "./components/IT20128036/AddEvaluation";
var _addEvaluation = require("./components/IT20128036/admin/AddEvaluation");
var _addEvaluationDefault = parcelHelpers.interopDefault(_addEvaluation);
var _evaluationList = require("./components/IT20128036/admin/EvaluationList");
var _evaluationListDefault = parcelHelpers.interopDefault(_evaluationList);
var _editEvaluation = require("./components/IT20128036/admin/editEvaluation");
var _editEvaluationDefault = parcelHelpers.interopDefault(_editEvaluation);
var _groupChatAdmin = require("./components/IT20127046/user/GroupChat_Admin");
var _groupChatAdminDefault = parcelHelpers.interopDefault(_groupChatAdmin);
//supervisor
var _coverJpg = require("./images/cover.jpg");
var _coverJpgDefault = parcelHelpers.interopDefault(_coverJpg);
var _createMarkingSchem = require("./components/IT20127046/admin/CreateMarkingSchem");
var _createMarkingSchemDefault = parcelHelpers.interopDefault(_createMarkingSchem);
var _viewSubmitions = require("./components/IT20128036/admin/viewSubmitions");
var _viewSubmitionsDefault = parcelHelpers.interopDefault(_viewSubmitions);
var _addMarks = require("./components/IT20128036/admin/addMarks");
var _addMarksDefault = parcelHelpers.interopDefault(_addMarks);
var _viewMarks = require("./components/IT20128036/admin/viewMarks");
var _viewMarksDefault = parcelHelpers.interopDefault(_viewMarks);
var _editMarks = require("./components/IT20128036/admin/editMarks");
var _editMarksDefault = parcelHelpers.interopDefault(_editMarks);
var _viewMarkingSchemAdmin = require("./components/IT20127046/admin/ViewMarkingSchem_Admin");
var _viewMarkingSchemAdminDefault = parcelHelpers.interopDefault(_viewMarkingSchemAdmin);
var _viewMarkingScheme = require("./components/IT20127046/user/ViewMarkingScheme");
var _viewMarkingSchemeDefault = parcelHelpers.interopDefault(_viewMarkingScheme);
var _viewDocumentTemplate = require("./components/IT20127046/user/ViewDocumentTemplate");
var _viewDocumentTemplateDefault = parcelHelpers.interopDefault(_viewDocumentTemplate);
var _displayMarkingScheme = require("./components/IT20127046/user/DisplayMarkingScheme");
var _displayMarkingSchemeDefault = parcelHelpers.interopDefault(_displayMarkingScheme);
var _contactUs = require("./components/IT20127046/user/ContactUs");
var _contactUsDefault = parcelHelpers.interopDefault(_contactUs);
var _aboutUs = require("./components/IT20127046/user/AboutUs");
var _aboutUsDefault = parcelHelpers.interopDefault(_aboutUs);
var _createResponse = require("./components/IT20131456/user/CreateResponse");
var _createResponseDefault = parcelHelpers.interopDefault(_createResponse);
class App extends _react.Component {
    render() {
        return /*#__PURE__*/ _jsxDevRuntime.jsxDEV(_reactRouterDom.BrowserRouter, {
            children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV(_reactRouterDom.Switch, {
                    children: [
                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV(_reactRouterDom.Route, {
                            path: "/admin/home",
                            exact: true,
                            component: _adminHomeDefault.default
                        }, void 0, false, {
                            fileName: "src/App.js",
                            lineNumber: 102,
                            columnNumber: 13
                        }, this),
                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV(_reactRouterDom.Route, {
                            path: "/admin/edituser/:id",
                            exact: true,
                            component: _editUserDefault.default
                        }, void 0, false, {
                            fileName: "src/App.js",
                            lineNumber: 103,
                            columnNumber: 13
                        }, this),
                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV(_reactRouterDom.Route, {
                            path: "/admin/user/:id",
                            exact: true,
                            component: _userDetailsDefault.default
                        }, void 0, false, {
                            fileName: "src/App.js",
                            lineNumber: 104,
                            columnNumber: 13
                        }, this),
                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV(_reactRouterDom.Route, {
                            path: "/admin/users",
                            exact: true,
                            component: _userRolesDefault.default
                        }, void 0, false, {
                            fileName: "src/App.js",
                            lineNumber: 105,
                            columnNumber: 13
                        }, this),
                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV(_reactRouterDom.Route, {
                            path: "/admin/topiclist",
                            exact: true,
                            component: _viewListAdminDefault.default
                        }, void 0, false, {
                            fileName: "src/App.js",
                            lineNumber: 106,
                            columnNumber: 13
                        }, this),
                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV(_reactRouterDom.Route, {
                            path: "/admin/login",
                            exact: true,
                            component: _adminLoginDefault.default
                        }, void 0, false, {
                            fileName: "src/App.js",
                            lineNumber: 107,
                            columnNumber: 13
                        }, this),
                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV(_reactRouterDom.Route, {
                            path: "/admin/panelmembers",
                            exact: true,
                            component: _userRolesPanelDefault.default
                        }, void 0, false, {
                            fileName: "src/App.js",
                            lineNumber: 108,
                            columnNumber: 13
                        }, this),
                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV(_reactRouterDom.Route, {
                            path: "/admin/students",
                            exact: true,
                            component: _userRolesStudentsDefault.default
                        }, void 0, false, {
                            fileName: "src/App.js",
                            lineNumber: 109,
                            columnNumber: 13
                        }, this),
                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV(_reactRouterDom.Route, {
                            path: "/admin/supervisors",
                            exact: true,
                            component: _userRolesSupervisorDefault.default
                        }, void 0, false, {
                            fileName: "src/App.js",
                            lineNumber: 110,
                            columnNumber: 13
                        }, this),
                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV(_reactRouterDom.Route, {
                            path: "/add/marking",
                            exact: true,
                            component: _createMarkingSchemDefault.default
                        }, void 0, false, {
                            fileName: "src/App.js",
                            lineNumber: 112,
                            columnNumber: 13
                        }, this),
                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV(_reactRouterDom.Route, {
                            path: "/view/marking",
                            exact: true,
                            component: _viewMarkingSchemAdminDefault.default
                        }, void 0, false, {
                            fileName: "src/App.js",
                            lineNumber: 113,
                            columnNumber: 13
                        }, this),
                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV(_reactRouterDom.Route, {
                            path: "/documentTemp",
                            exact: true,
                            component: _documentTempAdminDefault.default
                        }, void 0, false, {
                            fileName: "src/App.js",
                            lineNumber: 114,
                            columnNumber: 13
                        }, this),
                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV(_reactRouterDom.Route, {
                            path: "/add/documentTemp",
                            exact: true,
                            component: _documentTempCreateDefault.default
                        }, void 0, false, {
                            fileName: "src/App.js",
                            lineNumber: 115,
                            columnNumber: 13
                        }, this),
                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV(_reactRouterDom.Route, {
                            path: "/edit/documentTemp/:id",
                            exact: true,
                            component: _documentTempUpdateDefault.default
                        }, void 0, false, {
                            fileName: "src/App.js",
                            lineNumber: 116,
                            columnNumber: 13
                        }, this),
                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV(_reactRouterDom.Route, {
                            path: "/student/groups/view",
                            exact: true,
                            component: _studentGroupDefault.default
                        }, void 0, false, {
                            fileName: "src/App.js",
                            lineNumber: 118,
                            columnNumber: 13
                        }, this),
                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV(_reactRouterDom.Route, {
                            path: "/student/group/view/admin/:id",
                            component: _viewStudentGroupAdminDefault.default
                        }, void 0, false, {
                            fileName: "src/App.js",
                            lineNumber: 119,
                            columnNumber: 13
                        }, this),
                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV(_reactRouterDom.Route, {
                            path: "/student/group/update/:id",
                            component: _editStudentGroupDefault.default
                        }, void 0, false, {
                            fileName: "src/App.js",
                            lineNumber: 120,
                            columnNumber: 13
                        }, this),
                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV(_reactRouterDom.Route, {
                            path: "/supervisor/add",
                            component: _createSupervisorDetailsDefault.default
                        }, void 0, false, {
                            fileName: "src/App.js",
                            lineNumber: 122,
                            columnNumber: 13
                        }, this),
                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV(_reactRouterDom.Route, {
                            path: "/supervisor/view",
                            component: _supervisorDetailsDefault.default
                        }, void 0, false, {
                            fileName: "src/App.js",
                            lineNumber: 123,
                            columnNumber: 13
                        }, this),
                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV(_reactRouterDom.Route, {
                            path: "/supervisor/update/:id",
                            component: _editSupervisorDetailsDefault.default
                        }, void 0, false, {
                            fileName: "src/App.js",
                            lineNumber: 124,
                            columnNumber: 13
                        }, this),
                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV(_reactRouterDom.Route, {
                            path: "/submitiontype/add",
                            component: _addSubmitionTypeDefault.default
                        }, void 0, false, {
                            fileName: "src/App.js",
                            lineNumber: 129,
                            columnNumber: 18
                        }, this),
                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV(_reactRouterDom.Route, {
                            path: "/submitiontypelist",
                            component: _submitionTypeListDefault.default
                        }, void 0, false, {
                            fileName: "src/App.js",
                            lineNumber: 130,
                            columnNumber: 17
                        }, this),
                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV(_reactRouterDom.Route, {
                            path: "/submitiontype/edit/:id",
                            component: _editSubmitionTypeDefault.default
                        }, void 0, false, {
                            fileName: "src/App.js",
                            lineNumber: 131,
                            columnNumber: 17
                        }, this),
                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                            className: "jumbotron",
                            style: {
                                paddingLeft: '50px',
                                paddingRight: '50px',
                                paddingBottom: '50px',
                                paddingTop: '10px',
                                backgroundImage: `url(${_coverJpgDefault.default})`,
                                backgroundRepeat: 'no-repeat',
                                backgroundSize: 'cover',
                                margin: '0px 0px 0px 0px',
                                overflowY: 'scroll',
                                height: '100vh'
                            },
                            children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                className: "container",
                                style: {
                                    background: 'white',
                                    minHeight: '100vh',
                                    padding: '0px 0px 10px 0px'
                                },
                                children: [
                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV(_navBarDefault.default, {}, void 0, false, {
                                        fileName: "src/App.js",
                                        lineNumber: 140,
                                        columnNumber: 17
                                    }, this),
                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV(_reactRouterDom.Route, {
                                        path: "/",
                                        exact: true,
                                        component: _landingPageDefault.default
                                    }, void 0, false, {
                                        fileName: "src/App.js",
                                        lineNumber: 143,
                                        columnNumber: 17
                                    }, this),
                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV(_reactRouterDom.Route, {
                                        path: "/user/login",
                                        exact: true,
                                        component: _userLoginDefault.default
                                    }, void 0, false, {
                                        fileName: "src/App.js",
                                        lineNumber: 144,
                                        columnNumber: 17
                                    }, this),
                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV(_reactRouterDom.Route, {
                                        path: "/user/profile",
                                        exact: true,
                                        component: _userProfileDefault.default
                                    }, void 0, false, {
                                        fileName: "src/App.js",
                                        lineNumber: 145,
                                        columnNumber: 17
                                    }, this),
                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV(_reactRouterDom.Route, {
                                        path: "/user/registration",
                                        exact: true,
                                        component: _createUserDefault.default
                                    }, void 0, false, {
                                        fileName: "src/App.js",
                                        lineNumber: 146,
                                        columnNumber: 17
                                    }, this),
                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV(_reactRouterDom.Route, {
                                        path: "/home",
                                        exact: true,
                                        component: _homeDefault.default
                                    }, void 0, false, {
                                        fileName: "src/App.js",
                                        lineNumber: 149,
                                        columnNumber: 17
                                    }, this),
                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV(_reactRouterDom.Route, {
                                        path: "/chatAppStudent",
                                        exact: true,
                                        component: _groupChatStudentDefault.default
                                    }, void 0, false, {
                                        fileName: "src/App.js",
                                        lineNumber: 151,
                                        columnNumber: 17
                                    }, this),
                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV(_reactRouterDom.Route, {
                                        path: "/chatAppAdmin",
                                        exact: true,
                                        component: _groupChatAdminDefault.default
                                    }, void 0, false, {
                                        fileName: "src/App.js",
                                        lineNumber: 152,
                                        columnNumber: 17
                                    }, this),
                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV(_reactRouterDom.Route, {
                                        path: "/user/view/marking",
                                        exact: true,
                                        component: _viewMarkingSchemeDefault.default
                                    }, void 0, false, {
                                        fileName: "src/App.js",
                                        lineNumber: 153,
                                        columnNumber: 17
                                    }, this),
                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV(_reactRouterDom.Route, {
                                        path: "/user/view/documentTemplate",
                                        exact: true,
                                        component: _viewDocumentTemplateDefault.default
                                    }, void 0, false, {
                                        fileName: "src/App.js",
                                        lineNumber: 154,
                                        columnNumber: 17
                                    }, this),
                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV(_reactRouterDom.Route, {
                                        path: "/user/display/marking/:id",
                                        exact: true,
                                        component: _displayMarkingSchemeDefault.default
                                    }, void 0, false, {
                                        fileName: "src/App.js",
                                        lineNumber: 155,
                                        columnNumber: 17
                                    }, this),
                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV(_reactRouterDom.Route, {
                                        path: "/user/contactus",
                                        exact: true,
                                        component: _contactUsDefault.default
                                    }, void 0, false, {
                                        fileName: "src/App.js",
                                        lineNumber: 156,
                                        columnNumber: 17
                                    }, this),
                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV(_reactRouterDom.Route, {
                                        path: "/user/aboutus",
                                        exact: true,
                                        component: _aboutUsDefault.default
                                    }, void 0, false, {
                                        fileName: "src/App.js",
                                        lineNumber: 157,
                                        columnNumber: 17
                                    }, this),
                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV(_reactRouterDom.Route, {
                                        path: "/student/topic/registration",
                                        exact: true,
                                        component: _registerTopicDefault.default
                                    }, void 0, false, {
                                        fileName: "src/App.js",
                                        lineNumber: 162,
                                        columnNumber: 17
                                    }, this),
                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV(_reactRouterDom.Route, {
                                        path: "/student/topics",
                                        exact: true,
                                        component: _viewSubmissionsDefault.default
                                    }, void 0, false, {
                                        fileName: "src/App.js",
                                        lineNumber: 163,
                                        columnNumber: 17
                                    }, this),
                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV(_reactRouterDom.Route, {
                                        path: "/student/evaluation/view",
                                        component: _sviewEvaluationDefault.default
                                    }, void 0, false, {
                                        fileName: "src/App.js",
                                        lineNumber: 169,
                                        columnNumber: 17
                                    }, this),
                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV(_reactRouterDom.Route, {
                                        path: "/student/submitiontype/view",
                                        component: _sviewSubmitionTypeDefault.default
                                    }, void 0, false, {
                                        fileName: "src/App.js",
                                        lineNumber: 170,
                                        columnNumber: 17
                                    }, this),
                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV(_reactRouterDom.Route, {
                                        path: "/student/submition/add",
                                        component: _submitionsDefault.default
                                    }, void 0, false, {
                                        fileName: "src/App.js",
                                        lineNumber: 171,
                                        columnNumber: 17
                                    }, this),
                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV(_reactRouterDom.Route, {
                                        path: "/student/submitionsp/view",
                                        component: _viewSubmitionspDefault.default
                                    }, void 0, false, {
                                        fileName: "src/App.js",
                                        lineNumber: 172,
                                        columnNumber: 17
                                    }, this),
                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV(_reactRouterDom.Route, {
                                        path: "/student/marks/view",
                                        component: _sviewMarksDefault.default
                                    }, void 0, false, {
                                        fileName: "src/App.js",
                                        lineNumber: 173,
                                        columnNumber: 17
                                    }, this),
                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV(_reactRouterDom.Route, {
                                        path: "/student/submition/edit/:id",
                                        component: _editSubmitionDefault.default
                                    }, void 0, false, {
                                        fileName: "src/App.js",
                                        lineNumber: 174,
                                        columnNumber: 17
                                    }, this),
                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV(_reactRouterDom.Route, {
                                        path: "/student/pdf/export",
                                        component: _exportPDF.exportPDF
                                    }, void 0, false, {
                                        fileName: "src/App.js",
                                        lineNumber: 175,
                                        columnNumber: 17
                                    }, this),
                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV(_reactRouterDom.Route, {
                                        path: "/student/group/evaluation/view",
                                        component: _studentViewSpDataDefault.default
                                    }, void 0, false, {
                                        fileName: "src/App.js",
                                        lineNumber: 176,
                                        columnNumber: 17
                                    }, this),
                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV(_reactRouterDom.Route, {
                                        path: "/student/group/add",
                                        component: _createStudentGroupDefault.default
                                    }, void 0, false, {
                                        fileName: "src/App.js",
                                        lineNumber: 179,
                                        columnNumber: 17
                                    }, this),
                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV(_reactRouterDom.Route, {
                                        path: "/student/group/view",
                                        component: _viewStudentGroupDefault.default
                                    }, void 0, false, {
                                        fileName: "src/App.js",
                                        lineNumber: 180,
                                        columnNumber: 17
                                    }, this),
                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV(_reactRouterDom.Route, {
                                        path: "/supervisor/request",
                                        component: _requestSupervisorDefault.default
                                    }, void 0, false, {
                                        fileName: "src/App.js",
                                        lineNumber: 181,
                                        columnNumber: 17
                                    }, this),
                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV(_reactRouterDom.Route, {
                                        path: "/create/request",
                                        component: _createRequestDefault.default
                                    }, void 0, false, {
                                        fileName: "src/App.js",
                                        lineNumber: 182,
                                        columnNumber: 17
                                    }, this),
                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV(_reactRouterDom.Route, {
                                        path: "/request/summary",
                                        component: _requestSummaryDefault.default
                                    }, void 0, false, {
                                        fileName: "src/App.js",
                                        lineNumber: 183,
                                        columnNumber: 17
                                    }, this),
                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV(_reactRouterDom.Route, {
                                        path: "/submitions/view",
                                        component: _viewSubmitionsDefault.default
                                    }, void 0, false, {
                                        fileName: "src/App.js",
                                        lineNumber: 188,
                                        columnNumber: 17
                                    }, this),
                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV(_reactRouterDom.Route, {
                                        path: "/marks/add/:id",
                                        component: _addMarksDefault.default
                                    }, void 0, false, {
                                        fileName: "src/App.js",
                                        lineNumber: 189,
                                        columnNumber: 17
                                    }, this),
                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV(_reactRouterDom.Route, {
                                        path: "/marks/view",
                                        component: _viewMarksDefault.default
                                    }, void 0, false, {
                                        fileName: "src/App.js",
                                        lineNumber: 190,
                                        columnNumber: 17
                                    }, this),
                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV(_reactRouterDom.Route, {
                                        path: "/marks/edit/:id",
                                        component: _editMarksDefault.default
                                    }, void 0, false, {
                                        fileName: "src/App.js",
                                        lineNumber: 191,
                                        columnNumber: 17
                                    }, this),
                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV(_reactRouterDom.Route, {
                                        path: "/create/feedback",
                                        component: _createResponseDefault.default
                                    }, void 0, false, {
                                        fileName: "src/App.js",
                                        lineNumber: 194,
                                        columnNumber: 17
                                    }, this),
                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV(_reactRouterDom.Route, {
                                        path: "/panel/topic/list",
                                        exact: true,
                                        component: _viewTopicListDefault.default
                                    }, void 0, false, {
                                        fileName: "src/App.js",
                                        lineNumber: 199,
                                        columnNumber: 17
                                    }, this),
                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV(_reactRouterDom.Route, {
                                        path: "/panel/topic/details/:id",
                                        exact: true,
                                        component: _viewTopicDefault.default
                                    }, void 0, false, {
                                        fileName: "src/App.js",
                                        lineNumber: 200,
                                        columnNumber: 17
                                    }, this),
                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV(_reactRouterDom.Route, {
                                        path: "/panel/topic/update/:id",
                                        exact: true,
                                        component: _updateTopicDefault.default
                                    }, void 0, false, {
                                        fileName: "src/App.js",
                                        lineNumber: 201,
                                        columnNumber: 17
                                    }, this),
                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV(_reactRouterDom.Route, {
                                        path: "/evaluation/add",
                                        component: _addEvaluationDefault.default
                                    }, void 0, false, {
                                        fileName: "src/App.js",
                                        lineNumber: 204,
                                        columnNumber: 17
                                    }, this),
                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV(_reactRouterDom.Route, {
                                        path: "/evaluation/view",
                                        component: _evaluationListDefault.default
                                    }, void 0, false, {
                                        fileName: "src/App.js",
                                        lineNumber: 205,
                                        columnNumber: 17
                                    }, this),
                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV(_reactRouterDom.Route, {
                                        path: "/evaluation/edit/:id",
                                        component: _editEvaluationDefault.default
                                    }, void 0, false, {
                                        fileName: "src/App.js",
                                        lineNumber: 206,
                                        columnNumber: 17
                                    }, this)
                                ]
                            }, void 0, true, {
                                fileName: "src/App.js",
                                lineNumber: 139,
                                columnNumber: 15
                            }, this)
                        }, void 0, false, {
                            fileName: "src/App.js",
                            lineNumber: 138,
                            columnNumber: 13
                        }, this)
                    ]
                }, void 0, true, {
                    fileName: "src/App.js",
                    lineNumber: 97,
                    columnNumber: 11
                }, this)
            }, void 0, false, {
                fileName: "src/App.js",
                lineNumber: 95,
                columnNumber: 9
            }, this)
        }, void 0, false, {
            fileName: "src/App.js",
            lineNumber: 94,
            columnNumber: 7
        }, this);
    }
}
exports.default = App;

  $parcel$ReactRefreshHelpers$f00f.postlude(module);
} finally {
  window.$RefreshReg$ = prevRefreshReg;
  window.$RefreshSig$ = prevRefreshSig;
}
},{"react/jsx-dev-runtime":"iTorj","react":"21dqq","react-router-dom":"cHIiW","./components/IT20125202/user/CreateUser":"kwIfD","./components/IT20125202/user/UserProfile":"8v7IM","./components/IT20127046/Home":"bFcqZ","./components/IT20125202/user/UserLogin":"yp28y","./components/IT20125202/NavBar":"54lqX","./components/IT20125202/LandingPage":"6GQI0","./components/IT20125202/topic/RegisterTopic":"4ynNU","./components/IT20125202/topic/ViewSubmissions":"b5i5o","./components/IT20127046/user/GroupChat_Student":"kW0vu","./components/IT20128036/user/SviewEvaluation":"b8zLy","./components/IT20128036/user/SviewSubmitionType":"beKUB","./components/IT20128036/user/Submitions":"lpiEQ","./components/IT20128036/user/viewSubmitionsp":"bi20j","./components/IT20128036/user/sviewMarks":"5NXwT","./components/IT20128036/user/editSubmition":"c1a6J","./components/IT20128036/user/exportPDF":"8Xglg","./components/IT20128036/user/StudentViewSpData":"iHo1z","../src/components/IT20131456/user/CreateStudentGroup":"aOAYN","../src/components/IT20131456/user/ViewStudentGroup":"8oXp2","./components/IT20131456/user/RequestSupervisor":"7zWk5","./components/IT20131456/user/CreateRequest":"8utuE","./components/IT20131456/user/RequestSummary":"e1MVi","./components/IT20125202/user/EditUser":"aqqmv","./components/IT20125202/user/UserDetails":"aUJA5","./components/IT20125202/user/UserRoles":"8Ch7k","./components/IT20125202/admin/AdminHome":"1w2DX","./components/IT20125202/topic/ViewListAdmin":"cJRui","./components/IT20125202/admin/AdminLogin":"hj8RA","./components/IT20127046/admin/DocumentTemp_Admin":"1v78j","./components/IT20127046/admin/DocumentTempCreate":"gS2Jc","./components/IT20127046/admin/DocumentTempUpdate":"iRkUd","./components/IT20125202/user/UserRoles_Panel":"Hfo6h","./components/IT20125202/user/UserRoles_Students":"3GIAD","./components/IT20125202/user/UserRoles_Supervisor":"ikSOp","./components/IT20128036/admin/addSubmitionType":"bIB5s","./components/IT20128036/admin/SubmitionTypeList":"9MmSd","./components/IT20128036/admin/editSubmitionType":"3WGlv","../src/components/IT20131456/admin/StudentGroup":"jUWtU","../src/components/IT20131456/admin/ViewStudentGroupAdmin":"iOZTP","./components/IT20131456/admin/EditStudentGroup":"jlglv","./components/IT20131456/admin/CreateSupervisorDetails":"bue2i","./components/IT20131456/admin/EditSupervisorDetails":"5yu2d","./components/IT20131456/admin/SupervisorDetails":"hZryf","./components/IT20125202/topic/ViewTopicList":"fJiva","./components/IT20125202/topic/ViewTopic":"92cwI","./components/IT20125202/topic/UpdateTopic":"2chAM","./components/IT20128036/admin/AddEvaluation":"62LVF","./components/IT20128036/admin/EvaluationList":"g48St","./components/IT20128036/admin/editEvaluation":"e1Dqj","./components/IT20127046/user/GroupChat_Admin":"hepmj","./images/cover.jpg":"i9AMb","./components/IT20127046/admin/CreateMarkingSchem":"fE618","./components/IT20128036/admin/viewSubmitions":"575e8","./components/IT20128036/admin/addMarks":"dq82r","./components/IT20128036/admin/viewMarks":"3s75B","./components/IT20128036/admin/editMarks":"3m7HQ","./components/IT20127046/admin/ViewMarkingSchem_Admin":"gaQiH","./components/IT20127046/user/ViewMarkingScheme":"gUCdL","./components/IT20127046/user/ViewDocumentTemplate":"2k3nQ","./components/IT20127046/user/DisplayMarkingScheme":"eqFYY","./components/IT20127046/user/ContactUs":"dnRrr","./components/IT20127046/user/AboutUs":"7I6As","./components/IT20131456/user/CreateResponse":"2exFL","@parcel/transformer-js/src/esmodule-helpers.js":"gkKU3","@parcel/transformer-react-refresh-wrap/lib/helpers/helpers.js":"km3Ru"}],"cHIiW":[function(require,module,exports) {
var parcelHelpers = require("@parcel/transformer-js/src/esmodule-helpers.js");
parcelHelpers.defineInteropFlag(exports);
parcelHelpers.export(exports, "MemoryRouter", ()=>_reactRouter.MemoryRouter
);
parcelHelpers.export(exports, "Prompt", ()=>_reactRouter.Prompt
);
parcelHelpers.export(exports, "Redirect", ()=>_reactRouter.Redirect
);
parcelHelpers.export(exports, "Route", ()=>_reactRouter.Route
);
parcelHelpers.export(exports, "Router", ()=>_reactRouter.Router
);
parcelHelpers.export(exports, "StaticRouter", ()=>_reactRouter.StaticRouter
);
parcelHelpers.export(exports, "Switch", ()=>_reactRouter.Switch
);
parcelHelpers.export(exports, "generatePath", ()=>_reactRouter.generatePath
);
parcelHelpers.export(exports, "matchPath", ()=>_reactRouter.matchPath
);
parcelHelpers.export(exports, "useHistory", ()=>_reactRouter.useHistory
);
parcelHelpers.export(exports, "useLocation", ()=>_reactRouter.useLocation
);
parcelHelpers.export(exports, "useParams", ()=>_reactRouter.useParams
);
parcelHelpers.export(exports, "useRouteMatch", ()=>_reactRouter.useRouteMatch
);
parcelHelpers.export(exports, "withRouter", ()=>_reactRouter.withRouter
);
parcelHelpers.export(exports, "BrowserRouter", ()=>BrowserRouter
);
parcelHelpers.export(exports, "HashRouter", ()=>HashRouter
);
parcelHelpers.export(exports, "Link", ()=>Link
);
parcelHelpers.export(exports, "NavLink", ()=>NavLink
);
var _reactRouter = require("react-router");
var _inheritsLoose = require("@babel/runtime/helpers/esm/inheritsLoose");
var _inheritsLooseDefault = parcelHelpers.interopDefault(_inheritsLoose);
var _react = require("react");
var _reactDefault = parcelHelpers.interopDefault(_react);
var _history = require("history");
var _propTypes = require("prop-types");
var _propTypesDefault = parcelHelpers.interopDefault(_propTypes);
var _tinyWarning = require("tiny-warning");
var _tinyWarningDefault = parcelHelpers.interopDefault(_tinyWarning);
var _extends = require("@babel/runtime/helpers/esm/extends");
var _extendsDefault = parcelHelpers.interopDefault(_extends);
var _objectWithoutPropertiesLoose = require("@babel/runtime/helpers/esm/objectWithoutPropertiesLoose");
var _objectWithoutPropertiesLooseDefault = parcelHelpers.interopDefault(_objectWithoutPropertiesLoose);
var _tinyInvariant = require("tiny-invariant");
var _tinyInvariantDefault = parcelHelpers.interopDefault(_tinyInvariant);
/**
 * The public API for a <Router> that uses HTML5 history.
 */ var BrowserRouter = /*#__PURE__*/ function(_React$Component) {
    _inheritsLooseDefault.default(BrowserRouter1, _React$Component);
    function BrowserRouter1() {
        var _this;
        for(var _len = arguments.length, args = new Array(_len), _key = 0; _key < _len; _key++)args[_key] = arguments[_key];
        _this = _React$Component.call.apply(_React$Component, [
            this
        ].concat(args)) || this;
        _this.history = _history.createBrowserHistory(_this.props);
        return _this;
    }
    var _proto = BrowserRouter1.prototype;
    _proto.render = function render() {
        return _reactDefault.default.createElement(_reactRouter.Router, {
            history: this.history,
            children: this.props.children
        });
    };
    return BrowserRouter1;
}(_reactDefault.default.Component);
BrowserRouter.propTypes = {
    basename: _propTypesDefault.default.string,
    children: _propTypesDefault.default.node,
    forceRefresh: _propTypesDefault.default.bool,
    getUserConfirmation: _propTypesDefault.default.func,
    keyLength: _propTypesDefault.default.number
};
BrowserRouter.prototype.componentDidMount = function() {
    _tinyWarningDefault.default(!this.props.history, "<BrowserRouter> ignores the history prop. To use a custom history, use `import { Router }` instead of `import { BrowserRouter as Router }`.");
};
/**
 * The public API for a <Router> that uses window.location.hash.
 */ var HashRouter = /*#__PURE__*/ function(_React$Component) {
    _inheritsLooseDefault.default(HashRouter1, _React$Component);
    function HashRouter1() {
        var _this;
        for(var _len = arguments.length, args = new Array(_len), _key = 0; _key < _len; _key++)args[_key] = arguments[_key];
        _this = _React$Component.call.apply(_React$Component, [
            this
        ].concat(args)) || this;
        _this.history = _history.createHashHistory(_this.props);
        return _this;
    }
    var _proto = HashRouter1.prototype;
    _proto.render = function render() {
        return _reactDefault.default.createElement(_reactRouter.Router, {
            history: this.history,
            children: this.props.children
        });
    };
    return HashRouter1;
}(_reactDefault.default.Component);
HashRouter.propTypes = {
    basename: _propTypesDefault.default.string,
    children: _propTypesDefault.default.node,
    getUserConfirmation: _propTypesDefault.default.func,
    hashType: _propTypesDefault.default.oneOf([
        "hashbang",
        "noslash",
        "slash"
    ])
};
HashRouter.prototype.componentDidMount = function() {
    _tinyWarningDefault.default(!this.props.history, "<HashRouter> ignores the history prop. To use a custom history, use `import { Router }` instead of `import { HashRouter as Router }`.");
};
var resolveToLocation = function resolveToLocation(to, currentLocation) {
    return typeof to === "function" ? to(currentLocation) : to;
};
var normalizeToLocation = function normalizeToLocation(to, currentLocation) {
    return typeof to === "string" ? _history.createLocation(to, null, null, currentLocation) : to;
};
var forwardRefShim = function forwardRefShim(C) {
    return C;
};
var forwardRef = _reactDefault.default.forwardRef;
if (typeof forwardRef === "undefined") forwardRef = forwardRefShim;
function isModifiedEvent(event) {
    return !!(event.metaKey || event.altKey || event.ctrlKey || event.shiftKey);
}
var LinkAnchor = forwardRef(function(_ref, forwardedRef) {
    var innerRef = _ref.innerRef, navigate = _ref.navigate, _onClick = _ref.onClick, rest = _objectWithoutPropertiesLooseDefault.default(_ref, [
        "innerRef",
        "navigate",
        "onClick"
    ]);
    var target = rest.target;
    var props = _extendsDefault.default({}, rest, {
        onClick: function onClick(event) {
            try {
                if (_onClick) _onClick(event);
            } catch (ex) {
                event.preventDefault();
                throw ex;
            }
            if (!event.defaultPrevented && event.button === 0 && (!target || target === "_self") && !isModifiedEvent(event) // ignore clicks with modifier keys
            ) {
                event.preventDefault();
                navigate();
            }
        }
    }); // React 15 compat
    if (forwardRefShim !== forwardRef) props.ref = forwardedRef || innerRef;
    else props.ref = innerRef;
    /* eslint-disable-next-line jsx-a11y/anchor-has-content */ return _reactDefault.default.createElement("a", props);
});
LinkAnchor.displayName = "LinkAnchor";
/**
 * The public API for rendering a history-aware <a>.
 */ var Link = forwardRef(function(_ref2, forwardedRef) {
    var _ref2$component = _ref2.component, component = _ref2$component === void 0 ? LinkAnchor : _ref2$component, replace = _ref2.replace, to = _ref2.to, innerRef = _ref2.innerRef, rest = _objectWithoutPropertiesLooseDefault.default(_ref2, [
        "component",
        "replace",
        "to",
        "innerRef"
    ]);
    return _reactDefault.default.createElement(_reactRouter.__RouterContext.Consumer, null, function(context) {
        !context && _tinyInvariantDefault.default(false, "You should not use <Link> outside a <Router>");
        var history = context.history;
        var location1 = normalizeToLocation(resolveToLocation(to, context.location), context.location);
        var href = location1 ? history.createHref(location1) : "";
        var props = _extendsDefault.default({}, rest, {
            href: href,
            navigate: function navigate() {
                var location = resolveToLocation(to, context.location);
                var method = replace ? history.replace : history.push;
                method(location);
            }
        }); // React 15 compat
        if (forwardRefShim !== forwardRef) props.ref = forwardedRef || innerRef;
        else props.innerRef = innerRef;
        return _reactDefault.default.createElement(component, props);
    });
});
var toType = _propTypesDefault.default.oneOfType([
    _propTypesDefault.default.string,
    _propTypesDefault.default.object,
    _propTypesDefault.default.func
]);
var refType = _propTypesDefault.default.oneOfType([
    _propTypesDefault.default.string,
    _propTypesDefault.default.func,
    _propTypesDefault.default.shape({
        current: _propTypesDefault.default.any
    })
]);
Link.displayName = "Link";
Link.propTypes = {
    innerRef: refType,
    onClick: _propTypesDefault.default.func,
    replace: _propTypesDefault.default.bool,
    target: _propTypesDefault.default.string,
    to: toType.isRequired
};
var forwardRefShim$1 = function forwardRefShim(C) {
    return C;
};
var forwardRef$1 = _reactDefault.default.forwardRef;
if (typeof forwardRef$1 === "undefined") forwardRef$1 = forwardRefShim$1;
function joinClassnames() {
    for(var _len = arguments.length, classnames = new Array(_len), _key = 0; _key < _len; _key++)classnames[_key] = arguments[_key];
    return classnames.filter(function(i) {
        return i;
    }).join(" ");
}
/**
 * A <Link> wrapper that knows if it's "active" or not.
 */ var NavLink = forwardRef$1(function(_ref, forwardedRef) {
    var _ref$ariaCurrent = _ref["aria-current"], ariaCurrent = _ref$ariaCurrent === void 0 ? "page" : _ref$ariaCurrent, _ref$activeClassName = _ref.activeClassName, activeClassName = _ref$activeClassName === void 0 ? "active" : _ref$activeClassName, activeStyle = _ref.activeStyle, classNameProp = _ref.className, exact = _ref.exact, isActiveProp = _ref.isActive, locationProp = _ref.location, sensitive = _ref.sensitive, strict = _ref.strict, styleProp = _ref.style, to = _ref.to, innerRef = _ref.innerRef, rest = _objectWithoutPropertiesLooseDefault.default(_ref, [
        "aria-current",
        "activeClassName",
        "activeStyle",
        "className",
        "exact",
        "isActive",
        "location",
        "sensitive",
        "strict",
        "style",
        "to",
        "innerRef"
    ]);
    return _reactDefault.default.createElement(_reactRouter.__RouterContext.Consumer, null, function(context) {
        !context && _tinyInvariantDefault.default(false, "You should not use <NavLink> outside a <Router>");
        var currentLocation = locationProp || context.location;
        var toLocation = normalizeToLocation(resolveToLocation(to, currentLocation), currentLocation);
        var path = toLocation.pathname; // Regex taken from: https://github.com/pillarjs/path-to-regexp/blob/master/index.js#L202
        var escapedPath = path && path.replace(/([.+*?=^!:${}()[\]|/\\])/g, "\\$1");
        var match = escapedPath ? _reactRouter.matchPath(currentLocation.pathname, {
            path: escapedPath,
            exact: exact,
            sensitive: sensitive,
            strict: strict
        }) : null;
        var isActive = !!(isActiveProp ? isActiveProp(match, currentLocation) : match);
        var className = isActive ? joinClassnames(classNameProp, activeClassName) : classNameProp;
        var style = isActive ? _extendsDefault.default({}, styleProp, {}, activeStyle) : styleProp;
        var props = _extendsDefault.default({
            "aria-current": isActive && ariaCurrent || null,
            className: className,
            style: style,
            to: toLocation
        }, rest); // React 15 compat
        if (forwardRefShim$1 !== forwardRef$1) props.ref = forwardedRef || innerRef;
        else props.innerRef = innerRef;
        return _reactDefault.default.createElement(Link, props);
    });
});
NavLink.displayName = "NavLink";
var ariaCurrentType = _propTypesDefault.default.oneOf([
    "page",
    "step",
    "location",
    "date",
    "time",
    "true"
]);
NavLink.propTypes = _extendsDefault.default({}, Link.propTypes, {
    "aria-current": ariaCurrentType,
    activeClassName: _propTypesDefault.default.string,
    activeStyle: _propTypesDefault.default.object,
    className: _propTypesDefault.default.string,
    exact: _propTypesDefault.default.bool,
    isActive: _propTypesDefault.default.func,
    location: _propTypesDefault.default.object,
    sensitive: _propTypesDefault.default.bool,
    strict: _propTypesDefault.default.bool,
    style: _propTypesDefault.default.object
});

},{"react-router":"dvHa0","@babel/runtime/helpers/esm/inheritsLoose":"9u2Z8","react":"21dqq","history":"6yWeD","prop-types":"7wKI2","tiny-warning":"82vrW","@babel/runtime/helpers/esm/extends":"fTBFS","@babel/runtime/helpers/esm/objectWithoutPropertiesLoose":"adHgr","tiny-invariant":"fnIPv","@parcel/transformer-js/src/esmodule-helpers.js":"gkKU3"}],"dvHa0":[function(require,module,exports) {
var parcelHelpers = require("@parcel/transformer-js/src/esmodule-helpers.js");
parcelHelpers.defineInteropFlag(exports);
parcelHelpers.export(exports, "MemoryRouter", ()=>MemoryRouter
);
parcelHelpers.export(exports, "Prompt", ()=>Prompt
);
parcelHelpers.export(exports, "Redirect", ()=>Redirect
);
parcelHelpers.export(exports, "Route", ()=>Route
);
parcelHelpers.export(exports, "Router", ()=>Router
);
parcelHelpers.export(exports, "StaticRouter", ()=>StaticRouter
);
parcelHelpers.export(exports, "Switch", ()=>Switch
);
parcelHelpers.export(exports, "__HistoryContext", ()=>historyContext
);
parcelHelpers.export(exports, "__RouterContext", ()=>context
);
parcelHelpers.export(exports, "generatePath", ()=>generatePath
);
parcelHelpers.export(exports, "matchPath", ()=>matchPath
);
parcelHelpers.export(exports, "useHistory", ()=>useHistory
);
parcelHelpers.export(exports, "useLocation", ()=>useLocation
);
parcelHelpers.export(exports, "useParams", ()=>useParams
);
parcelHelpers.export(exports, "useRouteMatch", ()=>useRouteMatch
);
parcelHelpers.export(exports, "withRouter", ()=>withRouter
);
var _inheritsLoose = require("@babel/runtime/helpers/esm/inheritsLoose");
var _inheritsLooseDefault = parcelHelpers.interopDefault(_inheritsLoose);
var _react = require("react");
var _reactDefault = parcelHelpers.interopDefault(_react);
var _propTypes = require("prop-types");
var _propTypesDefault = parcelHelpers.interopDefault(_propTypes);
var _history = require("history");
var _tinyWarning = require("tiny-warning");
var _tinyWarningDefault = parcelHelpers.interopDefault(_tinyWarning);
var _miniCreateReactContext = require("mini-create-react-context");
var _miniCreateReactContextDefault = parcelHelpers.interopDefault(_miniCreateReactContext);
var _tinyInvariant = require("tiny-invariant");
var _tinyInvariantDefault = parcelHelpers.interopDefault(_tinyInvariant);
var _extends = require("@babel/runtime/helpers/esm/extends");
var _extendsDefault = parcelHelpers.interopDefault(_extends);
var _pathToRegexp = require("path-to-regexp");
var _pathToRegexpDefault = parcelHelpers.interopDefault(_pathToRegexp);
var _reactIs = require("react-is");
var _objectWithoutPropertiesLoose = require("@babel/runtime/helpers/esm/objectWithoutPropertiesLoose");
var _objectWithoutPropertiesLooseDefault = parcelHelpers.interopDefault(_objectWithoutPropertiesLoose);
var _hoistNonReactStatics = require("hoist-non-react-statics");
var _hoistNonReactStaticsDefault = parcelHelpers.interopDefault(_hoistNonReactStatics);
// TODO: Replace with React.createContext once we can assume React 16+
var createNamedContext = function createNamedContext(name) {
    var context1 = _miniCreateReactContextDefault.default();
    context1.displayName = name;
    return context1;
};
var historyContext = /*#__PURE__*/ createNamedContext("Router-History");
// TODO: Replace with React.createContext once we can assume React 16+
var createNamedContext$1 = function createNamedContext(name) {
    var context2 = _miniCreateReactContextDefault.default();
    context2.displayName = name;
    return context2;
};
var context = /*#__PURE__*/ createNamedContext$1("Router");
/**
 * The public API for putting history on context.
 */ var Router = /*#__PURE__*/ function(_React$Component) {
    _inheritsLooseDefault.default(Router1, _React$Component);
    Router1.computeRootMatch = function computeRootMatch(pathname) {
        return {
            path: "/",
            url: "/",
            params: {},
            isExact: pathname === "/"
        };
    };
    function Router1(props) {
        var _this;
        _this = _React$Component.call(this, props) || this;
        _this.state = {
            location: props.history.location
        }; // This is a bit of a hack. We have to start listening for location
        // changes here in the constructor in case there are any <Redirect>s
        // on the initial render. If there are, they will replace/push when
        // they mount and since cDM fires in children before parents, we may
        // get a new location before the <Router> is mounted.
        _this._isMounted = false;
        _this._pendingLocation = null;
        if (!props.staticContext) _this.unlisten = props.history.listen(function(location) {
            if (_this._isMounted) _this.setState({
                location: location
            });
            else _this._pendingLocation = location;
        });
        return _this;
    }
    var _proto = Router1.prototype;
    _proto.componentDidMount = function componentDidMount() {
        this._isMounted = true;
        if (this._pendingLocation) this.setState({
            location: this._pendingLocation
        });
    };
    _proto.componentWillUnmount = function componentWillUnmount() {
        if (this.unlisten) this.unlisten();
    };
    _proto.render = function render() {
        return _reactDefault.default.createElement(context.Provider, {
            value: {
                history: this.props.history,
                location: this.state.location,
                match: Router1.computeRootMatch(this.state.location.pathname),
                staticContext: this.props.staticContext
            }
        }, _reactDefault.default.createElement(historyContext.Provider, {
            children: this.props.children || null,
            value: this.props.history
        }));
    };
    return Router1;
}(_reactDefault.default.Component);
Router.propTypes = {
    children: _propTypesDefault.default.node,
    history: _propTypesDefault.default.object.isRequired,
    staticContext: _propTypesDefault.default.object
};
Router.prototype.componentDidUpdate = function(prevProps) {
    _tinyWarningDefault.default(prevProps.history === this.props.history, "You cannot change <Router history>");
};
/**
 * The public API for a <Router> that stores location in memory.
 */ var MemoryRouter = /*#__PURE__*/ function(_React$Component) {
    _inheritsLooseDefault.default(MemoryRouter1, _React$Component);
    function MemoryRouter1() {
        var _this;
        for(var _len = arguments.length, args = new Array(_len), _key = 0; _key < _len; _key++)args[_key] = arguments[_key];
        _this = _React$Component.call.apply(_React$Component, [
            this
        ].concat(args)) || this;
        _this.history = _history.createMemoryHistory(_this.props);
        return _this;
    }
    var _proto = MemoryRouter1.prototype;
    _proto.render = function render() {
        return _reactDefault.default.createElement(Router, {
            history: this.history,
            children: this.props.children
        });
    };
    return MemoryRouter1;
}(_reactDefault.default.Component);
MemoryRouter.propTypes = {
    initialEntries: _propTypesDefault.default.array,
    initialIndex: _propTypesDefault.default.number,
    getUserConfirmation: _propTypesDefault.default.func,
    keyLength: _propTypesDefault.default.number,
    children: _propTypesDefault.default.node
};
MemoryRouter.prototype.componentDidMount = function() {
    _tinyWarningDefault.default(!this.props.history, "<MemoryRouter> ignores the history prop. To use a custom history, use `import { Router }` instead of `import { MemoryRouter as Router }`.");
};
var Lifecycle = /*#__PURE__*/ function(_React$Component) {
    _inheritsLooseDefault.default(Lifecycle1, _React$Component);
    function Lifecycle1() {
        return _React$Component.apply(this, arguments) || this;
    }
    var _proto = Lifecycle1.prototype;
    _proto.componentDidMount = function componentDidMount() {
        if (this.props.onMount) this.props.onMount.call(this, this);
    };
    _proto.componentDidUpdate = function componentDidUpdate(prevProps) {
        if (this.props.onUpdate) this.props.onUpdate.call(this, this, prevProps);
    };
    _proto.componentWillUnmount = function componentWillUnmount() {
        if (this.props.onUnmount) this.props.onUnmount.call(this, this);
    };
    _proto.render = function render() {
        return null;
    };
    return Lifecycle1;
}(_reactDefault.default.Component);
/**
 * The public API for prompting the user before navigating away from a screen.
 */ function Prompt(_ref) {
    var message = _ref.message, _ref$when = _ref.when, when = _ref$when === void 0 ? true : _ref$when;
    return _reactDefault.default.createElement(context.Consumer, null, function(context3) {
        !context3 && _tinyInvariantDefault.default(false, "You should not use <Prompt> outside a <Router>");
        if (!when || context3.staticContext) return null;
        var method = context3.history.block;
        return _reactDefault.default.createElement(Lifecycle, {
            onMount: function onMount(self) {
                self.release = method(message);
            },
            onUpdate: function onUpdate(self, prevProps) {
                if (prevProps.message !== message) {
                    self.release();
                    self.release = method(message);
                }
            },
            onUnmount: function onUnmount(self) {
                self.release();
            },
            message: message
        });
    });
}
var messageType = _propTypesDefault.default.oneOfType([
    _propTypesDefault.default.func,
    _propTypesDefault.default.string
]);
Prompt.propTypes = {
    when: _propTypesDefault.default.bool,
    message: messageType.isRequired
};
var cache = {};
var cacheLimit = 10000;
var cacheCount = 0;
function compilePath(path) {
    if (cache[path]) return cache[path];
    var generator = _pathToRegexpDefault.default.compile(path);
    if (cacheCount < cacheLimit) {
        cache[path] = generator;
        cacheCount++;
    }
    return generator;
}
/**
 * Public API for generating a URL pathname from a path and parameters.
 */ function generatePath(path, params) {
    if (path === void 0) path = "/";
    if (params === void 0) params = {};
    return path === "/" ? path : compilePath(path)(params, {
        pretty: true
    });
}
/**
 * The public API for navigating programmatically with a component.
 */ function Redirect(_ref) {
    var computedMatch = _ref.computedMatch, to = _ref.to, _ref$push = _ref.push, push = _ref$push === void 0 ? false : _ref$push;
    return _reactDefault.default.createElement(context.Consumer, null, function(context4) {
        !context4 && _tinyInvariantDefault.default(false, "You should not use <Redirect> outside a <Router>");
        var history = context4.history, staticContext = context4.staticContext;
        var method = push ? history.push : history.replace;
        var location = _history.createLocation(computedMatch ? typeof to === "string" ? generatePath(to, computedMatch.params) : _extendsDefault.default({}, to, {
            pathname: generatePath(to.pathname, computedMatch.params)
        }) : to); // When rendering in a static context,
        // set the new location immediately.
        if (staticContext) {
            method(location);
            return null;
        }
        return _reactDefault.default.createElement(Lifecycle, {
            onMount: function onMount() {
                method(location);
            },
            onUpdate: function onUpdate(self, prevProps) {
                var prevLocation = _history.createLocation(prevProps.to);
                if (!_history.locationsAreEqual(prevLocation, _extendsDefault.default({}, location, {
                    key: prevLocation.key
                }))) method(location);
            },
            to: to
        });
    });
}
Redirect.propTypes = {
    push: _propTypesDefault.default.bool,
    from: _propTypesDefault.default.string,
    to: _propTypesDefault.default.oneOfType([
        _propTypesDefault.default.string,
        _propTypesDefault.default.object
    ]).isRequired
};
var cache$1 = {};
var cacheLimit$1 = 10000;
var cacheCount$1 = 0;
function compilePath$1(path, options) {
    var cacheKey = "" + options.end + options.strict + options.sensitive;
    var pathCache = cache$1[cacheKey] || (cache$1[cacheKey] = {});
    if (pathCache[path]) return pathCache[path];
    var keys = [];
    var regexp = _pathToRegexpDefault.default(path, keys, options);
    var result = {
        regexp: regexp,
        keys: keys
    };
    if (cacheCount$1 < cacheLimit$1) {
        pathCache[path] = result;
        cacheCount$1++;
    }
    return result;
}
/**
 * Public API for matching a URL pathname to a path.
 */ function matchPath(pathname, options) {
    if (options === void 0) options = {};
    if (typeof options === "string" || Array.isArray(options)) options = {
        path: options
    };
    var _options = options, path1 = _options.path, _options$exact = _options.exact, exact = _options$exact === void 0 ? false : _options$exact, _options$strict = _options.strict, strict = _options$strict === void 0 ? false : _options$strict, _options$sensitive = _options.sensitive, sensitive = _options$sensitive === void 0 ? false : _options$sensitive;
    var paths = [].concat(path1);
    return paths.reduce(function(matched, path) {
        if (!path && path !== "") return null;
        if (matched) return matched;
        var _compilePath = compilePath$1(path, {
            end: exact,
            strict: strict,
            sensitive: sensitive
        }), regexp = _compilePath.regexp, keys = _compilePath.keys;
        var match = regexp.exec(pathname);
        if (!match) return null;
        var url = match[0], values = match.slice(1);
        var isExact = pathname === url;
        if (exact && !isExact) return null;
        return {
            path: path,
            // the path used to match
            url: path === "/" && url === "" ? "/" : url,
            // the matched portion of the URL
            isExact: isExact,
            // whether or not we matched exactly
            params: keys.reduce(function(memo, key1, index) {
                memo[key1.name] = values[index];
                return memo;
            }, {})
        };
    }, null);
}
function isEmptyChildren(children) {
    return _reactDefault.default.Children.count(children) === 0;
}
function evalChildrenDev(children, props, path) {
    var value = children(props);
    _tinyWarningDefault.default(value !== undefined, "You returned `undefined` from the `children` function of " + ("<Route" + (path ? " path=\"" + path + "\"" : "") + ">, but you ") + "should have returned a React element or `null`");
    return value || null;
}
/**
 * The public API for matching a single path and rendering.
 */ var Route = /*#__PURE__*/ function(_React$Component) {
    _inheritsLooseDefault.default(Route1, _React$Component);
    function Route1() {
        return _React$Component.apply(this, arguments) || this;
    }
    var _proto = Route1.prototype;
    _proto.render = function render1() {
        var _this = this;
        return _reactDefault.default.createElement(context.Consumer, null, function(context$1) {
            !context$1 && _tinyInvariantDefault.default(false, "You should not use <Route> outside a <Router>");
            var location = _this.props.location || context$1.location;
            var match = _this.props.computedMatch ? _this.props.computedMatch // <Switch> already computed the match for us
             : _this.props.path ? matchPath(location.pathname, _this.props) : context$1.match;
            var props = _extendsDefault.default({}, context$1, {
                location: location,
                match: match
            });
            var _this$props = _this.props, children = _this$props.children, component = _this$props.component, render = _this$props.render; // Preact uses an empty array as children by
            // default, so use null if that's the case.
            if (Array.isArray(children) && children.length === 0) children = null;
            return _reactDefault.default.createElement(context.Provider, {
                value: props
            }, props.match ? children ? typeof children === "function" ? evalChildrenDev(children, props, _this.props.path) : children : component ? _reactDefault.default.createElement(component, props) : render ? render(props) : null : typeof children === "function" ? evalChildrenDev(children, props, _this.props.path) : null);
        });
    };
    return Route1;
}(_reactDefault.default.Component);
Route.propTypes = {
    children: _propTypesDefault.default.oneOfType([
        _propTypesDefault.default.func,
        _propTypesDefault.default.node
    ]),
    component: function component(props, propName) {
        if (props[propName] && !_reactIs.isValidElementType(props[propName])) return new Error("Invalid prop 'component' supplied to 'Route': the prop is not a valid React component");
    },
    exact: _propTypesDefault.default.bool,
    location: _propTypesDefault.default.object,
    path: _propTypesDefault.default.oneOfType([
        _propTypesDefault.default.string,
        _propTypesDefault.default.arrayOf(_propTypesDefault.default.string)
    ]),
    render: _propTypesDefault.default.func,
    sensitive: _propTypesDefault.default.bool,
    strict: _propTypesDefault.default.bool
};
Route.prototype.componentDidMount = function() {
    _tinyWarningDefault.default(!(this.props.children && !isEmptyChildren(this.props.children) && this.props.component), "You should not use <Route component> and <Route children> in the same route; <Route component> will be ignored");
    _tinyWarningDefault.default(!(this.props.children && !isEmptyChildren(this.props.children) && this.props.render), "You should not use <Route render> and <Route children> in the same route; <Route render> will be ignored");
    _tinyWarningDefault.default(!(this.props.component && this.props.render), "You should not use <Route component> and <Route render> in the same route; <Route render> will be ignored");
};
Route.prototype.componentDidUpdate = function(prevProps) {
    _tinyWarningDefault.default(!(this.props.location && !prevProps.location), '<Route> elements should not change from uncontrolled to controlled (or vice versa). You initially used no "location" prop and then provided one on a subsequent render.');
    _tinyWarningDefault.default(!(!this.props.location && prevProps.location), '<Route> elements should not change from controlled to uncontrolled (or vice versa). You provided a "location" prop initially but omitted it on a subsequent render.');
};
function addLeadingSlash(path) {
    return path.charAt(0) === "/" ? path : "/" + path;
}
function addBasename(basename, location) {
    if (!basename) return location;
    return _extendsDefault.default({}, location, {
        pathname: addLeadingSlash(basename) + location.pathname
    });
}
function stripBasename(basename, location) {
    if (!basename) return location;
    var base = addLeadingSlash(basename);
    if (location.pathname.indexOf(base) !== 0) return location;
    return _extendsDefault.default({}, location, {
        pathname: location.pathname.substr(base.length)
    });
}
function createURL(location) {
    return typeof location === "string" ? location : _history.createPath(location);
}
function staticHandler(methodName) {
    return function() {
        _tinyInvariantDefault.default(false, "You cannot %s with <StaticRouter>", methodName);
    };
}
function noop() {}
/**
 * The public top-level API for a "static" <Router>, so-called because it
 * can't actually change the current location. Instead, it just records
 * location changes in a context object. Useful mainly in testing and
 * server-rendering scenarios.
 */ var StaticRouter = /*#__PURE__*/ function(_React$Component) {
    _inheritsLooseDefault.default(StaticRouter1, _React$Component);
    function StaticRouter1() {
        var _this;
        for(var _len = arguments.length, args = new Array(_len), _key = 0; _key < _len; _key++)args[_key] = arguments[_key];
        _this = _React$Component.call.apply(_React$Component, [
            this
        ].concat(args)) || this;
        _this.handlePush = function(location) {
            return _this.navigateTo(location, "PUSH");
        };
        _this.handleReplace = function(location) {
            return _this.navigateTo(location, "REPLACE");
        };
        _this.handleListen = function() {
            return noop;
        };
        _this.handleBlock = function() {
            return noop;
        };
        return _this;
    }
    var _proto = StaticRouter1.prototype;
    _proto.navigateTo = function navigateTo(location, action) {
        var _this$props = this.props, _this$props$basename = _this$props.basename, basename = _this$props$basename === void 0 ? "" : _this$props$basename, _this$props$context = _this$props.context, context5 = _this$props$context === void 0 ? {} : _this$props$context;
        context5.action = action;
        context5.location = addBasename(basename, _history.createLocation(location));
        context5.url = createURL(context5.location);
    };
    _proto.render = function render() {
        var _this$props2 = this.props, _this$props2$basename = _this$props2.basename, basename = _this$props2$basename === void 0 ? "" : _this$props2$basename, _this$props2$context = _this$props2.context, context6 = _this$props2$context === void 0 ? {} : _this$props2$context, _this$props2$location = _this$props2.location, location = _this$props2$location === void 0 ? "/" : _this$props2$location, rest = _objectWithoutPropertiesLooseDefault.default(_this$props2, [
            "basename",
            "context",
            "location"
        ]);
        var history = {
            createHref: function createHref(path) {
                return addLeadingSlash(basename + createURL(path));
            },
            action: "POP",
            location: stripBasename(basename, _history.createLocation(location)),
            push: this.handlePush,
            replace: this.handleReplace,
            go: staticHandler("go"),
            goBack: staticHandler("goBack"),
            goForward: staticHandler("goForward"),
            listen: this.handleListen,
            block: this.handleBlock
        };
        return _reactDefault.default.createElement(Router, _extendsDefault.default({}, rest, {
            history: history,
            staticContext: context6
        }));
    };
    return StaticRouter1;
}(_reactDefault.default.Component);
StaticRouter.propTypes = {
    basename: _propTypesDefault.default.string,
    context: _propTypesDefault.default.object,
    location: _propTypesDefault.default.oneOfType([
        _propTypesDefault.default.string,
        _propTypesDefault.default.object
    ])
};
StaticRouter.prototype.componentDidMount = function() {
    _tinyWarningDefault.default(!this.props.history, "<StaticRouter> ignores the history prop. To use a custom history, use `import { Router }` instead of `import { StaticRouter as Router }`.");
};
/**
 * The public API for rendering the first <Route> that matches.
 */ var Switch = /*#__PURE__*/ function(_React$Component) {
    _inheritsLooseDefault.default(Switch1, _React$Component);
    function Switch1() {
        return _React$Component.apply(this, arguments) || this;
    }
    var _proto = Switch1.prototype;
    _proto.render = function render() {
        var _this = this;
        return _reactDefault.default.createElement(context.Consumer, null, function(context7) {
            !context7 && _tinyInvariantDefault.default(false, "You should not use <Switch> outside a <Router>");
            var location = _this.props.location || context7.location;
            var element, match; // We use React.Children.forEach instead of React.Children.toArray().find()
            // here because toArray adds keys to all child elements and we do not want
            // to trigger an unmount/remount for two <Route>s that render the same
            // component at different URLs.
            _reactDefault.default.Children.forEach(_this.props.children, function(child) {
                if (match == null && _reactDefault.default.isValidElement(child)) {
                    element = child;
                    var path = child.props.path || child.props.from;
                    match = path ? matchPath(location.pathname, _extendsDefault.default({}, child.props, {
                        path: path
                    })) : context7.match;
                }
            });
            return match ? _reactDefault.default.cloneElement(element, {
                location: location,
                computedMatch: match
            }) : null;
        });
    };
    return Switch1;
}(_reactDefault.default.Component);
Switch.propTypes = {
    children: _propTypesDefault.default.node,
    location: _propTypesDefault.default.object
};
Switch.prototype.componentDidUpdate = function(prevProps) {
    _tinyWarningDefault.default(!(this.props.location && !prevProps.location), '<Switch> elements should not change from uncontrolled to controlled (or vice versa). You initially used no "location" prop and then provided one on a subsequent render.');
    _tinyWarningDefault.default(!(!this.props.location && prevProps.location), '<Switch> elements should not change from controlled to uncontrolled (or vice versa). You provided a "location" prop initially but omitted it on a subsequent render.');
};
/**
 * A public higher-order component to access the imperative API
 */ function withRouter(Component) {
    var displayName = "withRouter(" + (Component.displayName || Component.name) + ")";
    var C = function C(props) {
        var wrappedComponentRef = props.wrappedComponentRef, remainingProps = _objectWithoutPropertiesLooseDefault.default(props, [
            "wrappedComponentRef"
        ]);
        return _reactDefault.default.createElement(context.Consumer, null, function(context8) {
            !context8 && _tinyInvariantDefault.default(false, "You should not use <" + displayName + " /> outside a <Router>");
            return _reactDefault.default.createElement(Component, _extendsDefault.default({}, remainingProps, context8, {
                ref: wrappedComponentRef
            }));
        });
    };
    C.displayName = displayName;
    C.WrappedComponent = Component;
    C.propTypes = {
        wrappedComponentRef: _propTypesDefault.default.oneOfType([
            _propTypesDefault.default.string,
            _propTypesDefault.default.func,
            _propTypesDefault.default.object
        ])
    };
    return _hoistNonReactStaticsDefault.default(C, Component);
}
var useContext = _reactDefault.default.useContext;
function useHistory() {
    !(typeof useContext === "function") && _tinyInvariantDefault.default(false, "You must use React >= 16.8 in order to use useHistory()");
    return useContext(historyContext);
}
function useLocation() {
    !(typeof useContext === "function") && _tinyInvariantDefault.default(false, "You must use React >= 16.8 in order to use useLocation()");
    return useContext(context).location;
}
function useParams() {
    !(typeof useContext === "function") && _tinyInvariantDefault.default(false, "You must use React >= 16.8 in order to use useParams()");
    var match = useContext(context).match;
    return match ? match.params : {};
}
function useRouteMatch(path) {
    !(typeof useContext === "function") && _tinyInvariantDefault.default(false, "You must use React >= 16.8 in order to use useRouteMatch()");
    var location = useLocation();
    var match = useContext(context).match;
    return path ? matchPath(location.pathname, path) : match;
}
if (typeof window !== "undefined") {
    var global = window;
    var key = "__react_router_build__";
    var buildNames = {
        cjs: "CommonJS",
        esm: "ES modules",
        umd: "UMD"
    };
    if (global[key] && global[key] !== "esm") {
        var initialBuildName = buildNames[global[key]];
        var secondaryBuildName = buildNames["esm"]; // TODO: Add link to article that explains in detail how to avoid
        // loading 2 different builds.
        throw new Error("You are loading the " + secondaryBuildName + " build of React Router " + ("on a page that is already running the " + initialBuildName + " ") + "build, so things won't work right.");
    }
    global[key] = "esm";
}

},{"@babel/runtime/helpers/esm/inheritsLoose":"9u2Z8","react":"21dqq","prop-types":"7wKI2","history":"6yWeD","tiny-warning":"82vrW","mini-create-react-context":"fRuCg","tiny-invariant":"fnIPv","@babel/runtime/helpers/esm/extends":"fTBFS","path-to-regexp":"6q3tZ","react-is":"7EuwB","@babel/runtime/helpers/esm/objectWithoutPropertiesLoose":"adHgr","hoist-non-react-statics":"1GfsB","@parcel/transformer-js/src/esmodule-helpers.js":"gkKU3"}],"9u2Z8":[function(require,module,exports) {
var parcelHelpers = require("@parcel/transformer-js/src/esmodule-helpers.js");
parcelHelpers.defineInteropFlag(exports);
var _setPrototypeOfJs = require("./setPrototypeOf.js");
var _setPrototypeOfJsDefault = parcelHelpers.interopDefault(_setPrototypeOfJs);
function _inheritsLoose(subClass, superClass) {
    subClass.prototype = Object.create(superClass.prototype);
    subClass.prototype.constructor = subClass;
    _setPrototypeOfJsDefault.default(subClass, superClass);
}
exports.default = _inheritsLoose;

},{"./setPrototypeOf.js":"3XDFA","@parcel/transformer-js/src/esmodule-helpers.js":"gkKU3"}],"3XDFA":[function(require,module,exports) {
var parcelHelpers = require("@parcel/transformer-js/src/esmodule-helpers.js");
parcelHelpers.defineInteropFlag(exports);
function _setPrototypeOf(o1, p1) {
    _setPrototypeOf = Object.setPrototypeOf || function _setPrototypeOf(o, p) {
        o.__proto__ = p;
        return o;
    };
    return _setPrototypeOf(o1, p1);
}
exports.default = _setPrototypeOf;

},{"@parcel/transformer-js/src/esmodule-helpers.js":"gkKU3"}],"gkKU3":[function(require,module,exports) {
exports.interopDefault = function(a) {
    return a && a.__esModule ? a : {
        default: a
    };
};
exports.defineInteropFlag = function(a) {
    Object.defineProperty(a, '__esModule', {
        value: true
    });
};
exports.exportAll = function(source, dest) {
    Object.keys(source).forEach(function(key) {
        if (key === 'default' || key === '__esModule' || dest.hasOwnProperty(key)) return;
        Object.defineProperty(dest, key, {
            enumerable: true,
            get: function() {
                return source[key];
            }
        });
    });
    return dest;
};
exports.export = function(dest, destName, get) {
    Object.defineProperty(dest, destName, {
        enumerable: true,
        get: get
    });
};

},{}],"7wKI2":[function(require,module,exports) {
var ReactIs = require('react-is');
// By explicitly using `prop-types` you are opting into new development behavior.
// http://fb.me/prop-types-in-prod
var throwOnDirectAccess = true;
module.exports = require('./factoryWithTypeCheckers')(ReactIs.isElement, throwOnDirectAccess);

},{"react-is":"7EuwB","./factoryWithTypeCheckers":"bBUgD"}],"7EuwB":[function(require,module,exports) {
'use strict';
module.exports = require('./cjs/react-is.development.js');

},{"./cjs/react-is.development.js":"5DsXl"}],"5DsXl":[function(require,module,exports) {
/** @license React v16.13.1
 * react-is.development.js
 *
 * Copyright (c) Facebook, Inc. and its affiliates.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE file in the root directory of this source tree.
 */ 'use strict';
(function() {
    // The Symbol used to tag the ReactElement-like types. If there is no native Symbol
    // nor polyfill, then a plain number is used for performance.
    var hasSymbol = typeof Symbol === 'function' && Symbol.for;
    var REACT_ELEMENT_TYPE = hasSymbol ? Symbol.for('react.element') : 0xeac7;
    var REACT_PORTAL_TYPE = hasSymbol ? Symbol.for('react.portal') : 0xeaca;
    var REACT_FRAGMENT_TYPE = hasSymbol ? Symbol.for('react.fragment') : 0xeacb;
    var REACT_STRICT_MODE_TYPE = hasSymbol ? Symbol.for('react.strict_mode') : 0xeacc;
    var REACT_PROFILER_TYPE = hasSymbol ? Symbol.for('react.profiler') : 0xead2;
    var REACT_PROVIDER_TYPE = hasSymbol ? Symbol.for('react.provider') : 0xeacd;
    var REACT_CONTEXT_TYPE = hasSymbol ? Symbol.for('react.context') : 0xeace; // TODO: We don't use AsyncMode or ConcurrentMode anymore. They were temporary
    // (unstable) APIs that have been removed. Can we remove the symbols?
    var REACT_ASYNC_MODE_TYPE = hasSymbol ? Symbol.for('react.async_mode') : 0xeacf;
    var REACT_CONCURRENT_MODE_TYPE = hasSymbol ? Symbol.for('react.concurrent_mode') : 0xeacf;
    var REACT_FORWARD_REF_TYPE = hasSymbol ? Symbol.for('react.forward_ref') : 0xead0;
    var REACT_SUSPENSE_TYPE = hasSymbol ? Symbol.for('react.suspense') : 0xead1;
    var REACT_SUSPENSE_LIST_TYPE = hasSymbol ? Symbol.for('react.suspense_list') : 0xead8;
    var REACT_MEMO_TYPE = hasSymbol ? Symbol.for('react.memo') : 0xead3;
    var REACT_LAZY_TYPE = hasSymbol ? Symbol.for('react.lazy') : 0xead4;
    var REACT_BLOCK_TYPE = hasSymbol ? Symbol.for('react.block') : 0xead9;
    var REACT_FUNDAMENTAL_TYPE = hasSymbol ? Symbol.for('react.fundamental') : 0xead5;
    var REACT_RESPONDER_TYPE = hasSymbol ? Symbol.for('react.responder') : 0xead6;
    var REACT_SCOPE_TYPE = hasSymbol ? Symbol.for('react.scope') : 0xead7;
    function isValidElementType(type) {
        return typeof type === 'string' || typeof type === 'function' || type === REACT_FRAGMENT_TYPE || type === REACT_CONCURRENT_MODE_TYPE || type === REACT_PROFILER_TYPE || type === REACT_STRICT_MODE_TYPE || type === REACT_SUSPENSE_TYPE || type === REACT_SUSPENSE_LIST_TYPE || typeof type === 'object' && type !== null && (type.$$typeof === REACT_LAZY_TYPE || type.$$typeof === REACT_MEMO_TYPE || type.$$typeof === REACT_PROVIDER_TYPE || type.$$typeof === REACT_CONTEXT_TYPE || type.$$typeof === REACT_FORWARD_REF_TYPE || type.$$typeof === REACT_FUNDAMENTAL_TYPE || type.$$typeof === REACT_RESPONDER_TYPE || type.$$typeof === REACT_SCOPE_TYPE || type.$$typeof === REACT_BLOCK_TYPE);
    }
    function typeOf(object) {
        if (typeof object === 'object' && object !== null) {
            var $$typeof = object.$$typeof;
            switch($$typeof){
                case REACT_ELEMENT_TYPE:
                    var type = object.type;
                    switch(type){
                        case REACT_ASYNC_MODE_TYPE:
                        case REACT_CONCURRENT_MODE_TYPE:
                        case REACT_FRAGMENT_TYPE:
                        case REACT_PROFILER_TYPE:
                        case REACT_STRICT_MODE_TYPE:
                        case REACT_SUSPENSE_TYPE:
                            return type;
                        default:
                            var $$typeofType = type && type.$$typeof;
                            switch($$typeofType){
                                case REACT_CONTEXT_TYPE:
                                case REACT_FORWARD_REF_TYPE:
                                case REACT_LAZY_TYPE:
                                case REACT_MEMO_TYPE:
                                case REACT_PROVIDER_TYPE:
                                    return $$typeofType;
                                default:
                                    return $$typeof;
                            }
                    }
                case REACT_PORTAL_TYPE:
                    return $$typeof;
            }
        }
        return undefined;
    } // AsyncMode is deprecated along with isAsyncMode
    var AsyncMode = REACT_ASYNC_MODE_TYPE;
    var ConcurrentMode = REACT_CONCURRENT_MODE_TYPE;
    var ContextConsumer = REACT_CONTEXT_TYPE;
    var ContextProvider = REACT_PROVIDER_TYPE;
    var Element = REACT_ELEMENT_TYPE;
    var ForwardRef = REACT_FORWARD_REF_TYPE;
    var Fragment = REACT_FRAGMENT_TYPE;
    var Lazy = REACT_LAZY_TYPE;
    var Memo = REACT_MEMO_TYPE;
    var Portal = REACT_PORTAL_TYPE;
    var Profiler = REACT_PROFILER_TYPE;
    var StrictMode = REACT_STRICT_MODE_TYPE;
    var Suspense = REACT_SUSPENSE_TYPE;
    var hasWarnedAboutDeprecatedIsAsyncMode = false; // AsyncMode should be deprecated
    function isAsyncMode(object) {
        if (!hasWarnedAboutDeprecatedIsAsyncMode) {
            hasWarnedAboutDeprecatedIsAsyncMode = true; // Using console['warn'] to evade Babel and ESLint
            console['warn']("The ReactIs.isAsyncMode() alias has been deprecated, and will be removed in React 17+. Update your code to use ReactIs.isConcurrentMode() instead. It has the exact same API.");
        }
        return isConcurrentMode(object) || typeOf(object) === REACT_ASYNC_MODE_TYPE;
    }
    function isConcurrentMode(object) {
        return typeOf(object) === REACT_CONCURRENT_MODE_TYPE;
    }
    function isContextConsumer(object) {
        return typeOf(object) === REACT_CONTEXT_TYPE;
    }
    function isContextProvider(object) {
        return typeOf(object) === REACT_PROVIDER_TYPE;
    }
    function isElement(object) {
        return typeof object === 'object' && object !== null && object.$$typeof === REACT_ELEMENT_TYPE;
    }
    function isForwardRef(object) {
        return typeOf(object) === REACT_FORWARD_REF_TYPE;
    }
    function isFragment(object) {
        return typeOf(object) === REACT_FRAGMENT_TYPE;
    }
    function isLazy(object) {
        return typeOf(object) === REACT_LAZY_TYPE;
    }
    function isMemo(object) {
        return typeOf(object) === REACT_MEMO_TYPE;
    }
    function isPortal(object) {
        return typeOf(object) === REACT_PORTAL_TYPE;
    }
    function isProfiler(object) {
        return typeOf(object) === REACT_PROFILER_TYPE;
    }
    function isStrictMode(object) {
        return typeOf(object) === REACT_STRICT_MODE_TYPE;
    }
    function isSuspense(object) {
        return typeOf(object) === REACT_SUSPENSE_TYPE;
    }
    exports.AsyncMode = AsyncMode;
    exports.ConcurrentMode = ConcurrentMode;
    exports.ContextConsumer = ContextConsumer;
    exports.ContextProvider = ContextProvider;
    exports.Element = Element;
    exports.ForwardRef = ForwardRef;
    exports.Fragment = Fragment;
    exports.Lazy = Lazy;
    exports.Memo = Memo;
    exports.Portal = Portal;
    exports.Profiler = Profiler;
    exports.StrictMode = StrictMode;
    exports.Suspense = Suspense;
    exports.isAsyncMode = isAsyncMode;
    exports.isConcurrentMode = isConcurrentMode;
    exports.isContextConsumer = isContextConsumer;
    exports.isContextProvider = isContextProvider;
    exports.isElement = isElement;
    exports.isForwardRef = isForwardRef;
    exports.isFragment = isFragment;
    exports.isLazy = isLazy;
    exports.isMemo = isMemo;
    exports.isPortal = isPortal;
    exports.isProfiler = isProfiler;
    exports.isStrictMode = isStrictMode;
    exports.isSuspense = isSuspense;
    exports.isValidElementType = isValidElementType;
    exports.typeOf = typeOf;
})();

},{}],"bBUgD":[function(require,module,exports) {
/**
 * Copyright (c) 2013-present, Facebook, Inc.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE file in the root directory of this source tree.
 */ 'use strict';
var ReactIs = require('react-is');
var assign = require('object-assign');
var ReactPropTypesSecret = require('./lib/ReactPropTypesSecret');
var has = require('./lib/has');
var checkPropTypes = require('./checkPropTypes');
var printWarning = function() {};
printWarning = function(text) {
    var message = 'Warning: ' + text;
    if (typeof console !== 'undefined') console.error(message);
    try {
        // --- Welcome to debugging React ---
        // This error was thrown as a convenience so that you can use this stack
        // to find the callsite that caused this warning to fire.
        throw new Error(message);
    } catch (x) {}
};
function emptyFunctionThatReturnsNull() {
    return null;
}
module.exports = function(isValidElement, throwOnDirectAccess) {
    /* global Symbol */ var ITERATOR_SYMBOL = typeof Symbol === 'function' && Symbol.iterator;
    var FAUX_ITERATOR_SYMBOL = '@@iterator'; // Before Symbol spec.
    /**
   * Returns the iterator method function contained on the iterable object.
   *
   * Be sure to invoke the function with the iterable as context:
   *
   *     var iteratorFn = getIteratorFn(myIterable);
   *     if (iteratorFn) {
   *       var iterator = iteratorFn.call(myIterable);
   *       ...
   *     }
   *
   * @param {?object} maybeIterable
   * @return {?function}
   */ function getIteratorFn(maybeIterable) {
        var iteratorFn = maybeIterable && (ITERATOR_SYMBOL && maybeIterable[ITERATOR_SYMBOL] || maybeIterable[FAUX_ITERATOR_SYMBOL]);
        if (typeof iteratorFn === 'function') return iteratorFn;
    }
    /**
   * Collection of methods that allow declaration and validation of props that are
   * supplied to React components. Example usage:
   *
   *   var Props = require('ReactPropTypes');
   *   var MyArticle = React.createClass({
   *     propTypes: {
   *       // An optional string prop named "description".
   *       description: Props.string,
   *
   *       // A required enum prop named "category".
   *       category: Props.oneOf(['News','Photos']).isRequired,
   *
   *       // A prop named "dialog" that requires an instance of Dialog.
   *       dialog: Props.instanceOf(Dialog).isRequired
   *     },
   *     render: function() { ... }
   *   });
   *
   * A more formal specification of how these methods are used:
   *
   *   type := array|bool|func|object|number|string|oneOf([...])|instanceOf(...)
   *   decl := ReactPropTypes.{type}(.isRequired)?
   *
   * Each and every declaration produces a function with the same signature. This
   * allows the creation of custom validation functions. For example:
   *
   *  var MyLink = React.createClass({
   *    propTypes: {
   *      // An optional string or URI prop named "href".
   *      href: function(props, propName, componentName) {
   *        var propValue = props[propName];
   *        if (propValue != null && typeof propValue !== 'string' &&
   *            !(propValue instanceof URI)) {
   *          return new Error(
   *            'Expected a string or an URI for ' + propName + ' in ' +
   *            componentName
   *          );
   *        }
   *      }
   *    },
   *    render: function() {...}
   *  });
   *
   * @internal
   */ var ANONYMOUS = '<<anonymous>>';
    // Important!
    // Keep this list in sync with production version in `./factoryWithThrowingShims.js`.
    var ReactPropTypes = {
        array: createPrimitiveTypeChecker('array'),
        bigint: createPrimitiveTypeChecker('bigint'),
        bool: createPrimitiveTypeChecker('boolean'),
        func: createPrimitiveTypeChecker('function'),
        number: createPrimitiveTypeChecker('number'),
        object: createPrimitiveTypeChecker('object'),
        string: createPrimitiveTypeChecker('string'),
        symbol: createPrimitiveTypeChecker('symbol'),
        any: createAnyTypeChecker(),
        arrayOf: createArrayOfTypeChecker,
        element: createElementTypeChecker(),
        elementType: createElementTypeTypeChecker(),
        instanceOf: createInstanceTypeChecker,
        node: createNodeChecker(),
        objectOf: createObjectOfTypeChecker,
        oneOf: createEnumTypeChecker,
        oneOfType: createUnionTypeChecker,
        shape: createShapeTypeChecker,
        exact: createStrictShapeTypeChecker
    };
    /**
   * inlined Object.is polyfill to avoid requiring consumers ship their own
   * https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Object/is
   */ /*eslint-disable no-self-compare*/ function is(x, y) {
        // SameValue algorithm
        if (x === y) // Steps 1-5, 7-10
        // Steps 6.b-6.e: +0 != -0
        return x !== 0 || 1 / x === 1 / y;
        else // Step 6.a: NaN == NaN
        return x !== x && y !== y;
    }
    /*eslint-enable no-self-compare*/ /**
   * We use an Error-like object for backward compatibility as people may call
   * PropTypes directly and inspect their output. However, we don't use real
   * Errors anymore. We don't inspect their stack anyway, and creating them
   * is prohibitively expensive if they are created too often, such as what
   * happens in oneOfType() for any type before the one that matched.
   */ function PropTypeError(message, data) {
        this.message = message;
        this.data = data && typeof data === 'object' ? data : {};
        this.stack = '';
    }
    // Make `instanceof Error` still work for returned errors.
    PropTypeError.prototype = Error.prototype;
    function createChainableTypeChecker(validate) {
        var manualPropTypeCallCache = {};
        var manualPropTypeWarningCount = 0;
        function checkType(isRequired, props, propName, componentName, location, propFullName, secret) {
            componentName = componentName || ANONYMOUS;
            propFullName = propFullName || propName;
            if (secret !== ReactPropTypesSecret) {
                if (throwOnDirectAccess) {
                    // New behavior only for users of `prop-types` package
                    var err = new Error("Calling PropTypes validators directly is not supported by the `prop-types` package. Use `PropTypes.checkPropTypes()` to call them. Read more at http://fb.me/use-check-prop-types");
                    err.name = 'Invariant Violation';
                    throw err;
                } else if (typeof console !== 'undefined') {
                    // Old behavior for people using React.PropTypes
                    var cacheKey = componentName + ':' + propName;
                    if (!manualPropTypeCallCache[cacheKey] && // Avoid spamming the console because they are often not actionable except for lib authors
                    manualPropTypeWarningCount < 3) {
                        printWarning("You are manually calling a React.PropTypes validation function for the `" + propFullName + '` prop on `' + componentName + '`. This is deprecated ' + 'and will throw in the standalone `prop-types` package. ' + 'You may be seeing this warning due to a third-party PropTypes ' + 'library. See https://fb.me/react-warning-dont-call-proptypes ' + 'for details.');
                        manualPropTypeCallCache[cacheKey] = true;
                        manualPropTypeWarningCount++;
                    }
                }
            }
            if (props[propName] == null) {
                if (isRequired) {
                    if (props[propName] === null) return new PropTypeError('The ' + location + ' `' + propFullName + '` is marked as required ' + ('in `' + componentName + '`, but its value is `null`.'));
                    return new PropTypeError('The ' + location + ' `' + propFullName + '` is marked as required in ' + ('`' + componentName + '`, but its value is `undefined`.'));
                }
                return null;
            } else return validate(props, propName, componentName, location, propFullName);
        }
        var chainedCheckType = checkType.bind(null, false);
        chainedCheckType.isRequired = checkType.bind(null, true);
        return chainedCheckType;
    }
    function createPrimitiveTypeChecker(expectedType) {
        function validate(props, propName, componentName, location, propFullName, secret) {
            var propValue = props[propName];
            var propType = getPropType(propValue);
            if (propType !== expectedType) {
                // `propValue` being instance of, say, date/regexp, pass the 'object'
                // check, but we can offer a more precise error message here rather than
                // 'of type `object`'.
                var preciseType = getPreciseType(propValue);
                return new PropTypeError('Invalid ' + location + ' `' + propFullName + '` of type ' + ('`' + preciseType + '` supplied to `' + componentName + '`, expected ') + ('`' + expectedType + '`.'), {
                    expectedType: expectedType
                });
            }
            return null;
        }
        return createChainableTypeChecker(validate);
    }
    function createAnyTypeChecker() {
        return createChainableTypeChecker(emptyFunctionThatReturnsNull);
    }
    function createArrayOfTypeChecker(typeChecker) {
        function validate(props, propName, componentName, location, propFullName) {
            if (typeof typeChecker !== 'function') return new PropTypeError('Property `' + propFullName + '` of component `' + componentName + '` has invalid PropType notation inside arrayOf.');
            var propValue = props[propName];
            if (!Array.isArray(propValue)) {
                var propType = getPropType(propValue);
                return new PropTypeError('Invalid ' + location + ' `' + propFullName + '` of type ' + ('`' + propType + '` supplied to `' + componentName + '`, expected an array.'));
            }
            for(var i = 0; i < propValue.length; i++){
                var error = typeChecker(propValue, i, componentName, location, propFullName + '[' + i + ']', ReactPropTypesSecret);
                if (error instanceof Error) return error;
            }
            return null;
        }
        return createChainableTypeChecker(validate);
    }
    function createElementTypeChecker() {
        function validate(props, propName, componentName, location, propFullName) {
            var propValue = props[propName];
            if (!isValidElement(propValue)) {
                var propType = getPropType(propValue);
                return new PropTypeError('Invalid ' + location + ' `' + propFullName + '` of type ' + ('`' + propType + '` supplied to `' + componentName + '`, expected a single ReactElement.'));
            }
            return null;
        }
        return createChainableTypeChecker(validate);
    }
    function createElementTypeTypeChecker() {
        function validate(props, propName, componentName, location, propFullName) {
            var propValue = props[propName];
            if (!ReactIs.isValidElementType(propValue)) {
                var propType = getPropType(propValue);
                return new PropTypeError('Invalid ' + location + ' `' + propFullName + '` of type ' + ('`' + propType + '` supplied to `' + componentName + '`, expected a single ReactElement type.'));
            }
            return null;
        }
        return createChainableTypeChecker(validate);
    }
    function createInstanceTypeChecker(expectedClass) {
        function validate(props, propName, componentName, location, propFullName) {
            if (!(props[propName] instanceof expectedClass)) {
                var expectedClassName = expectedClass.name || ANONYMOUS;
                var actualClassName = getClassName(props[propName]);
                return new PropTypeError('Invalid ' + location + ' `' + propFullName + '` of type ' + ('`' + actualClassName + '` supplied to `' + componentName + '`, expected ') + ('instance of `' + expectedClassName + '`.'));
            }
            return null;
        }
        return createChainableTypeChecker(validate);
    }
    function createEnumTypeChecker(expectedValues) {
        if (!Array.isArray(expectedValues)) {
            {
                if (arguments.length > 1) printWarning('Invalid arguments supplied to oneOf, expected an array, got ' + arguments.length + ' arguments. ' + 'A common mistake is to write oneOf(x, y, z) instead of oneOf([x, y, z]).');
                else printWarning('Invalid argument supplied to oneOf, expected an array.');
            }
            return emptyFunctionThatReturnsNull;
        }
        function validate(props, propName, componentName, location, propFullName) {
            var propValue = props[propName];
            for(var i = 0; i < expectedValues.length; i++){
                if (is(propValue, expectedValues[i])) return null;
            }
            var valuesString = JSON.stringify(expectedValues, function replacer(key, value) {
                var type = getPreciseType(value);
                if (type === 'symbol') return String(value);
                return value;
            });
            return new PropTypeError('Invalid ' + location + ' `' + propFullName + '` of value `' + String(propValue) + '` ' + ('supplied to `' + componentName + '`, expected one of ' + valuesString + '.'));
        }
        return createChainableTypeChecker(validate);
    }
    function createObjectOfTypeChecker(typeChecker) {
        function validate(props, propName, componentName, location, propFullName) {
            if (typeof typeChecker !== 'function') return new PropTypeError('Property `' + propFullName + '` of component `' + componentName + '` has invalid PropType notation inside objectOf.');
            var propValue = props[propName];
            var propType = getPropType(propValue);
            if (propType !== 'object') return new PropTypeError('Invalid ' + location + ' `' + propFullName + '` of type ' + ('`' + propType + '` supplied to `' + componentName + '`, expected an object.'));
            for(var key in propValue)if (has(propValue, key)) {
                var error = typeChecker(propValue, key, componentName, location, propFullName + '.' + key, ReactPropTypesSecret);
                if (error instanceof Error) return error;
            }
            return null;
        }
        return createChainableTypeChecker(validate);
    }
    function createUnionTypeChecker(arrayOfTypeCheckers) {
        if (!Array.isArray(arrayOfTypeCheckers)) {
            printWarning('Invalid argument supplied to oneOfType, expected an instance of array.');
            return emptyFunctionThatReturnsNull;
        }
        for(var i1 = 0; i1 < arrayOfTypeCheckers.length; i1++){
            var checker = arrayOfTypeCheckers[i1];
            if (typeof checker !== 'function') {
                printWarning("Invalid argument supplied to oneOfType. Expected an array of check functions, but received " + getPostfixForTypeWarning(checker) + ' at index ' + i1 + '.');
                return emptyFunctionThatReturnsNull;
            }
        }
        function validate(props, propName, componentName, location, propFullName) {
            var expectedTypes = [];
            for(var i = 0; i < arrayOfTypeCheckers.length; i++){
                var checker = arrayOfTypeCheckers[i];
                var checkerResult = checker(props, propName, componentName, location, propFullName, ReactPropTypesSecret);
                if (checkerResult == null) return null;
                if (checkerResult.data && has(checkerResult.data, 'expectedType')) expectedTypes.push(checkerResult.data.expectedType);
            }
            var expectedTypesMessage = expectedTypes.length > 0 ? ', expected one of type [' + expectedTypes.join(', ') + ']' : '';
            return new PropTypeError('Invalid ' + location + ' `' + propFullName + '` supplied to ' + ('`' + componentName + '`' + expectedTypesMessage + '.'));
        }
        return createChainableTypeChecker(validate);
    }
    function createNodeChecker() {
        function validate(props, propName, componentName, location, propFullName) {
            if (!isNode(props[propName])) return new PropTypeError('Invalid ' + location + ' `' + propFullName + '` supplied to ' + ('`' + componentName + '`, expected a ReactNode.'));
            return null;
        }
        return createChainableTypeChecker(validate);
    }
    function invalidValidatorError(componentName, location, propFullName, key, type) {
        return new PropTypeError((componentName || 'React class') + ': ' + location + ' type `' + propFullName + '.' + key + '` is invalid; ' + 'it must be a function, usually from the `prop-types` package, but received `' + type + '`.');
    }
    function createShapeTypeChecker(shapeTypes) {
        function validate(props, propName, componentName, location, propFullName) {
            var propValue = props[propName];
            var propType = getPropType(propValue);
            if (propType !== 'object') return new PropTypeError('Invalid ' + location + ' `' + propFullName + '` of type `' + propType + '` ' + ('supplied to `' + componentName + '`, expected `object`.'));
            for(var key in shapeTypes){
                var checker = shapeTypes[key];
                if (typeof checker !== 'function') return invalidValidatorError(componentName, location, propFullName, key, getPreciseType(checker));
                var error = checker(propValue, key, componentName, location, propFullName + '.' + key, ReactPropTypesSecret);
                if (error) return error;
            }
            return null;
        }
        return createChainableTypeChecker(validate);
    }
    function createStrictShapeTypeChecker(shapeTypes) {
        function validate(props, propName, componentName, location, propFullName) {
            var propValue = props[propName];
            var propType = getPropType(propValue);
            if (propType !== 'object') return new PropTypeError('Invalid ' + location + ' `' + propFullName + '` of type `' + propType + '` ' + ('supplied to `' + componentName + '`, expected `object`.'));
            // We need to check all keys in case some are required but missing from props.
            var allKeys = assign({}, props[propName], shapeTypes);
            for(var key in allKeys){
                var checker = shapeTypes[key];
                if (has(shapeTypes, key) && typeof checker !== 'function') return invalidValidatorError(componentName, location, propFullName, key, getPreciseType(checker));
                if (!checker) return new PropTypeError('Invalid ' + location + ' `' + propFullName + '` key `' + key + '` supplied to `' + componentName + '`.' + '\nBad object: ' + JSON.stringify(props[propName], null, '  ') + '\nValid keys: ' + JSON.stringify(Object.keys(shapeTypes), null, '  '));
                var error = checker(propValue, key, componentName, location, propFullName + '.' + key, ReactPropTypesSecret);
                if (error) return error;
            }
            return null;
        }
        return createChainableTypeChecker(validate);
    }
    function isNode(propValue) {
        switch(typeof propValue){
            case 'number':
            case 'string':
            case 'undefined':
                return true;
            case 'boolean':
                return !propValue;
            case 'object':
                if (Array.isArray(propValue)) return propValue.every(isNode);
                if (propValue === null || isValidElement(propValue)) return true;
                var iteratorFn = getIteratorFn(propValue);
                if (iteratorFn) {
                    var iterator = iteratorFn.call(propValue);
                    var step;
                    if (iteratorFn !== propValue.entries) while(!(step = iterator.next()).done){
                        if (!isNode(step.value)) return false;
                    }
                    else // Iterator will provide entry [k,v] tuples rather than values.
                    while(!(step = iterator.next()).done){
                        var entry = step.value;
                        if (entry) {
                            if (!isNode(entry[1])) return false;
                        }
                    }
                } else return false;
                return true;
            default:
                return false;
        }
    }
    function isSymbol(propType, propValue) {
        // Native Symbol.
        if (propType === 'symbol') return true;
        // falsy value can't be a Symbol
        if (!propValue) return false;
        // 19.4.3.5 Symbol.prototype[@@toStringTag] === 'Symbol'
        if (propValue['@@toStringTag'] === 'Symbol') return true;
        // Fallback for non-spec compliant Symbols which are polyfilled.
        if (typeof Symbol === 'function' && propValue instanceof Symbol) return true;
        return false;
    }
    // Equivalent of `typeof` but with special handling for array and regexp.
    function getPropType(propValue) {
        var propType = typeof propValue;
        if (Array.isArray(propValue)) return 'array';
        if (propValue instanceof RegExp) // Old webkits (at least until Android 4.0) return 'function' rather than
        // 'object' for typeof a RegExp. We'll normalize this here so that /bla/
        // passes PropTypes.object.
        return 'object';
        if (isSymbol(propType, propValue)) return 'symbol';
        return propType;
    }
    // This handles more types than `getPropType`. Only used for error messages.
    // See `createPrimitiveTypeChecker`.
    function getPreciseType(propValue) {
        if (typeof propValue === 'undefined' || propValue === null) return '' + propValue;
        var propType = getPropType(propValue);
        if (propType === 'object') {
            if (propValue instanceof Date) return 'date';
            else if (propValue instanceof RegExp) return 'regexp';
        }
        return propType;
    }
    // Returns a string that is postfixed to a warning about an invalid type.
    // For example, "undefined" or "of type array"
    function getPostfixForTypeWarning(value) {
        var type = getPreciseType(value);
        switch(type){
            case 'array':
            case 'object':
                return 'an ' + type;
            case 'boolean':
            case 'date':
            case 'regexp':
                return 'a ' + type;
            default:
                return type;
        }
    }
    // Returns class name of the object, if any.
    function getClassName(propValue) {
        if (!propValue.constructor || !propValue.constructor.name) return ANONYMOUS;
        return propValue.constructor.name;
    }
    ReactPropTypes.checkPropTypes = checkPropTypes;
    ReactPropTypes.resetWarningCache = checkPropTypes.resetWarningCache;
    ReactPropTypes.PropTypes = ReactPropTypes;
    return ReactPropTypes;
};

},{"react-is":"7EuwB","object-assign":"7OXxh","./lib/ReactPropTypesSecret":"jZTZJ","./lib/has":"fqKuf","./checkPropTypes":"5VwyJ"}],"7OXxh":[function(require,module,exports) {
/*
object-assign
(c) Sindre Sorhus
@license MIT
*/ 'use strict';
/* eslint-disable no-unused-vars */ var getOwnPropertySymbols = Object.getOwnPropertySymbols;
var hasOwnProperty = Object.prototype.hasOwnProperty;
var propIsEnumerable = Object.prototype.propertyIsEnumerable;
function toObject(val) {
    if (val === null || val === undefined) throw new TypeError('Object.assign cannot be called with null or undefined');
    return Object(val);
}
function shouldUseNative() {
    try {
        if (!Object.assign) return false;
        // Detect buggy property enumeration order in older V8 versions.
        // https://bugs.chromium.org/p/v8/issues/detail?id=4118
        var test1 = new String('abc'); // eslint-disable-line no-new-wrappers
        test1[5] = 'de';
        if (Object.getOwnPropertyNames(test1)[0] === '5') return false;
        // https://bugs.chromium.org/p/v8/issues/detail?id=3056
        var test2 = {};
        for(var i = 0; i < 10; i++)test2['_' + String.fromCharCode(i)] = i;
        var order2 = Object.getOwnPropertyNames(test2).map(function(n) {
            return test2[n];
        });
        if (order2.join('') !== '0123456789') return false;
        // https://bugs.chromium.org/p/v8/issues/detail?id=3056
        var test3 = {};
        'abcdefghijklmnopqrst'.split('').forEach(function(letter) {
            test3[letter] = letter;
        });
        if (Object.keys(Object.assign({}, test3)).join('') !== 'abcdefghijklmnopqrst') return false;
        return true;
    } catch (err) {
        // We don't expect any of the above to throw, but better to be safe.
        return false;
    }
}
module.exports = shouldUseNative() ? Object.assign : function(target, source) {
    var from;
    var to = toObject(target);
    var symbols;
    for(var s = 1; s < arguments.length; s++){
        from = Object(arguments[s]);
        for(var key in from)if (hasOwnProperty.call(from, key)) to[key] = from[key];
        if (getOwnPropertySymbols) {
            symbols = getOwnPropertySymbols(from);
            for(var i = 0; i < symbols.length; i++)if (propIsEnumerable.call(from, symbols[i])) to[symbols[i]] = from[symbols[i]];
        }
    }
    return to;
};

},{}],"jZTZJ":[function(require,module,exports) {
/**
 * Copyright (c) 2013-present, Facebook, Inc.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE file in the root directory of this source tree.
 */ 'use strict';
var ReactPropTypesSecret = 'SECRET_DO_NOT_PASS_THIS_OR_YOU_WILL_BE_FIRED';
module.exports = ReactPropTypesSecret;

},{}],"fqKuf":[function(require,module,exports) {
module.exports = Function.call.bind(Object.prototype.hasOwnProperty);

},{}],"5VwyJ":[function(require,module,exports) {
/**
 * Copyright (c) 2013-present, Facebook, Inc.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE file in the root directory of this source tree.
 */ 'use strict';
var printWarning = function() {};
var ReactPropTypesSecret = require('./lib/ReactPropTypesSecret');
var loggedTypeFailures = {};
var has = require('./lib/has');
printWarning = function(text) {
    var message = 'Warning: ' + text;
    if (typeof console !== 'undefined') console.error(message);
    try {
        // --- Welcome to debugging React ---
        // This error was thrown as a convenience so that you can use this stack
        // to find the callsite that caused this warning to fire.
        throw new Error(message);
    } catch (x) {}
};
/**
 * Assert that the values match with the type specs.
 * Error messages are memorized and will only be shown once.
 *
 * @param {object} typeSpecs Map of name to a ReactPropType
 * @param {object} values Runtime values that need to be type-checked
 * @param {string} location e.g. "prop", "context", "child context"
 * @param {string} componentName Name of the component for error messages.
 * @param {?Function} getStack Returns the component stack.
 * @private
 */ function checkPropTypes(typeSpecs, values, location, componentName, getStack) {
    for(var typeSpecName in typeSpecs)if (has(typeSpecs, typeSpecName)) {
        var error;
        // Prop type validation may throw. In case they do, we don't want to
        // fail the render phase where it didn't fail before. So we log it.
        // After these have been cleaned up, we'll let them throw.
        try {
            // This is intentionally an invariant that gets caught. It's the same
            // behavior as without this statement except with a better message.
            if (typeof typeSpecs[typeSpecName] !== 'function') {
                var err = Error((componentName || 'React class') + ': ' + location + ' type `' + typeSpecName + '` is invalid; ' + 'it must be a function, usually from the `prop-types` package, but received `' + typeof typeSpecs[typeSpecName] + '`.' + 'This often happens because of typos such as `PropTypes.function` instead of `PropTypes.func`.');
                err.name = 'Invariant Violation';
                throw err;
            }
            error = typeSpecs[typeSpecName](values, typeSpecName, componentName, location, null, ReactPropTypesSecret);
        } catch (ex) {
            error = ex;
        }
        if (error && !(error instanceof Error)) printWarning((componentName || 'React class') + ': type specification of ' + location + ' `' + typeSpecName + '` is invalid; the type checker ' + 'function must return `null` or an `Error` but returned a ' + typeof error + '. ' + 'You may have forgotten to pass an argument to the type checker ' + 'creator (arrayOf, instanceOf, objectOf, oneOf, oneOfType, and ' + 'shape all require an argument).');
        if (error instanceof Error && !(error.message in loggedTypeFailures)) {
            // Only monitor this failure once because there tends to be a lot of the
            // same error.
            loggedTypeFailures[error.message] = true;
            var stack = getStack ? getStack() : '';
            printWarning('Failed ' + location + ' type: ' + error.message + (stack != null ? stack : ''));
        }
    }
}
/**
 * Resets warning cache when testing.
 *
 * @private
 */ checkPropTypes.resetWarningCache = function() {
    loggedTypeFailures = {};
};
module.exports = checkPropTypes;

},{"./lib/ReactPropTypesSecret":"jZTZJ","./lib/has":"fqKuf"}],"6yWeD":[function(require,module,exports) {
var parcelHelpers = require("@parcel/transformer-js/src/esmodule-helpers.js");
parcelHelpers.defineInteropFlag(exports);
parcelHelpers.export(exports, "createBrowserHistory", ()=>createBrowserHistory
);
parcelHelpers.export(exports, "createHashHistory", ()=>createHashHistory
);
parcelHelpers.export(exports, "createMemoryHistory", ()=>createMemoryHistory
);
parcelHelpers.export(exports, "createLocation", ()=>createLocation
);
parcelHelpers.export(exports, "locationsAreEqual", ()=>locationsAreEqual
);
parcelHelpers.export(exports, "parsePath", ()=>parsePath
);
parcelHelpers.export(exports, "createPath", ()=>createPath
);
var _extends = require("@babel/runtime/helpers/esm/extends");
var _extendsDefault = parcelHelpers.interopDefault(_extends);
var _resolvePathname = require("resolve-pathname");
var _resolvePathnameDefault = parcelHelpers.interopDefault(_resolvePathname);
var _valueEqual = require("value-equal");
var _valueEqualDefault = parcelHelpers.interopDefault(_valueEqual);
var _tinyWarning = require("tiny-warning");
var _tinyWarningDefault = parcelHelpers.interopDefault(_tinyWarning);
var _tinyInvariant = require("tiny-invariant");
var _tinyInvariantDefault = parcelHelpers.interopDefault(_tinyInvariant);
function addLeadingSlash(path) {
    return path.charAt(0) === '/' ? path : '/' + path;
}
function stripLeadingSlash(path) {
    return path.charAt(0) === '/' ? path.substr(1) : path;
}
function hasBasename(path, prefix) {
    return path.toLowerCase().indexOf(prefix.toLowerCase()) === 0 && '/?#'.indexOf(path.charAt(prefix.length)) !== -1;
}
function stripBasename(path, prefix) {
    return hasBasename(path, prefix) ? path.substr(prefix.length) : path;
}
function stripTrailingSlash(path) {
    return path.charAt(path.length - 1) === '/' ? path.slice(0, -1) : path;
}
function parsePath(path) {
    var pathname = path || '/';
    var search = '';
    var hash = '';
    var hashIndex = pathname.indexOf('#');
    if (hashIndex !== -1) {
        hash = pathname.substr(hashIndex);
        pathname = pathname.substr(0, hashIndex);
    }
    var searchIndex = pathname.indexOf('?');
    if (searchIndex !== -1) {
        search = pathname.substr(searchIndex);
        pathname = pathname.substr(0, searchIndex);
    }
    return {
        pathname: pathname,
        search: search === '?' ? '' : search,
        hash: hash === '#' ? '' : hash
    };
}
function createPath(location) {
    var pathname = location.pathname, search = location.search, hash = location.hash;
    var path = pathname || '/';
    if (search && search !== '?') path += search.charAt(0) === '?' ? search : "?" + search;
    if (hash && hash !== '#') path += hash.charAt(0) === '#' ? hash : "#" + hash;
    return path;
}
function createLocation(path, state, key, currentLocation) {
    var location;
    if (typeof path === 'string') {
        // Two-arg form: push(path, state)
        location = parsePath(path);
        location.state = state;
    } else {
        // One-arg form: push(location)
        location = _extendsDefault.default({}, path);
        if (location.pathname === undefined) location.pathname = '';
        if (location.search) {
            if (location.search.charAt(0) !== '?') location.search = '?' + location.search;
        } else location.search = '';
        if (location.hash) {
            if (location.hash.charAt(0) !== '#') location.hash = '#' + location.hash;
        } else location.hash = '';
        if (state !== undefined && location.state === undefined) location.state = state;
    }
    try {
        location.pathname = decodeURI(location.pathname);
    } catch (e) {
        if (e instanceof URIError) throw new URIError('Pathname "' + location.pathname + '" could not be decoded. ' + 'This is likely caused by an invalid percent-encoding.');
        else throw e;
    }
    if (key) location.key = key;
    if (currentLocation) {
        // Resolve incomplete/relative pathname relative to current location.
        if (!location.pathname) location.pathname = currentLocation.pathname;
        else if (location.pathname.charAt(0) !== '/') location.pathname = _resolvePathnameDefault.default(location.pathname, currentLocation.pathname);
    } else // When there is no prior location and pathname is empty, set it to /
    if (!location.pathname) location.pathname = '/';
    return location;
}
function locationsAreEqual(a, b) {
    return a.pathname === b.pathname && a.search === b.search && a.hash === b.hash && a.key === b.key && _valueEqualDefault.default(a.state, b.state);
}
function createTransitionManager() {
    var prompt = null;
    function setPrompt(nextPrompt) {
        _tinyWarningDefault.default(prompt == null, 'A history supports only one prompt at a time');
        prompt = nextPrompt;
        return function() {
            if (prompt === nextPrompt) prompt = null;
        };
    }
    function confirmTransitionTo(location, action, getUserConfirmation, callback) {
        // TODO: If another transition starts while we're still confirming
        // the previous one, we may end up in a weird state. Figure out the
        // best way to handle this.
        if (prompt != null) {
            var result = typeof prompt === 'function' ? prompt(location, action) : prompt;
            if (typeof result === 'string') {
                if (typeof getUserConfirmation === 'function') getUserConfirmation(result, callback);
                else {
                    _tinyWarningDefault.default(false, 'A history needs a getUserConfirmation function in order to use a prompt message');
                    callback(true);
                }
            } else // Return false from a transition hook to cancel the transition.
            callback(result !== false);
        } else callback(true);
    }
    var listeners = [];
    function appendListener(fn) {
        var isActive = true;
        function listener() {
            if (isActive) fn.apply(void 0, arguments);
        }
        listeners.push(listener);
        return function() {
            isActive = false;
            listeners = listeners.filter(function(item) {
                return item !== listener;
            });
        };
    }
    function notifyListeners() {
        for(var _len = arguments.length, args = new Array(_len), _key = 0; _key < _len; _key++)args[_key] = arguments[_key];
        listeners.forEach(function(listener) {
            return listener.apply(void 0, args);
        });
    }
    return {
        setPrompt: setPrompt,
        confirmTransitionTo: confirmTransitionTo,
        appendListener: appendListener,
        notifyListeners: notifyListeners
    };
}
var canUseDOM = !!(typeof window !== 'undefined' && window.document && window.document.createElement);
function getConfirmation(message, callback) {
    callback(window.confirm(message)); // eslint-disable-line no-alert
}
/**
 * Returns true if the HTML5 history API is supported. Taken from Modernizr.
 *
 * https://github.com/Modernizr/Modernizr/blob/master/LICENSE
 * https://github.com/Modernizr/Modernizr/blob/master/feature-detects/history.js
 * changed to avoid false negatives for Windows Phones: https://github.com/reactjs/react-router/issues/586
 */ function supportsHistory() {
    var ua = window.navigator.userAgent;
    if ((ua.indexOf('Android 2.') !== -1 || ua.indexOf('Android 4.0') !== -1) && ua.indexOf('Mobile Safari') !== -1 && ua.indexOf('Chrome') === -1 && ua.indexOf('Windows Phone') === -1) return false;
    return window.history && 'pushState' in window.history;
}
/**
 * Returns true if browser fires popstate on hash change.
 * IE10 and IE11 do not.
 */ function supportsPopStateOnHashChange() {
    return window.navigator.userAgent.indexOf('Trident') === -1;
}
/**
 * Returns false if using go(n) with hash history causes a full page reload.
 */ function supportsGoWithoutReloadUsingHash() {
    return window.navigator.userAgent.indexOf('Firefox') === -1;
}
/**
 * Returns true if a given popstate event is an extraneous WebKit event.
 * Accounts for the fact that Chrome on iOS fires real popstate events
 * containing undefined state when pressing the back button.
 */ function isExtraneousPopstateEvent(event) {
    return event.state === undefined && navigator.userAgent.indexOf('CriOS') === -1;
}
var PopStateEvent = 'popstate';
var HashChangeEvent = 'hashchange';
function getHistoryState() {
    try {
        return window.history.state || {};
    } catch (e) {
        // IE 11 sometimes throws when accessing window.history.state
        // See https://github.com/ReactTraining/history/pull/289
        return {};
    }
}
/**
 * Creates a history object that uses the HTML5 history API including
 * pushState, replaceState, and the popstate event.
 */ function createBrowserHistory(props) {
    if (props === void 0) props = {};
    !canUseDOM && _tinyInvariantDefault.default(false, 'Browser history needs a DOM');
    var globalHistory = window.history;
    var canUseHistory = supportsHistory();
    var needsHashChangeListener = !supportsPopStateOnHashChange();
    var _props = props, _props$forceRefresh = _props.forceRefresh, forceRefresh = _props$forceRefresh === void 0 ? false : _props$forceRefresh, _props$getUserConfirm = _props.getUserConfirmation, getUserConfirmation = _props$getUserConfirm === void 0 ? getConfirmation : _props$getUserConfirm, _props$keyLength = _props.keyLength, keyLength = _props$keyLength === void 0 ? 6 : _props$keyLength;
    var basename = props.basename ? stripTrailingSlash(addLeadingSlash(props.basename)) : '';
    function getDOMLocation(historyState) {
        var _ref = historyState || {}, key = _ref.key, state = _ref.state;
        var _window$location = window.location, pathname = _window$location.pathname, search = _window$location.search, hash = _window$location.hash;
        var path = pathname + search + hash;
        _tinyWarningDefault.default(!basename || hasBasename(path, basename), 'You are attempting to use a basename on a page whose URL path does not begin with the basename. Expected path "' + path + '" to begin with "' + basename + '".');
        if (basename) path = stripBasename(path, basename);
        return createLocation(path, state, key);
    }
    function createKey() {
        return Math.random().toString(36).substr(2, keyLength);
    }
    var transitionManager = createTransitionManager();
    function setState(nextState) {
        _extendsDefault.default(history, nextState);
        history.length = globalHistory.length;
        transitionManager.notifyListeners(history.location, history.action);
    }
    function handlePopState(event) {
        // Ignore extraneous popstate events in WebKit.
        if (isExtraneousPopstateEvent(event)) return;
        handlePop(getDOMLocation(event.state));
    }
    function handleHashChange() {
        handlePop(getDOMLocation(getHistoryState()));
    }
    var forceNextPop = false;
    function handlePop(location) {
        if (forceNextPop) {
            forceNextPop = false;
            setState();
        } else {
            var action = 'POP';
            transitionManager.confirmTransitionTo(location, action, getUserConfirmation, function(ok) {
                if (ok) setState({
                    action: action,
                    location: location
                });
                else revertPop(location);
            });
        }
    }
    function revertPop(fromLocation) {
        var toLocation = history.location; // TODO: We could probably make this more reliable by
        // keeping a list of keys we've seen in sessionStorage.
        // Instead, we just default to 0 for keys we don't know.
        var toIndex = allKeys.indexOf(toLocation.key);
        if (toIndex === -1) toIndex = 0;
        var fromIndex = allKeys.indexOf(fromLocation.key);
        if (fromIndex === -1) fromIndex = 0;
        var delta = toIndex - fromIndex;
        if (delta) {
            forceNextPop = true;
            go(delta);
        }
    }
    var initialLocation = getDOMLocation(getHistoryState());
    var allKeys = [
        initialLocation.key
    ]; // Public interface
    function createHref(location) {
        return basename + createPath(location);
    }
    function push(path, state1) {
        _tinyWarningDefault.default(!(typeof path === 'object' && path.state !== undefined && state1 !== undefined), "You should avoid providing a 2nd state argument to push when the 1st argument is a location-like object that already has state; it is ignored");
        var action = 'PUSH';
        var location = createLocation(path, state1, createKey(), history.location);
        transitionManager.confirmTransitionTo(location, action, getUserConfirmation, function(ok) {
            if (!ok) return;
            var href = createHref(location);
            var key = location.key, state = location.state;
            if (canUseHistory) {
                globalHistory.pushState({
                    key: key,
                    state: state
                }, null, href);
                if (forceRefresh) window.location.href = href;
                else {
                    var prevIndex = allKeys.indexOf(history.location.key);
                    var nextKeys = allKeys.slice(0, prevIndex + 1);
                    nextKeys.push(location.key);
                    allKeys = nextKeys;
                    setState({
                        action: action,
                        location: location
                    });
                }
            } else {
                _tinyWarningDefault.default(state === undefined, 'Browser history cannot push state in browsers that do not support HTML5 history');
                window.location.href = href;
            }
        });
    }
    function replace(path, state2) {
        _tinyWarningDefault.default(!(typeof path === 'object' && path.state !== undefined && state2 !== undefined), "You should avoid providing a 2nd state argument to replace when the 1st argument is a location-like object that already has state; it is ignored");
        var action = 'REPLACE';
        var location = createLocation(path, state2, createKey(), history.location);
        transitionManager.confirmTransitionTo(location, action, getUserConfirmation, function(ok) {
            if (!ok) return;
            var href = createHref(location);
            var key = location.key, state = location.state;
            if (canUseHistory) {
                globalHistory.replaceState({
                    key: key,
                    state: state
                }, null, href);
                if (forceRefresh) window.location.replace(href);
                else {
                    var prevIndex = allKeys.indexOf(history.location.key);
                    if (prevIndex !== -1) allKeys[prevIndex] = location.key;
                    setState({
                        action: action,
                        location: location
                    });
                }
            } else {
                _tinyWarningDefault.default(state === undefined, 'Browser history cannot replace state in browsers that do not support HTML5 history');
                window.location.replace(href);
            }
        });
    }
    function go(n) {
        globalHistory.go(n);
    }
    function goBack() {
        go(-1);
    }
    function goForward() {
        go(1);
    }
    var listenerCount = 0;
    function checkDOMListeners(delta) {
        listenerCount += delta;
        if (listenerCount === 1 && delta === 1) {
            window.addEventListener(PopStateEvent, handlePopState);
            if (needsHashChangeListener) window.addEventListener(HashChangeEvent, handleHashChange);
        } else if (listenerCount === 0) {
            window.removeEventListener(PopStateEvent, handlePopState);
            if (needsHashChangeListener) window.removeEventListener(HashChangeEvent, handleHashChange);
        }
    }
    var isBlocked = false;
    function block(prompt) {
        if (prompt === void 0) prompt = false;
        var unblock = transitionManager.setPrompt(prompt);
        if (!isBlocked) {
            checkDOMListeners(1);
            isBlocked = true;
        }
        return function() {
            if (isBlocked) {
                isBlocked = false;
                checkDOMListeners(-1);
            }
            return unblock();
        };
    }
    function listen(listener) {
        var unlisten = transitionManager.appendListener(listener);
        checkDOMListeners(1);
        return function() {
            checkDOMListeners(-1);
            unlisten();
        };
    }
    var history = {
        length: globalHistory.length,
        action: 'POP',
        location: initialLocation,
        createHref: createHref,
        push: push,
        replace: replace,
        go: go,
        goBack: goBack,
        goForward: goForward,
        block: block,
        listen: listen
    };
    return history;
}
var HashChangeEvent$1 = 'hashchange';
var HashPathCoders = {
    hashbang: {
        encodePath: function encodePath(path) {
            return path.charAt(0) === '!' ? path : '!/' + stripLeadingSlash(path);
        },
        decodePath: function decodePath(path) {
            return path.charAt(0) === '!' ? path.substr(1) : path;
        }
    },
    noslash: {
        encodePath: stripLeadingSlash,
        decodePath: addLeadingSlash
    },
    slash: {
        encodePath: addLeadingSlash,
        decodePath: addLeadingSlash
    }
};
function stripHash(url) {
    var hashIndex = url.indexOf('#');
    return hashIndex === -1 ? url : url.slice(0, hashIndex);
}
function getHashPath() {
    // We can't use window.location.hash here because it's not
    // consistent across browsers - Firefox will pre-decode it!
    var href = window.location.href;
    var hashIndex = href.indexOf('#');
    return hashIndex === -1 ? '' : href.substring(hashIndex + 1);
}
function pushHashPath(path) {
    window.location.hash = path;
}
function replaceHashPath(path) {
    window.location.replace(stripHash(window.location.href) + '#' + path);
}
function createHashHistory(props) {
    if (props === void 0) props = {};
    !canUseDOM && _tinyInvariantDefault.default(false, 'Hash history needs a DOM');
    var globalHistory = window.history;
    var canGoWithoutReload = supportsGoWithoutReloadUsingHash();
    var _props = props, _props$getUserConfirm = _props.getUserConfirmation, getUserConfirmation = _props$getUserConfirm === void 0 ? getConfirmation : _props$getUserConfirm, _props$hashType = _props.hashType, hashType = _props$hashType === void 0 ? 'slash' : _props$hashType;
    var basename = props.basename ? stripTrailingSlash(addLeadingSlash(props.basename)) : '';
    var _HashPathCoders$hashT = HashPathCoders[hashType], encodePath = _HashPathCoders$hashT.encodePath, decodePath = _HashPathCoders$hashT.decodePath;
    function getDOMLocation() {
        var path = decodePath(getHashPath());
        _tinyWarningDefault.default(!basename || hasBasename(path, basename), 'You are attempting to use a basename on a page whose URL path does not begin with the basename. Expected path "' + path + '" to begin with "' + basename + '".');
        if (basename) path = stripBasename(path, basename);
        return createLocation(path);
    }
    var transitionManager = createTransitionManager();
    function setState(nextState) {
        _extendsDefault.default(history, nextState);
        history.length = globalHistory.length;
        transitionManager.notifyListeners(history.location, history.action);
    }
    var forceNextPop = false;
    var ignorePath = null;
    function locationsAreEqual$$1(a, b) {
        return a.pathname === b.pathname && a.search === b.search && a.hash === b.hash;
    }
    function handleHashChange() {
        var path = getHashPath();
        var encodedPath = encodePath(path);
        if (path !== encodedPath) // Ensure we always have a properly-encoded hash.
        replaceHashPath(encodedPath);
        else {
            var location = getDOMLocation();
            var prevLocation = history.location;
            if (!forceNextPop && locationsAreEqual$$1(prevLocation, location)) return; // A hashchange doesn't always == location change.
            if (ignorePath === createPath(location)) return; // Ignore this change; we already setState in push/replace.
            ignorePath = null;
            handlePop(location);
        }
    }
    function handlePop(location) {
        if (forceNextPop) {
            forceNextPop = false;
            setState();
        } else {
            var action = 'POP';
            transitionManager.confirmTransitionTo(location, action, getUserConfirmation, function(ok) {
                if (ok) setState({
                    action: action,
                    location: location
                });
                else revertPop(location);
            });
        }
    }
    function revertPop(fromLocation) {
        var toLocation = history.location; // TODO: We could probably make this more reliable by
        // keeping a list of paths we've seen in sessionStorage.
        // Instead, we just default to 0 for paths we don't know.
        var toIndex = allPaths.lastIndexOf(createPath(toLocation));
        if (toIndex === -1) toIndex = 0;
        var fromIndex = allPaths.lastIndexOf(createPath(fromLocation));
        if (fromIndex === -1) fromIndex = 0;
        var delta = toIndex - fromIndex;
        if (delta) {
            forceNextPop = true;
            go(delta);
        }
    } // Ensure the hash is encoded properly before doing anything else.
    var path1 = getHashPath();
    var encodedPath1 = encodePath(path1);
    if (path1 !== encodedPath1) replaceHashPath(encodedPath1);
    var initialLocation = getDOMLocation();
    var allPaths = [
        createPath(initialLocation)
    ]; // Public interface
    function createHref(location) {
        var baseTag = document.querySelector('base');
        var href = '';
        if (baseTag && baseTag.getAttribute('href')) href = stripHash(window.location.href);
        return href + '#' + encodePath(basename + createPath(location));
    }
    function push(path2, state) {
        _tinyWarningDefault.default(state === undefined, 'Hash history cannot push state; it is ignored');
        var action = 'PUSH';
        var location = createLocation(path2, undefined, undefined, history.location);
        transitionManager.confirmTransitionTo(location, action, getUserConfirmation, function(ok) {
            if (!ok) return;
            var path = createPath(location);
            var encodedPath = encodePath(basename + path);
            var hashChanged = getHashPath() !== encodedPath;
            if (hashChanged) {
                // We cannot tell if a hashchange was caused by a PUSH, so we'd
                // rather setState here and ignore the hashchange. The caveat here
                // is that other hash histories in the page will consider it a POP.
                ignorePath = path;
                pushHashPath(encodedPath);
                var prevIndex = allPaths.lastIndexOf(createPath(history.location));
                var nextPaths = allPaths.slice(0, prevIndex + 1);
                nextPaths.push(path);
                allPaths = nextPaths;
                setState({
                    action: action,
                    location: location
                });
            } else {
                _tinyWarningDefault.default(false, 'Hash history cannot PUSH the same path; a new entry will not be added to the history stack');
                setState();
            }
        });
    }
    function replace(path3, state) {
        _tinyWarningDefault.default(state === undefined, 'Hash history cannot replace state; it is ignored');
        var action = 'REPLACE';
        var location = createLocation(path3, undefined, undefined, history.location);
        transitionManager.confirmTransitionTo(location, action, getUserConfirmation, function(ok) {
            if (!ok) return;
            var path = createPath(location);
            var encodedPath = encodePath(basename + path);
            var hashChanged = getHashPath() !== encodedPath;
            if (hashChanged) {
                // We cannot tell if a hashchange was caused by a REPLACE, so we'd
                // rather setState here and ignore the hashchange. The caveat here
                // is that other hash histories in the page will consider it a POP.
                ignorePath = path;
                replaceHashPath(encodedPath);
            }
            var prevIndex = allPaths.indexOf(createPath(history.location));
            if (prevIndex !== -1) allPaths[prevIndex] = path;
            setState({
                action: action,
                location: location
            });
        });
    }
    function go(n) {
        _tinyWarningDefault.default(canGoWithoutReload, 'Hash history go(n) causes a full page reload in this browser');
        globalHistory.go(n);
    }
    function goBack() {
        go(-1);
    }
    function goForward() {
        go(1);
    }
    var listenerCount = 0;
    function checkDOMListeners(delta) {
        listenerCount += delta;
        if (listenerCount === 1 && delta === 1) window.addEventListener(HashChangeEvent$1, handleHashChange);
        else if (listenerCount === 0) window.removeEventListener(HashChangeEvent$1, handleHashChange);
    }
    var isBlocked = false;
    function block(prompt) {
        if (prompt === void 0) prompt = false;
        var unblock = transitionManager.setPrompt(prompt);
        if (!isBlocked) {
            checkDOMListeners(1);
            isBlocked = true;
        }
        return function() {
            if (isBlocked) {
                isBlocked = false;
                checkDOMListeners(-1);
            }
            return unblock();
        };
    }
    function listen(listener) {
        var unlisten = transitionManager.appendListener(listener);
        checkDOMListeners(1);
        return function() {
            checkDOMListeners(-1);
            unlisten();
        };
    }
    var history = {
        length: globalHistory.length,
        action: 'POP',
        location: initialLocation,
        createHref: createHref,
        push: push,
        replace: replace,
        go: go,
        goBack: goBack,
        goForward: goForward,
        block: block,
        listen: listen
    };
    return history;
}
function clamp(n, lowerBound, upperBound) {
    return Math.min(Math.max(n, lowerBound), upperBound);
}
/**
 * Creates a history object that stores locations in memory.
 */ function createMemoryHistory(props) {
    if (props === void 0) props = {};
    var _props = props, getUserConfirmation = _props.getUserConfirmation, _props$initialEntries = _props.initialEntries, initialEntries = _props$initialEntries === void 0 ? [
        '/'
    ] : _props$initialEntries, _props$initialIndex = _props.initialIndex, initialIndex = _props$initialIndex === void 0 ? 0 : _props$initialIndex, _props$keyLength = _props.keyLength, keyLength = _props$keyLength === void 0 ? 6 : _props$keyLength;
    var transitionManager = createTransitionManager();
    function setState(nextState) {
        _extendsDefault.default(history, nextState);
        history.length = history.entries.length;
        transitionManager.notifyListeners(history.location, history.action);
    }
    function createKey() {
        return Math.random().toString(36).substr(2, keyLength);
    }
    var index = clamp(initialIndex, 0, initialEntries.length - 1);
    var entries = initialEntries.map(function(entry) {
        return typeof entry === 'string' ? createLocation(entry, undefined, createKey()) : createLocation(entry, undefined, entry.key || createKey());
    }); // Public interface
    var createHref = createPath;
    function push(path, state) {
        _tinyWarningDefault.default(!(typeof path === 'object' && path.state !== undefined && state !== undefined), "You should avoid providing a 2nd state argument to push when the 1st argument is a location-like object that already has state; it is ignored");
        var action = 'PUSH';
        var location = createLocation(path, state, createKey(), history.location);
        transitionManager.confirmTransitionTo(location, action, getUserConfirmation, function(ok) {
            if (!ok) return;
            var prevIndex = history.index;
            var nextIndex = prevIndex + 1;
            var nextEntries = history.entries.slice(0);
            if (nextEntries.length > nextIndex) nextEntries.splice(nextIndex, nextEntries.length - nextIndex, location);
            else nextEntries.push(location);
            setState({
                action: action,
                location: location,
                index: nextIndex,
                entries: nextEntries
            });
        });
    }
    function replace(path, state) {
        _tinyWarningDefault.default(!(typeof path === 'object' && path.state !== undefined && state !== undefined), "You should avoid providing a 2nd state argument to replace when the 1st argument is a location-like object that already has state; it is ignored");
        var action = 'REPLACE';
        var location = createLocation(path, state, createKey(), history.location);
        transitionManager.confirmTransitionTo(location, action, getUserConfirmation, function(ok) {
            if (!ok) return;
            history.entries[history.index] = location;
            setState({
                action: action,
                location: location
            });
        });
    }
    function go(n) {
        var nextIndex = clamp(history.index + n, 0, history.entries.length - 1);
        var action = 'POP';
        var location = history.entries[nextIndex];
        transitionManager.confirmTransitionTo(location, action, getUserConfirmation, function(ok) {
            if (ok) setState({
                action: action,
                location: location,
                index: nextIndex
            });
            else // Mimic the behavior of DOM histories by
            // causing a render after a cancelled POP.
            setState();
        });
    }
    function goBack() {
        go(-1);
    }
    function goForward() {
        go(1);
    }
    function canGo(n) {
        var nextIndex = history.index + n;
        return nextIndex >= 0 && nextIndex < history.entries.length;
    }
    function block(prompt) {
        if (prompt === void 0) prompt = false;
        return transitionManager.setPrompt(prompt);
    }
    function listen(listener) {
        return transitionManager.appendListener(listener);
    }
    var history = {
        length: entries.length,
        action: 'POP',
        location: entries[index],
        index: index,
        entries: entries,
        createHref: createHref,
        push: push,
        replace: replace,
        go: go,
        goBack: goBack,
        goForward: goForward,
        canGo: canGo,
        block: block,
        listen: listen
    };
    return history;
}

},{"@babel/runtime/helpers/esm/extends":"fTBFS","resolve-pathname":"9VnMR","value-equal":"e46dy","tiny-warning":"82vrW","tiny-invariant":"fnIPv","@parcel/transformer-js/src/esmodule-helpers.js":"gkKU3"}],"fTBFS":[function(require,module,exports) {
var parcelHelpers = require("@parcel/transformer-js/src/esmodule-helpers.js");
parcelHelpers.defineInteropFlag(exports);
function _extends() {
    _extends = Object.assign || function(target) {
        for(var i = 1; i < arguments.length; i++){
            var source = arguments[i];
            for(var key in source)if (Object.prototype.hasOwnProperty.call(source, key)) target[key] = source[key];
        }
        return target;
    };
    return _extends.apply(this, arguments);
}
exports.default = _extends;

},{"@parcel/transformer-js/src/esmodule-helpers.js":"gkKU3"}],"9VnMR":[function(require,module,exports) {
var parcelHelpers = require("@parcel/transformer-js/src/esmodule-helpers.js");
parcelHelpers.defineInteropFlag(exports);
function isAbsolute(pathname) {
    return pathname.charAt(0) === '/';
}
// About 1.5x faster than the two-arg version of Array#splice()
function spliceOne(list, index) {
    for(var i = index, k = i + 1, n = list.length; k < n; i += 1, k += 1)list[i] = list[k];
    list.pop();
}
// This implementation is based heavily on node's url.parse
function resolvePathname(to, from) {
    if (from === undefined) from = '';
    var toParts = to && to.split('/') || [];
    var fromParts = from && from.split('/') || [];
    var isToAbs = to && isAbsolute(to);
    var isFromAbs = from && isAbsolute(from);
    var mustEndAbs = isToAbs || isFromAbs;
    if (to && isAbsolute(to)) // to is absolute
    fromParts = toParts;
    else if (toParts.length) {
        // to is relative, drop the filename
        fromParts.pop();
        fromParts = fromParts.concat(toParts);
    }
    if (!fromParts.length) return '/';
    var hasTrailingSlash;
    if (fromParts.length) {
        var last = fromParts[fromParts.length - 1];
        hasTrailingSlash = last === '.' || last === '..' || last === '';
    } else hasTrailingSlash = false;
    var up = 0;
    for(var i = fromParts.length; i >= 0; i--){
        var part = fromParts[i];
        if (part === '.') spliceOne(fromParts, i);
        else if (part === '..') {
            spliceOne(fromParts, i);
            up++;
        } else if (up) {
            spliceOne(fromParts, i);
            up--;
        }
    }
    if (!mustEndAbs) for(; up--;)fromParts.unshift('..');
    if (mustEndAbs && fromParts[0] !== '' && (!fromParts[0] || !isAbsolute(fromParts[0]))) fromParts.unshift('');
    var result = fromParts.join('/');
    if (hasTrailingSlash && result.substr(-1) !== '/') result += '/';
    return result;
}
exports.default = resolvePathname;

},{"@parcel/transformer-js/src/esmodule-helpers.js":"gkKU3"}],"e46dy":[function(require,module,exports) {
var parcelHelpers = require("@parcel/transformer-js/src/esmodule-helpers.js");
parcelHelpers.defineInteropFlag(exports);
function valueOf(obj) {
    return obj.valueOf ? obj.valueOf() : Object.prototype.valueOf.call(obj);
}
function valueEqual(a, b) {
    // Test for strict equality first.
    if (a === b) return true;
    // Otherwise, if either of them == null they are not equal.
    if (a == null || b == null) return false;
    if (Array.isArray(a)) return Array.isArray(b) && a.length === b.length && a.every(function(item, index) {
        return valueEqual(item, b[index]);
    });
    if (typeof a === 'object' || typeof b === 'object') {
        var aValue = valueOf(a);
        var bValue = valueOf(b);
        if (aValue !== a || bValue !== b) return valueEqual(aValue, bValue);
        return Object.keys(Object.assign({}, a, b)).every(function(key) {
            return valueEqual(a[key], b[key]);
        });
    }
    return false;
}
exports.default = valueEqual;

},{"@parcel/transformer-js/src/esmodule-helpers.js":"gkKU3"}],"82vrW":[function(require,module,exports) {
var parcelHelpers = require("@parcel/transformer-js/src/esmodule-helpers.js");
parcelHelpers.defineInteropFlag(exports);
var isProduction = false;
function warning(condition, message) {
    if (!isProduction) {
        if (condition) return;
        var text = "Warning: " + message;
        if (typeof console !== 'undefined') console.warn(text);
        try {
            throw Error(text);
        } catch (x) {}
    }
}
exports.default = warning;

},{"@parcel/transformer-js/src/esmodule-helpers.js":"gkKU3"}],"fnIPv":[function(require,module,exports) {
var parcelHelpers = require("@parcel/transformer-js/src/esmodule-helpers.js");
parcelHelpers.defineInteropFlag(exports);
parcelHelpers.export(exports, "default", ()=>invariant
);
var isProduction = false;
var prefix = 'Invariant failed';
function invariant(condition, message) {
    if (condition) return;
    if (isProduction) throw new Error(prefix);
    var provided = typeof message === 'function' ? message() : message;
    var value = provided ? prefix + ": " + provided : prefix;
    throw new Error(value);
}

},{"@parcel/transformer-js/src/esmodule-helpers.js":"gkKU3"}],"fRuCg":[function(require,module,exports) {
var parcelHelpers = require("@parcel/transformer-js/src/esmodule-helpers.js");
parcelHelpers.defineInteropFlag(exports);
var _react = require("react");
var _reactDefault = parcelHelpers.interopDefault(_react);
var _inheritsLoose = require("@babel/runtime/helpers/esm/inheritsLoose");
var _inheritsLooseDefault = parcelHelpers.interopDefault(_inheritsLoose);
var _propTypes = require("prop-types");
var _propTypesDefault = parcelHelpers.interopDefault(_propTypes);
var _tinyWarning = require("tiny-warning");
var _tinyWarningDefault = parcelHelpers.interopDefault(_tinyWarning);
var global = arguments[3];
var MAX_SIGNED_31_BIT_INT = 1073741823;
var commonjsGlobal = typeof globalThis !== 'undefined' ? globalThis : typeof window !== 'undefined' ? window : typeof global !== 'undefined' ? global : {};
function getUniqueId() {
    var key = '__global_unique_id__';
    return commonjsGlobal[key] = (commonjsGlobal[key] || 0) + 1;
}
function objectIs(x, y) {
    if (x === y) return x !== 0 || 1 / x === 1 / y;
    else return x !== x && y !== y;
}
function createEventEmitter(value) {
    var handlers = [];
    return {
        on: function on(handler) {
            handlers.push(handler);
        },
        off: function off(handler) {
            handlers = handlers.filter(function(h) {
                return h !== handler;
            });
        },
        get: function get() {
            return value;
        },
        set: function set(newValue, changedBits) {
            value = newValue;
            handlers.forEach(function(handler) {
                return handler(value, changedBits);
            });
        }
    };
}
function onlyChild(children) {
    return Array.isArray(children) ? children[0] : children;
}
function createReactContext(defaultValue, calculateChangedBits) {
    var _Provider$childContex, _Consumer$contextType;
    var contextProp = '__create-react-context-' + getUniqueId() + '__';
    var Provider1 = /*#__PURE__*/ function(_Component) {
        _inheritsLooseDefault.default(Provider, _Component);
        function Provider() {
            var _this;
            _this = _Component.apply(this, arguments) || this;
            _this.emitter = createEventEmitter(_this.props.value);
            return _this;
        }
        var _proto = Provider.prototype;
        _proto.getChildContext = function getChildContext() {
            var _ref;
            return _ref = {}, _ref[contextProp] = this.emitter, _ref;
        };
        _proto.componentWillReceiveProps = function componentWillReceiveProps(nextProps) {
            if (this.props.value !== nextProps.value) {
                var oldValue = this.props.value;
                var newValue = nextProps.value;
                var changedBits;
                if (objectIs(oldValue, newValue)) changedBits = 0;
                else {
                    changedBits = typeof calculateChangedBits === 'function' ? calculateChangedBits(oldValue, newValue) : MAX_SIGNED_31_BIT_INT;
                    _tinyWarningDefault.default((changedBits & MAX_SIGNED_31_BIT_INT) === changedBits, "calculateChangedBits: Expected the return value to be a 31-bit integer. Instead received: " + changedBits);
                    changedBits |= 0;
                    if (changedBits !== 0) this.emitter.set(nextProps.value, changedBits);
                }
            }
        };
        _proto.render = function render() {
            return this.props.children;
        };
        return Provider;
    }(_react.Component);
    Provider1.childContextTypes = (_Provider$childContex = {}, _Provider$childContex[contextProp] = _propTypesDefault.default.object.isRequired, _Provider$childContex);
    var Consumer1 = /*#__PURE__*/ function(_Component2) {
        _inheritsLooseDefault.default(Consumer, _Component2);
        function Consumer() {
            var _this2;
            _this2 = _Component2.apply(this, arguments) || this;
            _this2.state = {
                value: _this2.getValue()
            };
            _this2.onUpdate = function(newValue, changedBits) {
                var observedBits = _this2.observedBits | 0;
                if ((observedBits & changedBits) !== 0) _this2.setState({
                    value: _this2.getValue()
                });
            };
            return _this2;
        }
        var _proto2 = Consumer.prototype;
        _proto2.componentWillReceiveProps = function componentWillReceiveProps(nextProps) {
            var observedBits = nextProps.observedBits;
            this.observedBits = observedBits === undefined || observedBits === null ? MAX_SIGNED_31_BIT_INT : observedBits;
        };
        _proto2.componentDidMount = function componentDidMount() {
            if (this.context[contextProp]) this.context[contextProp].on(this.onUpdate);
            var observedBits = this.props.observedBits;
            this.observedBits = observedBits === undefined || observedBits === null ? MAX_SIGNED_31_BIT_INT : observedBits;
        };
        _proto2.componentWillUnmount = function componentWillUnmount() {
            if (this.context[contextProp]) this.context[contextProp].off(this.onUpdate);
        };
        _proto2.getValue = function getValue() {
            if (this.context[contextProp]) return this.context[contextProp].get();
            else return defaultValue;
        };
        _proto2.render = function render() {
            return onlyChild(this.props.children)(this.state.value);
        };
        return Consumer;
    }(_react.Component);
    Consumer1.contextTypes = (_Consumer$contextType = {}, _Consumer$contextType[contextProp] = _propTypesDefault.default.object, _Consumer$contextType);
    return {
        Provider: Provider1,
        Consumer: Consumer1
    };
}
var index = _reactDefault.default.createContext || createReactContext;
exports.default = index;

},{"react":"21dqq","@babel/runtime/helpers/esm/inheritsLoose":"9u2Z8","prop-types":"7wKI2","tiny-warning":"82vrW","@parcel/transformer-js/src/esmodule-helpers.js":"gkKU3"}],"6q3tZ":[function(require,module,exports) {
var isarray = require('isarray');
/**
 * Expose `pathToRegexp`.
 */ module.exports = pathToRegexp;
module.exports.parse = parse;
module.exports.compile = compile;
module.exports.tokensToFunction = tokensToFunction;
module.exports.tokensToRegExp = tokensToRegExp;
/**
 * The main path matching regexp utility.
 *
 * @type {RegExp}
 */ var PATH_REGEXP = new RegExp([
    // Match escaped characters that would otherwise appear in future matches.
    // This allows the user to escape special characters that won't transform.
    '(\\\\.)',
    // Match Express-style parameters and un-named parameters with a prefix
    // and optional suffixes. Matches appear as:
    //
    // "/:test(\\d+)?" => ["/", "test", "\d+", undefined, "?", undefined]
    // "/route(\\d+)"  => [undefined, undefined, undefined, "\d+", undefined, undefined]
    // "/*"            => ["/", undefined, undefined, undefined, undefined, "*"]
    '([\\/.])?(?:(?:\\:(\\w+)(?:\\(((?:\\\\.|[^\\\\()])+)\\))?|\\(((?:\\\\.|[^\\\\()])+)\\))([+*?])?|(\\*))'
].join('|'), 'g');
/**
 * Parse a string for the raw tokens.
 *
 * @param  {string}  str
 * @param  {Object=} options
 * @return {!Array}
 */ function parse(str, options) {
    var tokens = [];
    var key = 0;
    var index = 0;
    var path = '';
    var defaultDelimiter = options && options.delimiter || '/';
    var res;
    while((res = PATH_REGEXP.exec(str)) != null){
        var m = res[0];
        var escaped = res[1];
        var offset = res.index;
        path += str.slice(index, offset);
        index = offset + m.length;
        // Ignore already escaped sequences.
        if (escaped) {
            path += escaped[1];
            continue;
        }
        var next = str[index];
        var prefix = res[2];
        var name = res[3];
        var capture = res[4];
        var group = res[5];
        var modifier = res[6];
        var asterisk = res[7];
        // Push the current path onto the tokens.
        if (path) {
            tokens.push(path);
            path = '';
        }
        var partial = prefix != null && next != null && next !== prefix;
        var repeat = modifier === '+' || modifier === '*';
        var optional = modifier === '?' || modifier === '*';
        var delimiter = res[2] || defaultDelimiter;
        var pattern = capture || group;
        tokens.push({
            name: name || key++,
            prefix: prefix || '',
            delimiter: delimiter,
            optional: optional,
            repeat: repeat,
            partial: partial,
            asterisk: !!asterisk,
            pattern: pattern ? escapeGroup(pattern) : asterisk ? '.*' : '[^' + escapeString(delimiter) + ']+?'
        });
    }
    // Match any characters still remaining.
    if (index < str.length) path += str.substr(index);
    // If the path exists, push it onto the end.
    if (path) tokens.push(path);
    return tokens;
}
/**
 * Compile a string to a template function for the path.
 *
 * @param  {string}             str
 * @param  {Object=}            options
 * @return {!function(Object=, Object=)}
 */ function compile(str, options) {
    return tokensToFunction(parse(str, options), options);
}
/**
 * Prettier encoding of URI path segments.
 *
 * @param  {string}
 * @return {string}
 */ function encodeURIComponentPretty(str) {
    return encodeURI(str).replace(/[\/?#]/g, function(c) {
        return '%' + c.charCodeAt(0).toString(16).toUpperCase();
    });
}
/**
 * Encode the asterisk parameter. Similar to `pretty`, but allows slashes.
 *
 * @param  {string}
 * @return {string}
 */ function encodeAsterisk(str) {
    return encodeURI(str).replace(/[?#]/g, function(c) {
        return '%' + c.charCodeAt(0).toString(16).toUpperCase();
    });
}
/**
 * Expose a method for transforming tokens into the path function.
 */ function tokensToFunction(tokens, options1) {
    // Compile all the tokens into regexps.
    var matches = new Array(tokens.length);
    // Compile all the patterns before compilation.
    for(var i1 = 0; i1 < tokens.length; i1++)if (typeof tokens[i1] === 'object') matches[i1] = new RegExp('^(?:' + tokens[i1].pattern + ')$', flags(options1));
    return function(obj, opts) {
        var path = '';
        var data = obj || {};
        var options = opts || {};
        var encode = options.pretty ? encodeURIComponentPretty : encodeURIComponent;
        for(var i = 0; i < tokens.length; i++){
            var token = tokens[i];
            if (typeof token === 'string') {
                path += token;
                continue;
            }
            var value = data[token.name];
            var segment;
            if (value == null) {
                if (token.optional) {
                    // Prepend partial segment prefixes.
                    if (token.partial) path += token.prefix;
                    continue;
                } else throw new TypeError('Expected "' + token.name + '" to be defined');
            }
            if (isarray(value)) {
                if (!token.repeat) throw new TypeError('Expected "' + token.name + '" to not repeat, but received `' + JSON.stringify(value) + '`');
                if (value.length === 0) {
                    if (token.optional) continue;
                    else throw new TypeError('Expected "' + token.name + '" to not be empty');
                }
                for(var j = 0; j < value.length; j++){
                    segment = encode(value[j]);
                    if (!matches[i].test(segment)) throw new TypeError('Expected all "' + token.name + '" to match "' + token.pattern + '", but received `' + JSON.stringify(segment) + '`');
                    path += (j === 0 ? token.prefix : token.delimiter) + segment;
                }
                continue;
            }
            segment = token.asterisk ? encodeAsterisk(value) : encode(value);
            if (!matches[i].test(segment)) throw new TypeError('Expected "' + token.name + '" to match "' + token.pattern + '", but received "' + segment + '"');
            path += token.prefix + segment;
        }
        return path;
    };
}
/**
 * Escape a regular expression string.
 *
 * @param  {string} str
 * @return {string}
 */ function escapeString(str) {
    return str.replace(/([.+*?=^!:${}()[\]|\/\\])/g, '\\$1');
}
/**
 * Escape the capturing group by escaping special characters and meaning.
 *
 * @param  {string} group
 * @return {string}
 */ function escapeGroup(group) {
    return group.replace(/([=!:$\/()])/g, '\\$1');
}
/**
 * Attach the keys as a property of the regexp.
 *
 * @param  {!RegExp} re
 * @param  {Array}   keys
 * @return {!RegExp}
 */ function attachKeys(re, keys) {
    re.keys = keys;
    return re;
}
/**
 * Get the flags for a regexp from the options.
 *
 * @param  {Object} options
 * @return {string}
 */ function flags(options) {
    return options && options.sensitive ? '' : 'i';
}
/**
 * Pull out keys from a regexp.
 *
 * @param  {!RegExp} path
 * @param  {!Array}  keys
 * @return {!RegExp}
 */ function regexpToRegexp(path, keys) {
    // Use a negative lookahead to match only capturing groups.
    var groups = path.source.match(/\((?!\?)/g);
    if (groups) for(var i = 0; i < groups.length; i++)keys.push({
        name: i,
        prefix: null,
        delimiter: null,
        optional: false,
        repeat: false,
        partial: false,
        asterisk: false,
        pattern: null
    });
    return attachKeys(path, keys);
}
/**
 * Transform an array into a regexp.
 *
 * @param  {!Array}  path
 * @param  {Array}   keys
 * @param  {!Object} options
 * @return {!RegExp}
 */ function arrayToRegexp(path, keys, options) {
    var parts = [];
    for(var i = 0; i < path.length; i++)parts.push(pathToRegexp(path[i], keys, options).source);
    var regexp = new RegExp('(?:' + parts.join('|') + ')', flags(options));
    return attachKeys(regexp, keys);
}
/**
 * Create a path regexp from string input.
 *
 * @param  {string}  path
 * @param  {!Array}  keys
 * @param  {!Object} options
 * @return {!RegExp}
 */ function stringToRegexp(path, keys, options) {
    return tokensToRegExp(parse(path, options), keys, options);
}
/**
 * Expose a function for taking tokens and returning a RegExp.
 *
 * @param  {!Array}          tokens
 * @param  {(Array|Object)=} keys
 * @param  {Object=}         options
 * @return {!RegExp}
 */ function tokensToRegExp(tokens, keys, options) {
    if (!isarray(keys)) {
        options = keys || options;
        keys = [];
    }
    options = options || {};
    var strict = options.strict;
    var end = options.end !== false;
    var route = '';
    // Iterate over the tokens and create our regexp string.
    for(var i = 0; i < tokens.length; i++){
        var token = tokens[i];
        if (typeof token === 'string') route += escapeString(token);
        else {
            var prefix = escapeString(token.prefix);
            var capture = '(?:' + token.pattern + ')';
            keys.push(token);
            if (token.repeat) capture += '(?:' + prefix + capture + ')*';
            if (token.optional) {
                if (!token.partial) capture = '(?:' + prefix + '(' + capture + '))?';
                else capture = prefix + '(' + capture + ')?';
            } else capture = prefix + '(' + capture + ')';
            route += capture;
        }
    }
    var delimiter = escapeString(options.delimiter || '/');
    var endsWithDelimiter = route.slice(-delimiter.length) === delimiter;
    // In non-strict mode we allow a slash at the end of match. If the path to
    // match already ends with a slash, we remove it for consistency. The slash
    // is valid at the end of a path match, not in the middle. This is important
    // in non-ending mode, where "/test/" shouldn't match "/test//route".
    if (!strict) route = (endsWithDelimiter ? route.slice(0, -delimiter.length) : route) + '(?:' + delimiter + '(?=$))?';
    if (end) route += '$';
    else // In non-ending mode, we need the capturing groups to match as much as
    // possible by using a positive lookahead to the end or next path segment.
    route += strict && endsWithDelimiter ? '' : '(?=' + delimiter + '|$)';
    return attachKeys(new RegExp('^' + route, flags(options)), keys);
}
/**
 * Normalize the given path string, returning a regular expression.
 *
 * An empty array can be passed in for the keys, which will hold the
 * placeholder key descriptions. For example, using `/user/:id`, `keys` will
 * contain `[{ name: 'id', delimiter: '/', optional: false, repeat: false }]`.
 *
 * @param  {(string|RegExp|Array)} path
 * @param  {(Array|Object)=}       keys
 * @param  {Object=}               options
 * @return {!RegExp}
 */ function pathToRegexp(path, keys, options) {
    if (!isarray(keys)) {
        options = keys || options;
        keys = [];
    }
    options = options || {};
    if (path instanceof RegExp) return regexpToRegexp(path, keys);
    if (isarray(path)) return arrayToRegexp(path, keys, options);
    return stringToRegexp(path, keys, options);
}

},{"isarray":"hk7XJ"}],"hk7XJ":[function(require,module,exports) {
module.exports = Array.isArray || function(arr) {
    return Object.prototype.toString.call(arr) == '[object Array]';
};

},{}],"adHgr":[function(require,module,exports) {
var parcelHelpers = require("@parcel/transformer-js/src/esmodule-helpers.js");
parcelHelpers.defineInteropFlag(exports);
function _objectWithoutPropertiesLoose(source, excluded) {
    if (source == null) return {};
    var target = {};
    var sourceKeys = Object.keys(source);
    var key, i;
    for(i = 0; i < sourceKeys.length; i++){
        key = sourceKeys[i];
        if (excluded.indexOf(key) >= 0) continue;
        target[key] = source[key];
    }
    return target;
}
exports.default = _objectWithoutPropertiesLoose;

},{"@parcel/transformer-js/src/esmodule-helpers.js":"gkKU3"}],"1GfsB":[function(require,module,exports) {
'use strict';
var reactIs = require('react-is');
/**
 * Copyright 2015, Yahoo! Inc.
 * Copyrights licensed under the New BSD License. See the accompanying LICENSE file for terms.
 */ var REACT_STATICS = {
    childContextTypes: true,
    contextType: true,
    contextTypes: true,
    defaultProps: true,
    displayName: true,
    getDefaultProps: true,
    getDerivedStateFromError: true,
    getDerivedStateFromProps: true,
    mixins: true,
    propTypes: true,
    type: true
};
var KNOWN_STATICS = {
    name: true,
    length: true,
    prototype: true,
    caller: true,
    callee: true,
    arguments: true,
    arity: true
};
var FORWARD_REF_STATICS = {
    '$$typeof': true,
    render: true,
    defaultProps: true,
    displayName: true,
    propTypes: true
};
var MEMO_STATICS = {
    '$$typeof': true,
    compare: true,
    defaultProps: true,
    displayName: true,
    propTypes: true,
    type: true
};
var TYPE_STATICS = {};
TYPE_STATICS[reactIs.ForwardRef] = FORWARD_REF_STATICS;
TYPE_STATICS[reactIs.Memo] = MEMO_STATICS;
function getStatics(component) {
    // React v16.11 and below
    if (reactIs.isMemo(component)) return MEMO_STATICS;
     // React v16.12 and above
    return TYPE_STATICS[component['$$typeof']] || REACT_STATICS;
}
var defineProperty = Object.defineProperty;
var getOwnPropertyNames = Object.getOwnPropertyNames;
var getOwnPropertySymbols = Object.getOwnPropertySymbols;
var getOwnPropertyDescriptor = Object.getOwnPropertyDescriptor;
var getPrototypeOf = Object.getPrototypeOf;
var objectPrototype = Object.prototype;
function hoistNonReactStatics(targetComponent, sourceComponent, blacklist) {
    if (typeof sourceComponent !== 'string') {
        // don't hoist over string (html) components
        if (objectPrototype) {
            var inheritedComponent = getPrototypeOf(sourceComponent);
            if (inheritedComponent && inheritedComponent !== objectPrototype) hoistNonReactStatics(targetComponent, inheritedComponent, blacklist);
        }
        var keys = getOwnPropertyNames(sourceComponent);
        if (getOwnPropertySymbols) keys = keys.concat(getOwnPropertySymbols(sourceComponent));
        var targetStatics = getStatics(targetComponent);
        var sourceStatics = getStatics(sourceComponent);
        for(var i = 0; i < keys.length; ++i){
            var key = keys[i];
            if (!KNOWN_STATICS[key] && !(blacklist && blacklist[key]) && !(sourceStatics && sourceStatics[key]) && !(targetStatics && targetStatics[key])) {
                var descriptor = getOwnPropertyDescriptor(sourceComponent, key);
                try {
                    // Avoid failures from read-only properties
                    defineProperty(targetComponent, key, descriptor);
                } catch (e) {}
            }
        }
    }
    return targetComponent;
}
module.exports = hoistNonReactStatics;

},{"react-is":"7EuwB"}],"kwIfD":[function(require,module,exports) {
var $parcel$ReactRefreshHelpers$07fe = require("@parcel/transformer-react-refresh-wrap/lib/helpers/helpers.js");
var prevRefreshReg = window.$RefreshReg$;
var prevRefreshSig = window.$RefreshSig$;
$parcel$ReactRefreshHelpers$07fe.prelude(module);

try {
var parcelHelpers = require("@parcel/transformer-js/src/esmodule-helpers.js");
parcelHelpers.defineInteropFlag(exports);
var _jsxDevRuntime = require("react/jsx-dev-runtime");
//user registration
var _react = require("react");
var _reactDefault = parcelHelpers.interopDefault(_react);
var _userFunctions = require("./UserFunctions");
var _sweetalert = require("sweetalert");
var _sweetalertDefault = parcelHelpers.interopDefault(_sweetalert);
class CreateUser extends _react.Component {
    componentDidMount() {
        localStorage.removeItem('userToken');
        document.title = "User Registration";
    }
    constructor(){
        super();
        this.state = {
            idNumber: '',
            name: '',
            email: '',
            mobile: '',
            groupId: '',
            researchfield: '',
            panel: '',
            type: '',
            password: ''
        };
        //to handle the state changes
        this.onChange = this.onChange.bind(this);
        this.onSubmit = this.onSubmit.bind(this);
    }
    onChange(e) {
        this.setState({
            [e.target.name]: e.target.value
        });
    }
    onSubmit(e) {
        e.preventDefault();
        const user = {
            idNumber: this.state.idNumber,
            name: this.state.name,
            email: this.state.email,
            mobile: this.state.mobile,
            groupId: this.state.groupId,
            researchfield: this.state.researchfield,
            panel: this.state.panel,
            type: this.state.type,
            password: this.state.password
        };
        // validations----------------------------------------------------
        let validated = true;
        if (user.idNumber === '') {
            validated = false;
            _sweetalertDefault.default({
                title: "",
                text: "Please enter a valid ID number",
                icon: "warning"
            });
        } else if (user.name === '' || user.name.length < 5) {
            validated = false;
            _sweetalertDefault.default({
                title: "",
                text: "Please enter a valid name",
                icon: "warning"
            });
        } else if (!user.email.match(/^(([^<>()\[\]\.,;:\s@\"]+(\.[^<>()\[\]\.,;:\s@\"]+)*)|(\".+\"))@(([^<>()[\]\.,;:\s@\"]+\.)+[^<>()[\]\.,;:\s@\"]{2,})$/i)) {
            validated = false;
            _sweetalertDefault.default({
                title: "",
                text: "Please enter a valid email",
                icon: "warning"
            });
        } else if (!user.mobile.match(/^(\+\d{1,3}[- ]?)?\d{10}$/)) {
            validated = false;
            _sweetalertDefault.default({
                title: "",
                text: "Please enter a valid mobile number",
                icon: "warning"
            });
        } else if (user.type === '') {
            validated = false;
            _sweetalertDefault.default({
                title: "",
                text: "Please select a user type",
                icon: "warning"
            });
        } else if (user.password === '' || user.password.length < 8) {
            validated = false;
            _sweetalertDefault.default({
                title: "",
                text: "Password should contain at least 8 characters",
                icon: "warning"
            });
        }
        if (validated === true) _userFunctions.userRegister(user).then((res)=>{
            if (res) _sweetalertDefault.default("Registered successfully!", "", "success").then((value)=>{
                if (value) {
                    this.props.history.push(`/user/login`);
                    window.location.reload();
                }
            });
        });
    }
    render() {
        return /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
            children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                className: "container",
                style: {
                    marginTop: '10px',
                    marginBottom: '10px',
                    backgroundColor: 'white',
                    paddingBottom: '100px',
                    paddingTop: '50px',
                    paddingLeft: '100px',
                    paddingRight: '100px',
                    height: '800px'
                },
                children: [
                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("h1", {
                        style: {
                            textAlign: 'center',
                            paddingBottom: '10px'
                        },
                        children: "Research Project Management System"
                    }, void 0, false, {
                        fileName: "src/components/IT20125202/user/CreateUser.js",
                        lineNumber: 128,
                        columnNumber: 11
                    }, this),
                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("hr", {}, void 0, false, {
                        fileName: "src/components/IT20125202/user/CreateUser.js",
                        lineNumber: 129,
                        columnNumber: 11
                    }, this),
                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                        className: "col-md-8 mt-4 mx-auto",
                        children: [
                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("h1", {
                                className: "h3 mb-3 font-weight-normal",
                                style: {
                                    textAlign: 'center'
                                },
                                children: "User Registration"
                            }, void 0, false, {
                                fileName: "src/components/IT20125202/user/CreateUser.js",
                                lineNumber: 131,
                                columnNumber: 13
                            }, this),
                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("br", {}, void 0, false, {
                                fileName: "src/components/IT20125202/user/CreateUser.js",
                                lineNumber: 132,
                                columnNumber: 13
                            }, this),
                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("form", {
                                className: "needs-validation",
                                noValidate: true,
                                children: [
                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                        className: "form-group",
                                        style: {
                                            marginBottom: '15px'
                                        },
                                        children: [
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("label", {
                                                style: {
                                                    marginBottom: '5px'
                                                },
                                                children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("b", {
                                                    children: "ID number"
                                                }, void 0, false, {
                                                    fileName: "src/components/IT20125202/user/CreateUser.js",
                                                    lineNumber: 135,
                                                    columnNumber: 56
                                                }, this)
                                            }, void 0, false, {
                                                fileName: "src/components/IT20125202/user/CreateUser.js",
                                                lineNumber: 135,
                                                columnNumber: 17
                                            }, this),
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("input", {
                                                type: "text",
                                                className: "form-control",
                                                name: "idNumber",
                                                placeholder: "Enter your ID number given by the Institute",
                                                value: this.state.idNumber,
                                                onChange: this.onChange,
                                                required: true
                                            }, void 0, false, {
                                                fileName: "src/components/IT20125202/user/CreateUser.js",
                                                lineNumber: 136,
                                                columnNumber: 17
                                            }, this)
                                        ]
                                    }, void 0, true, {
                                        fileName: "src/components/IT20125202/user/CreateUser.js",
                                        lineNumber: 134,
                                        columnNumber: 15
                                    }, this),
                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                        className: "form-group",
                                        style: {
                                            marginBottom: '15px'
                                        },
                                        children: [
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("label", {
                                                style: {
                                                    marginBottom: '5px'
                                                },
                                                children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("b", {
                                                    children: "Name with Initials"
                                                }, void 0, false, {
                                                    fileName: "src/components/IT20125202/user/CreateUser.js",
                                                    lineNumber: 148,
                                                    columnNumber: 56
                                                }, this)
                                            }, void 0, false, {
                                                fileName: "src/components/IT20125202/user/CreateUser.js",
                                                lineNumber: 148,
                                                columnNumber: 17
                                            }, this),
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("input", {
                                                type: "text",
                                                className: "form-control",
                                                name: "name",
                                                placeholder: "Enter your name as the name given to the Institute",
                                                value: this.state.name,
                                                onChange: this.onChange,
                                                required: true
                                            }, void 0, false, {
                                                fileName: "src/components/IT20125202/user/CreateUser.js",
                                                lineNumber: 149,
                                                columnNumber: 17
                                            }, this)
                                        ]
                                    }, void 0, true, {
                                        fileName: "src/components/IT20125202/user/CreateUser.js",
                                        lineNumber: 147,
                                        columnNumber: 15
                                    }, this),
                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                        className: "form-group",
                                        style: {
                                            marginBottom: '15px'
                                        },
                                        children: [
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("label", {
                                                style: {
                                                    marginBottom: '5px'
                                                },
                                                children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("b", {
                                                    children: "Email"
                                                }, void 0, false, {
                                                    fileName: "src/components/IT20125202/user/CreateUser.js",
                                                    lineNumber: 161,
                                                    columnNumber: 56
                                                }, this)
                                            }, void 0, false, {
                                                fileName: "src/components/IT20125202/user/CreateUser.js",
                                                lineNumber: 161,
                                                columnNumber: 17
                                            }, this),
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("input", {
                                                type: "email",
                                                className: "form-control",
                                                name: "email",
                                                placeholder: "Enter a email address that you are currently using",
                                                value: this.state.email,
                                                onChange: this.onChange,
                                                required: true
                                            }, void 0, false, {
                                                fileName: "src/components/IT20125202/user/CreateUser.js",
                                                lineNumber: 162,
                                                columnNumber: 17
                                            }, this)
                                        ]
                                    }, void 0, true, {
                                        fileName: "src/components/IT20125202/user/CreateUser.js",
                                        lineNumber: 160,
                                        columnNumber: 15
                                    }, this),
                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                        className: "form-group",
                                        style: {
                                            marginBottom: '15px'
                                        },
                                        children: [
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("label", {
                                                style: {
                                                    marginBottom: '5px'
                                                },
                                                children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("b", {
                                                    children: "Mobile Number"
                                                }, void 0, false, {
                                                    fileName: "src/components/IT20125202/user/CreateUser.js",
                                                    lineNumber: 174,
                                                    columnNumber: 56
                                                }, this)
                                            }, void 0, false, {
                                                fileName: "src/components/IT20125202/user/CreateUser.js",
                                                lineNumber: 174,
                                                columnNumber: 17
                                            }, this),
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("input", {
                                                type: "text",
                                                className: "form-control",
                                                name: "mobile",
                                                placeholder: "Enter your mobile number",
                                                value: this.state.mobile,
                                                onChange: this.onChange,
                                                required: true
                                            }, void 0, false, {
                                                fileName: "src/components/IT20125202/user/CreateUser.js",
                                                lineNumber: 175,
                                                columnNumber: 17
                                            }, this)
                                        ]
                                    }, void 0, true, {
                                        fileName: "src/components/IT20125202/user/CreateUser.js",
                                        lineNumber: 173,
                                        columnNumber: 15
                                    }, this),
                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                        className: "form-group",
                                        style: {
                                            marginBottom: '15px'
                                        },
                                        children: [
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("label", {
                                                style: {
                                                    marginBottom: '5px'
                                                },
                                                children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("b", {
                                                    children: "Type"
                                                }, void 0, false, {
                                                    fileName: "src/components/IT20125202/user/CreateUser.js",
                                                    lineNumber: 187,
                                                    columnNumber: 56
                                                }, this)
                                            }, void 0, false, {
                                                fileName: "src/components/IT20125202/user/CreateUser.js",
                                                lineNumber: 187,
                                                columnNumber: 17
                                            }, this),
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("select", {
                                                name: "type",
                                                value: this.state.type,
                                                onChange: this.onChange,
                                                className: "form-select",
                                                required: true,
                                                children: [
                                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("option", {
                                                        value: "",
                                                        children: "Select..."
                                                    }, void 0, false, {
                                                        fileName: "src/components/IT20125202/user/CreateUser.js",
                                                        lineNumber: 189,
                                                        columnNumber: 19
                                                    }, this),
                                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("option", {
                                                        value: "Panel Member",
                                                        children: "Panel Member"
                                                    }, void 0, false, {
                                                        fileName: "src/components/IT20125202/user/CreateUser.js",
                                                        lineNumber: 190,
                                                        columnNumber: 19
                                                    }, this),
                                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("option", {
                                                        value: "Student",
                                                        children: "Student"
                                                    }, void 0, false, {
                                                        fileName: "src/components/IT20125202/user/CreateUser.js",
                                                        lineNumber: 191,
                                                        columnNumber: 19
                                                    }, this),
                                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("option", {
                                                        value: "Supervisor",
                                                        children: "Supervisor"
                                                    }, void 0, false, {
                                                        fileName: "src/components/IT20125202/user/CreateUser.js",
                                                        lineNumber: 192,
                                                        columnNumber: 19
                                                    }, this)
                                                ]
                                            }, void 0, true, {
                                                fileName: "src/components/IT20125202/user/CreateUser.js",
                                                lineNumber: 188,
                                                columnNumber: 17
                                            }, this)
                                        ]
                                    }, void 0, true, {
                                        fileName: "src/components/IT20125202/user/CreateUser.js",
                                        lineNumber: 186,
                                        columnNumber: 15
                                    }, this),
                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                        className: "form-group",
                                        style: {
                                            marginBottom: '15px'
                                        },
                                        children: [
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("label", {
                                                style: {
                                                    marginBottom: '5px'
                                                },
                                                children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("b", {
                                                    children: "Password"
                                                }, void 0, false, {
                                                    fileName: "src/components/IT20125202/user/CreateUser.js",
                                                    lineNumber: 197,
                                                    columnNumber: 56
                                                }, this)
                                            }, void 0, false, {
                                                fileName: "src/components/IT20125202/user/CreateUser.js",
                                                lineNumber: 197,
                                                columnNumber: 17
                                            }, this),
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("input", {
                                                type: "password",
                                                className: "form-control",
                                                name: "password",
                                                placeholder: "Enter a new password",
                                                value: this.state.password,
                                                onChange: this.onChange,
                                                required: true
                                            }, void 0, false, {
                                                fileName: "src/components/IT20125202/user/CreateUser.js",
                                                lineNumber: 198,
                                                columnNumber: 17
                                            }, this)
                                        ]
                                    }, void 0, true, {
                                        fileName: "src/components/IT20125202/user/CreateUser.js",
                                        lineNumber: 196,
                                        columnNumber: 15
                                    }, this),
                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                        className: "d-grid gap-2",
                                        children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("button", {
                                            className: "btn btn-outline-dark",
                                            type: "submit",
                                            style: {
                                                marginTop: '15px',
                                                width: 'cover'
                                            },
                                            onClick: this.onSubmit,
                                            children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("b", {
                                                children: [
                                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("i", {
                                                        className: "far fa-check-square"
                                                    }, void 0, false, {
                                                        fileName: "src/components/IT20125202/user/CreateUser.js",
                                                        lineNumber: 211,
                                                        columnNumber: 22
                                                    }, this),
                                                    "\xa0 Register"
                                                ]
                                            }, void 0, true, {
                                                fileName: "src/components/IT20125202/user/CreateUser.js",
                                                lineNumber: 211,
                                                columnNumber: 19
                                            }, this)
                                        }, void 0, false, {
                                            fileName: "src/components/IT20125202/user/CreateUser.js",
                                            lineNumber: 210,
                                            columnNumber: 17
                                        }, this)
                                    }, void 0, false, {
                                        fileName: "src/components/IT20125202/user/CreateUser.js",
                                        lineNumber: 209,
                                        columnNumber: 15
                                    }, this)
                                ]
                            }, void 0, true, {
                                fileName: "src/components/IT20125202/user/CreateUser.js",
                                lineNumber: 133,
                                columnNumber: 13
                            }, this)
                        ]
                    }, void 0, true, {
                        fileName: "src/components/IT20125202/user/CreateUser.js",
                        lineNumber: 130,
                        columnNumber: 11
                    }, this)
                ]
            }, void 0, true, {
                fileName: "src/components/IT20125202/user/CreateUser.js",
                lineNumber: 127,
                columnNumber: 9
            }, this)
        }, void 0, false, {
            fileName: "src/components/IT20125202/user/CreateUser.js",
            lineNumber: 126,
            columnNumber: 7
        }, this);
    }
}
exports.default = CreateUser;

  $parcel$ReactRefreshHelpers$07fe.postlude(module);
} finally {
  window.$RefreshReg$ = prevRefreshReg;
  window.$RefreshSig$ = prevRefreshSig;
}
},{"react/jsx-dev-runtime":"iTorj","react":"21dqq","./UserFunctions":"jO5cO","sweetalert":"38a65","@parcel/transformer-js/src/esmodule-helpers.js":"gkKU3","@parcel/transformer-react-refresh-wrap/lib/helpers/helpers.js":"km3Ru"}],"jO5cO":[function(require,module,exports) {
var parcelHelpers = require("@parcel/transformer-js/src/esmodule-helpers.js");
parcelHelpers.defineInteropFlag(exports);
parcelHelpers.export(exports, "userRegister", ()=>userRegister
);
//for registration
var _axios = require("axios");
var _axiosDefault = parcelHelpers.interopDefault(_axios);
const userRegister = (newUser)=>{
    return _axiosDefault.default.post('http://localhost:5000/user/registration', {
        idNumber: newUser.idNumber,
        name: newUser.name,
        email: newUser.email,
        mobile: newUser.mobile,
        type: newUser.type,
        password: newUser.password
    }).then((res)=>{
        res.data.success;
    }).catch((err)=>{
        return err;
    });
};

},{"axios":"jo6P5","@parcel/transformer-js/src/esmodule-helpers.js":"gkKU3"}],"jo6P5":[function(require,module,exports) {
module.exports = require('./lib/axios');

},{"./lib/axios":"63MyY"}],"63MyY":[function(require,module,exports) {
'use strict';
var utils = require('./utils');
var bind = require('./helpers/bind');
var Axios = require('./core/Axios');
var mergeConfig = require('./core/mergeConfig');
var defaults = require('./defaults');
/**
 * Create an instance of Axios
 *
 * @param {Object} defaultConfig The default config for the instance
 * @return {Axios} A new instance of Axios
 */ function createInstance(defaultConfig) {
    var context = new Axios(defaultConfig);
    var instance = bind(Axios.prototype.request, context);
    // Copy axios.prototype to instance
    utils.extend(instance, Axios.prototype, context);
    // Copy context to instance
    utils.extend(instance, context);
    // Factory for creating new instances
    instance.create = function create(instanceConfig) {
        return createInstance(mergeConfig(defaultConfig, instanceConfig));
    };
    return instance;
}
// Create the default instance to be exported
var axios = createInstance(defaults);
// Expose Axios class to allow class inheritance
axios.Axios = Axios;
// Expose Cancel & CancelToken
axios.Cancel = require('./cancel/Cancel');
axios.CancelToken = require('./cancel/CancelToken');
axios.isCancel = require('./cancel/isCancel');
axios.VERSION = require('./env/data').version;
// Expose all/spread
axios.all = function all(promises) {
    return Promise.all(promises);
};
axios.spread = require('./helpers/spread');
// Expose isAxiosError
axios.isAxiosError = require('./helpers/isAxiosError');
module.exports = axios;
// Allow use of default import syntax in TypeScript
module.exports.default = axios;

},{"./utils":"5By4s","./helpers/bind":"haRQb","./core/Axios":"cpqD8","./core/mergeConfig":"b85oP","./defaults":"hXfHM","./cancel/Cancel":"kjMy2","./cancel/CancelToken":"45wzn","./cancel/isCancel":"a0VmF","./env/data":"h29L9","./helpers/spread":"dyQ8N","./helpers/isAxiosError":"eyiLq"}],"5By4s":[function(require,module,exports) {
'use strict';
var bind = require('./helpers/bind');
// utils is a library of generic helper functions non-specific to axios
var toString = Object.prototype.toString;
/**
 * Determine if a value is an Array
 *
 * @param {Object} val The value to test
 * @returns {boolean} True if value is an Array, otherwise false
 */ function isArray(val) {
    return Array.isArray(val);
}
/**
 * Determine if a value is undefined
 *
 * @param {Object} val The value to test
 * @returns {boolean} True if the value is undefined, otherwise false
 */ function isUndefined(val) {
    return typeof val === 'undefined';
}
/**
 * Determine if a value is a Buffer
 *
 * @param {Object} val The value to test
 * @returns {boolean} True if value is a Buffer, otherwise false
 */ function isBuffer(val) {
    return val !== null && !isUndefined(val) && val.constructor !== null && !isUndefined(val.constructor) && typeof val.constructor.isBuffer === 'function' && val.constructor.isBuffer(val);
}
/**
 * Determine if a value is an ArrayBuffer
 *
 * @param {Object} val The value to test
 * @returns {boolean} True if value is an ArrayBuffer, otherwise false
 */ function isArrayBuffer(val) {
    return toString.call(val) === '[object ArrayBuffer]';
}
/**
 * Determine if a value is a FormData
 *
 * @param {Object} val The value to test
 * @returns {boolean} True if value is an FormData, otherwise false
 */ function isFormData(val) {
    return toString.call(val) === '[object FormData]';
}
/**
 * Determine if a value is a view on an ArrayBuffer
 *
 * @param {Object} val The value to test
 * @returns {boolean} True if value is a view on an ArrayBuffer, otherwise false
 */ function isArrayBufferView(val) {
    var result;
    if (typeof ArrayBuffer !== 'undefined' && ArrayBuffer.isView) result = ArrayBuffer.isView(val);
    else result = val && val.buffer && isArrayBuffer(val.buffer);
    return result;
}
/**
 * Determine if a value is a String
 *
 * @param {Object} val The value to test
 * @returns {boolean} True if value is a String, otherwise false
 */ function isString(val) {
    return typeof val === 'string';
}
/**
 * Determine if a value is a Number
 *
 * @param {Object} val The value to test
 * @returns {boolean} True if value is a Number, otherwise false
 */ function isNumber(val) {
    return typeof val === 'number';
}
/**
 * Determine if a value is an Object
 *
 * @param {Object} val The value to test
 * @returns {boolean} True if value is an Object, otherwise false
 */ function isObject(val) {
    return val !== null && typeof val === 'object';
}
/**
 * Determine if a value is a plain Object
 *
 * @param {Object} val The value to test
 * @return {boolean} True if value is a plain Object, otherwise false
 */ function isPlainObject(val) {
    if (toString.call(val) !== '[object Object]') return false;
    var prototype = Object.getPrototypeOf(val);
    return prototype === null || prototype === Object.prototype;
}
/**
 * Determine if a value is a Date
 *
 * @param {Object} val The value to test
 * @returns {boolean} True if value is a Date, otherwise false
 */ function isDate(val) {
    return toString.call(val) === '[object Date]';
}
/**
 * Determine if a value is a File
 *
 * @param {Object} val The value to test
 * @returns {boolean} True if value is a File, otherwise false
 */ function isFile(val) {
    return toString.call(val) === '[object File]';
}
/**
 * Determine if a value is a Blob
 *
 * @param {Object} val The value to test
 * @returns {boolean} True if value is a Blob, otherwise false
 */ function isBlob(val) {
    return toString.call(val) === '[object Blob]';
}
/**
 * Determine if a value is a Function
 *
 * @param {Object} val The value to test
 * @returns {boolean} True if value is a Function, otherwise false
 */ function isFunction(val) {
    return toString.call(val) === '[object Function]';
}
/**
 * Determine if a value is a Stream
 *
 * @param {Object} val The value to test
 * @returns {boolean} True if value is a Stream, otherwise false
 */ function isStream(val) {
    return isObject(val) && isFunction(val.pipe);
}
/**
 * Determine if a value is a URLSearchParams object
 *
 * @param {Object} val The value to test
 * @returns {boolean} True if value is a URLSearchParams object, otherwise false
 */ function isURLSearchParams(val) {
    return toString.call(val) === '[object URLSearchParams]';
}
/**
 * Trim excess whitespace off the beginning and end of a string
 *
 * @param {String} str The String to trim
 * @returns {String} The String freed of excess whitespace
 */ function trim(str) {
    return str.trim ? str.trim() : str.replace(/^\s+|\s+$/g, '');
}
/**
 * Determine if we're running in a standard browser environment
 *
 * This allows axios to run in a web worker, and react-native.
 * Both environments support XMLHttpRequest, but not fully standard globals.
 *
 * web workers:
 *  typeof window -> undefined
 *  typeof document -> undefined
 *
 * react-native:
 *  navigator.product -> 'ReactNative'
 * nativescript
 *  navigator.product -> 'NativeScript' or 'NS'
 */ function isStandardBrowserEnv() {
    if (typeof navigator !== 'undefined' && (navigator.product === 'ReactNative' || navigator.product === 'NativeScript' || navigator.product === 'NS')) return false;
    return typeof window !== 'undefined' && typeof document !== 'undefined';
}
/**
 * Iterate over an Array or an Object invoking a function for each item.
 *
 * If `obj` is an Array callback will be called passing
 * the value, index, and complete array for each item.
 *
 * If 'obj' is an Object callback will be called passing
 * the value, key, and complete object for each property.
 *
 * @param {Object|Array} obj The object to iterate
 * @param {Function} fn The callback to invoke for each item
 */ function forEach(obj, fn) {
    // Don't bother if no value provided
    if (obj === null || typeof obj === 'undefined') return;
    // Force an array if not already something iterable
    if (typeof obj !== 'object') /*eslint no-param-reassign:0*/ obj = [
        obj
    ];
    if (isArray(obj)) // Iterate over array values
    for(var i = 0, l = obj.length; i < l; i++)fn.call(null, obj[i], i, obj);
    else {
        // Iterate over object keys
        for(var key in obj)if (Object.prototype.hasOwnProperty.call(obj, key)) fn.call(null, obj[key], key, obj);
    }
}
/**
 * Accepts varargs expecting each argument to be an object, then
 * immutably merges the properties of each object and returns result.
 *
 * When multiple objects contain the same key the later object in
 * the arguments list will take precedence.
 *
 * Example:
 *
 * ```js
 * var result = merge({foo: 123}, {foo: 456});
 * console.log(result.foo); // outputs 456
 * ```
 *
 * @param {Object} obj1 Object to merge
 * @returns {Object} Result of all merge properties
 */ function merge() {
    var result = {};
    function assignValue(val, key) {
        if (isPlainObject(result[key]) && isPlainObject(val)) result[key] = merge(result[key], val);
        else if (isPlainObject(val)) result[key] = merge({}, val);
        else if (isArray(val)) result[key] = val.slice();
        else result[key] = val;
    }
    for(var i = 0, l = arguments.length; i < l; i++)forEach(arguments[i], assignValue);
    return result;
}
/**
 * Extends object a by mutably adding to it the properties of object b.
 *
 * @param {Object} a The object to be extended
 * @param {Object} b The object to copy properties from
 * @param {Object} thisArg The object to bind function to
 * @return {Object} The resulting value of object a
 */ function extend(a, b, thisArg) {
    forEach(b, function assignValue(val, key) {
        if (thisArg && typeof val === 'function') a[key] = bind(val, thisArg);
        else a[key] = val;
    });
    return a;
}
/**
 * Remove byte order marker. This catches EF BB BF (the UTF-8 BOM)
 *
 * @param {string} content with BOM
 * @return {string} content value without BOM
 */ function stripBOM(content) {
    if (content.charCodeAt(0) === 0xFEFF) content = content.slice(1);
    return content;
}
module.exports = {
    isArray: isArray,
    isArrayBuffer: isArrayBuffer,
    isBuffer: isBuffer,
    isFormData: isFormData,
    isArrayBufferView: isArrayBufferView,
    isString: isString,
    isNumber: isNumber,
    isObject: isObject,
    isPlainObject: isPlainObject,
    isUndefined: isUndefined,
    isDate: isDate,
    isFile: isFile,
    isBlob: isBlob,
    isFunction: isFunction,
    isStream: isStream,
    isURLSearchParams: isURLSearchParams,
    isStandardBrowserEnv: isStandardBrowserEnv,
    forEach: forEach,
    merge: merge,
    extend: extend,
    trim: trim,
    stripBOM: stripBOM
};

},{"./helpers/bind":"haRQb"}],"haRQb":[function(require,module,exports) {
'use strict';
module.exports = function bind(fn, thisArg) {
    return function wrap() {
        var args = new Array(arguments.length);
        for(var i = 0; i < args.length; i++)args[i] = arguments[i];
        return fn.apply(thisArg, args);
    };
};

},{}],"cpqD8":[function(require,module,exports) {
'use strict';
var utils = require('./../utils');
var buildURL = require('../helpers/buildURL');
var InterceptorManager = require('./InterceptorManager');
var dispatchRequest = require('./dispatchRequest');
var mergeConfig = require('./mergeConfig');
var validator = require('../helpers/validator');
var validators = validator.validators;
/**
 * Create a new instance of Axios
 *
 * @param {Object} instanceConfig The default config for the instance
 */ function Axios(instanceConfig) {
    this.defaults = instanceConfig;
    this.interceptors = {
        request: new InterceptorManager(),
        response: new InterceptorManager()
    };
}
/**
 * Dispatch a request
 *
 * @param {Object} config The config specific for this request (merged with this.defaults)
 */ Axios.prototype.request = function request(configOrUrl, config) {
    /*eslint no-param-reassign:0*/ // Allow for axios('example/url'[, config]) a la fetch API
    if (typeof configOrUrl === 'string') {
        config = config || {};
        config.url = configOrUrl;
    } else config = configOrUrl || {};
    config = mergeConfig(this.defaults, config);
    // Set config.method
    if (config.method) config.method = config.method.toLowerCase();
    else if (this.defaults.method) config.method = this.defaults.method.toLowerCase();
    else config.method = 'get';
    var transitional = config.transitional;
    if (transitional !== undefined) validator.assertOptions(transitional, {
        silentJSONParsing: validators.transitional(validators.boolean),
        forcedJSONParsing: validators.transitional(validators.boolean),
        clarifyTimeoutError: validators.transitional(validators.boolean)
    }, false);
    // filter out skipped interceptors
    var requestInterceptorChain = [];
    var synchronousRequestInterceptors = true;
    this.interceptors.request.forEach(function unshiftRequestInterceptors(interceptor) {
        if (typeof interceptor.runWhen === 'function' && interceptor.runWhen(config) === false) return;
        synchronousRequestInterceptors = synchronousRequestInterceptors && interceptor.synchronous;
        requestInterceptorChain.unshift(interceptor.fulfilled, interceptor.rejected);
    });
    var responseInterceptorChain = [];
    this.interceptors.response.forEach(function pushResponseInterceptors(interceptor) {
        responseInterceptorChain.push(interceptor.fulfilled, interceptor.rejected);
    });
    var promise;
    if (!synchronousRequestInterceptors) {
        var chain = [
            dispatchRequest,
            undefined
        ];
        Array.prototype.unshift.apply(chain, requestInterceptorChain);
        chain = chain.concat(responseInterceptorChain);
        promise = Promise.resolve(config);
        while(chain.length)promise = promise.then(chain.shift(), chain.shift());
        return promise;
    }
    var newConfig = config;
    while(requestInterceptorChain.length){
        var onFulfilled = requestInterceptorChain.shift();
        var onRejected = requestInterceptorChain.shift();
        try {
            newConfig = onFulfilled(newConfig);
        } catch (error) {
            onRejected(error);
            break;
        }
    }
    try {
        promise = dispatchRequest(newConfig);
    } catch (error) {
        return Promise.reject(error);
    }
    while(responseInterceptorChain.length)promise = promise.then(responseInterceptorChain.shift(), responseInterceptorChain.shift());
    return promise;
};
Axios.prototype.getUri = function getUri(config) {
    config = mergeConfig(this.defaults, config);
    return buildURL(config.url, config.params, config.paramsSerializer).replace(/^\?/, '');
};
// Provide aliases for supported request methods
utils.forEach([
    'delete',
    'get',
    'head',
    'options'
], function forEachMethodNoData(method) {
    /*eslint func-names:0*/ Axios.prototype[method] = function(url, config) {
        return this.request(mergeConfig(config || {}, {
            method: method,
            url: url,
            data: (config || {}).data
        }));
    };
});
utils.forEach([
    'post',
    'put',
    'patch'
], function forEachMethodWithData(method) {
    /*eslint func-names:0*/ Axios.prototype[method] = function(url, data, config) {
        return this.request(mergeConfig(config || {}, {
            method: method,
            url: url,
            data: data
        }));
    };
});
module.exports = Axios;

},{"./../utils":"5By4s","../helpers/buildURL":"3bwC2","./InterceptorManager":"1VRIM","./dispatchRequest":"6sjJ6","./mergeConfig":"b85oP","../helpers/validator":"9vgkY"}],"3bwC2":[function(require,module,exports) {
'use strict';
var utils = require('./../utils');
function encode(val) {
    return encodeURIComponent(val).replace(/%3A/gi, ':').replace(/%24/g, '$').replace(/%2C/gi, ',').replace(/%20/g, '+').replace(/%5B/gi, '[').replace(/%5D/gi, ']');
}
/**
 * Build a URL by appending params to the end
 *
 * @param {string} url The base of the url (e.g., http://www.google.com)
 * @param {object} [params] The params to be appended
 * @returns {string} The formatted url
 */ module.exports = function buildURL(url, params, paramsSerializer) {
    /*eslint no-param-reassign:0*/ if (!params) return url;
    var serializedParams;
    if (paramsSerializer) serializedParams = paramsSerializer(params);
    else if (utils.isURLSearchParams(params)) serializedParams = params.toString();
    else {
        var parts = [];
        utils.forEach(params, function serialize(val, key) {
            if (val === null || typeof val === 'undefined') return;
            if (utils.isArray(val)) key = key + '[]';
            else val = [
                val
            ];
            utils.forEach(val, function parseValue(v) {
                if (utils.isDate(v)) v = v.toISOString();
                else if (utils.isObject(v)) v = JSON.stringify(v);
                parts.push(encode(key) + '=' + encode(v));
            });
        });
        serializedParams = parts.join('&');
    }
    if (serializedParams) {
        var hashmarkIndex = url.indexOf('#');
        if (hashmarkIndex !== -1) url = url.slice(0, hashmarkIndex);
        url += (url.indexOf('?') === -1 ? '?' : '&') + serializedParams;
    }
    return url;
};

},{"./../utils":"5By4s"}],"1VRIM":[function(require,module,exports) {
'use strict';
var utils = require('./../utils');
function InterceptorManager() {
    this.handlers = [];
}
/**
 * Add a new interceptor to the stack
 *
 * @param {Function} fulfilled The function to handle `then` for a `Promise`
 * @param {Function} rejected The function to handle `reject` for a `Promise`
 *
 * @return {Number} An ID used to remove interceptor later
 */ InterceptorManager.prototype.use = function use(fulfilled, rejected, options) {
    this.handlers.push({
        fulfilled: fulfilled,
        rejected: rejected,
        synchronous: options ? options.synchronous : false,
        runWhen: options ? options.runWhen : null
    });
    return this.handlers.length - 1;
};
/**
 * Remove an interceptor from the stack
 *
 * @param {Number} id The ID that was returned by `use`
 */ InterceptorManager.prototype.eject = function eject(id) {
    if (this.handlers[id]) this.handlers[id] = null;
};
/**
 * Iterate over all the registered interceptors
 *
 * This method is particularly useful for skipping over any
 * interceptors that may have become `null` calling `eject`.
 *
 * @param {Function} fn The function to call for each interceptor
 */ InterceptorManager.prototype.forEach = function forEach(fn) {
    utils.forEach(this.handlers, function forEachHandler(h) {
        if (h !== null) fn(h);
    });
};
module.exports = InterceptorManager;

},{"./../utils":"5By4s"}],"6sjJ6":[function(require,module,exports) {
'use strict';
var utils = require('./../utils');
var transformData = require('./transformData');
var isCancel = require('../cancel/isCancel');
var defaults = require('../defaults');
var Cancel = require('../cancel/Cancel');
/**
 * Throws a `Cancel` if cancellation has been requested.
 */ function throwIfCancellationRequested(config) {
    if (config.cancelToken) config.cancelToken.throwIfRequested();
    if (config.signal && config.signal.aborted) throw new Cancel('canceled');
}
/**
 * Dispatch a request to the server using the configured adapter.
 *
 * @param {object} config The config that is to be used for the request
 * @returns {Promise} The Promise to be fulfilled
 */ module.exports = function dispatchRequest(config) {
    throwIfCancellationRequested(config);
    // Ensure headers exist
    config.headers = config.headers || {};
    // Transform request data
    config.data = transformData.call(config, config.data, config.headers, config.transformRequest);
    // Flatten headers
    config.headers = utils.merge(config.headers.common || {}, config.headers[config.method] || {}, config.headers);
    utils.forEach([
        'delete',
        'get',
        'head',
        'post',
        'put',
        'patch',
        'common'
    ], function cleanHeaderConfig(method) {
        delete config.headers[method];
    });
    var adapter = config.adapter || defaults.adapter;
    return adapter(config).then(function onAdapterResolution(response) {
        throwIfCancellationRequested(config);
        // Transform response data
        response.data = transformData.call(config, response.data, response.headers, config.transformResponse);
        return response;
    }, function onAdapterRejection(reason) {
        if (!isCancel(reason)) {
            throwIfCancellationRequested(config);
            // Transform response data
            if (reason && reason.response) reason.response.data = transformData.call(config, reason.response.data, reason.response.headers, config.transformResponse);
        }
        return Promise.reject(reason);
    });
};

},{"./../utils":"5By4s","./transformData":"eRqJY","../cancel/isCancel":"a0VmF","../defaults":"hXfHM","../cancel/Cancel":"kjMy2"}],"eRqJY":[function(require,module,exports) {
'use strict';
var utils = require('./../utils');
var defaults = require('../defaults');
/**
 * Transform the data for a request or a response
 *
 * @param {Object|String} data The data to be transformed
 * @param {Array} headers The headers for the request or response
 * @param {Array|Function} fns A single function or Array of functions
 * @returns {*} The resulting transformed data
 */ module.exports = function transformData(data, headers, fns) {
    var context = this || defaults;
    /*eslint no-param-reassign:0*/ utils.forEach(fns, function transform(fn) {
        data = fn.call(context, data, headers);
    });
    return data;
};

},{"./../utils":"5By4s","../defaults":"hXfHM"}],"hXfHM":[function(require,module,exports) {
'use strict';
var process = require("process");
var utils = require('../utils');
var normalizeHeaderName = require('../helpers/normalizeHeaderName');
var enhanceError = require('../core/enhanceError');
var transitionalDefaults = require('./transitional');
var DEFAULT_CONTENT_TYPE = {
    'Content-Type': 'application/x-www-form-urlencoded'
};
function setContentTypeIfUnset(headers, value) {
    if (!utils.isUndefined(headers) && utils.isUndefined(headers['Content-Type'])) headers['Content-Type'] = value;
}
function getDefaultAdapter() {
    var adapter;
    if (typeof XMLHttpRequest !== 'undefined') // For browsers use XHR adapter
    adapter = require('../adapters/xhr');
    else if (typeof process !== 'undefined' && Object.prototype.toString.call(process) === '[object process]') // For node use HTTP adapter
    adapter = require('../adapters/http');
    return adapter;
}
function stringifySafely(rawValue, parser, encoder) {
    if (utils.isString(rawValue)) try {
        (parser || JSON.parse)(rawValue);
        return utils.trim(rawValue);
    } catch (e) {
        if (e.name !== 'SyntaxError') throw e;
    }
    return (encoder || JSON.stringify)(rawValue);
}
var defaults = {
    transitional: transitionalDefaults,
    adapter: getDefaultAdapter(),
    transformRequest: [
        function transformRequest(data, headers) {
            normalizeHeaderName(headers, 'Accept');
            normalizeHeaderName(headers, 'Content-Type');
            if (utils.isFormData(data) || utils.isArrayBuffer(data) || utils.isBuffer(data) || utils.isStream(data) || utils.isFile(data) || utils.isBlob(data)) return data;
            if (utils.isArrayBufferView(data)) return data.buffer;
            if (utils.isURLSearchParams(data)) {
                setContentTypeIfUnset(headers, 'application/x-www-form-urlencoded;charset=utf-8');
                return data.toString();
            }
            if (utils.isObject(data) || headers && headers['Content-Type'] === 'application/json') {
                setContentTypeIfUnset(headers, 'application/json');
                return stringifySafely(data);
            }
            return data;
        }
    ],
    transformResponse: [
        function transformResponse(data) {
            var transitional = this.transitional || defaults.transitional;
            var silentJSONParsing = transitional && transitional.silentJSONParsing;
            var forcedJSONParsing = transitional && transitional.forcedJSONParsing;
            var strictJSONParsing = !silentJSONParsing && this.responseType === 'json';
            if (strictJSONParsing || forcedJSONParsing && utils.isString(data) && data.length) try {
                return JSON.parse(data);
            } catch (e) {
                if (strictJSONParsing) {
                    if (e.name === 'SyntaxError') throw enhanceError(e, this, 'E_JSON_PARSE');
                    throw e;
                }
            }
            return data;
        }
    ],
    /**
   * A timeout in milliseconds to abort a request. If set to 0 (default) a
   * timeout is not created.
   */ timeout: 0,
    xsrfCookieName: 'XSRF-TOKEN',
    xsrfHeaderName: 'X-XSRF-TOKEN',
    maxContentLength: -1,
    maxBodyLength: -1,
    validateStatus: function validateStatus(status) {
        return status >= 200 && status < 300;
    },
    headers: {
        common: {
            'Accept': 'application/json, text/plain, */*'
        }
    }
};
utils.forEach([
    'delete',
    'get',
    'head'
], function forEachMethodNoData(method) {
    defaults.headers[method] = {};
});
utils.forEach([
    'post',
    'put',
    'patch'
], function forEachMethodWithData(method) {
    defaults.headers[method] = utils.merge(DEFAULT_CONTENT_TYPE);
});
module.exports = defaults;

},{"process":"d5jf4","../utils":"5By4s","../helpers/normalizeHeaderName":"adBZo","../core/enhanceError":"itUQr","./transitional":"lM32f","../adapters/xhr":"ldm57","../adapters/http":"ldm57"}],"d5jf4":[function(require,module,exports) {
// shim for using process in browser
var process = module.exports = {};
// cached from whatever global is present so that test runners that stub it
// don't break things.  But we need to wrap it in a try catch in case it is
// wrapped in strict mode code which doesn't define any globals.  It's inside a
// function because try/catches deoptimize in certain engines.
var cachedSetTimeout;
var cachedClearTimeout;
function defaultSetTimout() {
    throw new Error('setTimeout has not been defined');
}
function defaultClearTimeout() {
    throw new Error('clearTimeout has not been defined');
}
(function() {
    try {
        if (typeof setTimeout === 'function') cachedSetTimeout = setTimeout;
        else cachedSetTimeout = defaultSetTimout;
    } catch (e) {
        cachedSetTimeout = defaultSetTimout;
    }
    try {
        if (typeof clearTimeout === 'function') cachedClearTimeout = clearTimeout;
        else cachedClearTimeout = defaultClearTimeout;
    } catch (e1) {
        cachedClearTimeout = defaultClearTimeout;
    }
})();
function runTimeout(fun) {
    if (cachedSetTimeout === setTimeout) //normal enviroments in sane situations
    return setTimeout(fun, 0);
    // if setTimeout wasn't available but was latter defined
    if ((cachedSetTimeout === defaultSetTimout || !cachedSetTimeout) && setTimeout) {
        cachedSetTimeout = setTimeout;
        return setTimeout(fun, 0);
    }
    try {
        // when when somebody has screwed with setTimeout but no I.E. maddness
        return cachedSetTimeout(fun, 0);
    } catch (e) {
        try {
            // When we are in I.E. but the script has been evaled so I.E. doesn't trust the global object when called normally
            return cachedSetTimeout.call(null, fun, 0);
        } catch (e) {
            // same as above but when it's a version of I.E. that must have the global object for 'this', hopfully our context correct otherwise it will throw a global error
            return cachedSetTimeout.call(this, fun, 0);
        }
    }
}
function runClearTimeout(marker) {
    if (cachedClearTimeout === clearTimeout) //normal enviroments in sane situations
    return clearTimeout(marker);
    // if clearTimeout wasn't available but was latter defined
    if ((cachedClearTimeout === defaultClearTimeout || !cachedClearTimeout) && clearTimeout) {
        cachedClearTimeout = clearTimeout;
        return clearTimeout(marker);
    }
    try {
        // when when somebody has screwed with setTimeout but no I.E. maddness
        return cachedClearTimeout(marker);
    } catch (e) {
        try {
            // When we are in I.E. but the script has been evaled so I.E. doesn't  trust the global object when called normally
            return cachedClearTimeout.call(null, marker);
        } catch (e) {
            // same as above but when it's a version of I.E. that must have the global object for 'this', hopfully our context correct otherwise it will throw a global error.
            // Some versions of I.E. have different rules for clearTimeout vs setTimeout
            return cachedClearTimeout.call(this, marker);
        }
    }
}
var queue = [];
var draining = false;
var currentQueue;
var queueIndex = -1;
function cleanUpNextTick() {
    if (!draining || !currentQueue) return;
    draining = false;
    if (currentQueue.length) queue = currentQueue.concat(queue);
    else queueIndex = -1;
    if (queue.length) drainQueue();
}
function drainQueue() {
    if (draining) return;
    var timeout = runTimeout(cleanUpNextTick);
    draining = true;
    var len = queue.length;
    while(len){
        currentQueue = queue;
        queue = [];
        while(++queueIndex < len)if (currentQueue) currentQueue[queueIndex].run();
        queueIndex = -1;
        len = queue.length;
    }
    currentQueue = null;
    draining = false;
    runClearTimeout(timeout);
}
process.nextTick = function(fun) {
    var args = new Array(arguments.length - 1);
    if (arguments.length > 1) for(var i = 1; i < arguments.length; i++)args[i - 1] = arguments[i];
    queue.push(new Item(fun, args));
    if (queue.length === 1 && !draining) runTimeout(drainQueue);
};
// v8 likes predictible objects
function Item(fun, array) {
    this.fun = fun;
    this.array = array;
}
Item.prototype.run = function() {
    this.fun.apply(null, this.array);
};
process.title = 'browser';
process.browser = true;
process.env = {};
process.argv = [];
process.version = ''; // empty string to avoid regexp issues
process.versions = {};
function noop() {}
process.on = noop;
process.addListener = noop;
process.once = noop;
process.off = noop;
process.removeListener = noop;
process.removeAllListeners = noop;
process.emit = noop;
process.prependListener = noop;
process.prependOnceListener = noop;
process.listeners = function(name) {
    return [];
};
process.binding = function(name) {
    throw new Error('process.binding is not supported');
};
process.cwd = function() {
    return '/';
};
process.chdir = function(dir) {
    throw new Error('process.chdir is not supported');
};
process.umask = function() {
    return 0;
};

},{}],"adBZo":[function(require,module,exports) {
'use strict';
var utils = require('../utils');
module.exports = function normalizeHeaderName(headers, normalizedName) {
    utils.forEach(headers, function processHeader(value, name) {
        if (name !== normalizedName && name.toUpperCase() === normalizedName.toUpperCase()) {
            headers[normalizedName] = value;
            delete headers[name];
        }
    });
};

},{"../utils":"5By4s"}],"itUQr":[function(require,module,exports) {
'use strict';
/**
 * Update an Error with the specified config, error code, and response.
 *
 * @param {Error} error The error to update.
 * @param {Object} config The config.
 * @param {string} [code] The error code (for example, 'ECONNABORTED').
 * @param {Object} [request] The request.
 * @param {Object} [response] The response.
 * @returns {Error} The error.
 */ module.exports = function enhanceError(error, config, code, request, response) {
    error.config = config;
    if (code) error.code = code;
    error.request = request;
    error.response = response;
    error.isAxiosError = true;
    error.toJSON = function toJSON() {
        return {
            // Standard
            message: this.message,
            name: this.name,
            // Microsoft
            description: this.description,
            number: this.number,
            // Mozilla
            fileName: this.fileName,
            lineNumber: this.lineNumber,
            columnNumber: this.columnNumber,
            stack: this.stack,
            // Axios
            config: this.config,
            code: this.code,
            status: this.response && this.response.status ? this.response.status : null
        };
    };
    return error;
};

},{}],"lM32f":[function(require,module,exports) {
'use strict';
module.exports = {
    silentJSONParsing: true,
    forcedJSONParsing: true,
    clarifyTimeoutError: false
};

},{}],"ldm57":[function(require,module,exports) {
'use strict';
var utils = require('./../utils');
var settle = require('./../core/settle');
var cookies = require('./../helpers/cookies');
var buildURL = require('./../helpers/buildURL');
var buildFullPath = require('../core/buildFullPath');
var parseHeaders = require('./../helpers/parseHeaders');
var isURLSameOrigin = require('./../helpers/isURLSameOrigin');
var createError = require('../core/createError');
var transitionalDefaults = require('../defaults/transitional');
var Cancel = require('../cancel/Cancel');
module.exports = function xhrAdapter(config) {
    return new Promise(function dispatchXhrRequest(resolve, reject) {
        var requestData = config.data;
        var requestHeaders = config.headers;
        var responseType = config.responseType;
        var onCanceled;
        function done() {
            if (config.cancelToken) config.cancelToken.unsubscribe(onCanceled);
            if (config.signal) config.signal.removeEventListener('abort', onCanceled);
        }
        if (utils.isFormData(requestData)) delete requestHeaders['Content-Type']; // Let the browser set it
        var request = new XMLHttpRequest();
        // HTTP basic authentication
        if (config.auth) {
            var username = config.auth.username || '';
            var password = config.auth.password ? unescape(encodeURIComponent(config.auth.password)) : '';
            requestHeaders.Authorization = 'Basic ' + btoa(username + ':' + password);
        }
        var fullPath = buildFullPath(config.baseURL, config.url);
        request.open(config.method.toUpperCase(), buildURL(fullPath, config.params, config.paramsSerializer), true);
        // Set the request timeout in MS
        request.timeout = config.timeout;
        function onloadend() {
            if (!request) return;
            // Prepare the response
            var responseHeaders = 'getAllResponseHeaders' in request ? parseHeaders(request.getAllResponseHeaders()) : null;
            var responseData = !responseType || responseType === 'text' || responseType === 'json' ? request.responseText : request.response;
            var response = {
                data: responseData,
                status: request.status,
                statusText: request.statusText,
                headers: responseHeaders,
                config: config,
                request: request
            };
            settle(function _resolve(value) {
                resolve(value);
                done();
            }, function _reject(err) {
                reject(err);
                done();
            }, response);
            // Clean up request
            request = null;
        }
        if ('onloadend' in request) // Use onloadend if available
        request.onloadend = onloadend;
        else // Listen for ready state to emulate onloadend
        request.onreadystatechange = function handleLoad() {
            if (!request || request.readyState !== 4) return;
            // The request errored out and we didn't get a response, this will be
            // handled by onerror instead
            // With one exception: request that using file: protocol, most browsers
            // will return status as 0 even though it's a successful request
            if (request.status === 0 && !(request.responseURL && request.responseURL.indexOf('file:') === 0)) return;
            // readystate handler is calling before onerror or ontimeout handlers,
            // so we should call onloadend on the next 'tick'
            setTimeout(onloadend);
        };
        // Handle browser request cancellation (as opposed to a manual cancellation)
        request.onabort = function handleAbort() {
            if (!request) return;
            reject(createError('Request aborted', config, 'ECONNABORTED', request));
            // Clean up request
            request = null;
        };
        // Handle low level network errors
        request.onerror = function handleError() {
            // Real errors are hidden from us by the browser
            // onerror should only fire if it's a network error
            reject(createError('Network Error', config, null, request));
            // Clean up request
            request = null;
        };
        // Handle timeout
        request.ontimeout = function handleTimeout() {
            var timeoutErrorMessage = config.timeout ? 'timeout of ' + config.timeout + 'ms exceeded' : 'timeout exceeded';
            var transitional = config.transitional || transitionalDefaults;
            if (config.timeoutErrorMessage) timeoutErrorMessage = config.timeoutErrorMessage;
            reject(createError(timeoutErrorMessage, config, transitional.clarifyTimeoutError ? 'ETIMEDOUT' : 'ECONNABORTED', request));
            // Clean up request
            request = null;
        };
        // Add xsrf header
        // This is only done if running in a standard browser environment.
        // Specifically not if we're in a web worker, or react-native.
        if (utils.isStandardBrowserEnv()) {
            // Add xsrf header
            var xsrfValue = (config.withCredentials || isURLSameOrigin(fullPath)) && config.xsrfCookieName ? cookies.read(config.xsrfCookieName) : undefined;
            if (xsrfValue) requestHeaders[config.xsrfHeaderName] = xsrfValue;
        }
        // Add headers to the request
        if ('setRequestHeader' in request) utils.forEach(requestHeaders, function setRequestHeader(val, key) {
            if (typeof requestData === 'undefined' && key.toLowerCase() === 'content-type') // Remove Content-Type if data is undefined
            delete requestHeaders[key];
            else // Otherwise add header to the request
            request.setRequestHeader(key, val);
        });
        // Add withCredentials to request if needed
        if (!utils.isUndefined(config.withCredentials)) request.withCredentials = !!config.withCredentials;
        // Add responseType to request if needed
        if (responseType && responseType !== 'json') request.responseType = config.responseType;
        // Handle progress if needed
        if (typeof config.onDownloadProgress === 'function') request.addEventListener('progress', config.onDownloadProgress);
        // Not all browsers support upload events
        if (typeof config.onUploadProgress === 'function' && request.upload) request.upload.addEventListener('progress', config.onUploadProgress);
        if (config.cancelToken || config.signal) {
            // Handle cancellation
            // eslint-disable-next-line func-names
            onCanceled = function(cancel) {
                if (!request) return;
                reject(!cancel || cancel && cancel.type ? new Cancel('canceled') : cancel);
                request.abort();
                request = null;
            };
            config.cancelToken && config.cancelToken.subscribe(onCanceled);
            if (config.signal) config.signal.aborted ? onCanceled() : config.signal.addEventListener('abort', onCanceled);
        }
        if (!requestData) requestData = null;
        // Send the request
        request.send(requestData);
    });
};

},{"./../utils":"5By4s","./../core/settle":"dD9aC","./../helpers/cookies":"4WJjt","./../helpers/buildURL":"3bwC2","../core/buildFullPath":"1I5TW","./../helpers/parseHeaders":"kqDd5","./../helpers/isURLSameOrigin":"lxXtv","../core/createError":"5nVS9","../defaults/transitional":"lM32f","../cancel/Cancel":"kjMy2"}],"dD9aC":[function(require,module,exports) {
'use strict';
var createError = require('./createError');
/**
 * Resolve or reject a Promise based on response status.
 *
 * @param {Function} resolve A function that resolves the promise.
 * @param {Function} reject A function that rejects the promise.
 * @param {object} response The response.
 */ module.exports = function settle(resolve, reject, response) {
    var validateStatus = response.config.validateStatus;
    if (!response.status || !validateStatus || validateStatus(response.status)) resolve(response);
    else reject(createError('Request failed with status code ' + response.status, response.config, null, response.request, response));
};

},{"./createError":"5nVS9"}],"5nVS9":[function(require,module,exports) {
'use strict';
var enhanceError = require('./enhanceError');
/**
 * Create an Error with the specified message, config, error code, request and response.
 *
 * @param {string} message The error message.
 * @param {Object} config The config.
 * @param {string} [code] The error code (for example, 'ECONNABORTED').
 * @param {Object} [request] The request.
 * @param {Object} [response] The response.
 * @returns {Error} The created error.
 */ module.exports = function createError(message, config, code, request, response) {
    var error = new Error(message);
    return enhanceError(error, config, code, request, response);
};

},{"./enhanceError":"itUQr"}],"4WJjt":[function(require,module,exports) {
'use strict';
var utils = require('./../utils');
module.exports = utils.isStandardBrowserEnv() ? // Standard browser envs support document.cookie
function standardBrowserEnv() {
    return {
        write: function write(name, value, expires, path, domain, secure) {
            var cookie = [];
            cookie.push(name + '=' + encodeURIComponent(value));
            if (utils.isNumber(expires)) cookie.push('expires=' + new Date(expires).toGMTString());
            if (utils.isString(path)) cookie.push('path=' + path);
            if (utils.isString(domain)) cookie.push('domain=' + domain);
            if (secure === true) cookie.push('secure');
            document.cookie = cookie.join('; ');
        },
        read: function read(name) {
            var match = document.cookie.match(new RegExp('(^|;\\s*)(' + name + ')=([^;]*)'));
            return match ? decodeURIComponent(match[3]) : null;
        },
        remove: function remove(name) {
            this.write(name, '', Date.now() - 86400000);
        }
    };
}() : // Non standard browser env (web workers, react-native) lack needed support.
function nonStandardBrowserEnv() {
    return {
        write: function write() {},
        read: function read() {
            return null;
        },
        remove: function remove() {}
    };
}();

},{"./../utils":"5By4s"}],"1I5TW":[function(require,module,exports) {
'use strict';
var isAbsoluteURL = require('../helpers/isAbsoluteURL');
var combineURLs = require('../helpers/combineURLs');
/**
 * Creates a new URL by combining the baseURL with the requestedURL,
 * only when the requestedURL is not already an absolute URL.
 * If the requestURL is absolute, this function returns the requestedURL untouched.
 *
 * @param {string} baseURL The base URL
 * @param {string} requestedURL Absolute or relative URL to combine
 * @returns {string} The combined full path
 */ module.exports = function buildFullPath(baseURL, requestedURL) {
    if (baseURL && !isAbsoluteURL(requestedURL)) return combineURLs(baseURL, requestedURL);
    return requestedURL;
};

},{"../helpers/isAbsoluteURL":"jD6NM","../helpers/combineURLs":"brOWK"}],"jD6NM":[function(require,module,exports) {
'use strict';
/**
 * Determines whether the specified URL is absolute
 *
 * @param {string} url The URL to test
 * @returns {boolean} True if the specified URL is absolute, otherwise false
 */ module.exports = function isAbsoluteURL(url) {
    // A URL is considered absolute if it begins with "<scheme>://" or "//" (protocol-relative URL).
    // RFC 3986 defines scheme name as a sequence of characters beginning with a letter and followed
    // by any combination of letters, digits, plus, period, or hyphen.
    return /^([a-z][a-z\d+\-.]*:)?\/\//i.test(url);
};

},{}],"brOWK":[function(require,module,exports) {
'use strict';
/**
 * Creates a new URL by combining the specified URLs
 *
 * @param {string} baseURL The base URL
 * @param {string} relativeURL The relative URL
 * @returns {string} The combined URL
 */ module.exports = function combineURLs(baseURL, relativeURL) {
    return relativeURL ? baseURL.replace(/\/+$/, '') + '/' + relativeURL.replace(/^\/+/, '') : baseURL;
};

},{}],"kqDd5":[function(require,module,exports) {
'use strict';
var utils = require('./../utils');
// Headers whose duplicates are ignored by node
// c.f. https://nodejs.org/api/http.html#http_message_headers
var ignoreDuplicateOf = [
    'age',
    'authorization',
    'content-length',
    'content-type',
    'etag',
    'expires',
    'from',
    'host',
    'if-modified-since',
    'if-unmodified-since',
    'last-modified',
    'location',
    'max-forwards',
    'proxy-authorization',
    'referer',
    'retry-after',
    'user-agent'
];
/**
 * Parse headers into an object
 *
 * ```
 * Date: Wed, 27 Aug 2014 08:58:49 GMT
 * Content-Type: application/json
 * Connection: keep-alive
 * Transfer-Encoding: chunked
 * ```
 *
 * @param {String} headers Headers needing to be parsed
 * @returns {Object} Headers parsed into an object
 */ module.exports = function parseHeaders(headers) {
    var parsed = {};
    var key;
    var val;
    var i;
    if (!headers) return parsed;
    utils.forEach(headers.split('\n'), function parser(line) {
        i = line.indexOf(':');
        key = utils.trim(line.substr(0, i)).toLowerCase();
        val = utils.trim(line.substr(i + 1));
        if (key) {
            if (parsed[key] && ignoreDuplicateOf.indexOf(key) >= 0) return;
            if (key === 'set-cookie') parsed[key] = (parsed[key] ? parsed[key] : []).concat([
                val
            ]);
            else parsed[key] = parsed[key] ? parsed[key] + ', ' + val : val;
        }
    });
    return parsed;
};

},{"./../utils":"5By4s"}],"lxXtv":[function(require,module,exports) {
'use strict';
var utils = require('./../utils');
module.exports = utils.isStandardBrowserEnv() ? // Standard browser envs have full support of the APIs needed to test
// whether the request URL is of the same origin as current location.
function standardBrowserEnv() {
    var msie = /(msie|trident)/i.test(navigator.userAgent);
    var urlParsingNode = document.createElement('a');
    var originURL;
    /**
    * Parse a URL to discover it's components
    *
    * @param {String} url The URL to be parsed
    * @returns {Object}
    */ function resolveURL(url) {
        var href = url;
        if (msie) {
            // IE needs attribute set twice to normalize properties
            urlParsingNode.setAttribute('href', href);
            href = urlParsingNode.href;
        }
        urlParsingNode.setAttribute('href', href);
        // urlParsingNode provides the UrlUtils interface - http://url.spec.whatwg.org/#urlutils
        return {
            href: urlParsingNode.href,
            protocol: urlParsingNode.protocol ? urlParsingNode.protocol.replace(/:$/, '') : '',
            host: urlParsingNode.host,
            search: urlParsingNode.search ? urlParsingNode.search.replace(/^\?/, '') : '',
            hash: urlParsingNode.hash ? urlParsingNode.hash.replace(/^#/, '') : '',
            hostname: urlParsingNode.hostname,
            port: urlParsingNode.port,
            pathname: urlParsingNode.pathname.charAt(0) === '/' ? urlParsingNode.pathname : '/' + urlParsingNode.pathname
        };
    }
    originURL = resolveURL(window.location.href);
    /**
    * Determine if a URL shares the same origin as the current location
    *
    * @param {String} requestURL The URL to test
    * @returns {boolean} True if URL shares the same origin, otherwise false
    */ return function isURLSameOrigin(requestURL) {
        var parsed = utils.isString(requestURL) ? resolveURL(requestURL) : requestURL;
        return parsed.protocol === originURL.protocol && parsed.host === originURL.host;
    };
}() : // Non standard browser envs (web workers, react-native) lack needed support.
function nonStandardBrowserEnv() {
    return function isURLSameOrigin() {
        return true;
    };
}();

},{"./../utils":"5By4s"}],"kjMy2":[function(require,module,exports) {
'use strict';
/**
 * A `Cancel` is an object that is thrown when an operation is canceled.
 *
 * @class
 * @param {string=} message The message.
 */ function Cancel(message) {
    this.message = message;
}
Cancel.prototype.toString = function toString() {
    return 'Cancel' + (this.message ? ': ' + this.message : '');
};
Cancel.prototype.__CANCEL__ = true;
module.exports = Cancel;

},{}],"a0VmF":[function(require,module,exports) {
'use strict';
module.exports = function isCancel(value) {
    return !!(value && value.__CANCEL__);
};

},{}],"b85oP":[function(require,module,exports) {
'use strict';
var utils = require('../utils');
/**
 * Config-specific merge-function which creates a new config-object
 * by merging two configuration objects together.
 *
 * @param {Object} config1
 * @param {Object} config2
 * @returns {Object} New object resulting from merging config2 to config1
 */ module.exports = function mergeConfig(config1, config2) {
    // eslint-disable-next-line no-param-reassign
    config2 = config2 || {};
    var config = {};
    function getMergedValue(target, source) {
        if (utils.isPlainObject(target) && utils.isPlainObject(source)) return utils.merge(target, source);
        else if (utils.isPlainObject(source)) return utils.merge({}, source);
        else if (utils.isArray(source)) return source.slice();
        return source;
    }
    // eslint-disable-next-line consistent-return
    function mergeDeepProperties(prop) {
        if (!utils.isUndefined(config2[prop])) return getMergedValue(config1[prop], config2[prop]);
        else if (!utils.isUndefined(config1[prop])) return getMergedValue(undefined, config1[prop]);
    }
    // eslint-disable-next-line consistent-return
    function valueFromConfig2(prop) {
        if (!utils.isUndefined(config2[prop])) return getMergedValue(undefined, config2[prop]);
    }
    // eslint-disable-next-line consistent-return
    function defaultToConfig2(prop) {
        if (!utils.isUndefined(config2[prop])) return getMergedValue(undefined, config2[prop]);
        else if (!utils.isUndefined(config1[prop])) return getMergedValue(undefined, config1[prop]);
    }
    // eslint-disable-next-line consistent-return
    function mergeDirectKeys(prop) {
        if (prop in config2) return getMergedValue(config1[prop], config2[prop]);
        else if (prop in config1) return getMergedValue(undefined, config1[prop]);
    }
    var mergeMap = {
        'url': valueFromConfig2,
        'method': valueFromConfig2,
        'data': valueFromConfig2,
        'baseURL': defaultToConfig2,
        'transformRequest': defaultToConfig2,
        'transformResponse': defaultToConfig2,
        'paramsSerializer': defaultToConfig2,
        'timeout': defaultToConfig2,
        'timeoutMessage': defaultToConfig2,
        'withCredentials': defaultToConfig2,
        'adapter': defaultToConfig2,
        'responseType': defaultToConfig2,
        'xsrfCookieName': defaultToConfig2,
        'xsrfHeaderName': defaultToConfig2,
        'onUploadProgress': defaultToConfig2,
        'onDownloadProgress': defaultToConfig2,
        'decompress': defaultToConfig2,
        'maxContentLength': defaultToConfig2,
        'maxBodyLength': defaultToConfig2,
        'transport': defaultToConfig2,
        'httpAgent': defaultToConfig2,
        'httpsAgent': defaultToConfig2,
        'cancelToken': defaultToConfig2,
        'socketPath': defaultToConfig2,
        'responseEncoding': defaultToConfig2,
        'validateStatus': mergeDirectKeys
    };
    utils.forEach(Object.keys(config1).concat(Object.keys(config2)), function computeConfigValue(prop) {
        var merge = mergeMap[prop] || mergeDeepProperties;
        var configValue = merge(prop);
        utils.isUndefined(configValue) && merge !== mergeDirectKeys || (config[prop] = configValue);
    });
    return config;
};

},{"../utils":"5By4s"}],"9vgkY":[function(require,module,exports) {
'use strict';
var VERSION = require('../env/data').version;
var validators = {};
// eslint-disable-next-line func-names
[
    'object',
    'boolean',
    'number',
    'function',
    'string',
    'symbol'
].forEach(function(type, i) {
    validators[type] = function validator(thing) {
        return typeof thing === type || 'a' + (i < 1 ? 'n ' : ' ') + type;
    };
});
var deprecatedWarnings = {};
/**
 * Transitional option validator
 * @param {function|boolean?} validator - set to false if the transitional option has been removed
 * @param {string?} version - deprecated version / removed since version
 * @param {string?} message - some message with additional info
 * @returns {function}
 */ validators.transitional = function transitional(validator, version, message) {
    function formatMessage(opt, desc) {
        return '[Axios v' + VERSION + '] Transitional option \'' + opt + '\'' + desc + (message ? '. ' + message : '');
    }
    // eslint-disable-next-line func-names
    return function(value, opt, opts) {
        if (validator === false) throw new Error(formatMessage(opt, ' has been removed' + (version ? ' in ' + version : '')));
        if (version && !deprecatedWarnings[opt]) {
            deprecatedWarnings[opt] = true;
            // eslint-disable-next-line no-console
            console.warn(formatMessage(opt, ' has been deprecated since v' + version + ' and will be removed in the near future'));
        }
        return validator ? validator(value, opt, opts) : true;
    };
};
/**
 * Assert object's properties type
 * @param {object} options
 * @param {object} schema
 * @param {boolean?} allowUnknown
 */ function assertOptions(options, schema, allowUnknown) {
    if (typeof options !== 'object') throw new TypeError('options must be an object');
    var keys = Object.keys(options);
    var i = keys.length;
    while(i-- > 0){
        var opt = keys[i];
        var validator = schema[opt];
        if (validator) {
            var value = options[opt];
            var result = value === undefined || validator(value, opt, options);
            if (result !== true) throw new TypeError('option ' + opt + ' must be ' + result);
            continue;
        }
        if (allowUnknown !== true) throw Error('Unknown option ' + opt);
    }
}
module.exports = {
    assertOptions: assertOptions,
    validators: validators
};

},{"../env/data":"h29L9"}],"h29L9":[function(require,module,exports) {
module.exports = {
    "version": "0.26.1"
};

},{}],"45wzn":[function(require,module,exports) {
'use strict';
var Cancel = require('./Cancel');
/**
 * A `CancelToken` is an object that can be used to request cancellation of an operation.
 *
 * @class
 * @param {Function} executor The executor function.
 */ function CancelToken(executor) {
    if (typeof executor !== 'function') throw new TypeError('executor must be a function.');
    var resolvePromise;
    this.promise = new Promise(function promiseExecutor(resolve) {
        resolvePromise = resolve;
    });
    var token = this;
    // eslint-disable-next-line func-names
    this.promise.then(function(cancel) {
        if (!token._listeners) return;
        var i;
        var l = token._listeners.length;
        for(i = 0; i < l; i++)token._listeners[i](cancel);
        token._listeners = null;
    });
    // eslint-disable-next-line func-names
    this.promise.then = function(onfulfilled) {
        var _resolve;
        // eslint-disable-next-line func-names
        var promise = new Promise(function(resolve) {
            token.subscribe(resolve);
            _resolve = resolve;
        }).then(onfulfilled);
        promise.cancel = function reject() {
            token.unsubscribe(_resolve);
        };
        return promise;
    };
    executor(function cancel(message) {
        if (token.reason) // Cancellation has already been requested
        return;
        token.reason = new Cancel(message);
        resolvePromise(token.reason);
    });
}
/**
 * Throws a `Cancel` if cancellation has been requested.
 */ CancelToken.prototype.throwIfRequested = function throwIfRequested() {
    if (this.reason) throw this.reason;
};
/**
 * Subscribe to the cancel signal
 */ CancelToken.prototype.subscribe = function subscribe(listener) {
    if (this.reason) {
        listener(this.reason);
        return;
    }
    if (this._listeners) this._listeners.push(listener);
    else this._listeners = [
        listener
    ];
};
/**
 * Unsubscribe from the cancel signal
 */ CancelToken.prototype.unsubscribe = function unsubscribe(listener) {
    if (!this._listeners) return;
    var index = this._listeners.indexOf(listener);
    if (index !== -1) this._listeners.splice(index, 1);
};
/**
 * Returns an object that contains a new `CancelToken` and a function that, when called,
 * cancels the `CancelToken`.
 */ CancelToken.source = function source() {
    var cancel;
    var token = new CancelToken(function executor(c) {
        cancel = c;
    });
    return {
        token: token,
        cancel: cancel
    };
};
module.exports = CancelToken;

},{"./Cancel":"kjMy2"}],"dyQ8N":[function(require,module,exports) {
'use strict';
/**
 * Syntactic sugar for invoking a function and expanding an array for arguments.
 *
 * Common use case would be to use `Function.prototype.apply`.
 *
 *  ```js
 *  function f(x, y, z) {}
 *  var args = [1, 2, 3];
 *  f.apply(null, args);
 *  ```
 *
 * With `spread` this example can be re-written.
 *
 *  ```js
 *  spread(function(x, y, z) {})([1, 2, 3]);
 *  ```
 *
 * @param {Function} callback
 * @returns {Function}
 */ module.exports = function spread(callback) {
    return function wrap(arr) {
        return callback.apply(null, arr);
    };
};

},{}],"eyiLq":[function(require,module,exports) {
'use strict';
var utils = require('./../utils');
/**
 * Determines whether the payload is an error thrown by Axios
 *
 * @param {*} payload The value to test
 * @returns {boolean} True if the payload is an error thrown by Axios, otherwise false
 */ module.exports = function isAxiosError(payload) {
    return utils.isObject(payload) && payload.isAxiosError === true;
};

},{"./../utils":"5By4s"}],"38a65":[function(require,module,exports) {
!function(t, e) {
    module.exports = e();
}(this, function() {
    return function(t1) {
        function e1(o) {
            if (n1[o]) return n1[o].exports;
            var r = n1[o] = {
                i: o,
                l: !1,
                exports: {}
            };
            return t1[o].call(r.exports, r, r.exports, e1), r.l = !0, r.exports;
        }
        var n1 = {};
        return e1.m = t1, e1.c = n1, e1.d = function(t, n, o) {
            e1.o(t, n) || Object.defineProperty(t, n, {
                configurable: !1,
                enumerable: !0,
                get: o
            });
        }, e1.n = function(t) {
            var n = t && t.__esModule ? function() {
                return t.default;
            } : function() {
                return t;
            };
            return e1.d(n, "a", n), n;
        }, e1.o = function(t, e) {
            return Object.prototype.hasOwnProperty.call(t, e);
        }, e1.p = "", e1(e1.s = 8);
    }([
        function(t, e, n) {
            "use strict";
            Object.defineProperty(e, "__esModule", {
                value: !0
            });
            var o = "swal-button";
            e.CLASS_NAMES = {
                MODAL: "swal-modal",
                OVERLAY: "swal-overlay",
                SHOW_MODAL: "swal-overlay--show-modal",
                MODAL_TITLE: "swal-title",
                MODAL_TEXT: "swal-text",
                ICON: "swal-icon",
                ICON_CUSTOM: "swal-icon--custom",
                CONTENT: "swal-content",
                FOOTER: "swal-footer",
                BUTTON_CONTAINER: "swal-button-container",
                BUTTON: o,
                CONFIRM_BUTTON: o + "--confirm",
                CANCEL_BUTTON: o + "--cancel",
                DANGER_BUTTON: o + "--danger",
                BUTTON_LOADING: o + "--loading",
                BUTTON_LOADER: o + "__loader"
            }, e.default = e.CLASS_NAMES;
        },
        function(t2, e2, n2) {
            "use strict";
            Object.defineProperty(e2, "__esModule", {
                value: !0
            }), e2.getNode = function(t) {
                var e = "." + t;
                return document.querySelector(e);
            }, e2.stringToNode = function(t) {
                var e = document.createElement("div");
                return e.innerHTML = t.trim(), e.firstChild;
            }, e2.insertAfter = function(t, e) {
                var n = e.nextSibling;
                e.parentNode.insertBefore(t, n);
            }, e2.removeNode = function(t) {
                t.parentElement.removeChild(t);
            }, e2.throwErr = function(t) {
                throw t = t.replace(/ +(?= )/g, ""), "SweetAlert: " + (t = t.trim());
            }, e2.isPlainObject = function(t) {
                if ("[object Object]" !== Object.prototype.toString.call(t)) return !1;
                var e = Object.getPrototypeOf(t);
                return null === e || e === Object.prototype;
            }, e2.ordinalSuffixOf = function(t) {
                var e = t % 10, n = t % 100;
                return 1 === e && 11 !== n ? t + "st" : 2 === e && 12 !== n ? t + "nd" : 3 === e && 13 !== n ? t + "rd" : t + "th";
            };
        },
        function(t3, e, n3) {
            "use strict";
            function o(t) {
                for(var n in t)e.hasOwnProperty(n) || (e[n] = t[n]);
            }
            Object.defineProperty(e, "__esModule", {
                value: !0
            }), o(n3(25));
            var r = n3(26);
            e.overlayMarkup = r.default, o(n3(27)), o(n3(28)), o(n3(29));
            var i = n3(0), a = i.default.MODAL_TITLE, s = i.default.MODAL_TEXT, c = i.default.ICON, l = i.default.FOOTER;
            e.iconMarkup = '\n  <div class="' + c + '"></div>', e.titleMarkup = '\n  <div class="' + a + '"></div>\n', e.textMarkup = '\n  <div class="' + s + '"></div>', e.footerMarkup = '\n  <div class="' + l + '"></div>\n';
        },
        function(t4, e3, n4) {
            "use strict";
            Object.defineProperty(e3, "__esModule", {
                value: !0
            });
            var o1 = n4(1);
            e3.CONFIRM_KEY = "confirm", e3.CANCEL_KEY = "cancel";
            var r1 = {
                visible: !0,
                text: null,
                value: null,
                className: "",
                closeModal: !0
            }, i = Object.assign({}, r1, {
                visible: !1,
                text: "Cancel",
                value: null
            }), a1 = Object.assign({}, r1, {
                text: "OK",
                value: !0
            });
            e3.defaultButtonList = {
                cancel: i,
                confirm: a1
            };
            var s1 = function(t) {
                switch(t){
                    case e3.CONFIRM_KEY:
                        return a1;
                    case e3.CANCEL_KEY:
                        return i;
                    default:
                        var n = t.charAt(0).toUpperCase() + t.slice(1);
                        return Object.assign({}, r1, {
                            text: n,
                            value: t
                        });
                }
            }, c = function(t, e) {
                var n = s1(t);
                return !0 === e ? Object.assign({}, n, {
                    visible: !0
                }) : "string" == typeof e ? Object.assign({}, n, {
                    visible: !0,
                    text: e
                }) : o1.isPlainObject(e) ? Object.assign({
                    visible: !0
                }, n, e) : Object.assign({}, n, {
                    visible: !1
                });
            }, l = function(t) {
                for(var e = {}, n = 0, o = Object.keys(t); n < o.length; n++){
                    var r = o[n], a = t[r], s = c(r, a);
                    e[r] = s;
                }
                return e.cancel || (e.cancel = i), e;
            }, u = function(t) {
                var n = {};
                switch(t.length){
                    case 1:
                        n[e3.CANCEL_KEY] = Object.assign({}, i, {
                            visible: !1
                        });
                        break;
                    case 2:
                        n[e3.CANCEL_KEY] = c(e3.CANCEL_KEY, t[0]), n[e3.CONFIRM_KEY] = c(e3.CONFIRM_KEY, t[1]);
                        break;
                    default:
                        o1.throwErr("Invalid number of 'buttons' in array (" + t.length + ").\n      If you want more than 2 buttons, you need to use an object!");
                }
                return n;
            };
            e3.getButtonListOpts = function(t) {
                var n = e3.defaultButtonList;
                return "string" == typeof t ? n[e3.CONFIRM_KEY] = c(e3.CONFIRM_KEY, t) : Array.isArray(t) ? n = u(t) : o1.isPlainObject(t) ? n = l(t) : !0 === t ? n = u([
                    !0,
                    !0
                ]) : !1 === t ? n = u([
                    !1,
                    !1
                ]) : void 0 === t && (n = e3.defaultButtonList), n;
            };
        },
        function(t5, e4, n5) {
            "use strict";
            Object.defineProperty(e4, "__esModule", {
                value: !0
            });
            var o = n5(1), r = n5(2), i = n5(0), a = i.default.MODAL, s = i.default.OVERLAY, c = n5(30), l = n5(31), u = n5(32), f = n5(33);
            e4.injectElIntoModal = function(t) {
                var e = o.getNode(a), n = o.stringToNode(t);
                return e.appendChild(n), n;
            };
            var d = function(t) {
                t.className = a, t.textContent = "";
            }, p = function(t, e) {
                d(t);
                var n = e.className;
                n && t.classList.add(n);
            };
            e4.initModalContent = function(t) {
                var e = o.getNode(a);
                p(e, t), c.default(t.icon), l.initTitle(t.title), l.initText(t.text), f.default(t.content), u.default(t.buttons, t.dangerMode);
            };
            var m = function() {
                var t = o.getNode(s), e = o.stringToNode(r.modalMarkup);
                t.appendChild(e);
            };
            e4.default = m;
        },
        function(t6, e5, n6) {
            "use strict";
            Object.defineProperty(e5, "__esModule", {
                value: !0
            });
            var o2 = n6(3), r = {
                isOpen: !1,
                promise: null,
                actions: {},
                timer: null
            }, i = Object.assign({}, r);
            e5.resetState = function() {
                i = Object.assign({}, r);
            }, e5.setActionValue = function(t) {
                if ("string" == typeof t) return a(o2.CONFIRM_KEY, t);
                for(var e in t)a(e, t[e]);
            };
            var a = function(t, e) {
                i.actions[t] || (i.actions[t] = {}), Object.assign(i.actions[t], {
                    value: e
                });
            };
            e5.setActionOptionsFor = function(t, e) {
                var n = (void 0 === e ? {} : e).closeModal, o = void 0 === n || n;
                Object.assign(i.actions[t], {
                    closeModal: o
                });
            }, e5.default = i;
        },
        function(t7, e6, n7) {
            "use strict";
            Object.defineProperty(e6, "__esModule", {
                value: !0
            });
            var o = n7(1), r = n7(3), i1 = n7(0), a = i1.default.OVERLAY, s = i1.default.SHOW_MODAL, c = i1.default.BUTTON, l = i1.default.BUTTON_LOADING, u = n7(5);
            e6.openModal = function() {
                o.getNode(a).classList.add(s), u.default.isOpen = !0;
            };
            var f = function() {
                o.getNode(a).classList.remove(s), u.default.isOpen = !1;
            };
            e6.onAction = function(t) {
                void 0 === t && (t = r.CANCEL_KEY);
                var e = u.default.actions[t], n = e.value;
                if (!1 === e.closeModal) {
                    var i = c + "--" + t;
                    o.getNode(i).classList.add(l);
                } else f();
                u.default.promise.resolve(n);
            }, e6.getState = function() {
                var t = Object.assign({}, u.default);
                return delete t.promise, delete t.timer, t;
            }, e6.stopLoading = function() {
                for(var t = document.querySelectorAll("." + c), e = 0; e < t.length; e++)t[e].classList.remove(l);
            };
        },
        function(t, e) {
            var n;
            n = function() {
                return this;
            }();
            try {
                n = n || Function("return this")() || (0, eval)("this");
            } catch (t8) {
                "object" == typeof window && (n = window);
            }
            t.exports = n;
        },
        function(t, e7, n) {
            (function(e) {
                t.exports = e.sweetAlert = n(9);
            }).call(e7, n(7));
        },
        function(t, e8, n) {
            (function(e) {
                t.exports = e.swal = n(10);
            }).call(e8, n(7));
        },
        function(t, e, n) {
            "undefined" != typeof window && n(11), n(16);
            var o = n(23).default;
            t.exports = o;
        },
        function(t, e, n) {
            var o = n(12);
            "string" == typeof o && (o = [
                [
                    t.i,
                    o,
                    ""
                ]
            ]);
            var r = {
                insertAt: "top"
            };
            r.transform = void 0;
            n(14)(o, r);
            o.locals && (t.exports = o.locals);
        },
        function(t, e, n) {
            e = t.exports = n(13)(void 0), e.push([
                t.i,
                '.swal-icon--error{border-color:#f27474;-webkit-animation:animateErrorIcon .5s;animation:animateErrorIcon .5s}.swal-icon--error__x-mark{position:relative;display:block;-webkit-animation:animateXMark .5s;animation:animateXMark .5s}.swal-icon--error__line{position:absolute;height:5px;width:47px;background-color:#f27474;display:block;top:37px;border-radius:2px}.swal-icon--error__line--left{-webkit-transform:rotate(45deg);transform:rotate(45deg);left:17px}.swal-icon--error__line--right{-webkit-transform:rotate(-45deg);transform:rotate(-45deg);right:16px}@-webkit-keyframes animateErrorIcon{0%{-webkit-transform:rotateX(100deg);transform:rotateX(100deg);opacity:0}to{-webkit-transform:rotateX(0deg);transform:rotateX(0deg);opacity:1}}@keyframes animateErrorIcon{0%{-webkit-transform:rotateX(100deg);transform:rotateX(100deg);opacity:0}to{-webkit-transform:rotateX(0deg);transform:rotateX(0deg);opacity:1}}@-webkit-keyframes animateXMark{0%{-webkit-transform:scale(.4);transform:scale(.4);margin-top:26px;opacity:0}50%{-webkit-transform:scale(.4);transform:scale(.4);margin-top:26px;opacity:0}80%{-webkit-transform:scale(1.15);transform:scale(1.15);margin-top:-6px}to{-webkit-transform:scale(1);transform:scale(1);margin-top:0;opacity:1}}@keyframes animateXMark{0%{-webkit-transform:scale(.4);transform:scale(.4);margin-top:26px;opacity:0}50%{-webkit-transform:scale(.4);transform:scale(.4);margin-top:26px;opacity:0}80%{-webkit-transform:scale(1.15);transform:scale(1.15);margin-top:-6px}to{-webkit-transform:scale(1);transform:scale(1);margin-top:0;opacity:1}}.swal-icon--warning{border-color:#f8bb86;-webkit-animation:pulseWarning .75s infinite alternate;animation:pulseWarning .75s infinite alternate}.swal-icon--warning__body{width:5px;height:47px;top:10px;border-radius:2px;margin-left:-2px}.swal-icon--warning__body,.swal-icon--warning__dot{position:absolute;left:50%;background-color:#f8bb86}.swal-icon--warning__dot{width:7px;height:7px;border-radius:50%;margin-left:-4px;bottom:-11px}@-webkit-keyframes pulseWarning{0%{border-color:#f8d486}to{border-color:#f8bb86}}@keyframes pulseWarning{0%{border-color:#f8d486}to{border-color:#f8bb86}}.swal-icon--success{border-color:#a5dc86}.swal-icon--success:after,.swal-icon--success:before{content:"";border-radius:50%;position:absolute;width:60px;height:120px;background:#fff;-webkit-transform:rotate(45deg);transform:rotate(45deg)}.swal-icon--success:before{border-radius:120px 0 0 120px;top:-7px;left:-33px;-webkit-transform:rotate(-45deg);transform:rotate(-45deg);-webkit-transform-origin:60px 60px;transform-origin:60px 60px}.swal-icon--success:after{border-radius:0 120px 120px 0;top:-11px;left:30px;-webkit-transform:rotate(-45deg);transform:rotate(-45deg);-webkit-transform-origin:0 60px;transform-origin:0 60px;-webkit-animation:rotatePlaceholder 4.25s ease-in;animation:rotatePlaceholder 4.25s ease-in}.swal-icon--success__ring{width:80px;height:80px;border:4px solid hsla(98,55%,69%,.2);border-radius:50%;box-sizing:content-box;position:absolute;left:-4px;top:-4px;z-index:2}.swal-icon--success__hide-corners{width:5px;height:90px;background-color:#fff;padding:1px;position:absolute;left:28px;top:8px;z-index:1;-webkit-transform:rotate(-45deg);transform:rotate(-45deg)}.swal-icon--success__line{height:5px;background-color:#a5dc86;display:block;border-radius:2px;position:absolute;z-index:2}.swal-icon--success__line--tip{width:25px;left:14px;top:46px;-webkit-transform:rotate(45deg);transform:rotate(45deg);-webkit-animation:animateSuccessTip .75s;animation:animateSuccessTip .75s}.swal-icon--success__line--long{width:47px;right:8px;top:38px;-webkit-transform:rotate(-45deg);transform:rotate(-45deg);-webkit-animation:animateSuccessLong .75s;animation:animateSuccessLong .75s}@-webkit-keyframes rotatePlaceholder{0%{-webkit-transform:rotate(-45deg);transform:rotate(-45deg)}5%{-webkit-transform:rotate(-45deg);transform:rotate(-45deg)}12%{-webkit-transform:rotate(-405deg);transform:rotate(-405deg)}to{-webkit-transform:rotate(-405deg);transform:rotate(-405deg)}}@keyframes rotatePlaceholder{0%{-webkit-transform:rotate(-45deg);transform:rotate(-45deg)}5%{-webkit-transform:rotate(-45deg);transform:rotate(-45deg)}12%{-webkit-transform:rotate(-405deg);transform:rotate(-405deg)}to{-webkit-transform:rotate(-405deg);transform:rotate(-405deg)}}@-webkit-keyframes animateSuccessTip{0%{width:0;left:1px;top:19px}54%{width:0;left:1px;top:19px}70%{width:50px;left:-8px;top:37px}84%{width:17px;left:21px;top:48px}to{width:25px;left:14px;top:45px}}@keyframes animateSuccessTip{0%{width:0;left:1px;top:19px}54%{width:0;left:1px;top:19px}70%{width:50px;left:-8px;top:37px}84%{width:17px;left:21px;top:48px}to{width:25px;left:14px;top:45px}}@-webkit-keyframes animateSuccessLong{0%{width:0;right:46px;top:54px}65%{width:0;right:46px;top:54px}84%{width:55px;right:0;top:35px}to{width:47px;right:8px;top:38px}}@keyframes animateSuccessLong{0%{width:0;right:46px;top:54px}65%{width:0;right:46px;top:54px}84%{width:55px;right:0;top:35px}to{width:47px;right:8px;top:38px}}.swal-icon--info{border-color:#c9dae1}.swal-icon--info:before{width:5px;height:29px;bottom:17px;border-radius:2px;margin-left:-2px}.swal-icon--info:after,.swal-icon--info:before{content:"";position:absolute;left:50%;background-color:#c9dae1}.swal-icon--info:after{width:7px;height:7px;border-radius:50%;margin-left:-3px;top:19px}.swal-icon{width:80px;height:80px;border-width:4px;border-style:solid;border-radius:50%;padding:0;position:relative;box-sizing:content-box;margin:20px auto}.swal-icon:first-child{margin-top:32px}.swal-icon--custom{width:auto;height:auto;max-width:100%;border:none;border-radius:0}.swal-icon img{max-width:100%;max-height:100%}.swal-title{color:rgba(0,0,0,.65);font-weight:600;text-transform:none;position:relative;display:block;padding:13px 16px;font-size:27px;line-height:normal;text-align:center;margin-bottom:0}.swal-title:first-child{margin-top:26px}.swal-title:not(:first-child){padding-bottom:0}.swal-title:not(:last-child){margin-bottom:13px}.swal-text{font-size:16px;position:relative;float:none;line-height:normal;vertical-align:top;text-align:left;display:inline-block;margin:0;padding:0 10px;font-weight:400;color:rgba(0,0,0,.64);max-width:calc(100% - 20px);overflow-wrap:break-word;box-sizing:border-box}.swal-text:first-child{margin-top:45px}.swal-text:last-child{margin-bottom:45px}.swal-footer{text-align:right;padding-top:13px;margin-top:13px;padding:13px 16px;border-radius:inherit;border-top-left-radius:0;border-top-right-radius:0}.swal-button-container{margin:5px;display:inline-block;position:relative}.swal-button{background-color:#7cd1f9;color:#fff;border:none;box-shadow:none;border-radius:5px;font-weight:600;font-size:14px;padding:10px 24px;margin:0;cursor:pointer}.swal-button:not([disabled]):hover{background-color:#78cbf2}.swal-button:active{background-color:#70bce0}.swal-button:focus{outline:none;box-shadow:0 0 0 1px #fff,0 0 0 3px rgba(43,114,165,.29)}.swal-button[disabled]{opacity:.5;cursor:default}.swal-button::-moz-focus-inner{border:0}.swal-button--cancel{color:#555;background-color:#efefef}.swal-button--cancel:not([disabled]):hover{background-color:#e8e8e8}.swal-button--cancel:active{background-color:#d7d7d7}.swal-button--cancel:focus{box-shadow:0 0 0 1px #fff,0 0 0 3px rgba(116,136,150,.29)}.swal-button--danger{background-color:#e64942}.swal-button--danger:not([disabled]):hover{background-color:#df4740}.swal-button--danger:active{background-color:#cf423b}.swal-button--danger:focus{box-shadow:0 0 0 1px #fff,0 0 0 3px rgba(165,43,43,.29)}.swal-content{padding:0 20px;margin-top:20px;font-size:medium}.swal-content:last-child{margin-bottom:20px}.swal-content__input,.swal-content__textarea{-webkit-appearance:none;background-color:#fff;border:none;font-size:14px;display:block;box-sizing:border-box;width:100%;border:1px solid rgba(0,0,0,.14);padding:10px 13px;border-radius:2px;transition:border-color .2s}.swal-content__input:focus,.swal-content__textarea:focus{outline:none;border-color:#6db8ff}.swal-content__textarea{resize:vertical}.swal-button--loading{color:transparent}.swal-button--loading~.swal-button__loader{opacity:1}.swal-button__loader{position:absolute;height:auto;width:43px;z-index:2;left:50%;top:50%;-webkit-transform:translateX(-50%) translateY(-50%);transform:translateX(-50%) translateY(-50%);text-align:center;pointer-events:none;opacity:0}.swal-button__loader div{display:inline-block;float:none;vertical-align:baseline;width:9px;height:9px;padding:0;border:none;margin:2px;opacity:.4;border-radius:7px;background-color:hsla(0,0%,100%,.9);transition:background .2s;-webkit-animation:swal-loading-anim 1s infinite;animation:swal-loading-anim 1s infinite}.swal-button__loader div:nth-child(3n+2){-webkit-animation-delay:.15s;animation-delay:.15s}.swal-button__loader div:nth-child(3n+3){-webkit-animation-delay:.3s;animation-delay:.3s}@-webkit-keyframes swal-loading-anim{0%{opacity:.4}20%{opacity:.4}50%{opacity:1}to{opacity:.4}}@keyframes swal-loading-anim{0%{opacity:.4}20%{opacity:.4}50%{opacity:1}to{opacity:.4}}.swal-overlay{position:fixed;top:0;bottom:0;left:0;right:0;text-align:center;font-size:0;overflow-y:auto;background-color:rgba(0,0,0,.4);z-index:10000;pointer-events:none;opacity:0;transition:opacity .3s}.swal-overlay:before{content:" ";display:inline-block;vertical-align:middle;height:100%}.swal-overlay--show-modal{opacity:1;pointer-events:auto}.swal-overlay--show-modal .swal-modal{opacity:1;pointer-events:auto;box-sizing:border-box;-webkit-animation:showSweetAlert .3s;animation:showSweetAlert .3s;will-change:transform}.swal-modal{width:478px;opacity:0;pointer-events:none;background-color:#fff;text-align:center;border-radius:5px;position:static;margin:20px auto;display:inline-block;vertical-align:middle;-webkit-transform:scale(1);transform:scale(1);-webkit-transform-origin:50% 50%;transform-origin:50% 50%;z-index:10001;transition:opacity .2s,-webkit-transform .3s;transition:transform .3s,opacity .2s;transition:transform .3s,opacity .2s,-webkit-transform .3s}@media (max-width:500px){.swal-modal{width:calc(100% - 20px)}}@-webkit-keyframes showSweetAlert{0%{-webkit-transform:scale(1);transform:scale(1)}1%{-webkit-transform:scale(.5);transform:scale(.5)}45%{-webkit-transform:scale(1.05);transform:scale(1.05)}80%{-webkit-transform:scale(.95);transform:scale(.95)}to{-webkit-transform:scale(1);transform:scale(1)}}@keyframes showSweetAlert{0%{-webkit-transform:scale(1);transform:scale(1)}1%{-webkit-transform:scale(.5);transform:scale(.5)}45%{-webkit-transform:scale(1.05);transform:scale(1.05)}80%{-webkit-transform:scale(.95);transform:scale(.95)}to{-webkit-transform:scale(1);transform:scale(1)}}',
                ""
            ]);
        },
        function(t9, e9) {
            function n8(t10, e) {
                var n = t10[1] || "", r = t10[3];
                if (!r) return n;
                if (e && "function" == typeof btoa) {
                    var i = o3(r);
                    return [
                        n
                    ].concat(r.sources.map(function(t) {
                        return "/*# sourceURL=" + r.sourceRoot + t + " */";
                    })).concat([
                        i
                    ]).join("\n");
                }
                return [
                    n
                ].join("\n");
            }
            function o3(t) {
                return "/*# sourceMappingURL=data:application/json;charset=utf-8;base64," + btoa(unescape(encodeURIComponent(JSON.stringify(t)))) + " */";
            }
            t9.exports = function(t11) {
                var e10 = [];
                return e10.toString = function() {
                    return this.map(function(e) {
                        var o = n8(e, t11);
                        return e[2] ? "@media " + e[2] + "{" + o + "}" : o;
                    }).join("");
                }, e10.i = function(t, n) {
                    "string" == typeof t && (t = [
                        [
                            null,
                            t,
                            ""
                        ]
                    ]);
                    for(var o = {}, r = 0; r < this.length; r++){
                        var i = this[r][0];
                        "number" == typeof i && (o[i] = !0);
                    }
                    for(r = 0; r < t.length; r++){
                        var a = t[r];
                        "number" == typeof a[0] && o[a[0]] || (n && !a[2] ? a[2] = n : n && (a[2] = "(" + a[2] + ") and (" + n + ")"), e10.push(a));
                    }
                }, e10;
            };
        },
        function(t12, e11, n9) {
            function o4(t, e) {
                for(var n = 0; n < t.length; n++){
                    var o = t[n], r = m[o.id];
                    if (r) {
                        r.refs++;
                        for(var i = 0; i < r.parts.length; i++)r.parts[i](o.parts[i]);
                        for(; i < o.parts.length; i++)r.parts.push(u1(o.parts[i], e));
                    } else {
                        for(var a = [], i = 0; i < o.parts.length; i++)a.push(u1(o.parts[i], e));
                        m[o.id] = {
                            id: o.id,
                            refs: 1,
                            parts: a
                        };
                    }
                }
            }
            function r2(t, e) {
                for(var n = [], o = {}, r = 0; r < t.length; r++){
                    var i = t[r], a = e.base ? i[0] + e.base : i[0], s = i[1], c = i[2], l = i[3], u = {
                        css: s,
                        media: c,
                        sourceMap: l
                    };
                    o[a] ? o[a].parts.push(u) : n.push(o[a] = {
                        id: a,
                        parts: [
                            u
                        ]
                    });
                }
                return n;
            }
            function i2(t, e) {
                var n = v(t.insertInto);
                if (!n) throw new Error("Couldn't find a style target. This probably means that the value for the 'insertInto' parameter is invalid.");
                var o = w[w.length - 1];
                if ("top" === t.insertAt) o ? o.nextSibling ? n.insertBefore(e, o.nextSibling) : n.appendChild(e) : n.insertBefore(e, n.firstChild), w.push(e);
                else {
                    if ("bottom" !== t.insertAt) throw new Error("Invalid value for parameter 'insertAt'. Must be 'top' or 'bottom'.");
                    n.appendChild(e);
                }
            }
            function a2(t) {
                if (null === t.parentNode) return !1;
                t.parentNode.removeChild(t);
                var e = w.indexOf(t);
                e >= 0 && w.splice(e, 1);
            }
            function s2(t) {
                var e = document.createElement("style");
                return t.attrs.type = "text/css", l1(e, t.attrs), i2(t, e), e;
            }
            function c1(t) {
                var e = document.createElement("link");
                return t.attrs.type = "text/css", t.attrs.rel = "stylesheet", l1(e, t.attrs), i2(t, e), e;
            }
            function l1(t, e) {
                Object.keys(e).forEach(function(n) {
                    t.setAttribute(n, e[n]);
                });
            }
            function u1(t, e12) {
                var n, o, r, i;
                if (e12.transform && t.css) {
                    if (!(i = e12.transform(t.css))) return function() {};
                    t.css = i;
                }
                if (e12.singleton) {
                    var l = h++;
                    n = g || (g = s2(e12)), o = f.bind(null, n, l, !1), r = f.bind(null, n, l, !0);
                } else t.sourceMap && "function" == typeof URL && "function" == typeof URL.createObjectURL && "function" == typeof URL.revokeObjectURL && "function" == typeof Blob && "function" == typeof btoa ? (n = c1(e12), o = p.bind(null, n, e12), r = function() {
                    a2(n), n.href && URL.revokeObjectURL(n.href);
                }) : (n = s2(e12), o = d.bind(null, n), r = function() {
                    a2(n);
                });
                return o(t), function(e) {
                    if (e) {
                        if (e.css === t.css && e.media === t.media && e.sourceMap === t.sourceMap) return;
                        o(t = e);
                    } else r();
                };
            }
            function f(t, e, n, o) {
                var r = n ? "" : o.css;
                if (t.styleSheet) t.styleSheet.cssText = x(e, r);
                else {
                    var i = document.createTextNode(r), a = t.childNodes;
                    a[e] && t.removeChild(a[e]), a.length ? t.insertBefore(i, a[e]) : t.appendChild(i);
                }
            }
            function d(t, e) {
                var n = e.css, o = e.media;
                if (o && t.setAttribute("media", o), t.styleSheet) t.styleSheet.cssText = n;
                else {
                    for(; t.firstChild;)t.removeChild(t.firstChild);
                    t.appendChild(document.createTextNode(n));
                }
            }
            function p(t, e, n) {
                var o = n.css, r = n.sourceMap, i = void 0 === e.convertToAbsoluteUrls && r;
                (e.convertToAbsoluteUrls || i) && (o = y(o)), r && (o += "\n/*# sourceMappingURL=data:application/json;base64," + btoa(unescape(encodeURIComponent(JSON.stringify(r)))) + " */");
                var a = new Blob([
                    o
                ], {
                    type: "text/css"
                }), s = t.href;
                t.href = URL.createObjectURL(a), s && URL.revokeObjectURL(s);
            }
            var m = {}, b = function(t) {
                var e;
                return function() {
                    return void 0 === e && (e = t.apply(this, arguments)), e;
                };
            }(function() {
                return window && document && document.all && !window.atob;
            }), v = function(t) {
                var e = {};
                return function(n) {
                    return void 0 === e[n] && (e[n] = t.call(this, n)), e[n];
                };
            }(function(t) {
                return document.querySelector(t);
            }), g = null, h = 0, w = [], y = n9(15);
            t12.exports = function(t13, e) {
                if ("undefined" != typeof DEBUG && DEBUG && "object" != typeof document) throw new Error("The style-loader cannot be used in a non-browser environment");
                e = e || {}, e.attrs = "object" == typeof e.attrs ? e.attrs : {}, e.singleton || (e.singleton = b()), e.insertInto || (e.insertInto = "head"), e.insertAt || (e.insertAt = "bottom");
                var n = r2(t13, e);
                return o4(n, e), function(t) {
                    for(var i = [], a = 0; a < n.length; a++){
                        var s = n[a], c = m[s.id];
                        c.refs--, i.push(c);
                    }
                    if (t) o4(r2(t, e), e);
                    for(var a = 0; a < i.length; a++){
                        var c = i[a];
                        if (0 === c.refs) {
                            for(var l = 0; l < c.parts.length; l++)c.parts[l]();
                            delete m[c.id];
                        }
                    }
                };
            };
            var x = function() {
                var t = [];
                return function(e, n) {
                    return t[e] = n, t.filter(Boolean).join("\n");
                };
            }();
        },
        function(t14, e13) {
            t14.exports = function(t15) {
                var e14 = "undefined" != typeof window && window.location;
                if (!e14) throw new Error("fixUrls requires window.location");
                if (!t15 || "string" != typeof t15) return t15;
                var n = e14.protocol + "//" + e14.host, o = n + e14.pathname.replace(/\/[^\/]*$/, "/");
                return t15.replace(/url\s*\(((?:[^)(]|\((?:[^)(]+|\([^)(]*\))*\))*)\)/gi, function(t, e15) {
                    var r = e15.trim().replace(/^"(.*)"$/, function(t, e) {
                        return e;
                    }).replace(/^'(.*)'$/, function(t, e) {
                        return e;
                    });
                    if (/^(#|data:|http:\/\/|https:\/\/|file:\/\/\/)/i.test(r)) return t;
                    var i;
                    return i = 0 === r.indexOf("//") ? r : 0 === r.indexOf("/") ? n + r : o + r.replace(/^\.\//, ""), "url(" + JSON.stringify(i) + ")";
                });
            };
        },
        function(t16, e16, n10) {
            var o5 = n10(17);
            "undefined" == typeof window || window.Promise || (window.Promise = o5), n10(21), String.prototype.includes || (String.prototype.includes = function(t, e) {
                return "number" != typeof e && (e = 0), !(e + t.length > this.length) && -1 !== this.indexOf(t, e);
            }), Array.prototype.includes || Object.defineProperty(Array.prototype, "includes", {
                value: function(t17, e17) {
                    if (null == this) throw new TypeError('"this" is null or not defined');
                    var n = Object(this), o = n.length >>> 0;
                    if (0 === o) return !1;
                    for(var r = 0 | e17, i = Math.max(r >= 0 ? r : o - Math.abs(r), 0); i < o;){
                        if (function(t, e) {
                            return t === e || "number" == typeof t && "number" == typeof e && isNaN(t) && isNaN(e);
                        }(n[i], t17)) return !0;
                        i++;
                    }
                    return !1;
                }
            }), "undefined" != typeof window && function(t18) {
                t18.forEach(function(t) {
                    t.hasOwnProperty("remove") || Object.defineProperty(t, "remove", {
                        configurable: !0,
                        enumerable: !0,
                        writable: !0,
                        value: function() {
                            this.parentNode.removeChild(this);
                        }
                    });
                });
            }([
                Element.prototype,
                CharacterData.prototype,
                DocumentType.prototype
            ]);
        },
        function(t19, e18, n11) {
            (function(e19) {
                !function(n12) {
                    function o6() {}
                    function r3(t, e) {
                        return function() {
                            t.apply(e, arguments);
                        };
                    }
                    function i3(t) {
                        if ("object" != typeof this) throw new TypeError("Promises must be constructed via new");
                        if ("function" != typeof t) throw new TypeError("not a function");
                        this._state = 0, this._handled = !1, this._value = void 0, this._deferreds = [], f(t, this);
                    }
                    function a3(t20, e) {
                        for(; 3 === t20._state;)t20 = t20._value;
                        if (0 === t20._state) return void t20._deferreds.push(e);
                        t20._handled = !0, i3._immediateFn(function() {
                            var n = 1 === t20._state ? e.onFulfilled : e.onRejected;
                            if (null === n) return void (1 === t20._state ? s3 : c)(e.promise, t20._value);
                            var o;
                            try {
                                o = n(t20._value);
                            } catch (t) {
                                return void c(e.promise, t);
                            }
                            s3(e.promise, o);
                        });
                    }
                    function s3(t, e) {
                        try {
                            if (e === t) throw new TypeError("A promise cannot be resolved with itself.");
                            if (e && ("object" == typeof e || "function" == typeof e)) {
                                var n = e.then;
                                if (e instanceof i3) return t._state = 3, t._value = e, void l(t);
                                if ("function" == typeof n) return void f(r3(n, e), t);
                            }
                            t._state = 1, t._value = e, l(t);
                        } catch (e20) {
                            c(t, e20);
                        }
                    }
                    function c(t, e) {
                        t._state = 2, t._value = e, l(t);
                    }
                    function l(t) {
                        2 === t._state && 0 === t._deferreds.length && i3._immediateFn(function() {
                            t._handled || i3._unhandledRejectionFn(t._value);
                        });
                        for(var e = 0, n = t._deferreds.length; e < n; e++)a3(t, t._deferreds[e]);
                        t._deferreds = null;
                    }
                    function u(t, e, n) {
                        this.onFulfilled = "function" == typeof t ? t : null, this.onRejected = "function" == typeof e ? e : null, this.promise = n;
                    }
                    function f(t21, e) {
                        var n = !1;
                        try {
                            t21(function(t) {
                                n || (n = !0, s3(e, t));
                            }, function(t) {
                                n || (n = !0, c(e, t));
                            });
                        } catch (t) {
                            if (n) return;
                            n = !0, c(e, t);
                        }
                    }
                    var d = setTimeout;
                    i3.prototype.catch = function(t) {
                        return this.then(null, t);
                    }, i3.prototype.then = function(t, e) {
                        var n = new this.constructor(o6);
                        return a3(this, new u(t, e, n)), n;
                    }, i3.all = function(t22) {
                        var e = Array.prototype.slice.call(t22);
                        return new i3(function(t23, n) {
                            function o(i, a) {
                                try {
                                    if (a && ("object" == typeof a || "function" == typeof a)) {
                                        var s = a.then;
                                        if ("function" == typeof s) return void s.call(a, function(t) {
                                            o(i, t);
                                        }, n);
                                    }
                                    e[i] = a, 0 == --r && t23(e);
                                } catch (t) {
                                    n(t);
                                }
                            }
                            if (0 === e.length) return t23([]);
                            for(var r = e.length, i4 = 0; i4 < e.length; i4++)o(i4, e[i4]);
                        });
                    }, i3.resolve = function(t) {
                        return t && "object" == typeof t && t.constructor === i3 ? t : new i3(function(e) {
                            e(t);
                        });
                    }, i3.reject = function(t) {
                        return new i3(function(e, n) {
                            n(t);
                        });
                    }, i3.race = function(t) {
                        return new i3(function(e, n) {
                            for(var o = 0, r = t.length; o < r; o++)t[o].then(e, n);
                        });
                    }, i3._immediateFn = "function" == typeof e19 && function(t) {
                        e19(t);
                    } || function(t) {
                        d(t, 0);
                    }, i3._unhandledRejectionFn = function(t) {
                        "undefined" != typeof console && console && console.warn("Possible Unhandled Promise Rejection:", t);
                    }, i3._setImmediateFn = function(t) {
                        i3._immediateFn = t;
                    }, i3._setUnhandledRejectionFn = function(t) {
                        i3._unhandledRejectionFn = t;
                    }, void 0 !== t19 && t19.exports ? t19.exports = i3 : n12.Promise || (n12.Promise = i3);
                }(this);
            }).call(e18, n11(18).setImmediate);
        },
        function(t24, e21, n) {
            function o(t, e) {
                this._id = t, this._clearFn = e;
            }
            var r = Function.prototype.apply;
            e21.setTimeout = function() {
                return new o(r.call(setTimeout, window, arguments), clearTimeout);
            }, e21.setInterval = function() {
                return new o(r.call(setInterval, window, arguments), clearInterval);
            }, e21.clearTimeout = e21.clearInterval = function(t) {
                t && t.close();
            }, o.prototype.unref = o.prototype.ref = function() {}, o.prototype.close = function() {
                this._clearFn.call(window, this._id);
            }, e21.enroll = function(t, e) {
                clearTimeout(t._idleTimeoutId), t._idleTimeout = e;
            }, e21.unenroll = function(t) {
                clearTimeout(t._idleTimeoutId), t._idleTimeout = -1;
            }, e21._unrefActive = e21.active = function(t) {
                clearTimeout(t._idleTimeoutId);
                var e = t._idleTimeout;
                e >= 0 && (t._idleTimeoutId = setTimeout(function() {
                    t._onTimeout && t._onTimeout();
                }, e));
            }, n(19), e21.setImmediate = setImmediate, e21.clearImmediate = clearImmediate;
        },
        function(t25, e22, n13) {
            (function(t26, e23) {
                !function(t27, n14) {
                    function o7(t) {
                        "function" != typeof t && (t = new Function("" + t));
                        for(var e = new Array(arguments.length - 1), n = 0; n < e.length; n++)e[n] = arguments[n + 1];
                        var o = {
                            callback: t,
                            args: e
                        };
                        return l[c] = o, s(c), c++;
                    }
                    function r(t) {
                        delete l[t];
                    }
                    function i(t) {
                        var e = t.callback, o = t.args;
                        switch(o.length){
                            case 0:
                                e();
                                break;
                            case 1:
                                e(o[0]);
                                break;
                            case 2:
                                e(o[0], o[1]);
                                break;
                            case 3:
                                e(o[0], o[1], o[2]);
                                break;
                            default:
                                e.apply(n14, o);
                        }
                    }
                    function a(t) {
                        if (u) setTimeout(a, 0, t);
                        else {
                            var e = l[t];
                            if (e) {
                                u = !0;
                                try {
                                    i(e);
                                } finally{
                                    r(t), u = !1;
                                }
                            }
                        }
                    }
                    if (!t27.setImmediate) {
                        var s, c = 1, l = {}, u = !1, f = t27.document, d = Object.getPrototypeOf && Object.getPrototypeOf(t27);
                        d = d && d.setTimeout ? d : t27, "[object process]" === ({}).toString.call(t27.process) ? function() {
                            s = function(t) {
                                e23.nextTick(function() {
                                    a(t);
                                });
                            };
                        }() : function() {
                            if (t27.postMessage && !t27.importScripts) {
                                var e = !0, n = t27.onmessage;
                                return t27.onmessage = function() {
                                    e = !1;
                                }, t27.postMessage("", "*"), t27.onmessage = n, e;
                            }
                        }() ? function() {
                            var e = "setImmediate$" + Math.random() + "$", n15 = function(n) {
                                n.source === t27 && "string" == typeof n.data && 0 === n.data.indexOf(e) && a(+n.data.slice(e.length));
                            };
                            t27.addEventListener ? t27.addEventListener("message", n15, !1) : t27.attachEvent("onmessage", n15), s = function(n) {
                                t27.postMessage(e + n, "*");
                            };
                        }() : t27.MessageChannel ? function() {
                            var t28 = new MessageChannel;
                            t28.port1.onmessage = function(t) {
                                a(t.data);
                            }, s = function(e) {
                                t28.port2.postMessage(e);
                            };
                        }() : f && "onreadystatechange" in f.createElement("script") ? function() {
                            var t = f.documentElement;
                            s = function(e) {
                                var n = f.createElement("script");
                                n.onreadystatechange = function() {
                                    a(e), n.onreadystatechange = null, t.removeChild(n), n = null;
                                }, t.appendChild(n);
                            };
                        }() : function() {
                            s = function(t) {
                                setTimeout(a, 0, t);
                            };
                        }(), d.setImmediate = o7, d.clearImmediate = r;
                    }
                }("undefined" == typeof self ? void 0 === t26 ? this : t26 : self);
            }).call(e22, n13(7), n13(20));
        },
        function(t29, e24) {
            function n16() {
                throw new Error("setTimeout has not been defined");
            }
            function o() {
                throw new Error("clearTimeout has not been defined");
            }
            function r(t) {
                if (u === setTimeout) return setTimeout(t, 0);
                if ((u === n16 || !u) && setTimeout) return u = setTimeout, setTimeout(t, 0);
                try {
                    return u(t, 0);
                } catch (e) {
                    try {
                        return u.call(null, t, 0);
                    } catch (e) {
                        return u.call(this, t, 0);
                    }
                }
            }
            function i(t) {
                if (f === clearTimeout) return clearTimeout(t);
                if ((f === o || !f) && clearTimeout) return f = clearTimeout, clearTimeout(t);
                try {
                    return f(t);
                } catch (e) {
                    try {
                        return f.call(null, t);
                    } catch (e) {
                        return f.call(this, t);
                    }
                }
            }
            function a() {
                b && p && (b = !1, p.length ? m = p.concat(m) : v = -1, m.length && s());
            }
            function s() {
                if (!b) {
                    var t = r(a);
                    b = !0;
                    for(var e = m.length; e;){
                        for(p = m, m = []; ++v < e;)p && p[v].run();
                        v = -1, e = m.length;
                    }
                    p = null, b = !1, i(t);
                }
            }
            function c(t, e) {
                this.fun = t, this.array = e;
            }
            function l() {}
            var u, f, d = t29.exports = {};
            !function() {
                try {
                    u = "function" == typeof setTimeout ? setTimeout : n16;
                } catch (t) {
                    u = n16;
                }
                try {
                    f = "function" == typeof clearTimeout ? clearTimeout : o;
                } catch (t30) {
                    f = o;
                }
            }();
            var p, m = [], b = !1, v = -1;
            d.nextTick = function(t) {
                var e = new Array(arguments.length - 1);
                if (arguments.length > 1) for(var n = 1; n < arguments.length; n++)e[n - 1] = arguments[n];
                m.push(new c(t, e)), 1 !== m.length || b || r(s);
            }, c.prototype.run = function() {
                this.fun.apply(null, this.array);
            }, d.title = "browser", d.browser = !0, d.env = {}, d.argv = [], d.version = "", d.versions = {}, d.on = l, d.addListener = l, d.once = l, d.off = l, d.removeListener = l, d.removeAllListeners = l, d.emit = l, d.prependListener = l, d.prependOnceListener = l, d.listeners = function(t) {
                return [];
            }, d.binding = function(t) {
                throw new Error("process.binding is not supported");
            }, d.cwd = function() {
                return "/";
            }, d.chdir = function(t) {
                throw new Error("process.chdir is not supported");
            }, d.umask = function() {
                return 0;
            };
        },
        function(t, e, n) {
            "use strict";
            n(22).polyfill();
        },
        function(t31, e, n17) {
            "use strict";
            function o8(t, e) {
                if (void 0 === t || null === t) throw new TypeError("Cannot convert first argument to object");
                for(var n = Object(t), o = 1; o < arguments.length; o++){
                    var r = arguments[o];
                    if (void 0 !== r && null !== r) for(var i = Object.keys(Object(r)), a = 0, s = i.length; a < s; a++){
                        var c = i[a], l = Object.getOwnPropertyDescriptor(r, c);
                        void 0 !== l && l.enumerable && (n[c] = r[c]);
                    }
                }
                return n;
            }
            function r4() {
                Object.assign || Object.defineProperty(Object, "assign", {
                    enumerable: !1,
                    configurable: !0,
                    writable: !0,
                    value: o8
                });
            }
            t31.exports = {
                assign: o8,
                polyfill: r4
            };
        },
        function(t32, e25, n18) {
            "use strict";
            Object.defineProperty(e25, "__esModule", {
                value: !0
            });
            var o = n18(24), r = n18(6), i = n18(5), a = n18(36), s = function() {
                for(var t33 = [], e26 = 0; e26 < arguments.length; e26++)t33[e26] = arguments[e26];
                if ("undefined" != typeof window) {
                    var n = a.getOpts.apply(void 0, t33);
                    return new Promise(function(t, e) {
                        i.default.promise = {
                            resolve: t,
                            reject: e
                        }, o.default(n), setTimeout(function() {
                            r.openModal();
                        });
                    });
                }
            };
            s.close = r.onAction, s.getState = r.getState, s.setActionValue = i.setActionValue, s.stopLoading = r.stopLoading, s.setDefaults = a.setDefaults, e25.default = s;
        },
        function(t34, e, n) {
            "use strict";
            Object.defineProperty(e, "__esModule", {
                value: !0
            });
            var o = n(1), r = n(0), i = r.default.MODAL, a = n(4), s = n(34), c = n(35), l = n(1);
            e.init = function(t) {
                o.getNode(i) || (document.body || l.throwErr("You can only use SweetAlert AFTER the DOM has loaded!"), s.default(), a.default()), a.initModalContent(t), c.default(t);
            }, e.default = e.init;
        },
        function(t, e, n) {
            "use strict";
            Object.defineProperty(e, "__esModule", {
                value: !0
            });
            var o = n(0), r = o.default.MODAL;
            e.modalMarkup = '\n  <div class="' + r + '" role="dialog" aria-modal="true"></div>', e.default = e.modalMarkup;
        },
        function(t, e, n) {
            "use strict";
            Object.defineProperty(e, "__esModule", {
                value: !0
            });
            var o = n(0), r = o.default.OVERLAY, i = '<div \n    class="' + r + '"\n    tabIndex="-1">\n  </div>';
            e.default = i;
        },
        function(t35, e27, n) {
            "use strict";
            Object.defineProperty(e27, "__esModule", {
                value: !0
            });
            var o = n(0), r = o.default.ICON;
            e27.errorIconMarkup = function() {
                var t = r + "--error", e = t + "__line";
                return '\n    <div class="' + t + '__x-mark">\n      <span class="' + e + " " + e + '--left"></span>\n      <span class="' + e + " " + e + '--right"></span>\n    </div>\n  ';
            }, e27.warningIconMarkup = function() {
                var t = r + "--warning";
                return '\n    <span class="' + t + '__body">\n      <span class="' + t + '__dot"></span>\n    </span>\n  ';
            }, e27.successIconMarkup = function() {
                var t = r + "--success";
                return '\n    <span class="' + t + "__line " + t + '__line--long"></span>\n    <span class="' + t + "__line " + t + '__line--tip"></span>\n\n    <div class="' + t + '__ring"></div>\n    <div class="' + t + '__hide-corners"></div>\n  ';
            };
        },
        function(t, e, n) {
            "use strict";
            Object.defineProperty(e, "__esModule", {
                value: !0
            });
            var o = n(0), r = o.default.CONTENT;
            e.contentMarkup = '\n  <div class="' + r + '">\n\n  </div>\n';
        },
        function(t, e, n) {
            "use strict";
            Object.defineProperty(e, "__esModule", {
                value: !0
            });
            var o = n(0), r = o.default.BUTTON_CONTAINER, i = o.default.BUTTON, a = o.default.BUTTON_LOADER;
            e.buttonMarkup = '\n  <div class="' + r + '">\n\n    <button\n      class="' + i + '"\n    ></button>\n\n    <div class="' + a + '">\n      <div></div>\n      <div></div>\n      <div></div>\n    </div>\n\n  </div>\n';
        },
        function(t36, e28, n19) {
            "use strict";
            Object.defineProperty(e28, "__esModule", {
                value: !0
            });
            var o9 = n19(4), r = n19(2), i = n19(0), a = i.default.ICON, s = i.default.ICON_CUSTOM, c = [
                "error",
                "warning",
                "success",
                "info"
            ], l = {
                error: r.errorIconMarkup(),
                warning: r.warningIconMarkup(),
                success: r.successIconMarkup()
            }, u = function(t, e) {
                var n = a + "--" + t;
                e.classList.add(n);
                var o = l[t];
                o && (e.innerHTML = o);
            }, f = function(t, e) {
                e.classList.add(s);
                var n = document.createElement("img");
                n.src = t, e.appendChild(n);
            }, d = function(t) {
                if (t) {
                    var e = o9.injectElIntoModal(r.iconMarkup);
                    c.includes(t) ? u(t, e) : f(t, e);
                }
            };
            e28.default = d;
        },
        function(t37, e29, n20) {
            "use strict";
            Object.defineProperty(e29, "__esModule", {
                value: !0
            });
            var o10 = n20(2), r = n20(4), i = function(t) {
                navigator.userAgent.includes("AppleWebKit") && (t.style.display = "none", t.offsetHeight, t.style.display = "");
            };
            e29.initTitle = function(t) {
                if (t) {
                    var e = r.injectElIntoModal(o10.titleMarkup);
                    e.textContent = t, i(e);
                }
            }, e29.initText = function(t38) {
                if (t38) {
                    var e = document.createDocumentFragment();
                    t38.split("\n").forEach(function(t, n, o) {
                        e.appendChild(document.createTextNode(t)), n < o.length - 1 && e.appendChild(document.createElement("br"));
                    });
                    var n21 = r.injectElIntoModal(o10.textMarkup);
                    n21.appendChild(e), i(n21);
                }
            };
        },
        function(t39, e30, n23) {
            "use strict";
            Object.defineProperty(e30, "__esModule", {
                value: !0
            });
            var o11 = n23(1), r5 = n23(4), i5 = n23(0), a4 = i5.default.BUTTON, s = i5.default.DANGER_BUTTON, c = n23(3), l = n23(2), u = n23(6), f = n23(5), d1 = function(t40, e, n) {
                var r = e.text, i = e.value, d = e.className, p = e.closeModal, m = o11.stringToNode(l.buttonMarkup), b = m.querySelector("." + a4), v = a4 + "--" + t40;
                if (b.classList.add(v), d) (Array.isArray(d) ? d : d.split(" ")).filter(function(t) {
                    return t.length > 0;
                }).forEach(function(t) {
                    b.classList.add(t);
                });
                n && t40 === c.CONFIRM_KEY && b.classList.add(s), b.textContent = r;
                var g = {};
                return g[t40] = i, f.setActionValue(g), f.setActionOptionsFor(t40, {
                    closeModal: p
                }), b.addEventListener("click", function() {
                    return u.onAction(t40);
                }), m;
            }, p1 = function(t, e) {
                var n = r5.injectElIntoModal(l.footerMarkup);
                for(var o in t){
                    var i = t[o], a = d1(o, i, e);
                    i.visible && n.appendChild(a);
                }
                0 === n.children.length && n.remove();
            };
            e30.default = p1;
        },
        function(t41, e31, n24) {
            "use strict";
            Object.defineProperty(e31, "__esModule", {
                value: !0
            });
            var o12 = n24(3), r6 = n24(4), i6 = n24(2), a5 = n24(5), s = n24(6), c = n24(0), l = c.default.CONTENT, u = function(t42) {
                t42.addEventListener("input", function(t) {
                    var e = t.target, n = e.value;
                    a5.setActionValue(n);
                }), t42.addEventListener("keyup", function(t) {
                    if ("Enter" === t.key) return s.onAction(o12.CONFIRM_KEY);
                }), setTimeout(function() {
                    t42.focus(), a5.setActionValue("");
                }, 0);
            }, f = function(t, e, n) {
                var o = document.createElement(e), r = l + "__" + e;
                o.classList.add(r);
                for(var i in n){
                    var a = n[i];
                    o[i] = a;
                }
                "input" === e && u(o), t.appendChild(o);
            }, d = function(t) {
                if (t) {
                    var e = r6.injectElIntoModal(i6.contentMarkup), n = t.element, o = t.attributes;
                    "string" == typeof n ? f(e, n, o) : e.appendChild(n);
                }
            };
            e31.default = d;
        },
        function(t43, e, n) {
            "use strict";
            Object.defineProperty(e, "__esModule", {
                value: !0
            });
            var o = n(1), r = n(2), i = function() {
                var t = o.stringToNode(r.overlayMarkup);
                document.body.appendChild(t);
            };
            e.default = i;
        },
        function(t44, e32, n25) {
            "use strict";
            Object.defineProperty(e32, "__esModule", {
                value: !0
            });
            var o13 = n25(5), r = n25(6), i = n25(1), a = n25(3), s = n25(0), c = s.default.MODAL, l = s.default.BUTTON, u = s.default.OVERLAY, f = function(t) {
                t.preventDefault(), v();
            }, d = function(t) {
                t.preventDefault(), g();
            }, p = function(t) {
                if (o13.default.isOpen) switch(t.key){
                    case "Escape":
                        return r.onAction(a.CANCEL_KEY);
                }
            }, m = function(t) {
                if (o13.default.isOpen) switch(t.key){
                    case "Tab":
                        return f(t);
                }
            }, b = function(t) {
                if (o13.default.isOpen) return "Tab" === t.key && t.shiftKey ? d(t) : void 0;
            }, v = function() {
                var t = i.getNode(l);
                t && (t.tabIndex = 0, t.focus());
            }, g = function() {
                var t = i.getNode(c), e = t.querySelectorAll("." + l), n = e.length - 1, o = e[n];
                o && o.focus();
            }, h = function(t) {
                t[t.length - 1].addEventListener("keydown", m);
            }, w = function(t) {
                t[0].addEventListener("keydown", b);
            }, y = function() {
                var t = i.getNode(c), e = t.querySelectorAll("." + l);
                e.length && (h(e), w(e));
            }, x = function(t) {
                if (i.getNode(u) === t.target) return r.onAction(a.CANCEL_KEY);
            }, _ = function(t) {
                var e = i.getNode(u);
                e.removeEventListener("click", x), t && e.addEventListener("click", x);
            }, k = function(t) {
                o13.default.timer && clearTimeout(o13.default.timer), t && (o13.default.timer = window.setTimeout(function() {
                    return r.onAction(a.CANCEL_KEY);
                }, t));
            }, O = function(t) {
                t.closeOnEsc ? document.addEventListener("keyup", p) : document.removeEventListener("keyup", p), t.dangerMode ? v() : g(), y(), _(t.closeOnClickOutside), k(t.timer);
            };
            e32.default = O;
        },
        function(t45, e33, n26) {
            "use strict";
            Object.defineProperty(e33, "__esModule", {
                value: !0
            });
            var o14 = n26(1), r7 = n26(3), i7 = n26(37), a6 = n26(38), s4 = {
                title: null,
                text: null,
                icon: null,
                buttons: r7.defaultButtonList,
                content: null,
                className: null,
                closeOnClickOutside: !0,
                closeOnEsc: !0,
                dangerMode: !1,
                timer: null
            }, c = Object.assign({}, s4);
            e33.setDefaults = function(t) {
                c = Object.assign({}, s4, t);
            };
            var l = function(t) {
                var e = t && t.button, n = t && t.buttons;
                return void 0 !== e && void 0 !== n && o14.throwErr("Cannot set both 'button' and 'buttons' options!"), void 0 !== e ? {
                    confirm: e
                } : n;
            }, u2 = function(t) {
                return o14.ordinalSuffixOf(t + 1);
            }, f = function(t, e) {
                o14.throwErr(u2(e) + " argument ('" + t + "') is invalid");
            }, d = function(t, e) {
                var n = t + 1, r = e[n];
                o14.isPlainObject(r) || void 0 === r || o14.throwErr("Expected " + u2(n) + " argument ('" + r + "') to be a plain object");
            }, p = function(t, e) {
                var n = t + 1, r = e[n];
                void 0 !== r && o14.throwErr("Unexpected " + u2(n) + " argument (" + r + ")");
            }, m = function(t, e, n, r) {
                var i = typeof e, a = "string" === i, s = e instanceof Element;
                if (a) {
                    if (0 === n) return {
                        text: e
                    };
                    if (1 === n) return {
                        text: e,
                        title: r[0]
                    };
                    if (2 === n) return d(n, r), {
                        icon: e
                    };
                    f(e, n);
                } else {
                    if (s && 0 === n) return d(n, r), {
                        content: e
                    };
                    if (o14.isPlainObject(e)) return p(n, r), e;
                    f(e, n);
                }
            };
            e33.getOpts = function() {
                for(var t46 = [], e34 = 0; e34 < arguments.length; e34++)t46[e34] = arguments[e34];
                var n = {};
                t46.forEach(function(e, o) {
                    var r = m(0, e, o, t46);
                    Object.assign(n, r);
                });
                var o15 = l(n);
                n.buttons = r7.getButtonListOpts(o15), delete n.button, n.content = i7.getContentOpts(n.content);
                var u = Object.assign({}, s4, c, n);
                return Object.keys(u).forEach(function(t) {
                    a6.DEPRECATED_OPTS[t] && a6.logDeprecation(t);
                }), u;
            };
        },
        function(t47, e35, n) {
            "use strict";
            Object.defineProperty(e35, "__esModule", {
                value: !0
            });
            var o = n(1), r = {
                element: "input",
                attributes: {
                    placeholder: ""
                }
            };
            e35.getContentOpts = function(t) {
                var e = {};
                return o.isPlainObject(t) ? Object.assign(e, t) : t instanceof Element ? {
                    element: t
                } : "input" === t ? r : null;
            };
        },
        function(t48, e, n27) {
            "use strict";
            Object.defineProperty(e, "__esModule", {
                value: !0
            }), e.logDeprecation = function(t) {
                var n = e.DEPRECATED_OPTS[t], o = n.onlyRename, r = n.replacement, i = n.subOption, a = n.link, s = o ? "renamed" : "deprecated", c = 'SweetAlert warning: "' + t + '" option has been ' + s + ".";
                if (r) c += " Please use" + (i ? ' "' + i + '" in ' : " ") + '"' + r + '" instead.';
                var l = "https://sweetalert.js.org";
                c += a ? " More details: " + l + a : " More details: " + l + "/guides/#upgrading-from-1x", console.warn(c);
            }, e.DEPRECATED_OPTS = {
                type: {
                    replacement: "icon",
                    link: "/docs/#icon"
                },
                imageUrl: {
                    replacement: "icon",
                    link: "/docs/#icon"
                },
                customClass: {
                    replacement: "className",
                    onlyRename: !0,
                    link: "/docs/#classname"
                },
                imageSize: {},
                showCancelButton: {
                    replacement: "buttons",
                    link: "/docs/#buttons"
                },
                showConfirmButton: {
                    replacement: "button",
                    link: "/docs/#button"
                },
                confirmButtonText: {
                    replacement: "button",
                    link: "/docs/#button"
                },
                confirmButtonColor: {},
                cancelButtonText: {
                    replacement: "buttons",
                    link: "/docs/#buttons"
                },
                closeOnConfirm: {
                    replacement: "button",
                    subOption: "closeModal",
                    link: "/docs/#button"
                },
                closeOnCancel: {
                    replacement: "buttons",
                    subOption: "closeModal",
                    link: "/docs/#buttons"
                },
                showLoaderOnConfirm: {
                    replacement: "buttons"
                },
                animation: {},
                inputType: {
                    replacement: "content",
                    link: "/docs/#content"
                },
                inputValue: {
                    replacement: "content",
                    link: "/docs/#content"
                },
                inputPlaceholder: {
                    replacement: "content",
                    link: "/docs/#content"
                },
                html: {
                    replacement: "content",
                    link: "/docs/#content"
                },
                allowEscapeKey: {
                    replacement: "closeOnEsc",
                    onlyRename: !0,
                    link: "/docs/#closeonesc"
                },
                allowClickOutside: {
                    replacement: "closeOnClickOutside",
                    onlyRename: !0,
                    link: "/docs/#closeonclickoutside"
                }
            };
        }
    ]);
});

},{}],"km3Ru":[function(require,module,exports) {
"use strict";
var Refresh = require('react-refresh/runtime');
function debounce(func, delay) {
    var args1;
    var timeout = undefined;
    return function(args) {
        clearTimeout(timeout);
        timeout = setTimeout(function() {
            timeout = undefined;
            func.call(null, args);
        }, delay);
    };
}
var enqueueUpdate = debounce(function() {
    Refresh.performReactRefresh();
}, 30); // Everthing below is either adapted or copied from
// https://github.com/facebook/metro/blob/61de16bd1edd7e738dd0311c89555a644023ab2d/packages/metro/src/lib/polyfills/require.js
// MIT License - Copyright (c) Facebook, Inc. and its affiliates.
module.exports.prelude = function(module) {
    window.$RefreshReg$ = function(type, id) {
        Refresh.register(type, module.id + ' ' + id);
    };
    window.$RefreshSig$ = Refresh.createSignatureFunctionForTransform;
};
module.exports.postlude = function(module) {
    if (isReactRefreshBoundary(module.exports)) {
        registerExportsForReactRefresh(module);
        if (module.hot) {
            module.hot.dispose(function(data) {
                if (Refresh.hasUnrecoverableErrors()) window.location.reload();
                data.prevExports = module.exports;
            });
            module.hot.accept(function(getParents) {
                var prevExports = module.hot.data.prevExports;
                var nextExports = module.exports; // Since we just executed the code for it, it's possible
                // that the new exports make it ineligible for being a boundary.
                var isNoLongerABoundary = !isReactRefreshBoundary(nextExports); // It can also become ineligible if its exports are incompatible
                // with the previous exports.
                // For example, if you add/remove/change exports, we'll want
                // to re-execute the importing modules, and force those components
                // to re-render. Similarly, if you convert a class component
                // to a function, we want to invalidate the boundary.
                var didInvalidate = shouldInvalidateReactRefreshBoundary(prevExports, nextExports);
                if (isNoLongerABoundary || didInvalidate) {
                    // We'll be conservative. The only case in which we won't do a full
                    // reload is if all parent modules are also refresh boundaries.
                    // In that case we'll add them to the current queue.
                    var parents = getParents();
                    if (parents.length === 0) {
                        // Looks like we bubbled to the root. Can't recover from that.
                        window.location.reload();
                        return;
                    }
                    return parents;
                }
                enqueueUpdate();
            });
        }
    }
};
function isReactRefreshBoundary(exports) {
    if (Refresh.isLikelyComponentType(exports)) return true;
    if (exports == null || typeof exports !== 'object') // Exit if we can't iterate over exports.
    return false;
    var hasExports = false;
    var areAllExportsComponents = true;
    let isESM = '__esModule' in exports;
    for(var key in exports){
        hasExports = true;
        if (key === '__esModule') continue;
        var desc = Object.getOwnPropertyDescriptor(exports, key);
        if (desc && desc.get && !isESM) // Don't invoke getters for CJS as they may have side effects.
        return false;
        var exportValue = exports[key];
        if (!Refresh.isLikelyComponentType(exportValue)) areAllExportsComponents = false;
    }
    return hasExports && areAllExportsComponents;
}
function shouldInvalidateReactRefreshBoundary(prevExports, nextExports) {
    var prevSignature = getRefreshBoundarySignature(prevExports);
    var nextSignature = getRefreshBoundarySignature(nextExports);
    if (prevSignature.length !== nextSignature.length) return true;
    for(var i = 0; i < nextSignature.length; i++){
        if (prevSignature[i] !== nextSignature[i]) return true;
    }
    return false;
} // When this signature changes, it's unsafe to stop at this refresh boundary.
function getRefreshBoundarySignature(exports) {
    var signature = [];
    signature.push(Refresh.getFamilyByType(exports));
    if (exports == null || typeof exports !== 'object') // Exit if we can't iterate over exports.
    // (This is important for legacy environments.)
    return signature;
    let isESM = '__esModule' in exports;
    for(var key in exports){
        if (key === '__esModule') continue;
        var desc = Object.getOwnPropertyDescriptor(exports, key);
        if (desc && desc.get && !isESM) continue;
        var exportValue = exports[key];
        signature.push(key);
        signature.push(Refresh.getFamilyByType(exportValue));
    }
    return signature;
}
function registerExportsForReactRefresh(module) {
    var exports = module.exports, id = module.id;
    Refresh.register(exports, id + ' %exports%');
    if (exports == null || typeof exports !== 'object') // Exit if we can't iterate over exports.
    // (This is important for legacy environments.)
    return;
    let isESM = '__esModule' in exports;
    for(var key in exports){
        var desc = Object.getOwnPropertyDescriptor(exports, key);
        if (desc && desc.get && !isESM) continue;
        var exportValue = exports[key];
        Refresh.register(exportValue, id + ' %exports% ' + key);
    }
}

},{"react-refresh/runtime":"786KC"}],"8v7IM":[function(require,module,exports) {
var $parcel$ReactRefreshHelpers$a4ba = require("@parcel/transformer-react-refresh-wrap/lib/helpers/helpers.js");
var prevRefreshReg = window.$RefreshReg$;
var prevRefreshSig = window.$RefreshSig$;
$parcel$ReactRefreshHelpers$a4ba.prelude(module);

try {
var parcelHelpers = require("@parcel/transformer-js/src/esmodule-helpers.js");
parcelHelpers.defineInteropFlag(exports);
var _jsxDevRuntime = require("react/jsx-dev-runtime");
// user- view user profile
var _react = require("react");
var _reactDefault = parcelHelpers.interopDefault(_react);
var _jwtDecode = require("jwt-decode");
var _jwtDecodeDefault = parcelHelpers.interopDefault(_jwtDecode);
var _axios = require("axios");
var _axiosDefault = parcelHelpers.interopDefault(_axios);
var _sweetalert = require("sweetalert");
var _sweetalertDefault = parcelHelpers.interopDefault(_sweetalert);
class UserProfile extends _react.Component {
    constructor(){
        super();
        this.state = {
            _id: '',
            idNumber: '',
            name: '',
            email: '',
            mobile: '',
            groupId: '',
            researchfield: '',
            panel: '',
            dateRegistered: '',
            type: '',
            password: '',
            enteredPassword: '',
            newPassword: '',
            confirmNewPassword: ''
        };
    }
    componentDidMount() {
        document.title = "User Profile";
        // redirect to the login page if the user is not logged in
        if (!localStorage.userToken) _sweetalertDefault.default("Please login first", "", "warning").then((value)=>{
            if (value) {
                this.props.history.push(`/user/login`);
                window.location.reload();
            }
        });
        const usertoken = localStorage.userToken;
        const decoded = _jwtDecodeDefault.default(usertoken);
        this.setState({
            _id: decoded._id
        });
        const id = decoded._id;
        this.retrieveProfile(id);
    // if(this.state.type === 'Supervisor' && this.state.researchfield === ''){
    //     alert('Please update your profile with your research interest')
    // }
    }
    retrieveProfile(id) {
        _axiosDefault.default.get(`http://localhost:5000/user/${id}`).then((res)=>{
            if (res.data.success) this.setState({
                idNumber: res.data.user.idNumber,
                name: res.data.user.name,
                email: res.data.user.email,
                mobile: res.data.user.mobile,
                groupId: res.data.user.groupId,
                researchfield: res.data.user.researchfield,
                panel: res.data.user.panel,
                dateRegistered: res.data.user.dateRegistered,
                type: res.data.user.type,
                password: res.data.user.password
            });
        });
    }
    handleInputChange = (e)=>{
        const { name , value  } = e.target;
        this.setState({
            ...this.state,
            [name]: value
        });
    };
    onSubmit = (e)=>{
        e.preventDefault();
        const { _id , idNumber , name , email , mobile , groupId , researchfield , panel , type , password  } = this.state;
        let data = {
            idNumber: idNumber,
            name: name,
            email: email,
            mobile: mobile,
            groupId: groupId,
            researchfield: researchfield,
            panel: panel,
            type: type,
            password: password
        };
        // console.log(data)
        // validations----------------------------------------------------------
        let validated = true;
        if (data.name === '' || data.name.length < 5) {
            validated = false;
            _sweetalertDefault.default({
                title: "",
                text: "Name cannot be empty",
                icon: "warning"
            });
        } else if (!data.email.match(/^(([^<>()\[\]\.,;:\s@\"]+(\.[^<>()\[\]\.,;:\s@\"]+)*)|(\".+\"))@(([^<>()[\]\.,;:\s@\"]+\.)+[^<>()[\]\.,;:\s@\"]{2,})$/i)) {
            validated = false;
            _sweetalertDefault.default({
                title: "",
                text: "Please enter a valid email",
                icon: "warning"
            });
        } else if (!data.mobile.match(/^(\+\d{1,3}[- ]?)?\d{10}$/)) {
            validated = false;
            _sweetalertDefault.default({
                title: "",
                text: "Please enter a valid mobile number",
                icon: "warning"
            });
        } else if (this.state.type === 'Supervisor' && data.researchfield === '') {
            validated = false;
            _sweetalertDefault.default({
                title: "",
                text: "Please add your research field",
                icon: "warning"
            });
        } else if (this.state.confirmNewPassword != this.state.newPassword) {
            validated = false;
            _sweetalertDefault.default({
                title: "",
                text: "Please check the new password and repeated new password",
                icon: "warning"
            });
        } else if (this.state.enteredPassword != '' && this.state.newPassword === '') {
            validated = false;
            _sweetalertDefault.default({
                title: "",
                text: "Please enter a new password",
                icon: "warning"
            });
        } else if (this.state.enteredPassword === '' && this.state.newPassword != '') {
            validated = false;
            _sweetalertDefault.default({
                title: "",
                text: "Please enter your existing password",
                icon: "warning"
            });
        } else if (this.state.enteredPassword != '' && this.state.newPassword.length < 8) {
            validated = false;
            _sweetalertDefault.default({
                title: "",
                text: "Password should have at least 8 characters",
                icon: "warning"
            });
        }
        // console.log(data)
        if (validated) {
            if (this.state.enteredPassword === '' || this.state.newPassword === '') _axiosDefault.default.put(`http://localhost:5000/user/update/${_id}`, data).then((res)=>{
                if (res.data.success) _sweetalertDefault.default("Profile updated successfully!", "", "success").then((value)=>{
                    if (value) {
                        this.props.history.push(`/user/profile`);
                        window.location.reload();
                    }
                });
            }).catch((err)=>{
                console.log(err);
                _sweetalertDefault.default({
                    title: "",
                    text: "Something went wrong! Please check the entered passwords",
                    icon: "warning"
                });
            });
            else {
                data.enteredPassword = this.state.enteredPassword;
                data.newPassword = this.state.newPassword;
                _axiosDefault.default.put(`http://localhost:5000/user/updateprofile/${_id}`, data).then((res)=>{
                    if (res.data.success) _sweetalertDefault.default("Profile updated successfully!", "", "success").then((value)=>{
                        if (value) {
                            this.props.history.push(`/user/profile`);
                            window.location.reload();
                        }
                    });
                }).catch((err)=>{
                    console.log(err);
                    _sweetalertDefault.default({
                        title: "",
                        text: "Something went wrong! Please check the entered passwords",
                        icon: "warning"
                    });
                });
            }
        }
    };
    render() {
        return /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
            className: "container",
            style: {
                padding: '50px 50px 50px 50px',
                background: 'white',
                minHeight: '100vh'
            },
            children: [
                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                    className: "col-lg-9 mt-2 mb-2",
                    children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("h1", {
                        children: "Profile"
                    }, void 0, false, {
                        fileName: "src/components/IT20125202/user/UserProfile.js",
                        lineNumber: 231,
                        columnNumber: 21
                    }, this)
                }, void 0, false, {
                    fileName: "src/components/IT20125202/user/UserProfile.js",
                    lineNumber: 230,
                    columnNumber: 17
                }, this),
                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("hr", {}, void 0, false, {
                    fileName: "src/components/IT20125202/user/UserProfile.js",
                    lineNumber: 233,
                    columnNumber: 17
                }, this),
                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                    className: "col-md-8 mt-4 mx-auto",
                    children: [
                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("br", {}, void 0, false, {
                            fileName: "src/components/IT20125202/user/UserProfile.js",
                            lineNumber: 236,
                            columnNumber: 21
                        }, this),
                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("form", {
                            className: "needs-validation",
                            noValidate: true,
                            children: [
                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                    className: "form-group",
                                    style: {
                                        marginBottom: '15px'
                                    },
                                    children: [
                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("label", {
                                            style: {
                                                marginBottom: '5px'
                                            },
                                            children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("b", {
                                                children: "Registration Number"
                                            }, void 0, false, {
                                                fileName: "src/components/IT20125202/user/UserProfile.js",
                                                lineNumber: 239,
                                                columnNumber: 68
                                            }, this)
                                        }, void 0, false, {
                                            fileName: "src/components/IT20125202/user/UserProfile.js",
                                            lineNumber: 239,
                                            columnNumber: 29
                                        }, this),
                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("input", {
                                            type: "text",
                                            className: "form-control",
                                            name: "idNumber",
                                            value: this.state.idNumber,
                                            onChange: this.handleInputChange,
                                            readOnly: true
                                        }, void 0, false, {
                                            fileName: "src/components/IT20125202/user/UserProfile.js",
                                            lineNumber: 240,
                                            columnNumber: 29
                                        }, this)
                                    ]
                                }, void 0, true, {
                                    fileName: "src/components/IT20125202/user/UserProfile.js",
                                    lineNumber: 238,
                                    columnNumber: 25
                                }, this),
                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                    className: "form-group",
                                    style: {
                                        marginBottom: '15px'
                                    },
                                    children: [
                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("label", {
                                            style: {
                                                marginBottom: '5px'
                                            },
                                            children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("b", {
                                                children: "Name"
                                            }, void 0, false, {
                                                fileName: "src/components/IT20125202/user/UserProfile.js",
                                                lineNumber: 251,
                                                columnNumber: 68
                                            }, this)
                                        }, void 0, false, {
                                            fileName: "src/components/IT20125202/user/UserProfile.js",
                                            lineNumber: 251,
                                            columnNumber: 29
                                        }, this),
                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("input", {
                                            type: "text",
                                            className: "form-control",
                                            name: "name",
                                            value: this.state.name,
                                            onChange: this.handleInputChange
                                        }, void 0, false, {
                                            fileName: "src/components/IT20125202/user/UserProfile.js",
                                            lineNumber: 252,
                                            columnNumber: 29
                                        }, this)
                                    ]
                                }, void 0, true, {
                                    fileName: "src/components/IT20125202/user/UserProfile.js",
                                    lineNumber: 250,
                                    columnNumber: 25
                                }, this),
                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                    className: "form-group",
                                    style: {
                                        marginBottom: '15px'
                                    },
                                    children: [
                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("label", {
                                            style: {
                                                marginBottom: '5px'
                                            },
                                            children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("b", {
                                                children: "Email"
                                            }, void 0, false, {
                                                fileName: "src/components/IT20125202/user/UserProfile.js",
                                                lineNumber: 263,
                                                columnNumber: 68
                                            }, this)
                                        }, void 0, false, {
                                            fileName: "src/components/IT20125202/user/UserProfile.js",
                                            lineNumber: 263,
                                            columnNumber: 29
                                        }, this),
                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("input", {
                                            type: "email",
                                            className: "form-control",
                                            name: "email",
                                            value: this.state.email,
                                            onChange: this.handleInputChange
                                        }, void 0, false, {
                                            fileName: "src/components/IT20125202/user/UserProfile.js",
                                            lineNumber: 264,
                                            columnNumber: 29
                                        }, this)
                                    ]
                                }, void 0, true, {
                                    fileName: "src/components/IT20125202/user/UserProfile.js",
                                    lineNumber: 262,
                                    columnNumber: 25
                                }, this),
                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                    className: "form-group",
                                    style: {
                                        marginBottom: '15px'
                                    },
                                    children: [
                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("label", {
                                            style: {
                                                marginBottom: '5px'
                                            },
                                            children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("b", {
                                                children: "Mobile Number"
                                            }, void 0, false, {
                                                fileName: "src/components/IT20125202/user/UserProfile.js",
                                                lineNumber: 275,
                                                columnNumber: 68
                                            }, this)
                                        }, void 0, false, {
                                            fileName: "src/components/IT20125202/user/UserProfile.js",
                                            lineNumber: 275,
                                            columnNumber: 29
                                        }, this),
                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("input", {
                                            type: "phone",
                                            className: "form-control",
                                            name: "mobile",
                                            value: this.state.mobile,
                                            onChange: this.handleInputChange
                                        }, void 0, false, {
                                            fileName: "src/components/IT20125202/user/UserProfile.js",
                                            lineNumber: 276,
                                            columnNumber: 29
                                        }, this)
                                    ]
                                }, void 0, true, {
                                    fileName: "src/components/IT20125202/user/UserProfile.js",
                                    lineNumber: 274,
                                    columnNumber: 25
                                }, this),
                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                    className: "form-group",
                                    style: {
                                        marginBottom: '15px'
                                    },
                                    children: [
                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("label", {
                                            style: {
                                                marginBottom: '5px'
                                            },
                                            children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("b", {
                                                children: "Registered Date"
                                            }, void 0, false, {
                                                fileName: "src/components/IT20125202/user/UserProfile.js",
                                                lineNumber: 287,
                                                columnNumber: 68
                                            }, this)
                                        }, void 0, false, {
                                            fileName: "src/components/IT20125202/user/UserProfile.js",
                                            lineNumber: 287,
                                            columnNumber: 29
                                        }, this),
                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("input", {
                                            type: "text",
                                            className: "form-control",
                                            name: "dateRegistered",
                                            value: this.state.dateRegistered,
                                            onChange: this.handleInputChange,
                                            readOnly: true
                                        }, void 0, false, {
                                            fileName: "src/components/IT20125202/user/UserProfile.js",
                                            lineNumber: 288,
                                            columnNumber: 29
                                        }, this)
                                    ]
                                }, void 0, true, {
                                    fileName: "src/components/IT20125202/user/UserProfile.js",
                                    lineNumber: 286,
                                    columnNumber: 25
                                }, this),
                                this.state.type === 'Student' && /*#__PURE__*/ _jsxDevRuntime.jsxDEV("span", {
                                    children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                        className: "form-group",
                                        style: {
                                            marginBottom: '15px'
                                        },
                                        children: [
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("label", {
                                                style: {
                                                    marginBottom: '5px'
                                                },
                                                children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("b", {
                                                    children: "Group ID"
                                                }, void 0, false, {
                                                    fileName: "src/components/IT20125202/user/UserProfile.js",
                                                    lineNumber: 302,
                                                    columnNumber: 76
                                                }, this)
                                            }, void 0, false, {
                                                fileName: "src/components/IT20125202/user/UserProfile.js",
                                                lineNumber: 302,
                                                columnNumber: 37
                                            }, this),
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("input", {
                                                type: "text",
                                                className: "form-control",
                                                name: "groupId",
                                                value: this.state.groupId,
                                                onChange: this.handleInputChange,
                                                readOnly: true
                                            }, void 0, false, {
                                                fileName: "src/components/IT20125202/user/UserProfile.js",
                                                lineNumber: 303,
                                                columnNumber: 37
                                            }, this)
                                        ]
                                    }, void 0, true, {
                                        fileName: "src/components/IT20125202/user/UserProfile.js",
                                        lineNumber: 301,
                                        columnNumber: 33
                                    }, this)
                                }, void 0, false, {
                                    fileName: "src/components/IT20125202/user/UserProfile.js",
                                    lineNumber: 300,
                                    columnNumber: 29
                                }, this),
                                this.state.type === 'Supervisor' && /*#__PURE__*/ _jsxDevRuntime.jsxDEV("span", {
                                    children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                        className: "form-group",
                                        style: {
                                            marginBottom: '15px'
                                        },
                                        children: [
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("label", {
                                                style: {
                                                    marginBottom: '5px'
                                                },
                                                children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("b", {
                                                    children: "Research Field"
                                                }, void 0, false, {
                                                    fileName: "src/components/IT20125202/user/UserProfile.js",
                                                    lineNumber: 318,
                                                    columnNumber: 76
                                                }, this)
                                            }, void 0, false, {
                                                fileName: "src/components/IT20125202/user/UserProfile.js",
                                                lineNumber: 318,
                                                columnNumber: 37
                                            }, this),
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("input", {
                                                type: "text",
                                                className: "form-control",
                                                name: "researchfield",
                                                value: this.state.researchfield,
                                                onChange: this.handleInputChange
                                            }, void 0, false, {
                                                fileName: "src/components/IT20125202/user/UserProfile.js",
                                                lineNumber: 319,
                                                columnNumber: 37
                                            }, this)
                                        ]
                                    }, void 0, true, {
                                        fileName: "src/components/IT20125202/user/UserProfile.js",
                                        lineNumber: 317,
                                        columnNumber: 33
                                    }, this)
                                }, void 0, false, {
                                    fileName: "src/components/IT20125202/user/UserProfile.js",
                                    lineNumber: 316,
                                    columnNumber: 29
                                }, this),
                                this.state.type === 'Panel Member' && /*#__PURE__*/ _jsxDevRuntime.jsxDEV("span", {
                                    children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                        className: "form-group",
                                        style: {
                                            marginBottom: '15px'
                                        },
                                        children: [
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("label", {
                                                style: {
                                                    marginBottom: '5px'
                                                },
                                                children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("b", {
                                                    children: "Panel"
                                                }, void 0, false, {
                                                    fileName: "src/components/IT20125202/user/UserProfile.js",
                                                    lineNumber: 334,
                                                    columnNumber: 76
                                                }, this)
                                            }, void 0, false, {
                                                fileName: "src/components/IT20125202/user/UserProfile.js",
                                                lineNumber: 334,
                                                columnNumber: 37
                                            }, this),
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("input", {
                                                type: "text",
                                                className: "form-control",
                                                name: "panel",
                                                value: this.state.panel,
                                                onChange: this.handleInputChange,
                                                readOnly: true
                                            }, void 0, false, {
                                                fileName: "src/components/IT20125202/user/UserProfile.js",
                                                lineNumber: 335,
                                                columnNumber: 37
                                            }, this)
                                        ]
                                    }, void 0, true, {
                                        fileName: "src/components/IT20125202/user/UserProfile.js",
                                        lineNumber: 333,
                                        columnNumber: 33
                                    }, this)
                                }, void 0, false, {
                                    fileName: "src/components/IT20125202/user/UserProfile.js",
                                    lineNumber: 332,
                                    columnNumber: 29
                                }, this),
                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("br", {}, void 0, false, {
                                    fileName: "src/components/IT20125202/user/UserProfile.js",
                                    lineNumber: 346,
                                    columnNumber: 25
                                }, this),
                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("br", {}, void 0, false, {
                                    fileName: "src/components/IT20125202/user/UserProfile.js",
                                    lineNumber: 346,
                                    columnNumber: 31
                                }, this),
                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("h5", {
                                    children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("b", {
                                        children: "Change Password"
                                    }, void 0, false, {
                                        fileName: "src/components/IT20125202/user/UserProfile.js",
                                        lineNumber: 347,
                                        columnNumber: 29
                                    }, this)
                                }, void 0, false, {
                                    fileName: "src/components/IT20125202/user/UserProfile.js",
                                    lineNumber: 347,
                                    columnNumber: 25
                                }, this),
                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                    className: "form-group",
                                    style: {
                                        marginBottom: '15px'
                                    },
                                    children: [
                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("label", {
                                            style: {
                                                marginBottom: '5px'
                                            },
                                            children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("b", {
                                                children: "Existing password"
                                            }, void 0, false, {
                                                fileName: "src/components/IT20125202/user/UserProfile.js",
                                                lineNumber: 349,
                                                columnNumber: 68
                                            }, this)
                                        }, void 0, false, {
                                            fileName: "src/components/IT20125202/user/UserProfile.js",
                                            lineNumber: 349,
                                            columnNumber: 29
                                        }, this),
                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("input", {
                                            type: "password",
                                            className: "form-control",
                                            name: "enteredPassword",
                                            value: this.state.enteredPassword,
                                            placeholder: "Fill only if you need to change the password",
                                            onChange: this.handleInputChange
                                        }, void 0, false, {
                                            fileName: "src/components/IT20125202/user/UserProfile.js",
                                            lineNumber: 350,
                                            columnNumber: 29
                                        }, this)
                                    ]
                                }, void 0, true, {
                                    fileName: "src/components/IT20125202/user/UserProfile.js",
                                    lineNumber: 348,
                                    columnNumber: 25
                                }, this),
                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                    className: "form-group",
                                    style: {
                                        marginBottom: '15px'
                                    },
                                    children: [
                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("label", {
                                            style: {
                                                marginBottom: '5px'
                                            },
                                            children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("b", {
                                                children: "New password"
                                            }, void 0, false, {
                                                fileName: "src/components/IT20125202/user/UserProfile.js",
                                                lineNumber: 361,
                                                columnNumber: 68
                                            }, this)
                                        }, void 0, false, {
                                            fileName: "src/components/IT20125202/user/UserProfile.js",
                                            lineNumber: 361,
                                            columnNumber: 29
                                        }, this),
                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("input", {
                                            type: "password",
                                            className: "form-control",
                                            name: "newPassword",
                                            value: this.state.newPassword,
                                            placeholder: "Fill only if you need to change the password",
                                            onChange: this.handleInputChange
                                        }, void 0, false, {
                                            fileName: "src/components/IT20125202/user/UserProfile.js",
                                            lineNumber: 362,
                                            columnNumber: 29
                                        }, this)
                                    ]
                                }, void 0, true, {
                                    fileName: "src/components/IT20125202/user/UserProfile.js",
                                    lineNumber: 360,
                                    columnNumber: 25
                                }, this),
                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                    className: "form-group",
                                    style: {
                                        marginBottom: '15px'
                                    },
                                    children: [
                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("label", {
                                            style: {
                                                marginBottom: '5px'
                                            },
                                            children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("b", {
                                                children: "Confirm New password"
                                            }, void 0, false, {
                                                fileName: "src/components/IT20125202/user/UserProfile.js",
                                                lineNumber: 373,
                                                columnNumber: 68
                                            }, this)
                                        }, void 0, false, {
                                            fileName: "src/components/IT20125202/user/UserProfile.js",
                                            lineNumber: 373,
                                            columnNumber: 29
                                        }, this),
                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("input", {
                                            type: "password",
                                            className: "form-control",
                                            name: "confirmNewPassword",
                                            value: this.state.confirmNewPassword,
                                            placeholder: "Fill only if you need to change the password",
                                            onChange: this.handleInputChange
                                        }, void 0, false, {
                                            fileName: "src/components/IT20125202/user/UserProfile.js",
                                            lineNumber: 374,
                                            columnNumber: 29
                                        }, this)
                                    ]
                                }, void 0, true, {
                                    fileName: "src/components/IT20125202/user/UserProfile.js",
                                    lineNumber: 372,
                                    columnNumber: 25
                                }, this),
                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("button", {
                                    className: "btn btn-success",
                                    type: "submit",
                                    style: {
                                        maeginTop: '15px'
                                    },
                                    onClick: this.onSubmit,
                                    children: [
                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("i", {
                                            className: "far fa-check-square"
                                        }, void 0, false, {
                                            fileName: "src/components/IT20125202/user/UserProfile.js",
                                            lineNumber: 386,
                                            columnNumber: 29
                                        }, this),
                                        "\xa0 ",
                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("b", {
                                            children: "Update"
                                        }, void 0, false, {
                                            fileName: "src/components/IT20125202/user/UserProfile.js",
                                            lineNumber: 387,
                                            columnNumber: 36
                                        }, this)
                                    ]
                                }, void 0, true, {
                                    fileName: "src/components/IT20125202/user/UserProfile.js",
                                    lineNumber: 385,
                                    columnNumber: 25
                                }, this)
                            ]
                        }, void 0, true, {
                            fileName: "src/components/IT20125202/user/UserProfile.js",
                            lineNumber: 237,
                            columnNumber: 21
                        }, this)
                    ]
                }, void 0, true, {
                    fileName: "src/components/IT20125202/user/UserProfile.js",
                    lineNumber: 234,
                    columnNumber: 17
                }, this)
            ]
        }, void 0, true, {
            fileName: "src/components/IT20125202/user/UserProfile.js",
            lineNumber: 229,
            columnNumber: 13
        }, this);
    }
}
exports.default = UserProfile;

  $parcel$ReactRefreshHelpers$a4ba.postlude(module);
} finally {
  window.$RefreshReg$ = prevRefreshReg;
  window.$RefreshSig$ = prevRefreshSig;
}
},{"react/jsx-dev-runtime":"iTorj","react":"21dqq","jwt-decode":"ljM1w","axios":"jo6P5","sweetalert":"38a65","@parcel/transformer-js/src/esmodule-helpers.js":"gkKU3","@parcel/transformer-react-refresh-wrap/lib/helpers/helpers.js":"km3Ru"}],"ljM1w":[function(require,module,exports) {
var parcelHelpers = require("@parcel/transformer-js/src/esmodule-helpers.js");
parcelHelpers.defineInteropFlag(exports);
parcelHelpers.export(exports, "InvalidTokenError", ()=>n
);
function e(e1) {
    this.message = e1;
}
e.prototype = new Error, e.prototype.name = "InvalidCharacterError";
var r = "undefined" != typeof window && window.atob && window.atob.bind(window) || function(r1) {
    var t1 = String(r1).replace(/=+$/, "");
    if (t1.length % 4 == 1) throw new e("'atob' failed: The string to be decoded is not correctly encoded.");
    for(var n1, o1, a = 0, i = 0, c = ""; o1 = t1.charAt(i++); ~o1 && (n1 = a % 4 ? 64 * n1 + o1 : o1, (a++) % 4) && (c += String.fromCharCode(255 & n1 >> (-2 * a & 6))))o1 = "ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789+/=".indexOf(o1);
    return c;
};
function t(e2) {
    var t2 = e2.replace(/-/g, "+").replace(/_/g, "/");
    switch(t2.length % 4){
        case 0:
            break;
        case 2:
            t2 += "==";
            break;
        case 3:
            t2 += "=";
            break;
        default:
            throw "Illegal base64url string!";
    }
    try {
        return function(e3) {
            return decodeURIComponent(r(e3).replace(/(.)/g, function(e, r2) {
                var t3 = r2.charCodeAt(0).toString(16).toUpperCase();
                return t3.length < 2 && (t3 = "0" + t3), "%" + t3;
            }));
        }(t2);
    } catch (e) {
        return r(t2);
    }
}
function n(e4) {
    this.message = e4;
}
function o(e5, r3) {
    if ("string" != typeof e5) throw new n("Invalid token specified");
    var o2 = !0 === (r3 = r3 || {}).header ? 0 : 1;
    try {
        return JSON.parse(t(e5.split(".")[o2]));
    } catch (e6) {
        throw new n("Invalid token specified: " + e6.message);
    }
}
n.prototype = new Error, n.prototype.name = "InvalidTokenError";
exports.default = o;

},{"@parcel/transformer-js/src/esmodule-helpers.js":"gkKU3"}],"bFcqZ":[function(require,module,exports) {
var $parcel$ReactRefreshHelpers$e714 = require("@parcel/transformer-react-refresh-wrap/lib/helpers/helpers.js");
var prevRefreshReg = window.$RefreshReg$;
var prevRefreshSig = window.$RefreshSig$;
$parcel$ReactRefreshHelpers$e714.prelude(module);

try {
var parcelHelpers = require("@parcel/transformer-js/src/esmodule-helpers.js");
parcelHelpers.defineInteropFlag(exports);
var _jsxDevRuntime = require("react/jsx-dev-runtime");
var _react = require("react");
var _reactDefault = parcelHelpers.interopDefault(_react);
var _homeImageJpg = require("../../../public/images/HomeImage.jpg");
var _homeImageJpgDefault = parcelHelpers.interopDefault(_homeImageJpg);
var _rightSidePanel = require("./RightSidePanel");
var _rightSidePanelDefault = parcelHelpers.interopDefault(_rightSidePanel);
var _jwtDecode = require("jwt-decode");
var _jwtDecodeDefault = parcelHelpers.interopDefault(_jwtDecode);
var _sliitresearchLogoPng = require("../../../public/images/SLIITResearchLogo.png");
var _sliitresearchLogoPngDefault = parcelHelpers.interopDefault(_sliitresearchLogoPng);
class Home extends _react.Component {
    constructor(){
        super();
        this.state = {
            userType: ""
        };
    }
    componentDidMount() {
        document.title = "Home";
        // Check user login
        if (!localStorage.userToken) swal({
            title: "Please login first",
            icon: "warning"
        }).then((value)=>{
            if (value) {
                this.props.history.push("/user/login");
                window.location.reload();
            }
        });
        else {
            const usertoken = localStorage.userToken;
            const decoded = _jwtDecodeDefault.default(usertoken);
            this.setState({
                userType: decoded.type
            });
        }
    }
    render() {
        return /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
            className: "container",
            children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                children: [
                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("br", {}, void 0, false, {
                        fileName: "src/components/IT20127046/Home.js",
                        lineNumber: 42,
                        columnNumber: 11
                    }, this),
                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                        className: "container",
                        children: [
                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("h4", {
                                children: [
                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("i", {
                                        class: "fa fa-home",
                                        "aria-hidden": "true"
                                    }, void 0, false, {
                                        fileName: "src/components/IT20127046/Home.js",
                                        lineNumber: 44,
                                        columnNumber: 17
                                    }, this),
                                    "\xa0\xa0Home"
                                ]
                            }, void 0, true, {
                                fileName: "src/components/IT20127046/Home.js",
                                lineNumber: 44,
                                columnNumber: 13
                            }, this),
                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("hr", {}, void 0, false, {
                                fileName: "src/components/IT20127046/Home.js",
                                lineNumber: 45,
                                columnNumber: 13
                            }, this)
                        ]
                    }, void 0, true, {
                        fileName: "src/components/IT20127046/Home.js",
                        lineNumber: 43,
                        columnNumber: 11
                    }, this),
                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                        className: "container",
                        children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                            className: "row",
                            children: [
                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                    className: "col-9",
                                    children: [
                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                            children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("img", {
                                                src: _homeImageJpgDefault.default,
                                                style: {
                                                    width: "100%",
                                                    height: "auto",
                                                    marginBottom: "20px"
                                                }
                                            }, void 0, false, {
                                                fileName: "src/components/IT20127046/Home.js",
                                                lineNumber: 51,
                                                columnNumber: 19
                                            }, this)
                                        }, void 0, false, {
                                            fileName: "src/components/IT20127046/Home.js",
                                            lineNumber: 50,
                                            columnNumber: 17
                                        }, this),
                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                            children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                                className: "p-3 mb-2 bg-light text-dark",
                                                children: [
                                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("p", {
                                                        className: "h6",
                                                        children: [
                                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("i", {
                                                                class: "fa fa-envelope-o",
                                                                "aria-hidden": "true"
                                                            }, void 0, false, {
                                                                fileName: "src/components/IT20127046/Home.js",
                                                                lineNumber: 63,
                                                                columnNumber: 23
                                                            }, this),
                                                            "\xa0\xa0Notice"
                                                        ]
                                                    }, void 0, true, {
                                                        fileName: "src/components/IT20127046/Home.js",
                                                        lineNumber: 62,
                                                        columnNumber: 21
                                                    }, this),
                                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("hr", {}, void 0, false, {
                                                        fileName: "src/components/IT20127046/Home.js",
                                                        lineNumber: 66,
                                                        columnNumber: 21
                                                    }, this)
                                                ]
                                            }, void 0, true, {
                                                fileName: "src/components/IT20127046/Home.js",
                                                lineNumber: 61,
                                                columnNumber: 19
                                            }, this)
                                        }, void 0, false, {
                                            fileName: "src/components/IT20127046/Home.js",
                                            lineNumber: 60,
                                            columnNumber: 17
                                        }, this)
                                    ]
                                }, void 0, true, {
                                    fileName: "src/components/IT20127046/Home.js",
                                    lineNumber: 49,
                                    columnNumber: 15
                                }, this),
                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                    className: "col-3",
                                    children: [
                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                            className: "container",
                                            children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("center", {
                                                children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("img", {
                                                    src: _sliitresearchLogoPngDefault.default,
                                                    style: {
                                                        width: "100px",
                                                        height: "100px"
                                                    }
                                                }, void 0, false, {
                                                    fileName: "src/components/IT20127046/Home.js",
                                                    lineNumber: 74,
                                                    columnNumber: 21
                                                }, this)
                                            }, void 0, false, {
                                                fileName: "src/components/IT20127046/Home.js",
                                                lineNumber: 73,
                                                columnNumber: 17
                                            }, this)
                                        }, void 0, false, {
                                            fileName: "src/components/IT20127046/Home.js",
                                            lineNumber: 72,
                                            columnNumber: 17
                                        }, this),
                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV(_rightSidePanelDefault.default, {}, void 0, false, {
                                            fileName: "src/components/IT20127046/Home.js",
                                            lineNumber: 80,
                                            columnNumber: 17
                                        }, this)
                                    ]
                                }, void 0, true, {
                                    fileName: "src/components/IT20127046/Home.js",
                                    lineNumber: 71,
                                    columnNumber: 15
                                }, this)
                            ]
                        }, void 0, true, {
                            fileName: "src/components/IT20127046/Home.js",
                            lineNumber: 48,
                            columnNumber: 13
                        }, this)
                    }, void 0, false, {
                        fileName: "src/components/IT20127046/Home.js",
                        lineNumber: 47,
                        columnNumber: 11
                    }, this)
                ]
            }, void 0, true, {
                fileName: "src/components/IT20127046/Home.js",
                lineNumber: 41,
                columnNumber: 9
            }, this)
        }, void 0, false, {
            fileName: "src/components/IT20127046/Home.js",
            lineNumber: 40,
            columnNumber: 7
        }, this);
    }
}
exports.default = Home;

  $parcel$ReactRefreshHelpers$e714.postlude(module);
} finally {
  window.$RefreshReg$ = prevRefreshReg;
  window.$RefreshSig$ = prevRefreshSig;
}
},{"react/jsx-dev-runtime":"iTorj","react":"21dqq","../../../public/images/HomeImage.jpg":"lBOrd","./RightSidePanel":"kymky","jwt-decode":"ljM1w","../../../public/images/SLIITResearchLogo.png":"2vpUV","@parcel/transformer-js/src/esmodule-helpers.js":"gkKU3","@parcel/transformer-react-refresh-wrap/lib/helpers/helpers.js":"km3Ru"}],"lBOrd":[function(require,module,exports) {
module.exports = require('./helpers/bundle-url').getBundleURL('bLxZJ') + "HomeImage.697f74dd.jpg" + "?" + Date.now();

},{"./helpers/bundle-url":"lgJ39"}],"lgJ39":[function(require,module,exports) {
"use strict";
var bundleURL = {};
function getBundleURLCached(id) {
    var value = bundleURL[id];
    if (!value) {
        value = getBundleURL();
        bundleURL[id] = value;
    }
    return value;
}
function getBundleURL() {
    try {
        throw new Error();
    } catch (err) {
        var matches = ('' + err.stack).match(/(https?|file|ftp|(chrome|moz)-extension):\/\/[^)\n]+/g);
        if (matches) // The first two stack frames will be this function and getBundleURLCached.
        // Use the 3rd one, which will be a runtime in the original bundle.
        return getBaseURL(matches[2]);
    }
    return '/';
}
function getBaseURL(url) {
    return ('' + url).replace(/^((?:https?|file|ftp|(chrome|moz)-extension):\/\/.+)\/[^/]+$/, '$1') + '/';
} // TODO: Replace uses with `new URL(url).origin` when ie11 is no longer supported.
function getOrigin(url) {
    var matches = ('' + url).match(/(https?|file|ftp|(chrome|moz)-extension):\/\/[^/]+/);
    if (!matches) throw new Error('Origin not found');
    return matches[0];
}
exports.getBundleURL = getBundleURLCached;
exports.getBaseURL = getBaseURL;
exports.getOrigin = getOrigin;

},{}],"kymky":[function(require,module,exports) {
var $parcel$ReactRefreshHelpers$e617 = require("@parcel/transformer-react-refresh-wrap/lib/helpers/helpers.js");
var prevRefreshReg = window.$RefreshReg$;
var prevRefreshSig = window.$RefreshSig$;
$parcel$ReactRefreshHelpers$e617.prelude(module);

try {
var parcelHelpers = require("@parcel/transformer-js/src/esmodule-helpers.js");
parcelHelpers.defineInteropFlag(exports);
var _jsxDevRuntime = require("react/jsx-dev-runtime");
var _react = require("react");
var _reactDefault = parcelHelpers.interopDefault(_react);
var _jwtDecode = require("jwt-decode");
var _jwtDecodeDefault = parcelHelpers.interopDefault(_jwtDecode);
class RightSidePanel extends _react.Component {
    constructor(){
        super();
        this.state = {
            userType: ""
        };
    }
    componentDidMount() {
        if (localStorage.userToken) {
            const usertoken = localStorage.userToken;
            const decoded = _jwtDecodeDefault.default(usertoken);
            this.setState({
                userType: decoded.type
            });
        }
    }
    render() {
        let markingSchemeLink;
        let documentTemplateLink;
        if (this.state.userType === "Student") {
            markingSchemeLink = "/user/view/marking";
            documentTemplateLink = "/user/view/documentTemplate";
        } else {
            this.state.userType === "Supervisor" || "Panel Member";
            markingSchemeLink = "/user/view/marking";
            documentTemplateLink = "/user/view/documentTemplate";
        }
        return /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
            children: [
                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                    className: "p-3 mb-2 bg-light text-dark",
                    children: [
                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("p", {
                            className: "h6",
                            children: [
                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("i", {
                                    class: "fa fa-external-link",
                                    "aria-hidden": "true"
                                }, void 0, false, {
                                    fileName: "src/components/IT20127046/RightSidePanel.js",
                                    lineNumber: 38,
                                    columnNumber: 29
                                }, this),
                                "\xa0\xa0Quick Links"
                            ]
                        }, void 0, true, {
                            fileName: "src/components/IT20127046/RightSidePanel.js",
                            lineNumber: 38,
                            columnNumber: 11
                        }, this),
                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("hr", {}, void 0, false, {
                            fileName: "src/components/IT20127046/RightSidePanel.js",
                            lineNumber: 39,
                            columnNumber: 11
                        }, this),
                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                            children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("a", {
                                href: "",
                                children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                    className: "p-2 mb-2 text-white",
                                    style: {
                                        background: '#212F3C',
                                        textDecoration: 'none'
                                    },
                                    children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("a", {
                                        className: "btn text-white",
                                        children: [
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("i", {
                                                class: "fa fa-user",
                                                "aria-hidden": "true"
                                            }, void 0, false, {
                                                fileName: "src/components/IT20127046/RightSidePanel.js",
                                                lineNumber: 45,
                                                columnNumber: 19
                                            }, this),
                                            "\xa0\xa0Users"
                                        ]
                                    }, void 0, true, {
                                        fileName: "src/components/IT20127046/RightSidePanel.js",
                                        lineNumber: 44,
                                        columnNumber: 17
                                    }, this)
                                }, void 0, false, {
                                    fileName: "src/components/IT20127046/RightSidePanel.js",
                                    lineNumber: 43,
                                    columnNumber: 15
                                }, this)
                            }, void 0, false, {
                                fileName: "src/components/IT20127046/RightSidePanel.js",
                                lineNumber: 42,
                                columnNumber: 13
                            }, this)
                        }, void 0, false, {
                            fileName: "src/components/IT20127046/RightSidePanel.js",
                            lineNumber: 41,
                            columnNumber: 11
                        }, this),
                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                            children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("a", {
                                href: "/student/group/view",
                                children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                    className: "p-2 mb-2 text-white",
                                    style: {
                                        background: '#212F3C',
                                        textDecoration: 'none'
                                    },
                                    children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("a", {
                                        className: "btn text-white",
                                        children: [
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("i", {
                                                class: "fa fa-users",
                                                "aria-hidden": "true"
                                            }, void 0, false, {
                                                fileName: "src/components/IT20127046/RightSidePanel.js",
                                                lineNumber: 56,
                                                columnNumber: 19
                                            }, this),
                                            "\xa0\xa0Student Groups"
                                        ]
                                    }, void 0, true, {
                                        fileName: "src/components/IT20127046/RightSidePanel.js",
                                        lineNumber: 55,
                                        columnNumber: 17
                                    }, this)
                                }, void 0, false, {
                                    fileName: "src/components/IT20127046/RightSidePanel.js",
                                    lineNumber: 54,
                                    columnNumber: 15
                                }, this)
                            }, void 0, false, {
                                fileName: "src/components/IT20127046/RightSidePanel.js",
                                lineNumber: 53,
                                columnNumber: 13
                            }, this)
                        }, void 0, false, {
                            fileName: "src/components/IT20127046/RightSidePanel.js",
                            lineNumber: 52,
                            columnNumber: 11
                        }, this),
                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                            children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("a", {
                                href: "",
                                children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                    className: "p-2 mb-2 text-white",
                                    style: {
                                        background: '#212F3C',
                                        textDecoration: 'none'
                                    },
                                    children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("a", {
                                        className: "btn text-white",
                                        children: [
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("i", {
                                                class: "fa fa-book",
                                                "aria-hidden": "true"
                                            }, void 0, false, {
                                                fileName: "src/components/IT20127046/RightSidePanel.js",
                                                lineNumber: 66,
                                                columnNumber: 19
                                            }, this),
                                            "\xa0\xa0Topics"
                                        ]
                                    }, void 0, true, {
                                        fileName: "src/components/IT20127046/RightSidePanel.js",
                                        lineNumber: 65,
                                        columnNumber: 17
                                    }, this)
                                }, void 0, false, {
                                    fileName: "src/components/IT20127046/RightSidePanel.js",
                                    lineNumber: 64,
                                    columnNumber: 15
                                }, this)
                            }, void 0, false, {
                                fileName: "src/components/IT20127046/RightSidePanel.js",
                                lineNumber: 63,
                                columnNumber: 13
                            }, this)
                        }, void 0, false, {
                            fileName: "src/components/IT20127046/RightSidePanel.js",
                            lineNumber: 62,
                            columnNumber: 11
                        }, this),
                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                            children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("a", {
                                href: "",
                                children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                    className: "p-2 mb-2 text-white",
                                    style: {
                                        background: '#212F3C',
                                        textDecoration: 'none'
                                    },
                                    children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("a", {
                                        className: "btn text-white",
                                        children: [
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("i", {
                                                class: "fa fa-file-text",
                                                "aria-hidden": "true"
                                            }, void 0, false, {
                                                fileName: "src/components/IT20127046/RightSidePanel.js",
                                                lineNumber: 76,
                                                columnNumber: 19
                                            }, this),
                                            "\xa0\xa0Submissions"
                                        ]
                                    }, void 0, true, {
                                        fileName: "src/components/IT20127046/RightSidePanel.js",
                                        lineNumber: 75,
                                        columnNumber: 17
                                    }, this)
                                }, void 0, false, {
                                    fileName: "src/components/IT20127046/RightSidePanel.js",
                                    lineNumber: 74,
                                    columnNumber: 15
                                }, this)
                            }, void 0, false, {
                                fileName: "src/components/IT20127046/RightSidePanel.js",
                                lineNumber: 73,
                                columnNumber: 13
                            }, this)
                        }, void 0, false, {
                            fileName: "src/components/IT20127046/RightSidePanel.js",
                            lineNumber: 72,
                            columnNumber: 11
                        }, this),
                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                            children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("a", {
                                href: documentTemplateLink,
                                children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                    className: "p-2 mb-2 text-white",
                                    style: {
                                        background: '#212F3C',
                                        textDecoration: 'none'
                                    },
                                    children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("a", {
                                        className: "btn text-white",
                                        children: [
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("i", {
                                                class: "fa fa-file-text",
                                                "aria-hidden": "true"
                                            }, void 0, false, {
                                                fileName: "src/components/IT20127046/RightSidePanel.js",
                                                lineNumber: 86,
                                                columnNumber: 19
                                            }, this),
                                            "\xa0\xa0Document Template"
                                        ]
                                    }, void 0, true, {
                                        fileName: "src/components/IT20127046/RightSidePanel.js",
                                        lineNumber: 85,
                                        columnNumber: 17
                                    }, this)
                                }, void 0, false, {
                                    fileName: "src/components/IT20127046/RightSidePanel.js",
                                    lineNumber: 84,
                                    columnNumber: 15
                                }, this)
                            }, void 0, false, {
                                fileName: "src/components/IT20127046/RightSidePanel.js",
                                lineNumber: 83,
                                columnNumber: 13
                            }, this)
                        }, void 0, false, {
                            fileName: "src/components/IT20127046/RightSidePanel.js",
                            lineNumber: 82,
                            columnNumber: 11
                        }, this),
                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                            children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("a", {
                                href: markingSchemeLink,
                                children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                    className: "p-2 mb-2 text-white",
                                    style: {
                                        background: '#212F3C',
                                        textDecoration: 'none'
                                    },
                                    children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("a", {
                                        className: "btn text-white",
                                        children: [
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("i", {
                                                class: "fa fa-table",
                                                "aria-hidden": "true"
                                            }, void 0, false, {
                                                fileName: "src/components/IT20127046/RightSidePanel.js",
                                                lineNumber: 96,
                                                columnNumber: 19
                                            }, this),
                                            "\xa0\xa0Marking Schemes"
                                        ]
                                    }, void 0, true, {
                                        fileName: "src/components/IT20127046/RightSidePanel.js",
                                        lineNumber: 95,
                                        columnNumber: 17
                                    }, this)
                                }, void 0, false, {
                                    fileName: "src/components/IT20127046/RightSidePanel.js",
                                    lineNumber: 94,
                                    columnNumber: 15
                                }, this)
                            }, void 0, false, {
                                fileName: "src/components/IT20127046/RightSidePanel.js",
                                lineNumber: 93,
                                columnNumber: 13
                            }, this)
                        }, void 0, false, {
                            fileName: "src/components/IT20127046/RightSidePanel.js",
                            lineNumber: 92,
                            columnNumber: 11
                        }, this)
                    ]
                }, void 0, true, {
                    fileName: "src/components/IT20127046/RightSidePanel.js",
                    lineNumber: 37,
                    columnNumber: 9
                }, this),
                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                    className: "p-3 mb-2 bg-light text-dark",
                    children: [
                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("p", {
                            className: "h6",
                            children: [
                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("i", {
                                    class: "fa fa-comments",
                                    "aria-hidden": "true"
                                }, void 0, false, {
                                    fileName: "src/components/IT20127046/RightSidePanel.js",
                                    lineNumber: 105,
                                    columnNumber: 29
                                }, this),
                                "\xa0\xa0Group Chat"
                            ]
                        }, void 0, true, {
                            fileName: "src/components/IT20127046/RightSidePanel.js",
                            lineNumber: 105,
                            columnNumber: 11
                        }, this),
                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("hr", {}, void 0, false, {
                            fileName: "src/components/IT20127046/RightSidePanel.js",
                            lineNumber: 106,
                            columnNumber: 11
                        }, this),
                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                            className: "p-2 mb-2 bg-success text-white",
                            children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("a", {
                                className: "btn text-white",
                                href: "",
                                style: {
                                    textDecoration: 'none'
                                },
                                children: [
                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("i", {
                                        class: "fa fa-comments",
                                        "aria-hidden": "true"
                                    }, void 0, false, {
                                        fileName: "src/components/IT20127046/RightSidePanel.js",
                                        lineNumber: 109,
                                        columnNumber: 13
                                    }, this),
                                    "\xa0\xa0Join Group Chat"
                                ]
                            }, void 0, true, {
                                fileName: "src/components/IT20127046/RightSidePanel.js",
                                lineNumber: 108,
                                columnNumber: 13
                            }, this)
                        }, void 0, false, {
                            fileName: "src/components/IT20127046/RightSidePanel.js",
                            lineNumber: 107,
                            columnNumber: 11
                        }, this)
                    ]
                }, void 0, true, {
                    fileName: "src/components/IT20127046/RightSidePanel.js",
                    lineNumber: 104,
                    columnNumber: 9
                }, this)
            ]
        }, void 0, true, {
            fileName: "src/components/IT20127046/RightSidePanel.js",
            lineNumber: 36,
            columnNumber: 7
        }, this);
    }
}
exports.default = RightSidePanel;

  $parcel$ReactRefreshHelpers$e617.postlude(module);
} finally {
  window.$RefreshReg$ = prevRefreshReg;
  window.$RefreshSig$ = prevRefreshSig;
}
},{"react/jsx-dev-runtime":"iTorj","react":"21dqq","jwt-decode":"ljM1w","@parcel/transformer-js/src/esmodule-helpers.js":"gkKU3","@parcel/transformer-react-refresh-wrap/lib/helpers/helpers.js":"km3Ru"}],"2vpUV":[function(require,module,exports) {
module.exports = require('./helpers/bundle-url').getBundleURL('bLxZJ') + "SLIITResearchLogo.ebe5b115.png" + "?" + Date.now();

},{"./helpers/bundle-url":"lgJ39"}],"yp28y":[function(require,module,exports) {
var $parcel$ReactRefreshHelpers$a5ac = require("@parcel/transformer-react-refresh-wrap/lib/helpers/helpers.js");
var prevRefreshReg = window.$RefreshReg$;
var prevRefreshSig = window.$RefreshSig$;
$parcel$ReactRefreshHelpers$a5ac.prelude(module);

try {
var parcelHelpers = require("@parcel/transformer-js/src/esmodule-helpers.js");
parcelHelpers.defineInteropFlag(exports);
var _jsxDevRuntime = require("react/jsx-dev-runtime");
var _react = require("react");
var _reactDefault = parcelHelpers.interopDefault(_react);
var _axios = require("axios");
var _axiosDefault = parcelHelpers.interopDefault(_axios);
var _sweetalert = require("sweetalert");
var _sweetalertDefault = parcelHelpers.interopDefault(_sweetalert);
class UserLogin extends _react.Component {
    componentDidMount() {
        localStorage.removeItem('userToken');
        document.title = "User Login";
    }
    constructor(){
        super();
        this.state = {
            idNumber: '',
            password: ''
        };
        //to handle the state changes
        this.onChange = this.onChange.bind(this);
        this.onSubmit = this.onSubmit.bind(this);
    }
    onChange(e) {
        this.setState({
            [e.target.name]: e.target.value
        });
    }
    onSubmit(e) {
        e.preventDefault();
        const user = {
            idNumber: this.state.idNumber,
            password: this.state.password
        };
        // validations----------------------------------------------------
        let validated = true;
        if (user.idNumber === '') {
            validated = false;
            _sweetalertDefault.default({
                title: "",
                text: "Please enter your ID number",
                icon: "warning"
            });
        } else if (user.password === '') {
            validated = false;
            _sweetalertDefault.default({
                title: "",
                text: "Please enter your password",
                icon: "warning"
            });
        }
        if (validated) _axiosDefault.default.post('http://localhost:5000/user/login', {
            idNumber: user.idNumber,
            password: user.password
        }).then((res)=>{
            _sweetalertDefault.default("Login successful!", "", "success").then((value)=>{
                if (value) {
                    localStorage.setItem('userToken', res.data);
                    this.props.history.push(`/`);
                    window.location.reload();
                }
            });
        }).catch((err)=>{
            console.log(err);
            _sweetalertDefault.default({
                title: "",
                text: "Please check your ID number and password",
                icon: "warning"
            });
        });
    }
    render() {
        return /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
            children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                className: "container",
                style: {
                    marginTop: '10px',
                    marginBottom: '10px',
                    backgroundColor: 'white',
                    paddingBottom: '100px',
                    paddingTop: '50px',
                    paddingLeft: '100px',
                    paddingRight: '100px',
                    height: '800px'
                },
                children: [
                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("h1", {
                        style: {
                            textAlign: 'center',
                            paddingBottom: '10px'
                        },
                        children: "Research Project Management System"
                    }, void 0, false, {
                        fileName: "src/components/IT20125202/user/UserLogin.js",
                        lineNumber: 85,
                        columnNumber: 21
                    }, this),
                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("hr", {}, void 0, false, {
                        fileName: "src/components/IT20125202/user/UserLogin.js",
                        lineNumber: 86,
                        columnNumber: 21
                    }, this),
                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                        className: "col-md-8 mt-4 mx-auto",
                        children: [
                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("h3", {
                                className: "h3 mb-3 font-weight-normal",
                                style: {
                                    textAlign: 'center'
                                },
                                children: "User Login"
                            }, void 0, false, {
                                fileName: "src/components/IT20125202/user/UserLogin.js",
                                lineNumber: 89,
                                columnNumber: 25
                            }, this),
                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("br", {}, void 0, false, {
                                fileName: "src/components/IT20125202/user/UserLogin.js",
                                lineNumber: 90,
                                columnNumber: 25
                            }, this),
                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("form", {
                                className: "needs-validation",
                                noValidate: true,
                                children: [
                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                        className: "form-group",
                                        style: {
                                            marginBottom: '15px'
                                        },
                                        children: [
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("label", {
                                                style: {
                                                    marginBottom: '5px'
                                                },
                                                children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("b", {
                                                    children: "ID Number"
                                                }, void 0, false, {
                                                    fileName: "src/components/IT20125202/user/UserLogin.js",
                                                    lineNumber: 93,
                                                    columnNumber: 72
                                                }, this)
                                            }, void 0, false, {
                                                fileName: "src/components/IT20125202/user/UserLogin.js",
                                                lineNumber: 93,
                                                columnNumber: 33
                                            }, this),
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("input", {
                                                type: "text",
                                                className: "form-control",
                                                name: "idNumber",
                                                placeholder: "Please enter your registration number",
                                                required: true,
                                                value: this.state.idNumber,
                                                onChange: this.onChange
                                            }, void 0, false, {
                                                fileName: "src/components/IT20125202/user/UserLogin.js",
                                                lineNumber: 94,
                                                columnNumber: 33
                                            }, this)
                                        ]
                                    }, void 0, true, {
                                        fileName: "src/components/IT20125202/user/UserLogin.js",
                                        lineNumber: 92,
                                        columnNumber: 29
                                    }, this),
                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                        className: "form-group",
                                        style: {
                                            marginBottom: '15px'
                                        },
                                        children: [
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("label", {
                                                style: {
                                                    marginBottom: '5px'
                                                },
                                                children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("b", {
                                                    children: "Password"
                                                }, void 0, false, {
                                                    fileName: "src/components/IT20125202/user/UserLogin.js",
                                                    lineNumber: 106,
                                                    columnNumber: 72
                                                }, this)
                                            }, void 0, false, {
                                                fileName: "src/components/IT20125202/user/UserLogin.js",
                                                lineNumber: 106,
                                                columnNumber: 33
                                            }, this),
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("input", {
                                                type: "password",
                                                className: "form-control",
                                                name: "password",
                                                placeholder: "Please enter the password",
                                                required: true,
                                                value: this.state.password,
                                                onChange: this.onChange
                                            }, void 0, false, {
                                                fileName: "src/components/IT20125202/user/UserLogin.js",
                                                lineNumber: 107,
                                                columnNumber: 33
                                            }, this)
                                        ]
                                    }, void 0, true, {
                                        fileName: "src/components/IT20125202/user/UserLogin.js",
                                        lineNumber: 105,
                                        columnNumber: 29
                                    }, this),
                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                        className: "d-grid gap-2",
                                        children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("button", {
                                            className: "btn btn-outline-dark",
                                            type: "submit",
                                            style: {
                                                marginTop: '15px',
                                                width: 'cover'
                                            },
                                            onClick: this.onSubmit,
                                            children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("b", {
                                                children: "Login"
                                            }, void 0, false, {
                                                fileName: "src/components/IT20125202/user/UserLogin.js",
                                                lineNumber: 126,
                                                columnNumber: 34
                                            }, this)
                                        }, void 0, false, {
                                            fileName: "src/components/IT20125202/user/UserLogin.js",
                                            lineNumber: 121,
                                            columnNumber: 33
                                        }, this)
                                    }, void 0, false, {
                                        fileName: "src/components/IT20125202/user/UserLogin.js",
                                        lineNumber: 120,
                                        columnNumber: 29
                                    }, this)
                                ]
                            }, void 0, true, {
                                fileName: "src/components/IT20125202/user/UserLogin.js",
                                lineNumber: 91,
                                columnNumber: 25
                            }, this)
                        ]
                    }, void 0, true, {
                        fileName: "src/components/IT20125202/user/UserLogin.js",
                        lineNumber: 87,
                        columnNumber: 21
                    }, this)
                ]
            }, void 0, true, {
                fileName: "src/components/IT20125202/user/UserLogin.js",
                lineNumber: 84,
                columnNumber: 17
            }, this)
        }, void 0, false, {
            fileName: "src/components/IT20125202/user/UserLogin.js",
            lineNumber: 83,
            columnNumber: 13
        }, this);
    }
}
exports.default = UserLogin;

  $parcel$ReactRefreshHelpers$a5ac.postlude(module);
} finally {
  window.$RefreshReg$ = prevRefreshReg;
  window.$RefreshSig$ = prevRefreshSig;
}
},{"react/jsx-dev-runtime":"iTorj","react":"21dqq","axios":"jo6P5","sweetalert":"38a65","@parcel/transformer-js/src/esmodule-helpers.js":"gkKU3","@parcel/transformer-react-refresh-wrap/lib/helpers/helpers.js":"km3Ru"}],"54lqX":[function(require,module,exports) {
var $parcel$ReactRefreshHelpers$cdbd = require("@parcel/transformer-react-refresh-wrap/lib/helpers/helpers.js");
var prevRefreshReg = window.$RefreshReg$;
var prevRefreshSig = window.$RefreshSig$;
$parcel$ReactRefreshHelpers$cdbd.prelude(module);

try {
var parcelHelpers = require("@parcel/transformer-js/src/esmodule-helpers.js");
parcelHelpers.defineInteropFlag(exports);
var _jsxDevRuntime = require("react/jsx-dev-runtime");
var _react = require("react");
var _reactDefault = parcelHelpers.interopDefault(_react);
var _reactRouterDomMin = require("react-router-dom/cjs/react-router-dom.min");
var _jwtDecode = require("jwt-decode");
var _jwtDecodeDefault = parcelHelpers.interopDefault(_jwtDecode);
var _sweetalert = require("sweetalert");
var _sweetalertDefault = parcelHelpers.interopDefault(_sweetalert);
class NavBar extends _react.Component {
    constructor(){
        super();
        this.state = {
            type: ''
        };
    }
    componentDidMount() {
        document.title = "NavBar";
        if (localStorage.userToken) {
            const usertoken = localStorage.userToken;
            const decoded = _jwtDecodeDefault.default(usertoken);
            this.setState({
                // type: decoded.type.toString(),
                type: decoded.type
            });
        }
    }
    logOut(e) {
        e.preventDefault();
        _sweetalertDefault.default({
            title: "Are you sure you want to log out?",
            text: "",
            icon: "warning",
            buttons: true,
            dangerMode: true
        }).then((willLogout)=>{
            if (willLogout) _sweetalertDefault.default("Logout successfully!", "", "success").then((value)=>{
                if (value) {
                    localStorage.removeItem('userToken');
                    this.props.history.push(`/user/login`);
                    window.location.reload();
                }
            });
            else _sweetalertDefault.default("Redirecting...");
        });
    }
    render() {
        const loginRegLink = /*#__PURE__*/ _jsxDevRuntime.jsxDEV("ul", {
            className: "nav nav-tabs",
            children: [
                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("li", {
                    className: "nav-item",
                    children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("a", {
                        className: "nav-link",
                        "aria-current": "page",
                        href: "/user/login",
                        style: {
                            textDecoration: 'none',
                            color: 'white'
                        },
                        children: "Login"
                    }, void 0, false, {
                        fileName: "src/components/IT20125202/NavBar.js",
                        lineNumber: 60,
                        columnNumber: 11
                    }, this)
                }, void 0, false, {
                    fileName: "src/components/IT20125202/NavBar.js",
                    lineNumber: 59,
                    columnNumber: 9
                }, this),
                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("li", {
                    className: "nav-item",
                    children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("a", {
                        className: "nav-link",
                        "aria-current": "page",
                        href: "/user/registration",
                        style: {
                            textDecoration: 'none',
                            color: 'white'
                        },
                        children: "Register"
                    }, void 0, false, {
                        fileName: "src/components/IT20125202/NavBar.js",
                        lineNumber: 63,
                        columnNumber: 11
                    }, this)
                }, void 0, false, {
                    fileName: "src/components/IT20125202/NavBar.js",
                    lineNumber: 62,
                    columnNumber: 9
                }, this)
            ]
        }, void 0, true, {
            fileName: "src/components/IT20125202/NavBar.js",
            lineNumber: 58,
            columnNumber: 7
        }, this);
        let userLink;
        // NavBar Links for students
        if (this.state.type === 'Student') userLink = /*#__PURE__*/ _jsxDevRuntime.jsxDEV("ul", {
            className: "nav nav-tabs",
            children: [
                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("li", {
                    className: "nav-item",
                    children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("a", {
                        className: "nav-link",
                        "aria-current": "page",
                        href: "/home",
                        style: {
                            textDecoration: 'none',
                            color: 'white'
                        },
                        children: "Home"
                    }, void 0, false, {
                        fileName: "src/components/IT20125202/NavBar.js",
                        lineNumber: 75,
                        columnNumber: 13
                    }, this)
                }, void 0, false, {
                    fileName: "src/components/IT20125202/NavBar.js",
                    lineNumber: 74,
                    columnNumber: 11
                }, this),
                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("li", {
                    className: "nav-item",
                    children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("a", {
                        className: "nav-link",
                        "aria-current": "page",
                        href: "/student/topics",
                        style: {
                            textDecoration: 'none',
                            color: 'white'
                        },
                        children: "Topics"
                    }, void 0, false, {
                        fileName: "src/components/IT20125202/NavBar.js",
                        lineNumber: 78,
                        columnNumber: 13
                    }, this)
                }, void 0, false, {
                    fileName: "src/components/IT20125202/NavBar.js",
                    lineNumber: 77,
                    columnNumber: 11
                }, this),
                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("li", {
                    className: "nav-item",
                    children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("a", {
                        className: "nav-link",
                        "aria-current": "page",
                        href: "/user/profile",
                        style: {
                            textDecoration: 'none',
                            color: 'white'
                        },
                        children: "Profile"
                    }, void 0, false, {
                        fileName: "src/components/IT20125202/NavBar.js",
                        lineNumber: 81,
                        columnNumber: 13
                    }, this)
                }, void 0, false, {
                    fileName: "src/components/IT20125202/NavBar.js",
                    lineNumber: 80,
                    columnNumber: 11
                }, this),
                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("li", {
                    className: "nav-item dropdown",
                    children: [
                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("a", {
                            className: "nav-link dropdown-toggle",
                            "aria-current": "page",
                            href: "#",
                            style: {
                                textDecoration: 'none',
                                color: 'white'
                            },
                            id: "navbarDropdown",
                            role: "button",
                            "data-bs-toggle": "dropdown",
                            children: "Student Groups"
                        }, void 0, false, {
                            fileName: "src/components/IT20125202/NavBar.js",
                            lineNumber: 97,
                            columnNumber: 13
                        }, this),
                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("ul", {
                            className: "dropdown-menu",
                            "aria-labelledby": "navbarDropdown",
                            children: [
                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("li", {
                                    children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("a", {
                                        className: "dropdown-item",
                                        href: "/student/group/add",
                                        children: "Create Group"
                                    }, void 0, false, {
                                        fileName: "src/components/IT20125202/NavBar.js",
                                        lineNumber: 101,
                                        columnNumber: 21
                                    }, this)
                                }, void 0, false, {
                                    fileName: "src/components/IT20125202/NavBar.js",
                                    lineNumber: 101,
                                    columnNumber: 17
                                }, this),
                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("li", {
                                    children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("a", {
                                        className: "dropdown-item",
                                        href: "/student/group/view",
                                        children: "View Group"
                                    }, void 0, false, {
                                        fileName: "src/components/IT20125202/NavBar.js",
                                        lineNumber: 102,
                                        columnNumber: 21
                                    }, this)
                                }, void 0, false, {
                                    fileName: "src/components/IT20125202/NavBar.js",
                                    lineNumber: 102,
                                    columnNumber: 17
                                }, this)
                            ]
                        }, void 0, true, {
                            fileName: "src/components/IT20125202/NavBar.js",
                            lineNumber: 100,
                            columnNumber: 13
                        }, this)
                    ]
                }, void 0, true, {
                    fileName: "src/components/IT20125202/NavBar.js",
                    lineNumber: 96,
                    columnNumber: 11
                }, this),
                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("li", {
                    className: "nav-item dropdown",
                    children: [
                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("a", {
                            className: "nav-link dropdown-toggle",
                            "aria-current": "page",
                            href: "#",
                            style: {
                                textDecoration: 'none',
                                color: 'white'
                            },
                            id: "navbarDropdown",
                            role: "button",
                            "data-bs-toggle": "dropdown",
                            children: "Requests"
                        }, void 0, false, {
                            fileName: "src/components/IT20125202/NavBar.js",
                            lineNumber: 107,
                            columnNumber: 13
                        }, this),
                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("ul", {
                            className: "dropdown-menu",
                            "aria-labelledby": "navbarDropdown",
                            children: [
                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("li", {
                                    children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("a", {
                                        className: "dropdown-item",
                                        href: "/supervisor/request",
                                        children: "Send Request"
                                    }, void 0, false, {
                                        fileName: "src/components/IT20125202/NavBar.js",
                                        lineNumber: 111,
                                        columnNumber: 21
                                    }, this)
                                }, void 0, false, {
                                    fileName: "src/components/IT20125202/NavBar.js",
                                    lineNumber: 111,
                                    columnNumber: 17
                                }, this),
                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("li", {
                                    children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("a", {
                                        className: "dropdown-item",
                                        href: "/request/summary",
                                        children: "Request Summary"
                                    }, void 0, false, {
                                        fileName: "src/components/IT20125202/NavBar.js",
                                        lineNumber: 112,
                                        columnNumber: 21
                                    }, this)
                                }, void 0, false, {
                                    fileName: "src/components/IT20125202/NavBar.js",
                                    lineNumber: 112,
                                    columnNumber: 17
                                }, this)
                            ]
                        }, void 0, true, {
                            fileName: "src/components/IT20125202/NavBar.js",
                            lineNumber: 110,
                            columnNumber: 13
                        }, this)
                    ]
                }, void 0, true, {
                    fileName: "src/components/IT20125202/NavBar.js",
                    lineNumber: 106,
                    columnNumber: 12
                }, this),
                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("li", {
                    className: "nav-item dropdown",
                    children: [
                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("a", {
                            className: "nav-link dropdown-toggle",
                            "aria-current": "page",
                            href: "#",
                            style: {
                                textDecoration: 'none',
                                color: 'white'
                            },
                            id: "navbarDropdown",
                            role: "button",
                            "data-bs-toggle": "dropdown",
                            children: "Submitions"
                        }, void 0, false, {
                            fileName: "src/components/IT20125202/NavBar.js",
                            lineNumber: 117,
                            columnNumber: 13
                        }, this),
                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("ul", {
                            className: "dropdown-menu",
                            "aria-labelledby": "navbarDropdown",
                            children: [
                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("li", {
                                    children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("a", {
                                        className: "dropdown-item",
                                        href: "/student/submitiontype/view",
                                        children: "View Submition"
                                    }, void 0, false, {
                                        fileName: "src/components/IT20125202/NavBar.js",
                                        lineNumber: 121,
                                        columnNumber: 21
                                    }, this)
                                }, void 0, false, {
                                    fileName: "src/components/IT20125202/NavBar.js",
                                    lineNumber: 121,
                                    columnNumber: 17
                                }, this),
                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("li", {
                                    children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("a", {
                                        className: "dropdown-item",
                                        href: "/student/submitionsp/view",
                                        children: "Add Submition"
                                    }, void 0, false, {
                                        fileName: "src/components/IT20125202/NavBar.js",
                                        lineNumber: 122,
                                        columnNumber: 21
                                    }, this)
                                }, void 0, false, {
                                    fileName: "src/components/IT20125202/NavBar.js",
                                    lineNumber: 122,
                                    columnNumber: 17
                                }, this),
                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("li", {
                                    children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("a", {
                                        className: "dropdown-item",
                                        href: "/student/marks/view",
                                        children: "View Marks"
                                    }, void 0, false, {
                                        fileName: "src/components/IT20125202/NavBar.js",
                                        lineNumber: 123,
                                        columnNumber: 21
                                    }, this)
                                }, void 0, false, {
                                    fileName: "src/components/IT20125202/NavBar.js",
                                    lineNumber: 123,
                                    columnNumber: 17
                                }, this)
                            ]
                        }, void 0, true, {
                            fileName: "src/components/IT20125202/NavBar.js",
                            lineNumber: 120,
                            columnNumber: 13
                        }, this)
                    ]
                }, void 0, true, {
                    fileName: "src/components/IT20125202/NavBar.js",
                    lineNumber: 116,
                    columnNumber: 11
                }, this),
                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("li", {
                    className: "nav-item",
                    children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("a", {
                        className: "nav-link",
                        "aria-current": "page",
                        href: "/user/contactus",
                        style: {
                            textDecoration: 'none',
                            color: 'white'
                        },
                        children: "Contact Us"
                    }, void 0, false, {
                        fileName: "src/components/IT20125202/NavBar.js",
                        lineNumber: 128,
                        columnNumber: 13
                    }, this)
                }, void 0, false, {
                    fileName: "src/components/IT20125202/NavBar.js",
                    lineNumber: 127,
                    columnNumber: 11
                }, this),
                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("li", {
                    className: "nav-item",
                    children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("a", {
                        className: "nav-link",
                        "aria-current": "page",
                        href: "/user/aboutus",
                        style: {
                            textDecoration: 'none',
                            color: 'white'
                        },
                        children: "About Us"
                    }, void 0, false, {
                        fileName: "src/components/IT20125202/NavBar.js",
                        lineNumber: 131,
                        columnNumber: 13
                    }, this)
                }, void 0, false, {
                    fileName: "src/components/IT20125202/NavBar.js",
                    lineNumber: 130,
                    columnNumber: 11
                }, this),
                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("li", {
                    className: "nav-item",
                    children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("a", {
                        href: "/user/login",
                        onClick: this.logOut.bind(this),
                        className: "nav-link",
                        style: {
                            textDecoration: 'none',
                            color: 'white'
                        },
                        children: "Log out"
                    }, void 0, false, {
                        fileName: "src/components/IT20125202/NavBar.js",
                        lineNumber: 135,
                        columnNumber: 13
                    }, this)
                }, void 0, false, {
                    fileName: "src/components/IT20125202/NavBar.js",
                    lineNumber: 134,
                    columnNumber: 11
                }, this)
            ]
        }, void 0, true, {
            fileName: "src/components/IT20125202/NavBar.js",
            lineNumber: 73,
            columnNumber: 9
        }, this);
        else if (this.state.type === 'Supervisor') userLink = /*#__PURE__*/ _jsxDevRuntime.jsxDEV("ul", {
            className: "nav nav-tabs",
            children: [
                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("li", {
                    className: "nav-item",
                    children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("a", {
                        className: "nav-link",
                        "aria-current": "page",
                        href: "/home",
                        style: {
                            textDecoration: 'none',
                            color: 'white'
                        },
                        children: "Home"
                    }, void 0, false, {
                        fileName: "src/components/IT20125202/NavBar.js",
                        lineNumber: 148,
                        columnNumber: 13
                    }, this)
                }, void 0, false, {
                    fileName: "src/components/IT20125202/NavBar.js",
                    lineNumber: 147,
                    columnNumber: 11
                }, this),
                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("li", {
                    className: "nav-item",
                    children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("a", {
                        className: "nav-link",
                        "aria-current": "page",
                        href: "/user/profile",
                        style: {
                            textDecoration: 'none',
                            color: 'white'
                        },
                        children: "Profile"
                    }, void 0, false, {
                        fileName: "src/components/IT20125202/NavBar.js",
                        lineNumber: 151,
                        columnNumber: 13
                    }, this)
                }, void 0, false, {
                    fileName: "src/components/IT20125202/NavBar.js",
                    lineNumber: 150,
                    columnNumber: 11
                }, this),
                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("li", {
                    className: "nav-item",
                    children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("a", {
                        className: "nav-link",
                        "aria-current": "page",
                        href: "/submitions/view",
                        style: {
                            textDecoration: 'none',
                            color: 'white'
                        },
                        children: "Submitions"
                    }, void 0, false, {
                        fileName: "src/components/IT20125202/NavBar.js",
                        lineNumber: 154,
                        columnNumber: 13
                    }, this)
                }, void 0, false, {
                    fileName: "src/components/IT20125202/NavBar.js",
                    lineNumber: 153,
                    columnNumber: 11
                }, this),
                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("li", {
                    className: "nav-item",
                    children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("a", {
                        className: "nav-link",
                        "aria-current": "page",
                        href: "/user/contactus",
                        style: {
                            textDecoration: 'none',
                            color: 'white'
                        },
                        children: "Contact Us"
                    }, void 0, false, {
                        fileName: "src/components/IT20125202/NavBar.js",
                        lineNumber: 157,
                        columnNumber: 13
                    }, this)
                }, void 0, false, {
                    fileName: "src/components/IT20125202/NavBar.js",
                    lineNumber: 156,
                    columnNumber: 11
                }, this),
                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("li", {
                    className: "nav-item",
                    children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("a", {
                        className: "nav-link",
                        "aria-current": "page",
                        href: "/user/aboutus",
                        style: {
                            textDecoration: 'none',
                            color: 'white'
                        },
                        children: "About Us"
                    }, void 0, false, {
                        fileName: "src/components/IT20125202/NavBar.js",
                        lineNumber: 160,
                        columnNumber: 13
                    }, this)
                }, void 0, false, {
                    fileName: "src/components/IT20125202/NavBar.js",
                    lineNumber: 159,
                    columnNumber: 11
                }, this),
                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("li", {
                    className: "nav-item",
                    children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("a", {
                        href: "/user/login",
                        onClick: this.logOut.bind(this),
                        className: "nav-link",
                        style: {
                            textDecoration: 'none',
                            color: 'white'
                        },
                        children: "Log out"
                    }, void 0, false, {
                        fileName: "src/components/IT20125202/NavBar.js",
                        lineNumber: 163,
                        columnNumber: 13
                    }, this)
                }, void 0, false, {
                    fileName: "src/components/IT20125202/NavBar.js",
                    lineNumber: 162,
                    columnNumber: 11
                }, this)
            ]
        }, void 0, true, {
            fileName: "src/components/IT20125202/NavBar.js",
            lineNumber: 146,
            columnNumber: 9
        }, this);
        else if (this.state.type === 'Panel Member') userLink = /*#__PURE__*/ _jsxDevRuntime.jsxDEV("ul", {
            className: "nav nav-tabs",
            children: [
                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("li", {
                    className: "nav-item",
                    children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("a", {
                        className: "nav-link",
                        "aria-current": "page",
                        href: "/home",
                        style: {
                            textDecoration: 'none',
                            color: 'white'
                        },
                        children: "Home"
                    }, void 0, false, {
                        fileName: "src/components/IT20125202/NavBar.js",
                        lineNumber: 176,
                        columnNumber: 13
                    }, this)
                }, void 0, false, {
                    fileName: "src/components/IT20125202/NavBar.js",
                    lineNumber: 175,
                    columnNumber: 11
                }, this),
                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("li", {
                    className: "nav-item",
                    children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("a", {
                        className: "nav-link",
                        "aria-current": "page",
                        href: "/user/profile",
                        style: {
                            textDecoration: 'none',
                            color: 'white'
                        },
                        children: "Profile"
                    }, void 0, false, {
                        fileName: "src/components/IT20125202/NavBar.js",
                        lineNumber: 179,
                        columnNumber: 13
                    }, this)
                }, void 0, false, {
                    fileName: "src/components/IT20125202/NavBar.js",
                    lineNumber: 178,
                    columnNumber: 11
                }, this),
                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("li", {
                    className: "nav-item",
                    children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("a", {
                        className: "nav-link",
                        "aria-current": "page",
                        href: "/panel/topic/list",
                        style: {
                            textDecoration: 'none',
                            color: 'white'
                        },
                        children: "Topics"
                    }, void 0, false, {
                        fileName: "src/components/IT20125202/NavBar.js",
                        lineNumber: 182,
                        columnNumber: 13
                    }, this)
                }, void 0, false, {
                    fileName: "src/components/IT20125202/NavBar.js",
                    lineNumber: 181,
                    columnNumber: 11
                }, this),
                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("li", {
                    className: "nav-item",
                    children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("a", {
                        className: "nav-link",
                        "aria-current": "page",
                        href: "/evaluation/add",
                        style: {
                            textDecoration: 'none',
                            color: 'white'
                        },
                        children: "Evaluation"
                    }, void 0, false, {
                        fileName: "src/components/IT20125202/NavBar.js",
                        lineNumber: 185,
                        columnNumber: 13
                    }, this)
                }, void 0, false, {
                    fileName: "src/components/IT20125202/NavBar.js",
                    lineNumber: 184,
                    columnNumber: 11
                }, this),
                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("li", {
                    className: "nav-item",
                    children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("a", {
                        className: "nav-link",
                        "aria-current": "page",
                        href: "/user/contactus",
                        style: {
                            textDecoration: 'none',
                            color: 'white'
                        },
                        children: "Contact Us"
                    }, void 0, false, {
                        fileName: "src/components/IT20125202/NavBar.js",
                        lineNumber: 188,
                        columnNumber: 13
                    }, this)
                }, void 0, false, {
                    fileName: "src/components/IT20125202/NavBar.js",
                    lineNumber: 187,
                    columnNumber: 11
                }, this),
                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("li", {
                    className: "nav-item",
                    children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("a", {
                        className: "nav-link",
                        "aria-current": "page",
                        href: "/user/aboutus",
                        style: {
                            textDecoration: 'none',
                            color: 'white'
                        },
                        children: "About Us"
                    }, void 0, false, {
                        fileName: "src/components/IT20125202/NavBar.js",
                        lineNumber: 191,
                        columnNumber: 13
                    }, this)
                }, void 0, false, {
                    fileName: "src/components/IT20125202/NavBar.js",
                    lineNumber: 190,
                    columnNumber: 11
                }, this),
                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("li", {
                    className: "nav-item",
                    children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("a", {
                        href: "/user/login",
                        onClick: this.logOut.bind(this),
                        className: "nav-link",
                        style: {
                            textDecoration: 'none',
                            color: 'white'
                        },
                        children: "Log out"
                    }, void 0, false, {
                        fileName: "src/components/IT20125202/NavBar.js",
                        lineNumber: 194,
                        columnNumber: 13
                    }, this)
                }, void 0, false, {
                    fileName: "src/components/IT20125202/NavBar.js",
                    lineNumber: 193,
                    columnNumber: 11
                }, this)
            ]
        }, void 0, true, {
            fileName: "src/components/IT20125202/NavBar.js",
            lineNumber: 174,
            columnNumber: 9
        }, this);
        return /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
            children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("nav", {
                className: "navbar navbar-expand-lg navbar-light",
                style: {
                    background: '#212F3C'
                },
                children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                    className: "container-fluid",
                    children: [
                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("a", {
                            className: "navbar-brand",
                            href: "/",
                            style: {
                                color: 'white'
                            },
                            children: "RPMS"
                        }, void 0, false, {
                            fileName: "src/components/IT20125202/NavBar.js",
                            lineNumber: 207,
                            columnNumber: 13
                        }, this),
                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("button", {
                            className: "navbar-toggler",
                            type: "button",
                            "data-bs-toggle": "collapse",
                            "data-bs-target": "#navbarSupportedContent",
                            "aria-controls": "navbarSupportedContent",
                            "aria-expanded": "false",
                            "aria-label": "Toggle navigation",
                            children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("span", {
                                className: "navbar-toggler-icon"
                            }, void 0, false, {
                                fileName: "src/components/IT20125202/NavBar.js",
                                lineNumber: 218,
                                columnNumber: 15
                            }, this)
                        }, void 0, false, {
                            fileName: "src/components/IT20125202/NavBar.js",
                            lineNumber: 209,
                            columnNumber: 13
                        }, this),
                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                            className: "collapse navbar-collapse justify-content-md-center",
                            id: "navbarSupportedContent",
                            children: [
                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("ul", {
                                    className: "navbar-nav ms-auto mb-2 mb-lg-0"
                                }, void 0, false, {
                                    fileName: "src/components/IT20125202/NavBar.js",
                                    lineNumber: 228,
                                    columnNumber: 15
                                }, this),
                                localStorage.userToken ? userLink : loginRegLink
                            ]
                        }, void 0, true, {
                            fileName: "src/components/IT20125202/NavBar.js",
                            lineNumber: 223,
                            columnNumber: 13
                        }, this)
                    ]
                }, void 0, true, {
                    fileName: "src/components/IT20125202/NavBar.js",
                    lineNumber: 206,
                    columnNumber: 11
                }, this)
            }, void 0, false, {
                fileName: "src/components/IT20125202/NavBar.js",
                lineNumber: 205,
                columnNumber: 9
            }, this)
        }, void 0, false, {
            fileName: "src/components/IT20125202/NavBar.js",
            lineNumber: 204,
            columnNumber: 7
        }, this);
    }
}
exports.default = _c = _reactRouterDomMin.withRouter(NavBar);
var _c;
$RefreshReg$(_c, "%default%");

  $parcel$ReactRefreshHelpers$cdbd.postlude(module);
} finally {
  window.$RefreshReg$ = prevRefreshReg;
  window.$RefreshSig$ = prevRefreshSig;
}
},{"react/jsx-dev-runtime":"iTorj","react":"21dqq","react-router-dom/cjs/react-router-dom.min":"4Y83O","jwt-decode":"ljM1w","sweetalert":"38a65","@parcel/transformer-js/src/esmodule-helpers.js":"gkKU3","@parcel/transformer-react-refresh-wrap/lib/helpers/helpers.js":"km3Ru"}],"4Y83O":[function(require,module,exports) {
"use strict";
function _interopDefault(e) {
    return e && "object" == typeof e && "default" in e ? e.default : e;
}
Object.defineProperty(exports, "__esModule", {
    value: !0
});
var reactRouter = require("react-router"), React = _interopDefault(require("react")), history = require("history");
require("prop-types"), require("tiny-warning");
var invariant = _interopDefault(require("tiny-invariant"));
function _extends() {
    return (_extends = Object.assign || function(e) {
        for(var t = 1; t < arguments.length; t++){
            var r = arguments[t];
            for(var o in r)Object.prototype.hasOwnProperty.call(r, o) && (e[o] = r[o]);
        }
        return e;
    }).apply(this, arguments);
}
function _inheritsLoose(e, t) {
    e.prototype = Object.create(t.prototype), (e.prototype.constructor = e).__proto__ = t;
}
function _objectWithoutPropertiesLoose(e, t) {
    if (null == e) return {};
    var r, o, n = {}, a = Object.keys(e);
    for(o = 0; o < a.length; o++)r = a[o], 0 <= t.indexOf(r) || (n[r] = e[r]);
    return n;
}
var BrowserRouter = function(n) {
    function e1() {
        for(var e, t = arguments.length, r = new Array(t), o = 0; o < t; o++)r[o] = arguments[o];
        return (e = n.call.apply(n, [
            this
        ].concat(r)) || this).history = history.createBrowserHistory(e.props), e;
    }
    return _inheritsLoose(e1, n), e1.prototype.render = function() {
        return React.createElement(reactRouter.Router, {
            history: this.history,
            children: this.props.children
        });
    }, e1;
}(React.Component), HashRouter = function(n) {
    function e2() {
        for(var e, t = arguments.length, r = new Array(t), o = 0; o < t; o++)r[o] = arguments[o];
        return (e = n.call.apply(n, [
            this
        ].concat(r)) || this).history = history.createHashHistory(e.props), e;
    }
    return _inheritsLoose(e2, n), e2.prototype.render = function() {
        return React.createElement(reactRouter.Router, {
            history: this.history,
            children: this.props.children
        });
    }, e2;
}(React.Component), resolveToLocation = function(e, t) {
    return "function" == typeof e ? e(t) : e;
}, normalizeToLocation = function(e, t) {
    return "string" == typeof e ? history.createLocation(e, null, null, t) : e;
}, forwardRefShim = function(e) {
    return e;
}, forwardRef = React.forwardRef;
function isModifiedEvent(e) {
    return !!(e.metaKey || e.altKey || e.ctrlKey || e.shiftKey);
}
void 0 === forwardRef && (forwardRef = forwardRefShim);
var LinkAnchor = forwardRef(function(e3, t1) {
    var r = e3.innerRef, o = e3.navigate, n = e3.onClick, a = _objectWithoutPropertiesLoose(e3, [
        "innerRef",
        "navigate",
        "onClick"
    ]), i = a.target, c = _extends({}, a, {
        onClick: function(t) {
            try {
                n && n(t);
            } catch (e) {
                throw t.preventDefault(), e;
            }
            t.defaultPrevented || 0 !== t.button || i && "_self" !== i || isModifiedEvent(t) || (t.preventDefault(), o());
        }
    });
    return c.ref = forwardRefShim !== forwardRef && t1 || r, React.createElement("a", c);
}), Link = forwardRef(function(e4, a) {
    var t2 = e4.component, i = void 0 === t2 ? LinkAnchor : t2, c = e4.replace, u = e4.to, s = e4.innerRef, f = _objectWithoutPropertiesLoose(e4, [
        "component",
        "replace",
        "to",
        "innerRef"
    ]);
    return React.createElement(reactRouter.__RouterContext.Consumer, null, function(t) {
        t || invariant(!1);
        var r = t.history, e5 = normalizeToLocation(resolveToLocation(u, t.location), t.location), o = e5 ? r.createHref(e5) : "", n = _extends({}, f, {
            href: o,
            navigate: function() {
                var e = resolveToLocation(u, t.location);
                (c ? r.replace : r.push)(e);
            }
        });
        return forwardRefShim !== forwardRef ? n.ref = a || s : n.innerRef = s, React.createElement(i, n);
    });
}), forwardRefShim$1 = function(e) {
    return e;
}, forwardRef$1 = React.forwardRef;
function joinClassnames() {
    for(var e6 = arguments.length, t = new Array(e6), r = 0; r < e6; r++)t[r] = arguments[r];
    return t.filter(function(e) {
        return e;
    }).join(" ");
}
void 0 === forwardRef$1 && (forwardRef$1 = forwardRefShim$1);
var NavLink = forwardRef$1(function(e7, f) {
    var t3 = e7["aria-current"], l = void 0 === t3 ? "page" : t3, r1 = e7.activeClassName, p = void 0 === r1 ? "active" : r1, R = e7.activeStyle, h = e7.className, d = e7.exact, y = e7.isActive, m = e7.location, v = e7.sensitive, b = e7.strict, w = e7.style, x = e7.to, g = e7.innerRef, P = _objectWithoutPropertiesLoose(e7, [
        "aria-current",
        "activeClassName",
        "activeStyle",
        "className",
        "exact",
        "isActive",
        "location",
        "sensitive",
        "strict",
        "style",
        "to",
        "innerRef"
    ]);
    return React.createElement(reactRouter.__RouterContext.Consumer, null, function(e) {
        e || invariant(!1);
        var t = m || e.location, r = normalizeToLocation(resolveToLocation(x, t), t), o = r.pathname, n = o && o.replace(/([.+*?=^!:${}()[\]|/\\])/g, "\\$1"), a = n ? reactRouter.matchPath(t.pathname, {
            path: n,
            exact: d,
            sensitive: v,
            strict: b
        }) : null, i = !!(y ? y(a, t) : a), c = i ? joinClassnames(h, p) : h, u = i ? _extends({}, w, {}, R) : w, s = _extends({
            "aria-current": i && l || null,
            className: c,
            style: u,
            to: r
        }, P);
        return forwardRefShim$1 !== forwardRef$1 ? s.ref = f || g : s.innerRef = g, React.createElement(Link, s);
    });
});
Object.defineProperty(exports, "MemoryRouter", {
    enumerable: !0,
    get: function() {
        return reactRouter.MemoryRouter;
    }
}), Object.defineProperty(exports, "Prompt", {
    enumerable: !0,
    get: function() {
        return reactRouter.Prompt;
    }
}), Object.defineProperty(exports, "Redirect", {
    enumerable: !0,
    get: function() {
        return reactRouter.Redirect;
    }
}), Object.defineProperty(exports, "Route", {
    enumerable: !0,
    get: function() {
        return reactRouter.Route;
    }
}), Object.defineProperty(exports, "Router", {
    enumerable: !0,
    get: function() {
        return reactRouter.Router;
    }
}), Object.defineProperty(exports, "StaticRouter", {
    enumerable: !0,
    get: function() {
        return reactRouter.StaticRouter;
    }
}), Object.defineProperty(exports, "Switch", {
    enumerable: !0,
    get: function() {
        return reactRouter.Switch;
    }
}), Object.defineProperty(exports, "generatePath", {
    enumerable: !0,
    get: function() {
        return reactRouter.generatePath;
    }
}), Object.defineProperty(exports, "matchPath", {
    enumerable: !0,
    get: function() {
        return reactRouter.matchPath;
    }
}), Object.defineProperty(exports, "useHistory", {
    enumerable: !0,
    get: function() {
        return reactRouter.useHistory;
    }
}), Object.defineProperty(exports, "useLocation", {
    enumerable: !0,
    get: function() {
        return reactRouter.useLocation;
    }
}), Object.defineProperty(exports, "useParams", {
    enumerable: !0,
    get: function() {
        return reactRouter.useParams;
    }
}), Object.defineProperty(exports, "useRouteMatch", {
    enumerable: !0,
    get: function() {
        return reactRouter.useRouteMatch;
    }
}), Object.defineProperty(exports, "withRouter", {
    enumerable: !0,
    get: function() {
        return reactRouter.withRouter;
    }
}), exports.BrowserRouter = BrowserRouter, exports.HashRouter = HashRouter, exports.Link = Link, exports.NavLink = NavLink;

},{"react-router":"dvHa0","react":"21dqq","history":"6yWeD","prop-types":"7wKI2","tiny-warning":"82vrW","tiny-invariant":"fnIPv"}],"6GQI0":[function(require,module,exports) {
var $parcel$ReactRefreshHelpers$61c8 = require("@parcel/transformer-react-refresh-wrap/lib/helpers/helpers.js");
var prevRefreshReg = window.$RefreshReg$;
var prevRefreshSig = window.$RefreshSig$;
$parcel$ReactRefreshHelpers$61c8.prelude(module);

try {
var parcelHelpers = require("@parcel/transformer-js/src/esmodule-helpers.js");
parcelHelpers.defineInteropFlag(exports);
var _jsxDevRuntime = require("react/jsx-dev-runtime");
var _react = require("react");
var _reactDefault = parcelHelpers.interopDefault(_react);
var _jwtDecode = require("jwt-decode");
var _jwtDecodeDefault = parcelHelpers.interopDefault(_jwtDecode);
var _landJpg = require("../../images/land.jpg");
var _landJpgDefault = parcelHelpers.interopDefault(_landJpg);
class LandingPage extends _react.Component {
    componentDidMount() {
        document.title = "Home";
    }
    render() {
        return /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
            className: "container",
            style: {
                minHeight: '100vh'
            },
            children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                className: "container",
                style: {
                    position: 'relative',
                    textAlign: 'center'
                },
                children: [
                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("img", {
                        src: _landJpgDefault.default,
                        class: "img-fluid",
                        alt: "Research Project Management Tool",
                        style: {
                            minWidth: '100%',
                            minHeight: '100vh',
                            opacity: '0.7'
                        }
                    }, void 0, false, {
                        fileName: "src/components/IT20125202/LandingPage.js",
                        lineNumber: 20,
                        columnNumber: 13
                    }, this),
                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                        style: {
                            position: 'absolute',
                            top: '50%',
                            left: '50%',
                            transform: 'translate(-50%, -50%)'
                        },
                        children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("h1", {
                            class: "display-5",
                            style: {
                                fontFamily: 'Stencil Std, fantasy'
                            },
                            children: "Research Project Management Tool"
                        }, void 0, false, {
                            fileName: "src/components/IT20125202/LandingPage.js",
                            lineNumber: 22,
                            columnNumber: 15
                        }, this)
                    }, void 0, false, {
                        fileName: "src/components/IT20125202/LandingPage.js",
                        lineNumber: 21,
                        columnNumber: 13
                    }, this)
                ]
            }, void 0, true, {
                fileName: "src/components/IT20125202/LandingPage.js",
                lineNumber: 19,
                columnNumber: 11
            }, this)
        }, void 0, false, {
            fileName: "src/components/IT20125202/LandingPage.js",
            lineNumber: 11,
            columnNumber: 7
        }, this);
    }
}
exports.default = LandingPage;

  $parcel$ReactRefreshHelpers$61c8.postlude(module);
} finally {
  window.$RefreshReg$ = prevRefreshReg;
  window.$RefreshSig$ = prevRefreshSig;
}
},{"react/jsx-dev-runtime":"iTorj","react":"21dqq","jwt-decode":"ljM1w","../../images/land.jpg":"iqYsB","@parcel/transformer-js/src/esmodule-helpers.js":"gkKU3","@parcel/transformer-react-refresh-wrap/lib/helpers/helpers.js":"km3Ru"}],"iqYsB":[function(require,module,exports) {
module.exports = require('./helpers/bundle-url').getBundleURL('bLxZJ') + "land.301f770a.jpg" + "?" + Date.now();

},{"./helpers/bundle-url":"lgJ39"}],"4ynNU":[function(require,module,exports) {
var $parcel$ReactRefreshHelpers$5cdf = require("@parcel/transformer-react-refresh-wrap/lib/helpers/helpers.js");
var prevRefreshReg = window.$RefreshReg$;
var prevRefreshSig = window.$RefreshSig$;
$parcel$ReactRefreshHelpers$5cdf.prelude(module);

try {
var parcelHelpers = require("@parcel/transformer-js/src/esmodule-helpers.js");
parcelHelpers.defineInteropFlag(exports);
var _jsxDevRuntime = require("react/jsx-dev-runtime");
//topic registration
var _react = require("react");
var _reactDefault = parcelHelpers.interopDefault(_react);
var _axios = require("axios");
var _axiosDefault = parcelHelpers.interopDefault(_axios);
var _jwtDecode = require("jwt-decode");
var _jwtDecodeDefault = parcelHelpers.interopDefault(_jwtDecode);
var _sweetalert = require("sweetalert");
var _sweetalertDefault = parcelHelpers.interopDefault(_sweetalert);
class RegisterTopic extends _react.Component {
    constructor(props){
        super(props);
        this.state = {
            groupId: "",
            topicR: "",
            description: "",
            status: "Pending",
            comments: ""
        };
    }
    componentDidMount() {
        document.title = "Topic Registration";
        // redirect to the login page if the user is not logged in
        if (!localStorage.userToken) _sweetalertDefault.default("Please login first", "", "warning").then((value)=>{
            if (value) {
                this.props.history.push(`/user/login`);
                window.location.reload();
            }
        });
        //get group id using the user token
        const usertoken = localStorage.userToken;
        const decoded = _jwtDecodeDefault.default(usertoken);
        const id = decoded.groupId;
        this.setState({
            groupId: id
        });
    }
    handleInputChange = (e)=>{
        const { name , value  } = e.target;
        this.setState({
            ...this.state,
            [name]: value
        });
    };
    onSubmit = (e)=>{
        e.preventDefault();
        const { groupId , topicR , description , status , comments  } = this.state;
        const data = {
            groupId: groupId,
            topicR: topicR,
            description: description,
            status: status,
            comments: comments
        };
        // console.log(data)
        // Validations-----------------------------------------------------------------
        let validated = true;
        if (groupId === '') {
            validated = false;
            _sweetalertDefault.default({
                title: "",
                text: "You still do not have a group",
                icon: "warning"
            });
        } else if (topicR === '') {
            validated = false;
            _sweetalertDefault.default({
                title: "",
                text: "Please enter the topic",
                icon: "warning"
            });
        } else if (description === '') {
            validated = false;
            _sweetalertDefault.default({
                title: "",
                text: "Please enter a description about your project",
                icon: "warning"
            });
        }
        // save the validated data
        if (validated) _axiosDefault.default.post('http://localhost:5000/topic/save', data).then((res)=>{
            if (res.data.success) _sweetalertDefault.default("Registered successfully!", "", "success").then((value)=>{
                if (value) {
                    this.props.history.push(`/student/topics`);
                    window.location.reload();
                }
            });
        });
    };
    render() {
        return /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
            className: "container",
            style: {
                padding: '50px 50px 50px 50px',
                background: 'white',
                minHeight: '100vh'
            },
            children: [
                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                    className: "col-lg-9 mt-2 mb-2",
                    children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("h1", {
                        children: "Research Topic Registration"
                    }, void 0, false, {
                        fileName: "src/components/IT20125202/topic/RegisterTopic.js",
                        lineNumber: 121,
                        columnNumber: 21
                    }, this)
                }, void 0, false, {
                    fileName: "src/components/IT20125202/topic/RegisterTopic.js",
                    lineNumber: 120,
                    columnNumber: 17
                }, this),
                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("hr", {}, void 0, false, {
                    fileName: "src/components/IT20125202/topic/RegisterTopic.js",
                    lineNumber: 123,
                    columnNumber: 17
                }, this),
                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                    className: "col-md-8 mt-4 mx-auto",
                    children: [
                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("br", {}, void 0, false, {
                            fileName: "src/components/IT20125202/topic/RegisterTopic.js",
                            lineNumber: 126,
                            columnNumber: 21
                        }, this),
                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("form", {
                            className: "needs-validation",
                            noValidate: true,
                            children: [
                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                    className: "form-group",
                                    style: {
                                        marginBottom: '15px'
                                    },
                                    children: [
                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("label", {
                                            style: {
                                                marginBottom: '5px'
                                            },
                                            children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("b", {
                                                children: "Group ID"
                                            }, void 0, false, {
                                                fileName: "src/components/IT20125202/topic/RegisterTopic.js",
                                                lineNumber: 129,
                                                columnNumber: 68
                                            }, this)
                                        }, void 0, false, {
                                            fileName: "src/components/IT20125202/topic/RegisterTopic.js",
                                            lineNumber: 129,
                                            columnNumber: 29
                                        }, this),
                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("input", {
                                            type: "text",
                                            className: "form-control",
                                            name: "groupId",
                                            placeholder: "Enter your Group ID",
                                            value: this.state.groupId,
                                            onChange: this.handleInputChange,
                                            required: true,
                                            readOnly: true
                                        }, void 0, false, {
                                            fileName: "src/components/IT20125202/topic/RegisterTopic.js",
                                            lineNumber: 130,
                                            columnNumber: 29
                                        }, this)
                                    ]
                                }, void 0, true, {
                                    fileName: "src/components/IT20125202/topic/RegisterTopic.js",
                                    lineNumber: 128,
                                    columnNumber: 25
                                }, this),
                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                    className: "form-group",
                                    style: {
                                        marginBottom: '15px'
                                    },
                                    children: [
                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("label", {
                                            style: {
                                                marginBottom: '5px'
                                            },
                                            children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("b", {
                                                children: "Topic"
                                            }, void 0, false, {
                                                fileName: "src/components/IT20125202/topic/RegisterTopic.js",
                                                lineNumber: 143,
                                                columnNumber: 68
                                            }, this)
                                        }, void 0, false, {
                                            fileName: "src/components/IT20125202/topic/RegisterTopic.js",
                                            lineNumber: 143,
                                            columnNumber: 29
                                        }, this),
                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("input", {
                                            type: "text",
                                            className: "form-control",
                                            name: "topicR",
                                            placeholder: "Enter research topic",
                                            value: this.state.topicR,
                                            onChange: this.handleInputChange,
                                            required: true
                                        }, void 0, false, {
                                            fileName: "src/components/IT20125202/topic/RegisterTopic.js",
                                            lineNumber: 144,
                                            columnNumber: 29
                                        }, this)
                                    ]
                                }, void 0, true, {
                                    fileName: "src/components/IT20125202/topic/RegisterTopic.js",
                                    lineNumber: 142,
                                    columnNumber: 25
                                }, this),
                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                    className: "form-group",
                                    style: {
                                        marginBottom: '15px'
                                    },
                                    children: [
                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("label", {
                                            style: {
                                                marginBottom: '5px'
                                            },
                                            children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("b", {
                                                children: "Description"
                                            }, void 0, false, {
                                                fileName: "src/components/IT20125202/topic/RegisterTopic.js",
                                                lineNumber: 156,
                                                columnNumber: 68
                                            }, this)
                                        }, void 0, false, {
                                            fileName: "src/components/IT20125202/topic/RegisterTopic.js",
                                            lineNumber: 156,
                                            columnNumber: 29
                                        }, this),
                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("textarea", {
                                            type: "text",
                                            className: "form-control",
                                            name: "description",
                                            placeholder: "Enter a description about the topic",
                                            value: this.state.description,
                                            onChange: this.handleInputChange,
                                            required: true
                                        }, void 0, false, {
                                            fileName: "src/components/IT20125202/topic/RegisterTopic.js",
                                            lineNumber: 157,
                                            columnNumber: 29
                                        }, this)
                                    ]
                                }, void 0, true, {
                                    fileName: "src/components/IT20125202/topic/RegisterTopic.js",
                                    lineNumber: 155,
                                    columnNumber: 25
                                }, this),
                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("button", {
                                    className: "btn btn-outline-success",
                                    type: "submit",
                                    style: {
                                        maeginTop: '15px'
                                    },
                                    onClick: this.onSubmit,
                                    children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("b", {
                                        children: [
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("i", {
                                                className: "far fa-check-square"
                                            }, void 0, false, {
                                                fileName: "src/components/IT20125202/topic/RegisterTopic.js",
                                                lineNumber: 179,
                                                columnNumber: 32
                                            }, this),
                                            "\xa0 Register"
                                        ]
                                    }, void 0, true, {
                                        fileName: "src/components/IT20125202/topic/RegisterTopic.js",
                                        lineNumber: 179,
                                        columnNumber: 29
                                    }, this)
                                }, void 0, false, {
                                    fileName: "src/components/IT20125202/topic/RegisterTopic.js",
                                    lineNumber: 178,
                                    columnNumber: 25
                                }, this)
                            ]
                        }, void 0, true, {
                            fileName: "src/components/IT20125202/topic/RegisterTopic.js",
                            lineNumber: 127,
                            columnNumber: 21
                        }, this)
                    ]
                }, void 0, true, {
                    fileName: "src/components/IT20125202/topic/RegisterTopic.js",
                    lineNumber: 124,
                    columnNumber: 17
                }, this)
            ]
        }, void 0, true, {
            fileName: "src/components/IT20125202/topic/RegisterTopic.js",
            lineNumber: 119,
            columnNumber: 13
        }, this);
    }
}
exports.default = RegisterTopic;

  $parcel$ReactRefreshHelpers$5cdf.postlude(module);
} finally {
  window.$RefreshReg$ = prevRefreshReg;
  window.$RefreshSig$ = prevRefreshSig;
}
},{"react/jsx-dev-runtime":"iTorj","react":"21dqq","axios":"jo6P5","jwt-decode":"ljM1w","sweetalert":"38a65","@parcel/transformer-js/src/esmodule-helpers.js":"gkKU3","@parcel/transformer-react-refresh-wrap/lib/helpers/helpers.js":"km3Ru"}],"b5i5o":[function(require,module,exports) {
var $parcel$ReactRefreshHelpers$d8d3 = require("@parcel/transformer-react-refresh-wrap/lib/helpers/helpers.js");
var prevRefreshReg = window.$RefreshReg$;
var prevRefreshSig = window.$RefreshSig$;
$parcel$ReactRefreshHelpers$d8d3.prelude(module);

try {
var parcelHelpers = require("@parcel/transformer-js/src/esmodule-helpers.js");
parcelHelpers.defineInteropFlag(exports);
var _jsxDevRuntime = require("react/jsx-dev-runtime");
//student - view submitted topics
var _react = require("react");
var _reactDefault = parcelHelpers.interopDefault(_react);
var _jwtDecode = require("jwt-decode");
var _jwtDecodeDefault = parcelHelpers.interopDefault(_jwtDecode);
var _axios = require("axios");
var _axiosDefault = parcelHelpers.interopDefault(_axios);
var _sweetalert = require("sweetalert");
var _sweetalertDefault = parcelHelpers.interopDefault(_sweetalert);
class ViewSubmissions extends _react.Component {
    constructor(){
        super();
        this.state = {
            groupId: '',
            topics: []
        };
    }
    componentDidMount() {
        document.title = "Topic Submissions";
        // redirect to the login page if the user is not logged in
        if (!localStorage.userToken) _sweetalertDefault.default("Please login first", "", "warning").then((value)=>{
            if (value) {
                this.props.history.push(`/user/login`);
                window.location.reload();
            }
        });
        const usertoken = localStorage.userToken;
        const decoded = _jwtDecodeDefault.default(usertoken);
        const id = decoded.groupId;
        this.setState({
            groupId: id
        });
        this.retrieveTopics(id);
    }
    retrieveTopics(id) {
        _axiosDefault.default.get(`http://localhost:5000/topic/submissions/${id}`).then((res)=>{
            if (res.data.success) this.setState({
                topics: res.data.existingTopics
            });
        });
    }
    onDelete = (id, grpID, status)=>{
        // with a confirmation 
        // allow a group member(student) to delete their topic only if the status is pending
        if (status === 'Pending') _sweetalertDefault.default({
            title: "Are you sure?",
            text: "Once deleted, you will not be able to recover the details",
            icon: "warning",
            buttons: true,
            dangerMode: true
        }).then((willDelete)=>{
            if (willDelete) _axiosDefault.default.delete(`http://localhost:5000/topic/delete/${id}`).then((res)=>{
                _sweetalertDefault.default("Topic details deleted permanently!", "", "success").then((value)=>{
                    if (value) this.retrieveTopics(grpID);
                });
            });
            else _sweetalertDefault.default("Cancelled. Topic details are safe!");
        });
        else _sweetalertDefault.default({
            title: "",
            text: "You cannot delete this anymore",
            icon: "warning"
        });
    };
    render() {
        return /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
            className: "container",
            style: {
                padding: '50px 50px 50px 50px',
                background: 'white',
                minHeight: '100vh'
            },
            children: [
                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                    className: "row",
                    children: [
                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                            className: "col-lg-9 mt-2 mb-2",
                            children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("h1", {
                                children: "Topics"
                            }, void 0, false, {
                                fileName: "src/components/IT20125202/topic/ViewSubmissions.js",
                                lineNumber: 101,
                                columnNumber: 13
                            }, this)
                        }, void 0, false, {
                            fileName: "src/components/IT20125202/topic/ViewSubmissions.js",
                            lineNumber: 100,
                            columnNumber: 11
                        }, this),
                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("hr", {}, void 0, false, {
                            fileName: "src/components/IT20125202/topic/ViewSubmissions.js",
                            lineNumber: 103,
                            columnNumber: 11
                        }, this),
                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("h5", {
                            style: {
                                textAlign: 'right',
                                color: '#4682B4'
                            },
                            children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("b", {
                                children: [
                                    "Group ID: ",
                                    this.state.groupId
                                ]
                            }, void 0, true, {
                                fileName: "src/components/IT20125202/topic/ViewSubmissions.js",
                                lineNumber: 104,
                                columnNumber: 64
                            }, this)
                        }, void 0, false, {
                            fileName: "src/components/IT20125202/topic/ViewSubmissions.js",
                            lineNumber: 104,
                            columnNumber: 11
                        }, this)
                    ]
                }, void 0, true, {
                    fileName: "src/components/IT20125202/topic/ViewSubmissions.js",
                    lineNumber: 99,
                    columnNumber: 9
                }, this),
                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("table", {
                    className: "table",
                    children: [
                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("thead", {
                            children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("tr", {
                                children: [
                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("th", {
                                        scope: "col",
                                        children: " # "
                                    }, void 0, false, {
                                        fileName: "src/components/IT20125202/topic/ViewSubmissions.js",
                                        lineNumber: 110,
                                        columnNumber: 15
                                    }, this),
                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("th", {
                                        scope: "col",
                                        children: " Topic "
                                    }, void 0, false, {
                                        fileName: "src/components/IT20125202/topic/ViewSubmissions.js",
                                        lineNumber: 112,
                                        columnNumber: 15
                                    }, this),
                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("th", {
                                        scope: "col",
                                        children: " Description "
                                    }, void 0, false, {
                                        fileName: "src/components/IT20125202/topic/ViewSubmissions.js",
                                        lineNumber: 113,
                                        columnNumber: 15
                                    }, this),
                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("th", {
                                        scope: "col",
                                        children: " Status "
                                    }, void 0, false, {
                                        fileName: "src/components/IT20125202/topic/ViewSubmissions.js",
                                        lineNumber: 114,
                                        columnNumber: 15
                                    }, this),
                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("th", {
                                        scope: "col",
                                        children: " Comments "
                                    }, void 0, false, {
                                        fileName: "src/components/IT20125202/topic/ViewSubmissions.js",
                                        lineNumber: 115,
                                        columnNumber: 15
                                    }, this),
                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("th", {
                                        scope: "col"
                                    }, void 0, false, {
                                        fileName: "src/components/IT20125202/topic/ViewSubmissions.js",
                                        lineNumber: 116,
                                        columnNumber: 15
                                    }, this)
                                ]
                            }, void 0, true, {
                                fileName: "src/components/IT20125202/topic/ViewSubmissions.js",
                                lineNumber: 109,
                                columnNumber: 13
                            }, this)
                        }, void 0, false, {
                            fileName: "src/components/IT20125202/topic/ViewSubmissions.js",
                            lineNumber: 108,
                            columnNumber: 11
                        }, this),
                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("tbody", {
                            children: this.state.topics.map((topics, index)=>/*#__PURE__*/ _jsxDevRuntime.jsxDEV("tr", {
                                    children: [
                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("th", {
                                            scope: "row",
                                            children: index + 1
                                        }, void 0, false, {
                                            fileName: "src/components/IT20125202/topic/ViewSubmissions.js",
                                            lineNumber: 123,
                                            columnNumber: 17
                                        }, this),
                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("td", {
                                            children: topics.topicR
                                        }, void 0, false, {
                                            fileName: "src/components/IT20125202/topic/ViewSubmissions.js",
                                            lineNumber: 125,
                                            columnNumber: 17
                                        }, this),
                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("td", {
                                            children: topics.description
                                        }, void 0, false, {
                                            fileName: "src/components/IT20125202/topic/ViewSubmissions.js",
                                            lineNumber: 126,
                                            columnNumber: 17
                                        }, this),
                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("td", {
                                            children: topics.status
                                        }, void 0, false, {
                                            fileName: "src/components/IT20125202/topic/ViewSubmissions.js",
                                            lineNumber: 127,
                                            columnNumber: 17
                                        }, this),
                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("td", {
                                            children: topics.comments
                                        }, void 0, false, {
                                            fileName: "src/components/IT20125202/topic/ViewSubmissions.js",
                                            lineNumber: 128,
                                            columnNumber: 17
                                        }, this),
                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("td", {
                                            children: [
                                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("a", {
                                                    className: "btn btn-outline-success",
                                                    href: `/panel/topic/update/${topics._id}`,
                                                    children: [
                                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("i", {
                                                            className: "fas fa-edit"
                                                        }, void 0, false, {
                                                            fileName: "src/components/IT20125202/topic/ViewSubmissions.js",
                                                            lineNumber: 131,
                                                            columnNumber: 21
                                                        }, this),
                                                        " \xa0Update"
                                                    ]
                                                }, void 0, true, {
                                                    fileName: "src/components/IT20125202/topic/ViewSubmissions.js",
                                                    lineNumber: 130,
                                                    columnNumber: 19
                                                }, this),
                                                "\xa0",
                                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("a", {
                                                    className: "btn btn-outline-danger",
                                                    href: "#",
                                                    onClick: ()=>this.onDelete(topics._id, topics.groupId, topics.status)
                                                    ,
                                                    children: [
                                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("i", {
                                                            className: "fas fa-trash"
                                                        }, void 0, false, {
                                                            fileName: "src/components/IT20125202/topic/ViewSubmissions.js",
                                                            lineNumber: 135,
                                                            columnNumber: 21
                                                        }, this),
                                                        " \xa0Delete"
                                                    ]
                                                }, void 0, true, {
                                                    fileName: "src/components/IT20125202/topic/ViewSubmissions.js",
                                                    lineNumber: 134,
                                                    columnNumber: 19
                                                }, this)
                                            ]
                                        }, void 0, true, {
                                            fileName: "src/components/IT20125202/topic/ViewSubmissions.js",
                                            lineNumber: 129,
                                            columnNumber: 17
                                        }, this)
                                    ]
                                }, index, true, {
                                    fileName: "src/components/IT20125202/topic/ViewSubmissions.js",
                                    lineNumber: 122,
                                    columnNumber: 15
                                }, this)
                            )
                        }, void 0, false, {
                            fileName: "src/components/IT20125202/topic/ViewSubmissions.js",
                            lineNumber: 120,
                            columnNumber: 11
                        }, this)
                    ]
                }, void 0, true, {
                    fileName: "src/components/IT20125202/topic/ViewSubmissions.js",
                    lineNumber: 107,
                    columnNumber: 9
                }, this),
                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("br", {}, void 0, false, {
                    fileName: "src/components/IT20125202/topic/ViewSubmissions.js",
                    lineNumber: 144,
                    columnNumber: 9
                }, this),
                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("a", {
                    className: "btn btn-outline-success",
                    href: "/student/topic/registration",
                    children: [
                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("i", {
                            className: "fas fa-edit"
                        }, void 0, false, {
                            fileName: "src/components/IT20125202/topic/ViewSubmissions.js",
                            lineNumber: 146,
                            columnNumber: 11
                        }, this),
                        " \xa0 New Topic"
                    ]
                }, void 0, true, {
                    fileName: "src/components/IT20125202/topic/ViewSubmissions.js",
                    lineNumber: 145,
                    columnNumber: 9
                }, this)
            ]
        }, void 0, true, {
            fileName: "src/components/IT20125202/topic/ViewSubmissions.js",
            lineNumber: 98,
            columnNumber: 7
        }, this);
    }
}
exports.default = ViewSubmissions;

  $parcel$ReactRefreshHelpers$d8d3.postlude(module);
} finally {
  window.$RefreshReg$ = prevRefreshReg;
  window.$RefreshSig$ = prevRefreshSig;
}
},{"react/jsx-dev-runtime":"iTorj","react":"21dqq","jwt-decode":"ljM1w","axios":"jo6P5","sweetalert":"38a65","@parcel/transformer-js/src/esmodule-helpers.js":"gkKU3","@parcel/transformer-react-refresh-wrap/lib/helpers/helpers.js":"km3Ru"}],"kW0vu":[function(require,module,exports) {
var $parcel$ReactRefreshHelpers$7867 = require("@parcel/transformer-react-refresh-wrap/lib/helpers/helpers.js");
var prevRefreshReg = window.$RefreshReg$;
var prevRefreshSig = window.$RefreshSig$;
$parcel$ReactRefreshHelpers$7867.prelude(module);

try {
var parcelHelpers = require("@parcel/transformer-js/src/esmodule-helpers.js");
parcelHelpers.defineInteropFlag(exports);
var _jsxDevRuntime = require("react/jsx-dev-runtime");
var _groupChatCss = require("../css/GroupChat.css");
var _socketIoClient = require("socket.io-client");
var _socketIoClientDefault = parcelHelpers.interopDefault(_socketIoClient);
var _react = require("react");
var _axios = require("axios");
var _axiosDefault = parcelHelpers.interopDefault(_axios);
var _jwtDecode = require("jwt-decode");
var _jwtDecodeDefault = parcelHelpers.interopDefault(_jwtDecode);
var _groupChat = require("./GroupChat");
var _groupChatDefault = parcelHelpers.interopDefault(_groupChat);
var _s = $RefreshSig$();
const socket = _socketIoClientDefault.default.connect("http://localhost:3001");
function GroupChatStudent() {
    _s();
    const [username, setUsername] = _react.useState("");
    const [room, setRoom] = _react.useState("");
    const [showChat, setShowChat] = _react.useState(false);
    const [userID, setUserID] = _react.useState("");
    const [groupMembers, setGroupMembers] = _react.useState([]);
    const joinRoom = ()=>{
        if (username !== "" && room !== "") {
            socket.emit("join_room", room);
            setShowChat(true);
        }
    };
    _react.useEffect(()=>{
        const usertoken = localStorage.userToken;
        const decoded = _jwtDecodeDefault.default(usertoken);
        setUserID(decoded.idNumber);
        setUsername(decoded.name);
        setRoom(decoded.groupId);
    }, []);
    const getMembers = ()=>{
        _axiosDefault.default.get(`http://localhost:5000/groupMembers/get/${room}`).then((res)=>{
            setGroupMembers(res.data);
        });
        console.log(groupMembers);
    };
    return /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
        className: "container",
        children: [
            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("br", {}, void 0, false, {
                fileName: "src/components/IT20127046/user/GroupChat_Student.js",
                lineNumber: 46,
                columnNumber: 7
            }, this),
            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                className: "col-lg-9 mt-0 mb-2",
                children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("h2", {
                    children: "Group Chat"
                }, void 0, false, {
                    fileName: "src/components/IT20127046/user/GroupChat_Student.js",
                    lineNumber: 48,
                    columnNumber: 9
                }, this)
            }, void 0, false, {
                fileName: "src/components/IT20127046/user/GroupChat_Student.js",
                lineNumber: 47,
                columnNumber: 7
            }, this),
            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("hr", {}, void 0, false, {
                fileName: "src/components/IT20127046/user/GroupChat_Student.js",
                lineNumber: 50,
                columnNumber: 7
            }, this),
            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                className: "container",
                children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                    className: "container chatBox",
                    children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                        className: "row text-center groupSeletePanal",
                        children: [
                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                className: "col-3 bg-dark text-white detailsPanel",
                                children: [
                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("br", {}, void 0, false, {
                                        fileName: "src/components/IT20127046/user/GroupChat_Student.js",
                                        lineNumber: 55,
                                        columnNumber: 15
                                    }, this),
                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("h5", {
                                        children: "Group ID"
                                    }, void 0, false, {
                                        fileName: "src/components/IT20127046/user/GroupChat_Student.js",
                                        lineNumber: 56,
                                        columnNumber: 15
                                    }, this),
                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("p", {
                                        children: room
                                    }, void 0, false, {
                                        fileName: "src/components/IT20127046/user/GroupChat_Student.js",
                                        lineNumber: 57,
                                        columnNumber: 15
                                    }, this),
                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("br", {}, void 0, false, {
                                        fileName: "src/components/IT20127046/user/GroupChat_Student.js",
                                        lineNumber: 58,
                                        columnNumber: 15
                                    }, this),
                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("h5", {
                                        children: "Group Members"
                                    }, void 0, false, {
                                        fileName: "src/components/IT20127046/user/GroupChat_Student.js",
                                        lineNumber: 60,
                                        columnNumber: 15
                                    }, this),
                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("button", {
                                        onClick: getMembers,
                                        style: {
                                            background: "none",
                                            border: "none",
                                            margin: "0px 5px 5px 5px",
                                            color: "#40E0D0"
                                        },
                                        children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("i", {
                                            class: "fa fa-refresh",
                                            "aria-hidden": "true"
                                        }, void 0, false, {
                                            fileName: "src/components/IT20127046/user/GroupChat_Student.js",
                                            lineNumber: 62,
                                            columnNumber: 17
                                        }, this)
                                    }, void 0, false, {
                                        fileName: "src/components/IT20127046/user/GroupChat_Student.js",
                                        lineNumber: 61,
                                        columnNumber: 15
                                    }, this),
                                    groupMembers.map((members, index)=>/*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                            children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("p", {
                                                children: [
                                                    members.name,
                                                    " - ",
                                                    members.idNumber
                                                ]
                                            }, void 0, true, {
                                                fileName: "src/components/IT20127046/user/GroupChat_Student.js",
                                                lineNumber: 67,
                                                columnNumber: 19
                                            }, this)
                                        }, index, false, {
                                            fileName: "src/components/IT20127046/user/GroupChat_Student.js",
                                            lineNumber: 66,
                                            columnNumber: 17
                                        }, this)
                                    ),
                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("br", {}, void 0, false, {
                                        fileName: "src/components/IT20127046/user/GroupChat_Student.js",
                                        lineNumber: 70,
                                        columnNumber: 15
                                    }, this),
                                    !showChat ? /*#__PURE__*/ _jsxDevRuntime.jsxDEV("button", {
                                        className: "btn btn-danger",
                                        onClick: ()=>{
                                            setShowChat(false);
                                        },
                                        disabled: true,
                                        children: "Leave Chat"
                                    }, void 0, false, {
                                        fileName: "src/components/IT20127046/user/GroupChat_Student.js",
                                        lineNumber: 73,
                                        columnNumber: 17
                                    }, this) : /*#__PURE__*/ _jsxDevRuntime.jsxDEV("button", {
                                        className: "btn btn-danger",
                                        onClick: ()=>{
                                            setShowChat(false);
                                        },
                                        children: "Leave Chat"
                                    }, void 0, false, {
                                        fileName: "src/components/IT20127046/user/GroupChat_Student.js",
                                        lineNumber: 75,
                                        columnNumber: 17
                                    }, this)
                                ]
                            }, void 0, true, {
                                fileName: "src/components/IT20127046/user/GroupChat_Student.js",
                                lineNumber: 54,
                                columnNumber: 13
                            }, this),
                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                className: "col-9 p-3 mb-2 bg-light text-dark",
                                children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                    className: "d-flex justify-content-center chatBoxAlign",
                                    children: !showChat ? /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                        className: "chatInput",
                                        children: [
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("br", {}, void 0, false, {
                                                fileName: "src/components/IT20127046/user/GroupChat_Student.js",
                                                lineNumber: 84,
                                                columnNumber: 21
                                            }, this),
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("br", {}, void 0, false, {
                                                fileName: "src/components/IT20127046/user/GroupChat_Student.js",
                                                lineNumber: 85,
                                                columnNumber: 21
                                            }, this),
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("br", {}, void 0, false, {
                                                fileName: "src/components/IT20127046/user/GroupChat_Student.js",
                                                lineNumber: 86,
                                                columnNumber: 21
                                            }, this),
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("h3", {
                                                children: "Join A Group Chat"
                                            }, void 0, false, {
                                                fileName: "src/components/IT20127046/user/GroupChat_Student.js",
                                                lineNumber: 87,
                                                columnNumber: 21
                                            }, this),
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("br", {}, void 0, false, {
                                                fileName: "src/components/IT20127046/user/GroupChat_Student.js",
                                                lineNumber: 88,
                                                columnNumber: 21
                                            }, this),
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("input", {
                                                type: "text",
                                                placeholder: "Name",
                                                class: "form-control",
                                                value: username,
                                                onChange: (event)=>{
                                                    setUsername(event.target.value);
                                                }
                                            }, void 0, false, {
                                                fileName: "src/components/IT20127046/user/GroupChat_Student.js",
                                                lineNumber: 89,
                                                columnNumber: 21
                                            }, this),
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("br", {}, void 0, false, {
                                                fileName: "src/components/IT20127046/user/GroupChat_Student.js",
                                                lineNumber: 98,
                                                columnNumber: 21
                                            }, this),
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("input", {
                                                type: "text",
                                                placeholder: "Group ID",
                                                class: "form-control",
                                                value: room,
                                                onChange: (event)=>{
                                                    setRoom(event.target.value);
                                                }
                                            }, void 0, false, {
                                                fileName: "src/components/IT20127046/user/GroupChat_Student.js",
                                                lineNumber: 99,
                                                columnNumber: 21
                                            }, this),
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("br", {}, void 0, false, {
                                                fileName: "src/components/IT20127046/user/GroupChat_Student.js",
                                                lineNumber: 108,
                                                columnNumber: 21
                                            }, this),
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("button", {
                                                onClick: joinRoom,
                                                class: "btn btn-outline-info",
                                                children: "Join with Group"
                                            }, void 0, false, {
                                                fileName: "src/components/IT20127046/user/GroupChat_Student.js",
                                                lineNumber: 109,
                                                columnNumber: 21
                                            }, this)
                                        ]
                                    }, void 0, true, {
                                        fileName: "src/components/IT20127046/user/GroupChat_Student.js",
                                        lineNumber: 83,
                                        columnNumber: 19
                                    }, this) : /*#__PURE__*/ _jsxDevRuntime.jsxDEV(_groupChatDefault.default, {
                                        socket: socket,
                                        username: username,
                                        room: room
                                    }, void 0, false, {
                                        fileName: "src/components/IT20127046/user/GroupChat_Student.js",
                                        lineNumber: 114,
                                        columnNumber: 19
                                    }, this)
                                }, void 0, false, {
                                    fileName: "src/components/IT20127046/user/GroupChat_Student.js",
                                    lineNumber: 81,
                                    columnNumber: 15
                                }, this)
                            }, void 0, false, {
                                fileName: "src/components/IT20127046/user/GroupChat_Student.js",
                                lineNumber: 80,
                                columnNumber: 13
                            }, this)
                        ]
                    }, void 0, true, {
                        fileName: "src/components/IT20127046/user/GroupChat_Student.js",
                        lineNumber: 53,
                        columnNumber: 11
                    }, this)
                }, void 0, false, {
                    fileName: "src/components/IT20127046/user/GroupChat_Student.js",
                    lineNumber: 52,
                    columnNumber: 9
                }, this)
            }, void 0, false, {
                fileName: "src/components/IT20127046/user/GroupChat_Student.js",
                lineNumber: 51,
                columnNumber: 7
            }, this)
        ]
    }, void 0, true, {
        fileName: "src/components/IT20127046/user/GroupChat_Student.js",
        lineNumber: 45,
        columnNumber: 5
    }, this);
}
exports.default = GroupChatStudent;
_s(GroupChatStudent, "LGXTKdpUi81kz7jGJK3ZC85ekXI=");
_c = GroupChatStudent;
var _c;
$RefreshReg$(_c, "GroupChatStudent");

  $parcel$ReactRefreshHelpers$7867.postlude(module);
} finally {
  window.$RefreshReg$ = prevRefreshReg;
  window.$RefreshSig$ = prevRefreshSig;
}
},{"react/jsx-dev-runtime":"iTorj","../css/GroupChat.css":"5wanh","socket.io-client":"8HBJR","react":"21dqq","axios":"jo6P5","jwt-decode":"ljM1w","./GroupChat":"8GZZG","@parcel/transformer-js/src/esmodule-helpers.js":"gkKU3","@parcel/transformer-react-refresh-wrap/lib/helpers/helpers.js":"km3Ru"}],"5wanh":[function() {},{}],"8HBJR":[function(require,module,exports) {
var parcelHelpers = require("@parcel/transformer-js/src/esmodule-helpers.js");
parcelHelpers.defineInteropFlag(exports);
/**
 * Protocol version.
 *
 * @public
 */ parcelHelpers.export(exports, "protocol", ()=>_socketIoParser.protocol
);
/**
 * Expose constructors for standalone build.
 *
 * @public
 */ parcelHelpers.export(exports, "Manager", ()=>_managerJs.Manager
);
parcelHelpers.export(exports, "Socket", ()=>_socketJs.Socket
);
parcelHelpers.export(exports, "io", ()=>lookup
);
parcelHelpers.export(exports, "connect", ()=>lookup
);
parcelHelpers.export(exports, "default", ()=>lookup
);
var _urlJs = require("./url.js");
var _managerJs = require("./manager.js");
var _socketJs = require("./socket.js");
var _socketIoParser = require("socket.io-parser");
/**
 * Managers cache.
 */ const cache = {};
function lookup(uri, opts) {
    if (typeof uri === "object") {
        opts = uri;
        uri = undefined;
    }
    opts = opts || {};
    const parsed = _urlJs.url(uri, opts.path || "/socket.io");
    const source = parsed.source;
    const id = parsed.id;
    const path = parsed.path;
    const sameNamespace = cache[id] && path in cache[id]["nsps"];
    const newConnection = opts.forceNew || opts["force new connection"] || false === opts.multiplex || sameNamespace;
    let io;
    if (newConnection) io = new _managerJs.Manager(source, opts);
    else {
        if (!cache[id]) cache[id] = new _managerJs.Manager(source, opts);
        io = cache[id];
    }
    if (parsed.query && !opts.query) opts.query = parsed.queryKey;
    return io.socket(parsed.path, opts);
}
// so that "lookup" can be used both as a function (e.g. `io(...)`) and as a
// namespace (e.g. `io.connect(...)`), for backward compatibility
Object.assign(lookup, {
    Manager: _managerJs.Manager,
    Socket: _socketJs.Socket,
    io: lookup,
    connect: lookup
});

},{"./url.js":"9Ze3o","./manager.js":"94vh9","./socket.js":"kbWgu","socket.io-parser":"2lQL3","@parcel/transformer-js/src/esmodule-helpers.js":"gkKU3"}],"9Ze3o":[function(require,module,exports) {
var parcelHelpers = require("@parcel/transformer-js/src/esmodule-helpers.js");
parcelHelpers.defineInteropFlag(exports);
/**
 * URL parser.
 *
 * @param uri - url
 * @param path - the request path of the connection
 * @param loc - An object meant to mimic window.location.
 *        Defaults to window.location.
 * @public
 */ parcelHelpers.export(exports, "url", ()=>url
);
var _engineIoClient = require("engine.io-client");
function url(uri, path = "", loc) {
    let obj = uri;
    // default to window.location
    loc = loc || typeof location !== "undefined" && location;
    if (null == uri) uri = loc.protocol + "//" + loc.host;
    // relative path support
    if (typeof uri === "string") {
        if ("/" === uri.charAt(0)) {
            if ("/" === uri.charAt(1)) uri = loc.protocol + uri;
            else uri = loc.host + uri;
        }
        if (!/^(https?|wss?):\/\//.test(uri)) {
            if ("undefined" !== typeof loc) uri = loc.protocol + "//" + uri;
            else uri = "https://" + uri;
        }
        // parse
        obj = _engineIoClient.parse(uri);
    }
    // make sure we treat `localhost:80` and `localhost` equally
    if (!obj.port) {
        if (/^(http|ws)$/.test(obj.protocol)) obj.port = "80";
        else if (/^(http|ws)s$/.test(obj.protocol)) obj.port = "443";
    }
    obj.path = obj.path || "/";
    const ipv6 = obj.host.indexOf(":") !== -1;
    const host = ipv6 ? "[" + obj.host + "]" : obj.host;
    // define unique id
    obj.id = obj.protocol + "://" + host + ":" + obj.port + path;
    // define href
    obj.href = obj.protocol + "://" + host + (loc && loc.port === obj.port ? "" : ":" + obj.port);
    return obj;
}

},{"engine.io-client":"jBHFs","@parcel/transformer-js/src/esmodule-helpers.js":"gkKU3"}],"jBHFs":[function(require,module,exports) {
var parcelHelpers = require("@parcel/transformer-js/src/esmodule-helpers.js");
parcelHelpers.defineInteropFlag(exports);
parcelHelpers.export(exports, "Socket", ()=>_socketJs.Socket
);
parcelHelpers.export(exports, "protocol", ()=>protocol
);
parcelHelpers.export(exports, "Transport", ()=>_transportJs.Transport
);
parcelHelpers.export(exports, "transports", ()=>_indexJs.transports
);
parcelHelpers.export(exports, "installTimerFunctions", ()=>_utilJs.installTimerFunctions
);
parcelHelpers.export(exports, "parse", ()=>_parseuriJs.parse
);
var _socketJs = require("./socket.js");
var _transportJs = require("./transport.js");
var _indexJs = require("./transports/index.js");
var _utilJs = require("./util.js");
var _parseuriJs = require("./contrib/parseuri.js");
const protocol = _socketJs.Socket.protocol;

},{"./socket.js":"kpkbh","./transport.js":"kwKKC","./transports/index.js":"2neV7","./util.js":"d7eyH","./contrib/parseuri.js":"5RFyz","@parcel/transformer-js/src/esmodule-helpers.js":"gkKU3"}],"kpkbh":[function(require,module,exports) {
var parcelHelpers = require("@parcel/transformer-js/src/esmodule-helpers.js");
parcelHelpers.defineInteropFlag(exports);
parcelHelpers.export(exports, "Socket", ()=>Socket
);
var _indexJs = require("./transports/index.js");
var _utilJs = require("./util.js");
var _parseqsJs = require("./contrib/parseqs.js");
var _parseuriJs = require("./contrib/parseuri.js");
var _componentEmitter = require("@socket.io/component-emitter");
var _engineIoParser = require("engine.io-parser");
class Socket extends _componentEmitter.Emitter {
    /**
     * Socket constructor.
     *
     * @param {String|Object} uri or options
     * @param {Object} opts - options
     * @api public
     */ constructor(uri, opts = {}){
        super();
        if (uri && "object" === typeof uri) {
            opts = uri;
            uri = null;
        }
        if (uri) {
            uri = _parseuriJs.parse(uri);
            opts.hostname = uri.host;
            opts.secure = uri.protocol === "https" || uri.protocol === "wss";
            opts.port = uri.port;
            if (uri.query) opts.query = uri.query;
        } else if (opts.host) opts.hostname = _parseuriJs.parse(opts.host).host;
        _utilJs.installTimerFunctions(this, opts);
        this.secure = null != opts.secure ? opts.secure : typeof location !== "undefined" && "https:" === location.protocol;
        if (opts.hostname && !opts.port) // if no port is specified manually, use the protocol default
        opts.port = this.secure ? "443" : "80";
        this.hostname = opts.hostname || (typeof location !== "undefined" ? location.hostname : "localhost");
        this.port = opts.port || (typeof location !== "undefined" && location.port ? location.port : this.secure ? "443" : "80");
        this.transports = opts.transports || [
            "polling",
            "websocket"
        ];
        this.readyState = "";
        this.writeBuffer = [];
        this.prevBufferLen = 0;
        this.opts = Object.assign({
            path: "/engine.io",
            agent: false,
            withCredentials: false,
            upgrade: true,
            timestampParam: "t",
            rememberUpgrade: false,
            rejectUnauthorized: true,
            perMessageDeflate: {
                threshold: 1024
            },
            transportOptions: {},
            closeOnBeforeunload: true
        }, opts);
        this.opts.path = this.opts.path.replace(/\/$/, "") + "/";
        if (typeof this.opts.query === "string") this.opts.query = _parseqsJs.decode(this.opts.query);
        // set on handshake
        this.id = null;
        this.upgrades = null;
        this.pingInterval = null;
        this.pingTimeout = null;
        // set on heartbeat
        this.pingTimeoutTimer = null;
        if (typeof addEventListener === "function") {
            if (this.opts.closeOnBeforeunload) // Firefox closes the connection when the "beforeunload" event is emitted but not Chrome. This event listener
            // ensures every browser behaves the same (no "disconnect" event at the Socket.IO level when the page is
            // closed/reloaded)
            addEventListener("beforeunload", ()=>{
                if (this.transport) {
                    // silently close the transport
                    this.transport.removeAllListeners();
                    this.transport.close();
                }
            }, false);
            if (this.hostname !== "localhost") {
                this.offlineEventListener = ()=>{
                    this.onClose("transport close", {
                        description: "network connection lost"
                    });
                };
                addEventListener("offline", this.offlineEventListener, false);
            }
        }
        this.open();
    }
    /**
     * Creates transport of the given type.
     *
     * @param {String} transport name
     * @return {Transport}
     * @api private
     */ createTransport(name) {
        const query = Object.assign({}, this.opts.query);
        // append engine.io protocol identifier
        query.EIO = _engineIoParser.protocol;
        // transport name
        query.transport = name;
        // session id if we already have one
        if (this.id) query.sid = this.id;
        const opts = Object.assign({}, this.opts.transportOptions[name], this.opts, {
            query,
            socket: this,
            hostname: this.hostname,
            secure: this.secure,
            port: this.port
        });
        return new _indexJs.transports[name](opts);
    }
    /**
     * Initializes transport to use and starts probe.
     *
     * @api private
     */ open() {
        let transport;
        if (this.opts.rememberUpgrade && Socket.priorWebsocketSuccess && this.transports.indexOf("websocket") !== -1) transport = "websocket";
        else if (0 === this.transports.length) {
            // Emit error on next tick so it can be listened to
            this.setTimeoutFn(()=>{
                this.emitReserved("error", "No transports available");
            }, 0);
            return;
        } else transport = this.transports[0];
        this.readyState = "opening";
        // Retry with the next transport if the transport is disabled (jsonp: false)
        try {
            transport = this.createTransport(transport);
        } catch (e) {
            this.transports.shift();
            this.open();
            return;
        }
        transport.open();
        this.setTransport(transport);
    }
    /**
     * Sets the current transport. Disables the existing one (if any).
     *
     * @api private
     */ setTransport(transport) {
        if (this.transport) this.transport.removeAllListeners();
        // set up transport
        this.transport = transport;
        // set up transport listeners
        transport.on("drain", this.onDrain.bind(this)).on("packet", this.onPacket.bind(this)).on("error", this.onError.bind(this)).on("close", (reason)=>this.onClose("transport close", reason)
        );
    }
    /**
     * Probes a transport.
     *
     * @param {String} transport name
     * @api private
     */ probe(name) {
        let transport = this.createTransport(name);
        let failed = false;
        Socket.priorWebsocketSuccess = false;
        const onTransportOpen = ()=>{
            if (failed) return;
            transport.send([
                {
                    type: "ping",
                    data: "probe"
                }
            ]);
            transport.once("packet", (msg)=>{
                if (failed) return;
                if ("pong" === msg.type && "probe" === msg.data) {
                    this.upgrading = true;
                    this.emitReserved("upgrading", transport);
                    if (!transport) return;
                    Socket.priorWebsocketSuccess = "websocket" === transport.name;
                    this.transport.pause(()=>{
                        if (failed) return;
                        if ("closed" === this.readyState) return;
                        cleanup();
                        this.setTransport(transport);
                        transport.send([
                            {
                                type: "upgrade"
                            }
                        ]);
                        this.emitReserved("upgrade", transport);
                        transport = null;
                        this.upgrading = false;
                        this.flush();
                    });
                } else {
                    const err = new Error("probe error");
                    // @ts-ignore
                    err.transport = transport.name;
                    this.emitReserved("upgradeError", err);
                }
            });
        };
        function freezeTransport() {
            if (failed) return;
            // Any callback called by transport should be ignored since now
            failed = true;
            cleanup();
            transport.close();
            transport = null;
        }
        // Handle any error that happens while probing
        const onerror = (err)=>{
            const error = new Error("probe error: " + err);
            // @ts-ignore
            error.transport = transport.name;
            freezeTransport();
            this.emitReserved("upgradeError", error);
        };
        function onTransportClose() {
            onerror("transport closed");
        }
        // When the socket is closed while we're probing
        function onclose() {
            onerror("socket closed");
        }
        // When the socket is upgraded while we're probing
        function onupgrade(to) {
            if (transport && to.name !== transport.name) freezeTransport();
        }
        // Remove all listeners on the transport and on self
        const cleanup = ()=>{
            transport.removeListener("open", onTransportOpen);
            transport.removeListener("error", onerror);
            transport.removeListener("close", onTransportClose);
            this.off("close", onclose);
            this.off("upgrading", onupgrade);
        };
        transport.once("open", onTransportOpen);
        transport.once("error", onerror);
        transport.once("close", onTransportClose);
        this.once("close", onclose);
        this.once("upgrading", onupgrade);
        transport.open();
    }
    /**
     * Called when connection is deemed open.
     *
     * @api private
     */ onOpen() {
        this.readyState = "open";
        Socket.priorWebsocketSuccess = "websocket" === this.transport.name;
        this.emitReserved("open");
        this.flush();
        // we check for `readyState` in case an `open`
        // listener already closed the socket
        if ("open" === this.readyState && this.opts.upgrade && this.transport.pause) {
            let i = 0;
            const l = this.upgrades.length;
            for(; i < l; i++)this.probe(this.upgrades[i]);
        }
    }
    /**
     * Handles a packet.
     *
     * @api private
     */ onPacket(packet) {
        if ("opening" === this.readyState || "open" === this.readyState || "closing" === this.readyState) {
            this.emitReserved("packet", packet);
            // Socket is live - any packet counts
            this.emitReserved("heartbeat");
            switch(packet.type){
                case "open":
                    this.onHandshake(JSON.parse(packet.data));
                    break;
                case "ping":
                    this.resetPingTimeout();
                    this.sendPacket("pong");
                    this.emitReserved("ping");
                    this.emitReserved("pong");
                    break;
                case "error":
                    const err = new Error("server error");
                    // @ts-ignore
                    err.code = packet.data;
                    this.onError(err);
                    break;
                case "message":
                    this.emitReserved("data", packet.data);
                    this.emitReserved("message", packet.data);
                    break;
            }
        }
    }
    /**
     * Called upon handshake completion.
     *
     * @param {Object} data - handshake obj
     * @api private
     */ onHandshake(data) {
        this.emitReserved("handshake", data);
        this.id = data.sid;
        this.transport.query.sid = data.sid;
        this.upgrades = this.filterUpgrades(data.upgrades);
        this.pingInterval = data.pingInterval;
        this.pingTimeout = data.pingTimeout;
        this.maxPayload = data.maxPayload;
        this.onOpen();
        // In case open handler closes socket
        if ("closed" === this.readyState) return;
        this.resetPingTimeout();
    }
    /**
     * Sets and resets ping timeout timer based on server pings.
     *
     * @api private
     */ resetPingTimeout() {
        this.clearTimeoutFn(this.pingTimeoutTimer);
        this.pingTimeoutTimer = this.setTimeoutFn(()=>{
            this.onClose("ping timeout");
        }, this.pingInterval + this.pingTimeout);
        if (this.opts.autoUnref) this.pingTimeoutTimer.unref();
    }
    /**
     * Called on `drain` event
     *
     * @api private
     */ onDrain() {
        this.writeBuffer.splice(0, this.prevBufferLen);
        // setting prevBufferLen = 0 is very important
        // for example, when upgrading, upgrade packet is sent over,
        // and a nonzero prevBufferLen could cause problems on `drain`
        this.prevBufferLen = 0;
        if (0 === this.writeBuffer.length) this.emitReserved("drain");
        else this.flush();
    }
    /**
     * Flush write buffers.
     *
     * @api private
     */ flush() {
        if ("closed" !== this.readyState && this.transport.writable && !this.upgrading && this.writeBuffer.length) {
            const packets = this.getWritablePackets();
            this.transport.send(packets);
            // keep track of current length of writeBuffer
            // splice writeBuffer and callbackBuffer on `drain`
            this.prevBufferLen = packets.length;
            this.emitReserved("flush");
        }
    }
    /**
     * Ensure the encoded size of the writeBuffer is below the maxPayload value sent by the server (only for HTTP
     * long-polling)
     *
     * @private
     */ getWritablePackets() {
        const shouldCheckPayloadSize = this.maxPayload && this.transport.name === "polling" && this.writeBuffer.length > 1;
        if (!shouldCheckPayloadSize) return this.writeBuffer;
        let payloadSize = 1; // first packet type
        for(let i = 0; i < this.writeBuffer.length; i++){
            const data = this.writeBuffer[i].data;
            if (data) payloadSize += _utilJs.byteLength(data);
            if (i > 0 && payloadSize > this.maxPayload) return this.writeBuffer.slice(0, i);
            payloadSize += 2; // separator + packet type
        }
        return this.writeBuffer;
    }
    /**
     * Sends a message.
     *
     * @param {String} message.
     * @param {Function} callback function.
     * @param {Object} options.
     * @return {Socket} for chaining.
     * @api public
     */ write(msg, options, fn) {
        this.sendPacket("message", msg, options, fn);
        return this;
    }
    send(msg, options, fn) {
        this.sendPacket("message", msg, options, fn);
        return this;
    }
    /**
     * Sends a packet.
     *
     * @param {String} packet type.
     * @param {String} data.
     * @param {Object} options.
     * @param {Function} callback function.
     * @api private
     */ sendPacket(type, data, options, fn) {
        if ("function" === typeof data) {
            fn = data;
            data = undefined;
        }
        if ("function" === typeof options) {
            fn = options;
            options = null;
        }
        if ("closing" === this.readyState || "closed" === this.readyState) return;
        options = options || {};
        options.compress = false !== options.compress;
        const packet = {
            type: type,
            data: data,
            options: options
        };
        this.emitReserved("packetCreate", packet);
        this.writeBuffer.push(packet);
        if (fn) this.once("flush", fn);
        this.flush();
    }
    /**
     * Closes the connection.
     *
     * @api public
     */ close() {
        const close = ()=>{
            this.onClose("forced close");
            this.transport.close();
        };
        const cleanupAndClose = ()=>{
            this.off("upgrade", cleanupAndClose);
            this.off("upgradeError", cleanupAndClose);
            close();
        };
        const waitForUpgrade = ()=>{
            // wait for upgrade to finish since we can't send packets while pausing a transport
            this.once("upgrade", cleanupAndClose);
            this.once("upgradeError", cleanupAndClose);
        };
        if ("opening" === this.readyState || "open" === this.readyState) {
            this.readyState = "closing";
            if (this.writeBuffer.length) this.once("drain", ()=>{
                if (this.upgrading) waitForUpgrade();
                else close();
            });
            else if (this.upgrading) waitForUpgrade();
            else close();
        }
        return this;
    }
    /**
     * Called upon transport error
     *
     * @api private
     */ onError(err) {
        Socket.priorWebsocketSuccess = false;
        this.emitReserved("error", err);
        this.onClose("transport error", err);
    }
    /**
     * Called upon transport close.
     *
     * @api private
     */ onClose(reason, description) {
        if ("opening" === this.readyState || "open" === this.readyState || "closing" === this.readyState) {
            // clear timers
            this.clearTimeoutFn(this.pingTimeoutTimer);
            // stop event from firing again for transport
            this.transport.removeAllListeners("close");
            // ensure transport won't stay open
            this.transport.close();
            // ignore further transport communication
            this.transport.removeAllListeners();
            if (typeof removeEventListener === "function") removeEventListener("offline", this.offlineEventListener, false);
            // set ready state
            this.readyState = "closed";
            // clear session id
            this.id = null;
            // emit close event
            this.emitReserved("close", reason, description);
            // clean buffers after, so users can still
            // grab the buffers on `close` event
            this.writeBuffer = [];
            this.prevBufferLen = 0;
        }
    }
    /**
     * Filters upgrades, returning only those matching client transports.
     *
     * @param {Array} server upgrades
     * @api private
     *
     */ filterUpgrades(upgrades) {
        const filteredUpgrades = [];
        let i = 0;
        const j = upgrades.length;
        for(; i < j; i++)if (~this.transports.indexOf(upgrades[i])) filteredUpgrades.push(upgrades[i]);
        return filteredUpgrades;
    }
}
Socket.protocol = _engineIoParser.protocol;

},{"./transports/index.js":"2neV7","./util.js":"d7eyH","./contrib/parseqs.js":"aFNEN","./contrib/parseuri.js":"5RFyz","@socket.io/component-emitter":"dzQbR","engine.io-parser":"2SHiP","@parcel/transformer-js/src/esmodule-helpers.js":"gkKU3"}],"2neV7":[function(require,module,exports) {
var parcelHelpers = require("@parcel/transformer-js/src/esmodule-helpers.js");
parcelHelpers.defineInteropFlag(exports);
parcelHelpers.export(exports, "transports", ()=>transports
);
var _pollingJs = require("./polling.js");
var _websocketJs = require("./websocket.js");
const transports = {
    websocket: _websocketJs.WS,
    polling: _pollingJs.Polling
};

},{"./polling.js":"HgHsi","./websocket.js":"8y2e2","@parcel/transformer-js/src/esmodule-helpers.js":"gkKU3"}],"HgHsi":[function(require,module,exports) {
var parcelHelpers = require("@parcel/transformer-js/src/esmodule-helpers.js");
parcelHelpers.defineInteropFlag(exports);
parcelHelpers.export(exports, "Polling", ()=>Polling
);
parcelHelpers.export(exports, "Request", ()=>Request
);
var _transportJs = require("../transport.js");
var _yeastJs = require("../contrib/yeast.js");
var _parseqsJs = require("../contrib/parseqs.js");
var _engineIoParser = require("engine.io-parser");
var _xmlhttprequestJs = require("./xmlhttprequest.js");
var _componentEmitter = require("@socket.io/component-emitter");
var _utilJs = require("../util.js");
var _globalThisJs = require("../globalThis.js");
function empty() {}
const hasXHR2 = function() {
    const xhr = new _xmlhttprequestJs.XHR({
        xdomain: false
    });
    return null != xhr.responseType;
}();
class Polling extends _transportJs.Transport {
    /**
     * XHR Polling constructor.
     *
     * @param {Object} opts
     * @api public
     */ constructor(opts){
        super(opts);
        this.polling = false;
        if (typeof location !== "undefined") {
            const isSSL = "https:" === location.protocol;
            let port = location.port;
            // some user agents have empty `location.port`
            if (!port) port = isSSL ? "443" : "80";
            this.xd = typeof location !== "undefined" && opts.hostname !== location.hostname || port !== opts.port;
            this.xs = opts.secure !== isSSL;
        }
        /**
         * XHR supports binary
         */ const forceBase64 = opts && opts.forceBase64;
        this.supportsBinary = hasXHR2 && !forceBase64;
    }
    /**
     * Transport name.
     */ get name() {
        return "polling";
    }
    /**
     * Opens the socket (triggers polling). We write a PING message to determine
     * when the transport is open.
     *
     * @api private
     */ doOpen() {
        this.poll();
    }
    /**
     * Pauses polling.
     *
     * @param {Function} callback upon buffers are flushed and transport is paused
     * @api private
     */ pause(onPause) {
        this.readyState = "pausing";
        const pause = ()=>{
            this.readyState = "paused";
            onPause();
        };
        if (this.polling || !this.writable) {
            let total = 0;
            if (this.polling) {
                total++;
                this.once("pollComplete", function() {
                    --total || pause();
                });
            }
            if (!this.writable) {
                total++;
                this.once("drain", function() {
                    --total || pause();
                });
            }
        } else pause();
    }
    /**
     * Starts polling cycle.
     *
     * @api public
     */ poll() {
        this.polling = true;
        this.doPoll();
        this.emitReserved("poll");
    }
    /**
     * Overloads onData to detect payloads.
     *
     * @api private
     */ onData(data) {
        const callback = (packet)=>{
            // if its the first message we consider the transport open
            if ("opening" === this.readyState && packet.type === "open") this.onOpen();
            // if its a close packet, we close the ongoing requests
            if ("close" === packet.type) {
                this.onClose({
                    description: "transport closed by the server"
                });
                return false;
            }
            // otherwise bypass onData and handle the message
            this.onPacket(packet);
        };
        // decode payload
        _engineIoParser.decodePayload(data, this.socket.binaryType).forEach(callback);
        // if an event did not trigger closing
        if ("closed" !== this.readyState) {
            // if we got data we're not polling
            this.polling = false;
            this.emitReserved("pollComplete");
            if ("open" === this.readyState) this.poll();
        }
    }
    /**
     * For polling, send a close packet.
     *
     * @api private
     */ doClose() {
        const close = ()=>{
            this.write([
                {
                    type: "close"
                }
            ]);
        };
        if ("open" === this.readyState) close();
        else // in case we're trying to close while
        // handshaking is in progress (GH-164)
        this.once("open", close);
    }
    /**
     * Writes a packets payload.
     *
     * @param {Array} data packets
     * @param {Function} drain callback
     * @api private
     */ write(packets) {
        this.writable = false;
        _engineIoParser.encodePayload(packets, (data)=>{
            this.doWrite(data, ()=>{
                this.writable = true;
                this.emitReserved("drain");
            });
        });
    }
    /**
     * Generates uri for connection.
     *
     * @api private
     */ uri() {
        let query = this.query || {};
        const schema = this.opts.secure ? "https" : "http";
        let port = "";
        // cache busting is forced
        if (false !== this.opts.timestampRequests) query[this.opts.timestampParam] = _yeastJs.yeast();
        if (!this.supportsBinary && !query.sid) query.b64 = 1;
        // avoid port if default for schema
        if (this.opts.port && ("https" === schema && Number(this.opts.port) !== 443 || "http" === schema && Number(this.opts.port) !== 80)) port = ":" + this.opts.port;
        const encodedQuery = _parseqsJs.encode(query);
        const ipv6 = this.opts.hostname.indexOf(":") !== -1;
        return schema + "://" + (ipv6 ? "[" + this.opts.hostname + "]" : this.opts.hostname) + port + this.opts.path + (encodedQuery.length ? "?" + encodedQuery : "");
    }
    /**
     * Creates a request.
     *
     * @param {String} method
     * @api private
     */ request(opts = {}) {
        Object.assign(opts, {
            xd: this.xd,
            xs: this.xs
        }, this.opts);
        return new Request(this.uri(), opts);
    }
    /**
     * Sends data.
     *
     * @param {String} data to send.
     * @param {Function} called upon flush.
     * @api private
     */ doWrite(data, fn) {
        const req = this.request({
            method: "POST",
            data: data
        });
        req.on("success", fn);
        req.on("error", (xhrStatus, context)=>{
            this.onError("xhr post error", xhrStatus, context);
        });
    }
    /**
     * Starts a poll cycle.
     *
     * @api private
     */ doPoll() {
        const req = this.request();
        req.on("data", this.onData.bind(this));
        req.on("error", (xhrStatus, context)=>{
            this.onError("xhr poll error", xhrStatus, context);
        });
        this.pollXhr = req;
    }
}
class Request extends _componentEmitter.Emitter {
    /**
     * Request constructor
     *
     * @param {Object} options
     * @api public
     */ constructor(uri, opts){
        super();
        _utilJs.installTimerFunctions(this, opts);
        this.opts = opts;
        this.method = opts.method || "GET";
        this.uri = uri;
        this.async = false !== opts.async;
        this.data = undefined !== opts.data ? opts.data : null;
        this.create();
    }
    /**
     * Creates the XHR object and sends the request.
     *
     * @api private
     */ create() {
        const opts = _utilJs.pick(this.opts, "agent", "pfx", "key", "passphrase", "cert", "ca", "ciphers", "rejectUnauthorized", "autoUnref");
        opts.xdomain = !!this.opts.xd;
        opts.xscheme = !!this.opts.xs;
        const xhr = this.xhr = new _xmlhttprequestJs.XHR(opts);
        try {
            xhr.open(this.method, this.uri, this.async);
            try {
                if (this.opts.extraHeaders) {
                    xhr.setDisableHeaderCheck && xhr.setDisableHeaderCheck(true);
                    for(let i in this.opts.extraHeaders)if (this.opts.extraHeaders.hasOwnProperty(i)) xhr.setRequestHeader(i, this.opts.extraHeaders[i]);
                }
            } catch (e) {}
            if ("POST" === this.method) try {
                xhr.setRequestHeader("Content-type", "text/plain;charset=UTF-8");
            } catch (e1) {}
            try {
                xhr.setRequestHeader("Accept", "*/*");
            } catch (e2) {}
            // ie6 check
            if ("withCredentials" in xhr) xhr.withCredentials = this.opts.withCredentials;
            if (this.opts.requestTimeout) xhr.timeout = this.opts.requestTimeout;
            xhr.onreadystatechange = ()=>{
                if (4 !== xhr.readyState) return;
                if (200 === xhr.status || 1223 === xhr.status) this.onLoad();
                else // make sure the `error` event handler that's user-set
                // does not throw in the same tick and gets caught here
                this.setTimeoutFn(()=>{
                    this.onError(typeof xhr.status === "number" ? xhr.status : 0);
                }, 0);
            };
            xhr.send(this.data);
        } catch (e) {
            // Need to defer since .create() is called directly from the constructor
            // and thus the 'error' event can only be only bound *after* this exception
            // occurs.  Therefore, also, we cannot throw here at all.
            this.setTimeoutFn(()=>{
                this.onError(e);
            }, 0);
            return;
        }
        if (typeof document !== "undefined") {
            this.index = Request.requestsCount++;
            Request.requests[this.index] = this;
        }
    }
    /**
     * Called upon error.
     *
     * @api private
     */ onError(err) {
        this.emitReserved("error", err, this.xhr);
        this.cleanup(true);
    }
    /**
     * Cleans up house.
     *
     * @api private
     */ cleanup(fromError) {
        if ("undefined" === typeof this.xhr || null === this.xhr) return;
        this.xhr.onreadystatechange = empty;
        if (fromError) try {
            this.xhr.abort();
        } catch (e) {}
        if (typeof document !== "undefined") delete Request.requests[this.index];
        this.xhr = null;
    }
    /**
     * Called upon load.
     *
     * @api private
     */ onLoad() {
        const data = this.xhr.responseText;
        if (data !== null) {
            this.emitReserved("data", data);
            this.emitReserved("success");
            this.cleanup();
        }
    }
    /**
     * Aborts the request.
     *
     * @api public
     */ abort() {
        this.cleanup();
    }
}
Request.requestsCount = 0;
Request.requests = {};
/**
 * Aborts pending requests when unloading the window. This is needed to prevent
 * memory leaks (e.g. when using IE) and to ensure that no spurious error is
 * emitted.
 */ if (typeof document !== "undefined") {
    // @ts-ignore
    if (typeof attachEvent === "function") // @ts-ignore
    attachEvent("onunload", unloadHandler);
    else if (typeof addEventListener === "function") {
        const terminationEvent = "onpagehide" in _globalThisJs.globalThisShim ? "pagehide" : "unload";
        addEventListener(terminationEvent, unloadHandler, false);
    }
}
function unloadHandler() {
    for(let i in Request.requests)if (Request.requests.hasOwnProperty(i)) Request.requests[i].abort();
}

},{"../transport.js":"kwKKC","../contrib/yeast.js":"5cPiI","../contrib/parseqs.js":"aFNEN","engine.io-parser":"2SHiP","./xmlhttprequest.js":"gG0MI","@socket.io/component-emitter":"dzQbR","../util.js":"d7eyH","../globalThis.js":"7zbHc","@parcel/transformer-js/src/esmodule-helpers.js":"gkKU3"}],"kwKKC":[function(require,module,exports) {
var parcelHelpers = require("@parcel/transformer-js/src/esmodule-helpers.js");
parcelHelpers.defineInteropFlag(exports);
parcelHelpers.export(exports, "Transport", ()=>Transport
);
var _engineIoParser = require("engine.io-parser");
var _componentEmitter = require("@socket.io/component-emitter");
var _utilJs = require("./util.js");
class TransportError extends Error {
    constructor(reason, description, context){
        super(reason);
        this.description = description;
        this.context = context;
        this.type = "TransportError";
    }
}
class Transport extends _componentEmitter.Emitter {
    /**
     * Transport abstract constructor.
     *
     * @param {Object} options.
     * @api private
     */ constructor(opts){
        super();
        this.writable = false;
        _utilJs.installTimerFunctions(this, opts);
        this.opts = opts;
        this.query = opts.query;
        this.readyState = "";
        this.socket = opts.socket;
    }
    /**
     * Emits an error.
     *
     * @param {String} reason
     * @param description
     * @param context - the error context
     * @return {Transport} for chaining
     * @api protected
     */ onError(reason, description, context) {
        super.emitReserved("error", new TransportError(reason, description, context));
        return this;
    }
    /**
     * Opens the transport.
     *
     * @api public
     */ open() {
        if ("closed" === this.readyState || "" === this.readyState) {
            this.readyState = "opening";
            this.doOpen();
        }
        return this;
    }
    /**
     * Closes the transport.
     *
     * @api public
     */ close() {
        if ("opening" === this.readyState || "open" === this.readyState) {
            this.doClose();
            this.onClose();
        }
        return this;
    }
    /**
     * Sends multiple packets.
     *
     * @param {Array} packets
     * @api public
     */ send(packets) {
        if ("open" === this.readyState) this.write(packets);
    }
    /**
     * Called upon open
     *
     * @api protected
     */ onOpen() {
        this.readyState = "open";
        this.writable = true;
        super.emitReserved("open");
    }
    /**
     * Called with data.
     *
     * @param {String} data
     * @api protected
     */ onData(data) {
        const packet = _engineIoParser.decodePacket(data, this.socket.binaryType);
        this.onPacket(packet);
    }
    /**
     * Called with a decoded packet.
     *
     * @api protected
     */ onPacket(packet) {
        super.emitReserved("packet", packet);
    }
    /**
     * Called upon close.
     *
     * @api protected
     */ onClose(details) {
        this.readyState = "closed";
        super.emitReserved("close", details);
    }
}

},{"engine.io-parser":"2SHiP","@socket.io/component-emitter":"dzQbR","./util.js":"d7eyH","@parcel/transformer-js/src/esmodule-helpers.js":"gkKU3"}],"2SHiP":[function(require,module,exports) {
var parcelHelpers = require("@parcel/transformer-js/src/esmodule-helpers.js");
parcelHelpers.defineInteropFlag(exports);
parcelHelpers.export(exports, "protocol", ()=>protocol
);
parcelHelpers.export(exports, "encodePacket", ()=>_encodePacketJsDefault.default
);
parcelHelpers.export(exports, "encodePayload", ()=>encodePayload
);
parcelHelpers.export(exports, "decodePacket", ()=>_decodePacketJsDefault.default
);
parcelHelpers.export(exports, "decodePayload", ()=>decodePayload
);
var _encodePacketJs = require("./encodePacket.js");
var _encodePacketJsDefault = parcelHelpers.interopDefault(_encodePacketJs);
var _decodePacketJs = require("./decodePacket.js");
var _decodePacketJsDefault = parcelHelpers.interopDefault(_decodePacketJs);
const SEPARATOR = String.fromCharCode(30); // see https://en.wikipedia.org/wiki/Delimiter#ASCII_delimited_text
const encodePayload = (packets, callback)=>{
    // some packets may be added to the array while encoding, so the initial length must be saved
    const length = packets.length;
    const encodedPackets = new Array(length);
    let count = 0;
    packets.forEach((packet, i)=>{
        // force base64 encoding for binary packets
        _encodePacketJsDefault.default(packet, false, (encodedPacket)=>{
            encodedPackets[i] = encodedPacket;
            if (++count === length) callback(encodedPackets.join(SEPARATOR));
        });
    });
};
const decodePayload = (encodedPayload, binaryType)=>{
    const encodedPackets = encodedPayload.split(SEPARATOR);
    const packets = [];
    for(let i = 0; i < encodedPackets.length; i++){
        const decodedPacket = _decodePacketJsDefault.default(encodedPackets[i], binaryType);
        packets.push(decodedPacket);
        if (decodedPacket.type === "error") break;
    }
    return packets;
};
const protocol = 4;

},{"./encodePacket.js":"lUiyb","./decodePacket.js":"k0BCP","@parcel/transformer-js/src/esmodule-helpers.js":"gkKU3"}],"lUiyb":[function(require,module,exports) {
var parcelHelpers = require("@parcel/transformer-js/src/esmodule-helpers.js");
parcelHelpers.defineInteropFlag(exports);
var _commonsJs = require("./commons.js");
const withNativeBlob = typeof Blob === "function" || typeof Blob !== "undefined" && Object.prototype.toString.call(Blob) === "[object BlobConstructor]";
const withNativeArrayBuffer = typeof ArrayBuffer === "function";
// ArrayBuffer.isView method is not defined in IE10
const isView = (obj)=>{
    return typeof ArrayBuffer.isView === "function" ? ArrayBuffer.isView(obj) : obj && obj.buffer instanceof ArrayBuffer;
};
const encodePacket = ({ type , data  }, supportsBinary, callback)=>{
    if (withNativeBlob && data instanceof Blob) {
        if (supportsBinary) return callback(data);
        else return encodeBlobAsBase64(data, callback);
    } else if (withNativeArrayBuffer && (data instanceof ArrayBuffer || isView(data))) {
        if (supportsBinary) return callback(data);
        else return encodeBlobAsBase64(new Blob([
            data
        ]), callback);
    }
    // plain string
    return callback(_commonsJs.PACKET_TYPES[type] + (data || ""));
};
const encodeBlobAsBase64 = (data, callback)=>{
    const fileReader = new FileReader();
    fileReader.onload = function() {
        const content = fileReader.result.split(",")[1];
        callback("b" + content);
    };
    return fileReader.readAsDataURL(data);
};
exports.default = encodePacket;

},{"./commons.js":"kLmJ7","@parcel/transformer-js/src/esmodule-helpers.js":"gkKU3"}],"kLmJ7":[function(require,module,exports) {
var parcelHelpers = require("@parcel/transformer-js/src/esmodule-helpers.js");
parcelHelpers.defineInteropFlag(exports);
parcelHelpers.export(exports, "PACKET_TYPES", ()=>PACKET_TYPES
);
parcelHelpers.export(exports, "PACKET_TYPES_REVERSE", ()=>PACKET_TYPES_REVERSE
);
parcelHelpers.export(exports, "ERROR_PACKET", ()=>ERROR_PACKET
);
const PACKET_TYPES = Object.create(null); // no Map = no polyfill
PACKET_TYPES["open"] = "0";
PACKET_TYPES["close"] = "1";
PACKET_TYPES["ping"] = "2";
PACKET_TYPES["pong"] = "3";
PACKET_TYPES["message"] = "4";
PACKET_TYPES["upgrade"] = "5";
PACKET_TYPES["noop"] = "6";
const PACKET_TYPES_REVERSE = Object.create(null);
Object.keys(PACKET_TYPES).forEach((key)=>{
    PACKET_TYPES_REVERSE[PACKET_TYPES[key]] = key;
});
const ERROR_PACKET = {
    type: "error",
    data: "parser error"
};

},{"@parcel/transformer-js/src/esmodule-helpers.js":"gkKU3"}],"k0BCP":[function(require,module,exports) {
var parcelHelpers = require("@parcel/transformer-js/src/esmodule-helpers.js");
parcelHelpers.defineInteropFlag(exports);
var _commonsJs = require("./commons.js");
var _base64ArraybufferJs = require("./contrib/base64-arraybuffer.js");
const withNativeArrayBuffer = typeof ArrayBuffer === "function";
const decodePacket = (encodedPacket, binaryType)=>{
    if (typeof encodedPacket !== "string") return {
        type: "message",
        data: mapBinary(encodedPacket, binaryType)
    };
    const type = encodedPacket.charAt(0);
    if (type === "b") return {
        type: "message",
        data: decodeBase64Packet(encodedPacket.substring(1), binaryType)
    };
    const packetType = _commonsJs.PACKET_TYPES_REVERSE[type];
    if (!packetType) return _commonsJs.ERROR_PACKET;
    return encodedPacket.length > 1 ? {
        type: _commonsJs.PACKET_TYPES_REVERSE[type],
        data: encodedPacket.substring(1)
    } : {
        type: _commonsJs.PACKET_TYPES_REVERSE[type]
    };
};
const decodeBase64Packet = (data, binaryType)=>{
    if (withNativeArrayBuffer) {
        const decoded = _base64ArraybufferJs.decode(data);
        return mapBinary(decoded, binaryType);
    } else return {
        base64: true,
        data
    }; // fallback for old browsers
};
const mapBinary = (data, binaryType)=>{
    switch(binaryType){
        case "blob":
            return data instanceof ArrayBuffer ? new Blob([
                data
            ]) : data;
        case "arraybuffer":
        default:
            return data; // assuming the data is already an ArrayBuffer
    }
};
exports.default = decodePacket;

},{"./commons.js":"kLmJ7","./contrib/base64-arraybuffer.js":"c3dDo","@parcel/transformer-js/src/esmodule-helpers.js":"gkKU3"}],"c3dDo":[function(require,module,exports) {
var parcelHelpers = require("@parcel/transformer-js/src/esmodule-helpers.js");
parcelHelpers.defineInteropFlag(exports);
parcelHelpers.export(exports, "encode", ()=>encode
);
parcelHelpers.export(exports, "decode", ()=>decode
);
const chars = 'ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789+/';
// Use a lookup table to find the index.
const lookup = typeof Uint8Array === 'undefined' ? [] : new Uint8Array(256);
for(let i = 0; i < chars.length; i++)lookup[chars.charCodeAt(i)] = i;
const encode = (arraybuffer)=>{
    let bytes = new Uint8Array(arraybuffer), i1, len = bytes.length, base64 = '';
    for(i1 = 0; i1 < len; i1 += 3){
        base64 += chars[bytes[i1] >> 2];
        base64 += chars[(bytes[i1] & 3) << 4 | bytes[i1 + 1] >> 4];
        base64 += chars[(bytes[i1 + 1] & 15) << 2 | bytes[i1 + 2] >> 6];
        base64 += chars[bytes[i1 + 2] & 63];
    }
    if (len % 3 === 2) base64 = base64.substring(0, base64.length - 1) + '=';
    else if (len % 3 === 1) base64 = base64.substring(0, base64.length - 2) + '==';
    return base64;
};
const decode = (base64)=>{
    let bufferLength = base64.length * 0.75, len = base64.length, i2, p = 0, encoded1, encoded2, encoded3, encoded4;
    if (base64[base64.length - 1] === '=') {
        bufferLength--;
        if (base64[base64.length - 2] === '=') bufferLength--;
    }
    const arraybuffer = new ArrayBuffer(bufferLength), bytes = new Uint8Array(arraybuffer);
    for(i2 = 0; i2 < len; i2 += 4){
        encoded1 = lookup[base64.charCodeAt(i2)];
        encoded2 = lookup[base64.charCodeAt(i2 + 1)];
        encoded3 = lookup[base64.charCodeAt(i2 + 2)];
        encoded4 = lookup[base64.charCodeAt(i2 + 3)];
        bytes[p++] = encoded1 << 2 | encoded2 >> 4;
        bytes[p++] = (encoded2 & 15) << 4 | encoded3 >> 2;
        bytes[p++] = (encoded3 & 3) << 6 | encoded4 & 63;
    }
    return arraybuffer;
};

},{"@parcel/transformer-js/src/esmodule-helpers.js":"gkKU3"}],"dzQbR":[function(require,module,exports) {
var parcelHelpers = require("@parcel/transformer-js/src/esmodule-helpers.js");
parcelHelpers.defineInteropFlag(exports);
/**
 * Initialize a new `Emitter`.
 *
 * @api public
 */ parcelHelpers.export(exports, "Emitter", ()=>Emitter
);
function Emitter(obj) {
    if (obj) return mixin(obj);
}
/**
 * Mixin the emitter properties.
 *
 * @param {Object} obj
 * @return {Object}
 * @api private
 */ function mixin(obj) {
    for(var key in Emitter.prototype)obj[key] = Emitter.prototype[key];
    return obj;
}
/**
 * Listen on the given `event` with `fn`.
 *
 * @param {String} event
 * @param {Function} fn
 * @return {Emitter}
 * @api public
 */ Emitter.prototype.on = Emitter.prototype.addEventListener = function(event, fn) {
    this._callbacks = this._callbacks || {};
    (this._callbacks['$' + event] = this._callbacks['$' + event] || []).push(fn);
    return this;
};
/**
 * Adds an `event` listener that will be invoked a single
 * time then automatically removed.
 *
 * @param {String} event
 * @param {Function} fn
 * @return {Emitter}
 * @api public
 */ Emitter.prototype.once = function(event, fn) {
    function on() {
        this.off(event, on);
        fn.apply(this, arguments);
    }
    on.fn = fn;
    this.on(event, on);
    return this;
};
/**
 * Remove the given callback for `event` or all
 * registered callbacks.
 *
 * @param {String} event
 * @param {Function} fn
 * @return {Emitter}
 * @api public
 */ Emitter.prototype.off = Emitter.prototype.removeListener = Emitter.prototype.removeAllListeners = Emitter.prototype.removeEventListener = function(event, fn) {
    this._callbacks = this._callbacks || {};
    // all
    if (0 == arguments.length) {
        this._callbacks = {};
        return this;
    }
    // specific event
    var callbacks = this._callbacks['$' + event];
    if (!callbacks) return this;
    // remove all handlers
    if (1 == arguments.length) {
        delete this._callbacks['$' + event];
        return this;
    }
    // remove specific handler
    var cb;
    for(var i = 0; i < callbacks.length; i++){
        cb = callbacks[i];
        if (cb === fn || cb.fn === fn) {
            callbacks.splice(i, 1);
            break;
        }
    }
    // Remove event specific arrays for event types that no
    // one is subscribed for to avoid memory leak.
    if (callbacks.length === 0) delete this._callbacks['$' + event];
    return this;
};
/**
 * Emit `event` with the given args.
 *
 * @param {String} event
 * @param {Mixed} ...
 * @return {Emitter}
 */ Emitter.prototype.emit = function(event) {
    this._callbacks = this._callbacks || {};
    var args = new Array(arguments.length - 1), callbacks = this._callbacks['$' + event];
    for(var i = 1; i < arguments.length; i++)args[i - 1] = arguments[i];
    if (callbacks) {
        callbacks = callbacks.slice(0);
        for(var i = 0, len = callbacks.length; i < len; ++i)callbacks[i].apply(this, args);
    }
    return this;
};
// alias used for reserved events (protected method)
Emitter.prototype.emitReserved = Emitter.prototype.emit;
/**
 * Return array of callbacks for `event`.
 *
 * @param {String} event
 * @return {Array}
 * @api public
 */ Emitter.prototype.listeners = function(event) {
    this._callbacks = this._callbacks || {};
    return this._callbacks['$' + event] || [];
};
/**
 * Check if this emitter has `event` handlers.
 *
 * @param {String} event
 * @return {Boolean}
 * @api public
 */ Emitter.prototype.hasListeners = function(event) {
    return !!this.listeners(event).length;
};

},{"@parcel/transformer-js/src/esmodule-helpers.js":"gkKU3"}],"d7eyH":[function(require,module,exports) {
var parcelHelpers = require("@parcel/transformer-js/src/esmodule-helpers.js");
parcelHelpers.defineInteropFlag(exports);
parcelHelpers.export(exports, "pick", ()=>pick
);
parcelHelpers.export(exports, "installTimerFunctions", ()=>installTimerFunctions
);
// we could also have used `new Blob([obj]).size`, but it isn't supported in IE9
parcelHelpers.export(exports, "byteLength", ()=>byteLength
);
var _globalThisJs = require("./globalThis.js");
function pick(obj, ...attr) {
    return attr.reduce((acc, k)=>{
        if (obj.hasOwnProperty(k)) acc[k] = obj[k];
        return acc;
    }, {});
}
// Keep a reference to the real timeout functions so they can be used when overridden
const NATIVE_SET_TIMEOUT = setTimeout;
const NATIVE_CLEAR_TIMEOUT = clearTimeout;
function installTimerFunctions(obj, opts) {
    if (opts.useNativeTimers) {
        obj.setTimeoutFn = NATIVE_SET_TIMEOUT.bind(_globalThisJs.globalThisShim);
        obj.clearTimeoutFn = NATIVE_CLEAR_TIMEOUT.bind(_globalThisJs.globalThisShim);
    } else {
        obj.setTimeoutFn = setTimeout.bind(_globalThisJs.globalThisShim);
        obj.clearTimeoutFn = clearTimeout.bind(_globalThisJs.globalThisShim);
    }
}
// base64 encoded buffers are about 33% bigger (https://en.wikipedia.org/wiki/Base64)
const BASE64_OVERHEAD = 1.33;
function byteLength(obj) {
    if (typeof obj === "string") return utf8Length(obj);
    // arraybuffer or blob
    return Math.ceil((obj.byteLength || obj.size) * BASE64_OVERHEAD);
}
function utf8Length(str) {
    let c = 0, length = 0;
    for(let i = 0, l = str.length; i < l; i++){
        c = str.charCodeAt(i);
        if (c < 0x80) length += 1;
        else if (c < 0x800) length += 2;
        else if (c < 0xd800 || c >= 0xe000) length += 3;
        else {
            i++;
            length += 4;
        }
    }
    return length;
}

},{"./globalThis.js":"7zbHc","@parcel/transformer-js/src/esmodule-helpers.js":"gkKU3"}],"7zbHc":[function(require,module,exports) {
var parcelHelpers = require("@parcel/transformer-js/src/esmodule-helpers.js");
parcelHelpers.defineInteropFlag(exports);
parcelHelpers.export(exports, "globalThisShim", ()=>globalThisShim
);
const globalThisShim = (()=>{
    if (typeof self !== "undefined") return self;
    else if (typeof window !== "undefined") return window;
    else return Function("return this")();
})();

},{"@parcel/transformer-js/src/esmodule-helpers.js":"gkKU3"}],"5cPiI":[function(require,module,exports) {
var parcelHelpers = require("@parcel/transformer-js/src/esmodule-helpers.js");
parcelHelpers.defineInteropFlag(exports);
/**
 * Return a string representing the specified number.
 *
 * @param {Number} num The number to convert.
 * @returns {String} The string representation of the number.
 * @api public
 */ parcelHelpers.export(exports, "encode", ()=>encode
);
/**
 * Return the integer value specified by the given string.
 *
 * @param {String} str The string to convert.
 * @returns {Number} The integer value represented by the string.
 * @api public
 */ parcelHelpers.export(exports, "decode", ()=>decode
);
/**
 * Yeast: A tiny growing id generator.
 *
 * @returns {String} A unique id.
 * @api public
 */ parcelHelpers.export(exports, "yeast", ()=>yeast
);
// imported from https://github.com/unshiftio/yeast
'use strict';
const alphabet = '0123456789ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz-_'.split(''), length = 64, map = {};
let seed = 0, i = 0, prev;
function encode(num) {
    let encoded = '';
    do {
        encoded = alphabet[num % length] + encoded;
        num = Math.floor(num / length);
    }while (num > 0)
    return encoded;
}
function decode(str) {
    let decoded = 0;
    for(i = 0; i < str.length; i++)decoded = decoded * length + map[str.charAt(i)];
    return decoded;
}
function yeast() {
    const now = encode(+new Date());
    if (now !== prev) return seed = 0, prev = now;
    return now + '.' + encode(seed++);
}
//
// Map each character to its index.
//
for(; i < length; i++)map[alphabet[i]] = i;

},{"@parcel/transformer-js/src/esmodule-helpers.js":"gkKU3"}],"aFNEN":[function(require,module,exports) {
var parcelHelpers = require("@parcel/transformer-js/src/esmodule-helpers.js");
parcelHelpers.defineInteropFlag(exports);
// imported from https://github.com/galkn/querystring
/**
 * Compiles a querystring
 * Returns string representation of the object
 *
 * @param {Object}
 * @api private
 */ parcelHelpers.export(exports, "encode", ()=>encode
);
/**
 * Parses a simple querystring into an object
 *
 * @param {String} qs
 * @api private
 */ parcelHelpers.export(exports, "decode", ()=>decode
);
function encode(obj) {
    let str = '';
    for(let i in obj)if (obj.hasOwnProperty(i)) {
        if (str.length) str += '&';
        str += encodeURIComponent(i) + '=' + encodeURIComponent(obj[i]);
    }
    return str;
}
function decode(qs) {
    let qry = {};
    let pairs = qs.split('&');
    for(let i = 0, l = pairs.length; i < l; i++){
        let pair = pairs[i].split('=');
        qry[decodeURIComponent(pair[0])] = decodeURIComponent(pair[1]);
    }
    return qry;
}

},{"@parcel/transformer-js/src/esmodule-helpers.js":"gkKU3"}],"gG0MI":[function(require,module,exports) {
var parcelHelpers = require("@parcel/transformer-js/src/esmodule-helpers.js");
parcelHelpers.defineInteropFlag(exports);
parcelHelpers.export(exports, "XHR", ()=>XHR
);
// browser shim for xmlhttprequest module
var _hasCorsJs = require("../contrib/has-cors.js");
var _globalThisJs = require("../globalThis.js");
function XHR(opts) {
    const xdomain = opts.xdomain;
    // XMLHttpRequest can be disabled on IE
    try {
        if ("undefined" !== typeof XMLHttpRequest && (!xdomain || _hasCorsJs.hasCORS)) return new XMLHttpRequest();
    } catch (e) {}
    if (!xdomain) try {
        return new _globalThisJs.globalThisShim[[
            "Active"
        ].concat("Object").join("X")]("Microsoft.XMLHTTP");
    } catch (e1) {}
}

},{"../contrib/has-cors.js":"kPgMI","../globalThis.js":"7zbHc","@parcel/transformer-js/src/esmodule-helpers.js":"gkKU3"}],"kPgMI":[function(require,module,exports) {
var parcelHelpers = require("@parcel/transformer-js/src/esmodule-helpers.js");
parcelHelpers.defineInteropFlag(exports);
parcelHelpers.export(exports, "hasCORS", ()=>hasCORS
);
// imported from https://github.com/component/has-cors
let value = false;
try {
    value = typeof XMLHttpRequest !== 'undefined' && 'withCredentials' in new XMLHttpRequest();
} catch (err) {
// if XMLHttp support is disabled in IE then it will throw
// when trying to create
}
const hasCORS = value;

},{"@parcel/transformer-js/src/esmodule-helpers.js":"gkKU3"}],"8y2e2":[function(require,module,exports) {
var parcelHelpers = require("@parcel/transformer-js/src/esmodule-helpers.js");
parcelHelpers.defineInteropFlag(exports);
parcelHelpers.export(exports, "WS", ()=>WS
);
var _transportJs = require("../transport.js");
var _parseqsJs = require("../contrib/parseqs.js");
var _yeastJs = require("../contrib/yeast.js");
var _utilJs = require("../util.js");
var _websocketConstructorJs = require("./websocket-constructor.js");
var _engineIoParser = require("engine.io-parser");
var Buffer = require("buffer").Buffer;
// detect ReactNative environment
const isReactNative = typeof navigator !== "undefined" && typeof navigator.product === "string" && navigator.product.toLowerCase() === "reactnative";
class WS extends _transportJs.Transport {
    /**
     * WebSocket transport constructor.
     *
     * @api {Object} connection options
     * @api public
     */ constructor(opts){
        super(opts);
        this.supportsBinary = !opts.forceBase64;
    }
    /**
     * Transport name.
     *
     * @api public
     */ get name() {
        return "websocket";
    }
    /**
     * Opens socket.
     *
     * @api private
     */ doOpen() {
        if (!this.check()) // let probe timeout
        return;
        const uri = this.uri();
        const protocols = this.opts.protocols;
        // React Native only supports the 'headers' option, and will print a warning if anything else is passed
        const opts = isReactNative ? {} : _utilJs.pick(this.opts, "agent", "perMessageDeflate", "pfx", "key", "passphrase", "cert", "ca", "ciphers", "rejectUnauthorized", "localAddress", "protocolVersion", "origin", "maxPayload", "family", "checkServerIdentity");
        if (this.opts.extraHeaders) opts.headers = this.opts.extraHeaders;
        try {
            this.ws = _websocketConstructorJs.usingBrowserWebSocket && !isReactNative ? protocols ? new _websocketConstructorJs.WebSocket(uri, protocols) : new _websocketConstructorJs.WebSocket(uri) : new _websocketConstructorJs.WebSocket(uri, protocols, opts);
        } catch (err) {
            return this.emitReserved("error", err);
        }
        this.ws.binaryType = this.socket.binaryType || _websocketConstructorJs.defaultBinaryType;
        this.addEventListeners();
    }
    /**
     * Adds event listeners to the socket
     *
     * @api private
     */ addEventListeners() {
        this.ws.onopen = ()=>{
            if (this.opts.autoUnref) this.ws._socket.unref();
            this.onOpen();
        };
        this.ws.onclose = (closeEvent)=>this.onClose({
                description: "websocket connection closed",
                context: closeEvent
            })
        ;
        this.ws.onmessage = (ev)=>this.onData(ev.data)
        ;
        this.ws.onerror = (e)=>this.onError("websocket error", e)
        ;
    }
    /**
     * Writes data to socket.
     *
     * @param {Array} array of packets.
     * @api private
     */ write(packets) {
        this.writable = false;
        // encodePacket efficient as it uses WS framing
        // no need for encodePayload
        for(let i = 0; i < packets.length; i++){
            const packet = packets[i];
            const lastPacket = i === packets.length - 1;
            _engineIoParser.encodePacket(packet, this.supportsBinary, (data)=>{
                // always create a new object (GH-437)
                const opts = {};
                if (!_websocketConstructorJs.usingBrowserWebSocket) {
                    if (packet.options) opts.compress = packet.options.compress;
                    if (this.opts.perMessageDeflate) {
                        const len = // @ts-ignore
                        "string" === typeof data ? Buffer.byteLength(data) : data.length;
                        if (len < this.opts.perMessageDeflate.threshold) opts.compress = false;
                    }
                }
                // Sometimes the websocket has already been closed but the browser didn't
                // have a chance of informing us about it yet, in that case send will
                // throw an error
                try {
                    if (_websocketConstructorJs.usingBrowserWebSocket) // TypeError is thrown when passing the second argument on Safari
                    this.ws.send(data);
                    else this.ws.send(data, opts);
                } catch (e) {}
                if (lastPacket) // fake drain
                // defer to next tick to allow Socket to clear writeBuffer
                _websocketConstructorJs.nextTick(()=>{
                    this.writable = true;
                    this.emitReserved("drain");
                }, this.setTimeoutFn);
            });
        }
    }
    /**
     * Closes socket.
     *
     * @api private
     */ doClose() {
        if (typeof this.ws !== "undefined") {
            this.ws.close();
            this.ws = null;
        }
    }
    /**
     * Generates uri for connection.
     *
     * @api private
     */ uri() {
        let query = this.query || {};
        const schema = this.opts.secure ? "wss" : "ws";
        let port = "";
        // avoid port if default for schema
        if (this.opts.port && ("wss" === schema && Number(this.opts.port) !== 443 || "ws" === schema && Number(this.opts.port) !== 80)) port = ":" + this.opts.port;
        // append timestamp to URI
        if (this.opts.timestampRequests) query[this.opts.timestampParam] = _yeastJs.yeast();
        // communicate binary support capabilities
        if (!this.supportsBinary) query.b64 = 1;
        const encodedQuery = _parseqsJs.encode(query);
        const ipv6 = this.opts.hostname.indexOf(":") !== -1;
        return schema + "://" + (ipv6 ? "[" + this.opts.hostname + "]" : this.opts.hostname) + port + this.opts.path + (encodedQuery.length ? "?" + encodedQuery : "");
    }
    /**
     * Feature detection for WebSocket.
     *
     * @return {Boolean} whether this transport is available.
     * @api public
     */ check() {
        return !!_websocketConstructorJs.WebSocket;
    }
}

},{"../transport.js":"kwKKC","../contrib/parseqs.js":"aFNEN","../contrib/yeast.js":"5cPiI","../util.js":"d7eyH","./websocket-constructor.js":"dDJnh","engine.io-parser":"2SHiP","buffer":"fCgem","@parcel/transformer-js/src/esmodule-helpers.js":"gkKU3"}],"dDJnh":[function(require,module,exports) {
var parcelHelpers = require("@parcel/transformer-js/src/esmodule-helpers.js");
parcelHelpers.defineInteropFlag(exports);
parcelHelpers.export(exports, "nextTick", ()=>nextTick
);
parcelHelpers.export(exports, "WebSocket", ()=>WebSocket
);
parcelHelpers.export(exports, "usingBrowserWebSocket", ()=>usingBrowserWebSocket
);
parcelHelpers.export(exports, "defaultBinaryType", ()=>defaultBinaryType
);
var _globalThisJs = require("../globalThis.js");
const nextTick = (()=>{
    const isPromiseAvailable = typeof Promise === "function" && typeof Promise.resolve === "function";
    if (isPromiseAvailable) return (cb)=>Promise.resolve().then(cb)
    ;
    else return (cb, setTimeoutFn)=>setTimeoutFn(cb, 0)
    ;
})();
const WebSocket = _globalThisJs.globalThisShim.WebSocket || _globalThisJs.globalThisShim.MozWebSocket;
const usingBrowserWebSocket = true;
const defaultBinaryType = "arraybuffer";

},{"../globalThis.js":"7zbHc","@parcel/transformer-js/src/esmodule-helpers.js":"gkKU3"}],"fCgem":[function(require,module,exports) {
/*!
 * The buffer module from node.js, for the browser.
 *
 * @author   Feross Aboukhadijeh <https://feross.org>
 * @license  MIT
 */ /* eslint-disable no-proto */ 'use strict';
const base64 = require('base64-js');
const ieee754 = require('ieee754');
const customInspectSymbol = typeof Symbol === 'function' && typeof Symbol['for'] === 'function' // eslint-disable-line dot-notation
 ? Symbol['for']('nodejs.util.inspect.custom') // eslint-disable-line dot-notation
 : null;
exports.Buffer = Buffer;
exports.SlowBuffer = SlowBuffer;
exports.INSPECT_MAX_BYTES = 50;
const K_MAX_LENGTH = 0x7fffffff;
exports.kMaxLength = K_MAX_LENGTH;
/**
 * If `Buffer.TYPED_ARRAY_SUPPORT`:
 *   === true    Use Uint8Array implementation (fastest)
 *   === false   Print warning and recommend using `buffer` v4.x which has an Object
 *               implementation (most compatible, even IE6)
 *
 * Browsers that support typed arrays are IE 10+, Firefox 4+, Chrome 7+, Safari 5.1+,
 * Opera 11.6+, iOS 4.2+.
 *
 * We report that the browser does not support typed arrays if the are not subclassable
 * using __proto__. Firefox 4-29 lacks support for adding new properties to `Uint8Array`
 * (See: https://bugzilla.mozilla.org/show_bug.cgi?id=695438). IE 10 lacks support
 * for __proto__ and has a buggy typed array implementation.
 */ Buffer.TYPED_ARRAY_SUPPORT = typedArraySupport();
if (!Buffer.TYPED_ARRAY_SUPPORT && typeof console !== 'undefined' && typeof console.error === 'function') console.error("This browser lacks typed array (Uint8Array) support which is required by `buffer` v5.x. Use `buffer` v4.x if you require old browser support.");
function typedArraySupport() {
    // Can typed array instances can be augmented?
    try {
        const arr = new Uint8Array(1);
        const proto = {
            foo: function() {
                return 42;
            }
        };
        Object.setPrototypeOf(proto, Uint8Array.prototype);
        Object.setPrototypeOf(arr, proto);
        return arr.foo() === 42;
    } catch (e) {
        return false;
    }
}
Object.defineProperty(Buffer.prototype, 'parent', {
    enumerable: true,
    get: function() {
        if (!Buffer.isBuffer(this)) return undefined;
        return this.buffer;
    }
});
Object.defineProperty(Buffer.prototype, 'offset', {
    enumerable: true,
    get: function() {
        if (!Buffer.isBuffer(this)) return undefined;
        return this.byteOffset;
    }
});
function createBuffer(length) {
    if (length > K_MAX_LENGTH) throw new RangeError('The value "' + length + '" is invalid for option "size"');
    // Return an augmented `Uint8Array` instance
    const buf = new Uint8Array(length);
    Object.setPrototypeOf(buf, Buffer.prototype);
    return buf;
}
/**
 * The Buffer constructor returns instances of `Uint8Array` that have their
 * prototype changed to `Buffer.prototype`. Furthermore, `Buffer` is a subclass of
 * `Uint8Array`, so the returned instances will have all the node `Buffer` methods
 * and the `Uint8Array` methods. Square bracket notation works as expected -- it
 * returns a single octet.
 *
 * The `Uint8Array` prototype remains unmodified.
 */ function Buffer(arg, encodingOrOffset, length) {
    // Common case.
    if (typeof arg === 'number') {
        if (typeof encodingOrOffset === 'string') throw new TypeError('The "string" argument must be of type string. Received type number');
        return allocUnsafe(arg);
    }
    return from(arg, encodingOrOffset, length);
}
Buffer.poolSize = 8192 // not used by this implementation
;
function from(value, encodingOrOffset, length) {
    if (typeof value === 'string') return fromString(value, encodingOrOffset);
    if (ArrayBuffer.isView(value)) return fromArrayView(value);
    if (value == null) throw new TypeError("The first argument must be one of type string, Buffer, ArrayBuffer, Array, or Array-like Object. Received type " + typeof value);
    if (isInstance(value, ArrayBuffer) || value && isInstance(value.buffer, ArrayBuffer)) return fromArrayBuffer(value, encodingOrOffset, length);
    if (typeof SharedArrayBuffer !== 'undefined' && (isInstance(value, SharedArrayBuffer) || value && isInstance(value.buffer, SharedArrayBuffer))) return fromArrayBuffer(value, encodingOrOffset, length);
    if (typeof value === 'number') throw new TypeError('The "value" argument must not be of type number. Received type number');
    const valueOf = value.valueOf && value.valueOf();
    if (valueOf != null && valueOf !== value) return Buffer.from(valueOf, encodingOrOffset, length);
    const b = fromObject(value);
    if (b) return b;
    if (typeof Symbol !== 'undefined' && Symbol.toPrimitive != null && typeof value[Symbol.toPrimitive] === 'function') return Buffer.from(value[Symbol.toPrimitive]('string'), encodingOrOffset, length);
    throw new TypeError("The first argument must be one of type string, Buffer, ArrayBuffer, Array, or Array-like Object. Received type " + typeof value);
}
/**
 * Functionally equivalent to Buffer(arg, encoding) but throws a TypeError
 * if value is a number.
 * Buffer.from(str[, encoding])
 * Buffer.from(array)
 * Buffer.from(buffer)
 * Buffer.from(arrayBuffer[, byteOffset[, length]])
 **/ Buffer.from = function(value, encodingOrOffset, length) {
    return from(value, encodingOrOffset, length);
};
// Note: Change prototype *after* Buffer.from is defined to workaround Chrome bug:
// https://github.com/feross/buffer/pull/148
Object.setPrototypeOf(Buffer.prototype, Uint8Array.prototype);
Object.setPrototypeOf(Buffer, Uint8Array);
function assertSize(size) {
    if (typeof size !== 'number') throw new TypeError('"size" argument must be of type number');
    else if (size < 0) throw new RangeError('The value "' + size + '" is invalid for option "size"');
}
function alloc(size, fill, encoding) {
    assertSize(size);
    if (size <= 0) return createBuffer(size);
    if (fill !== undefined) // Only pay attention to encoding if it's a string. This
    // prevents accidentally sending in a number that would
    // be interpreted as a start offset.
    return typeof encoding === 'string' ? createBuffer(size).fill(fill, encoding) : createBuffer(size).fill(fill);
    return createBuffer(size);
}
/**
 * Creates a new filled Buffer instance.
 * alloc(size[, fill[, encoding]])
 **/ Buffer.alloc = function(size, fill, encoding) {
    return alloc(size, fill, encoding);
};
function allocUnsafe(size) {
    assertSize(size);
    return createBuffer(size < 0 ? 0 : checked(size) | 0);
}
/**
 * Equivalent to Buffer(num), by default creates a non-zero-filled Buffer instance.
 * */ Buffer.allocUnsafe = function(size) {
    return allocUnsafe(size);
};
/**
 * Equivalent to SlowBuffer(num), by default creates a non-zero-filled Buffer instance.
 */ Buffer.allocUnsafeSlow = function(size) {
    return allocUnsafe(size);
};
function fromString(string, encoding) {
    if (typeof encoding !== 'string' || encoding === '') encoding = 'utf8';
    if (!Buffer.isEncoding(encoding)) throw new TypeError('Unknown encoding: ' + encoding);
    const length = byteLength(string, encoding) | 0;
    let buf = createBuffer(length);
    const actual = buf.write(string, encoding);
    if (actual !== length) // Writing a hex string, for example, that contains invalid characters will
    // cause everything after the first invalid character to be ignored. (e.g.
    // 'abxxcd' will be treated as 'ab')
    buf = buf.slice(0, actual);
    return buf;
}
function fromArrayLike(array) {
    const length = array.length < 0 ? 0 : checked(array.length) | 0;
    const buf = createBuffer(length);
    for(let i = 0; i < length; i += 1)buf[i] = array[i] & 255;
    return buf;
}
function fromArrayView(arrayView) {
    if (isInstance(arrayView, Uint8Array)) {
        const copy = new Uint8Array(arrayView);
        return fromArrayBuffer(copy.buffer, copy.byteOffset, copy.byteLength);
    }
    return fromArrayLike(arrayView);
}
function fromArrayBuffer(array, byteOffset, length) {
    if (byteOffset < 0 || array.byteLength < byteOffset) throw new RangeError('"offset" is outside of buffer bounds');
    if (array.byteLength < byteOffset + (length || 0)) throw new RangeError('"length" is outside of buffer bounds');
    let buf;
    if (byteOffset === undefined && length === undefined) buf = new Uint8Array(array);
    else if (length === undefined) buf = new Uint8Array(array, byteOffset);
    else buf = new Uint8Array(array, byteOffset, length);
    // Return an augmented `Uint8Array` instance
    Object.setPrototypeOf(buf, Buffer.prototype);
    return buf;
}
function fromObject(obj) {
    if (Buffer.isBuffer(obj)) {
        const len = checked(obj.length) | 0;
        const buf = createBuffer(len);
        if (buf.length === 0) return buf;
        obj.copy(buf, 0, 0, len);
        return buf;
    }
    if (obj.length !== undefined) {
        if (typeof obj.length !== 'number' || numberIsNaN(obj.length)) return createBuffer(0);
        return fromArrayLike(obj);
    }
    if (obj.type === 'Buffer' && Array.isArray(obj.data)) return fromArrayLike(obj.data);
}
function checked(length) {
    // Note: cannot use `length < K_MAX_LENGTH` here because that fails when
    // length is NaN (which is otherwise coerced to zero.)
    if (length >= K_MAX_LENGTH) throw new RangeError("Attempt to allocate Buffer larger than maximum size: 0x" + K_MAX_LENGTH.toString(16) + ' bytes');
    return length | 0;
}
function SlowBuffer(length) {
    if (+length != length) length = 0;
    return Buffer.alloc(+length);
}
Buffer.isBuffer = function isBuffer(b) {
    return b != null && b._isBuffer === true && b !== Buffer.prototype // so Buffer.isBuffer(Buffer.prototype) will be false
    ;
};
Buffer.compare = function compare(a, b) {
    if (isInstance(a, Uint8Array)) a = Buffer.from(a, a.offset, a.byteLength);
    if (isInstance(b, Uint8Array)) b = Buffer.from(b, b.offset, b.byteLength);
    if (!Buffer.isBuffer(a) || !Buffer.isBuffer(b)) throw new TypeError('The "buf1", "buf2" arguments must be one of type Buffer or Uint8Array');
    if (a === b) return 0;
    let x = a.length;
    let y = b.length;
    for(let i = 0, len = Math.min(x, y); i < len; ++i)if (a[i] !== b[i]) {
        x = a[i];
        y = b[i];
        break;
    }
    if (x < y) return -1;
    if (y < x) return 1;
    return 0;
};
Buffer.isEncoding = function isEncoding(encoding) {
    switch(String(encoding).toLowerCase()){
        case 'hex':
        case 'utf8':
        case 'utf-8':
        case 'ascii':
        case 'latin1':
        case 'binary':
        case 'base64':
        case 'ucs2':
        case 'ucs-2':
        case 'utf16le':
        case 'utf-16le':
            return true;
        default:
            return false;
    }
};
Buffer.concat = function concat(list, length) {
    if (!Array.isArray(list)) throw new TypeError('"list" argument must be an Array of Buffers');
    if (list.length === 0) return Buffer.alloc(0);
    let i;
    if (length === undefined) {
        length = 0;
        for(i = 0; i < list.length; ++i)length += list[i].length;
    }
    const buffer = Buffer.allocUnsafe(length);
    let pos = 0;
    for(i = 0; i < list.length; ++i){
        let buf = list[i];
        if (isInstance(buf, Uint8Array)) {
            if (pos + buf.length > buffer.length) {
                if (!Buffer.isBuffer(buf)) buf = Buffer.from(buf);
                buf.copy(buffer, pos);
            } else Uint8Array.prototype.set.call(buffer, buf, pos);
        } else if (!Buffer.isBuffer(buf)) throw new TypeError('"list" argument must be an Array of Buffers');
        else buf.copy(buffer, pos);
        pos += buf.length;
    }
    return buffer;
};
function byteLength(string, encoding) {
    if (Buffer.isBuffer(string)) return string.length;
    if (ArrayBuffer.isView(string) || isInstance(string, ArrayBuffer)) return string.byteLength;
    if (typeof string !== 'string') throw new TypeError('The "string" argument must be one of type string, Buffer, or ArrayBuffer. Received type ' + typeof string);
    const len = string.length;
    const mustMatch = arguments.length > 2 && arguments[2] === true;
    if (!mustMatch && len === 0) return 0;
    // Use a for loop to avoid recursion
    let loweredCase = false;
    for(;;)switch(encoding){
        case 'ascii':
        case 'latin1':
        case 'binary':
            return len;
        case 'utf8':
        case 'utf-8':
            return utf8ToBytes(string).length;
        case 'ucs2':
        case 'ucs-2':
        case 'utf16le':
        case 'utf-16le':
            return len * 2;
        case 'hex':
            return len >>> 1;
        case 'base64':
            return base64ToBytes(string).length;
        default:
            if (loweredCase) return mustMatch ? -1 : utf8ToBytes(string).length // assume utf8
            ;
            encoding = ('' + encoding).toLowerCase();
            loweredCase = true;
    }
}
Buffer.byteLength = byteLength;
function slowToString(encoding, start, end) {
    let loweredCase = false;
    // No need to verify that "this.length <= MAX_UINT32" since it's a read-only
    // property of a typed array.
    // This behaves neither like String nor Uint8Array in that we set start/end
    // to their upper/lower bounds if the value passed is out of range.
    // undefined is handled specially as per ECMA-262 6th Edition,
    // Section 13.3.3.7 Runtime Semantics: KeyedBindingInitialization.
    if (start === undefined || start < 0) start = 0;
    // Return early if start > this.length. Done here to prevent potential uint32
    // coercion fail below.
    if (start > this.length) return '';
    if (end === undefined || end > this.length) end = this.length;
    if (end <= 0) return '';
    // Force coercion to uint32. This will also coerce falsey/NaN values to 0.
    end >>>= 0;
    start >>>= 0;
    if (end <= start) return '';
    if (!encoding) encoding = 'utf8';
    while(true)switch(encoding){
        case 'hex':
            return hexSlice(this, start, end);
        case 'utf8':
        case 'utf-8':
            return utf8Slice(this, start, end);
        case 'ascii':
            return asciiSlice(this, start, end);
        case 'latin1':
        case 'binary':
            return latin1Slice(this, start, end);
        case 'base64':
            return base64Slice(this, start, end);
        case 'ucs2':
        case 'ucs-2':
        case 'utf16le':
        case 'utf-16le':
            return utf16leSlice(this, start, end);
        default:
            if (loweredCase) throw new TypeError('Unknown encoding: ' + encoding);
            encoding = (encoding + '').toLowerCase();
            loweredCase = true;
    }
}
// This property is used by `Buffer.isBuffer` (and the `is-buffer` npm package)
// to detect a Buffer instance. It's not possible to use `instanceof Buffer`
// reliably in a browserify context because there could be multiple different
// copies of the 'buffer' package in use. This method works even for Buffer
// instances that were created from another copy of the `buffer` package.
// See: https://github.com/feross/buffer/issues/154
Buffer.prototype._isBuffer = true;
function swap(b, n, m) {
    const i = b[n];
    b[n] = b[m];
    b[m] = i;
}
Buffer.prototype.swap16 = function swap16() {
    const len = this.length;
    if (len % 2 !== 0) throw new RangeError('Buffer size must be a multiple of 16-bits');
    for(let i = 0; i < len; i += 2)swap(this, i, i + 1);
    return this;
};
Buffer.prototype.swap32 = function swap32() {
    const len = this.length;
    if (len % 4 !== 0) throw new RangeError('Buffer size must be a multiple of 32-bits');
    for(let i = 0; i < len; i += 4){
        swap(this, i, i + 3);
        swap(this, i + 1, i + 2);
    }
    return this;
};
Buffer.prototype.swap64 = function swap64() {
    const len = this.length;
    if (len % 8 !== 0) throw new RangeError('Buffer size must be a multiple of 64-bits');
    for(let i = 0; i < len; i += 8){
        swap(this, i, i + 7);
        swap(this, i + 1, i + 6);
        swap(this, i + 2, i + 5);
        swap(this, i + 3, i + 4);
    }
    return this;
};
Buffer.prototype.toString = function toString() {
    const length = this.length;
    if (length === 0) return '';
    if (arguments.length === 0) return utf8Slice(this, 0, length);
    return slowToString.apply(this, arguments);
};
Buffer.prototype.toLocaleString = Buffer.prototype.toString;
Buffer.prototype.equals = function equals(b) {
    if (!Buffer.isBuffer(b)) throw new TypeError('Argument must be a Buffer');
    if (this === b) return true;
    return Buffer.compare(this, b) === 0;
};
Buffer.prototype.inspect = function inspect() {
    let str = '';
    const max = exports.INSPECT_MAX_BYTES;
    str = this.toString('hex', 0, max).replace(/(.{2})/g, '$1 ').trim();
    if (this.length > max) str += ' ... ';
    return '<Buffer ' + str + '>';
};
if (customInspectSymbol) Buffer.prototype[customInspectSymbol] = Buffer.prototype.inspect;
Buffer.prototype.compare = function compare(target, start, end, thisStart, thisEnd) {
    if (isInstance(target, Uint8Array)) target = Buffer.from(target, target.offset, target.byteLength);
    if (!Buffer.isBuffer(target)) throw new TypeError('The "target" argument must be one of type Buffer or Uint8Array. Received type ' + typeof target);
    if (start === undefined) start = 0;
    if (end === undefined) end = target ? target.length : 0;
    if (thisStart === undefined) thisStart = 0;
    if (thisEnd === undefined) thisEnd = this.length;
    if (start < 0 || end > target.length || thisStart < 0 || thisEnd > this.length) throw new RangeError('out of range index');
    if (thisStart >= thisEnd && start >= end) return 0;
    if (thisStart >= thisEnd) return -1;
    if (start >= end) return 1;
    start >>>= 0;
    end >>>= 0;
    thisStart >>>= 0;
    thisEnd >>>= 0;
    if (this === target) return 0;
    let x = thisEnd - thisStart;
    let y = end - start;
    const len = Math.min(x, y);
    const thisCopy = this.slice(thisStart, thisEnd);
    const targetCopy = target.slice(start, end);
    for(let i = 0; i < len; ++i)if (thisCopy[i] !== targetCopy[i]) {
        x = thisCopy[i];
        y = targetCopy[i];
        break;
    }
    if (x < y) return -1;
    if (y < x) return 1;
    return 0;
};
// Finds either the first index of `val` in `buffer` at offset >= `byteOffset`,
// OR the last index of `val` in `buffer` at offset <= `byteOffset`.
//
// Arguments:
// - buffer - a Buffer to search
// - val - a string, Buffer, or number
// - byteOffset - an index into `buffer`; will be clamped to an int32
// - encoding - an optional encoding, relevant is val is a string
// - dir - true for indexOf, false for lastIndexOf
function bidirectionalIndexOf(buffer, val, byteOffset, encoding, dir) {
    // Empty buffer means no match
    if (buffer.length === 0) return -1;
    // Normalize byteOffset
    if (typeof byteOffset === 'string') {
        encoding = byteOffset;
        byteOffset = 0;
    } else if (byteOffset > 0x7fffffff) byteOffset = 0x7fffffff;
    else if (byteOffset < -2147483648) byteOffset = -2147483648;
    byteOffset = +byteOffset // Coerce to Number.
    ;
    if (numberIsNaN(byteOffset)) // byteOffset: it it's undefined, null, NaN, "foo", etc, search whole buffer
    byteOffset = dir ? 0 : buffer.length - 1;
    // Normalize byteOffset: negative offsets start from the end of the buffer
    if (byteOffset < 0) byteOffset = buffer.length + byteOffset;
    if (byteOffset >= buffer.length) {
        if (dir) return -1;
        else byteOffset = buffer.length - 1;
    } else if (byteOffset < 0) {
        if (dir) byteOffset = 0;
        else return -1;
    }
    // Normalize val
    if (typeof val === 'string') val = Buffer.from(val, encoding);
    // Finally, search either indexOf (if dir is true) or lastIndexOf
    if (Buffer.isBuffer(val)) {
        // Special case: looking for empty string/buffer always fails
        if (val.length === 0) return -1;
        return arrayIndexOf(buffer, val, byteOffset, encoding, dir);
    } else if (typeof val === 'number') {
        val = val & 0xFF // Search for a byte value [0-255]
        ;
        if (typeof Uint8Array.prototype.indexOf === 'function') {
            if (dir) return Uint8Array.prototype.indexOf.call(buffer, val, byteOffset);
            else return Uint8Array.prototype.lastIndexOf.call(buffer, val, byteOffset);
        }
        return arrayIndexOf(buffer, [
            val
        ], byteOffset, encoding, dir);
    }
    throw new TypeError('val must be string, number or Buffer');
}
function arrayIndexOf(arr, val, byteOffset, encoding, dir) {
    let indexSize = 1;
    let arrLength = arr.length;
    let valLength = val.length;
    if (encoding !== undefined) {
        encoding = String(encoding).toLowerCase();
        if (encoding === 'ucs2' || encoding === 'ucs-2' || encoding === 'utf16le' || encoding === 'utf-16le') {
            if (arr.length < 2 || val.length < 2) return -1;
            indexSize = 2;
            arrLength /= 2;
            valLength /= 2;
            byteOffset /= 2;
        }
    }
    function read(buf, i) {
        if (indexSize === 1) return buf[i];
        else return buf.readUInt16BE(i * indexSize);
    }
    let i1;
    if (dir) {
        let foundIndex = -1;
        for(i1 = byteOffset; i1 < arrLength; i1++)if (read(arr, i1) === read(val, foundIndex === -1 ? 0 : i1 - foundIndex)) {
            if (foundIndex === -1) foundIndex = i1;
            if (i1 - foundIndex + 1 === valLength) return foundIndex * indexSize;
        } else {
            if (foundIndex !== -1) i1 -= i1 - foundIndex;
            foundIndex = -1;
        }
    } else {
        if (byteOffset + valLength > arrLength) byteOffset = arrLength - valLength;
        for(i1 = byteOffset; i1 >= 0; i1--){
            let found = true;
            for(let j = 0; j < valLength; j++)if (read(arr, i1 + j) !== read(val, j)) {
                found = false;
                break;
            }
            if (found) return i1;
        }
    }
    return -1;
}
Buffer.prototype.includes = function includes(val, byteOffset, encoding) {
    return this.indexOf(val, byteOffset, encoding) !== -1;
};
Buffer.prototype.indexOf = function indexOf(val, byteOffset, encoding) {
    return bidirectionalIndexOf(this, val, byteOffset, encoding, true);
};
Buffer.prototype.lastIndexOf = function lastIndexOf(val, byteOffset, encoding) {
    return bidirectionalIndexOf(this, val, byteOffset, encoding, false);
};
function hexWrite(buf, string, offset, length) {
    offset = Number(offset) || 0;
    const remaining = buf.length - offset;
    if (!length) length = remaining;
    else {
        length = Number(length);
        if (length > remaining) length = remaining;
    }
    const strLen = string.length;
    if (length > strLen / 2) length = strLen / 2;
    let i;
    for(i = 0; i < length; ++i){
        const parsed = parseInt(string.substr(i * 2, 2), 16);
        if (numberIsNaN(parsed)) return i;
        buf[offset + i] = parsed;
    }
    return i;
}
function utf8Write(buf, string, offset, length) {
    return blitBuffer(utf8ToBytes(string, buf.length - offset), buf, offset, length);
}
function asciiWrite(buf, string, offset, length) {
    return blitBuffer(asciiToBytes(string), buf, offset, length);
}
function base64Write(buf, string, offset, length) {
    return blitBuffer(base64ToBytes(string), buf, offset, length);
}
function ucs2Write(buf, string, offset, length) {
    return blitBuffer(utf16leToBytes(string, buf.length - offset), buf, offset, length);
}
Buffer.prototype.write = function write(string, offset, length, encoding) {
    // Buffer#write(string)
    if (offset === undefined) {
        encoding = 'utf8';
        length = this.length;
        offset = 0;
    // Buffer#write(string, encoding)
    } else if (length === undefined && typeof offset === 'string') {
        encoding = offset;
        length = this.length;
        offset = 0;
    // Buffer#write(string, offset[, length][, encoding])
    } else if (isFinite(offset)) {
        offset = offset >>> 0;
        if (isFinite(length)) {
            length = length >>> 0;
            if (encoding === undefined) encoding = 'utf8';
        } else {
            encoding = length;
            length = undefined;
        }
    } else throw new Error('Buffer.write(string, encoding, offset[, length]) is no longer supported');
    const remaining = this.length - offset;
    if (length === undefined || length > remaining) length = remaining;
    if (string.length > 0 && (length < 0 || offset < 0) || offset > this.length) throw new RangeError('Attempt to write outside buffer bounds');
    if (!encoding) encoding = 'utf8';
    let loweredCase = false;
    for(;;)switch(encoding){
        case 'hex':
            return hexWrite(this, string, offset, length);
        case 'utf8':
        case 'utf-8':
            return utf8Write(this, string, offset, length);
        case 'ascii':
        case 'latin1':
        case 'binary':
            return asciiWrite(this, string, offset, length);
        case 'base64':
            // Warning: maxLength not taken into account in base64Write
            return base64Write(this, string, offset, length);
        case 'ucs2':
        case 'ucs-2':
        case 'utf16le':
        case 'utf-16le':
            return ucs2Write(this, string, offset, length);
        default:
            if (loweredCase) throw new TypeError('Unknown encoding: ' + encoding);
            encoding = ('' + encoding).toLowerCase();
            loweredCase = true;
    }
};
Buffer.prototype.toJSON = function toJSON() {
    return {
        type: 'Buffer',
        data: Array.prototype.slice.call(this._arr || this, 0)
    };
};
function base64Slice(buf, start, end) {
    if (start === 0 && end === buf.length) return base64.fromByteArray(buf);
    else return base64.fromByteArray(buf.slice(start, end));
}
function utf8Slice(buf, start, end) {
    end = Math.min(buf.length, end);
    const res = [];
    let i = start;
    while(i < end){
        const firstByte = buf[i];
        let codePoint = null;
        let bytesPerSequence = firstByte > 0xEF ? 4 : firstByte > 0xDF ? 3 : firstByte > 0xBF ? 2 : 1;
        if (i + bytesPerSequence <= end) {
            let secondByte, thirdByte, fourthByte, tempCodePoint;
            switch(bytesPerSequence){
                case 1:
                    if (firstByte < 0x80) codePoint = firstByte;
                    break;
                case 2:
                    secondByte = buf[i + 1];
                    if ((secondByte & 0xC0) === 0x80) {
                        tempCodePoint = (firstByte & 0x1F) << 0x6 | secondByte & 0x3F;
                        if (tempCodePoint > 0x7F) codePoint = tempCodePoint;
                    }
                    break;
                case 3:
                    secondByte = buf[i + 1];
                    thirdByte = buf[i + 2];
                    if ((secondByte & 0xC0) === 0x80 && (thirdByte & 0xC0) === 0x80) {
                        tempCodePoint = (firstByte & 0xF) << 0xC | (secondByte & 0x3F) << 0x6 | thirdByte & 0x3F;
                        if (tempCodePoint > 0x7FF && (tempCodePoint < 0xD800 || tempCodePoint > 0xDFFF)) codePoint = tempCodePoint;
                    }
                    break;
                case 4:
                    secondByte = buf[i + 1];
                    thirdByte = buf[i + 2];
                    fourthByte = buf[i + 3];
                    if ((secondByte & 0xC0) === 0x80 && (thirdByte & 0xC0) === 0x80 && (fourthByte & 0xC0) === 0x80) {
                        tempCodePoint = (firstByte & 0xF) << 0x12 | (secondByte & 0x3F) << 0xC | (thirdByte & 0x3F) << 0x6 | fourthByte & 0x3F;
                        if (tempCodePoint > 0xFFFF && tempCodePoint < 0x110000) codePoint = tempCodePoint;
                    }
            }
        }
        if (codePoint === null) {
            // we did not generate a valid codePoint so insert a
            // replacement char (U+FFFD) and advance only 1 byte
            codePoint = 0xFFFD;
            bytesPerSequence = 1;
        } else if (codePoint > 0xFFFF) {
            // encode to utf16 (surrogate pair dance)
            codePoint -= 0x10000;
            res.push(codePoint >>> 10 & 0x3FF | 0xD800);
            codePoint = 0xDC00 | codePoint & 0x3FF;
        }
        res.push(codePoint);
        i += bytesPerSequence;
    }
    return decodeCodePointsArray(res);
}
// Based on http://stackoverflow.com/a/22747272/680742, the browser with
// the lowest limit is Chrome, with 0x10000 args.
// We go 1 magnitude less, for safety
const MAX_ARGUMENTS_LENGTH = 0x1000;
function decodeCodePointsArray(codePoints) {
    const len = codePoints.length;
    if (len <= MAX_ARGUMENTS_LENGTH) return String.fromCharCode.apply(String, codePoints) // avoid extra slice()
    ;
    // Decode in chunks to avoid "call stack size exceeded".
    let res = '';
    let i = 0;
    while(i < len)res += String.fromCharCode.apply(String, codePoints.slice(i, i += MAX_ARGUMENTS_LENGTH));
    return res;
}
function asciiSlice(buf, start, end) {
    let ret = '';
    end = Math.min(buf.length, end);
    for(let i = start; i < end; ++i)ret += String.fromCharCode(buf[i] & 0x7F);
    return ret;
}
function latin1Slice(buf, start, end) {
    let ret = '';
    end = Math.min(buf.length, end);
    for(let i = start; i < end; ++i)ret += String.fromCharCode(buf[i]);
    return ret;
}
function hexSlice(buf, start, end) {
    const len = buf.length;
    if (!start || start < 0) start = 0;
    if (!end || end < 0 || end > len) end = len;
    let out = '';
    for(let i = start; i < end; ++i)out += hexSliceLookupTable[buf[i]];
    return out;
}
function utf16leSlice(buf, start, end) {
    const bytes = buf.slice(start, end);
    let res = '';
    // If bytes.length is odd, the last 8 bits must be ignored (same as node.js)
    for(let i = 0; i < bytes.length - 1; i += 2)res += String.fromCharCode(bytes[i] + bytes[i + 1] * 256);
    return res;
}
Buffer.prototype.slice = function slice(start, end) {
    const len = this.length;
    start = ~~start;
    end = end === undefined ? len : ~~end;
    if (start < 0) {
        start += len;
        if (start < 0) start = 0;
    } else if (start > len) start = len;
    if (end < 0) {
        end += len;
        if (end < 0) end = 0;
    } else if (end > len) end = len;
    if (end < start) end = start;
    const newBuf = this.subarray(start, end);
    // Return an augmented `Uint8Array` instance
    Object.setPrototypeOf(newBuf, Buffer.prototype);
    return newBuf;
};
/*
 * Need to make sure that buffer isn't trying to write out of bounds.
 */ function checkOffset(offset, ext, length) {
    if (offset % 1 !== 0 || offset < 0) throw new RangeError('offset is not uint');
    if (offset + ext > length) throw new RangeError('Trying to access beyond buffer length');
}
Buffer.prototype.readUintLE = Buffer.prototype.readUIntLE = function readUIntLE(offset, byteLength1, noAssert) {
    offset = offset >>> 0;
    byteLength1 = byteLength1 >>> 0;
    if (!noAssert) checkOffset(offset, byteLength1, this.length);
    let val = this[offset];
    let mul = 1;
    let i = 0;
    while(++i < byteLength1 && (mul *= 0x100))val += this[offset + i] * mul;
    return val;
};
Buffer.prototype.readUintBE = Buffer.prototype.readUIntBE = function readUIntBE(offset, byteLength2, noAssert) {
    offset = offset >>> 0;
    byteLength2 = byteLength2 >>> 0;
    if (!noAssert) checkOffset(offset, byteLength2, this.length);
    let val = this[offset + --byteLength2];
    let mul = 1;
    while(byteLength2 > 0 && (mul *= 0x100))val += this[offset + --byteLength2] * mul;
    return val;
};
Buffer.prototype.readUint8 = Buffer.prototype.readUInt8 = function readUInt8(offset, noAssert) {
    offset = offset >>> 0;
    if (!noAssert) checkOffset(offset, 1, this.length);
    return this[offset];
};
Buffer.prototype.readUint16LE = Buffer.prototype.readUInt16LE = function readUInt16LE(offset, noAssert) {
    offset = offset >>> 0;
    if (!noAssert) checkOffset(offset, 2, this.length);
    return this[offset] | this[offset + 1] << 8;
};
Buffer.prototype.readUint16BE = Buffer.prototype.readUInt16BE = function readUInt16BE(offset, noAssert) {
    offset = offset >>> 0;
    if (!noAssert) checkOffset(offset, 2, this.length);
    return this[offset] << 8 | this[offset + 1];
};
Buffer.prototype.readUint32LE = Buffer.prototype.readUInt32LE = function readUInt32LE(offset, noAssert) {
    offset = offset >>> 0;
    if (!noAssert) checkOffset(offset, 4, this.length);
    return (this[offset] | this[offset + 1] << 8 | this[offset + 2] << 16) + this[offset + 3] * 0x1000000;
};
Buffer.prototype.readUint32BE = Buffer.prototype.readUInt32BE = function readUInt32BE(offset, noAssert) {
    offset = offset >>> 0;
    if (!noAssert) checkOffset(offset, 4, this.length);
    return this[offset] * 0x1000000 + (this[offset + 1] << 16 | this[offset + 2] << 8 | this[offset + 3]);
};
Buffer.prototype.readBigUInt64LE = defineBigIntMethod(function readBigUInt64LE(offset) {
    offset = offset >>> 0;
    validateNumber(offset, 'offset');
    const first = this[offset];
    const last = this[offset + 7];
    if (first === undefined || last === undefined) boundsError(offset, this.length - 8);
    const lo = first + this[++offset] * 256 + this[++offset] * 65536 + this[++offset] * 2 ** 24;
    const hi = this[++offset] + this[++offset] * 256 + this[++offset] * 65536 + last * 2 ** 24;
    return BigInt(lo) + (BigInt(hi) << BigInt(32));
});
Buffer.prototype.readBigUInt64BE = defineBigIntMethod(function readBigUInt64BE(offset) {
    offset = offset >>> 0;
    validateNumber(offset, 'offset');
    const first = this[offset];
    const last = this[offset + 7];
    if (first === undefined || last === undefined) boundsError(offset, this.length - 8);
    const hi = first * 2 ** 24 + this[++offset] * 65536 + this[++offset] * 256 + this[++offset];
    const lo = this[++offset] * 2 ** 24 + this[++offset] * 65536 + this[++offset] * 256 + last;
    return (BigInt(hi) << BigInt(32)) + BigInt(lo);
});
Buffer.prototype.readIntLE = function readIntLE(offset, byteLength3, noAssert) {
    offset = offset >>> 0;
    byteLength3 = byteLength3 >>> 0;
    if (!noAssert) checkOffset(offset, byteLength3, this.length);
    let val = this[offset];
    let mul = 1;
    let i = 0;
    while(++i < byteLength3 && (mul *= 0x100))val += this[offset + i] * mul;
    mul *= 0x80;
    if (val >= mul) val -= Math.pow(2, 8 * byteLength3);
    return val;
};
Buffer.prototype.readIntBE = function readIntBE(offset, byteLength4, noAssert) {
    offset = offset >>> 0;
    byteLength4 = byteLength4 >>> 0;
    if (!noAssert) checkOffset(offset, byteLength4, this.length);
    let i = byteLength4;
    let mul = 1;
    let val = this[offset + --i];
    while(i > 0 && (mul *= 0x100))val += this[offset + --i] * mul;
    mul *= 0x80;
    if (val >= mul) val -= Math.pow(2, 8 * byteLength4);
    return val;
};
Buffer.prototype.readInt8 = function readInt8(offset, noAssert) {
    offset = offset >>> 0;
    if (!noAssert) checkOffset(offset, 1, this.length);
    if (!(this[offset] & 0x80)) return this[offset];
    return (0xff - this[offset] + 1) * -1;
};
Buffer.prototype.readInt16LE = function readInt16LE(offset, noAssert) {
    offset = offset >>> 0;
    if (!noAssert) checkOffset(offset, 2, this.length);
    const val = this[offset] | this[offset + 1] << 8;
    return val & 0x8000 ? val | 0xFFFF0000 : val;
};
Buffer.prototype.readInt16BE = function readInt16BE(offset, noAssert) {
    offset = offset >>> 0;
    if (!noAssert) checkOffset(offset, 2, this.length);
    const val = this[offset + 1] | this[offset] << 8;
    return val & 0x8000 ? val | 0xFFFF0000 : val;
};
Buffer.prototype.readInt32LE = function readInt32LE(offset, noAssert) {
    offset = offset >>> 0;
    if (!noAssert) checkOffset(offset, 4, this.length);
    return this[offset] | this[offset + 1] << 8 | this[offset + 2] << 16 | this[offset + 3] << 24;
};
Buffer.prototype.readInt32BE = function readInt32BE(offset, noAssert) {
    offset = offset >>> 0;
    if (!noAssert) checkOffset(offset, 4, this.length);
    return this[offset] << 24 | this[offset + 1] << 16 | this[offset + 2] << 8 | this[offset + 3];
};
Buffer.prototype.readBigInt64LE = defineBigIntMethod(function readBigInt64LE(offset) {
    offset = offset >>> 0;
    validateNumber(offset, 'offset');
    const first = this[offset];
    const last = this[offset + 7];
    if (first === undefined || last === undefined) boundsError(offset, this.length - 8);
    const val = this[offset + 4] + this[offset + 5] * 256 + this[offset + 6] * 65536 + (last << 24 // Overflow
    );
    return (BigInt(val) << BigInt(32)) + BigInt(first + this[++offset] * 256 + this[++offset] * 65536 + this[++offset] * 2 ** 24);
});
Buffer.prototype.readBigInt64BE = defineBigIntMethod(function readBigInt64BE(offset) {
    offset = offset >>> 0;
    validateNumber(offset, 'offset');
    const first = this[offset];
    const last = this[offset + 7];
    if (first === undefined || last === undefined) boundsError(offset, this.length - 8);
    const val = (first << 24) + this[++offset] * 65536 + this[++offset] * 256 + this[++offset];
    return (BigInt(val) << BigInt(32)) + BigInt(this[++offset] * 2 ** 24 + this[++offset] * 65536 + this[++offset] * 256 + last);
});
Buffer.prototype.readFloatLE = function readFloatLE(offset, noAssert) {
    offset = offset >>> 0;
    if (!noAssert) checkOffset(offset, 4, this.length);
    return ieee754.read(this, offset, true, 23, 4);
};
Buffer.prototype.readFloatBE = function readFloatBE(offset, noAssert) {
    offset = offset >>> 0;
    if (!noAssert) checkOffset(offset, 4, this.length);
    return ieee754.read(this, offset, false, 23, 4);
};
Buffer.prototype.readDoubleLE = function readDoubleLE(offset, noAssert) {
    offset = offset >>> 0;
    if (!noAssert) checkOffset(offset, 8, this.length);
    return ieee754.read(this, offset, true, 52, 8);
};
Buffer.prototype.readDoubleBE = function readDoubleBE(offset, noAssert) {
    offset = offset >>> 0;
    if (!noAssert) checkOffset(offset, 8, this.length);
    return ieee754.read(this, offset, false, 52, 8);
};
function checkInt(buf, value, offset, ext, max, min) {
    if (!Buffer.isBuffer(buf)) throw new TypeError('"buffer" argument must be a Buffer instance');
    if (value > max || value < min) throw new RangeError('"value" argument is out of bounds');
    if (offset + ext > buf.length) throw new RangeError('Index out of range');
}
Buffer.prototype.writeUintLE = Buffer.prototype.writeUIntLE = function writeUIntLE(value, offset, byteLength5, noAssert) {
    value = +value;
    offset = offset >>> 0;
    byteLength5 = byteLength5 >>> 0;
    if (!noAssert) {
        const maxBytes = Math.pow(2, 8 * byteLength5) - 1;
        checkInt(this, value, offset, byteLength5, maxBytes, 0);
    }
    let mul = 1;
    let i = 0;
    this[offset] = value & 0xFF;
    while(++i < byteLength5 && (mul *= 0x100))this[offset + i] = value / mul & 0xFF;
    return offset + byteLength5;
};
Buffer.prototype.writeUintBE = Buffer.prototype.writeUIntBE = function writeUIntBE(value, offset, byteLength6, noAssert) {
    value = +value;
    offset = offset >>> 0;
    byteLength6 = byteLength6 >>> 0;
    if (!noAssert) {
        const maxBytes = Math.pow(2, 8 * byteLength6) - 1;
        checkInt(this, value, offset, byteLength6, maxBytes, 0);
    }
    let i = byteLength6 - 1;
    let mul = 1;
    this[offset + i] = value & 0xFF;
    while(--i >= 0 && (mul *= 0x100))this[offset + i] = value / mul & 0xFF;
    return offset + byteLength6;
};
Buffer.prototype.writeUint8 = Buffer.prototype.writeUInt8 = function writeUInt8(value, offset, noAssert) {
    value = +value;
    offset = offset >>> 0;
    if (!noAssert) checkInt(this, value, offset, 1, 0xff, 0);
    this[offset] = value & 0xff;
    return offset + 1;
};
Buffer.prototype.writeUint16LE = Buffer.prototype.writeUInt16LE = function writeUInt16LE(value, offset, noAssert) {
    value = +value;
    offset = offset >>> 0;
    if (!noAssert) checkInt(this, value, offset, 2, 0xffff, 0);
    this[offset] = value & 0xff;
    this[offset + 1] = value >>> 8;
    return offset + 2;
};
Buffer.prototype.writeUint16BE = Buffer.prototype.writeUInt16BE = function writeUInt16BE(value, offset, noAssert) {
    value = +value;
    offset = offset >>> 0;
    if (!noAssert) checkInt(this, value, offset, 2, 0xffff, 0);
    this[offset] = value >>> 8;
    this[offset + 1] = value & 0xff;
    return offset + 2;
};
Buffer.prototype.writeUint32LE = Buffer.prototype.writeUInt32LE = function writeUInt32LE(value, offset, noAssert) {
    value = +value;
    offset = offset >>> 0;
    if (!noAssert) checkInt(this, value, offset, 4, 0xffffffff, 0);
    this[offset + 3] = value >>> 24;
    this[offset + 2] = value >>> 16;
    this[offset + 1] = value >>> 8;
    this[offset] = value & 0xff;
    return offset + 4;
};
Buffer.prototype.writeUint32BE = Buffer.prototype.writeUInt32BE = function writeUInt32BE(value, offset, noAssert) {
    value = +value;
    offset = offset >>> 0;
    if (!noAssert) checkInt(this, value, offset, 4, 0xffffffff, 0);
    this[offset] = value >>> 24;
    this[offset + 1] = value >>> 16;
    this[offset + 2] = value >>> 8;
    this[offset + 3] = value & 0xff;
    return offset + 4;
};
function wrtBigUInt64LE(buf, value, offset, min, max) {
    checkIntBI(value, min, max, buf, offset, 7);
    let lo = Number(value & BigInt(0xffffffff));
    buf[offset++] = lo;
    lo = lo >> 8;
    buf[offset++] = lo;
    lo = lo >> 8;
    buf[offset++] = lo;
    lo = lo >> 8;
    buf[offset++] = lo;
    let hi = Number(value >> BigInt(32) & BigInt(0xffffffff));
    buf[offset++] = hi;
    hi = hi >> 8;
    buf[offset++] = hi;
    hi = hi >> 8;
    buf[offset++] = hi;
    hi = hi >> 8;
    buf[offset++] = hi;
    return offset;
}
function wrtBigUInt64BE(buf, value, offset, min, max) {
    checkIntBI(value, min, max, buf, offset, 7);
    let lo = Number(value & BigInt(0xffffffff));
    buf[offset + 7] = lo;
    lo = lo >> 8;
    buf[offset + 6] = lo;
    lo = lo >> 8;
    buf[offset + 5] = lo;
    lo = lo >> 8;
    buf[offset + 4] = lo;
    let hi = Number(value >> BigInt(32) & BigInt(0xffffffff));
    buf[offset + 3] = hi;
    hi = hi >> 8;
    buf[offset + 2] = hi;
    hi = hi >> 8;
    buf[offset + 1] = hi;
    hi = hi >> 8;
    buf[offset] = hi;
    return offset + 8;
}
Buffer.prototype.writeBigUInt64LE = defineBigIntMethod(function writeBigUInt64LE(value, offset = 0) {
    return wrtBigUInt64LE(this, value, offset, BigInt(0), BigInt('0xffffffffffffffff'));
});
Buffer.prototype.writeBigUInt64BE = defineBigIntMethod(function writeBigUInt64BE(value, offset = 0) {
    return wrtBigUInt64BE(this, value, offset, BigInt(0), BigInt('0xffffffffffffffff'));
});
Buffer.prototype.writeIntLE = function writeIntLE(value, offset, byteLength7, noAssert) {
    value = +value;
    offset = offset >>> 0;
    if (!noAssert) {
        const limit = Math.pow(2, 8 * byteLength7 - 1);
        checkInt(this, value, offset, byteLength7, limit - 1, -limit);
    }
    let i = 0;
    let mul = 1;
    let sub = 0;
    this[offset] = value & 0xFF;
    while(++i < byteLength7 && (mul *= 0x100)){
        if (value < 0 && sub === 0 && this[offset + i - 1] !== 0) sub = 1;
        this[offset + i] = (value / mul >> 0) - sub & 0xFF;
    }
    return offset + byteLength7;
};
Buffer.prototype.writeIntBE = function writeIntBE(value, offset, byteLength8, noAssert) {
    value = +value;
    offset = offset >>> 0;
    if (!noAssert) {
        const limit = Math.pow(2, 8 * byteLength8 - 1);
        checkInt(this, value, offset, byteLength8, limit - 1, -limit);
    }
    let i = byteLength8 - 1;
    let mul = 1;
    let sub = 0;
    this[offset + i] = value & 0xFF;
    while(--i >= 0 && (mul *= 0x100)){
        if (value < 0 && sub === 0 && this[offset + i + 1] !== 0) sub = 1;
        this[offset + i] = (value / mul >> 0) - sub & 0xFF;
    }
    return offset + byteLength8;
};
Buffer.prototype.writeInt8 = function writeInt8(value, offset, noAssert) {
    value = +value;
    offset = offset >>> 0;
    if (!noAssert) checkInt(this, value, offset, 1, 0x7f, -128);
    if (value < 0) value = 0xff + value + 1;
    this[offset] = value & 0xff;
    return offset + 1;
};
Buffer.prototype.writeInt16LE = function writeInt16LE(value, offset, noAssert) {
    value = +value;
    offset = offset >>> 0;
    if (!noAssert) checkInt(this, value, offset, 2, 0x7fff, -32768);
    this[offset] = value & 0xff;
    this[offset + 1] = value >>> 8;
    return offset + 2;
};
Buffer.prototype.writeInt16BE = function writeInt16BE(value, offset, noAssert) {
    value = +value;
    offset = offset >>> 0;
    if (!noAssert) checkInt(this, value, offset, 2, 0x7fff, -32768);
    this[offset] = value >>> 8;
    this[offset + 1] = value & 0xff;
    return offset + 2;
};
Buffer.prototype.writeInt32LE = function writeInt32LE(value, offset, noAssert) {
    value = +value;
    offset = offset >>> 0;
    if (!noAssert) checkInt(this, value, offset, 4, 0x7fffffff, -2147483648);
    this[offset] = value & 0xff;
    this[offset + 1] = value >>> 8;
    this[offset + 2] = value >>> 16;
    this[offset + 3] = value >>> 24;
    return offset + 4;
};
Buffer.prototype.writeInt32BE = function writeInt32BE(value, offset, noAssert) {
    value = +value;
    offset = offset >>> 0;
    if (!noAssert) checkInt(this, value, offset, 4, 0x7fffffff, -2147483648);
    if (value < 0) value = 0xffffffff + value + 1;
    this[offset] = value >>> 24;
    this[offset + 1] = value >>> 16;
    this[offset + 2] = value >>> 8;
    this[offset + 3] = value & 0xff;
    return offset + 4;
};
Buffer.prototype.writeBigInt64LE = defineBigIntMethod(function writeBigInt64LE(value, offset = 0) {
    return wrtBigUInt64LE(this, value, offset, -BigInt('0x8000000000000000'), BigInt('0x7fffffffffffffff'));
});
Buffer.prototype.writeBigInt64BE = defineBigIntMethod(function writeBigInt64BE(value, offset = 0) {
    return wrtBigUInt64BE(this, value, offset, -BigInt('0x8000000000000000'), BigInt('0x7fffffffffffffff'));
});
function checkIEEE754(buf, value, offset, ext, max, min) {
    if (offset + ext > buf.length) throw new RangeError('Index out of range');
    if (offset < 0) throw new RangeError('Index out of range');
}
function writeFloat(buf, value, offset, littleEndian, noAssert) {
    value = +value;
    offset = offset >>> 0;
    if (!noAssert) checkIEEE754(buf, value, offset, 4, 3.4028234663852886e+38, -340282346638528860000000000000000000000);
    ieee754.write(buf, value, offset, littleEndian, 23, 4);
    return offset + 4;
}
Buffer.prototype.writeFloatLE = function writeFloatLE(value, offset, noAssert) {
    return writeFloat(this, value, offset, true, noAssert);
};
Buffer.prototype.writeFloatBE = function writeFloatBE(value, offset, noAssert) {
    return writeFloat(this, value, offset, false, noAssert);
};
function writeDouble(buf, value, offset, littleEndian, noAssert) {
    value = +value;
    offset = offset >>> 0;
    if (!noAssert) checkIEEE754(buf, value, offset, 8, 1.7976931348623157E+308, -);
    ieee754.write(buf, value, offset, littleEndian, 52, 8);
    return offset + 8;
}
Buffer.prototype.writeDoubleLE = function writeDoubleLE(value, offset, noAssert) {
    return writeDouble(this, value, offset, true, noAssert);
};
Buffer.prototype.writeDoubleBE = function writeDoubleBE(value, offset, noAssert) {
    return writeDouble(this, value, offset, false, noAssert);
};
// copy(targetBuffer, targetStart=0, sourceStart=0, sourceEnd=buffer.length)
Buffer.prototype.copy = function copy(target, targetStart, start, end) {
    if (!Buffer.isBuffer(target)) throw new TypeError('argument should be a Buffer');
    if (!start) start = 0;
    if (!end && end !== 0) end = this.length;
    if (targetStart >= target.length) targetStart = target.length;
    if (!targetStart) targetStart = 0;
    if (end > 0 && end < start) end = start;
    // Copy 0 bytes; we're done
    if (end === start) return 0;
    if (target.length === 0 || this.length === 0) return 0;
    // Fatal error conditions
    if (targetStart < 0) throw new RangeError('targetStart out of bounds');
    if (start < 0 || start >= this.length) throw new RangeError('Index out of range');
    if (end < 0) throw new RangeError('sourceEnd out of bounds');
    // Are we oob?
    if (end > this.length) end = this.length;
    if (target.length - targetStart < end - start) end = target.length - targetStart + start;
    const len = end - start;
    if (this === target && typeof Uint8Array.prototype.copyWithin === 'function') // Use built-in when available, missing from IE11
    this.copyWithin(targetStart, start, end);
    else Uint8Array.prototype.set.call(target, this.subarray(start, end), targetStart);
    return len;
};
// Usage:
//    buffer.fill(number[, offset[, end]])
//    buffer.fill(buffer[, offset[, end]])
//    buffer.fill(string[, offset[, end]][, encoding])
Buffer.prototype.fill = function fill(val, start, end, encoding) {
    // Handle string cases:
    if (typeof val === 'string') {
        if (typeof start === 'string') {
            encoding = start;
            start = 0;
            end = this.length;
        } else if (typeof end === 'string') {
            encoding = end;
            end = this.length;
        }
        if (encoding !== undefined && typeof encoding !== 'string') throw new TypeError('encoding must be a string');
        if (typeof encoding === 'string' && !Buffer.isEncoding(encoding)) throw new TypeError('Unknown encoding: ' + encoding);
        if (val.length === 1) {
            const code = val.charCodeAt(0);
            if (encoding === 'utf8' && code < 128 || encoding === 'latin1') // Fast path: If `val` fits into a single byte, use that numeric value.
            val = code;
        }
    } else if (typeof val === 'number') val = val & 255;
    else if (typeof val === 'boolean') val = Number(val);
    // Invalid ranges are not set to a default, so can range check early.
    if (start < 0 || this.length < start || this.length < end) throw new RangeError('Out of range index');
    if (end <= start) return this;
    start = start >>> 0;
    end = end === undefined ? this.length : end >>> 0;
    if (!val) val = 0;
    let i;
    if (typeof val === 'number') for(i = start; i < end; ++i)this[i] = val;
    else {
        const bytes = Buffer.isBuffer(val) ? val : Buffer.from(val, encoding);
        const len = bytes.length;
        if (len === 0) throw new TypeError('The value "' + val + '" is invalid for argument "value"');
        for(i = 0; i < end - start; ++i)this[i + start] = bytes[i % len];
    }
    return this;
};
// CUSTOM ERRORS
// =============
// Simplified versions from Node, changed for Buffer-only usage
const errors = {};
function E(sym, getMessage, Base) {
    errors[sym] = class NodeError extends Base {
        constructor(){
            super();
            Object.defineProperty(this, 'message', {
                value: getMessage.apply(this, arguments),
                writable: true,
                configurable: true
            });
            // Add the error code to the name to include it in the stack trace.
            this.name = `${this.name} [${sym}]`;
            // Access the stack to generate the error message including the error code
            // from the name.
            this.stack // eslint-disable-line no-unused-expressions
            ;
            // Reset the name to the actual name.
            delete this.name;
        }
        get code() {
            return sym;
        }
        set code(value) {
            Object.defineProperty(this, 'code', {
                configurable: true,
                enumerable: true,
                value,
                writable: true
            });
        }
        toString() {
            return `${this.name} [${sym}]: ${this.message}`;
        }
    };
}
E('ERR_BUFFER_OUT_OF_BOUNDS', function(name) {
    if (name) return `${name} is outside of buffer bounds`;
    return 'Attempt to access memory outside buffer bounds';
}, RangeError);
E('ERR_INVALID_ARG_TYPE', function(name, actual) {
    return `The "${name}" argument must be of type number. Received type ${typeof actual}`;
}, TypeError);
E('ERR_OUT_OF_RANGE', function(str, range, input) {
    let msg = `The value of "${str}" is out of range.`;
    let received = input;
    if (Number.isInteger(input) && Math.abs(input) > 2 ** 32) received = addNumericalSeparator(String(input));
    else if (typeof input === 'bigint') {
        received = String(input);
        if (input > BigInt(2) ** BigInt(32) || input < -(BigInt(2) ** BigInt(32))) received = addNumericalSeparator(received);
        received += 'n';
    }
    msg += ` It must be ${range}. Received ${received}`;
    return msg;
}, RangeError);
function addNumericalSeparator(val) {
    let res = '';
    let i = val.length;
    const start = val[0] === '-' ? 1 : 0;
    for(; i >= start + 4; i -= 3)res = `_${val.slice(i - 3, i)}${res}`;
    return `${val.slice(0, i)}${res}`;
}
// CHECK FUNCTIONS
// ===============
function checkBounds(buf, offset, byteLength9) {
    validateNumber(offset, 'offset');
    if (buf[offset] === undefined || buf[offset + byteLength9] === undefined) boundsError(offset, buf.length - (byteLength9 + 1));
}
function checkIntBI(value, min, max, buf, offset, byteLength10) {
    if (value > max || value < min) {
        const n = typeof min === 'bigint' ? 'n' : '';
        let range;
        if (byteLength10 > 3) {
            if (min === 0 || min === BigInt(0)) range = `>= 0${n} and < 2${n} ** ${(byteLength10 + 1) * 8}${n}`;
            else range = `>= -(2${n} ** ${(byteLength10 + 1) * 8 - 1}${n}) and < 2 ** ` + `${(byteLength10 + 1) * 8 - 1}${n}`;
        } else range = `>= ${min}${n} and <= ${max}${n}`;
        throw new errors.ERR_OUT_OF_RANGE('value', range, value);
    }
    checkBounds(buf, offset, byteLength10);
}
function validateNumber(value, name) {
    if (typeof value !== 'number') throw new errors.ERR_INVALID_ARG_TYPE(name, 'number', value);
}
function boundsError(value, length, type) {
    if (Math.floor(value) !== value) {
        validateNumber(value, type);
        throw new errors.ERR_OUT_OF_RANGE(type || 'offset', 'an integer', value);
    }
    if (length < 0) throw new errors.ERR_BUFFER_OUT_OF_BOUNDS();
    throw new errors.ERR_OUT_OF_RANGE(type || 'offset', `>= ${type ? 1 : 0} and <= ${length}`, value);
}
// HELPER FUNCTIONS
// ================
const INVALID_BASE64_RE = /[^+/0-9A-Za-z-_]/g;
function base64clean(str) {
    // Node takes equal signs as end of the Base64 encoding
    str = str.split('=')[0];
    // Node strips out invalid characters like \n and \t from the string, base64-js does not
    str = str.trim().replace(INVALID_BASE64_RE, '');
    // Node converts strings with length < 2 to ''
    if (str.length < 2) return '';
    // Node allows for non-padded base64 strings (missing trailing ===), base64-js does not
    while(str.length % 4 !== 0)str = str + '=';
    return str;
}
function utf8ToBytes(string, units) {
    units = units || Infinity;
    let codePoint;
    const length = string.length;
    let leadSurrogate = null;
    const bytes = [];
    for(let i = 0; i < length; ++i){
        codePoint = string.charCodeAt(i);
        // is surrogate component
        if (codePoint > 0xD7FF && codePoint < 0xE000) {
            // last char was a lead
            if (!leadSurrogate) {
                // no lead yet
                if (codePoint > 0xDBFF) {
                    // unexpected trail
                    if ((units -= 3) > -1) bytes.push(0xEF, 0xBF, 0xBD);
                    continue;
                } else if (i + 1 === length) {
                    // unpaired lead
                    if ((units -= 3) > -1) bytes.push(0xEF, 0xBF, 0xBD);
                    continue;
                }
                // valid lead
                leadSurrogate = codePoint;
                continue;
            }
            // 2 leads in a row
            if (codePoint < 0xDC00) {
                if ((units -= 3) > -1) bytes.push(0xEF, 0xBF, 0xBD);
                leadSurrogate = codePoint;
                continue;
            }
            // valid surrogate pair
            codePoint = (leadSurrogate - 0xD800 << 10 | codePoint - 0xDC00) + 0x10000;
        } else if (leadSurrogate) // valid bmp char, but last char was a lead
        {
            if ((units -= 3) > -1) bytes.push(0xEF, 0xBF, 0xBD);
        }
        leadSurrogate = null;
        // encode utf8
        if (codePoint < 0x80) {
            if ((units -= 1) < 0) break;
            bytes.push(codePoint);
        } else if (codePoint < 0x800) {
            if ((units -= 2) < 0) break;
            bytes.push(codePoint >> 0x6 | 0xC0, codePoint & 0x3F | 0x80);
        } else if (codePoint < 0x10000) {
            if ((units -= 3) < 0) break;
            bytes.push(codePoint >> 0xC | 0xE0, codePoint >> 0x6 & 0x3F | 0x80, codePoint & 0x3F | 0x80);
        } else if (codePoint < 0x110000) {
            if ((units -= 4) < 0) break;
            bytes.push(codePoint >> 0x12 | 0xF0, codePoint >> 0xC & 0x3F | 0x80, codePoint >> 0x6 & 0x3F | 0x80, codePoint & 0x3F | 0x80);
        } else throw new Error('Invalid code point');
    }
    return bytes;
}
function asciiToBytes(str) {
    const byteArray = [];
    for(let i = 0; i < str.length; ++i)// Node's code seems to be doing this and not & 0x7F..
    byteArray.push(str.charCodeAt(i) & 0xFF);
    return byteArray;
}
function utf16leToBytes(str, units) {
    let c, hi, lo;
    const byteArray = [];
    for(let i = 0; i < str.length; ++i){
        if ((units -= 2) < 0) break;
        c = str.charCodeAt(i);
        hi = c >> 8;
        lo = c % 256;
        byteArray.push(lo);
        byteArray.push(hi);
    }
    return byteArray;
}
function base64ToBytes(str) {
    return base64.toByteArray(base64clean(str));
}
function blitBuffer(src, dst, offset, length) {
    let i;
    for(i = 0; i < length; ++i){
        if (i + offset >= dst.length || i >= src.length) break;
        dst[i + offset] = src[i];
    }
    return i;
}
// ArrayBuffer or Uint8Array objects from other contexts (i.e. iframes) do not pass
// the `instanceof` check but they should be treated as of that type.
// See: https://github.com/feross/buffer/issues/166
function isInstance(obj, type) {
    return obj instanceof type || obj != null && obj.constructor != null && obj.constructor.name != null && obj.constructor.name === type.name;
}
function numberIsNaN(obj) {
    // For IE11 support
    return obj !== obj // eslint-disable-line no-self-compare
    ;
}
// Create lookup table for `toString('hex')`
// See: https://github.com/feross/buffer/issues/219
const hexSliceLookupTable = function() {
    const alphabet = '0123456789abcdef';
    const table = new Array(256);
    for(let i = 0; i < 16; ++i){
        const i16 = i * 16;
        for(let j = 0; j < 16; ++j)table[i16 + j] = alphabet[i] + alphabet[j];
    }
    return table;
}();
// Return not function with Error if BigInt not supported
function defineBigIntMethod(fn) {
    return typeof BigInt === 'undefined' ? BufferBigIntNotDefined : fn;
}
function BufferBigIntNotDefined() {
    throw new Error('BigInt not supported');
}

},{"base64-js":"eIiSV","ieee754":"cO95r"}],"eIiSV":[function(require,module,exports) {
'use strict';
exports.byteLength = byteLength;
exports.toByteArray = toByteArray;
exports.fromByteArray = fromByteArray;
var lookup = [];
var revLookup = [];
var Arr = typeof Uint8Array !== 'undefined' ? Uint8Array : Array;
var code = 'ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789+/';
for(var i = 0, len = code.length; i < len; ++i){
    lookup[i] = code[i];
    revLookup[code.charCodeAt(i)] = i;
}
// Support decoding URL-safe base64 strings, as Node.js does.
// See: https://en.wikipedia.org/wiki/Base64#URL_applications
revLookup['-'.charCodeAt(0)] = 62;
revLookup['_'.charCodeAt(0)] = 63;
function getLens(b64) {
    var len1 = b64.length;
    if (len1 % 4 > 0) throw new Error('Invalid string. Length must be a multiple of 4');
    // Trim off extra bytes after placeholder bytes are found
    // See: https://github.com/beatgammit/base64-js/issues/42
    var validLen = b64.indexOf('=');
    if (validLen === -1) validLen = len1;
    var placeHoldersLen = validLen === len1 ? 0 : 4 - validLen % 4;
    return [
        validLen,
        placeHoldersLen
    ];
}
// base64 is 4/3 + up to two characters of the original data
function byteLength(b64) {
    var lens = getLens(b64);
    var validLen = lens[0];
    var placeHoldersLen = lens[1];
    return (validLen + placeHoldersLen) * 3 / 4 - placeHoldersLen;
}
function _byteLength(b64, validLen, placeHoldersLen) {
    return (validLen + placeHoldersLen) * 3 / 4 - placeHoldersLen;
}
function toByteArray(b64) {
    var tmp;
    var lens = getLens(b64);
    var validLen = lens[0];
    var placeHoldersLen = lens[1];
    var arr = new Arr(_byteLength(b64, validLen, placeHoldersLen));
    var curByte = 0;
    // if there are placeholders, only get up to the last complete 4 chars
    var len2 = placeHoldersLen > 0 ? validLen - 4 : validLen;
    var i1;
    for(i1 = 0; i1 < len2; i1 += 4){
        tmp = revLookup[b64.charCodeAt(i1)] << 18 | revLookup[b64.charCodeAt(i1 + 1)] << 12 | revLookup[b64.charCodeAt(i1 + 2)] << 6 | revLookup[b64.charCodeAt(i1 + 3)];
        arr[curByte++] = tmp >> 16 & 0xFF;
        arr[curByte++] = tmp >> 8 & 0xFF;
        arr[curByte++] = tmp & 0xFF;
    }
    if (placeHoldersLen === 2) {
        tmp = revLookup[b64.charCodeAt(i1)] << 2 | revLookup[b64.charCodeAt(i1 + 1)] >> 4;
        arr[curByte++] = tmp & 0xFF;
    }
    if (placeHoldersLen === 1) {
        tmp = revLookup[b64.charCodeAt(i1)] << 10 | revLookup[b64.charCodeAt(i1 + 1)] << 4 | revLookup[b64.charCodeAt(i1 + 2)] >> 2;
        arr[curByte++] = tmp >> 8 & 0xFF;
        arr[curByte++] = tmp & 0xFF;
    }
    return arr;
}
function tripletToBase64(num) {
    return lookup[num >> 18 & 0x3F] + lookup[num >> 12 & 0x3F] + lookup[num >> 6 & 0x3F] + lookup[num & 0x3F];
}
function encodeChunk(uint8, start, end) {
    var tmp;
    var output = [];
    for(var i2 = start; i2 < end; i2 += 3){
        tmp = (uint8[i2] << 16 & 0xFF0000) + (uint8[i2 + 1] << 8 & 0xFF00) + (uint8[i2 + 2] & 0xFF);
        output.push(tripletToBase64(tmp));
    }
    return output.join('');
}
function fromByteArray(uint8) {
    var tmp;
    var len3 = uint8.length;
    var extraBytes = len3 % 3 // if we have 1 byte left, pad 2 bytes
    ;
    var parts = [];
    var maxChunkLength = 16383 // must be multiple of 3
    ;
    // go through the array every three bytes, we'll deal with trailing stuff later
    for(var i3 = 0, len2 = len3 - extraBytes; i3 < len2; i3 += maxChunkLength)parts.push(encodeChunk(uint8, i3, i3 + maxChunkLength > len2 ? len2 : i3 + maxChunkLength));
    // pad the end with zeros, but make sure to not forget the extra bytes
    if (extraBytes === 1) {
        tmp = uint8[len3 - 1];
        parts.push(lookup[tmp >> 2] + lookup[tmp << 4 & 0x3F] + '==');
    } else if (extraBytes === 2) {
        tmp = (uint8[len3 - 2] << 8) + uint8[len3 - 1];
        parts.push(lookup[tmp >> 10] + lookup[tmp >> 4 & 0x3F] + lookup[tmp << 2 & 0x3F] + '=');
    }
    return parts.join('');
}

},{}],"cO95r":[function(require,module,exports) {
/*! ieee754. BSD-3-Clause License. Feross Aboukhadijeh <https://feross.org/opensource> */ exports.read = function(buffer, offset, isLE, mLen, nBytes) {
    var e, m;
    var eLen = nBytes * 8 - mLen - 1;
    var eMax = (1 << eLen) - 1;
    var eBias = eMax >> 1;
    var nBits = -7;
    var i = isLE ? nBytes - 1 : 0;
    var d = isLE ? -1 : 1;
    var s = buffer[offset + i];
    i += d;
    e = s & (1 << -nBits) - 1;
    s >>= -nBits;
    nBits += eLen;
    for(; nBits > 0; e = e * 256 + buffer[offset + i], i += d, nBits -= 8);
    m = e & (1 << -nBits) - 1;
    e >>= -nBits;
    nBits += mLen;
    for(; nBits > 0; m = m * 256 + buffer[offset + i], i += d, nBits -= 8);
    if (e === 0) e = 1 - eBias;
    else if (e === eMax) return m ? NaN : (s ? -1 : 1) * Infinity;
    else {
        m = m + Math.pow(2, mLen);
        e = e - eBias;
    }
    return (s ? -1 : 1) * m * Math.pow(2, e - mLen);
};
exports.write = function(buffer, value, offset, isLE, mLen, nBytes) {
    var e, m, c;
    var eLen = nBytes * 8 - mLen - 1;
    var eMax = (1 << eLen) - 1;
    var eBias = eMax >> 1;
    var rt = mLen === 23 ? Math.pow(2, -24) - Math.pow(2, -77) : 0;
    var i = isLE ? 0 : nBytes - 1;
    var d = isLE ? 1 : -1;
    var s = value < 0 || value === 0 && 1 / value < 0 ? 1 : 0;
    value = Math.abs(value);
    if (isNaN(value) || value === Infinity) {
        m = isNaN(value) ? 1 : 0;
        e = eMax;
    } else {
        e = Math.floor(Math.log(value) / Math.LN2);
        if (value * (c = Math.pow(2, -e)) < 1) {
            e--;
            c *= 2;
        }
        if (e + eBias >= 1) value += rt / c;
        else value += rt * Math.pow(2, 1 - eBias);
        if (value * c >= 2) {
            e++;
            c /= 2;
        }
        if (e + eBias >= eMax) {
            m = 0;
            e = eMax;
        } else if (e + eBias >= 1) {
            m = (value * c - 1) * Math.pow(2, mLen);
            e = e + eBias;
        } else {
            m = value * Math.pow(2, eBias - 1) * Math.pow(2, mLen);
            e = 0;
        }
    }
    for(; mLen >= 8; buffer[offset + i] = m & 0xff, i += d, m /= 256, mLen -= 8);
    e = e << mLen | m;
    eLen += mLen;
    for(; eLen > 0; buffer[offset + i] = e & 0xff, i += d, e /= 256, eLen -= 8);
    buffer[offset + i - d] |= s * 128;
};

},{}],"5RFyz":[function(require,module,exports) {
var parcelHelpers = require("@parcel/transformer-js/src/esmodule-helpers.js");
parcelHelpers.defineInteropFlag(exports);
parcelHelpers.export(exports, "parse", ()=>parse
);
// imported from https://github.com/galkn/parseuri
/**
 * Parses an URI
 *
 * @author Steven Levithan <stevenlevithan.com> (MIT license)
 * @api private
 */ const re = /^(?:(?![^:@]+:[^:@\/]*@)(http|https|ws|wss):\/\/)?((?:(([^:@]*)(?::([^:@]*))?)?@)?((?:[a-f0-9]{0,4}:){2,7}[a-f0-9]{0,4}|[^:\/?#]*)(?::(\d*))?)(((\/(?:[^?#](?![^?#\/]*\.[^?#\/.]+(?:[?#]|$)))*\/?)?([^?#\/]*))(?:\?([^#]*))?(?:#(.*))?)/;
const parts = [
    'source',
    'protocol',
    'authority',
    'userInfo',
    'user',
    'password',
    'host',
    'port',
    'relative',
    'path',
    'directory',
    'file',
    'query',
    'anchor'
];
function parse(str) {
    const src = str, b = str.indexOf('['), e = str.indexOf(']');
    if (b != -1 && e != -1) str = str.substring(0, b) + str.substring(b, e).replace(/:/g, ';') + str.substring(e, str.length);
    let m = re.exec(str || ''), uri = {}, i = 14;
    while(i--)uri[parts[i]] = m[i] || '';
    if (b != -1 && e != -1) {
        uri.source = src;
        uri.host = uri.host.substring(1, uri.host.length - 1).replace(/;/g, ':');
        uri.authority = uri.authority.replace('[', '').replace(']', '').replace(/;/g, ':');
        uri.ipv6uri = true;
    }
    uri.pathNames = pathNames(uri, uri['path']);
    uri.queryKey = queryKey(uri, uri['query']);
    return uri;
}
function pathNames(obj, path) {
    const regx = /\/{2,9}/g, names = path.replace(regx, "/").split("/");
    if (path.substr(0, 1) == '/' || path.length === 0) names.splice(0, 1);
    if (path.substr(path.length - 1, 1) == '/') names.splice(names.length - 1, 1);
    return names;
}
function queryKey(uri, query) {
    const data = {};
    query.replace(/(?:^|&)([^&=]*)=?([^&]*)/g, function($0, $1, $2) {
        if ($1) data[$1] = $2;
    });
    return data;
}

},{"@parcel/transformer-js/src/esmodule-helpers.js":"gkKU3"}],"94vh9":[function(require,module,exports) {
var parcelHelpers = require("@parcel/transformer-js/src/esmodule-helpers.js");
parcelHelpers.defineInteropFlag(exports);
parcelHelpers.export(exports, "Manager", ()=>Manager
);
var _engineIoClient = require("engine.io-client");
var _socketJs = require("./socket.js");
var _socketIoParser = require("socket.io-parser");
var _onJs = require("./on.js");
var _backo2Js = require("./contrib/backo2.js");
var _componentEmitter = require("@socket.io/component-emitter");
class Manager extends _componentEmitter.Emitter {
    constructor(uri, opts){
        var _a;
        super();
        this.nsps = {};
        this.subs = [];
        if (uri && "object" === typeof uri) {
            opts = uri;
            uri = undefined;
        }
        opts = opts || {};
        opts.path = opts.path || "/socket.io";
        this.opts = opts;
        _engineIoClient.installTimerFunctions(this, opts);
        this.reconnection(opts.reconnection !== false);
        this.reconnectionAttempts(opts.reconnectionAttempts || Infinity);
        this.reconnectionDelay(opts.reconnectionDelay || 1000);
        this.reconnectionDelayMax(opts.reconnectionDelayMax || 5000);
        this.randomizationFactor((_a = opts.randomizationFactor) !== null && _a !== void 0 ? _a : 0.5);
        this.backoff = new _backo2Js.Backoff({
            min: this.reconnectionDelay(),
            max: this.reconnectionDelayMax(),
            jitter: this.randomizationFactor()
        });
        this.timeout(null == opts.timeout ? 20000 : opts.timeout);
        this._readyState = "closed";
        this.uri = uri;
        const _parser = opts.parser || _socketIoParser;
        this.encoder = new _parser.Encoder();
        this.decoder = new _parser.Decoder();
        this._autoConnect = opts.autoConnect !== false;
        if (this._autoConnect) this.open();
    }
    reconnection(v) {
        if (!arguments.length) return this._reconnection;
        this._reconnection = !!v;
        return this;
    }
    reconnectionAttempts(v) {
        if (v === undefined) return this._reconnectionAttempts;
        this._reconnectionAttempts = v;
        return this;
    }
    reconnectionDelay(v) {
        var _a;
        if (v === undefined) return this._reconnectionDelay;
        this._reconnectionDelay = v;
        (_a = this.backoff) === null || _a === void 0 || _a.setMin(v);
        return this;
    }
    randomizationFactor(v) {
        var _a;
        if (v === undefined) return this._randomizationFactor;
        this._randomizationFactor = v;
        (_a = this.backoff) === null || _a === void 0 || _a.setJitter(v);
        return this;
    }
    reconnectionDelayMax(v) {
        var _a;
        if (v === undefined) return this._reconnectionDelayMax;
        this._reconnectionDelayMax = v;
        (_a = this.backoff) === null || _a === void 0 || _a.setMax(v);
        return this;
    }
    timeout(v) {
        if (!arguments.length) return this._timeout;
        this._timeout = v;
        return this;
    }
    /**
     * Starts trying to reconnect if reconnection is enabled and we have not
     * started reconnecting yet
     *
     * @private
     */ maybeReconnectOnOpen() {
        // Only try to reconnect if it's the first time we're connecting
        if (!this._reconnecting && this._reconnection && this.backoff.attempts === 0) // keeps reconnection from firing twice for the same reconnection loop
        this.reconnect();
    }
    /**
     * Sets the current transport `socket`.
     *
     * @param {Function} fn - optional, callback
     * @return self
     * @public
     */ open(fn) {
        if (~this._readyState.indexOf("open")) return this;
        this.engine = new _engineIoClient.Socket(this.uri, this.opts);
        const socket = this.engine;
        const self = this;
        this._readyState = "opening";
        this.skipReconnect = false;
        // emit `open`
        const openSubDestroy = _onJs.on(socket, "open", function() {
            self.onopen();
            fn && fn();
        });
        // emit `error`
        const errorSub = _onJs.on(socket, "error", (err)=>{
            self.cleanup();
            self._readyState = "closed";
            this.emitReserved("error", err);
            if (fn) fn(err);
            else // Only do this if there is no fn to handle the error
            self.maybeReconnectOnOpen();
        });
        if (false !== this._timeout) {
            const timeout = this._timeout;
            if (timeout === 0) openSubDestroy(); // prevents a race condition with the 'open' event
            // set timer
            const timer = this.setTimeoutFn(()=>{
                openSubDestroy();
                socket.close();
                // @ts-ignore
                socket.emit("error", new Error("timeout"));
            }, timeout);
            if (this.opts.autoUnref) timer.unref();
            this.subs.push(function subDestroy() {
                clearTimeout(timer);
            });
        }
        this.subs.push(openSubDestroy);
        this.subs.push(errorSub);
        return this;
    }
    /**
     * Alias for open()
     *
     * @return self
     * @public
     */ connect(fn) {
        return this.open(fn);
    }
    /**
     * Called upon transport open.
     *
     * @private
     */ onopen() {
        // clear old subs
        this.cleanup();
        // mark as open
        this._readyState = "open";
        this.emitReserved("open");
        // add new subs
        const socket = this.engine;
        this.subs.push(_onJs.on(socket, "ping", this.onping.bind(this)), _onJs.on(socket, "data", this.ondata.bind(this)), _onJs.on(socket, "error", this.onerror.bind(this)), _onJs.on(socket, "close", this.onclose.bind(this)), _onJs.on(this.decoder, "decoded", this.ondecoded.bind(this)));
    }
    /**
     * Called upon a ping.
     *
     * @private
     */ onping() {
        this.emitReserved("ping");
    }
    /**
     * Called with data.
     *
     * @private
     */ ondata(data) {
        this.decoder.add(data);
    }
    /**
     * Called when parser fully decodes a packet.
     *
     * @private
     */ ondecoded(packet) {
        this.emitReserved("packet", packet);
    }
    /**
     * Called upon socket error.
     *
     * @private
     */ onerror(err) {
        this.emitReserved("error", err);
    }
    /**
     * Creates a new socket for the given `nsp`.
     *
     * @return {Socket}
     * @public
     */ socket(nsp, opts) {
        let socket = this.nsps[nsp];
        if (!socket) {
            socket = new _socketJs.Socket(this, nsp, opts);
            this.nsps[nsp] = socket;
        }
        return socket;
    }
    /**
     * Called upon a socket close.
     *
     * @param socket
     * @private
     */ _destroy(socket) {
        const nsps = Object.keys(this.nsps);
        for (const nsp of nsps){
            const socket = this.nsps[nsp];
            if (socket.active) return;
        }
        this._close();
    }
    /**
     * Writes a packet.
     *
     * @param packet
     * @private
     */ _packet(packet) {
        const encodedPackets = this.encoder.encode(packet);
        for(let i = 0; i < encodedPackets.length; i++)this.engine.write(encodedPackets[i], packet.options);
    }
    /**
     * Clean up transport subscriptions and packet buffer.
     *
     * @private
     */ cleanup() {
        this.subs.forEach((subDestroy)=>subDestroy()
        );
        this.subs.length = 0;
        this.decoder.destroy();
    }
    /**
     * Close the current socket.
     *
     * @private
     */ _close() {
        this.skipReconnect = true;
        this._reconnecting = false;
        this.onclose("forced close");
        if (this.engine) this.engine.close();
    }
    /**
     * Alias for close()
     *
     * @private
     */ disconnect() {
        return this._close();
    }
    /**
     * Called upon engine close.
     *
     * @private
     */ onclose(reason, description) {
        this.cleanup();
        this.backoff.reset();
        this._readyState = "closed";
        this.emitReserved("close", reason, description);
        if (this._reconnection && !this.skipReconnect) this.reconnect();
    }
    /**
     * Attempt a reconnection.
     *
     * @private
     */ reconnect() {
        if (this._reconnecting || this.skipReconnect) return this;
        const self = this;
        if (this.backoff.attempts >= this._reconnectionAttempts) {
            this.backoff.reset();
            this.emitReserved("reconnect_failed");
            this._reconnecting = false;
        } else {
            const delay = this.backoff.duration();
            this._reconnecting = true;
            const timer = this.setTimeoutFn(()=>{
                if (self.skipReconnect) return;
                this.emitReserved("reconnect_attempt", self.backoff.attempts);
                // check again for the case socket closed in above events
                if (self.skipReconnect) return;
                self.open((err)=>{
                    if (err) {
                        self._reconnecting = false;
                        self.reconnect();
                        this.emitReserved("reconnect_error", err);
                    } else self.onreconnect();
                });
            }, delay);
            if (this.opts.autoUnref) timer.unref();
            this.subs.push(function subDestroy() {
                clearTimeout(timer);
            });
        }
    }
    /**
     * Called upon successful reconnect.
     *
     * @private
     */ onreconnect() {
        const attempt = this.backoff.attempts;
        this._reconnecting = false;
        this.backoff.reset();
        this.emitReserved("reconnect", attempt);
    }
}

},{"engine.io-client":"jBHFs","./socket.js":"kbWgu","socket.io-parser":"2lQL3","./on.js":"8FFT1","./contrib/backo2.js":"cZLsm","@socket.io/component-emitter":"dzQbR","@parcel/transformer-js/src/esmodule-helpers.js":"gkKU3"}],"kbWgu":[function(require,module,exports) {
var parcelHelpers = require("@parcel/transformer-js/src/esmodule-helpers.js");
parcelHelpers.defineInteropFlag(exports);
parcelHelpers.export(exports, "Socket", ()=>Socket
);
var _socketIoParser = require("socket.io-parser");
var _onJs = require("./on.js");
var _componentEmitter = require("@socket.io/component-emitter");
/**
 * Internal events.
 * These events can't be emitted by the user.
 */ const RESERVED_EVENTS = Object.freeze({
    connect: 1,
    connect_error: 1,
    disconnect: 1,
    disconnecting: 1,
    // EventEmitter reserved events: https://nodejs.org/api/events.html#events_event_newlistener
    newListener: 1,
    removeListener: 1
});
class Socket extends _componentEmitter.Emitter {
    /**
     * `Socket` constructor.
     *
     * @public
     */ constructor(io, nsp, opts){
        super();
        this.connected = false;
        this.receiveBuffer = [];
        this.sendBuffer = [];
        this.ids = 0;
        this.acks = {};
        this.flags = {};
        this.io = io;
        this.nsp = nsp;
        if (opts && opts.auth) this.auth = opts.auth;
        if (this.io._autoConnect) this.open();
    }
    /**
     * Whether the socket is currently disconnected
     */ get disconnected() {
        return !this.connected;
    }
    /**
     * Subscribe to open, close and packet events
     *
     * @private
     */ subEvents() {
        if (this.subs) return;
        const io = this.io;
        this.subs = [
            _onJs.on(io, "open", this.onopen.bind(this)),
            _onJs.on(io, "packet", this.onpacket.bind(this)),
            _onJs.on(io, "error", this.onerror.bind(this)),
            _onJs.on(io, "close", this.onclose.bind(this)), 
        ];
    }
    /**
     * Whether the Socket will try to reconnect when its Manager connects or reconnects
     */ get active() {
        return !!this.subs;
    }
    /**
     * "Opens" the socket.
     *
     * @public
     */ connect() {
        if (this.connected) return this;
        this.subEvents();
        if (!this.io["_reconnecting"]) this.io.open(); // ensure open
        if ("open" === this.io._readyState) this.onopen();
        return this;
    }
    /**
     * Alias for connect()
     */ open() {
        return this.connect();
    }
    /**
     * Sends a `message` event.
     *
     * @return self
     * @public
     */ send(...args) {
        args.unshift("message");
        this.emit.apply(this, args);
        return this;
    }
    /**
     * Override `emit`.
     * If the event is in `events`, it's emitted normally.
     *
     * @return self
     * @public
     */ emit(ev, ...args) {
        if (RESERVED_EVENTS.hasOwnProperty(ev)) throw new Error('"' + ev + '" is a reserved event name');
        args.unshift(ev);
        const packet = {
            type: _socketIoParser.PacketType.EVENT,
            data: args
        };
        packet.options = {};
        packet.options.compress = this.flags.compress !== false;
        // event ack callback
        if ("function" === typeof args[args.length - 1]) {
            const id = this.ids++;
            const ack = args.pop();
            this._registerAckCallback(id, ack);
            packet.id = id;
        }
        const isTransportWritable = this.io.engine && this.io.engine.transport && this.io.engine.transport.writable;
        const discardPacket = this.flags.volatile && (!isTransportWritable || !this.connected);
        if (discardPacket) ;
        else if (this.connected) {
            this.notifyOutgoingListeners(packet);
            this.packet(packet);
        } else this.sendBuffer.push(packet);
        this.flags = {};
        return this;
    }
    /**
     * @private
     */ _registerAckCallback(id, ack) {
        const timeout = this.flags.timeout;
        if (timeout === undefined) {
            this.acks[id] = ack;
            return;
        }
        // @ts-ignore
        const timer = this.io.setTimeoutFn(()=>{
            delete this.acks[id];
            for(let i = 0; i < this.sendBuffer.length; i++)if (this.sendBuffer[i].id === id) this.sendBuffer.splice(i, 1);
            ack.call(this, new Error("operation has timed out"));
        }, timeout);
        this.acks[id] = (...args)=>{
            // @ts-ignore
            this.io.clearTimeoutFn(timer);
            ack.apply(this, [
                null,
                ...args
            ]);
        };
    }
    /**
     * Sends a packet.
     *
     * @param packet
     * @private
     */ packet(packet) {
        packet.nsp = this.nsp;
        this.io._packet(packet);
    }
    /**
     * Called upon engine `open`.
     *
     * @private
     */ onopen() {
        if (typeof this.auth == "function") this.auth((data)=>{
            this.packet({
                type: _socketIoParser.PacketType.CONNECT,
                data
            });
        });
        else this.packet({
            type: _socketIoParser.PacketType.CONNECT,
            data: this.auth
        });
    }
    /**
     * Called upon engine or manager `error`.
     *
     * @param err
     * @private
     */ onerror(err) {
        if (!this.connected) this.emitReserved("connect_error", err);
    }
    /**
     * Called upon engine `close`.
     *
     * @param reason
     * @param description
     * @private
     */ onclose(reason, description) {
        this.connected = false;
        delete this.id;
        this.emitReserved("disconnect", reason, description);
    }
    /**
     * Called with socket packet.
     *
     * @param packet
     * @private
     */ onpacket(packet) {
        const sameNamespace = packet.nsp === this.nsp;
        if (!sameNamespace) return;
        switch(packet.type){
            case _socketIoParser.PacketType.CONNECT:
                if (packet.data && packet.data.sid) {
                    const id = packet.data.sid;
                    this.onconnect(id);
                } else this.emitReserved("connect_error", new Error("It seems you are trying to reach a Socket.IO server in v2.x with a v3.x client, but they are not compatible (more information here: https://socket.io/docs/v3/migrating-from-2-x-to-3-0/)"));
                break;
            case _socketIoParser.PacketType.EVENT:
            case _socketIoParser.PacketType.BINARY_EVENT:
                this.onevent(packet);
                break;
            case _socketIoParser.PacketType.ACK:
            case _socketIoParser.PacketType.BINARY_ACK:
                this.onack(packet);
                break;
            case _socketIoParser.PacketType.DISCONNECT:
                this.ondisconnect();
                break;
            case _socketIoParser.PacketType.CONNECT_ERROR:
                this.destroy();
                const err = new Error(packet.data.message);
                // @ts-ignore
                err.data = packet.data.data;
                this.emitReserved("connect_error", err);
                break;
        }
    }
    /**
     * Called upon a server event.
     *
     * @param packet
     * @private
     */ onevent(packet) {
        const args = packet.data || [];
        if (null != packet.id) args.push(this.ack(packet.id));
        if (this.connected) this.emitEvent(args);
        else this.receiveBuffer.push(Object.freeze(args));
    }
    emitEvent(args) {
        if (this._anyListeners && this._anyListeners.length) {
            const listeners = this._anyListeners.slice();
            for (const listener of listeners)listener.apply(this, args);
        }
        super.emit.apply(this, args);
    }
    /**
     * Produces an ack callback to emit with an event.
     *
     * @private
     */ ack(id) {
        const self = this;
        let sent = false;
        return function(...args) {
            // prevent double callbacks
            if (sent) return;
            sent = true;
            self.packet({
                type: _socketIoParser.PacketType.ACK,
                id: id,
                data: args
            });
        };
    }
    /**
     * Called upon a server acknowlegement.
     *
     * @param packet
     * @private
     */ onack(packet) {
        const ack = this.acks[packet.id];
        if ("function" === typeof ack) {
            ack.apply(this, packet.data);
            delete this.acks[packet.id];
        }
    }
    /**
     * Called upon server connect.
     *
     * @private
     */ onconnect(id) {
        this.id = id;
        this.connected = true;
        this.emitBuffered();
        this.emitReserved("connect");
    }
    /**
     * Emit buffered events (received and emitted).
     *
     * @private
     */ emitBuffered() {
        this.receiveBuffer.forEach((args)=>this.emitEvent(args)
        );
        this.receiveBuffer = [];
        this.sendBuffer.forEach((packet)=>{
            this.notifyOutgoingListeners(packet);
            this.packet(packet);
        });
        this.sendBuffer = [];
    }
    /**
     * Called upon server disconnect.
     *
     * @private
     */ ondisconnect() {
        this.destroy();
        this.onclose("io server disconnect");
    }
    /**
     * Called upon forced client/server side disconnections,
     * this method ensures the manager stops tracking us and
     * that reconnections don't get triggered for this.
     *
     * @private
     */ destroy() {
        if (this.subs) {
            // clean subscriptions to avoid reconnections
            this.subs.forEach((subDestroy)=>subDestroy()
            );
            this.subs = undefined;
        }
        this.io["_destroy"](this);
    }
    /**
     * Disconnects the socket manually.
     *
     * @return self
     * @public
     */ disconnect() {
        if (this.connected) this.packet({
            type: _socketIoParser.PacketType.DISCONNECT
        });
        // remove socket from pool
        this.destroy();
        if (this.connected) // fire events
        this.onclose("io client disconnect");
        return this;
    }
    /**
     * Alias for disconnect()
     *
     * @return self
     * @public
     */ close() {
        return this.disconnect();
    }
    /**
     * Sets the compress flag.
     *
     * @param compress - if `true`, compresses the sending data
     * @return self
     * @public
     */ compress(compress) {
        this.flags.compress = compress;
        return this;
    }
    /**
     * Sets a modifier for a subsequent event emission that the event message will be dropped when this socket is not
     * ready to send messages.
     *
     * @returns self
     * @public
     */ get volatile() {
        this.flags.volatile = true;
        return this;
    }
    /**
     * Sets a modifier for a subsequent event emission that the callback will be called with an error when the
     * given number of milliseconds have elapsed without an acknowledgement from the server:
     *
     * ```
     * socket.timeout(5000).emit("my-event", (err) => {
     *   if (err) {
     *     // the server did not acknowledge the event in the given delay
     *   }
     * });
     * ```
     *
     * @returns self
     * @public
     */ timeout(timeout) {
        this.flags.timeout = timeout;
        return this;
    }
    /**
     * Adds a listener that will be fired when any event is emitted. The event name is passed as the first argument to the
     * callback.
     *
     * @param listener
     * @public
     */ onAny(listener) {
        this._anyListeners = this._anyListeners || [];
        this._anyListeners.push(listener);
        return this;
    }
    /**
     * Adds a listener that will be fired when any event is emitted. The event name is passed as the first argument to the
     * callback. The listener is added to the beginning of the listeners array.
     *
     * @param listener
     * @public
     */ prependAny(listener) {
        this._anyListeners = this._anyListeners || [];
        this._anyListeners.unshift(listener);
        return this;
    }
    /**
     * Removes the listener that will be fired when any event is emitted.
     *
     * @param listener
     * @public
     */ offAny(listener) {
        if (!this._anyListeners) return this;
        if (listener) {
            const listeners = this._anyListeners;
            for(let i = 0; i < listeners.length; i++)if (listener === listeners[i]) {
                listeners.splice(i, 1);
                return this;
            }
        } else this._anyListeners = [];
        return this;
    }
    /**
     * Returns an array of listeners that are listening for any event that is specified. This array can be manipulated,
     * e.g. to remove listeners.
     *
     * @public
     */ listenersAny() {
        return this._anyListeners || [];
    }
    /**
     * Adds a listener that will be fired when any event is emitted. The event name is passed as the first argument to the
     * callback.
     *
     * @param listener
     *
     * <pre><code>
     *
     * socket.onAnyOutgoing((event, ...args) => {
     *   console.log(event);
     * });
     *
     * </pre></code>
     *
     * @public
     */ onAnyOutgoing(listener) {
        this._anyOutgoingListeners = this._anyOutgoingListeners || [];
        this._anyOutgoingListeners.push(listener);
        return this;
    }
    /**
     * Adds a listener that will be fired when any event is emitted. The event name is passed as the first argument to the
     * callback. The listener is added to the beginning of the listeners array.
     *
     * @param listener
     *
     * <pre><code>
     *
     * socket.prependAnyOutgoing((event, ...args) => {
     *   console.log(event);
     * });
     *
     * </pre></code>
     *
     * @public
     */ prependAnyOutgoing(listener) {
        this._anyOutgoingListeners = this._anyOutgoingListeners || [];
        this._anyOutgoingListeners.unshift(listener);
        return this;
    }
    /**
     * Removes the listener that will be fired when any event is emitted.
     *
     * @param listener
     *
     * <pre><code>
     *
     * const handler = (event, ...args) => {
     *   console.log(event);
     * }
     *
     * socket.onAnyOutgoing(handler);
     *
     * // then later
     * socket.offAnyOutgoing(handler);
     *
     * </pre></code>
     *
     * @public
     */ offAnyOutgoing(listener) {
        if (!this._anyOutgoingListeners) return this;
        if (listener) {
            const listeners = this._anyOutgoingListeners;
            for(let i = 0; i < listeners.length; i++)if (listener === listeners[i]) {
                listeners.splice(i, 1);
                return this;
            }
        } else this._anyOutgoingListeners = [];
        return this;
    }
    /**
     * Returns an array of listeners that are listening for any event that is specified. This array can be manipulated,
     * e.g. to remove listeners.
     *
     * @public
     */ listenersAnyOutgoing() {
        return this._anyOutgoingListeners || [];
    }
    /**
     * Notify the listeners for each packet sent
     *
     * @param packet
     *
     * @private
     */ notifyOutgoingListeners(packet) {
        if (this._anyOutgoingListeners && this._anyOutgoingListeners.length) {
            const listeners = this._anyOutgoingListeners.slice();
            for (const listener of listeners)listener.apply(this, packet.data);
        }
    }
}

},{"socket.io-parser":"2lQL3","./on.js":"8FFT1","@socket.io/component-emitter":"dzQbR","@parcel/transformer-js/src/esmodule-helpers.js":"gkKU3"}],"2lQL3":[function(require,module,exports) {
var parcelHelpers = require("@parcel/transformer-js/src/esmodule-helpers.js");
parcelHelpers.defineInteropFlag(exports);
parcelHelpers.export(exports, "protocol", ()=>protocol
);
parcelHelpers.export(exports, "PacketType", ()=>PacketType
);
/**
 * A socket.io Encoder instance
 */ parcelHelpers.export(exports, "Encoder", ()=>Encoder
);
/**
 * A socket.io Decoder instance
 *
 * @return {Object} decoder
 */ parcelHelpers.export(exports, "Decoder", ()=>Decoder
);
var _componentEmitter = require("@socket.io/component-emitter");
var _binaryJs = require("./binary.js");
var _isBinaryJs = require("./is-binary.js");
const protocol = 5;
var PacketType;
(function(PacketType1) {
    PacketType1[PacketType1["CONNECT"] = 0] = "CONNECT";
    PacketType1[PacketType1["DISCONNECT"] = 1] = "DISCONNECT";
    PacketType1[PacketType1["EVENT"] = 2] = "EVENT";
    PacketType1[PacketType1["ACK"] = 3] = "ACK";
    PacketType1[PacketType1["CONNECT_ERROR"] = 4] = "CONNECT_ERROR";
    PacketType1[PacketType1["BINARY_EVENT"] = 5] = "BINARY_EVENT";
    PacketType1[PacketType1["BINARY_ACK"] = 6] = "BINARY_ACK";
})(PacketType || (PacketType = {}));
class Encoder {
    /**
     * Encoder constructor
     *
     * @param {function} replacer - custom replacer to pass down to JSON.parse
     */ constructor(replacer){
        this.replacer = replacer;
    }
    /**
     * Encode a packet as a single string if non-binary, or as a
     * buffer sequence, depending on packet type.
     *
     * @param {Object} obj - packet object
     */ encode(obj) {
        if (obj.type === PacketType.EVENT || obj.type === PacketType.ACK) {
            if (_isBinaryJs.hasBinary(obj)) {
                obj.type = obj.type === PacketType.EVENT ? PacketType.BINARY_EVENT : PacketType.BINARY_ACK;
                return this.encodeAsBinary(obj);
            }
        }
        return [
            this.encodeAsString(obj)
        ];
    }
    /**
     * Encode packet as string.
     */ encodeAsString(obj) {
        // first is type
        let str = "" + obj.type;
        // attachments if we have them
        if (obj.type === PacketType.BINARY_EVENT || obj.type === PacketType.BINARY_ACK) str += obj.attachments + "-";
        // if we have a namespace other than `/`
        // we append it followed by a comma `,`
        if (obj.nsp && "/" !== obj.nsp) str += obj.nsp + ",";
        // immediately followed by the id
        if (null != obj.id) str += obj.id;
        // json data
        if (null != obj.data) str += JSON.stringify(obj.data, this.replacer);
        return str;
    }
    /**
     * Encode packet as 'buffer sequence' by removing blobs, and
     * deconstructing packet into object with placeholders and
     * a list of buffers.
     */ encodeAsBinary(obj) {
        const deconstruction = _binaryJs.deconstructPacket(obj);
        const pack = this.encodeAsString(deconstruction.packet);
        const buffers = deconstruction.buffers;
        buffers.unshift(pack); // add packet info to beginning of data list
        return buffers; // write all the buffers
    }
}
class Decoder extends _componentEmitter.Emitter {
    /**
     * Decoder constructor
     *
     * @param {function} reviver - custom reviver to pass down to JSON.stringify
     */ constructor(reviver){
        super();
        this.reviver = reviver;
    }
    /**
     * Decodes an encoded packet string into packet JSON.
     *
     * @param {String} obj - encoded packet
     */ add(obj) {
        let packet;
        if (typeof obj === "string") {
            packet = this.decodeString(obj);
            if (packet.type === PacketType.BINARY_EVENT || packet.type === PacketType.BINARY_ACK) {
                // binary packet's json
                this.reconstructor = new BinaryReconstructor(packet);
                // no attachments, labeled binary but no binary data to follow
                if (packet.attachments === 0) super.emitReserved("decoded", packet);
            } else // non-binary full packet
            super.emitReserved("decoded", packet);
        } else if (_isBinaryJs.isBinary(obj) || obj.base64) {
            // raw binary data
            if (!this.reconstructor) throw new Error("got binary data when not reconstructing a packet");
            else {
                packet = this.reconstructor.takeBinaryData(obj);
                if (packet) {
                    // received final buffer
                    this.reconstructor = null;
                    super.emitReserved("decoded", packet);
                }
            }
        } else throw new Error("Unknown type: " + obj);
    }
    /**
     * Decode a packet String (JSON data)
     *
     * @param {String} str
     * @return {Object} packet
     */ decodeString(str) {
        let i = 0;
        // look up type
        const p = {
            type: Number(str.charAt(0))
        };
        if (PacketType[p.type] === undefined) throw new Error("unknown packet type " + p.type);
        // look up attachments if type binary
        if (p.type === PacketType.BINARY_EVENT || p.type === PacketType.BINARY_ACK) {
            const start = i + 1;
            while(str.charAt(++i) !== "-" && i != str.length);
            const buf = str.substring(start, i);
            if (buf != Number(buf) || str.charAt(i) !== "-") throw new Error("Illegal attachments");
            p.attachments = Number(buf);
        }
        // look up namespace (if any)
        if ("/" === str.charAt(i + 1)) {
            const start = i + 1;
            while(++i){
                const c = str.charAt(i);
                if ("," === c) break;
                if (i === str.length) break;
            }
            p.nsp = str.substring(start, i);
        } else p.nsp = "/";
        // look up id
        const next = str.charAt(i + 1);
        if ("" !== next && Number(next) == next) {
            const start = i + 1;
            while(++i){
                const c = str.charAt(i);
                if (null == c || Number(c) != c) {
                    --i;
                    break;
                }
                if (i === str.length) break;
            }
            p.id = Number(str.substring(start, i + 1));
        }
        // look up json data
        if (str.charAt(++i)) {
            const payload = this.tryParse(str.substr(i));
            if (Decoder.isPayloadValid(p.type, payload)) p.data = payload;
            else throw new Error("invalid payload");
        }
        return p;
    }
    tryParse(str) {
        try {
            return JSON.parse(str, this.reviver);
        } catch (e) {
            return false;
        }
    }
    static isPayloadValid(type, payload) {
        switch(type){
            case PacketType.CONNECT:
                return typeof payload === "object";
            case PacketType.DISCONNECT:
                return payload === undefined;
            case PacketType.CONNECT_ERROR:
                return typeof payload === "string" || typeof payload === "object";
            case PacketType.EVENT:
            case PacketType.BINARY_EVENT:
                return Array.isArray(payload) && payload.length > 0;
            case PacketType.ACK:
            case PacketType.BINARY_ACK:
                return Array.isArray(payload);
        }
    }
    /**
     * Deallocates a parser's resources
     */ destroy() {
        if (this.reconstructor) this.reconstructor.finishedReconstruction();
    }
}
/**
 * A manager of a binary event's 'buffer sequence'. Should
 * be constructed whenever a packet of type BINARY_EVENT is
 * decoded.
 *
 * @param {Object} packet
 * @return {BinaryReconstructor} initialized reconstructor
 */ class BinaryReconstructor {
    constructor(packet){
        this.packet = packet;
        this.buffers = [];
        this.reconPack = packet;
    }
    /**
     * Method to be called when binary data received from connection
     * after a BINARY_EVENT packet.
     *
     * @param {Buffer | ArrayBuffer} binData - the raw binary data received
     * @return {null | Object} returns null if more binary data is expected or
     *   a reconstructed packet object if all buffers have been received.
     */ takeBinaryData(binData) {
        this.buffers.push(binData);
        if (this.buffers.length === this.reconPack.attachments) {
            // done with buffer list
            const packet = _binaryJs.reconstructPacket(this.reconPack, this.buffers);
            this.finishedReconstruction();
            return packet;
        }
        return null;
    }
    /**
     * Cleans up binary packet reconstruction variables.
     */ finishedReconstruction() {
        this.reconPack = null;
        this.buffers = [];
    }
}

},{"@socket.io/component-emitter":"dzQbR","./binary.js":"juaze","./is-binary.js":"0mMso","@parcel/transformer-js/src/esmodule-helpers.js":"gkKU3"}],"juaze":[function(require,module,exports) {
var parcelHelpers = require("@parcel/transformer-js/src/esmodule-helpers.js");
parcelHelpers.defineInteropFlag(exports);
/**
 * Replaces every Buffer | ArrayBuffer | Blob | File in packet with a numbered placeholder.
 *
 * @param {Object} packet - socket.io event packet
 * @return {Object} with deconstructed packet and list of buffers
 * @public
 */ parcelHelpers.export(exports, "deconstructPacket", ()=>deconstructPacket
);
/**
 * Reconstructs a binary packet from its placeholder packet and buffers
 *
 * @param {Object} packet - event packet with placeholders
 * @param {Array} buffers - binary buffers to put in placeholder positions
 * @return {Object} reconstructed packet
 * @public
 */ parcelHelpers.export(exports, "reconstructPacket", ()=>reconstructPacket
);
var _isBinaryJs = require("./is-binary.js");
function deconstructPacket(packet) {
    const buffers = [];
    const packetData = packet.data;
    const pack = packet;
    pack.data = _deconstructPacket(packetData, buffers);
    pack.attachments = buffers.length; // number of binary 'attachments'
    return {
        packet: pack,
        buffers: buffers
    };
}
function _deconstructPacket(data, buffers) {
    if (!data) return data;
    if (_isBinaryJs.isBinary(data)) {
        const placeholder = {
            _placeholder: true,
            num: buffers.length
        };
        buffers.push(data);
        return placeholder;
    } else if (Array.isArray(data)) {
        const newData = new Array(data.length);
        for(let i = 0; i < data.length; i++)newData[i] = _deconstructPacket(data[i], buffers);
        return newData;
    } else if (typeof data === "object" && !(data instanceof Date)) {
        const newData = {};
        for(const key in data)if (Object.prototype.hasOwnProperty.call(data, key)) newData[key] = _deconstructPacket(data[key], buffers);
        return newData;
    }
    return data;
}
function reconstructPacket(packet, buffers) {
    packet.data = _reconstructPacket(packet.data, buffers);
    packet.attachments = undefined; // no longer useful
    return packet;
}
function _reconstructPacket(data, buffers) {
    if (!data) return data;
    if (data && data._placeholder) return buffers[data.num]; // appropriate buffer (should be natural order anyway)
    else if (Array.isArray(data)) for(let i = 0; i < data.length; i++)data[i] = _reconstructPacket(data[i], buffers);
    else if (typeof data === "object") {
        for(const key in data)if (Object.prototype.hasOwnProperty.call(data, key)) data[key] = _reconstructPacket(data[key], buffers);
    }
    return data;
}

},{"./is-binary.js":"0mMso","@parcel/transformer-js/src/esmodule-helpers.js":"gkKU3"}],"0mMso":[function(require,module,exports) {
var parcelHelpers = require("@parcel/transformer-js/src/esmodule-helpers.js");
parcelHelpers.defineInteropFlag(exports);
/**
 * Returns true if obj is a Buffer, an ArrayBuffer, a Blob or a File.
 *
 * @private
 */ parcelHelpers.export(exports, "isBinary", ()=>isBinary
);
parcelHelpers.export(exports, "hasBinary", ()=>hasBinary
);
const withNativeArrayBuffer = typeof ArrayBuffer === "function";
const isView = (obj)=>{
    return typeof ArrayBuffer.isView === "function" ? ArrayBuffer.isView(obj) : obj.buffer instanceof ArrayBuffer;
};
const toString = Object.prototype.toString;
const withNativeBlob = typeof Blob === "function" || typeof Blob !== "undefined" && toString.call(Blob) === "[object BlobConstructor]";
const withNativeFile = typeof File === "function" || typeof File !== "undefined" && toString.call(File) === "[object FileConstructor]";
function isBinary(obj) {
    return withNativeArrayBuffer && (obj instanceof ArrayBuffer || isView(obj)) || withNativeBlob && obj instanceof Blob || withNativeFile && obj instanceof File;
}
function hasBinary(obj, toJSON) {
    if (!obj || typeof obj !== "object") return false;
    if (Array.isArray(obj)) {
        for(let i = 0, l = obj.length; i < l; i++){
            if (hasBinary(obj[i])) return true;
        }
        return false;
    }
    if (isBinary(obj)) return true;
    if (obj.toJSON && typeof obj.toJSON === "function" && arguments.length === 1) return hasBinary(obj.toJSON(), true);
    for(const key in obj){
        if (Object.prototype.hasOwnProperty.call(obj, key) && hasBinary(obj[key])) return true;
    }
    return false;
}

},{"@parcel/transformer-js/src/esmodule-helpers.js":"gkKU3"}],"8FFT1":[function(require,module,exports) {
var parcelHelpers = require("@parcel/transformer-js/src/esmodule-helpers.js");
parcelHelpers.defineInteropFlag(exports);
parcelHelpers.export(exports, "on", ()=>on
);
function on(obj, ev, fn) {
    obj.on(ev, fn);
    return function subDestroy() {
        obj.off(ev, fn);
    };
}

},{"@parcel/transformer-js/src/esmodule-helpers.js":"gkKU3"}],"cZLsm":[function(require,module,exports) {
var parcelHelpers = require("@parcel/transformer-js/src/esmodule-helpers.js");
parcelHelpers.defineInteropFlag(exports);
/**
 * Initialize backoff timer with `opts`.
 *
 * - `min` initial timeout in milliseconds [100]
 * - `max` max timeout [10000]
 * - `jitter` [0]
 * - `factor` [2]
 *
 * @param {Object} opts
 * @api public
 */ parcelHelpers.export(exports, "Backoff", ()=>Backoff
);
function Backoff(opts) {
    opts = opts || {};
    this.ms = opts.min || 100;
    this.max = opts.max || 10000;
    this.factor = opts.factor || 2;
    this.jitter = opts.jitter > 0 && opts.jitter <= 1 ? opts.jitter : 0;
    this.attempts = 0;
}
/**
 * Return the backoff duration.
 *
 * @return {Number}
 * @api public
 */ Backoff.prototype.duration = function() {
    var ms = this.ms * Math.pow(this.factor, this.attempts++);
    if (this.jitter) {
        var rand = Math.random();
        var deviation = Math.floor(rand * this.jitter * ms);
        ms = (Math.floor(rand * 10) & 1) == 0 ? ms - deviation : ms + deviation;
    }
    return Math.min(ms, this.max) | 0;
};
/**
 * Reset the number of attempts.
 *
 * @api public
 */ Backoff.prototype.reset = function() {
    this.attempts = 0;
};
/**
 * Set the minimum duration
 *
 * @api public
 */ Backoff.prototype.setMin = function(min) {
    this.ms = min;
};
/**
 * Set the maximum duration
 *
 * @api public
 */ Backoff.prototype.setMax = function(max) {
    this.max = max;
};
/**
 * Set the jitter
 *
 * @api public
 */ Backoff.prototype.setJitter = function(jitter) {
    this.jitter = jitter;
};

},{"@parcel/transformer-js/src/esmodule-helpers.js":"gkKU3"}],"8GZZG":[function(require,module,exports) {
var $parcel$ReactRefreshHelpers$7602 = require("@parcel/transformer-react-refresh-wrap/lib/helpers/helpers.js");
var prevRefreshReg = window.$RefreshReg$;
var prevRefreshSig = window.$RefreshSig$;
$parcel$ReactRefreshHelpers$7602.prelude(module);

try {
var parcelHelpers = require("@parcel/transformer-js/src/esmodule-helpers.js");
parcelHelpers.defineInteropFlag(exports);
var _jsxDevRuntime = require("react/jsx-dev-runtime");
var _react = require("react");
var _reactDefault = parcelHelpers.interopDefault(_react);
var _axios = require("axios");
var _axiosDefault = parcelHelpers.interopDefault(_axios);
var _reactScrollToBottom = require("react-scroll-to-bottom");
var _reactScrollToBottomDefault = parcelHelpers.interopDefault(_reactScrollToBottom);
var _s = $RefreshSig$();
function GroupChat({ socket , username , room  }) {
    _s();
    const [currentMessage, setCurrentMessage] = _react.useState("");
    const [messageList, setMessageList] = _react.useState([]);
    const [savedMsg, setSavedMsg] = _react.useState([]);
    const sendMessage = async ()=>{
        if (currentMessage !== "") {
            const messageData = {
                room: room,
                author: username,
                message: currentMessage,
                time: new Date(Date.now()).getHours() + ":" + new Date(Date.now()).getMinutes()
            };
            console.log(messageData);
            _axiosDefault.default.post("http://localhost:5000/save/chatMsg", messageData).then(()=>{
            //alert('Message Save Successful');
            }).catch((err)=>{
                alert(err);
            });
            await socket.emit("send_message", messageData);
            setMessageList((list)=>[
                    ...list,
                    messageData
                ]
            );
            setCurrentMessage("");
        }
    };
    _react.useEffect(()=>{
        socket.on("receive_message", (data)=>{
            setMessageList((list)=>[
                    ...list,
                    data
                ]
            );
        });
    }, [
        socket
    ]);
    const getPreMsg = ()=>{
        _axiosDefault.default.get(`http://localhost:5000/chatMsg/get/${room}`).then((res)=>{
            setSavedMsg(res.data);
        });
        console.log(savedMsg);
    };
    // const arr = savedMsg.map((data, index) => {
    //   return(
    //     <tr>
    //       <td>{data.message}</td>
    //       <td>{data.author}</td>
    //       <td>{data.time}</td>
    //     </tr>
    //   )
    // })
    return /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
        className: "chat-window",
        children: [
            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("br", {}, void 0, false, {
                fileName: "src/components/IT20127046/user/GroupChat.js",
                lineNumber: 66,
                columnNumber: 9
            }, this),
            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                className: "chat-body",
                children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV(_reactScrollToBottomDefault.default, {
                    className: "message-container",
                    children: [
                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("button", {
                            onClick: getPreMsg,
                            className: "preMessage",
                            children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("i", {
                                class: "fa fa-chevron-up fa-2x",
                                "aria-hidden": "true"
                            }, void 0, false, {
                                fileName: "src/components/IT20127046/user/GroupChat.js",
                                lineNumber: 72,
                                columnNumber: 64
                            }, this)
                        }, void 0, false, {
                            fileName: "src/components/IT20127046/user/GroupChat.js",
                            lineNumber: 72,
                            columnNumber: 13
                        }, this),
                        savedMsg.map((data, index)=>{
                            return /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                className: "message",
                                id: username === data.author ? "you" : "other",
                                children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                    children: [
                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                            className: "message-content",
                                            children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("p", {
                                                children: data.message
                                            }, void 0, false, {
                                                fileName: "src/components/IT20127046/user/GroupChat.js",
                                                lineNumber: 79,
                                                columnNumber: 25
                                            }, this)
                                        }, void 0, false, {
                                            fileName: "src/components/IT20127046/user/GroupChat.js",
                                            lineNumber: 78,
                                            columnNumber: 23
                                        }, this),
                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                            className: "message-meta",
                                            children: [
                                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("p", {
                                                    id: "time",
                                                    children: data.author
                                                }, void 0, false, {
                                                    fileName: "src/components/IT20127046/user/GroupChat.js",
                                                    lineNumber: 82,
                                                    columnNumber: 25
                                                }, this),
                                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("p", {
                                                    id: "author",
                                                    children: data.time
                                                }, void 0, false, {
                                                    fileName: "src/components/IT20127046/user/GroupChat.js",
                                                    lineNumber: 83,
                                                    columnNumber: 25
                                                }, this)
                                            ]
                                        }, void 0, true, {
                                            fileName: "src/components/IT20127046/user/GroupChat.js",
                                            lineNumber: 81,
                                            columnNumber: 23
                                        }, this)
                                    ]
                                }, void 0, true, {
                                    fileName: "src/components/IT20127046/user/GroupChat.js",
                                    lineNumber: 77,
                                    columnNumber: 19
                                }, this)
                            }, void 0, false, {
                                fileName: "src/components/IT20127046/user/GroupChat.js",
                                lineNumber: 76,
                                columnNumber: 17
                            }, this);
                        }),
                        messageList.map((messageContent)=>{
                            return /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                className: "message",
                                id: username === messageContent.author ? "you" : "other",
                                children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                    children: [
                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                            className: "message-content",
                                            children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("p", {
                                                children: messageContent.message
                                            }, void 0, false, {
                                                fileName: "src/components/IT20127046/user/GroupChat.js",
                                                lineNumber: 98,
                                                columnNumber: 23
                                            }, this)
                                        }, void 0, false, {
                                            fileName: "src/components/IT20127046/user/GroupChat.js",
                                            lineNumber: 97,
                                            columnNumber: 21
                                        }, this),
                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                            className: "message-meta",
                                            children: [
                                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("p", {
                                                    id: "time",
                                                    children: messageContent.time
                                                }, void 0, false, {
                                                    fileName: "src/components/IT20127046/user/GroupChat.js",
                                                    lineNumber: 101,
                                                    columnNumber: 23
                                                }, this),
                                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("p", {
                                                    id: "author",
                                                    children: messageContent.author
                                                }, void 0, false, {
                                                    fileName: "src/components/IT20127046/user/GroupChat.js",
                                                    lineNumber: 102,
                                                    columnNumber: 23
                                                }, this)
                                            ]
                                        }, void 0, true, {
                                            fileName: "src/components/IT20127046/user/GroupChat.js",
                                            lineNumber: 100,
                                            columnNumber: 21
                                        }, this)
                                    ]
                                }, void 0, true, {
                                    fileName: "src/components/IT20127046/user/GroupChat.js",
                                    lineNumber: 96,
                                    columnNumber: 19
                                }, this)
                            }, void 0, false, {
                                fileName: "src/components/IT20127046/user/GroupChat.js",
                                lineNumber: 92,
                                columnNumber: 17
                            }, this);
                        })
                    ]
                }, void 0, true, {
                    fileName: "src/components/IT20127046/user/GroupChat.js",
                    lineNumber: 70,
                    columnNumber: 11
                }, this)
            }, void 0, false, {
                fileName: "src/components/IT20127046/user/GroupChat.js",
                lineNumber: 68,
                columnNumber: 9
            }, this),
            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("br", {}, void 0, false, {
                fileName: "src/components/IT20127046/user/GroupChat.js",
                lineNumber: 109,
                columnNumber: 15
            }, this),
            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("br", {}, void 0, false, {
                fileName: "src/components/IT20127046/user/GroupChat.js",
                lineNumber: 109,
                columnNumber: 20
            }, this),
            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                className: "chat-footer",
                children: [
                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("input", {
                        type: "text",
                        value: currentMessage,
                        placeholder: "Hey...",
                        onChange: (event)=>{
                            setCurrentMessage(event.target.value);
                        },
                        onKeyPress: (event)=>{
                            event.key === "Enter" && sendMessage();
                        }
                    }, void 0, false, {
                        fileName: "src/components/IT20127046/user/GroupChat.js",
                        lineNumber: 112,
                        columnNumber: 11
                    }, this),
                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("button", {
                        onClick: sendMessage,
                        children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("i", {
                            class: "fa fa-paper-plane",
                            "aria-hidden": "true"
                        }, void 0, false, {
                            fileName: "src/components/IT20127046/user/GroupChat.js",
                            lineNumber: 123,
                            columnNumber: 41
                        }, this)
                    }, void 0, false, {
                        fileName: "src/components/IT20127046/user/GroupChat.js",
                        lineNumber: 123,
                        columnNumber: 11
                    }, this)
                ]
            }, void 0, true, {
                fileName: "src/components/IT20127046/user/GroupChat.js",
                lineNumber: 111,
                columnNumber: 9
            }, this)
        ]
    }, void 0, true, {
        fileName: "src/components/IT20127046/user/GroupChat.js",
        lineNumber: 64,
        columnNumber: 7
    }, this);
}
exports.default = GroupChat;
_s(GroupChat, "lb66S7uu8QVTj7avQbrwDRAUxIE=");
_c = GroupChat;
var _c;
$RefreshReg$(_c, "GroupChat");

  $parcel$ReactRefreshHelpers$7602.postlude(module);
} finally {
  window.$RefreshReg$ = prevRefreshReg;
  window.$RefreshSig$ = prevRefreshSig;
}
},{"react/jsx-dev-runtime":"iTorj","react":"21dqq","axios":"jo6P5","react-scroll-to-bottom":"ht5tR","@parcel/transformer-js/src/esmodule-helpers.js":"gkKU3","@parcel/transformer-react-refresh-wrap/lib/helpers/helpers.js":"km3Ru"}],"ht5tR":[function(require,module,exports) {
var parcelHelpers = require("@parcel/transformer-js/src/esmodule-helpers.js");
parcelHelpers.defineInteropFlag(exports);
parcelHelpers.export(exports, "AutoHideFollowButton", ()=>_autoHideFollowButtonDefault.default
);
parcelHelpers.export(exports, "Composer", ()=>_composerDefault.default
);
parcelHelpers.export(exports, "FunctionContext", ()=>_functionContextDefault.default
);
parcelHelpers.export(exports, "Panel", ()=>_panelDefault.default
);
parcelHelpers.export(exports, "StateContext", ()=>_stateContextDefault.default
);
parcelHelpers.export(exports, "useAnimating", ()=>_useAnimatingDefault.default
);
parcelHelpers.export(exports, "useAnimatingToEnd", ()=>_useAnimatingToEndDefault.default
);
parcelHelpers.export(exports, "useAtBottom", ()=>_useAtBottomDefault.default
);
parcelHelpers.export(exports, "useAtEnd", ()=>_useAtEndDefault.default
);
parcelHelpers.export(exports, "useAtStart", ()=>_useAtStartDefault.default
);
parcelHelpers.export(exports, "useAtTop", ()=>_useAtTopDefault.default
);
parcelHelpers.export(exports, "useMode", ()=>_useModeDefault.default
);
parcelHelpers.export(exports, "useObserveScrollPosition", ()=>_useObserveScrollPositionDefault.default
);
parcelHelpers.export(exports, "useScrollTo", ()=>_useScrollToDefault.default
);
parcelHelpers.export(exports, "useScrollToBottom", ()=>_useScrollToBottomDefault.default
);
parcelHelpers.export(exports, "useScrollToEnd", ()=>_useScrollToEndDefault.default
);
parcelHelpers.export(exports, "useScrollToStart", ()=>_useScrollToStartDefault.default
);
parcelHelpers.export(exports, "useScrollToTop", ()=>_useScrollToTopDefault.default
);
parcelHelpers.export(exports, "useSticky", ()=>_useStickyDefault.default
);
var _addVersionToMetaTag = require("./addVersionToMetaTag");
var _addVersionToMetaTagDefault = parcelHelpers.interopDefault(_addVersionToMetaTag);
var _autoHideFollowButton = require("./ScrollToBottom/AutoHideFollowButton");
var _autoHideFollowButtonDefault = parcelHelpers.interopDefault(_autoHideFollowButton);
var _basicScrollToBottom = require("./BasicScrollToBottom");
var _basicScrollToBottomDefault = parcelHelpers.interopDefault(_basicScrollToBottom);
var _composer = require("./ScrollToBottom/Composer");
var _composerDefault = parcelHelpers.interopDefault(_composer);
var _functionContext = require("./ScrollToBottom/FunctionContext");
var _functionContextDefault = parcelHelpers.interopDefault(_functionContext);
var _panel = require("./ScrollToBottom/Panel");
var _panelDefault = parcelHelpers.interopDefault(_panel);
var _stateContext = require("./ScrollToBottom/StateContext");
var _stateContextDefault = parcelHelpers.interopDefault(_stateContext);
var _useAnimating = require("./hooks/useAnimating");
var _useAnimatingDefault = parcelHelpers.interopDefault(_useAnimating);
var _useAnimatingToEnd = require("./hooks/useAnimatingToEnd");
var _useAnimatingToEndDefault = parcelHelpers.interopDefault(_useAnimatingToEnd);
var _useAtBottom = require("./hooks/useAtBottom");
var _useAtBottomDefault = parcelHelpers.interopDefault(_useAtBottom);
var _useAtEnd = require("./hooks/useAtEnd");
var _useAtEndDefault = parcelHelpers.interopDefault(_useAtEnd);
var _useAtStart = require("./hooks/useAtStart");
var _useAtStartDefault = parcelHelpers.interopDefault(_useAtStart);
var _useAtTop = require("./hooks/useAtTop");
var _useAtTopDefault = parcelHelpers.interopDefault(_useAtTop);
var _useMode = require("./hooks/useMode");
var _useModeDefault = parcelHelpers.interopDefault(_useMode);
var _useObserveScrollPosition = require("./hooks/useObserveScrollPosition");
var _useObserveScrollPositionDefault = parcelHelpers.interopDefault(_useObserveScrollPosition);
var _useScrollTo = require("./hooks/useScrollTo");
var _useScrollToDefault = parcelHelpers.interopDefault(_useScrollTo);
var _useScrollToBottom = require("./hooks/useScrollToBottom");
var _useScrollToBottomDefault = parcelHelpers.interopDefault(_useScrollToBottom);
var _useScrollToEnd = require("./hooks/useScrollToEnd");
var _useScrollToEndDefault = parcelHelpers.interopDefault(_useScrollToEnd);
var _useScrollToStart = require("./hooks/useScrollToStart");
var _useScrollToStartDefault = parcelHelpers.interopDefault(_useScrollToStart);
var _useScrollToTop = require("./hooks/useScrollToTop");
var _useScrollToTopDefault = parcelHelpers.interopDefault(_useScrollToTop);
var _useSticky = require("./hooks/useSticky");
var _useStickyDefault = parcelHelpers.interopDefault(_useSticky);
exports.default = _basicScrollToBottomDefault.default;
_addVersionToMetaTagDefault.default();

},{"./addVersionToMetaTag":"6w0rK","./ScrollToBottom/AutoHideFollowButton":"fIwZZ","./BasicScrollToBottom":"6XrE5","./ScrollToBottom/Composer":"ds9c1","./ScrollToBottom/FunctionContext":"kFpSF","./ScrollToBottom/Panel":"cLIsq","./ScrollToBottom/StateContext":"58zlr","./hooks/useAnimating":"f32Wy","./hooks/useAnimatingToEnd":"akM5y","./hooks/useAtBottom":"4HyyS","./hooks/useAtEnd":"andwH","./hooks/useAtStart":"8zBlc","./hooks/useAtTop":"a6yzr","./hooks/useMode":"9Harq","./hooks/useObserveScrollPosition":"i4hpz","./hooks/useScrollTo":"72iJH","./hooks/useScrollToBottom":"g5UMp","./hooks/useScrollToEnd":"6A30V","./hooks/useScrollToStart":"2XEqu","./hooks/useScrollToTop":"kDVCC","./hooks/useSticky":"1N64J","@parcel/transformer-js/src/esmodule-helpers.js":"gkKU3"}],"6w0rK":[function(require,module,exports) {
var parcelHelpers = require("@parcel/transformer-js/src/esmodule-helpers.js");
parcelHelpers.defineInteropFlag(exports);
var global = arguments[3];
/* global global:readonly, process:readonly */ /* eslint no-empty: ["error", { "allowEmptyCatch": true }] */ function setMetaTag(name, content) {
    try {
        var _global = global, document = _global.document;
        if (typeof document !== 'undefined' && document.createElement && document.head && document.head.appendChild) {
            var meta = document.querySelector("html meta[name=\"".concat(encodeURI(name), "\"]")) || document.createElement('meta');
            meta.setAttribute('name', name);
            meta.setAttribute('content', content);
            document.head.appendChild(meta);
        }
    } catch (err) {}
}
function addVersionToMetaTag() {
    setMetaTag('react-scroll-to-bottom:version', "4.2.0");
}
exports.default = addVersionToMetaTag;

},{"@parcel/transformer-js/src/esmodule-helpers.js":"gkKU3"}],"fIwZZ":[function(require,module,exports) {
var parcelHelpers = require("@parcel/transformer-js/src/esmodule-helpers.js");
parcelHelpers.defineInteropFlag(exports);
var _slicedToArray = require("@babel/runtime-corejs3/helpers/slicedToArray");
var _slicedToArrayDefault = parcelHelpers.interopDefault(_slicedToArray);
var _classnames = require("classnames");
var _classnamesDefault = parcelHelpers.interopDefault(_classnames);
var _propTypes = require("prop-types");
var _propTypesDefault = parcelHelpers.interopDefault(_propTypes);
var _react = require("react");
var _reactDefault = parcelHelpers.interopDefault(_react);
var _useScrollToEnd = require("../hooks/useScrollToEnd");
var _useScrollToEndDefault = parcelHelpers.interopDefault(_useScrollToEnd);
var _useSticky = require("../hooks/useSticky");
var _useStickyDefault = parcelHelpers.interopDefault(_useSticky);
var _useStyleToClassName = require("../hooks/internal/useStyleToClassName");
var _useStyleToClassNameDefault = parcelHelpers.interopDefault(_useStyleToClassName);
var ROOT_STYLE = {
    backgroundColor: 'rgba(0, 0, 0, .2)',
    borderRadius: 10,
    borderWidth: 0,
    bottom: 5,
    cursor: 'pointer',
    height: 20,
    outline: 0,
    position: 'absolute',
    right: 20,
    width: 20,
    '&:hover': {
        backgroundColor: 'rgba(0, 0, 0, .4)'
    },
    '&:active': {
        backgroundColor: 'rgba(0, 0, 0, .6)'
    }
};
var AutoHideFollowButton = function AutoHideFollowButton(_ref) {
    var children = _ref.children, className = _ref.className;
    var _useSticky1 = _useStickyDefault.default(), _useSticky2 = _slicedToArrayDefault.default(_useSticky1, 1), sticky = _useSticky2[0];
    var rootCSS = _useStyleToClassNameDefault.default()(ROOT_STYLE);
    var scrollToEnd = _useScrollToEndDefault.default();
    return !sticky && /*#__PURE__*/ _reactDefault.default.createElement("button", {
        className: _classnamesDefault.default(rootCSS, (className || '') + ''),
        onClick: scrollToEnd,
        type: "button"
    }, children);
};
AutoHideFollowButton.defaultProps = {
    children: undefined,
    className: ''
};
AutoHideFollowButton.propTypes = {
    children: _propTypesDefault.default.any,
    className: _propTypesDefault.default.string
};
exports.default = AutoHideFollowButton;

},{"@babel/runtime-corejs3/helpers/slicedToArray":"36E7Q","classnames":"jocGM","prop-types":"2ipKq","react":"21dqq","../hooks/useScrollToEnd":"6A30V","../hooks/useSticky":"1N64J","../hooks/internal/useStyleToClassName":"7aM5G","@parcel/transformer-js/src/esmodule-helpers.js":"gkKU3"}],"36E7Q":[function(require,module,exports) {
var arrayWithHoles = require("./arrayWithHoles.js");
var iterableToArrayLimit = require("./iterableToArrayLimit.js");
var unsupportedIterableToArray = require("./unsupportedIterableToArray.js");
var nonIterableRest = require("./nonIterableRest.js");
function _slicedToArray(arr, i) {
    return arrayWithHoles(arr) || iterableToArrayLimit(arr, i) || unsupportedIterableToArray(arr, i) || nonIterableRest();
}
module.exports = _slicedToArray, module.exports.__esModule = true, module.exports["default"] = module.exports;

},{"./arrayWithHoles.js":"bc8rY","./iterableToArrayLimit.js":"1k8Yk","./unsupportedIterableToArray.js":"1HHW3","./nonIterableRest.js":"dmp8i"}],"bc8rY":[function(require,module,exports) {
var _Array$isArray = require("@babel/runtime-corejs3/core-js/array/is-array");
function _arrayWithHoles(arr) {
    if (_Array$isArray(arr)) return arr;
}
module.exports = _arrayWithHoles, module.exports.__esModule = true, module.exports["default"] = module.exports;

},{"@babel/runtime-corejs3/core-js/array/is-array":"NmEBJ"}],"NmEBJ":[function(require,module,exports) {
module.exports = require("core-js-pure/features/array/is-array");

},{"core-js-pure/features/array/is-array":"iRGSS"}],"iRGSS":[function(require,module,exports) {
module.exports = require('../../full/array/is-array');

},{"../../full/array/is-array":"k17HN"}],"k17HN":[function(require,module,exports) {
var parent = require('../../actual/array/is-array');
module.exports = parent;

},{"../../actual/array/is-array":"7quIe"}],"7quIe":[function(require,module,exports) {
var parent = require('../../stable/array/is-array');
module.exports = parent;

},{"../../stable/array/is-array":"8ekNG"}],"8ekNG":[function(require,module,exports) {
var parent = require('../../es/array/is-array');
module.exports = parent;

},{"../../es/array/is-array":"4GbqU"}],"4GbqU":[function(require,module,exports) {
require('../../modules/es.array.is-array');
var path = require('../../internals/path');
module.exports = path.Array.isArray;

},{"../../modules/es.array.is-array":"bvNL1","../../internals/path":"8QO0z"}],"bvNL1":[function(require,module,exports) {
var $ = require('../internals/export');
var isArray = require('../internals/is-array');
// `Array.isArray` method
// https://tc39.es/ecma262/#sec-array.isarray
$({
    target: 'Array',
    stat: true
}, {
    isArray: isArray
});

},{"../internals/export":"jjems","../internals/is-array":"byYNs"}],"jjems":[function(require,module,exports) {
'use strict';
var global = require('../internals/global');
var apply = require('../internals/function-apply');
var uncurryThis = require('../internals/function-uncurry-this');
var isCallable = require('../internals/is-callable');
var getOwnPropertyDescriptor = require('../internals/object-get-own-property-descriptor').f;
var isForced = require('../internals/is-forced');
var path = require('../internals/path');
var bind = require('../internals/function-bind-context');
var createNonEnumerableProperty = require('../internals/create-non-enumerable-property');
var hasOwn = require('../internals/has-own-property');
var wrapConstructor = function(NativeConstructor) {
    var Wrapper = function(a, b, c) {
        if (this instanceof Wrapper) {
            switch(arguments.length){
                case 0:
                    return new NativeConstructor();
                case 1:
                    return new NativeConstructor(a);
                case 2:
                    return new NativeConstructor(a, b);
            }
            return new NativeConstructor(a, b, c);
        }
        return apply(NativeConstructor, this, arguments);
    };
    Wrapper.prototype = NativeConstructor.prototype;
    return Wrapper;
};
/*
  options.target      - name of the target object
  options.global      - target is the global object
  options.stat        - export as static methods of target
  options.proto       - export as prototype methods of target
  options.real        - real prototype method for the `pure` version
  options.forced      - export even if the native feature is available
  options.bind        - bind methods to the target, required for the `pure` version
  options.wrap        - wrap constructors to preventing global pollution, required for the `pure` version
  options.unsafe      - use the simple assignment of property instead of delete + defineProperty
  options.sham        - add a flag to not completely full polyfills
  options.enumerable  - export as enumerable property
  options.noTargetGet - prevent calling a getter on target
  options.name        - the .name of the function if it does not match the key
*/ module.exports = function(options, source) {
    var TARGET = options.target;
    var GLOBAL = options.global;
    var STATIC = options.stat;
    var PROTO = options.proto;
    var nativeSource = GLOBAL ? global : STATIC ? global[TARGET] : (global[TARGET] || {}).prototype;
    var target = GLOBAL ? path : path[TARGET] || createNonEnumerableProperty(path, TARGET, {})[TARGET];
    var targetPrototype = target.prototype;
    var FORCED, USE_NATIVE, VIRTUAL_PROTOTYPE;
    var key, sourceProperty, targetProperty, nativeProperty, resultProperty, descriptor;
    for(key in source){
        FORCED = isForced(GLOBAL ? key : TARGET + (STATIC ? '.' : '#') + key, options.forced);
        // contains in native
        USE_NATIVE = !FORCED && nativeSource && hasOwn(nativeSource, key);
        targetProperty = target[key];
        if (USE_NATIVE) {
            if (options.noTargetGet) {
                descriptor = getOwnPropertyDescriptor(nativeSource, key);
                nativeProperty = descriptor && descriptor.value;
            } else nativeProperty = nativeSource[key];
        }
        // export native or implementation
        sourceProperty = USE_NATIVE && nativeProperty ? nativeProperty : source[key];
        if (USE_NATIVE && typeof targetProperty == typeof sourceProperty) continue;
        // bind timers to global for call from export context
        if (options.bind && USE_NATIVE) resultProperty = bind(sourceProperty, global);
        else if (options.wrap && USE_NATIVE) resultProperty = wrapConstructor(sourceProperty);
        else if (PROTO && isCallable(sourceProperty)) resultProperty = uncurryThis(sourceProperty);
        else resultProperty = sourceProperty;
        // add a flag to not completely full polyfills
        if (options.sham || sourceProperty && sourceProperty.sham || targetProperty && targetProperty.sham) createNonEnumerableProperty(resultProperty, 'sham', true);
        createNonEnumerableProperty(target, key, resultProperty);
        if (PROTO) {
            VIRTUAL_PROTOTYPE = TARGET + 'Prototype';
            if (!hasOwn(path, VIRTUAL_PROTOTYPE)) createNonEnumerableProperty(path, VIRTUAL_PROTOTYPE, {});
            // export virtual prototype methods
            createNonEnumerableProperty(path[VIRTUAL_PROTOTYPE], key, sourceProperty);
            // export real prototype methods
            if (options.real && targetPrototype && !targetPrototype[key]) createNonEnumerableProperty(targetPrototype, key, sourceProperty);
        }
    }
};

},{"../internals/global":"avqB7","../internals/function-apply":"kLBNq","../internals/function-uncurry-this":"5P4uT","../internals/is-callable":"fqYwo","../internals/object-get-own-property-descriptor":"bs1FJ","../internals/is-forced":"1D6eY","../internals/path":"8QO0z","../internals/function-bind-context":"jZFzY","../internals/create-non-enumerable-property":"2bHDS","../internals/has-own-property":"lacXY"}],"avqB7":[function(require,module,exports) {
var global = arguments[3];
var check = function(it) {
    return it && it.Math == Math && it;
};
// https://github.com/zloirock/core-js/issues/86#issuecomment-115759028
module.exports = // eslint-disable-next-line es-x/no-global-this -- safe
check(typeof globalThis == 'object' && globalThis) || check(typeof window == 'object' && window) || // eslint-disable-next-line no-restricted-globals -- safe
check(typeof self == 'object' && self) || check(typeof global == 'object' && global) || // eslint-disable-next-line no-new-func -- fallback
function() {
    return this;
}() || Function('return this')();

},{}],"kLBNq":[function(require,module,exports) {
var NATIVE_BIND = require('../internals/function-bind-native');
var FunctionPrototype = Function.prototype;
var apply = FunctionPrototype.apply;
var call = FunctionPrototype.call;
// eslint-disable-next-line es-x/no-reflect -- safe
module.exports = typeof Reflect == 'object' && Reflect.apply || (NATIVE_BIND ? call.bind(apply) : function() {
    return call.apply(apply, arguments);
});

},{"../internals/function-bind-native":"bFMAq"}],"bFMAq":[function(require,module,exports) {
var fails = require('../internals/fails');
module.exports = !fails(function() {
    // eslint-disable-next-line es-x/no-function-prototype-bind -- safe
    var test = (function() {}).bind();
    // eslint-disable-next-line no-prototype-builtins -- safe
    return typeof test != 'function' || test.hasOwnProperty('prototype');
});

},{"../internals/fails":"luPpk"}],"luPpk":[function(require,module,exports) {
module.exports = function(exec) {
    try {
        return !!exec();
    } catch (error) {
        return true;
    }
};

},{}],"5P4uT":[function(require,module,exports) {
var NATIVE_BIND = require('../internals/function-bind-native');
var FunctionPrototype = Function.prototype;
var bind = FunctionPrototype.bind;
var call = FunctionPrototype.call;
var uncurryThis = NATIVE_BIND && bind.bind(call, call);
module.exports = NATIVE_BIND ? function(fn) {
    return fn && uncurryThis(fn);
} : function(fn) {
    return fn && function() {
        return call.apply(fn, arguments);
    };
};

},{"../internals/function-bind-native":"bFMAq"}],"fqYwo":[function(require,module,exports) {
// `IsCallable` abstract operation
// https://tc39.es/ecma262/#sec-iscallable
module.exports = function(argument) {
    return typeof argument == 'function';
};

},{}],"bs1FJ":[function(require,module,exports) {
var DESCRIPTORS = require('../internals/descriptors');
var call = require('../internals/function-call');
var propertyIsEnumerableModule = require('../internals/object-property-is-enumerable');
var createPropertyDescriptor = require('../internals/create-property-descriptor');
var toIndexedObject = require('../internals/to-indexed-object');
var toPropertyKey = require('../internals/to-property-key');
var hasOwn = require('../internals/has-own-property');
var IE8_DOM_DEFINE = require('../internals/ie8-dom-define');
// eslint-disable-next-line es-x/no-object-getownpropertydescriptor -- safe
var $getOwnPropertyDescriptor = Object.getOwnPropertyDescriptor;
// `Object.getOwnPropertyDescriptor` method
// https://tc39.es/ecma262/#sec-object.getownpropertydescriptor
exports.f = DESCRIPTORS ? $getOwnPropertyDescriptor : function getOwnPropertyDescriptor(O, P) {
    O = toIndexedObject(O);
    P = toPropertyKey(P);
    if (IE8_DOM_DEFINE) try {
        return $getOwnPropertyDescriptor(O, P);
    } catch (error) {}
    if (hasOwn(O, P)) return createPropertyDescriptor(!call(propertyIsEnumerableModule.f, O, P), O[P]);
};

},{"../internals/descriptors":"87WOB","../internals/function-call":"acNwo","../internals/object-property-is-enumerable":"lvpvk","../internals/create-property-descriptor":"1E79z","../internals/to-indexed-object":"kvdfV","../internals/to-property-key":"2GDiT","../internals/has-own-property":"lacXY","../internals/ie8-dom-define":"451hU"}],"87WOB":[function(require,module,exports) {
var fails = require('../internals/fails');
// Detect IE8's incomplete defineProperty implementation
module.exports = !fails(function() {
    // eslint-disable-next-line es-x/no-object-defineproperty -- required for testing
    return Object.defineProperty({}, 1, {
        get: function() {
            return 7;
        }
    })[1] != 7;
});

},{"../internals/fails":"luPpk"}],"acNwo":[function(require,module,exports) {
var NATIVE_BIND = require('../internals/function-bind-native');
var call = Function.prototype.call;
module.exports = NATIVE_BIND ? call.bind(call) : function() {
    return call.apply(call, arguments);
};

},{"../internals/function-bind-native":"bFMAq"}],"lvpvk":[function(require,module,exports) {
'use strict';
var $propertyIsEnumerable = {}.propertyIsEnumerable;
// eslint-disable-next-line es-x/no-object-getownpropertydescriptor -- safe
var getOwnPropertyDescriptor = Object.getOwnPropertyDescriptor;
// Nashorn ~ JDK8 bug
var NASHORN_BUG = getOwnPropertyDescriptor && !$propertyIsEnumerable.call({
    1: 2
}, 1);
// `Object.prototype.propertyIsEnumerable` method implementation
// https://tc39.es/ecma262/#sec-object.prototype.propertyisenumerable
exports.f = NASHORN_BUG ? function propertyIsEnumerable(V) {
    var descriptor = getOwnPropertyDescriptor(this, V);
    return !!descriptor && descriptor.enumerable;
} : $propertyIsEnumerable;

},{}],"1E79z":[function(require,module,exports) {
module.exports = function(bitmap, value) {
    return {
        enumerable: !(bitmap & 1),
        configurable: !(bitmap & 2),
        writable: !(bitmap & 4),
        value: value
    };
};

},{}],"kvdfV":[function(require,module,exports) {
// toObject with fallback for non-array-like ES3 strings
var IndexedObject = require('../internals/indexed-object');
var requireObjectCoercible = require('../internals/require-object-coercible');
module.exports = function(it) {
    return IndexedObject(requireObjectCoercible(it));
};

},{"../internals/indexed-object":"6NkN4","../internals/require-object-coercible":"3XBjA"}],"6NkN4":[function(require,module,exports) {
var global = require('../internals/global');
var uncurryThis = require('../internals/function-uncurry-this');
var fails = require('../internals/fails');
var classof = require('../internals/classof-raw');
var Object = global.Object;
var split = uncurryThis(''.split);
// fallback for non-array-like ES3 and non-enumerable old V8 strings
module.exports = fails(function() {
    // throws an error in rhino, see https://github.com/mozilla/rhino/issues/346
    // eslint-disable-next-line no-prototype-builtins -- safe
    return !Object('z').propertyIsEnumerable(0);
}) ? function(it) {
    return classof(it) == 'String' ? split(it, '') : Object(it);
} : Object;

},{"../internals/global":"avqB7","../internals/function-uncurry-this":"5P4uT","../internals/fails":"luPpk","../internals/classof-raw":"fZmv0"}],"fZmv0":[function(require,module,exports) {
var uncurryThis = require('../internals/function-uncurry-this');
var toString = uncurryThis({}.toString);
var stringSlice = uncurryThis(''.slice);
module.exports = function(it) {
    return stringSlice(toString(it), 8, -1);
};

},{"../internals/function-uncurry-this":"5P4uT"}],"3XBjA":[function(require,module,exports) {
var global = require('../internals/global');
var TypeError = global.TypeError;
// `RequireObjectCoercible` abstract operation
// https://tc39.es/ecma262/#sec-requireobjectcoercible
module.exports = function(it) {
    if (it == undefined) throw TypeError("Can't call method on " + it);
    return it;
};

},{"../internals/global":"avqB7"}],"2GDiT":[function(require,module,exports) {
var toPrimitive = require('../internals/to-primitive');
var isSymbol = require('../internals/is-symbol');
// `ToPropertyKey` abstract operation
// https://tc39.es/ecma262/#sec-topropertykey
module.exports = function(argument) {
    var key = toPrimitive(argument, 'string');
    return isSymbol(key) ? key : key + '';
};

},{"../internals/to-primitive":"e0Xko","../internals/is-symbol":"58L3i"}],"e0Xko":[function(require,module,exports) {
var global = require('../internals/global');
var call = require('../internals/function-call');
var isObject = require('../internals/is-object');
var isSymbol = require('../internals/is-symbol');
var getMethod = require('../internals/get-method');
var ordinaryToPrimitive = require('../internals/ordinary-to-primitive');
var wellKnownSymbol = require('../internals/well-known-symbol');
var TypeError = global.TypeError;
var TO_PRIMITIVE = wellKnownSymbol('toPrimitive');
// `ToPrimitive` abstract operation
// https://tc39.es/ecma262/#sec-toprimitive
module.exports = function(input, pref) {
    if (!isObject(input) || isSymbol(input)) return input;
    var exoticToPrim = getMethod(input, TO_PRIMITIVE);
    var result;
    if (exoticToPrim) {
        if (pref === undefined) pref = 'default';
        result = call(exoticToPrim, input, pref);
        if (!isObject(result) || isSymbol(result)) return result;
        throw TypeError("Can't convert object to primitive value");
    }
    if (pref === undefined) pref = 'number';
    return ordinaryToPrimitive(input, pref);
};

},{"../internals/global":"avqB7","../internals/function-call":"acNwo","../internals/is-object":"fRYFX","../internals/is-symbol":"58L3i","../internals/get-method":"goREb","../internals/ordinary-to-primitive":"iTXKY","../internals/well-known-symbol":"cSGfj"}],"fRYFX":[function(require,module,exports) {
var isCallable = require('../internals/is-callable');
module.exports = function(it) {
    return typeof it == 'object' ? it !== null : isCallable(it);
};

},{"../internals/is-callable":"fqYwo"}],"58L3i":[function(require,module,exports) {
var global = require('../internals/global');
var getBuiltIn = require('../internals/get-built-in');
var isCallable = require('../internals/is-callable');
var isPrototypeOf = require('../internals/object-is-prototype-of');
var USE_SYMBOL_AS_UID = require('../internals/use-symbol-as-uid');
var Object = global.Object;
module.exports = USE_SYMBOL_AS_UID ? function(it) {
    return typeof it == 'symbol';
} : function(it) {
    var $Symbol = getBuiltIn('Symbol');
    return isCallable($Symbol) && isPrototypeOf($Symbol.prototype, Object(it));
};

},{"../internals/global":"avqB7","../internals/get-built-in":"6wm25","../internals/is-callable":"fqYwo","../internals/object-is-prototype-of":"qlM4L","../internals/use-symbol-as-uid":"aDtCm"}],"6wm25":[function(require,module,exports) {
var path = require('../internals/path');
var global = require('../internals/global');
var isCallable = require('../internals/is-callable');
var aFunction = function(variable) {
    return isCallable(variable) ? variable : undefined;
};
module.exports = function(namespace, method) {
    return arguments.length < 2 ? aFunction(path[namespace]) || aFunction(global[namespace]) : path[namespace] && path[namespace][method] || global[namespace] && global[namespace][method];
};

},{"../internals/path":"8QO0z","../internals/global":"avqB7","../internals/is-callable":"fqYwo"}],"8QO0z":[function(require,module,exports) {
module.exports = {};

},{}],"qlM4L":[function(require,module,exports) {
var uncurryThis = require('../internals/function-uncurry-this');
module.exports = uncurryThis({}.isPrototypeOf);

},{"../internals/function-uncurry-this":"5P4uT"}],"aDtCm":[function(require,module,exports) {
/* eslint-disable es-x/no-symbol -- required for testing */ var NATIVE_SYMBOL = require('../internals/native-symbol');
module.exports = NATIVE_SYMBOL && !Symbol.sham && typeof Symbol.iterator == 'symbol';

},{"../internals/native-symbol":"kChWP"}],"kChWP":[function(require,module,exports) {
/* eslint-disable es-x/no-symbol -- required for testing */ var V8_VERSION = require('../internals/engine-v8-version');
var fails = require('../internals/fails');
// eslint-disable-next-line es-x/no-object-getownpropertysymbols -- required for testing
module.exports = !!Object.getOwnPropertySymbols && !fails(function() {
    var symbol = Symbol();
    // Chrome 38 Symbol has incorrect toString conversion
    // `get-own-property-symbols` polyfill symbols converted to object are not Symbol instances
    return !String(symbol) || !(Object(symbol) instanceof Symbol) || // Chrome 38-40 symbols are not inherited from DOM collections prototypes to instances
    !Symbol.sham && V8_VERSION && V8_VERSION < 41;
});

},{"../internals/engine-v8-version":"diVIs","../internals/fails":"luPpk"}],"diVIs":[function(require,module,exports) {
var global = require('../internals/global');
var userAgent = require('../internals/engine-user-agent');
var process = global.process;
var Deno = global.Deno;
var versions = process && process.versions || Deno && Deno.version;
var v8 = versions && versions.v8;
var match, version;
if (v8) {
    match = v8.split('.');
    // in old Chrome, versions of V8 isn't V8 = Chrome / 10
    // but their correct versions are not interesting for us
    version = match[0] > 0 && match[0] < 4 ? 1 : +(match[0] + match[1]);
}
// BrowserFS NodeJS `process` polyfill incorrectly set `.v8` to `0.0`
// so check `userAgent` even if `.v8` exists, but 0
if (!version && userAgent) {
    match = userAgent.match(/Edge\/(\d+)/);
    if (!match || match[1] >= 74) {
        match = userAgent.match(/Chrome\/(\d+)/);
        if (match) version = +match[1];
    }
}
module.exports = version;

},{"../internals/global":"avqB7","../internals/engine-user-agent":"jumDW"}],"jumDW":[function(require,module,exports) {
var getBuiltIn = require('../internals/get-built-in');
module.exports = getBuiltIn('navigator', 'userAgent') || '';

},{"../internals/get-built-in":"6wm25"}],"goREb":[function(require,module,exports) {
var aCallable = require('../internals/a-callable');
// `GetMethod` abstract operation
// https://tc39.es/ecma262/#sec-getmethod
module.exports = function(V, P) {
    var func = V[P];
    return func == null ? undefined : aCallable(func);
};

},{"../internals/a-callable":"7r1lP"}],"7r1lP":[function(require,module,exports) {
var global = require('../internals/global');
var isCallable = require('../internals/is-callable');
var tryToString = require('../internals/try-to-string');
var TypeError = global.TypeError;
// `Assert: IsCallable(argument) is true`
module.exports = function(argument) {
    if (isCallable(argument)) return argument;
    throw TypeError(tryToString(argument) + ' is not a function');
};

},{"../internals/global":"avqB7","../internals/is-callable":"fqYwo","../internals/try-to-string":"c7C3w"}],"c7C3w":[function(require,module,exports) {
var global = require('../internals/global');
var String = global.String;
module.exports = function(argument) {
    try {
        return String(argument);
    } catch (error) {
        return 'Object';
    }
};

},{"../internals/global":"avqB7"}],"iTXKY":[function(require,module,exports) {
var global = require('../internals/global');
var call = require('../internals/function-call');
var isCallable = require('../internals/is-callable');
var isObject = require('../internals/is-object');
var TypeError = global.TypeError;
// `OrdinaryToPrimitive` abstract operation
// https://tc39.es/ecma262/#sec-ordinarytoprimitive
module.exports = function(input, pref) {
    var fn, val;
    if (pref === 'string' && isCallable(fn = input.toString) && !isObject(val = call(fn, input))) return val;
    if (isCallable(fn = input.valueOf) && !isObject(val = call(fn, input))) return val;
    if (pref !== 'string' && isCallable(fn = input.toString) && !isObject(val = call(fn, input))) return val;
    throw TypeError("Can't convert object to primitive value");
};

},{"../internals/global":"avqB7","../internals/function-call":"acNwo","../internals/is-callable":"fqYwo","../internals/is-object":"fRYFX"}],"cSGfj":[function(require,module,exports) {
var global = require('../internals/global');
var shared = require('../internals/shared');
var hasOwn = require('../internals/has-own-property');
var uid = require('../internals/uid');
var NATIVE_SYMBOL = require('../internals/native-symbol');
var USE_SYMBOL_AS_UID = require('../internals/use-symbol-as-uid');
var WellKnownSymbolsStore = shared('wks');
var Symbol = global.Symbol;
var symbolFor = Symbol && Symbol['for'];
var createWellKnownSymbol = USE_SYMBOL_AS_UID ? Symbol : Symbol && Symbol.withoutSetter || uid;
module.exports = function(name) {
    if (!hasOwn(WellKnownSymbolsStore, name) || !(NATIVE_SYMBOL || typeof WellKnownSymbolsStore[name] == 'string')) {
        var description = 'Symbol.' + name;
        if (NATIVE_SYMBOL && hasOwn(Symbol, name)) WellKnownSymbolsStore[name] = Symbol[name];
        else if (USE_SYMBOL_AS_UID && symbolFor) WellKnownSymbolsStore[name] = symbolFor(description);
        else WellKnownSymbolsStore[name] = createWellKnownSymbol(description);
    }
    return WellKnownSymbolsStore[name];
};

},{"../internals/global":"avqB7","../internals/shared":"9VSA0","../internals/has-own-property":"lacXY","../internals/uid":"cLNyV","../internals/native-symbol":"kChWP","../internals/use-symbol-as-uid":"aDtCm"}],"9VSA0":[function(require,module,exports) {
var IS_PURE = require('../internals/is-pure');
var store = require('../internals/shared-store');
(module.exports = function(key, value) {
    return store[key] || (store[key] = value !== undefined ? value : {});
})('versions', []).push({
    version: '3.22.5',
    mode: IS_PURE ? 'pure' : 'global',
    copyright: '© 2014-2022 Denis Pushkarev (zloirock.ru)',
    license: 'https://github.com/zloirock/core-js/blob/v3.22.5/LICENSE',
    source: 'https://github.com/zloirock/core-js'
});

},{"../internals/is-pure":"k905C","../internals/shared-store":"4KS55"}],"k905C":[function(require,module,exports) {
module.exports = true;

},{}],"4KS55":[function(require,module,exports) {
var global = require('../internals/global');
var setGlobal = require('../internals/set-global');
var SHARED = '__core-js_shared__';
var store = global[SHARED] || setGlobal(SHARED, {});
module.exports = store;

},{"../internals/global":"avqB7","../internals/set-global":"avsP6"}],"avsP6":[function(require,module,exports) {
var global = require('../internals/global');
// eslint-disable-next-line es-x/no-object-defineproperty -- safe
var defineProperty = Object.defineProperty;
module.exports = function(key, value) {
    try {
        defineProperty(global, key, {
            value: value,
            configurable: true,
            writable: true
        });
    } catch (error) {
        global[key] = value;
    }
    return value;
};

},{"../internals/global":"avqB7"}],"lacXY":[function(require,module,exports) {
var uncurryThis = require('../internals/function-uncurry-this');
var toObject = require('../internals/to-object');
var hasOwnProperty = uncurryThis({}.hasOwnProperty);
// `HasOwnProperty` abstract operation
// https://tc39.es/ecma262/#sec-hasownproperty
// eslint-disable-next-line es-x/no-object-hasown -- safe
module.exports = Object.hasOwn || function hasOwn(it, key) {
    return hasOwnProperty(toObject(it), key);
};

},{"../internals/function-uncurry-this":"5P4uT","../internals/to-object":"70Utf"}],"70Utf":[function(require,module,exports) {
var global = require('../internals/global');
var requireObjectCoercible = require('../internals/require-object-coercible');
var Object = global.Object;
// `ToObject` abstract operation
// https://tc39.es/ecma262/#sec-toobject
module.exports = function(argument) {
    return Object(requireObjectCoercible(argument));
};

},{"../internals/global":"avqB7","../internals/require-object-coercible":"3XBjA"}],"cLNyV":[function(require,module,exports) {
var uncurryThis = require('../internals/function-uncurry-this');
var id = 0;
var postfix = Math.random();
var toString = uncurryThis(1.0.toString);
module.exports = function(key) {
    return 'Symbol(' + (key === undefined ? '' : key) + ')_' + toString(++id + postfix, 36);
};

},{"../internals/function-uncurry-this":"5P4uT"}],"451hU":[function(require,module,exports) {
var DESCRIPTORS = require('../internals/descriptors');
var fails = require('../internals/fails');
var createElement = require('../internals/document-create-element');
// Thanks to IE8 for its funny defineProperty
module.exports = !DESCRIPTORS && !fails(function() {
    // eslint-disable-next-line es-x/no-object-defineproperty -- required for testing
    return Object.defineProperty(createElement('div'), 'a', {
        get: function() {
            return 7;
        }
    }).a != 7;
});

},{"../internals/descriptors":"87WOB","../internals/fails":"luPpk","../internals/document-create-element":"Ioo8t"}],"Ioo8t":[function(require,module,exports) {
var global = require('../internals/global');
var isObject = require('../internals/is-object');
var document = global.document;
// typeof document.createElement is 'object' in old IE
var EXISTS = isObject(document) && isObject(document.createElement);
module.exports = function(it) {
    return EXISTS ? document.createElement(it) : {};
};

},{"../internals/global":"avqB7","../internals/is-object":"fRYFX"}],"1D6eY":[function(require,module,exports) {
var fails = require('../internals/fails');
var isCallable = require('../internals/is-callable');
var replacement = /#|\.prototype\./;
var isForced = function(feature, detection) {
    var value = data[normalize(feature)];
    return value == POLYFILL ? true : value == NATIVE ? false : isCallable(detection) ? fails(detection) : !!detection;
};
var normalize = isForced.normalize = function(string) {
    return String(string).replace(replacement, '.').toLowerCase();
};
var data = isForced.data = {};
var NATIVE = isForced.NATIVE = 'N';
var POLYFILL = isForced.POLYFILL = 'P';
module.exports = isForced;

},{"../internals/fails":"luPpk","../internals/is-callable":"fqYwo"}],"jZFzY":[function(require,module,exports) {
var uncurryThis = require('../internals/function-uncurry-this');
var aCallable = require('../internals/a-callable');
var NATIVE_BIND = require('../internals/function-bind-native');
var bind = uncurryThis(uncurryThis.bind);
// optional / simple context binding
module.exports = function(fn, that) {
    aCallable(fn);
    return that === undefined ? fn : NATIVE_BIND ? bind(fn, that) : function() {
        return fn.apply(that, arguments);
    };
};

},{"../internals/function-uncurry-this":"5P4uT","../internals/a-callable":"7r1lP","../internals/function-bind-native":"bFMAq"}],"2bHDS":[function(require,module,exports) {
var DESCRIPTORS = require('../internals/descriptors');
var definePropertyModule = require('../internals/object-define-property');
var createPropertyDescriptor = require('../internals/create-property-descriptor');
module.exports = DESCRIPTORS ? function(object, key, value) {
    return definePropertyModule.f(object, key, createPropertyDescriptor(1, value));
} : function(object, key, value) {
    object[key] = value;
    return object;
};

},{"../internals/descriptors":"87WOB","../internals/object-define-property":"gbIb1","../internals/create-property-descriptor":"1E79z"}],"gbIb1":[function(require,module,exports) {
var global = require('../internals/global');
var DESCRIPTORS = require('../internals/descriptors');
var IE8_DOM_DEFINE = require('../internals/ie8-dom-define');
var V8_PROTOTYPE_DEFINE_BUG = require('../internals/v8-prototype-define-bug');
var anObject = require('../internals/an-object');
var toPropertyKey = require('../internals/to-property-key');
var TypeError = global.TypeError;
// eslint-disable-next-line es-x/no-object-defineproperty -- safe
var $defineProperty = Object.defineProperty;
// eslint-disable-next-line es-x/no-object-getownpropertydescriptor -- safe
var $getOwnPropertyDescriptor = Object.getOwnPropertyDescriptor;
var ENUMERABLE = 'enumerable';
var CONFIGURABLE = 'configurable';
var WRITABLE = 'writable';
// `Object.defineProperty` method
// https://tc39.es/ecma262/#sec-object.defineproperty
exports.f = DESCRIPTORS ? V8_PROTOTYPE_DEFINE_BUG ? function defineProperty(O, P, Attributes) {
    anObject(O);
    P = toPropertyKey(P);
    anObject(Attributes);
    if (typeof O === 'function' && P === 'prototype' && 'value' in Attributes && WRITABLE in Attributes && !Attributes[WRITABLE]) {
        var current = $getOwnPropertyDescriptor(O, P);
        if (current && current[WRITABLE]) {
            O[P] = Attributes.value;
            Attributes = {
                configurable: CONFIGURABLE in Attributes ? Attributes[CONFIGURABLE] : current[CONFIGURABLE],
                enumerable: ENUMERABLE in Attributes ? Attributes[ENUMERABLE] : current[ENUMERABLE],
                writable: false
            };
        }
    }
    return $defineProperty(O, P, Attributes);
} : $defineProperty : function defineProperty(O, P, Attributes) {
    anObject(O);
    P = toPropertyKey(P);
    anObject(Attributes);
    if (IE8_DOM_DEFINE) try {
        return $defineProperty(O, P, Attributes);
    } catch (error) {}
    if ('get' in Attributes || 'set' in Attributes) throw TypeError('Accessors not supported');
    if ('value' in Attributes) O[P] = Attributes.value;
    return O;
};

},{"../internals/global":"avqB7","../internals/descriptors":"87WOB","../internals/ie8-dom-define":"451hU","../internals/v8-prototype-define-bug":"fElse","../internals/an-object":"bTNyr","../internals/to-property-key":"2GDiT"}],"fElse":[function(require,module,exports) {
var DESCRIPTORS = require('../internals/descriptors');
var fails = require('../internals/fails');
// V8 ~ Chrome 36-
// https://bugs.chromium.org/p/v8/issues/detail?id=3334
module.exports = DESCRIPTORS && fails(function() {
    // eslint-disable-next-line es-x/no-object-defineproperty -- required for testing
    return Object.defineProperty(function() {}, 'prototype', {
        value: 42,
        writable: false
    }).prototype != 42;
});

},{"../internals/descriptors":"87WOB","../internals/fails":"luPpk"}],"bTNyr":[function(require,module,exports) {
var global = require('../internals/global');
var isObject = require('../internals/is-object');
var String = global.String;
var TypeError = global.TypeError;
// `Assert: Type(argument) is Object`
module.exports = function(argument) {
    if (isObject(argument)) return argument;
    throw TypeError(String(argument) + ' is not an object');
};

},{"../internals/global":"avqB7","../internals/is-object":"fRYFX"}],"byYNs":[function(require,module,exports) {
var classof = require('../internals/classof-raw');
// `IsArray` abstract operation
// https://tc39.es/ecma262/#sec-isarray
// eslint-disable-next-line es-x/no-array-isarray -- safe
module.exports = Array.isArray || function isArray(argument) {
    return classof(argument) == 'Array';
};

},{"../internals/classof-raw":"fZmv0"}],"1k8Yk":[function(require,module,exports) {
var _Symbol = require("@babel/runtime-corejs3/core-js/symbol");
var _getIteratorMethod = require("@babel/runtime-corejs3/core-js/get-iterator-method");
function _iterableToArrayLimit(arr, i) {
    var _i = arr == null ? null : typeof _Symbol !== "undefined" && _getIteratorMethod(arr) || arr["@@iterator"];
    if (_i == null) return;
    var _arr = [];
    var _n = true;
    var _d = false;
    var _s, _e;
    try {
        for(_i = _i.call(arr); !(_n = (_s = _i.next()).done); _n = true){
            _arr.push(_s.value);
            if (i && _arr.length === i) break;
        }
    } catch (err) {
        _d = true;
        _e = err;
    } finally{
        try {
            if (!_n && _i["return"] != null) _i["return"]();
        } finally{
            if (_d) throw _e;
        }
    }
    return _arr;
}
module.exports = _iterableToArrayLimit, module.exports.__esModule = true, module.exports["default"] = module.exports;

},{"@babel/runtime-corejs3/core-js/symbol":"2n1d6","@babel/runtime-corejs3/core-js/get-iterator-method":"hNMZV"}],"2n1d6":[function(require,module,exports) {
module.exports = require("core-js-pure/features/symbol");

},{"core-js-pure/features/symbol":"kYvJN"}],"kYvJN":[function(require,module,exports) {
module.exports = require('../../full/symbol');

},{"../../full/symbol":"g3qGE"}],"g3qGE":[function(require,module,exports) {
var parent = require('../../actual/symbol');
require('../../modules/esnext.symbol.async-dispose');
require('../../modules/esnext.symbol.dispose');
require('../../modules/esnext.symbol.matcher');
require('../../modules/esnext.symbol.metadata');
require('../../modules/esnext.symbol.observable');
// TODO: Remove from `core-js@4`
require('../../modules/esnext.symbol.pattern-match');
// TODO: Remove from `core-js@4`
require('../../modules/esnext.symbol.replace-all');
module.exports = parent;

},{"../../actual/symbol":"2GrGW","../../modules/esnext.symbol.async-dispose":"8vr0u","../../modules/esnext.symbol.dispose":"1nv2L","../../modules/esnext.symbol.matcher":"aZlnJ","../../modules/esnext.symbol.metadata":"fOMFf","../../modules/esnext.symbol.observable":"7OliP","../../modules/esnext.symbol.pattern-match":"h404G","../../modules/esnext.symbol.replace-all":"8hMxk"}],"2GrGW":[function(require,module,exports) {
var parent = require('../../stable/symbol');
module.exports = parent;

},{"../../stable/symbol":"5PK61"}],"5PK61":[function(require,module,exports) {
var parent = require('../../es/symbol');
require('../../modules/web.dom-collections.iterator');
module.exports = parent;

},{"../../es/symbol":"7vWcl","../../modules/web.dom-collections.iterator":"fVWQ7"}],"7vWcl":[function(require,module,exports) {
require('../../modules/es.array.concat');
require('../../modules/es.object.to-string');
require('../../modules/es.symbol');
require('../../modules/es.symbol.async-iterator');
require('../../modules/es.symbol.description');
require('../../modules/es.symbol.has-instance');
require('../../modules/es.symbol.is-concat-spreadable');
require('../../modules/es.symbol.iterator');
require('../../modules/es.symbol.match');
require('../../modules/es.symbol.match-all');
require('../../modules/es.symbol.replace');
require('../../modules/es.symbol.search');
require('../../modules/es.symbol.species');
require('../../modules/es.symbol.split');
require('../../modules/es.symbol.to-primitive');
require('../../modules/es.symbol.to-string-tag');
require('../../modules/es.symbol.unscopables');
require('../../modules/es.json.to-string-tag');
require('../../modules/es.math.to-string-tag');
require('../../modules/es.reflect.to-string-tag');
var path = require('../../internals/path');
module.exports = path.Symbol;

},{"../../modules/es.array.concat":"liOpx","../../modules/es.object.to-string":"jCxf4","../../modules/es.symbol":"eRNGH","../../modules/es.symbol.async-iterator":"jgMqD","../../modules/es.symbol.description":"1l1uI","../../modules/es.symbol.has-instance":"fnPvK","../../modules/es.symbol.is-concat-spreadable":"iibvg","../../modules/es.symbol.iterator":"cpY48","../../modules/es.symbol.match":"jPvhH","../../modules/es.symbol.match-all":"2cyj8","../../modules/es.symbol.replace":"12aQP","../../modules/es.symbol.search":"lC05F","../../modules/es.symbol.species":"itj89","../../modules/es.symbol.split":"7fQ1B","../../modules/es.symbol.to-primitive":"hFsWD","../../modules/es.symbol.to-string-tag":"dlZcB","../../modules/es.symbol.unscopables":"jUhyE","../../modules/es.json.to-string-tag":"6hVtO","../../modules/es.math.to-string-tag":"10AwK","../../modules/es.reflect.to-string-tag":"jxoXe","../../internals/path":"8QO0z"}],"liOpx":[function(require,module,exports) {
'use strict';
var $ = require('../internals/export');
var global = require('../internals/global');
var fails = require('../internals/fails');
var isArray = require('../internals/is-array');
var isObject = require('../internals/is-object');
var toObject = require('../internals/to-object');
var lengthOfArrayLike = require('../internals/length-of-array-like');
var createProperty = require('../internals/create-property');
var arraySpeciesCreate = require('../internals/array-species-create');
var arrayMethodHasSpeciesSupport = require('../internals/array-method-has-species-support');
var wellKnownSymbol = require('../internals/well-known-symbol');
var V8_VERSION = require('../internals/engine-v8-version');
var IS_CONCAT_SPREADABLE = wellKnownSymbol('isConcatSpreadable');
var MAX_SAFE_INTEGER = 0x1FFFFFFFFFFFFF;
var MAXIMUM_ALLOWED_INDEX_EXCEEDED = 'Maximum allowed index exceeded';
var TypeError = global.TypeError;
// We can't use this feature detection in V8 since it causes
// deoptimization and serious performance degradation
// https://github.com/zloirock/core-js/issues/679
var IS_CONCAT_SPREADABLE_SUPPORT = V8_VERSION >= 51 || !fails(function() {
    var array = [];
    array[IS_CONCAT_SPREADABLE] = false;
    return array.concat()[0] !== array;
});
var SPECIES_SUPPORT = arrayMethodHasSpeciesSupport('concat');
var isConcatSpreadable = function(O) {
    if (!isObject(O)) return false;
    var spreadable = O[IS_CONCAT_SPREADABLE];
    return spreadable !== undefined ? !!spreadable : isArray(O);
};
var FORCED = !IS_CONCAT_SPREADABLE_SUPPORT || !SPECIES_SUPPORT;
// `Array.prototype.concat` method
// https://tc39.es/ecma262/#sec-array.prototype.concat
// with adding support of @@isConcatSpreadable and @@species
$({
    target: 'Array',
    proto: true,
    arity: 1,
    forced: FORCED
}, {
    // eslint-disable-next-line no-unused-vars -- required for `.length`
    concat: function concat(arg) {
        var O = toObject(this);
        var A = arraySpeciesCreate(O, 0);
        var n = 0;
        var i, k, length, len, E;
        for(i = -1, length = arguments.length; i < length; i++){
            E = i === -1 ? O : arguments[i];
            if (isConcatSpreadable(E)) {
                len = lengthOfArrayLike(E);
                if (n + len > MAX_SAFE_INTEGER) throw TypeError(MAXIMUM_ALLOWED_INDEX_EXCEEDED);
                for(k = 0; k < len; k++, n++)if (k in E) createProperty(A, n, E[k]);
            } else {
                if (n >= MAX_SAFE_INTEGER) throw TypeError(MAXIMUM_ALLOWED_INDEX_EXCEEDED);
                createProperty(A, n++, E);
            }
        }
        A.length = n;
        return A;
    }
});

},{"../internals/export":"jjems","../internals/global":"avqB7","../internals/fails":"luPpk","../internals/is-array":"byYNs","../internals/is-object":"fRYFX","../internals/to-object":"70Utf","../internals/length-of-array-like":"icIfm","../internals/create-property":"9Ttit","../internals/array-species-create":"jQr4t","../internals/array-method-has-species-support":"4n6gw","../internals/well-known-symbol":"cSGfj","../internals/engine-v8-version":"diVIs"}],"icIfm":[function(require,module,exports) {
var toLength = require('../internals/to-length');
// `LengthOfArrayLike` abstract operation
// https://tc39.es/ecma262/#sec-lengthofarraylike
module.exports = function(obj) {
    return toLength(obj.length);
};

},{"../internals/to-length":"9kM19"}],"9kM19":[function(require,module,exports) {
var toIntegerOrInfinity = require('../internals/to-integer-or-infinity');
var min = Math.min;
// `ToLength` abstract operation
// https://tc39.es/ecma262/#sec-tolength
module.exports = function(argument) {
    return argument > 0 ? min(toIntegerOrInfinity(argument), 0x1FFFFFFFFFFFFF) : 0; // 2 ** 53 - 1 == 9007199254740991
};

},{"../internals/to-integer-or-infinity":"dQ2Uo"}],"dQ2Uo":[function(require,module,exports) {
var ceil = Math.ceil;
var floor = Math.floor;
// `ToIntegerOrInfinity` abstract operation
// https://tc39.es/ecma262/#sec-tointegerorinfinity
module.exports = function(argument) {
    var number = +argument;
    // eslint-disable-next-line no-self-compare -- safe
    return number !== number || number === 0 ? 0 : (number > 0 ? floor : ceil)(number);
};

},{}],"9Ttit":[function(require,module,exports) {
'use strict';
var toPropertyKey = require('../internals/to-property-key');
var definePropertyModule = require('../internals/object-define-property');
var createPropertyDescriptor = require('../internals/create-property-descriptor');
module.exports = function(object, key, value) {
    var propertyKey = toPropertyKey(key);
    if (propertyKey in object) definePropertyModule.f(object, propertyKey, createPropertyDescriptor(0, value));
    else object[propertyKey] = value;
};

},{"../internals/to-property-key":"2GDiT","../internals/object-define-property":"gbIb1","../internals/create-property-descriptor":"1E79z"}],"jQr4t":[function(require,module,exports) {
var arraySpeciesConstructor = require('../internals/array-species-constructor');
// `ArraySpeciesCreate` abstract operation
// https://tc39.es/ecma262/#sec-arrayspeciescreate
module.exports = function(originalArray, length) {
    return new (arraySpeciesConstructor(originalArray))(length === 0 ? 0 : length);
};

},{"../internals/array-species-constructor":"cVDs2"}],"cVDs2":[function(require,module,exports) {
var global = require('../internals/global');
var isArray = require('../internals/is-array');
var isConstructor = require('../internals/is-constructor');
var isObject = require('../internals/is-object');
var wellKnownSymbol = require('../internals/well-known-symbol');
var SPECIES = wellKnownSymbol('species');
var Array = global.Array;
// a part of `ArraySpeciesCreate` abstract operation
// https://tc39.es/ecma262/#sec-arrayspeciescreate
module.exports = function(originalArray) {
    var C;
    if (isArray(originalArray)) {
        C = originalArray.constructor;
        // cross-realm fallback
        if (isConstructor(C) && (C === Array || isArray(C.prototype))) C = undefined;
        else if (isObject(C)) {
            C = C[SPECIES];
            if (C === null) C = undefined;
        }
    }
    return C === undefined ? Array : C;
};

},{"../internals/global":"avqB7","../internals/is-array":"byYNs","../internals/is-constructor":"15Sn8","../internals/is-object":"fRYFX","../internals/well-known-symbol":"cSGfj"}],"15Sn8":[function(require,module,exports) {
var uncurryThis = require('../internals/function-uncurry-this');
var fails = require('../internals/fails');
var isCallable = require('../internals/is-callable');
var classof = require('../internals/classof');
var getBuiltIn = require('../internals/get-built-in');
var inspectSource = require('../internals/inspect-source');
var noop = function() {};
var empty = [];
var construct = getBuiltIn('Reflect', 'construct');
var constructorRegExp = /^\s*(?:class|function)\b/;
var exec = uncurryThis(constructorRegExp.exec);
var INCORRECT_TO_STRING = !constructorRegExp.exec(noop);
var isConstructorModern = function isConstructor(argument) {
    if (!isCallable(argument)) return false;
    try {
        construct(noop, empty, argument);
        return true;
    } catch (error) {
        return false;
    }
};
var isConstructorLegacy = function isConstructor(argument) {
    if (!isCallable(argument)) return false;
    switch(classof(argument)){
        case 'AsyncFunction':
        case 'GeneratorFunction':
        case 'AsyncGeneratorFunction':
            return false;
    }
    try {
        // we can't check .prototype since constructors produced by .bind haven't it
        // `Function#toString` throws on some built-it function in some legacy engines
        // (for example, `DOMQuad` and similar in FF41-)
        return INCORRECT_TO_STRING || !!exec(constructorRegExp, inspectSource(argument));
    } catch (error) {
        return true;
    }
};
isConstructorLegacy.sham = true;
// `IsConstructor` abstract operation
// https://tc39.es/ecma262/#sec-isconstructor
module.exports = !construct || fails(function() {
    var called;
    return isConstructorModern(isConstructorModern.call) || !isConstructorModern(Object) || !isConstructorModern(function() {
        called = true;
    }) || called;
}) ? isConstructorLegacy : isConstructorModern;

},{"../internals/function-uncurry-this":"5P4uT","../internals/fails":"luPpk","../internals/is-callable":"fqYwo","../internals/classof":"5J26j","../internals/get-built-in":"6wm25","../internals/inspect-source":"17BO4"}],"5J26j":[function(require,module,exports) {
var global = require('../internals/global');
var TO_STRING_TAG_SUPPORT = require('../internals/to-string-tag-support');
var isCallable = require('../internals/is-callable');
var classofRaw = require('../internals/classof-raw');
var wellKnownSymbol = require('../internals/well-known-symbol');
var TO_STRING_TAG = wellKnownSymbol('toStringTag');
var Object = global.Object;
// ES3 wrong here
var CORRECT_ARGUMENTS = classofRaw(function() {
    return arguments;
}()) == 'Arguments';
// fallback for IE11 Script Access Denied error
var tryGet = function(it, key) {
    try {
        return it[key];
    } catch (error) {}
};
// getting tag from ES6+ `Object.prototype.toString`
module.exports = TO_STRING_TAG_SUPPORT ? classofRaw : function(it) {
    var O, tag, result;
    return it === undefined ? 'Undefined' : it === null ? 'Null' : typeof (tag = tryGet(O = Object(it), TO_STRING_TAG)) == 'string' ? tag : CORRECT_ARGUMENTS ? classofRaw(O) : (result = classofRaw(O)) == 'Object' && isCallable(O.callee) ? 'Arguments' : result;
};

},{"../internals/global":"avqB7","../internals/to-string-tag-support":"7vtNP","../internals/is-callable":"fqYwo","../internals/classof-raw":"fZmv0","../internals/well-known-symbol":"cSGfj"}],"7vtNP":[function(require,module,exports) {
var wellKnownSymbol = require('../internals/well-known-symbol');
var TO_STRING_TAG = wellKnownSymbol('toStringTag');
var test = {};
test[TO_STRING_TAG] = 'z';
module.exports = String(test) === '[object z]';

},{"../internals/well-known-symbol":"cSGfj"}],"17BO4":[function(require,module,exports) {
var uncurryThis = require('../internals/function-uncurry-this');
var isCallable = require('../internals/is-callable');
var store = require('../internals/shared-store');
var functionToString = uncurryThis(Function.toString);
// this helper broken in `core-js@3.4.1-3.4.4`, so we can't use `shared` helper
if (!isCallable(store.inspectSource)) store.inspectSource = function(it) {
    return functionToString(it);
};
module.exports = store.inspectSource;

},{"../internals/function-uncurry-this":"5P4uT","../internals/is-callable":"fqYwo","../internals/shared-store":"4KS55"}],"4n6gw":[function(require,module,exports) {
var fails = require('../internals/fails');
var wellKnownSymbol = require('../internals/well-known-symbol');
var V8_VERSION = require('../internals/engine-v8-version');
var SPECIES = wellKnownSymbol('species');
module.exports = function(METHOD_NAME) {
    // We can't use this feature detection in V8 since it causes
    // deoptimization and serious performance degradation
    // https://github.com/zloirock/core-js/issues/677
    return V8_VERSION >= 51 || !fails(function() {
        var array = [];
        var constructor = array.constructor = {};
        constructor[SPECIES] = function() {
            return {
                foo: 1
            };
        };
        return array[METHOD_NAME](Boolean).foo !== 1;
    });
};

},{"../internals/fails":"luPpk","../internals/well-known-symbol":"cSGfj","../internals/engine-v8-version":"diVIs"}],"jCxf4":[function(require,module,exports) {

},{}],"eRNGH":[function(require,module,exports) {
// TODO: Remove this module from `core-js@4` since it's split to modules listed below
require('../modules/es.symbol.constructor');
require('../modules/es.symbol.for');
require('../modules/es.symbol.key-for');
require('../modules/es.json.stringify');
require('../modules/es.object.get-own-property-symbols');

},{"../modules/es.symbol.constructor":"9BzOL","../modules/es.symbol.for":"kn0pm","../modules/es.symbol.key-for":"iBU0N","../modules/es.json.stringify":"2qOVD","../modules/es.object.get-own-property-symbols":"ga77Y"}],"9BzOL":[function(require,module,exports) {
'use strict';
var $ = require('../internals/export');
var global = require('../internals/global');
var call = require('../internals/function-call');
var uncurryThis = require('../internals/function-uncurry-this');
var IS_PURE = require('../internals/is-pure');
var DESCRIPTORS = require('../internals/descriptors');
var NATIVE_SYMBOL = require('../internals/native-symbol');
var fails = require('../internals/fails');
var hasOwn = require('../internals/has-own-property');
var isPrototypeOf = require('../internals/object-is-prototype-of');
var anObject = require('../internals/an-object');
var toIndexedObject = require('../internals/to-indexed-object');
var toPropertyKey = require('../internals/to-property-key');
var $toString = require('../internals/to-string');
var createPropertyDescriptor = require('../internals/create-property-descriptor');
var nativeObjectCreate = require('../internals/object-create');
var objectKeys = require('../internals/object-keys');
var getOwnPropertyNamesModule = require('../internals/object-get-own-property-names');
var getOwnPropertyNamesExternal = require('../internals/object-get-own-property-names-external');
var getOwnPropertySymbolsModule = require('../internals/object-get-own-property-symbols');
var getOwnPropertyDescriptorModule = require('../internals/object-get-own-property-descriptor');
var definePropertyModule = require('../internals/object-define-property');
var definePropertiesModule = require('../internals/object-define-properties');
var propertyIsEnumerableModule = require('../internals/object-property-is-enumerable');
var defineBuiltIn = require('../internals/define-built-in');
var shared = require('../internals/shared');
var sharedKey = require('../internals/shared-key');
var hiddenKeys = require('../internals/hidden-keys');
var uid = require('../internals/uid');
var wellKnownSymbol = require('../internals/well-known-symbol');
var wrappedWellKnownSymbolModule = require('../internals/well-known-symbol-wrapped');
var defineWellKnownSymbol = require('../internals/define-well-known-symbol');
var defineSymbolToPrimitive = require('../internals/symbol-define-to-primitive');
var setToStringTag = require('../internals/set-to-string-tag');
var InternalStateModule = require('../internals/internal-state');
var $forEach = require('../internals/array-iteration').forEach;
var HIDDEN = sharedKey('hidden');
var SYMBOL = 'Symbol';
var PROTOTYPE = 'prototype';
var setInternalState = InternalStateModule.set;
var getInternalState = InternalStateModule.getterFor(SYMBOL);
var ObjectPrototype = Object[PROTOTYPE];
var $Symbol = global.Symbol;
var SymbolPrototype = $Symbol && $Symbol[PROTOTYPE];
var TypeError = global.TypeError;
var QObject = global.QObject;
var nativeGetOwnPropertyDescriptor = getOwnPropertyDescriptorModule.f;
var nativeDefineProperty = definePropertyModule.f;
var nativeGetOwnPropertyNames = getOwnPropertyNamesExternal.f;
var nativePropertyIsEnumerable = propertyIsEnumerableModule.f;
var push = uncurryThis([].push);
var AllSymbols = shared('symbols');
var ObjectPrototypeSymbols = shared('op-symbols');
var WellKnownSymbolsStore = shared('wks');
// Don't use setters in Qt Script, https://github.com/zloirock/core-js/issues/173
var USE_SETTER = !QObject || !QObject[PROTOTYPE] || !QObject[PROTOTYPE].findChild;
// fallback for old Android, https://code.google.com/p/v8/issues/detail?id=687
var setSymbolDescriptor = DESCRIPTORS && fails(function() {
    return nativeObjectCreate(nativeDefineProperty({}, 'a', {
        get: function() {
            return nativeDefineProperty(this, 'a', {
                value: 7
            }).a;
        }
    })).a != 7;
}) ? function(O, P, Attributes) {
    var ObjectPrototypeDescriptor = nativeGetOwnPropertyDescriptor(ObjectPrototype, P);
    if (ObjectPrototypeDescriptor) delete ObjectPrototype[P];
    nativeDefineProperty(O, P, Attributes);
    if (ObjectPrototypeDescriptor && O !== ObjectPrototype) nativeDefineProperty(ObjectPrototype, P, ObjectPrototypeDescriptor);
} : nativeDefineProperty;
var wrap = function(tag, description) {
    var symbol = AllSymbols[tag] = nativeObjectCreate(SymbolPrototype);
    setInternalState(symbol, {
        type: SYMBOL,
        tag: tag,
        description: description
    });
    if (!DESCRIPTORS) symbol.description = description;
    return symbol;
};
var $defineProperty = function defineProperty(O, P, Attributes) {
    if (O === ObjectPrototype) $defineProperty(ObjectPrototypeSymbols, P, Attributes);
    anObject(O);
    var key = toPropertyKey(P);
    anObject(Attributes);
    if (hasOwn(AllSymbols, key)) {
        if (!Attributes.enumerable) {
            if (!hasOwn(O, HIDDEN)) nativeDefineProperty(O, HIDDEN, createPropertyDescriptor(1, {}));
            O[HIDDEN][key] = true;
        } else {
            if (hasOwn(O, HIDDEN) && O[HIDDEN][key]) O[HIDDEN][key] = false;
            Attributes = nativeObjectCreate(Attributes, {
                enumerable: createPropertyDescriptor(0, false)
            });
        }
        return setSymbolDescriptor(O, key, Attributes);
    }
    return nativeDefineProperty(O, key, Attributes);
};
var $defineProperties = function defineProperties(O, Properties) {
    anObject(O);
    var properties = toIndexedObject(Properties);
    var keys = objectKeys(properties).concat($getOwnPropertySymbols(properties));
    $forEach(keys, function(key) {
        if (!DESCRIPTORS || call($propertyIsEnumerable, properties, key)) $defineProperty(O, key, properties[key]);
    });
    return O;
};
var $create = function create(O, Properties) {
    return Properties === undefined ? nativeObjectCreate(O) : $defineProperties(nativeObjectCreate(O), Properties);
};
var $propertyIsEnumerable = function propertyIsEnumerable(V) {
    var P = toPropertyKey(V);
    var enumerable = call(nativePropertyIsEnumerable, this, P);
    if (this === ObjectPrototype && hasOwn(AllSymbols, P) && !hasOwn(ObjectPrototypeSymbols, P)) return false;
    return enumerable || !hasOwn(this, P) || !hasOwn(AllSymbols, P) || hasOwn(this, HIDDEN) && this[HIDDEN][P] ? enumerable : true;
};
var $getOwnPropertyDescriptor = function getOwnPropertyDescriptor(O, P) {
    var it = toIndexedObject(O);
    var key = toPropertyKey(P);
    if (it === ObjectPrototype && hasOwn(AllSymbols, key) && !hasOwn(ObjectPrototypeSymbols, key)) return;
    var descriptor = nativeGetOwnPropertyDescriptor(it, key);
    if (descriptor && hasOwn(AllSymbols, key) && !(hasOwn(it, HIDDEN) && it[HIDDEN][key])) descriptor.enumerable = true;
    return descriptor;
};
var $getOwnPropertyNames = function getOwnPropertyNames(O) {
    var names = nativeGetOwnPropertyNames(toIndexedObject(O));
    var result = [];
    $forEach(names, function(key) {
        if (!hasOwn(AllSymbols, key) && !hasOwn(hiddenKeys, key)) push(result, key);
    });
    return result;
};
var $getOwnPropertySymbols = function(O) {
    var IS_OBJECT_PROTOTYPE = O === ObjectPrototype;
    var names = nativeGetOwnPropertyNames(IS_OBJECT_PROTOTYPE ? ObjectPrototypeSymbols : toIndexedObject(O));
    var result = [];
    $forEach(names, function(key) {
        if (hasOwn(AllSymbols, key) && (!IS_OBJECT_PROTOTYPE || hasOwn(ObjectPrototype, key))) push(result, AllSymbols[key]);
    });
    return result;
};
// `Symbol` constructor
// https://tc39.es/ecma262/#sec-symbol-constructor
if (!NATIVE_SYMBOL) {
    $Symbol = function Symbol() {
        if (isPrototypeOf(SymbolPrototype, this)) throw TypeError('Symbol is not a constructor');
        var description = !arguments.length || arguments[0] === undefined ? undefined : $toString(arguments[0]);
        var tag = uid(description);
        var setter = function(value) {
            if (this === ObjectPrototype) call(setter, ObjectPrototypeSymbols, value);
            if (hasOwn(this, HIDDEN) && hasOwn(this[HIDDEN], tag)) this[HIDDEN][tag] = false;
            setSymbolDescriptor(this, tag, createPropertyDescriptor(1, value));
        };
        if (DESCRIPTORS && USE_SETTER) setSymbolDescriptor(ObjectPrototype, tag, {
            configurable: true,
            set: setter
        });
        return wrap(tag, description);
    };
    SymbolPrototype = $Symbol[PROTOTYPE];
    defineBuiltIn(SymbolPrototype, 'toString', function toString() {
        return getInternalState(this).tag;
    });
    defineBuiltIn($Symbol, 'withoutSetter', function(description) {
        return wrap(uid(description), description);
    });
    propertyIsEnumerableModule.f = $propertyIsEnumerable;
    definePropertyModule.f = $defineProperty;
    definePropertiesModule.f = $defineProperties;
    getOwnPropertyDescriptorModule.f = $getOwnPropertyDescriptor;
    getOwnPropertyNamesModule.f = getOwnPropertyNamesExternal.f = $getOwnPropertyNames;
    getOwnPropertySymbolsModule.f = $getOwnPropertySymbols;
    wrappedWellKnownSymbolModule.f = function(name) {
        return wrap(wellKnownSymbol(name), name);
    };
    if (DESCRIPTORS) {
        // https://github.com/tc39/proposal-Symbol-description
        nativeDefineProperty(SymbolPrototype, 'description', {
            configurable: true,
            get: function description() {
                return getInternalState(this).description;
            }
        });
        if (!IS_PURE) defineBuiltIn(ObjectPrototype, 'propertyIsEnumerable', $propertyIsEnumerable, {
            unsafe: true
        });
    }
}
$({
    global: true,
    constructor: true,
    wrap: true,
    forced: !NATIVE_SYMBOL,
    sham: !NATIVE_SYMBOL
}, {
    Symbol: $Symbol
});
$forEach(objectKeys(WellKnownSymbolsStore), function(name) {
    defineWellKnownSymbol(name);
});
$({
    target: SYMBOL,
    stat: true,
    forced: !NATIVE_SYMBOL
}, {
    useSetter: function() {
        USE_SETTER = true;
    },
    useSimple: function() {
        USE_SETTER = false;
    }
});
$({
    target: 'Object',
    stat: true,
    forced: !NATIVE_SYMBOL,
    sham: !DESCRIPTORS
}, {
    // `Object.create` method
    // https://tc39.es/ecma262/#sec-object.create
    create: $create,
    // `Object.defineProperty` method
    // https://tc39.es/ecma262/#sec-object.defineproperty
    defineProperty: $defineProperty,
    // `Object.defineProperties` method
    // https://tc39.es/ecma262/#sec-object.defineproperties
    defineProperties: $defineProperties,
    // `Object.getOwnPropertyDescriptor` method
    // https://tc39.es/ecma262/#sec-object.getownpropertydescriptors
    getOwnPropertyDescriptor: $getOwnPropertyDescriptor
});
$({
    target: 'Object',
    stat: true,
    forced: !NATIVE_SYMBOL
}, {
    // `Object.getOwnPropertyNames` method
    // https://tc39.es/ecma262/#sec-object.getownpropertynames
    getOwnPropertyNames: $getOwnPropertyNames
});
// `Symbol.prototype[@@toPrimitive]` method
// https://tc39.es/ecma262/#sec-symbol.prototype-@@toprimitive
defineSymbolToPrimitive();
// `Symbol.prototype[@@toStringTag]` property
// https://tc39.es/ecma262/#sec-symbol.prototype-@@tostringtag
setToStringTag($Symbol, SYMBOL);
hiddenKeys[HIDDEN] = true;

},{"../internals/export":"jjems","../internals/global":"avqB7","../internals/function-call":"acNwo","../internals/function-uncurry-this":"5P4uT","../internals/is-pure":"k905C","../internals/descriptors":"87WOB","../internals/native-symbol":"kChWP","../internals/fails":"luPpk","../internals/has-own-property":"lacXY","../internals/object-is-prototype-of":"qlM4L","../internals/an-object":"bTNyr","../internals/to-indexed-object":"kvdfV","../internals/to-property-key":"2GDiT","../internals/to-string":"3DQpG","../internals/create-property-descriptor":"1E79z","../internals/object-create":"eqWCY","../internals/object-keys":"9NKcn","../internals/object-get-own-property-names":"lJsHK","../internals/object-get-own-property-names-external":"8ONpC","../internals/object-get-own-property-symbols":"1qW3G","../internals/object-get-own-property-descriptor":"bs1FJ","../internals/object-define-property":"gbIb1","../internals/object-define-properties":"8IwWp","../internals/object-property-is-enumerable":"lvpvk","../internals/define-built-in":"fXiQ5","../internals/shared":"9VSA0","../internals/shared-key":"fjVxw","../internals/hidden-keys":"7lBVj","../internals/uid":"cLNyV","../internals/well-known-symbol":"cSGfj","../internals/well-known-symbol-wrapped":"fnRxR","../internals/define-well-known-symbol":"9h0sm","../internals/symbol-define-to-primitive":"h6U5e","../internals/set-to-string-tag":"8BLll","../internals/internal-state":"73CuX","../internals/array-iteration":"8g5by"}],"3DQpG":[function(require,module,exports) {
var global = require('../internals/global');
var classof = require('../internals/classof');
var String = global.String;
module.exports = function(argument) {
    if (classof(argument) === 'Symbol') throw TypeError('Cannot convert a Symbol value to a string');
    return String(argument);
};

},{"../internals/global":"avqB7","../internals/classof":"5J26j"}],"eqWCY":[function(require,module,exports) {
/* global ActiveXObject -- old IE, WSH */ var anObject = require('../internals/an-object');
var definePropertiesModule = require('../internals/object-define-properties');
var enumBugKeys = require('../internals/enum-bug-keys');
var hiddenKeys = require('../internals/hidden-keys');
var html = require('../internals/html');
var documentCreateElement = require('../internals/document-create-element');
var sharedKey = require('../internals/shared-key');
var GT = '>';
var LT = '<';
var PROTOTYPE = 'prototype';
var SCRIPT = 'script';
var IE_PROTO = sharedKey('IE_PROTO');
var EmptyConstructor = function() {};
var scriptTag = function(content) {
    return LT + SCRIPT + GT + content + LT + '/' + SCRIPT + GT;
};
// Create object with fake `null` prototype: use ActiveX Object with cleared prototype
var NullProtoObjectViaActiveX = function(activeXDocument1) {
    activeXDocument1.write(scriptTag(''));
    activeXDocument1.close();
    var temp = activeXDocument1.parentWindow.Object;
    activeXDocument1 = null; // avoid memory leak
    return temp;
};
// Create object with fake `null` prototype: use iframe Object with cleared prototype
var NullProtoObjectViaIFrame = function() {
    // Thrash, waste and sodomy: IE GC bug
    var iframe = documentCreateElement('iframe');
    var JS = 'java' + SCRIPT + ':';
    var iframeDocument;
    iframe.style.display = 'none';
    html.appendChild(iframe);
    // https://github.com/zloirock/core-js/issues/475
    iframe.src = String(JS);
    iframeDocument = iframe.contentWindow.document;
    iframeDocument.open();
    iframeDocument.write(scriptTag('document.F=Object'));
    iframeDocument.close();
    return iframeDocument.F;
};
// Check for document.domain and active x support
// No need to use active x approach when document.domain is not set
// see https://github.com/es-shims/es5-shim/issues/150
// variation of https://github.com/kitcambridge/es5-shim/commit/4f738ac066346
// avoid IE GC bug
var activeXDocument;
var NullProtoObject = function() {
    try {
        activeXDocument = new ActiveXObject('htmlfile');
    } catch (error) {}
    NullProtoObject = typeof document != 'undefined' ? document.domain && activeXDocument ? NullProtoObjectViaActiveX(activeXDocument) // old IE
     : NullProtoObjectViaIFrame() : NullProtoObjectViaActiveX(activeXDocument); // WSH
    var length = enumBugKeys.length;
    while(length--)delete NullProtoObject[PROTOTYPE][enumBugKeys[length]];
    return NullProtoObject();
};
hiddenKeys[IE_PROTO] = true;
// `Object.create` method
// https://tc39.es/ecma262/#sec-object.create
// eslint-disable-next-line es-x/no-object-create -- safe
module.exports = Object.create || function create(O, Properties) {
    var result;
    if (O !== null) {
        EmptyConstructor[PROTOTYPE] = anObject(O);
        result = new EmptyConstructor();
        EmptyConstructor[PROTOTYPE] = null;
        // add "__proto__" for Object.getPrototypeOf polyfill
        result[IE_PROTO] = O;
    } else result = NullProtoObject();
    return Properties === undefined ? result : definePropertiesModule.f(result, Properties);
};

},{"../internals/an-object":"bTNyr","../internals/object-define-properties":"8IwWp","../internals/enum-bug-keys":"dvPi1","../internals/hidden-keys":"7lBVj","../internals/html":"4RwZR","../internals/document-create-element":"Ioo8t","../internals/shared-key":"fjVxw"}],"8IwWp":[function(require,module,exports) {
var DESCRIPTORS = require('../internals/descriptors');
var V8_PROTOTYPE_DEFINE_BUG = require('../internals/v8-prototype-define-bug');
var definePropertyModule = require('../internals/object-define-property');
var anObject = require('../internals/an-object');
var toIndexedObject = require('../internals/to-indexed-object');
var objectKeys = require('../internals/object-keys');
// `Object.defineProperties` method
// https://tc39.es/ecma262/#sec-object.defineproperties
// eslint-disable-next-line es-x/no-object-defineproperties -- safe
exports.f = DESCRIPTORS && !V8_PROTOTYPE_DEFINE_BUG ? Object.defineProperties : function defineProperties(O, Properties) {
    anObject(O);
    var props = toIndexedObject(Properties);
    var keys = objectKeys(Properties);
    var length = keys.length;
    var index = 0;
    var key;
    while(length > index)definePropertyModule.f(O, key = keys[index++], props[key]);
    return O;
};

},{"../internals/descriptors":"87WOB","../internals/v8-prototype-define-bug":"fElse","../internals/object-define-property":"gbIb1","../internals/an-object":"bTNyr","../internals/to-indexed-object":"kvdfV","../internals/object-keys":"9NKcn"}],"9NKcn":[function(require,module,exports) {
var internalObjectKeys = require('../internals/object-keys-internal');
var enumBugKeys = require('../internals/enum-bug-keys');
// `Object.keys` method
// https://tc39.es/ecma262/#sec-object.keys
// eslint-disable-next-line es-x/no-object-keys -- safe
module.exports = Object.keys || function keys(O) {
    return internalObjectKeys(O, enumBugKeys);
};

},{"../internals/object-keys-internal":"4pxTL","../internals/enum-bug-keys":"dvPi1"}],"4pxTL":[function(require,module,exports) {
var uncurryThis = require('../internals/function-uncurry-this');
var hasOwn = require('../internals/has-own-property');
var toIndexedObject = require('../internals/to-indexed-object');
var indexOf = require('../internals/array-includes').indexOf;
var hiddenKeys = require('../internals/hidden-keys');
var push = uncurryThis([].push);
module.exports = function(object, names) {
    var O = toIndexedObject(object);
    var i = 0;
    var result = [];
    var key;
    for(key in O)!hasOwn(hiddenKeys, key) && hasOwn(O, key) && push(result, key);
    // Don't enum bug & hidden keys
    while(names.length > i)if (hasOwn(O, key = names[i++])) ~indexOf(result, key) || push(result, key);
    return result;
};

},{"../internals/function-uncurry-this":"5P4uT","../internals/has-own-property":"lacXY","../internals/to-indexed-object":"kvdfV","../internals/array-includes":"9QVVn","../internals/hidden-keys":"7lBVj"}],"9QVVn":[function(require,module,exports) {
var toIndexedObject = require('../internals/to-indexed-object');
var toAbsoluteIndex = require('../internals/to-absolute-index');
var lengthOfArrayLike = require('../internals/length-of-array-like');
// `Array.prototype.{ indexOf, includes }` methods implementation
var createMethod = function(IS_INCLUDES) {
    return function($this, el, fromIndex) {
        var O = toIndexedObject($this);
        var length = lengthOfArrayLike(O);
        var index = toAbsoluteIndex(fromIndex, length);
        var value;
        // Array#includes uses SameValueZero equality algorithm
        // eslint-disable-next-line no-self-compare -- NaN check
        if (IS_INCLUDES && el != el) while(length > index){
            value = O[index++];
            // eslint-disable-next-line no-self-compare -- NaN check
            if (value != value) return true;
        // Array#indexOf ignores holes, Array#includes - not
        }
        else for(; length > index; index++){
            if ((IS_INCLUDES || index in O) && O[index] === el) return IS_INCLUDES || index || 0;
        }
        return !IS_INCLUDES && -1;
    };
};
module.exports = {
    // `Array.prototype.includes` method
    // https://tc39.es/ecma262/#sec-array.prototype.includes
    includes: createMethod(true),
    // `Array.prototype.indexOf` method
    // https://tc39.es/ecma262/#sec-array.prototype.indexof
    indexOf: createMethod(false)
};

},{"../internals/to-indexed-object":"kvdfV","../internals/to-absolute-index":"1L8Z0","../internals/length-of-array-like":"icIfm"}],"1L8Z0":[function(require,module,exports) {
var toIntegerOrInfinity = require('../internals/to-integer-or-infinity');
var max = Math.max;
var min = Math.min;
// Helper for a popular repeating case of the spec:
// Let integer be ? ToInteger(index).
// If integer < 0, let result be max((length + integer), 0); else let result be min(integer, length).
module.exports = function(index, length) {
    var integer = toIntegerOrInfinity(index);
    return integer < 0 ? max(integer + length, 0) : min(integer, length);
};

},{"../internals/to-integer-or-infinity":"dQ2Uo"}],"7lBVj":[function(require,module,exports) {
module.exports = {};

},{}],"dvPi1":[function(require,module,exports) {
// IE8- don't enum bug keys
module.exports = [
    'constructor',
    'hasOwnProperty',
    'isPrototypeOf',
    'propertyIsEnumerable',
    'toLocaleString',
    'toString',
    'valueOf'
];

},{}],"4RwZR":[function(require,module,exports) {
var getBuiltIn = require('../internals/get-built-in');
module.exports = getBuiltIn('document', 'documentElement');

},{"../internals/get-built-in":"6wm25"}],"fjVxw":[function(require,module,exports) {
var shared = require('../internals/shared');
var uid = require('../internals/uid');
var keys = shared('keys');
module.exports = function(key) {
    return keys[key] || (keys[key] = uid(key));
};

},{"../internals/shared":"9VSA0","../internals/uid":"cLNyV"}],"lJsHK":[function(require,module,exports) {
var internalObjectKeys = require('../internals/object-keys-internal');
var enumBugKeys = require('../internals/enum-bug-keys');
var hiddenKeys = enumBugKeys.concat('length', 'prototype');
// `Object.getOwnPropertyNames` method
// https://tc39.es/ecma262/#sec-object.getownpropertynames
// eslint-disable-next-line es-x/no-object-getownpropertynames -- safe
exports.f = Object.getOwnPropertyNames || function getOwnPropertyNames(O) {
    return internalObjectKeys(O, hiddenKeys);
};

},{"../internals/object-keys-internal":"4pxTL","../internals/enum-bug-keys":"dvPi1"}],"8ONpC":[function(require,module,exports) {
/* eslint-disable es-x/no-object-getownpropertynames -- safe */ var classof = require('../internals/classof-raw');
var toIndexedObject = require('../internals/to-indexed-object');
var $getOwnPropertyNames = require('../internals/object-get-own-property-names').f;
var arraySlice = require('../internals/array-slice-simple');
var windowNames = typeof window == 'object' && window && Object.getOwnPropertyNames ? Object.getOwnPropertyNames(window) : [];
var getWindowNames = function(it) {
    try {
        return $getOwnPropertyNames(it);
    } catch (error) {
        return arraySlice(windowNames);
    }
};
// fallback for IE11 buggy Object.getOwnPropertyNames with iframe and window
module.exports.f = function getOwnPropertyNames(it) {
    return windowNames && classof(it) == 'Window' ? getWindowNames(it) : $getOwnPropertyNames(toIndexedObject(it));
};

},{"../internals/classof-raw":"fZmv0","../internals/to-indexed-object":"kvdfV","../internals/object-get-own-property-names":"lJsHK","../internals/array-slice-simple":"bhPIa"}],"bhPIa":[function(require,module,exports) {
var global = require('../internals/global');
var toAbsoluteIndex = require('../internals/to-absolute-index');
var lengthOfArrayLike = require('../internals/length-of-array-like');
var createProperty = require('../internals/create-property');
var Array = global.Array;
var max = Math.max;
module.exports = function(O, start, end) {
    var length = lengthOfArrayLike(O);
    var k = toAbsoluteIndex(start, length);
    var fin = toAbsoluteIndex(end === undefined ? length : end, length);
    var result = Array(max(fin - k, 0));
    for(var n = 0; k < fin; k++, n++)createProperty(result, n, O[k]);
    result.length = n;
    return result;
};

},{"../internals/global":"avqB7","../internals/to-absolute-index":"1L8Z0","../internals/length-of-array-like":"icIfm","../internals/create-property":"9Ttit"}],"1qW3G":[function(require,module,exports) {
// eslint-disable-next-line es-x/no-object-getownpropertysymbols -- safe
exports.f = Object.getOwnPropertySymbols;

},{}],"fXiQ5":[function(require,module,exports) {
var createNonEnumerableProperty = require('../internals/create-non-enumerable-property');
module.exports = function(target, key, value, options) {
    if (options && options.enumerable) target[key] = value;
    else createNonEnumerableProperty(target, key, value);
    return target;
};

},{"../internals/create-non-enumerable-property":"2bHDS"}],"fnRxR":[function(require,module,exports) {
var wellKnownSymbol = require('../internals/well-known-symbol');
exports.f = wellKnownSymbol;

},{"../internals/well-known-symbol":"cSGfj"}],"9h0sm":[function(require,module,exports) {
var path = require('../internals/path');
var hasOwn = require('../internals/has-own-property');
var wrappedWellKnownSymbolModule = require('../internals/well-known-symbol-wrapped');
var defineProperty = require('../internals/object-define-property').f;
module.exports = function(NAME) {
    var Symbol = path.Symbol || (path.Symbol = {});
    if (!hasOwn(Symbol, NAME)) defineProperty(Symbol, NAME, {
        value: wrappedWellKnownSymbolModule.f(NAME)
    });
};

},{"../internals/path":"8QO0z","../internals/has-own-property":"lacXY","../internals/well-known-symbol-wrapped":"fnRxR","../internals/object-define-property":"gbIb1"}],"h6U5e":[function(require,module,exports) {
var call = require('../internals/function-call');
var getBuiltIn = require('../internals/get-built-in');
var wellKnownSymbol = require('../internals/well-known-symbol');
var defineBuiltIn = require('../internals/define-built-in');
module.exports = function() {
    var Symbol = getBuiltIn('Symbol');
    var SymbolPrototype = Symbol && Symbol.prototype;
    var valueOf = SymbolPrototype && SymbolPrototype.valueOf;
    var TO_PRIMITIVE = wellKnownSymbol('toPrimitive');
    if (SymbolPrototype && !SymbolPrototype[TO_PRIMITIVE]) // `Symbol.prototype[@@toPrimitive]` method
    // https://tc39.es/ecma262/#sec-symbol.prototype-@@toprimitive
    // eslint-disable-next-line no-unused-vars -- required for .length
    defineBuiltIn(SymbolPrototype, TO_PRIMITIVE, function(hint) {
        return call(valueOf, this);
    }, {
        arity: 1
    });
};

},{"../internals/function-call":"acNwo","../internals/get-built-in":"6wm25","../internals/well-known-symbol":"cSGfj","../internals/define-built-in":"fXiQ5"}],"8BLll":[function(require,module,exports) {
var TO_STRING_TAG_SUPPORT = require('../internals/to-string-tag-support');
var defineProperty = require('../internals/object-define-property').f;
var createNonEnumerableProperty = require('../internals/create-non-enumerable-property');
var hasOwn = require('../internals/has-own-property');
var toString = require('../internals/object-to-string');
var wellKnownSymbol = require('../internals/well-known-symbol');
var TO_STRING_TAG = wellKnownSymbol('toStringTag');
module.exports = function(it, TAG, STATIC, SET_METHOD) {
    if (it) {
        var target = STATIC ? it : it.prototype;
        if (!hasOwn(target, TO_STRING_TAG)) defineProperty(target, TO_STRING_TAG, {
            configurable: true,
            value: TAG
        });
        if (SET_METHOD && !TO_STRING_TAG_SUPPORT) createNonEnumerableProperty(target, 'toString', toString);
    }
};

},{"../internals/to-string-tag-support":"7vtNP","../internals/object-define-property":"gbIb1","../internals/create-non-enumerable-property":"2bHDS","../internals/has-own-property":"lacXY","../internals/object-to-string":"5lBHV","../internals/well-known-symbol":"cSGfj"}],"5lBHV":[function(require,module,exports) {
'use strict';
var TO_STRING_TAG_SUPPORT = require('../internals/to-string-tag-support');
var classof = require('../internals/classof');
// `Object.prototype.toString` method implementation
// https://tc39.es/ecma262/#sec-object.prototype.tostring
module.exports = TO_STRING_TAG_SUPPORT ? ({}).toString : function toString() {
    return '[object ' + classof(this) + ']';
};

},{"../internals/to-string-tag-support":"7vtNP","../internals/classof":"5J26j"}],"73CuX":[function(require,module,exports) {
var NATIVE_WEAK_MAP = require('../internals/native-weak-map');
var global = require('../internals/global');
var uncurryThis = require('../internals/function-uncurry-this');
var isObject = require('../internals/is-object');
var createNonEnumerableProperty = require('../internals/create-non-enumerable-property');
var hasOwn = require('../internals/has-own-property');
var shared = require('../internals/shared-store');
var sharedKey = require('../internals/shared-key');
var hiddenKeys = require('../internals/hidden-keys');
var OBJECT_ALREADY_INITIALIZED = 'Object already initialized';
var TypeError = global.TypeError;
var WeakMap = global.WeakMap;
var set, get, has;
var enforce = function(it) {
    return has(it) ? get(it) : set(it, {});
};
var getterFor = function(TYPE) {
    return function(it) {
        var state;
        if (!isObject(it) || (state = get(it)).type !== TYPE) throw TypeError('Incompatible receiver, ' + TYPE + ' required');
        return state;
    };
};
if (NATIVE_WEAK_MAP || shared.state) {
    var store = shared.state || (shared.state = new WeakMap());
    var wmget = uncurryThis(store.get);
    var wmhas = uncurryThis(store.has);
    var wmset = uncurryThis(store.set);
    set = function(it, metadata) {
        if (wmhas(store, it)) throw new TypeError(OBJECT_ALREADY_INITIALIZED);
        metadata.facade = it;
        wmset(store, it, metadata);
        return metadata;
    };
    get = function(it) {
        return wmget(store, it) || {};
    };
    has = function(it) {
        return wmhas(store, it);
    };
} else {
    var STATE = sharedKey('state');
    hiddenKeys[STATE] = true;
    set = function(it, metadata) {
        if (hasOwn(it, STATE)) throw new TypeError(OBJECT_ALREADY_INITIALIZED);
        metadata.facade = it;
        createNonEnumerableProperty(it, STATE, metadata);
        return metadata;
    };
    get = function(it) {
        return hasOwn(it, STATE) ? it[STATE] : {};
    };
    has = function(it) {
        return hasOwn(it, STATE);
    };
}
module.exports = {
    set: set,
    get: get,
    has: has,
    enforce: enforce,
    getterFor: getterFor
};

},{"../internals/native-weak-map":"ed6x7","../internals/global":"avqB7","../internals/function-uncurry-this":"5P4uT","../internals/is-object":"fRYFX","../internals/create-non-enumerable-property":"2bHDS","../internals/has-own-property":"lacXY","../internals/shared-store":"4KS55","../internals/shared-key":"fjVxw","../internals/hidden-keys":"7lBVj"}],"ed6x7":[function(require,module,exports) {
var global = require('../internals/global');
var isCallable = require('../internals/is-callable');
var inspectSource = require('../internals/inspect-source');
var WeakMap = global.WeakMap;
module.exports = isCallable(WeakMap) && /native code/.test(inspectSource(WeakMap));

},{"../internals/global":"avqB7","../internals/is-callable":"fqYwo","../internals/inspect-source":"17BO4"}],"8g5by":[function(require,module,exports) {
var bind = require('../internals/function-bind-context');
var uncurryThis = require('../internals/function-uncurry-this');
var IndexedObject = require('../internals/indexed-object');
var toObject = require('../internals/to-object');
var lengthOfArrayLike = require('../internals/length-of-array-like');
var arraySpeciesCreate = require('../internals/array-species-create');
var push = uncurryThis([].push);
// `Array.prototype.{ forEach, map, filter, some, every, find, findIndex, filterReject }` methods implementation
var createMethod = function(TYPE) {
    var IS_MAP = TYPE == 1;
    var IS_FILTER = TYPE == 2;
    var IS_SOME = TYPE == 3;
    var IS_EVERY = TYPE == 4;
    var IS_FIND_INDEX = TYPE == 6;
    var IS_FILTER_REJECT = TYPE == 7;
    var NO_HOLES = TYPE == 5 || IS_FIND_INDEX;
    return function($this, callbackfn, that, specificCreate) {
        var O = toObject($this);
        var self = IndexedObject(O);
        var boundFunction = bind(callbackfn, that);
        var length = lengthOfArrayLike(self);
        var index = 0;
        var create = specificCreate || arraySpeciesCreate;
        var target = IS_MAP ? create($this, length) : IS_FILTER || IS_FILTER_REJECT ? create($this, 0) : undefined;
        var value, result;
        for(; length > index; index++)if (NO_HOLES || index in self) {
            value = self[index];
            result = boundFunction(value, index, O);
            if (TYPE) {
                if (IS_MAP) target[index] = result; // map
                else if (result) switch(TYPE){
                    case 3:
                        return true; // some
                    case 5:
                        return value; // find
                    case 6:
                        return index; // findIndex
                    case 2:
                        push(target, value); // filter
                }
                else switch(TYPE){
                    case 4:
                        return false; // every
                    case 7:
                        push(target, value); // filterReject
                }
            }
        }
        return IS_FIND_INDEX ? -1 : IS_SOME || IS_EVERY ? IS_EVERY : target;
    };
};
module.exports = {
    // `Array.prototype.forEach` method
    // https://tc39.es/ecma262/#sec-array.prototype.foreach
    forEach: createMethod(0),
    // `Array.prototype.map` method
    // https://tc39.es/ecma262/#sec-array.prototype.map
    map: createMethod(1),
    // `Array.prototype.filter` method
    // https://tc39.es/ecma262/#sec-array.prototype.filter
    filter: createMethod(2),
    // `Array.prototype.some` method
    // https://tc39.es/ecma262/#sec-array.prototype.some
    some: createMethod(3),
    // `Array.prototype.every` method
    // https://tc39.es/ecma262/#sec-array.prototype.every
    every: createMethod(4),
    // `Array.prototype.find` method
    // https://tc39.es/ecma262/#sec-array.prototype.find
    find: createMethod(5),
    // `Array.prototype.findIndex` method
    // https://tc39.es/ecma262/#sec-array.prototype.findIndex
    findIndex: createMethod(6),
    // `Array.prototype.filterReject` method
    // https://github.com/tc39/proposal-array-filtering
    filterReject: createMethod(7)
};

},{"../internals/function-bind-context":"jZFzY","../internals/function-uncurry-this":"5P4uT","../internals/indexed-object":"6NkN4","../internals/to-object":"70Utf","../internals/length-of-array-like":"icIfm","../internals/array-species-create":"jQr4t"}],"kn0pm":[function(require,module,exports) {
var $ = require('../internals/export');
var getBuiltIn = require('../internals/get-built-in');
var hasOwn = require('../internals/has-own-property');
var toString = require('../internals/to-string');
var shared = require('../internals/shared');
var NATIVE_SYMBOL_REGISTRY = require('../internals/native-symbol-registry');
var StringToSymbolRegistry = shared('string-to-symbol-registry');
var SymbolToStringRegistry = shared('symbol-to-string-registry');
// `Symbol.for` method
// https://tc39.es/ecma262/#sec-symbol.for
$({
    target: 'Symbol',
    stat: true,
    forced: !NATIVE_SYMBOL_REGISTRY
}, {
    'for': function(key) {
        var string = toString(key);
        if (hasOwn(StringToSymbolRegistry, string)) return StringToSymbolRegistry[string];
        var symbol = getBuiltIn('Symbol')(string);
        StringToSymbolRegistry[string] = symbol;
        SymbolToStringRegistry[symbol] = string;
        return symbol;
    }
});

},{"../internals/export":"jjems","../internals/get-built-in":"6wm25","../internals/has-own-property":"lacXY","../internals/to-string":"3DQpG","../internals/shared":"9VSA0","../internals/native-symbol-registry":"9mtfK"}],"9mtfK":[function(require,module,exports) {
var NATIVE_SYMBOL = require('../internals/native-symbol');
/* eslint-disable es-x/no-symbol -- safe */ module.exports = NATIVE_SYMBOL && !!Symbol['for'] && !!Symbol.keyFor;

},{"../internals/native-symbol":"kChWP"}],"iBU0N":[function(require,module,exports) {
var $ = require('../internals/export');
var hasOwn = require('../internals/has-own-property');
var isSymbol = require('../internals/is-symbol');
var tryToString = require('../internals/try-to-string');
var shared = require('../internals/shared');
var NATIVE_SYMBOL_REGISTRY = require('../internals/native-symbol-registry');
var SymbolToStringRegistry = shared('symbol-to-string-registry');
// `Symbol.keyFor` method
// https://tc39.es/ecma262/#sec-symbol.keyfor
$({
    target: 'Symbol',
    stat: true,
    forced: !NATIVE_SYMBOL_REGISTRY
}, {
    keyFor: function keyFor(sym) {
        if (!isSymbol(sym)) throw TypeError(tryToString(sym) + ' is not a symbol');
        if (hasOwn(SymbolToStringRegistry, sym)) return SymbolToStringRegistry[sym];
    }
});

},{"../internals/export":"jjems","../internals/has-own-property":"lacXY","../internals/is-symbol":"58L3i","../internals/try-to-string":"c7C3w","../internals/shared":"9VSA0","../internals/native-symbol-registry":"9mtfK"}],"2qOVD":[function(require,module,exports) {
var $ = require('../internals/export');
var getBuiltIn = require('../internals/get-built-in');
var apply = require('../internals/function-apply');
var call = require('../internals/function-call');
var uncurryThis = require('../internals/function-uncurry-this');
var fails = require('../internals/fails');
var isArray = require('../internals/is-array');
var isCallable = require('../internals/is-callable');
var isObject = require('../internals/is-object');
var isSymbol = require('../internals/is-symbol');
var arraySlice = require('../internals/array-slice');
var NATIVE_SYMBOL = require('../internals/native-symbol');
var $stringify = getBuiltIn('JSON', 'stringify');
var exec = uncurryThis(/./.exec);
var charAt = uncurryThis(''.charAt);
var charCodeAt = uncurryThis(''.charCodeAt);
var replace = uncurryThis(''.replace);
var numberToString = uncurryThis(1.0.toString);
var tester = /[\uD800-\uDFFF]/g;
var low = /^[\uD800-\uDBFF]$/;
var hi = /^[\uDC00-\uDFFF]$/;
var WRONG_SYMBOLS_CONVERSION = !NATIVE_SYMBOL || fails(function() {
    var symbol = getBuiltIn('Symbol')();
    // MS Edge converts symbol values to JSON as {}
    return $stringify([
        symbol
    ]) != '[null]' || $stringify({
        a: symbol
    }) != '{}' || $stringify(Object(symbol)) != '{}';
});
// https://github.com/tc39/proposal-well-formed-stringify
var ILL_FORMED_UNICODE = fails(function() {
    return $stringify('\uDF06\uD834') !== '"\\udf06\\ud834"' || $stringify('\uDEAD') !== '"\\udead"';
});
var stringifyWithSymbolsFix = function(it, replacer) {
    var args = arraySlice(arguments);
    var $replacer = replacer;
    if (!isObject(replacer) && it === undefined || isSymbol(it)) return; // IE8 returns string on undefined
    if (!isArray(replacer)) replacer = function(key, value) {
        if (isCallable($replacer)) value = call($replacer, this, key, value);
        if (!isSymbol(value)) return value;
    };
    args[1] = replacer;
    return apply($stringify, null, args);
};
var fixIllFormed = function(match, offset, string) {
    var prev = charAt(string, offset - 1);
    var next = charAt(string, offset + 1);
    if (exec(low, match) && !exec(hi, next) || exec(hi, match) && !exec(low, prev)) return '\\u' + numberToString(charCodeAt(match, 0), 16);
    return match;
};
if ($stringify) // `JSON.stringify` method
// https://tc39.es/ecma262/#sec-json.stringify
$({
    target: 'JSON',
    stat: true,
    arity: 3,
    forced: WRONG_SYMBOLS_CONVERSION || ILL_FORMED_UNICODE
}, {
    // eslint-disable-next-line no-unused-vars -- required for `.length`
    stringify: function stringify(it, replacer, space) {
        var args = arraySlice(arguments);
        var result = apply(WRONG_SYMBOLS_CONVERSION ? stringifyWithSymbolsFix : $stringify, null, args);
        return ILL_FORMED_UNICODE && typeof result == 'string' ? replace(result, tester, fixIllFormed) : result;
    }
});

},{"../internals/export":"jjems","../internals/get-built-in":"6wm25","../internals/function-apply":"kLBNq","../internals/function-call":"acNwo","../internals/function-uncurry-this":"5P4uT","../internals/fails":"luPpk","../internals/is-array":"byYNs","../internals/is-callable":"fqYwo","../internals/is-object":"fRYFX","../internals/is-symbol":"58L3i","../internals/array-slice":"gooNL","../internals/native-symbol":"kChWP"}],"gooNL":[function(require,module,exports) {
var uncurryThis = require('../internals/function-uncurry-this');
module.exports = uncurryThis([].slice);

},{"../internals/function-uncurry-this":"5P4uT"}],"ga77Y":[function(require,module,exports) {
var $ = require('../internals/export');
var NATIVE_SYMBOL = require('../internals/native-symbol');
var fails = require('../internals/fails');
var getOwnPropertySymbolsModule = require('../internals/object-get-own-property-symbols');
var toObject = require('../internals/to-object');
// V8 ~ Chrome 38 and 39 `Object.getOwnPropertySymbols` fails on primitives
// https://bugs.chromium.org/p/v8/issues/detail?id=3443
var FORCED = !NATIVE_SYMBOL || fails(function() {
    getOwnPropertySymbolsModule.f(1);
});
// `Object.getOwnPropertySymbols` method
// https://tc39.es/ecma262/#sec-object.getownpropertysymbols
$({
    target: 'Object',
    stat: true,
    forced: FORCED
}, {
    getOwnPropertySymbols: function getOwnPropertySymbols(it) {
        var $getOwnPropertySymbols = getOwnPropertySymbolsModule.f;
        return $getOwnPropertySymbols ? $getOwnPropertySymbols(toObject(it)) : [];
    }
});

},{"../internals/export":"jjems","../internals/native-symbol":"kChWP","../internals/fails":"luPpk","../internals/object-get-own-property-symbols":"1qW3G","../internals/to-object":"70Utf"}],"jgMqD":[function(require,module,exports) {
var defineWellKnownSymbol = require('../internals/define-well-known-symbol');
// `Symbol.asyncIterator` well-known symbol
// https://tc39.es/ecma262/#sec-symbol.asynciterator
defineWellKnownSymbol('asyncIterator');

},{"../internals/define-well-known-symbol":"9h0sm"}],"1l1uI":[function(require,module,exports) {

},{}],"fnPvK":[function(require,module,exports) {
var defineWellKnownSymbol = require('../internals/define-well-known-symbol');
// `Symbol.hasInstance` well-known symbol
// https://tc39.es/ecma262/#sec-symbol.hasinstance
defineWellKnownSymbol('hasInstance');

},{"../internals/define-well-known-symbol":"9h0sm"}],"iibvg":[function(require,module,exports) {
var defineWellKnownSymbol = require('../internals/define-well-known-symbol');
// `Symbol.isConcatSpreadable` well-known symbol
// https://tc39.es/ecma262/#sec-symbol.isconcatspreadable
defineWellKnownSymbol('isConcatSpreadable');

},{"../internals/define-well-known-symbol":"9h0sm"}],"cpY48":[function(require,module,exports) {
var defineWellKnownSymbol = require('../internals/define-well-known-symbol');
// `Symbol.iterator` well-known symbol
// https://tc39.es/ecma262/#sec-symbol.iterator
defineWellKnownSymbol('iterator');

},{"../internals/define-well-known-symbol":"9h0sm"}],"jPvhH":[function(require,module,exports) {
var defineWellKnownSymbol = require('../internals/define-well-known-symbol');
// `Symbol.match` well-known symbol
// https://tc39.es/ecma262/#sec-symbol.match
defineWellKnownSymbol('match');

},{"../internals/define-well-known-symbol":"9h0sm"}],"2cyj8":[function(require,module,exports) {
var defineWellKnownSymbol = require('../internals/define-well-known-symbol');
// `Symbol.matchAll` well-known symbol
// https://tc39.es/ecma262/#sec-symbol.matchall
defineWellKnownSymbol('matchAll');

},{"../internals/define-well-known-symbol":"9h0sm"}],"12aQP":[function(require,module,exports) {
var defineWellKnownSymbol = require('../internals/define-well-known-symbol');
// `Symbol.replace` well-known symbol
// https://tc39.es/ecma262/#sec-symbol.replace
defineWellKnownSymbol('replace');

},{"../internals/define-well-known-symbol":"9h0sm"}],"lC05F":[function(require,module,exports) {
var defineWellKnownSymbol = require('../internals/define-well-known-symbol');
// `Symbol.search` well-known symbol
// https://tc39.es/ecma262/#sec-symbol.search
defineWellKnownSymbol('search');

},{"../internals/define-well-known-symbol":"9h0sm"}],"itj89":[function(require,module,exports) {
var defineWellKnownSymbol = require('../internals/define-well-known-symbol');
// `Symbol.species` well-known symbol
// https://tc39.es/ecma262/#sec-symbol.species
defineWellKnownSymbol('species');

},{"../internals/define-well-known-symbol":"9h0sm"}],"7fQ1B":[function(require,module,exports) {
var defineWellKnownSymbol = require('../internals/define-well-known-symbol');
// `Symbol.split` well-known symbol
// https://tc39.es/ecma262/#sec-symbol.split
defineWellKnownSymbol('split');

},{"../internals/define-well-known-symbol":"9h0sm"}],"hFsWD":[function(require,module,exports) {
var defineWellKnownSymbol = require('../internals/define-well-known-symbol');
var defineSymbolToPrimitive = require('../internals/symbol-define-to-primitive');
// `Symbol.toPrimitive` well-known symbol
// https://tc39.es/ecma262/#sec-symbol.toprimitive
defineWellKnownSymbol('toPrimitive');
// `Symbol.prototype[@@toPrimitive]` method
// https://tc39.es/ecma262/#sec-symbol.prototype-@@toprimitive
defineSymbolToPrimitive();

},{"../internals/define-well-known-symbol":"9h0sm","../internals/symbol-define-to-primitive":"h6U5e"}],"dlZcB":[function(require,module,exports) {
var getBuiltIn = require('../internals/get-built-in');
var defineWellKnownSymbol = require('../internals/define-well-known-symbol');
var setToStringTag = require('../internals/set-to-string-tag');
// `Symbol.toStringTag` well-known symbol
// https://tc39.es/ecma262/#sec-symbol.tostringtag
defineWellKnownSymbol('toStringTag');
// `Symbol.prototype[@@toStringTag]` property
// https://tc39.es/ecma262/#sec-symbol.prototype-@@tostringtag
setToStringTag(getBuiltIn('Symbol'), 'Symbol');

},{"../internals/get-built-in":"6wm25","../internals/define-well-known-symbol":"9h0sm","../internals/set-to-string-tag":"8BLll"}],"jUhyE":[function(require,module,exports) {
var defineWellKnownSymbol = require('../internals/define-well-known-symbol');
// `Symbol.unscopables` well-known symbol
// https://tc39.es/ecma262/#sec-symbol.unscopables
defineWellKnownSymbol('unscopables');

},{"../internals/define-well-known-symbol":"9h0sm"}],"6hVtO":[function(require,module,exports) {
var global = require('../internals/global');
var setToStringTag = require('../internals/set-to-string-tag');
// JSON[@@toStringTag] property
// https://tc39.es/ecma262/#sec-json-@@tostringtag
setToStringTag(global.JSON, 'JSON', true);

},{"../internals/global":"avqB7","../internals/set-to-string-tag":"8BLll"}],"10AwK":[function(require,module,exports) {

},{}],"jxoXe":[function(require,module,exports) {

},{}],"fVWQ7":[function(require,module,exports) {
require('../modules/es.array.iterator');
var DOMIterables = require('../internals/dom-iterables');
var global = require('../internals/global');
var classof = require('../internals/classof');
var createNonEnumerableProperty = require('../internals/create-non-enumerable-property');
var Iterators = require('../internals/iterators');
var wellKnownSymbol = require('../internals/well-known-symbol');
var TO_STRING_TAG = wellKnownSymbol('toStringTag');
for(var COLLECTION_NAME in DOMIterables){
    var Collection = global[COLLECTION_NAME];
    var CollectionPrototype = Collection && Collection.prototype;
    if (CollectionPrototype && classof(CollectionPrototype) !== TO_STRING_TAG) createNonEnumerableProperty(CollectionPrototype, TO_STRING_TAG, COLLECTION_NAME);
    Iterators[COLLECTION_NAME] = Iterators.Array;
}

},{"../modules/es.array.iterator":"4Hhzg","../internals/dom-iterables":"1GaCC","../internals/global":"avqB7","../internals/classof":"5J26j","../internals/create-non-enumerable-property":"2bHDS","../internals/iterators":"jgzzK","../internals/well-known-symbol":"cSGfj"}],"4Hhzg":[function(require,module,exports) {
'use strict';
var toIndexedObject = require('../internals/to-indexed-object');
var addToUnscopables = require('../internals/add-to-unscopables');
var Iterators = require('../internals/iterators');
var InternalStateModule = require('../internals/internal-state');
var defineProperty = require('../internals/object-define-property').f;
var defineIterator = require('../internals/define-iterator');
var IS_PURE = require('../internals/is-pure');
var DESCRIPTORS = require('../internals/descriptors');
var ARRAY_ITERATOR = 'Array Iterator';
var setInternalState = InternalStateModule.set;
var getInternalState = InternalStateModule.getterFor(ARRAY_ITERATOR);
// `Array.prototype.entries` method
// https://tc39.es/ecma262/#sec-array.prototype.entries
// `Array.prototype.keys` method
// https://tc39.es/ecma262/#sec-array.prototype.keys
// `Array.prototype.values` method
// https://tc39.es/ecma262/#sec-array.prototype.values
// `Array.prototype[@@iterator]` method
// https://tc39.es/ecma262/#sec-array.prototype-@@iterator
// `CreateArrayIterator` internal method
// https://tc39.es/ecma262/#sec-createarrayiterator
module.exports = defineIterator(Array, 'Array', function(iterated, kind) {
    setInternalState(this, {
        type: ARRAY_ITERATOR,
        target: toIndexedObject(iterated),
        index: 0,
        kind: kind // kind
    });
// `%ArrayIteratorPrototype%.next` method
// https://tc39.es/ecma262/#sec-%arrayiteratorprototype%.next
}, function() {
    var state = getInternalState(this);
    var target = state.target;
    var kind = state.kind;
    var index = state.index++;
    if (!target || index >= target.length) {
        state.target = undefined;
        return {
            value: undefined,
            done: true
        };
    }
    if (kind == 'keys') return {
        value: index,
        done: false
    };
    if (kind == 'values') return {
        value: target[index],
        done: false
    };
    return {
        value: [
            index,
            target[index]
        ],
        done: false
    };
}, 'values');
// argumentsList[@@iterator] is %ArrayProto_values%
// https://tc39.es/ecma262/#sec-createunmappedargumentsobject
// https://tc39.es/ecma262/#sec-createmappedargumentsobject
var values = Iterators.Arguments = Iterators.Array;
// https://tc39.es/ecma262/#sec-array.prototype-@@unscopables
addToUnscopables('keys');
addToUnscopables('values');
addToUnscopables('entries');
// V8 ~ Chrome 45- bug
if (!IS_PURE && DESCRIPTORS && values.name !== 'values') try {
    defineProperty(values, 'name', {
        value: 'values'
    });
} catch (error) {}

},{"../internals/to-indexed-object":"kvdfV","../internals/add-to-unscopables":"g7y8z","../internals/iterators":"jgzzK","../internals/internal-state":"73CuX","../internals/object-define-property":"gbIb1","../internals/define-iterator":"2Z73h","../internals/is-pure":"k905C","../internals/descriptors":"87WOB"}],"g7y8z":[function(require,module,exports) {
module.exports = function() {};

},{}],"jgzzK":[function(require,module,exports) {
module.exports = {};

},{}],"2Z73h":[function(require,module,exports) {
'use strict';
var $ = require('../internals/export');
var call = require('../internals/function-call');
var IS_PURE = require('../internals/is-pure');
var FunctionName = require('../internals/function-name');
var isCallable = require('../internals/is-callable');
var createIteratorConstructor = require('../internals/create-iterator-constructor');
var getPrototypeOf = require('../internals/object-get-prototype-of');
var setPrototypeOf = require('../internals/object-set-prototype-of');
var setToStringTag = require('../internals/set-to-string-tag');
var createNonEnumerableProperty = require('../internals/create-non-enumerable-property');
var defineBuiltIn = require('../internals/define-built-in');
var wellKnownSymbol = require('../internals/well-known-symbol');
var Iterators = require('../internals/iterators');
var IteratorsCore = require('../internals/iterators-core');
var PROPER_FUNCTION_NAME = FunctionName.PROPER;
var CONFIGURABLE_FUNCTION_NAME = FunctionName.CONFIGURABLE;
var IteratorPrototype = IteratorsCore.IteratorPrototype;
var BUGGY_SAFARI_ITERATORS = IteratorsCore.BUGGY_SAFARI_ITERATORS;
var ITERATOR = wellKnownSymbol('iterator');
var KEYS = 'keys';
var VALUES = 'values';
var ENTRIES = 'entries';
var returnThis = function() {
    return this;
};
module.exports = function(Iterable, NAME, IteratorConstructor, next, DEFAULT, IS_SET, FORCED) {
    createIteratorConstructor(IteratorConstructor, NAME, next);
    var getIterationMethod = function(KIND) {
        if (KIND === DEFAULT && defaultIterator) return defaultIterator;
        if (!BUGGY_SAFARI_ITERATORS && KIND in IterablePrototype) return IterablePrototype[KIND];
        switch(KIND){
            case KEYS:
                return function keys() {
                    return new IteratorConstructor(this, KIND);
                };
            case VALUES:
                return function values() {
                    return new IteratorConstructor(this, KIND);
                };
            case ENTRIES:
                return function entries() {
                    return new IteratorConstructor(this, KIND);
                };
        }
        return function() {
            return new IteratorConstructor(this);
        };
    };
    var TO_STRING_TAG = NAME + ' Iterator';
    var INCORRECT_VALUES_NAME = false;
    var IterablePrototype = Iterable.prototype;
    var nativeIterator = IterablePrototype[ITERATOR] || IterablePrototype['@@iterator'] || DEFAULT && IterablePrototype[DEFAULT];
    var defaultIterator = !BUGGY_SAFARI_ITERATORS && nativeIterator || getIterationMethod(DEFAULT);
    var anyNativeIterator = NAME == 'Array' ? IterablePrototype.entries || nativeIterator : nativeIterator;
    var CurrentIteratorPrototype, methods, KEY;
    // fix native
    if (anyNativeIterator) {
        CurrentIteratorPrototype = getPrototypeOf(anyNativeIterator.call(new Iterable()));
        if (CurrentIteratorPrototype !== Object.prototype && CurrentIteratorPrototype.next) {
            if (!IS_PURE && getPrototypeOf(CurrentIteratorPrototype) !== IteratorPrototype) {
                if (setPrototypeOf) setPrototypeOf(CurrentIteratorPrototype, IteratorPrototype);
                else if (!isCallable(CurrentIteratorPrototype[ITERATOR])) defineBuiltIn(CurrentIteratorPrototype, ITERATOR, returnThis);
            }
            // Set @@toStringTag to native iterators
            setToStringTag(CurrentIteratorPrototype, TO_STRING_TAG, true, true);
            if (IS_PURE) Iterators[TO_STRING_TAG] = returnThis;
        }
    }
    // fix Array.prototype.{ values, @@iterator }.name in V8 / FF
    if (PROPER_FUNCTION_NAME && DEFAULT == VALUES && nativeIterator && nativeIterator.name !== VALUES) {
        if (!IS_PURE && CONFIGURABLE_FUNCTION_NAME) createNonEnumerableProperty(IterablePrototype, 'name', VALUES);
        else {
            INCORRECT_VALUES_NAME = true;
            defaultIterator = function values() {
                return call(nativeIterator, this);
            };
        }
    }
    // export additional methods
    if (DEFAULT) {
        methods = {
            values: getIterationMethod(VALUES),
            keys: IS_SET ? defaultIterator : getIterationMethod(KEYS),
            entries: getIterationMethod(ENTRIES)
        };
        if (FORCED) {
            for(KEY in methods)if (BUGGY_SAFARI_ITERATORS || INCORRECT_VALUES_NAME || !(KEY in IterablePrototype)) defineBuiltIn(IterablePrototype, KEY, methods[KEY]);
        } else $({
            target: NAME,
            proto: true,
            forced: BUGGY_SAFARI_ITERATORS || INCORRECT_VALUES_NAME
        }, methods);
    }
    // define iterator
    if ((!IS_PURE || FORCED) && IterablePrototype[ITERATOR] !== defaultIterator) defineBuiltIn(IterablePrototype, ITERATOR, defaultIterator, {
        name: DEFAULT
    });
    Iterators[NAME] = defaultIterator;
    return methods;
};

},{"../internals/export":"jjems","../internals/function-call":"acNwo","../internals/is-pure":"k905C","../internals/function-name":"efeAk","../internals/is-callable":"fqYwo","../internals/create-iterator-constructor":"Gj6Fg","../internals/object-get-prototype-of":"64xAh","../internals/object-set-prototype-of":"khGss","../internals/set-to-string-tag":"8BLll","../internals/create-non-enumerable-property":"2bHDS","../internals/define-built-in":"fXiQ5","../internals/well-known-symbol":"cSGfj","../internals/iterators":"jgzzK","../internals/iterators-core":"9WtXa"}],"efeAk":[function(require,module,exports) {
var DESCRIPTORS = require('../internals/descriptors');
var hasOwn = require('../internals/has-own-property');
var FunctionPrototype = Function.prototype;
// eslint-disable-next-line es-x/no-object-getownpropertydescriptor -- safe
var getDescriptor = DESCRIPTORS && Object.getOwnPropertyDescriptor;
var EXISTS = hasOwn(FunctionPrototype, 'name');
// additional protection from minified / mangled / dropped function names
var PROPER = EXISTS && (function something() {}).name === 'something';
var CONFIGURABLE = EXISTS && (!DESCRIPTORS || DESCRIPTORS && getDescriptor(FunctionPrototype, 'name').configurable);
module.exports = {
    EXISTS: EXISTS,
    PROPER: PROPER,
    CONFIGURABLE: CONFIGURABLE
};

},{"../internals/descriptors":"87WOB","../internals/has-own-property":"lacXY"}],"Gj6Fg":[function(require,module,exports) {
'use strict';
var IteratorPrototype = require('../internals/iterators-core').IteratorPrototype;
var create = require('../internals/object-create');
var createPropertyDescriptor = require('../internals/create-property-descriptor');
var setToStringTag = require('../internals/set-to-string-tag');
var Iterators = require('../internals/iterators');
var returnThis = function() {
    return this;
};
module.exports = function(IteratorConstructor, NAME, next, ENUMERABLE_NEXT) {
    var TO_STRING_TAG = NAME + ' Iterator';
    IteratorConstructor.prototype = create(IteratorPrototype, {
        next: createPropertyDescriptor(+!ENUMERABLE_NEXT, next)
    });
    setToStringTag(IteratorConstructor, TO_STRING_TAG, false, true);
    Iterators[TO_STRING_TAG] = returnThis;
    return IteratorConstructor;
};

},{"../internals/iterators-core":"9WtXa","../internals/object-create":"eqWCY","../internals/create-property-descriptor":"1E79z","../internals/set-to-string-tag":"8BLll","../internals/iterators":"jgzzK"}],"9WtXa":[function(require,module,exports) {
'use strict';
var fails = require('../internals/fails');
var isCallable = require('../internals/is-callable');
var create = require('../internals/object-create');
var getPrototypeOf = require('../internals/object-get-prototype-of');
var defineBuiltIn = require('../internals/define-built-in');
var wellKnownSymbol = require('../internals/well-known-symbol');
var IS_PURE = require('../internals/is-pure');
var ITERATOR = wellKnownSymbol('iterator');
var BUGGY_SAFARI_ITERATORS = false;
// `%IteratorPrototype%` object
// https://tc39.es/ecma262/#sec-%iteratorprototype%-object
var IteratorPrototype, PrototypeOfArrayIteratorPrototype, arrayIterator;
/* eslint-disable es-x/no-array-prototype-keys -- safe */ if ([].keys) {
    arrayIterator = [].keys();
    // Safari 8 has buggy iterators w/o `next`
    if (!('next' in arrayIterator)) BUGGY_SAFARI_ITERATORS = true;
    else {
        PrototypeOfArrayIteratorPrototype = getPrototypeOf(getPrototypeOf(arrayIterator));
        if (PrototypeOfArrayIteratorPrototype !== Object.prototype) IteratorPrototype = PrototypeOfArrayIteratorPrototype;
    }
}
var NEW_ITERATOR_PROTOTYPE = IteratorPrototype == undefined || fails(function() {
    var test = {};
    // FF44- legacy iterators case
    return IteratorPrototype[ITERATOR].call(test) !== test;
});
if (NEW_ITERATOR_PROTOTYPE) IteratorPrototype = {};
else if (IS_PURE) IteratorPrototype = create(IteratorPrototype);
// `%IteratorPrototype%[@@iterator]()` method
// https://tc39.es/ecma262/#sec-%iteratorprototype%-@@iterator
if (!isCallable(IteratorPrototype[ITERATOR])) defineBuiltIn(IteratorPrototype, ITERATOR, function() {
    return this;
});
module.exports = {
    IteratorPrototype: IteratorPrototype,
    BUGGY_SAFARI_ITERATORS: BUGGY_SAFARI_ITERATORS
};

},{"../internals/fails":"luPpk","../internals/is-callable":"fqYwo","../internals/object-create":"eqWCY","../internals/object-get-prototype-of":"64xAh","../internals/define-built-in":"fXiQ5","../internals/well-known-symbol":"cSGfj","../internals/is-pure":"k905C"}],"64xAh":[function(require,module,exports) {
var global = require('../internals/global');
var hasOwn = require('../internals/has-own-property');
var isCallable = require('../internals/is-callable');
var toObject = require('../internals/to-object');
var sharedKey = require('../internals/shared-key');
var CORRECT_PROTOTYPE_GETTER = require('../internals/correct-prototype-getter');
var IE_PROTO = sharedKey('IE_PROTO');
var Object = global.Object;
var ObjectPrototype = Object.prototype;
// `Object.getPrototypeOf` method
// https://tc39.es/ecma262/#sec-object.getprototypeof
module.exports = CORRECT_PROTOTYPE_GETTER ? Object.getPrototypeOf : function(O) {
    var object = toObject(O);
    if (hasOwn(object, IE_PROTO)) return object[IE_PROTO];
    var constructor = object.constructor;
    if (isCallable(constructor) && object instanceof constructor) return constructor.prototype;
    return object instanceof Object ? ObjectPrototype : null;
};

},{"../internals/global":"avqB7","../internals/has-own-property":"lacXY","../internals/is-callable":"fqYwo","../internals/to-object":"70Utf","../internals/shared-key":"fjVxw","../internals/correct-prototype-getter":"2TgmB"}],"2TgmB":[function(require,module,exports) {
var fails = require('../internals/fails');
module.exports = !fails(function() {
    function F() {}
    F.prototype.constructor = null;
    // eslint-disable-next-line es-x/no-object-getprototypeof -- required for testing
    return Object.getPrototypeOf(new F()) !== F.prototype;
});

},{"../internals/fails":"luPpk"}],"khGss":[function(require,module,exports) {
/* eslint-disable no-proto -- safe */ var uncurryThis = require('../internals/function-uncurry-this');
var anObject = require('../internals/an-object');
var aPossiblePrototype = require('../internals/a-possible-prototype');
// `Object.setPrototypeOf` method
// https://tc39.es/ecma262/#sec-object.setprototypeof
// Works with __proto__ only. Old v8 can't work with null proto objects.
// eslint-disable-next-line es-x/no-object-setprototypeof -- safe
module.exports = Object.setPrototypeOf || ('__proto__' in {} ? function() {
    var CORRECT_SETTER = false;
    var test = {};
    var setter;
    try {
        // eslint-disable-next-line es-x/no-object-getownpropertydescriptor -- safe
        setter = uncurryThis(Object.getOwnPropertyDescriptor(Object.prototype, '__proto__').set);
        setter(test, []);
        CORRECT_SETTER = test instanceof Array;
    } catch (error) {}
    return function setPrototypeOf(O, proto) {
        anObject(O);
        aPossiblePrototype(proto);
        if (CORRECT_SETTER) setter(O, proto);
        else O.__proto__ = proto;
        return O;
    };
}() : undefined);

},{"../internals/function-uncurry-this":"5P4uT","../internals/an-object":"bTNyr","../internals/a-possible-prototype":"lqVYU"}],"lqVYU":[function(require,module,exports) {
var global = require('../internals/global');
var isCallable = require('../internals/is-callable');
var String = global.String;
var TypeError = global.TypeError;
module.exports = function(argument) {
    if (typeof argument == 'object' || isCallable(argument)) return argument;
    throw TypeError("Can't set " + String(argument) + ' as a prototype');
};

},{"../internals/global":"avqB7","../internals/is-callable":"fqYwo"}],"1GaCC":[function(require,module,exports) {
// iterable DOM collections
// flag - `iterable` interface - 'entries', 'keys', 'values', 'forEach' methods
module.exports = {
    CSSRuleList: 0,
    CSSStyleDeclaration: 0,
    CSSValueList: 0,
    ClientRectList: 0,
    DOMRectList: 0,
    DOMStringList: 0,
    DOMTokenList: 1,
    DataTransferItemList: 0,
    FileList: 0,
    HTMLAllCollection: 0,
    HTMLCollection: 0,
    HTMLFormElement: 0,
    HTMLSelectElement: 0,
    MediaList: 0,
    MimeTypeArray: 0,
    NamedNodeMap: 0,
    NodeList: 1,
    PaintRequestList: 0,
    Plugin: 0,
    PluginArray: 0,
    SVGLengthList: 0,
    SVGNumberList: 0,
    SVGPathSegList: 0,
    SVGPointList: 0,
    SVGStringList: 0,
    SVGTransformList: 0,
    SourceBufferList: 0,
    StyleSheetList: 0,
    TextTrackCueList: 0,
    TextTrackList: 0,
    TouchList: 0
};

},{}],"8vr0u":[function(require,module,exports) {
var defineWellKnownSymbol = require('../internals/define-well-known-symbol');
// `Symbol.asyncDispose` well-known symbol
// https://github.com/tc39/proposal-using-statement
defineWellKnownSymbol('asyncDispose');

},{"../internals/define-well-known-symbol":"9h0sm"}],"1nv2L":[function(require,module,exports) {
var defineWellKnownSymbol = require('../internals/define-well-known-symbol');
// `Symbol.dispose` well-known symbol
// https://github.com/tc39/proposal-using-statement
defineWellKnownSymbol('dispose');

},{"../internals/define-well-known-symbol":"9h0sm"}],"aZlnJ":[function(require,module,exports) {
var defineWellKnownSymbol = require('../internals/define-well-known-symbol');
// `Symbol.matcher` well-known symbol
// https://github.com/tc39/proposal-pattern-matching
defineWellKnownSymbol('matcher');

},{"../internals/define-well-known-symbol":"9h0sm"}],"fOMFf":[function(require,module,exports) {
var defineWellKnownSymbol = require('../internals/define-well-known-symbol');
// `Symbol.metadata` well-known symbol
// https://github.com/tc39/proposal-decorators
defineWellKnownSymbol('metadata');

},{"../internals/define-well-known-symbol":"9h0sm"}],"7OliP":[function(require,module,exports) {
var defineWellKnownSymbol = require('../internals/define-well-known-symbol');
// `Symbol.observable` well-known symbol
// https://github.com/tc39/proposal-observable
defineWellKnownSymbol('observable');

},{"../internals/define-well-known-symbol":"9h0sm"}],"h404G":[function(require,module,exports) {
// TODO: remove from `core-js@4`
var defineWellKnownSymbol = require('../internals/define-well-known-symbol');
// `Symbol.patternMatch` well-known symbol
// https://github.com/tc39/proposal-pattern-matching
defineWellKnownSymbol('patternMatch');

},{"../internals/define-well-known-symbol":"9h0sm"}],"8hMxk":[function(require,module,exports) {
// TODO: remove from `core-js@4`
var defineWellKnownSymbol = require('../internals/define-well-known-symbol');
defineWellKnownSymbol('replaceAll');

},{"../internals/define-well-known-symbol":"9h0sm"}],"hNMZV":[function(require,module,exports) {
module.exports = require("core-js-pure/features/get-iterator-method");

},{"core-js-pure/features/get-iterator-method":"gqGjW"}],"gqGjW":[function(require,module,exports) {
module.exports = require('../full/get-iterator-method');

},{"../full/get-iterator-method":"7Op7c"}],"7Op7c":[function(require,module,exports) {
var parent = require('../actual/get-iterator-method');
module.exports = parent;

},{"../actual/get-iterator-method":"2DIog"}],"2DIog":[function(require,module,exports) {
var parent = require('../stable/get-iterator-method');
module.exports = parent;

},{"../stable/get-iterator-method":"gLYky"}],"gLYky":[function(require,module,exports) {
var parent = require('../es/get-iterator-method');
require('../modules/web.dom-collections.iterator');
module.exports = parent;

},{"../es/get-iterator-method":"aF91r","../modules/web.dom-collections.iterator":"fVWQ7"}],"aF91r":[function(require,module,exports) {
require('../modules/es.array.iterator');
require('../modules/es.string.iterator');
var getIteratorMethod = require('../internals/get-iterator-method');
module.exports = getIteratorMethod;

},{"../modules/es.array.iterator":"4Hhzg","../modules/es.string.iterator":"jHlRH","../internals/get-iterator-method":"hgHU0"}],"jHlRH":[function(require,module,exports) {
'use strict';
var charAt = require('../internals/string-multibyte').charAt;
var toString = require('../internals/to-string');
var InternalStateModule = require('../internals/internal-state');
var defineIterator = require('../internals/define-iterator');
var STRING_ITERATOR = 'String Iterator';
var setInternalState = InternalStateModule.set;
var getInternalState = InternalStateModule.getterFor(STRING_ITERATOR);
// `String.prototype[@@iterator]` method
// https://tc39.es/ecma262/#sec-string.prototype-@@iterator
defineIterator(String, 'String', function(iterated) {
    setInternalState(this, {
        type: STRING_ITERATOR,
        string: toString(iterated),
        index: 0
    });
// `%StringIteratorPrototype%.next` method
// https://tc39.es/ecma262/#sec-%stringiteratorprototype%.next
}, function next() {
    var state = getInternalState(this);
    var string = state.string;
    var index = state.index;
    var point;
    if (index >= string.length) return {
        value: undefined,
        done: true
    };
    point = charAt(string, index);
    state.index += point.length;
    return {
        value: point,
        done: false
    };
});

},{"../internals/string-multibyte":"97cgB","../internals/to-string":"3DQpG","../internals/internal-state":"73CuX","../internals/define-iterator":"2Z73h"}],"97cgB":[function(require,module,exports) {
var uncurryThis = require('../internals/function-uncurry-this');
var toIntegerOrInfinity = require('../internals/to-integer-or-infinity');
var toString = require('../internals/to-string');
var requireObjectCoercible = require('../internals/require-object-coercible');
var charAt = uncurryThis(''.charAt);
var charCodeAt = uncurryThis(''.charCodeAt);
var stringSlice = uncurryThis(''.slice);
var createMethod = function(CONVERT_TO_STRING) {
    return function($this, pos) {
        var S = toString(requireObjectCoercible($this));
        var position = toIntegerOrInfinity(pos);
        var size = S.length;
        var first, second;
        if (position < 0 || position >= size) return CONVERT_TO_STRING ? '' : undefined;
        first = charCodeAt(S, position);
        return first < 0xD800 || first > 0xDBFF || position + 1 === size || (second = charCodeAt(S, position + 1)) < 0xDC00 || second > 0xDFFF ? CONVERT_TO_STRING ? charAt(S, position) : first : CONVERT_TO_STRING ? stringSlice(S, position, position + 2) : (first - 0xD800 << 10) + (second - 0xDC00) + 0x10000;
    };
};
module.exports = {
    // `String.prototype.codePointAt` method
    // https://tc39.es/ecma262/#sec-string.prototype.codepointat
    codeAt: createMethod(false),
    // `String.prototype.at` method
    // https://github.com/mathiasbynens/String.prototype.at
    charAt: createMethod(true)
};

},{"../internals/function-uncurry-this":"5P4uT","../internals/to-integer-or-infinity":"dQ2Uo","../internals/to-string":"3DQpG","../internals/require-object-coercible":"3XBjA"}],"hgHU0":[function(require,module,exports) {
var classof = require('../internals/classof');
var getMethod = require('../internals/get-method');
var Iterators = require('../internals/iterators');
var wellKnownSymbol = require('../internals/well-known-symbol');
var ITERATOR = wellKnownSymbol('iterator');
module.exports = function(it) {
    if (it != undefined) return getMethod(it, ITERATOR) || getMethod(it, '@@iterator') || Iterators[classof(it)];
};

},{"../internals/classof":"5J26j","../internals/get-method":"goREb","../internals/iterators":"jgzzK","../internals/well-known-symbol":"cSGfj"}],"1HHW3":[function(require,module,exports) {
var _sliceInstanceProperty = require("@babel/runtime-corejs3/core-js/instance/slice");
var _Array$from = require("@babel/runtime-corejs3/core-js/array/from");
var arrayLikeToArray = require("./arrayLikeToArray.js");
function _unsupportedIterableToArray(o, minLen) {
    var _context;
    if (!o) return;
    if (typeof o === "string") return arrayLikeToArray(o, minLen);
    var n = _sliceInstanceProperty(_context = Object.prototype.toString.call(o)).call(_context, 8, -1);
    if (n === "Object" && o.constructor) n = o.constructor.name;
    if (n === "Map" || n === "Set") return _Array$from(o);
    if (n === "Arguments" || /^(?:Ui|I)nt(?:8|16|32)(?:Clamped)?Array$/.test(n)) return arrayLikeToArray(o, minLen);
}
module.exports = _unsupportedIterableToArray, module.exports.__esModule = true, module.exports["default"] = module.exports;

},{"@babel/runtime-corejs3/core-js/instance/slice":"fG2dr","@babel/runtime-corejs3/core-js/array/from":"71yCE","./arrayLikeToArray.js":"8tv1x"}],"fG2dr":[function(require,module,exports) {
module.exports = require("core-js-pure/features/instance/slice");

},{"core-js-pure/features/instance/slice":"lhol3"}],"lhol3":[function(require,module,exports) {
module.exports = require('../../full/instance/slice');

},{"../../full/instance/slice":"ibtxu"}],"ibtxu":[function(require,module,exports) {
var parent = require('../../actual/instance/slice');
module.exports = parent;

},{"../../actual/instance/slice":"llNlP"}],"llNlP":[function(require,module,exports) {
var parent = require('../../stable/instance/slice');
module.exports = parent;

},{"../../stable/instance/slice":"7vNvC"}],"7vNvC":[function(require,module,exports) {
var parent = require('../../es/instance/slice');
module.exports = parent;

},{"../../es/instance/slice":"a03fI"}],"a03fI":[function(require,module,exports) {
var isPrototypeOf = require('../../internals/object-is-prototype-of');
var method = require('../array/virtual/slice');
var ArrayPrototype = Array.prototype;
module.exports = function(it) {
    var own = it.slice;
    return it === ArrayPrototype || isPrototypeOf(ArrayPrototype, it) && own === ArrayPrototype.slice ? method : own;
};

},{"../../internals/object-is-prototype-of":"qlM4L","../array/virtual/slice":"auZbe"}],"auZbe":[function(require,module,exports) {
require('../../../modules/es.array.slice');
var entryVirtual = require('../../../internals/entry-virtual');
module.exports = entryVirtual('Array').slice;

},{"../../../modules/es.array.slice":"lM7kR","../../../internals/entry-virtual":"5RMpR"}],"lM7kR":[function(require,module,exports) {
'use strict';
var $ = require('../internals/export');
var global = require('../internals/global');
var isArray = require('../internals/is-array');
var isConstructor = require('../internals/is-constructor');
var isObject = require('../internals/is-object');
var toAbsoluteIndex = require('../internals/to-absolute-index');
var lengthOfArrayLike = require('../internals/length-of-array-like');
var toIndexedObject = require('../internals/to-indexed-object');
var createProperty = require('../internals/create-property');
var wellKnownSymbol = require('../internals/well-known-symbol');
var arrayMethodHasSpeciesSupport = require('../internals/array-method-has-species-support');
var un$Slice = require('../internals/array-slice');
var HAS_SPECIES_SUPPORT = arrayMethodHasSpeciesSupport('slice');
var SPECIES = wellKnownSymbol('species');
var Array = global.Array;
var max = Math.max;
// `Array.prototype.slice` method
// https://tc39.es/ecma262/#sec-array.prototype.slice
// fallback for not array-like ES3 strings and DOM objects
$({
    target: 'Array',
    proto: true,
    forced: !HAS_SPECIES_SUPPORT
}, {
    slice: function slice(start, end) {
        var O = toIndexedObject(this);
        var length = lengthOfArrayLike(O);
        var k = toAbsoluteIndex(start, length);
        var fin = toAbsoluteIndex(end === undefined ? length : end, length);
        // inline `ArraySpeciesCreate` for usage native `Array#slice` where it's possible
        var Constructor, result, n;
        if (isArray(O)) {
            Constructor = O.constructor;
            // cross-realm fallback
            if (isConstructor(Constructor) && (Constructor === Array || isArray(Constructor.prototype))) Constructor = undefined;
            else if (isObject(Constructor)) {
                Constructor = Constructor[SPECIES];
                if (Constructor === null) Constructor = undefined;
            }
            if (Constructor === Array || Constructor === undefined) return un$Slice(O, k, fin);
        }
        result = new (Constructor === undefined ? Array : Constructor)(max(fin - k, 0));
        for(n = 0; k < fin; k++, n++)if (k in O) createProperty(result, n, O[k]);
        result.length = n;
        return result;
    }
});

},{"../internals/export":"jjems","../internals/global":"avqB7","../internals/is-array":"byYNs","../internals/is-constructor":"15Sn8","../internals/is-object":"fRYFX","../internals/to-absolute-index":"1L8Z0","../internals/length-of-array-like":"icIfm","../internals/to-indexed-object":"kvdfV","../internals/create-property":"9Ttit","../internals/well-known-symbol":"cSGfj","../internals/array-method-has-species-support":"4n6gw","../internals/array-slice":"gooNL"}],"5RMpR":[function(require,module,exports) {
var path = require('../internals/path');
module.exports = function(CONSTRUCTOR) {
    return path[CONSTRUCTOR + 'Prototype'];
};

},{"../internals/path":"8QO0z"}],"71yCE":[function(require,module,exports) {
module.exports = require("core-js-pure/features/array/from");

},{"core-js-pure/features/array/from":"ettkI"}],"ettkI":[function(require,module,exports) {
module.exports = require('../../full/array/from');

},{"../../full/array/from":"1JCEZ"}],"1JCEZ":[function(require,module,exports) {
var parent = require('../../actual/array/from');
module.exports = parent;

},{"../../actual/array/from":"2yhgO"}],"2yhgO":[function(require,module,exports) {
var parent = require('../../stable/array/from');
module.exports = parent;

},{"../../stable/array/from":"3oqqF"}],"3oqqF":[function(require,module,exports) {
var parent = require('../../es/array/from');
module.exports = parent;

},{"../../es/array/from":"9vXSD"}],"9vXSD":[function(require,module,exports) {
require('../../modules/es.string.iterator');
require('../../modules/es.array.from');
var path = require('../../internals/path');
module.exports = path.Array.from;

},{"../../modules/es.string.iterator":"jHlRH","../../modules/es.array.from":"2vRb5","../../internals/path":"8QO0z"}],"2vRb5":[function(require,module,exports) {
var $ = require('../internals/export');
var from = require('../internals/array-from');
var checkCorrectnessOfIteration = require('../internals/check-correctness-of-iteration');
var INCORRECT_ITERATION = !checkCorrectnessOfIteration(function(iterable) {
    // eslint-disable-next-line es-x/no-array-from -- required for testing
    Array.from(iterable);
});
// `Array.from` method
// https://tc39.es/ecma262/#sec-array.from
$({
    target: 'Array',
    stat: true,
    forced: INCORRECT_ITERATION
}, {
    from: from
});

},{"../internals/export":"jjems","../internals/array-from":"8Ag9d","../internals/check-correctness-of-iteration":"dlTKW"}],"8Ag9d":[function(require,module,exports) {
'use strict';
var global = require('../internals/global');
var bind = require('../internals/function-bind-context');
var call = require('../internals/function-call');
var toObject = require('../internals/to-object');
var callWithSafeIterationClosing = require('../internals/call-with-safe-iteration-closing');
var isArrayIteratorMethod = require('../internals/is-array-iterator-method');
var isConstructor = require('../internals/is-constructor');
var lengthOfArrayLike = require('../internals/length-of-array-like');
var createProperty = require('../internals/create-property');
var getIterator = require('../internals/get-iterator');
var getIteratorMethod = require('../internals/get-iterator-method');
var Array = global.Array;
// `Array.from` method implementation
// https://tc39.es/ecma262/#sec-array.from
module.exports = function from(arrayLike /* , mapfn = undefined, thisArg = undefined */ ) {
    var O = toObject(arrayLike);
    var IS_CONSTRUCTOR = isConstructor(this);
    var argumentsLength = arguments.length;
    var mapfn = argumentsLength > 1 ? arguments[1] : undefined;
    var mapping = mapfn !== undefined;
    if (mapping) mapfn = bind(mapfn, argumentsLength > 2 ? arguments[2] : undefined);
    var iteratorMethod = getIteratorMethod(O);
    var index = 0;
    var length, result, step, iterator, next, value;
    // if the target is not iterable or it's an array with the default iterator - use a simple case
    if (iteratorMethod && !(this == Array && isArrayIteratorMethod(iteratorMethod))) {
        iterator = getIterator(O, iteratorMethod);
        next = iterator.next;
        result = IS_CONSTRUCTOR ? new this() : [];
        for(; !(step = call(next, iterator)).done; index++){
            value = mapping ? callWithSafeIterationClosing(iterator, mapfn, [
                step.value,
                index
            ], true) : step.value;
            createProperty(result, index, value);
        }
    } else {
        length = lengthOfArrayLike(O);
        result = IS_CONSTRUCTOR ? new this(length) : Array(length);
        for(; length > index; index++){
            value = mapping ? mapfn(O[index], index) : O[index];
            createProperty(result, index, value);
        }
    }
    result.length = index;
    return result;
};

},{"../internals/global":"avqB7","../internals/function-bind-context":"jZFzY","../internals/function-call":"acNwo","../internals/to-object":"70Utf","../internals/call-with-safe-iteration-closing":"fqRGo","../internals/is-array-iterator-method":"kAmRO","../internals/is-constructor":"15Sn8","../internals/length-of-array-like":"icIfm","../internals/create-property":"9Ttit","../internals/get-iterator":"bqGJA","../internals/get-iterator-method":"hgHU0"}],"fqRGo":[function(require,module,exports) {
var anObject = require('../internals/an-object');
var iteratorClose = require('../internals/iterator-close');
// call something on iterator step with safe closing on error
module.exports = function(iterator, fn, value, ENTRIES) {
    try {
        return ENTRIES ? fn(anObject(value)[0], value[1]) : fn(value);
    } catch (error) {
        iteratorClose(iterator, 'throw', error);
    }
};

},{"../internals/an-object":"bTNyr","../internals/iterator-close":"dYFQp"}],"dYFQp":[function(require,module,exports) {
var call = require('../internals/function-call');
var anObject = require('../internals/an-object');
var getMethod = require('../internals/get-method');
module.exports = function(iterator, kind, value) {
    var innerResult, innerError;
    anObject(iterator);
    try {
        innerResult = getMethod(iterator, 'return');
        if (!innerResult) {
            if (kind === 'throw') throw value;
            return value;
        }
        innerResult = call(innerResult, iterator);
    } catch (error) {
        innerError = true;
        innerResult = error;
    }
    if (kind === 'throw') throw value;
    if (innerError) throw innerResult;
    anObject(innerResult);
    return value;
};

},{"../internals/function-call":"acNwo","../internals/an-object":"bTNyr","../internals/get-method":"goREb"}],"kAmRO":[function(require,module,exports) {
var wellKnownSymbol = require('../internals/well-known-symbol');
var Iterators = require('../internals/iterators');
var ITERATOR = wellKnownSymbol('iterator');
var ArrayPrototype = Array.prototype;
// check on default Array iterator
module.exports = function(it) {
    return it !== undefined && (Iterators.Array === it || ArrayPrototype[ITERATOR] === it);
};

},{"../internals/well-known-symbol":"cSGfj","../internals/iterators":"jgzzK"}],"bqGJA":[function(require,module,exports) {
var global = require('../internals/global');
var call = require('../internals/function-call');
var aCallable = require('../internals/a-callable');
var anObject = require('../internals/an-object');
var tryToString = require('../internals/try-to-string');
var getIteratorMethod = require('../internals/get-iterator-method');
var TypeError = global.TypeError;
module.exports = function(argument, usingIterator) {
    var iteratorMethod = arguments.length < 2 ? getIteratorMethod(argument) : usingIterator;
    if (aCallable(iteratorMethod)) return anObject(call(iteratorMethod, argument));
    throw TypeError(tryToString(argument) + ' is not iterable');
};

},{"../internals/global":"avqB7","../internals/function-call":"acNwo","../internals/a-callable":"7r1lP","../internals/an-object":"bTNyr","../internals/try-to-string":"c7C3w","../internals/get-iterator-method":"hgHU0"}],"dlTKW":[function(require,module,exports) {
var wellKnownSymbol = require('../internals/well-known-symbol');
var ITERATOR = wellKnownSymbol('iterator');
var SAFE_CLOSING = false;
try {
    var called = 0;
    var iteratorWithReturn = {
        next: function() {
            return {
                done: !!called++
            };
        },
        'return': function() {
            SAFE_CLOSING = true;
        }
    };
    iteratorWithReturn[ITERATOR] = function() {
        return this;
    };
    // eslint-disable-next-line es-x/no-array-from, no-throw-literal -- required for testing
    Array.from(iteratorWithReturn, function() {
        throw 2;
    });
} catch (error) {}
module.exports = function(exec, SKIP_CLOSING) {
    if (!SKIP_CLOSING && !SAFE_CLOSING) return false;
    var ITERATION_SUPPORT = false;
    try {
        var object = {};
        object[ITERATOR] = function() {
            return {
                next: function() {
                    return {
                        done: ITERATION_SUPPORT = true
                    };
                }
            };
        };
        exec(object);
    } catch (error) {}
    return ITERATION_SUPPORT;
};

},{"../internals/well-known-symbol":"cSGfj"}],"8tv1x":[function(require,module,exports) {
function _arrayLikeToArray(arr, len) {
    if (len == null || len > arr.length) len = arr.length;
    for(var i = 0, arr2 = new Array(len); i < len; i++)arr2[i] = arr[i];
    return arr2;
}
module.exports = _arrayLikeToArray, module.exports.__esModule = true, module.exports["default"] = module.exports;

},{}],"dmp8i":[function(require,module,exports) {
function _nonIterableRest() {
    throw new TypeError("Invalid attempt to destructure non-iterable instance.\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method.");
}
module.exports = _nonIterableRest, module.exports.__esModule = true, module.exports["default"] = module.exports;

},{}],"jocGM":[function(require,module,exports) {
/*!
  Copyright (c) 2018 Jed Watson.
  Licensed under the MIT License (MIT), see
  http://jedwatson.github.io/classnames
*/ /* global define */ (function() {
    var hasOwn = {}.hasOwnProperty;
    function classNames() {
        var classes = [];
        for(var i = 0; i < arguments.length; i++){
            var arg = arguments[i];
            if (!arg) continue;
            var argType = typeof arg;
            if (argType === 'string' || argType === 'number') classes.push(arg);
            else if (Array.isArray(arg)) {
                if (arg.length) {
                    var inner = classNames.apply(null, arg);
                    if (inner) classes.push(inner);
                }
            } else if (argType === 'object') {
                if (arg.toString === Object.prototype.toString) {
                    for(var key in arg)if (hasOwn.call(arg, key) && arg[key]) classes.push(key);
                } else classes.push(arg.toString());
            }
        }
        return classes.join(' ');
    }
    if (module.exports) {
        classNames.default = classNames;
        module.exports = classNames;
    } else if (typeof define === 'function' && typeof define.amd === 'object' && define.amd) // register as 'classnames', consistent with npm package name
    define('classnames', [], function() {
        return classNames;
    });
    else window.classNames = classNames;
})();

},{}],"2ipKq":[function(require,module,exports) {
var ReactIs = require('react-is');
// By explicitly using `prop-types` you are opting into new development behavior.
// http://fb.me/prop-types-in-prod
var throwOnDirectAccess = true;
module.exports = require('./factoryWithTypeCheckers')(ReactIs.isElement, throwOnDirectAccess);

},{"react-is":"7EuwB","./factoryWithTypeCheckers":"eNr9W"}],"eNr9W":[function(require,module,exports) {
/**
 * Copyright (c) 2013-present, Facebook, Inc.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE file in the root directory of this source tree.
 */ 'use strict';
var ReactIs = require('react-is');
var assign = require('object-assign');
var ReactPropTypesSecret = require('./lib/ReactPropTypesSecret');
var checkPropTypes = require('./checkPropTypes');
var has = Function.call.bind(Object.prototype.hasOwnProperty);
var printWarning = function() {};
printWarning = function(text) {
    var message = 'Warning: ' + text;
    if (typeof console !== 'undefined') console.error(message);
    try {
        // --- Welcome to debugging React ---
        // This error was thrown as a convenience so that you can use this stack
        // to find the callsite that caused this warning to fire.
        throw new Error(message);
    } catch (x) {}
};
function emptyFunctionThatReturnsNull() {
    return null;
}
module.exports = function(isValidElement, throwOnDirectAccess) {
    /* global Symbol */ var ITERATOR_SYMBOL = typeof Symbol === 'function' && Symbol.iterator;
    var FAUX_ITERATOR_SYMBOL = '@@iterator'; // Before Symbol spec.
    /**
   * Returns the iterator method function contained on the iterable object.
   *
   * Be sure to invoke the function with the iterable as context:
   *
   *     var iteratorFn = getIteratorFn(myIterable);
   *     if (iteratorFn) {
   *       var iterator = iteratorFn.call(myIterable);
   *       ...
   *     }
   *
   * @param {?object} maybeIterable
   * @return {?function}
   */ function getIteratorFn(maybeIterable) {
        var iteratorFn = maybeIterable && (ITERATOR_SYMBOL && maybeIterable[ITERATOR_SYMBOL] || maybeIterable[FAUX_ITERATOR_SYMBOL]);
        if (typeof iteratorFn === 'function') return iteratorFn;
    }
    /**
   * Collection of methods that allow declaration and validation of props that are
   * supplied to React components. Example usage:
   *
   *   var Props = require('ReactPropTypes');
   *   var MyArticle = React.createClass({
   *     propTypes: {
   *       // An optional string prop named "description".
   *       description: Props.string,
   *
   *       // A required enum prop named "category".
   *       category: Props.oneOf(['News','Photos']).isRequired,
   *
   *       // A prop named "dialog" that requires an instance of Dialog.
   *       dialog: Props.instanceOf(Dialog).isRequired
   *     },
   *     render: function() { ... }
   *   });
   *
   * A more formal specification of how these methods are used:
   *
   *   type := array|bool|func|object|number|string|oneOf([...])|instanceOf(...)
   *   decl := ReactPropTypes.{type}(.isRequired)?
   *
   * Each and every declaration produces a function with the same signature. This
   * allows the creation of custom validation functions. For example:
   *
   *  var MyLink = React.createClass({
   *    propTypes: {
   *      // An optional string or URI prop named "href".
   *      href: function(props, propName, componentName) {
   *        var propValue = props[propName];
   *        if (propValue != null && typeof propValue !== 'string' &&
   *            !(propValue instanceof URI)) {
   *          return new Error(
   *            'Expected a string or an URI for ' + propName + ' in ' +
   *            componentName
   *          );
   *        }
   *      }
   *    },
   *    render: function() {...}
   *  });
   *
   * @internal
   */ var ANONYMOUS = '<<anonymous>>';
    // Important!
    // Keep this list in sync with production version in `./factoryWithThrowingShims.js`.
    var ReactPropTypes = {
        array: createPrimitiveTypeChecker('array'),
        bool: createPrimitiveTypeChecker('boolean'),
        func: createPrimitiveTypeChecker('function'),
        number: createPrimitiveTypeChecker('number'),
        object: createPrimitiveTypeChecker('object'),
        string: createPrimitiveTypeChecker('string'),
        symbol: createPrimitiveTypeChecker('symbol'),
        any: createAnyTypeChecker(),
        arrayOf: createArrayOfTypeChecker,
        element: createElementTypeChecker(),
        elementType: createElementTypeTypeChecker(),
        instanceOf: createInstanceTypeChecker,
        node: createNodeChecker(),
        objectOf: createObjectOfTypeChecker,
        oneOf: createEnumTypeChecker,
        oneOfType: createUnionTypeChecker,
        shape: createShapeTypeChecker,
        exact: createStrictShapeTypeChecker
    };
    /**
   * inlined Object.is polyfill to avoid requiring consumers ship their own
   * https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Object/is
   */ /*eslint-disable no-self-compare*/ function is(x, y) {
        // SameValue algorithm
        if (x === y) // Steps 1-5, 7-10
        // Steps 6.b-6.e: +0 != -0
        return x !== 0 || 1 / x === 1 / y;
        else // Step 6.a: NaN == NaN
        return x !== x && y !== y;
    }
    /*eslint-enable no-self-compare*/ /**
   * We use an Error-like object for backward compatibility as people may call
   * PropTypes directly and inspect their output. However, we don't use real
   * Errors anymore. We don't inspect their stack anyway, and creating them
   * is prohibitively expensive if they are created too often, such as what
   * happens in oneOfType() for any type before the one that matched.
   */ function PropTypeError(message) {
        this.message = message;
        this.stack = '';
    }
    // Make `instanceof Error` still work for returned errors.
    PropTypeError.prototype = Error.prototype;
    function createChainableTypeChecker(validate) {
        var manualPropTypeCallCache = {};
        var manualPropTypeWarningCount = 0;
        function checkType(isRequired, props, propName, componentName, location, propFullName, secret) {
            componentName = componentName || ANONYMOUS;
            propFullName = propFullName || propName;
            if (secret !== ReactPropTypesSecret) {
                if (throwOnDirectAccess) {
                    // New behavior only for users of `prop-types` package
                    var err = new Error("Calling PropTypes validators directly is not supported by the `prop-types` package. Use `PropTypes.checkPropTypes()` to call them. Read more at http://fb.me/use-check-prop-types");
                    err.name = 'Invariant Violation';
                    throw err;
                } else if (typeof console !== 'undefined') {
                    // Old behavior for people using React.PropTypes
                    var cacheKey = componentName + ':' + propName;
                    if (!manualPropTypeCallCache[cacheKey] && // Avoid spamming the console because they are often not actionable except for lib authors
                    manualPropTypeWarningCount < 3) {
                        printWarning("You are manually calling a React.PropTypes validation function for the `" + propFullName + '` prop on `' + componentName + '`. This is deprecated ' + 'and will throw in the standalone `prop-types` package. ' + 'You may be seeing this warning due to a third-party PropTypes ' + 'library. See https://fb.me/react-warning-dont-call-proptypes ' + 'for details.');
                        manualPropTypeCallCache[cacheKey] = true;
                        manualPropTypeWarningCount++;
                    }
                }
            }
            if (props[propName] == null) {
                if (isRequired) {
                    if (props[propName] === null) return new PropTypeError('The ' + location + ' `' + propFullName + '` is marked as required ' + ('in `' + componentName + '`, but its value is `null`.'));
                    return new PropTypeError('The ' + location + ' `' + propFullName + '` is marked as required in ' + ('`' + componentName + '`, but its value is `undefined`.'));
                }
                return null;
            } else return validate(props, propName, componentName, location, propFullName);
        }
        var chainedCheckType = checkType.bind(null, false);
        chainedCheckType.isRequired = checkType.bind(null, true);
        return chainedCheckType;
    }
    function createPrimitiveTypeChecker(expectedType) {
        function validate(props, propName, componentName, location, propFullName, secret) {
            var propValue = props[propName];
            var propType = getPropType(propValue);
            if (propType !== expectedType) {
                // `propValue` being instance of, say, date/regexp, pass the 'object'
                // check, but we can offer a more precise error message here rather than
                // 'of type `object`'.
                var preciseType = getPreciseType(propValue);
                return new PropTypeError('Invalid ' + location + ' `' + propFullName + '` of type ' + ('`' + preciseType + '` supplied to `' + componentName + '`, expected ') + ('`' + expectedType + '`.'));
            }
            return null;
        }
        return createChainableTypeChecker(validate);
    }
    function createAnyTypeChecker() {
        return createChainableTypeChecker(emptyFunctionThatReturnsNull);
    }
    function createArrayOfTypeChecker(typeChecker) {
        function validate(props, propName, componentName, location, propFullName) {
            if (typeof typeChecker !== 'function') return new PropTypeError('Property `' + propFullName + '` of component `' + componentName + '` has invalid PropType notation inside arrayOf.');
            var propValue = props[propName];
            if (!Array.isArray(propValue)) {
                var propType = getPropType(propValue);
                return new PropTypeError('Invalid ' + location + ' `' + propFullName + '` of type ' + ('`' + propType + '` supplied to `' + componentName + '`, expected an array.'));
            }
            for(var i = 0; i < propValue.length; i++){
                var error = typeChecker(propValue, i, componentName, location, propFullName + '[' + i + ']', ReactPropTypesSecret);
                if (error instanceof Error) return error;
            }
            return null;
        }
        return createChainableTypeChecker(validate);
    }
    function createElementTypeChecker() {
        function validate(props, propName, componentName, location, propFullName) {
            var propValue = props[propName];
            if (!isValidElement(propValue)) {
                var propType = getPropType(propValue);
                return new PropTypeError('Invalid ' + location + ' `' + propFullName + '` of type ' + ('`' + propType + '` supplied to `' + componentName + '`, expected a single ReactElement.'));
            }
            return null;
        }
        return createChainableTypeChecker(validate);
    }
    function createElementTypeTypeChecker() {
        function validate(props, propName, componentName, location, propFullName) {
            var propValue = props[propName];
            if (!ReactIs.isValidElementType(propValue)) {
                var propType = getPropType(propValue);
                return new PropTypeError('Invalid ' + location + ' `' + propFullName + '` of type ' + ('`' + propType + '` supplied to `' + componentName + '`, expected a single ReactElement type.'));
            }
            return null;
        }
        return createChainableTypeChecker(validate);
    }
    function createInstanceTypeChecker(expectedClass) {
        function validate(props, propName, componentName, location, propFullName) {
            if (!(props[propName] instanceof expectedClass)) {
                var expectedClassName = expectedClass.name || ANONYMOUS;
                var actualClassName = getClassName(props[propName]);
                return new PropTypeError('Invalid ' + location + ' `' + propFullName + '` of type ' + ('`' + actualClassName + '` supplied to `' + componentName + '`, expected ') + ('instance of `' + expectedClassName + '`.'));
            }
            return null;
        }
        return createChainableTypeChecker(validate);
    }
    function createEnumTypeChecker(expectedValues) {
        if (!Array.isArray(expectedValues)) {
            {
                if (arguments.length > 1) printWarning('Invalid arguments supplied to oneOf, expected an array, got ' + arguments.length + ' arguments. ' + 'A common mistake is to write oneOf(x, y, z) instead of oneOf([x, y, z]).');
                else printWarning('Invalid argument supplied to oneOf, expected an array.');
            }
            return emptyFunctionThatReturnsNull;
        }
        function validate(props, propName, componentName, location, propFullName) {
            var propValue = props[propName];
            for(var i = 0; i < expectedValues.length; i++){
                if (is(propValue, expectedValues[i])) return null;
            }
            var valuesString = JSON.stringify(expectedValues, function replacer(key, value) {
                var type = getPreciseType(value);
                if (type === 'symbol') return String(value);
                return value;
            });
            return new PropTypeError('Invalid ' + location + ' `' + propFullName + '` of value `' + String(propValue) + '` ' + ('supplied to `' + componentName + '`, expected one of ' + valuesString + '.'));
        }
        return createChainableTypeChecker(validate);
    }
    function createObjectOfTypeChecker(typeChecker) {
        function validate(props, propName, componentName, location, propFullName) {
            if (typeof typeChecker !== 'function') return new PropTypeError('Property `' + propFullName + '` of component `' + componentName + '` has invalid PropType notation inside objectOf.');
            var propValue = props[propName];
            var propType = getPropType(propValue);
            if (propType !== 'object') return new PropTypeError('Invalid ' + location + ' `' + propFullName + '` of type ' + ('`' + propType + '` supplied to `' + componentName + '`, expected an object.'));
            for(var key in propValue)if (has(propValue, key)) {
                var error = typeChecker(propValue, key, componentName, location, propFullName + '.' + key, ReactPropTypesSecret);
                if (error instanceof Error) return error;
            }
            return null;
        }
        return createChainableTypeChecker(validate);
    }
    function createUnionTypeChecker(arrayOfTypeCheckers) {
        if (!Array.isArray(arrayOfTypeCheckers)) {
            printWarning('Invalid argument supplied to oneOfType, expected an instance of array.');
            return emptyFunctionThatReturnsNull;
        }
        for(var i1 = 0; i1 < arrayOfTypeCheckers.length; i1++){
            var checker = arrayOfTypeCheckers[i1];
            if (typeof checker !== 'function') {
                printWarning("Invalid argument supplied to oneOfType. Expected an array of check functions, but received " + getPostfixForTypeWarning(checker) + ' at index ' + i1 + '.');
                return emptyFunctionThatReturnsNull;
            }
        }
        function validate(props, propName, componentName, location, propFullName) {
            for(var i = 0; i < arrayOfTypeCheckers.length; i++){
                var checker = arrayOfTypeCheckers[i];
                if (checker(props, propName, componentName, location, propFullName, ReactPropTypesSecret) == null) return null;
            }
            return new PropTypeError('Invalid ' + location + ' `' + propFullName + '` supplied to ' + ('`' + componentName + '`.'));
        }
        return createChainableTypeChecker(validate);
    }
    function createNodeChecker() {
        function validate(props, propName, componentName, location, propFullName) {
            if (!isNode(props[propName])) return new PropTypeError('Invalid ' + location + ' `' + propFullName + '` supplied to ' + ('`' + componentName + '`, expected a ReactNode.'));
            return null;
        }
        return createChainableTypeChecker(validate);
    }
    function createShapeTypeChecker(shapeTypes) {
        function validate(props, propName, componentName, location, propFullName) {
            var propValue = props[propName];
            var propType = getPropType(propValue);
            if (propType !== 'object') return new PropTypeError('Invalid ' + location + ' `' + propFullName + '` of type `' + propType + '` ' + ('supplied to `' + componentName + '`, expected `object`.'));
            for(var key in shapeTypes){
                var checker = shapeTypes[key];
                if (!checker) continue;
                var error = checker(propValue, key, componentName, location, propFullName + '.' + key, ReactPropTypesSecret);
                if (error) return error;
            }
            return null;
        }
        return createChainableTypeChecker(validate);
    }
    function createStrictShapeTypeChecker(shapeTypes) {
        function validate(props, propName, componentName, location, propFullName) {
            var propValue = props[propName];
            var propType = getPropType(propValue);
            if (propType !== 'object') return new PropTypeError('Invalid ' + location + ' `' + propFullName + '` of type `' + propType + '` ' + ('supplied to `' + componentName + '`, expected `object`.'));
            // We need to check all keys in case some are required but missing from
            // props.
            var allKeys = assign({}, props[propName], shapeTypes);
            for(var key in allKeys){
                var checker = shapeTypes[key];
                if (!checker) return new PropTypeError('Invalid ' + location + ' `' + propFullName + '` key `' + key + '` supplied to `' + componentName + '`.' + '\nBad object: ' + JSON.stringify(props[propName], null, '  ') + '\nValid keys: ' + JSON.stringify(Object.keys(shapeTypes), null, '  '));
                var error = checker(propValue, key, componentName, location, propFullName + '.' + key, ReactPropTypesSecret);
                if (error) return error;
            }
            return null;
        }
        return createChainableTypeChecker(validate);
    }
    function isNode(propValue) {
        switch(typeof propValue){
            case 'number':
            case 'string':
            case 'undefined':
                return true;
            case 'boolean':
                return !propValue;
            case 'object':
                if (Array.isArray(propValue)) return propValue.every(isNode);
                if (propValue === null || isValidElement(propValue)) return true;
                var iteratorFn = getIteratorFn(propValue);
                if (iteratorFn) {
                    var iterator = iteratorFn.call(propValue);
                    var step;
                    if (iteratorFn !== propValue.entries) while(!(step = iterator.next()).done){
                        if (!isNode(step.value)) return false;
                    }
                    else // Iterator will provide entry [k,v] tuples rather than values.
                    while(!(step = iterator.next()).done){
                        var entry = step.value;
                        if (entry) {
                            if (!isNode(entry[1])) return false;
                        }
                    }
                } else return false;
                return true;
            default:
                return false;
        }
    }
    function isSymbol(propType, propValue) {
        // Native Symbol.
        if (propType === 'symbol') return true;
        // falsy value can't be a Symbol
        if (!propValue) return false;
        // 19.4.3.5 Symbol.prototype[@@toStringTag] === 'Symbol'
        if (propValue['@@toStringTag'] === 'Symbol') return true;
        // Fallback for non-spec compliant Symbols which are polyfilled.
        if (typeof Symbol === 'function' && propValue instanceof Symbol) return true;
        return false;
    }
    // Equivalent of `typeof` but with special handling for array and regexp.
    function getPropType(propValue) {
        var propType = typeof propValue;
        if (Array.isArray(propValue)) return 'array';
        if (propValue instanceof RegExp) // Old webkits (at least until Android 4.0) return 'function' rather than
        // 'object' for typeof a RegExp. We'll normalize this here so that /bla/
        // passes PropTypes.object.
        return 'object';
        if (isSymbol(propType, propValue)) return 'symbol';
        return propType;
    }
    // This handles more types than `getPropType`. Only used for error messages.
    // See `createPrimitiveTypeChecker`.
    function getPreciseType(propValue) {
        if (typeof propValue === 'undefined' || propValue === null) return '' + propValue;
        var propType = getPropType(propValue);
        if (propType === 'object') {
            if (propValue instanceof Date) return 'date';
            else if (propValue instanceof RegExp) return 'regexp';
        }
        return propType;
    }
    // Returns a string that is postfixed to a warning about an invalid type.
    // For example, "undefined" or "of type array"
    function getPostfixForTypeWarning(value) {
        var type = getPreciseType(value);
        switch(type){
            case 'array':
            case 'object':
                return 'an ' + type;
            case 'boolean':
            case 'date':
            case 'regexp':
                return 'a ' + type;
            default:
                return type;
        }
    }
    // Returns class name of the object, if any.
    function getClassName(propValue) {
        if (!propValue.constructor || !propValue.constructor.name) return ANONYMOUS;
        return propValue.constructor.name;
    }
    ReactPropTypes.checkPropTypes = checkPropTypes;
    ReactPropTypes.resetWarningCache = checkPropTypes.resetWarningCache;
    ReactPropTypes.PropTypes = ReactPropTypes;
    return ReactPropTypes;
};

},{"react-is":"7EuwB","object-assign":"7OXxh","./lib/ReactPropTypesSecret":"lDtxk","./checkPropTypes":"9x3eD"}],"lDtxk":[function(require,module,exports) {
/**
 * Copyright (c) 2013-present, Facebook, Inc.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE file in the root directory of this source tree.
 */ 'use strict';
var ReactPropTypesSecret = 'SECRET_DO_NOT_PASS_THIS_OR_YOU_WILL_BE_FIRED';
module.exports = ReactPropTypesSecret;

},{}],"9x3eD":[function(require,module,exports) {
/**
 * Copyright (c) 2013-present, Facebook, Inc.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE file in the root directory of this source tree.
 */ 'use strict';
var printWarning = function() {};
var ReactPropTypesSecret = require('./lib/ReactPropTypesSecret');
var loggedTypeFailures = {};
var has = Function.call.bind(Object.prototype.hasOwnProperty);
printWarning = function(text) {
    var message = 'Warning: ' + text;
    if (typeof console !== 'undefined') console.error(message);
    try {
        // --- Welcome to debugging React ---
        // This error was thrown as a convenience so that you can use this stack
        // to find the callsite that caused this warning to fire.
        throw new Error(message);
    } catch (x) {}
};
/**
 * Assert that the values match with the type specs.
 * Error messages are memorized and will only be shown once.
 *
 * @param {object} typeSpecs Map of name to a ReactPropType
 * @param {object} values Runtime values that need to be type-checked
 * @param {string} location e.g. "prop", "context", "child context"
 * @param {string} componentName Name of the component for error messages.
 * @param {?Function} getStack Returns the component stack.
 * @private
 */ function checkPropTypes(typeSpecs, values, location, componentName, getStack) {
    for(var typeSpecName in typeSpecs)if (has(typeSpecs, typeSpecName)) {
        var error;
        // Prop type validation may throw. In case they do, we don't want to
        // fail the render phase where it didn't fail before. So we log it.
        // After these have been cleaned up, we'll let them throw.
        try {
            // This is intentionally an invariant that gets caught. It's the same
            // behavior as without this statement except with a better message.
            if (typeof typeSpecs[typeSpecName] !== 'function') {
                var err = Error((componentName || 'React class') + ': ' + location + ' type `' + typeSpecName + '` is invalid; ' + 'it must be a function, usually from the `prop-types` package, but received `' + typeof typeSpecs[typeSpecName] + '`.');
                err.name = 'Invariant Violation';
                throw err;
            }
            error = typeSpecs[typeSpecName](values, typeSpecName, componentName, location, null, ReactPropTypesSecret);
        } catch (ex) {
            error = ex;
        }
        if (error && !(error instanceof Error)) printWarning((componentName || 'React class') + ': type specification of ' + location + ' `' + typeSpecName + '` is invalid; the type checker ' + 'function must return `null` or an `Error` but returned a ' + typeof error + '. ' + 'You may have forgotten to pass an argument to the type checker ' + 'creator (arrayOf, instanceOf, objectOf, oneOf, oneOfType, and ' + 'shape all require an argument).');
        if (error instanceof Error && !(error.message in loggedTypeFailures)) {
            // Only monitor this failure once because there tends to be a lot of the
            // same error.
            loggedTypeFailures[error.message] = true;
            var stack = getStack ? getStack() : '';
            printWarning('Failed ' + location + ' type: ' + error.message + (stack != null ? stack : ''));
        }
    }
}
/**
 * Resets warning cache when testing.
 *
 * @private
 */ checkPropTypes.resetWarningCache = function() {
    loggedTypeFailures = {};
};
module.exports = checkPropTypes;

},{"./lib/ReactPropTypesSecret":"lDtxk"}],"6A30V":[function(require,module,exports) {
var parcelHelpers = require("@parcel/transformer-js/src/esmodule-helpers.js");
parcelHelpers.defineInteropFlag(exports);
var _useFunctionContext = require("./internal/useFunctionContext");
var _useFunctionContextDefault = parcelHelpers.interopDefault(_useFunctionContext);
function useScrollToEnd() {
    var _useFunctionContext1 = _useFunctionContextDefault.default(), scrollToEnd = _useFunctionContext1.scrollToEnd;
    return scrollToEnd;
}
exports.default = useScrollToEnd;

},{"./internal/useFunctionContext":"fJDZI","@parcel/transformer-js/src/esmodule-helpers.js":"gkKU3"}],"fJDZI":[function(require,module,exports) {
var parcelHelpers = require("@parcel/transformer-js/src/esmodule-helpers.js");
parcelHelpers.defineInteropFlag(exports);
var _react = require("react");
var _functionContext = require("../../ScrollToBottom/FunctionContext");
var _functionContextDefault = parcelHelpers.interopDefault(_functionContext);
function useFunctionContext() {
    return _react.useContext(_functionContextDefault.default);
}
exports.default = useFunctionContext;

},{"react":"21dqq","../../ScrollToBottom/FunctionContext":"kFpSF","@parcel/transformer-js/src/esmodule-helpers.js":"gkKU3"}],"kFpSF":[function(require,module,exports) {
var parcelHelpers = require("@parcel/transformer-js/src/esmodule-helpers.js");
parcelHelpers.defineInteropFlag(exports);
var _react = require("react");
var _reactDefault = parcelHelpers.interopDefault(_react);
var context = /*#__PURE__*/ _reactDefault.default.createContext({
    scrollTo: function scrollTo() {
        return 0;
    },
    scrollToBottom: function scrollToBottom() {
        return 0;
    },
    scrollToEnd: function scrollToEnd() {
        return 0;
    },
    scrollToStart: function scrollToStart() {
        return 0;
    },
    scrollToTop: function scrollToTop() {
        return 0;
    }
});
context.displayName = 'ScrollToBottomFunctionContext';
exports.default = context;

},{"react":"21dqq","@parcel/transformer-js/src/esmodule-helpers.js":"gkKU3"}],"1N64J":[function(require,module,exports) {
var parcelHelpers = require("@parcel/transformer-js/src/esmodule-helpers.js");
parcelHelpers.defineInteropFlag(exports);
/* eslint no-magic-numbers: ["error", { "ignore": [2] }] */ var _useStateContext = require("./internal/useStateContext");
var _useStateContextDefault = parcelHelpers.interopDefault(_useStateContext);
function useSticky() {
    var _useStateContext1 = _useStateContextDefault.default(2), sticky = _useStateContext1.sticky;
    return [
        sticky
    ];
}
exports.default = useSticky;

},{"./internal/useStateContext":"edkS9","@parcel/transformer-js/src/esmodule-helpers.js":"gkKU3"}],"edkS9":[function(require,module,exports) {
var parcelHelpers = require("@parcel/transformer-js/src/esmodule-helpers.js");
parcelHelpers.defineInteropFlag(exports);
var _react = require("react");
var _state1Context = require("../../ScrollToBottom/State1Context");
var _state1ContextDefault = parcelHelpers.interopDefault(_state1Context);
var _state2Context = require("../../ScrollToBottom/State2Context");
var _state2ContextDefault = parcelHelpers.interopDefault(_state2Context);
var _stateContext = require("../../ScrollToBottom/StateContext");
var _stateContextDefault = parcelHelpers.interopDefault(_stateContext);
var stateContexts = [
    _stateContextDefault.default,
    _state1ContextDefault.default,
    _state2ContextDefault.default
];
function useStateContext(tier) {
    return _react.useContext(stateContexts[tier] || stateContexts[0]);
}
exports.default = useStateContext;

},{"react":"21dqq","../../ScrollToBottom/State1Context":"5l40r","../../ScrollToBottom/State2Context":"hBQod","../../ScrollToBottom/StateContext":"58zlr","@parcel/transformer-js/src/esmodule-helpers.js":"gkKU3"}],"5l40r":[function(require,module,exports) {
var parcelHelpers = require("@parcel/transformer-js/src/esmodule-helpers.js");
parcelHelpers.defineInteropFlag(exports);
var _react = require("react");
var _reactDefault = parcelHelpers.interopDefault(_react);
var context = /*#__PURE__*/ _reactDefault.default.createContext({
    atBottom: true,
    atEnd: true,
    atStart: false,
    atTop: true,
    mode: 'bottom'
});
context.displayName = 'ScrollToBottomState1Context';
exports.default = context;

},{"react":"21dqq","@parcel/transformer-js/src/esmodule-helpers.js":"gkKU3"}],"hBQod":[function(require,module,exports) {
var parcelHelpers = require("@parcel/transformer-js/src/esmodule-helpers.js");
parcelHelpers.defineInteropFlag(exports);
var _react = require("react");
var _reactDefault = parcelHelpers.interopDefault(_react);
var context = /*#__PURE__*/ _reactDefault.default.createContext({
    animating: false,
    animatingToEnd: false,
    sticky: true
});
context.displayName = 'ScrollToBottomState2Context';
exports.default = context;

},{"react":"21dqq","@parcel/transformer-js/src/esmodule-helpers.js":"gkKU3"}],"58zlr":[function(require,module,exports) {
var parcelHelpers = require("@parcel/transformer-js/src/esmodule-helpers.js");
parcelHelpers.defineInteropFlag(exports);
var _react = require("react");
var _reactDefault = parcelHelpers.interopDefault(_react);
var context = /*#__PURE__*/ _reactDefault.default.createContext({
    animating: false,
    animatingToEnd: false,
    atBottom: true,
    atEnd: true,
    atStart: false,
    atTop: true,
    mode: 'bottom',
    sticky: true
});
context.displayName = 'ScrollToBottomStateContext';
exports.default = context;

},{"react":"21dqq","@parcel/transformer-js/src/esmodule-helpers.js":"gkKU3"}],"7aM5G":[function(require,module,exports) {
var parcelHelpers = require("@parcel/transformer-js/src/esmodule-helpers.js");
parcelHelpers.defineInteropFlag(exports);
var _useInternalContext = require("./useInternalContext");
var _useInternalContextDefault = parcelHelpers.interopDefault(_useInternalContext);
function useStyleToClassName() {
    var _useInternalContext1 = _useInternalContextDefault.default(), styleToClassName = _useInternalContext1.styleToClassName;
    return styleToClassName;
}
exports.default = useStyleToClassName;

},{"./useInternalContext":"hYi9A","@parcel/transformer-js/src/esmodule-helpers.js":"gkKU3"}],"hYi9A":[function(require,module,exports) {
var parcelHelpers = require("@parcel/transformer-js/src/esmodule-helpers.js");
parcelHelpers.defineInteropFlag(exports);
var _react = require("react");
var _internalContext = require("../../ScrollToBottom/InternalContext");
var _internalContextDefault = parcelHelpers.interopDefault(_internalContext);
function useInternalContext() {
    return _react.useContext(_internalContextDefault.default);
}
exports.default = useInternalContext;

},{"react":"21dqq","../../ScrollToBottom/InternalContext":"9Xnjg","@parcel/transformer-js/src/esmodule-helpers.js":"gkKU3"}],"9Xnjg":[function(require,module,exports) {
var parcelHelpers = require("@parcel/transformer-js/src/esmodule-helpers.js");
parcelHelpers.defineInteropFlag(exports);
var _react = require("react");
var _reactDefault = parcelHelpers.interopDefault(_react);
var context = /*#__PURE__*/ _reactDefault.default.createContext({
    offsetHeight: 0,
    scrollHeight: 0,
    setTarget: function setTarget() {
        return 0;
    },
    styleToClassName: function styleToClassName() {
        return '';
    }
});
context.displayName = 'ScrollToBottomInternalContext';
exports.default = context;

},{"react":"21dqq","@parcel/transformer-js/src/esmodule-helpers.js":"gkKU3"}],"6XrE5":[function(require,module,exports) {
var parcelHelpers = require("@parcel/transformer-js/src/esmodule-helpers.js");
parcelHelpers.defineInteropFlag(exports);
var _classnames = require("classnames");
var _classnamesDefault = parcelHelpers.interopDefault(_classnames);
var _propTypes = require("prop-types");
var _propTypesDefault = parcelHelpers.interopDefault(_propTypes);
var _react = require("react");
var _reactDefault = parcelHelpers.interopDefault(_react);
var _autoHideFollowButton = require("./ScrollToBottom/AutoHideFollowButton");
var _autoHideFollowButtonDefault = parcelHelpers.interopDefault(_autoHideFollowButton);
var _composer = require("./ScrollToBottom/Composer");
var _composerDefault = parcelHelpers.interopDefault(_composer);
var _panel = require("./ScrollToBottom/Panel");
var _panelDefault = parcelHelpers.interopDefault(_panel);
var _useStyleToClassName = require("./hooks/internal/useStyleToClassName");
var _useStyleToClassNameDefault = parcelHelpers.interopDefault(_useStyleToClassName);
var ROOT_STYLE = {
    position: 'relative'
};
var BasicScrollToBottomCore = function BasicScrollToBottomCore(_ref) {
    var children = _ref.children, className = _ref.className, followButtonClassName = _ref.followButtonClassName, scrollViewClassName = _ref.scrollViewClassName;
    var rootCSS = _useStyleToClassNameDefault.default()(ROOT_STYLE);
    return /*#__PURE__*/ _reactDefault.default.createElement("div", {
        className: _classnamesDefault.default(rootCSS, (className || '') + '')
    }, /*#__PURE__*/ _reactDefault.default.createElement(_panelDefault.default, {
        className: (scrollViewClassName || '') + ''
    }, children), /*#__PURE__*/ _reactDefault.default.createElement(_autoHideFollowButtonDefault.default, {
        className: (followButtonClassName || '') + ''
    }));
};
BasicScrollToBottomCore.defaultProps = {
    children: undefined,
    className: undefined,
    followButtonClassName: undefined,
    scrollViewClassName: undefined
};
BasicScrollToBottomCore.propTypes = {
    children: _propTypesDefault.default.any,
    className: _propTypesDefault.default.string,
    followButtonClassName: _propTypesDefault.default.string,
    scrollViewClassName: _propTypesDefault.default.string
};
var BasicScrollToBottom = function BasicScrollToBottom(_ref2) {
    var checkInterval = _ref2.checkInterval, children = _ref2.children, className = _ref2.className, debounce = _ref2.debounce, debug = _ref2.debug, followButtonClassName = _ref2.followButtonClassName, initialScrollBehavior = _ref2.initialScrollBehavior, mode = _ref2.mode, nonce = _ref2.nonce, scroller = _ref2.scroller, scrollViewClassName = _ref2.scrollViewClassName;
    return /*#__PURE__*/ _reactDefault.default.createElement(_composerDefault.default, {
        checkInterval: checkInterval,
        debounce: debounce,
        debug: debug,
        initialScrollBehavior: initialScrollBehavior,
        mode: mode,
        nonce: nonce,
        scroller: scroller
    }, /*#__PURE__*/ _reactDefault.default.createElement(BasicScrollToBottomCore, {
        className: className,
        followButtonClassName: followButtonClassName,
        scrollViewClassName: scrollViewClassName
    }, children));
};
BasicScrollToBottom.defaultProps = {
    checkInterval: undefined,
    children: undefined,
    className: undefined,
    debounce: undefined,
    debug: undefined,
    followButtonClassName: undefined,
    initialScrollBehavior: 'smooth',
    mode: undefined,
    nonce: undefined,
    scroller: undefined,
    scrollViewClassName: undefined
};
BasicScrollToBottom.propTypes = {
    checkInterval: _propTypesDefault.default.number,
    children: _propTypesDefault.default.any,
    className: _propTypesDefault.default.string,
    debounce: _propTypesDefault.default.number,
    debug: _propTypesDefault.default.bool,
    followButtonClassName: _propTypesDefault.default.string,
    initialScrollBehavior: _propTypesDefault.default.oneOf([
        'auto',
        'smooth'
    ]),
    mode: _propTypesDefault.default.oneOf([
        'bottom',
        'top'
    ]),
    nonce: _propTypesDefault.default.string,
    scroller: _propTypesDefault.default.func,
    scrollViewClassName: _propTypesDefault.default.string
};
exports.default = BasicScrollToBottom;

},{"classnames":"jocGM","prop-types":"2ipKq","react":"21dqq","./ScrollToBottom/AutoHideFollowButton":"fIwZZ","./ScrollToBottom/Composer":"ds9c1","./ScrollToBottom/Panel":"cLIsq","./hooks/internal/useStyleToClassName":"7aM5G","@parcel/transformer-js/src/esmodule-helpers.js":"gkKU3"}],"ds9c1":[function(require,module,exports) {
var parcelHelpers = require("@parcel/transformer-js/src/esmodule-helpers.js");
parcelHelpers.defineInteropFlag(exports);
var _defineProperty = require("@babel/runtime-corejs3/helpers/defineProperty");
var _definePropertyDefault = parcelHelpers.interopDefault(_defineProperty);
var _toConsumableArray = require("@babel/runtime-corejs3/helpers/toConsumableArray");
var _toConsumableArrayDefault = parcelHelpers.interopDefault(_toConsumableArray);
var _slicedToArray = require("@babel/runtime-corejs3/helpers/slicedToArray");
var _slicedToArrayDefault = parcelHelpers.interopDefault(_slicedToArray);
var _esRegexpExecJs = require("core-js/modules/es.regexp.exec.js");
var _esStringReplaceJs = require("core-js/modules/es.string.replace.js");
var _setInterval = require("@babel/runtime-corejs3/core-js-stable/set-interval");
var _setIntervalDefault = parcelHelpers.interopDefault(_setInterval);
var _indexOf = require("@babel/runtime-corejs3/core-js-stable/instance/index-of");
var _indexOfDefault = parcelHelpers.interopDefault(_indexOf);
var _splice = require("@babel/runtime-corejs3/core-js-stable/instance/splice");
var _spliceDefault = parcelHelpers.interopDefault(_splice);
var _concat = require("@babel/runtime-corejs3/core-js-stable/instance/concat");
var _concatDefault = parcelHelpers.interopDefault(_concat);
var _now = require("@babel/runtime-corejs3/core-js-stable/date/now");
var _nowDefault = parcelHelpers.interopDefault(_now);
var _forEach = require("@babel/runtime-corejs3/core-js-stable/instance/for-each");
var _forEachDefault = parcelHelpers.interopDefault(_forEach);
var _keys = require("@babel/runtime-corejs3/core-js-stable/object/keys");
var _keysDefault = parcelHelpers.interopDefault(_keys);
var _getOwnPropertySymbols = require("@babel/runtime-corejs3/core-js-stable/object/get-own-property-symbols");
var _getOwnPropertySymbolsDefault = parcelHelpers.interopDefault(_getOwnPropertySymbols);
var _filter = require("@babel/runtime-corejs3/core-js-stable/instance/filter");
var _filterDefault = parcelHelpers.interopDefault(_filter);
var _getOwnPropertyDescriptor = require("@babel/runtime-corejs3/core-js-stable/object/get-own-property-descriptor");
var _getOwnPropertyDescriptorDefault = parcelHelpers.interopDefault(_getOwnPropertyDescriptor);
var _getOwnPropertyDescriptors = require("@babel/runtime-corejs3/core-js-stable/object/get-own-property-descriptors");
var _getOwnPropertyDescriptorsDefault = parcelHelpers.interopDefault(_getOwnPropertyDescriptors);
var _defineProperties = require("@babel/runtime-corejs3/core-js-stable/object/define-properties");
var _definePropertiesDefault = parcelHelpers.interopDefault(_defineProperties);
var _defineProperty1 = require("@babel/runtime-corejs3/core-js-stable/object/define-property");
var _definePropertyDefault1 = parcelHelpers.interopDefault(_defineProperty1);
var _createInstance = require("@emotion/css/create-instance");
var _createInstanceDefault = parcelHelpers.interopDefault(_createInstance);
var _propTypes = require("prop-types");
var _propTypesDefault = parcelHelpers.interopDefault(_propTypes);
var _react = require("react");
var _reactDefault = parcelHelpers.interopDefault(_react);
var _createCSSKey = require("../createCSSKey");
var _createCSSKeyDefault = parcelHelpers.interopDefault(_createCSSKey);
var _debug = require("../utils/debug");
var _debugDefault = parcelHelpers.interopDefault(_debug);
var _eventSpy = require("../EventSpy");
var _eventSpyDefault = parcelHelpers.interopDefault(_eventSpy);
var _functionContext = require("./FunctionContext");
var _functionContextDefault = parcelHelpers.interopDefault(_functionContext);
var _internalContext = require("./InternalContext");
var _internalContextDefault = parcelHelpers.interopDefault(_internalContext);
var _spineTo = require("../SpineTo");
var _spineToDefault = parcelHelpers.interopDefault(_spineTo);
var _state1Context = require("./State1Context");
var _state1ContextDefault = parcelHelpers.interopDefault(_state1Context);
var _state2Context = require("./State2Context");
var _state2ContextDefault = parcelHelpers.interopDefault(_state2Context);
var _stateContext = require("./StateContext");
var _stateContextDefault = parcelHelpers.interopDefault(_stateContext);
var _styleConsole = require("../utils/styleConsole");
var _styleConsoleDefault = parcelHelpers.interopDefault(_styleConsole);
var _useStateRef = require("../hooks/internal/useStateRef");
var _useStateRefDefault = parcelHelpers.interopDefault(_useStateRef);
function ownKeys(object, enumerableOnly) {
    var keys = _keysDefault.default(object);
    if (_getOwnPropertySymbolsDefault.default) {
        var symbols = _getOwnPropertySymbolsDefault.default(object);
        if (enumerableOnly) symbols = _filterDefault.default(symbols).call(symbols, function(sym) {
            return _getOwnPropertyDescriptorDefault.default(object, sym).enumerable;
        });
        keys.push.apply(keys, symbols);
    }
    return keys;
}
function _objectSpread(target) {
    for(var i = 1; i < arguments.length; i++){
        var source = arguments[i] != null ? arguments[i] : {};
        if (i % 2) {
            var _context21;
            _forEachDefault.default(_context21 = ownKeys(Object(source), true)).call(_context21, function(key) {
                _definePropertyDefault.default(target, key, source[key]);
            });
        } else if (_getOwnPropertyDescriptorsDefault.default) _definePropertiesDefault.default(target, _getOwnPropertyDescriptorsDefault.default(source));
        else {
            var _context22;
            _forEachDefault.default(_context22 = ownKeys(Object(source))).call(_context22, function(key) {
                _definePropertyDefault1.default(target, key, _getOwnPropertyDescriptorDefault.default(source, key));
            });
        }
    }
    return target;
}
var DEFAULT_SCROLLER = function DEFAULT_SCROLLER() {
    return Infinity;
};
var MIN_CHECK_INTERVAL = 17; // 1 frame
var MODE_BOTTOM = 'bottom';
var MODE_TOP = 'top';
var NEAR_END_THRESHOLD = 1;
var SCROLL_DECISION_DURATION = 34; // 2 frames
// We pool the emotion object by nonce.
// This is to make sure we don't generate too many unneeded <style> tags.
var emotionPool = {};
function setImmediateInterval(fn, ms) {
    fn();
    return _setIntervalDefault.default(fn, ms);
}
function computeViewState(_ref) {
    var mode = _ref.mode, _ref$target = _ref.target, offsetHeight = _ref$target.offsetHeight, scrollHeight = _ref$target.scrollHeight, scrollTop = _ref$target.scrollTop;
    var atBottom = scrollHeight - scrollTop - offsetHeight < NEAR_END_THRESHOLD;
    var atTop = scrollTop < NEAR_END_THRESHOLD;
    var atEnd = mode === MODE_TOP ? atTop : atBottom;
    var atStart = mode !== MODE_TOP ? atTop : atBottom;
    return {
        atBottom: atBottom,
        atEnd: atEnd,
        atStart: atStart,
        atTop: atTop
    };
}
function isEnd(animateTo, mode) {
    return animateTo === (mode === MODE_TOP ? 0 : '100%');
}
var Composer = function Composer(_ref2) {
    var checkInterval = _ref2.checkInterval, children = _ref2.children, debounce = _ref2.debounce, debugFromProp = _ref2.debug, initialScrollBehavior = _ref2.initialScrollBehavior, mode = _ref2.mode, nonce = _ref2.nonce, scroller = _ref2.scroller;
    var debug = _react.useMemo(function() {
        return _debugDefault.default("<ScrollToBottom>", {
            force: debugFromProp
        });
    }, [
        debugFromProp
    ]);
    mode = mode === MODE_TOP ? MODE_TOP : MODE_BOTTOM;
    var ignoreScrollEventBeforeRef = _react.useRef(0);
    var initialScrollBehaviorRef = _react.useRef(initialScrollBehavior);
    var _useStateRef1 = _useStateRefDefault.default(mode === MODE_TOP ? 0 : '100%'), _useStateRef2 = _slicedToArrayDefault.default(_useStateRef1, 3), animateTo1 = _useStateRef2[0], setAnimateTo = _useStateRef2[1], animateToRef = _useStateRef2[2];
    var _useStateRef3 = _useStateRefDefault.default(null), _useStateRef4 = _slicedToArrayDefault.default(_useStateRef3, 3), target1 = _useStateRef4[0], setTarget = _useStateRef4[1], targetRef = _useStateRef4[2]; // Internal context
    var animateFromRef = _react.useRef(0);
    var offsetHeightRef = _react.useRef(0);
    var scrollHeightRef = _react.useRef(0); // State context
    var _useState = _react.useState(true), _useState2 = _slicedToArrayDefault.default(_useState, 2), atBottom1 = _useState2[0], setAtBottom = _useState2[1];
    var _useState3 = _react.useState(true), _useState4 = _slicedToArrayDefault.default(_useState3, 2), atEnd1 = _useState4[0], setAtEnd = _useState4[1];
    var _useState5 = _react.useState(true), _useState6 = _slicedToArrayDefault.default(_useState5, 2), atTop1 = _useState6[0], setAtTop = _useState6[1];
    var _useState7 = _react.useState(false), _useState8 = _slicedToArrayDefault.default(_useState7, 2), atStart1 = _useState8[0], setAtStart = _useState8[1];
    var _useStateRef5 = _useStateRefDefault.default(true), _useStateRef6 = _slicedToArrayDefault.default(_useStateRef5, 3), sticky = _useStateRef6[0], setSticky = _useStateRef6[1], stickyRef = _useStateRef6[2]; // High-rate state context
    var scrollPositionObserversRef = _react.useRef([]);
    var observeScrollPosition = _react.useCallback(function(fn) {
        var target = targetRef.current;
        scrollPositionObserversRef.current.push(fn);
        target && fn({
            scrollTop: target.scrollTop
        });
        return function() {
            var scrollPositionObservers = scrollPositionObserversRef.current;
            var index = _indexOfDefault.default(scrollPositionObservers).call(scrollPositionObservers, fn);
            ~index && _spliceDefault.default(scrollPositionObservers).call(scrollPositionObservers, index, 1);
        };
    }, [
        scrollPositionObserversRef,
        targetRef
    ]);
    var handleSpineToEnd = _react.useCallback(function() {
        var animateTo = animateToRef.current;
        debug(function() {
            var _context;
            return _concatDefault.default(_context = [
                '%cSpineTo%c: %conEnd%c is fired.'
            ]).call(_context, _toConsumableArrayDefault.default(_styleConsoleDefault.default('magenta')), _toConsumableArrayDefault.default(_styleConsoleDefault.default('orange')), [
                {
                    animateTo: animateTo
                }
            ]);
        });
        ignoreScrollEventBeforeRef.current = _nowDefault.default(); // handleScrollEnd may end at a position which should lose stickiness.
        // In that case, we will need to set sticky to false to stop the interval check.
        // Test case:
        // 1. Add a scroller that always return 0
        // 2. Show a panel with mode === MODE_BOTTOM
        // 3. Programmatically scroll to 0 (set element.scrollTop = 0)
        // Expected: it should not repetitively call scrollTo(0)
        //           it should set stickiness to false
        isEnd(animateTo, mode) || setSticky(false);
        setAnimateTo(null);
    }, [
        animateToRef,
        debug,
        ignoreScrollEventBeforeRef,
        mode,
        setAnimateTo,
        setSticky
    ]); // Function context
    var scrollTo = _react.useCallback(function(nextAnimateTo) {
        var _ref3 = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : {}, behavior = _ref3.behavior;
        var target = targetRef.current;
        if (typeof nextAnimateTo !== 'number' && nextAnimateTo !== '100%') return console.warn('react-scroll-to-bottom: Arguments passed to scrollTo() must be either number or "100%".');
         // If it is trying to scroll to a position which is not "atEnd", it should set sticky to false after scroll ended.
        debug(function() {
            var _context2;
            return [
                _concatDefault.default(_context2 = [
                    "%cscrollTo%c: Will scroll to %c".concat(typeof nextAnimateTo === 'number' ? nextAnimateTo + 'px' : nextAnimateTo.replace(/%/g, '%%'), "%c")
                ]).call(_context2, _toConsumableArrayDefault.default(_styleConsoleDefault.default('lime', '')), _toConsumableArrayDefault.default(_styleConsoleDefault.default('purple'))),
                {
                    behavior: behavior,
                    nextAnimateTo: nextAnimateTo,
                    target: target
                }
            ];
        });
        if (behavior === 'auto') {
            // Stop any existing animation
            handleSpineToEnd();
            if (target) // Jump to the scroll position
            target.scrollTop = nextAnimateTo === '100%' ? target.scrollHeight - target.offsetHeight : nextAnimateTo;
        } else {
            behavior !== 'smooth' && console.warn('react-scroll-to-bottom: Please set "behavior" when calling "scrollTo". In future versions, the default behavior will be changed from smooth scrolling to discrete scrolling to align with HTML Standard.');
            setAnimateTo(nextAnimateTo);
        } // This is for handling a case. When calling scrollTo('100%', { behavior: 'auto' }) multiple times, it would lose stickiness.
        if (isEnd(nextAnimateTo, mode)) {
            debug(function() {
                var _context3;
                return [
                    _concatDefault.default(_context3 = [
                        "%cscrollTo%c: Scrolling to end, will set sticky to %ctrue%c."
                    ]).call(_context3, _toConsumableArrayDefault.default(_styleConsoleDefault.default('lime', '')), _toConsumableArrayDefault.default(_styleConsoleDefault.default('purple'))),
                    [
                        {
                            mode: mode,
                            nextAnimateTo: nextAnimateTo
                        }
                    ]
                ];
            });
            setSticky(true);
        }
    }, [
        debug,
        handleSpineToEnd,
        mode,
        setAnimateTo,
        setSticky,
        targetRef
    ]);
    var scrollToBottom = _react.useCallback(function() {
        var _ref4 = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : {}, behavior = _ref4.behavior;
        debug(function() {
            var _context4;
            return _concatDefault.default(_context4 = [
                '%cscrollToBottom%c: Called'
            ]).call(_context4, _toConsumableArrayDefault.default(_styleConsoleDefault.default('yellow', '')));
        });
        behavior !== 'smooth' && console.warn('react-scroll-to-bottom: Please set "behavior" when calling "scrollToBottom". In future versions, the default behavior will be changed from smooth scrolling to discrete scrolling to align with HTML Standard.');
        scrollTo('100%', {
            behavior: behavior || 'smooth'
        });
    }, [
        debug,
        scrollTo
    ]);
    var scrollToTop = _react.useCallback(function() {
        var _ref5 = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : {}, behavior = _ref5.behavior;
        debug(function() {
            var _context5;
            return _concatDefault.default(_context5 = [
                '%cscrollToTop%c: Called'
            ]).call(_context5, _toConsumableArrayDefault.default(_styleConsoleDefault.default('yellow', '')));
        });
        behavior !== 'smooth' && console.warn('react-scroll-to-bottom: Please set "behavior" when calling "scrollToTop". In future versions, the default behavior will be changed from smooth scrolling to discrete scrolling to align with HTML Standard.');
        scrollTo(0, {
            behavior: behavior || 'smooth'
        });
    }, [
        debug,
        scrollTo
    ]);
    var scrollToEnd = _react.useCallback(function() {
        var _ref6 = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : {}, behavior = _ref6.behavior;
        debug(function() {
            var _context6;
            return _concatDefault.default(_context6 = [
                '%cscrollToEnd%c: Called'
            ]).call(_context6, _toConsumableArrayDefault.default(_styleConsoleDefault.default('yellow', '')));
        });
        behavior !== 'smooth' && console.warn('react-scroll-to-bottom: Please set "behavior" when calling "scrollToEnd". In future versions, the default behavior will be changed from smooth scrolling to discrete scrolling to align with HTML Standard.');
        var options = {
            behavior: behavior || 'smooth'
        };
        mode === MODE_TOP ? scrollToTop(options) : scrollToBottom(options);
    }, [
        debug,
        mode,
        scrollToBottom,
        scrollToTop
    ]);
    var scrollToStart = _react.useCallback(function() {
        var _ref7 = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : {}, behavior = _ref7.behavior;
        debug(function() {
            var _context7;
            return _concatDefault.default(_context7 = [
                '%cscrollToStart%c: Called'
            ]).call(_context7, _toConsumableArrayDefault.default(_styleConsoleDefault.default('yellow', '')));
        });
        behavior !== 'smooth' && console.warn('react-scroll-to-bottom: Please set "behavior" when calling "scrollToStart". In future versions, the default behavior will be changed from smooth scrolling to discrete scrolling to align with HTML Standard.');
        var options = {
            behavior: behavior || 'smooth'
        };
        mode === MODE_TOP ? scrollToBottom(options) : scrollToTop(options);
    }, [
        debug,
        mode,
        scrollToBottom,
        scrollToTop
    ]);
    var scrollToSticky = _react.useCallback(function() {
        var target = targetRef.current;
        if (target) {
            if (initialScrollBehaviorRef.current === 'auto') {
                debug(function() {
                    var _context8;
                    return _concatDefault.default(_context8 = [
                        "%ctarget changed%c: Initial scroll"
                    ]).call(_context8, _toConsumableArrayDefault.default(_styleConsoleDefault.default('blue')));
                });
                target.scrollTop = mode === MODE_TOP ? 0 : target.scrollHeight - target.offsetHeight;
                initialScrollBehaviorRef.current = false;
                return;
            } // This is very similar to scrollToEnd().
            // Instead of scrolling to end, it will call props.scroller() to determines how far it should scroll.
            // This function could be called while it is auto-scrolling.
            var animateFrom = animateFromRef.current;
            var offsetHeight = target.offsetHeight, scrollHeight = target.scrollHeight, scrollTop = target.scrollTop;
            var maxValue = mode === MODE_TOP ? 0 : Math.max(0, scrollHeight - offsetHeight - scrollTop);
            var minValue = Math.max(0, animateFrom - scrollTop);
            var rawNextValue = scroller({
                maxValue: maxValue,
                minValue: minValue,
                offsetHeight: offsetHeight,
                scrollHeight: scrollHeight,
                scrollTop: scrollTop
            });
            var nextValue = Math.max(0, Math.min(maxValue, rawNextValue));
            var nextAnimateTo;
            if (mode === MODE_TOP || nextValue !== maxValue) nextAnimateTo = scrollTop + nextValue;
            else // When scrolling to bottom, we should scroll to "100%".
            // Otherwise, if we scroll to any number, it will lose stickiness when elements are adding too fast.
            // "100%" is a special argument intended to make sure stickiness is not lost while new elements are being added.
            nextAnimateTo = '100%';
            debug(function() {
                var _context9, _context10, _context11;
                return [
                    _concatDefault.default(_context9 = [
                        _concatDefault.default(_context10 = _concatDefault.default(_context11 = "%cscrollToSticky%c: Will animate from %c".concat(animateFrom, "px%c to %c")).call(_context11, typeof nextAnimateTo === 'number' ? nextAnimateTo + 'px' : nextAnimateTo.replace(/%/g, '%%'), "%c (%c")).call(_context10, (nextAnimateTo === '100%' ? maxValue : nextAnimateTo) + animateFrom, "px%c)")
                    ]).call(_context9, _toConsumableArrayDefault.default(_styleConsoleDefault.default('orange')), _toConsumableArrayDefault.default(_styleConsoleDefault.default('purple')), _toConsumableArrayDefault.default(_styleConsoleDefault.default('purple')), _toConsumableArrayDefault.default(_styleConsoleDefault.default('purple'))),
                    {
                        animateFrom: animateFrom,
                        maxValue: maxValue,
                        minValue: minValue,
                        nextAnimateTo: nextAnimateTo,
                        nextValue: nextValue,
                        offsetHeight: offsetHeight,
                        rawNextValue: rawNextValue,
                        scrollHeight: scrollHeight,
                        scrollTop: scrollTop
                    }
                ];
            });
            scrollTo(nextAnimateTo, {
                behavior: 'smooth'
            });
        }
    }, [
        animateFromRef,
        debug,
        mode,
        scroller,
        scrollTo,
        targetRef
    ]);
    var handleScroll = _react.useCallback(function(_ref8) {
        var _context17;
        var timeStampLow = _ref8.timeStampLow;
        var animateTo = animateToRef.current;
        var target = targetRef.current;
        var animating = animateTo !== null; // Currently, there are no reliable way to check if the "scroll" event is trigger due to
        // user gesture, programmatic scrolling, or Chrome-synthesized "scroll" event to compensate size change.
        // Thus, we use our best-effort to guess if it is triggered by user gesture, and disable sticky if it is heading towards the start direction.
        if (timeStampLow <= ignoreScrollEventBeforeRef.current || !target) // Since we debounce "scroll" event, this handler might be called after spineTo.onEnd (a.k.a. artificial scrolling).
        // We should ignore debounced event fired after scrollEnd, because without skipping them, the userInitiatedScroll calculated below will not be accurate.
        // Thus, on a fast machine, adding elements super fast will lose the "stickiness".
        return;
        var _computeViewState = computeViewState({
            mode: mode,
            target: target
        }), atBottom = _computeViewState.atBottom, atEnd = _computeViewState.atEnd, atStart = _computeViewState.atStart, atTop = _computeViewState.atTop;
        setAtBottom(atBottom);
        setAtEnd(atEnd);
        setAtStart(atStart);
        setAtTop(atTop); // Chrome will emit "synthetic" scroll event if the container is resized or an element is added
        // We need to ignore these "synthetic" events
        // Repro: In playground, press 4-1-5-1-1 (small, add one, normal, add one, add one)
        //        Nomatter how fast or slow the sequence is being pressed, it should still stick to the bottom
        var nextOffsetHeight = target.offsetHeight, nextScrollHeight = target.scrollHeight;
        var offsetHeight = offsetHeightRef.current;
        var scrollHeight = scrollHeightRef.current;
        var offsetHeightChanged = nextOffsetHeight !== offsetHeight;
        var scrollHeightChanged = nextScrollHeight !== scrollHeight;
        if (offsetHeightChanged) offsetHeightRef.current = nextOffsetHeight;
        if (scrollHeightChanged) scrollHeightRef.current = nextScrollHeight;
         // Sticky means:
        // - If it is scrolled programatically, we are still in sticky mode
        // - If it is scrolled by the user, then sticky means if we are at the end
        // Only update stickiness if the scroll event is not due to synthetic scroll done by Chrome
        if (!offsetHeightChanged && !scrollHeightChanged) {
            // We are sticky if we are animating to the end, or we are already at the end.
            // We can be "animating but not sticky" by calling "scrollTo(100)" where the container scrollHeight is 200px.
            var nextSticky = animating && isEnd(animateTo, mode) || atEnd;
            if (stickyRef.current !== nextSticky) {
                debug(function() {
                    var _context12, _context13, _context14, _context15;
                    return [
                        _concatDefault.default(_context12 = [
                            "%conScroll%c: %csetSticky%c(%c".concat(nextSticky, "%c)")
                        ]).call(_context12, _toConsumableArrayDefault.default(_styleConsoleDefault.default('red')), _toConsumableArrayDefault.default(_styleConsoleDefault.default('red')), _toConsumableArrayDefault.default(_styleConsoleDefault.default('purple'))),
                        _concatDefault.default(_context13 = [
                            _concatDefault.default(_context14 = _concatDefault.default(_context15 = "(animating = %c".concat(animating, "%c && isEnd = %c")).call(_context15, isEnd(animateTo, mode), "%c) || atEnd = %c")).call(_context14, atEnd, "%c")
                        ]).call(_context13, _toConsumableArrayDefault.default(_styleConsoleDefault.default('purple')), _toConsumableArrayDefault.default(_styleConsoleDefault.default('purple')), _toConsumableArrayDefault.default(_styleConsoleDefault.default('purple')), [
                            {
                                animating: animating,
                                animateTo: animateTo,
                                atEnd: atEnd,
                                mode: mode,
                                offsetHeight: target.offsetHeight,
                                scrollHeight: target.scrollHeight,
                                sticky: stickyRef.current,
                                nextSticky: nextSticky
                            }
                        ])
                    ];
                });
                setSticky(nextSticky);
            }
        } else if (stickyRef.current) {
            debug(function() {
                var _context16;
                return [
                    _concatDefault.default(_context16 = [
                        "%conScroll%c: Size changed while sticky, calling %cscrollToSticky()%c"
                    ]).call(_context16, _toConsumableArrayDefault.default(_styleConsoleDefault.default('red')), _toConsumableArrayDefault.default(_styleConsoleDefault.default('orange')), [
                        {
                            offsetHeightChanged: offsetHeightChanged,
                            scrollHeightChanged: scrollHeightChanged
                        }
                    ]),
                    {
                        nextOffsetHeight: nextOffsetHeight,
                        prevOffsetHeight: offsetHeight,
                        nextScrollHeight: nextScrollHeight,
                        prevScrollHeight: scrollHeight
                    }
                ];
            });
            scrollToSticky();
        }
        var actualScrollTop = target.scrollTop;
        _forEachDefault.default(_context17 = scrollPositionObserversRef.current).call(_context17, function(observer) {
            return observer({
                scrollTop: actualScrollTop
            });
        });
    }, [
        animateToRef,
        debug,
        ignoreScrollEventBeforeRef,
        mode,
        offsetHeightRef,
        scrollHeightRef,
        scrollPositionObserversRef,
        scrollToSticky,
        setAtBottom,
        setAtEnd,
        setAtStart,
        setAtTop,
        setSticky,
        stickyRef,
        targetRef
    ]);
    _react.useEffect(function() {
        if (target1) {
            var stickyButNotAtEndSince = false;
            var timeout = setImmediateInterval(function() {
                var target = targetRef.current;
                var animating = animateToRef.current !== null;
                if (stickyRef.current) {
                    if (!computeViewState({
                        mode: mode,
                        target: target
                    }).atEnd) {
                        if (!stickyButNotAtEndSince) stickyButNotAtEndSince = _nowDefault.default();
                        else if (_nowDefault.default() - stickyButNotAtEndSince > SCROLL_DECISION_DURATION) {
                            // Quirks: In Firefox, after user scroll down, Firefox do two things:
                            //         1. Set to a new "scrollTop"
                            //         2. Fire "scroll" event
                            //         For what we observed, #1 is fired about 20ms before #2. There is a chance that this stickyCheckTimeout is being scheduled between 1 and 2.
                            //         That means, if we just look at #1 to decide if we should scroll, we will always scroll, in oppose to the user's intention.
                            // Repro: Open Firefox, set checkInterval to a lower number, and try to scroll by dragging the scroll handler. It will jump back.
                            // The "animating" check will make sure stickiness is not lost when elements are adding at a very fast pace.
                            if (!animating) {
                                animateFromRef.current = target.scrollTop;
                                debug(function() {
                                    var _context18;
                                    return _concatDefault.default(_context18 = [
                                        "%cInterval check%c: Should sticky but not at end, calling %cscrollToSticky()%c to scroll"
                                    ]).call(_context18, _toConsumableArrayDefault.default(_styleConsoleDefault.default('navy')), _toConsumableArrayDefault.default(_styleConsoleDefault.default('orange')));
                                });
                                scrollToSticky();
                            }
                            stickyButNotAtEndSince = false;
                        }
                    } else stickyButNotAtEndSince = false;
                } else if (target.scrollHeight <= target.offsetHeight && !stickyRef.current) {
                    // When the container is emptied, we will set sticky back to true.
                    debug(function() {
                        var _context19;
                        return [
                            _concatDefault.default(_context19 = [
                                "%cInterval check%c: Container is emptied, setting sticky back to %ctrue%c"
                            ]).call(_context19, _toConsumableArrayDefault.default(_styleConsoleDefault.default('navy')), _toConsumableArrayDefault.default(_styleConsoleDefault.default('purple'))),
                            [
                                {
                                    offsetHeight: target.offsetHeight,
                                    scrollHeight: target.scrollHeight,
                                    sticky: stickyRef.current
                                }
                            ]
                        ];
                    });
                    setSticky(true);
                }
            }, Math.max(MIN_CHECK_INTERVAL, checkInterval) || MIN_CHECK_INTERVAL);
            return function() {
                return clearInterval(timeout);
            };
        }
    }, [
        animateToRef,
        checkInterval,
        debug,
        mode,
        scrollToSticky,
        setSticky,
        stickyRef,
        target1,
        targetRef
    ]);
    var styleToClassName = _react.useMemo(function() {
        var emotion = emotionPool[nonce] || (emotionPool[nonce] = _createInstanceDefault.default({
            key: 'react-scroll-to-bottom--css-' + _createCSSKeyDefault.default(),
            nonce: nonce
        }));
        return function(style) {
            return emotion.css(style) + '';
        };
    }, [
        nonce
    ]);
    var internalContext = _react.useMemo(function() {
        return {
            observeScrollPosition: observeScrollPosition,
            setTarget: setTarget,
            styleToClassName: styleToClassName
        };
    }, [
        observeScrollPosition,
        setTarget,
        styleToClassName
    ]);
    var state1Context = _react.useMemo(function() {
        return {
            atBottom: atBottom1,
            atEnd: atEnd1,
            atStart: atStart1,
            atTop: atTop1,
            mode: mode
        };
    }, [
        atBottom1,
        atEnd1,
        atStart1,
        atTop1,
        mode
    ]);
    var state2Context = _react.useMemo(function() {
        var animating = animateTo1 !== null;
        return {
            animating: animating,
            animatingToEnd: animating && isEnd(animateTo1, mode),
            sticky: sticky
        };
    }, [
        animateTo1,
        mode,
        sticky
    ]);
    var combinedStateContext = _react.useMemo(function() {
        return _objectSpread(_objectSpread({}, state1Context), state2Context);
    }, [
        state1Context,
        state2Context
    ]);
    var functionContext = _react.useMemo(function() {
        return {
            scrollTo: scrollTo,
            scrollToBottom: scrollToBottom,
            scrollToEnd: scrollToEnd,
            scrollToStart: scrollToStart,
            scrollToTop: scrollToTop
        };
    }, [
        scrollTo,
        scrollToBottom,
        scrollToEnd,
        scrollToStart,
        scrollToTop
    ]);
    _react.useEffect(function() {
        // We need to update the "scrollHeight" value to latest when the user do a focus inside the box.
        //
        // This is because:
        // - In our code that mitigate Chrome synthetic scrolling, that code will look at whether "scrollHeight" value is latest or not.
        // - That code only run on "scroll" event.
        // - That means, on every "scroll" event, if the "scrollHeight" value is not latest, we will skip modifying the stickiness.
        // - That means, if the user "focus" to an element that cause the scroll view to scroll to the bottom, the user agent will fire "scroll" event.
        //   Since the "scrollHeight" is not latest value, this "scroll" event will be ignored and stickiness will not be modified.
        // - That means, if the user "focus" to a newly added element that is at the end of the scroll view, the "scroll to bottom" button will continue to show.
        //
        // Repro in Chrome:
        // 1. Fill up a scroll view
        // 2. Scroll up, the "scroll to bottom" button should show up
        // 3. Click "Add a button"
        // 4. Click on the scroll view (to pseudo-focus on it)
        // 5. Press TAB, the scroll view will be at the bottom
        //
        // Expect:
        // - The "scroll to bottom" button should be gone.
        if (target1) {
            var handleFocus = function handleFocus() {
                scrollHeightRef.current = target1.scrollHeight;
            };
            target1.addEventListener('focus', handleFocus, {
                capture: true,
                passive: true
            });
            return function() {
                return target1.removeEventListener('focus', handleFocus);
            };
        }
    }, [
        target1
    ]);
    debug(function() {
        var _context20;
        return [
            _concatDefault.default(_context20 = [
                "%cRender%c: Render"
            ]).call(_context20, _toConsumableArrayDefault.default(_styleConsoleDefault.default('cyan', ''))),
            {
                animateTo: animateTo1,
                animating: animateTo1 !== null,
                sticky: sticky,
                target: target1
            }
        ];
    });
    return /*#__PURE__*/ _reactDefault.default.createElement(_internalContextDefault.default.Provider, {
        value: internalContext
    }, /*#__PURE__*/ _reactDefault.default.createElement(_functionContextDefault.default.Provider, {
        value: functionContext
    }, /*#__PURE__*/ _reactDefault.default.createElement(_stateContextDefault.default.Provider, {
        value: combinedStateContext
    }, /*#__PURE__*/ _reactDefault.default.createElement(_state1ContextDefault.default.Provider, {
        value: state1Context
    }, /*#__PURE__*/ _reactDefault.default.createElement(_state2ContextDefault.default.Provider, {
        value: state2Context
    }, children, target1 && /*#__PURE__*/ _reactDefault.default.createElement(_eventSpyDefault.default, {
        debounce: debounce,
        name: "scroll",
        onEvent: handleScroll,
        target: target1
    }), target1 && animateTo1 !== null && /*#__PURE__*/ _reactDefault.default.createElement(_spineToDefault.default, {
        name: "scrollTop",
        onEnd: handleSpineToEnd,
        target: target1,
        value: animateTo1
    }))))));
};
Composer.defaultProps = {
    checkInterval: 100,
    children: undefined,
    debounce: 17,
    debug: undefined,
    initialScrollBehavior: 'smooth',
    mode: undefined,
    nonce: undefined,
    scroller: DEFAULT_SCROLLER
};
Composer.propTypes = {
    checkInterval: _propTypesDefault.default.number,
    children: _propTypesDefault.default.any,
    debounce: _propTypesDefault.default.number,
    debug: _propTypesDefault.default.bool,
    initialScrollBehavior: _propTypesDefault.default.oneOf([
        'auto',
        'smooth'
    ]),
    mode: _propTypesDefault.default.oneOf([
        'bottom',
        'top'
    ]),
    nonce: _propTypesDefault.default.string,
    scroller: _propTypesDefault.default.func
};
exports.default = Composer;

},{"@babel/runtime-corejs3/helpers/defineProperty":"54h5f","@babel/runtime-corejs3/helpers/toConsumableArray":"k0LvR","@babel/runtime-corejs3/helpers/slicedToArray":"36E7Q","core-js/modules/es.regexp.exec.js":"pNALB","core-js/modules/es.string.replace.js":"bE0gl","@babel/runtime-corejs3/core-js-stable/set-interval":"3mMHk","@babel/runtime-corejs3/core-js-stable/instance/index-of":"boWFK","@babel/runtime-corejs3/core-js-stable/instance/splice":"e3Y4r","@babel/runtime-corejs3/core-js-stable/instance/concat":"tXYDQ","@babel/runtime-corejs3/core-js-stable/date/now":"j7X90","@babel/runtime-corejs3/core-js-stable/instance/for-each":"fi9Yx","@babel/runtime-corejs3/core-js-stable/object/keys":"kYmBK","@babel/runtime-corejs3/core-js-stable/object/get-own-property-symbols":"2gUV7","@babel/runtime-corejs3/core-js-stable/instance/filter":"fVgp6","@babel/runtime-corejs3/core-js-stable/object/get-own-property-descriptor":"lQ0dj","@babel/runtime-corejs3/core-js-stable/object/get-own-property-descriptors":"5Vu5K","@babel/runtime-corejs3/core-js-stable/object/define-properties":"jscfH","@babel/runtime-corejs3/core-js-stable/object/define-property":"kv9Cz","@emotion/css/create-instance":"k89zX","prop-types":"2ipKq","react":"21dqq","../createCSSKey":"fjyst","../utils/debug":"1D0Dy","../EventSpy":"cL9HT","./FunctionContext":"kFpSF","./InternalContext":"9Xnjg","../SpineTo":"9BVSj","./State1Context":"5l40r","./State2Context":"hBQod","./StateContext":"58zlr","../utils/styleConsole":"1rrzA","../hooks/internal/useStateRef":"7ubbf","@parcel/transformer-js/src/esmodule-helpers.js":"gkKU3"}],"54h5f":[function(require,module,exports) {
var _Object$defineProperty = require("@babel/runtime-corejs3/core-js/object/define-property");
function _defineProperty(obj, key, value) {
    if (key in obj) _Object$defineProperty(obj, key, {
        value: value,
        enumerable: true,
        configurable: true,
        writable: true
    });
    else obj[key] = value;
    return obj;
}
module.exports = _defineProperty, module.exports.__esModule = true, module.exports["default"] = module.exports;

},{"@babel/runtime-corejs3/core-js/object/define-property":"1tVMP"}],"1tVMP":[function(require,module,exports) {
module.exports = require("core-js-pure/features/object/define-property");

},{"core-js-pure/features/object/define-property":"eAK3A"}],"eAK3A":[function(require,module,exports) {
module.exports = require('../../full/object/define-property');

},{"../../full/object/define-property":"1SOH7"}],"1SOH7":[function(require,module,exports) {
var parent = require('../../actual/object/define-property');
module.exports = parent;

},{"../../actual/object/define-property":"l5Ixk"}],"l5Ixk":[function(require,module,exports) {
var parent = require('../../stable/object/define-property');
module.exports = parent;

},{"../../stable/object/define-property":"fpclb"}],"fpclb":[function(require,module,exports) {
var parent = require('../../es/object/define-property');
module.exports = parent;

},{"../../es/object/define-property":"i49oF"}],"i49oF":[function(require,module,exports) {
require('../../modules/es.object.define-property');
var path = require('../../internals/path');
var Object = path.Object;
var defineProperty = module.exports = function defineProperty(it, key, desc) {
    return Object.defineProperty(it, key, desc);
};
if (Object.defineProperty.sham) defineProperty.sham = true;

},{"../../modules/es.object.define-property":"bQpcG","../../internals/path":"8QO0z"}],"bQpcG":[function(require,module,exports) {
var $ = require('../internals/export');
var DESCRIPTORS = require('../internals/descriptors');
var defineProperty = require('../internals/object-define-property').f;
// `Object.defineProperty` method
// https://tc39.es/ecma262/#sec-object.defineproperty
// eslint-disable-next-line es-x/no-object-defineproperty -- safe
$({
    target: 'Object',
    stat: true,
    forced: Object.defineProperty !== defineProperty,
    sham: !DESCRIPTORS
}, {
    defineProperty: defineProperty
});

},{"../internals/export":"jjems","../internals/descriptors":"87WOB","../internals/object-define-property":"gbIb1"}],"k0LvR":[function(require,module,exports) {
var arrayWithoutHoles = require("./arrayWithoutHoles.js");
var iterableToArray = require("./iterableToArray.js");
var unsupportedIterableToArray = require("./unsupportedIterableToArray.js");
var nonIterableSpread = require("./nonIterableSpread.js");
function _toConsumableArray(arr) {
    return arrayWithoutHoles(arr) || iterableToArray(arr) || unsupportedIterableToArray(arr) || nonIterableSpread();
}
module.exports = _toConsumableArray, module.exports.__esModule = true, module.exports["default"] = module.exports;

},{"./arrayWithoutHoles.js":"e90Sm","./iterableToArray.js":"a3Suu","./unsupportedIterableToArray.js":"1HHW3","./nonIterableSpread.js":"hd27P"}],"e90Sm":[function(require,module,exports) {
var _Array$isArray = require("@babel/runtime-corejs3/core-js/array/is-array");
var arrayLikeToArray = require("./arrayLikeToArray.js");
function _arrayWithoutHoles(arr) {
    if (_Array$isArray(arr)) return arrayLikeToArray(arr);
}
module.exports = _arrayWithoutHoles, module.exports.__esModule = true, module.exports["default"] = module.exports;

},{"@babel/runtime-corejs3/core-js/array/is-array":"NmEBJ","./arrayLikeToArray.js":"8tv1x"}],"a3Suu":[function(require,module,exports) {
var _Symbol = require("@babel/runtime-corejs3/core-js/symbol");
var _getIteratorMethod = require("@babel/runtime-corejs3/core-js/get-iterator-method");
var _Array$from = require("@babel/runtime-corejs3/core-js/array/from");
function _iterableToArray(iter) {
    if (typeof _Symbol !== "undefined" && _getIteratorMethod(iter) != null || iter["@@iterator"] != null) return _Array$from(iter);
}
module.exports = _iterableToArray, module.exports.__esModule = true, module.exports["default"] = module.exports;

},{"@babel/runtime-corejs3/core-js/symbol":"2n1d6","@babel/runtime-corejs3/core-js/get-iterator-method":"hNMZV","@babel/runtime-corejs3/core-js/array/from":"71yCE"}],"hd27P":[function(require,module,exports) {
function _nonIterableSpread() {
    throw new TypeError("Invalid attempt to spread non-iterable instance.\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method.");
}
module.exports = _nonIterableSpread, module.exports.__esModule = true, module.exports["default"] = module.exports;

},{}],"pNALB":[function(require,module,exports) {
'use strict';
var $ = require('../internals/export');
var exec = require('../internals/regexp-exec');
// `RegExp.prototype.exec` method
// https://tc39.es/ecma262/#sec-regexp.prototype.exec
$({
    target: 'RegExp',
    proto: true,
    forced: /./.exec !== exec
}, {
    exec: exec
});

},{"../internals/export":"dIGt4","../internals/regexp-exec":"1iqnJ"}],"dIGt4":[function(require,module,exports) {
var global = require('../internals/global');
var getOwnPropertyDescriptor = require('../internals/object-get-own-property-descriptor').f;
var createNonEnumerableProperty = require('../internals/create-non-enumerable-property');
var redefine = require('../internals/redefine');
var setGlobal = require('../internals/set-global');
var copyConstructorProperties = require('../internals/copy-constructor-properties');
var isForced = require('../internals/is-forced');
/*
  options.target      - name of the target object
  options.global      - target is the global object
  options.stat        - export as static methods of target
  options.proto       - export as prototype methods of target
  options.real        - real prototype method for the `pure` version
  options.forced      - export even if the native feature is available
  options.bind        - bind methods to the target, required for the `pure` version
  options.wrap        - wrap constructors to preventing global pollution, required for the `pure` version
  options.unsafe      - use the simple assignment of property instead of delete + defineProperty
  options.sham        - add a flag to not completely full polyfills
  options.enumerable  - export as enumerable property
  options.noTargetGet - prevent calling a getter on target
  options.name        - the .name of the function if it does not match the key
*/ module.exports = function(options, source) {
    var TARGET = options.target;
    var GLOBAL = options.global;
    var STATIC = options.stat;
    var FORCED, target, key, targetProperty, sourceProperty, descriptor;
    if (GLOBAL) target = global;
    else if (STATIC) target = global[TARGET] || setGlobal(TARGET, {});
    else target = (global[TARGET] || {}).prototype;
    if (target) for(key in source){
        sourceProperty = source[key];
        if (options.noTargetGet) {
            descriptor = getOwnPropertyDescriptor(target, key);
            targetProperty = descriptor && descriptor.value;
        } else targetProperty = target[key];
        FORCED = isForced(GLOBAL ? key : TARGET + (STATIC ? '.' : '#') + key, options.forced);
        // contained in target
        if (!FORCED && targetProperty !== undefined) {
            if (typeof sourceProperty === typeof targetProperty) continue;
            copyConstructorProperties(sourceProperty, targetProperty);
        }
        // add a flag to not completely full polyfills
        if (options.sham || targetProperty && targetProperty.sham) createNonEnumerableProperty(sourceProperty, 'sham', true);
        // extend global
        redefine(target, key, sourceProperty, options);
    }
};

},{"../internals/global":"i8HOC","../internals/object-get-own-property-descriptor":"lk5NI","../internals/create-non-enumerable-property":"8CL42","../internals/redefine":"1ZKnU","../internals/set-global":"ldqR5","../internals/copy-constructor-properties":"9Z12i","../internals/is-forced":"6uTCZ"}],"i8HOC":[function(require,module,exports) {
var global = arguments[3];
var check = function(it) {
    return it && it.Math == Math && it;
};
// https://github.com/zloirock/core-js/issues/86#issuecomment-115759028
module.exports = // eslint-disable-next-line es/no-global-this -- safe
check(typeof globalThis == 'object' && globalThis) || check(typeof window == 'object' && window) || // eslint-disable-next-line no-restricted-globals -- safe
check(typeof self == 'object' && self) || check(typeof global == 'object' && global) || // eslint-disable-next-line no-new-func -- fallback
function() {
    return this;
}() || Function('return this')();

},{}],"lk5NI":[function(require,module,exports) {
var DESCRIPTORS = require('../internals/descriptors');
var propertyIsEnumerableModule = require('../internals/object-property-is-enumerable');
var createPropertyDescriptor = require('../internals/create-property-descriptor');
var toIndexedObject = require('../internals/to-indexed-object');
var toPropertyKey = require('../internals/to-property-key');
var hasOwn = require('../internals/has-own-property');
var IE8_DOM_DEFINE = require('../internals/ie8-dom-define');
// eslint-disable-next-line es/no-object-getownpropertydescriptor -- safe
var $getOwnPropertyDescriptor = Object.getOwnPropertyDescriptor;
// `Object.getOwnPropertyDescriptor` method
// https://tc39.es/ecma262/#sec-object.getownpropertydescriptor
exports.f = DESCRIPTORS ? $getOwnPropertyDescriptor : function getOwnPropertyDescriptor(O, P) {
    O = toIndexedObject(O);
    P = toPropertyKey(P);
    if (IE8_DOM_DEFINE) try {
        return $getOwnPropertyDescriptor(O, P);
    } catch (error) {}
    if (hasOwn(O, P)) return createPropertyDescriptor(!propertyIsEnumerableModule.f.call(O, P), O[P]);
};

},{"../internals/descriptors":"92ZIi","../internals/object-property-is-enumerable":"7SuiS","../internals/create-property-descriptor":"1lpav","../internals/to-indexed-object":"jLWwQ","../internals/to-property-key":"5XWKd","../internals/has-own-property":"gC2Q5","../internals/ie8-dom-define":"qS9uN"}],"92ZIi":[function(require,module,exports) {
var fails = require('../internals/fails');
// Detect IE8's incomplete defineProperty implementation
module.exports = !fails(function() {
    // eslint-disable-next-line es/no-object-defineproperty -- required for testing
    return Object.defineProperty({}, 1, {
        get: function() {
            return 7;
        }
    })[1] != 7;
});

},{"../internals/fails":"hL6D2"}],"hL6D2":[function(require,module,exports) {
module.exports = function(exec) {
    try {
        return !!exec();
    } catch (error) {
        return true;
    }
};

},{}],"7SuiS":[function(require,module,exports) {
'use strict';
var $propertyIsEnumerable = {}.propertyIsEnumerable;
// eslint-disable-next-line es/no-object-getownpropertydescriptor -- safe
var getOwnPropertyDescriptor = Object.getOwnPropertyDescriptor;
// Nashorn ~ JDK8 bug
var NASHORN_BUG = getOwnPropertyDescriptor && !$propertyIsEnumerable.call({
    1: 2
}, 1);
// `Object.prototype.propertyIsEnumerable` method implementation
// https://tc39.es/ecma262/#sec-object.prototype.propertyisenumerable
exports.f = NASHORN_BUG ? function propertyIsEnumerable(V) {
    var descriptor = getOwnPropertyDescriptor(this, V);
    return !!descriptor && descriptor.enumerable;
} : $propertyIsEnumerable;

},{}],"1lpav":[function(require,module,exports) {
module.exports = function(bitmap, value) {
    return {
        enumerable: !(bitmap & 1),
        configurable: !(bitmap & 2),
        writable: !(bitmap & 4),
        value: value
    };
};

},{}],"jLWwQ":[function(require,module,exports) {
// toObject with fallback for non-array-like ES3 strings
var IndexedObject = require('../internals/indexed-object');
var requireObjectCoercible = require('../internals/require-object-coercible');
module.exports = function(it) {
    return IndexedObject(requireObjectCoercible(it));
};

},{"../internals/indexed-object":"kPk5h","../internals/require-object-coercible":"fOR0B"}],"kPk5h":[function(require,module,exports) {
var fails = require('../internals/fails');
var classof = require('../internals/classof-raw');
var split = ''.split;
// fallback for non-array-like ES3 and non-enumerable old V8 strings
module.exports = fails(function() {
    // throws an error in rhino, see https://github.com/mozilla/rhino/issues/346
    // eslint-disable-next-line no-prototype-builtins -- safe
    return !Object('z').propertyIsEnumerable(0);
}) ? function(it) {
    return classof(it) == 'String' ? split.call(it, '') : Object(it);
} : Object;

},{"../internals/fails":"hL6D2","../internals/classof-raw":"bdfmm"}],"bdfmm":[function(require,module,exports) {
var toString = {}.toString;
module.exports = function(it) {
    return toString.call(it).slice(8, -1);
};

},{}],"fOR0B":[function(require,module,exports) {
// `RequireObjectCoercible` abstract operation
// https://tc39.es/ecma262/#sec-requireobjectcoercible
module.exports = function(it) {
    if (it == undefined) throw TypeError("Can't call method on " + it);
    return it;
};

},{}],"5XWKd":[function(require,module,exports) {
var toPrimitive = require('../internals/to-primitive');
var isSymbol = require('../internals/is-symbol');
// `ToPropertyKey` abstract operation
// https://tc39.es/ecma262/#sec-topropertykey
module.exports = function(argument) {
    var key = toPrimitive(argument, 'string');
    return isSymbol(key) ? key : String(key);
};

},{"../internals/to-primitive":"a2mK1","../internals/is-symbol":"4aV4F"}],"a2mK1":[function(require,module,exports) {
var isObject = require('../internals/is-object');
var isSymbol = require('../internals/is-symbol');
var getMethod = require('../internals/get-method');
var ordinaryToPrimitive = require('../internals/ordinary-to-primitive');
var wellKnownSymbol = require('../internals/well-known-symbol');
var TO_PRIMITIVE = wellKnownSymbol('toPrimitive');
// `ToPrimitive` abstract operation
// https://tc39.es/ecma262/#sec-toprimitive
module.exports = function(input, pref) {
    if (!isObject(input) || isSymbol(input)) return input;
    var exoticToPrim = getMethod(input, TO_PRIMITIVE);
    var result;
    if (exoticToPrim) {
        if (pref === undefined) pref = 'default';
        result = exoticToPrim.call(input, pref);
        if (!isObject(result) || isSymbol(result)) return result;
        throw TypeError("Can't convert object to primitive value");
    }
    if (pref === undefined) pref = 'number';
    return ordinaryToPrimitive(input, pref);
};

},{"../internals/is-object":"Z0pBo","../internals/is-symbol":"4aV4F","../internals/get-method":"9Zfiw","../internals/ordinary-to-primitive":"7MME2","../internals/well-known-symbol":"gTwyA"}],"Z0pBo":[function(require,module,exports) {
var isCallable = require('../internals/is-callable');
module.exports = function(it) {
    return typeof it === 'object' ? it !== null : isCallable(it);
};

},{"../internals/is-callable":"l3Kyn"}],"l3Kyn":[function(require,module,exports) {
// `IsCallable` abstract operation
// https://tc39.es/ecma262/#sec-iscallable
module.exports = function(argument) {
    return typeof argument === 'function';
};

},{}],"4aV4F":[function(require,module,exports) {
var isCallable = require('../internals/is-callable');
var getBuiltIn = require('../internals/get-built-in');
var USE_SYMBOL_AS_UID = require('../internals/use-symbol-as-uid');
module.exports = USE_SYMBOL_AS_UID ? function(it) {
    return typeof it == 'symbol';
} : function(it) {
    var $Symbol = getBuiltIn('Symbol');
    return isCallable($Symbol) && Object(it) instanceof $Symbol;
};

},{"../internals/is-callable":"l3Kyn","../internals/get-built-in":"6ZUSY","../internals/use-symbol-as-uid":"2Ye8Q"}],"6ZUSY":[function(require,module,exports) {
var global = require('../internals/global');
var isCallable = require('../internals/is-callable');
var aFunction = function(argument) {
    return isCallable(argument) ? argument : undefined;
};
module.exports = function(namespace, method) {
    return arguments.length < 2 ? aFunction(global[namespace]) : global[namespace] && global[namespace][method];
};

},{"../internals/global":"i8HOC","../internals/is-callable":"l3Kyn"}],"2Ye8Q":[function(require,module,exports) {
/* eslint-disable es/no-symbol -- required for testing */ var NATIVE_SYMBOL = require('../internals/native-symbol');
module.exports = NATIVE_SYMBOL && !Symbol.sham && typeof Symbol.iterator == 'symbol';

},{"../internals/native-symbol":"grUXC"}],"grUXC":[function(require,module,exports) {
/* eslint-disable es/no-symbol -- required for testing */ var V8_VERSION = require('../internals/engine-v8-version');
var fails = require('../internals/fails');
// eslint-disable-next-line es/no-object-getownpropertysymbols -- required for testing
module.exports = !!Object.getOwnPropertySymbols && !fails(function() {
    var symbol = Symbol();
    // Chrome 38 Symbol has incorrect toString conversion
    // `get-own-property-symbols` polyfill symbols converted to object are not Symbol instances
    return !String(symbol) || !(Object(symbol) instanceof Symbol) || // Chrome 38-40 symbols are not inherited from DOM collections prototypes to instances
    !Symbol.sham && V8_VERSION && V8_VERSION < 41;
});

},{"../internals/engine-v8-version":"bjFlO","../internals/fails":"hL6D2"}],"bjFlO":[function(require,module,exports) {
var global = require('../internals/global');
var userAgent = require('../internals/engine-user-agent');
var process = global.process;
var Deno = global.Deno;
var versions = process && process.versions || Deno && Deno.version;
var v8 = versions && versions.v8;
var match, version;
if (v8) {
    match = v8.split('.');
    version = match[0] < 4 ? 1 : match[0] + match[1];
} else if (userAgent) {
    match = userAgent.match(/Edge\/(\d+)/);
    if (!match || match[1] >= 74) {
        match = userAgent.match(/Chrome\/(\d+)/);
        if (match) version = match[1];
    }
}
module.exports = version && +version;

},{"../internals/global":"i8HOC","../internals/engine-user-agent":"73xBt"}],"73xBt":[function(require,module,exports) {
var getBuiltIn = require('../internals/get-built-in');
module.exports = getBuiltIn('navigator', 'userAgent') || '';

},{"../internals/get-built-in":"6ZUSY"}],"9Zfiw":[function(require,module,exports) {
var aCallable = require('../internals/a-callable');
// `GetMethod` abstract operation
// https://tc39.es/ecma262/#sec-getmethod
module.exports = function(V, P) {
    var func = V[P];
    return func == null ? undefined : aCallable(func);
};

},{"../internals/a-callable":"gOMir"}],"gOMir":[function(require,module,exports) {
var isCallable = require('../internals/is-callable');
var tryToString = require('../internals/try-to-string');
// `Assert: IsCallable(argument) is true`
module.exports = function(argument) {
    if (isCallable(argument)) return argument;
    throw TypeError(tryToString(argument) + ' is not a function');
};

},{"../internals/is-callable":"l3Kyn","../internals/try-to-string":"4O7d7"}],"4O7d7":[function(require,module,exports) {
module.exports = function(argument) {
    try {
        return String(argument);
    } catch (error) {
        return 'Object';
    }
};

},{}],"7MME2":[function(require,module,exports) {
var isCallable = require('../internals/is-callable');
var isObject = require('../internals/is-object');
// `OrdinaryToPrimitive` abstract operation
// https://tc39.es/ecma262/#sec-ordinarytoprimitive
module.exports = function(input, pref) {
    var fn, val;
    if (pref === 'string' && isCallable(fn = input.toString) && !isObject(val = fn.call(input))) return val;
    if (isCallable(fn = input.valueOf) && !isObject(val = fn.call(input))) return val;
    if (pref !== 'string' && isCallable(fn = input.toString) && !isObject(val = fn.call(input))) return val;
    throw TypeError("Can't convert object to primitive value");
};

},{"../internals/is-callable":"l3Kyn","../internals/is-object":"Z0pBo"}],"gTwyA":[function(require,module,exports) {
var global = require('../internals/global');
var shared = require('../internals/shared');
var hasOwn = require('../internals/has-own-property');
var uid = require('../internals/uid');
var NATIVE_SYMBOL = require('../internals/native-symbol');
var USE_SYMBOL_AS_UID = require('../internals/use-symbol-as-uid');
var WellKnownSymbolsStore = shared('wks');
var Symbol = global.Symbol;
var createWellKnownSymbol = USE_SYMBOL_AS_UID ? Symbol : Symbol && Symbol.withoutSetter || uid;
module.exports = function(name) {
    if (!hasOwn(WellKnownSymbolsStore, name) || !(NATIVE_SYMBOL || typeof WellKnownSymbolsStore[name] == 'string')) {
        if (NATIVE_SYMBOL && hasOwn(Symbol, name)) WellKnownSymbolsStore[name] = Symbol[name];
        else WellKnownSymbolsStore[name] = createWellKnownSymbol('Symbol.' + name);
    }
    return WellKnownSymbolsStore[name];
};

},{"../internals/global":"i8HOC","../internals/shared":"i1mHK","../internals/has-own-property":"gC2Q5","../internals/uid":"a3SEE","../internals/native-symbol":"grUXC","../internals/use-symbol-as-uid":"2Ye8Q"}],"i1mHK":[function(require,module,exports) {
var IS_PURE = require('../internals/is-pure');
var store = require('../internals/shared-store');
(module.exports = function(key, value) {
    return store[key] || (store[key] = value !== undefined ? value : {});
})('versions', []).push({
    version: '3.18.3',
    mode: IS_PURE ? 'pure' : 'global',
    copyright: '© 2021 Denis Pushkarev (zloirock.ru)'
});

},{"../internals/is-pure":"5ZsyC","../internals/shared-store":"l4ncH"}],"5ZsyC":[function(require,module,exports) {
module.exports = false;

},{}],"l4ncH":[function(require,module,exports) {
var global = require('../internals/global');
var setGlobal = require('../internals/set-global');
var SHARED = '__core-js_shared__';
var store = global[SHARED] || setGlobal(SHARED, {});
module.exports = store;

},{"../internals/global":"i8HOC","../internals/set-global":"ldqR5"}],"ldqR5":[function(require,module,exports) {
var global = require('../internals/global');
module.exports = function(key, value) {
    try {
        // eslint-disable-next-line es/no-object-defineproperty -- safe
        Object.defineProperty(global, key, {
            value: value,
            configurable: true,
            writable: true
        });
    } catch (error) {
        global[key] = value;
    }
    return value;
};

},{"../internals/global":"i8HOC"}],"gC2Q5":[function(require,module,exports) {
var toObject = require('../internals/to-object');
var hasOwnProperty = {}.hasOwnProperty;
// `HasOwnProperty` abstract operation
// https://tc39.es/ecma262/#sec-hasownproperty
module.exports = Object.hasOwn || function hasOwn(it, key) {
    return hasOwnProperty.call(toObject(it), key);
};

},{"../internals/to-object":"5cb35"}],"5cb35":[function(require,module,exports) {
var requireObjectCoercible = require('../internals/require-object-coercible');
// `ToObject` abstract operation
// https://tc39.es/ecma262/#sec-toobject
module.exports = function(argument) {
    return Object(requireObjectCoercible(argument));
};

},{"../internals/require-object-coercible":"fOR0B"}],"a3SEE":[function(require,module,exports) {
var id = 0;
var postfix = Math.random();
module.exports = function(key) {
    return 'Symbol(' + String(key === undefined ? '' : key) + ')_' + (++id + postfix).toString(36);
};

},{}],"qS9uN":[function(require,module,exports) {
var DESCRIPTORS = require('../internals/descriptors');
var fails = require('../internals/fails');
var createElement = require('../internals/document-create-element');
// Thank's IE8 for his funny defineProperty
module.exports = !DESCRIPTORS && !fails(function() {
    // eslint-disable-next-line es/no-object-defineproperty -- requied for testing
    return Object.defineProperty(createElement('div'), 'a', {
        get: function() {
            return 7;
        }
    }).a != 7;
});

},{"../internals/descriptors":"92ZIi","../internals/fails":"hL6D2","../internals/document-create-element":"4bOHl"}],"4bOHl":[function(require,module,exports) {
var global = require('../internals/global');
var isObject = require('../internals/is-object');
var document = global.document;
// typeof document.createElement is 'object' in old IE
var EXISTS = isObject(document) && isObject(document.createElement);
module.exports = function(it) {
    return EXISTS ? document.createElement(it) : {};
};

},{"../internals/global":"i8HOC","../internals/is-object":"Z0pBo"}],"8CL42":[function(require,module,exports) {
var DESCRIPTORS = require('../internals/descriptors');
var definePropertyModule = require('../internals/object-define-property');
var createPropertyDescriptor = require('../internals/create-property-descriptor');
module.exports = DESCRIPTORS ? function(object, key, value) {
    return definePropertyModule.f(object, key, createPropertyDescriptor(1, value));
} : function(object, key, value) {
    object[key] = value;
    return object;
};

},{"../internals/descriptors":"92ZIi","../internals/object-define-property":"iJR4w","../internals/create-property-descriptor":"1lpav"}],"iJR4w":[function(require,module,exports) {
var DESCRIPTORS = require('../internals/descriptors');
var IE8_DOM_DEFINE = require('../internals/ie8-dom-define');
var anObject = require('../internals/an-object');
var toPropertyKey = require('../internals/to-property-key');
// eslint-disable-next-line es/no-object-defineproperty -- safe
var $defineProperty = Object.defineProperty;
// `Object.defineProperty` method
// https://tc39.es/ecma262/#sec-object.defineproperty
exports.f = DESCRIPTORS ? $defineProperty : function defineProperty(O, P, Attributes) {
    anObject(O);
    P = toPropertyKey(P);
    anObject(Attributes);
    if (IE8_DOM_DEFINE) try {
        return $defineProperty(O, P, Attributes);
    } catch (error) {}
    if ('get' in Attributes || 'set' in Attributes) throw TypeError('Accessors not supported');
    if ('value' in Attributes) O[P] = Attributes.value;
    return O;
};

},{"../internals/descriptors":"92ZIi","../internals/ie8-dom-define":"qS9uN","../internals/an-object":"4isCr","../internals/to-property-key":"5XWKd"}],"4isCr":[function(require,module,exports) {
var isObject = require('../internals/is-object');
// `Assert: Type(argument) is Object`
module.exports = function(argument) {
    if (isObject(argument)) return argument;
    throw TypeError(String(argument) + ' is not an object');
};

},{"../internals/is-object":"Z0pBo"}],"1ZKnU":[function(require,module,exports) {
var global = require('../internals/global');
var isCallable = require('../internals/is-callable');
var hasOwn = require('../internals/has-own-property');
var createNonEnumerableProperty = require('../internals/create-non-enumerable-property');
var setGlobal = require('../internals/set-global');
var inspectSource = require('../internals/inspect-source');
var InternalStateModule = require('../internals/internal-state');
var CONFIGURABLE_FUNCTION_NAME = require('../internals/function-name').CONFIGURABLE;
var getInternalState = InternalStateModule.get;
var enforceInternalState = InternalStateModule.enforce;
var TEMPLATE = String(String).split('String');
(module.exports = function(O, key, value, options) {
    var unsafe = options ? !!options.unsafe : false;
    var simple = options ? !!options.enumerable : false;
    var noTargetGet = options ? !!options.noTargetGet : false;
    var name = options && options.name !== undefined ? options.name : key;
    var state;
    if (isCallable(value)) {
        if (String(name).slice(0, 7) === 'Symbol(') name = '[' + String(name).replace(/^Symbol\(([^)]*)\)/, '$1') + ']';
        if (!hasOwn(value, 'name') || CONFIGURABLE_FUNCTION_NAME && value.name !== name) createNonEnumerableProperty(value, 'name', name);
        state = enforceInternalState(value);
        if (!state.source) state.source = TEMPLATE.join(typeof name == 'string' ? name : '');
    }
    if (O === global) {
        if (simple) O[key] = value;
        else setGlobal(key, value);
        return;
    } else if (!unsafe) delete O[key];
    else if (!noTargetGet && O[key]) simple = true;
    if (simple) O[key] = value;
    else createNonEnumerableProperty(O, key, value);
// add fake Function#toString for correct work wrapped methods / constructors with methods like LoDash isNative
})(Function.prototype, 'toString', function toString() {
    return isCallable(this) && getInternalState(this).source || inspectSource(this);
});

},{"../internals/global":"i8HOC","../internals/is-callable":"l3Kyn","../internals/has-own-property":"gC2Q5","../internals/create-non-enumerable-property":"8CL42","../internals/set-global":"ldqR5","../internals/inspect-source":"9jh7O","../internals/internal-state":"7AMlF","../internals/function-name":"l6Kgd"}],"9jh7O":[function(require,module,exports) {
var isCallable = require('../internals/is-callable');
var store = require('../internals/shared-store');
var functionToString = Function.toString;
// this helper broken in `core-js@3.4.1-3.4.4`, so we can't use `shared` helper
if (!isCallable(store.inspectSource)) store.inspectSource = function(it) {
    return functionToString.call(it);
};
module.exports = store.inspectSource;

},{"../internals/is-callable":"l3Kyn","../internals/shared-store":"l4ncH"}],"7AMlF":[function(require,module,exports) {
var NATIVE_WEAK_MAP = require('../internals/native-weak-map');
var global = require('../internals/global');
var isObject = require('../internals/is-object');
var createNonEnumerableProperty = require('../internals/create-non-enumerable-property');
var hasOwn = require('../internals/has-own-property');
var shared = require('../internals/shared-store');
var sharedKey = require('../internals/shared-key');
var hiddenKeys = require('../internals/hidden-keys');
var OBJECT_ALREADY_INITIALIZED = 'Object already initialized';
var WeakMap = global.WeakMap;
var set, get, has;
var enforce = function(it) {
    return has(it) ? get(it) : set(it, {});
};
var getterFor = function(TYPE) {
    return function(it) {
        var state;
        if (!isObject(it) || (state = get(it)).type !== TYPE) throw TypeError('Incompatible receiver, ' + TYPE + ' required');
        return state;
    };
};
if (NATIVE_WEAK_MAP || shared.state) {
    var store = shared.state || (shared.state = new WeakMap());
    var wmget = store.get;
    var wmhas = store.has;
    var wmset = store.set;
    set = function(it, metadata) {
        if (wmhas.call(store, it)) throw new TypeError(OBJECT_ALREADY_INITIALIZED);
        metadata.facade = it;
        wmset.call(store, it, metadata);
        return metadata;
    };
    get = function(it) {
        return wmget.call(store, it) || {};
    };
    has = function(it) {
        return wmhas.call(store, it);
    };
} else {
    var STATE = sharedKey('state');
    hiddenKeys[STATE] = true;
    set = function(it, metadata) {
        if (hasOwn(it, STATE)) throw new TypeError(OBJECT_ALREADY_INITIALIZED);
        metadata.facade = it;
        createNonEnumerableProperty(it, STATE, metadata);
        return metadata;
    };
    get = function(it) {
        return hasOwn(it, STATE) ? it[STATE] : {};
    };
    has = function(it) {
        return hasOwn(it, STATE);
    };
}
module.exports = {
    set: set,
    get: get,
    has: has,
    enforce: enforce,
    getterFor: getterFor
};

},{"../internals/native-weak-map":"7LdJl","../internals/global":"i8HOC","../internals/is-object":"Z0pBo","../internals/create-non-enumerable-property":"8CL42","../internals/has-own-property":"gC2Q5","../internals/shared-store":"l4ncH","../internals/shared-key":"eAjGz","../internals/hidden-keys":"661m4"}],"7LdJl":[function(require,module,exports) {
var global = require('../internals/global');
var isCallable = require('../internals/is-callable');
var inspectSource = require('../internals/inspect-source');
var WeakMap = global.WeakMap;
module.exports = isCallable(WeakMap) && /native code/.test(inspectSource(WeakMap));

},{"../internals/global":"i8HOC","../internals/is-callable":"l3Kyn","../internals/inspect-source":"9jh7O"}],"eAjGz":[function(require,module,exports) {
var shared = require('../internals/shared');
var uid = require('../internals/uid');
var keys = shared('keys');
module.exports = function(key) {
    return keys[key] || (keys[key] = uid(key));
};

},{"../internals/shared":"i1mHK","../internals/uid":"a3SEE"}],"661m4":[function(require,module,exports) {
module.exports = {};

},{}],"l6Kgd":[function(require,module,exports) {
var DESCRIPTORS = require('../internals/descriptors');
var hasOwn = require('../internals/has-own-property');
var FunctionPrototype = Function.prototype;
// eslint-disable-next-line es/no-object-getownpropertydescriptor -- safe
var getDescriptor = DESCRIPTORS && Object.getOwnPropertyDescriptor;
var EXISTS = hasOwn(FunctionPrototype, 'name');
// additional protection from minified / mangled / dropped function names
var PROPER = EXISTS && (function something() {}).name === 'something';
var CONFIGURABLE = EXISTS && (!DESCRIPTORS || DESCRIPTORS && getDescriptor(FunctionPrototype, 'name').configurable);
module.exports = {
    EXISTS: EXISTS,
    PROPER: PROPER,
    CONFIGURABLE: CONFIGURABLE
};

},{"../internals/descriptors":"92ZIi","../internals/has-own-property":"gC2Q5"}],"9Z12i":[function(require,module,exports) {
var hasOwn = require('../internals/has-own-property');
var ownKeys = require('../internals/own-keys');
var getOwnPropertyDescriptorModule = require('../internals/object-get-own-property-descriptor');
var definePropertyModule = require('../internals/object-define-property');
module.exports = function(target, source) {
    var keys = ownKeys(source);
    var defineProperty = definePropertyModule.f;
    var getOwnPropertyDescriptor = getOwnPropertyDescriptorModule.f;
    for(var i = 0; i < keys.length; i++){
        var key = keys[i];
        if (!hasOwn(target, key)) defineProperty(target, key, getOwnPropertyDescriptor(source, key));
    }
};

},{"../internals/has-own-property":"gC2Q5","../internals/own-keys":"1CX1A","../internals/object-get-own-property-descriptor":"lk5NI","../internals/object-define-property":"iJR4w"}],"1CX1A":[function(require,module,exports) {
var getBuiltIn = require('../internals/get-built-in');
var getOwnPropertyNamesModule = require('../internals/object-get-own-property-names');
var getOwnPropertySymbolsModule = require('../internals/object-get-own-property-symbols');
var anObject = require('../internals/an-object');
// all object keys, includes non-enumerable and symbols
module.exports = getBuiltIn('Reflect', 'ownKeys') || function ownKeys(it) {
    var keys = getOwnPropertyNamesModule.f(anObject(it));
    var getOwnPropertySymbols = getOwnPropertySymbolsModule.f;
    return getOwnPropertySymbols ? keys.concat(getOwnPropertySymbols(it)) : keys;
};

},{"../internals/get-built-in":"6ZUSY","../internals/object-get-own-property-names":"fjY04","../internals/object-get-own-property-symbols":"4DWO3","../internals/an-object":"4isCr"}],"fjY04":[function(require,module,exports) {
var internalObjectKeys = require('../internals/object-keys-internal');
var enumBugKeys = require('../internals/enum-bug-keys');
var hiddenKeys = enumBugKeys.concat('length', 'prototype');
// `Object.getOwnPropertyNames` method
// https://tc39.es/ecma262/#sec-object.getownpropertynames
// eslint-disable-next-line es/no-object-getownpropertynames -- safe
exports.f = Object.getOwnPropertyNames || function getOwnPropertyNames(O) {
    return internalObjectKeys(O, hiddenKeys);
};

},{"../internals/object-keys-internal":"hl5T1","../internals/enum-bug-keys":"9RnJm"}],"hl5T1":[function(require,module,exports) {
var hasOwn = require('../internals/has-own-property');
var toIndexedObject = require('../internals/to-indexed-object');
var indexOf = require('../internals/array-includes').indexOf;
var hiddenKeys = require('../internals/hidden-keys');
module.exports = function(object, names) {
    var O = toIndexedObject(object);
    var i = 0;
    var result = [];
    var key;
    for(key in O)!hasOwn(hiddenKeys, key) && hasOwn(O, key) && result.push(key);
    // Don't enum bug & hidden keys
    while(names.length > i)if (hasOwn(O, key = names[i++])) ~indexOf(result, key) || result.push(key);
    return result;
};

},{"../internals/has-own-property":"gC2Q5","../internals/to-indexed-object":"jLWwQ","../internals/array-includes":"n5IsC","../internals/hidden-keys":"661m4"}],"n5IsC":[function(require,module,exports) {
var toIndexedObject = require('../internals/to-indexed-object');
var toAbsoluteIndex = require('../internals/to-absolute-index');
var lengthOfArrayLike = require('../internals/length-of-array-like');
// `Array.prototype.{ indexOf, includes }` methods implementation
var createMethod = function(IS_INCLUDES) {
    return function($this, el, fromIndex) {
        var O = toIndexedObject($this);
        var length = lengthOfArrayLike(O);
        var index = toAbsoluteIndex(fromIndex, length);
        var value;
        // Array#includes uses SameValueZero equality algorithm
        // eslint-disable-next-line no-self-compare -- NaN check
        if (IS_INCLUDES && el != el) while(length > index){
            value = O[index++];
            // eslint-disable-next-line no-self-compare -- NaN check
            if (value != value) return true;
        // Array#indexOf ignores holes, Array#includes - not
        }
        else for(; length > index; index++){
            if ((IS_INCLUDES || index in O) && O[index] === el) return IS_INCLUDES || index || 0;
        }
        return !IS_INCLUDES && -1;
    };
};
module.exports = {
    // `Array.prototype.includes` method
    // https://tc39.es/ecma262/#sec-array.prototype.includes
    includes: createMethod(true),
    // `Array.prototype.indexOf` method
    // https://tc39.es/ecma262/#sec-array.prototype.indexof
    indexOf: createMethod(false)
};

},{"../internals/to-indexed-object":"jLWwQ","../internals/to-absolute-index":"5yh27","../internals/length-of-array-like":"lY4mS"}],"5yh27":[function(require,module,exports) {
var toIntegerOrInfinity = require('../internals/to-integer-or-infinity');
var max = Math.max;
var min = Math.min;
// Helper for a popular repeating case of the spec:
// Let integer be ? ToInteger(index).
// If integer < 0, let result be max((length + integer), 0); else let result be min(integer, length).
module.exports = function(index, length) {
    var integer = toIntegerOrInfinity(index);
    return integer < 0 ? max(integer + length, 0) : min(integer, length);
};

},{"../internals/to-integer-or-infinity":"kLXGe"}],"kLXGe":[function(require,module,exports) {
var ceil = Math.ceil;
var floor = Math.floor;
// `ToIntegerOrInfinity` abstract operation
// https://tc39.es/ecma262/#sec-tointegerorinfinity
module.exports = function(argument) {
    var number = +argument;
    // eslint-disable-next-line no-self-compare -- safe
    return number !== number || number === 0 ? 0 : (number > 0 ? floor : ceil)(number);
};

},{}],"lY4mS":[function(require,module,exports) {
var toLength = require('../internals/to-length');
// `LengthOfArrayLike` abstract operation
// https://tc39.es/ecma262/#sec-lengthofarraylike
module.exports = function(obj) {
    return toLength(obj.length);
};

},{"../internals/to-length":"fU04N"}],"fU04N":[function(require,module,exports) {
var toIntegerOrInfinity = require('../internals/to-integer-or-infinity');
var min = Math.min;
// `ToLength` abstract operation
// https://tc39.es/ecma262/#sec-tolength
module.exports = function(argument) {
    return argument > 0 ? min(toIntegerOrInfinity(argument), 0x1FFFFFFFFFFFFF) : 0; // 2 ** 53 - 1 == 9007199254740991
};

},{"../internals/to-integer-or-infinity":"kLXGe"}],"9RnJm":[function(require,module,exports) {
// IE8- don't enum bug keys
module.exports = [
    'constructor',
    'hasOwnProperty',
    'isPrototypeOf',
    'propertyIsEnumerable',
    'toLocaleString',
    'toString',
    'valueOf'
];

},{}],"4DWO3":[function(require,module,exports) {
// eslint-disable-next-line es/no-object-getownpropertysymbols -- safe
exports.f = Object.getOwnPropertySymbols;

},{}],"6uTCZ":[function(require,module,exports) {
var fails = require('../internals/fails');
var isCallable = require('../internals/is-callable');
var replacement = /#|\.prototype\./;
var isForced = function(feature, detection) {
    var value = data[normalize(feature)];
    return value == POLYFILL ? true : value == NATIVE ? false : isCallable(detection) ? fails(detection) : !!detection;
};
var normalize = isForced.normalize = function(string) {
    return String(string).replace(replacement, '.').toLowerCase();
};
var data = isForced.data = {};
var NATIVE = isForced.NATIVE = 'N';
var POLYFILL = isForced.POLYFILL = 'P';
module.exports = isForced;

},{"../internals/fails":"hL6D2","../internals/is-callable":"l3Kyn"}],"1iqnJ":[function(require,module,exports) {
'use strict';
/* eslint-disable regexp/no-empty-capturing-group, regexp/no-empty-group, regexp/no-lazy-ends -- testing */ /* eslint-disable regexp/no-useless-quantifier -- testing */ var toString = require('../internals/to-string');
var regexpFlags = require('../internals/regexp-flags');
var stickyHelpers = require('../internals/regexp-sticky-helpers');
var shared = require('../internals/shared');
var create = require('../internals/object-create');
var getInternalState = require('../internals/internal-state').get;
var UNSUPPORTED_DOT_ALL = require('../internals/regexp-unsupported-dot-all');
var UNSUPPORTED_NCG = require('../internals/regexp-unsupported-ncg');
var nativeExec = RegExp.prototype.exec;
var nativeReplace = shared('native-string-replace', String.prototype.replace);
var patchedExec = nativeExec;
var UPDATES_LAST_INDEX_WRONG = function() {
    var re1 = /a/;
    var re2 = /b*/g;
    nativeExec.call(re1, 'a');
    nativeExec.call(re2, 'a');
    return re1.lastIndex !== 0 || re2.lastIndex !== 0;
}();
var UNSUPPORTED_Y = stickyHelpers.UNSUPPORTED_Y || stickyHelpers.BROKEN_CARET;
// nonparticipating capturing group, copied from es5-shim's String#split patch.
var NPCG_INCLUDED = /()??/.exec('')[1] !== undefined;
var PATCH = UPDATES_LAST_INDEX_WRONG || NPCG_INCLUDED || UNSUPPORTED_Y || UNSUPPORTED_DOT_ALL || UNSUPPORTED_NCG;
if (PATCH) // eslint-disable-next-line max-statements -- TODO
patchedExec = function exec(string) {
    var re = this;
    var state = getInternalState(re);
    var str = toString(string);
    var raw = state.raw;
    var result, reCopy, lastIndex, match, i, object, group;
    if (raw) {
        raw.lastIndex = re.lastIndex;
        result = patchedExec.call(raw, str);
        re.lastIndex = raw.lastIndex;
        return result;
    }
    var groups = state.groups;
    var sticky = UNSUPPORTED_Y && re.sticky;
    var flags = regexpFlags.call(re);
    var source = re.source;
    var charsAdded = 0;
    var strCopy = str;
    if (sticky) {
        flags = flags.replace('y', '');
        if (flags.indexOf('g') === -1) flags += 'g';
        strCopy = str.slice(re.lastIndex);
        // Support anchored sticky behavior.
        if (re.lastIndex > 0 && (!re.multiline || re.multiline && str.charAt(re.lastIndex - 1) !== '\n')) {
            source = '(?: ' + source + ')';
            strCopy = ' ' + strCopy;
            charsAdded++;
        }
        // ^(? + rx + ) is needed, in combination with some str slicing, to
        // simulate the 'y' flag.
        reCopy = new RegExp('^(?:' + source + ')', flags);
    }
    if (NPCG_INCLUDED) reCopy = new RegExp('^' + source + '$(?!\\s)', flags);
    if (UPDATES_LAST_INDEX_WRONG) lastIndex = re.lastIndex;
    match = nativeExec.call(sticky ? reCopy : re, strCopy);
    if (sticky) {
        if (match) {
            match.input = match.input.slice(charsAdded);
            match[0] = match[0].slice(charsAdded);
            match.index = re.lastIndex;
            re.lastIndex += match[0].length;
        } else re.lastIndex = 0;
    } else if (UPDATES_LAST_INDEX_WRONG && match) re.lastIndex = re.global ? match.index + match[0].length : lastIndex;
    if (NPCG_INCLUDED && match && match.length > 1) // Fix browsers whose `exec` methods don't consistently return `undefined`
    // for NPCG, like IE8. NOTE: This doesn' work for /(.?)?/
    nativeReplace.call(match[0], reCopy, function() {
        for(i = 1; i < arguments.length - 2; i++)if (arguments[i] === undefined) match[i] = undefined;
    });
    if (match && groups) {
        match.groups = object = create(null);
        for(i = 0; i < groups.length; i++){
            group = groups[i];
            object[group[0]] = match[group[1]];
        }
    }
    return match;
};
module.exports = patchedExec;

},{"../internals/to-string":"a1yl4","../internals/regexp-flags":"9bz1x","../internals/regexp-sticky-helpers":"dG6kl","../internals/shared":"i1mHK","../internals/object-create":"duSQE","../internals/internal-state":"7AMlF","../internals/regexp-unsupported-dot-all":"7w3XA","../internals/regexp-unsupported-ncg":"j2d9g"}],"a1yl4":[function(require,module,exports) {
var classof = require('../internals/classof');
module.exports = function(argument) {
    if (classof(argument) === 'Symbol') throw TypeError('Cannot convert a Symbol value to a string');
    return String(argument);
};

},{"../internals/classof":"dKT7A"}],"dKT7A":[function(require,module,exports) {
var TO_STRING_TAG_SUPPORT = require('../internals/to-string-tag-support');
var isCallable = require('../internals/is-callable');
var classofRaw = require('../internals/classof-raw');
var wellKnownSymbol = require('../internals/well-known-symbol');
var TO_STRING_TAG = wellKnownSymbol('toStringTag');
// ES3 wrong here
var CORRECT_ARGUMENTS = classofRaw(function() {
    return arguments;
}()) == 'Arguments';
// fallback for IE11 Script Access Denied error
var tryGet = function(it, key) {
    try {
        return it[key];
    } catch (error) {}
};
// getting tag from ES6+ `Object.prototype.toString`
module.exports = TO_STRING_TAG_SUPPORT ? classofRaw : function(it) {
    var O, tag, result;
    return it === undefined ? 'Undefined' : it === null ? 'Null' : typeof (tag = tryGet(O = Object(it), TO_STRING_TAG)) == 'string' ? tag : CORRECT_ARGUMENTS ? classofRaw(O) : (result = classofRaw(O)) == 'Object' && isCallable(O.callee) ? 'Arguments' : result;
};

},{"../internals/to-string-tag-support":"3Do6Z","../internals/is-callable":"l3Kyn","../internals/classof-raw":"bdfmm","../internals/well-known-symbol":"gTwyA"}],"3Do6Z":[function(require,module,exports) {
var wellKnownSymbol = require('../internals/well-known-symbol');
var TO_STRING_TAG = wellKnownSymbol('toStringTag');
var test = {};
test[TO_STRING_TAG] = 'z';
module.exports = String(test) === '[object z]';

},{"../internals/well-known-symbol":"gTwyA"}],"9bz1x":[function(require,module,exports) {
'use strict';
var anObject = require('../internals/an-object');
// `RegExp.prototype.flags` getter implementation
// https://tc39.es/ecma262/#sec-get-regexp.prototype.flags
module.exports = function() {
    var that = anObject(this);
    var result = '';
    if (that.global) result += 'g';
    if (that.ignoreCase) result += 'i';
    if (that.multiline) result += 'm';
    if (that.dotAll) result += 's';
    if (that.unicode) result += 'u';
    if (that.sticky) result += 'y';
    return result;
};

},{"../internals/an-object":"4isCr"}],"dG6kl":[function(require,module,exports) {
var fails = require('../internals/fails');
var global = require('../internals/global');
// babel-minify and Closure Compiler transpiles RegExp('a', 'y') -> /a/y and it causes SyntaxError
var $RegExp = global.RegExp;
exports.UNSUPPORTED_Y = fails(function() {
    var re = $RegExp('a', 'y');
    re.lastIndex = 2;
    return re.exec('abcd') != null;
});
exports.BROKEN_CARET = fails(function() {
    // https://bugzilla.mozilla.org/show_bug.cgi?id=773687
    var re = $RegExp('^r', 'gy');
    re.lastIndex = 2;
    return re.exec('str') != null;
});

},{"../internals/fails":"hL6D2","../internals/global":"i8HOC"}],"duSQE":[function(require,module,exports) {
/* global ActiveXObject -- old IE, WSH */ var anObject = require('../internals/an-object');
var defineProperties = require('../internals/object-define-properties');
var enumBugKeys = require('../internals/enum-bug-keys');
var hiddenKeys = require('../internals/hidden-keys');
var html = require('../internals/html');
var documentCreateElement = require('../internals/document-create-element');
var sharedKey = require('../internals/shared-key');
var GT = '>';
var LT = '<';
var PROTOTYPE = 'prototype';
var SCRIPT = 'script';
var IE_PROTO = sharedKey('IE_PROTO');
var EmptyConstructor = function() {};
var scriptTag = function(content) {
    return LT + SCRIPT + GT + content + LT + '/' + SCRIPT + GT;
};
// Create object with fake `null` prototype: use ActiveX Object with cleared prototype
var NullProtoObjectViaActiveX = function(activeXDocument1) {
    activeXDocument1.write(scriptTag(''));
    activeXDocument1.close();
    var temp = activeXDocument1.parentWindow.Object;
    activeXDocument1 = null; // avoid memory leak
    return temp;
};
// Create object with fake `null` prototype: use iframe Object with cleared prototype
var NullProtoObjectViaIFrame = function() {
    // Thrash, waste and sodomy: IE GC bug
    var iframe = documentCreateElement('iframe');
    var JS = 'java' + SCRIPT + ':';
    var iframeDocument;
    iframe.style.display = 'none';
    html.appendChild(iframe);
    // https://github.com/zloirock/core-js/issues/475
    iframe.src = String(JS);
    iframeDocument = iframe.contentWindow.document;
    iframeDocument.open();
    iframeDocument.write(scriptTag('document.F=Object'));
    iframeDocument.close();
    return iframeDocument.F;
};
// Check for document.domain and active x support
// No need to use active x approach when document.domain is not set
// see https://github.com/es-shims/es5-shim/issues/150
// variation of https://github.com/kitcambridge/es5-shim/commit/4f738ac066346
// avoid IE GC bug
var activeXDocument;
var NullProtoObject = function() {
    try {
        activeXDocument = new ActiveXObject('htmlfile');
    } catch (error) {}
    NullProtoObject = typeof document != 'undefined' ? document.domain && activeXDocument ? NullProtoObjectViaActiveX(activeXDocument) // old IE
     : NullProtoObjectViaIFrame() : NullProtoObjectViaActiveX(activeXDocument); // WSH
    var length = enumBugKeys.length;
    while(length--)delete NullProtoObject[PROTOTYPE][enumBugKeys[length]];
    return NullProtoObject();
};
hiddenKeys[IE_PROTO] = true;
// `Object.create` method
// https://tc39.es/ecma262/#sec-object.create
module.exports = Object.create || function create(O, Properties) {
    var result;
    if (O !== null) {
        EmptyConstructor[PROTOTYPE] = anObject(O);
        result = new EmptyConstructor();
        EmptyConstructor[PROTOTYPE] = null;
        // add "__proto__" for Object.getPrototypeOf polyfill
        result[IE_PROTO] = O;
    } else result = NullProtoObject();
    return Properties === undefined ? result : defineProperties(result, Properties);
};

},{"../internals/an-object":"4isCr","../internals/object-define-properties":"duA6W","../internals/enum-bug-keys":"9RnJm","../internals/hidden-keys":"661m4","../internals/html":"2pze4","../internals/document-create-element":"4bOHl","../internals/shared-key":"eAjGz"}],"duA6W":[function(require,module,exports) {
var DESCRIPTORS = require('../internals/descriptors');
var definePropertyModule = require('../internals/object-define-property');
var anObject = require('../internals/an-object');
var objectKeys = require('../internals/object-keys');
// `Object.defineProperties` method
// https://tc39.es/ecma262/#sec-object.defineproperties
// eslint-disable-next-line es/no-object-defineproperties -- safe
module.exports = DESCRIPTORS ? Object.defineProperties : function defineProperties(O, Properties) {
    anObject(O);
    var keys = objectKeys(Properties);
    var length = keys.length;
    var index = 0;
    var key;
    while(length > index)definePropertyModule.f(O, key = keys[index++], Properties[key]);
    return O;
};

},{"../internals/descriptors":"92ZIi","../internals/object-define-property":"iJR4w","../internals/an-object":"4isCr","../internals/object-keys":"kzBf4"}],"kzBf4":[function(require,module,exports) {
var internalObjectKeys = require('../internals/object-keys-internal');
var enumBugKeys = require('../internals/enum-bug-keys');
// `Object.keys` method
// https://tc39.es/ecma262/#sec-object.keys
// eslint-disable-next-line es/no-object-keys -- safe
module.exports = Object.keys || function keys(O) {
    return internalObjectKeys(O, enumBugKeys);
};

},{"../internals/object-keys-internal":"hl5T1","../internals/enum-bug-keys":"9RnJm"}],"2pze4":[function(require,module,exports) {
var getBuiltIn = require('../internals/get-built-in');
module.exports = getBuiltIn('document', 'documentElement');

},{"../internals/get-built-in":"6ZUSY"}],"7w3XA":[function(require,module,exports) {
var fails = require('../internals/fails');
var global = require('../internals/global');
// babel-minify and Closure Compiler transpiles RegExp('.', 's') -> /./s and it causes SyntaxError
var $RegExp = global.RegExp;
module.exports = fails(function() {
    var re = $RegExp('.', 's');
    return !(re.dotAll && re.exec('\n') && re.flags === 's');
});

},{"../internals/fails":"hL6D2","../internals/global":"i8HOC"}],"j2d9g":[function(require,module,exports) {
var fails = require('../internals/fails');
var global = require('../internals/global');
// babel-minify and Closure Compiler transpiles RegExp('(?<a>b)', 'g') -> /(?<a>b)/g and it causes SyntaxError
var $RegExp = global.RegExp;
module.exports = fails(function() {
    var re = $RegExp('(?<a>b)', 'g');
    return re.exec('b').groups.a !== 'b' || 'b'.replace(re, '$<a>c') !== 'bc';
});

},{"../internals/fails":"hL6D2","../internals/global":"i8HOC"}],"bE0gl":[function(require,module,exports) {
'use strict';
var fixRegExpWellKnownSymbolLogic = require('../internals/fix-regexp-well-known-symbol-logic');
var fails = require('../internals/fails');
var anObject = require('../internals/an-object');
var isCallable = require('../internals/is-callable');
var toIntegerOrInfinity = require('../internals/to-integer-or-infinity');
var toLength = require('../internals/to-length');
var toString = require('../internals/to-string');
var requireObjectCoercible = require('../internals/require-object-coercible');
var advanceStringIndex = require('../internals/advance-string-index');
var getMethod = require('../internals/get-method');
var getSubstitution = require('../internals/get-substitution');
var regExpExec = require('../internals/regexp-exec-abstract');
var wellKnownSymbol = require('../internals/well-known-symbol');
var REPLACE = wellKnownSymbol('replace');
var max = Math.max;
var min = Math.min;
var maybeToString = function(it) {
    return it === undefined ? it : String(it);
};
// IE <= 11 replaces $0 with the whole match, as if it was $&
// https://stackoverflow.com/questions/6024666/getting-ie-to-replace-a-regex-with-the-literal-string-0
var REPLACE_KEEPS_$0 = function() {
    // eslint-disable-next-line regexp/prefer-escape-replacement-dollar-char -- required for testing
    return 'a'.replace(/./, '$0') === '$0';
}();
// Safari <= 13.0.3(?) substitutes nth capture where n>m with an empty string
var REGEXP_REPLACE_SUBSTITUTES_UNDEFINED_CAPTURE = function() {
    if (/./[REPLACE]) return /./[REPLACE]('a', '$0') === '';
    return false;
}();
var REPLACE_SUPPORTS_NAMED_GROUPS = !fails(function() {
    var re = /./;
    re.exec = function() {
        var result = [];
        result.groups = {
            a: '7'
        };
        return result;
    };
    // eslint-disable-next-line regexp/no-useless-dollar-replacements -- false positive
    return ''.replace(re, '$<a>') !== '7';
});
// @@replace logic
fixRegExpWellKnownSymbolLogic('replace', function(_, nativeReplace, maybeCallNative) {
    var UNSAFE_SUBSTITUTE = REGEXP_REPLACE_SUBSTITUTES_UNDEFINED_CAPTURE ? '$' : '$0';
    return [
        // `String.prototype.replace` method
        // https://tc39.es/ecma262/#sec-string.prototype.replace
        function replace(searchValue, replaceValue) {
            var O = requireObjectCoercible(this);
            var replacer = searchValue == undefined ? undefined : getMethod(searchValue, REPLACE);
            return replacer ? replacer.call(searchValue, O, replaceValue) : nativeReplace.call(toString(O), searchValue, replaceValue);
        },
        // `RegExp.prototype[@@replace]` method
        // https://tc39.es/ecma262/#sec-regexp.prototype-@@replace
        function(string, replaceValue) {
            var rx = anObject(this);
            var S = toString(string);
            if (typeof replaceValue === 'string' && replaceValue.indexOf(UNSAFE_SUBSTITUTE) === -1 && replaceValue.indexOf('$<') === -1) {
                var res = maybeCallNative(nativeReplace, rx, S, replaceValue);
                if (res.done) return res.value;
            }
            var functionalReplace = isCallable(replaceValue);
            if (!functionalReplace) replaceValue = toString(replaceValue);
            var global = rx.global;
            if (global) {
                var fullUnicode = rx.unicode;
                rx.lastIndex = 0;
            }
            var results = [];
            while(true){
                var result = regExpExec(rx, S);
                if (result === null) break;
                results.push(result);
                if (!global) break;
                var matchStr = toString(result[0]);
                if (matchStr === '') rx.lastIndex = advanceStringIndex(S, toLength(rx.lastIndex), fullUnicode);
            }
            var accumulatedResult = '';
            var nextSourcePosition = 0;
            for(var i = 0; i < results.length; i++){
                result = results[i];
                var matched = toString(result[0]);
                var position = max(min(toIntegerOrInfinity(result.index), S.length), 0);
                var captures = [];
                // NOTE: This is equivalent to
                //   captures = result.slice(1).map(maybeToString)
                // but for some reason `nativeSlice.call(result, 1, result.length)` (called in
                // the slice polyfill when slicing native arrays) "doesn't work" in safari 9 and
                // causes a crash (https://pastebin.com/N21QzeQA) when trying to debug it.
                for(var j = 1; j < result.length; j++)captures.push(maybeToString(result[j]));
                var namedCaptures = result.groups;
                if (functionalReplace) {
                    var replacerArgs = [
                        matched
                    ].concat(captures, position, S);
                    if (namedCaptures !== undefined) replacerArgs.push(namedCaptures);
                    var replacement = toString(replaceValue.apply(undefined, replacerArgs));
                } else replacement = getSubstitution(matched, S, position, captures, namedCaptures, replaceValue);
                if (position >= nextSourcePosition) {
                    accumulatedResult += S.slice(nextSourcePosition, position) + replacement;
                    nextSourcePosition = position + matched.length;
                }
            }
            return accumulatedResult + S.slice(nextSourcePosition);
        }
    ];
}, !REPLACE_SUPPORTS_NAMED_GROUPS || !REPLACE_KEEPS_$0 || REGEXP_REPLACE_SUBSTITUTES_UNDEFINED_CAPTURE);

},{"../internals/fix-regexp-well-known-symbol-logic":"82MZ8","../internals/fails":"hL6D2","../internals/an-object":"4isCr","../internals/is-callable":"l3Kyn","../internals/to-integer-or-infinity":"kLXGe","../internals/to-length":"fU04N","../internals/to-string":"a1yl4","../internals/require-object-coercible":"fOR0B","../internals/advance-string-index":"hEviL","../internals/get-method":"9Zfiw","../internals/get-substitution":"1vLvA","../internals/regexp-exec-abstract":"5jYjC","../internals/well-known-symbol":"gTwyA"}],"82MZ8":[function(require,module,exports) {
'use strict';
// TODO: Remove from `core-js@4` since it's moved to entry points
require('../modules/es.regexp.exec');
var redefine = require('../internals/redefine');
var regexpExec = require('../internals/regexp-exec');
var fails = require('../internals/fails');
var wellKnownSymbol = require('../internals/well-known-symbol');
var createNonEnumerableProperty = require('../internals/create-non-enumerable-property');
var SPECIES = wellKnownSymbol('species');
var RegExpPrototype = RegExp.prototype;
module.exports = function(KEY, exec, FORCED, SHAM) {
    var SYMBOL = wellKnownSymbol(KEY);
    var DELEGATES_TO_SYMBOL = !fails(function() {
        // String methods call symbol-named RegEp methods
        var O = {};
        O[SYMBOL] = function() {
            return 7;
        };
        return ''[KEY](O) != 7;
    });
    var DELEGATES_TO_EXEC = DELEGATES_TO_SYMBOL && !fails(function() {
        // Symbol-named RegExp methods call .exec
        var execCalled = false;
        var re = /a/;
        if (KEY === 'split') {
            // We can't use real regex here since it causes deoptimization
            // and serious performance degradation in V8
            // https://github.com/zloirock/core-js/issues/306
            re = {};
            // RegExp[@@split] doesn't call the regex's exec method, but first creates
            // a new one. We need to return the patched regex when creating the new one.
            re.constructor = {};
            re.constructor[SPECIES] = function() {
                return re;
            };
            re.flags = '';
            re[SYMBOL] = /./[SYMBOL];
        }
        re.exec = function() {
            execCalled = true;
            return null;
        };
        re[SYMBOL]('');
        return !execCalled;
    });
    if (!DELEGATES_TO_SYMBOL || !DELEGATES_TO_EXEC || FORCED) {
        var nativeRegExpMethod = /./[SYMBOL];
        var methods = exec(SYMBOL, ''[KEY], function(nativeMethod, regexp, str, arg2, forceStringMethod) {
            var $exec = regexp.exec;
            if ($exec === regexpExec || $exec === RegExpPrototype.exec) {
                if (DELEGATES_TO_SYMBOL && !forceStringMethod) // The native String method already delegates to @@method (this
                // polyfilled function), leasing to infinite recursion.
                // We avoid it by directly calling the native @@method method.
                return {
                    done: true,
                    value: nativeRegExpMethod.call(regexp, str, arg2)
                };
                return {
                    done: true,
                    value: nativeMethod.call(str, regexp, arg2)
                };
            }
            return {
                done: false
            };
        });
        redefine(String.prototype, KEY, methods[0]);
        redefine(RegExpPrototype, SYMBOL, methods[1]);
    }
    if (SHAM) createNonEnumerableProperty(RegExpPrototype[SYMBOL], 'sham', true);
};

},{"../modules/es.regexp.exec":"pNALB","../internals/redefine":"1ZKnU","../internals/regexp-exec":"1iqnJ","../internals/fails":"hL6D2","../internals/well-known-symbol":"gTwyA","../internals/create-non-enumerable-property":"8CL42"}],"hEviL":[function(require,module,exports) {
'use strict';
var charAt = require('../internals/string-multibyte').charAt;
// `AdvanceStringIndex` abstract operation
// https://tc39.es/ecma262/#sec-advancestringindex
module.exports = function(S, index, unicode) {
    return index + (unicode ? charAt(S, index).length : 1);
};

},{"../internals/string-multibyte":"gGTTm"}],"gGTTm":[function(require,module,exports) {
var toIntegerOrInfinity = require('../internals/to-integer-or-infinity');
var toString = require('../internals/to-string');
var requireObjectCoercible = require('../internals/require-object-coercible');
var createMethod = function(CONVERT_TO_STRING) {
    return function($this, pos) {
        var S = toString(requireObjectCoercible($this));
        var position = toIntegerOrInfinity(pos);
        var size = S.length;
        var first, second;
        if (position < 0 || position >= size) return CONVERT_TO_STRING ? '' : undefined;
        first = S.charCodeAt(position);
        return first < 0xD800 || first > 0xDBFF || position + 1 === size || (second = S.charCodeAt(position + 1)) < 0xDC00 || second > 0xDFFF ? CONVERT_TO_STRING ? S.charAt(position) : first : CONVERT_TO_STRING ? S.slice(position, position + 2) : (first - 0xD800 << 10) + (second - 0xDC00) + 0x10000;
    };
};
module.exports = {
    // `String.prototype.codePointAt` method
    // https://tc39.es/ecma262/#sec-string.prototype.codepointat
    codeAt: createMethod(false),
    // `String.prototype.at` method
    // https://github.com/mathiasbynens/String.prototype.at
    charAt: createMethod(true)
};

},{"../internals/to-integer-or-infinity":"kLXGe","../internals/to-string":"a1yl4","../internals/require-object-coercible":"fOR0B"}],"1vLvA":[function(require,module,exports) {
var toObject = require('../internals/to-object');
var floor = Math.floor;
var replace = ''.replace;
var SUBSTITUTION_SYMBOLS = /\$([$&'`]|\d{1,2}|<[^>]*>)/g;
var SUBSTITUTION_SYMBOLS_NO_NAMED = /\$([$&'`]|\d{1,2})/g;
// `GetSubstitution` abstract operation
// https://tc39.es/ecma262/#sec-getsubstitution
module.exports = function(matched, str, position, captures, namedCaptures, replacement) {
    var tailPos = position + matched.length;
    var m = captures.length;
    var symbols = SUBSTITUTION_SYMBOLS_NO_NAMED;
    if (namedCaptures !== undefined) {
        namedCaptures = toObject(namedCaptures);
        symbols = SUBSTITUTION_SYMBOLS;
    }
    return replace.call(replacement, symbols, function(match, ch) {
        var capture;
        switch(ch.charAt(0)){
            case '$':
                return '$';
            case '&':
                return matched;
            case '`':
                return str.slice(0, position);
            case "'":
                return str.slice(tailPos);
            case '<':
                capture = namedCaptures[ch.slice(1, -1)];
                break;
            default:
                var n = +ch;
                if (n === 0) return match;
                if (n > m) {
                    var f = floor(n / 10);
                    if (f === 0) return match;
                    if (f <= m) return captures[f - 1] === undefined ? ch.charAt(1) : captures[f - 1] + ch.charAt(1);
                    return match;
                }
                capture = captures[n - 1];
        }
        return capture === undefined ? '' : capture;
    });
};

},{"../internals/to-object":"5cb35"}],"5jYjC":[function(require,module,exports) {
var anObject = require('../internals/an-object');
var isCallable = require('../internals/is-callable');
var classof = require('../internals/classof-raw');
var regexpExec = require('../internals/regexp-exec');
// `RegExpExec` abstract operation
// https://tc39.es/ecma262/#sec-regexpexec
module.exports = function(R, S) {
    var exec = R.exec;
    if (isCallable(exec)) {
        var result = exec.call(R, S);
        if (result !== null) anObject(result);
        return result;
    }
    if (classof(R) === 'RegExp') return regexpExec.call(R, S);
    throw TypeError('RegExp#exec called on incompatible receiver');
};

},{"../internals/an-object":"4isCr","../internals/is-callable":"l3Kyn","../internals/classof-raw":"bdfmm","../internals/regexp-exec":"1iqnJ"}],"3mMHk":[function(require,module,exports) {
module.exports = require("core-js-pure/stable/set-interval");

},{"core-js-pure/stable/set-interval":"kLdqw"}],"kLdqw":[function(require,module,exports) {
require('../modules/web.timers');
var path = require('../internals/path');
module.exports = path.setInterval;

},{"../modules/web.timers":"KNLky","../internals/path":"8QO0z"}],"KNLky":[function(require,module,exports) {
// TODO: Remove this module from `core-js@4` since it's split to modules listed below
require('../modules/web.set-interval');
require('../modules/web.set-timeout');

},{"../modules/web.set-interval":"cI3Fc","../modules/web.set-timeout":"iiqOa"}],"cI3Fc":[function(require,module,exports) {
var $ = require('../internals/export');
var global = require('../internals/global');
var setInterval = require('../internals/schedulers-fix').setInterval;
// ie9- setInterval additional parameters fix
// https://html.spec.whatwg.org/multipage/timers-and-user-prompts.html#dom-setinterval
$({
    global: true,
    bind: true,
    forced: global.setInterval !== setInterval
}, {
    setInterval: setInterval
});

},{"../internals/export":"jjems","../internals/global":"avqB7","../internals/schedulers-fix":"9TklU"}],"9TklU":[function(require,module,exports) {
var global = require('../internals/global');
var apply = require('../internals/function-apply');
var isCallable = require('../internals/is-callable');
var userAgent = require('../internals/engine-user-agent');
var arraySlice = require('../internals/array-slice');
var validateArgumentsLength = require('../internals/validate-arguments-length');
var MSIE = /MSIE .\./.test(userAgent); // <- dirty ie9- check
var Function = global.Function;
var wrap = function(scheduler) {
    return MSIE ? function(handler, timeout /* , ...arguments */ ) {
        var boundArgs = validateArgumentsLength(arguments.length, 1) > 2;
        var fn = isCallable(handler) ? handler : Function(handler);
        var args = boundArgs ? arraySlice(arguments, 2) : undefined;
        return scheduler(boundArgs ? function() {
            apply(fn, this, args);
        } : fn, timeout);
    } : scheduler;
};
// ie9- setTimeout & setInterval additional parameters fix
// https://html.spec.whatwg.org/multipage/timers-and-user-prompts.html#timers
module.exports = {
    // `setTimeout` method
    // https://html.spec.whatwg.org/multipage/timers-and-user-prompts.html#dom-settimeout
    setTimeout: wrap(global.setTimeout),
    // `setInterval` method
    // https://html.spec.whatwg.org/multipage/timers-and-user-prompts.html#dom-setinterval
    setInterval: wrap(global.setInterval)
};

},{"../internals/global":"avqB7","../internals/function-apply":"kLBNq","../internals/is-callable":"fqYwo","../internals/engine-user-agent":"jumDW","../internals/array-slice":"gooNL","../internals/validate-arguments-length":"82N6M"}],"82N6M":[function(require,module,exports) {
var global = require('../internals/global');
var TypeError = global.TypeError;
module.exports = function(passed, required) {
    if (passed < required) throw TypeError('Not enough arguments');
    return passed;
};

},{"../internals/global":"avqB7"}],"iiqOa":[function(require,module,exports) {
var $ = require('../internals/export');
var global = require('../internals/global');
var setTimeout = require('../internals/schedulers-fix').setTimeout;
// ie9- setTimeout additional parameters fix
// https://html.spec.whatwg.org/multipage/timers-and-user-prompts.html#dom-settimeout
$({
    global: true,
    bind: true,
    forced: global.setTimeout !== setTimeout
}, {
    setTimeout: setTimeout
});

},{"../internals/export":"jjems","../internals/global":"avqB7","../internals/schedulers-fix":"9TklU"}],"boWFK":[function(require,module,exports) {
module.exports = require("core-js-pure/stable/instance/index-of");

},{"core-js-pure/stable/instance/index-of":"1IYNZ"}],"1IYNZ":[function(require,module,exports) {
var parent = require('../../es/instance/index-of');
module.exports = parent;

},{"../../es/instance/index-of":"L2zGq"}],"L2zGq":[function(require,module,exports) {
var isPrototypeOf = require('../../internals/object-is-prototype-of');
var method = require('../array/virtual/index-of');
var ArrayPrototype = Array.prototype;
module.exports = function(it) {
    var own = it.indexOf;
    return it === ArrayPrototype || isPrototypeOf(ArrayPrototype, it) && own === ArrayPrototype.indexOf ? method : own;
};

},{"../../internals/object-is-prototype-of":"qlM4L","../array/virtual/index-of":"1SUNt"}],"1SUNt":[function(require,module,exports) {
require('../../../modules/es.array.index-of');
var entryVirtual = require('../../../internals/entry-virtual');
module.exports = entryVirtual('Array').indexOf;

},{"../../../modules/es.array.index-of":"jPuvf","../../../internals/entry-virtual":"5RMpR"}],"jPuvf":[function(require,module,exports) {
'use strict';
/* eslint-disable es-x/no-array-prototype-indexof -- required for testing */ var $ = require('../internals/export');
var uncurryThis = require('../internals/function-uncurry-this');
var $IndexOf = require('../internals/array-includes').indexOf;
var arrayMethodIsStrict = require('../internals/array-method-is-strict');
var un$IndexOf = uncurryThis([].indexOf);
var NEGATIVE_ZERO = !!un$IndexOf && 1 / un$IndexOf([
    1
], 1, -0) < 0;
var STRICT_METHOD = arrayMethodIsStrict('indexOf');
// `Array.prototype.indexOf` method
// https://tc39.es/ecma262/#sec-array.prototype.indexof
$({
    target: 'Array',
    proto: true,
    forced: NEGATIVE_ZERO || !STRICT_METHOD
}, {
    indexOf: function indexOf(searchElement /* , fromIndex = 0 */ ) {
        var fromIndex = arguments.length > 1 ? arguments[1] : undefined;
        return NEGATIVE_ZERO ? un$IndexOf(this, searchElement, fromIndex) || 0 : $IndexOf(this, searchElement, fromIndex);
    }
});

},{"../internals/export":"jjems","../internals/function-uncurry-this":"5P4uT","../internals/array-includes":"9QVVn","../internals/array-method-is-strict":"jxw0S"}],"jxw0S":[function(require,module,exports) {
'use strict';
var fails = require('../internals/fails');
module.exports = function(METHOD_NAME, argument) {
    var method = [][METHOD_NAME];
    return !!method && fails(function() {
        // eslint-disable-next-line no-useless-call -- required for testing
        method.call(null, argument || function() {
            return 1;
        }, 1);
    });
};

},{"../internals/fails":"luPpk"}],"e3Y4r":[function(require,module,exports) {
module.exports = require("core-js-pure/stable/instance/splice");

},{"core-js-pure/stable/instance/splice":"eL10e"}],"eL10e":[function(require,module,exports) {
var parent = require('../../es/instance/splice');
module.exports = parent;

},{"../../es/instance/splice":"cgZc3"}],"cgZc3":[function(require,module,exports) {
var isPrototypeOf = require('../../internals/object-is-prototype-of');
var method = require('../array/virtual/splice');
var ArrayPrototype = Array.prototype;
module.exports = function(it) {
    var own = it.splice;
    return it === ArrayPrototype || isPrototypeOf(ArrayPrototype, it) && own === ArrayPrototype.splice ? method : own;
};

},{"../../internals/object-is-prototype-of":"qlM4L","../array/virtual/splice":"2zxjv"}],"2zxjv":[function(require,module,exports) {
require('../../../modules/es.array.splice');
var entryVirtual = require('../../../internals/entry-virtual');
module.exports = entryVirtual('Array').splice;

},{"../../../modules/es.array.splice":"9ZNHj","../../../internals/entry-virtual":"5RMpR"}],"9ZNHj":[function(require,module,exports) {
'use strict';
var $ = require('../internals/export');
var global = require('../internals/global');
var toAbsoluteIndex = require('../internals/to-absolute-index');
var toIntegerOrInfinity = require('../internals/to-integer-or-infinity');
var lengthOfArrayLike = require('../internals/length-of-array-like');
var toObject = require('../internals/to-object');
var arraySpeciesCreate = require('../internals/array-species-create');
var createProperty = require('../internals/create-property');
var arrayMethodHasSpeciesSupport = require('../internals/array-method-has-species-support');
var HAS_SPECIES_SUPPORT = arrayMethodHasSpeciesSupport('splice');
var TypeError = global.TypeError;
var max = Math.max;
var min = Math.min;
var MAX_SAFE_INTEGER = 0x1FFFFFFFFFFFFF;
var MAXIMUM_ALLOWED_LENGTH_EXCEEDED = 'Maximum allowed length exceeded';
// `Array.prototype.splice` method
// https://tc39.es/ecma262/#sec-array.prototype.splice
// with adding support of @@species
$({
    target: 'Array',
    proto: true,
    forced: !HAS_SPECIES_SUPPORT
}, {
    splice: function splice(start, deleteCount /* , ...items */ ) {
        var O = toObject(this);
        var len = lengthOfArrayLike(O);
        var actualStart = toAbsoluteIndex(start, len);
        var argumentsLength = arguments.length;
        var insertCount, actualDeleteCount, A, k, from, to;
        if (argumentsLength === 0) insertCount = actualDeleteCount = 0;
        else if (argumentsLength === 1) {
            insertCount = 0;
            actualDeleteCount = len - actualStart;
        } else {
            insertCount = argumentsLength - 2;
            actualDeleteCount = min(max(toIntegerOrInfinity(deleteCount), 0), len - actualStart);
        }
        if (len + insertCount - actualDeleteCount > MAX_SAFE_INTEGER) throw TypeError(MAXIMUM_ALLOWED_LENGTH_EXCEEDED);
        A = arraySpeciesCreate(O, actualDeleteCount);
        for(k = 0; k < actualDeleteCount; k++){
            from = actualStart + k;
            if (from in O) createProperty(A, k, O[from]);
        }
        A.length = actualDeleteCount;
        if (insertCount < actualDeleteCount) {
            for(k = actualStart; k < len - actualDeleteCount; k++){
                from = k + actualDeleteCount;
                to = k + insertCount;
                if (from in O) O[to] = O[from];
                else delete O[to];
            }
            for(k = len; k > len - actualDeleteCount + insertCount; k--)delete O[k - 1];
        } else if (insertCount > actualDeleteCount) for(k = len - actualDeleteCount; k > actualStart; k--){
            from = k + actualDeleteCount - 1;
            to = k + insertCount - 1;
            if (from in O) O[to] = O[from];
            else delete O[to];
        }
        for(k = 0; k < insertCount; k++)O[k + actualStart] = arguments[k + 2];
        O.length = len - actualDeleteCount + insertCount;
        return A;
    }
});

},{"../internals/export":"jjems","../internals/global":"avqB7","../internals/to-absolute-index":"1L8Z0","../internals/to-integer-or-infinity":"dQ2Uo","../internals/length-of-array-like":"icIfm","../internals/to-object":"70Utf","../internals/array-species-create":"jQr4t","../internals/create-property":"9Ttit","../internals/array-method-has-species-support":"4n6gw"}],"tXYDQ":[function(require,module,exports) {
module.exports = require("core-js-pure/stable/instance/concat");

},{"core-js-pure/stable/instance/concat":"aiUCw"}],"aiUCw":[function(require,module,exports) {
var parent = require('../../es/instance/concat');
module.exports = parent;

},{"../../es/instance/concat":"5dgPy"}],"5dgPy":[function(require,module,exports) {
var isPrototypeOf = require('../../internals/object-is-prototype-of');
var method = require('../array/virtual/concat');
var ArrayPrototype = Array.prototype;
module.exports = function(it) {
    var own = it.concat;
    return it === ArrayPrototype || isPrototypeOf(ArrayPrototype, it) && own === ArrayPrototype.concat ? method : own;
};

},{"../../internals/object-is-prototype-of":"qlM4L","../array/virtual/concat":"7dfuZ"}],"7dfuZ":[function(require,module,exports) {
require('../../../modules/es.array.concat');
var entryVirtual = require('../../../internals/entry-virtual');
module.exports = entryVirtual('Array').concat;

},{"../../../modules/es.array.concat":"liOpx","../../../internals/entry-virtual":"5RMpR"}],"j7X90":[function(require,module,exports) {
module.exports = require("core-js-pure/stable/date/now");

},{"core-js-pure/stable/date/now":"axK4a"}],"axK4a":[function(require,module,exports) {
var parent = require('../../es/date/now');
module.exports = parent;

},{"../../es/date/now":"5JCo1"}],"5JCo1":[function(require,module,exports) {
require('../../modules/es.date.now');
var path = require('../../internals/path');
module.exports = path.Date.now;

},{"../../modules/es.date.now":"gJhpO","../../internals/path":"8QO0z"}],"gJhpO":[function(require,module,exports) {
// TODO: Remove from `core-js@4`
var $ = require('../internals/export');
var global = require('../internals/global');
var uncurryThis = require('../internals/function-uncurry-this');
var Date = global.Date;
var getTime = uncurryThis(Date.prototype.getTime);
// `Date.now` method
// https://tc39.es/ecma262/#sec-date.now
$({
    target: 'Date',
    stat: true
}, {
    now: function now() {
        return getTime(new Date());
    }
});

},{"../internals/export":"jjems","../internals/global":"avqB7","../internals/function-uncurry-this":"5P4uT"}],"fi9Yx":[function(require,module,exports) {
module.exports = require("core-js-pure/stable/instance/for-each");

},{"core-js-pure/stable/instance/for-each":"35zbh"}],"35zbh":[function(require,module,exports) {
require('../../modules/web.dom-collections.iterator');
var classof = require('../../internals/classof');
var hasOwn = require('../../internals/has-own-property');
var isPrototypeOf = require('../../internals/object-is-prototype-of');
var method = require('../array/virtual/for-each');
var ArrayPrototype = Array.prototype;
var DOMIterables = {
    DOMTokenList: true,
    NodeList: true
};
module.exports = function(it) {
    var own = it.forEach;
    return it === ArrayPrototype || isPrototypeOf(ArrayPrototype, it) && own === ArrayPrototype.forEach || hasOwn(DOMIterables, classof(it)) ? method : own;
};

},{"../../modules/web.dom-collections.iterator":"fVWQ7","../../internals/classof":"5J26j","../../internals/has-own-property":"lacXY","../../internals/object-is-prototype-of":"qlM4L","../array/virtual/for-each":"c1oZH"}],"c1oZH":[function(require,module,exports) {
var parent = require('../../../es/array/virtual/for-each');
module.exports = parent;

},{"../../../es/array/virtual/for-each":"lUrLA"}],"lUrLA":[function(require,module,exports) {
require('../../../modules/es.array.for-each');
var entryVirtual = require('../../../internals/entry-virtual');
module.exports = entryVirtual('Array').forEach;

},{"../../../modules/es.array.for-each":"kKUIW","../../../internals/entry-virtual":"5RMpR"}],"kKUIW":[function(require,module,exports) {
'use strict';
var $ = require('../internals/export');
var forEach = require('../internals/array-for-each');
// `Array.prototype.forEach` method
// https://tc39.es/ecma262/#sec-array.prototype.foreach
// eslint-disable-next-line es-x/no-array-prototype-foreach -- safe
$({
    target: 'Array',
    proto: true,
    forced: [].forEach != forEach
}, {
    forEach: forEach
});

},{"../internals/export":"jjems","../internals/array-for-each":"g8jm8"}],"g8jm8":[function(require,module,exports) {
'use strict';
var $forEach = require('../internals/array-iteration').forEach;
var arrayMethodIsStrict = require('../internals/array-method-is-strict');
var STRICT_METHOD = arrayMethodIsStrict('forEach');
// `Array.prototype.forEach` method implementation
// https://tc39.es/ecma262/#sec-array.prototype.foreach
module.exports = !STRICT_METHOD ? function forEach(callbackfn /* , thisArg */ ) {
    return $forEach(this, callbackfn, arguments.length > 1 ? arguments[1] : undefined);
// eslint-disable-next-line es-x/no-array-prototype-foreach -- safe
} : [].forEach;

},{"../internals/array-iteration":"8g5by","../internals/array-method-is-strict":"jxw0S"}],"kYmBK":[function(require,module,exports) {
module.exports = require("core-js-pure/stable/object/keys");

},{"core-js-pure/stable/object/keys":"3hXU8"}],"3hXU8":[function(require,module,exports) {
var parent = require('../../es/object/keys');
module.exports = parent;

},{"../../es/object/keys":"hmmRM"}],"hmmRM":[function(require,module,exports) {
require('../../modules/es.object.keys');
var path = require('../../internals/path');
module.exports = path.Object.keys;

},{"../../modules/es.object.keys":"arc4F","../../internals/path":"8QO0z"}],"arc4F":[function(require,module,exports) {
var $ = require('../internals/export');
var toObject = require('../internals/to-object');
var nativeKeys = require('../internals/object-keys');
var fails = require('../internals/fails');
var FAILS_ON_PRIMITIVES = fails(function() {
    nativeKeys(1);
});
// `Object.keys` method
// https://tc39.es/ecma262/#sec-object.keys
$({
    target: 'Object',
    stat: true,
    forced: FAILS_ON_PRIMITIVES
}, {
    keys: function keys(it) {
        return nativeKeys(toObject(it));
    }
});

},{"../internals/export":"jjems","../internals/to-object":"70Utf","../internals/object-keys":"9NKcn","../internals/fails":"luPpk"}],"2gUV7":[function(require,module,exports) {
module.exports = require("core-js-pure/stable/object/get-own-property-symbols");

},{"core-js-pure/stable/object/get-own-property-symbols":"lycR6"}],"lycR6":[function(require,module,exports) {
var parent = require('../../es/object/get-own-property-symbols');
module.exports = parent;

},{"../../es/object/get-own-property-symbols":"fK69X"}],"fK69X":[function(require,module,exports) {
require('../../modules/es.symbol');
var path = require('../../internals/path');
module.exports = path.Object.getOwnPropertySymbols;

},{"../../modules/es.symbol":"eRNGH","../../internals/path":"8QO0z"}],"fVgp6":[function(require,module,exports) {
module.exports = require("core-js-pure/stable/instance/filter");

},{"core-js-pure/stable/instance/filter":"7Rz77"}],"7Rz77":[function(require,module,exports) {
var parent = require('../../es/instance/filter');
module.exports = parent;

},{"../../es/instance/filter":"5RQrT"}],"5RQrT":[function(require,module,exports) {
var isPrototypeOf = require('../../internals/object-is-prototype-of');
var method = require('../array/virtual/filter');
var ArrayPrototype = Array.prototype;
module.exports = function(it) {
    var own = it.filter;
    return it === ArrayPrototype || isPrototypeOf(ArrayPrototype, it) && own === ArrayPrototype.filter ? method : own;
};

},{"../../internals/object-is-prototype-of":"qlM4L","../array/virtual/filter":"j3F3D"}],"j3F3D":[function(require,module,exports) {
require('../../../modules/es.array.filter');
var entryVirtual = require('../../../internals/entry-virtual');
module.exports = entryVirtual('Array').filter;

},{"../../../modules/es.array.filter":"keBoU","../../../internals/entry-virtual":"5RMpR"}],"keBoU":[function(require,module,exports) {
'use strict';
var $ = require('../internals/export');
var $filter = require('../internals/array-iteration').filter;
var arrayMethodHasSpeciesSupport = require('../internals/array-method-has-species-support');
var HAS_SPECIES_SUPPORT = arrayMethodHasSpeciesSupport('filter');
// `Array.prototype.filter` method
// https://tc39.es/ecma262/#sec-array.prototype.filter
// with adding support of @@species
$({
    target: 'Array',
    proto: true,
    forced: !HAS_SPECIES_SUPPORT
}, {
    filter: function filter(callbackfn /* , thisArg */ ) {
        return $filter(this, callbackfn, arguments.length > 1 ? arguments[1] : undefined);
    }
});

},{"../internals/export":"jjems","../internals/array-iteration":"8g5by","../internals/array-method-has-species-support":"4n6gw"}],"lQ0dj":[function(require,module,exports) {
module.exports = require("core-js-pure/stable/object/get-own-property-descriptor");

},{"core-js-pure/stable/object/get-own-property-descriptor":"7bDJk"}],"7bDJk":[function(require,module,exports) {
var parent = require('../../es/object/get-own-property-descriptor');
module.exports = parent;

},{"../../es/object/get-own-property-descriptor":"hm6Dg"}],"hm6Dg":[function(require,module,exports) {
require('../../modules/es.object.get-own-property-descriptor');
var path = require('../../internals/path');
var Object = path.Object;
var getOwnPropertyDescriptor = module.exports = function getOwnPropertyDescriptor(it, key) {
    return Object.getOwnPropertyDescriptor(it, key);
};
if (Object.getOwnPropertyDescriptor.sham) getOwnPropertyDescriptor.sham = true;

},{"../../modules/es.object.get-own-property-descriptor":"c8MlR","../../internals/path":"8QO0z"}],"c8MlR":[function(require,module,exports) {
var $ = require('../internals/export');
var fails = require('../internals/fails');
var toIndexedObject = require('../internals/to-indexed-object');
var nativeGetOwnPropertyDescriptor = require('../internals/object-get-own-property-descriptor').f;
var DESCRIPTORS = require('../internals/descriptors');
var FAILS_ON_PRIMITIVES = fails(function() {
    nativeGetOwnPropertyDescriptor(1);
});
var FORCED = !DESCRIPTORS || FAILS_ON_PRIMITIVES;
// `Object.getOwnPropertyDescriptor` method
// https://tc39.es/ecma262/#sec-object.getownpropertydescriptor
$({
    target: 'Object',
    stat: true,
    forced: FORCED,
    sham: !DESCRIPTORS
}, {
    getOwnPropertyDescriptor: function getOwnPropertyDescriptor(it, key) {
        return nativeGetOwnPropertyDescriptor(toIndexedObject(it), key);
    }
});

},{"../internals/export":"jjems","../internals/fails":"luPpk","../internals/to-indexed-object":"kvdfV","../internals/object-get-own-property-descriptor":"bs1FJ","../internals/descriptors":"87WOB"}],"5Vu5K":[function(require,module,exports) {
module.exports = require("core-js-pure/stable/object/get-own-property-descriptors");

},{"core-js-pure/stable/object/get-own-property-descriptors":"jbWnf"}],"jbWnf":[function(require,module,exports) {
var parent = require('../../es/object/get-own-property-descriptors');
module.exports = parent;

},{"../../es/object/get-own-property-descriptors":"jeSQR"}],"jeSQR":[function(require,module,exports) {
require('../../modules/es.object.get-own-property-descriptors');
var path = require('../../internals/path');
module.exports = path.Object.getOwnPropertyDescriptors;

},{"../../modules/es.object.get-own-property-descriptors":"GEvCx","../../internals/path":"8QO0z"}],"GEvCx":[function(require,module,exports) {
var $ = require('../internals/export');
var DESCRIPTORS = require('../internals/descriptors');
var ownKeys = require('../internals/own-keys');
var toIndexedObject = require('../internals/to-indexed-object');
var getOwnPropertyDescriptorModule = require('../internals/object-get-own-property-descriptor');
var createProperty = require('../internals/create-property');
// `Object.getOwnPropertyDescriptors` method
// https://tc39.es/ecma262/#sec-object.getownpropertydescriptors
$({
    target: 'Object',
    stat: true,
    sham: !DESCRIPTORS
}, {
    getOwnPropertyDescriptors: function getOwnPropertyDescriptors(object) {
        var O = toIndexedObject(object);
        var getOwnPropertyDescriptor = getOwnPropertyDescriptorModule.f;
        var keys = ownKeys(O);
        var result = {};
        var index = 0;
        var key, descriptor;
        while(keys.length > index){
            descriptor = getOwnPropertyDescriptor(O, key = keys[index++]);
            if (descriptor !== undefined) createProperty(result, key, descriptor);
        }
        return result;
    }
});

},{"../internals/export":"jjems","../internals/descriptors":"87WOB","../internals/own-keys":"iC5zJ","../internals/to-indexed-object":"kvdfV","../internals/object-get-own-property-descriptor":"bs1FJ","../internals/create-property":"9Ttit"}],"iC5zJ":[function(require,module,exports) {
var getBuiltIn = require('../internals/get-built-in');
var uncurryThis = require('../internals/function-uncurry-this');
var getOwnPropertyNamesModule = require('../internals/object-get-own-property-names');
var getOwnPropertySymbolsModule = require('../internals/object-get-own-property-symbols');
var anObject = require('../internals/an-object');
var concat = uncurryThis([].concat);
// all object keys, includes non-enumerable and symbols
module.exports = getBuiltIn('Reflect', 'ownKeys') || function ownKeys(it) {
    var keys = getOwnPropertyNamesModule.f(anObject(it));
    var getOwnPropertySymbols = getOwnPropertySymbolsModule.f;
    return getOwnPropertySymbols ? concat(keys, getOwnPropertySymbols(it)) : keys;
};

},{"../internals/get-built-in":"6wm25","../internals/function-uncurry-this":"5P4uT","../internals/object-get-own-property-names":"lJsHK","../internals/object-get-own-property-symbols":"1qW3G","../internals/an-object":"bTNyr"}],"jscfH":[function(require,module,exports) {
module.exports = require("core-js-pure/stable/object/define-properties");

},{"core-js-pure/stable/object/define-properties":"a8w3R"}],"a8w3R":[function(require,module,exports) {
var parent = require('../../es/object/define-properties');
module.exports = parent;

},{"../../es/object/define-properties":"hsdVp"}],"hsdVp":[function(require,module,exports) {
require('../../modules/es.object.define-properties');
var path = require('../../internals/path');
var Object = path.Object;
var defineProperties = module.exports = function defineProperties(T, D) {
    return Object.defineProperties(T, D);
};
if (Object.defineProperties.sham) defineProperties.sham = true;

},{"../../modules/es.object.define-properties":"dsgW7","../../internals/path":"8QO0z"}],"dsgW7":[function(require,module,exports) {
var $ = require('../internals/export');
var DESCRIPTORS = require('../internals/descriptors');
var defineProperties = require('../internals/object-define-properties').f;
// `Object.defineProperties` method
// https://tc39.es/ecma262/#sec-object.defineproperties
// eslint-disable-next-line es-x/no-object-defineproperties -- safe
$({
    target: 'Object',
    stat: true,
    forced: Object.defineProperties !== defineProperties,
    sham: !DESCRIPTORS
}, {
    defineProperties: defineProperties
});

},{"../internals/export":"jjems","../internals/descriptors":"87WOB","../internals/object-define-properties":"8IwWp"}],"kv9Cz":[function(require,module,exports) {
module.exports = require("core-js-pure/stable/object/define-property");

},{"core-js-pure/stable/object/define-property":"fpclb"}],"k89zX":[function(require,module,exports) {
var parcelHelpers = require("@parcel/transformer-js/src/esmodule-helpers.js");
parcelHelpers.defineInteropFlag(exports);
var _cache = require("@emotion/cache");
var _cacheDefault = parcelHelpers.interopDefault(_cache);
var _serialize = require("@emotion/serialize");
var _utils = require("@emotion/utils");
function insertWithoutScoping(cache, serialized) {
    if (cache.inserted[serialized.name] === undefined) return cache.insert('', serialized, cache.sheet, true);
}
function merge(registered, css, className) {
    var registeredStyles = [];
    var rawClassName = _utils.getRegisteredStyles(registered, registeredStyles, className);
    if (registeredStyles.length < 2) return className;
    return rawClassName + css(registeredStyles);
}
var createEmotion = function createEmotion(options) {
    var cache = _cacheDefault.default(options); // $FlowFixMe
    cache.sheet.speedy = function(value) {
        if (this.ctr !== 0) throw new Error('speedy must be changed before any rules are inserted');
        this.isSpeedy = value;
    };
    cache.compat = true;
    var css = function css() {
        for(var _len = arguments.length, args = new Array(_len), _key = 0; _key < _len; _key++)args[_key] = arguments[_key];
        var serialized = _serialize.serializeStyles(args, cache.registered, undefined);
        _utils.insertStyles(cache, serialized, false);
        return cache.key + "-" + serialized.name;
    };
    var keyframes = function keyframes() {
        for(var _len2 = arguments.length, args = new Array(_len2), _key2 = 0; _key2 < _len2; _key2++)args[_key2] = arguments[_key2];
        var serialized = _serialize.serializeStyles(args, cache.registered);
        var animation = "animation-" + serialized.name;
        insertWithoutScoping(cache, {
            name: serialized.name,
            styles: "@keyframes " + animation + "{" + serialized.styles + "}"
        });
        return animation;
    };
    var injectGlobal = function injectGlobal() {
        for(var _len3 = arguments.length, args = new Array(_len3), _key3 = 0; _key3 < _len3; _key3++)args[_key3] = arguments[_key3];
        var serialized = _serialize.serializeStyles(args, cache.registered);
        insertWithoutScoping(cache, serialized);
    };
    var cx = function cx() {
        for(var _len4 = arguments.length, args = new Array(_len4), _key4 = 0; _key4 < _len4; _key4++)args[_key4] = arguments[_key4];
        return merge(cache.registered, css, classnames(args));
    };
    return {
        css: css,
        cx: cx,
        injectGlobal: injectGlobal,
        keyframes: keyframes,
        hydrate: function hydrate(ids) {
            ids.forEach(function(key) {
                cache.inserted[key] = true;
            });
        },
        flush: function flush() {
            cache.registered = {};
            cache.inserted = {};
            cache.sheet.flush();
        },
        // $FlowFixMe
        sheet: cache.sheet,
        cache: cache,
        getRegisteredStyles: _utils.getRegisteredStyles.bind(null, cache.registered),
        merge: merge.bind(null, cache.registered, css)
    };
};
var classnames = function classnames1(args) {
    var cls = '';
    for(var i = 0; i < args.length; i++){
        var arg = args[i];
        if (arg == null) continue;
        var toAdd = void 0;
        switch(typeof arg){
            case 'boolean':
                break;
            case 'object':
                if (Array.isArray(arg)) toAdd = classnames1(arg);
                else {
                    toAdd = '';
                    for(var k in arg)if (arg[k] && k) {
                        toAdd && (toAdd += ' ');
                        toAdd += k;
                    }
                }
                break;
            default:
                toAdd = arg;
        }
        if (toAdd) {
            cls && (cls += ' ');
            cls += toAdd;
        }
    }
    return cls;
};
exports.default = createEmotion;

},{"@emotion/cache":"3Umtj","@emotion/serialize":"kS2E2","@emotion/utils":"6UI8e","@parcel/transformer-js/src/esmodule-helpers.js":"gkKU3"}],"3Umtj":[function(require,module,exports) {
var parcelHelpers = require("@parcel/transformer-js/src/esmodule-helpers.js");
parcelHelpers.defineInteropFlag(exports);
var _sheet = require("@emotion/sheet");
var _stylis = require("stylis");
var _weakMemoize = require("@emotion/weak-memoize");
var _memoize = require("@emotion/memoize");
var last = function last(arr) {
    return arr.length ? arr[arr.length - 1] : null;
}; // based on https://github.com/thysultan/stylis.js/blob/e6843c373ebcbbfade25ebcc23f540ed8508da0a/src/Tokenizer.js#L239-L244
var identifierWithPointTracking = function identifierWithPointTracking(begin, points, index) {
    var previous = 0;
    var character = 0;
    while(true){
        previous = character;
        character = _stylis.peek(); // &\f
        if (previous === 38 && character === 12) points[index] = 1;
        if (_stylis.token(character)) break;
        _stylis.next();
    }
    return _stylis.slice(begin, _stylis.position);
};
var toRules = function toRules(parsed, points) {
    // pretend we've started with a comma
    var index = -1;
    var character = 44;
    do switch(_stylis.token(character)){
        case 0:
            // &\f
            if (character === 38 && _stylis.peek() === 12) // this is not 100% correct, we don't account for literal sequences here - like for example quoted strings
            // stylis inserts \f after & to know when & where it should replace this sequence with the context selector
            // and when it should just concatenate the outer and inner selectors
            // it's very unlikely for this sequence to actually appear in a different context, so we just leverage this fact here
            points[index] = 1;
            parsed[index] += identifierWithPointTracking(_stylis.position - 1, points, index);
            break;
        case 2:
            parsed[index] += _stylis.delimit(character);
            break;
        case 4:
            // comma
            if (character === 44) {
                // colon
                parsed[++index] = _stylis.peek() === 58 ? '&\f' : '';
                points[index] = parsed[index].length;
                break;
            }
        // fallthrough
        default:
            parsed[index] += _stylis.from(character);
    }
    while (character = _stylis.next())
    return parsed;
};
var getRules = function getRules(value, points) {
    return _stylis.dealloc(toRules(_stylis.alloc(value), points));
}; // WeakSet would be more appropriate, but only WeakMap is supported in IE11
var fixedElements = /* #__PURE__ */ new WeakMap();
var compat = function compat(element) {
    if (element.type !== 'rule' || !element.parent || // negative .length indicates that this rule has been already prefixed
    element.length < 1) return;
    var value = element.value, parent = element.parent;
    var isImplicitRule = element.column === parent.column && element.line === parent.line;
    while(parent.type !== 'rule'){
        parent = parent.parent;
        if (!parent) return;
    } // short-circuit for the simplest case
    if (element.props.length === 1 && value.charCodeAt(0) !== 58 && !fixedElements.get(parent)) return;
     // if this is an implicitly inserted rule (the one eagerly inserted at the each new nested level)
    // then the props has already been manipulated beforehand as they that array is shared between it and its "rule parent"
    if (isImplicitRule) return;
    fixedElements.set(element, true);
    var points = [];
    var rules = getRules(value, points);
    var parentRules = parent.props;
    for(var i = 0, k = 0; i < rules.length; i++)for(var j = 0; j < parentRules.length; j++, k++)element.props[k] = points[i] ? rules[i].replace(/&\f/g, parentRules[j]) : parentRules[j] + " " + rules[i];
};
var removeLabel = function removeLabel(element) {
    if (element.type === 'decl') {
        var value = element.value;
        if (value.charCodeAt(0) === 108 && value.charCodeAt(2) === 98) {
            // this ignores label
            element["return"] = '';
            element.value = '';
        }
    }
};
var ignoreFlag = 'emotion-disable-server-rendering-unsafe-selector-warning-please-do-not-use-this-the-warning-exists-for-a-reason';
var isIgnoringComment = function isIgnoringComment(element) {
    return !!element && element.type === 'comm' && element.children.indexOf(ignoreFlag) > -1;
};
var createUnsafeSelectorsAlarm = function createUnsafeSelectorsAlarm(cache) {
    return function(element, index, children) {
        if (element.type !== 'rule') return;
        var unsafePseudoClasses = element.value.match(/(:first|:nth|:nth-last)-child/g);
        if (unsafePseudoClasses && cache.compat !== true) {
            var prevElement = index > 0 ? children[index - 1] : null;
            if (prevElement && isIgnoringComment(last(prevElement.children))) return;
            unsafePseudoClasses.forEach(function(unsafePseudoClass) {
                console.error("The pseudo class \"" + unsafePseudoClass + "\" is potentially unsafe when doing server-side rendering. Try changing it to \"" + unsafePseudoClass.split('-child')[0] + "-of-type\".");
            });
        }
    };
};
var isImportRule = function isImportRule(element) {
    return element.type.charCodeAt(1) === 105 && element.type.charCodeAt(0) === 64;
};
var isPrependedWithRegularRules = function isPrependedWithRegularRules(index, children) {
    for(var i = index - 1; i >= 0; i--){
        if (!isImportRule(children[i])) return true;
    }
    return false;
}; // use this to remove incorrect elements from further processing
// so they don't get handed to the `sheet` (or anything else)
// as that could potentially lead to additional logs which in turn could be overhelming to the user
var nullifyElement = function nullifyElement(element) {
    element.type = '';
    element.value = '';
    element["return"] = '';
    element.children = '';
    element.props = '';
};
var incorrectImportAlarm = function incorrectImportAlarm(element, index, children) {
    if (!isImportRule(element)) return;
    if (element.parent) {
        console.error("`@import` rules can't be nested inside other rules. Please move it to the top level and put it before regular rules. Keep in mind that they can only be used within global styles.");
        nullifyElement(element);
    } else if (isPrependedWithRegularRules(index, children)) {
        console.error("`@import` rules can't be after other rules. Please put your `@import` rules before your other rules.");
        nullifyElement(element);
    }
};
var defaultStylisPlugins = [
    _stylis.prefixer
];
var createCache = function createCache(options) {
    var key = options.key;
    if (!key) throw new Error("You have to configure `key` for your cache. Please make sure it's unique (and not equal to 'css') as it's used for linking styles to your cache.\nIf multiple caches share the same key they might \"fight\" for each other's style elements.");
    if (key === 'css') {
        var ssrStyles = document.querySelectorAll("style[data-emotion]:not([data-s])"); // get SSRed styles out of the way of React's hydration
        // document.head is a safe place to move them to(though note document.head is not necessarily the last place they will be)
        // note this very very intentionally targets all style elements regardless of the key to ensure
        // that creating a cache works inside of render of a React component
        Array.prototype.forEach.call(ssrStyles, function(node) {
            // we want to only move elements which have a space in the data-emotion attribute value
            // because that indicates that it is an Emotion 11 server-side rendered style elements
            // while we will already ignore Emotion 11 client-side inserted styles because of the :not([data-s]) part in the selector
            // Emotion 10 client-side inserted styles did not have data-s (but importantly did not have a space in their data-emotion attributes)
            // so checking for the space ensures that loading Emotion 11 after Emotion 10 has inserted some styles
            // will not result in the Emotion 10 styles being destroyed
            var dataEmotionAttribute = node.getAttribute('data-emotion');
            if (dataEmotionAttribute.indexOf(' ') === -1) return;
            document.head.appendChild(node);
            node.setAttribute('data-s', '');
        });
    }
    var stylisPlugins = options.stylisPlugins || defaultStylisPlugins;
    // $FlowFixMe
    if (/[^a-z-]/.test(key)) throw new Error("Emotion key must only contain lower case alphabetical characters and - but \"" + key + "\" was passed");
    var inserted = {}; // $FlowFixMe
    var container;
    var nodesToHydrate = [];
    container = options.container || document.head;
    Array.prototype.forEach.call(// means that the style elements we're looking at are only Emotion 11 server-rendered style elements
    document.querySelectorAll("style[data-emotion^=\"" + key + " \"]"), function(node) {
        var attrib = node.getAttribute("data-emotion").split(' '); // $FlowFixMe
        for(var i = 1; i < attrib.length; i++)inserted[attrib[i]] = true;
        nodesToHydrate.push(node);
    });
    var _insert;
    var omnipresentPlugins = [
        compat,
        removeLabel
    ];
    omnipresentPlugins.push(createUnsafeSelectorsAlarm({
        get compat () {
            return cache.compat;
        }
    }), incorrectImportAlarm);
    var currentSheet;
    var finalizingPlugins = [
        _stylis.stringify,
        function(element) {
            if (!element.root) {
                if (element["return"]) currentSheet.insert(element["return"]);
                else if (element.value && element.type !== _stylis.COMMENT) // insert empty rule in non-production environments
                // so @emotion/jest can grab `key` from the (JS)DOM for caches without any rules inserted yet
                currentSheet.insert(element.value + "{}");
            }
        }
    ];
    var serializer = _stylis.middleware(omnipresentPlugins.concat(stylisPlugins, finalizingPlugins));
    var stylis = function stylis(styles) {
        return _stylis.serialize(_stylis.compile(styles), serializer);
    };
    _insert = function insert(selector, serialized, sheet, shouldCache) {
        currentSheet = sheet;
        if (serialized.map !== undefined) currentSheet = {
            insert: function insert(rule) {
                sheet.insert(rule + serialized.map);
            }
        };
        stylis(selector ? selector + "{" + serialized.styles + "}" : serialized.styles);
        if (shouldCache) cache.inserted[serialized.name] = true;
    };
    var cache = {
        key: key,
        sheet: new _sheet.StyleSheet({
            key: key,
            container: container,
            nonce: options.nonce,
            speedy: options.speedy,
            prepend: options.prepend,
            insertionPoint: options.insertionPoint
        }),
        nonce: options.nonce,
        inserted: inserted,
        registered: {},
        insert: _insert
    };
    cache.sheet.hydrate(nodesToHydrate);
    return cache;
};
exports.default = createCache;

},{"@emotion/sheet":"1BWeq","stylis":"bMCXt","@emotion/weak-memoize":"iicyL","@emotion/memoize":"WW7h8","@parcel/transformer-js/src/esmodule-helpers.js":"gkKU3"}],"1BWeq":[function(require,module,exports) {
var parcelHelpers = require("@parcel/transformer-js/src/esmodule-helpers.js");
parcelHelpers.defineInteropFlag(exports);
parcelHelpers.export(exports, "StyleSheet", ()=>StyleSheet
);
/*

Based off glamor's StyleSheet, thanks Sunil ❤️

high performance StyleSheet for css-in-js systems

- uses multiple style tags behind the scenes for millions of rules
- uses `insertRule` for appending in production for *much* faster performance

// usage

import { StyleSheet } from '@emotion/sheet'

let styleSheet = new StyleSheet({ key: '', container: document.head })

styleSheet.insert('#box { border: 1px solid red; }')
- appends a css rule into the stylesheet

styleSheet.flush()
- empties the stylesheet of all its contents

*/ // $FlowFixMe
function sheetForTag(tag) {
    if (tag.sheet) // $FlowFixMe
    return tag.sheet;
     // this weirdness brought to you by firefox
    /* istanbul ignore next */ for(var i = 0; i < document.styleSheets.length; i++){
        if (document.styleSheets[i].ownerNode === tag) // $FlowFixMe
        return document.styleSheets[i];
    }
}
function createStyleElement(options) {
    var tag = document.createElement('style');
    tag.setAttribute('data-emotion', options.key);
    if (options.nonce !== undefined) tag.setAttribute('nonce', options.nonce);
    tag.appendChild(document.createTextNode(''));
    tag.setAttribute('data-s', '');
    return tag;
}
var StyleSheet = /*#__PURE__*/ function() {
    function StyleSheet1(options) {
        var _this = this;
        this._insertTag = function(tag) {
            var before;
            if (_this.tags.length === 0) {
                if (_this.insertionPoint) before = _this.insertionPoint.nextSibling;
                else if (_this.prepend) before = _this.container.firstChild;
                else before = _this.before;
            } else before = _this.tags[_this.tags.length - 1].nextSibling;
            _this.container.insertBefore(tag, before);
            _this.tags.push(tag);
        };
        this.isSpeedy = options.speedy === undefined ? false : options.speedy;
        this.tags = [];
        this.ctr = 0;
        this.nonce = options.nonce; // key is the value of the data-emotion attribute, it's used to identify different sheets
        this.key = options.key;
        this.container = options.container;
        this.prepend = options.prepend;
        this.insertionPoint = options.insertionPoint;
        this.before = null;
    }
    var _proto = StyleSheet1.prototype;
    _proto.hydrate = function hydrate(nodes) {
        nodes.forEach(this._insertTag);
    };
    _proto.insert = function insert(rule) {
        // the max length is how many rules we have per style tag, it's 65000 in speedy mode
        // it's 1 in dev because we insert source maps that map a single rule to a location
        // and you can only have one source map per style tag
        if (this.ctr % (this.isSpeedy ? 65000 : 1) === 0) this._insertTag(createStyleElement(this));
        var tag = this.tags[this.tags.length - 1];
        var isImportRule = rule.charCodeAt(0) === 64 && rule.charCodeAt(1) === 105;
        if (isImportRule && this._alreadyInsertedOrderInsensitiveRule) // this would only cause problem in speedy mode
        // but we don't want enabling speedy to affect the observable behavior
        // so we report this error at all times
        console.error("You're attempting to insert the following rule:\n" + rule + '\n\n`@import` rules must be before all other types of rules in a stylesheet but other rules have already been inserted. Please ensure that `@import` rules are before all other rules.');
        this._alreadyInsertedOrderInsensitiveRule = this._alreadyInsertedOrderInsensitiveRule || !isImportRule;
        if (this.isSpeedy) {
            var sheet = sheetForTag(tag);
            try {
                // this is the ultrafast version, works across browsers
                // the big drawback is that the css won't be editable in devtools
                sheet.insertRule(rule, sheet.cssRules.length);
            } catch (e) {
                if (!/:(-moz-placeholder|-moz-focus-inner|-moz-focusring|-ms-input-placeholder|-moz-read-write|-moz-read-only|-ms-clear){/.test(rule)) console.error("There was a problem inserting the following rule: \"" + rule + "\"", e);
            }
        } else tag.appendChild(document.createTextNode(rule));
        this.ctr++;
    };
    _proto.flush = function flush() {
        // $FlowFixMe
        this.tags.forEach(function(tag) {
            return tag.parentNode && tag.parentNode.removeChild(tag);
        });
        this.tags = [];
        this.ctr = 0;
        this._alreadyInsertedOrderInsensitiveRule = false;
    };
    return StyleSheet1;
}();

},{"@parcel/transformer-js/src/esmodule-helpers.js":"gkKU3"}],"bMCXt":[function(require,module,exports) {
var parcelHelpers = require("@parcel/transformer-js/src/esmodule-helpers.js");
parcelHelpers.defineInteropFlag(exports);
parcelHelpers.export(exports, "CHARSET", ()=>f
);
parcelHelpers.export(exports, "COMMENT", ()=>c
);
parcelHelpers.export(exports, "COUNTER_STYLE", ()=>w
);
parcelHelpers.export(exports, "DECLARATION", ()=>t
);
parcelHelpers.export(exports, "DOCUMENT", ()=>v
);
parcelHelpers.export(exports, "FONT_FACE", ()=>b
);
parcelHelpers.export(exports, "FONT_FEATURE_VALUES", ()=>$
);
parcelHelpers.export(exports, "IMPORT", ()=>i
);
parcelHelpers.export(exports, "KEYFRAMES", ()=>p
);
parcelHelpers.export(exports, "MEDIA", ()=>u
);
parcelHelpers.export(exports, "MOZ", ()=>r
);
parcelHelpers.export(exports, "MS", ()=>e
);
parcelHelpers.export(exports, "NAMESPACE", ()=>h
);
parcelHelpers.export(exports, "PAGE", ()=>s
);
parcelHelpers.export(exports, "RULESET", ()=>n
);
parcelHelpers.export(exports, "SUPPORTS", ()=>l
);
parcelHelpers.export(exports, "VIEWPORT", ()=>o
);
parcelHelpers.export(exports, "WEBKIT", ()=>a
);
parcelHelpers.export(exports, "abs", ()=>k
);
parcelHelpers.export(exports, "alloc", ()=>U
);
parcelHelpers.export(exports, "append", ()=>S
);
parcelHelpers.export(exports, "assign", ()=>g
);
parcelHelpers.export(exports, "caret", ()=>Q
);
parcelHelpers.export(exports, "char", ()=>K
);
parcelHelpers.export(exports, "character", ()=>G
);
parcelHelpers.export(exports, "characters", ()=>H
);
parcelHelpers.export(exports, "charat", ()=>z
);
parcelHelpers.export(exports, "column", ()=>D
);
parcelHelpers.export(exports, "combine", ()=>q
);
parcelHelpers.export(exports, "comment", ()=>se
);
parcelHelpers.export(exports, "commenter", ()=>re
);
parcelHelpers.export(exports, "compile", ()=>ce
);
parcelHelpers.export(exports, "copy", ()=>J
);
parcelHelpers.export(exports, "dealloc", ()=>V
);
parcelHelpers.export(exports, "declaration", ()=>ue
);
parcelHelpers.export(exports, "delimit", ()=>W
);
parcelHelpers.export(exports, "delimiter", ()=>ee
);
parcelHelpers.export(exports, "escaping", ()=>_
);
parcelHelpers.export(exports, "from", ()=>d
);
parcelHelpers.export(exports, "hash", ()=>m
);
parcelHelpers.export(exports, "identifier", ()=>ae
);
parcelHelpers.export(exports, "indexof", ()=>C
);
parcelHelpers.export(exports, "length", ()=>E
);
parcelHelpers.export(exports, "line", ()=>B
);
parcelHelpers.export(exports, "match", ()=>y
);
parcelHelpers.export(exports, "middleware", ()=>le
);
parcelHelpers.export(exports, "namespace", ()=>pe
);
parcelHelpers.export(exports, "next", ()=>N
);
parcelHelpers.export(exports, "node", ()=>I
);
parcelHelpers.export(exports, "parse", ()=>ne
);
parcelHelpers.export(exports, "peek", ()=>P
);
parcelHelpers.export(exports, "position", ()=>F
);
parcelHelpers.export(exports, "prefix", ()=>ie
);
parcelHelpers.export(exports, "prefixer", ()=>he
);
parcelHelpers.export(exports, "prev", ()=>L
);
parcelHelpers.export(exports, "replace", ()=>j
);
parcelHelpers.export(exports, "ruleset", ()=>te
);
parcelHelpers.export(exports, "rulesheet", ()=>ve
);
parcelHelpers.export(exports, "serialize", ()=>fe
);
parcelHelpers.export(exports, "sizeof", ()=>M
);
parcelHelpers.export(exports, "slice", ()=>R
);
parcelHelpers.export(exports, "stringify", ()=>oe
);
parcelHelpers.export(exports, "strlen", ()=>O
);
parcelHelpers.export(exports, "substr", ()=>A
);
parcelHelpers.export(exports, "token", ()=>T
);
parcelHelpers.export(exports, "tokenize", ()=>X
);
parcelHelpers.export(exports, "tokenizer", ()=>Z
);
parcelHelpers.export(exports, "trim", ()=>x
);
parcelHelpers.export(exports, "whitespace", ()=>Y
);
var e = "-ms-";
var r = "-moz-";
var a = "-webkit-";
var c = "comm";
var n = "rule";
var t = "decl";
var s = "@page";
var u = "@media";
var i = "@import";
var f = "@charset";
var o = "@viewport";
var l = "@supports";
var v = "@document";
var h = "@namespace";
var p = "@keyframes";
var b = "@font-face";
var w = "@counter-style";
var $ = "@font-feature-values";
var k = Math.abs;
var d = String.fromCharCode;
var g = Object.assign;
function m(e1, r1) {
    return (((r1 << 2 ^ z(e1, 0)) << 2 ^ z(e1, 1)) << 2 ^ z(e1, 2)) << 2 ^ z(e1, 3);
}
function x(e2) {
    return e2.trim();
}
function y(e3, r2) {
    return (e3 = r2.exec(e3)) ? e3[0] : e3;
}
function j(e4, r3, a1) {
    return e4.replace(r3, a1);
}
function C(e5, r4) {
    return e5.indexOf(r4);
}
function z(e6, r5) {
    return e6.charCodeAt(r5) | 0;
}
function A(e7, r6, a2) {
    return e7.slice(r6, a2);
}
function O(e8) {
    return e8.length;
}
function M(e9) {
    return e9.length;
}
function S(e10, r7) {
    return r7.push(e10), e10;
}
function q(e11, r8) {
    return e11.map(r8).join("");
}
var B = 1;
var D = 1;
var E = 0;
var F = 0;
var G = 0;
var H = "";
function I(e12, r9, a3, c1, n1, t1, s1) {
    return {
        value: e12,
        root: r9,
        parent: a3,
        type: c1,
        props: n1,
        children: t1,
        line: B,
        column: D,
        length: s1,
        return: ""
    };
}
function J(e13, r10) {
    return g(I("", null, null, "", null, null, 0), e13, {
        length: -e13.length
    }, r10);
}
function K() {
    return G;
}
function L() {
    G = F > 0 ? z(H, --F) : 0;
    if (D--, G === 10) D = 1, B--;
    return G;
}
function N() {
    G = F < E ? z(H, F++) : 0;
    if (D++, G === 10) D = 1, B++;
    return G;
}
function P() {
    return z(H, F);
}
function Q() {
    return F;
}
function R(e14, r11) {
    return A(H, e14, r11);
}
function T(e15) {
    switch(e15){
        case 0:
        case 9:
        case 10:
        case 13:
        case 32:
            return 5;
        case 33:
        case 43:
        case 44:
        case 47:
        case 62:
        case 64:
        case 126:
        case 59:
        case 123:
        case 125:
            return 4;
        case 58:
            return 3;
        case 34:
        case 39:
        case 40:
        case 91:
            return 2;
        case 41:
        case 93:
            return 1;
    }
    return 0;
}
function U(e16) {
    return B = D = 1, E = O(H = e16), F = 0, [];
}
function V(e17) {
    return H = "", e17;
}
function W(e18) {
    return x(R(F - 1, ee(e18 === 91 ? e18 + 2 : e18 === 40 ? e18 + 1 : e18)));
}
function X(e19) {
    return V(Z(U(e19)));
}
function Y(e20) {
    while(G = P())if (G < 33) N();
    else break;
    return T(e20) > 2 || T(G) > 3 ? "" : " ";
}
function Z(e21) {
    while(N())switch(T(G)){
        case 0:
            S(ae(F - 1), e21);
            break;
        case 2:
            S(W(G), e21);
            break;
        default:
            S(d(G), e21);
    }
    return e21;
}
function _(e22, r12) {
    while(--r12 && N())if (G < 48 || G > 102 || G > 57 && G < 65 || G > 70 && G < 97) break;
    return R(e22, Q() + (r12 < 6 && P() == 32 && N() == 32));
}
function ee(e23) {
    while(N())switch(G){
        case e23:
            return F;
        case 34:
        case 39:
            if (e23 !== 34 && e23 !== 39) ee(G);
            break;
        case 40:
            if (e23 === 41) ee(e23);
            break;
        case 92:
            N();
            break;
    }
    return F;
}
function re(e24, r13) {
    while(N())if (e24 + G === 57) break;
    else if (e24 + G === 84 && P() === 47) break;
    return "/*" + R(r13, F - 1) + "*" + d(e24 === 47 ? e24 : N());
}
function ae(e25) {
    while(!T(P()))N();
    return R(e25, F);
}
function ce(e26) {
    return V(ne("", null, null, null, [
        ""
    ], e26 = U(e26), 0, [
        0
    ], e26));
}
function ne(e27, r14, a4, c2, n2, t2, s2, u1, i1) {
    var f1 = 0;
    var o1 = 0;
    var l1 = s2;
    var v1 = 0;
    var h1 = 0;
    var p1 = 0;
    var b1 = 1;
    var w1 = 1;
    var $1 = 1;
    var k1 = 0;
    var g1 = "";
    var m1 = n2;
    var x1 = t2;
    var y1 = c2;
    var z1 = g1;
    while(w1)switch(p1 = k1, k1 = N()){
        case 40:
            if (p1 != 108 && z1.charCodeAt(l1 - 1) == 58) {
                if (C(z1 += j(W(k1), "&", "&\f"), "&\f") != -1) $1 = -1;
                break;
            }
        case 34:
        case 39:
        case 91:
            z1 += W(k1);
            break;
        case 9:
        case 10:
        case 13:
        case 32:
            z1 += Y(p1);
            break;
        case 92:
            z1 += _(Q() - 1, 7);
            continue;
        case 47:
            switch(P()){
                case 42:
                case 47:
                    S(se(re(N(), Q()), r14, a4), i1);
                    break;
                default:
                    z1 += "/";
            }
            break;
        case 123 * b1:
            u1[f1++] = O(z1) * $1;
        case 125 * b1:
        case 59:
        case 0:
            switch(k1){
                case 0:
                case 125:
                    w1 = 0;
                case 59 + o1:
                    if (h1 > 0 && O(z1) - l1) S(h1 > 32 ? ue(z1 + ";", c2, a4, l1 - 1) : ue(j(z1, " ", "") + ";", c2, a4, l1 - 2), i1);
                    break;
                case 59:
                    z1 += ";";
                default:
                    S(y1 = te(z1, r14, a4, f1, o1, n2, u1, g1, m1 = [], x1 = [], l1), t2);
                    if (k1 === 123) {
                        if (o1 === 0) ne(z1, r14, y1, y1, m1, t2, l1, u1, x1);
                        else switch(v1){
                            case 100:
                            case 109:
                            case 115:
                                ne(e27, y1, y1, c2 && S(te(e27, y1, y1, 0, 0, n2, u1, g1, n2, m1 = [], l1), x1), n2, x1, l1, u1, c2 ? m1 : x1);
                                break;
                            default:
                                ne(z1, y1, y1, y1, [
                                    ""
                                ], x1, 0, u1, x1);
                        }
                    }
            }
            f1 = o1 = h1 = 0, b1 = $1 = 1, g1 = z1 = "", l1 = s2;
            break;
        case 58:
            l1 = 1 + O(z1), h1 = p1;
        default:
            if (b1 < 1) {
                if (k1 == 123) --b1;
                else if (k1 == 125 && (b1++) == 0 && L() == 125) continue;
            }
            switch(z1 += d(k1), k1 * b1){
                case 38:
                    $1 = o1 > 0 ? 1 : (z1 += "\f", -1);
                    break;
                case 44:
                    u1[f1++] = (O(z1) - 1) * $1, $1 = 1;
                    break;
                case 64:
                    if (P() === 45) z1 += W(N());
                    v1 = P(), o1 = l1 = O(g1 = z1 += ae(Q())), k1++;
                    break;
                case 45:
                    if (p1 === 45 && O(z1) == 2) b1 = 0;
            }
    }
    return t2;
}
function te(e28, r15, a5, c3, t3, s3, u2, i2, f2, o2, l2) {
    var v2 = t3 - 1;
    var h2 = t3 === 0 ? s3 : [
        ""
    ];
    var p2 = M(h2);
    for(var b2 = 0, w2 = 0, $2 = 0; b2 < c3; ++b2)for(var d1 = 0, g2 = A(e28, v2 + 1, v2 = k(w2 = u2[b2])), m2 = e28; d1 < p2; ++d1)if (m2 = x(w2 > 0 ? h2[d1] + " " + g2 : j(g2, /&\f/g, h2[d1]))) f2[$2++] = m2;
    return I(e28, r15, a5, t3 === 0 ? n : i2, f2, o2, l2);
}
function se(e29, r16, a6) {
    return I(e29, r16, a6, c, d(K()), A(e29, 2, -2), 0);
}
function ue(e30, r17, a7, c4) {
    return I(e30, r17, a7, t, A(e30, 0, c4), A(e30, c4 + 1, -1), c4);
}
function ie(c5, n3) {
    switch(m(c5, n3)){
        case 5103:
            return a + "print-" + c5 + c5;
        case 5737:
        case 4201:
        case 3177:
        case 3433:
        case 1641:
        case 4457:
        case 2921:
        case 5572:
        case 6356:
        case 5844:
        case 3191:
        case 6645:
        case 3005:
        case 6391:
        case 5879:
        case 5623:
        case 6135:
        case 4599:
        case 4855:
        case 4215:
        case 6389:
        case 5109:
        case 5365:
        case 5621:
        case 3829:
            return a + c5 + c5;
        case 5349:
        case 4246:
        case 4810:
        case 6968:
        case 2756:
            return a + c5 + r + c5 + e + c5 + c5;
        case 6828:
        case 4268:
            return a + c5 + e + c5 + c5;
        case 6165:
            return a + c5 + e + "flex-" + c5 + c5;
        case 5187:
            return a + c5 + j(c5, /(\w+).+(:[^]+)/, a + "box-$1$2" + e + "flex-$1$2") + c5;
        case 5443:
            return a + c5 + e + "flex-item-" + j(c5, /flex-|-self/, "") + c5;
        case 4675:
            return a + c5 + e + "flex-line-pack" + j(c5, /align-content|flex-|-self/, "") + c5;
        case 5548:
            return a + c5 + e + j(c5, "shrink", "negative") + c5;
        case 5292:
            return a + c5 + e + j(c5, "basis", "preferred-size") + c5;
        case 6060:
            return a + "box-" + j(c5, "-grow", "") + a + c5 + e + j(c5, "grow", "positive") + c5;
        case 4554:
            return a + j(c5, /([^-])(transform)/g, "$1" + a + "$2") + c5;
        case 6187:
            return j(j(j(c5, /(zoom-|grab)/, a + "$1"), /(image-set)/, a + "$1"), c5, "") + c5;
        case 5495:
        case 3959:
            return j(c5, /(image-set\([^]*)/, a + "$1" + "$`$1");
        case 4968:
            return j(j(c5, /(.+:)(flex-)?(.*)/, a + "box-pack:$3" + e + "flex-pack:$3"), /s.+-b[^;]+/, "justify") + a + c5 + c5;
        case 4095:
        case 3583:
        case 4068:
        case 2532:
            return j(c5, /(.+)-inline(.+)/, a + "$1$2") + c5;
        case 8116:
        case 7059:
        case 5753:
        case 5535:
        case 5445:
        case 5701:
        case 4933:
        case 4677:
        case 5533:
        case 5789:
        case 5021:
        case 4765:
            if (O(c5) - 1 - n3 > 6) switch(z(c5, n3 + 1)){
                case 109:
                    if (z(c5, n3 + 4) !== 45) break;
                case 102:
                    return j(c5, /(.+:)(.+)-([^]+)/, "$1" + a + "$2-$3" + "$1" + r + (z(c5, n3 + 3) == 108 ? "$3" : "$2-$3")) + c5;
                case 115:
                    return ~C(c5, "stretch") ? ie(j(c5, "stretch", "fill-available"), n3) + c5 : c5;
            }
            break;
        case 4949:
            if (z(c5, n3 + 1) !== 115) break;
        case 6444:
            switch(z(c5, O(c5) - 3 - (~C(c5, "!important") && 10))){
                case 107:
                    return j(c5, ":", ":" + a) + c5;
                case 101:
                    return j(c5, /(.+:)([^;!]+)(;|!.+)?/, "$1" + a + (z(c5, 14) === 45 ? "inline-" : "") + "box$3" + "$1" + a + "$2$3" + "$1" + e + "$2box$3") + c5;
            }
            break;
        case 5936:
            switch(z(c5, n3 + 11)){
                case 114:
                    return a + c5 + e + j(c5, /[svh]\w+-[tblr]{2}/, "tb") + c5;
                case 108:
                    return a + c5 + e + j(c5, /[svh]\w+-[tblr]{2}/, "tb-rl") + c5;
                case 45:
                    return a + c5 + e + j(c5, /[svh]\w+-[tblr]{2}/, "lr") + c5;
            }
            return a + c5 + e + c5 + c5;
    }
    return c5;
}
function fe(e31, r18) {
    var a8 = "";
    var c6 = M(e31);
    for(var n4 = 0; n4 < c6; n4++)a8 += r18(e31[n4], n4, e31, r18) || "";
    return a8;
}
function oe(e32, r, a9, s4) {
    switch(e32.type){
        case i:
        case t:
            return e32.return = e32.return || e32.value;
        case c:
            return "";
        case p:
            return e32.return = e32.value + "{" + fe(e32.children, s4) + "}";
        case n:
            e32.value = e32.props.join(",");
    }
    return O(a9 = fe(e32.children, s4)) ? e32.return = e32.value + "{" + a9 + "}" : "";
}
function le(e33) {
    var r19 = M(e33);
    return function(a10, c7, n5, t4) {
        var s5 = "";
        for(var u3 = 0; u3 < r19; u3++)s5 += e33[u3](a10, c7, n5, t4) || "";
        return s5;
    };
}
function ve(e34) {
    return function(r20) {
        if (!r20.root) {
            if (r20 = r20.return) e34(r20);
        }
    };
}
function he(c8, s, u, i3) {
    if (c8.length > -1) {
        if (!c8.return) switch(c8.type){
            case t:
                c8.return = ie(c8.value, c8.length);
                break;
            case p:
                return fe([
                    J(c8, {
                        value: j(c8.value, "@", "@" + a)
                    })
                ], i3);
            case n:
                if (c8.length) return q(c8.props, function(n6) {
                    switch(y(n6, /(::plac\w+|:read-\w+)/)){
                        case ":read-only":
                        case ":read-write":
                            return fe([
                                J(c8, {
                                    props: [
                                        j(n6, /:(read-\w+)/, ":" + r + "$1")
                                    ]
                                })
                            ], i3);
                        case "::placeholder":
                            return fe([
                                J(c8, {
                                    props: [
                                        j(n6, /:(plac\w+)/, ":" + a + "input-$1")
                                    ]
                                }),
                                J(c8, {
                                    props: [
                                        j(n6, /:(plac\w+)/, ":" + r + "$1")
                                    ]
                                }),
                                J(c8, {
                                    props: [
                                        j(n6, /:(plac\w+)/, e + "input-$1")
                                    ]
                                })
                            ], i3);
                    }
                    return "";
                });
        }
    }
}
function pe(e35) {
    switch(e35.type){
        case n:
            e35.props = e35.props.map(function(r21) {
                return q(X(r21), function(r22, a11, c9) {
                    switch(z(r22, 0)){
                        case 12:
                            return A(r22, 1, O(r22));
                        case 0:
                        case 40:
                        case 43:
                        case 62:
                        case 126:
                            return r22;
                        case 58:
                            if (c9[++a11] === "global") c9[a11] = "", c9[++a11] = "\f" + A(c9[a11], a11 = 1, -1);
                        case 32:
                            return a11 === 1 ? "" : r22;
                        default:
                            switch(a11){
                                case 0:
                                    e35 = r22;
                                    return M(c9) > 1 ? "" : r22;
                                case a11 = M(c9) - 1:
                                case 2:
                                    return a11 === 2 ? r22 + e35 + e35 : r22 + e35;
                                default:
                                    return r22;
                            }
                    }
                });
            });
    }
}

},{"@parcel/transformer-js/src/esmodule-helpers.js":"gkKU3"}],"iicyL":[function(require,module,exports) {
var parcelHelpers = require("@parcel/transformer-js/src/esmodule-helpers.js");
parcelHelpers.defineInteropFlag(exports);
var weakMemoize = function weakMemoize(func) {
    // $FlowFixMe flow doesn't include all non-primitive types as allowed for weakmaps
    var cache = new WeakMap();
    return function(arg) {
        if (cache.has(arg)) // $FlowFixMe
        return cache.get(arg);
        var ret = func(arg);
        cache.set(arg, ret);
        return ret;
    };
};
exports.default = weakMemoize;

},{"@parcel/transformer-js/src/esmodule-helpers.js":"gkKU3"}],"WW7h8":[function(require,module,exports) {
var parcelHelpers = require("@parcel/transformer-js/src/esmodule-helpers.js");
parcelHelpers.defineInteropFlag(exports);
function memoize(fn) {
    var cache = Object.create(null);
    return function(arg) {
        if (cache[arg] === undefined) cache[arg] = fn(arg);
        return cache[arg];
    };
}
exports.default = memoize;

},{"@parcel/transformer-js/src/esmodule-helpers.js":"gkKU3"}],"kS2E2":[function(require,module,exports) {
var parcelHelpers = require("@parcel/transformer-js/src/esmodule-helpers.js");
parcelHelpers.defineInteropFlag(exports);
parcelHelpers.export(exports, "serializeStyles", ()=>serializeStyles
);
var _hash = require("@emotion/hash");
var _hashDefault = parcelHelpers.interopDefault(_hash);
var _unitless = require("@emotion/unitless");
var _unitlessDefault = parcelHelpers.interopDefault(_unitless);
var _memoize = require("@emotion/memoize");
var _memoizeDefault = parcelHelpers.interopDefault(_memoize);
var ILLEGAL_ESCAPE_SEQUENCE_ERROR = "You have illegal escape sequence in your template literal, most likely inside content's property value.\nBecause you write your CSS inside a JavaScript string you actually have to do double escaping, so for example \"content: '\\00d7';\" should become \"content: '\\\\00d7';\".\nYou can read more about this here:\nhttps://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Template_literals#ES2018_revision_of_illegal_escape_sequences";
var UNDEFINED_AS_OBJECT_KEY_ERROR = "You have passed in falsy value as style object's key (can happen when in example you pass unexported component as computed key).";
var hyphenateRegex = /[A-Z]|^ms/g;
var animationRegex = /_EMO_([^_]+?)_([^]*?)_EMO_/g;
var isCustomProperty = function isCustomProperty(property) {
    return property.charCodeAt(1) === 45;
};
var isProcessableValue = function isProcessableValue(value) {
    return value != null && typeof value !== 'boolean';
};
var processStyleName = /* #__PURE__ */ _memoizeDefault.default(function(styleName) {
    return isCustomProperty(styleName) ? styleName : styleName.replace(hyphenateRegex, '-$&').toLowerCase();
});
var processStyleValue = function processStyleValue(key, value) {
    switch(key){
        case 'animation':
        case 'animationName':
            if (typeof value === 'string') return value.replace(animationRegex, function(match, p1, p2) {
                cursor = {
                    name: p1,
                    styles: p2,
                    next: cursor
                };
                return p1;
            });
    }
    if (_unitlessDefault.default[key] !== 1 && !isCustomProperty(key) && typeof value === 'number' && value !== 0) return value + 'px';
    return value;
};
var contentValuePattern = /(var|attr|counters?|url|(((repeating-)?(linear|radial))|conic)-gradient)\(|(no-)?(open|close)-quote/;
var contentValues = [
    'normal',
    'none',
    'initial',
    'inherit',
    'unset'
];
var oldProcessStyleValue = processStyleValue;
var msPattern = /^-ms-/;
var hyphenPattern = /-(.)/g;
var hyphenatedCache = {};
processStyleValue = function processStyleValue(key, value) {
    if (key === 'content') {
        if (typeof value !== 'string' || contentValues.indexOf(value) === -1 && !contentValuePattern.test(value) && (value.charAt(0) !== value.charAt(value.length - 1) || value.charAt(0) !== '"' && value.charAt(0) !== "'")) throw new Error("You seem to be using a value for 'content' without quotes, try replacing it with `content: '\"" + value + "\"'`");
    }
    var processed = oldProcessStyleValue(key, value);
    if (processed !== '' && !isCustomProperty(key) && key.indexOf('-') !== -1 && hyphenatedCache[key] === undefined) {
        hyphenatedCache[key] = true;
        console.error("Using kebab-case for css properties in objects is not supported. Did you mean " + key.replace(msPattern, 'ms-').replace(hyphenPattern, function(str, _char) {
            return _char.toUpperCase();
        }) + "?");
    }
    return processed;
};
function handleInterpolation(mergedProps, registered, interpolation) {
    if (interpolation == null) return '';
    if (interpolation.__emotion_styles !== undefined) {
        if (interpolation.toString() === 'NO_COMPONENT_SELECTOR') throw new Error('Component selectors can only be used in conjunction with @emotion/babel-plugin.');
        return interpolation;
    }
    switch(typeof interpolation){
        case 'boolean':
            return '';
        case 'object':
            if (interpolation.anim === 1) {
                cursor = {
                    name: interpolation.name,
                    styles: interpolation.styles,
                    next: cursor
                };
                return interpolation.name;
            }
            if (interpolation.styles !== undefined) {
                var next = interpolation.next;
                if (next !== undefined) // not the most efficient thing ever but this is a pretty rare case
                // and there will be very few iterations of this generally
                while(next !== undefined){
                    cursor = {
                        name: next.name,
                        styles: next.styles,
                        next: cursor
                    };
                    next = next.next;
                }
                var styles = interpolation.styles + ";";
                if (interpolation.map !== undefined) styles += interpolation.map;
                return styles;
            }
            return createStringFromObject(mergedProps, registered, interpolation);
        case 'function':
            if (mergedProps !== undefined) {
                var previousCursor = cursor;
                var result = interpolation(mergedProps);
                cursor = previousCursor;
                return handleInterpolation(mergedProps, registered, result);
            } else console.error("Functions that are interpolated in css calls will be stringified.\nIf you want to have a css call based on props, create a function that returns a css call like this\nlet dynamicStyle = (props) => css`color: ${props.color}`\nIt can be called directly with props or interpolated in a styled call like this\nlet SomeComponent = styled('div')`${dynamicStyle}`");
            break;
        case 'string':
            var matched = [];
            var replaced = interpolation.replace(animationRegex, function(match, p1, p2) {
                var fakeVarName = "animation" + matched.length;
                matched.push("const " + fakeVarName + " = keyframes`" + p2.replace(/^@keyframes animation-\w+/, '') + "`");
                return "${" + fakeVarName + "}";
            });
            if (matched.length) console.error("`keyframes` output got interpolated into plain string, please wrap it with `css`.\n\nInstead of doing this:\n\n" + [].concat(matched, [
                "`" + replaced + "`"
            ]).join('\n') + '\n\nYou should wrap it with `css` like this:\n\n' + ("css`" + replaced + "`"));
            break;
    } // finalize string values (regular strings and functions interpolated into css calls)
    if (registered == null) return interpolation;
    var cached = registered[interpolation];
    return cached !== undefined ? cached : interpolation;
}
function createStringFromObject(mergedProps, registered, obj) {
    var string = '';
    if (Array.isArray(obj)) for(var i = 0; i < obj.length; i++)string += handleInterpolation(mergedProps, registered, obj[i]) + ";";
    else for(var _key in obj){
        var value = obj[_key];
        if (typeof value !== 'object') {
            if (registered != null && registered[value] !== undefined) string += _key + "{" + registered[value] + "}";
            else if (isProcessableValue(value)) string += processStyleName(_key) + ":" + processStyleValue(_key, value) + ";";
        } else {
            if (_key === 'NO_COMPONENT_SELECTOR' && true) throw new Error('Component selectors can only be used in conjunction with @emotion/babel-plugin.');
            if (Array.isArray(value) && typeof value[0] === 'string' && (registered == null || registered[value[0]] === undefined)) {
                for(var _i = 0; _i < value.length; _i++)if (isProcessableValue(value[_i])) string += processStyleName(_key) + ":" + processStyleValue(_key, value[_i]) + ";";
            } else {
                var interpolated = handleInterpolation(mergedProps, registered, value);
                switch(_key){
                    case 'animation':
                    case 'animationName':
                        string += processStyleName(_key) + ":" + interpolated + ";";
                        break;
                    default:
                        if (_key === 'undefined') console.error(UNDEFINED_AS_OBJECT_KEY_ERROR);
                        string += _key + "{" + interpolated + "}";
                }
            }
        }
    }
    return string;
}
var labelPattern = /label:\s*([^\s;\n{]+)\s*(;|$)/g;
var sourceMapPattern;
sourceMapPattern = /\/\*#\ssourceMappingURL=data:application\/json;\S+\s+\*\//g;
// keyframes are stored on the SerializedStyles object as a linked list
var cursor;
var serializeStyles = function serializeStyles(args, registered, mergedProps) {
    if (args.length === 1 && typeof args[0] === 'object' && args[0] !== null && args[0].styles !== undefined) return args[0];
    var stringMode = true;
    var styles = '';
    cursor = undefined;
    var strings = args[0];
    if (strings == null || strings.raw === undefined) {
        stringMode = false;
        styles += handleInterpolation(mergedProps, registered, strings);
    } else {
        if (strings[0] === undefined) console.error(ILLEGAL_ESCAPE_SEQUENCE_ERROR);
        styles += strings[0];
    } // we start at 1 since we've already handled the first arg
    for(var i = 1; i < args.length; i++){
        styles += handleInterpolation(mergedProps, registered, args[i]);
        if (stringMode) {
            if (strings[i] === undefined) console.error(ILLEGAL_ESCAPE_SEQUENCE_ERROR);
            styles += strings[i];
        }
    }
    var sourceMap;
    styles = styles.replace(sourceMapPattern, function(match) {
        sourceMap = match;
        return '';
    });
    labelPattern.lastIndex = 0;
    var identifierName = '';
    var match1; // https://esbench.com/bench/5b809c2cf2949800a0f61fb5
    while((match1 = labelPattern.exec(styles)) !== null)identifierName += '-' + match1[1];
    var name = _hashDefault.default(styles) + identifierName;
    // $FlowFixMe SerializedStyles type doesn't have toString property (and we don't want to add it)
    return {
        name: name,
        styles: styles,
        map: sourceMap,
        next: cursor,
        toString: function toString() {
            return "You have tried to stringify object returned from `css` function. It isn't supposed to be used directly (e.g. as value of the `className` prop), but rather handed to emotion so it can handle it (e.g. as value of `css` prop).";
        }
    };
};

},{"@emotion/hash":"h4XqR","@emotion/unitless":"pVndT","@emotion/memoize":"WW7h8","@parcel/transformer-js/src/esmodule-helpers.js":"gkKU3"}],"h4XqR":[function(require,module,exports) {
var parcelHelpers = require("@parcel/transformer-js/src/esmodule-helpers.js");
parcelHelpers.defineInteropFlag(exports);
/* eslint-disable */ // Inspired by https://github.com/garycourt/murmurhash-js
// Ported from https://github.com/aappleby/smhasher/blob/61a0530f28277f2e850bfc39600ce61d02b518de/src/MurmurHash2.cpp#L37-L86
function murmur2(str) {
    // 'm' and 'r' are mixing constants generated offline.
    // They're not really 'magic', they just happen to work well.
    // const m = 0x5bd1e995;
    // const r = 24;
    // Initialize the hash
    var h = 0; // Mix 4 bytes at a time into the hash
    var k, i = 0, len = str.length;
    for(; len >= 4; ++i, len -= 4){
        k = str.charCodeAt(i) & 0xff | (str.charCodeAt(++i) & 0xff) << 8 | (str.charCodeAt(++i) & 0xff) << 16 | (str.charCodeAt(++i) & 0xff) << 24;
        k = /* Math.imul(k, m): */ (k & 0xffff) * 0x5bd1e995 + ((k >>> 16) * 0xe995 << 16);
        k ^= /* k >>> r: */ k >>> 24;
        h = /* Math.imul(k, m): */ (k & 0xffff) * 0x5bd1e995 + ((k >>> 16) * 0xe995 << 16) ^ /* Math.imul(h, m): */ (h & 0xffff) * 0x5bd1e995 + ((h >>> 16) * 0xe995 << 16);
    } // Handle the last few bytes of the input array
    switch(len){
        case 3:
            h ^= (str.charCodeAt(i + 2) & 0xff) << 16;
        case 2:
            h ^= (str.charCodeAt(i + 1) & 0xff) << 8;
        case 1:
            h ^= str.charCodeAt(i) & 0xff;
            h = /* Math.imul(h, m): */ (h & 0xffff) * 0x5bd1e995 + ((h >>> 16) * 0xe995 << 16);
    } // Do a few final mixes of the hash to ensure the last few
    // bytes are well-incorporated.
    h ^= h >>> 13;
    h = /* Math.imul(h, m): */ (h & 0xffff) * 0x5bd1e995 + ((h >>> 16) * 0xe995 << 16);
    return ((h ^ h >>> 15) >>> 0).toString(36);
}
exports.default = murmur2;

},{"@parcel/transformer-js/src/esmodule-helpers.js":"gkKU3"}],"pVndT":[function(require,module,exports) {
var parcelHelpers = require("@parcel/transformer-js/src/esmodule-helpers.js");
parcelHelpers.defineInteropFlag(exports);
var unitlessKeys = {
    animationIterationCount: 1,
    borderImageOutset: 1,
    borderImageSlice: 1,
    borderImageWidth: 1,
    boxFlex: 1,
    boxFlexGroup: 1,
    boxOrdinalGroup: 1,
    columnCount: 1,
    columns: 1,
    flex: 1,
    flexGrow: 1,
    flexPositive: 1,
    flexShrink: 1,
    flexNegative: 1,
    flexOrder: 1,
    gridRow: 1,
    gridRowEnd: 1,
    gridRowSpan: 1,
    gridRowStart: 1,
    gridColumn: 1,
    gridColumnEnd: 1,
    gridColumnSpan: 1,
    gridColumnStart: 1,
    msGridRow: 1,
    msGridRowSpan: 1,
    msGridColumn: 1,
    msGridColumnSpan: 1,
    fontWeight: 1,
    lineHeight: 1,
    opacity: 1,
    order: 1,
    orphans: 1,
    tabSize: 1,
    widows: 1,
    zIndex: 1,
    zoom: 1,
    WebkitLineClamp: 1,
    // SVG-related properties
    fillOpacity: 1,
    floodOpacity: 1,
    stopOpacity: 1,
    strokeDasharray: 1,
    strokeDashoffset: 1,
    strokeMiterlimit: 1,
    strokeOpacity: 1,
    strokeWidth: 1
};
exports.default = unitlessKeys;

},{"@parcel/transformer-js/src/esmodule-helpers.js":"gkKU3"}],"6UI8e":[function(require,module,exports) {
var parcelHelpers = require("@parcel/transformer-js/src/esmodule-helpers.js");
parcelHelpers.defineInteropFlag(exports);
parcelHelpers.export(exports, "getRegisteredStyles", ()=>getRegisteredStyles
);
parcelHelpers.export(exports, "insertStyles", ()=>insertStyles
);
parcelHelpers.export(exports, "registerStyles", ()=>registerStyles
);
var isBrowser = true;
function getRegisteredStyles(registered, registeredStyles, classNames) {
    var rawClassName = '';
    classNames.split(' ').forEach(function(className) {
        if (registered[className] !== undefined) registeredStyles.push(registered[className] + ";");
        else rawClassName += className + " ";
    });
    return rawClassName;
}
var registerStyles = function registerStyles(cache, serialized, isStringTag) {
    var className = cache.key + "-" + serialized.name;
    if (// class name could be used further down
    // the tree but if it's a string tag, we know it won't
    // so we don't have to add it to registered cache.
    // this improves memory usage since we can avoid storing the whole style string
    (isStringTag === false || // in node since emotion-server relies on whether a style is in
    // the registered cache to know whether a style is global or not
    // also, note that this check will be dead code eliminated in the browser
    isBrowser === false) && cache.registered[className] === undefined) cache.registered[className] = serialized.styles;
};
var insertStyles = function insertStyles(cache, serialized, isStringTag) {
    registerStyles(cache, serialized, isStringTag);
    var className = cache.key + "-" + serialized.name;
    if (cache.inserted[serialized.name] === undefined) {
        var current = serialized;
        do {
            var maybeStyles = cache.insert(serialized === current ? "." + className : '', current, cache.sheet, true);
            current = current.next;
        }while (current !== undefined)
    }
};

},{"@parcel/transformer-js/src/esmodule-helpers.js":"gkKU3"}],"fjyst":[function(require,module,exports) {
var parcelHelpers = require("@parcel/transformer-js/src/esmodule-helpers.js");
parcelHelpers.defineInteropFlag(exports);
var _esRegexpExecJs = require("core-js/modules/es.regexp.exec.js");
var _esStringReplaceJs = require("core-js/modules/es.string.replace.js");
var _esDateToStringJs = require("core-js/modules/es.date.to-string.js");
var _esObjectToStringJs = require("core-js/modules/es.object.to-string.js");
var _esRegexpToStringJs = require("core-js/modules/es.regexp.to-string.js");
/* eslint no-magic-numbers: "off" */ var _mathRandom = require("math-random");
var _mathRandomDefault = parcelHelpers.interopDefault(_mathRandom);
function useCSSKey() {
    return _mathRandomDefault.default().toString(26).substr(2, 5).replace(/[0-9]/g, function(value) {
        return String.fromCharCode(value.charCodeAt(0) + 65);
    });
}
exports.default = useCSSKey;

},{"core-js/modules/es.regexp.exec.js":"pNALB","core-js/modules/es.string.replace.js":"bE0gl","core-js/modules/es.date.to-string.js":"adsY7","core-js/modules/es.object.to-string.js":"9XvHS","core-js/modules/es.regexp.to-string.js":"8bEcW","math-random":"ipuxK","@parcel/transformer-js/src/esmodule-helpers.js":"gkKU3"}],"adsY7":[function(require,module,exports) {
var redefine = require('../internals/redefine');
var DatePrototype = Date.prototype;
var INVALID_DATE = 'Invalid Date';
var TO_STRING = 'toString';
var nativeDateToString = DatePrototype[TO_STRING];
var getTime = DatePrototype.getTime;
// `Date.prototype.toString` method
// https://tc39.es/ecma262/#sec-date.prototype.tostring
if (String(new Date(NaN)) != INVALID_DATE) redefine(DatePrototype, TO_STRING, function toString() {
    var value = getTime.call(this);
    // eslint-disable-next-line no-self-compare -- NaN check
    return value === value ? nativeDateToString.call(this) : INVALID_DATE;
});

},{"../internals/redefine":"1ZKnU"}],"9XvHS":[function(require,module,exports) {
var TO_STRING_TAG_SUPPORT = require('../internals/to-string-tag-support');
var redefine = require('../internals/redefine');
var toString = require('../internals/object-to-string');
// `Object.prototype.toString` method
// https://tc39.es/ecma262/#sec-object.prototype.tostring
if (!TO_STRING_TAG_SUPPORT) redefine(Object.prototype, 'toString', toString, {
    unsafe: true
});

},{"../internals/to-string-tag-support":"3Do6Z","../internals/redefine":"1ZKnU","../internals/object-to-string":"9etkB"}],"9etkB":[function(require,module,exports) {
'use strict';
var TO_STRING_TAG_SUPPORT = require('../internals/to-string-tag-support');
var classof = require('../internals/classof');
// `Object.prototype.toString` method implementation
// https://tc39.es/ecma262/#sec-object.prototype.tostring
module.exports = TO_STRING_TAG_SUPPORT ? ({}).toString : function toString() {
    return '[object ' + classof(this) + ']';
};

},{"../internals/to-string-tag-support":"3Do6Z","../internals/classof":"dKT7A"}],"8bEcW":[function(require,module,exports) {
'use strict';
var PROPER_FUNCTION_NAME = require('../internals/function-name').PROPER;
var redefine = require('../internals/redefine');
var anObject = require('../internals/an-object');
var $toString = require('../internals/to-string');
var fails = require('../internals/fails');
var flags = require('../internals/regexp-flags');
var TO_STRING = 'toString';
var RegExpPrototype = RegExp.prototype;
var nativeToString = RegExpPrototype[TO_STRING];
var NOT_GENERIC = fails(function() {
    return nativeToString.call({
        source: 'a',
        flags: 'b'
    }) != '/a/b';
});
// FF44- RegExp#toString has a wrong name
var INCORRECT_NAME = PROPER_FUNCTION_NAME && nativeToString.name != TO_STRING;
// `RegExp.prototype.toString` method
// https://tc39.es/ecma262/#sec-regexp.prototype.tostring
if (NOT_GENERIC || INCORRECT_NAME) redefine(RegExp.prototype, TO_STRING, function toString() {
    var R = anObject(this);
    var p = $toString(R.source);
    var rf = R.flags;
    var f = $toString(rf === undefined && R instanceof RegExp && !('flags' in RegExpPrototype) ? flags.call(R) : rf);
    return '/' + p + '/' + f;
}, {
    unsafe: true
});

},{"../internals/function-name":"l6Kgd","../internals/redefine":"1ZKnU","../internals/an-object":"4isCr","../internals/to-string":"a1yl4","../internals/fails":"hL6D2","../internals/regexp-flags":"9bz1x"}],"ipuxK":[function(require,module,exports) {
module.exports = function(crypto) {
    if (!crypto) return Math.random;
    var max = Math.pow(2, 32);
    var u32 = new Uint32Array(1);
    return function random() {
        return crypto.getRandomValues(u32)[0] / max;
    };
}(require('./crypto'));

},{"./crypto":"bohi6"}],"bohi6":[function(require,module,exports) {
var global = typeof window !== 'undefined' ? window : self;
module.exports = global.crypto || global.msCrypto;

},{}],"1D0Dy":[function(require,module,exports) {
var parcelHelpers = require("@parcel/transformer-js/src/esmodule-helpers.js");
parcelHelpers.defineInteropFlag(exports);
var _slicedToArray = require("@babel/runtime-corejs3/helpers/slicedToArray");
var _slicedToArrayDefault = parcelHelpers.interopDefault(_slicedToArray);
var _toConsumableArray = require("@babel/runtime-corejs3/helpers/toConsumableArray");
var _toConsumableArrayDefault = parcelHelpers.interopDefault(_toConsumableArray);
var _concat = require("@babel/runtime-corejs3/core-js-stable/instance/concat");
var _concatDefault = parcelHelpers.interopDefault(_concat);
var _isArray = require("@babel/runtime-corejs3/core-js-stable/array/is-array");
var _isArrayDefault = parcelHelpers.interopDefault(_isArray);
var _forEach = require("@babel/runtime-corejs3/core-js-stable/instance/for-each");
var _forEachDefault = parcelHelpers.interopDefault(_forEach);
/* eslint no-console: ["off"] */ var _styleConsole = require("./styleConsole");
var _styleConsoleDefault = parcelHelpers.interopDefault(_styleConsole);
function format(category, arg0) {
    var _context, _context2;
    for(var _len = arguments.length, args = new Array(_len > 2 ? _len - 2 : 0), _key = 2; _key < _len; _key++)args[_key - 2] = arguments[_key];
    return _concatDefault.default(_context = [
        _concatDefault.default(_context2 = "%c".concat(category, "%c ")).call(_context2, arg0)
    ]).call(_context, _toConsumableArrayDefault.default(_styleConsoleDefault.default('green', 'white')), args);
}
function debug(category) {
    var _ref = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : {}, _ref$force = _ref.force, force = _ref$force === void 0 ? false : _ref$force;
    if (!force) return function() {
        return 0;
    };
    return function() {
        for(var _len2 = arguments.length, args = new Array(_len2), _key2 = 0; _key2 < _len2; _key2++)args[_key2] = arguments[_key2];
        if (!args.length) return;
        var _args = args, _args2 = _slicedToArrayDefault.default(_args, 1), arg0 = _args2[0];
        if (typeof arg0 === 'function') args = arg0();
        var lines = _isArrayDefault.default(args[0]) ? args : [
            args
        ];
        var oneLiner = lines.length === 1;
        _forEachDefault.default(lines).call(lines, function(line, index) {
            if (oneLiner) {
                var _console, _context3;
                (_console = console).log.apply(_console, _toConsumableArrayDefault.default(format.apply(void 0, _concatDefault.default(_context3 = [
                    category
                ]).call(_context3, _toConsumableArrayDefault.default(line)))));
            } else if (index) {
                var _console2;
                (_console2 = console).log.apply(_console2, _toConsumableArrayDefault.default(_isArrayDefault.default(line) ? line : [
                    line
                ]));
            } else {
                var _console3, _context4;
                (_console3 = console).groupCollapsed.apply(_console3, _toConsumableArrayDefault.default(format.apply(void 0, _concatDefault.default(_context4 = [
                    category
                ]).call(_context4, _toConsumableArrayDefault.default(line)))));
            }
        });
        oneLiner || console.groupEnd();
    };
}
exports.default = debug;

},{"@babel/runtime-corejs3/helpers/slicedToArray":"36E7Q","@babel/runtime-corejs3/helpers/toConsumableArray":"k0LvR","@babel/runtime-corejs3/core-js-stable/instance/concat":"tXYDQ","@babel/runtime-corejs3/core-js-stable/array/is-array":"9wWJ7","@babel/runtime-corejs3/core-js-stable/instance/for-each":"fi9Yx","./styleConsole":"1rrzA","@parcel/transformer-js/src/esmodule-helpers.js":"gkKU3"}],"9wWJ7":[function(require,module,exports) {
module.exports = require("core-js-pure/stable/array/is-array");

},{"core-js-pure/stable/array/is-array":"8ekNG"}],"1rrzA":[function(require,module,exports) {
var parcelHelpers = require("@parcel/transformer-js/src/esmodule-helpers.js");
parcelHelpers.defineInteropFlag(exports);
function styleConsole(backgroundColor) {
    var color = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : 'white';
    var styles = "background-color: ".concat(backgroundColor, "; border-radius: 4px; padding: 2px 4px;");
    if (color) styles += " color: ".concat(color, ";");
    return [
        styles,
        ''
    ];
}
exports.default = styleConsole;

},{"@parcel/transformer-js/src/esmodule-helpers.js":"gkKU3"}],"cL9HT":[function(require,module,exports) {
var parcelHelpers = require("@parcel/transformer-js/src/esmodule-helpers.js");
parcelHelpers.defineInteropFlag(exports);
var _now = require("@babel/runtime-corejs3/core-js-stable/date/now");
var _nowDefault = parcelHelpers.interopDefault(_now);
var _esFunctionNameJs = require("core-js/modules/es.function.name.js");
var _react = require("react");
var _debounce = require("./debounce");
var _debounceDefault = parcelHelpers.interopDefault(_debounce);
var EventSpy = function EventSpy(_ref) {
    var debounce = _ref.debounce, name = _ref.name, onEvent = _ref.onEvent, target = _ref.target;
    // We need to save the "onEvent" to ref.
    // This is because "onEvent" may change from time to time, but debounce may still fire to the older callback.
    var onEventRef = _react.useRef();
    onEventRef.current = onEvent;
    var debouncer = _react.useMemo(function() {
        return _debounceDefault.default(function(event) {
            var current = onEventRef.current;
            current && current(event);
        }, debounce);
    }, [
        debounce,
        onEventRef
    ]);
    var handleEvent = _react.useCallback(function(event) {
        event.timeStampLow = _nowDefault.default();
        debouncer(event);
    }, [
        debouncer
    ]);
    _react.useLayoutEffect(function() {
        target.addEventListener(name, handleEvent, {
            passive: true
        });
        handleEvent({
            target: target,
            type: name
        });
        return function() {
            return target.removeEventListener(name, handleEvent);
        };
    }, [
        name,
        handleEvent,
        target
    ]);
    return false;
};
EventSpy.defaultProps = {
    debounce: 200
};
exports.default = EventSpy;

},{"@babel/runtime-corejs3/core-js-stable/date/now":"j7X90","core-js/modules/es.function.name.js":"glBcr","react":"21dqq","./debounce":"kgSB4","@parcel/transformer-js/src/esmodule-helpers.js":"gkKU3"}],"glBcr":[function(require,module,exports) {
var DESCRIPTORS = require('../internals/descriptors');
var FUNCTION_NAME_EXISTS = require('../internals/function-name').EXISTS;
var defineProperty = require('../internals/object-define-property').f;
var FunctionPrototype = Function.prototype;
var FunctionPrototypeToString = FunctionPrototype.toString;
var nameRE = /^\s*function ([^ (]*)/;
var NAME = 'name';
// Function instances `.name` property
// https://tc39.es/ecma262/#sec-function-instances-name
if (DESCRIPTORS && !FUNCTION_NAME_EXISTS) defineProperty(FunctionPrototype, NAME, {
    configurable: true,
    get: function() {
        try {
            return FunctionPrototypeToString.call(this).match(nameRE)[1];
        } catch (error) {
            return '';
        }
    }
});

},{"../internals/descriptors":"92ZIi","../internals/function-name":"l6Kgd","../internals/object-define-property":"iJR4w"}],"kgSB4":[function(require,module,exports) {
var parcelHelpers = require("@parcel/transformer-js/src/esmodule-helpers.js");
parcelHelpers.defineInteropFlag(exports);
var _now = require("@babel/runtime-corejs3/core-js-stable/date/now");
var _nowDefault = parcelHelpers.interopDefault(_now);
var _setTimeout = require("@babel/runtime-corejs3/core-js-stable/set-timeout");
var _setTimeoutDefault = parcelHelpers.interopDefault(_setTimeout);
exports.default = function(fn, ms) {
    if (!ms) return fn;
    var last = 0;
    var timeout = null;
    return function() {
        for(var _len = arguments.length, args = new Array(_len), _key = 0; _key < _len; _key++)args[_key] = arguments[_key];
        var now = _nowDefault.default();
        if (now - last > ms) {
            fn.apply(void 0, args);
            last = now;
        } else {
            clearTimeout(timeout);
            timeout = _setTimeoutDefault.default(function() {
                fn.apply(void 0, args);
                last = _nowDefault.default();
            }, Math.max(0, ms - now + last));
        }
    };
};

},{"@babel/runtime-corejs3/core-js-stable/date/now":"j7X90","@babel/runtime-corejs3/core-js-stable/set-timeout":"krONS","@parcel/transformer-js/src/esmodule-helpers.js":"gkKU3"}],"krONS":[function(require,module,exports) {
module.exports = require("core-js-pure/stable/set-timeout");

},{"core-js-pure/stable/set-timeout":"iSKg2"}],"iSKg2":[function(require,module,exports) {
require('../modules/web.timers');
var path = require('../internals/path');
module.exports = path.setTimeout;

},{"../modules/web.timers":"KNLky","../internals/path":"8QO0z"}],"9BVSj":[function(require,module,exports) {
var parcelHelpers = require("@parcel/transformer-js/src/esmodule-helpers.js");
parcelHelpers.defineInteropFlag(exports);
var _esFunctionNameJs = require("core-js/modules/es.function.name.js");
var _sign = require("@babel/runtime-corejs3/core-js-stable/math/sign");
var _signDefault = parcelHelpers.interopDefault(_sign);
var _now = require("@babel/runtime-corejs3/core-js-stable/date/now");
var _nowDefault = parcelHelpers.interopDefault(_now);
/* eslint no-magic-numbers: ["error", { "ignore": [0, 1, 1.5, 5] }] */ var _propTypes = require("prop-types");
var _propTypesDefault = parcelHelpers.interopDefault(_propTypes);
var _react = require("react");
function squareStepper(current, to) {
    var sign = _signDefault.default(to - current);
    var step1 = Math.sqrt(Math.abs(to - current));
    var next = current + step1 * sign;
    if (sign > 0) return Math.min(to, next);
    return Math.max(to, next);
}
function step(from, to, stepper, index) {
    var next = from;
    for(var i = 0; i < index; i++)next = stepper(next, to);
    return next;
}
var SpineTo = function SpineTo(_ref) {
    var name1 = _ref.name, onEnd = _ref.onEnd, target = _ref.target, value = _ref.value;
    var animator = _react.useRef();
    var animate = _react.useCallback(function(name, from, to, index) {
        var start = arguments.length > 4 && arguments[4] !== undefined ? arguments[4] : _nowDefault.default();
        if (to === '100%' || typeof to === 'number') {
            cancelAnimationFrame(animator.current);
            animator.current = requestAnimationFrame(function() {
                if (target) {
                    var toNumber = to === '100%' ? target.scrollHeight - target.offsetHeight : to;
                    var nextValue = step(from, toNumber, squareStepper, (_nowDefault.default() - start) / 5);
                    if (Math.abs(toNumber - nextValue) < 1.5) nextValue = toNumber;
                    target[name] = nextValue;
                    if (toNumber === nextValue) onEnd && onEnd(true);
                    else animate(name, from, to, index + 1, start);
                }
            });
        }
    }, [
        animator,
        onEnd,
        target
    ]);
    var handleCancelAnimation = _react.useCallback(function() {
        cancelAnimationFrame(animator.current);
        onEnd && onEnd(false);
    }, [
        onEnd
    ]);
    _react.useLayoutEffect(function() {
        animate(name1, target[name1], value, 1);
        if (target) {
            target.addEventListener('pointerdown', handleCancelAnimation, {
                passive: true
            });
            target.addEventListener('wheel', handleCancelAnimation, {
                passive: true
            });
            return function() {
                target.removeEventListener('pointerdown', handleCancelAnimation);
                target.removeEventListener('wheel', handleCancelAnimation);
                cancelAnimationFrame(animator.current);
            };
        }
        return function() {
            return cancelAnimationFrame(animator.current);
        };
    }, [
        animate,
        animator,
        handleCancelAnimation,
        name1,
        target,
        value
    ]);
    return false;
};
SpineTo.propTypes = {
    name: _propTypesDefault.default.string.isRequired,
    onEnd: _propTypesDefault.default.func,
    target: _propTypesDefault.default.any.isRequired,
    value: _propTypesDefault.default.oneOfType([
        _propTypesDefault.default.number,
        _propTypesDefault.default.oneOf([
            '100%'
        ])
    ]).isRequired
};
exports.default = SpineTo;

},{"core-js/modules/es.function.name.js":"glBcr","@babel/runtime-corejs3/core-js-stable/math/sign":"bCLsX","@babel/runtime-corejs3/core-js-stable/date/now":"j7X90","prop-types":"2ipKq","react":"21dqq","@parcel/transformer-js/src/esmodule-helpers.js":"gkKU3"}],"bCLsX":[function(require,module,exports) {
module.exports = require("core-js-pure/stable/math/sign");

},{"core-js-pure/stable/math/sign":"j3Wea"}],"j3Wea":[function(require,module,exports) {
var parent = require('../../es/math/sign');
module.exports = parent;

},{"../../es/math/sign":"jUxlK"}],"jUxlK":[function(require,module,exports) {
require('../../modules/es.math.sign');
var path = require('../../internals/path');
module.exports = path.Math.sign;

},{"../../modules/es.math.sign":"cqz0A","../../internals/path":"8QO0z"}],"cqz0A":[function(require,module,exports) {
var $ = require('../internals/export');
var sign = require('../internals/math-sign');
// `Math.sign` method
// https://tc39.es/ecma262/#sec-math.sign
$({
    target: 'Math',
    stat: true
}, {
    sign: sign
});

},{"../internals/export":"jjems","../internals/math-sign":"XJ2WB"}],"XJ2WB":[function(require,module,exports) {
// `Math.sign` method implementation
// https://tc39.es/ecma262/#sec-math.sign
// eslint-disable-next-line es-x/no-math-sign -- safe
module.exports = Math.sign || function sign(x) {
    // eslint-disable-next-line no-self-compare -- NaN check
    return (x = +x) == 0 || x != x ? x : x < 0 ? -1 : 1;
};

},{}],"7ubbf":[function(require,module,exports) {
var parcelHelpers = require("@parcel/transformer-js/src/esmodule-helpers.js");
parcelHelpers.defineInteropFlag(exports);
var _slicedToArray = require("@babel/runtime-corejs3/helpers/slicedToArray");
var _slicedToArrayDefault = parcelHelpers.interopDefault(_slicedToArray);
var _react = require("react");
function useStateRef(initialState) {
    var _useState = _react.useState(initialState), _useState2 = _slicedToArrayDefault.default(_useState, 2), state1 = _useState2[0], setState = _useState2[1];
    var ref = _react.useRef();
    var setValue = _react.useCallback(function(nextValue) {
        if (typeof nextValue === 'function') setValue(function(state) {
            nextValue = nextValue(state);
            ref.current = nextValue;
            return nextValue;
        });
        else {
            ref.current = nextValue;
            setValue(nextValue);
        }
    }, [
        ref
    ]);
    ref.current = state1;
    return [
        state1,
        setState,
        ref
    ];
}
exports.default = useStateRef;

},{"@babel/runtime-corejs3/helpers/slicedToArray":"36E7Q","react":"21dqq","@parcel/transformer-js/src/esmodule-helpers.js":"gkKU3"}],"cLIsq":[function(require,module,exports) {
var parcelHelpers = require("@parcel/transformer-js/src/esmodule-helpers.js");
parcelHelpers.defineInteropFlag(exports);
var _classnames = require("classnames");
var _classnamesDefault = parcelHelpers.interopDefault(_classnames);
var _propTypes = require("prop-types");
var _propTypesDefault = parcelHelpers.interopDefault(_propTypes);
var _react = require("react");
var _reactDefault = parcelHelpers.interopDefault(_react);
var _internalContext = require("./InternalContext");
var _internalContextDefault = parcelHelpers.interopDefault(_internalContext);
var _useStyleToClassName = require("../hooks/internal/useStyleToClassName");
var _useStyleToClassNameDefault = parcelHelpers.interopDefault(_useStyleToClassName);
var ROOT_STYLE = {
    height: '100%',
    overflowY: 'auto',
    width: '100%'
};
var Panel = function Panel(_ref) {
    var children = _ref.children, className = _ref.className;
    var _useContext = _react.useContext(_internalContextDefault.default), setTarget = _useContext.setTarget;
    var rootCSS = _useStyleToClassNameDefault.default()(ROOT_STYLE);
    return /*#__PURE__*/ _reactDefault.default.createElement("div", {
        className: _classnamesDefault.default(rootCSS, (className || '') + ''),
        ref: setTarget
    }, children);
};
Panel.defaultProps = {
    children: undefined,
    className: undefined
};
Panel.propTypes = {
    children: _propTypesDefault.default.any,
    className: _propTypesDefault.default.string
};
exports.default = Panel;

},{"classnames":"jocGM","prop-types":"2ipKq","react":"21dqq","./InternalContext":"9Xnjg","../hooks/internal/useStyleToClassName":"7aM5G","@parcel/transformer-js/src/esmodule-helpers.js":"gkKU3"}],"f32Wy":[function(require,module,exports) {
var parcelHelpers = require("@parcel/transformer-js/src/esmodule-helpers.js");
parcelHelpers.defineInteropFlag(exports);
/* eslint no-magic-numbers: ["error", { "ignore": [2] }] */ var _useStateContext = require("./internal/useStateContext");
var _useStateContextDefault = parcelHelpers.interopDefault(_useStateContext);
function useAnimating() {
    var _useStateContext1 = _useStateContextDefault.default(2), animating = _useStateContext1.animating;
    return [
        animating
    ];
}
exports.default = useAnimating;

},{"./internal/useStateContext":"edkS9","@parcel/transformer-js/src/esmodule-helpers.js":"gkKU3"}],"akM5y":[function(require,module,exports) {
var parcelHelpers = require("@parcel/transformer-js/src/esmodule-helpers.js");
parcelHelpers.defineInteropFlag(exports);
/* eslint no-magic-numbers: ["error", { "ignore": [2] }] */ var _useStateContext = require("./internal/useStateContext");
var _useStateContextDefault = parcelHelpers.interopDefault(_useStateContext);
function useAnimatingToEnd() {
    var _useStateContext1 = _useStateContextDefault.default(2), animatingToEnd = _useStateContext1.animatingToEnd;
    return [
        animatingToEnd
    ];
}
exports.default = useAnimatingToEnd;

},{"./internal/useStateContext":"edkS9","@parcel/transformer-js/src/esmodule-helpers.js":"gkKU3"}],"4HyyS":[function(require,module,exports) {
var parcelHelpers = require("@parcel/transformer-js/src/esmodule-helpers.js");
parcelHelpers.defineInteropFlag(exports);
var _useStateContext = require("./internal/useStateContext");
var _useStateContextDefault = parcelHelpers.interopDefault(_useStateContext);
function useAtBottom() {
    var _useStateContext1 = _useStateContextDefault.default(1), atBottom = _useStateContext1.atBottom;
    return [
        atBottom
    ];
}
exports.default = useAtBottom;

},{"./internal/useStateContext":"edkS9","@parcel/transformer-js/src/esmodule-helpers.js":"gkKU3"}],"andwH":[function(require,module,exports) {
var parcelHelpers = require("@parcel/transformer-js/src/esmodule-helpers.js");
parcelHelpers.defineInteropFlag(exports);
var _useStateContext = require("./internal/useStateContext");
var _useStateContextDefault = parcelHelpers.interopDefault(_useStateContext);
function useAtEnd() {
    var _useStateContext1 = _useStateContextDefault.default(1), atEnd = _useStateContext1.atEnd;
    return [
        atEnd
    ];
}
exports.default = useAtEnd;

},{"./internal/useStateContext":"edkS9","@parcel/transformer-js/src/esmodule-helpers.js":"gkKU3"}],"8zBlc":[function(require,module,exports) {
var parcelHelpers = require("@parcel/transformer-js/src/esmodule-helpers.js");
parcelHelpers.defineInteropFlag(exports);
var _useStateContext = require("./internal/useStateContext");
var _useStateContextDefault = parcelHelpers.interopDefault(_useStateContext);
function useAtStart() {
    var _useStateContext1 = _useStateContextDefault.default(1), atStart = _useStateContext1.atStart;
    return [
        atStart
    ];
}
exports.default = useAtStart;

},{"./internal/useStateContext":"edkS9","@parcel/transformer-js/src/esmodule-helpers.js":"gkKU3"}],"a6yzr":[function(require,module,exports) {
var parcelHelpers = require("@parcel/transformer-js/src/esmodule-helpers.js");
parcelHelpers.defineInteropFlag(exports);
var _useStateContext = require("./internal/useStateContext");
var _useStateContextDefault = parcelHelpers.interopDefault(_useStateContext);
function useAtTop() {
    var _useStateContext1 = _useStateContextDefault.default(1), atTop = _useStateContext1.atTop;
    return [
        atTop
    ];
}
exports.default = useAtTop;

},{"./internal/useStateContext":"edkS9","@parcel/transformer-js/src/esmodule-helpers.js":"gkKU3"}],"9Harq":[function(require,module,exports) {
var parcelHelpers = require("@parcel/transformer-js/src/esmodule-helpers.js");
parcelHelpers.defineInteropFlag(exports);
var _useStateContext = require("./internal/useStateContext");
var _useStateContextDefault = parcelHelpers.interopDefault(_useStateContext);
function useMode() {
    var _useStateContext1 = _useStateContextDefault.default(1), mode = _useStateContext1.mode;
    return [
        mode
    ];
}
exports.default = useMode;

},{"./internal/useStateContext":"edkS9","@parcel/transformer-js/src/esmodule-helpers.js":"gkKU3"}],"i4hpz":[function(require,module,exports) {
var parcelHelpers = require("@parcel/transformer-js/src/esmodule-helpers.js");
parcelHelpers.defineInteropFlag(exports);
var _toConsumableArray = require("@babel/runtime-corejs3/helpers/toConsumableArray");
var _toConsumableArrayDefault = parcelHelpers.interopDefault(_toConsumableArray);
var _isArray = require("@babel/runtime-corejs3/core-js-stable/array/is-array");
var _isArrayDefault = parcelHelpers.interopDefault(_isArray);
var _concat = require("@babel/runtime-corejs3/core-js-stable/instance/concat");
var _concatDefault = parcelHelpers.interopDefault(_concat);
var _react = require("react");
var _useInternalContext = require("./internal/useInternalContext");
var _useInternalContextDefault = parcelHelpers.interopDefault(_useInternalContext);
function useObserveScrollPosition(observer) {
    var _context;
    var deps = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : [];
    if (observer && typeof observer !== 'function') console.error('react-scroll-to-bottom: First argument passed to "useObserveScrollPosition" must be a function.');
    else if (!_isArrayDefault.default(deps)) console.error('react-scroll-to-bottom: Second argument passed to "useObserveScrollPosition" must be an array if specified.');
    var _useInternalContext1 = _useInternalContextDefault.default(), observeScrollPosition = _useInternalContext1.observeScrollPosition;
    /* eslint-disable-next-line react-hooks/exhaustive-deps */ _react.useEffect(function() {
        return observer && observeScrollPosition(observer);
    }, _concatDefault.default(_context = []).call(_context, _toConsumableArrayDefault.default(deps), [
        !observer,
        observeScrollPosition
    ]));
}
exports.default = useObserveScrollPosition;

},{"@babel/runtime-corejs3/helpers/toConsumableArray":"k0LvR","@babel/runtime-corejs3/core-js-stable/array/is-array":"9wWJ7","@babel/runtime-corejs3/core-js-stable/instance/concat":"tXYDQ","react":"21dqq","./internal/useInternalContext":"hYi9A","@parcel/transformer-js/src/esmodule-helpers.js":"gkKU3"}],"72iJH":[function(require,module,exports) {
var parcelHelpers = require("@parcel/transformer-js/src/esmodule-helpers.js");
parcelHelpers.defineInteropFlag(exports);
var _useFunctionContext = require("./internal/useFunctionContext");
var _useFunctionContextDefault = parcelHelpers.interopDefault(_useFunctionContext);
function useScrollTo() {
    var _useFunctionContext1 = _useFunctionContextDefault.default(), scrollTo = _useFunctionContext1.scrollTo;
    return scrollTo;
}
exports.default = useScrollTo;

},{"./internal/useFunctionContext":"fJDZI","@parcel/transformer-js/src/esmodule-helpers.js":"gkKU3"}],"g5UMp":[function(require,module,exports) {
var parcelHelpers = require("@parcel/transformer-js/src/esmodule-helpers.js");
parcelHelpers.defineInteropFlag(exports);
var _useFunctionContext = require("./internal/useFunctionContext");
var _useFunctionContextDefault = parcelHelpers.interopDefault(_useFunctionContext);
function useScrollToBottom() {
    var _useFunctionContext1 = _useFunctionContextDefault.default(), scrollToBottom = _useFunctionContext1.scrollToBottom;
    return scrollToBottom;
}
exports.default = useScrollToBottom;

},{"./internal/useFunctionContext":"fJDZI","@parcel/transformer-js/src/esmodule-helpers.js":"gkKU3"}],"2XEqu":[function(require,module,exports) {
var parcelHelpers = require("@parcel/transformer-js/src/esmodule-helpers.js");
parcelHelpers.defineInteropFlag(exports);
var _useFunctionContext = require("./internal/useFunctionContext");
var _useFunctionContextDefault = parcelHelpers.interopDefault(_useFunctionContext);
function useScrollToStart() {
    var _useFunctionContext1 = _useFunctionContextDefault.default(), scrollToStart = _useFunctionContext1.scrollToStart;
    return scrollToStart;
}
exports.default = useScrollToStart;

},{"./internal/useFunctionContext":"fJDZI","@parcel/transformer-js/src/esmodule-helpers.js":"gkKU3"}],"kDVCC":[function(require,module,exports) {
var parcelHelpers = require("@parcel/transformer-js/src/esmodule-helpers.js");
parcelHelpers.defineInteropFlag(exports);
var _useFunctionContext = require("./internal/useFunctionContext");
var _useFunctionContextDefault = parcelHelpers.interopDefault(_useFunctionContext);
function useScrollToTop() {
    var _useFunctionContext1 = _useFunctionContextDefault.default(), scrollToTop = _useFunctionContext1.scrollToTop;
    return scrollToTop;
}
exports.default = useScrollToTop;

},{"./internal/useFunctionContext":"fJDZI","@parcel/transformer-js/src/esmodule-helpers.js":"gkKU3"}],"b8zLy":[function(require,module,exports) {
var $parcel$ReactRefreshHelpers$c778 = require("@parcel/transformer-react-refresh-wrap/lib/helpers/helpers.js");
var prevRefreshReg = window.$RefreshReg$;
var prevRefreshSig = window.$RefreshSig$;
$parcel$ReactRefreshHelpers$c778.prelude(module);

try {
var parcelHelpers = require("@parcel/transformer-js/src/esmodule-helpers.js");
parcelHelpers.defineInteropFlag(exports);
var _jsxDevRuntime = require("react/jsx-dev-runtime");
var _react = require("react");
var _reactDefault = parcelHelpers.interopDefault(_react);
var _axios = require("axios");
var _axiosDefault = parcelHelpers.interopDefault(_axios);
class SviewEvaluation extends _react.Component {
    constructor(props){
        super(props);
        this.state = {
            evaluations: []
        };
    }
    componentDidMount() {
        this.retriveEvaluations();
    }
    //retrive evaluations
    retriveEvaluations() {
        _axiosDefault.default.get("http://localhost:5000/evaluations").then((res)=>{
            if (res.data.success) {
                this.setState({
                    evaluations: res.data.existingEvaluations
                });
                console.log(this.state.evaluations);
            }
        });
    }
    //delete evaluation
    onDelete = (id)=>{
        _axiosDefault.default.delete(`http://localhost:5000/evaluation/delete/${id}`).then((res)=>{
            alert("Deleted Successfully");
            this.retriveEvaluations();
        });
    };
    //filter evaluation
    filterData(evaluations, searchKey) {
        const result = evaluations.filter((evaluation)=>evaluation.groupId.toLowerCase().includes(searchKey) || evaluation.evaluationTopic.toLowerCase().includes(searchKey) || evaluation.date.toLowerCase().includes(searchKey) || evaluation.groupId.includes(searchKey) || evaluation.evaluationTopic.includes(searchKey) || evaluation.date.includes(searchKey)
        );
        this.setState({
            evaluations: result
        });
    }
    //search evaluation
    handleSearchArea = (e)=>{
        const searchKey = e.currentTarget.value;
        _axiosDefault.default.get("http://localhost:5000/evaluations").then((res)=>{
            if (res.data.success) this.filterData(res.data.existingEvaluations, searchKey);
        });
    };
    render() {
        return /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
            className: "me-4 mt-4 mb-2",
            children: [
                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                    className: "row",
                    children: [
                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                            className: "col-lg-6 mt-2 mb-2",
                            children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("h4", {
                                children: "All Evaluation Sessions"
                            }, void 0, false, {
                                fileName: "src/components/IT20128036/user/SviewEvaluation.js",
                                lineNumber: 69,
                                columnNumber: 13
                            }, this)
                        }, void 0, false, {
                            fileName: "src/components/IT20128036/user/SviewEvaluation.js",
                            lineNumber: 68,
                            columnNumber: 11
                        }, this),
                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                            className: "col-lg-6 mt-2 mb-2",
                            children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("input", {
                                className: "form-control",
                                type: "search",
                                placeholder: "Search",
                                name: "searchQuery",
                                onChange: this.handleSearchArea
                            }, void 0, false, {
                                fileName: "src/components/IT20128036/user/SviewEvaluation.js",
                                lineNumber: 72,
                                columnNumber: 13
                            }, this)
                        }, void 0, false, {
                            fileName: "src/components/IT20128036/user/SviewEvaluation.js",
                            lineNumber: 71,
                            columnNumber: 11
                        }, this)
                    ]
                }, void 0, true, {
                    fileName: "src/components/IT20128036/user/SviewEvaluation.js",
                    lineNumber: 67,
                    columnNumber: 9
                }, this),
                this.state.evaluations.map((evaluations, index)=>/*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                        class: "row",
                        children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                            class: "col-sm-12",
                            children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                class: "card border-primary mt-2 mb-2",
                                children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                    class: "card-body",
                                    children: [
                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                            class: "card-header",
                                            children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("h5", {
                                                class: "card-title",
                                                children: [
                                                    " Session ",
                                                    index + 1,
                                                    " "
                                                ]
                                            }, void 0, true, {
                                                fileName: "src/components/IT20128036/user/SviewEvaluation.js",
                                                lineNumber: 88,
                                                columnNumber: 21
                                            }, this)
                                        }, void 0, false, {
                                            fileName: "src/components/IT20128036/user/SviewEvaluation.js",
                                            lineNumber: 87,
                                            columnNumber: 19
                                        }, this),
                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("p", {
                                            class: "card-text",
                                            children: [
                                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("strong", {
                                                    children: [
                                                        evaluations.groupId,
                                                        " "
                                                    ]
                                                }, void 0, true, {
                                                    fileName: "src/components/IT20128036/user/SviewEvaluation.js",
                                                    lineNumber: 92,
                                                    columnNumber: 21
                                                }, this),
                                                "Students, your",
                                                " ",
                                                evaluations.evaluationTopic,
                                                " session will be held on",
                                                " ",
                                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("strong", {
                                                    children: evaluations.date
                                                }, void 0, false, {
                                                    fileName: "src/components/IT20128036/user/SviewEvaluation.js",
                                                    lineNumber: 94,
                                                    columnNumber: 21
                                                }, this),
                                                " from",
                                                " ",
                                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("strong", {
                                                    children: evaluations.from
                                                }, void 0, false, {
                                                    fileName: "src/components/IT20128036/user/SviewEvaluation.js",
                                                    lineNumber: 95,
                                                    columnNumber: 21
                                                }, this),
                                                " to",
                                                " ",
                                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("strong", {
                                                    children: evaluations.to
                                                }, void 0, false, {
                                                    fileName: "src/components/IT20128036/user/SviewEvaluation.js",
                                                    lineNumber: 96,
                                                    columnNumber: 21
                                                }, this),
                                                " with the",
                                                " ",
                                                evaluations.panel,
                                                ". Your participation is mandatory. You can connect via the link below.",
                                                " "
                                            ]
                                        }, void 0, true, {
                                            fileName: "src/components/IT20128036/user/SviewEvaluation.js",
                                            lineNumber: 91,
                                            columnNumber: 19
                                        }, this),
                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                            className: "row",
                                            children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                                className: "col-lg-4",
                                                children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("a", {
                                                    href: evaluations.link,
                                                    target: "_blank",
                                                    class: "btn btn-outline-primary",
                                                    children: [
                                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("i", {
                                                            class: "fa fa-link",
                                                            "aria-hidden": "true"
                                                        }, void 0, false, {
                                                            fileName: "src/components/IT20128036/user/SviewEvaluation.js",
                                                            lineNumber: 109,
                                                            columnNumber: 25
                                                        }, this),
                                                        "Join"
                                                    ]
                                                }, void 0, true, {
                                                    fileName: "src/components/IT20128036/user/SviewEvaluation.js",
                                                    lineNumber: 104,
                                                    columnNumber: 23
                                                }, this)
                                            }, void 0, false, {
                                                fileName: "src/components/IT20128036/user/SviewEvaluation.js",
                                                lineNumber: 103,
                                                columnNumber: 21
                                            }, this)
                                        }, void 0, false, {
                                            fileName: "src/components/IT20128036/user/SviewEvaluation.js",
                                            lineNumber: 102,
                                            columnNumber: 19
                                        }, this)
                                    ]
                                }, void 0, true, {
                                    fileName: "src/components/IT20128036/user/SviewEvaluation.js",
                                    lineNumber: 86,
                                    columnNumber: 17
                                }, this)
                            }, void 0, false, {
                                fileName: "src/components/IT20128036/user/SviewEvaluation.js",
                                lineNumber: 85,
                                columnNumber: 15
                            }, this)
                        }, void 0, false, {
                            fileName: "src/components/IT20128036/user/SviewEvaluation.js",
                            lineNumber: 84,
                            columnNumber: 13
                        }, this)
                    }, void 0, false, {
                        fileName: "src/components/IT20128036/user/SviewEvaluation.js",
                        lineNumber: 83,
                        columnNumber: 11
                    }, this)
                )
            ]
        }, void 0, true, {
            fileName: "src/components/IT20128036/user/SviewEvaluation.js",
            lineNumber: 66,
            columnNumber: 7
        }, this);
    }
}
exports.default = SviewEvaluation;

  $parcel$ReactRefreshHelpers$c778.postlude(module);
} finally {
  window.$RefreshReg$ = prevRefreshReg;
  window.$RefreshSig$ = prevRefreshSig;
}
},{"react/jsx-dev-runtime":"iTorj","react":"21dqq","axios":"jo6P5","@parcel/transformer-js/src/esmodule-helpers.js":"gkKU3","@parcel/transformer-react-refresh-wrap/lib/helpers/helpers.js":"km3Ru"}],"beKUB":[function(require,module,exports) {
var $parcel$ReactRefreshHelpers$6bb2 = require("@parcel/transformer-react-refresh-wrap/lib/helpers/helpers.js");
var prevRefreshReg = window.$RefreshReg$;
var prevRefreshSig = window.$RefreshSig$;
$parcel$ReactRefreshHelpers$6bb2.prelude(module);

try {
var parcelHelpers = require("@parcel/transformer-js/src/esmodule-helpers.js");
parcelHelpers.defineInteropFlag(exports);
var _jsxDevRuntime = require("react/jsx-dev-runtime");
var _react = require("react");
var _reactDefault = parcelHelpers.interopDefault(_react);
var _axios = require("axios");
var _axiosDefault = parcelHelpers.interopDefault(_axios);
var _sviewEvaluation = require("./SviewEvaluation");
var _sviewEvaluationDefault = parcelHelpers.interopDefault(_sviewEvaluation);
var _jwtDecode = require("jwt-decode");
var _jwtDecodeDefault = parcelHelpers.interopDefault(_jwtDecode);
class SviewSubmitionType extends _react.Component {
    constructor(props){
        super(props);
        this.state = {
            submitiontypes: [],
            groupId: ""
        };
    }
    componentDidMount() {
        if (localStorage.userToken) {
            const usertoken = localStorage.userToken;
            const decoded = _jwtDecodeDefault.default(usertoken);
            this.setState({
                groupId: decoded.groupId
            });
        }
        setTimeout(()=>{
            this.retrivesubmitionTypes();
        }, 1000);
    }
    //retrive submitiontypes
    retrivesubmitionTypes() {
        _axiosDefault.default.get("http://localhost:5000/submitiontypes").then((res)=>{
            if (res.data.success) {
                this.setState({
                    submitiontypes: res.data.existingsubmitonTypes
                });
                console.log(this.state.submitiontypes);
            }
        });
    }
    //delete submitiontype
    onDelete = (id)=>{
        _axiosDefault.default.delete(`http://localhost:5000/submitiontype/delete/${id}`).then((res)=>{
            alert("Deleted Successfully");
            this.retrivesubmitionTypes();
        });
    };
    //filter submitiontype
    filterData(submitiontypes, searchKey) {
        const result = submitiontypes.filter((submitiontype)=>submitiontype.submitionType.toLowerCase().includes(searchKey) || submitiontype.deadLine.toLowerCase().includes(searchKey) || submitiontype.Status.toLowerCase().includes(searchKey) || submitiontype.submitionType.includes(searchKey) || submitiontype.deadLine.includes(searchKey) || submitiontype.Status.includes(searchKey)
        );
        this.setState({
            submitiontypes: result
        });
    }
    //search submitiontype
    handleSearchArea = (e)=>{
        const searchKey = e.currentTarget.value;
        _axiosDefault.default.get("http://localhost:5000/submitiontypes").then((res)=>{
            if (res.data.success) this.filterData(res.data.existingsubmitonTypes, searchKey);
        });
    };
    render() {
        return /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
            className: "container",
            children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                className: "row",
                children: [
                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                        className: "col-sm-8",
                        children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                            className: "ms-4",
                            children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                className: "mt-4",
                                children: [
                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                        className: "row",
                                        children: [
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                                className: "col-lg-7 mt-2 mb-2",
                                                children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("h4", {
                                                    children: [
                                                        "Submitions List - ",
                                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("a", {
                                                            href: "/student/group/evaluation/view",
                                                            style: {
                                                                textDecoration: 'none'
                                                            },
                                                            children: this.state.groupId
                                                        }, void 0, false, {
                                                            fileName: "src/components/IT20128036/user/SviewSubmitionType.js",
                                                            lineNumber: 95,
                                                            columnNumber: 41
                                                        }, this)
                                                    ]
                                                }, void 0, true, {
                                                    fileName: "src/components/IT20128036/user/SviewSubmitionType.js",
                                                    lineNumber: 95,
                                                    columnNumber: 19
                                                }, this)
                                            }, void 0, false, {
                                                fileName: "src/components/IT20128036/user/SviewSubmitionType.js",
                                                lineNumber: 94,
                                                columnNumber: 17
                                            }, this),
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                                className: "col-lg-5 mt-2 mb-2",
                                                children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("input", {
                                                    className: "form-control",
                                                    type: "search",
                                                    placeholder: "Search",
                                                    name: "searchQuery",
                                                    onChange: this.handleSearchArea
                                                }, void 0, false, {
                                                    fileName: "src/components/IT20128036/user/SviewSubmitionType.js",
                                                    lineNumber: 98,
                                                    columnNumber: 19
                                                }, this)
                                            }, void 0, false, {
                                                fileName: "src/components/IT20128036/user/SviewSubmitionType.js",
                                                lineNumber: 97,
                                                columnNumber: 17
                                            }, this)
                                        ]
                                    }, void 0, true, {
                                        fileName: "src/components/IT20128036/user/SviewSubmitionType.js",
                                        lineNumber: 93,
                                        columnNumber: 15
                                    }, this),
                                    this.state.submitiontypes.map((submitiontypes, index)=>/*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                            class: "row",
                                            children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                                class: "col-sm-12",
                                                children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                                    class: "card border-secondary mt-2 mb-4",
                                                    children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                                        class: "card-body",
                                                        children: [
                                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                                                class: "card-header",
                                                                children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("h5", {
                                                                    class: "card-title",
                                                                    children: [
                                                                        " ",
                                                                        submitiontypes.submitionType,
                                                                        " Submition",
                                                                        " "
                                                                    ]
                                                                }, void 0, true, {
                                                                    fileName: "src/components/IT20128036/user/SviewSubmitionType.js",
                                                                    lineNumber: 114,
                                                                    columnNumber: 27
                                                                }, this)
                                                            }, void 0, false, {
                                                                fileName: "src/components/IT20128036/user/SviewSubmitionType.js",
                                                                lineNumber: 113,
                                                                columnNumber: 25
                                                            }, this),
                                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("p", {
                                                                class: "card-text"
                                                            }, void 0, false, {
                                                                fileName: "src/components/IT20128036/user/SviewSubmitionType.js",
                                                                lineNumber: 120,
                                                                columnNumber: 25
                                                            }, this),
                                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("p", {
                                                                children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("strong", {
                                                                    children: "Description"
                                                                }, void 0, false, {
                                                                    fileName: "src/components/IT20128036/user/SviewSubmitionType.js",
                                                                    lineNumber: 122,
                                                                    columnNumber: 27
                                                                }, this)
                                                            }, void 0, false, {
                                                                fileName: "src/components/IT20128036/user/SviewSubmitionType.js",
                                                                lineNumber: 121,
                                                                columnNumber: 25
                                                            }, this),
                                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("p", {
                                                                children: [
                                                                    " ",
                                                                    submitiontypes.description
                                                                ]
                                                            }, void 0, true, {
                                                                fileName: "src/components/IT20128036/user/SviewSubmitionType.js",
                                                                lineNumber: 124,
                                                                columnNumber: 25
                                                            }, this),
                                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("p", {
                                                                children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("strong", {
                                                                    children: "Guideline"
                                                                }, void 0, false, {
                                                                    fileName: "src/components/IT20128036/user/SviewSubmitionType.js",
                                                                    lineNumber: 126,
                                                                    columnNumber: 27
                                                                }, this)
                                                            }, void 0, false, {
                                                                fileName: "src/components/IT20128036/user/SviewSubmitionType.js",
                                                                lineNumber: 125,
                                                                columnNumber: 25
                                                            }, this),
                                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("p", {
                                                                children: submitiontypes.guidelines
                                                            }, void 0, false, {
                                                                fileName: "src/components/IT20128036/user/SviewSubmitionType.js",
                                                                lineNumber: 128,
                                                                columnNumber: 25
                                                            }, this),
                                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("p", {
                                                                children: [
                                                                    " ",
                                                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("strong", {
                                                                        children: "Allocated Marks"
                                                                    }, void 0, false, {
                                                                        fileName: "src/components/IT20128036/user/SviewSubmitionType.js",
                                                                        lineNumber: 133,
                                                                        columnNumber: 27
                                                                    }, this)
                                                                ]
                                                            }, void 0, true, {
                                                                fileName: "src/components/IT20128036/user/SviewSubmitionType.js",
                                                                lineNumber: 131,
                                                                columnNumber: 25
                                                            }, this),
                                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("p", {
                                                                children: [
                                                                    submitiontypes.almarks,
                                                                    "%"
                                                                ]
                                                            }, void 0, true, {
                                                                fileName: "src/components/IT20128036/user/SviewSubmitionType.js",
                                                                lineNumber: 135,
                                                                columnNumber: 25
                                                            }, this),
                                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("p", {
                                                                children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("strong", {
                                                                    children: "Deadline"
                                                                }, void 0, false, {
                                                                    fileName: "src/components/IT20128036/user/SviewSubmitionType.js",
                                                                    lineNumber: 138,
                                                                    columnNumber: 27
                                                                }, this)
                                                            }, void 0, false, {
                                                                fileName: "src/components/IT20128036/user/SviewSubmitionType.js",
                                                                lineNumber: 137,
                                                                columnNumber: 25
                                                            }, this),
                                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("p", {
                                                                children: [
                                                                    submitiontypes.deadLine,
                                                                    " \xa0\xa0\xa0\xa0 -",
                                                                    submitiontypes.Status,
                                                                    "-"
                                                                ]
                                                            }, void 0, true, {
                                                                fileName: "src/components/IT20128036/user/SviewSubmitionType.js",
                                                                lineNumber: 140,
                                                                columnNumber: 25
                                                            }, this),
                                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                                                className: "row",
                                                                children: [
                                                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                                                        className: "col-lg-4"
                                                                    }, void 0, false, {
                                                                        fileName: "src/components/IT20128036/user/SviewSubmitionType.js",
                                                                        lineNumber: 145,
                                                                        columnNumber: 27
                                                                    }, this),
                                                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                                                        className: "col-lg-4",
                                                                        children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("a", {
                                                                            href: "/student/submitionsp/view",
                                                                            class: "btn btn-outline-success",
                                                                            children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("i", {
                                                                                class: "fa fa-plus",
                                                                                "aria-hidden": "true",
                                                                                children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("strong", {
                                                                                    children: "\xa0Submit Here"
                                                                                }, void 0, false, {
                                                                                    fileName: "src/components/IT20128036/user/SviewSubmitionType.js",
                                                                                    lineNumber: 150,
                                                                                    columnNumber: 33
                                                                                }, this)
                                                                            }, void 0, false, {
                                                                                fileName: "src/components/IT20128036/user/SviewSubmitionType.js",
                                                                                lineNumber: 149,
                                                                                columnNumber: 31
                                                                            }, this)
                                                                        }, void 0, false, {
                                                                            fileName: "src/components/IT20128036/user/SviewSubmitionType.js",
                                                                            lineNumber: 148,
                                                                            columnNumber: 29
                                                                        }, this)
                                                                    }, void 0, false, {
                                                                        fileName: "src/components/IT20128036/user/SviewSubmitionType.js",
                                                                        lineNumber: 147,
                                                                        columnNumber: 27
                                                                    }, this),
                                                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                                                        className: "col-lg-4"
                                                                    }, void 0, false, {
                                                                        fileName: "src/components/IT20128036/user/SviewSubmitionType.js",
                                                                        lineNumber: 155,
                                                                        columnNumber: 27
                                                                    }, this)
                                                                ]
                                                            }, void 0, true, {
                                                                fileName: "src/components/IT20128036/user/SviewSubmitionType.js",
                                                                lineNumber: 144,
                                                                columnNumber: 25
                                                            }, this)
                                                        ]
                                                    }, void 0, true, {
                                                        fileName: "src/components/IT20128036/user/SviewSubmitionType.js",
                                                        lineNumber: 112,
                                                        columnNumber: 23
                                                    }, this)
                                                }, void 0, false, {
                                                    fileName: "src/components/IT20128036/user/SviewSubmitionType.js",
                                                    lineNumber: 111,
                                                    columnNumber: 21
                                                }, this)
                                            }, void 0, false, {
                                                fileName: "src/components/IT20128036/user/SviewSubmitionType.js",
                                                lineNumber: 110,
                                                columnNumber: 19
                                            }, this)
                                        }, void 0, false, {
                                            fileName: "src/components/IT20128036/user/SviewSubmitionType.js",
                                            lineNumber: 109,
                                            columnNumber: 17
                                        }, this)
                                    )
                                ]
                            }, void 0, true, {
                                fileName: "src/components/IT20128036/user/SviewSubmitionType.js",
                                lineNumber: 92,
                                columnNumber: 13
                            }, this)
                        }, void 0, false, {
                            fileName: "src/components/IT20128036/user/SviewSubmitionType.js",
                            lineNumber: 91,
                            columnNumber: 13
                        }, this)
                    }, void 0, false, {
                        fileName: "src/components/IT20128036/user/SviewSubmitionType.js",
                        lineNumber: 90,
                        columnNumber: 11
                    }, this),
                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                        className: "col-sm-4",
                        children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV(_sviewEvaluationDefault.default, {}, void 0, false, {
                            fileName: "src/components/IT20128036/user/SviewSubmitionType.js",
                            lineNumber: 167,
                            columnNumber: 13
                        }, this)
                    }, void 0, false, {
                        fileName: "src/components/IT20128036/user/SviewSubmitionType.js",
                        lineNumber: 166,
                        columnNumber: 11
                    }, this)
                ]
            }, void 0, true, {
                fileName: "src/components/IT20128036/user/SviewSubmitionType.js",
                lineNumber: 89,
                columnNumber: 9
            }, this)
        }, void 0, false, {
            fileName: "src/components/IT20128036/user/SviewSubmitionType.js",
            lineNumber: 88,
            columnNumber: 7
        }, this);
    }
}
exports.default = SviewSubmitionType;

  $parcel$ReactRefreshHelpers$6bb2.postlude(module);
} finally {
  window.$RefreshReg$ = prevRefreshReg;
  window.$RefreshSig$ = prevRefreshSig;
}
},{"react/jsx-dev-runtime":"iTorj","react":"21dqq","axios":"jo6P5","./SviewEvaluation":"b8zLy","jwt-decode":"ljM1w","@parcel/transformer-js/src/esmodule-helpers.js":"gkKU3","@parcel/transformer-react-refresh-wrap/lib/helpers/helpers.js":"km3Ru"}],"lpiEQ":[function(require,module,exports) {
var $parcel$ReactRefreshHelpers$0c6b = require("@parcel/transformer-react-refresh-wrap/lib/helpers/helpers.js");
var prevRefreshReg = window.$RefreshReg$;
var prevRefreshSig = window.$RefreshSig$;
$parcel$ReactRefreshHelpers$0c6b.prelude(module);

try {
var parcelHelpers = require("@parcel/transformer-js/src/esmodule-helpers.js");
parcelHelpers.defineInteropFlag(exports);
var _jsxDevRuntime = require("react/jsx-dev-runtime");
var _react = require("react");
var _reactDefault = parcelHelpers.interopDefault(_react);
var _axios = require("axios");
var _axiosDefault = parcelHelpers.interopDefault(_axios);
var _sweetalert = require("sweetalert");
var _sweetalertDefault = parcelHelpers.interopDefault(_sweetalert);
var _jwtDecode = require("jwt-decode");
var _jwtDecodeDefault = parcelHelpers.interopDefault(_jwtDecode);
var _s = $RefreshSig$();
function AddSubmition() {
    _s();
    const [groupId, setgroupId] = _react.useState("");
    const [type, settype] = _react.useState("");
    const [file, setfile] = _react.useState("");
    const [description, setdescription] = _react.useState("");
    _react.useEffect(()=>{
        const usertoken = localStorage.userToken;
        const decoded = _jwtDecodeDefault.default(usertoken);
        setgroupId(decoded.groupId);
    }, []);
    // componentDidMount() {
    //   document.title = "User Profile";
    //   if (localStorage.userToken) {
    //     const usertoken = localStorage.userToken;
    //     const decoded = jwt_decode(usertoken);
    //     this.setState({
    //       userType: decoded.type,
    //     });
    //   }
    // }
    const onChangeFile = (e)=>{
        setfile(e.target.files[0]);
    };
    const onChangeClick = (e)=>{
        e.preventDefault();
        const formdata = new FormData();
        formdata.append("groupId", groupId);
        formdata.append("type", type);
        formdata.append("file", file);
        formdata.append("description", description);
        setgroupId("");
        settype("");
        setdescription("");
        //save submition
        _axiosDefault.default.post("http://localhost:5000/submition", formdata).then(()=>{
            _sweetalertDefault.default("Submitted Successfully !", "", "success").then((value)=>{
                window.location = `/student/submitionsp/view`;
            });
        }).catch((err)=>{
            _sweetalertDefault.default("Error", "Something Wrong !", "warning");
        });
    };
    return /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
        className: "ms-4 mt-4 mb-4",
        children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
            children: [
                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("center", {
                    children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("h3", {
                        style: _react.set,
                        children: "Add Submition"
                    }, void 0, false, {
                        fileName: "src/components/IT20128036/user/Submitions.js",
                        lineNumber: 135,
                        columnNumber: 19
                    }, this)
                }, void 0, false, {
                    fileName: "src/components/IT20128036/user/Submitions.js",
                    lineNumber: 134,
                    columnNumber: 17
                }, this),
                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("br", {}, void 0, false, {
                    fileName: "src/components/IT20128036/user/Submitions.js",
                    lineNumber: 137,
                    columnNumber: 17
                }, this),
                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("form", {
                    onSubmit: onChangeClick,
                    encType: "multipart/form-data",
                    children: [
                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                            className: "row",
                            children: [
                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                    className: "col-sm-5",
                                    children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                        class: "mb-3",
                                        children: [
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("label", {
                                                for: "exampleInputText1",
                                                class: "form-label",
                                                children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("strong", {
                                                    children: " Group ID"
                                                }, void 0, false, {
                                                    fileName: "src/components/IT20128036/user/Submitions.js",
                                                    lineNumber: 146,
                                                    columnNumber: 22
                                                }, this)
                                            }, void 0, false, {
                                                fileName: "src/components/IT20128036/user/Submitions.js",
                                                lineNumber: 145,
                                                columnNumber: 21
                                            }, this),
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("input", {
                                                type: "text",
                                                class: "form-control",
                                                id: "groupId",
                                                name: "groupId",
                                                pattern: "G[0-9]{3}",
                                                title: "Group ID is Invalid",
                                                placeholder: "Enter Group ID -Gxxx",
                                                "aria-describedby": "emailHelp",
                                                value: groupId,
                                                onChange: (e)=>{
                                                    setgroupId(e.target.value);
                                                },
                                                required: true,
                                                disabled: true
                                            }, void 0, false, {
                                                fileName: "src/components/IT20128036/user/Submitions.js",
                                                lineNumber: 148,
                                                columnNumber: 21
                                            }, this)
                                        ]
                                    }, void 0, true, {
                                        fileName: "src/components/IT20128036/user/Submitions.js",
                                        lineNumber: 144,
                                        columnNumber: 21
                                    }, this)
                                }, void 0, false, {
                                    fileName: "src/components/IT20128036/user/Submitions.js",
                                    lineNumber: 142,
                                    columnNumber: 21
                                }, this),
                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                    className: "col-sm-7 ",
                                    children: [
                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("label", {
                                            for: "exampleInputText1",
                                            class: "form-label",
                                            children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("strong", {
                                                children: "Submition Type"
                                            }, void 0, false, {
                                                fileName: "src/components/IT20128036/user/Submitions.js",
                                                lineNumber: 173,
                                                columnNumber: 22
                                            }, this)
                                        }, void 0, false, {
                                            fileName: "src/components/IT20128036/user/Submitions.js",
                                            lineNumber: 172,
                                            columnNumber: 21
                                        }, this),
                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("select", {
                                            class: "form-select",
                                            "aria-label": "Default select example",
                                            name: "type",
                                            value: type,
                                            onChange: (e)=>{
                                                settype(e.target.value);
                                            },
                                            required: true,
                                            children: [
                                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("option", {
                                                    selected: true,
                                                    children: " Select Type"
                                                }, void 0, false, {
                                                    fileName: "src/components/IT20128036/user/Submitions.js",
                                                    lineNumber: 186,
                                                    columnNumber: 21
                                                }, this),
                                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("option", {
                                                    value: "Topic Assessment Document",
                                                    children: "Topic Assessment Document"
                                                }, void 0, false, {
                                                    fileName: "src/components/IT20128036/user/Submitions.js",
                                                    lineNumber: 187,
                                                    columnNumber: 21
                                                }, this),
                                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("option", {
                                                    value: "Proposal Document",
                                                    children: "Proposal Document"
                                                }, void 0, false, {
                                                    fileName: "src/components/IT20128036/user/Submitions.js",
                                                    lineNumber: 188,
                                                    columnNumber: 21
                                                }, this),
                                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("option", {
                                                    value: "Presentation Slides",
                                                    children: "Presentation Slides"
                                                }, void 0, false, {
                                                    fileName: "src/components/IT20128036/user/Submitions.js",
                                                    lineNumber: 189,
                                                    columnNumber: 21
                                                }, this),
                                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("option", {
                                                    value: "Final Thesis",
                                                    children: "Final Thesis"
                                                }, void 0, false, {
                                                    fileName: "src/components/IT20128036/user/Submitions.js",
                                                    lineNumber: 190,
                                                    columnNumber: 21
                                                }, this)
                                            ]
                                        }, void 0, true, {
                                            fileName: "src/components/IT20128036/user/Submitions.js",
                                            lineNumber: 176,
                                            columnNumber: 21
                                        }, this)
                                    ]
                                }, void 0, true, {
                                    fileName: "src/components/IT20128036/user/Submitions.js",
                                    lineNumber: 170,
                                    columnNumber: 21
                                }, this)
                            ]
                        }, void 0, true, {
                            fileName: "src/components/IT20128036/user/Submitions.js",
                            lineNumber: 141,
                            columnNumber: 19
                        }, this),
                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                            class: "mb-3 mt-3",
                            children: [
                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("label", {
                                    for: "exampleInputText1",
                                    class: "form-label",
                                    children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("strong", {
                                        children: "Enter Description"
                                    }, void 0, false, {
                                        fileName: "src/components/IT20128036/user/Submitions.js",
                                        lineNumber: 210,
                                        columnNumber: 22
                                    }, this)
                                }, void 0, false, {
                                    fileName: "src/components/IT20128036/user/Submitions.js",
                                    lineNumber: 209,
                                    columnNumber: 21
                                }, this),
                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("textarea", {
                                    rows: "4",
                                    cols: "50",
                                    class: "form-control",
                                    id: "description",
                                    name: "description",
                                    placeholder: "Enter Description .......",
                                    "aria-describedby": "emailHelp",
                                    value: description,
                                    onChange: (e)=>{
                                        setdescription(e.target.value);
                                    },
                                    required: true
                                }, void 0, false, {
                                    fileName: "src/components/IT20128036/user/Submitions.js",
                                    lineNumber: 212,
                                    columnNumber: 21
                                }, this)
                            ]
                        }, void 0, true, {
                            fileName: "src/components/IT20128036/user/Submitions.js",
                            lineNumber: 208,
                            columnNumber: 19
                        }, this),
                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                            class: "mb-3",
                            children: [
                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("label", {
                                    for: "formFile",
                                    class: "form-label",
                                    children: [
                                        " ",
                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("strong", {
                                            children: " File Input"
                                        }, void 0, false, {
                                            fileName: "src/components/IT20128036/user/Submitions.js",
                                            lineNumber: 230,
                                            columnNumber: 21
                                        }, this),
                                        " ",
                                        " "
                                    ]
                                }, void 0, true, {
                                    fileName: "src/components/IT20128036/user/Submitions.js",
                                    lineNumber: 228,
                                    columnNumber: 21
                                }, this),
                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("input", {
                                    class: "form-control",
                                    type: "file",
                                    id: "file",
                                    filename: "file",
                                    onChange: onChangeFile,
                                    required: true
                                }, void 0, false, {
                                    fileName: "src/components/IT20128036/user/Submitions.js",
                                    lineNumber: 232,
                                    columnNumber: 21
                                }, this)
                            ]
                        }, void 0, true, {
                            fileName: "src/components/IT20128036/user/Submitions.js",
                            lineNumber: 227,
                            columnNumber: 19
                        }, this),
                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("center", {
                            children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("button", {
                                type: "submit",
                                class: "btn btn-outline-success",
                                children: [
                                    " ",
                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("i", {
                                        class: "fa fa-plus",
                                        "aria-hidden": "true",
                                        children: [
                                            " \xa0",
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("strong", {
                                                children: "Submit"
                                            }, void 0, false, {
                                                fileName: "src/components/IT20128036/user/Submitions.js",
                                                lineNumber: 244,
                                                columnNumber: 71
                                            }, this)
                                        ]
                                    }, void 0, true, {
                                        fileName: "src/components/IT20128036/user/Submitions.js",
                                        lineNumber: 244,
                                        columnNumber: 23
                                    }, this)
                                ]
                            }, void 0, true, {
                                fileName: "src/components/IT20128036/user/Submitions.js",
                                lineNumber: 242,
                                columnNumber: 21
                            }, this)
                        }, void 0, false, {
                            fileName: "src/components/IT20128036/user/Submitions.js",
                            lineNumber: 241,
                            columnNumber: 19
                        }, this)
                    ]
                }, void 0, true, {
                    fileName: "src/components/IT20128036/user/Submitions.js",
                    lineNumber: 138,
                    columnNumber: 17
                }, this)
            ]
        }, void 0, true, {
            fileName: "src/components/IT20128036/user/Submitions.js",
            lineNumber: 125,
            columnNumber: 15
        }, this)
    }, void 0, false, {
        fileName: "src/components/IT20128036/user/Submitions.js",
        lineNumber: 122,
        columnNumber: 5
    }, this);
}
_s(AddSubmition, "5H3RRz+BX3+1BMEofYPBdUwD8go=");
_c = AddSubmition;
exports.default = AddSubmition;
var _c;
$RefreshReg$(_c, "AddSubmition");

  $parcel$ReactRefreshHelpers$0c6b.postlude(module);
} finally {
  window.$RefreshReg$ = prevRefreshReg;
  window.$RefreshSig$ = prevRefreshSig;
}
},{"react/jsx-dev-runtime":"iTorj","react":"21dqq","axios":"jo6P5","sweetalert":"38a65","jwt-decode":"ljM1w","@parcel/transformer-js/src/esmodule-helpers.js":"gkKU3","@parcel/transformer-react-refresh-wrap/lib/helpers/helpers.js":"km3Ru"}],"bi20j":[function(require,module,exports) {
var $parcel$ReactRefreshHelpers$cb31 = require("@parcel/transformer-react-refresh-wrap/lib/helpers/helpers.js");
var prevRefreshReg = window.$RefreshReg$;
var prevRefreshSig = window.$RefreshSig$;
$parcel$ReactRefreshHelpers$cb31.prelude(module);

try {
var parcelHelpers = require("@parcel/transformer-js/src/esmodule-helpers.js");
parcelHelpers.defineInteropFlag(exports);
var _jsxDevRuntime = require("react/jsx-dev-runtime");
var _react = require("react");
var _reactDefault = parcelHelpers.interopDefault(_react);
var _axios = require("axios");
var _axiosDefault = parcelHelpers.interopDefault(_axios);
var _jsFileDownload = require("js-file-download");
var _jsFileDownloadDefault = parcelHelpers.interopDefault(_jsFileDownload);
var _submitions = require("./Submitions");
var _submitionsDefault = parcelHelpers.interopDefault(_submitions);
var _sweetalert = require("sweetalert");
var _sweetalertDefault = parcelHelpers.interopDefault(_sweetalert);
var _jwtDecode = require("jwt-decode");
var _jwtDecodeDefault = parcelHelpers.interopDefault(_jwtDecode);
class ViewSubmitionssp extends _react.Component {
    constructor(props){
        super(props);
        this.state = {
            groupId: "",
            submitions: []
        };
    }
    componentDidMount() {
        if (localStorage.userToken) {
            const usertoken = localStorage.userToken;
            const decoded = _jwtDecodeDefault.default(usertoken);
            this.setState({
                groupId: decoded.groupId
            });
        }
        setTimeout(()=>{
            this.retrieveSubmitions();
        }, 1000);
    }
    //retrive submition using group id
    retrieveSubmitions() {
        // const gid = this.props.match.params.id ;
        console.log(this.state.groupId);
        //console.log("Hello")
        const gid = this.state.groupId;
        _axiosDefault.default.get(`http://localhost:5000/submition/group/${gid}`).then((res)=>{
            if (res.data.success) {
                this.setState({
                    submitions: res.data.exsitingSubmitions
                });
                console.log(this.state.submitions);
            }
        });
    }
    //delete submition  
    onDelete = (id)=>{
        _sweetalertDefault.default({
            title: "Are you sure?",
            text: "Once deleted, you will not be able to recover this file!",
            icon: "warning",
            buttons: true,
            dangerMode: true
        }).then((willDelete)=>{
            if (willDelete) {
                _axiosDefault.default.delete(`http://localhost:5000/submition/delete/${id}`).then((res)=>{
                    this.retrieveSubmitions();
                });
                _sweetalertDefault.default("Your Submition has been deleted", "", "success");
            } else _sweetalertDefault.default("Your file is safe!");
        });
    };
    //Download File
    downloadFile = (fileName)=>{
        const data = {
            fileName: fileName
        };
        _axiosDefault.default({
            url: "http://localhost:5000/file/download",
            data,
            method: "POST",
            responseType: "blob"
        }).then((res)=>{
            console.log(res);
            _jsFileDownloadDefault.default(res.data, fileName);
        });
    };
    //filter submition
    filterData(submitions, searchKey) {
        const result = submitions.filter((submition)=>submition.type.toLowerCase().includes(searchKey) || submition.files.toLowerCase().includes(searchKey) || submition.type.includes(searchKey) || submition.files.includes(searchKey)
        );
        this.setState({
            submitions: result
        });
    }
    //search submition
    handleSearchArea = (e)=>{
        const searchKey = e.currentTarget.value;
        const gid = this.state.groupId;
        _axiosDefault.default.get(`http://localhost:5000/submition/group/${gid}`).then((res)=>{
            if (res.data.success) this.filterData(res.data.exsitingSubmitions, searchKey);
        });
    };
    render() {
        return /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
            className: "container",
            children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                className: "row",
                children: [
                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                        className: "col-sm-6",
                        children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV(_submitionsDefault.default, {}, void 0, false, {
                            fileName: "src/components/IT20128036/user/viewSubmitionsp.js",
                            lineNumber: 176,
                            columnNumber: 9
                        }, this)
                    }, void 0, false, {
                        fileName: "src/components/IT20128036/user/viewSubmitionsp.js",
                        lineNumber: 175,
                        columnNumber: 5
                    }, this),
                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                        className: "col-sm-6",
                        children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                            className: "mt-4 mb-2 me-4",
                            children: [
                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                    className: "row",
                                    children: [
                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                            className: "col-lg-7 mt-2 mb-2",
                                            children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("h4", {
                                                children: "Your Submitions"
                                            }, void 0, false, {
                                                fileName: "src/components/IT20128036/user/viewSubmitionsp.js",
                                                lineNumber: 187,
                                                columnNumber: 15
                                            }, this)
                                        }, void 0, false, {
                                            fileName: "src/components/IT20128036/user/viewSubmitionsp.js",
                                            lineNumber: 186,
                                            columnNumber: 13
                                        }, this),
                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                            className: "col-lg-5 mt-2 mb-2",
                                            children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("input", {
                                                className: "form-control",
                                                type: "search",
                                                placeholder: "Search",
                                                name: "searchQuery",
                                                onChange: this.handleSearchArea
                                            }, void 0, false, {
                                                fileName: "src/components/IT20128036/user/viewSubmitionsp.js",
                                                lineNumber: 190,
                                                columnNumber: 15
                                            }, this)
                                        }, void 0, false, {
                                            fileName: "src/components/IT20128036/user/viewSubmitionsp.js",
                                            lineNumber: 189,
                                            columnNumber: 13
                                        }, this)
                                    ]
                                }, void 0, true, {
                                    fileName: "src/components/IT20128036/user/viewSubmitionsp.js",
                                    lineNumber: 185,
                                    columnNumber: 11
                                }, this),
                                this.state.submitions.map((submitions, index)=>/*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                        class: "row",
                                        children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                            class: "col-sm-12",
                                            children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                                class: "card border-secondary mt-3 mb-3",
                                                children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                                    class: "card-body",
                                                    children: [
                                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                                            class: "card-header",
                                                            children: [
                                                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("h5", {
                                                                    class: "card-title",
                                                                    children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("p", {
                                                                        children: [
                                                                            "Group ",
                                                                            submitions.groupId,
                                                                            " "
                                                                        ]
                                                                    }, void 0, true, {
                                                                        fileName: "src/components/IT20128036/user/viewSubmitionsp.js",
                                                                        lineNumber: 208,
                                                                        columnNumber: 24
                                                                    }, this)
                                                                }, void 0, false, {
                                                                    fileName: "src/components/IT20128036/user/viewSubmitionsp.js",
                                                                    lineNumber: 206,
                                                                    columnNumber: 23
                                                                }, this),
                                                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                                                    className: "row mt-4 mb-4",
                                                                    children: [
                                                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                                                            className: "col-lg-4",
                                                                            children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("a", {
                                                                                className: "btn btn-outline-success",
                                                                                onClick: ()=>this.downloadFile(submitions.files)
                                                                                ,
                                                                                children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("i", {
                                                                                    class: "fa fa-download",
                                                                                    "aria-hidden": "true",
                                                                                    children: [
                                                                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("br", {}, void 0, false, {
                                                                                            fileName: "src/components/IT20128036/user/viewSubmitionsp.js",
                                                                                            lineNumber: 219,
                                                                                            columnNumber: 159
                                                                                        }, this),
                                                                                        "Download"
                                                                                    ]
                                                                                }, void 0, true, {
                                                                                    fileName: "src/components/IT20128036/user/viewSubmitionsp.js",
                                                                                    lineNumber: 219,
                                                                                    columnNumber: 114
                                                                                }, this)
                                                                            }, void 0, false, {
                                                                                fileName: "src/components/IT20128036/user/viewSubmitionsp.js",
                                                                                lineNumber: 219,
                                                                                columnNumber: 23
                                                                            }, this)
                                                                        }, void 0, false, {
                                                                            fileName: "src/components/IT20128036/user/viewSubmitionsp.js",
                                                                            lineNumber: 217,
                                                                            columnNumber: 23
                                                                        }, this),
                                                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                                                            className: "col-lg-3",
                                                                            children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("a", {
                                                                                className: "btn btn-outline-primary",
                                                                                href: `/student/submition/edit/${submitions._id}`,
                                                                                children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("i", {
                                                                                    className: "fas fa-edit",
                                                                                    children: [
                                                                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("br", {}, void 0, false, {
                                                                                            fileName: "src/components/IT20128036/user/viewSubmitionsp.js",
                                                                                            lineNumber: 225,
                                                                                            columnNumber: 46
                                                                                        }, this),
                                                                                        "Edit"
                                                                                    ]
                                                                                }, void 0, true, {
                                                                                    fileName: "src/components/IT20128036/user/viewSubmitionsp.js",
                                                                                    lineNumber: 225,
                                                                                    columnNumber: 19
                                                                                }, this)
                                                                            }, void 0, false, {
                                                                                fileName: "src/components/IT20128036/user/viewSubmitionsp.js",
                                                                                lineNumber: 224,
                                                                                columnNumber: 23
                                                                            }, this)
                                                                        }, void 0, false, {
                                                                            fileName: "src/components/IT20128036/user/viewSubmitionsp.js",
                                                                            lineNumber: 223,
                                                                            columnNumber: 23
                                                                        }, this),
                                                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                                                            className: "col-lg-4",
                                                                            children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("a", {
                                                                                className: "btn btn-outline-danger",
                                                                                href: "#",
                                                                                onClick: ()=>{
                                                                                    this.onDelete(submitions._id);
                                                                                },
                                                                                children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("i", {
                                                                                    className: "fas fa-trash-alt",
                                                                                    children: [
                                                                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("br", {}, void 0, false, {
                                                                                            fileName: "src/components/IT20128036/user/viewSubmitionsp.js",
                                                                                            lineNumber: 234,
                                                                                            columnNumber: 51
                                                                                        }, this),
                                                                                        "Delete"
                                                                                    ]
                                                                                }, void 0, true, {
                                                                                    fileName: "src/components/IT20128036/user/viewSubmitionsp.js",
                                                                                    lineNumber: 234,
                                                                                    columnNumber: 19
                                                                                }, this)
                                                                            }, void 0, false, {
                                                                                fileName: "src/components/IT20128036/user/viewSubmitionsp.js",
                                                                                lineNumber: 231,
                                                                                columnNumber: 23
                                                                            }, this)
                                                                        }, void 0, false, {
                                                                            fileName: "src/components/IT20128036/user/viewSubmitionsp.js",
                                                                            lineNumber: 229,
                                                                            columnNumber: 23
                                                                        }, this)
                                                                    ]
                                                                }, void 0, true, {
                                                                    fileName: "src/components/IT20128036/user/viewSubmitionsp.js",
                                                                    lineNumber: 216,
                                                                    columnNumber: 23
                                                                }, this)
                                                            ]
                                                        }, void 0, true, {
                                                            fileName: "src/components/IT20128036/user/viewSubmitionsp.js",
                                                            lineNumber: 205,
                                                            columnNumber: 21
                                                        }, this),
                                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("p", {
                                                            class: "card-text"
                                                        }, void 0, false, {
                                                            fileName: "src/components/IT20128036/user/viewSubmitionsp.js",
                                                            lineNumber: 250,
                                                            columnNumber: 21
                                                        }, this),
                                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("p", {
                                                            children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("strong", {
                                                                children: "Type"
                                                            }, void 0, false, {
                                                                fileName: "src/components/IT20128036/user/viewSubmitionsp.js",
                                                                lineNumber: 252,
                                                                columnNumber: 23
                                                            }, this)
                                                        }, void 0, false, {
                                                            fileName: "src/components/IT20128036/user/viewSubmitionsp.js",
                                                            lineNumber: 251,
                                                            columnNumber: 21
                                                        }, this),
                                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("p", {
                                                            children: [
                                                                " ",
                                                                submitions.type
                                                            ]
                                                        }, void 0, true, {
                                                            fileName: "src/components/IT20128036/user/viewSubmitionsp.js",
                                                            lineNumber: 254,
                                                            columnNumber: 21
                                                        }, this),
                                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("p", {
                                                            children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("strong", {
                                                                children: "Description"
                                                            }, void 0, false, {
                                                                fileName: "src/components/IT20128036/user/viewSubmitionsp.js",
                                                                lineNumber: 256,
                                                                columnNumber: 23
                                                            }, this)
                                                        }, void 0, false, {
                                                            fileName: "src/components/IT20128036/user/viewSubmitionsp.js",
                                                            lineNumber: 255,
                                                            columnNumber: 21
                                                        }, this),
                                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("p", {
                                                            children: submitions.description
                                                        }, void 0, false, {
                                                            fileName: "src/components/IT20128036/user/viewSubmitionsp.js",
                                                            lineNumber: 258,
                                                            columnNumber: 21
                                                        }, this),
                                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("p", {
                                                            children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("strong", {
                                                                children: "File Name"
                                                            }, void 0, false, {
                                                                fileName: "src/components/IT20128036/user/viewSubmitionsp.js",
                                                                lineNumber: 262,
                                                                columnNumber: 23
                                                            }, this)
                                                        }, void 0, false, {
                                                            fileName: "src/components/IT20128036/user/viewSubmitionsp.js",
                                                            lineNumber: 260,
                                                            columnNumber: 21
                                                        }, this),
                                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("p", {
                                                            children: submitions.files
                                                        }, void 0, false, {
                                                            fileName: "src/components/IT20128036/user/viewSubmitionsp.js",
                                                            lineNumber: 264,
                                                            columnNumber: 21
                                                        }, this)
                                                    ]
                                                }, void 0, true, {
                                                    fileName: "src/components/IT20128036/user/viewSubmitionsp.js",
                                                    lineNumber: 204,
                                                    columnNumber: 19
                                                }, this)
                                            }, void 0, false, {
                                                fileName: "src/components/IT20128036/user/viewSubmitionsp.js",
                                                lineNumber: 203,
                                                columnNumber: 17
                                            }, this)
                                        }, void 0, false, {
                                            fileName: "src/components/IT20128036/user/viewSubmitionsp.js",
                                            lineNumber: 202,
                                            columnNumber: 15
                                        }, this)
                                    }, void 0, false, {
                                        fileName: "src/components/IT20128036/user/viewSubmitionsp.js",
                                        lineNumber: 201,
                                        columnNumber: 13
                                    }, this)
                                )
                            ]
                        }, void 0, true, {
                            fileName: "src/components/IT20128036/user/viewSubmitionsp.js",
                            lineNumber: 184,
                            columnNumber: 9
                        }, this)
                    }, void 0, false, {
                        fileName: "src/components/IT20128036/user/viewSubmitionsp.js",
                        lineNumber: 182,
                        columnNumber: 7
                    }, this)
                ]
            }, void 0, true, {
                fileName: "src/components/IT20128036/user/viewSubmitionsp.js",
                lineNumber: 172,
                columnNumber: 5
            }, this)
        }, void 0, false, {
            fileName: "src/components/IT20128036/user/viewSubmitionsp.js",
            lineNumber: 171,
            columnNumber: 5
        }, this);
    }
}
exports.default = ViewSubmitionssp;

  $parcel$ReactRefreshHelpers$cb31.postlude(module);
} finally {
  window.$RefreshReg$ = prevRefreshReg;
  window.$RefreshSig$ = prevRefreshSig;
}
},{"react/jsx-dev-runtime":"iTorj","react":"21dqq","axios":"jo6P5","js-file-download":"cLODl","./Submitions":"lpiEQ","sweetalert":"38a65","jwt-decode":"ljM1w","@parcel/transformer-js/src/esmodule-helpers.js":"gkKU3","@parcel/transformer-react-refresh-wrap/lib/helpers/helpers.js":"km3Ru"}],"cLODl":[function(require,module,exports) {
module.exports = function(data, filename, mime, bom) {
    var blobData = typeof bom !== 'undefined' ? [
        bom,
        data
    ] : [
        data
    ];
    var blob = new Blob(blobData, {
        type: mime || 'application/octet-stream'
    });
    if (typeof window.navigator.msSaveBlob !== 'undefined') // IE workaround for "HTML7007: One or more blob URLs were
    // revoked by closing the blob for which they were created.
    // These URLs will no longer resolve as the data backing
    // the URL has been freed."
    window.navigator.msSaveBlob(blob, filename);
    else {
        var blobURL = window.URL && window.URL.createObjectURL ? window.URL.createObjectURL(blob) : window.webkitURL.createObjectURL(blob);
        var tempLink = document.createElement('a');
        tempLink.style.display = 'none';
        tempLink.href = blobURL;
        tempLink.setAttribute('download', filename);
        // Safari thinks _blank anchor are pop ups. We only want to set _blank
        // target if the browser does not support the HTML5 download attribute.
        // This allows you to download files in desktop safari if pop up blocking
        // is enabled.
        if (typeof tempLink.download === 'undefined') tempLink.setAttribute('target', '_blank');
        document.body.appendChild(tempLink);
        tempLink.click();
        // Fixes "webkit blob resource error 1"
        setTimeout(function() {
            document.body.removeChild(tempLink);
            window.URL.revokeObjectURL(blobURL);
        }, 200);
    }
};

},{}],"5NXwT":[function(require,module,exports) {
var $parcel$ReactRefreshHelpers$1e06 = require("@parcel/transformer-react-refresh-wrap/lib/helpers/helpers.js");
var prevRefreshReg = window.$RefreshReg$;
var prevRefreshSig = window.$RefreshSig$;
$parcel$ReactRefreshHelpers$1e06.prelude(module);

try {
var parcelHelpers = require("@parcel/transformer-js/src/esmodule-helpers.js");
parcelHelpers.defineInteropFlag(exports);
var _jsxDevRuntime = require("react/jsx-dev-runtime");
var _react = require("react");
var _reactDefault = parcelHelpers.interopDefault(_react);
var _axios = require("axios");
var _axiosDefault = parcelHelpers.interopDefault(_axios);
var _reactToPrint = require("react-to-print");
var _reactToPrintDefault = parcelHelpers.interopDefault(_reactToPrint);
class sviewMarks extends _react.Component {
    constructor(props){
        super(props);
        this.state = {
            marks: []
        };
    }
    componentDidMount() {
        this.retriveMarks();
    }
    //retrive marks
    retriveMarks() {
        _axiosDefault.default.get("http://localhost:5000/marks").then((res)=>{
            if (res.data.success) {
                this.setState({
                    marks: res.data.existingMarks
                });
                console.log(this.state.marks);
            }
        });
    }
    //delete marks
    onDelete = (id)=>{
        _axiosDefault.default.delete(`http://localhost:5000/mark/delete/${id}`).then((res)=>{
            alert("Deleted Successfully");
            this.retriveMarks();
        });
    };
    //filter marks
    filterData(marks, searchKey) {
        const result = marks.filter((mark)=>mark.groupId.toLowerCase().includes(searchKey) || mark.type.toLowerCase().includes(searchKey) || mark.gradingStatus.toLowerCase().includes(searchKey) || mark.groupId.includes(searchKey) || mark.type.includes(searchKey) || mark.gradingStatus.includes(searchKey)
        );
        this.setState({
            marks: result
        });
    }
    //search marks
    handleSearchArea = (e)=>{
        const searchKey = e.currentTarget.value;
        _axiosDefault.default.get("http://localhost:5000/marks").then((res)=>{
            if (res.data.success) this.filterData(res.data.existingMarks, searchKey);
        });
    };
    render() {
        return /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
            className: "container",
            children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                className: "ms-4 me-4 mt-2 mb-2",
                children: [
                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                        className: "row mt-2 mb-2",
                        children: [
                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                className: "col-lg-4 mt-2 mb-2",
                                children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("a", {
                                    href: "/student/pdf/export",
                                    className: "btn btn-outline-dark",
                                    children: "Export PDF"
                                }, void 0, false, {
                                    fileName: "src/components/IT20128036/user/sviewMarks.js",
                                    lineNumber: 85,
                                    columnNumber: 9
                                }, this)
                            }, void 0, false, {
                                fileName: "src/components/IT20128036/user/sviewMarks.js",
                                lineNumber: 81,
                                columnNumber: 7
                            }, this),
                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                className: "col-lg-5 mt-2 mb-2",
                                children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("h4", {
                                    children: "Marks Of Submitions"
                                }, void 0, false, {
                                    fileName: "src/components/IT20128036/user/sviewMarks.js",
                                    lineNumber: 98,
                                    columnNumber: 7
                                }, this)
                            }, void 0, false, {
                                fileName: "src/components/IT20128036/user/sviewMarks.js",
                                lineNumber: 93,
                                columnNumber: 7
                            }, this),
                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                className: "col-lg-3 mt-2 mb-2",
                                children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("input", {
                                    className: "form-control",
                                    type: "search",
                                    placeholder: "Search",
                                    name: "searchQuery",
                                    onChange: this.handleSearchArea
                                }, void 0, false, {
                                    fileName: "src/components/IT20128036/user/sviewMarks.js",
                                    lineNumber: 109,
                                    columnNumber: 9
                                }, this)
                            }, void 0, false, {
                                fileName: "src/components/IT20128036/user/sviewMarks.js",
                                lineNumber: 107,
                                columnNumber: 7
                            }, this)
                        ]
                    }, void 0, true, {
                        fileName: "src/components/IT20128036/user/sviewMarks.js",
                        lineNumber: 80,
                        columnNumber: 7
                    }, this),
                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("table", {
                        className: "table table-light table-striped table-hover mt-4 mb-4",
                        children: [
                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("thead", {
                                children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("tr", {
                                    children: [
                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("th", {
                                            scope: "col",
                                            children: "#"
                                        }, void 0, false, {
                                            fileName: "src/components/IT20128036/user/sviewMarks.js",
                                            lineNumber: 126,
                                            columnNumber: 12
                                        }, this),
                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("th", {
                                            scope: "col",
                                            children: "Group ID"
                                        }, void 0, false, {
                                            fileName: "src/components/IT20128036/user/sviewMarks.js",
                                            lineNumber: 127,
                                            columnNumber: 12
                                        }, this),
                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("th", {
                                            scope: "col",
                                            children: "Submition Type"
                                        }, void 0, false, {
                                            fileName: "src/components/IT20128036/user/sviewMarks.js",
                                            lineNumber: 128,
                                            columnNumber: 12
                                        }, this),
                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("th", {
                                            scope: "col",
                                            children: "Marks (?/100)"
                                        }, void 0, false, {
                                            fileName: "src/components/IT20128036/user/sviewMarks.js",
                                            lineNumber: 129,
                                            columnNumber: 12
                                        }, this),
                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("th", {
                                            scope: "col",
                                            children: "Grading Status"
                                        }, void 0, false, {
                                            fileName: "src/components/IT20128036/user/sviewMarks.js",
                                            lineNumber: 130,
                                            columnNumber: 12
                                        }, this),
                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("th", {
                                            scope: "col",
                                            children: "Marked By"
                                        }, void 0, false, {
                                            fileName: "src/components/IT20128036/user/sviewMarks.js",
                                            lineNumber: 131,
                                            columnNumber: 12
                                        }, this)
                                    ]
                                }, void 0, true, {
                                    fileName: "src/components/IT20128036/user/sviewMarks.js",
                                    lineNumber: 125,
                                    columnNumber: 10
                                }, this)
                            }, void 0, false, {
                                fileName: "src/components/IT20128036/user/sviewMarks.js",
                                lineNumber: 124,
                                columnNumber: 8
                            }, this),
                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("tbody", {
                                children: this.state.marks.map((marks, index)=>/*#__PURE__*/ _jsxDevRuntime.jsxDEV("tr", {
                                        children: [
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("th", {
                                                scope: "row",
                                                children: index + 1
                                            }, void 0, false, {
                                                fileName: "src/components/IT20128036/user/sviewMarks.js",
                                                lineNumber: 138,
                                                columnNumber: 14
                                            }, this),
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("td", {
                                                children: marks.groupId
                                            }, void 0, false, {
                                                fileName: "src/components/IT20128036/user/sviewMarks.js",
                                                lineNumber: 141,
                                                columnNumber: 14
                                            }, this),
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("td", {
                                                children: marks.type
                                            }, void 0, false, {
                                                fileName: "src/components/IT20128036/user/sviewMarks.js",
                                                lineNumber: 147,
                                                columnNumber: 14
                                            }, this),
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("td", {
                                                children: marks.marks
                                            }, void 0, false, {
                                                fileName: "src/components/IT20128036/user/sviewMarks.js",
                                                lineNumber: 149,
                                                columnNumber: 14
                                            }, this),
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("td", {
                                                children: marks.gradingStatus
                                            }, void 0, false, {
                                                fileName: "src/components/IT20128036/user/sviewMarks.js",
                                                lineNumber: 150,
                                                columnNumber: 14
                                            }, this),
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("td", {
                                                children: marks.markedby
                                            }, void 0, false, {
                                                fileName: "src/components/IT20128036/user/sviewMarks.js",
                                                lineNumber: 151,
                                                columnNumber: 14
                                            }, this)
                                        ]
                                    }, index, true, {
                                        fileName: "src/components/IT20128036/user/sviewMarks.js",
                                        lineNumber: 137,
                                        columnNumber: 12
                                    }, this)
                                )
                            }, void 0, false, {
                                fileName: "src/components/IT20128036/user/sviewMarks.js",
                                lineNumber: 135,
                                columnNumber: 8
                            }, this)
                        ]
                    }, void 0, true, {
                        fileName: "src/components/IT20128036/user/sviewMarks.js",
                        lineNumber: 123,
                        columnNumber: 6
                    }, this)
                ]
            }, void 0, true, {
                fileName: "src/components/IT20128036/user/sviewMarks.js",
                lineNumber: 78,
                columnNumber: 1
            }, this)
        }, void 0, false, {
            fileName: "src/components/IT20128036/user/sviewMarks.js",
            lineNumber: 76,
            columnNumber: 5
        }, this);
    }
}
exports.default = sviewMarks;

  $parcel$ReactRefreshHelpers$1e06.postlude(module);
} finally {
  window.$RefreshReg$ = prevRefreshReg;
  window.$RefreshSig$ = prevRefreshSig;
}
},{"react/jsx-dev-runtime":"iTorj","react":"21dqq","axios":"jo6P5","react-to-print":"amkqM","@parcel/transformer-js/src/esmodule-helpers.js":"gkKU3","@parcel/transformer-react-refresh-wrap/lib/helpers/helpers.js":"km3Ru"}],"amkqM":[function(require,module,exports) {
/*! For license information please see index.js.LICENSE.txt */ !function(e, t) {
    module.exports = t(require("react"), require("react-dom"));
}("undefined" != typeof self ? self : this, function(e1, t1) {
    return function() {
        var n1 = {
            655: function(e2, t2, n2) {
                n2.r(t2), n2.d(t2, {
                    __extends: function() {
                        return o2;
                    },
                    __assign: function() {
                        return i2;
                    },
                    __rest: function() {
                        return a1;
                    },
                    __decorate: function() {
                        return l1;
                    },
                    __param: function() {
                        return c1;
                    },
                    __metadata: function() {
                        return u1;
                    },
                    __awaiter: function() {
                        return s1;
                    },
                    __generator: function() {
                        return f;
                    },
                    __createBinding: function() {
                        return d;
                    },
                    __exportStar: function() {
                        return p;
                    },
                    __values: function() {
                        return h;
                    },
                    __read: function() {
                        return y;
                    },
                    __spread: function() {
                        return b;
                    },
                    __spreadArrays: function() {
                        return v;
                    },
                    __spreadArray: function() {
                        return g;
                    },
                    __await: function() {
                        return m;
                    },
                    __asyncGenerator: function() {
                        return w;
                    },
                    __asyncDelegator: function() {
                        return _;
                    },
                    __asyncValues: function() {
                        return P;
                    },
                    __makeTemplateObject: function() {
                        return O;
                    },
                    __importStar: function() {
                        return x;
                    },
                    __importDefault: function() {
                        return A;
                    },
                    __classPrivateFieldGet: function() {
                        return T;
                    },
                    __classPrivateFieldSet: function() {
                        return j;
                    }
                });
                var r2 = function(e3, t3) {
                    return r2 = Object.setPrototypeOf || ({
                        __proto__: []
                    }) instanceof Array && function(e, t) {
                        e.__proto__ = t;
                    } || function(e, t) {
                        for(var n in t)Object.prototype.hasOwnProperty.call(t, n) && (e[n] = t[n]);
                    }, r2(e3, t3);
                };
                function o2(e, t) {
                    if ("function" != typeof t && null !== t) throw new TypeError("Class extends value " + String(t) + " is not a constructor or null");
                    function n() {
                        this.constructor = e;
                    }
                    r2(e, t), e.prototype = null === t ? Object.create(t) : (n.prototype = t.prototype, new n);
                }
                var i2 = function() {
                    return i2 = Object.assign || function(e) {
                        for(var t, n = 1, r = arguments.length; n < r; n++)for(var o in t = arguments[n])Object.prototype.hasOwnProperty.call(t, o) && (e[o] = t[o]);
                        return e;
                    }, i2.apply(this, arguments);
                };
                function a1(e, t) {
                    var n = {};
                    for(var r in e)Object.prototype.hasOwnProperty.call(e, r) && t.indexOf(r) < 0 && (n[r] = e[r]);
                    if (null != e && "function" == typeof Object.getOwnPropertySymbols) {
                        var o = 0;
                        for(r = Object.getOwnPropertySymbols(e); o < r.length; o++)t.indexOf(r[o]) < 0 && Object.prototype.propertyIsEnumerable.call(e, r[o]) && (n[r[o]] = e[r[o]]);
                    }
                    return n;
                }
                function l1(e, t, n, r) {
                    var o, i = arguments.length, a = i < 3 ? t : null === r ? r = Object.getOwnPropertyDescriptor(t, n) : r;
                    if ("object" == typeof Reflect && "function" == typeof Reflect.decorate) a = Reflect.decorate(e, t, n, r);
                    else for(var l = e.length - 1; l >= 0; l--)(o = e[l]) && (a = (i < 3 ? o(a) : i > 3 ? o(t, n, a) : o(t, n)) || a);
                    return i > 3 && a && Object.defineProperty(t, n, a), a;
                }
                function c1(e, t) {
                    return function(n, r) {
                        t(n, r, e);
                    };
                }
                function u1(e, t) {
                    if ("object" == typeof Reflect && "function" == typeof Reflect.metadata) return Reflect.metadata(e, t);
                }
                function s1(e4, t4, n, r) {
                    return new (n || (n = Promise))(function(o, i) {
                        function a(e) {
                            try {
                                c(r.next(e));
                            } catch (e5) {
                                i(e5);
                            }
                        }
                        function l(e) {
                            try {
                                c(r.throw(e));
                            } catch (e6) {
                                i(e6);
                            }
                        }
                        function c(e7) {
                            var t;
                            e7.done ? o(e7.value) : (t = e7.value, t instanceof n ? t : new n(function(e) {
                                e(t);
                            })).then(a, l);
                        }
                        c((r = r.apply(e4, t4 || [])).next());
                    });
                }
                function f(e8, t) {
                    var n, r, o, i3, a = {
                        label: 0,
                        sent: function() {
                            if (1 & o[0]) throw o[1];
                            return o[1];
                        },
                        trys: [],
                        ops: []
                    };
                    return i3 = {
                        next: l2(0),
                        throw: l2(1),
                        return: l2(2)
                    }, "function" == typeof Symbol && (i3[Symbol.iterator] = function() {
                        return this;
                    }), i3;
                    function l2(i4) {
                        return function(l) {
                            return function(i) {
                                if (n) throw new TypeError("Generator is already executing.");
                                for(; a;)try {
                                    if (n = 1, r && (o = 2 & i[0] ? r.return : i[0] ? r.throw || ((o = r.return) && o.call(r), 0) : r.next) && !(o = o.call(r, i[1])).done) return o;
                                    switch(r = 0, o && (i = [
                                        2 & i[0],
                                        o.value
                                    ]), i[0]){
                                        case 0:
                                        case 1:
                                            o = i;
                                            break;
                                        case 4:
                                            return a.label++, {
                                                value: i[1],
                                                done: !1
                                            };
                                        case 5:
                                            a.label++, r = i[1], i = [
                                                0
                                            ];
                                            continue;
                                        case 7:
                                            i = a.ops.pop(), a.trys.pop();
                                            continue;
                                        default:
                                            if (!((o = (o = a.trys).length > 0 && o[o.length - 1]) || 6 !== i[0] && 2 !== i[0])) {
                                                a = 0;
                                                continue;
                                            }
                                            if (3 === i[0] && (!o || i[1] > o[0] && i[1] < o[3])) {
                                                a.label = i[1];
                                                break;
                                            }
                                            if (6 === i[0] && a.label < o[1]) {
                                                a.label = o[1], o = i;
                                                break;
                                            }
                                            if (o && a.label < o[2]) {
                                                a.label = o[2], a.ops.push(i);
                                                break;
                                            }
                                            o[2] && a.ops.pop(), a.trys.pop();
                                            continue;
                                    }
                                    i = t.call(e8, a);
                                } catch (e) {
                                    i = [
                                        6,
                                        e
                                    ], r = 0;
                                } finally{
                                    n = o = 0;
                                }
                                if (5 & i[0]) throw i[1];
                                return {
                                    value: i[0] ? i[1] : void 0,
                                    done: !0
                                };
                            }([
                                i4,
                                l
                            ]);
                        };
                    }
                }
                var d = Object.create ? function(e, t, n, r) {
                    void 0 === r && (r = n), Object.defineProperty(e, r, {
                        enumerable: !0,
                        get: function() {
                            return t[n];
                        }
                    });
                } : function(e, t, n, r) {
                    void 0 === r && (r = n), e[r] = t[n];
                };
                function p(e, t) {
                    for(var n in e)"default" === n || Object.prototype.hasOwnProperty.call(t, n) || d(t, e, n);
                }
                function h(e) {
                    var t = "function" == typeof Symbol && Symbol.iterator, n = t && e[t], r = 0;
                    if (n) return n.call(e);
                    if (e && "number" == typeof e.length) return {
                        next: function() {
                            return e && r >= e.length && (e = void 0), {
                                value: e && e[r++],
                                done: !e
                            };
                        }
                    };
                    throw new TypeError(t ? "Object is not iterable." : "Symbol.iterator is not defined.");
                }
                function y(e, t) {
                    var n = "function" == typeof Symbol && e[Symbol.iterator];
                    if (!n) return e;
                    var r, o, i = n.call(e), a = [];
                    try {
                        for(; (void 0 === t || t-- > 0) && !(r = i.next()).done;)a.push(r.value);
                    } catch (e9) {
                        o = {
                            error: e9
                        };
                    } finally{
                        try {
                            r && !r.done && (n = i.return) && n.call(i);
                        } finally{
                            if (o) throw o.error;
                        }
                    }
                    return a;
                }
                function b() {
                    for(var e = [], t = 0; t < arguments.length; t++)e = e.concat(y(arguments[t]));
                    return e;
                }
                function v() {
                    for(var e = 0, t = 0, n = arguments.length; t < n; t++)e += arguments[t].length;
                    var r = Array(e), o = 0;
                    for(t = 0; t < n; t++)for(var i = arguments[t], a = 0, l = i.length; a < l; a++, o++)r[o] = i[a];
                    return r;
                }
                function g(e, t, n) {
                    if (n || 2 === arguments.length) for(var r, o = 0, i = t.length; o < i; o++)!r && o in t || (r || (r = Array.prototype.slice.call(t, 0, o)), r[o] = t[o]);
                    return e.concat(r || Array.prototype.slice.call(t));
                }
                function m(e) {
                    return this instanceof m ? (this.v = e, this) : new m(e);
                }
                function w(e10, t5, n3) {
                    if (!Symbol.asyncIterator) throw new TypeError("Symbol.asyncIterator is not defined.");
                    var r3, o = n3.apply(e10, t5 || []), i = [];
                    return r3 = {}, a("next"), a("throw"), a("return"), r3[Symbol.asyncIterator] = function() {
                        return this;
                    }, r3;
                    function a(e) {
                        o[e] && (r3[e] = function(t) {
                            return new Promise(function(n, r) {
                                i.push([
                                    e,
                                    t,
                                    n,
                                    r
                                ]) > 1 || l(e, t);
                            });
                        });
                    }
                    function l(e, t) {
                        try {
                            (n = o[e](t)).value instanceof m ? Promise.resolve(n.value.v).then(c, u) : s(i[0][2], n);
                        } catch (e11) {
                            s(i[0][3], e11);
                        }
                        var n;
                    }
                    function c(e) {
                        l("next", e);
                    }
                    function u(e) {
                        l("throw", e);
                    }
                    function s(e, t) {
                        e(t), i.shift(), i.length && l(i[0][0], i[0][1]);
                    }
                }
                function _(e12) {
                    var t6, n;
                    return t6 = {}, r4("next"), r4("throw", function(e) {
                        throw e;
                    }), r4("return"), t6[Symbol.iterator] = function() {
                        return this;
                    }, t6;
                    function r4(r, o) {
                        t6[r] = e12[r] ? function(t) {
                            return (n = !n) ? {
                                value: m(e12[r](t)),
                                done: "return" === r
                            } : o ? o(t) : t;
                        } : o;
                    }
                }
                function P(e13) {
                    if (!Symbol.asyncIterator) throw new TypeError("Symbol.asyncIterator is not defined.");
                    var t7, n4 = e13[Symbol.asyncIterator];
                    return n4 ? n4.call(e13) : (e13 = h(e13), t7 = {}, r5("next"), r5("throw"), r5("return"), t7[Symbol.asyncIterator] = function() {
                        return this;
                    }, t7);
                    function r5(n5) {
                        t7[n5] = e13[n5] && function(t8) {
                            return new Promise(function(r6, o) {
                                !function(e, t9, n, r) {
                                    Promise.resolve(r).then(function(t) {
                                        e({
                                            value: t,
                                            done: n
                                        });
                                    }, t9);
                                }(r6, o, (t8 = e13[n5](t8)).done, t8.value);
                            });
                        };
                    }
                }
                function O(e, t) {
                    return Object.defineProperty ? Object.defineProperty(e, "raw", {
                        value: t
                    }) : e.raw = t, e;
                }
                var S = Object.create ? function(e, t) {
                    Object.defineProperty(e, "default", {
                        enumerable: !0,
                        value: t
                    });
                } : function(e, t) {
                    e.default = t;
                };
                function x(e) {
                    if (e && e.__esModule) return e;
                    var t = {};
                    if (null != e) for(var n in e)"default" !== n && Object.prototype.hasOwnProperty.call(e, n) && d(t, e, n);
                    return S(t, e), t;
                }
                function A(e) {
                    return e && e.__esModule ? e : {
                        default: e
                    };
                }
                function T(e, t, n, r) {
                    if ("a" === n && !r) throw new TypeError("Private accessor was defined without a getter");
                    if ("function" == typeof t ? e !== t || !r : !t.has(e)) throw new TypeError("Cannot read private member from an object whose class did not declare it");
                    return "m" === n ? r : "a" === n ? r.call(e) : r ? r.value : t.get(e);
                }
                function j(e, t, n, r, o) {
                    if ("m" === r) throw new TypeError("Private method is not writable");
                    if ("a" === r && !o) throw new TypeError("Private accessor was defined without a setter");
                    if ("function" == typeof t ? e !== t || !o : !t.has(e)) throw new TypeError("Cannot write private member to an object whose class did not declare it");
                    return "a" === r ? o.call(e, n) : o ? o.value = n : t.set(e, n), n;
                }
            },
            156: function(t) {
                t.exports = e1;
            },
            111: function(e) {
                e.exports = t1;
            }
        }, r1 = {};
        function o1(e) {
            var t = r1[e];
            if (void 0 !== t) return t.exports;
            var i = r1[e] = {
                exports: {}
            };
            return n1[e](i, i.exports, o1), i.exports;
        }
        o1.d = function(e, t) {
            for(var n in t)o1.o(t, n) && !o1.o(e, n) && Object.defineProperty(e, n, {
                enumerable: !0,
                get: t[n]
            });
        }, o1.o = function(e, t) {
            return Object.prototype.hasOwnProperty.call(e, t);
        }, o1.r = function(e) {
            "undefined" != typeof Symbol && Symbol.toStringTag && Object.defineProperty(e, Symbol.toStringTag, {
                value: "Module"
            }), Object.defineProperty(e, "__esModule", {
                value: !0
            });
        };
        var i1 = {};
        return function() {
            var e14 = i1;
            Object.defineProperty(e14, "__esModule", {
                value: !0
            }), e14.useReactToPrint = e14.PrintContextConsumer = void 0;
            var t10 = o1(655), n6 = o1(156), r7 = o1(111), a2 = Object.prototype.hasOwnProperty.call(n6, "createContext"), l3 = Object.prototype.hasOwnProperty.call(n6, "useMemo") && Object.prototype.hasOwnProperty.call(n6, "useCallback"), c2 = a2 ? n6.createContext({}) : null;
            e14.PrintContextConsumer = c2 ? c2.Consumer : function() {
                return null;
            };
            var u2 = {
                copyStyles: !0,
                pageStyle: "@page { size: auto;  margin: 0mm; } @media print { body { -webkit-print-color-adjust: exact; } }",
                removeAfterPrint: !1,
                suppressErrors: !1
            }, s2 = function(e15) {
                function o3() {
                    var n = null !== e15 && e15.apply(this, arguments) || this;
                    return n.startPrint = function(e16) {
                        var t11 = n.props, r = t11.onAfterPrint, o = t11.onPrintError, i = t11.print, a = t11.documentTitle;
                        setTimeout(function() {
                            var t, l;
                            if (e16.contentWindow) {
                                if (e16.contentWindow.focus(), i) i(e16).then(n.handleRemoveIframe).catch(function(e) {
                                    o ? o("print", e) : n.logMessages([
                                        "An error was thrown by the specified `print` function"
                                    ]);
                                });
                                else if (e16.contentWindow.print) {
                                    var c = null !== (l = null === (t = e16.contentDocument) || void 0 === t ? void 0 : t.title) && void 0 !== l ? l : "", u = e16.ownerDocument.title;
                                    a && (e16.ownerDocument.title = a, e16.contentDocument && (e16.contentDocument.title = a)), e16.contentWindow.print(), a && (e16.ownerDocument.title = u, e16.contentDocument && (e16.contentDocument.title = c));
                                } else n.logMessages([
                                    "Printing for this browser is not currently possible: the browser does not have a `print` method available for iframes."
                                ]);
                                r && r(), n.handleRemoveIframe();
                            } else n.logMessages([
                                "Printing failed because the `contentWindow` of the print iframe did not load. This is possibly an error with `react-to-print`. Please file an issue: https://github.com/gregnb/react-to-print/issues/"
                            ]);
                        }, 500);
                    }, n.triggerPrint = function(e17) {
                        var t = n.props, r = t.onBeforePrint, o = t.onPrintError;
                        if (r) {
                            var i = r();
                            i && "function" == typeof i.then ? i.then(function() {
                                n.startPrint(e17);
                            }).catch(function(e) {
                                o && o("onBeforePrint", e);
                            }) : n.startPrint(e17);
                        } else n.startPrint(e17);
                    }, n.handleClick = function() {
                        var e18 = n.props, t = e18.onBeforeGetContent, r = e18.onPrintError;
                        if (t) {
                            var o = t();
                            o && "function" == typeof o.then ? o.then(n.handlePrint).catch(function(e) {
                                r && r("onBeforeGetContent", e);
                            }) : n.handlePrint();
                        } else n.handlePrint();
                    }, n.handlePrint = function() {
                        var e19 = n.props, o = e19.bodyClass, i5 = e19.content, a = e19.copyStyles, l = e19.fonts, c = e19.pageStyle, u = e19.nonce, s3 = i5();
                        if (void 0 !== s3) {
                            if (null !== s3) {
                                var f = document.createElement("iframe");
                                f.style.position = "absolute", f.style.top = "-1000px", f.style.left = "-1000px", f.id = "printWindow", f.srcdoc = "<!DOCTYPE html>";
                                var d = (0, r7.findDOMNode)(s3);
                                if (d) {
                                    var p = d.cloneNode(!0), h = p instanceof Text, y1 = document.querySelectorAll("link[rel='stylesheet']"), b = h ? [] : p.querySelectorAll("img"), v = h ? [] : p.querySelectorAll("video");
                                    n.linkTotal = y1.length + b.length + v.length, n.linksLoaded = [], n.linksErrored = [], n.fontsLoaded = [], n.fontsErrored = [];
                                    var g = function(e, t) {
                                        t ? n.linksLoaded.push(e) : (n.logMessages([
                                            '"react-to-print" was unable to load a linked node. It may be invalid. "react-to-print" will continue attempting to print the page. The linked node that errored was:',
                                            e
                                        ]), n.linksErrored.push(e)), n.linksLoaded.length + n.linksErrored.length + n.fontsLoaded.length + n.fontsErrored.length === n.linkTotal && n.triggerPrint(f);
                                    };
                                    f.onload = function() {
                                        var e20, r, i, s;
                                        f.onload = null;
                                        var y = f.contentDocument || (null === (r = f.contentWindow) || void 0 === r ? void 0 : r.document);
                                        if (y) {
                                            y.body.appendChild(p), l && ((null === (i = f.contentDocument) || void 0 === i ? void 0 : i.fonts) && (null === (s = f.contentWindow) || void 0 === s ? void 0 : s.FontFace) ? l.forEach(function(e21) {
                                                var t = new FontFace(e21.family, e21.source);
                                                f.contentDocument.fonts.add(t), t.loaded.then(function(e) {
                                                    n.fontsLoaded.push(e);
                                                }).catch(function(e) {
                                                    n.fontsErrored.push(t), n.logMessages([
                                                        '"react-to-print" was unable to load a font. "react-to-print" will continue attempting to print the page. The font that failed to load is:',
                                                        t,
                                                        "The error from loading the font is:",
                                                        e
                                                    ]);
                                                });
                                            }) : n.logMessages([
                                                '"react-to-print" is not able to load custom fonts because the browser does not support the FontFace API'
                                            ]));
                                            var m = "function" == typeof c ? c() : c;
                                            if ("string" != typeof m) n.logMessages([
                                                '"react-to-print" expected a "string" from `pageStyle` but received "'.concat(typeof m, '". Styles from `pageStyle` will not be applied.')
                                            ]);
                                            else {
                                                var w = y.createElement("style");
                                                u && (w.setAttribute("nonce", u), y.head.setAttribute("nonce", u)), w.appendChild(y.createTextNode(m)), y.head.appendChild(w);
                                            }
                                            if (o && (e20 = y.body.classList).add.apply(e20, (0, t10.__spreadArray)([], (0, t10.__read)(o.split(" ")), !1)), !h) {
                                                for(var _ = h ? [] : d.querySelectorAll("canvas"), P = y.querySelectorAll("canvas"), O = 0; O < _.length; ++O){
                                                    var S = _[O], x = P[O].getContext("2d");
                                                    x && x.drawImage(S, 0, 0);
                                                }
                                                for(O = 0; O < b.length; O++){
                                                    var A = b[O], T = A.getAttribute("src");
                                                    T ? ((k = new Image).onload = g.bind(null, A, !0), k.onerror = g.bind(null, A, !1), k.src = T) : (n.logMessages([
                                                        '"react-to-print" encountered an <img> tag with an empty "src" attribute. It will not attempt to pre-load it. The <img> is:',
                                                        A
                                                    ], "warning"), g(A, !1));
                                                }
                                                for(O = 0; O < v.length; O++){
                                                    var j = v[O];
                                                    j.preload = "auto";
                                                    var k, E = j.getAttribute("poster");
                                                    E ? ((k = new Image).onload = g.bind(null, j, !0), k.onerror = g.bind(null, j, !1), k.src = E) : j.readyState >= 2 ? g(j, !0) : (j.onloadeddata = g.bind(null, j, !0), j.onerror = g.bind(null, j, !1), j.onstalled = g.bind(null, j, !1));
                                                }
                                                var C = "input", M = d.querySelectorAll(C), R = y.querySelectorAll(C);
                                                for(O = 0; O < M.length; O++)R[O].value = M[O].value;
                                                var I = "input[type=checkbox],input[type=radio]", q = d.querySelectorAll(I), D = y.querySelectorAll(I);
                                                for(O = 0; O < q.length; O++)D[O].checked = q[O].checked;
                                                var L = "select", W = d.querySelectorAll(L), F = y.querySelectorAll(L);
                                                for(O = 0; O < W.length; O++)F[O].value = W[O].value;
                                            }
                                            if (a) for(var N = document.querySelectorAll("style, link[rel='stylesheet']"), B = (O = 0, N.length); O < B; ++O){
                                                var G = N[O];
                                                if ("style" === G.tagName.toLowerCase()) {
                                                    var V = y.createElement(G.tagName), Y = G.sheet;
                                                    if (Y) {
                                                        var z = "";
                                                        try {
                                                            for(var H = Y.cssRules.length, J = 0; J < H; ++J)"string" == typeof Y.cssRules[J].cssText && (z += "".concat(Y.cssRules[J].cssText, "\r\n"));
                                                        } catch (e) {
                                                            n.logMessages([
                                                                "A stylesheet could not be accessed. This is likely due to the stylesheet having cross-origin imports, and many browsers block script access to cross-origin stylesheets. See https://github.com/gregnb/react-to-print/issues/429 for details. You may be able to load the sheet by both marking the stylesheet with the cross `crossorigin` attribute, and setting the `Access-Control-Allow-Origin` header on the server serving the stylesheet. Alternatively, host the stylesheet on your domain to avoid this issue entirely.",
                                                                G
                                                            ], "warning");
                                                        }
                                                        V.setAttribute("id", "react-to-print-".concat(O)), u && V.setAttribute("nonce", u), V.appendChild(y.createTextNode(z)), y.head.appendChild(V);
                                                    }
                                                } else if (G.getAttribute("href")) {
                                                    V = y.createElement(G.tagName), J = 0;
                                                    for(var K = G.attributes.length; J < K; ++J){
                                                        var Q = G.attributes[J];
                                                        Q && V.setAttribute(Q.nodeName, Q.nodeValue || "");
                                                    }
                                                    V.onload = g.bind(null, V, !0), V.onerror = g.bind(null, V, !1), u && V.setAttribute("nonce", u), y.head.appendChild(V);
                                                } else n.logMessages([
                                                    '"react-to-print" encountered a <link> tag with an empty "href" attribute. In addition to being invalid HTML, this can cause problems in many browsers, and so the <link> was not loaded. The <link> is:',
                                                    G
                                                ], "warning"), g(G, !0);
                                            }
                                        }
                                        0 !== n.linkTotal && a || n.triggerPrint(f);
                                    }, n.handleRemoveIframe(!0), document.body.appendChild(f);
                                } else n.logMessages([
                                    '"react-to-print" could not locate the DOM node corresponding with the `content` prop'
                                ]);
                            } else n.logMessages([
                                'There is nothing to print because the "content" prop returned "null". Please ensure "content" is renderable before allowing "react-to-print" to be called.'
                            ]);
                        } else n.logMessages([
                            "To print a functional component ensure it is wrapped with `React.forwardRef`, and ensure the forwarded ref is used. See the README for an example: https://github.com/gregnb/react-to-print#examples"
                        ]);
                    }, n.handleRemoveIframe = function(e) {
                        var t = n.props.removeAfterPrint;
                        if (e || t) {
                            var r = document.getElementById("printWindow");
                            r && document.body.removeChild(r);
                        }
                    }, n.logMessages = function(e, t) {
                        void 0 === t && (t = "error"), n.props.suppressErrors || ("error" === t ? console.error(e) : "warning" === t && console.warn(e));
                    }, n;
                }
                return (0, t10.__extends)(o3, e15), o3.prototype.render = function() {
                    var e = this.props, t = e.children, r = e.trigger;
                    if (r) return n6.cloneElement(r(), {
                        onClick: this.handleClick
                    });
                    if (!c2) return this.logMessages([
                        '"react-to-print" requires React ^16.3.0 to be able to use "PrintContext"'
                    ]), null;
                    var o = {
                        handlePrint: this.handleClick
                    };
                    return n6.createElement(c2.Provider, {
                        value: o
                    }, t);
                }, o3.defaultProps = u2, o3;
            }(n6.Component);
            e14.default = s2, e14.useReactToPrint = function(e) {
                if (!l3) return e.suppressErrors || console.error('"react-to-print" requires React ^16.8.0 to be able to use "useReactToPrint"'), function() {
                    throw new Error('"react-to-print" requires React ^16.8.0 to be able to use "useReactToPrint"');
                };
                var r = n6.useMemo(function() {
                    return new s2((0, t10.__assign)((0, t10.__assign)({}, u2), e));
                }, [
                    e
                ]);
                return n6.useCallback(function() {
                    return r.handleClick();
                }, [
                    r
                ]);
            };
        }(), i1;
    }();
});

},{"react":"21dqq","react-dom":"j6uA9"}],"c1a6J":[function(require,module,exports) {
var $parcel$ReactRefreshHelpers$6d16 = require("@parcel/transformer-react-refresh-wrap/lib/helpers/helpers.js");
var prevRefreshReg = window.$RefreshReg$;
var prevRefreshSig = window.$RefreshSig$;
$parcel$ReactRefreshHelpers$6d16.prelude(module);

try {
var parcelHelpers = require("@parcel/transformer-js/src/esmodule-helpers.js");
parcelHelpers.defineInteropFlag(exports);
var _jsxDevRuntime = require("react/jsx-dev-runtime");
var _react = require("react");
var _reactDefault = parcelHelpers.interopDefault(_react);
var _axios = require("axios");
var _axiosDefault = parcelHelpers.interopDefault(_axios);
var _sweetalert = require("sweetalert");
var _sweetalertDefault = parcelHelpers.interopDefault(_sweetalert);
class EditSubmition extends _react.Component {
    constructor(props){
        super(props);
        this.state = {
            groupId: "",
            type: "",
            description: "",
            files: ""
        };
    }
    handleInputChange = (e)=>{
        const { name , value  } = e.target;
        this.setState({
            ...this.state,
            [name]: value
        });
    };
    onSubmit = (e)=>{
        e.preventDefault();
        const id = this.props.match.params.id;
        const { groupId , type , description , files  } = this.state;
        const data = {
            groupId: groupId,
            type: type,
            description: description,
            files: files
        };
        console.log(data);
        //update submition
        _axiosDefault.default.put(`http://localhost:5000/submition/update/${id}`, data).then((res)=>{
            if (res.data.success) {
                _sweetalertDefault.default("Updated Successfully", "", "success").then((value)=>{
                    window.location = `/student/submitionsp/view/${groupId}`;
                });
                this.setState({
                    groupId: "",
                    type: "",
                    description: "",
                    files: ""
                });
            }
        });
    };
    componentDidMount() {
        // if(this.props.match && this.props.match.params.id){
        const id = this.props.match.params.id;
        //retrive submition to update
        _axiosDefault.default.get(`http://localhost:5000/submition/${id}`).then((res)=>{
            if (res.data.success) {
                this.setState({
                    groupId: res.data.submition.groupId,
                    type: res.data.submition.type,
                    description: res.data.submition.description,
                    files: res.data.submition.files
                });
                console.log(this.state);
            }
        });
    // }
    }
    render() {
        return /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
            className: "container",
            children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                className: "col-md-12 mt-4 ",
                children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                    className: "mx-5",
                    children: [
                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("h1", {
                            className: "h3 mb-3 font-weight-normal",
                            children: "Edit Submition"
                        }, void 0, false, {
                            fileName: "src/components/IT20128036/user/editSubmition.js",
                            lineNumber: 80,
                            columnNumber: 9
                        }, this),
                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("form", {
                            className: "needs-validation",
                            noValidate: true,
                            children: [
                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                    className: "row",
                                    children: [
                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                            className: "col-4",
                                            children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                                className: "form-group",
                                                style: {
                                                    marginBottom: "15px"
                                                },
                                                children: [
                                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("label", {
                                                        style: {
                                                            marginBottom: "5px"
                                                        },
                                                        children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("strong", {
                                                            children: "Group Id"
                                                        }, void 0, false, {
                                                            fileName: "src/components/IT20128036/user/editSubmition.js",
                                                            lineNumber: 88,
                                                            columnNumber: 52
                                                        }, this)
                                                    }, void 0, false, {
                                                        fileName: "src/components/IT20128036/user/editSubmition.js",
                                                        lineNumber: 88,
                                                        columnNumber: 13
                                                    }, this),
                                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("input", {
                                                        type: "text",
                                                        className: "form-control",
                                                        name: "groupId",
                                                        placeholder: "Edit Group Id",
                                                        value: this.state.groupId,
                                                        onChange: this.handleInputChange
                                                    }, void 0, false, {
                                                        fileName: "src/components/IT20128036/user/editSubmition.js",
                                                        lineNumber: 89,
                                                        columnNumber: 13
                                                    }, this)
                                                ]
                                            }, void 0, true, {
                                                fileName: "src/components/IT20128036/user/editSubmition.js",
                                                lineNumber: 87,
                                                columnNumber: 13
                                            }, this)
                                        }, void 0, false, {
                                            fileName: "src/components/IT20128036/user/editSubmition.js",
                                            lineNumber: 85,
                                            columnNumber: 13
                                        }, this),
                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                            className: "col-8",
                                            children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                                className: "form-group",
                                                style: {
                                                    marginBottom: "15px"
                                                },
                                                children: [
                                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("label", {
                                                        style: {
                                                            marginBottom: "5px"
                                                        },
                                                        children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("strong", {
                                                            children: "Type"
                                                        }, void 0, false, {
                                                            fileName: "src/components/IT20128036/user/editSubmition.js",
                                                            lineNumber: 106,
                                                            columnNumber: 52
                                                        }, this)
                                                    }, void 0, false, {
                                                        fileName: "src/components/IT20128036/user/editSubmition.js",
                                                        lineNumber: 106,
                                                        columnNumber: 13
                                                    }, this),
                                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("input", {
                                                        type: "text",
                                                        className: "form-control",
                                                        name: "type",
                                                        placeholder: "Edit Type",
                                                        value: this.state.type,
                                                        onChange: this.handleInputChange
                                                    }, void 0, false, {
                                                        fileName: "src/components/IT20128036/user/editSubmition.js",
                                                        lineNumber: 107,
                                                        columnNumber: 13
                                                    }, this)
                                                ]
                                            }, void 0, true, {
                                                fileName: "src/components/IT20128036/user/editSubmition.js",
                                                lineNumber: 105,
                                                columnNumber: 13
                                            }, this)
                                        }, void 0, false, {
                                            fileName: "src/components/IT20128036/user/editSubmition.js",
                                            lineNumber: 103,
                                            columnNumber: 13
                                        }, this)
                                    ]
                                }, void 0, true, {
                                    fileName: "src/components/IT20128036/user/editSubmition.js",
                                    lineNumber: 84,
                                    columnNumber: 11
                                }, this),
                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                    className: "form-group",
                                    style: {
                                        marginBottom: "15px"
                                    },
                                    children: [
                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("label", {
                                            style: {
                                                marginBottom: "5px"
                                            },
                                            children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("strong", {
                                                children: "Description"
                                            }, void 0, false, {
                                                fileName: "src/components/IT20128036/user/editSubmition.js",
                                                lineNumber: 126,
                                                columnNumber: 52
                                            }, this)
                                        }, void 0, false, {
                                            fileName: "src/components/IT20128036/user/editSubmition.js",
                                            lineNumber: 126,
                                            columnNumber: 13
                                        }, this),
                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("textarea", {
                                            className: "form-control",
                                            name: "description",
                                            placeholder: "Edit Description",
                                            value: this.state.description,
                                            onChange: this.handleInputChange
                                        }, void 0, false, {
                                            fileName: "src/components/IT20128036/user/editSubmition.js",
                                            lineNumber: 127,
                                            columnNumber: 13
                                        }, this)
                                    ]
                                }, void 0, true, {
                                    fileName: "src/components/IT20128036/user/editSubmition.js",
                                    lineNumber: 125,
                                    columnNumber: 11
                                }, this),
                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                    className: "form-group",
                                    style: {
                                        marginBottom: "15px"
                                    },
                                    children: [
                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("label", {
                                            style: {
                                                marginBottom: "5px"
                                            },
                                            children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("strong", {
                                                children: "Files"
                                            }, void 0, false, {
                                                fileName: "src/components/IT20128036/user/editSubmition.js",
                                                lineNumber: 139,
                                                columnNumber: 52
                                            }, this)
                                        }, void 0, false, {
                                            fileName: "src/components/IT20128036/user/editSubmition.js",
                                            lineNumber: 139,
                                            columnNumber: 13
                                        }, this),
                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("input", {
                                            type: "text",
                                            className: "form-control",
                                            name: "files",
                                            placeholder: "Edit Files",
                                            value: this.state.files,
                                            onChange: this.handleInputChange,
                                            disabled: true
                                        }, void 0, false, {
                                            fileName: "src/components/IT20128036/user/editSubmition.js",
                                            lineNumber: 140,
                                            columnNumber: 13
                                        }, this)
                                    ]
                                }, void 0, true, {
                                    fileName: "src/components/IT20128036/user/editSubmition.js",
                                    lineNumber: 138,
                                    columnNumber: 11
                                }, this),
                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("button", {
                                    className: "btn btn-outline-success",
                                    type: "submit",
                                    style: {
                                        margintop: "15px"
                                    },
                                    onClick: this.onSubmit,
                                    children: [
                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("i", {
                                            className: "far fa-check-square"
                                        }, void 0, false, {
                                            fileName: "src/components/IT20128036/user/editSubmition.js",
                                            lineNumber: 157,
                                            columnNumber: 13
                                        }, this),
                                        "\xa0 Update"
                                    ]
                                }, void 0, true, {
                                    fileName: "src/components/IT20128036/user/editSubmition.js",
                                    lineNumber: 151,
                                    columnNumber: 11
                                }, this)
                            ]
                        }, void 0, true, {
                            fileName: "src/components/IT20128036/user/editSubmition.js",
                            lineNumber: 81,
                            columnNumber: 9
                        }, this)
                    ]
                }, void 0, true, {
                    fileName: "src/components/IT20128036/user/editSubmition.js",
                    lineNumber: 79,
                    columnNumber: 9
                }, this)
            }, void 0, false, {
                fileName: "src/components/IT20128036/user/editSubmition.js",
                lineNumber: 78,
                columnNumber: 7
            }, this)
        }, void 0, false, {
            fileName: "src/components/IT20128036/user/editSubmition.js",
            lineNumber: 77,
            columnNumber: 7
        }, this);
    }
}
exports.default = EditSubmition;

  $parcel$ReactRefreshHelpers$6d16.postlude(module);
} finally {
  window.$RefreshReg$ = prevRefreshReg;
  window.$RefreshSig$ = prevRefreshSig;
}
},{"react/jsx-dev-runtime":"iTorj","react":"21dqq","axios":"jo6P5","sweetalert":"38a65","@parcel/transformer-js/src/esmodule-helpers.js":"gkKU3","@parcel/transformer-react-refresh-wrap/lib/helpers/helpers.js":"km3Ru"}],"8Xglg":[function(require,module,exports) {
var $parcel$ReactRefreshHelpers$638d = require("@parcel/transformer-react-refresh-wrap/lib/helpers/helpers.js");
var prevRefreshReg = window.$RefreshReg$;
var prevRefreshSig = window.$RefreshSig$;
$parcel$ReactRefreshHelpers$638d.prelude(module);

try {
var parcelHelpers = require("@parcel/transformer-js/src/esmodule-helpers.js");
parcelHelpers.defineInteropFlag(exports);
parcelHelpers.export(exports, "exportPDF", ()=>exportPDF
);
var _jsxDevRuntime = require("react/jsx-dev-runtime");
var _react = require("react");
var _reactDefault = parcelHelpers.interopDefault(_react);
var _reactToPrint = require("react-to-print");
var _reactToPrintDefault = parcelHelpers.interopDefault(_reactToPrint);
var _componentToPrint = require("./ComponentToPrint");
class exportPDF extends _reactDefault.default.PureComponent {
    render() {
        return /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
            className: "container",
            children: [
                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                    className: "mt-5",
                    children: [
                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV(_reactToPrintDefault.default, {
                            content: ()=>this.componentRef
                            ,
                            children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                className: "row",
                                children: [
                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                        className: "col-md-6",
                                        children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("h4", {
                                            children: "Preview Of Mark Sheet"
                                        }, void 0, false, {
                                            fileName: "src/components/IT20128036/user/exportPDF.js",
                                            lineNumber: 17,
                                            columnNumber: 15
                                        }, this)
                                    }, void 0, false, {
                                        fileName: "src/components/IT20128036/user/exportPDF.js",
                                        lineNumber: 16,
                                        columnNumber: 15
                                    }, this),
                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                        className: "col-md-6",
                                        children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV(_reactToPrint.PrintContextConsumer, {
                                            children: ({ handlePrint  })=>/*#__PURE__*/ _jsxDevRuntime.jsxDEV("button", {
                                                    className: "btn btn-outline-success",
                                                    onClick: handlePrint,
                                                    children: "Export Mark Sheet"
                                                }, void 0, false, {
                                                    fileName: "src/components/IT20128036/user/exportPDF.js",
                                                    lineNumber: 23,
                                                    columnNumber: 15
                                                }, this)
                                        }, void 0, false, {
                                            fileName: "src/components/IT20128036/user/exportPDF.js",
                                            lineNumber: 21,
                                            columnNumber: 15
                                        }, this)
                                    }, void 0, false, {
                                        fileName: "src/components/IT20128036/user/exportPDF.js",
                                        lineNumber: 20,
                                        columnNumber: 15
                                    }, this)
                                ]
                            }, void 0, true, {
                                fileName: "src/components/IT20128036/user/exportPDF.js",
                                lineNumber: 15,
                                columnNumber: 9
                            }, this)
                        }, void 0, false, {
                            fileName: "src/components/IT20128036/user/exportPDF.js",
                            lineNumber: 12,
                            columnNumber: 9
                        }, this),
                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV(_componentToPrint.ComponentToPrint, {
                            ref: (el)=>this.componentRef = el
                        }, void 0, false, {
                            fileName: "src/components/IT20128036/user/exportPDF.js",
                            lineNumber: 35,
                            columnNumber: 9
                        }, this)
                    ]
                }, void 0, true, {
                    fileName: "src/components/IT20128036/user/exportPDF.js",
                    lineNumber: 10,
                    columnNumber: 11
                }, this),
                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("a", {
                    href: "/student/marks/view",
                    className: "btn btn-outline-dark",
                    children: "Back"
                }, void 0, false, {
                    fileName: "src/components/IT20128036/user/exportPDF.js",
                    lineNumber: 38,
                    columnNumber: 9
                }, this)
            ]
        }, void 0, true, {
            fileName: "src/components/IT20128036/user/exportPDF.js",
            lineNumber: 9,
            columnNumber: 7
        }, this);
    }
}

  $parcel$ReactRefreshHelpers$638d.postlude(module);
} finally {
  window.$RefreshReg$ = prevRefreshReg;
  window.$RefreshSig$ = prevRefreshSig;
}
},{"react/jsx-dev-runtime":"iTorj","react":"21dqq","react-to-print":"amkqM","./ComponentToPrint":"5vNPm","@parcel/transformer-js/src/esmodule-helpers.js":"gkKU3","@parcel/transformer-react-refresh-wrap/lib/helpers/helpers.js":"km3Ru"}],"5vNPm":[function(require,module,exports) {
var $parcel$ReactRefreshHelpers$8e0c = require("@parcel/transformer-react-refresh-wrap/lib/helpers/helpers.js");
var prevRefreshReg = window.$RefreshReg$;
var prevRefreshSig = window.$RefreshSig$;
$parcel$ReactRefreshHelpers$8e0c.prelude(module);

try {
var parcelHelpers = require("@parcel/transformer-js/src/esmodule-helpers.js");
parcelHelpers.defineInteropFlag(exports);
parcelHelpers.export(exports, "ComponentToPrint", ()=>ComponentToPrint
);
var _jsxDevRuntime = require("react/jsx-dev-runtime");
var _react = require("react");
var _reactDefault = parcelHelpers.interopDefault(_react);
var _axios = require("axios");
var _axiosDefault = parcelHelpers.interopDefault(_axios);
class ComponentToPrint extends _reactDefault.default.PureComponent {
    constructor(props){
        super(props);
        this.state = {
            marks: []
        };
    }
    componentDidMount() {
        this.retriveMarks();
    }
    //retrive marks
    retriveMarks() {
        _axiosDefault.default.get("http://localhost:5000/marks").then((res)=>{
            if (res.data.success) {
                this.setState({
                    marks: res.data.existingMarks
                });
                console.log(this.state.marks);
            }
        });
    }
    render() {
        return /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
            className: "container",
            children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("table", {
                className: "table table-light table-striped table-hover mt-4 mb-4",
                children: [
                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("thead", {
                        children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("tr", {
                            children: [
                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("th", {
                                    scope: "col",
                                    children: "#"
                                }, void 0, false, {
                                    fileName: "src/components/IT20128036/user/ComponentToPrint.js",
                                    lineNumber: 50,
                                    columnNumber: 12
                                }, this),
                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("th", {
                                    scope: "col",
                                    children: "Group ID"
                                }, void 0, false, {
                                    fileName: "src/components/IT20128036/user/ComponentToPrint.js",
                                    lineNumber: 51,
                                    columnNumber: 12
                                }, this),
                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("th", {
                                    scope: "col",
                                    children: "Submition Type"
                                }, void 0, false, {
                                    fileName: "src/components/IT20128036/user/ComponentToPrint.js",
                                    lineNumber: 52,
                                    columnNumber: 12
                                }, this),
                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("th", {
                                    scope: "col",
                                    children: "Marks (?/100)"
                                }, void 0, false, {
                                    fileName: "src/components/IT20128036/user/ComponentToPrint.js",
                                    lineNumber: 53,
                                    columnNumber: 12
                                }, this),
                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("th", {
                                    scope: "col",
                                    children: "Grading Status"
                                }, void 0, false, {
                                    fileName: "src/components/IT20128036/user/ComponentToPrint.js",
                                    lineNumber: 54,
                                    columnNumber: 12
                                }, this)
                            ]
                        }, void 0, true, {
                            fileName: "src/components/IT20128036/user/ComponentToPrint.js",
                            lineNumber: 49,
                            columnNumber: 10
                        }, this)
                    }, void 0, false, {
                        fileName: "src/components/IT20128036/user/ComponentToPrint.js",
                        lineNumber: 48,
                        columnNumber: 8
                    }, this),
                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("tbody", {
                        children: this.state.marks.map((marks, index)=>/*#__PURE__*/ _jsxDevRuntime.jsxDEV("tr", {
                                children: [
                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("th", {
                                        scope: "row",
                                        children: index + 1
                                    }, void 0, false, {
                                        fileName: "src/components/IT20128036/user/ComponentToPrint.js",
                                        lineNumber: 61,
                                        columnNumber: 14
                                    }, this),
                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("td", {
                                        children: marks.groupId
                                    }, void 0, false, {
                                        fileName: "src/components/IT20128036/user/ComponentToPrint.js",
                                        lineNumber: 64,
                                        columnNumber: 14
                                    }, this),
                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("td", {
                                        children: marks.type
                                    }, void 0, false, {
                                        fileName: "src/components/IT20128036/user/ComponentToPrint.js",
                                        lineNumber: 70,
                                        columnNumber: 14
                                    }, this),
                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("td", {
                                        children: marks.marks
                                    }, void 0, false, {
                                        fileName: "src/components/IT20128036/user/ComponentToPrint.js",
                                        lineNumber: 72,
                                        columnNumber: 14
                                    }, this),
                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("td", {
                                        children: marks.gradingStatus
                                    }, void 0, false, {
                                        fileName: "src/components/IT20128036/user/ComponentToPrint.js",
                                        lineNumber: 73,
                                        columnNumber: 14
                                    }, this)
                                ]
                            }, index, true, {
                                fileName: "src/components/IT20128036/user/ComponentToPrint.js",
                                lineNumber: 60,
                                columnNumber: 12
                            }, this)
                        )
                    }, void 0, false, {
                        fileName: "src/components/IT20128036/user/ComponentToPrint.js",
                        lineNumber: 58,
                        columnNumber: 8
                    }, this)
                ]
            }, void 0, true, {
                fileName: "src/components/IT20128036/user/ComponentToPrint.js",
                lineNumber: 47,
                columnNumber: 6
            }, this)
        }, void 0, false, {
            fileName: "src/components/IT20128036/user/ComponentToPrint.js",
            lineNumber: 45,
            columnNumber: 9
        }, this);
    }
}

  $parcel$ReactRefreshHelpers$8e0c.postlude(module);
} finally {
  window.$RefreshReg$ = prevRefreshReg;
  window.$RefreshSig$ = prevRefreshSig;
}
},{"react/jsx-dev-runtime":"iTorj","react":"21dqq","axios":"jo6P5","@parcel/transformer-js/src/esmodule-helpers.js":"gkKU3","@parcel/transformer-react-refresh-wrap/lib/helpers/helpers.js":"km3Ru"}],"iHo1z":[function(require,module,exports) {
var $parcel$ReactRefreshHelpers$281e = require("@parcel/transformer-react-refresh-wrap/lib/helpers/helpers.js");
var prevRefreshReg = window.$RefreshReg$;
var prevRefreshSig = window.$RefreshSig$;
$parcel$ReactRefreshHelpers$281e.prelude(module);

try {
var parcelHelpers = require("@parcel/transformer-js/src/esmodule-helpers.js");
parcelHelpers.defineInteropFlag(exports);
var _jsxDevRuntime = require("react/jsx-dev-runtime");
var _react = require("react");
var _reactDefault = parcelHelpers.interopDefault(_react);
var _axios = require("axios");
var _axiosDefault = parcelHelpers.interopDefault(_axios);
var _studentMarksSp = require("./StudentMarksSp");
var _jwtDecode = require("jwt-decode");
var _jwtDecodeDefault = parcelHelpers.interopDefault(_jwtDecode);
class StudentViewSpData extends _react.Component {
    constructor(props){
        super(props);
        this.state = {
            groupId: "",
            evaluations: []
        };
    }
    componentDidMount() {
        if (localStorage.userToken) {
            const usertoken = localStorage.userToken;
            const decoded = _jwtDecodeDefault.default(usertoken);
            this.setState({
                groupId: decoded.groupId
            });
        }
        setTimeout(()=>{
            this.retriveEvaluations();
        }, 1000);
    }
    //retrive evaluations
    retriveEvaluations() {
        const gid = this.state.groupId;
        console.log(gid);
        _axiosDefault.default.get(`http://localhost:5000/evaluation/group/${gid}`).then((res)=>{
            if (res.data.success) {
                this.setState({
                    evaluations: res.data.existingEvaluations
                });
                console.log(this.state.evaluations);
            }
        });
    }
    //filter evaluation
    filterData(evaluations, searchKey) {
        const result = evaluations.filter((evaluation)=>evaluation.groupId.toLowerCase().includes(searchKey) || evaluation.evaluationTopic.toLowerCase().includes(searchKey) || evaluation.date.toLowerCase().includes(searchKey) || evaluation.groupId.includes(searchKey) || evaluation.evaluationTopic.includes(searchKey) || evaluation.date.includes(searchKey)
        );
        this.setState({
            evaluations: result
        });
    }
    //search evaluation
    handleSearchArea = (e)=>{
        const searchKey = e.currentTarget.value;
        id = this.state.groupId;
        _axiosDefault.default.get(`http://localhost:5000/evaluation/group/${id}`).then((res)=>{
            if (res.data.success) this.filterData(res.data.existingEvaluations, searchKey);
        });
    };
    render() {
        return /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
            className: "container",
            children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                className: "row",
                children: [
                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("center", {
                        children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("h5", {
                            className: "mt-4 ",
                            children: [
                                "Group ID : ",
                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("span", {
                                    className: "text-primary",
                                    style: {
                                        fontSize: "19px"
                                    },
                                    children: this.state.groupId
                                }, void 0, false, {
                                    fileName: "src/components/IT20128036/user/StudentViewSpData.js",
                                    lineNumber: 88,
                                    columnNumber: 58
                                }, this)
                            ]
                        }, void 0, true, {
                            fileName: "src/components/IT20128036/user/StudentViewSpData.js",
                            lineNumber: 88,
                            columnNumber: 25
                        }, this)
                    }, void 0, false, {
                        fileName: "src/components/IT20128036/user/StudentViewSpData.js",
                        lineNumber: 88,
                        columnNumber: 17
                    }, this),
                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                        className: "col-sm-4",
                        children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                            className: "mt-2 mb-2 ms-2",
                            children: [
                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                    className: "row",
                                    children: [
                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                            className: "col-lg-10 mt-2 mb-2",
                                            children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("h4", {
                                                children: "Your Evaluation Session"
                                            }, void 0, false, {
                                                fileName: "src/components/IT20128036/user/StudentViewSpData.js",
                                                lineNumber: 95,
                                                columnNumber: 13
                                            }, this)
                                        }, void 0, false, {
                                            fileName: "src/components/IT20128036/user/StudentViewSpData.js",
                                            lineNumber: 94,
                                            columnNumber: 11
                                        }, this),
                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                            className: "col-lg-2 mt-2 mb-2"
                                        }, void 0, false, {
                                            fileName: "src/components/IT20128036/user/StudentViewSpData.js",
                                            lineNumber: 97,
                                            columnNumber: 11
                                        }, this)
                                    ]
                                }, void 0, true, {
                                    fileName: "src/components/IT20128036/user/StudentViewSpData.js",
                                    lineNumber: 93,
                                    columnNumber: 9
                                }, this),
                                this.state.evaluations.map((evaluations, index)=>/*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                        class: "row",
                                        children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                            class: "col-sm-12",
                                            children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                                class: "card border-primary mt-2 mb-2",
                                                children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                                    class: "card-body",
                                                    children: [
                                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                                            class: "card-header",
                                                            children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("h5", {
                                                                class: "card-title",
                                                                children: [
                                                                    " Session ",
                                                                    index + 1,
                                                                    " "
                                                                ]
                                                            }, void 0, true, {
                                                                fileName: "src/components/IT20128036/user/StudentViewSpData.js",
                                                                lineNumber: 115,
                                                                columnNumber: 21
                                                            }, this)
                                                        }, void 0, false, {
                                                            fileName: "src/components/IT20128036/user/StudentViewSpData.js",
                                                            lineNumber: 114,
                                                            columnNumber: 19
                                                        }, this),
                                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("p", {
                                                            class: "card-text",
                                                            children: [
                                                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("strong", {
                                                                    children: [
                                                                        evaluations.groupId,
                                                                        " "
                                                                    ]
                                                                }, void 0, true, {
                                                                    fileName: "src/components/IT20128036/user/StudentViewSpData.js",
                                                                    lineNumber: 119,
                                                                    columnNumber: 21
                                                                }, this),
                                                                "Students, your",
                                                                " ",
                                                                evaluations.evaluationTopic,
                                                                " session will be held on",
                                                                " ",
                                                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("strong", {
                                                                    children: evaluations.date
                                                                }, void 0, false, {
                                                                    fileName: "src/components/IT20128036/user/StudentViewSpData.js",
                                                                    lineNumber: 121,
                                                                    columnNumber: 21
                                                                }, this),
                                                                " from",
                                                                " ",
                                                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("strong", {
                                                                    children: evaluations.from
                                                                }, void 0, false, {
                                                                    fileName: "src/components/IT20128036/user/StudentViewSpData.js",
                                                                    lineNumber: 122,
                                                                    columnNumber: 21
                                                                }, this),
                                                                " to",
                                                                " ",
                                                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("strong", {
                                                                    children: evaluations.to
                                                                }, void 0, false, {
                                                                    fileName: "src/components/IT20128036/user/StudentViewSpData.js",
                                                                    lineNumber: 123,
                                                                    columnNumber: 21
                                                                }, this),
                                                                " with the",
                                                                " ",
                                                                evaluations.panel,
                                                                ". Your participation is mandatory. You can connect via the link below.",
                                                                " "
                                                            ]
                                                        }, void 0, true, {
                                                            fileName: "src/components/IT20128036/user/StudentViewSpData.js",
                                                            lineNumber: 118,
                                                            columnNumber: 19
                                                        }, this),
                                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                                            className: "row",
                                                            children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                                                className: "col-lg-4",
                                                                children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("a", {
                                                                    href: evaluations.link,
                                                                    target: "_blank",
                                                                    class: "btn btn-outline-primary",
                                                                    children: [
                                                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("i", {
                                                                            class: "fa fa-link",
                                                                            "aria-hidden": "true"
                                                                        }, void 0, false, {
                                                                            fileName: "src/components/IT20128036/user/StudentViewSpData.js",
                                                                            lineNumber: 136,
                                                                            columnNumber: 25
                                                                        }, this),
                                                                        "Join"
                                                                    ]
                                                                }, void 0, true, {
                                                                    fileName: "src/components/IT20128036/user/StudentViewSpData.js",
                                                                    lineNumber: 131,
                                                                    columnNumber: 23
                                                                }, this)
                                                            }, void 0, false, {
                                                                fileName: "src/components/IT20128036/user/StudentViewSpData.js",
                                                                lineNumber: 130,
                                                                columnNumber: 21
                                                            }, this)
                                                        }, void 0, false, {
                                                            fileName: "src/components/IT20128036/user/StudentViewSpData.js",
                                                            lineNumber: 129,
                                                            columnNumber: 19
                                                        }, this)
                                                    ]
                                                }, void 0, true, {
                                                    fileName: "src/components/IT20128036/user/StudentViewSpData.js",
                                                    lineNumber: 113,
                                                    columnNumber: 17
                                                }, this)
                                            }, void 0, false, {
                                                fileName: "src/components/IT20128036/user/StudentViewSpData.js",
                                                lineNumber: 112,
                                                columnNumber: 15
                                            }, this)
                                        }, void 0, false, {
                                            fileName: "src/components/IT20128036/user/StudentViewSpData.js",
                                            lineNumber: 111,
                                            columnNumber: 13
                                        }, this)
                                    }, void 0, false, {
                                        fileName: "src/components/IT20128036/user/StudentViewSpData.js",
                                        lineNumber: 110,
                                        columnNumber: 11
                                    }, this)
                                )
                            ]
                        }, void 0, true, {
                            fileName: "src/components/IT20128036/user/StudentViewSpData.js",
                            lineNumber: 92,
                            columnNumber: 7
                        }, this)
                    }, void 0, false, {
                        fileName: "src/components/IT20128036/user/StudentViewSpData.js",
                        lineNumber: 90,
                        columnNumber: 17
                    }, this),
                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                        className: "col-sm-8",
                        children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV(_studentMarksSp.StudentMarksSp, {}, void 0, false, {
                            fileName: "src/components/IT20128036/user/StudentViewSpData.js",
                            lineNumber: 151,
                            columnNumber: 11
                        }, this)
                    }, void 0, false, {
                        fileName: "src/components/IT20128036/user/StudentViewSpData.js",
                        lineNumber: 150,
                        columnNumber: 7
                    }, this)
                ]
            }, void 0, true, {
                fileName: "src/components/IT20128036/user/StudentViewSpData.js",
                lineNumber: 87,
                columnNumber: 13
            }, this)
        }, void 0, false, {
            fileName: "src/components/IT20128036/user/StudentViewSpData.js",
            lineNumber: 86,
            columnNumber: 9
        }, this);
    }
}
exports.default = StudentViewSpData;

  $parcel$ReactRefreshHelpers$281e.postlude(module);
} finally {
  window.$RefreshReg$ = prevRefreshReg;
  window.$RefreshSig$ = prevRefreshSig;
}
},{"react/jsx-dev-runtime":"iTorj","react":"21dqq","axios":"jo6P5","./StudentMarksSp":"4LkLo","jwt-decode":"ljM1w","@parcel/transformer-js/src/esmodule-helpers.js":"gkKU3","@parcel/transformer-react-refresh-wrap/lib/helpers/helpers.js":"km3Ru"}],"4LkLo":[function(require,module,exports) {
var $parcel$ReactRefreshHelpers$bfdc = require("@parcel/transformer-react-refresh-wrap/lib/helpers/helpers.js");
var prevRefreshReg = window.$RefreshReg$;
var prevRefreshSig = window.$RefreshSig$;
$parcel$ReactRefreshHelpers$bfdc.prelude(module);

try {
var parcelHelpers = require("@parcel/transformer-js/src/esmodule-helpers.js");
parcelHelpers.defineInteropFlag(exports);
parcelHelpers.export(exports, "StudentMarksSp", ()=>StudentMarksSp
);
var _jsxDevRuntime = require("react/jsx-dev-runtime");
var _react = require("react");
var _reactDefault = parcelHelpers.interopDefault(_react);
var _axios = require("axios");
var _axiosDefault = parcelHelpers.interopDefault(_axios);
var _jwtDecode = require("jwt-decode");
var _jwtDecodeDefault = parcelHelpers.interopDefault(_jwtDecode);
class StudentMarksSp extends _react.Component {
    constructor(props){
        super(props);
        this.state = {
            groupId: "",
            marks: []
        };
    }
    componentDidMount() {
        if (localStorage.userToken) {
            const usertoken = localStorage.userToken;
            const decoded = _jwtDecodeDefault.default(usertoken);
            this.setState({
                groupId: decoded.groupId
            });
        }
        setTimeout(()=>{
            this.retriveMarks();
        }, 1000);
    }
    //retrive marks
    retriveMarks() {
        const id = this.state.groupId;
        _axiosDefault.default.get(`http://localhost:5000/marks/group/${id}`).then((res)=>{
            if (res.data.success) {
                this.setState({
                    marks: res.data.existingMarks
                });
                console.log(this.state.marks);
            }
        });
    }
    render() {
        return /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
            className: "me-2",
            children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("table", {
                className: "table table-white mt-4 mb-4",
                children: [
                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("thead", {
                        children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("tr", {
                            children: [
                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("th", {
                                    scope: "col",
                                    children: "Submition Type"
                                }, void 0, false, {
                                    fileName: "src/components/IT20128036/user/StudentMarksSp.js",
                                    lineNumber: 80,
                                    columnNumber: 12
                                }, this),
                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("th", {
                                    scope: "col",
                                    children: "Marks (?/100)"
                                }, void 0, false, {
                                    fileName: "src/components/IT20128036/user/StudentMarksSp.js",
                                    lineNumber: 81,
                                    columnNumber: 12
                                }, this),
                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("th", {
                                    scope: "col",
                                    children: "Grading Status"
                                }, void 0, false, {
                                    fileName: "src/components/IT20128036/user/StudentMarksSp.js",
                                    lineNumber: 82,
                                    columnNumber: 12
                                }, this)
                            ]
                        }, void 0, true, {
                            fileName: "src/components/IT20128036/user/StudentMarksSp.js",
                            lineNumber: 77,
                            columnNumber: 10
                        }, this)
                    }, void 0, false, {
                        fileName: "src/components/IT20128036/user/StudentMarksSp.js",
                        lineNumber: 76,
                        columnNumber: 8
                    }, this),
                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("tbody", {
                        children: this.state.marks.map((marks, index)=>/*#__PURE__*/ _jsxDevRuntime.jsxDEV("tr", {
                                children: [
                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("td", {
                                        children: marks.type
                                    }, void 0, false, {
                                        fileName: "src/components/IT20128036/user/StudentMarksSp.js",
                                        lineNumber: 94,
                                        columnNumber: 14
                                    }, this),
                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("td", {
                                        children: marks.marks
                                    }, void 0, false, {
                                        fileName: "src/components/IT20128036/user/StudentMarksSp.js",
                                        lineNumber: 96,
                                        columnNumber: 14
                                    }, this),
                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("td", {
                                        children: marks.gradingStatus
                                    }, void 0, false, {
                                        fileName: "src/components/IT20128036/user/StudentMarksSp.js",
                                        lineNumber: 97,
                                        columnNumber: 14
                                    }, this)
                                ]
                            }, void 0, true, {
                                fileName: "src/components/IT20128036/user/StudentMarksSp.js",
                                lineNumber: 88,
                                columnNumber: 12
                            }, this)
                        )
                    }, void 0, false, {
                        fileName: "src/components/IT20128036/user/StudentMarksSp.js",
                        lineNumber: 86,
                        columnNumber: 8
                    }, this)
                ]
            }, void 0, true, {
                fileName: "src/components/IT20128036/user/StudentMarksSp.js",
                lineNumber: 75,
                columnNumber: 6
            }, this)
        }, void 0, false, {
            fileName: "src/components/IT20128036/user/StudentMarksSp.js",
            lineNumber: 73,
            columnNumber: 9
        }, this);
    }
}

  $parcel$ReactRefreshHelpers$bfdc.postlude(module);
} finally {
  window.$RefreshReg$ = prevRefreshReg;
  window.$RefreshSig$ = prevRefreshSig;
}
},{"react/jsx-dev-runtime":"iTorj","react":"21dqq","axios":"jo6P5","jwt-decode":"ljM1w","@parcel/transformer-js/src/esmodule-helpers.js":"gkKU3","@parcel/transformer-react-refresh-wrap/lib/helpers/helpers.js":"km3Ru"}],"aOAYN":[function(require,module,exports) {
var $parcel$ReactRefreshHelpers$1505 = require("@parcel/transformer-react-refresh-wrap/lib/helpers/helpers.js");
var prevRefreshReg = window.$RefreshReg$;
var prevRefreshSig = window.$RefreshSig$;
$parcel$ReactRefreshHelpers$1505.prelude(module);

try {
var parcelHelpers = require("@parcel/transformer-js/src/esmodule-helpers.js");
parcelHelpers.defineInteropFlag(exports);
var _jsxDevRuntime = require("react/jsx-dev-runtime");
var _react = require("react");
var _reactDefault = parcelHelpers.interopDefault(_react);
var _axios = require("axios");
var _axiosDefault = parcelHelpers.interopDefault(_axios);
class CreateStudentGroup extends _react.Component {
    constructor(props){
        super(props);
        this.state = {
            userType: "",
            groupid: "",
            groupname: "",
            studentid1: "",
            studentname1: "",
            studentid2: "",
            studentname2: "",
            studentid3: "",
            studentname3: "",
            studentid4: "",
            studentname4: "",
            topic: "Not Updated",
            supervisorname: "Not Updated",
            cosupervisorname: "Not Updated",
            panelmembername: "Not Updated",
            status: "Not Reviewed"
        };
    }
    handleInputChange = (e)=>{
        const { name , value  } = e.target;
        this.setState({
            ...this.state,
            [name]: value
        });
    };
    onSubmit = (e)=>{
        e.preventDefault();
        const { groupid , groupname , studentid1 , studentname1 , studentid2 , studentname2 , studentid3 , studentname3 , studentid4 , studentname4 , supervisorname , cosupervisorname , panelmembername , status ,  } = this.state;
        const data = {
            groupid: groupid,
            groupname: groupname,
            studentid1: studentid1,
            studentname1: studentname1,
            studentid2: studentid2,
            studentname2: studentname2,
            studentid3: studentid3,
            studentname3: studentname3,
            studentid4: studentid4,
            studentname4: studentname4,
            supervisorname: supervisorname,
            cosupervisorname: cosupervisorname,
            panelmembername: panelmembername,
            status: status
        };
        console.log(data);
        _axiosDefault.default.post("http://localhost:5000/sgroup/save", data).then((res)=>{
            if (res.data.success) {
                swal("Group Registerd Successfully", "", "success").then((value)=>{
                    window.location = "/home";
                });
                this.setState({
                    groupid: "",
                    groupname: "",
                    studentid1: "",
                    studentname1: "",
                    studentid2: "",
                    studentname2: "",
                    studentid3: "",
                    studentname3: "",
                    studentid4: "",
                    studentname4: "",
                    supervisorname: "Not Updated",
                    cosupervisorname: "Not Updated",
                    panelmembername: "Not Updated",
                    status: "Not Reviewed"
                });
            }
        });
    };
    render() {
        return /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
            className: "container px-5 my-5",
            children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                className: "container border border-dark bg-light mt-5 ",
                children: [
                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                        className: "form-group row",
                        children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                            className: "col-lg-12 margin-tb",
                            children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                className: "float-left",
                                children: [
                                    "\xa0",
                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("h2", {
                                        children: "Create Student Group"
                                    }, void 0, false, {
                                        fileName: "src/components/IT20131456/user/CreateStudentGroup.js",
                                        lineNumber: 112,
                                        columnNumber: 17
                                    }, this),
                                    "\xa0"
                                ]
                            }, void 0, true, {
                                fileName: "src/components/IT20131456/user/CreateStudentGroup.js",
                                lineNumber: 110,
                                columnNumber: 15
                            }, this)
                        }, void 0, false, {
                            fileName: "src/components/IT20131456/user/CreateStudentGroup.js",
                            lineNumber: 109,
                            columnNumber: 13
                        }, this)
                    }, void 0, false, {
                        fileName: "src/components/IT20131456/user/CreateStudentGroup.js",
                        lineNumber: 108,
                        columnNumber: 11
                    }, this),
                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("form", {
                        onSubmit: this.onSubmit,
                        children: [
                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                className: "row ",
                                children: [
                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                        className: "col-md-6",
                                        children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                            className: "form-group",
                                            children: [
                                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("strong", {
                                                    children: "Group ID :"
                                                }, void 0, false, {
                                                    fileName: "src/components/IT20131456/user/CreateStudentGroup.js",
                                                    lineNumber: 122,
                                                    columnNumber: 19
                                                }, this),
                                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("input", {
                                                    type: "text",
                                                    className: "form-control",
                                                    placeholder: "Enter Group id -Gxxx",
                                                    name: "groupid",
                                                    pattern: "G[0-9]{3}",
                                                    title: "Group ID is Invalid",
                                                    value: this.state.groupid,
                                                    onChange: this.handleInputChange,
                                                    required: true
                                                }, void 0, false, {
                                                    fileName: "src/components/IT20131456/user/CreateStudentGroup.js",
                                                    lineNumber: 123,
                                                    columnNumber: 19
                                                }, this)
                                            ]
                                        }, void 0, true, {
                                            fileName: "src/components/IT20131456/user/CreateStudentGroup.js",
                                            lineNumber: 121,
                                            columnNumber: 17
                                        }, this)
                                    }, void 0, false, {
                                        fileName: "src/components/IT20131456/user/CreateStudentGroup.js",
                                        lineNumber: 120,
                                        columnNumber: 15
                                    }, this),
                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                        className: "col-md-6",
                                        children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                            className: "form-group",
                                            children: [
                                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("strong", {
                                                    children: "Group Name :"
                                                }, void 0, false, {
                                                    fileName: "src/components/IT20131456/user/CreateStudentGroup.js",
                                                    lineNumber: 138,
                                                    columnNumber: 19
                                                }, this),
                                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("input", {
                                                    type: "text",
                                                    className: "form-control",
                                                    placeholder: "Enter Group name",
                                                    name: "groupname",
                                                    value: this.state.groupname,
                                                    onChange: this.handleInputChange,
                                                    required: true
                                                }, void 0, false, {
                                                    fileName: "src/components/IT20131456/user/CreateStudentGroup.js",
                                                    lineNumber: 139,
                                                    columnNumber: 19
                                                }, this)
                                            ]
                                        }, void 0, true, {
                                            fileName: "src/components/IT20131456/user/CreateStudentGroup.js",
                                            lineNumber: 137,
                                            columnNumber: 17
                                        }, this)
                                    }, void 0, false, {
                                        fileName: "src/components/IT20131456/user/CreateStudentGroup.js",
                                        lineNumber: 136,
                                        columnNumber: 15
                                    }, this)
                                ]
                            }, void 0, true, {
                                fileName: "src/components/IT20131456/user/CreateStudentGroup.js",
                                lineNumber: 119,
                                columnNumber: 13
                            }, this),
                            "\xa0",
                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                className: "row",
                                children: [
                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                        className: "col-md-6",
                                        children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                            className: "form-group",
                                            children: [
                                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("strong", {
                                                    children: "Student_1 ID (group leader) :"
                                                }, void 0, false, {
                                                    fileName: "src/components/IT20131456/user/CreateStudentGroup.js",
                                                    lineNumber: 155,
                                                    columnNumber: 19
                                                }, this),
                                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("input", {
                                                    type: "text",
                                                    className: "form-control",
                                                    placeholder: "Enter id -ITxxxx",
                                                    name: "studentid1",
                                                    pattern: "IT[0-9]{4}",
                                                    title: "Student ID is Invalid",
                                                    value: this.state.studentid1,
                                                    onChange: this.handleInputChange,
                                                    required: true
                                                }, void 0, false, {
                                                    fileName: "src/components/IT20131456/user/CreateStudentGroup.js",
                                                    lineNumber: 156,
                                                    columnNumber: 19
                                                }, this)
                                            ]
                                        }, void 0, true, {
                                            fileName: "src/components/IT20131456/user/CreateStudentGroup.js",
                                            lineNumber: 154,
                                            columnNumber: 17
                                        }, this)
                                    }, void 0, false, {
                                        fileName: "src/components/IT20131456/user/CreateStudentGroup.js",
                                        lineNumber: 153,
                                        columnNumber: 15
                                    }, this),
                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                        className: "col-md-6",
                                        children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                            className: "form-group",
                                            children: [
                                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("strong", {
                                                    children: "Student_1 Name (group leader) :"
                                                }, void 0, false, {
                                                    fileName: "src/components/IT20131456/user/CreateStudentGroup.js",
                                                    lineNumber: 171,
                                                    columnNumber: 19
                                                }, this),
                                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("input", {
                                                    type: "text",
                                                    className: "form-control",
                                                    placeholder: "Enter name",
                                                    name: "studentname1",
                                                    value: this.state.studentname1,
                                                    onChange: this.handleInputChange,
                                                    required: true
                                                }, void 0, false, {
                                                    fileName: "src/components/IT20131456/user/CreateStudentGroup.js",
                                                    lineNumber: 172,
                                                    columnNumber: 19
                                                }, this)
                                            ]
                                        }, void 0, true, {
                                            fileName: "src/components/IT20131456/user/CreateStudentGroup.js",
                                            lineNumber: 170,
                                            columnNumber: 17
                                        }, this)
                                    }, void 0, false, {
                                        fileName: "src/components/IT20131456/user/CreateStudentGroup.js",
                                        lineNumber: 169,
                                        columnNumber: 15
                                    }, this)
                                ]
                            }, void 0, true, {
                                fileName: "src/components/IT20131456/user/CreateStudentGroup.js",
                                lineNumber: 152,
                                columnNumber: 13
                            }, this),
                            "\xa0",
                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                className: "row",
                                children: [
                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                        className: "col-md-6",
                                        children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                            className: "form-group",
                                            children: [
                                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("strong", {
                                                    children: "Student_2 ID :"
                                                }, void 0, false, {
                                                    fileName: "src/components/IT20131456/user/CreateStudentGroup.js",
                                                    lineNumber: 188,
                                                    columnNumber: 19
                                                }, this),
                                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("input", {
                                                    type: "text",
                                                    className: "form-control",
                                                    placeholder: "Enter id",
                                                    name: "studentid2",
                                                    pattern: "IT[0-9]{4}",
                                                    title: "Student ID is Invalid",
                                                    value: this.state.studentid2,
                                                    onChange: this.handleInputChange,
                                                    required: true
                                                }, void 0, false, {
                                                    fileName: "src/components/IT20131456/user/CreateStudentGroup.js",
                                                    lineNumber: 189,
                                                    columnNumber: 19
                                                }, this)
                                            ]
                                        }, void 0, true, {
                                            fileName: "src/components/IT20131456/user/CreateStudentGroup.js",
                                            lineNumber: 187,
                                            columnNumber: 17
                                        }, this)
                                    }, void 0, false, {
                                        fileName: "src/components/IT20131456/user/CreateStudentGroup.js",
                                        lineNumber: 186,
                                        columnNumber: 15
                                    }, this),
                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                        className: "col-md-6",
                                        children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                            className: "form-group",
                                            children: [
                                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("strong", {
                                                    children: "Student_2 Name :"
                                                }, void 0, false, {
                                                    fileName: "src/components/IT20131456/user/CreateStudentGroup.js",
                                                    lineNumber: 204,
                                                    columnNumber: 19
                                                }, this),
                                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("input", {
                                                    type: "text",
                                                    className: "form-control",
                                                    placeholder: "Enter name",
                                                    name: "studentname2",
                                                    value: this.state.studentname2,
                                                    onChange: this.handleInputChange,
                                                    required: true
                                                }, void 0, false, {
                                                    fileName: "src/components/IT20131456/user/CreateStudentGroup.js",
                                                    lineNumber: 205,
                                                    columnNumber: 19
                                                }, this)
                                            ]
                                        }, void 0, true, {
                                            fileName: "src/components/IT20131456/user/CreateStudentGroup.js",
                                            lineNumber: 203,
                                            columnNumber: 17
                                        }, this)
                                    }, void 0, false, {
                                        fileName: "src/components/IT20131456/user/CreateStudentGroup.js",
                                        lineNumber: 202,
                                        columnNumber: 15
                                    }, this)
                                ]
                            }, void 0, true, {
                                fileName: "src/components/IT20131456/user/CreateStudentGroup.js",
                                lineNumber: 185,
                                columnNumber: 13
                            }, this),
                            "\xa0",
                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                className: "row",
                                children: [
                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                        className: "col-md-6",
                                        children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                            className: "form-group",
                                            children: [
                                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("strong", {
                                                    children: "Student_3 ID :"
                                                }, void 0, false, {
                                                    fileName: "src/components/IT20131456/user/CreateStudentGroup.js",
                                                    lineNumber: 221,
                                                    columnNumber: 19
                                                }, this),
                                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("input", {
                                                    type: "text",
                                                    className: "form-control",
                                                    placeholder: "Enter id",
                                                    name: "studentid3",
                                                    pattern: "IT[0-9]{4}",
                                                    title: "Student ID is Invalid",
                                                    value: this.state.studentid3,
                                                    onChange: this.handleInputChange,
                                                    required: true
                                                }, void 0, false, {
                                                    fileName: "src/components/IT20131456/user/CreateStudentGroup.js",
                                                    lineNumber: 222,
                                                    columnNumber: 19
                                                }, this)
                                            ]
                                        }, void 0, true, {
                                            fileName: "src/components/IT20131456/user/CreateStudentGroup.js",
                                            lineNumber: 220,
                                            columnNumber: 17
                                        }, this)
                                    }, void 0, false, {
                                        fileName: "src/components/IT20131456/user/CreateStudentGroup.js",
                                        lineNumber: 219,
                                        columnNumber: 15
                                    }, this),
                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                        className: "col-md-6",
                                        children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                            className: "form-group",
                                            children: [
                                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("strong", {
                                                    children: "Student_3 Name :"
                                                }, void 0, false, {
                                                    fileName: "src/components/IT20131456/user/CreateStudentGroup.js",
                                                    lineNumber: 237,
                                                    columnNumber: 19
                                                }, this),
                                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("input", {
                                                    type: "text",
                                                    className: "form-control",
                                                    placeholder: "Enter name",
                                                    name: "studentname3",
                                                    value: this.state.studentname3,
                                                    onChange: this.handleInputChange,
                                                    required: true
                                                }, void 0, false, {
                                                    fileName: "src/components/IT20131456/user/CreateStudentGroup.js",
                                                    lineNumber: 238,
                                                    columnNumber: 19
                                                }, this)
                                            ]
                                        }, void 0, true, {
                                            fileName: "src/components/IT20131456/user/CreateStudentGroup.js",
                                            lineNumber: 236,
                                            columnNumber: 17
                                        }, this)
                                    }, void 0, false, {
                                        fileName: "src/components/IT20131456/user/CreateStudentGroup.js",
                                        lineNumber: 235,
                                        columnNumber: 15
                                    }, this)
                                ]
                            }, void 0, true, {
                                fileName: "src/components/IT20131456/user/CreateStudentGroup.js",
                                lineNumber: 218,
                                columnNumber: 13
                            }, this),
                            "\xa0",
                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                className: "row",
                                children: [
                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                        className: "col-md-6",
                                        children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                            className: "form-group",
                                            children: [
                                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("strong", {
                                                    children: "Student_4 ID :"
                                                }, void 0, false, {
                                                    fileName: "src/components/IT20131456/user/CreateStudentGroup.js",
                                                    lineNumber: 254,
                                                    columnNumber: 19
                                                }, this),
                                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("input", {
                                                    type: "text",
                                                    className: "form-control",
                                                    placeholder: "Enter id",
                                                    name: "studentid4",
                                                    pattern: "IT[0-9]{4}",
                                                    title: "Student ID is Invalid",
                                                    value: this.state.studentid4,
                                                    onChange: this.handleInputChange,
                                                    required: true
                                                }, void 0, false, {
                                                    fileName: "src/components/IT20131456/user/CreateStudentGroup.js",
                                                    lineNumber: 255,
                                                    columnNumber: 19
                                                }, this)
                                            ]
                                        }, void 0, true, {
                                            fileName: "src/components/IT20131456/user/CreateStudentGroup.js",
                                            lineNumber: 253,
                                            columnNumber: 17
                                        }, this)
                                    }, void 0, false, {
                                        fileName: "src/components/IT20131456/user/CreateStudentGroup.js",
                                        lineNumber: 252,
                                        columnNumber: 15
                                    }, this),
                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                        className: "col-md-6",
                                        children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                            className: "form-group",
                                            children: [
                                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("strong", {
                                                    children: "Student_4 Name :"
                                                }, void 0, false, {
                                                    fileName: "src/components/IT20131456/user/CreateStudentGroup.js",
                                                    lineNumber: 270,
                                                    columnNumber: 19
                                                }, this),
                                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("input", {
                                                    type: "text",
                                                    className: "form-control",
                                                    placeholder: "Enter name",
                                                    name: "studentname4",
                                                    value: this.state.studentname4,
                                                    onChange: this.handleInputChange,
                                                    required: true
                                                }, void 0, false, {
                                                    fileName: "src/components/IT20131456/user/CreateStudentGroup.js",
                                                    lineNumber: 271,
                                                    columnNumber: 19
                                                }, this)
                                            ]
                                        }, void 0, true, {
                                            fileName: "src/components/IT20131456/user/CreateStudentGroup.js",
                                            lineNumber: 269,
                                            columnNumber: 17
                                        }, this)
                                    }, void 0, false, {
                                        fileName: "src/components/IT20131456/user/CreateStudentGroup.js",
                                        lineNumber: 268,
                                        columnNumber: 15
                                    }, this)
                                ]
                            }, void 0, true, {
                                fileName: "src/components/IT20131456/user/CreateStudentGroup.js",
                                lineNumber: 251,
                                columnNumber: 13
                            }, this),
                            "\xa0",
                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                className: "row",
                                children: [
                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                        className: "col-md-6",
                                        children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                            className: "form-group",
                                            children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("input", {
                                                type: "hidden",
                                                className: "form-control",
                                                name: "supervisorname",
                                                onChange: this.handleInputChange
                                            }, void 0, false, {
                                                fileName: "src/components/IT20131456/user/CreateStudentGroup.js",
                                                lineNumber: 287,
                                                columnNumber: 19
                                            }, this)
                                        }, void 0, false, {
                                            fileName: "src/components/IT20131456/user/CreateStudentGroup.js",
                                            lineNumber: 286,
                                            columnNumber: 17
                                        }, this)
                                    }, void 0, false, {
                                        fileName: "src/components/IT20131456/user/CreateStudentGroup.js",
                                        lineNumber: 285,
                                        columnNumber: 15
                                    }, this),
                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                        className: "col-md-6",
                                        children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                            className: "form-group",
                                            children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("input", {
                                                type: "hidden",
                                                className: "form-control",
                                                name: "cosupervisorname",
                                                onChange: this.handleInputChange
                                            }, void 0, false, {
                                                fileName: "src/components/IT20131456/user/CreateStudentGroup.js",
                                                lineNumber: 297,
                                                columnNumber: 19
                                            }, this)
                                        }, void 0, false, {
                                            fileName: "src/components/IT20131456/user/CreateStudentGroup.js",
                                            lineNumber: 296,
                                            columnNumber: 17
                                        }, this)
                                    }, void 0, false, {
                                        fileName: "src/components/IT20131456/user/CreateStudentGroup.js",
                                        lineNumber: 295,
                                        columnNumber: 15
                                    }, this)
                                ]
                            }, void 0, true, {
                                fileName: "src/components/IT20131456/user/CreateStudentGroup.js",
                                lineNumber: 284,
                                columnNumber: 13
                            }, this),
                            "\xa0",
                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                className: "row",
                                children: [
                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                        className: "col-md-6",
                                        children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                            className: "form-group",
                                            children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("input", {
                                                type: "hidden",
                                                className: "form-control",
                                                name: "panelmembername",
                                                onChange: this.handleInputChange
                                            }, void 0, false, {
                                                fileName: "src/components/IT20131456/user/CreateStudentGroup.js",
                                                lineNumber: 310,
                                                columnNumber: 19
                                            }, this)
                                        }, void 0, false, {
                                            fileName: "src/components/IT20131456/user/CreateStudentGroup.js",
                                            lineNumber: 309,
                                            columnNumber: 17
                                        }, this)
                                    }, void 0, false, {
                                        fileName: "src/components/IT20131456/user/CreateStudentGroup.js",
                                        lineNumber: 308,
                                        columnNumber: 15
                                    }, this),
                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                        className: "col-md-6",
                                        children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                            className: "form-group",
                                            children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("input", {
                                                type: "hidden",
                                                className: "form-control",
                                                name: "status",
                                                onChange: this.handleInputChange
                                            }, void 0, false, {
                                                fileName: "src/components/IT20131456/user/CreateStudentGroup.js",
                                                lineNumber: 320,
                                                columnNumber: 19
                                            }, this)
                                        }, void 0, false, {
                                            fileName: "src/components/IT20131456/user/CreateStudentGroup.js",
                                            lineNumber: 319,
                                            columnNumber: 17
                                        }, this)
                                    }, void 0, false, {
                                        fileName: "src/components/IT20131456/user/CreateStudentGroup.js",
                                        lineNumber: 318,
                                        columnNumber: 15
                                    }, this)
                                ]
                            }, void 0, true, {
                                fileName: "src/components/IT20131456/user/CreateStudentGroup.js",
                                lineNumber: 307,
                                columnNumber: 13
                            }, this),
                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                className: "col-md-12",
                                children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                    className: "form-group",
                                    children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("button", {
                                        className: "btn btn-outline-primary",
                                        type: "submit",
                                        children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("i", {
                                            className: "fa fa-save",
                                            children: " Save "
                                        }, void 0, false, {
                                            fileName: "src/components/IT20131456/user/CreateStudentGroup.js",
                                            lineNumber: 332,
                                            columnNumber: 19
                                        }, this)
                                    }, void 0, false, {
                                        fileName: "src/components/IT20131456/user/CreateStudentGroup.js",
                                        lineNumber: 331,
                                        columnNumber: 17
                                    }, this)
                                }, void 0, false, {
                                    fileName: "src/components/IT20131456/user/CreateStudentGroup.js",
                                    lineNumber: 330,
                                    columnNumber: 15
                                }, this)
                            }, void 0, false, {
                                fileName: "src/components/IT20131456/user/CreateStudentGroup.js",
                                lineNumber: 329,
                                columnNumber: 13
                            }, this),
                            "\xa0"
                        ]
                    }, void 0, true, {
                        fileName: "src/components/IT20131456/user/CreateStudentGroup.js",
                        lineNumber: 118,
                        columnNumber: 11
                    }, this)
                ]
            }, void 0, true, {
                fileName: "src/components/IT20131456/user/CreateStudentGroup.js",
                lineNumber: 107,
                columnNumber: 9
            }, this)
        }, void 0, false, {
            fileName: "src/components/IT20131456/user/CreateStudentGroup.js",
            lineNumber: 106,
            columnNumber: 7
        }, this);
    }
}
exports.default = CreateStudentGroup;

  $parcel$ReactRefreshHelpers$1505.postlude(module);
} finally {
  window.$RefreshReg$ = prevRefreshReg;
  window.$RefreshSig$ = prevRefreshSig;
}
},{"react/jsx-dev-runtime":"iTorj","react":"21dqq","axios":"jo6P5","@parcel/transformer-js/src/esmodule-helpers.js":"gkKU3","@parcel/transformer-react-refresh-wrap/lib/helpers/helpers.js":"km3Ru"}],"8oXp2":[function(require,module,exports) {
var $parcel$ReactRefreshHelpers$6256 = require("@parcel/transformer-react-refresh-wrap/lib/helpers/helpers.js");
var prevRefreshReg = window.$RefreshReg$;
var prevRefreshSig = window.$RefreshSig$;
$parcel$ReactRefreshHelpers$6256.prelude(module);

try {
var parcelHelpers = require("@parcel/transformer-js/src/esmodule-helpers.js");
parcelHelpers.defineInteropFlag(exports);
var _jsxDevRuntime = require("react/jsx-dev-runtime");
var _react = require("react");
var _reactDefault = parcelHelpers.interopDefault(_react);
var _axios = require("axios");
var _axiosDefault = parcelHelpers.interopDefault(_axios);
var _jwtDecode = require("jwt-decode");
var _jwtDecodeDefault = parcelHelpers.interopDefault(_jwtDecode);
var _groupJpg = require("../../../images/group.jpg");
var _groupJpgDefault = parcelHelpers.interopDefault(_groupJpg);
class ViewStudentGroup extends _react.Component {
    constructor(){
        super();
        this.state = {
            studentgroup: [],
            groupid: "",
            groupname: "",
            studentid1: "",
            studentname1: "",
            studentid2: "",
            studentname2: "",
            studentid3: "",
            studentname3: "",
            studentid4: "",
            studentname4: "",
            supervisorname: "",
            cosupervisorname: "",
            panelmembername: "",
            status: ""
        };
    }
    componentDidMount() {
        if (localStorage.userToken) {
            const usertoken = localStorage.userToken;
            const decoded = _jwtDecodeDefault.default(usertoken);
            this.setState({
                groupId: decoded.groupId
            });
        }
        setTimeout(()=>{
            this.retrivesgroup();
        }, 1000);
    }
    retrivesgroup() {
        const gId = this.state.groupId;
        console.log(gId);
        _axiosDefault.default.get(`http://localhost:5000/stugroup/${gId}`).then((res)=>{
            if (res.data.success) {
                console.log("scuuess");
                this.setState({
                    studentgroup: res.data.stugroup,
                    groupid: res.data.stugroup.groupid,
                    groupname: res.data.stugroup.groupname,
                    studentid1: res.data.stugroup.studentid1,
                    studentname1: res.data.stugroup.studentname1,
                    studentid2: res.data.stugroup.studentid2,
                    studentname2: res.data.stugroup.studentname2,
                    studentid3: res.data.stugroup.studentid3,
                    studentname3: res.data.stugroup.studentname3,
                    studentid4: res.data.stugroup.studentid4,
                    studentname4: res.data.stugroup.studentname4,
                    supervisorname: res.data.stugroup.supervisorname,
                    cosupervisorname: res.data.stugroup.cosupervisorname,
                    panelmembername: res.data.stugroup.panelmembername,
                    status: res.data.stugroup.status
                });
                console.log(this.state.studentgroup);
            }
        });
    }
    render() {
        return /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
            className: "container px-5 my-5",
            children: [
                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                    className: "float-left my-3",
                    children: [
                        "\xa0",
                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("h2", {
                            children: "View Student Group (Student)"
                        }, void 0, false, {
                            fileName: "src/components/IT20131456/user/ViewStudentGroup.js",
                            lineNumber: 80,
                            columnNumber: 11
                        }, this)
                    ]
                }, void 0, true, {
                    fileName: "src/components/IT20131456/user/ViewStudentGroup.js",
                    lineNumber: 78,
                    columnNumber: 9
                }, this),
                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("hr", {}, void 0, false, {
                    fileName: "src/components/IT20131456/user/ViewStudentGroup.js",
                    lineNumber: 82,
                    columnNumber: 9
                }, this),
                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                    class: "card mb-3 col-md-12 card border-dark my-3",
                    children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                        class: "row g-0",
                        children: [
                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                class: "col-md-4",
                                children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("img", {
                                    src: _groupJpgDefault.default,
                                    style: {
                                        height: "580px",
                                        width: "420px"
                                    },
                                    className: "img-fluid rounded-start mt-1",
                                    alt: "..."
                                }, void 0, false, {
                                    fileName: "src/components/IT20131456/user/ViewStudentGroup.js",
                                    lineNumber: 87,
                                    columnNumber: 15
                                }, this)
                            }, void 0, false, {
                                fileName: "src/components/IT20131456/user/ViewStudentGroup.js",
                                lineNumber: 86,
                                columnNumber: 13
                            }, this),
                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                class: "col-md-8",
                                children: [
                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                        className: "card-header text-center",
                                        children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("h3", {
                                            children: [
                                                this.state.groupname,
                                                " - ",
                                                this.state.groupid
                                            ]
                                        }, void 0, true, {
                                            fileName: "src/components/IT20131456/user/ViewStudentGroup.js",
                                            lineNumber: 96,
                                            columnNumber: 17
                                        }, this)
                                    }, void 0, false, {
                                        fileName: "src/components/IT20131456/user/ViewStudentGroup.js",
                                        lineNumber: 95,
                                        columnNumber: 15
                                    }, this),
                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                        class: "card-body mx-5 my-5",
                                        children: [
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("h5", {
                                                children: [
                                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("span", {
                                                        style: {
                                                            fontSize: "24px"
                                                        },
                                                        children: "Student_1 : "
                                                    }, void 0, false, {
                                                        fileName: "src/components/IT20131456/user/ViewStudentGroup.js",
                                                        lineNumber: 100,
                                                        columnNumber: 19
                                                    }, this),
                                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("span", {
                                                        className: "text-primary",
                                                        style: {
                                                            fontSize: "19px"
                                                        },
                                                        children: [
                                                            this.state.studentid1,
                                                            " - ",
                                                            this.state.studentname1
                                                        ]
                                                    }, void 0, true, {
                                                        fileName: "src/components/IT20131456/user/ViewStudentGroup.js",
                                                        lineNumber: 101,
                                                        columnNumber: 19
                                                    }, this)
                                                ]
                                            }, void 0, true, {
                                                fileName: "src/components/IT20131456/user/ViewStudentGroup.js",
                                                lineNumber: 99,
                                                columnNumber: 17
                                            }, this),
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("h5", {
                                                children: [
                                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("span", {
                                                        style: {
                                                            fontSize: "24px"
                                                        },
                                                        children: "Student_2 : "
                                                    }, void 0, false, {
                                                        fileName: "src/components/IT20131456/user/ViewStudentGroup.js",
                                                        lineNumber: 107,
                                                        columnNumber: 19
                                                    }, this),
                                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("span", {
                                                        className: "text-primary",
                                                        style: {
                                                            fontSize: "19px"
                                                        },
                                                        children: [
                                                            this.state.studentid2,
                                                            " - ",
                                                            this.state.studentname2
                                                        ]
                                                    }, void 0, true, {
                                                        fileName: "src/components/IT20131456/user/ViewStudentGroup.js",
                                                        lineNumber: 108,
                                                        columnNumber: 19
                                                    }, this)
                                                ]
                                            }, void 0, true, {
                                                fileName: "src/components/IT20131456/user/ViewStudentGroup.js",
                                                lineNumber: 106,
                                                columnNumber: 17
                                            }, this),
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("h5", {
                                                children: [
                                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("span", {
                                                        style: {
                                                            fontSize: "24px"
                                                        },
                                                        children: "Student_3 : "
                                                    }, void 0, false, {
                                                        fileName: "src/components/IT20131456/user/ViewStudentGroup.js",
                                                        lineNumber: 115,
                                                        columnNumber: 19
                                                    }, this),
                                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("span", {
                                                        className: "text-primary",
                                                        style: {
                                                            fontSize: "19px"
                                                        },
                                                        children: [
                                                            this.state.studentid3,
                                                            " - ",
                                                            this.state.studentname3
                                                        ]
                                                    }, void 0, true, {
                                                        fileName: "src/components/IT20131456/user/ViewStudentGroup.js",
                                                        lineNumber: 116,
                                                        columnNumber: 19
                                                    }, this)
                                                ]
                                            }, void 0, true, {
                                                fileName: "src/components/IT20131456/user/ViewStudentGroup.js",
                                                lineNumber: 114,
                                                columnNumber: 17
                                            }, this),
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("h5", {
                                                children: [
                                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("span", {
                                                        style: {
                                                            fontSize: "24px"
                                                        },
                                                        children: "Student_4 : "
                                                    }, void 0, false, {
                                                        fileName: "src/components/IT20131456/user/ViewStudentGroup.js",
                                                        lineNumber: 123,
                                                        columnNumber: 19
                                                    }, this),
                                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("span", {
                                                        className: "text-primary",
                                                        style: {
                                                            fontSize: "19px"
                                                        },
                                                        children: [
                                                            this.state.studentid4,
                                                            " - ",
                                                            this.state.studentname4
                                                        ]
                                                    }, void 0, true, {
                                                        fileName: "src/components/IT20131456/user/ViewStudentGroup.js",
                                                        lineNumber: 124,
                                                        columnNumber: 19
                                                    }, this)
                                                ]
                                            }, void 0, true, {
                                                fileName: "src/components/IT20131456/user/ViewStudentGroup.js",
                                                lineNumber: 122,
                                                columnNumber: 17
                                            }, this),
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("br", {}, void 0, false, {
                                                fileName: "src/components/IT20131456/user/ViewStudentGroup.js",
                                                lineNumber: 129,
                                                columnNumber: 17
                                            }, this),
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("h5", {
                                                children: [
                                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("span", {
                                                        style: {
                                                            fontSize: "24px"
                                                        },
                                                        children: "Supervisor : "
                                                    }, void 0, false, {
                                                        fileName: "src/components/IT20131456/user/ViewStudentGroup.js",
                                                        lineNumber: 131,
                                                        columnNumber: 19
                                                    }, this),
                                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("span", {
                                                        className: "text-primary",
                                                        style: {
                                                            fontSize: "19px"
                                                        },
                                                        children: this.state.supervisorname
                                                    }, void 0, false, {
                                                        fileName: "src/components/IT20131456/user/ViewStudentGroup.js",
                                                        lineNumber: 132,
                                                        columnNumber: 19
                                                    }, this)
                                                ]
                                            }, void 0, true, {
                                                fileName: "src/components/IT20131456/user/ViewStudentGroup.js",
                                                lineNumber: 130,
                                                columnNumber: 17
                                            }, this),
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("h5", {
                                                children: [
                                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("span", {
                                                        style: {
                                                            fontSize: "24px"
                                                        },
                                                        children: "Cosupervisor : "
                                                    }, void 0, false, {
                                                        fileName: "src/components/IT20131456/user/ViewStudentGroup.js",
                                                        lineNumber: 139,
                                                        columnNumber: 19
                                                    }, this),
                                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("span", {
                                                        className: "text-primary",
                                                        style: {
                                                            fontSize: "19px"
                                                        },
                                                        children: this.state.cosupervisorname
                                                    }, void 0, false, {
                                                        fileName: "src/components/IT20131456/user/ViewStudentGroup.js",
                                                        lineNumber: 140,
                                                        columnNumber: 19
                                                    }, this)
                                                ]
                                            }, void 0, true, {
                                                fileName: "src/components/IT20131456/user/ViewStudentGroup.js",
                                                lineNumber: 138,
                                                columnNumber: 17
                                            }, this),
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("h5", {
                                                children: [
                                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("span", {
                                                        style: {
                                                            fontSize: "24px"
                                                        },
                                                        children: "Panelmember : "
                                                    }, void 0, false, {
                                                        fileName: "src/components/IT20131456/user/ViewStudentGroup.js",
                                                        lineNumber: 147,
                                                        columnNumber: 19
                                                    }, this),
                                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("span", {
                                                        className: "text-primary",
                                                        style: {
                                                            fontSize: "19px"
                                                        },
                                                        children: this.state.panelmembername
                                                    }, void 0, false, {
                                                        fileName: "src/components/IT20131456/user/ViewStudentGroup.js",
                                                        lineNumber: 148,
                                                        columnNumber: 19
                                                    }, this)
                                                ]
                                            }, void 0, true, {
                                                fileName: "src/components/IT20131456/user/ViewStudentGroup.js",
                                                lineNumber: 146,
                                                columnNumber: 17
                                            }, this),
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("br", {}, void 0, false, {
                                                fileName: "src/components/IT20131456/user/ViewStudentGroup.js",
                                                lineNumber: 153,
                                                columnNumber: 17
                                            }, this),
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("h4", {
                                                children: [
                                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("span", {
                                                        style: {
                                                            fontSize: "24px"
                                                        },
                                                        children: "Status : "
                                                    }, void 0, false, {
                                                        fileName: "src/components/IT20131456/user/ViewStudentGroup.js",
                                                        lineNumber: 155,
                                                        columnNumber: 19
                                                    }, this),
                                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("span", {
                                                        className: "text-primary",
                                                        style: {
                                                            fontSize: "19px"
                                                        },
                                                        children: this.state.status
                                                    }, void 0, false, {
                                                        fileName: "src/components/IT20131456/user/ViewStudentGroup.js",
                                                        lineNumber: 156,
                                                        columnNumber: 19
                                                    }, this)
                                                ]
                                            }, void 0, true, {
                                                fileName: "src/components/IT20131456/user/ViewStudentGroup.js",
                                                lineNumber: 154,
                                                columnNumber: 17
                                            }, this)
                                        ]
                                    }, void 0, true, {
                                        fileName: "src/components/IT20131456/user/ViewStudentGroup.js",
                                        lineNumber: 98,
                                        columnNumber: 15
                                    }, this),
                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                        className: "col-md-3 px-5",
                                        children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("a", {
                                            className: "btn btn-outline-primary",
                                            type: "submit",
                                            href: `/home`,
                                            children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("i", {
                                                className: "fa fa-arrow-circle-left",
                                                children: " Back "
                                            }, void 0, false, {
                                                fileName: "src/components/IT20131456/user/ViewStudentGroup.js",
                                                lineNumber: 164,
                                                columnNumber: 19
                                            }, this)
                                        }, void 0, false, {
                                            fileName: "src/components/IT20131456/user/ViewStudentGroup.js",
                                            lineNumber: 163,
                                            columnNumber: 17
                                        }, this)
                                    }, void 0, false, {
                                        fileName: "src/components/IT20131456/user/ViewStudentGroup.js",
                                        lineNumber: 162,
                                        columnNumber: 15
                                    }, this),
                                    "\xa0"
                                ]
                            }, void 0, true, {
                                fileName: "src/components/IT20131456/user/ViewStudentGroup.js",
                                lineNumber: 94,
                                columnNumber: 13
                            }, this)
                        ]
                    }, void 0, true, {
                        fileName: "src/components/IT20131456/user/ViewStudentGroup.js",
                        lineNumber: 85,
                        columnNumber: 11
                    }, this)
                }, void 0, false, {
                    fileName: "src/components/IT20131456/user/ViewStudentGroup.js",
                    lineNumber: 84,
                    columnNumber: 9
                }, this)
            ]
        }, void 0, true, {
            fileName: "src/components/IT20131456/user/ViewStudentGroup.js",
            lineNumber: 77,
            columnNumber: 7
        }, this);
    }
}
exports.default = ViewStudentGroup;

  $parcel$ReactRefreshHelpers$6256.postlude(module);
} finally {
  window.$RefreshReg$ = prevRefreshReg;
  window.$RefreshSig$ = prevRefreshSig;
}
},{"react/jsx-dev-runtime":"iTorj","react":"21dqq","axios":"jo6P5","jwt-decode":"ljM1w","../../../images/group.jpg":"imiRg","@parcel/transformer-js/src/esmodule-helpers.js":"gkKU3","@parcel/transformer-react-refresh-wrap/lib/helpers/helpers.js":"km3Ru"}],"imiRg":[function(require,module,exports) {
module.exports = require('./helpers/bundle-url').getBundleURL('bLxZJ') + "group.679a8e60.jpg" + "?" + Date.now();

},{"./helpers/bundle-url":"lgJ39"}],"7zWk5":[function(require,module,exports) {
var $parcel$ReactRefreshHelpers$619c = require("@parcel/transformer-react-refresh-wrap/lib/helpers/helpers.js");
var prevRefreshReg = window.$RefreshReg$;
var prevRefreshSig = window.$RefreshSig$;
$parcel$ReactRefreshHelpers$619c.prelude(module);

try {
var parcelHelpers = require("@parcel/transformer-js/src/esmodule-helpers.js");
parcelHelpers.defineInteropFlag(exports);
var _jsxDevRuntime = require("react/jsx-dev-runtime");
var _react = require("react");
var _reactDefault = parcelHelpers.interopDefault(_react);
var _axios = require("axios");
var _axiosDefault = parcelHelpers.interopDefault(_axios);
class RequestSupervisor extends _react.Component {
    constructor(props){
        super(props);
        this.state = {
            supervisordetails: []
        };
    }
    componentDidMount() {
        this.retrieveSupervisorDetails();
    }
    retrieveSupervisorDetails() {
        _axiosDefault.default.get("http://localhost:5000/supervisors").then((res)=>{
            if (res.data.success) {
                this.setState({
                    supervisordetails: res.data.existingsupervisordetails
                });
                console.log(this.state.supervisordetails);
            }
        });
    }
    filterData(supervisordetails, searchKey) {
        const result = supervisordetails.filter((supervisordetail)=>supervisordetail.supervisorid.toLowerCase().includes(searchKey) || supervisordetail.supervisorname.toLowerCase().includes(searchKey) || supervisordetail.researchfield.toLowerCase().includes(searchKey) || supervisordetail.email.toLowerCase().includes(searchKey) || supervisordetail.supervisorid.toUpperCase().includes(searchKey) || supervisordetail.supervisorname.toUpperCase().includes(searchKey) || supervisordetail.researchfield.toUpperCase().includes(searchKey) || supervisordetail.email.toUpperCase().includes(searchKey) || supervisordetail.supervisorid.includes(searchKey) || supervisordetail.supervisorname.includes(searchKey) || supervisordetail.researchfield.includes(searchKey) || supervisordetail.email.includes(searchKey)
        );
        this.setState({
            supervisordetails: result
        });
    }
    handleSearchArea = (e)=>{
        const searchKey = e.currentTarget.value;
        _axiosDefault.default.get("http://localhost:5000/supervisors").then((res)=>{
            if (res.data.success) this.filterData(res.data.existingsupervisordetails, searchKey);
        });
    };
    render() {
        return /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
            className: "container px-5 my-3",
            children: [
                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                    className: "row",
                    children: [
                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                            className: "float-left col-lg-9 mt-2 mb-2",
                            children: [
                                "\xa0",
                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("h2", {
                                    children: "Send Request"
                                }, void 0, false, {
                                    fileName: "src/components/IT20131456/user/RequestSupervisor.js",
                                    lineNumber: 68,
                                    columnNumber: 13
                                }, this)
                            ]
                        }, void 0, true, {
                            fileName: "src/components/IT20131456/user/RequestSupervisor.js",
                            lineNumber: 66,
                            columnNumber: 11
                        }, this),
                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                            className: "col-lg-3 mt-2 mb-2",
                            children: [
                                "\xa0",
                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("input", {
                                    className: "form-control border border-dark",
                                    type: "search",
                                    placeholder: "Search",
                                    name: "searchQuery",
                                    onChange: this.handleSearchArea
                                }, void 0, false, {
                                    fileName: "src/components/IT20131456/user/RequestSupervisor.js",
                                    lineNumber: 73,
                                    columnNumber: 13
                                }, this)
                            ]
                        }, void 0, true, {
                            fileName: "src/components/IT20131456/user/RequestSupervisor.js",
                            lineNumber: 71,
                            columnNumber: 11
                        }, this)
                    ]
                }, void 0, true, {
                    fileName: "src/components/IT20131456/user/RequestSupervisor.js",
                    lineNumber: 65,
                    columnNumber: 9
                }, this),
                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("hr", {}, void 0, false, {
                    fileName: "src/components/IT20131456/user/RequestSupervisor.js",
                    lineNumber: 82,
                    columnNumber: 11
                }, this),
                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("br", {}, void 0, false, {
                    fileName: "src/components/IT20131456/user/RequestSupervisor.js",
                    lineNumber: 83,
                    columnNumber: 11
                }, this),
                this.state.supervisordetails.map((supervisordetails, index)=>/*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                        class: "card border border-dark my-3",
                        children: [
                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("h5", {
                                class: "card-header",
                                children: [
                                    index + 1,
                                    " - ",
                                    supervisordetails.researchfield
                                ]
                            }, void 0, true, {
                                fileName: "src/components/IT20131456/user/RequestSupervisor.js",
                                lineNumber: 87,
                                columnNumber: 11
                            }, this),
                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                class: "card-body",
                                children: [
                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("h5", {
                                        class: "card-title",
                                        children: supervisordetails.supervisorname
                                    }, void 0, false, {
                                        fileName: "src/components/IT20131456/user/RequestSupervisor.js",
                                        lineNumber: 89,
                                        columnNumber: 15
                                    }, this),
                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("p", {
                                        class: "card-text",
                                        children: supervisordetails.email
                                    }, void 0, false, {
                                        fileName: "src/components/IT20131456/user/RequestSupervisor.js",
                                        lineNumber: 90,
                                        columnNumber: 15
                                    }, this),
                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("a", {
                                        href: `/create/request/${supervisordetails._id}`,
                                        class: "btn btn-outline-primary",
                                        children: [
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("i", {
                                                class: "fa fa-paper-plane"
                                            }, void 0, false, {
                                                fileName: "src/components/IT20131456/user/RequestSupervisor.js",
                                                lineNumber: 92,
                                                columnNumber: 15
                                            }, this),
                                            "\xa0Send Request"
                                        ]
                                    }, void 0, true, {
                                        fileName: "src/components/IT20131456/user/RequestSupervisor.js",
                                        lineNumber: 91,
                                        columnNumber: 15
                                    }, this)
                                ]
                            }, void 0, true, {
                                fileName: "src/components/IT20131456/user/RequestSupervisor.js",
                                lineNumber: 88,
                                columnNumber: 13
                            }, this)
                        ]
                    }, void 0, true, {
                        fileName: "src/components/IT20131456/user/RequestSupervisor.js",
                        lineNumber: 86,
                        columnNumber: 9
                    }, this)
                )
            ]
        }, void 0, true, {
            fileName: "src/components/IT20131456/user/RequestSupervisor.js",
            lineNumber: 64,
            columnNumber: 7
        }, this);
    }
}
exports.default = RequestSupervisor;

  $parcel$ReactRefreshHelpers$619c.postlude(module);
} finally {
  window.$RefreshReg$ = prevRefreshReg;
  window.$RefreshSig$ = prevRefreshSig;
}
},{"react/jsx-dev-runtime":"iTorj","react":"21dqq","axios":"jo6P5","@parcel/transformer-js/src/esmodule-helpers.js":"gkKU3","@parcel/transformer-react-refresh-wrap/lib/helpers/helpers.js":"km3Ru"}],"8utuE":[function(require,module,exports) {
var $parcel$ReactRefreshHelpers$8620 = require("@parcel/transformer-react-refresh-wrap/lib/helpers/helpers.js");
var prevRefreshReg = window.$RefreshReg$;
var prevRefreshSig = window.$RefreshSig$;
$parcel$ReactRefreshHelpers$8620.prelude(module);

try {
var parcelHelpers = require("@parcel/transformer-js/src/esmodule-helpers.js");
parcelHelpers.defineInteropFlag(exports);
var _jsxDevRuntime = require("react/jsx-dev-runtime");
var _react = require("react");
var _reactDefault = parcelHelpers.interopDefault(_react);
var _emailjsCom = require("emailjs-com");
var _emailjsComDefault = parcelHelpers.interopDefault(_emailjsCom);
const CreateRequest = ()=>{
    function sendEmail(e) {
        e.preventDefault();
        _emailjsComDefault.default.sendForm("service_hf0hikc", "template_ajn4t7c", e.target, "AopuNlecWrsPFXStu").then((res)=>{
            console.log(res);
            swal("Request Send Successfull", "", "success");
        }).catch((err)=>console.log(err)
        );
    }
    return /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
        className: "container px-5 my-3",
        children: [
            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("br", {}, void 0, false, {
                fileName: "src/components/IT20131456/user/CreateRequest.js",
                lineNumber: 26,
                columnNumber: 7
            }, undefined),
            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                className: "container border border-dark bg-light mt-5 ",
                children: [
                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                        className: "form-group row",
                        children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                            className: "col-lg-12 margin-tb",
                            children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                className: "float-left",
                                children: [
                                    "\xa0",
                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("h2", {
                                        children: "New Request"
                                    }, void 0, false, {
                                        fileName: "src/components/IT20131456/user/CreateRequest.js",
                                        lineNumber: 32,
                                        columnNumber: 15
                                    }, undefined),
                                    "\xa0"
                                ]
                            }, void 0, true, {
                                fileName: "src/components/IT20131456/user/CreateRequest.js",
                                lineNumber: 30,
                                columnNumber: 13
                            }, undefined)
                        }, void 0, false, {
                            fileName: "src/components/IT20131456/user/CreateRequest.js",
                            lineNumber: 29,
                            columnNumber: 11
                        }, undefined)
                    }, void 0, false, {
                        fileName: "src/components/IT20131456/user/CreateRequest.js",
                        lineNumber: 28,
                        columnNumber: 9
                    }, undefined),
                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("form", {
                        onSubmit: sendEmail,
                        children: [
                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                className: "row",
                                children: [
                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                        className: "col-md-6",
                                        children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                            className: "form-group",
                                            children: [
                                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("strong", {
                                                    children: "Supervisor Name :"
                                                }, void 0, false, {
                                                    fileName: "src/components/IT20131456/user/CreateRequest.js",
                                                    lineNumber: 41,
                                                    columnNumber: 17
                                                }, undefined),
                                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("input", {
                                                    type: "text",
                                                    className: "form-control",
                                                    name: "supervisorname",
                                                    placeholder: "Enter supervisor name",
                                                    required: true
                                                }, void 0, false, {
                                                    fileName: "src/components/IT20131456/user/CreateRequest.js",
                                                    lineNumber: 42,
                                                    columnNumber: 17
                                                }, undefined)
                                            ]
                                        }, void 0, true, {
                                            fileName: "src/components/IT20131456/user/CreateRequest.js",
                                            lineNumber: 40,
                                            columnNumber: 15
                                        }, undefined)
                                    }, void 0, false, {
                                        fileName: "src/components/IT20131456/user/CreateRequest.js",
                                        lineNumber: 39,
                                        columnNumber: 13
                                    }, undefined),
                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                        className: "col-md-6",
                                        children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                            className: "form-group",
                                            children: [
                                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("strong", {
                                                    children: "Group ID :"
                                                }, void 0, false, {
                                                    fileName: "src/components/IT20131456/user/CreateRequest.js",
                                                    lineNumber: 53,
                                                    columnNumber: 17
                                                }, undefined),
                                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("input", {
                                                    type: "text",
                                                    className: "form-control",
                                                    name: "groupid",
                                                    pattern: "[G]+[0-9]{3}",
                                                    placeholder: "Enter group id",
                                                    required: true
                                                }, void 0, false, {
                                                    fileName: "src/components/IT20131456/user/CreateRequest.js",
                                                    lineNumber: 54,
                                                    columnNumber: 17
                                                }, undefined)
                                            ]
                                        }, void 0, true, {
                                            fileName: "src/components/IT20131456/user/CreateRequest.js",
                                            lineNumber: 52,
                                            columnNumber: 15
                                        }, undefined)
                                    }, void 0, false, {
                                        fileName: "src/components/IT20131456/user/CreateRequest.js",
                                        lineNumber: 51,
                                        columnNumber: 13
                                    }, undefined)
                                ]
                            }, void 0, true, {
                                fileName: "src/components/IT20131456/user/CreateRequest.js",
                                lineNumber: 38,
                                columnNumber: 11
                            }, undefined),
                            "\xa0",
                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                className: "row",
                                children: [
                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                        className: "col-md-6",
                                        children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                            className: "form-group",
                                            children: [
                                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("strong", {
                                                    children: "Subject :"
                                                }, void 0, false, {
                                                    fileName: "src/components/IT20131456/user/CreateRequest.js",
                                                    lineNumber: 69,
                                                    columnNumber: 17
                                                }, undefined),
                                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("select", {
                                                    className: "form-control",
                                                    name: "subject",
                                                    children: [
                                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("option", {
                                                            value: "Not",
                                                            children: "Not Selected"
                                                        }, void 0, false, {
                                                            fileName: "src/components/IT20131456/user/CreateRequest.js",
                                                            lineNumber: 71,
                                                            columnNumber: 19
                                                        }, undefined),
                                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("option", {
                                                            value: "Selecting Supervisor for a Research Project",
                                                            children: "Selecting Supervisor for a Research Project"
                                                        }, void 0, false, {
                                                            fileName: "src/components/IT20131456/user/CreateRequest.js",
                                                            lineNumber: 72,
                                                            columnNumber: 19
                                                        }, undefined),
                                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("option", {
                                                            value: "Selecting Co Supervisor for a Research Project",
                                                            children: "Selecting Co Supervisor for a Research Project"
                                                        }, void 0, false, {
                                                            fileName: "src/components/IT20131456/user/CreateRequest.js",
                                                            lineNumber: 75,
                                                            columnNumber: 19
                                                        }, undefined)
                                                    ]
                                                }, void 0, true, {
                                                    fileName: "src/components/IT20131456/user/CreateRequest.js",
                                                    lineNumber: 70,
                                                    columnNumber: 17
                                                }, undefined)
                                            ]
                                        }, void 0, true, {
                                            fileName: "src/components/IT20131456/user/CreateRequest.js",
                                            lineNumber: 68,
                                            columnNumber: 15
                                        }, undefined)
                                    }, void 0, false, {
                                        fileName: "src/components/IT20131456/user/CreateRequest.js",
                                        lineNumber: 67,
                                        columnNumber: 13
                                    }, undefined),
                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                        className: "col-md-6",
                                        children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                            className: "form-group",
                                            children: [
                                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("strong", {
                                                    children: "Research Topic :"
                                                }, void 0, false, {
                                                    fileName: "src/components/IT20131456/user/CreateRequest.js",
                                                    lineNumber: 83,
                                                    columnNumber: 17
                                                }, undefined),
                                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("input", {
                                                    type: "text",
                                                    className: "form-control",
                                                    placeholder: "Enter research topic",
                                                    name: "topic",
                                                    required: true
                                                }, void 0, false, {
                                                    fileName: "src/components/IT20131456/user/CreateRequest.js",
                                                    lineNumber: 84,
                                                    columnNumber: 17
                                                }, undefined)
                                            ]
                                        }, void 0, true, {
                                            fileName: "src/components/IT20131456/user/CreateRequest.js",
                                            lineNumber: 82,
                                            columnNumber: 15
                                        }, undefined)
                                    }, void 0, false, {
                                        fileName: "src/components/IT20131456/user/CreateRequest.js",
                                        lineNumber: 81,
                                        columnNumber: 13
                                    }, undefined)
                                ]
                            }, void 0, true, {
                                fileName: "src/components/IT20131456/user/CreateRequest.js",
                                lineNumber: 66,
                                columnNumber: 11
                            }, undefined),
                            "\xa0",
                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                className: "col-md-6",
                                children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                    className: "form-group",
                                    children: [
                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("strong", {
                                            children: "Discription (word count-75) :"
                                        }, void 0, false, {
                                            fileName: "src/components/IT20131456/user/CreateRequest.js",
                                            lineNumber: 97,
                                            columnNumber: 15
                                        }, undefined),
                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("textarea", {
                                            type: "text",
                                            className: "form-control",
                                            rows: "3",
                                            maxLength: "75",
                                            name: "discription",
                                            placeholder: "Discription ",
                                            required: true
                                        }, void 0, false, {
                                            fileName: "src/components/IT20131456/user/CreateRequest.js",
                                            lineNumber: 98,
                                            columnNumber: 15
                                        }, undefined)
                                    ]
                                }, void 0, true, {
                                    fileName: "src/components/IT20131456/user/CreateRequest.js",
                                    lineNumber: 96,
                                    columnNumber: 13
                                }, undefined)
                            }, void 0, false, {
                                fileName: "src/components/IT20131456/user/CreateRequest.js",
                                lineNumber: 95,
                                columnNumber: 11
                            }, undefined),
                            "\xa0",
                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                className: "col-md-12",
                                children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                    className: "form-group",
                                    children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("button", {
                                        className: "btn btn-outline-primary",
                                        type: "submit",
                                        value: "send",
                                        children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("i", {
                                            className: "fa fa-paper-plane",
                                            children: " Send "
                                        }, void 0, false, {
                                            fileName: "src/components/IT20131456/user/CreateRequest.js",
                                            lineNumber: 117,
                                            columnNumber: 17
                                        }, undefined)
                                    }, void 0, false, {
                                        fileName: "src/components/IT20131456/user/CreateRequest.js",
                                        lineNumber: 112,
                                        columnNumber: 15
                                    }, undefined)
                                }, void 0, false, {
                                    fileName: "src/components/IT20131456/user/CreateRequest.js",
                                    lineNumber: 111,
                                    columnNumber: 13
                                }, undefined)
                            }, void 0, false, {
                                fileName: "src/components/IT20131456/user/CreateRequest.js",
                                lineNumber: 110,
                                columnNumber: 11
                            }, undefined),
                            "\xa0"
                        ]
                    }, void 0, true, {
                        fileName: "src/components/IT20131456/user/CreateRequest.js",
                        lineNumber: 37,
                        columnNumber: 9
                    }, undefined)
                ]
            }, void 0, true, {
                fileName: "src/components/IT20131456/user/CreateRequest.js",
                lineNumber: 27,
                columnNumber: 7
            }, undefined)
        ]
    }, void 0, true, {
        fileName: "src/components/IT20131456/user/CreateRequest.js",
        lineNumber: 25,
        columnNumber: 5
    }, undefined);
};
_c = CreateRequest;
exports.default = CreateRequest;
var _c;
$RefreshReg$(_c, "CreateRequest");

  $parcel$ReactRefreshHelpers$8620.postlude(module);
} finally {
  window.$RefreshReg$ = prevRefreshReg;
  window.$RefreshSig$ = prevRefreshSig;
}
},{"react/jsx-dev-runtime":"iTorj","react":"21dqq","emailjs-com":"18oDj","@parcel/transformer-js/src/esmodule-helpers.js":"gkKU3","@parcel/transformer-react-refresh-wrap/lib/helpers/helpers.js":"km3Ru"}],"18oDj":[function(require,module,exports) {
var parcelHelpers = require("@parcel/transformer-js/src/esmodule-helpers.js");
parcelHelpers.defineInteropFlag(exports);
parcelHelpers.export(exports, "init", ()=>_init.init
);
parcelHelpers.export(exports, "send", ()=>_send.send
);
parcelHelpers.export(exports, "sendForm", ()=>_sendForm.sendForm
);
var _init = require("./methods/init/init");
var _send = require("./methods/send/send");
var _sendForm = require("./methods/sendForm/sendForm");
exports.default = {
    init: _init.init,
    send: _send.send,
    sendForm: _sendForm.sendForm
};

},{"./methods/init/init":"67aa8","./methods/send/send":"8d53v","./methods/sendForm/sendForm":"5YW33","@parcel/transformer-js/src/esmodule-helpers.js":"gkKU3"}],"67aa8":[function(require,module,exports) {
var parcelHelpers = require("@parcel/transformer-js/src/esmodule-helpers.js");
parcelHelpers.defineInteropFlag(exports);
parcelHelpers.export(exports, "init", ()=>init
);
var _store = require("../../store/store");
const init = (userID, origin = 'https://api.emailjs.com')=>{
    _store.store._userID = userID;
    _store.store._origin = origin;
};

},{"../../store/store":"bpIqO","@parcel/transformer-js/src/esmodule-helpers.js":"gkKU3"}],"bpIqO":[function(require,module,exports) {
var parcelHelpers = require("@parcel/transformer-js/src/esmodule-helpers.js");
parcelHelpers.defineInteropFlag(exports);
parcelHelpers.export(exports, "store", ()=>store
);
const store = {
    _origin: 'https://api.emailjs.com'
};

},{"@parcel/transformer-js/src/esmodule-helpers.js":"gkKU3"}],"8d53v":[function(require,module,exports) {
var parcelHelpers = require("@parcel/transformer-js/src/esmodule-helpers.js");
parcelHelpers.defineInteropFlag(exports);
parcelHelpers.export(exports, "send", ()=>send
);
var _store = require("../../store/store");
var _validateParams = require("../../utils/validateParams");
var _sendPost = require("../../api/sendPost");
const send = (serviceID, templateID, templatePrams, userID)=>{
    const uID = userID || _store.store._userID;
    _validateParams.validateParams(uID, serviceID, templateID);
    const params = {
        lib_version: '3.2.0',
        user_id: uID,
        service_id: serviceID,
        template_id: templateID,
        template_params: templatePrams
    };
    return _sendPost.sendPost('/api/v1.0/email/send', JSON.stringify(params), {
        'Content-type': 'application/json'
    });
};

},{"../../store/store":"bpIqO","../../utils/validateParams":"l47ah","../../api/sendPost":"etqkd","@parcel/transformer-js/src/esmodule-helpers.js":"gkKU3"}],"l47ah":[function(require,module,exports) {
var parcelHelpers = require("@parcel/transformer-js/src/esmodule-helpers.js");
parcelHelpers.defineInteropFlag(exports);
parcelHelpers.export(exports, "validateParams", ()=>validateParams
);
const validateParams = (userID, serviceID, templateID)=>{
    if (!userID) throw 'The user ID is required. Visit https://dashboard.emailjs.com/admin/integration';
    if (!serviceID) throw 'The service ID is required. Visit https://dashboard.emailjs.com/admin';
    if (!templateID) throw 'The template ID is required. Visit https://dashboard.emailjs.com/admin/templates';
    return true;
};

},{"@parcel/transformer-js/src/esmodule-helpers.js":"gkKU3"}],"etqkd":[function(require,module,exports) {
var parcelHelpers = require("@parcel/transformer-js/src/esmodule-helpers.js");
parcelHelpers.defineInteropFlag(exports);
parcelHelpers.export(exports, "sendPost", ()=>sendPost
);
var _emailJSResponseStatus = require("../models/EmailJSResponseStatus");
var _store = require("../store/store");
const sendPost = (url, data, headers = {})=>{
    return new Promise((resolve, reject)=>{
        const xhr = new XMLHttpRequest();
        xhr.addEventListener('load', ({ target  })=>{
            const responseStatus = new _emailJSResponseStatus.EmailJSResponseStatus(target);
            if (responseStatus.status === 200 || responseStatus.text === 'OK') resolve(responseStatus);
            else reject(responseStatus);
        });
        xhr.addEventListener('error', ({ target  })=>{
            reject(new _emailJSResponseStatus.EmailJSResponseStatus(target));
        });
        xhr.open('POST', _store.store._origin + url, true);
        Object.keys(headers).forEach((key)=>{
            xhr.setRequestHeader(key, headers[key]);
        });
        xhr.send(data);
    });
};

},{"../models/EmailJSResponseStatus":"7E7PA","../store/store":"bpIqO","@parcel/transformer-js/src/esmodule-helpers.js":"gkKU3"}],"7E7PA":[function(require,module,exports) {
var parcelHelpers = require("@parcel/transformer-js/src/esmodule-helpers.js");
parcelHelpers.defineInteropFlag(exports);
parcelHelpers.export(exports, "EmailJSResponseStatus", ()=>EmailJSResponseStatus
);
class EmailJSResponseStatus {
    constructor(httpResponse){
        this.status = httpResponse.status;
        this.text = httpResponse.responseText;
    }
}

},{"@parcel/transformer-js/src/esmodule-helpers.js":"gkKU3"}],"5YW33":[function(require,module,exports) {
var parcelHelpers = require("@parcel/transformer-js/src/esmodule-helpers.js");
parcelHelpers.defineInteropFlag(exports);
parcelHelpers.export(exports, "sendForm", ()=>sendForm
);
var _store = require("../../store/store");
var _validateParams = require("../../utils/validateParams");
var _sendPost = require("../../api/sendPost");
const findHTMLForm = (form)=>{
    let currentForm;
    if (typeof form === 'string') currentForm = document.querySelector(form);
    else currentForm = form;
    if (!currentForm || currentForm.nodeName !== 'FORM') throw 'The 3rd parameter is expected to be the HTML form element or the style selector of form';
    return currentForm;
};
const sendForm = (serviceID, templateID, form, userID)=>{
    const uID = userID || _store.store._userID;
    const currentForm = findHTMLForm(form);
    _validateParams.validateParams(uID, serviceID, templateID);
    const formData = new FormData(currentForm);
    formData.append('lib_version', '3.2.0');
    formData.append('service_id', serviceID);
    formData.append('template_id', templateID);
    formData.append('user_id', uID);
    return _sendPost.sendPost('/api/v1.0/email/send-form', formData);
};

},{"../../store/store":"bpIqO","../../utils/validateParams":"l47ah","../../api/sendPost":"etqkd","@parcel/transformer-js/src/esmodule-helpers.js":"gkKU3"}],"e1MVi":[function(require,module,exports) {
var $parcel$ReactRefreshHelpers$c9ee = require("@parcel/transformer-react-refresh-wrap/lib/helpers/helpers.js");
var prevRefreshReg = window.$RefreshReg$;
var prevRefreshSig = window.$RefreshSig$;
$parcel$ReactRefreshHelpers$c9ee.prelude(module);

try {
var parcelHelpers = require("@parcel/transformer-js/src/esmodule-helpers.js");
parcelHelpers.defineInteropFlag(exports);
var _jsxDevRuntime = require("react/jsx-dev-runtime");
var _react = require("react");
var _reactDefault = parcelHelpers.interopDefault(_react);
var _axios = require("axios");
var _axiosDefault = parcelHelpers.interopDefault(_axios);
var _jwtDecode = require("jwt-decode");
var _jwtDecodeDefault = parcelHelpers.interopDefault(_jwtDecode);
class RequestSummary extends _react.Component {
    constructor(props){
        super(props);
        this.state = {
            requestsummary: []
        };
    }
    componentDidMount() {
        if (localStorage.userToken) {
            const usertoken = localStorage.userToken;
            const decoded = _jwtDecodeDefault.default(usertoken);
            this.setState({
                groupId: decoded.groupId
            });
        }
        setTimeout(()=>{
            this.retriverequest();
        }, 1000);
    }
    retriverequest() {
        const gId = this.state.groupId;
        console.log(gId);
        _axiosDefault.default.get(`http://localhost:5000/request/summary/${gId}`).then((res)=>{
            if (res.data.success) {
                this.setState({
                    requestsummary: res.data.supervisorrequest
                });
                console.log(this.state.requestsummary);
            }
        });
    }
    render() {
        return /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
            className: "container px-5 my-3",
            children: [
                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                    className: "row",
                    children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                        className: "float-left col-lg-9 mt-2 mb-2",
                        children: [
                            "\xa0",
                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("h2", {
                                children: [
                                    "Request Summary - ",
                                    this.state.groupId,
                                    " "
                                ]
                            }, void 0, true, {
                                fileName: "src/components/IT20131456/user/RequestSummary.js",
                                lineNumber: 51,
                                columnNumber: 13
                            }, this),
                            "\xa0"
                        ]
                    }, void 0, true, {
                        fileName: "src/components/IT20131456/user/RequestSummary.js",
                        lineNumber: 49,
                        columnNumber: 11
                    }, this)
                }, void 0, false, {
                    fileName: "src/components/IT20131456/user/RequestSummary.js",
                    lineNumber: 48,
                    columnNumber: 9
                }, this),
                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("table", {
                    className: "table ",
                    children: [
                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("thead", {
                            children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("tr", {
                                children: [
                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("th", {
                                        scope: "col",
                                        children: "#"
                                    }, void 0, false, {
                                        fileName: "src/components/IT20131456/user/RequestSummary.js",
                                        lineNumber: 59,
                                        columnNumber: 15
                                    }, this),
                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("th", {
                                        scope: "col",
                                        children: "Name"
                                    }, void 0, false, {
                                        fileName: "src/components/IT20131456/user/RequestSummary.js",
                                        lineNumber: 60,
                                        columnNumber: 15
                                    }, this),
                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("th", {
                                        scope: "col",
                                        children: "Position"
                                    }, void 0, false, {
                                        fileName: "src/components/IT20131456/user/RequestSummary.js",
                                        lineNumber: 61,
                                        columnNumber: 15
                                    }, this),
                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("th", {
                                        scope: "col",
                                        children: "Feedback"
                                    }, void 0, false, {
                                        fileName: "src/components/IT20131456/user/RequestSummary.js",
                                        lineNumber: 62,
                                        columnNumber: 15
                                    }, this),
                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("th", {
                                        scope: "col",
                                        children: "Status"
                                    }, void 0, false, {
                                        fileName: "src/components/IT20131456/user/RequestSummary.js",
                                        lineNumber: 63,
                                        columnNumber: 15
                                    }, this)
                                ]
                            }, void 0, true, {
                                fileName: "src/components/IT20131456/user/RequestSummary.js",
                                lineNumber: 58,
                                columnNumber: 13
                            }, this)
                        }, void 0, false, {
                            fileName: "src/components/IT20131456/user/RequestSummary.js",
                            lineNumber: 57,
                            columnNumber: 11
                        }, this),
                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("tbody", {
                            children: this.state.requestsummary.map((requestsummary, index)=>/*#__PURE__*/ _jsxDevRuntime.jsxDEV("tr", {
                                    children: [
                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("th", {
                                            children: index + 1
                                        }, void 0, false, {
                                            fileName: "src/components/IT20131456/user/RequestSummary.js",
                                            lineNumber: 69,
                                            columnNumber: 17
                                        }, this),
                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("td", {
                                            children: requestsummary.name
                                        }, void 0, false, {
                                            fileName: "src/components/IT20131456/user/RequestSummary.js",
                                            lineNumber: 70,
                                            columnNumber: 17
                                        }, this),
                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("td", {
                                            children: requestsummary.position
                                        }, void 0, false, {
                                            fileName: "src/components/IT20131456/user/RequestSummary.js",
                                            lineNumber: 71,
                                            columnNumber: 17
                                        }, this),
                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("td", {
                                            children: requestsummary.feedback
                                        }, void 0, false, {
                                            fileName: "src/components/IT20131456/user/RequestSummary.js",
                                            lineNumber: 72,
                                            columnNumber: 17
                                        }, this),
                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("td", {
                                            children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("a", {
                                                className: "btn btn-secondary disabled",
                                                href: "",
                                                children: requestsummary.status
                                            }, void 0, false, {
                                                fileName: "src/components/IT20131456/user/RequestSummary.js",
                                                lineNumber: 74,
                                                columnNumber: 17
                                            }, this)
                                        }, void 0, false, {
                                            fileName: "src/components/IT20131456/user/RequestSummary.js",
                                            lineNumber: 73,
                                            columnNumber: 17
                                        }, this)
                                    ]
                                }, void 0, true, {
                                    fileName: "src/components/IT20131456/user/RequestSummary.js",
                                    lineNumber: 68,
                                    columnNumber: 15
                                }, this)
                            )
                        }, void 0, false, {
                            fileName: "src/components/IT20131456/user/RequestSummary.js",
                            lineNumber: 66,
                            columnNumber: 11
                        }, this)
                    ]
                }, void 0, true, {
                    fileName: "src/components/IT20131456/user/RequestSummary.js",
                    lineNumber: 56,
                    columnNumber: 9
                }, this)
            ]
        }, void 0, true, {
            fileName: "src/components/IT20131456/user/RequestSummary.js",
            lineNumber: 47,
            columnNumber: 7
        }, this);
    }
}
exports.default = RequestSummary;

  $parcel$ReactRefreshHelpers$c9ee.postlude(module);
} finally {
  window.$RefreshReg$ = prevRefreshReg;
  window.$RefreshSig$ = prevRefreshSig;
}
},{"react/jsx-dev-runtime":"iTorj","react":"21dqq","axios":"jo6P5","jwt-decode":"ljM1w","@parcel/transformer-js/src/esmodule-helpers.js":"gkKU3","@parcel/transformer-react-refresh-wrap/lib/helpers/helpers.js":"km3Ru"}],"aqqmv":[function(require,module,exports) {
var $parcel$ReactRefreshHelpers$a668 = require("@parcel/transformer-react-refresh-wrap/lib/helpers/helpers.js");
var prevRefreshReg = window.$RefreshReg$;
var prevRefreshSig = window.$RefreshSig$;
$parcel$ReactRefreshHelpers$a668.prelude(module);

try {
var parcelHelpers = require("@parcel/transformer-js/src/esmodule-helpers.js");
parcelHelpers.defineInteropFlag(exports);
var _jsxDevRuntime = require("react/jsx-dev-runtime");
// admin - update users
var _react = require("react");
var _reactDefault = parcelHelpers.interopDefault(_react);
var _adminNavBar = require("../admin/AdminNavBar");
var _adminNavBarDefault = parcelHelpers.interopDefault(_adminNavBar);
var _axios = require("axios");
var _axiosDefault = parcelHelpers.interopDefault(_axios);
var _sweetalert = require("sweetalert");
var _sweetalertDefault = parcelHelpers.interopDefault(_sweetalert);
class EditUser extends _react.Component {
    constructor(props){
        super(props);
        this.state = {
            idNumber: "",
            name: "",
            email: "",
            mobile: "",
            groupId: "",
            researchfield: "",
            panel: "",
            type: "",
            password: ""
        };
    }
    handleInputChange = (e)=>{
        const { name , value  } = e.target;
        this.setState({
            ...this.state,
            [name]: value
        });
    };
    onSubmit = (e)=>{
        e.preventDefault();
        const id = this.props.match.params.id;
        const { idNumber , name , email , mobile , groupId , researchfield , panel , type , password  } = this.state;
        const data = {
            idNumber: idNumber,
            name: name,
            email: email,
            mobile: mobile,
            groupId: groupId,
            researchfield: researchfield,
            panel: panel,
            type: type,
            password: password
        };
        // validations-------------------------------------------------------------
        let validated = true;
        if (data.idNumber === '') {
            validated = false;
            _sweetalertDefault.default({
                title: "",
                text: "ID number cannot be empty",
                icon: "warning"
            });
        } else if (data.name === '' || data.name.length < 5) {
            validated = false;
            _sweetalertDefault.default({
                title: "",
                text: "Name cannot be empty",
                icon: "warning"
            });
        } else if (!data.email.match(/^(([^<>()\[\]\.,;:\s@\"]+(\.[^<>()\[\]\.,;:\s@\"]+)*)|(\".+\"))@(([^<>()[\]\.,;:\s@\"]+\.)+[^<>()[\]\.,;:\s@\"]{2,})$/i)) {
            validated = false;
            _sweetalertDefault.default({
                title: "",
                text: "Please enter a valid email",
                icon: "warning"
            });
        } else if (!data.mobile.match(/^(\+\d{1,3}[- ]?)?\d{10}$/)) {
            validated = false;
            _sweetalertDefault.default({
                title: "",
                text: "Please enter a valid mobile number",
                icon: "warning"
            });
        }
        // console.log(data)
        if (validated) _axiosDefault.default.put(`http://localhost:5000/user/update/${id}`, data).then((res)=>{
            if (res.data.success) _sweetalertDefault.default("User details updated successfully!", "", "success").then((value)=>{
                if (value) {
                    this.props.history.push(`/admin/users`);
                    window.location.reload();
                }
            });
        });
    };
    componentDidMount() {
        document.title = "Edit user details";
        // redirect to the login page if the user is not logged in
        if (!localStorage.token) _sweetalertDefault.default("Please login first", "", "warning").then((value)=>{
            if (value) {
                this.props.history.push(`/admin/login`);
                window.location.reload();
            }
        });
        const id = this.props.match.params.id;
        _axiosDefault.default.get(`http://localhost:5000/user/${id}`).then((res)=>{
            if (res.data.success) this.setState({
                idNumber: res.data.user.idNumber,
                name: res.data.user.name,
                email: res.data.user.email,
                mobile: res.data.user.mobile,
                groupId: res.data.user.groupId,
                researchfield: res.data.user.researchfield,
                panel: res.data.user.panel,
                type: res.data.user.type,
                password: res.data.user.password
            });
        });
    }
    render() {
        return /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
            className: "container",
            children: [
                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("br", {}, void 0, false, {
                    fileName: "src/components/IT20125202/user/EditUser.js",
                    lineNumber: 149,
                    columnNumber: 9
                }, this),
                /*#__PURE__*/ _jsxDevRuntime.jsxDEV(_adminNavBarDefault.default, {}, void 0, false, {
                    fileName: "src/components/IT20125202/user/EditUser.js",
                    lineNumber: 150,
                    columnNumber: 9
                }, this),
                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("br", {}, void 0, false, {
                    fileName: "src/components/IT20125202/user/EditUser.js",
                    lineNumber: 151,
                    columnNumber: 9
                }, this),
                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                    className: "container",
                    children: [
                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                            className: "row",
                            children: [
                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                    className: "col-lg-9 mt-2 mb-2",
                                    children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("h1", {
                                        children: "User Details"
                                    }, void 0, false, {
                                        fileName: "src/components/IT20125202/user/EditUser.js",
                                        lineNumber: 155,
                                        columnNumber: 15
                                    }, this)
                                }, void 0, false, {
                                    fileName: "src/components/IT20125202/user/EditUser.js",
                                    lineNumber: 154,
                                    columnNumber: 13
                                }, this),
                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("hr", {}, void 0, false, {
                                    fileName: "src/components/IT20125202/user/EditUser.js",
                                    lineNumber: 157,
                                    columnNumber: 13
                                }, this),
                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("br", {}, void 0, false, {
                                    fileName: "src/components/IT20125202/user/EditUser.js",
                                    lineNumber: 157,
                                    columnNumber: 19
                                }, this)
                            ]
                        }, void 0, true, {
                            fileName: "src/components/IT20125202/user/EditUser.js",
                            lineNumber: 153,
                            columnNumber: 11
                        }, this),
                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                            className: "col-md-8 mt-4 mx-auto",
                            children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("form", {
                                className: "needs-validation",
                                noValidate: true,
                                children: [
                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                        className: "form-group",
                                        style: {
                                            marginBottom: '15px'
                                        },
                                        children: [
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("label", {
                                                style: {
                                                    marginBottom: '5px'
                                                },
                                                children: "ID number"
                                            }, void 0, false, {
                                                fileName: "src/components/IT20125202/user/EditUser.js",
                                                lineNumber: 162,
                                                columnNumber: 17
                                            }, this),
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("input", {
                                                type: "text",
                                                className: "form-control",
                                                name: "idNumber",
                                                placeholder: "",
                                                value: this.state.idNumber,
                                                onChange: this.handleInputChange
                                            }, void 0, false, {
                                                fileName: "src/components/IT20125202/user/EditUser.js",
                                                lineNumber: 163,
                                                columnNumber: 17
                                            }, this)
                                        ]
                                    }, void 0, true, {
                                        fileName: "src/components/IT20125202/user/EditUser.js",
                                        lineNumber: 161,
                                        columnNumber: 15
                                    }, this),
                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                        className: "form-group",
                                        style: {
                                            marginBottom: '15px'
                                        },
                                        children: [
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("label", {
                                                style: {
                                                    marginBottom: '5px'
                                                },
                                                children: "Name with Initials"
                                            }, void 0, false, {
                                                fileName: "src/components/IT20125202/user/EditUser.js",
                                                lineNumber: 174,
                                                columnNumber: 17
                                            }, this),
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("input", {
                                                type: "text",
                                                className: "form-control",
                                                name: "name",
                                                placeholder: "",
                                                value: this.state.name,
                                                onChange: this.handleInputChange
                                            }, void 0, false, {
                                                fileName: "src/components/IT20125202/user/EditUser.js",
                                                lineNumber: 175,
                                                columnNumber: 17
                                            }, this)
                                        ]
                                    }, void 0, true, {
                                        fileName: "src/components/IT20125202/user/EditUser.js",
                                        lineNumber: 173,
                                        columnNumber: 15
                                    }, this),
                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                        className: "form-group",
                                        style: {
                                            marginBottom: '15px'
                                        },
                                        children: [
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("label", {
                                                style: {
                                                    marginBottom: '5px'
                                                },
                                                children: "Email"
                                            }, void 0, false, {
                                                fileName: "src/components/IT20125202/user/EditUser.js",
                                                lineNumber: 186,
                                                columnNumber: 17
                                            }, this),
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("input", {
                                                type: "email",
                                                className: "form-control",
                                                name: "email",
                                                placeholder: "",
                                                value: this.state.email,
                                                onChange: this.handleInputChange
                                            }, void 0, false, {
                                                fileName: "src/components/IT20125202/user/EditUser.js",
                                                lineNumber: 187,
                                                columnNumber: 17
                                            }, this)
                                        ]
                                    }, void 0, true, {
                                        fileName: "src/components/IT20125202/user/EditUser.js",
                                        lineNumber: 185,
                                        columnNumber: 15
                                    }, this),
                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                        className: "form-group",
                                        style: {
                                            marginBottom: '15px'
                                        },
                                        children: [
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("label", {
                                                style: {
                                                    marginBottom: '5px'
                                                },
                                                children: "Mobile"
                                            }, void 0, false, {
                                                fileName: "src/components/IT20125202/user/EditUser.js",
                                                lineNumber: 198,
                                                columnNumber: 17
                                            }, this),
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("input", {
                                                type: "email",
                                                className: "form-control",
                                                name: "mobile",
                                                placeholder: "",
                                                value: this.state.mobile,
                                                onChange: this.handleInputChange
                                            }, void 0, false, {
                                                fileName: "src/components/IT20125202/user/EditUser.js",
                                                lineNumber: 199,
                                                columnNumber: 17
                                            }, this)
                                        ]
                                    }, void 0, true, {
                                        fileName: "src/components/IT20125202/user/EditUser.js",
                                        lineNumber: 197,
                                        columnNumber: 15
                                    }, this),
                                    this.state.type === 'Student' && /*#__PURE__*/ _jsxDevRuntime.jsxDEV("span", {
                                        children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                            className: "form-group",
                                            style: {
                                                marginBottom: '15px'
                                            },
                                            children: [
                                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("label", {
                                                    style: {
                                                        marginBottom: '5px'
                                                    },
                                                    children: "Group ID"
                                                }, void 0, false, {
                                                    fileName: "src/components/IT20125202/user/EditUser.js",
                                                    lineNumber: 212,
                                                    columnNumber: 21
                                                }, this),
                                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("input", {
                                                    type: "text",
                                                    className: "form-control",
                                                    name: "groupId",
                                                    value: this.state.groupId,
                                                    onChange: this.handleInputChange
                                                }, void 0, false, {
                                                    fileName: "src/components/IT20125202/user/EditUser.js",
                                                    lineNumber: 213,
                                                    columnNumber: 21
                                                }, this)
                                            ]
                                        }, void 0, true, {
                                            fileName: "src/components/IT20125202/user/EditUser.js",
                                            lineNumber: 211,
                                            columnNumber: 19
                                        }, this)
                                    }, void 0, false, {
                                        fileName: "src/components/IT20125202/user/EditUser.js",
                                        lineNumber: 210,
                                        columnNumber: 17
                                    }, this),
                                    this.state.type === 'Supervisor' && /*#__PURE__*/ _jsxDevRuntime.jsxDEV("span", {
                                        children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                            className: "form-group",
                                            style: {
                                                marginBottom: '15px'
                                            },
                                            children: [
                                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("label", {
                                                    style: {
                                                        marginBottom: '5px'
                                                    },
                                                    children: "Research Field"
                                                }, void 0, false, {
                                                    fileName: "src/components/IT20125202/user/EditUser.js",
                                                    lineNumber: 226,
                                                    columnNumber: 21
                                                }, this),
                                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("input", {
                                                    type: "text",
                                                    className: "form-control",
                                                    name: "researchfield",
                                                    value: this.state.researchfield,
                                                    onChange: this.handleInputChange
                                                }, void 0, false, {
                                                    fileName: "src/components/IT20125202/user/EditUser.js",
                                                    lineNumber: 227,
                                                    columnNumber: 21
                                                }, this)
                                            ]
                                        }, void 0, true, {
                                            fileName: "src/components/IT20125202/user/EditUser.js",
                                            lineNumber: 225,
                                            columnNumber: 19
                                        }, this)
                                    }, void 0, false, {
                                        fileName: "src/components/IT20125202/user/EditUser.js",
                                        lineNumber: 224,
                                        columnNumber: 17
                                    }, this),
                                    this.state.type === 'Panel Member' && /*#__PURE__*/ _jsxDevRuntime.jsxDEV("span", {
                                        children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                            className: "form-group",
                                            style: {
                                                marginBottom: '15px'
                                            },
                                            children: [
                                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("label", {
                                                    style: {
                                                        marginBottom: '5px'
                                                    },
                                                    children: "Panel"
                                                }, void 0, false, {
                                                    fileName: "src/components/IT20125202/user/EditUser.js",
                                                    lineNumber: 240,
                                                    columnNumber: 21
                                                }, this),
                                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("input", {
                                                    type: "text",
                                                    className: "form-control",
                                                    name: "panel",
                                                    value: this.state.panel,
                                                    onChange: this.handleInputChange
                                                }, void 0, false, {
                                                    fileName: "src/components/IT20125202/user/EditUser.js",
                                                    lineNumber: 241,
                                                    columnNumber: 21
                                                }, this)
                                            ]
                                        }, void 0, true, {
                                            fileName: "src/components/IT20125202/user/EditUser.js",
                                            lineNumber: 239,
                                            columnNumber: 19
                                        }, this)
                                    }, void 0, false, {
                                        fileName: "src/components/IT20125202/user/EditUser.js",
                                        lineNumber: 238,
                                        columnNumber: 17
                                    }, this),
                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                        className: "form-group",
                                        style: {
                                            marginBottom: '15px'
                                        },
                                        children: [
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("label", {
                                                style: {
                                                    marginBottom: '5px'
                                                },
                                                children: "Type"
                                            }, void 0, false, {
                                                fileName: "src/components/IT20125202/user/EditUser.js",
                                                lineNumber: 253,
                                                columnNumber: 17
                                            }, this),
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("input", {
                                                type: "text",
                                                className: "form-control",
                                                name: "type",
                                                value: this.state.type,
                                                onChange: this.handleInputChange,
                                                readOnly: true
                                            }, void 0, false, {
                                                fileName: "src/components/IT20125202/user/EditUser.js",
                                                lineNumber: 254,
                                                columnNumber: 17
                                            }, this)
                                        ]
                                    }, void 0, true, {
                                        fileName: "src/components/IT20125202/user/EditUser.js",
                                        lineNumber: 252,
                                        columnNumber: 15
                                    }, this),
                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("button", {
                                        className: "btn btn-success",
                                        type: "submit",
                                        style: {
                                            maeginTop: '15px'
                                        },
                                        onClick: this.onSubmit,
                                        children: [
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("i", {
                                                className: "far fa-check-square"
                                            }, void 0, false, {
                                                fileName: "src/components/IT20125202/user/EditUser.js",
                                                lineNumber: 278,
                                                columnNumber: 17
                                            }, this),
                                            "\xa0 Update"
                                        ]
                                    }, void 0, true, {
                                        fileName: "src/components/IT20125202/user/EditUser.js",
                                        lineNumber: 277,
                                        columnNumber: 15
                                    }, this),
                                    "\xa0\xa0",
                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("a", {
                                        href: "/admin/users",
                                        class: "btn btn-outline-success",
                                        tabindex: "-1",
                                        role: "button",
                                        "aria-disabled": "true",
                                        children: "Back"
                                    }, void 0, false, {
                                        fileName: "src/components/IT20125202/user/EditUser.js",
                                        lineNumber: 282,
                                        columnNumber: 15
                                    }, this)
                                ]
                            }, void 0, true, {
                                fileName: "src/components/IT20125202/user/EditUser.js",
                                lineNumber: 160,
                                columnNumber: 13
                            }, this)
                        }, void 0, false, {
                            fileName: "src/components/IT20125202/user/EditUser.js",
                            lineNumber: 159,
                            columnNumber: 11
                        }, this)
                    ]
                }, void 0, true, {
                    fileName: "src/components/IT20125202/user/EditUser.js",
                    lineNumber: 152,
                    columnNumber: 9
                }, this)
            ]
        }, void 0, true, {
            fileName: "src/components/IT20125202/user/EditUser.js",
            lineNumber: 148,
            columnNumber: 7
        }, this);
    }
}
exports.default = EditUser;

  $parcel$ReactRefreshHelpers$a668.postlude(module);
} finally {
  window.$RefreshReg$ = prevRefreshReg;
  window.$RefreshSig$ = prevRefreshSig;
}
},{"react/jsx-dev-runtime":"iTorj","react":"21dqq","../admin/AdminNavBar":"kb2lK","axios":"jo6P5","sweetalert":"38a65","@parcel/transformer-js/src/esmodule-helpers.js":"gkKU3","@parcel/transformer-react-refresh-wrap/lib/helpers/helpers.js":"km3Ru"}],"kb2lK":[function(require,module,exports) {
var $parcel$ReactRefreshHelpers$439c = require("@parcel/transformer-react-refresh-wrap/lib/helpers/helpers.js");
var prevRefreshReg = window.$RefreshReg$;
var prevRefreshSig = window.$RefreshSig$;
$parcel$ReactRefreshHelpers$439c.prelude(module);

try {
var parcelHelpers = require("@parcel/transformer-js/src/esmodule-helpers.js");
parcelHelpers.defineInteropFlag(exports);
var _jsxDevRuntime = require("react/jsx-dev-runtime");
var _react = require("react");
var _reactDefault = parcelHelpers.interopDefault(_react);
class AdminNavBar extends _react.Component {
    onlogout = (e)=>{
        swal({
            title: "Are you sure you want to log out?",
            text: "",
            icon: "warning",
            buttons: true,
            dangerMode: true
        }).then((willLogout)=>{
            if (willLogout) swal("Logout successfully!", "", "success").then((value)=>{
                if (value) {
                    window.history.forward();
                    window.location = "http://localhost:3000/admin/login";
                }
            });
            else swal("Redirecting...");
        });
        e.preventDefault();
    };
    render() {
        return /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
            children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("nav", {
                className: "navbar navbar-expand-lg navbar-light bg-light",
                children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                    className: "container-fluid",
                    children: [
                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("a", {
                            className: "navbar-brand",
                            href: "/admin/home",
                            children: "RPMS - Admin"
                        }, void 0, false, {
                            fileName: "src/components/IT20125202/admin/AdminNavBar.js",
                            lineNumber: 34,
                            columnNumber: 13
                        }, this),
                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("button", {
                            className: "navbar-toggler",
                            type: "button",
                            "data-bs-toggle": "collapse",
                            "data-bs-target": "#navbarSupportedContent",
                            "aria-controls": "navbarSupportedContent",
                            "aria-expanded": "false",
                            "aria-label": "Toggle navigation",
                            children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("span", {
                                className: "navbar-toggler-icon"
                            }, void 0, false, {
                                fileName: "src/components/IT20125202/admin/AdminNavBar.js",
                                lineNumber: 36,
                                columnNumber: 15
                            }, this)
                        }, void 0, false, {
                            fileName: "src/components/IT20125202/admin/AdminNavBar.js",
                            lineNumber: 35,
                            columnNumber: 13
                        }, this),
                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                            className: "collapse navbar-collapse",
                            id: "navbarSupportedContent",
                            children: [
                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("ul", {
                                    className: "navbar-nav me-auto mb-2 mb-lg-0",
                                    children: [
                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("li", {
                                            className: "nav-item",
                                            children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("a", {
                                                className: "nav-link active",
                                                "aria-current": "page",
                                                href: "/admin/home",
                                                children: "Home"
                                            }, void 0, false, {
                                                fileName: "src/components/IT20125202/admin/AdminNavBar.js",
                                                lineNumber: 41,
                                                columnNumber: 19
                                            }, this)
                                        }, void 0, false, {
                                            fileName: "src/components/IT20125202/admin/AdminNavBar.js",
                                            lineNumber: 40,
                                            columnNumber: 17
                                        }, this),
                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("li", {
                                            className: "nav-item",
                                            children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("a", {
                                                className: "nav-link",
                                                href: "/admin/users",
                                                children: "User Roles"
                                            }, void 0, false, {
                                                fileName: "src/components/IT20125202/admin/AdminNavBar.js",
                                                lineNumber: 44,
                                                columnNumber: 19
                                            }, this)
                                        }, void 0, false, {
                                            fileName: "src/components/IT20125202/admin/AdminNavBar.js",
                                            lineNumber: 43,
                                            columnNumber: 17
                                        }, this),
                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("li", {
                                            className: "nav-item",
                                            children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("a", {
                                                className: "nav-link",
                                                href: "/admin/topiclist",
                                                children: "Topics"
                                            }, void 0, false, {
                                                fileName: "src/components/IT20125202/admin/AdminNavBar.js",
                                                lineNumber: 47,
                                                columnNumber: 19
                                            }, this)
                                        }, void 0, false, {
                                            fileName: "src/components/IT20125202/admin/AdminNavBar.js",
                                            lineNumber: 46,
                                            columnNumber: 17
                                        }, this),
                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("li", {
                                            className: "nav-item",
                                            children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("a", {
                                                className: "nav-link",
                                                href: "/student/groups/view",
                                                children: "Student Groups"
                                            }, void 0, false, {
                                                fileName: "src/components/IT20125202/admin/AdminNavBar.js",
                                                lineNumber: 51,
                                                columnNumber: 19
                                            }, this)
                                        }, void 0, false, {
                                            fileName: "src/components/IT20125202/admin/AdminNavBar.js",
                                            lineNumber: 50,
                                            columnNumber: 18
                                        }, this),
                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("li", {
                                            className: "nav-item",
                                            children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("a", {
                                                className: "nav-link",
                                                href: "/submitiontype/add",
                                                children: "Submissions"
                                            }, void 0, false, {
                                                fileName: "src/components/IT20125202/admin/AdminNavBar.js",
                                                lineNumber: 55,
                                                columnNumber: 19
                                            }, this)
                                        }, void 0, false, {
                                            fileName: "src/components/IT20125202/admin/AdminNavBar.js",
                                            lineNumber: 54,
                                            columnNumber: 17
                                        }, this),
                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("li", {
                                            className: "nav-item",
                                            children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("a", {
                                                className: "nav-link",
                                                href: "/documentTemp",
                                                children: "Template"
                                            }, void 0, false, {
                                                fileName: "src/components/IT20125202/admin/AdminNavBar.js",
                                                lineNumber: 59,
                                                columnNumber: 19
                                            }, this)
                                        }, void 0, false, {
                                            fileName: "src/components/IT20125202/admin/AdminNavBar.js",
                                            lineNumber: 58,
                                            columnNumber: 17
                                        }, this),
                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("li", {
                                            className: "nav-item dropdown",
                                            children: [
                                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("a", {
                                                    className: "nav-link dropdown-toggle",
                                                    href: "#",
                                                    id: "navbarDropdown",
                                                    role: "button",
                                                    "data-bs-toggle": "dropdown",
                                                    "aria-expanded": "false",
                                                    children: "Manage Supervisors"
                                                }, void 0, false, {
                                                    fileName: "src/components/IT20125202/admin/AdminNavBar.js",
                                                    lineNumber: 63,
                                                    columnNumber: 19
                                                }, this),
                                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("ul", {
                                                    className: "dropdown-menu",
                                                    "aria-labelledby": "navbarDropdown",
                                                    children: [
                                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("li", {
                                                            children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("a", {
                                                                className: "dropdown-item",
                                                                href: "/supervisor/add",
                                                                children: "Add Supervisor Details"
                                                            }, void 0, false, {
                                                                fileName: "src/components/IT20125202/admin/AdminNavBar.js",
                                                                lineNumber: 67,
                                                                columnNumber: 25
                                                            }, this)
                                                        }, void 0, false, {
                                                            fileName: "src/components/IT20125202/admin/AdminNavBar.js",
                                                            lineNumber: 67,
                                                            columnNumber: 21
                                                        }, this),
                                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("li", {
                                                            children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("a", {
                                                                className: "dropdown-item",
                                                                href: "/supervisor/view",
                                                                children: "View Supervisor Details"
                                                            }, void 0, false, {
                                                                fileName: "src/components/IT20125202/admin/AdminNavBar.js",
                                                                lineNumber: 68,
                                                                columnNumber: 25
                                                            }, this)
                                                        }, void 0, false, {
                                                            fileName: "src/components/IT20125202/admin/AdminNavBar.js",
                                                            lineNumber: 68,
                                                            columnNumber: 21
                                                        }, this)
                                                    ]
                                                }, void 0, true, {
                                                    fileName: "src/components/IT20125202/admin/AdminNavBar.js",
                                                    lineNumber: 66,
                                                    columnNumber: 17
                                                }, this)
                                            ]
                                        }, void 0, true, {
                                            fileName: "src/components/IT20125202/admin/AdminNavBar.js",
                                            lineNumber: 62,
                                            columnNumber: 17
                                        }, this)
                                    ]
                                }, void 0, true, {
                                    fileName: "src/components/IT20125202/admin/AdminNavBar.js",
                                    lineNumber: 39,
                                    columnNumber: 15
                                }, this),
                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                    className: "btn-group dropstart",
                                    children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("ul", {
                                        className: "navbar-nav me-auto mb-2 mb-lg-0",
                                        children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("li", {
                                            className: "nav-item",
                                            children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("a", {
                                                className: "btn btn-outline-dark btn-sm",
                                                "aria-current": "page",
                                                onClick: this.onlogout,
                                                children: "Logout"
                                            }, void 0, false, {
                                                fileName: "src/components/IT20125202/admin/AdminNavBar.js",
                                                lineNumber: 82,
                                                columnNumber: 21
                                            }, this)
                                        }, void 0, false, {
                                            fileName: "src/components/IT20125202/admin/AdminNavBar.js",
                                            lineNumber: 81,
                                            columnNumber: 19
                                        }, this)
                                    }, void 0, false, {
                                        fileName: "src/components/IT20125202/admin/AdminNavBar.js",
                                        lineNumber: 80,
                                        columnNumber: 17
                                    }, this)
                                }, void 0, false, {
                                    fileName: "src/components/IT20125202/admin/AdminNavBar.js",
                                    lineNumber: 79,
                                    columnNumber: 15
                                }, this)
                            ]
                        }, void 0, true, {
                            fileName: "src/components/IT20125202/admin/AdminNavBar.js",
                            lineNumber: 38,
                            columnNumber: 13
                        }, this)
                    ]
                }, void 0, true, {
                    fileName: "src/components/IT20125202/admin/AdminNavBar.js",
                    lineNumber: 33,
                    columnNumber: 11
                }, this)
            }, void 0, false, {
                fileName: "src/components/IT20125202/admin/AdminNavBar.js",
                lineNumber: 32,
                columnNumber: 9
            }, this)
        }, void 0, false, {
            fileName: "src/components/IT20125202/admin/AdminNavBar.js",
            lineNumber: 31,
            columnNumber: 7
        }, this);
    }
}
exports.default = AdminNavBar;

  $parcel$ReactRefreshHelpers$439c.postlude(module);
} finally {
  window.$RefreshReg$ = prevRefreshReg;
  window.$RefreshSig$ = prevRefreshSig;
}
},{"react/jsx-dev-runtime":"iTorj","react":"21dqq","@parcel/transformer-js/src/esmodule-helpers.js":"gkKU3","@parcel/transformer-react-refresh-wrap/lib/helpers/helpers.js":"km3Ru"}],"aUJA5":[function(require,module,exports) {
var $parcel$ReactRefreshHelpers$84ba = require("@parcel/transformer-react-refresh-wrap/lib/helpers/helpers.js");
var prevRefreshReg = window.$RefreshReg$;
var prevRefreshSig = window.$RefreshSig$;
$parcel$ReactRefreshHelpers$84ba.prelude(module);

try {
var parcelHelpers = require("@parcel/transformer-js/src/esmodule-helpers.js");
parcelHelpers.defineInteropFlag(exports);
var _jsxDevRuntime = require("react/jsx-dev-runtime");
//admin - view specific user details
// user- view user profile
var _react = require("react");
var _reactDefault = parcelHelpers.interopDefault(_react);
var _axios = require("axios");
var _axiosDefault = parcelHelpers.interopDefault(_axios);
var _adminNavBar = require("../admin/AdminNavBar");
var _adminNavBarDefault = parcelHelpers.interopDefault(_adminNavBar);
var _sweetalert = require("sweetalert");
var _sweetalertDefault = parcelHelpers.interopDefault(_sweetalert);
class UserProfile extends _react.Component {
    constructor(props){
        super(props);
        this.state = {
            user: {}
        };
    }
    componentDidMount() {
        document.title = "User Details";
        // redirect to the login page if the user is not logged in
        if (!localStorage.token) _sweetalertDefault.default("Please login first", "", "warning").then((value)=>{
            if (value) {
                this.props.history.push(`/admin/login`);
                window.location.reload();
            }
        });
        const id = this.props.match.params.id;
        _axiosDefault.default.get(`http://localhost:5000/user/${id}`).then((res)=>{
            if (res.data.success) this.setState({
                user: res.data.user
            });
        });
    }
    render() {
        const { idNumber , name , email , groupId , type , dateRegistered  } = this.state.user;
        return /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
            className: "container",
            children: [
                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("br", {}, void 0, false, {
                    fileName: "src/components/IT20125202/user/UserDetails.js",
                    lineNumber: 51,
                    columnNumber: 17
                }, this),
                /*#__PURE__*/ _jsxDevRuntime.jsxDEV(_adminNavBarDefault.default, {}, void 0, false, {
                    fileName: "src/components/IT20125202/user/UserDetails.js",
                    lineNumber: 52,
                    columnNumber: 17
                }, this),
                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("br", {}, void 0, false, {
                    fileName: "src/components/IT20125202/user/UserDetails.js",
                    lineNumber: 53,
                    columnNumber: 17
                }, this),
                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                    className: "container",
                    children: [
                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                            className: "row",
                            children: [
                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                    className: "col-lg-9 mt-2 mb-2",
                                    children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("h1", {
                                        children: "User Details"
                                    }, void 0, false, {
                                        fileName: "src/components/IT20125202/user/UserDetails.js",
                                        lineNumber: 57,
                                        columnNumber: 29
                                    }, this)
                                }, void 0, false, {
                                    fileName: "src/components/IT20125202/user/UserDetails.js",
                                    lineNumber: 56,
                                    columnNumber: 25
                                }, this),
                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("hr", {}, void 0, false, {
                                    fileName: "src/components/IT20125202/user/UserDetails.js",
                                    lineNumber: 59,
                                    columnNumber: 25
                                }, this),
                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("br", {}, void 0, false, {
                                    fileName: "src/components/IT20125202/user/UserDetails.js",
                                    lineNumber: 59,
                                    columnNumber: 31
                                }, this)
                            ]
                        }, void 0, true, {
                            fileName: "src/components/IT20125202/user/UserDetails.js",
                            lineNumber: 55,
                            columnNumber: 21
                        }, this),
                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("dl", {
                            className: "row",
                            children: [
                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("dt", {
                                    className: "col-sm-3",
                                    children: "ID Number"
                                }, void 0, false, {
                                    fileName: "src/components/IT20125202/user/UserDetails.js",
                                    lineNumber: 63,
                                    columnNumber: 25
                                }, this),
                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("dd", {
                                    className: "col-sm-9",
                                    children: idNumber
                                }, void 0, false, {
                                    fileName: "src/components/IT20125202/user/UserDetails.js",
                                    lineNumber: 64,
                                    columnNumber: 25
                                }, this),
                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("dt", {
                                    className: "col-sm-3",
                                    children: "Name"
                                }, void 0, false, {
                                    fileName: "src/components/IT20125202/user/UserDetails.js",
                                    lineNumber: 66,
                                    columnNumber: 25
                                }, this),
                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("dd", {
                                    className: "col-sm-9",
                                    children: name
                                }, void 0, false, {
                                    fileName: "src/components/IT20125202/user/UserDetails.js",
                                    lineNumber: 67,
                                    columnNumber: 25
                                }, this),
                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("dt", {
                                    className: "col-sm-3",
                                    children: "Email"
                                }, void 0, false, {
                                    fileName: "src/components/IT20125202/user/UserDetails.js",
                                    lineNumber: 69,
                                    columnNumber: 25
                                }, this),
                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("dd", {
                                    className: "col-sm-9",
                                    children: email
                                }, void 0, false, {
                                    fileName: "src/components/IT20125202/user/UserDetails.js",
                                    lineNumber: 70,
                                    columnNumber: 25
                                }, this),
                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("dt", {
                                    className: "col-sm-3",
                                    children: "Group ID"
                                }, void 0, false, {
                                    fileName: "src/components/IT20125202/user/UserDetails.js",
                                    lineNumber: 72,
                                    columnNumber: 25
                                }, this),
                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("dd", {
                                    className: "col-sm-9",
                                    children: groupId
                                }, void 0, false, {
                                    fileName: "src/components/IT20125202/user/UserDetails.js",
                                    lineNumber: 73,
                                    columnNumber: 25
                                }, this),
                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("dt", {
                                    className: "col-sm-3",
                                    children: "Type"
                                }, void 0, false, {
                                    fileName: "src/components/IT20125202/user/UserDetails.js",
                                    lineNumber: 75,
                                    columnNumber: 25
                                }, this),
                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("dd", {
                                    className: "col-sm-9",
                                    children: type
                                }, void 0, false, {
                                    fileName: "src/components/IT20125202/user/UserDetails.js",
                                    lineNumber: 76,
                                    columnNumber: 25
                                }, this),
                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("dt", {
                                    className: "col-sm-3",
                                    children: "Registered Date"
                                }, void 0, false, {
                                    fileName: "src/components/IT20125202/user/UserDetails.js",
                                    lineNumber: 78,
                                    columnNumber: 25
                                }, this),
                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("dd", {
                                    className: "col-sm-9",
                                    children: dateRegistered
                                }, void 0, false, {
                                    fileName: "src/components/IT20125202/user/UserDetails.js",
                                    lineNumber: 79,
                                    columnNumber: 25
                                }, this)
                            ]
                        }, void 0, true, {
                            fileName: "src/components/IT20125202/user/UserDetails.js",
                            lineNumber: 62,
                            columnNumber: 21
                        }, this),
                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("a", {
                            href: "/admin/users",
                            class: "btn btn-outline-dark",
                            tabindex: "-1",
                            role: "button",
                            "aria-disabled": "true",
                            children: "Back"
                        }, void 0, false, {
                            fileName: "src/components/IT20125202/user/UserDetails.js",
                            lineNumber: 83,
                            columnNumber: 21
                        }, this)
                    ]
                }, void 0, true, {
                    fileName: "src/components/IT20125202/user/UserDetails.js",
                    lineNumber: 54,
                    columnNumber: 17
                }, this)
            ]
        }, void 0, true, {
            fileName: "src/components/IT20125202/user/UserDetails.js",
            lineNumber: 50,
            columnNumber: 13
        }, this);
    }
}
exports.default = UserProfile;

  $parcel$ReactRefreshHelpers$84ba.postlude(module);
} finally {
  window.$RefreshReg$ = prevRefreshReg;
  window.$RefreshSig$ = prevRefreshSig;
}
},{"react/jsx-dev-runtime":"iTorj","react":"21dqq","axios":"jo6P5","../admin/AdminNavBar":"kb2lK","sweetalert":"38a65","@parcel/transformer-js/src/esmodule-helpers.js":"gkKU3","@parcel/transformer-react-refresh-wrap/lib/helpers/helpers.js":"km3Ru"}],"8Ch7k":[function(require,module,exports) {
var $parcel$ReactRefreshHelpers$6d1d = require("@parcel/transformer-react-refresh-wrap/lib/helpers/helpers.js");
var prevRefreshReg = window.$RefreshReg$;
var prevRefreshSig = window.$RefreshSig$;
$parcel$ReactRefreshHelpers$6d1d.prelude(module);

try {
var parcelHelpers = require("@parcel/transformer-js/src/esmodule-helpers.js");
parcelHelpers.defineInteropFlag(exports);
var _jsxDevRuntime = require("react/jsx-dev-runtime");
// admin - view user roles
var _react = require("react");
var _reactDefault = parcelHelpers.interopDefault(_react);
var _axios = require("axios");
var _axiosDefault = parcelHelpers.interopDefault(_axios);
var _adminNavBar = require("../admin/AdminNavBar");
var _adminNavBarDefault = parcelHelpers.interopDefault(_adminNavBar);
var _sweetalert = require("sweetalert");
var _sweetalertDefault = parcelHelpers.interopDefault(_sweetalert);
var _reactMinimalPieChart = require("react-minimal-pie-chart");
class UserRoles extends _react.Component {
    constructor(props){
        super(props);
        this.state = {
            all: 0,
            students: 0,
            supervisors: 0,
            panelmembers: 0,
            users: []
        };
    }
    componentDidMount() {
        document.title = "User Roles";
        // redirect to the login page if the user is not logged in
        if (!localStorage.token) _sweetalertDefault.default("Please login first", "", "warning").then((value)=>{
            if (value) {
                this.props.history.push(`/admin/login`);
                window.location.reload();
            }
        });
        this.retrieveUsers();
    }
    retrieveUsers() {
        let all = 0;
        let students = 0;
        let panel = 0;
        let supervisors = 0;
        _axiosDefault.default.get('http://localhost:5000/users').then((res)=>{
            if (res.data.success) {
                this.setState({
                    users: res.data.existingUsers
                });
                this.state.users.forEach((user)=>{
                    if (user.type === "Student") students = students + 1;
                    else if (user.type === "Supervisor") supervisors = supervisors + 1;
                    else if (user.type === "Panel Member") panel = panel + 1;
                    all = all + 1;
                });
                this.setState({
                    all: all,
                    students: students,
                    supervisors: supervisors,
                    panelmembers: panel
                });
            // console.log(this.state.users);
            }
        });
    }
    onDelete = (id)=>{
        //with a confirmation 
        _sweetalertDefault.default({
            title: "Are you sure?",
            text: "Once deleted, you will not be able to recover this user details",
            icon: "warning",
            buttons: true,
            dangerMode: true
        }).then((willDelete)=>{
            if (willDelete) _axiosDefault.default.delete(`http://localhost:5000/user/delete/${id}`).then((res)=>{
                _sweetalertDefault.default("User Deleted Permanently!", "", "success").then((value)=>{
                    if (value) this.retrieveUsers();
                });
            });
            else _sweetalertDefault.default("Cancelled. The user details are safe!");
        });
    };
    handleSearchArea = (e)=>{
        // console.log(e.currentTarget.value)
        const searchKey = e.currentTarget.value;
        _axiosDefault.default.get('http://localhost:5000/users').then((res)=>{
            if (res.data.success) this.filterData(res.data.existingUsers, searchKey);
        });
    };
    filterData(users, searchKey) {
        const searchResult = users.filter((user)=>user.idNumber.toLowerCase().includes(searchKey) || user.name.toLowerCase().includes(searchKey) || user.type.toLowerCase().includes(searchKey) || user.idNumber.toUpperCase().includes(searchKey) || user.name.toUpperCase().includes(searchKey) || user.type.toUpperCase().includes(searchKey) || user.idNumber.includes(searchKey) || user.name.includes(searchKey) || user.type.includes(searchKey)
        );
        this.setState({
            users: searchResult
        });
    }
    getStat = ()=>{};
    render() {
        return /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
            className: "container",
            children: [
                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("br", {}, void 0, false, {
                    fileName: "src/components/IT20125202/user/UserRoles.js",
                    lineNumber: 145,
                    columnNumber: 9
                }, this),
                /*#__PURE__*/ _jsxDevRuntime.jsxDEV(_adminNavBarDefault.default, {}, void 0, false, {
                    fileName: "src/components/IT20125202/user/UserRoles.js",
                    lineNumber: 146,
                    columnNumber: 9
                }, this),
                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("br", {}, void 0, false, {
                    fileName: "src/components/IT20125202/user/UserRoles.js",
                    lineNumber: 147,
                    columnNumber: 9
                }, this),
                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                    className: "container",
                    children: [
                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                            className: "row",
                            children: [
                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                    className: "col-lg-9 mt-2 mb-2",
                                    children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("h1", {
                                        children: "Users"
                                    }, void 0, false, {
                                        fileName: "src/components/IT20125202/user/UserRoles.js",
                                        lineNumber: 152,
                                        columnNumber: 15
                                    }, this)
                                }, void 0, false, {
                                    fileName: "src/components/IT20125202/user/UserRoles.js",
                                    lineNumber: 151,
                                    columnNumber: 13
                                }, this),
                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                    className: "col-lg-3 mt-2 mb-2",
                                    children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("input", {
                                        className: "form-control",
                                        type: "search",
                                        placeholder: "Search User",
                                        name: "searchQuery",
                                        onChange: this.handleSearchArea
                                    }, void 0, false, {
                                        fileName: "src/components/IT20125202/user/UserRoles.js",
                                        lineNumber: 155,
                                        columnNumber: 15
                                    }, this)
                                }, void 0, false, {
                                    fileName: "src/components/IT20125202/user/UserRoles.js",
                                    lineNumber: 154,
                                    columnNumber: 13
                                }, this),
                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("hr", {}, void 0, false, {
                                    fileName: "src/components/IT20125202/user/UserRoles.js",
                                    lineNumber: 163,
                                    columnNumber: 13
                                }, this),
                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("br", {}, void 0, false, {
                                    fileName: "src/components/IT20125202/user/UserRoles.js",
                                    lineNumber: 163,
                                    columnNumber: 19
                                }, this)
                            ]
                        }, void 0, true, {
                            fileName: "src/components/IT20125202/user/UserRoles.js",
                            lineNumber: 150,
                            columnNumber: 11
                        }, this),
                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                            className: "row",
                            children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                className: "btn-group",
                                children: [
                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("a", {
                                        href: "/admin/users",
                                        className: "btn btn-outline-dark active",
                                        "aria-current": "page",
                                        children: "All Users"
                                    }, void 0, false, {
                                        fileName: "src/components/IT20125202/user/UserRoles.js",
                                        lineNumber: 168,
                                        columnNumber: 15
                                    }, this),
                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("a", {
                                        href: "/admin/panelmembers",
                                        className: "btn btn-outline-dark",
                                        children: "Panel Members"
                                    }, void 0, false, {
                                        fileName: "src/components/IT20125202/user/UserRoles.js",
                                        lineNumber: 169,
                                        columnNumber: 15
                                    }, this),
                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("a", {
                                        href: "/admin/students",
                                        className: "btn btn-outline-dark",
                                        children: "Students"
                                    }, void 0, false, {
                                        fileName: "src/components/IT20125202/user/UserRoles.js",
                                        lineNumber: 170,
                                        columnNumber: 15
                                    }, this),
                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("a", {
                                        href: "/admin/supervisors",
                                        className: "btn btn-outline-dark",
                                        children: "Supervisors"
                                    }, void 0, false, {
                                        fileName: "src/components/IT20125202/user/UserRoles.js",
                                        lineNumber: 171,
                                        columnNumber: 15
                                    }, this)
                                ]
                            }, void 0, true, {
                                fileName: "src/components/IT20125202/user/UserRoles.js",
                                lineNumber: 167,
                                columnNumber: 13
                            }, this)
                        }, void 0, false, {
                            fileName: "src/components/IT20125202/user/UserRoles.js",
                            lineNumber: 166,
                            columnNumber: 11
                        }, this),
                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("br", {}, void 0, false, {
                            fileName: "src/components/IT20125202/user/UserRoles.js",
                            lineNumber: 174,
                            columnNumber: 11
                        }, this),
                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("table", {
                            className: "table",
                            children: [
                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("thead", {
                                    children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("tr", {
                                        children: [
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("th", {
                                                scope: "col",
                                                children: " # "
                                            }, void 0, false, {
                                                fileName: "src/components/IT20125202/user/UserRoles.js",
                                                lineNumber: 178,
                                                columnNumber: 17
                                            }, this),
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("th", {
                                                scope: "col",
                                                children: " ID Number "
                                            }, void 0, false, {
                                                fileName: "src/components/IT20125202/user/UserRoles.js",
                                                lineNumber: 179,
                                                columnNumber: 17
                                            }, this),
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("th", {
                                                scope: "col",
                                                children: " Name "
                                            }, void 0, false, {
                                                fileName: "src/components/IT20125202/user/UserRoles.js",
                                                lineNumber: 180,
                                                columnNumber: 17
                                            }, this),
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("th", {
                                                scope: "col",
                                                children: " Email "
                                            }, void 0, false, {
                                                fileName: "src/components/IT20125202/user/UserRoles.js",
                                                lineNumber: 181,
                                                columnNumber: 17
                                            }, this),
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("th", {
                                                scope: "col",
                                                children: " Mobile "
                                            }, void 0, false, {
                                                fileName: "src/components/IT20125202/user/UserRoles.js",
                                                lineNumber: 182,
                                                columnNumber: 17
                                            }, this),
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("th", {
                                                scope: "col",
                                                children: " Type "
                                            }, void 0, false, {
                                                fileName: "src/components/IT20125202/user/UserRoles.js",
                                                lineNumber: 183,
                                                columnNumber: 17
                                            }, this),
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("th", {
                                                scope: "col",
                                                children: " "
                                            }, void 0, false, {
                                                fileName: "src/components/IT20125202/user/UserRoles.js",
                                                lineNumber: 185,
                                                columnNumber: 17
                                            }, this)
                                        ]
                                    }, void 0, true, {
                                        fileName: "src/components/IT20125202/user/UserRoles.js",
                                        lineNumber: 177,
                                        columnNumber: 15
                                    }, this)
                                }, void 0, false, {
                                    fileName: "src/components/IT20125202/user/UserRoles.js",
                                    lineNumber: 176,
                                    columnNumber: 13
                                }, this),
                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("tbody", {
                                    children: this.state.users.map((users, index)=>/*#__PURE__*/ _jsxDevRuntime.jsxDEV("tr", {
                                            children: [
                                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("th", {
                                                    scope: "row",
                                                    children: index + 1
                                                }, void 0, false, {
                                                    fileName: "src/components/IT20125202/user/UserRoles.js",
                                                    lineNumber: 193,
                                                    columnNumber: 19
                                                }, this),
                                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("td", {
                                                    children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("a", {
                                                        href: `/admin/user/${users._id}`,
                                                        style: {
                                                            textDecoration: 'none'
                                                        },
                                                        children: users.idNumber
                                                    }, void 0, false, {
                                                        fileName: "src/components/IT20125202/user/UserRoles.js",
                                                        lineNumber: 195,
                                                        columnNumber: 21
                                                    }, this)
                                                }, void 0, false, {
                                                    fileName: "src/components/IT20125202/user/UserRoles.js",
                                                    lineNumber: 194,
                                                    columnNumber: 19
                                                }, this),
                                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("td", {
                                                    children: users.name
                                                }, void 0, false, {
                                                    fileName: "src/components/IT20125202/user/UserRoles.js",
                                                    lineNumber: 199,
                                                    columnNumber: 19
                                                }, this),
                                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("td", {
                                                    children: users.email
                                                }, void 0, false, {
                                                    fileName: "src/components/IT20125202/user/UserRoles.js",
                                                    lineNumber: 200,
                                                    columnNumber: 19
                                                }, this),
                                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("td", {
                                                    children: users.mobile
                                                }, void 0, false, {
                                                    fileName: "src/components/IT20125202/user/UserRoles.js",
                                                    lineNumber: 201,
                                                    columnNumber: 19
                                                }, this),
                                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("td", {
                                                    children: users.type
                                                }, void 0, false, {
                                                    fileName: "src/components/IT20125202/user/UserRoles.js",
                                                    lineNumber: 202,
                                                    columnNumber: 19
                                                }, this),
                                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("td", {
                                                    children: [
                                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("a", {
                                                            className: "btn btn-outline-success",
                                                            href: `/admin/edituser/${users._id}`,
                                                            children: [
                                                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("i", {
                                                                    className: "fas fa-edit"
                                                                }, void 0, false, {
                                                                    fileName: "src/components/IT20125202/user/UserRoles.js",
                                                                    lineNumber: 207,
                                                                    columnNumber: 23
                                                                }, this),
                                                                " \xa0Update"
                                                            ]
                                                        }, void 0, true, {
                                                            fileName: "src/components/IT20125202/user/UserRoles.js",
                                                            lineNumber: 206,
                                                            columnNumber: 21
                                                        }, this),
                                                        "\xa0",
                                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("a", {
                                                            className: "btn btn-outline-danger",
                                                            href: "#",
                                                            onClick: ()=>this.onDelete(users._id)
                                                            ,
                                                            children: [
                                                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("i", {
                                                                    className: "fas fa-trash"
                                                                }, void 0, false, {
                                                                    fileName: "src/components/IT20125202/user/UserRoles.js",
                                                                    lineNumber: 211,
                                                                    columnNumber: 23
                                                                }, this),
                                                                " \xa0Delete"
                                                            ]
                                                        }, void 0, true, {
                                                            fileName: "src/components/IT20125202/user/UserRoles.js",
                                                            lineNumber: 210,
                                                            columnNumber: 21
                                                        }, this)
                                                    ]
                                                }, void 0, true, {
                                                    fileName: "src/components/IT20125202/user/UserRoles.js",
                                                    lineNumber: 205,
                                                    columnNumber: 19
                                                }, this)
                                            ]
                                        }, index, true, {
                                            fileName: "src/components/IT20125202/user/UserRoles.js",
                                            lineNumber: 192,
                                            columnNumber: 17
                                        }, this)
                                    )
                                }, void 0, false, {
                                    fileName: "src/components/IT20125202/user/UserRoles.js",
                                    lineNumber: 190,
                                    columnNumber: 13
                                }, this)
                            ]
                        }, void 0, true, {
                            fileName: "src/components/IT20125202/user/UserRoles.js",
                            lineNumber: 175,
                            columnNumber: 11
                        }, this),
                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("button", {
                            type: "button",
                            class: "btn btn-dark",
                            "data-bs-toggle": "modal",
                            "data-bs-target": "#exampleModal",
                            onClick: ()=>this.getStat()
                            ,
                            children: "Show Statistics"
                        }, void 0, false, {
                            fileName: "src/components/IT20125202/user/UserRoles.js",
                            lineNumber: 219,
                            columnNumber: 11
                        }, this),
                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("br", {}, void 0, false, {
                            fileName: "src/components/IT20125202/user/UserRoles.js",
                            lineNumber: 222,
                            columnNumber: 11
                        }, this),
                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("br", {}, void 0, false, {
                            fileName: "src/components/IT20125202/user/UserRoles.js",
                            lineNumber: 222,
                            columnNumber: 17
                        }, this),
                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("br", {}, void 0, false, {
                            fileName: "src/components/IT20125202/user/UserRoles.js",
                            lineNumber: 222,
                            columnNumber: 23
                        }, this),
                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                            class: "modal fade",
                            id: "exampleModal",
                            tabIndex: "-1",
                            "aria-labelledby": "exampleModalLabel",
                            "aria-hidden": "true",
                            children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                class: "modal-dialog",
                                children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                    class: "modal-content",
                                    children: [
                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                            class: "modal-header",
                                            children: [
                                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("h5", {
                                                    class: "modal-title",
                                                    id: "exampleModalLabel",
                                                    children: "Registered user counts"
                                                }, void 0, false, {
                                                    fileName: "src/components/IT20125202/user/UserRoles.js",
                                                    lineNumber: 228,
                                                    columnNumber: 19
                                                }, this),
                                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("button", {
                                                    type: "button",
                                                    class: "btn-close",
                                                    "data-bs-dismiss": "modal",
                                                    "aria-label": "Close"
                                                }, void 0, false, {
                                                    fileName: "src/components/IT20125202/user/UserRoles.js",
                                                    lineNumber: 229,
                                                    columnNumber: 19
                                                }, this)
                                            ]
                                        }, void 0, true, {
                                            fileName: "src/components/IT20125202/user/UserRoles.js",
                                            lineNumber: 227,
                                            columnNumber: 17
                                        }, this),
                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                            class: "modal-body",
                                            children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("dl", {
                                                className: "row",
                                                style: {
                                                    width: '100vh',
                                                    margin: '0px 0px 0px 0px'
                                                },
                                                children: [
                                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("dt", {
                                                        className: "col-sm-3",
                                                        children: "All users"
                                                    }, void 0, false, {
                                                        fileName: "src/components/IT20125202/user/UserRoles.js",
                                                        lineNumber: 233,
                                                        columnNumber: 21
                                                    }, this),
                                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("dd", {
                                                        className: "col-sm-9",
                                                        children: this.state.all
                                                    }, void 0, false, {
                                                        fileName: "src/components/IT20125202/user/UserRoles.js",
                                                        lineNumber: 234,
                                                        columnNumber: 21
                                                    }, this),
                                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("dt", {
                                                        className: "col-sm-3",
                                                        children: "Panel Members"
                                                    }, void 0, false, {
                                                        fileName: "src/components/IT20125202/user/UserRoles.js",
                                                        lineNumber: 236,
                                                        columnNumber: 21
                                                    }, this),
                                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("dd", {
                                                        className: "col-sm-9",
                                                        children: this.state.panelmembers
                                                    }, void 0, false, {
                                                        fileName: "src/components/IT20125202/user/UserRoles.js",
                                                        lineNumber: 237,
                                                        columnNumber: 21
                                                    }, this),
                                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("dt", {
                                                        className: "col-sm-3",
                                                        children: "Students"
                                                    }, void 0, false, {
                                                        fileName: "src/components/IT20125202/user/UserRoles.js",
                                                        lineNumber: 239,
                                                        columnNumber: 21
                                                    }, this),
                                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("dd", {
                                                        className: "col-sm-9",
                                                        children: this.state.students
                                                    }, void 0, false, {
                                                        fileName: "src/components/IT20125202/user/UserRoles.js",
                                                        lineNumber: 240,
                                                        columnNumber: 21
                                                    }, this),
                                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("dt", {
                                                        className: "col-sm-3",
                                                        children: "Supervisors"
                                                    }, void 0, false, {
                                                        fileName: "src/components/IT20125202/user/UserRoles.js",
                                                        lineNumber: 242,
                                                        columnNumber: 21
                                                    }, this),
                                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("dd", {
                                                        className: "col-sm-9",
                                                        children: this.state.supervisors
                                                    }, void 0, false, {
                                                        fileName: "src/components/IT20125202/user/UserRoles.js",
                                                        lineNumber: 243,
                                                        columnNumber: 21
                                                    }, this)
                                                ]
                                            }, void 0, true, {
                                                fileName: "src/components/IT20125202/user/UserRoles.js",
                                                lineNumber: 232,
                                                columnNumber: 19
                                            }, this)
                                        }, void 0, false, {
                                            fileName: "src/components/IT20125202/user/UserRoles.js",
                                            lineNumber: 231,
                                            columnNumber: 17
                                        }, this),
                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV(_reactMinimalPieChart.PieChart, {
                                            radius: 40,
                                            label: (data)=>data.dataEntry.title
                                            ,
                                            labelStyle: {
                                                fontSize: "5px",
                                                fontColor: "FFFFFA",
                                                fontWeight: "100"
                                            },
                                            reveal: true,
                                            data: [
                                                {
                                                    title: 'Panel Members',
                                                    value: this.state.panelmembers,
                                                    color: '#E38627'
                                                },
                                                {
                                                    title: 'Students',
                                                    value: this.state.students,
                                                    color: '#C13C37'
                                                },
                                                {
                                                    title: 'Supervisors',
                                                    value: this.state.supervisors,
                                                    color: '#6A2135'
                                                }, 
                                            ]
                                        }, void 0, false, {
                                            fileName: "src/components/IT20125202/user/UserRoles.js",
                                            lineNumber: 246,
                                            columnNumber: 17
                                        }, this),
                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                            class: "modal-footer",
                                            children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("button", {
                                                type: "button",
                                                class: "btn btn-secondary",
                                                "data-bs-dismiss": "modal",
                                                children: "Close"
                                            }, void 0, false, {
                                                fileName: "src/components/IT20125202/user/UserRoles.js",
                                                lineNumber: 261,
                                                columnNumber: 19
                                            }, this)
                                        }, void 0, false, {
                                            fileName: "src/components/IT20125202/user/UserRoles.js",
                                            lineNumber: 260,
                                            columnNumber: 17
                                        }, this)
                                    ]
                                }, void 0, true, {
                                    fileName: "src/components/IT20125202/user/UserRoles.js",
                                    lineNumber: 226,
                                    columnNumber: 15
                                }, this)
                            }, void 0, false, {
                                fileName: "src/components/IT20125202/user/UserRoles.js",
                                lineNumber: 225,
                                columnNumber: 13
                            }, this)
                        }, void 0, false, {
                            fileName: "src/components/IT20125202/user/UserRoles.js",
                            lineNumber: 224,
                            columnNumber: 11
                        }, this)
                    ]
                }, void 0, true, {
                    fileName: "src/components/IT20125202/user/UserRoles.js",
                    lineNumber: 149,
                    columnNumber: 9
                }, this)
            ]
        }, void 0, true, {
            fileName: "src/components/IT20125202/user/UserRoles.js",
            lineNumber: 144,
            columnNumber: 7
        }, this);
    }
}
exports.default = UserRoles;

  $parcel$ReactRefreshHelpers$6d1d.postlude(module);
} finally {
  window.$RefreshReg$ = prevRefreshReg;
  window.$RefreshSig$ = prevRefreshSig;
}
},{"react/jsx-dev-runtime":"iTorj","react":"21dqq","axios":"jo6P5","../admin/AdminNavBar":"kb2lK","sweetalert":"38a65","react-minimal-pie-chart":"fw8bg","@parcel/transformer-js/src/esmodule-helpers.js":"gkKU3","@parcel/transformer-react-refresh-wrap/lib/helpers/helpers.js":"km3Ru"}],"fw8bg":[function(require,module,exports) {
(function(global, factory) {
    factory(exports, require('react'));
})(this, function(exports, React) {
    'use strict';
    function _interopDefaultLegacy(e) {
        return e && typeof e === 'object' && 'default' in e ? e : {
            'default': e
        };
    }
    var React__default = /*#__PURE__*/ _interopDefaultLegacy(React);
    function degreesToRadians(degrees) {
        return degrees * Math.PI / 180;
    }
    function valueBetween(value, min, max) {
        if (value > max) return max;
        if (value < min) return min;
        return value;
    }
    function extractPercentage(value, percentage) {
        return percentage / 100 * value;
    }
    function bisectorAngle(startAngle, lengthAngle) {
        return startAngle + lengthAngle / 2;
    }
    function shiftVectorAlongAngle(angle, distance) {
        var angleRadians = degreesToRadians(angle);
        return {
            dx: distance * Math.cos(angleRadians),
            dy: distance * Math.sin(angleRadians)
        };
    }
    function isNumber(value) {
        return typeof value === 'number';
    }
    function functionProp(prop, payload) {
        return typeof prop === 'function' ? prop(payload) : prop;
    }
    function sumValues(data) {
        var sum = 0;
        for(var i = 0; i < data.length; i++)sum += data[i].value;
        return sum;
    } // Append "percentage", "degrees" and "startAngle" to each data entry
    function extendData(_ref) {
        var data = _ref.data, totalAngle = _ref.lengthAngle, totalValue = _ref.totalValue, paddingAngle = _ref.paddingAngle, chartStartAngle = _ref.startAngle;
        var total = totalValue || sumValues(data);
        var normalizedTotalAngle = valueBetween(totalAngle, -360, 360);
        var numberOfPaddings = Math.abs(normalizedTotalAngle) === 360 ? data.length : data.length - 1;
        var singlePaddingDegrees = Math.abs(paddingAngle) * Math.sign(totalAngle);
        var degreesTakenByPadding = singlePaddingDegrees * numberOfPaddings;
        var degreesTakenByPaths = normalizedTotalAngle - degreesTakenByPadding;
        var lastSegmentEnd = 0;
        var extendedData = []; // @NOTE: Shall we evaluate percentage accordingly to dataEntry.value's sign?
        for(var i = 0; i < data.length; i++){
            var dataEntry = data[i];
            var valueInPercentage = total === 0 ? 0 : dataEntry.value / total * 100;
            var degrees = extractPercentage(degreesTakenByPaths, valueInPercentage);
            var startAngle = lastSegmentEnd + chartStartAngle;
            lastSegmentEnd = lastSegmentEnd + degrees + singlePaddingDegrees;
            extendedData.push(Object.assign({
                percentage: valueInPercentage,
                startAngle: startAngle,
                degrees: degrees
            }, dataEntry));
        }
        return extendedData;
    }
    function _objectWithoutPropertiesLoose(source, excluded) {
        if (source == null) return {};
        var target = {};
        var sourceKeys = Object.keys(source);
        var key, i;
        for(i = 0; i < sourceKeys.length; i++){
            key = sourceKeys[i];
            if (excluded.indexOf(key) >= 0) continue;
            target[key] = source[key];
        }
        return target;
    }
    function ReactMinimalPieChartLabel(_ref) {
        _ref.dataEntry;
        _ref.dataIndex;
        var props = _objectWithoutPropertiesLoose(_ref, [
            "dataEntry",
            "dataIndex"
        ]);
        return /*#__PURE__*/ React__default['default'].createElement("text", Object.assign({
            dominantBaseline: "central"
        }, props));
    }
    function round(number) {
        var divisor = 1e14; // 14 decimals
        return Math.round((number + Number.EPSILON) * divisor) / divisor;
    }
    function evaluateTextAnchorPosition(_ref) {
        var labelPosition = _ref.labelPosition, lineWidth = _ref.lineWidth, labelHorizontalShift = _ref.labelHorizontalShift;
        var dx = round(labelHorizontalShift); // Label in the vertical center
        if (dx === 0) return 'middle';
         // Outward label
        if (labelPosition > 100) return dx > 0 ? 'start' : 'end';
         // Inward label
        var innerRadius = 100 - lineWidth;
        if (labelPosition < innerRadius) return dx > 0 ? 'end' : 'start';
         // Overlying label
        return 'middle';
    }
    function renderLabelElement(renderLabel, labelProps) {
        var label = renderLabel(labelProps);
        if (typeof label === 'string' || typeof label === 'number') return /*#__PURE__*/ React__default['default'].createElement(ReactMinimalPieChartLabel, Object.assign({
            key: "label-" + (labelProps.dataEntry.key || labelProps.dataIndex)
        }, labelProps), label);
        if (React__default['default'].isValidElement(label)) return label;
        return null;
    }
    function renderLabels(data, props) {
        return data.map(function(dataEntry, index) {
            var _functionProp;
            var segmentsShift = (_functionProp = functionProp(props.segmentsShift, index)) != null ? _functionProp : 0;
            var distanceFromCenter = extractPercentage(props.radius, props.labelPosition) + segmentsShift;
            var _shiftVectorAlongAngl = shiftVectorAlongAngle(bisectorAngle(dataEntry.startAngle, dataEntry.degrees), distanceFromCenter), dx = _shiftVectorAlongAngl.dx, dy = _shiftVectorAlongAngl.dy; // This object is passed as argument to the "label" function prop
            var labelRenderProps = {
                x: props.center[0],
                y: props.center[1],
                dx: dx,
                dy: dy,
                textAnchor: evaluateTextAnchorPosition({
                    labelPosition: props.labelPosition,
                    lineWidth: props.lineWidth,
                    labelHorizontalShift: dx
                }),
                dataEntry: dataEntry,
                dataIndex: index,
                style: functionProp(props.labelStyle, index)
            };
            return props.label && renderLabelElement(props.label, labelRenderProps);
        });
    }
    var partialCircle = function partialCircle(cx, cy, r, start, end) {
        var length = end - start;
        if (length === 0) return [];
        var fromX = r * Math.cos(start) + cx;
        var fromY = r * Math.sin(start) + cy;
        var toX = r * Math.cos(end) + cx;
        var toY = r * Math.sin(end) + cy;
        var large = Math.abs(length) <= Math.PI ? '0' : '1';
        var sweep = length < 0 ? '0' : '1';
        return [
            [
                'M',
                fromX,
                fromY
            ],
            [
                'A',
                r,
                r,
                0,
                large,
                sweep,
                toX,
                toY
            ]
        ];
    };
    var svgPartialCircle = partialCircle;
    function makePathCommands(cx, cy, startAngle, lengthAngle, radius) {
        var patchedLengthAngle = valueBetween(lengthAngle, -359.999, 359.999);
        return svgPartialCircle(cx, cy, radius, degreesToRadians(startAngle), degreesToRadians(startAngle + patchedLengthAngle)).map(function(command) {
            return command.join(' ');
        }).join(' ');
    }
    function ReactMinimalPieChartPath(_ref) {
        var cx = _ref.cx, cy = _ref.cy, lengthAngle = _ref.lengthAngle, lineWidth = _ref.lineWidth, radius = _ref.radius, _ref$shift = _ref.shift, shift = _ref$shift === void 0 ? 0 : _ref$shift, reveal = _ref.reveal, rounded = _ref.rounded, startAngle = _ref.startAngle, title = _ref.title, props = _objectWithoutPropertiesLoose(_ref, [
            "cx",
            "cy",
            "lengthAngle",
            "lineWidth",
            "radius",
            "shift",
            "reveal",
            "rounded",
            "startAngle",
            "title"
        ]);
        var pathRadius = radius - lineWidth / 2; //@NOTE This shift might be rendered as a translation in future
        var _shiftVectorAlongAngl = shiftVectorAlongAngle(bisectorAngle(startAngle, lengthAngle), shift), dx = _shiftVectorAlongAngl.dx, dy = _shiftVectorAlongAngl.dy;
        var pathCommands = makePathCommands(cx + dx, cy + dy, startAngle, lengthAngle, pathRadius);
        var strokeDasharray;
        var strokeDashoffset; // Animate/hide paths with "stroke-dasharray" + "stroke-dashoffset"
        // https://css-tricks.com/svg-line-animation-works/
        if (isNumber(reveal)) {
            var pathLength = degreesToRadians(pathRadius) * lengthAngle;
            strokeDasharray = Math.abs(pathLength);
            strokeDashoffset = strokeDasharray - extractPercentage(strokeDasharray, reveal);
        }
        return /*#__PURE__*/ React__default['default'].createElement("path", Object.assign({
            d: pathCommands,
            fill: "none",
            strokeWidth: lineWidth,
            strokeDasharray: strokeDasharray,
            strokeDashoffset: strokeDashoffset,
            strokeLinecap: rounded ? 'round' : undefined
        }, props), title && /*#__PURE__*/ React__default['default'].createElement("title", null, title));
    }
    function combineSegmentTransitionsStyle(duration, easing, customStyle) {
        // Merge chart's animation CSS transition with "transition" found to customStyle
        var transition = "stroke-dashoffset " + duration + "ms " + easing;
        if (customStyle && customStyle.transition) transition = transition + "," + customStyle.transition;
        return {
            transition: transition
        };
    }
    function getRevealValue(props) {
        //@NOTE When animation is on, chart has to be fully revealed when reveal is not set
        if (props.animate && !isNumber(props.reveal)) return 100;
        return props.reveal;
    }
    function makeEventHandler(eventHandler, payload) {
        return eventHandler && function(e) {
            eventHandler(e, payload);
        };
    }
    function renderSegments(data, props, revealOverride) {
        // @NOTE this should go in Path component. Here for performance reasons
        var reveal = revealOverride != null ? revealOverride : getRevealValue(props);
        var radius = props.radius, _props$center = props.center, cx = _props$center[0], cy = _props$center[1];
        var lineWidth = extractPercentage(radius, props.lineWidth);
        var paths = data.map(function(dataEntry, index) {
            var segmentsStyle = functionProp(props.segmentsStyle, index);
            return /*#__PURE__*/ React__default['default'].createElement(ReactMinimalPieChartPath, {
                cx: cx,
                cy: cy,
                key: dataEntry.key || index,
                lengthAngle: dataEntry.degrees,
                lineWidth: lineWidth,
                radius: radius,
                rounded: props.rounded,
                reveal: reveal,
                shift: functionProp(props.segmentsShift, index),
                startAngle: dataEntry.startAngle,
                title: dataEntry.title,
                style: Object.assign({}, segmentsStyle, props.animate && combineSegmentTransitionsStyle(props.animationDuration, props.animationEasing, segmentsStyle)),
                stroke: dataEntry.color,
                tabIndex: props.segmentsTabIndex,
                onBlur: makeEventHandler(props.onBlur, index),
                onClick: makeEventHandler(props.onClick, index),
                onFocus: makeEventHandler(props.onFocus, index),
                onKeyDown: makeEventHandler(props.onKeyDown, index),
                onMouseOver: makeEventHandler(props.onMouseOver, index),
                onMouseOut: makeEventHandler(props.onMouseOut, index)
            });
        });
        if (props.background) paths.unshift(/*#__PURE__*/ React__default['default'].createElement(ReactMinimalPieChartPath, {
            cx: cx,
            cy: cy,
            key: "bg",
            lengthAngle: props.lengthAngle,
            lineWidth: lineWidth,
            radius: radius,
            rounded: props.rounded,
            startAngle: props.startAngle,
            stroke: props.background
        }));
        return paths;
    }
    var defaultProps = {
        animationDuration: 500,
        animationEasing: 'ease-out',
        center: [
            50,
            50
        ],
        data: [],
        labelPosition: 50,
        lengthAngle: 360,
        lineWidth: 100,
        paddingAngle: 0,
        radius: 50,
        startAngle: 0,
        viewBoxSize: [
            100,
            100
        ]
    };
    function ReactMinimalPieChart(props) {
        var _useState = React.useState(props.animate ? 0 : null), revealOverride = _useState[0], setRevealOverride = _useState[1];
        React.useEffect(function() {
            if (props.animate) return startInitialAnimation();
            function startInitialAnimation() {
                var animationTimerId;
                var animationRAFId;
                animationTimerId = setTimeout(function() {
                    animationTimerId = null;
                    animationRAFId = requestAnimationFrame(function() {
                        animationRAFId = null;
                        setRevealOverride(null); // Start animation
                    });
                });
                return function() {
                    animationTimerId && clearTimeout(animationTimerId);
                    animationRAFId && cancelAnimationFrame(animationRAFId);
                };
            }
        }, []);
        var extendedData = extendData(props);
        return /*#__PURE__*/ React__default['default'].createElement("svg", {
            viewBox: "0 0 " + props.viewBoxSize[0] + " " + props.viewBoxSize[1],
            width: "100%",
            height: "100%",
            className: props.className,
            style: props.style
        }, renderSegments(extendedData, props, revealOverride), props.label && renderLabels(extendedData, props), props.children);
    }
    ReactMinimalPieChart.defaultProps = defaultProps;
    exports.PieChart = ReactMinimalPieChart;
    Object.defineProperty(exports, '__esModule', {
        value: true
    });
});

},{"react":"21dqq"}],"1w2DX":[function(require,module,exports) {
var $parcel$ReactRefreshHelpers$0764 = require("@parcel/transformer-react-refresh-wrap/lib/helpers/helpers.js");
var prevRefreshReg = window.$RefreshReg$;
var prevRefreshSig = window.$RefreshSig$;
$parcel$ReactRefreshHelpers$0764.prelude(module);

try {
var parcelHelpers = require("@parcel/transformer-js/src/esmodule-helpers.js");
parcelHelpers.defineInteropFlag(exports);
var _jsxDevRuntime = require("react/jsx-dev-runtime");
var _react = require("react");
var _reactDefault = parcelHelpers.interopDefault(_react);
var _adminNavBar = require("./AdminNavBar");
var _adminNavBarDefault = parcelHelpers.interopDefault(_adminNavBar);
var _homeJpg = require("../../../images/home.jpg");
var _homeJpgDefault = parcelHelpers.interopDefault(_homeJpg);
var _sweetalert = require("sweetalert");
var _sweetalertDefault = parcelHelpers.interopDefault(_sweetalert);
class AdminHome extends _react.Component {
    componentDidMount() {
        document.title = "Admin Home";
        // redirect to the login page if the user is not logged in
        if (!localStorage.token) _sweetalertDefault.default("Please login first", "", "warning").then((value)=>{
            if (value) {
                this.props.history.push(`/admin/login`);
                window.location.reload();
            }
        });
    }
    render() {
        return /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
            className: "container",
            children: [
                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("br", {}, void 0, false, {
                    fileName: "src/components/IT20125202/admin/AdminHome.js",
                    lineNumber: 28,
                    columnNumber: 9
                }, this),
                /*#__PURE__*/ _jsxDevRuntime.jsxDEV(_adminNavBarDefault.default, {}, void 0, false, {
                    fileName: "src/components/IT20125202/admin/AdminHome.js",
                    lineNumber: 29,
                    columnNumber: 9
                }, this),
                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("br", {}, void 0, false, {
                    fileName: "src/components/IT20125202/admin/AdminHome.js",
                    lineNumber: 30,
                    columnNumber: 9
                }, this),
                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                    className: "container",
                    style: {
                        position: 'relative',
                        textAlign: 'center',
                        color: 'white'
                    },
                    children: [
                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("img", {
                            src: _homeJpgDefault.default,
                            class: "img-fluid",
                            alt: "Research Project Management Tool"
                        }, void 0, false, {
                            fileName: "src/components/IT20125202/admin/AdminHome.js",
                            lineNumber: 34,
                            columnNumber: 11
                        }, this),
                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                            style: {
                                position: 'absolute',
                                top: '50%',
                                left: '50%',
                                transform: 'translate(-50%, -50%)'
                            },
                            children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("h1", {
                                class: "display-5",
                                style: {
                                    fontFamily: 'Stencil Std, fantasy'
                                },
                                children: "Research Project Management Tool"
                            }, void 0, false, {
                                fileName: "src/components/IT20125202/admin/AdminHome.js",
                                lineNumber: 36,
                                columnNumber: 13
                            }, this)
                        }, void 0, false, {
                            fileName: "src/components/IT20125202/admin/AdminHome.js",
                            lineNumber: 35,
                            columnNumber: 11
                        }, this)
                    ]
                }, void 0, true, {
                    fileName: "src/components/IT20125202/admin/AdminHome.js",
                    lineNumber: 33,
                    columnNumber: 9
                }, this),
                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("br", {}, void 0, false, {
                    fileName: "src/components/IT20125202/admin/AdminHome.js",
                    lineNumber: 39,
                    columnNumber: 9
                }, this),
                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                    children: [
                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                            className: "row",
                            style: {
                                height: "150px"
                            },
                            children: [
                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                    className: "col p-3 mb-2 m-2 bg-light text-dark rounded",
                                    children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("center", {
                                        children: [
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("i", {
                                                class: "fa fa-user",
                                                "aria-hidden": "true"
                                            }, void 0, false, {
                                                fileName: "src/components/IT20125202/admin/AdminHome.js",
                                                lineNumber: 46,
                                                columnNumber: 17
                                            }, this),
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("h6", {
                                                children: "User Management"
                                            }, void 0, false, {
                                                fileName: "src/components/IT20125202/admin/AdminHome.js",
                                                lineNumber: 47,
                                                columnNumber: 17
                                            }, this),
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("br", {}, void 0, false, {
                                                fileName: "src/components/IT20125202/admin/AdminHome.js",
                                                lineNumber: 48,
                                                columnNumber: 17
                                            }, this),
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("a", {
                                                type: "button",
                                                className: "btn btn-outline-dark",
                                                href: '/admin/users',
                                                children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("i", {
                                                    class: "fa fa-arrow-circle-o-right",
                                                    "aria-hidden": "true"
                                                }, void 0, false, {
                                                    fileName: "src/components/IT20125202/admin/AdminHome.js",
                                                    lineNumber: 49,
                                                    columnNumber: 89
                                                }, this)
                                            }, void 0, false, {
                                                fileName: "src/components/IT20125202/admin/AdminHome.js",
                                                lineNumber: 49,
                                                columnNumber: 17
                                            }, this)
                                        ]
                                    }, void 0, true, {
                                        fileName: "src/components/IT20125202/admin/AdminHome.js",
                                        lineNumber: 45,
                                        columnNumber: 15
                                    }, this)
                                }, void 0, false, {
                                    fileName: "src/components/IT20125202/admin/AdminHome.js",
                                    lineNumber: 44,
                                    columnNumber: 13
                                }, this),
                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                    className: "col p-3 mb-2 m-2 bg-light text-dark rounded",
                                    children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("center", {
                                        children: [
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("i", {
                                                class: "fa fa-users",
                                                "aria-hidden": "true"
                                            }, void 0, false, {
                                                fileName: "src/components/IT20125202/admin/AdminHome.js",
                                                lineNumber: 54,
                                                columnNumber: 17
                                            }, this),
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("h6", {
                                                children: "Student Group Management"
                                            }, void 0, false, {
                                                fileName: "src/components/IT20125202/admin/AdminHome.js",
                                                lineNumber: 55,
                                                columnNumber: 17
                                            }, this),
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("br", {}, void 0, false, {
                                                fileName: "src/components/IT20125202/admin/AdminHome.js",
                                                lineNumber: 56,
                                                columnNumber: 17
                                            }, this),
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("a", {
                                                type: "button",
                                                className: "btn btn-outline-dark",
                                                href: '/student/groups/view',
                                                children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("i", {
                                                    class: "fa fa-arrow-circle-o-right",
                                                    "aria-hidden": "true"
                                                }, void 0, false, {
                                                    fileName: "src/components/IT20125202/admin/AdminHome.js",
                                                    lineNumber: 57,
                                                    columnNumber: 97
                                                }, this)
                                            }, void 0, false, {
                                                fileName: "src/components/IT20125202/admin/AdminHome.js",
                                                lineNumber: 57,
                                                columnNumber: 17
                                            }, this)
                                        ]
                                    }, void 0, true, {
                                        fileName: "src/components/IT20125202/admin/AdminHome.js",
                                        lineNumber: 53,
                                        columnNumber: 15
                                    }, this)
                                }, void 0, false, {
                                    fileName: "src/components/IT20125202/admin/AdminHome.js",
                                    lineNumber: 52,
                                    columnNumber: 13
                                }, this),
                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                    className: "col p-3 mb-2 m-2 bg-light text-dark rounded",
                                    children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("center", {
                                        children: [
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("i", {
                                                class: "fa fa-book",
                                                "aria-hidden": "true"
                                            }, void 0, false, {
                                                fileName: "src/components/IT20125202/admin/AdminHome.js",
                                                lineNumber: 62,
                                                columnNumber: 17
                                            }, this),
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("h6", {
                                                children: "Topic Management"
                                            }, void 0, false, {
                                                fileName: "src/components/IT20125202/admin/AdminHome.js",
                                                lineNumber: 63,
                                                columnNumber: 17
                                            }, this),
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("br", {}, void 0, false, {
                                                fileName: "src/components/IT20125202/admin/AdminHome.js",
                                                lineNumber: 64,
                                                columnNumber: 17
                                            }, this),
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("a", {
                                                type: "button",
                                                className: "btn btn-outline-dark",
                                                href: '/admin/topiclist',
                                                children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("i", {
                                                    class: "fa fa-arrow-circle-o-right",
                                                    "aria-hidden": "true"
                                                }, void 0, false, {
                                                    fileName: "src/components/IT20125202/admin/AdminHome.js",
                                                    lineNumber: 65,
                                                    columnNumber: 93
                                                }, this)
                                            }, void 0, false, {
                                                fileName: "src/components/IT20125202/admin/AdminHome.js",
                                                lineNumber: 65,
                                                columnNumber: 17
                                            }, this)
                                        ]
                                    }, void 0, true, {
                                        fileName: "src/components/IT20125202/admin/AdminHome.js",
                                        lineNumber: 61,
                                        columnNumber: 15
                                    }, this)
                                }, void 0, false, {
                                    fileName: "src/components/IT20125202/admin/AdminHome.js",
                                    lineNumber: 60,
                                    columnNumber: 13
                                }, this)
                            ]
                        }, void 0, true, {
                            fileName: "src/components/IT20125202/admin/AdminHome.js",
                            lineNumber: 43,
                            columnNumber: 11
                        }, this),
                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                            className: "row",
                            style: {
                                height: "150px"
                            },
                            children: [
                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                    className: "col p-3 mb-2 m-2 bg-light text-dark rounded",
                                    children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("center", {
                                        children: [
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("i", {
                                                class: "fa fa-arrow-up",
                                                "aria-hidden": "true"
                                            }, void 0, false, {
                                                fileName: "src/components/IT20125202/admin/AdminHome.js",
                                                lineNumber: 73,
                                                columnNumber: 17
                                            }, this),
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("h6", {
                                                children: "Submission Management"
                                            }, void 0, false, {
                                                fileName: "src/components/IT20125202/admin/AdminHome.js",
                                                lineNumber: 74,
                                                columnNumber: 17
                                            }, this),
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("br", {}, void 0, false, {
                                                fileName: "src/components/IT20125202/admin/AdminHome.js",
                                                lineNumber: 75,
                                                columnNumber: 17
                                            }, this),
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("a", {
                                                type: "button",
                                                className: "btn btn-outline-dark",
                                                href: '/submitiontype/add',
                                                children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("i", {
                                                    class: "fa fa-arrow-circle-o-right",
                                                    "aria-hidden": "true"
                                                }, void 0, false, {
                                                    fileName: "src/components/IT20125202/admin/AdminHome.js",
                                                    lineNumber: 76,
                                                    columnNumber: 95
                                                }, this)
                                            }, void 0, false, {
                                                fileName: "src/components/IT20125202/admin/AdminHome.js",
                                                lineNumber: 76,
                                                columnNumber: 17
                                            }, this)
                                        ]
                                    }, void 0, true, {
                                        fileName: "src/components/IT20125202/admin/AdminHome.js",
                                        lineNumber: 72,
                                        columnNumber: 15
                                    }, this)
                                }, void 0, false, {
                                    fileName: "src/components/IT20125202/admin/AdminHome.js",
                                    lineNumber: 71,
                                    columnNumber: 13
                                }, this),
                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                    className: "col p-3 mb-2 m-2 bg-light text-dark rounded",
                                    children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("center", {
                                        children: [
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("i", {
                                                class: "fa fa-file-text",
                                                "aria-hidden": "true"
                                            }, void 0, false, {
                                                fileName: "src/components/IT20125202/admin/AdminHome.js",
                                                lineNumber: 81,
                                                columnNumber: 17
                                            }, this),
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("h6", {
                                                children: "Template Management"
                                            }, void 0, false, {
                                                fileName: "src/components/IT20125202/admin/AdminHome.js",
                                                lineNumber: 82,
                                                columnNumber: 17
                                            }, this),
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("br", {}, void 0, false, {
                                                fileName: "src/components/IT20125202/admin/AdminHome.js",
                                                lineNumber: 83,
                                                columnNumber: 17
                                            }, this),
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("a", {
                                                type: "button",
                                                className: "btn btn-outline-dark",
                                                href: '/documentTemp',
                                                children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("i", {
                                                    class: "fa fa-arrow-circle-o-right",
                                                    "aria-hidden": "true"
                                                }, void 0, false, {
                                                    fileName: "src/components/IT20125202/admin/AdminHome.js",
                                                    lineNumber: 84,
                                                    columnNumber: 90
                                                }, this)
                                            }, void 0, false, {
                                                fileName: "src/components/IT20125202/admin/AdminHome.js",
                                                lineNumber: 84,
                                                columnNumber: 17
                                            }, this)
                                        ]
                                    }, void 0, true, {
                                        fileName: "src/components/IT20125202/admin/AdminHome.js",
                                        lineNumber: 80,
                                        columnNumber: 15
                                    }, this)
                                }, void 0, false, {
                                    fileName: "src/components/IT20125202/admin/AdminHome.js",
                                    lineNumber: 79,
                                    columnNumber: 13
                                }, this),
                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                    className: "col p-3 mb-2 m-2 bg-light text-dark rounded",
                                    children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("center", {
                                        children: [
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("i", {
                                                class: "fa fa-table",
                                                "aria-hidden": "true"
                                            }, void 0, false, {
                                                fileName: "src/components/IT20125202/admin/AdminHome.js",
                                                lineNumber: 89,
                                                columnNumber: 17
                                            }, this),
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("h6", {
                                                children: "Marking Scheme Management"
                                            }, void 0, false, {
                                                fileName: "src/components/IT20125202/admin/AdminHome.js",
                                                lineNumber: 90,
                                                columnNumber: 17
                                            }, this),
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("br", {}, void 0, false, {
                                                fileName: "src/components/IT20125202/admin/AdminHome.js",
                                                lineNumber: 91,
                                                columnNumber: 17
                                            }, this),
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("a", {
                                                type: "button",
                                                className: "btn btn-outline-dark",
                                                href: '/view/marking',
                                                children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("i", {
                                                    class: "fa fa-arrow-circle-o-right",
                                                    "aria-hidden": "true"
                                                }, void 0, false, {
                                                    fileName: "src/components/IT20125202/admin/AdminHome.js",
                                                    lineNumber: 92,
                                                    columnNumber: 90
                                                }, this)
                                            }, void 0, false, {
                                                fileName: "src/components/IT20125202/admin/AdminHome.js",
                                                lineNumber: 92,
                                                columnNumber: 17
                                            }, this)
                                        ]
                                    }, void 0, true, {
                                        fileName: "src/components/IT20125202/admin/AdminHome.js",
                                        lineNumber: 88,
                                        columnNumber: 15
                                    }, this)
                                }, void 0, false, {
                                    fileName: "src/components/IT20125202/admin/AdminHome.js",
                                    lineNumber: 87,
                                    columnNumber: 13
                                }, this)
                            ]
                        }, void 0, true, {
                            fileName: "src/components/IT20125202/admin/AdminHome.js",
                            lineNumber: 70,
                            columnNumber: 11
                        }, this),
                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                            className: "row",
                            style: {
                                height: "150px"
                            },
                            children: [
                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                    className: "col p-3 mb-2 m-2 bg-light text-dark rounded",
                                    children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("center", {
                                        children: [
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("i", {
                                                class: "fa fa-user",
                                                "aria-hidden": "true"
                                            }, void 0, false, {
                                                fileName: "src/components/IT20125202/admin/AdminHome.js",
                                                lineNumber: 100,
                                                columnNumber: 17
                                            }, this),
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("h6", {
                                                children: "Test"
                                            }, void 0, false, {
                                                fileName: "src/components/IT20125202/admin/AdminHome.js",
                                                lineNumber: 101,
                                                columnNumber: 17
                                            }, this),
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("br", {}, void 0, false, {
                                                fileName: "src/components/IT20125202/admin/AdminHome.js",
                                                lineNumber: 102,
                                                columnNumber: 17
                                            }, this),
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("a", {
                                                type: "button",
                                                className: "btn btn-outline-dark",
                                                href: '#',
                                                children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("i", {
                                                    class: "fa fa-arrow-circle-o-right",
                                                    "aria-hidden": "true"
                                                }, void 0, false, {
                                                    fileName: "src/components/IT20125202/admin/AdminHome.js",
                                                    lineNumber: 103,
                                                    columnNumber: 78
                                                }, this)
                                            }, void 0, false, {
                                                fileName: "src/components/IT20125202/admin/AdminHome.js",
                                                lineNumber: 103,
                                                columnNumber: 17
                                            }, this)
                                        ]
                                    }, void 0, true, {
                                        fileName: "src/components/IT20125202/admin/AdminHome.js",
                                        lineNumber: 99,
                                        columnNumber: 15
                                    }, this)
                                }, void 0, false, {
                                    fileName: "src/components/IT20125202/admin/AdminHome.js",
                                    lineNumber: 98,
                                    columnNumber: 13
                                }, this),
                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                    className: "col p-3 mb-2 m-2 bg-light text-dark rounded",
                                    children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("center", {
                                        children: [
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("i", {
                                                class: "fa fa-users",
                                                "aria-hidden": "true"
                                            }, void 0, false, {
                                                fileName: "src/components/IT20125202/admin/AdminHome.js",
                                                lineNumber: 108,
                                                columnNumber: 17
                                            }, this),
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("h6", {
                                                children: "Test"
                                            }, void 0, false, {
                                                fileName: "src/components/IT20125202/admin/AdminHome.js",
                                                lineNumber: 109,
                                                columnNumber: 17
                                            }, this),
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("br", {}, void 0, false, {
                                                fileName: "src/components/IT20125202/admin/AdminHome.js",
                                                lineNumber: 110,
                                                columnNumber: 17
                                            }, this),
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("a", {
                                                type: "button",
                                                className: "btn btn-outline-dark",
                                                href: '#',
                                                children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("i", {
                                                    class: "fa fa-arrow-circle-o-right",
                                                    "aria-hidden": "true"
                                                }, void 0, false, {
                                                    fileName: "src/components/IT20125202/admin/AdminHome.js",
                                                    lineNumber: 111,
                                                    columnNumber: 78
                                                }, this)
                                            }, void 0, false, {
                                                fileName: "src/components/IT20125202/admin/AdminHome.js",
                                                lineNumber: 111,
                                                columnNumber: 17
                                            }, this)
                                        ]
                                    }, void 0, true, {
                                        fileName: "src/components/IT20125202/admin/AdminHome.js",
                                        lineNumber: 107,
                                        columnNumber: 15
                                    }, this)
                                }, void 0, false, {
                                    fileName: "src/components/IT20125202/admin/AdminHome.js",
                                    lineNumber: 106,
                                    columnNumber: 13
                                }, this),
                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                    className: "col p-3 mb-2 m-2 bg-light text-dark rounded",
                                    children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("center", {
                                        children: [
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("i", {
                                                class: "fa fa-book",
                                                "aria-hidden": "true"
                                            }, void 0, false, {
                                                fileName: "src/components/IT20125202/admin/AdminHome.js",
                                                lineNumber: 116,
                                                columnNumber: 17
                                            }, this),
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("h6", {
                                                children: "Test"
                                            }, void 0, false, {
                                                fileName: "src/components/IT20125202/admin/AdminHome.js",
                                                lineNumber: 117,
                                                columnNumber: 17
                                            }, this),
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("br", {}, void 0, false, {
                                                fileName: "src/components/IT20125202/admin/AdminHome.js",
                                                lineNumber: 118,
                                                columnNumber: 17
                                            }, this),
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("a", {
                                                type: "button",
                                                className: "btn btn-outline-dark",
                                                href: '#',
                                                children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("i", {
                                                    class: "fa fa-arrow-circle-o-right",
                                                    "aria-hidden": "true"
                                                }, void 0, false, {
                                                    fileName: "src/components/IT20125202/admin/AdminHome.js",
                                                    lineNumber: 119,
                                                    columnNumber: 78
                                                }, this)
                                            }, void 0, false, {
                                                fileName: "src/components/IT20125202/admin/AdminHome.js",
                                                lineNumber: 119,
                                                columnNumber: 17
                                            }, this)
                                        ]
                                    }, void 0, true, {
                                        fileName: "src/components/IT20125202/admin/AdminHome.js",
                                        lineNumber: 115,
                                        columnNumber: 15
                                    }, this)
                                }, void 0, false, {
                                    fileName: "src/components/IT20125202/admin/AdminHome.js",
                                    lineNumber: 114,
                                    columnNumber: 13
                                }, this)
                            ]
                        }, void 0, true, {
                            fileName: "src/components/IT20125202/admin/AdminHome.js",
                            lineNumber: 97,
                            columnNumber: 11
                        }, this),
                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("br", {}, void 0, false, {
                            fileName: "src/components/IT20125202/admin/AdminHome.js",
                            lineNumber: 123,
                            columnNumber: 11
                        }, this)
                    ]
                }, void 0, true, {
                    fileName: "src/components/IT20125202/admin/AdminHome.js",
                    lineNumber: 42,
                    columnNumber: 9
                }, this)
            ]
        }, void 0, true, {
            fileName: "src/components/IT20125202/admin/AdminHome.js",
            lineNumber: 27,
            columnNumber: 7
        }, this);
    }
}
exports.default = AdminHome;

  $parcel$ReactRefreshHelpers$0764.postlude(module);
} finally {
  window.$RefreshReg$ = prevRefreshReg;
  window.$RefreshSig$ = prevRefreshSig;
}
},{"react/jsx-dev-runtime":"iTorj","react":"21dqq","./AdminNavBar":"kb2lK","../../../images/home.jpg":"ePNea","sweetalert":"38a65","@parcel/transformer-js/src/esmodule-helpers.js":"gkKU3","@parcel/transformer-react-refresh-wrap/lib/helpers/helpers.js":"km3Ru"}],"ePNea":[function(require,module,exports) {
module.exports = require('./helpers/bundle-url').getBundleURL('bLxZJ') + "home.0a1d7060.jpg" + "?" + Date.now();

},{"./helpers/bundle-url":"lgJ39"}],"cJRui":[function(require,module,exports) {
var $parcel$ReactRefreshHelpers$eca1 = require("@parcel/transformer-react-refresh-wrap/lib/helpers/helpers.js");
var prevRefreshReg = window.$RefreshReg$;
var prevRefreshSig = window.$RefreshSig$;
$parcel$ReactRefreshHelpers$eca1.prelude(module);

try {
var parcelHelpers = require("@parcel/transformer-js/src/esmodule-helpers.js");
parcelHelpers.defineInteropFlag(exports);
var _jsxDevRuntime = require("react/jsx-dev-runtime");
// view the topic list - admin  
var _react = require("react");
var _reactDefault = parcelHelpers.interopDefault(_react);
var _axios = require("axios");
var _axiosDefault = parcelHelpers.interopDefault(_axios);
var _adminNavBar = require("../admin/AdminNavBar");
var _adminNavBarDefault = parcelHelpers.interopDefault(_adminNavBar);
var _sweetalert = require("sweetalert");
var _sweetalertDefault = parcelHelpers.interopDefault(_sweetalert);
class ViewListAdmin extends _react.Component {
    constructor(props){
        super(props);
        this.state = {
            topics: []
        };
    }
    componentDidMount() {
        document.title = "Research Topic Requests";
        // redirect to the login page if the user is not logged in
        if (!localStorage.adminToken) _sweetalertDefault.default("Please login first", "", "warning").then((value)=>{
            if (value) {
                this.props.history.push(`/admin/login`);
                window.location.reload();
            }
        });
        this.retrieveTopics();
    }
    //method for get request
    retrieveTopics() {
        _axiosDefault.default.get('http://localhost:5000/topics').then((res)=>{
            if (res.data.success) this.setState({
                topics: res.data.existingTopics
            });
        });
    }
    onDelete = (id)=>{
        //with a confirmation 
        _sweetalertDefault.default({
            title: "Are you sure?",
            text: "Once deleted, you will not be able to recover this topic request",
            icon: "warning",
            buttons: true,
            dangerMode: true
        }).then((willDelete)=>{
            if (willDelete) _axiosDefault.default.delete(`http://localhost:5000/topic/delete/${id}`).then((res)=>{
                _sweetalertDefault.default("Deleted Permanently!", "", "success").then((value)=>{
                    if (value) this.retrieveTopics();
                });
            });
            else _sweetalertDefault.default("Cancelled.");
        });
    };
    handleSearchArea = (e)=>{
        // console.log(e.currentTarget.value)
        const searchKey = e.currentTarget.value;
        _axiosDefault.default.get('http://localhost:5000/topics').then((res)=>{
            if (res.data.success) this.filterData(res.data.existingTopics, searchKey);
        });
    };
    filterData(topics, searchKey) {
        const searchResult = topics.filter((topic)=>topic.groupId.toLowerCase().includes(searchKey) || topic.topicR.toLowerCase().includes(searchKey) || topic.status.toLowerCase().includes(searchKey) || topic.groupId.toUpperCase().includes(searchKey) || topic.topicR.toUpperCase().includes(searchKey) || topic.status.toUpperCase().includes(searchKey) || topic.groupId.includes(searchKey) || topic.topicR.includes(searchKey) || topic.status.includes(searchKey)
        );
        this.setState({
            topics: searchResult
        });
    }
    render() {
        return /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
            className: "container",
            children: [
                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("br", {}, void 0, false, {
                    fileName: "src/components/IT20125202/topic/ViewListAdmin.js",
                    lineNumber: 114,
                    columnNumber: 9
                }, this),
                /*#__PURE__*/ _jsxDevRuntime.jsxDEV(_adminNavBarDefault.default, {}, void 0, false, {
                    fileName: "src/components/IT20125202/topic/ViewListAdmin.js",
                    lineNumber: 115,
                    columnNumber: 9
                }, this),
                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("br", {}, void 0, false, {
                    fileName: "src/components/IT20125202/topic/ViewListAdmin.js",
                    lineNumber: 116,
                    columnNumber: 9
                }, this),
                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                    className: "container",
                    children: [
                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                            className: "row",
                            children: [
                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                    className: "col-lg-9 mt-2 mb-2",
                                    children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("h1", {
                                        children: "Topics"
                                    }, void 0, false, {
                                        fileName: "src/components/IT20125202/topic/ViewListAdmin.js",
                                        lineNumber: 121,
                                        columnNumber: 15
                                    }, this)
                                }, void 0, false, {
                                    fileName: "src/components/IT20125202/topic/ViewListAdmin.js",
                                    lineNumber: 120,
                                    columnNumber: 13
                                }, this),
                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                    className: "col-lg-3 mt-2 mb-2",
                                    children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("input", {
                                        className: "form-control",
                                        type: "search",
                                        placeholder: "Search",
                                        name: "searchQuery",
                                        onChange: this.handleSearchArea
                                    }, void 0, false, {
                                        fileName: "src/components/IT20125202/topic/ViewListAdmin.js",
                                        lineNumber: 124,
                                        columnNumber: 15
                                    }, this)
                                }, void 0, false, {
                                    fileName: "src/components/IT20125202/topic/ViewListAdmin.js",
                                    lineNumber: 123,
                                    columnNumber: 13
                                }, this),
                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("hr", {}, void 0, false, {
                                    fileName: "src/components/IT20125202/topic/ViewListAdmin.js",
                                    lineNumber: 132,
                                    columnNumber: 13
                                }, this),
                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("br", {}, void 0, false, {
                                    fileName: "src/components/IT20125202/topic/ViewListAdmin.js",
                                    lineNumber: 132,
                                    columnNumber: 18
                                }, this)
                            ]
                        }, void 0, true, {
                            fileName: "src/components/IT20125202/topic/ViewListAdmin.js",
                            lineNumber: 119,
                            columnNumber: 11
                        }, this),
                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("table", {
                            className: "table",
                            children: [
                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("thead", {
                                    children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("tr", {
                                        children: [
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("th", {
                                                scope: "col",
                                                children: " # "
                                            }, void 0, false, {
                                                fileName: "src/components/IT20125202/topic/ViewListAdmin.js",
                                                lineNumber: 138,
                                                columnNumber: 17
                                            }, this),
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("th", {
                                                scope: "col",
                                                children: " Group ID "
                                            }, void 0, false, {
                                                fileName: "src/components/IT20125202/topic/ViewListAdmin.js",
                                                lineNumber: 139,
                                                columnNumber: 17
                                            }, this),
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("th", {
                                                scope: "col",
                                                children: " Topic "
                                            }, void 0, false, {
                                                fileName: "src/components/IT20125202/topic/ViewListAdmin.js",
                                                lineNumber: 140,
                                                columnNumber: 17
                                            }, this),
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("th", {
                                                scope: "col",
                                                children: " Description "
                                            }, void 0, false, {
                                                fileName: "src/components/IT20125202/topic/ViewListAdmin.js",
                                                lineNumber: 141,
                                                columnNumber: 17
                                            }, this),
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("th", {
                                                scope: "col",
                                                children: " Status "
                                            }, void 0, false, {
                                                fileName: "src/components/IT20125202/topic/ViewListAdmin.js",
                                                lineNumber: 142,
                                                columnNumber: 17
                                            }, this),
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("th", {
                                                scope: "col",
                                                children: " Comments "
                                            }, void 0, false, {
                                                fileName: "src/components/IT20125202/topic/ViewListAdmin.js",
                                                lineNumber: 143,
                                                columnNumber: 17
                                            }, this),
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("th", {
                                                children: "Action"
                                            }, void 0, false, {
                                                fileName: "src/components/IT20125202/topic/ViewListAdmin.js",
                                                lineNumber: 144,
                                                columnNumber: 17
                                            }, this)
                                        ]
                                    }, void 0, true, {
                                        fileName: "src/components/IT20125202/topic/ViewListAdmin.js",
                                        lineNumber: 137,
                                        columnNumber: 15
                                    }, this)
                                }, void 0, false, {
                                    fileName: "src/components/IT20125202/topic/ViewListAdmin.js",
                                    lineNumber: 136,
                                    columnNumber: 13
                                }, this),
                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("tbody", {
                                    children: this.state.topics.map((topics, index)=>/*#__PURE__*/ _jsxDevRuntime.jsxDEV("tr", {
                                            children: [
                                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("th", {
                                                    scope: "row",
                                                    children: index + 1
                                                }, void 0, false, {
                                                    fileName: "src/components/IT20125202/topic/ViewListAdmin.js",
                                                    lineNumber: 151,
                                                    columnNumber: 19
                                                }, this),
                                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("td", {
                                                    children: topics.groupId
                                                }, void 0, false, {
                                                    fileName: "src/components/IT20125202/topic/ViewListAdmin.js",
                                                    lineNumber: 152,
                                                    columnNumber: 19
                                                }, this),
                                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("td", {
                                                    children: topics.topicR
                                                }, void 0, false, {
                                                    fileName: "src/components/IT20125202/topic/ViewListAdmin.js",
                                                    lineNumber: 158,
                                                    columnNumber: 19
                                                }, this),
                                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("td", {
                                                    children: topics.description
                                                }, void 0, false, {
                                                    fileName: "src/components/IT20125202/topic/ViewListAdmin.js",
                                                    lineNumber: 159,
                                                    columnNumber: 19
                                                }, this),
                                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("td", {
                                                    children: topics.status
                                                }, void 0, false, {
                                                    fileName: "src/components/IT20125202/topic/ViewListAdmin.js",
                                                    lineNumber: 160,
                                                    columnNumber: 19
                                                }, this),
                                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("td", {
                                                    children: topics.comments
                                                }, void 0, false, {
                                                    fileName: "src/components/IT20125202/topic/ViewListAdmin.js",
                                                    lineNumber: 161,
                                                    columnNumber: 19
                                                }, this),
                                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("td", {
                                                    children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("a", {
                                                        className: "btn btn-outline-danger",
                                                        href: "#",
                                                        onClick: ()=>this.onDelete(topics._id)
                                                        ,
                                                        children: [
                                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("i", {
                                                                className: "fas fa-trash"
                                                            }, void 0, false, {
                                                                fileName: "src/components/IT20125202/topic/ViewListAdmin.js",
                                                                lineNumber: 168,
                                                                columnNumber: 23
                                                            }, this),
                                                            " \xa0Delete"
                                                        ]
                                                    }, void 0, true, {
                                                        fileName: "src/components/IT20125202/topic/ViewListAdmin.js",
                                                        lineNumber: 167,
                                                        columnNumber: 21
                                                    }, this)
                                                }, void 0, false, {
                                                    fileName: "src/components/IT20125202/topic/ViewListAdmin.js",
                                                    lineNumber: 162,
                                                    columnNumber: 19
                                                }, this)
                                            ]
                                        }, index, true, {
                                            fileName: "src/components/IT20125202/topic/ViewListAdmin.js",
                                            lineNumber: 150,
                                            columnNumber: 17
                                        }, this)
                                    )
                                }, void 0, false, {
                                    fileName: "src/components/IT20125202/topic/ViewListAdmin.js",
                                    lineNumber: 148,
                                    columnNumber: 13
                                }, this)
                            ]
                        }, void 0, true, {
                            fileName: "src/components/IT20125202/topic/ViewListAdmin.js",
                            lineNumber: 135,
                            columnNumber: 11
                        }, this)
                    ]
                }, void 0, true, {
                    fileName: "src/components/IT20125202/topic/ViewListAdmin.js",
                    lineNumber: 118,
                    columnNumber: 9
                }, this)
            ]
        }, void 0, true, {
            fileName: "src/components/IT20125202/topic/ViewListAdmin.js",
            lineNumber: 113,
            columnNumber: 7
        }, this);
    }
}
exports.default = ViewListAdmin;

  $parcel$ReactRefreshHelpers$eca1.postlude(module);
} finally {
  window.$RefreshReg$ = prevRefreshReg;
  window.$RefreshSig$ = prevRefreshSig;
}
},{"react/jsx-dev-runtime":"iTorj","react":"21dqq","axios":"jo6P5","../admin/AdminNavBar":"kb2lK","sweetalert":"38a65","@parcel/transformer-js/src/esmodule-helpers.js":"gkKU3","@parcel/transformer-react-refresh-wrap/lib/helpers/helpers.js":"km3Ru"}],"hj8RA":[function(require,module,exports) {
var $parcel$ReactRefreshHelpers$7446 = require("@parcel/transformer-react-refresh-wrap/lib/helpers/helpers.js");
var prevRefreshReg = window.$RefreshReg$;
var prevRefreshSig = window.$RefreshSig$;
$parcel$ReactRefreshHelpers$7446.prelude(module);

try {
var parcelHelpers = require("@parcel/transformer-js/src/esmodule-helpers.js");
parcelHelpers.defineInteropFlag(exports);
var _jsxDevRuntime = require("react/jsx-dev-runtime");
var _react = require("react");
var _sweetalert = require("sweetalert");
var _sweetalertDefault = parcelHelpers.interopDefault(_sweetalert);
var _coverJpg = require("../../../images/cover.jpg");
var _coverJpgDefault = parcelHelpers.interopDefault(_coverJpg);
var _s = $RefreshSig$();
function AdminLogin() {
    _s();
    const [username, setUsername] = _react.useState('');
    const [password, setPassword] = _react.useState('');
    async function loginAdmin(e) {
        e.preventDefault();
        if (username === '' || password === '') _sweetalertDefault.default({
            title: "",
            text: "Please fill ID number and password",
            icon: "warning"
        });
        else {
            const res = await fetch('https://rpmt-server.herokuapp.com/admin', {
                method: 'POST',
                headers: {
                    'Content-Type': 'application/json'
                },
                body: JSON.stringify({
                    username,
                    password
                })
            });
            const data = await res.json();
            if (data.admin) _sweetalertDefault.default("Login successful!", "", "success").then((value)=>{
                if (value) {
                    localStorage.setItem('token', data.admin);
                    window.location.href = '/admin/home';
                }
            });
            else _sweetalertDefault.default({
                title: "",
                text: "Please check your ID number and password",
                icon: "warning"
            });
        }
    }
    return(// <div className="jumbotron" style={{ paddingLeft: '50px', paddingRight: '50px', paddingTop: '10px', backgroundSize: 'cover', height: '100vh', backgroundImage: 'linear-gradient(to right, gray, #2B3856, white, #2B3856, gray, #2B3856, white, #2B3856, gray)' }}>
    // <div className="jumbotron" style={{ paddingLeft: '50px', paddingRight: '50px', paddingTop: '10px', backgroundSize: 'cover', height: '100vh', background: '#36454F' }}>
    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
        className: "jumbotron",
        style: {
            paddingLeft: '50px',
            paddingRight: '50px',
            paddingBottom: '50px',
            paddingTop: '10px',
            backgroundImage: `url(${_coverJpgDefault.default})`,
            backgroundRepeat: 'no-repeat',
            backgroundSize: 'cover',
            margin: '0px 0px 0px 0px',
            overflow: 'hidden',
            minHeight: '100vh'
        },
        children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
            className: "container",
            style: {
                marginTop: '50px',
                marginBottom: '50px',
                backgroundColor: 'white',
                paddingBottom: '200px',
                paddingTop: '50px',
                paddingLeft: '100px',
                paddingRight: '100px'
            },
            children: [
                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("h1", {
                    style: {
                        textAlign: 'center',
                        paddingBottom: '50px'
                    },
                    children: "Research Project Management Tool"
                }, void 0, false, {
                    fileName: "src/components/IT20125202/admin/AdminLogin.js",
                    lineNumber: 58,
                    columnNumber: 17
                }, this),
                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                    className: "col-md-8 mt-4 mx-auto",
                    children: [
                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("br", {}, void 0, false, {
                            fileName: "src/components/IT20125202/admin/AdminLogin.js",
                            lineNumber: 60,
                            columnNumber: 21
                        }, this),
                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("h3", {
                            className: "h3 mb-3 font-weight-normal",
                            style: {
                                textAlign: 'center'
                            },
                            children: "Admin Login"
                        }, void 0, false, {
                            fileName: "src/components/IT20125202/admin/AdminLogin.js",
                            lineNumber: 61,
                            columnNumber: 21
                        }, this),
                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("form", {
                            className: "needs-validation",
                            noValidate: true,
                            children: [
                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                    className: "form-group",
                                    style: {
                                        marginBottom: '15px'
                                    },
                                    children: [
                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("label", {
                                            style: {
                                                marginBottom: '5px'
                                            },
                                            children: "Username"
                                        }, void 0, false, {
                                            fileName: "src/components/IT20125202/admin/AdminLogin.js",
                                            lineNumber: 65,
                                            columnNumber: 29
                                        }, this),
                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("input", {
                                            type: "text",
                                            className: "form-control",
                                            name: "username",
                                            placeholder: "Please enter the username",
                                            required: true,
                                            value: username,
                                            onChange: (e)=>setUsername(e.target.value)
                                        }, void 0, false, {
                                            fileName: "src/components/IT20125202/admin/AdminLogin.js",
                                            lineNumber: 66,
                                            columnNumber: 29
                                        }, this)
                                    ]
                                }, void 0, true, {
                                    fileName: "src/components/IT20125202/admin/AdminLogin.js",
                                    lineNumber: 64,
                                    columnNumber: 25
                                }, this),
                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                    className: "form-group",
                                    style: {
                                        marginBottom: '15px'
                                    },
                                    children: [
                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("label", {
                                            style: {
                                                marginBottom: '5px'
                                            },
                                            children: "Password"
                                        }, void 0, false, {
                                            fileName: "src/components/IT20125202/admin/AdminLogin.js",
                                            lineNumber: 77,
                                            columnNumber: 29
                                        }, this),
                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("input", {
                                            type: "password",
                                            className: "form-control",
                                            name: "password",
                                            placeholder: "Please enter the password",
                                            required: true,
                                            value: password,
                                            onChange: (e)=>setPassword(e.target.value)
                                        }, void 0, false, {
                                            fileName: "src/components/IT20125202/admin/AdminLogin.js",
                                            lineNumber: 78,
                                            columnNumber: 29
                                        }, this)
                                    ]
                                }, void 0, true, {
                                    fileName: "src/components/IT20125202/admin/AdminLogin.js",
                                    lineNumber: 76,
                                    columnNumber: 25
                                }, this),
                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                    class: "d-grid gap-2",
                                    children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("button", {
                                        className: "btn btn-outline-dark",
                                        type: "submit",
                                        style: {
                                            marginTop: '15px',
                                            width: 'cover'
                                        },
                                        onClick: loginAdmin,
                                        children: "Login"
                                    }, void 0, false, {
                                        fileName: "src/components/IT20125202/admin/AdminLogin.js",
                                        lineNumber: 91,
                                        columnNumber: 29
                                    }, this)
                                }, void 0, false, {
                                    fileName: "src/components/IT20125202/admin/AdminLogin.js",
                                    lineNumber: 90,
                                    columnNumber: 25
                                }, this)
                            ]
                        }, void 0, true, {
                            fileName: "src/components/IT20125202/admin/AdminLogin.js",
                            lineNumber: 63,
                            columnNumber: 21
                        }, this)
                    ]
                }, void 0, true, {
                    fileName: "src/components/IT20125202/admin/AdminLogin.js",
                    lineNumber: 59,
                    columnNumber: 17
                }, this)
            ]
        }, void 0, true, {
            fileName: "src/components/IT20125202/admin/AdminLogin.js",
            lineNumber: 57,
            columnNumber: 13
        }, this)
    }, void 0, false, {
        fileName: "src/components/IT20125202/admin/AdminLogin.js",
        lineNumber: 55,
        columnNumber: 9
    }, this));
}
_s(AdminLogin, "wuQOK7xaXdVz4RMrZQhWbI751Oc=");
_c = AdminLogin;
exports.default = AdminLogin;
var _c;
$RefreshReg$(_c, "AdminLogin");

  $parcel$ReactRefreshHelpers$7446.postlude(module);
} finally {
  window.$RefreshReg$ = prevRefreshReg;
  window.$RefreshSig$ = prevRefreshSig;
}
},{"react/jsx-dev-runtime":"iTorj","react":"21dqq","sweetalert":"38a65","../../../images/cover.jpg":"i9AMb","@parcel/transformer-js/src/esmodule-helpers.js":"gkKU3","@parcel/transformer-react-refresh-wrap/lib/helpers/helpers.js":"km3Ru"}],"i9AMb":[function(require,module,exports) {
module.exports = require('./helpers/bundle-url').getBundleURL('bLxZJ') + "cover.d2d9f605.jpg" + "?" + Date.now();

},{"./helpers/bundle-url":"lgJ39"}],"1v78j":[function(require,module,exports) {
var $parcel$ReactRefreshHelpers$aaac = require("@parcel/transformer-react-refresh-wrap/lib/helpers/helpers.js");
var prevRefreshReg = window.$RefreshReg$;
var prevRefreshSig = window.$RefreshSig$;
$parcel$ReactRefreshHelpers$aaac.prelude(module);

try {
var parcelHelpers = require("@parcel/transformer-js/src/esmodule-helpers.js");
parcelHelpers.defineInteropFlag(exports);
var _jsxDevRuntime = require("react/jsx-dev-runtime");
var _react = require("react");
var _reactDefault = parcelHelpers.interopDefault(_react);
var _axios = require("axios");
var _axiosDefault = parcelHelpers.interopDefault(_axios);
var _jsFileDownload = require("js-file-download");
var _jsFileDownloadDefault = parcelHelpers.interopDefault(_jsFileDownload);
var _adminNavBar = require("../../IT20125202/admin/AdminNavBar");
var _adminNavBarDefault = parcelHelpers.interopDefault(_adminNavBar);
var _sweetalert = require("sweetalert");
var _sweetalertDefault = parcelHelpers.interopDefault(_sweetalert);
class DocumentTemp_Admin extends _react.Component {
    constructor(props){
        super(props);
        this.state = {
            documentTemp: []
        };
    }
    componentDidMount() {
        this.retrieveDocumentTemp();
    }
    // To get all data
    retrieveDocumentTemp() {
        _axiosDefault.default.get("http://localhost:5000/template/getAll").then((res)=>{
            if (res.data.success) {
                this.setState({
                    documentTemp: res.data.exsitingDocumentTemp
                });
                console.log(this.state.documentTemp);
            }
        });
    }
    // To delete data
    onDelete = (id)=>{
        _sweetalertDefault.default({
            title: "Are you sure?",
            text: "Delete this Document",
            icon: "warning",
            buttons: true,
            dangerMode: true
        }).then((willDelete)=>{
            if (willDelete) _axiosDefault.default.delete(`http://localhost:5000/document/delete/${id}`).then((res)=>{
                _sweetalertDefault.default("Deleted Successfull!", {
                    icon: "success"
                });
                setTimeout(()=>{
                    this.retrieveDocumentTemp();
                }, 1000);
            });
        });
    // axios.delete(`http://localhost:5000/document/delete/${id}`).then((res) => {
    //   alert("Delete Successful");
    //   this.retrieveDocumentTemp();
    // });
    };
    // Download File
    downloadFile = (fileName)=>{
        const data = {
            fileName: fileName
        };
        _axiosDefault.default({
            url: "http://localhost:5000/file/download",
            data,
            method: "POST",
            responseType: "blob"
        }).then((res)=>{
            console.log(res);
            _jsFileDownloadDefault.default(res.data, fileName);
        });
    };
    render() {
        return /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
            className: "container",
            children: [
                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("br", {}, void 0, false, {
                    fileName: "src/components/IT20127046/admin/DocumentTemp_Admin.js",
                    lineNumber: 81,
                    columnNumber: 9
                }, this),
                /*#__PURE__*/ _jsxDevRuntime.jsxDEV(_adminNavBarDefault.default, {}, void 0, false, {
                    fileName: "src/components/IT20127046/admin/DocumentTemp_Admin.js",
                    lineNumber: 82,
                    columnNumber: 9
                }, this),
                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("br", {}, void 0, false, {
                    fileName: "src/components/IT20127046/admin/DocumentTemp_Admin.js",
                    lineNumber: 83,
                    columnNumber: 9
                }, this),
                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("h4", {
                    children: "Document Template"
                }, void 0, false, {
                    fileName: "src/components/IT20127046/admin/DocumentTemp_Admin.js",
                    lineNumber: 84,
                    columnNumber: 9
                }, this),
                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("hr", {}, void 0, false, {
                    fileName: "src/components/IT20127046/admin/DocumentTemp_Admin.js",
                    lineNumber: 85,
                    columnNumber: 9
                }, this),
                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                    className: "container",
                    children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                        className: "row",
                        children: [
                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                className: "col-3",
                                children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                    children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("center", {
                                        children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("a", {
                                            className: "btn btn-outline-success m-2",
                                            href: "/add/documentTemp",
                                            children: "Add Template"
                                        }, void 0, false, {
                                            fileName: "src/components/IT20127046/admin/DocumentTemp_Admin.js",
                                            lineNumber: 92,
                                            columnNumber: 19
                                        }, this)
                                    }, void 0, false, {
                                        fileName: "src/components/IT20127046/admin/DocumentTemp_Admin.js",
                                        lineNumber: 91,
                                        columnNumber: 17
                                    }, this)
                                }, void 0, false, {
                                    fileName: "src/components/IT20127046/admin/DocumentTemp_Admin.js",
                                    lineNumber: 90,
                                    columnNumber: 15
                                }, this)
                            }, void 0, false, {
                                fileName: "src/components/IT20127046/admin/DocumentTemp_Admin.js",
                                lineNumber: 89,
                                columnNumber: 13
                            }, this),
                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                className: "col-9",
                                children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                    className: "container",
                                    children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("table", {
                                        className: "table",
                                        children: [
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("thead", {
                                                children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("tr", {
                                                    children: [
                                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("th", {
                                                            scope: "col",
                                                            children: "No"
                                                        }, void 0, false, {
                                                            fileName: "src/components/IT20127046/admin/DocumentTemp_Admin.js",
                                                            lineNumber: 106,
                                                            columnNumber: 23
                                                        }, this),
                                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("th", {
                                                            scope: "col",
                                                            children: "Document Type"
                                                        }, void 0, false, {
                                                            fileName: "src/components/IT20127046/admin/DocumentTemp_Admin.js",
                                                            lineNumber: 107,
                                                            columnNumber: 23
                                                        }, this),
                                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("th", {
                                                            scope: "col",
                                                            children: "Other Type"
                                                        }, void 0, false, {
                                                            fileName: "src/components/IT20127046/admin/DocumentTemp_Admin.js",
                                                            lineNumber: 108,
                                                            columnNumber: 23
                                                        }, this),
                                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("th", {
                                                            scope: "col",
                                                            children: "Description"
                                                        }, void 0, false, {
                                                            fileName: "src/components/IT20127046/admin/DocumentTemp_Admin.js",
                                                            lineNumber: 109,
                                                            columnNumber: 23
                                                        }, this),
                                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("th", {
                                                            scope: "col",
                                                            children: "File"
                                                        }, void 0, false, {
                                                            fileName: "src/components/IT20127046/admin/DocumentTemp_Admin.js",
                                                            lineNumber: 110,
                                                            columnNumber: 23
                                                        }, this),
                                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("th", {
                                                            scope: "col",
                                                            children: "Preview"
                                                        }, void 0, false, {
                                                            fileName: "src/components/IT20127046/admin/DocumentTemp_Admin.js",
                                                            lineNumber: 111,
                                                            columnNumber: 23
                                                        }, this),
                                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("th", {
                                                            scope: "col",
                                                            children: "Update"
                                                        }, void 0, false, {
                                                            fileName: "src/components/IT20127046/admin/DocumentTemp_Admin.js",
                                                            lineNumber: 112,
                                                            columnNumber: 23
                                                        }, this),
                                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("th", {
                                                            scope: "col",
                                                            children: "Delete"
                                                        }, void 0, false, {
                                                            fileName: "src/components/IT20127046/admin/DocumentTemp_Admin.js",
                                                            lineNumber: 113,
                                                            columnNumber: 23
                                                        }, this)
                                                    ]
                                                }, void 0, true, {
                                                    fileName: "src/components/IT20127046/admin/DocumentTemp_Admin.js",
                                                    lineNumber: 105,
                                                    columnNumber: 21
                                                }, this)
                                            }, void 0, false, {
                                                fileName: "src/components/IT20127046/admin/DocumentTemp_Admin.js",
                                                lineNumber: 104,
                                                columnNumber: 19
                                            }, this),
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("tbody", {
                                                children: this.state.documentTemp.map((documentTemp, index)=>/*#__PURE__*/ _jsxDevRuntime.jsxDEV("tr", {
                                                        children: [
                                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("th", {
                                                                scope: "row",
                                                                children: index + 1
                                                            }, void 0, false, {
                                                                fileName: "src/components/IT20127046/admin/DocumentTemp_Admin.js",
                                                                lineNumber: 119,
                                                                columnNumber: 25
                                                            }, this),
                                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("td", {
                                                                children: documentTemp.documentType
                                                            }, void 0, false, {
                                                                fileName: "src/components/IT20127046/admin/DocumentTemp_Admin.js",
                                                                lineNumber: 120,
                                                                columnNumber: 25
                                                            }, this),
                                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("td", {
                                                                children: documentTemp.otherType
                                                            }, void 0, false, {
                                                                fileName: "src/components/IT20127046/admin/DocumentTemp_Admin.js",
                                                                lineNumber: 121,
                                                                columnNumber: 25
                                                            }, this),
                                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("td", {
                                                                children: documentTemp.description
                                                            }, void 0, false, {
                                                                fileName: "src/components/IT20127046/admin/DocumentTemp_Admin.js",
                                                                lineNumber: 122,
                                                                columnNumber: 25
                                                            }, this),
                                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("td", {
                                                                children: documentTemp.files
                                                            }, void 0, false, {
                                                                fileName: "src/components/IT20127046/admin/DocumentTemp_Admin.js",
                                                                lineNumber: 123,
                                                                columnNumber: 25
                                                            }, this),
                                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("td", {
                                                                children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("a", {
                                                                    className: "btn btn-outline-primary m-2",
                                                                    onClick: ()=>this.downloadFile(documentTemp.files)
                                                                    ,
                                                                    children: "Download"
                                                                }, void 0, false, {
                                                                    fileName: "src/components/IT20127046/admin/DocumentTemp_Admin.js",
                                                                    lineNumber: 125,
                                                                    columnNumber: 27
                                                                }, this)
                                                            }, void 0, false, {
                                                                fileName: "src/components/IT20127046/admin/DocumentTemp_Admin.js",
                                                                lineNumber: 124,
                                                                columnNumber: 25
                                                            }, this),
                                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("td", {
                                                                children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("a", {
                                                                    className: "btn btn-outline-success m-2",
                                                                    href: `/edit/documentTemp/${documentTemp._id}`,
                                                                    children: "Update"
                                                                }, void 0, false, {
                                                                    fileName: "src/components/IT20127046/admin/DocumentTemp_Admin.js",
                                                                    lineNumber: 135,
                                                                    columnNumber: 27
                                                                }, this)
                                                            }, void 0, false, {
                                                                fileName: "src/components/IT20127046/admin/DocumentTemp_Admin.js",
                                                                lineNumber: 134,
                                                                columnNumber: 25
                                                            }, this),
                                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("td", {
                                                                children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("a", {
                                                                    className: "btn btn-outline-danger m-2",
                                                                    onClick: ()=>this.onDelete(documentTemp._id)
                                                                    ,
                                                                    children: "Delete"
                                                                }, void 0, false, {
                                                                    fileName: "src/components/IT20127046/admin/DocumentTemp_Admin.js",
                                                                    lineNumber: 143,
                                                                    columnNumber: 27
                                                                }, this)
                                                            }, void 0, false, {
                                                                fileName: "src/components/IT20127046/admin/DocumentTemp_Admin.js",
                                                                lineNumber: 142,
                                                                columnNumber: 25
                                                            }, this)
                                                        ]
                                                    }, index, true, {
                                                        fileName: "src/components/IT20127046/admin/DocumentTemp_Admin.js",
                                                        lineNumber: 118,
                                                        columnNumber: 23
                                                    }, this)
                                                )
                                            }, void 0, false, {
                                                fileName: "src/components/IT20127046/admin/DocumentTemp_Admin.js",
                                                lineNumber: 116,
                                                columnNumber: 19
                                            }, this)
                                        ]
                                    }, void 0, true, {
                                        fileName: "src/components/IT20127046/admin/DocumentTemp_Admin.js",
                                        lineNumber: 103,
                                        columnNumber: 17
                                    }, this)
                                }, void 0, false, {
                                    fileName: "src/components/IT20127046/admin/DocumentTemp_Admin.js",
                                    lineNumber: 102,
                                    columnNumber: 15
                                }, this)
                            }, void 0, false, {
                                fileName: "src/components/IT20127046/admin/DocumentTemp_Admin.js",
                                lineNumber: 101,
                                columnNumber: 13
                            }, this)
                        ]
                    }, void 0, true, {
                        fileName: "src/components/IT20127046/admin/DocumentTemp_Admin.js",
                        lineNumber: 88,
                        columnNumber: 11
                    }, this)
                }, void 0, false, {
                    fileName: "src/components/IT20127046/admin/DocumentTemp_Admin.js",
                    lineNumber: 87,
                    columnNumber: 9
                }, this)
            ]
        }, void 0, true, {
            fileName: "src/components/IT20127046/admin/DocumentTemp_Admin.js",
            lineNumber: 80,
            columnNumber: 7
        }, this);
    }
}
exports.default = DocumentTemp_Admin;

  $parcel$ReactRefreshHelpers$aaac.postlude(module);
} finally {
  window.$RefreshReg$ = prevRefreshReg;
  window.$RefreshSig$ = prevRefreshSig;
}
},{"react/jsx-dev-runtime":"iTorj","react":"21dqq","axios":"jo6P5","js-file-download":"cLODl","../../IT20125202/admin/AdminNavBar":"kb2lK","sweetalert":"38a65","@parcel/transformer-js/src/esmodule-helpers.js":"gkKU3","@parcel/transformer-react-refresh-wrap/lib/helpers/helpers.js":"km3Ru"}],"gS2Jc":[function(require,module,exports) {
var $parcel$ReactRefreshHelpers$243b = require("@parcel/transformer-react-refresh-wrap/lib/helpers/helpers.js");
var prevRefreshReg = window.$RefreshReg$;
var prevRefreshSig = window.$RefreshSig$;
$parcel$ReactRefreshHelpers$243b.prelude(module);

try {
var parcelHelpers = require("@parcel/transformer-js/src/esmodule-helpers.js");
parcelHelpers.defineInteropFlag(exports);
var _jsxDevRuntime = require("react/jsx-dev-runtime");
var _react = require("react");
var _reactDefault = parcelHelpers.interopDefault(_react);
var _axios = require("axios");
var _axiosDefault = parcelHelpers.interopDefault(_axios);
var _adminNavBar = require("../../IT20125202/admin/AdminNavBar");
var _adminNavBarDefault = parcelHelpers.interopDefault(_adminNavBar);
var _sweetalert = require("sweetalert");
var _sweetalertDefault = parcelHelpers.interopDefault(_sweetalert);
var _s = $RefreshSig$();
function DocumentTempCreate() {
    _s();
    state = {
        disabled: true
    };
    const [documentType, setDocumentType] = _react.useState("");
    const [otherType, setOtherType] = _react.useState("-");
    const [description, setDescription] = _react.useState("");
    const [file, setfile] = _react.useState("");
    const onChangeFile = (e)=>{
        setfile(e.target.files[0]);
    };
    const onChangeClick = (e)=>{
        e.preventDefault();
        const formdata = new FormData();
        formdata.append("documentType", documentType);
        formdata.append("otherType", otherType);
        formdata.append("description", description);
        formdata.append("file", file);
        setDocumentType("");
        setOtherType("-");
        setDescription("");
        setfile("");
        _axiosDefault.default.post("http://localhost:5000/template/add", formdata).then(()=>{
            _sweetalertDefault.default("Document Upload Successful!").then((value)=>{
                window.location = '/documentTemp';
            });
        }).catch((err)=>{
            alert(err);
        });
    };
    return /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
        className: "container",
        children: [
            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("br", {}, void 0, false, {
                fileName: "src/components/IT20127046/admin/DocumentTempCreate.js",
                lineNumber: 49,
                columnNumber: 7
            }, this),
            /*#__PURE__*/ _jsxDevRuntime.jsxDEV(_adminNavBarDefault.default, {}, void 0, false, {
                fileName: "src/components/IT20127046/admin/DocumentTempCreate.js",
                lineNumber: 50,
                columnNumber: 7
            }, this),
            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("br", {}, void 0, false, {
                fileName: "src/components/IT20127046/admin/DocumentTempCreate.js",
                lineNumber: 51,
                columnNumber: 7
            }, this),
            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("h4", {
                children: "Add Document Template"
            }, void 0, false, {
                fileName: "src/components/IT20127046/admin/DocumentTempCreate.js",
                lineNumber: 52,
                columnNumber: 7
            }, this),
            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("hr", {}, void 0, false, {
                fileName: "src/components/IT20127046/admin/DocumentTempCreate.js",
                lineNumber: 53,
                columnNumber: 7
            }, this),
            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                className: "container",
                children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                    className: "container bg-light text-dark p-5",
                    style: {
                        "max-width": "500px",
                        "border": "2px solid #000000"
                    },
                    children: [
                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("center", {
                            children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("h4", {
                                children: "Add Document Template"
                            }, void 0, false, {
                                fileName: "src/components/IT20127046/admin/DocumentTempCreate.js",
                                lineNumber: 58,
                                columnNumber: 15
                            }, this)
                        }, void 0, false, {
                            fileName: "src/components/IT20127046/admin/DocumentTempCreate.js",
                            lineNumber: 57,
                            columnNumber: 11
                        }, this),
                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("br", {}, void 0, false, {
                            fileName: "src/components/IT20127046/admin/DocumentTempCreate.js",
                            lineNumber: 60,
                            columnNumber: 11
                        }, this),
                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("form", {
                            onSubmit: onChangeClick,
                            encType: "multipart/form-data",
                            children: [
                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("label", {
                                    className: "form-label",
                                    children: "Select Document Type"
                                }, void 0, false, {
                                    fileName: "src/components/IT20127046/admin/DocumentTempCreate.js",
                                    lineNumber: 62,
                                    columnNumber: 13
                                }, this),
                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("select", {
                                    className: "form-select",
                                    "aria-label": "Default select example",
                                    name: "documentType",
                                    value: documentType,
                                    onChange: (e)=>{
                                        setDocumentType(e.target.value);
                                    },
                                    children: [
                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("option", {
                                            defaultValue: true,
                                            children: "Select Dcoument Type"
                                        }, void 0, false, {
                                            fileName: "src/components/IT20127046/admin/DocumentTempCreate.js",
                                            lineNumber: 72,
                                            columnNumber: 15
                                        }, this),
                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("option", {
                                            value: "Proposal",
                                            children: "Proposal Document Template"
                                        }, void 0, false, {
                                            fileName: "src/components/IT20127046/admin/DocumentTempCreate.js",
                                            lineNumber: 73,
                                            columnNumber: 15
                                        }, this),
                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("option", {
                                            value: "Presentation",
                                            children: "Presentation Template"
                                        }, void 0, false, {
                                            fileName: "src/components/IT20127046/admin/DocumentTempCreate.js",
                                            lineNumber: 74,
                                            columnNumber: 15
                                        }, this),
                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("option", {
                                            value: "Final Thesis",
                                            children: "Final Thesis Template"
                                        }, void 0, false, {
                                            fileName: "src/components/IT20127046/admin/DocumentTempCreate.js",
                                            lineNumber: 75,
                                            columnNumber: 15
                                        }, this),
                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("option", {
                                            value: "Other",
                                            children: "Other Template"
                                        }, void 0, false, {
                                            fileName: "src/components/IT20127046/admin/DocumentTempCreate.js",
                                            lineNumber: 76,
                                            columnNumber: 15
                                        }, this)
                                    ]
                                }, void 0, true, {
                                    fileName: "src/components/IT20127046/admin/DocumentTempCreate.js",
                                    lineNumber: 63,
                                    columnNumber: 13
                                }, this),
                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                    className: "mb-3",
                                    children: [
                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("label", {
                                            className: "form-label",
                                            children: "Other Type"
                                        }, void 0, false, {
                                            fileName: "src/components/IT20127046/admin/DocumentTempCreate.js",
                                            lineNumber: 80,
                                            columnNumber: 15
                                        }, this),
                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("input", {
                                            type: "text",
                                            className: "form-control",
                                            id: "otherType",
                                            name: "otherType",
                                            "aria-describedby": "emailHelp",
                                            value: otherType,
                                            onChange: (e)=>{
                                                setOtherType(e.target.value);
                                            },
                                            disabled: this.state.disabled
                                        }, void 0, false, {
                                            fileName: "src/components/IT20127046/admin/DocumentTempCreate.js",
                                            lineNumber: 81,
                                            columnNumber: 15
                                        }, this)
                                    ]
                                }, void 0, true, {
                                    fileName: "src/components/IT20127046/admin/DocumentTempCreate.js",
                                    lineNumber: 79,
                                    columnNumber: 13
                                }, this),
                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                    className: "mb-3",
                                    children: [
                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("label", {
                                            className: "form-label",
                                            children: "Description"
                                        }, void 0, false, {
                                            fileName: "src/components/IT20127046/admin/DocumentTempCreate.js",
                                            lineNumber: 96,
                                            columnNumber: 15
                                        }, this),
                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("input", {
                                            type: "text",
                                            className: "form-control",
                                            id: "description",
                                            name: "description",
                                            "aria-describedby": "emailHelp",
                                            value: description,
                                            onChange: (e)=>{
                                                setDescription(e.target.value);
                                            }
                                        }, void 0, false, {
                                            fileName: "src/components/IT20127046/admin/DocumentTempCreate.js",
                                            lineNumber: 97,
                                            columnNumber: 15
                                        }, this)
                                    ]
                                }, void 0, true, {
                                    fileName: "src/components/IT20127046/admin/DocumentTempCreate.js",
                                    lineNumber: 95,
                                    columnNumber: 13
                                }, this),
                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                    className: "mb-3",
                                    children: [
                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("label", {
                                            className: "form-label",
                                            children: "Default file input example"
                                        }, void 0, false, {
                                            fileName: "src/components/IT20127046/admin/DocumentTempCreate.js",
                                            lineNumber: 111,
                                            columnNumber: 15
                                        }, this),
                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("input", {
                                            className: "form-control",
                                            type: "file",
                                            id: "file",
                                            filename: "file",
                                            onChange: onChangeFile
                                        }, void 0, false, {
                                            fileName: "src/components/IT20127046/admin/DocumentTempCreate.js",
                                            lineNumber: 112,
                                            columnNumber: 15
                                        }, this)
                                    ]
                                }, void 0, true, {
                                    fileName: "src/components/IT20127046/admin/DocumentTempCreate.js",
                                    lineNumber: 110,
                                    columnNumber: 13
                                }, this),
                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("center", {
                                    children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("button", {
                                        type: "submit",
                                        className: "btn btn-primary",
                                        children: "Submit"
                                    }, void 0, false, {
                                        fileName: "src/components/IT20127046/admin/DocumentTempCreate.js",
                                        lineNumber: 122,
                                        columnNumber: 15
                                    }, this)
                                }, void 0, false, {
                                    fileName: "src/components/IT20127046/admin/DocumentTempCreate.js",
                                    lineNumber: 121,
                                    columnNumber: 13
                                }, this)
                            ]
                        }, void 0, true, {
                            fileName: "src/components/IT20127046/admin/DocumentTempCreate.js",
                            lineNumber: 61,
                            columnNumber: 11
                        }, this)
                    ]
                }, void 0, true, {
                    fileName: "src/components/IT20127046/admin/DocumentTempCreate.js",
                    lineNumber: 56,
                    columnNumber: 9
                }, this)
            }, void 0, false, {
                fileName: "src/components/IT20127046/admin/DocumentTempCreate.js",
                lineNumber: 55,
                columnNumber: 7
            }, this)
        ]
    }, void 0, true, {
        fileName: "src/components/IT20127046/admin/DocumentTempCreate.js",
        lineNumber: 48,
        columnNumber: 5
    }, this);
}
exports.default = DocumentTempCreate;
_s(DocumentTempCreate, "b58qGSYhEMAiKsXQ1Lr6+n2y1/A=");
_c = DocumentTempCreate;
var _c;
$RefreshReg$(_c, "DocumentTempCreate");

  $parcel$ReactRefreshHelpers$243b.postlude(module);
} finally {
  window.$RefreshReg$ = prevRefreshReg;
  window.$RefreshSig$ = prevRefreshSig;
}
},{"react/jsx-dev-runtime":"iTorj","react":"21dqq","axios":"jo6P5","../../IT20125202/admin/AdminNavBar":"kb2lK","sweetalert":"38a65","@parcel/transformer-js/src/esmodule-helpers.js":"gkKU3","@parcel/transformer-react-refresh-wrap/lib/helpers/helpers.js":"km3Ru"}],"iRkUd":[function(require,module,exports) {
var $parcel$ReactRefreshHelpers$85c4 = require("@parcel/transformer-react-refresh-wrap/lib/helpers/helpers.js");
var prevRefreshReg = window.$RefreshReg$;
var prevRefreshSig = window.$RefreshSig$;
$parcel$ReactRefreshHelpers$85c4.prelude(module);

try {
var parcelHelpers = require("@parcel/transformer-js/src/esmodule-helpers.js");
parcelHelpers.defineInteropFlag(exports);
var _jsxDevRuntime = require("react/jsx-dev-runtime");
var _react = require("react");
var _reactDefault = parcelHelpers.interopDefault(_react);
var _axios = require("axios");
var _axiosDefault = parcelHelpers.interopDefault(_axios);
var _adminNavBar = require("../../IT20125202/admin/AdminNavBar");
var _adminNavBarDefault = parcelHelpers.interopDefault(_adminNavBar);
var _sweetalert = require("sweetalert");
var _sweetalertDefault = parcelHelpers.interopDefault(_sweetalert);
class DocumentTempUpdate extends _react.Component {
    constructor(props){
        super(props);
        this.state = {
            documentType: "",
            otherType: "",
            description: "",
            file: ""
        };
    }
    handleInputChange = (e)=>{
        const { name , value  } = e.target;
        this.setState({
            ...this.state,
            [name]: value
        });
    };
    onSubmit = (e)=>{
        e.preventDefault();
        const id = this.props.match.params.id;
        const { documentType , otherType , description  } = this.state;
        const data = {
            documentType: documentType,
            otherType: otherType,
            description: description
        };
        _axiosDefault.default.put(`http://localhost:5000/document/update/${id}`, data).then((res)=>{
            if (res.data.success) _sweetalertDefault.default("Document Update Successful!").then((value)=>{
                window.location = '/documentTemp';
            });
        });
    };
    componentDidMount() {
        const id = this.props.match.params.id;
        _axiosDefault.default.get(`http://localhost:5000/document/get/${id}`).then((res)=>{
            if (res.data.success) this.setState({
                documentType: res.data.document.documentType,
                otherType: res.data.document.otherType,
                description: res.data.document.description,
                file: res.data.document.files
            });
        });
    }
    render() {
        return /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
            className: "container",
            children: [
                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("br", {}, void 0, false, {
                    fileName: "src/components/IT20127046/admin/DocumentTempUpdate.js",
                    lineNumber: 68,
                    columnNumber: 9
                }, this),
                /*#__PURE__*/ _jsxDevRuntime.jsxDEV(_adminNavBarDefault.default, {}, void 0, false, {
                    fileName: "src/components/IT20127046/admin/DocumentTempUpdate.js",
                    lineNumber: 69,
                    columnNumber: 9
                }, this),
                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("br", {}, void 0, false, {
                    fileName: "src/components/IT20127046/admin/DocumentTempUpdate.js",
                    lineNumber: 70,
                    columnNumber: 9
                }, this),
                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("h4", {
                    children: "Update Document Template"
                }, void 0, false, {
                    fileName: "src/components/IT20127046/admin/DocumentTempUpdate.js",
                    lineNumber: 71,
                    columnNumber: 9
                }, this),
                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("hr", {}, void 0, false, {
                    fileName: "src/components/IT20127046/admin/DocumentTempUpdate.js",
                    lineNumber: 72,
                    columnNumber: 9
                }, this),
                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                    className: "container",
                    children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                        className: "container bg-light text-dark p-5",
                        style: {
                            "max-width": "500px",
                            border: "2px solid #000000"
                        },
                        children: [
                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("center", {
                                children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("h4", {
                                    children: "Update Document Template"
                                }, void 0, false, {
                                    fileName: "src/components/IT20127046/admin/DocumentTempUpdate.js",
                                    lineNumber: 80,
                                    columnNumber: 15
                                }, this)
                            }, void 0, false, {
                                fileName: "src/components/IT20127046/admin/DocumentTempUpdate.js",
                                lineNumber: 79,
                                columnNumber: 13
                            }, this),
                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("br", {}, void 0, false, {
                                fileName: "src/components/IT20127046/admin/DocumentTempUpdate.js",
                                lineNumber: 82,
                                columnNumber: 13
                            }, this),
                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("button", {
                                type: "button",
                                className: "btn btn-secondary",
                                children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("a", {
                                    href: "/documentTemp",
                                    style: {
                                        textDecoration: "none",
                                        color: "white"
                                    },
                                    children: "Back to Previuos Page"
                                }, void 0, false, {
                                    fileName: "src/components/IT20127046/admin/DocumentTempUpdate.js",
                                    lineNumber: 84,
                                    columnNumber: 15
                                }, this)
                            }, void 0, false, {
                                fileName: "src/components/IT20127046/admin/DocumentTempUpdate.js",
                                lineNumber: 83,
                                columnNumber: 13
                            }, this),
                            " ",
                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("br", {}, void 0, false, {
                                fileName: "src/components/IT20127046/admin/DocumentTempUpdate.js",
                                lineNumber: 91,
                                columnNumber: 13
                            }, this),
                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("br", {}, void 0, false, {
                                fileName: "src/components/IT20127046/admin/DocumentTempUpdate.js",
                                lineNumber: 92,
                                columnNumber: 13
                            }, this),
                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("form", {
                                encType: "multipart/form-data",
                                children: [
                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("label", {
                                        className: "form-label",
                                        children: "Select Document Type"
                                    }, void 0, false, {
                                        fileName: "src/components/IT20127046/admin/DocumentTempUpdate.js",
                                        lineNumber: 94,
                                        columnNumber: 15
                                    }, this),
                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("select", {
                                        className: "form-select",
                                        "aria-label": "Default select example",
                                        name: "documentType",
                                        value: this.state.documentType,
                                        onChange: this.handleInputChange,
                                        children: [
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("option", {
                                                defaultValue: true,
                                                children: "Select Dcoument Type"
                                            }, void 0, false, {
                                                fileName: "src/components/IT20127046/admin/DocumentTempUpdate.js",
                                                lineNumber: 102,
                                                columnNumber: 17
                                            }, this),
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("option", {
                                                value: "Proposal",
                                                children: "Proposal Document Template"
                                            }, void 0, false, {
                                                fileName: "src/components/IT20127046/admin/DocumentTempUpdate.js",
                                                lineNumber: 103,
                                                columnNumber: 17
                                            }, this),
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("option", {
                                                value: "Presentation",
                                                children: "Presentation Template"
                                            }, void 0, false, {
                                                fileName: "src/components/IT20127046/admin/DocumentTempUpdate.js",
                                                lineNumber: 104,
                                                columnNumber: 17
                                            }, this),
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("option", {
                                                value: "Final Thesis",
                                                children: "Final Thesis Template"
                                            }, void 0, false, {
                                                fileName: "src/components/IT20127046/admin/DocumentTempUpdate.js",
                                                lineNumber: 105,
                                                columnNumber: 17
                                            }, this),
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("option", {
                                                value: "Other",
                                                children: "Other Template"
                                            }, void 0, false, {
                                                fileName: "src/components/IT20127046/admin/DocumentTempUpdate.js",
                                                lineNumber: 106,
                                                columnNumber: 17
                                            }, this)
                                        ]
                                    }, void 0, true, {
                                        fileName: "src/components/IT20127046/admin/DocumentTempUpdate.js",
                                        lineNumber: 95,
                                        columnNumber: 15
                                    }, this),
                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                        className: "mb-3",
                                        children: [
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("label", {
                                                className: "form-label",
                                                children: "Other Type"
                                            }, void 0, false, {
                                                fileName: "src/components/IT20127046/admin/DocumentTempUpdate.js",
                                                lineNumber: 110,
                                                columnNumber: 17
                                            }, this),
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("input", {
                                                type: "text",
                                                className: "form-control",
                                                id: "otherType",
                                                name: "otherType",
                                                "aria-describedby": "emailHelp",
                                                value: this.state.otherType,
                                                onChange: this.handleInputChange
                                            }, void 0, false, {
                                                fileName: "src/components/IT20127046/admin/DocumentTempUpdate.js",
                                                lineNumber: 111,
                                                columnNumber: 17
                                            }, this)
                                        ]
                                    }, void 0, true, {
                                        fileName: "src/components/IT20127046/admin/DocumentTempUpdate.js",
                                        lineNumber: 109,
                                        columnNumber: 15
                                    }, this),
                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                        className: "mb-3",
                                        children: [
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("label", {
                                                className: "form-label",
                                                children: "Description"
                                            }, void 0, false, {
                                                fileName: "src/components/IT20127046/admin/DocumentTempUpdate.js",
                                                lineNumber: 123,
                                                columnNumber: 17
                                            }, this),
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("input", {
                                                type: "text",
                                                className: "form-control",
                                                id: "description",
                                                name: "description",
                                                "aria-describedby": "emailHelp",
                                                value: this.state.description,
                                                onChange: this.handleInputChange
                                            }, void 0, false, {
                                                fileName: "src/components/IT20127046/admin/DocumentTempUpdate.js",
                                                lineNumber: 124,
                                                columnNumber: 17
                                            }, this)
                                        ]
                                    }, void 0, true, {
                                        fileName: "src/components/IT20127046/admin/DocumentTempUpdate.js",
                                        lineNumber: 122,
                                        columnNumber: 15
                                    }, this),
                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                        className: "mb-3",
                                        children: [
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("label", {
                                                className: "form-label",
                                                children: [
                                                    "Default file input example ",
                                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("p", {
                                                        children: this.state.file
                                                    }, void 0, false, {
                                                        fileName: "src/components/IT20127046/admin/DocumentTempUpdate.js",
                                                        lineNumber: 137,
                                                        columnNumber: 46
                                                    }, this)
                                                ]
                                            }, void 0, true, {
                                                fileName: "src/components/IT20127046/admin/DocumentTempUpdate.js",
                                                lineNumber: 136,
                                                columnNumber: 17
                                            }, this),
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("input", {
                                                className: "form-control",
                                                type: "file",
                                                id: "file",
                                                filename: "file"
                                            }, void 0, false, {
                                                fileName: "src/components/IT20127046/admin/DocumentTempUpdate.js",
                                                lineNumber: 139,
                                                columnNumber: 17
                                            }, this)
                                        ]
                                    }, void 0, true, {
                                        fileName: "src/components/IT20127046/admin/DocumentTempUpdate.js",
                                        lineNumber: 135,
                                        columnNumber: 15
                                    }, this),
                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("center", {
                                        children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("button", {
                                            type: "submit",
                                            className: "btn btn-primary",
                                            onClick: this.onSubmit,
                                            children: "Update"
                                        }, void 0, false, {
                                            fileName: "src/components/IT20127046/admin/DocumentTempUpdate.js",
                                            lineNumber: 149,
                                            columnNumber: 17
                                        }, this)
                                    }, void 0, false, {
                                        fileName: "src/components/IT20127046/admin/DocumentTempUpdate.js",
                                        lineNumber: 148,
                                        columnNumber: 15
                                    }, this)
                                ]
                            }, void 0, true, {
                                fileName: "src/components/IT20127046/admin/DocumentTempUpdate.js",
                                lineNumber: 93,
                                columnNumber: 13
                            }, this)
                        ]
                    }, void 0, true, {
                        fileName: "src/components/IT20127046/admin/DocumentTempUpdate.js",
                        lineNumber: 75,
                        columnNumber: 11
                    }, this)
                }, void 0, false, {
                    fileName: "src/components/IT20127046/admin/DocumentTempUpdate.js",
                    lineNumber: 74,
                    columnNumber: 9
                }, this),
                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("br", {}, void 0, false, {
                    fileName: "src/components/IT20127046/admin/DocumentTempUpdate.js",
                    lineNumber: 160,
                    columnNumber: 9
                }, this)
            ]
        }, void 0, true, {
            fileName: "src/components/IT20127046/admin/DocumentTempUpdate.js",
            lineNumber: 67,
            columnNumber: 7
        }, this);
    }
}
exports.default = DocumentTempUpdate;

  $parcel$ReactRefreshHelpers$85c4.postlude(module);
} finally {
  window.$RefreshReg$ = prevRefreshReg;
  window.$RefreshSig$ = prevRefreshSig;
}
},{"react/jsx-dev-runtime":"iTorj","react":"21dqq","axios":"jo6P5","../../IT20125202/admin/AdminNavBar":"kb2lK","sweetalert":"38a65","@parcel/transformer-js/src/esmodule-helpers.js":"gkKU3","@parcel/transformer-react-refresh-wrap/lib/helpers/helpers.js":"km3Ru"}],"Hfo6h":[function(require,module,exports) {
var $parcel$ReactRefreshHelpers$71ec = require("@parcel/transformer-react-refresh-wrap/lib/helpers/helpers.js");
var prevRefreshReg = window.$RefreshReg$;
var prevRefreshSig = window.$RefreshSig$;
$parcel$ReactRefreshHelpers$71ec.prelude(module);

try {
var parcelHelpers = require("@parcel/transformer-js/src/esmodule-helpers.js");
parcelHelpers.defineInteropFlag(exports);
var _jsxDevRuntime = require("react/jsx-dev-runtime");
// admin - view user roles
var _react = require("react");
var _reactDefault = parcelHelpers.interopDefault(_react);
var _axios = require("axios");
var _axiosDefault = parcelHelpers.interopDefault(_axios);
var _adminNavBar = require("../admin/AdminNavBar");
var _adminNavBarDefault = parcelHelpers.interopDefault(_adminNavBar);
var _sweetalert = require("sweetalert");
var _sweetalertDefault = parcelHelpers.interopDefault(_sweetalert);
class UserRoles_Panel extends _react.Component {
    constructor(props){
        super(props);
        this.state = {
            users: []
        };
    }
    componentDidMount() {
        document.title = "User Roles";
        // redirect to the login page if the user is not logged in
        if (!localStorage.token) _sweetalertDefault.default("Please login first", "", "warning").then((value)=>{
            if (value) {
                this.props.history.push(`/admin/login`);
                window.location.reload();
            }
        });
        this.retrieveUsers();
    }
    retrieveUsers() {
        const type = 'Panel Member';
        _axiosDefault.default.get(`http://localhost:5000/users/${type}`).then((res)=>{
            if (res.data.success) this.setState({
                users: res.data.existingUsers
            });
        });
    }
    onDelete = (id)=>{
        //with a confirmation 
        _sweetalertDefault.default({
            title: "Are you sure?",
            text: "Once deleted, you will not be able to recover this user details",
            icon: "warning",
            buttons: true,
            dangerMode: true
        }).then((willDelete)=>{
            if (willDelete) _axiosDefault.default.delete(`http://localhost:5000/user/delete/${id}`).then((res)=>{
                _sweetalertDefault.default("User Deleted Permanently!", "", "success").then((value)=>{
                    if (value) this.retrieveUsers();
                });
            });
            else _sweetalertDefault.default("Cancelled. The user details are safe!");
        });
    };
    handleSearchArea = (e)=>{
        // console.log(e.currentTarget.value)
        const searchKey = e.currentTarget.value;
        _axiosDefault.default.get('http://localhost:5000/users').then((res)=>{
            if (res.data.success) this.filterData(res.data.existingUsers, searchKey);
        });
    };
    filterData(users, searchKey) {
        const searchResult = users.filter((user)=>user.idNumber.toLowerCase().includes(searchKey) || user.name.toLowerCase().includes(searchKey) || user.idNumber.toUpperCase().includes(searchKey) || user.name.toUpperCase().includes(searchKey) || user.idNumber.includes(searchKey) || user.name.includes(searchKey)
        );
        this.setState({
            users: searchResult
        });
    }
    render() {
        return /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
            className: "container",
            children: [
                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("br", {}, void 0, false, {
                    fileName: "src/components/IT20125202/user/UserRoles_Panel.js",
                    lineNumber: 110,
                    columnNumber: 9
                }, this),
                /*#__PURE__*/ _jsxDevRuntime.jsxDEV(_adminNavBarDefault.default, {}, void 0, false, {
                    fileName: "src/components/IT20125202/user/UserRoles_Panel.js",
                    lineNumber: 111,
                    columnNumber: 9
                }, this),
                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("br", {}, void 0, false, {
                    fileName: "src/components/IT20125202/user/UserRoles_Panel.js",
                    lineNumber: 112,
                    columnNumber: 9
                }, this),
                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                    className: "container",
                    children: [
                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                            className: "row",
                            children: [
                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                    className: "col-lg-9 mt-2 mb-2",
                                    children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("h1", {
                                        children: "Users"
                                    }, void 0, false, {
                                        fileName: "src/components/IT20125202/user/UserRoles_Panel.js",
                                        lineNumber: 117,
                                        columnNumber: 15
                                    }, this)
                                }, void 0, false, {
                                    fileName: "src/components/IT20125202/user/UserRoles_Panel.js",
                                    lineNumber: 116,
                                    columnNumber: 13
                                }, this),
                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                    className: "col-lg-3 mt-2 mb-2",
                                    children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("input", {
                                        className: "form-control",
                                        type: "search",
                                        placeholder: "Search User",
                                        name: "searchQuery",
                                        onChange: this.handleSearchArea
                                    }, void 0, false, {
                                        fileName: "src/components/IT20125202/user/UserRoles_Panel.js",
                                        lineNumber: 120,
                                        columnNumber: 15
                                    }, this)
                                }, void 0, false, {
                                    fileName: "src/components/IT20125202/user/UserRoles_Panel.js",
                                    lineNumber: 119,
                                    columnNumber: 13
                                }, this),
                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("hr", {}, void 0, false, {
                                    fileName: "src/components/IT20125202/user/UserRoles_Panel.js",
                                    lineNumber: 128,
                                    columnNumber: 13
                                }, this),
                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("br", {}, void 0, false, {
                                    fileName: "src/components/IT20125202/user/UserRoles_Panel.js",
                                    lineNumber: 128,
                                    columnNumber: 19
                                }, this)
                            ]
                        }, void 0, true, {
                            fileName: "src/components/IT20125202/user/UserRoles_Panel.js",
                            lineNumber: 115,
                            columnNumber: 11
                        }, this),
                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                            className: "row",
                            children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                className: "btn-group",
                                children: [
                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("a", {
                                        href: "/admin/users",
                                        className: "btn btn-outline-dark active",
                                        "aria-current": "page",
                                        children: "All Users"
                                    }, void 0, false, {
                                        fileName: "src/components/IT20125202/user/UserRoles_Panel.js",
                                        lineNumber: 132,
                                        columnNumber: 15
                                    }, this),
                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("a", {
                                        href: "/admin/panelmembers",
                                        className: "btn btn-outline-dark",
                                        children: "Panel Members"
                                    }, void 0, false, {
                                        fileName: "src/components/IT20125202/user/UserRoles_Panel.js",
                                        lineNumber: 133,
                                        columnNumber: 15
                                    }, this),
                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("a", {
                                        href: "/admin/students",
                                        className: "btn btn-outline-dark",
                                        children: "Students"
                                    }, void 0, false, {
                                        fileName: "src/components/IT20125202/user/UserRoles_Panel.js",
                                        lineNumber: 134,
                                        columnNumber: 15
                                    }, this),
                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("a", {
                                        href: "/admin/supervisors",
                                        className: "btn btn-outline-dark",
                                        children: "Supervisors"
                                    }, void 0, false, {
                                        fileName: "src/components/IT20125202/user/UserRoles_Panel.js",
                                        lineNumber: 135,
                                        columnNumber: 15
                                    }, this)
                                ]
                            }, void 0, true, {
                                fileName: "src/components/IT20125202/user/UserRoles_Panel.js",
                                lineNumber: 131,
                                columnNumber: 13
                            }, this)
                        }, void 0, false, {
                            fileName: "src/components/IT20125202/user/UserRoles_Panel.js",
                            lineNumber: 130,
                            columnNumber: 11
                        }, this),
                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("br", {}, void 0, false, {
                            fileName: "src/components/IT20125202/user/UserRoles_Panel.js",
                            lineNumber: 138,
                            columnNumber: 11
                        }, this),
                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("table", {
                            className: "table",
                            children: [
                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("thead", {
                                    children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("tr", {
                                        children: [
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("th", {
                                                scope: "col",
                                                children: " # "
                                            }, void 0, false, {
                                                fileName: "src/components/IT20125202/user/UserRoles_Panel.js",
                                                lineNumber: 142,
                                                columnNumber: 17
                                            }, this),
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("th", {
                                                scope: "col",
                                                children: " ID Number "
                                            }, void 0, false, {
                                                fileName: "src/components/IT20125202/user/UserRoles_Panel.js",
                                                lineNumber: 143,
                                                columnNumber: 17
                                            }, this),
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("th", {
                                                scope: "col",
                                                children: " Name "
                                            }, void 0, false, {
                                                fileName: "src/components/IT20125202/user/UserRoles_Panel.js",
                                                lineNumber: 144,
                                                columnNumber: 17
                                            }, this),
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("th", {
                                                scope: "col",
                                                children: " Email "
                                            }, void 0, false, {
                                                fileName: "src/components/IT20125202/user/UserRoles_Panel.js",
                                                lineNumber: 145,
                                                columnNumber: 17
                                            }, this),
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("th", {
                                                scope: "col",
                                                children: " Mobile "
                                            }, void 0, false, {
                                                fileName: "src/components/IT20125202/user/UserRoles_Panel.js",
                                                lineNumber: 146,
                                                columnNumber: 17
                                            }, this),
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("th", {
                                                scope: "col",
                                                children: " Panel "
                                            }, void 0, false, {
                                                fileName: "src/components/IT20125202/user/UserRoles_Panel.js",
                                                lineNumber: 147,
                                                columnNumber: 17
                                            }, this),
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("th", {
                                                scope: "col",
                                                children: " "
                                            }, void 0, false, {
                                                fileName: "src/components/IT20125202/user/UserRoles_Panel.js",
                                                lineNumber: 149,
                                                columnNumber: 17
                                            }, this)
                                        ]
                                    }, void 0, true, {
                                        fileName: "src/components/IT20125202/user/UserRoles_Panel.js",
                                        lineNumber: 141,
                                        columnNumber: 15
                                    }, this)
                                }, void 0, false, {
                                    fileName: "src/components/IT20125202/user/UserRoles_Panel.js",
                                    lineNumber: 140,
                                    columnNumber: 13
                                }, this),
                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("tbody", {
                                    children: this.state.users.map((users, index)=>/*#__PURE__*/ _jsxDevRuntime.jsxDEV("tr", {
                                            children: [
                                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("th", {
                                                    scope: "row",
                                                    children: index + 1
                                                }, void 0, false, {
                                                    fileName: "src/components/IT20125202/user/UserRoles_Panel.js",
                                                    lineNumber: 157,
                                                    columnNumber: 19
                                                }, this),
                                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("td", {
                                                    children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("a", {
                                                        href: `/admin/user/${users._id}`,
                                                        style: {
                                                            textDecoration: 'none'
                                                        },
                                                        children: users.idNumber
                                                    }, void 0, false, {
                                                        fileName: "src/components/IT20125202/user/UserRoles_Panel.js",
                                                        lineNumber: 159,
                                                        columnNumber: 21
                                                    }, this)
                                                }, void 0, false, {
                                                    fileName: "src/components/IT20125202/user/UserRoles_Panel.js",
                                                    lineNumber: 158,
                                                    columnNumber: 19
                                                }, this),
                                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("td", {
                                                    children: users.name
                                                }, void 0, false, {
                                                    fileName: "src/components/IT20125202/user/UserRoles_Panel.js",
                                                    lineNumber: 163,
                                                    columnNumber: 19
                                                }, this),
                                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("td", {
                                                    children: users.email
                                                }, void 0, false, {
                                                    fileName: "src/components/IT20125202/user/UserRoles_Panel.js",
                                                    lineNumber: 164,
                                                    columnNumber: 19
                                                }, this),
                                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("td", {
                                                    children: users.mobile
                                                }, void 0, false, {
                                                    fileName: "src/components/IT20125202/user/UserRoles_Panel.js",
                                                    lineNumber: 165,
                                                    columnNumber: 19
                                                }, this),
                                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("td", {
                                                    children: users.panel
                                                }, void 0, false, {
                                                    fileName: "src/components/IT20125202/user/UserRoles_Panel.js",
                                                    lineNumber: 166,
                                                    columnNumber: 19
                                                }, this),
                                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("td", {
                                                    children: [
                                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("a", {
                                                            className: "btn btn-outline-success",
                                                            href: `/admin/edituser/${users._id}`,
                                                            children: [
                                                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("i", {
                                                                    className: "fas fa-edit"
                                                                }, void 0, false, {
                                                                    fileName: "src/components/IT20125202/user/UserRoles_Panel.js",
                                                                    lineNumber: 171,
                                                                    columnNumber: 23
                                                                }, this),
                                                                " \xa0Update"
                                                            ]
                                                        }, void 0, true, {
                                                            fileName: "src/components/IT20125202/user/UserRoles_Panel.js",
                                                            lineNumber: 170,
                                                            columnNumber: 21
                                                        }, this),
                                                        "\xa0",
                                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("a", {
                                                            className: "btn btn-outline-danger",
                                                            href: "#",
                                                            onClick: ()=>this.onDelete(users._id)
                                                            ,
                                                            children: [
                                                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("i", {
                                                                    className: "fas fa-trash"
                                                                }, void 0, false, {
                                                                    fileName: "src/components/IT20125202/user/UserRoles_Panel.js",
                                                                    lineNumber: 175,
                                                                    columnNumber: 23
                                                                }, this),
                                                                " \xa0Delete"
                                                            ]
                                                        }, void 0, true, {
                                                            fileName: "src/components/IT20125202/user/UserRoles_Panel.js",
                                                            lineNumber: 174,
                                                            columnNumber: 21
                                                        }, this)
                                                    ]
                                                }, void 0, true, {
                                                    fileName: "src/components/IT20125202/user/UserRoles_Panel.js",
                                                    lineNumber: 169,
                                                    columnNumber: 19
                                                }, this)
                                            ]
                                        }, index, true, {
                                            fileName: "src/components/IT20125202/user/UserRoles_Panel.js",
                                            lineNumber: 156,
                                            columnNumber: 17
                                        }, this)
                                    )
                                }, void 0, false, {
                                    fileName: "src/components/IT20125202/user/UserRoles_Panel.js",
                                    lineNumber: 154,
                                    columnNumber: 13
                                }, this)
                            ]
                        }, void 0, true, {
                            fileName: "src/components/IT20125202/user/UserRoles_Panel.js",
                            lineNumber: 139,
                            columnNumber: 11
                        }, this)
                    ]
                }, void 0, true, {
                    fileName: "src/components/IT20125202/user/UserRoles_Panel.js",
                    lineNumber: 114,
                    columnNumber: 9
                }, this)
            ]
        }, void 0, true, {
            fileName: "src/components/IT20125202/user/UserRoles_Panel.js",
            lineNumber: 109,
            columnNumber: 7
        }, this);
    }
}
exports.default = UserRoles_Panel;

  $parcel$ReactRefreshHelpers$71ec.postlude(module);
} finally {
  window.$RefreshReg$ = prevRefreshReg;
  window.$RefreshSig$ = prevRefreshSig;
}
},{"react/jsx-dev-runtime":"iTorj","react":"21dqq","axios":"jo6P5","../admin/AdminNavBar":"kb2lK","sweetalert":"38a65","@parcel/transformer-js/src/esmodule-helpers.js":"gkKU3","@parcel/transformer-react-refresh-wrap/lib/helpers/helpers.js":"km3Ru"}],"3GIAD":[function(require,module,exports) {
var $parcel$ReactRefreshHelpers$346c = require("@parcel/transformer-react-refresh-wrap/lib/helpers/helpers.js");
var prevRefreshReg = window.$RefreshReg$;
var prevRefreshSig = window.$RefreshSig$;
$parcel$ReactRefreshHelpers$346c.prelude(module);

try {
var parcelHelpers = require("@parcel/transformer-js/src/esmodule-helpers.js");
parcelHelpers.defineInteropFlag(exports);
var _jsxDevRuntime = require("react/jsx-dev-runtime");
// admin - view user roles
var _react = require("react");
var _reactDefault = parcelHelpers.interopDefault(_react);
var _axios = require("axios");
var _axiosDefault = parcelHelpers.interopDefault(_axios);
var _adminNavBar = require("../admin/AdminNavBar");
var _adminNavBarDefault = parcelHelpers.interopDefault(_adminNavBar);
var _sweetalert = require("sweetalert");
var _sweetalertDefault = parcelHelpers.interopDefault(_sweetalert);
class UserRoles_Students extends _react.Component {
    constructor(props){
        super(props);
        this.state = {
            users: []
        };
    }
    componentDidMount() {
        document.title = "User Roles";
        // redirect to the login page if the user is not logged in
        if (!localStorage.token) _sweetalertDefault.default("Please login first", "", "warning").then((value)=>{
            if (value) {
                this.props.history.push(`/admin/login`);
                window.location.reload();
            }
        });
        this.retrieveUsers();
    }
    retrieveUsers() {
        const type = 'Student';
        _axiosDefault.default.get(`http://localhost:5000/users/${type}`).then((res)=>{
            if (res.data.success) this.setState({
                users: res.data.existingUsers
            });
        });
    }
    onDelete = (id)=>{
        //with a confirmation 
        _sweetalertDefault.default({
            title: "Are you sure?",
            text: "Once deleted, you will not be able to recover this user details",
            icon: "warning",
            buttons: true,
            dangerMode: true
        }).then((willDelete)=>{
            if (willDelete) _axiosDefault.default.delete(`http://localhost:5000/user/delete/${id}`).then((res)=>{
                _sweetalertDefault.default("User Deleted Permanently!", "", "success").then((value)=>{
                    if (value) this.retrieveUsers();
                });
            });
            else _sweetalertDefault.default("Cancelled. The user details are safe!");
        });
    };
    handleSearchArea = (e)=>{
        // console.log(e.currentTarget.value)
        const searchKey = e.currentTarget.value;
        _axiosDefault.default.get('http://localhost:5000/users').then((res)=>{
            if (res.data.success) this.filterData(res.data.existingUsers, searchKey);
        });
    };
    filterData(users, searchKey) {
        const searchResult = users.filter((user)=>user.idNumber.toLowerCase().includes(searchKey) || user.name.toLowerCase().includes(searchKey) || user.groupId.toLowerCase().includes(searchKey) || user.idNumber.toUpperCase().includes(searchKey) || user.name.toUpperCase().includes(searchKey) || user.groupId.toUpperCase().includes(searchKey) || user.idNumber.includes(searchKey) || user.name.includes(searchKey) || user.groupId.includes(searchKey)
        );
        this.setState({
            users: searchResult
        });
    }
    render() {
        return /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
            className: "container",
            children: [
                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("br", {}, void 0, false, {
                    fileName: "src/components/IT20125202/user/UserRoles_Students.js",
                    lineNumber: 114,
                    columnNumber: 9
                }, this),
                /*#__PURE__*/ _jsxDevRuntime.jsxDEV(_adminNavBarDefault.default, {}, void 0, false, {
                    fileName: "src/components/IT20125202/user/UserRoles_Students.js",
                    lineNumber: 115,
                    columnNumber: 9
                }, this),
                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("br", {}, void 0, false, {
                    fileName: "src/components/IT20125202/user/UserRoles_Students.js",
                    lineNumber: 116,
                    columnNumber: 9
                }, this),
                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                    className: "container",
                    children: [
                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                            className: "row",
                            children: [
                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                    className: "col-lg-9 mt-2 mb-2",
                                    children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("h1", {
                                        children: "Users"
                                    }, void 0, false, {
                                        fileName: "src/components/IT20125202/user/UserRoles_Students.js",
                                        lineNumber: 121,
                                        columnNumber: 15
                                    }, this)
                                }, void 0, false, {
                                    fileName: "src/components/IT20125202/user/UserRoles_Students.js",
                                    lineNumber: 120,
                                    columnNumber: 13
                                }, this),
                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                    className: "col-lg-3 mt-2 mb-2",
                                    children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("input", {
                                        className: "form-control",
                                        type: "search",
                                        placeholder: "Search User",
                                        name: "searchQuery",
                                        onChange: this.handleSearchArea
                                    }, void 0, false, {
                                        fileName: "src/components/IT20125202/user/UserRoles_Students.js",
                                        lineNumber: 124,
                                        columnNumber: 15
                                    }, this)
                                }, void 0, false, {
                                    fileName: "src/components/IT20125202/user/UserRoles_Students.js",
                                    lineNumber: 123,
                                    columnNumber: 13
                                }, this),
                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("hr", {}, void 0, false, {
                                    fileName: "src/components/IT20125202/user/UserRoles_Students.js",
                                    lineNumber: 132,
                                    columnNumber: 13
                                }, this),
                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("br", {}, void 0, false, {
                                    fileName: "src/components/IT20125202/user/UserRoles_Students.js",
                                    lineNumber: 132,
                                    columnNumber: 19
                                }, this)
                            ]
                        }, void 0, true, {
                            fileName: "src/components/IT20125202/user/UserRoles_Students.js",
                            lineNumber: 119,
                            columnNumber: 11
                        }, this),
                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                            className: "row",
                            children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                className: "btn-group",
                                children: [
                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("a", {
                                        href: "/admin/users",
                                        className: "btn btn-outline-dark active",
                                        "aria-current": "page",
                                        children: "All Users"
                                    }, void 0, false, {
                                        fileName: "src/components/IT20125202/user/UserRoles_Students.js",
                                        lineNumber: 136,
                                        columnNumber: 15
                                    }, this),
                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("a", {
                                        href: "/admin/panelmembers",
                                        className: "btn btn-outline-dark",
                                        children: "Panel Members"
                                    }, void 0, false, {
                                        fileName: "src/components/IT20125202/user/UserRoles_Students.js",
                                        lineNumber: 137,
                                        columnNumber: 15
                                    }, this),
                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("a", {
                                        href: "/admin/students",
                                        className: "btn btn-outline-dark",
                                        children: "Students"
                                    }, void 0, false, {
                                        fileName: "src/components/IT20125202/user/UserRoles_Students.js",
                                        lineNumber: 138,
                                        columnNumber: 15
                                    }, this),
                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("a", {
                                        href: "/admin/supervisors",
                                        className: "btn btn-outline-dark",
                                        children: "Supervisors"
                                    }, void 0, false, {
                                        fileName: "src/components/IT20125202/user/UserRoles_Students.js",
                                        lineNumber: 139,
                                        columnNumber: 15
                                    }, this)
                                ]
                            }, void 0, true, {
                                fileName: "src/components/IT20125202/user/UserRoles_Students.js",
                                lineNumber: 135,
                                columnNumber: 13
                            }, this)
                        }, void 0, false, {
                            fileName: "src/components/IT20125202/user/UserRoles_Students.js",
                            lineNumber: 134,
                            columnNumber: 11
                        }, this),
                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("br", {}, void 0, false, {
                            fileName: "src/components/IT20125202/user/UserRoles_Students.js",
                            lineNumber: 142,
                            columnNumber: 11
                        }, this),
                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("table", {
                            className: "table",
                            children: [
                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("thead", {
                                    children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("tr", {
                                        children: [
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("th", {
                                                scope: "col",
                                                children: " # "
                                            }, void 0, false, {
                                                fileName: "src/components/IT20125202/user/UserRoles_Students.js",
                                                lineNumber: 146,
                                                columnNumber: 17
                                            }, this),
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("th", {
                                                scope: "col",
                                                children: " ID Number "
                                            }, void 0, false, {
                                                fileName: "src/components/IT20125202/user/UserRoles_Students.js",
                                                lineNumber: 147,
                                                columnNumber: 17
                                            }, this),
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("th", {
                                                scope: "col",
                                                children: " Name "
                                            }, void 0, false, {
                                                fileName: "src/components/IT20125202/user/UserRoles_Students.js",
                                                lineNumber: 148,
                                                columnNumber: 17
                                            }, this),
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("th", {
                                                scope: "col",
                                                children: " Email "
                                            }, void 0, false, {
                                                fileName: "src/components/IT20125202/user/UserRoles_Students.js",
                                                lineNumber: 149,
                                                columnNumber: 17
                                            }, this),
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("th", {
                                                scope: "col",
                                                children: " Mobile "
                                            }, void 0, false, {
                                                fileName: "src/components/IT20125202/user/UserRoles_Students.js",
                                                lineNumber: 150,
                                                columnNumber: 17
                                            }, this),
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("th", {
                                                scope: "col",
                                                children: " Group ID "
                                            }, void 0, false, {
                                                fileName: "src/components/IT20125202/user/UserRoles_Students.js",
                                                lineNumber: 151,
                                                columnNumber: 17
                                            }, this),
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("th", {
                                                scope: "col",
                                                children: " "
                                            }, void 0, false, {
                                                fileName: "src/components/IT20125202/user/UserRoles_Students.js",
                                                lineNumber: 154,
                                                columnNumber: 17
                                            }, this)
                                        ]
                                    }, void 0, true, {
                                        fileName: "src/components/IT20125202/user/UserRoles_Students.js",
                                        lineNumber: 145,
                                        columnNumber: 15
                                    }, this)
                                }, void 0, false, {
                                    fileName: "src/components/IT20125202/user/UserRoles_Students.js",
                                    lineNumber: 144,
                                    columnNumber: 13
                                }, this),
                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("tbody", {
                                    children: this.state.users.map((users, index)=>/*#__PURE__*/ _jsxDevRuntime.jsxDEV("tr", {
                                            children: [
                                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("th", {
                                                    scope: "row",
                                                    children: index + 1
                                                }, void 0, false, {
                                                    fileName: "src/components/IT20125202/user/UserRoles_Students.js",
                                                    lineNumber: 161,
                                                    columnNumber: 19
                                                }, this),
                                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("td", {
                                                    children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("a", {
                                                        href: `/admin/user/${users._id}`,
                                                        style: {
                                                            textDecoration: 'none'
                                                        },
                                                        children: users.idNumber
                                                    }, void 0, false, {
                                                        fileName: "src/components/IT20125202/user/UserRoles_Students.js",
                                                        lineNumber: 163,
                                                        columnNumber: 21
                                                    }, this)
                                                }, void 0, false, {
                                                    fileName: "src/components/IT20125202/user/UserRoles_Students.js",
                                                    lineNumber: 162,
                                                    columnNumber: 19
                                                }, this),
                                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("td", {
                                                    children: users.name
                                                }, void 0, false, {
                                                    fileName: "src/components/IT20125202/user/UserRoles_Students.js",
                                                    lineNumber: 167,
                                                    columnNumber: 19
                                                }, this),
                                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("td", {
                                                    children: users.email
                                                }, void 0, false, {
                                                    fileName: "src/components/IT20125202/user/UserRoles_Students.js",
                                                    lineNumber: 168,
                                                    columnNumber: 19
                                                }, this),
                                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("td", {
                                                    children: users.mobile
                                                }, void 0, false, {
                                                    fileName: "src/components/IT20125202/user/UserRoles_Students.js",
                                                    lineNumber: 169,
                                                    columnNumber: 19
                                                }, this),
                                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("td", {
                                                    children: users.groupId
                                                }, void 0, false, {
                                                    fileName: "src/components/IT20125202/user/UserRoles_Students.js",
                                                    lineNumber: 170,
                                                    columnNumber: 19
                                                }, this),
                                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("td", {
                                                    children: [
                                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("a", {
                                                            className: "btn btn-outline-success",
                                                            href: `/admin/edituser/${users._id}`,
                                                            children: [
                                                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("i", {
                                                                    className: "fas fa-edit"
                                                                }, void 0, false, {
                                                                    fileName: "src/components/IT20125202/user/UserRoles_Students.js",
                                                                    lineNumber: 175,
                                                                    columnNumber: 23
                                                                }, this),
                                                                " \xa0Update"
                                                            ]
                                                        }, void 0, true, {
                                                            fileName: "src/components/IT20125202/user/UserRoles_Students.js",
                                                            lineNumber: 174,
                                                            columnNumber: 21
                                                        }, this),
                                                        "\xa0",
                                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("a", {
                                                            className: "btn btn-outline-danger",
                                                            href: "#",
                                                            onClick: ()=>this.onDelete(users._id)
                                                            ,
                                                            children: [
                                                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("i", {
                                                                    className: "fas fa-trash"
                                                                }, void 0, false, {
                                                                    fileName: "src/components/IT20125202/user/UserRoles_Students.js",
                                                                    lineNumber: 179,
                                                                    columnNumber: 23
                                                                }, this),
                                                                " \xa0Delete"
                                                            ]
                                                        }, void 0, true, {
                                                            fileName: "src/components/IT20125202/user/UserRoles_Students.js",
                                                            lineNumber: 178,
                                                            columnNumber: 21
                                                        }, this)
                                                    ]
                                                }, void 0, true, {
                                                    fileName: "src/components/IT20125202/user/UserRoles_Students.js",
                                                    lineNumber: 173,
                                                    columnNumber: 19
                                                }, this)
                                            ]
                                        }, index, true, {
                                            fileName: "src/components/IT20125202/user/UserRoles_Students.js",
                                            lineNumber: 160,
                                            columnNumber: 17
                                        }, this)
                                    )
                                }, void 0, false, {
                                    fileName: "src/components/IT20125202/user/UserRoles_Students.js",
                                    lineNumber: 158,
                                    columnNumber: 13
                                }, this)
                            ]
                        }, void 0, true, {
                            fileName: "src/components/IT20125202/user/UserRoles_Students.js",
                            lineNumber: 143,
                            columnNumber: 11
                        }, this)
                    ]
                }, void 0, true, {
                    fileName: "src/components/IT20125202/user/UserRoles_Students.js",
                    lineNumber: 118,
                    columnNumber: 9
                }, this)
            ]
        }, void 0, true, {
            fileName: "src/components/IT20125202/user/UserRoles_Students.js",
            lineNumber: 113,
            columnNumber: 7
        }, this);
    }
}
exports.default = UserRoles_Students;

  $parcel$ReactRefreshHelpers$346c.postlude(module);
} finally {
  window.$RefreshReg$ = prevRefreshReg;
  window.$RefreshSig$ = prevRefreshSig;
}
},{"react/jsx-dev-runtime":"iTorj","react":"21dqq","axios":"jo6P5","../admin/AdminNavBar":"kb2lK","sweetalert":"38a65","@parcel/transformer-js/src/esmodule-helpers.js":"gkKU3","@parcel/transformer-react-refresh-wrap/lib/helpers/helpers.js":"km3Ru"}],"ikSOp":[function(require,module,exports) {
var $parcel$ReactRefreshHelpers$ae5e = require("@parcel/transformer-react-refresh-wrap/lib/helpers/helpers.js");
var prevRefreshReg = window.$RefreshReg$;
var prevRefreshSig = window.$RefreshSig$;
$parcel$ReactRefreshHelpers$ae5e.prelude(module);

try {
var parcelHelpers = require("@parcel/transformer-js/src/esmodule-helpers.js");
parcelHelpers.defineInteropFlag(exports);
var _jsxDevRuntime = require("react/jsx-dev-runtime");
// admin - view user roles
var _react = require("react");
var _reactDefault = parcelHelpers.interopDefault(_react);
var _axios = require("axios");
var _axiosDefault = parcelHelpers.interopDefault(_axios);
var _adminNavBar = require("../admin/AdminNavBar");
var _adminNavBarDefault = parcelHelpers.interopDefault(_adminNavBar);
var _sweetalert = require("sweetalert");
var _sweetalertDefault = parcelHelpers.interopDefault(_sweetalert);
class UserRoles_Supervisors extends _react.Component {
    constructor(props){
        super(props);
        this.state = {
            users: []
        };
    }
    componentDidMount() {
        document.title = "User Roles";
        // redirect to the login page if the user is not logged in
        if (!localStorage.token) _sweetalertDefault.default("Please login first", "", "warning").then((value)=>{
            if (value) {
                this.props.history.push(`/admin/login`);
                window.location.reload();
            }
        });
        this.retrieveUsers();
    }
    retrieveUsers() {
        const type = 'Supervisor';
        _axiosDefault.default.get(`http://localhost:5000/users/${type}`).then((res)=>{
            if (res.data.success) this.setState({
                users: res.data.existingUsers
            });
        });
    }
    onDelete = (id)=>{
        //with a confirmation 
        _sweetalertDefault.default({
            title: "Are you sure?",
            text: "Once deleted, you will not be able to recover this user details",
            icon: "warning",
            buttons: true,
            dangerMode: true
        }).then((willDelete)=>{
            if (willDelete) _axiosDefault.default.delete(`http://localhost:5000/user/delete/${id}`).then((res)=>{
                _sweetalertDefault.default("User Deleted Permanently!", "", "success").then((value)=>{
                    if (value) this.retrieveUsers();
                });
            });
            else _sweetalertDefault.default("Cancelled. The user details are safe!");
        });
    };
    handleSearchArea = (e)=>{
        // console.log(e.currentTarget.value)
        const searchKey = e.currentTarget.value;
        _axiosDefault.default.get('http://localhost:5000/users').then((res)=>{
            if (res.data.success) this.filterData(res.data.existingUsers, searchKey);
        });
    };
    filterData(users, searchKey) {
        const searchResult = users.filter((user)=>user.idNumber.toLowerCase().includes(searchKey) || user.name.toLowerCase().includes(searchKey) || user.idNumber.toUpperCase().includes(searchKey) || user.name.toUpperCase().includes(searchKey) || user.idNumber.includes(searchKey) || user.name.includes(searchKey)
        );
        this.setState({
            users: searchResult
        });
    }
    render() {
        return /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
            className: "container",
            children: [
                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("br", {}, void 0, false, {
                    fileName: "src/components/IT20125202/user/UserRoles_Supervisor.js",
                    lineNumber: 110,
                    columnNumber: 9
                }, this),
                /*#__PURE__*/ _jsxDevRuntime.jsxDEV(_adminNavBarDefault.default, {}, void 0, false, {
                    fileName: "src/components/IT20125202/user/UserRoles_Supervisor.js",
                    lineNumber: 111,
                    columnNumber: 9
                }, this),
                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("br", {}, void 0, false, {
                    fileName: "src/components/IT20125202/user/UserRoles_Supervisor.js",
                    lineNumber: 112,
                    columnNumber: 9
                }, this),
                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                    className: "container",
                    children: [
                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                            className: "row",
                            children: [
                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                    className: "col-lg-9 mt-2 mb-2",
                                    children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("h1", {
                                        children: "Users"
                                    }, void 0, false, {
                                        fileName: "src/components/IT20125202/user/UserRoles_Supervisor.js",
                                        lineNumber: 117,
                                        columnNumber: 15
                                    }, this)
                                }, void 0, false, {
                                    fileName: "src/components/IT20125202/user/UserRoles_Supervisor.js",
                                    lineNumber: 116,
                                    columnNumber: 13
                                }, this),
                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                    className: "col-lg-3 mt-2 mb-2",
                                    children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("input", {
                                        className: "form-control",
                                        type: "search",
                                        placeholder: "Search User",
                                        name: "searchQuery",
                                        onChange: this.handleSearchArea
                                    }, void 0, false, {
                                        fileName: "src/components/IT20125202/user/UserRoles_Supervisor.js",
                                        lineNumber: 120,
                                        columnNumber: 15
                                    }, this)
                                }, void 0, false, {
                                    fileName: "src/components/IT20125202/user/UserRoles_Supervisor.js",
                                    lineNumber: 119,
                                    columnNumber: 13
                                }, this),
                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("hr", {}, void 0, false, {
                                    fileName: "src/components/IT20125202/user/UserRoles_Supervisor.js",
                                    lineNumber: 128,
                                    columnNumber: 13
                                }, this),
                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("br", {}, void 0, false, {
                                    fileName: "src/components/IT20125202/user/UserRoles_Supervisor.js",
                                    lineNumber: 128,
                                    columnNumber: 19
                                }, this)
                            ]
                        }, void 0, true, {
                            fileName: "src/components/IT20125202/user/UserRoles_Supervisor.js",
                            lineNumber: 115,
                            columnNumber: 11
                        }, this),
                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                            className: "row",
                            children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                className: "btn-group",
                                children: [
                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("a", {
                                        href: "/admin/users",
                                        className: "btn btn-outline-dark active",
                                        "aria-current": "page",
                                        children: "All Users"
                                    }, void 0, false, {
                                        fileName: "src/components/IT20125202/user/UserRoles_Supervisor.js",
                                        lineNumber: 132,
                                        columnNumber: 15
                                    }, this),
                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("a", {
                                        href: "/admin/panelmembers",
                                        className: "btn btn-outline-dark",
                                        children: "Panel Members"
                                    }, void 0, false, {
                                        fileName: "src/components/IT20125202/user/UserRoles_Supervisor.js",
                                        lineNumber: 133,
                                        columnNumber: 15
                                    }, this),
                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("a", {
                                        href: "/admin/students",
                                        className: "btn btn-outline-dark",
                                        children: "Students"
                                    }, void 0, false, {
                                        fileName: "src/components/IT20125202/user/UserRoles_Supervisor.js",
                                        lineNumber: 134,
                                        columnNumber: 15
                                    }, this),
                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("a", {
                                        href: "/admin/supervisors",
                                        className: "btn btn-outline-dark",
                                        children: "Supervisors"
                                    }, void 0, false, {
                                        fileName: "src/components/IT20125202/user/UserRoles_Supervisor.js",
                                        lineNumber: 135,
                                        columnNumber: 15
                                    }, this)
                                ]
                            }, void 0, true, {
                                fileName: "src/components/IT20125202/user/UserRoles_Supervisor.js",
                                lineNumber: 131,
                                columnNumber: 13
                            }, this)
                        }, void 0, false, {
                            fileName: "src/components/IT20125202/user/UserRoles_Supervisor.js",
                            lineNumber: 130,
                            columnNumber: 11
                        }, this),
                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("br", {}, void 0, false, {
                            fileName: "src/components/IT20125202/user/UserRoles_Supervisor.js",
                            lineNumber: 138,
                            columnNumber: 11
                        }, this),
                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("table", {
                            className: "table",
                            children: [
                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("thead", {
                                    children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("tr", {
                                        children: [
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("th", {
                                                scope: "col",
                                                children: " # "
                                            }, void 0, false, {
                                                fileName: "src/components/IT20125202/user/UserRoles_Supervisor.js",
                                                lineNumber: 142,
                                                columnNumber: 17
                                            }, this),
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("th", {
                                                scope: "col",
                                                children: " ID Number "
                                            }, void 0, false, {
                                                fileName: "src/components/IT20125202/user/UserRoles_Supervisor.js",
                                                lineNumber: 143,
                                                columnNumber: 17
                                            }, this),
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("th", {
                                                scope: "col",
                                                children: " Name "
                                            }, void 0, false, {
                                                fileName: "src/components/IT20125202/user/UserRoles_Supervisor.js",
                                                lineNumber: 144,
                                                columnNumber: 17
                                            }, this),
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("th", {
                                                scope: "col",
                                                children: " Email "
                                            }, void 0, false, {
                                                fileName: "src/components/IT20125202/user/UserRoles_Supervisor.js",
                                                lineNumber: 145,
                                                columnNumber: 17
                                            }, this),
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("th", {
                                                scope: "col",
                                                children: " Mobile "
                                            }, void 0, false, {
                                                fileName: "src/components/IT20125202/user/UserRoles_Supervisor.js",
                                                lineNumber: 146,
                                                columnNumber: 17
                                            }, this),
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("th", {
                                                scope: "col",
                                                children: " Research Field "
                                            }, void 0, false, {
                                                fileName: "src/components/IT20125202/user/UserRoles_Supervisor.js",
                                                lineNumber: 147,
                                                columnNumber: 17
                                            }, this),
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("th", {
                                                scope: "col",
                                                children: " "
                                            }, void 0, false, {
                                                fileName: "src/components/IT20125202/user/UserRoles_Supervisor.js",
                                                lineNumber: 149,
                                                columnNumber: 17
                                            }, this)
                                        ]
                                    }, void 0, true, {
                                        fileName: "src/components/IT20125202/user/UserRoles_Supervisor.js",
                                        lineNumber: 141,
                                        columnNumber: 15
                                    }, this)
                                }, void 0, false, {
                                    fileName: "src/components/IT20125202/user/UserRoles_Supervisor.js",
                                    lineNumber: 140,
                                    columnNumber: 13
                                }, this),
                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("tbody", {
                                    children: this.state.users.map((users, index)=>/*#__PURE__*/ _jsxDevRuntime.jsxDEV("tr", {
                                            children: [
                                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("th", {
                                                    scope: "row",
                                                    children: index + 1
                                                }, void 0, false, {
                                                    fileName: "src/components/IT20125202/user/UserRoles_Supervisor.js",
                                                    lineNumber: 157,
                                                    columnNumber: 19
                                                }, this),
                                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("td", {
                                                    children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("a", {
                                                        href: `/admin/user/${users._id}`,
                                                        style: {
                                                            textDecoration: 'none'
                                                        },
                                                        children: users.idNumber
                                                    }, void 0, false, {
                                                        fileName: "src/components/IT20125202/user/UserRoles_Supervisor.js",
                                                        lineNumber: 159,
                                                        columnNumber: 21
                                                    }, this)
                                                }, void 0, false, {
                                                    fileName: "src/components/IT20125202/user/UserRoles_Supervisor.js",
                                                    lineNumber: 158,
                                                    columnNumber: 19
                                                }, this),
                                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("td", {
                                                    children: users.name
                                                }, void 0, false, {
                                                    fileName: "src/components/IT20125202/user/UserRoles_Supervisor.js",
                                                    lineNumber: 163,
                                                    columnNumber: 19
                                                }, this),
                                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("td", {
                                                    children: users.email
                                                }, void 0, false, {
                                                    fileName: "src/components/IT20125202/user/UserRoles_Supervisor.js",
                                                    lineNumber: 164,
                                                    columnNumber: 19
                                                }, this),
                                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("td", {
                                                    children: users.mobile
                                                }, void 0, false, {
                                                    fileName: "src/components/IT20125202/user/UserRoles_Supervisor.js",
                                                    lineNumber: 165,
                                                    columnNumber: 19
                                                }, this),
                                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("td", {
                                                    children: users.researchfield
                                                }, void 0, false, {
                                                    fileName: "src/components/IT20125202/user/UserRoles_Supervisor.js",
                                                    lineNumber: 166,
                                                    columnNumber: 19
                                                }, this),
                                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("td", {
                                                    children: [
                                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("a", {
                                                            className: "btn btn-outline-success",
                                                            href: `/admin/edituser/${users._id}`,
                                                            children: [
                                                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("i", {
                                                                    className: "fas fa-edit"
                                                                }, void 0, false, {
                                                                    fileName: "src/components/IT20125202/user/UserRoles_Supervisor.js",
                                                                    lineNumber: 171,
                                                                    columnNumber: 23
                                                                }, this),
                                                                " \xa0Update"
                                                            ]
                                                        }, void 0, true, {
                                                            fileName: "src/components/IT20125202/user/UserRoles_Supervisor.js",
                                                            lineNumber: 170,
                                                            columnNumber: 21
                                                        }, this),
                                                        "\xa0",
                                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("a", {
                                                            className: "btn btn-outline-danger",
                                                            href: "#",
                                                            onClick: ()=>this.onDelete(users._id)
                                                            ,
                                                            children: [
                                                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("i", {
                                                                    className: "fas fa-trash"
                                                                }, void 0, false, {
                                                                    fileName: "src/components/IT20125202/user/UserRoles_Supervisor.js",
                                                                    lineNumber: 175,
                                                                    columnNumber: 23
                                                                }, this),
                                                                " \xa0Delete"
                                                            ]
                                                        }, void 0, true, {
                                                            fileName: "src/components/IT20125202/user/UserRoles_Supervisor.js",
                                                            lineNumber: 174,
                                                            columnNumber: 21
                                                        }, this)
                                                    ]
                                                }, void 0, true, {
                                                    fileName: "src/components/IT20125202/user/UserRoles_Supervisor.js",
                                                    lineNumber: 169,
                                                    columnNumber: 19
                                                }, this)
                                            ]
                                        }, index, true, {
                                            fileName: "src/components/IT20125202/user/UserRoles_Supervisor.js",
                                            lineNumber: 156,
                                            columnNumber: 17
                                        }, this)
                                    )
                                }, void 0, false, {
                                    fileName: "src/components/IT20125202/user/UserRoles_Supervisor.js",
                                    lineNumber: 154,
                                    columnNumber: 13
                                }, this)
                            ]
                        }, void 0, true, {
                            fileName: "src/components/IT20125202/user/UserRoles_Supervisor.js",
                            lineNumber: 139,
                            columnNumber: 11
                        }, this)
                    ]
                }, void 0, true, {
                    fileName: "src/components/IT20125202/user/UserRoles_Supervisor.js",
                    lineNumber: 114,
                    columnNumber: 9
                }, this)
            ]
        }, void 0, true, {
            fileName: "src/components/IT20125202/user/UserRoles_Supervisor.js",
            lineNumber: 109,
            columnNumber: 7
        }, this);
    }
}
exports.default = UserRoles_Supervisors;

  $parcel$ReactRefreshHelpers$ae5e.postlude(module);
} finally {
  window.$RefreshReg$ = prevRefreshReg;
  window.$RefreshSig$ = prevRefreshSig;
}
},{"react/jsx-dev-runtime":"iTorj","react":"21dqq","axios":"jo6P5","../admin/AdminNavBar":"kb2lK","sweetalert":"38a65","@parcel/transformer-js/src/esmodule-helpers.js":"gkKU3","@parcel/transformer-react-refresh-wrap/lib/helpers/helpers.js":"km3Ru"}],"bIB5s":[function(require,module,exports) {
var $parcel$ReactRefreshHelpers$cca1 = require("@parcel/transformer-react-refresh-wrap/lib/helpers/helpers.js");
var prevRefreshReg = window.$RefreshReg$;
var prevRefreshSig = window.$RefreshSig$;
$parcel$ReactRefreshHelpers$cca1.prelude(module);

try {
var parcelHelpers = require("@parcel/transformer-js/src/esmodule-helpers.js");
parcelHelpers.defineInteropFlag(exports);
var _jsxDevRuntime = require("react/jsx-dev-runtime");
var _react = require("react");
var _reactDefault = parcelHelpers.interopDefault(_react);
var _axios = require("axios");
var _axiosDefault = parcelHelpers.interopDefault(_axios);
var _sweetalert = require("sweetalert");
var _sweetalertDefault = parcelHelpers.interopDefault(_sweetalert);
var _submitionTypeList = require("./SubmitionTypeList");
var _submitionTypeListDefault = parcelHelpers.interopDefault(_submitionTypeList);
var _adminNavBar = require("../../IT20125202/admin/AdminNavBar");
var _adminNavBarDefault = parcelHelpers.interopDefault(_adminNavBar);
class AddSubmitionType extends _react.Component {
    constructor(props){
        super(props);
        this.state = {
            submitionType: "",
            description: "",
            almarks: "",
            deadLine: "",
            Status: "",
            guidelines: "",
            subject: "About New Submission",
            text: "Added a new submition. Please visit the website and do it before the due date.  --Auto-Generated Email--"
        };
    }
    handleInputChange = (e)=>{
        const { name , value  } = e.target;
        this.setState({
            ...this.state,
            [name]: value
        });
    };
    onSubmit = (e)=>{
        e.preventDefault();
        const { submitionType , description , almarks , deadLine , Status , guidelines , subject , text  } = this.state;
        const data = {
            submitionType: submitionType,
            description: description,
            almarks: almarks,
            deadLine: deadLine,
            Status: Status,
            guidelines: guidelines
        };
        console.log(data);
        //save submitiontypes
        _axiosDefault.default.post("http://localhost:5000/submitiontype/save", data).then((res)=>{
            if (res.data.success) {
                _sweetalertDefault.default("Added SubmitionType Successfully", "", "success").then((value)=>{
                    window.location = "/submitiontype/add";
                });
                this.setState({
                    submitionType: "",
                    description: "",
                    almarks: "",
                    deadLine: "",
                    Status: "",
                    guidelines: ""
                });
            }
        });
        // Send data to Email Service
        const emailData = {
            subject: subject,
            text: text
        };
        //send email to all students
        _axiosDefault.default.post("http://localhost:5000/submitiontype/email", emailData).then((res)=>{
            if (res.data.success) console.log("Sent Email Successfully");
        });
    };
    render() {
        return /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
            className: "container",
            children: [
                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("br", {}, void 0, false, {
                    fileName: "src/components/IT20128036/admin/addSubmitionType.js",
                    lineNumber: 116,
                    columnNumber: 1
                }, this),
                /*#__PURE__*/ _jsxDevRuntime.jsxDEV(_adminNavBarDefault.default, {}, void 0, false, {
                    fileName: "src/components/IT20128036/admin/addSubmitionType.js",
                    lineNumber: 117,
                    columnNumber: 9
                }, this),
                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                    className: "row",
                    children: [
                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                            className: "col-sm-6",
                            children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                className: "ms-2",
                                children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                    className: "row",
                                    children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                        className: "col-sm-12 mt-4 mx-auto",
                                        children: [
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("h3", {
                                                className: "h3 mb-3 font-weight-normal",
                                                children: "Add New Submition Type"
                                            }, void 0, false, {
                                                fileName: "src/components/IT20128036/admin/addSubmitionType.js",
                                                lineNumber: 132,
                                                columnNumber: 17
                                            }, this),
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("br", {}, void 0, false, {
                                                fileName: "src/components/IT20128036/admin/addSubmitionType.js",
                                                lineNumber: 135,
                                                columnNumber: 17
                                            }, this),
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("form", {
                                                className: "needs-validation",
                                                onSubmit: this.onSubmit,
                                                children: [
                                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                                        className: "row",
                                                        children: [
                                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                                                className: "col-sm-7",
                                                                children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                                                    className: "form-group",
                                                                    style: {
                                                                        marginBottom: "15px"
                                                                    },
                                                                    children: [
                                                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("label", {
                                                                            style: {
                                                                                marginBottom: "5px"
                                                                            },
                                                                            children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("strong", {
                                                                                children: "Submition Type"
                                                                            }, void 0, false, {
                                                                                fileName: "src/components/IT20128036/admin/addSubmitionType.js",
                                                                                lineNumber: 144,
                                                                                columnNumber: 26
                                                                            }, this)
                                                                        }, void 0, false, {
                                                                            fileName: "src/components/IT20128036/admin/addSubmitionType.js",
                                                                            lineNumber: 143,
                                                                            columnNumber: 25
                                                                        }, this),
                                                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("select", {
                                                                            className: "form-select",
                                                                            name: "submitionType",
                                                                            value: this.state.submitionType,
                                                                            onChange: this.handleInputChange,
                                                                            required: true,
                                                                            children: [
                                                                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("option", {
                                                                                    submitionType: "not selected yet",
                                                                                    selected: true,
                                                                                    children: "Select Type"
                                                                                }, void 0, false, {
                                                                                    fileName: "src/components/IT20128036/admin/addSubmitionType.js",
                                                                                    lineNumber: 154,
                                                                                    columnNumber: 27
                                                                                }, this),
                                                                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("option", {
                                                                                    submitionType: "Topic Assessment Document",
                                                                                    children: "Topic Assessment Document"
                                                                                }, void 0, false, {
                                                                                    fileName: "src/components/IT20128036/admin/addSubmitionType.js",
                                                                                    lineNumber: 157,
                                                                                    columnNumber: 27
                                                                                }, this),
                                                                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("option", {
                                                                                    submitionType: "Proposal Document",
                                                                                    children: "Proposal Document"
                                                                                }, void 0, false, {
                                                                                    fileName: "src/components/IT20128036/admin/addSubmitionType.js",
                                                                                    lineNumber: 160,
                                                                                    columnNumber: 27
                                                                                }, this),
                                                                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("option", {
                                                                                    submitionType: "Presentation Slides",
                                                                                    children: "Presentation Slides"
                                                                                }, void 0, false, {
                                                                                    fileName: "src/components/IT20128036/admin/addSubmitionType.js",
                                                                                    lineNumber: 163,
                                                                                    columnNumber: 27
                                                                                }, this),
                                                                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("option", {
                                                                                    submitionType: "Final Thesis",
                                                                                    children: "Final Thesis"
                                                                                }, void 0, false, {
                                                                                    fileName: "src/components/IT20128036/admin/addSubmitionType.js",
                                                                                    lineNumber: 166,
                                                                                    columnNumber: 27
                                                                                }, this)
                                                                            ]
                                                                        }, void 0, true, {
                                                                            fileName: "src/components/IT20128036/admin/addSubmitionType.js",
                                                                            lineNumber: 147,
                                                                            columnNumber: 25
                                                                        }, this)
                                                                    ]
                                                                }, void 0, true, {
                                                                    fileName: "src/components/IT20128036/admin/addSubmitionType.js",
                                                                    lineNumber: 139,
                                                                    columnNumber: 23
                                                                }, this)
                                                            }, void 0, false, {
                                                                fileName: "src/components/IT20128036/admin/addSubmitionType.js",
                                                                lineNumber: 138,
                                                                columnNumber: 21
                                                            }, this),
                                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                                                className: "col-sm-5",
                                                                children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                                                    className: "form-group",
                                                                    style: {
                                                                        marginBottom: "15px"
                                                                    },
                                                                    children: [
                                                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("label", {
                                                                            style: {
                                                                                marginBottom: "5px"
                                                                            },
                                                                            children: [
                                                                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("strong", {
                                                                                    children: " Allocated Marks"
                                                                                }, void 0, false, {
                                                                                    fileName: "src/components/IT20128036/admin/addSubmitionType.js",
                                                                                    lineNumber: 178,
                                                                                    columnNumber: 26
                                                                                }, this),
                                                                                " "
                                                                            ]
                                                                        }, void 0, true, {
                                                                            fileName: "src/components/IT20128036/admin/addSubmitionType.js",
                                                                            lineNumber: 177,
                                                                            columnNumber: 25
                                                                        }, this),
                                                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("select", {
                                                                            className: "form-select",
                                                                            name: "almarks",
                                                                            value: this.state.almarks,
                                                                            onChange: this.handleInputChange,
                                                                            required: true,
                                                                            children: [
                                                                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("option", {
                                                                                    almarks: "Select Marks",
                                                                                    children: "Select Marks"
                                                                                }, void 0, false, {
                                                                                    fileName: "src/components/IT20128036/admin/addSubmitionType.js",
                                                                                    lineNumber: 191,
                                                                                    columnNumber: 27
                                                                                }, this),
                                                                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("option", {
                                                                                    almarks: "50",
                                                                                    children: "20"
                                                                                }, void 0, false, {
                                                                                    fileName: "src/components/IT20128036/admin/addSubmitionType.js",
                                                                                    lineNumber: 192,
                                                                                    columnNumber: 27
                                                                                }, this),
                                                                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("option", {
                                                                                    almarks: "30",
                                                                                    children: "30"
                                                                                }, void 0, false, {
                                                                                    fileName: "src/components/IT20128036/admin/addSubmitionType.js",
                                                                                    lineNumber: 193,
                                                                                    columnNumber: 27
                                                                                }, this),
                                                                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("option", {
                                                                                    almarks: "50",
                                                                                    children: "50"
                                                                                }, void 0, false, {
                                                                                    fileName: "src/components/IT20128036/admin/addSubmitionType.js",
                                                                                    lineNumber: 194,
                                                                                    columnNumber: 27
                                                                                }, this)
                                                                            ]
                                                                        }, void 0, true, {
                                                                            fileName: "src/components/IT20128036/admin/addSubmitionType.js",
                                                                            lineNumber: 183,
                                                                            columnNumber: 24
                                                                        }, this)
                                                                    ]
                                                                }, void 0, true, {
                                                                    fileName: "src/components/IT20128036/admin/addSubmitionType.js",
                                                                    lineNumber: 173,
                                                                    columnNumber: 23
                                                                }, this)
                                                            }, void 0, false, {
                                                                fileName: "src/components/IT20128036/admin/addSubmitionType.js",
                                                                lineNumber: 172,
                                                                columnNumber: 21
                                                            }, this)
                                                        ]
                                                    }, void 0, true, {
                                                        fileName: "src/components/IT20128036/admin/addSubmitionType.js",
                                                        lineNumber: 137,
                                                        columnNumber: 19
                                                    }, this),
                                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                                        className: "form-group",
                                                        style: {
                                                            marginBottom: "15px"
                                                        },
                                                        children: [
                                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("label", {
                                                                style: {
                                                                    marginBottom: "5px"
                                                                },
                                                                children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("strong", {
                                                                    children: "Description"
                                                                }, void 0, false, {
                                                                    fileName: "src/components/IT20128036/admin/addSubmitionType.js",
                                                                    lineNumber: 207,
                                                                    columnNumber: 60
                                                                }, this)
                                                            }, void 0, false, {
                                                                fileName: "src/components/IT20128036/admin/addSubmitionType.js",
                                                                lineNumber: 207,
                                                                columnNumber: 21
                                                            }, this),
                                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("textarea", {
                                                                rows: "4",
                                                                cols: "60",
                                                                className: "form-control",
                                                                name: "description",
                                                                placeholder: "Enter Description....",
                                                                value: this.state.description,
                                                                onChange: this.handleInputChange,
                                                                required: true
                                                            }, void 0, false, {
                                                                fileName: "src/components/IT20128036/admin/addSubmitionType.js",
                                                                lineNumber: 208,
                                                                columnNumber: 21
                                                            }, this)
                                                        ]
                                                    }, void 0, true, {
                                                        fileName: "src/components/IT20128036/admin/addSubmitionType.js",
                                                        lineNumber: 206,
                                                        columnNumber: 19
                                                    }, this),
                                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                                        className: "form-group",
                                                        style: {
                                                            marginBottom: "15px"
                                                        },
                                                        children: [
                                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("label", {
                                                                style: {
                                                                    marginBottom: "5px"
                                                                },
                                                                children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("strong", {
                                                                    children: "Guidelines"
                                                                }, void 0, false, {
                                                                    fileName: "src/components/IT20128036/admin/addSubmitionType.js",
                                                                    lineNumber: 222,
                                                                    columnNumber: 60
                                                                }, this)
                                                            }, void 0, false, {
                                                                fileName: "src/components/IT20128036/admin/addSubmitionType.js",
                                                                lineNumber: 222,
                                                                columnNumber: 21
                                                            }, this),
                                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("textarea", {
                                                                rows: "3",
                                                                cols: "60",
                                                                className: "form-control",
                                                                name: "guidelines",
                                                                placeholder: "Enter Guidelines.....",
                                                                value: this.state.guidelines,
                                                                onChange: this.handleInputChange,
                                                                required: true
                                                            }, void 0, false, {
                                                                fileName: "src/components/IT20128036/admin/addSubmitionType.js",
                                                                lineNumber: 223,
                                                                columnNumber: 21
                                                            }, this)
                                                        ]
                                                    }, void 0, true, {
                                                        fileName: "src/components/IT20128036/admin/addSubmitionType.js",
                                                        lineNumber: 221,
                                                        columnNumber: 19
                                                    }, this),
                                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                                        className: "row",
                                                        children: [
                                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                                                className: "col-sm-6",
                                                                children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                                                    className: "form-group",
                                                                    style: {
                                                                        marginBottom: "15px"
                                                                    },
                                                                    children: [
                                                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("label", {
                                                                            style: {
                                                                                marginBottom: "5px"
                                                                            },
                                                                            children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("strong", {
                                                                                children: "Deadline"
                                                                            }, void 0, false, {
                                                                                fileName: "src/components/IT20128036/admin/addSubmitionType.js",
                                                                                lineNumber: 245,
                                                                                columnNumber: 64
                                                                            }, this)
                                                                        }, void 0, false, {
                                                                            fileName: "src/components/IT20128036/admin/addSubmitionType.js",
                                                                            lineNumber: 245,
                                                                            columnNumber: 25
                                                                        }, this),
                                                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("input", {
                                                                            type: "date",
                                                                            className: "form-control",
                                                                            name: "deadLine",
                                                                            value: this.state.deadLine,
                                                                            onChange: this.handleInputChange,
                                                                            required: true
                                                                        }, void 0, false, {
                                                                            fileName: "src/components/IT20128036/admin/addSubmitionType.js",
                                                                            lineNumber: 246,
                                                                            columnNumber: 25
                                                                        }, this)
                                                                    ]
                                                                }, void 0, true, {
                                                                    fileName: "src/components/IT20128036/admin/addSubmitionType.js",
                                                                    lineNumber: 241,
                                                                    columnNumber: 23
                                                                }, this)
                                                            }, void 0, false, {
                                                                fileName: "src/components/IT20128036/admin/addSubmitionType.js",
                                                                lineNumber: 240,
                                                                columnNumber: 21
                                                            }, this),
                                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                                                className: "col-sm-6",
                                                                children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                                                    className: "form-group",
                                                                    style: {
                                                                        marginBottom: "15px"
                                                                    },
                                                                    children: [
                                                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("label", {
                                                                            style: {
                                                                                marginBottom: "5px"
                                                                            },
                                                                            children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("strong", {
                                                                                children: "Submition Status"
                                                                            }, void 0, false, {
                                                                                fileName: "src/components/IT20128036/admin/addSubmitionType.js",
                                                                                lineNumber: 262,
                                                                                columnNumber: 25
                                                                            }, this)
                                                                        }, void 0, false, {
                                                                            fileName: "src/components/IT20128036/admin/addSubmitionType.js",
                                                                            lineNumber: 261,
                                                                            columnNumber: 25
                                                                        }, this),
                                                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("select", {
                                                                            className: "form-select",
                                                                            name: "Status",
                                                                            value: this.state.Status,
                                                                            onChange: this.handleInputChange,
                                                                            required: true,
                                                                            children: [
                                                                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("option", {
                                                                                    checkPanel: "not selected yet",
                                                                                    selected: true,
                                                                                    children: "Select Status"
                                                                                }, void 0, false, {
                                                                                    fileName: "src/components/IT20128036/admin/addSubmitionType.js",
                                                                                    lineNumber: 272,
                                                                                    columnNumber: 27
                                                                                }, this),
                                                                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("option", {
                                                                                    Status: "Mandatory",
                                                                                    children: "Mandatory"
                                                                                }, void 0, false, {
                                                                                    fileName: "src/components/IT20128036/admin/addSubmitionType.js",
                                                                                    lineNumber: 275,
                                                                                    columnNumber: 27
                                                                                }, this),
                                                                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("option", {
                                                                                    Status: "Not Mandatory",
                                                                                    children: "Not Mandatory"
                                                                                }, void 0, false, {
                                                                                    fileName: "src/components/IT20128036/admin/addSubmitionType.js",
                                                                                    lineNumber: 276,
                                                                                    columnNumber: 27
                                                                                }, this)
                                                                            ]
                                                                        }, void 0, true, {
                                                                            fileName: "src/components/IT20128036/admin/addSubmitionType.js",
                                                                            lineNumber: 265,
                                                                            columnNumber: 25
                                                                        }, this)
                                                                    ]
                                                                }, void 0, true, {
                                                                    fileName: "src/components/IT20128036/admin/addSubmitionType.js",
                                                                    lineNumber: 257,
                                                                    columnNumber: 23
                                                                }, this)
                                                            }, void 0, false, {
                                                                fileName: "src/components/IT20128036/admin/addSubmitionType.js",
                                                                lineNumber: 256,
                                                                columnNumber: 21
                                                            }, this)
                                                        ]
                                                    }, void 0, true, {
                                                        fileName: "src/components/IT20128036/admin/addSubmitionType.js",
                                                        lineNumber: 239,
                                                        columnNumber: 19
                                                    }, this),
                                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("button", {
                                                        className: "btn btn-outline-success",
                                                        type: "submit",
                                                        style: {
                                                            margintop: "15px"
                                                        },
                                                        children: [
                                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("i", {
                                                                className: "far fa-check-square"
                                                            }, void 0, false, {
                                                                fileName: "src/components/IT20128036/admin/addSubmitionType.js",
                                                                lineNumber: 291,
                                                                columnNumber: 21
                                                            }, this),
                                                            "\xa0 Save"
                                                        ]
                                                    }, void 0, true, {
                                                        fileName: "src/components/IT20128036/admin/addSubmitionType.js",
                                                        lineNumber: 285,
                                                        columnNumber: 19
                                                    }, this)
                                                ]
                                            }, void 0, true, {
                                                fileName: "src/components/IT20128036/admin/addSubmitionType.js",
                                                lineNumber: 136,
                                                columnNumber: 17
                                            }, this)
                                        ]
                                    }, void 0, true, {
                                        fileName: "src/components/IT20128036/admin/addSubmitionType.js",
                                        lineNumber: 131,
                                        columnNumber: 15
                                    }, this)
                                }, void 0, false, {
                                    fileName: "src/components/IT20128036/admin/addSubmitionType.js",
                                    lineNumber: 130,
                                    columnNumber: 13
                                }, this)
                            }, void 0, false, {
                                fileName: "src/components/IT20128036/admin/addSubmitionType.js",
                                lineNumber: 129,
                                columnNumber: 13
                            }, this)
                        }, void 0, false, {
                            fileName: "src/components/IT20128036/admin/addSubmitionType.js",
                            lineNumber: 128,
                            columnNumber: 11
                        }, this),
                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                            className: "col-sm-6",
                            children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV(_submitionTypeListDefault.default, {}, void 0, false, {
                                fileName: "src/components/IT20128036/admin/addSubmitionType.js",
                                lineNumber: 301,
                                columnNumber: 13
                            }, this)
                        }, void 0, false, {
                            fileName: "src/components/IT20128036/admin/addSubmitionType.js",
                            lineNumber: 300,
                            columnNumber: 11
                        }, this)
                    ]
                }, void 0, true, {
                    fileName: "src/components/IT20128036/admin/addSubmitionType.js",
                    lineNumber: 127,
                    columnNumber: 9
                }, this)
            ]
        }, void 0, true, {
            fileName: "src/components/IT20128036/admin/addSubmitionType.js",
            lineNumber: 113,
            columnNumber: 7
        }, this);
    }
}
exports.default = AddSubmitionType;

  $parcel$ReactRefreshHelpers$cca1.postlude(module);
} finally {
  window.$RefreshReg$ = prevRefreshReg;
  window.$RefreshSig$ = prevRefreshSig;
}
},{"react/jsx-dev-runtime":"iTorj","react":"21dqq","axios":"jo6P5","sweetalert":"38a65","./SubmitionTypeList":"9MmSd","../../IT20125202/admin/AdminNavBar":"kb2lK","@parcel/transformer-js/src/esmodule-helpers.js":"gkKU3","@parcel/transformer-react-refresh-wrap/lib/helpers/helpers.js":"km3Ru"}],"9MmSd":[function(require,module,exports) {
var $parcel$ReactRefreshHelpers$d430 = require("@parcel/transformer-react-refresh-wrap/lib/helpers/helpers.js");
var prevRefreshReg = window.$RefreshReg$;
var prevRefreshSig = window.$RefreshSig$;
$parcel$ReactRefreshHelpers$d430.prelude(module);

try {
var parcelHelpers = require("@parcel/transformer-js/src/esmodule-helpers.js");
parcelHelpers.defineInteropFlag(exports);
var _jsxDevRuntime = require("react/jsx-dev-runtime");
var _react = require("react");
var _reactDefault = parcelHelpers.interopDefault(_react);
var _axios = require("axios");
var _axiosDefault = parcelHelpers.interopDefault(_axios);
var _sweetalert = require("sweetalert");
var _sweetalertDefault = parcelHelpers.interopDefault(_sweetalert);
class SubmitionTypeList extends _react.Component {
    constructor(props){
        super(props);
        this.state = {
            submitiontypes: []
        };
    }
    componentDidMount() {
        this.retrivesubmitionTypes();
    }
    //retrive submitiontype
    retrivesubmitionTypes() {
        _axiosDefault.default.get("http://localhost:5000/submitiontypes").then((res)=>{
            if (res.data.success) {
                this.setState({
                    submitiontypes: res.data.existingsubmitonTypes
                });
                console.log(this.state.submitiontypes);
            }
        });
    }
    //delete submitiontype
    onDelete = (id)=>{
        _sweetalertDefault.default({
            title: "Are you sure?",
            text: "Once deleted, you will not be able to recover this file!",
            icon: "warning",
            buttons: true,
            dangerMode: true
        }).then((willDelete)=>{
            if (willDelete) {
                _axiosDefault.default.delete(`http://localhost:5000/submitiontype/delete/${id}`).then((res)=>{
                    this.retrivesubmitionTypes();
                });
                _sweetalertDefault.default("Submition Type has been deleted", "", "success");
            } else _sweetalertDefault.default("Your file is safe!");
        });
    };
    //filter submitiontype
    filterData(submitiontypes, searchKey) {
        const result = submitiontypes.filter((submitiontype)=>submitiontype.submitionType.toLowerCase().includes(searchKey) || submitiontype.deadLine.toLowerCase().includes(searchKey) || submitiontype.Status.toLowerCase().includes(searchKey) || submitiontype.submitionType.includes(searchKey) || submitiontype.deadLine.includes(searchKey) || submitiontype.Status.includes(searchKey)
        );
        this.setState({
            submitiontypes: result
        });
    }
    //search submitiontypes
    handleSearchArea = (e)=>{
        const searchKey = e.currentTarget.value;
        _axiosDefault.default.get("http://localhost:5000/submitiontypes").then((res)=>{
            if (res.data.success) this.filterData(res.data.existingsubmitonTypes, searchKey);
        });
    };
    render() {
        return /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
            className: "me-2",
            children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                className: "mt-4 mb-4",
                children: [
                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                        className: "row",
                        children: [
                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                className: "col-lg-7 mt-2 mb-2",
                                children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("h4", {
                                    children: "Submitions List"
                                }, void 0, false, {
                                    fileName: "src/components/IT20128036/admin/SubmitionTypeList.js",
                                    lineNumber: 112,
                                    columnNumber: 15
                                }, this)
                            }, void 0, false, {
                                fileName: "src/components/IT20128036/admin/SubmitionTypeList.js",
                                lineNumber: 111,
                                columnNumber: 13
                            }, this),
                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                className: "col-lg-5 mt-2 mb-2",
                                children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("input", {
                                    className: "form-control",
                                    type: "search",
                                    placeholder: "Search . . .",
                                    name: "searchQuery",
                                    onChange: this.handleSearchArea
                                }, void 0, false, {
                                    fileName: "src/components/IT20128036/admin/SubmitionTypeList.js",
                                    lineNumber: 115,
                                    columnNumber: 15
                                }, this)
                            }, void 0, false, {
                                fileName: "src/components/IT20128036/admin/SubmitionTypeList.js",
                                lineNumber: 114,
                                columnNumber: 13
                            }, this)
                        ]
                    }, void 0, true, {
                        fileName: "src/components/IT20128036/admin/SubmitionTypeList.js",
                        lineNumber: 110,
                        columnNumber: 11
                    }, this),
                    this.state.submitiontypes.map((submitiontypes, index)=>/*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                            class: "row",
                            children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                class: "col-sm-12",
                                children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                    class: "card mt-2 mb-4",
                                    children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                        class: "card-body",
                                        children: [
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                                class: "card-header",
                                                children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("h5", {
                                                    class: "card-title",
                                                    children: [
                                                        " ",
                                                        submitiontypes.submitionType,
                                                        " Submition",
                                                        " "
                                                    ]
                                                }, void 0, true, {
                                                    fileName: "src/components/IT20128036/admin/SubmitionTypeList.js",
                                                    lineNumber: 131,
                                                    columnNumber: 23
                                                }, this)
                                            }, void 0, false, {
                                                fileName: "src/components/IT20128036/admin/SubmitionTypeList.js",
                                                lineNumber: 130,
                                                columnNumber: 21
                                            }, this),
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("p", {
                                                class: "card-text"
                                            }, void 0, false, {
                                                fileName: "src/components/IT20128036/admin/SubmitionTypeList.js",
                                                lineNumber: 145,
                                                columnNumber: 21
                                            }, this),
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("p", {
                                                children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("strong", {
                                                    children: "Description"
                                                }, void 0, false, {
                                                    fileName: "src/components/IT20128036/admin/SubmitionTypeList.js",
                                                    lineNumber: 147,
                                                    columnNumber: 23
                                                }, this)
                                            }, void 0, false, {
                                                fileName: "src/components/IT20128036/admin/SubmitionTypeList.js",
                                                lineNumber: 146,
                                                columnNumber: 21
                                            }, this),
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("p", {
                                                children: [
                                                    " ",
                                                    submitiontypes.description
                                                ]
                                            }, void 0, true, {
                                                fileName: "src/components/IT20128036/admin/SubmitionTypeList.js",
                                                lineNumber: 149,
                                                columnNumber: 21
                                            }, this),
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("p", {
                                                children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("strong", {
                                                    children: "Guidelines"
                                                }, void 0, false, {
                                                    fileName: "src/components/IT20128036/admin/SubmitionTypeList.js",
                                                    lineNumber: 151,
                                                    columnNumber: 23
                                                }, this)
                                            }, void 0, false, {
                                                fileName: "src/components/IT20128036/admin/SubmitionTypeList.js",
                                                lineNumber: 150,
                                                columnNumber: 21
                                            }, this),
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("p", {
                                                children: submitiontypes.guidelines
                                            }, void 0, false, {
                                                fileName: "src/components/IT20128036/admin/SubmitionTypeList.js",
                                                lineNumber: 153,
                                                columnNumber: 21
                                            }, this),
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("p", {
                                                children: [
                                                    " ",
                                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("strong", {
                                                        children: "Allocated Marks"
                                                    }, void 0, false, {
                                                        fileName: "src/components/IT20128036/admin/SubmitionTypeList.js",
                                                        lineNumber: 158,
                                                        columnNumber: 23
                                                    }, this)
                                                ]
                                            }, void 0, true, {
                                                fileName: "src/components/IT20128036/admin/SubmitionTypeList.js",
                                                lineNumber: 156,
                                                columnNumber: 21
                                            }, this),
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("p", {
                                                children: [
                                                    submitiontypes.almarks,
                                                    "%"
                                                ]
                                            }, void 0, true, {
                                                fileName: "src/components/IT20128036/admin/SubmitionTypeList.js",
                                                lineNumber: 160,
                                                columnNumber: 21
                                            }, this),
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("p", {
                                                children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("strong", {
                                                    children: "Deadline"
                                                }, void 0, false, {
                                                    fileName: "src/components/IT20128036/admin/SubmitionTypeList.js",
                                                    lineNumber: 162,
                                                    columnNumber: 23
                                                }, this)
                                            }, void 0, false, {
                                                fileName: "src/components/IT20128036/admin/SubmitionTypeList.js",
                                                lineNumber: 161,
                                                columnNumber: 21
                                            }, this),
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("p", {
                                                children: [
                                                    submitiontypes.deadLine,
                                                    "  \xa0\xa0\xa0\xa0 -",
                                                    submitiontypes.Status,
                                                    "-"
                                                ]
                                            }, void 0, true, {
                                                fileName: "src/components/IT20128036/admin/SubmitionTypeList.js",
                                                lineNumber: 164,
                                                columnNumber: 21
                                            }, this),
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("p", {
                                                children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("strong", {}, void 0, false, {
                                                    fileName: "src/components/IT20128036/admin/SubmitionTypeList.js",
                                                    lineNumber: 167,
                                                    columnNumber: 23
                                                }, this)
                                            }, void 0, false, {
                                                fileName: "src/components/IT20128036/admin/SubmitionTypeList.js",
                                                lineNumber: 166,
                                                columnNumber: 21
                                            }, this),
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                                className: "row",
                                                children: [
                                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                                        className: "col-lg-4",
                                                        children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("a", {
                                                            href: "#",
                                                            class: "btn btn-outline-success",
                                                            children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("i", {
                                                                class: "fa fa-plus",
                                                                "aria-hidden": "true",
                                                                children: [
                                                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("br", {}, void 0, false, {
                                                                        fileName: "src/components/IT20128036/admin/SubmitionTypeList.js",
                                                                        lineNumber: 173,
                                                                        columnNumber: 68
                                                                    }, this),
                                                                    "Submit"
                                                                ]
                                                            }, void 0, true, {
                                                                fileName: "src/components/IT20128036/admin/SubmitionTypeList.js",
                                                                lineNumber: 173,
                                                                columnNumber: 27
                                                            }, this)
                                                        }, void 0, false, {
                                                            fileName: "src/components/IT20128036/admin/SubmitionTypeList.js",
                                                            lineNumber: 172,
                                                            columnNumber: 25
                                                        }, this)
                                                    }, void 0, false, {
                                                        fileName: "src/components/IT20128036/admin/SubmitionTypeList.js",
                                                        lineNumber: 171,
                                                        columnNumber: 23
                                                    }, this),
                                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                                        className: "col-lg-4",
                                                        children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("a", {
                                                            href: `/submitiontype/edit/${submitiontypes._id}`,
                                                            class: "btn btn-outline-primary",
                                                            children: [
                                                                " ",
                                                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("i", {
                                                                    class: "fa fa-pencil-square-o",
                                                                    "aria-hidden": "true",
                                                                    children: [
                                                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("br", {}, void 0, false, {
                                                                            fileName: "src/components/IT20128036/admin/SubmitionTypeList.js",
                                                                            lineNumber: 183,
                                                                            columnNumber: 80
                                                                        }, this),
                                                                        "Update"
                                                                    ]
                                                                }, void 0, true, {
                                                                    fileName: "src/components/IT20128036/admin/SubmitionTypeList.js",
                                                                    lineNumber: 183,
                                                                    columnNumber: 27
                                                                }, this)
                                                            ]
                                                        }, void 0, true, {
                                                            fileName: "src/components/IT20128036/admin/SubmitionTypeList.js",
                                                            lineNumber: 178,
                                                            columnNumber: 25
                                                        }, this)
                                                    }, void 0, false, {
                                                        fileName: "src/components/IT20128036/admin/SubmitionTypeList.js",
                                                        lineNumber: 177,
                                                        columnNumber: 23
                                                    }, this),
                                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                                        className: "col-lg-4",
                                                        children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("a", {
                                                            href: "#",
                                                            class: "btn btn-outline-danger",
                                                            onClick: ()=>{
                                                                this.onDelete(submitiontypes._id);
                                                            },
                                                            children: [
                                                                " ",
                                                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("i", {
                                                                    class: "fa fa-trash-o",
                                                                    "aria-hidden": "true",
                                                                    children: [
                                                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("br", {}, void 0, false, {
                                                                            fileName: "src/components/IT20128036/admin/SubmitionTypeList.js",
                                                                            lineNumber: 196,
                                                                            columnNumber: 71
                                                                        }, this),
                                                                        "Delete"
                                                                    ]
                                                                }, void 0, true, {
                                                                    fileName: "src/components/IT20128036/admin/SubmitionTypeList.js",
                                                                    lineNumber: 196,
                                                                    columnNumber: 27
                                                                }, this)
                                                            ]
                                                        }, void 0, true, {
                                                            fileName: "src/components/IT20128036/admin/SubmitionTypeList.js",
                                                            lineNumber: 188,
                                                            columnNumber: 25
                                                        }, this)
                                                    }, void 0, false, {
                                                        fileName: "src/components/IT20128036/admin/SubmitionTypeList.js",
                                                        lineNumber: 187,
                                                        columnNumber: 23
                                                    }, this)
                                                ]
                                            }, void 0, true, {
                                                fileName: "src/components/IT20128036/admin/SubmitionTypeList.js",
                                                lineNumber: 170,
                                                columnNumber: 21
                                            }, this)
                                        ]
                                    }, void 0, true, {
                                        fileName: "src/components/IT20128036/admin/SubmitionTypeList.js",
                                        lineNumber: 129,
                                        columnNumber: 19
                                    }, this)
                                }, void 0, false, {
                                    fileName: "src/components/IT20128036/admin/SubmitionTypeList.js",
                                    lineNumber: 128,
                                    columnNumber: 17
                                }, this)
                            }, void 0, false, {
                                fileName: "src/components/IT20128036/admin/SubmitionTypeList.js",
                                lineNumber: 127,
                                columnNumber: 15
                            }, this)
                        }, void 0, false, {
                            fileName: "src/components/IT20128036/admin/SubmitionTypeList.js",
                            lineNumber: 126,
                            columnNumber: 13
                        }, this)
                    )
                ]
            }, void 0, true, {
                fileName: "src/components/IT20128036/admin/SubmitionTypeList.js",
                lineNumber: 109,
                columnNumber: 9
            }, this)
        }, void 0, false, {
            fileName: "src/components/IT20128036/admin/SubmitionTypeList.js",
            lineNumber: 108,
            columnNumber: 7
        }, this);
    }
}
exports.default = SubmitionTypeList;

  $parcel$ReactRefreshHelpers$d430.postlude(module);
} finally {
  window.$RefreshReg$ = prevRefreshReg;
  window.$RefreshSig$ = prevRefreshSig;
}
},{"react/jsx-dev-runtime":"iTorj","react":"21dqq","axios":"jo6P5","sweetalert":"38a65","@parcel/transformer-js/src/esmodule-helpers.js":"gkKU3","@parcel/transformer-react-refresh-wrap/lib/helpers/helpers.js":"km3Ru"}],"3WGlv":[function(require,module,exports) {
var $parcel$ReactRefreshHelpers$0d40 = require("@parcel/transformer-react-refresh-wrap/lib/helpers/helpers.js");
var prevRefreshReg = window.$RefreshReg$;
var prevRefreshSig = window.$RefreshSig$;
$parcel$ReactRefreshHelpers$0d40.prelude(module);

try {
var parcelHelpers = require("@parcel/transformer-js/src/esmodule-helpers.js");
parcelHelpers.defineInteropFlag(exports);
var _jsxDevRuntime = require("react/jsx-dev-runtime");
var _react = require("react");
var _reactDefault = parcelHelpers.interopDefault(_react);
var _axios = require("axios");
var _axiosDefault = parcelHelpers.interopDefault(_axios);
var _sweetalert = require("sweetalert");
var _sweetalertDefault = parcelHelpers.interopDefault(_sweetalert);
var _adminNavBar = require("../../IT20125202/admin/AdminNavBar");
var _adminNavBarDefault = parcelHelpers.interopDefault(_adminNavBar);
class EditSubmitonType extends _react.Component {
    constructor(props){
        super(props);
        this.state = {
            submitionType: "",
            description: "",
            almarks: "",
            deadLine: "",
            Status: "",
            guidelines: ""
        };
    }
    handleInputChange = (e)=>{
        const { name , value  } = e.target;
        this.setState({
            ...this.state,
            [name]: value
        });
    };
    onSubmit = (e)=>{
        e.preventDefault();
        const id = this.props.match.params.id;
        const { submitionType , description , almarks , deadLine , Status , guidelines  } = this.state;
        const data = {
            submitionType: submitionType,
            description: description,
            almarks: almarks,
            deadLine: deadLine,
            Status: Status,
            guidelines: guidelines
        };
        console.log(data);
        //update submitiontype
        _axiosDefault.default.put(`http://localhost:5000/submitiontype/update/${id}`, data).then((res)=>{
            if (res.data.success) {
                _sweetalertDefault.default(" Updated Successfully !", "", "success").then((value)=>{
                    window.location = "/submitiontype/add";
                });
                this.setState({
                    submitionType: "",
                    description: "",
                    almarks: "",
                    deadLine: "",
                    Status: "",
                    guidelines: ""
                });
            }
        });
    };
    componentDidMount() {
        // if(this.props.match && this.props.match.params.id){
        const id = this.props.match.params.id;
        //retrive submitiontype to update   
        _axiosDefault.default.get(`http://localhost:5000/submitiontype/${id}`).then((res)=>{
            if (res.data.success) {
                this.setState({
                    submitionType: res.data.submitiontype.submitionType,
                    description: res.data.submitiontype.description,
                    almarks: res.data.submitiontype.almarks,
                    deadLine: res.data.submitiontype.deadLine,
                    Status: res.data.submitiontype.Status,
                    guidelines: res.data.submitiontype.guidelines
                });
                console.log(this.state);
            }
        });
    // }
    }
    render() {
        return /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
            className: "container",
            children: [
                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("br", {}, void 0, false, {
                    fileName: "src/components/IT20128036/admin/editSubmitionType.js",
                    lineNumber: 123,
                    columnNumber: 1
                }, this),
                /*#__PURE__*/ _jsxDevRuntime.jsxDEV(_adminNavBarDefault.default, {}, void 0, false, {
                    fileName: "src/components/IT20128036/admin/editSubmitionType.js",
                    lineNumber: 124,
                    columnNumber: 9
                }, this),
                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                    className: "col-12 mt-4",
                    children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                        className: "ms-4 me-4",
                        children: [
                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("h1", {
                                className: "h3 mb-3 font-weight-normal",
                                children: "Edit Submition Type"
                            }, void 0, false, {
                                fileName: "src/components/IT20128036/admin/editSubmitionType.js",
                                lineNumber: 129,
                                columnNumber: 8
                            }, this),
                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("form", {
                                className: "needs-validation",
                                noValidate: true,
                                children: [
                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                        className: "row",
                                        children: [
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                                className: "col-sm-7",
                                                children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                                    className: "form-group",
                                                    style: {
                                                        marginBottom: '15px'
                                                    },
                                                    children: [
                                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("label", {
                                                            style: {
                                                                marginBottom: '5px'
                                                            },
                                                            children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("strong", {
                                                                children: "Submition Type"
                                                            }, void 0, false, {
                                                                fileName: "src/components/IT20128036/admin/editSubmitionType.js",
                                                                lineNumber: 136,
                                                                columnNumber: 48
                                                            }, this)
                                                        }, void 0, false, {
                                                            fileName: "src/components/IT20128036/admin/editSubmitionType.js",
                                                            lineNumber: 136,
                                                            columnNumber: 12
                                                        }, this),
                                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("input", {
                                                            type: "text",
                                                            className: "form-control",
                                                            name: "submitionType",
                                                            placeholder: "Edit Submition Type",
                                                            value: this.state.submitionType,
                                                            onChange: this.handleInputChange
                                                        }, void 0, false, {
                                                            fileName: "src/components/IT20128036/admin/editSubmitionType.js",
                                                            lineNumber: 137,
                                                            columnNumber: 12
                                                        }, this)
                                                    ]
                                                }, void 0, true, {
                                                    fileName: "src/components/IT20128036/admin/editSubmitionType.js",
                                                    lineNumber: 135,
                                                    columnNumber: 3
                                                }, this)
                                            }, void 0, false, {
                                                fileName: "src/components/IT20128036/admin/editSubmitionType.js",
                                                lineNumber: 133,
                                                columnNumber: 3
                                            }, this),
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                                className: "col-sm-5",
                                                children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                                    className: "form-group",
                                                    style: {
                                                        marginBottom: '15px'
                                                    },
                                                    children: [
                                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("label", {
                                                            style: {
                                                                marginBottom: '5px'
                                                            },
                                                            children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("strong", {
                                                                children: "Allocated Marks"
                                                            }, void 0, false, {
                                                                fileName: "src/components/IT20128036/admin/editSubmitionType.js",
                                                                lineNumber: 150,
                                                                columnNumber: 48
                                                            }, this)
                                                        }, void 0, false, {
                                                            fileName: "src/components/IT20128036/admin/editSubmitionType.js",
                                                            lineNumber: 150,
                                                            columnNumber: 12
                                                        }, this),
                                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("input", {
                                                            type: "text",
                                                            className: "form-control",
                                                            name: "almarks",
                                                            placeholder: "Edit Allocated Marks",
                                                            value: this.state.almarks,
                                                            onChange: this.handleInputChange
                                                        }, void 0, false, {
                                                            fileName: "src/components/IT20128036/admin/editSubmitionType.js",
                                                            lineNumber: 151,
                                                            columnNumber: 12
                                                        }, this)
                                                    ]
                                                }, void 0, true, {
                                                    fileName: "src/components/IT20128036/admin/editSubmitionType.js",
                                                    lineNumber: 149,
                                                    columnNumber: 3
                                                }, this)
                                            }, void 0, false, {
                                                fileName: "src/components/IT20128036/admin/editSubmitionType.js",
                                                lineNumber: 148,
                                                columnNumber: 3
                                            }, this)
                                        ]
                                    }, void 0, true, {
                                        fileName: "src/components/IT20128036/admin/editSubmitionType.js",
                                        lineNumber: 132,
                                        columnNumber: 1
                                    }, this),
                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                        className: "form-group",
                                        style: {
                                            marginBottom: '15px'
                                        },
                                        children: [
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("label", {
                                                style: {
                                                    marginBottom: '5px'
                                                },
                                                children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("strong", {
                                                    children: "Description"
                                                }, void 0, false, {
                                                    fileName: "src/components/IT20128036/admin/editSubmitionType.js",
                                                    lineNumber: 169,
                                                    columnNumber: 48
                                                }, this)
                                            }, void 0, false, {
                                                fileName: "src/components/IT20128036/admin/editSubmitionType.js",
                                                lineNumber: 169,
                                                columnNumber: 12
                                            }, this),
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("textarea", {
                                                className: "form-control",
                                                name: "description",
                                                placeholder: "Edit Description",
                                                value: this.state.description,
                                                onChange: this.handleInputChange
                                            }, void 0, false, {
                                                fileName: "src/components/IT20128036/admin/editSubmitionType.js",
                                                lineNumber: 170,
                                                columnNumber: 12
                                            }, this)
                                        ]
                                    }, void 0, true, {
                                        fileName: "src/components/IT20128036/admin/editSubmitionType.js",
                                        lineNumber: 168,
                                        columnNumber: 10
                                    }, this),
                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                        className: "row",
                                        children: [
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                                className: "col-6",
                                                children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                                    className: "form-group",
                                                    style: {
                                                        marginBottom: '15px'
                                                    },
                                                    children: [
                                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("label", {
                                                            style: {
                                                                marginBottom: '5px'
                                                            },
                                                            children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("strong", {
                                                                children: "Deadline"
                                                            }, void 0, false, {
                                                                fileName: "src/components/IT20128036/admin/editSubmitionType.js",
                                                                lineNumber: 187,
                                                                columnNumber: 48
                                                            }, this)
                                                        }, void 0, false, {
                                                            fileName: "src/components/IT20128036/admin/editSubmitionType.js",
                                                            lineNumber: 187,
                                                            columnNumber: 12
                                                        }, this),
                                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("input", {
                                                            type: "date",
                                                            className: "form-control",
                                                            name: "deadLine",
                                                            value: this.state.deadLine,
                                                            onChange: this.handleInputChange
                                                        }, void 0, false, {
                                                            fileName: "src/components/IT20128036/admin/editSubmitionType.js",
                                                            lineNumber: 188,
                                                            columnNumber: 12
                                                        }, this)
                                                    ]
                                                }, void 0, true, {
                                                    fileName: "src/components/IT20128036/admin/editSubmitionType.js",
                                                    lineNumber: 186,
                                                    columnNumber: 1
                                                }, this)
                                            }, void 0, false, {
                                                fileName: "src/components/IT20128036/admin/editSubmitionType.js",
                                                lineNumber: 185,
                                                columnNumber: 1
                                            }, this),
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                                className: "col-6",
                                                children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                                    className: "form-group",
                                                    style: {
                                                        marginBottom: '15px'
                                                    },
                                                    children: [
                                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("label", {
                                                            style: {
                                                                marginBottom: '5px'
                                                            },
                                                            children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("strong", {
                                                                children: "Submition Status"
                                                            }, void 0, false, {
                                                                fileName: "src/components/IT20128036/admin/editSubmitionType.js",
                                                                lineNumber: 202,
                                                                columnNumber: 48
                                                            }, this)
                                                        }, void 0, false, {
                                                            fileName: "src/components/IT20128036/admin/editSubmitionType.js",
                                                            lineNumber: 202,
                                                            columnNumber: 12
                                                        }, this),
                                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("input", {
                                                            type: "text",
                                                            className: "form-control",
                                                            name: "Status",
                                                            value: this.state.Status,
                                                            onChange: this.handleInputChange
                                                        }, void 0, false, {
                                                            fileName: "src/components/IT20128036/admin/editSubmitionType.js",
                                                            lineNumber: 203,
                                                            columnNumber: 12
                                                        }, this)
                                                    ]
                                                }, void 0, true, {
                                                    fileName: "src/components/IT20128036/admin/editSubmitionType.js",
                                                    lineNumber: 201,
                                                    columnNumber: 1
                                                }, this)
                                            }, void 0, false, {
                                                fileName: "src/components/IT20128036/admin/editSubmitionType.js",
                                                lineNumber: 200,
                                                columnNumber: 1
                                            }, this)
                                        ]
                                    }, void 0, true, {
                                        fileName: "src/components/IT20128036/admin/editSubmitionType.js",
                                        lineNumber: 184,
                                        columnNumber: 9
                                    }, this),
                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                        className: "form-group",
                                        style: {
                                            marginBottom: '15px'
                                        },
                                        children: [
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("label", {
                                                style: {
                                                    marginBottom: '5px'
                                                },
                                                children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("strong", {
                                                    children: "Guidelines"
                                                }, void 0, false, {
                                                    fileName: "src/components/IT20128036/admin/editSubmitionType.js",
                                                    lineNumber: 225,
                                                    columnNumber: 48
                                                }, this)
                                            }, void 0, false, {
                                                fileName: "src/components/IT20128036/admin/editSubmitionType.js",
                                                lineNumber: 225,
                                                columnNumber: 12
                                            }, this),
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("textarea", {
                                                className: "form-control",
                                                name: "guidelines",
                                                value: this.state.guidelines,
                                                onChange: this.handleInputChange
                                            }, void 0, false, {
                                                fileName: "src/components/IT20128036/admin/editSubmitionType.js",
                                                lineNumber: 226,
                                                columnNumber: 12
                                            }, this)
                                        ]
                                    }, void 0, true, {
                                        fileName: "src/components/IT20128036/admin/editSubmitionType.js",
                                        lineNumber: 224,
                                        columnNumber: 10
                                    }, this),
                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                        className: "row",
                                        children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                            className: "col-sm-6",
                                            children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("button", {
                                                className: "btn btn-outline-success",
                                                type: "submit",
                                                style: {
                                                    margintop: '15px'
                                                },
                                                onClick: this.onSubmit,
                                                children: [
                                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("i", {
                                                        className: "far fa-check-square"
                                                    }, void 0, false, {
                                                        fileName: "src/components/IT20128036/admin/editSubmitionType.js",
                                                        lineNumber: 243,
                                                        columnNumber: 12
                                                    }, this),
                                                    "\xa0 Update"
                                                ]
                                            }, void 0, true, {
                                                fileName: "src/components/IT20128036/admin/editSubmitionType.js",
                                                lineNumber: 242,
                                                columnNumber: 1
                                            }, this)
                                        }, void 0, false, {
                                            fileName: "src/components/IT20128036/admin/editSubmitionType.js",
                                            lineNumber: 241,
                                            columnNumber: 1
                                        }, this)
                                    }, void 0, false, {
                                        fileName: "src/components/IT20128036/admin/editSubmitionType.js",
                                        lineNumber: 240,
                                        columnNumber: 1
                                    }, this)
                                ]
                            }, void 0, true, {
                                fileName: "src/components/IT20128036/admin/editSubmitionType.js",
                                lineNumber: 130,
                                columnNumber: 8
                            }, this)
                        ]
                    }, void 0, true, {
                        fileName: "src/components/IT20128036/admin/editSubmitionType.js",
                        lineNumber: 128,
                        columnNumber: 8
                    }, this)
                }, void 0, false, {
                    fileName: "src/components/IT20128036/admin/editSubmitionType.js",
                    lineNumber: 127,
                    columnNumber: 6
                }, this)
            ]
        }, void 0, true, {
            fileName: "src/components/IT20128036/admin/editSubmitionType.js",
            lineNumber: 119,
            columnNumber: 7
        }, this);
    }
}
exports.default = EditSubmitonType;

  $parcel$ReactRefreshHelpers$0d40.postlude(module);
} finally {
  window.$RefreshReg$ = prevRefreshReg;
  window.$RefreshSig$ = prevRefreshSig;
}
},{"react/jsx-dev-runtime":"iTorj","react":"21dqq","axios":"jo6P5","sweetalert":"38a65","../../IT20125202/admin/AdminNavBar":"kb2lK","@parcel/transformer-js/src/esmodule-helpers.js":"gkKU3","@parcel/transformer-react-refresh-wrap/lib/helpers/helpers.js":"km3Ru"}],"jUWtU":[function(require,module,exports) {
var $parcel$ReactRefreshHelpers$c341 = require("@parcel/transformer-react-refresh-wrap/lib/helpers/helpers.js");
var prevRefreshReg = window.$RefreshReg$;
var prevRefreshSig = window.$RefreshSig$;
$parcel$ReactRefreshHelpers$c341.prelude(module);

try {
var parcelHelpers = require("@parcel/transformer-js/src/esmodule-helpers.js");
parcelHelpers.defineInteropFlag(exports);
var _jsxDevRuntime = require("react/jsx-dev-runtime");
var _react = require("react");
var _reactDefault = parcelHelpers.interopDefault(_react);
var _axios = require("axios");
var _axiosDefault = parcelHelpers.interopDefault(_axios);
var _adminNavBar = require("../../IT20125202/admin/AdminNavBar");
var _adminNavBarDefault = parcelHelpers.interopDefault(_adminNavBar);
class StudentGroup extends _react.Component {
    constructor(props){
        super(props);
        this.state = {
            studentgroups: []
        };
    }
    componentDidMount() {
        this.retrieveStudentGropus();
    }
    retrieveStudentGropus() {
        _axiosDefault.default.get("http://localhost:5000/sgroups").then((res)=>{
            if (res.data.success) {
                this.setState({
                    studentgroups: res.data.existingstudentgroups
                });
                console.log(this.state.studentgroups);
            }
        });
    }
    onDelete = (id)=>{
        _axiosDefault.default.delete(`http://localhost:5000/sgroup/delete/${id}`).then((res)=>{
            swal("Are you sure to delete the Group Details?", "", "warning");
            this.retrieveStudentGropus();
        });
    };
    filterData(studentgroups, searchKey) {
        const result = studentgroups.filter((studentgroup)=>studentgroup.groupid.toLowerCase().includes(searchKey) || studentgroup.groupname.toLowerCase().includes(searchKey) || studentgroup.status.toLowerCase().includes(searchKey) || studentgroup.groupid.toUpperCase().includes(searchKey) || studentgroup.groupname.toUpperCase().includes(searchKey) || studentgroup.status.toUpperCase().includes(searchKey) || studentgroup.groupid.includes(searchKey) || studentgroup.groupname.includes(searchKey) || studentgroup.status.includes(searchKey)
        );
        this.setState({
            studentgroups: result
        });
    }
    handleSearchArea = (e)=>{
        const searchKey = e.currentTarget.value;
        _axiosDefault.default.get("http://localhost:5000/sgroups").then((res)=>{
            if (res.data.success) this.filterData(res.data.existingstudentgroups, searchKey);
        });
    };
    render() {
        return /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
            className: "container px-5 my-3",
            children: [
                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("br", {}, void 0, false, {
                    fileName: "src/components/IT20131456/admin/StudentGroup.js",
                    lineNumber: 65,
                    columnNumber: 9
                }, this),
                /*#__PURE__*/ _jsxDevRuntime.jsxDEV(_adminNavBarDefault.default, {}, void 0, false, {
                    fileName: "src/components/IT20131456/admin/StudentGroup.js",
                    lineNumber: 66,
                    columnNumber: 9
                }, this),
                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("br", {}, void 0, false, {
                    fileName: "src/components/IT20131456/admin/StudentGroup.js",
                    lineNumber: 67,
                    columnNumber: 9
                }, this),
                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                    className: "row",
                    children: [
                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                            className: "float-left col-lg-9 mt-2 mb-2",
                            children: [
                                "\xa0",
                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("h2", {
                                    children: "Student Groups"
                                }, void 0, false, {
                                    fileName: "src/components/IT20131456/admin/StudentGroup.js",
                                    lineNumber: 72,
                                    columnNumber: 13
                                }, this)
                            ]
                        }, void 0, true, {
                            fileName: "src/components/IT20131456/admin/StudentGroup.js",
                            lineNumber: 70,
                            columnNumber: 11
                        }, this),
                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                            className: "col-lg-3 mt-2 mb-2",
                            children: [
                                "\xa0",
                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("input", {
                                    className: "form-control border border-dark",
                                    type: "search",
                                    placeholder: "Search",
                                    name: "searchQuery",
                                    onChange: this.handleSearchArea
                                }, void 0, false, {
                                    fileName: "src/components/IT20131456/admin/StudentGroup.js",
                                    lineNumber: 77,
                                    columnNumber: 13
                                }, this)
                            ]
                        }, void 0, true, {
                            fileName: "src/components/IT20131456/admin/StudentGroup.js",
                            lineNumber: 75,
                            columnNumber: 11
                        }, this),
                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("hr", {}, void 0, false, {
                            fileName: "src/components/IT20131456/admin/StudentGroup.js",
                            lineNumber: 85,
                            columnNumber: 11
                        }, this)
                    ]
                }, void 0, true, {
                    fileName: "src/components/IT20131456/admin/StudentGroup.js",
                    lineNumber: 69,
                    columnNumber: 9
                }, this),
                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("table", {
                    className: "table table-striped table-bordered",
                    children: [
                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("thead", {
                            className: " text-light",
                            style: {
                                background: "#000080"
                            },
                            children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("tr", {
                                children: [
                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("th", {
                                        scope: "col",
                                        children: "#"
                                    }, void 0, false, {
                                        fileName: "src/components/IT20131456/admin/StudentGroup.js",
                                        lineNumber: 91,
                                        columnNumber: 15
                                    }, this),
                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("th", {
                                        scope: "col",
                                        children: "Group ID"
                                    }, void 0, false, {
                                        fileName: "src/components/IT20131456/admin/StudentGroup.js",
                                        lineNumber: 92,
                                        columnNumber: 15
                                    }, this),
                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("th", {
                                        scope: "col",
                                        children: "Group Name"
                                    }, void 0, false, {
                                        fileName: "src/components/IT20131456/admin/StudentGroup.js",
                                        lineNumber: 93,
                                        columnNumber: 15
                                    }, this),
                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("th", {
                                        scope: "col",
                                        children: "Student ID"
                                    }, void 0, false, {
                                        fileName: "src/components/IT20131456/admin/StudentGroup.js",
                                        lineNumber: 94,
                                        columnNumber: 15
                                    }, this),
                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("th", {
                                        scope: "col",
                                        children: "Student Name"
                                    }, void 0, false, {
                                        fileName: "src/components/IT20131456/admin/StudentGroup.js",
                                        lineNumber: 95,
                                        columnNumber: 15
                                    }, this),
                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("th", {
                                        scope: "col",
                                        children: "Status"
                                    }, void 0, false, {
                                        fileName: "src/components/IT20131456/admin/StudentGroup.js",
                                        lineNumber: 96,
                                        columnNumber: 15
                                    }, this),
                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("th", {
                                        scope: "col",
                                        children: "Action"
                                    }, void 0, false, {
                                        fileName: "src/components/IT20131456/admin/StudentGroup.js",
                                        lineNumber: 97,
                                        columnNumber: 15
                                    }, this)
                                ]
                            }, void 0, true, {
                                fileName: "src/components/IT20131456/admin/StudentGroup.js",
                                lineNumber: 90,
                                columnNumber: 13
                            }, this)
                        }, void 0, false, {
                            fileName: "src/components/IT20131456/admin/StudentGroup.js",
                            lineNumber: 89,
                            columnNumber: 11
                        }, this),
                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("tbody", {
                            children: this.state.studentgroups.map((studentgroups, index)=>/*#__PURE__*/ _jsxDevRuntime.jsxDEV("tr", {
                                    children: [
                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("th", {
                                            children: index + 1
                                        }, void 0, false, {
                                            fileName: "src/components/IT20131456/admin/StudentGroup.js",
                                            lineNumber: 104,
                                            columnNumber: 17
                                        }, this),
                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("td", {
                                            children: studentgroups.groupid
                                        }, void 0, false, {
                                            fileName: "src/components/IT20131456/admin/StudentGroup.js",
                                            lineNumber: 106,
                                            columnNumber: 17
                                        }, this),
                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("td", {
                                            children: studentgroups.groupname
                                        }, void 0, false, {
                                            fileName: "src/components/IT20131456/admin/StudentGroup.js",
                                            lineNumber: 108,
                                            columnNumber: 17
                                        }, this),
                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("td", {
                                            children: [
                                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("tr", {
                                                    children: studentgroups.studentid1
                                                }, void 0, false, {
                                                    fileName: "src/components/IT20131456/admin/StudentGroup.js",
                                                    lineNumber: 111,
                                                    columnNumber: 19
                                                }, this),
                                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("tr", {
                                                    children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("td", {
                                                        children: [
                                                            studentgroups.studentid2,
                                                            " "
                                                        ]
                                                    }, void 0, true, {
                                                        fileName: "src/components/IT20131456/admin/StudentGroup.js",
                                                        lineNumber: 114,
                                                        columnNumber: 21
                                                    }, this)
                                                }, void 0, false, {
                                                    fileName: "src/components/IT20131456/admin/StudentGroup.js",
                                                    lineNumber: 113,
                                                    columnNumber: 19
                                                }, this),
                                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("tr", {
                                                    children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("td", {
                                                        children: [
                                                            studentgroups.studentid3,
                                                            " "
                                                        ]
                                                    }, void 0, true, {
                                                        fileName: "src/components/IT20131456/admin/StudentGroup.js",
                                                        lineNumber: 118,
                                                        columnNumber: 21
                                                    }, this)
                                                }, void 0, false, {
                                                    fileName: "src/components/IT20131456/admin/StudentGroup.js",
                                                    lineNumber: 117,
                                                    columnNumber: 19
                                                }, this),
                                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("tr", {
                                                    children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("td", {
                                                        children: [
                                                            studentgroups.studentid4,
                                                            " "
                                                        ]
                                                    }, void 0, true, {
                                                        fileName: "src/components/IT20131456/admin/StudentGroup.js",
                                                        lineNumber: 122,
                                                        columnNumber: 21
                                                    }, this)
                                                }, void 0, false, {
                                                    fileName: "src/components/IT20131456/admin/StudentGroup.js",
                                                    lineNumber: 121,
                                                    columnNumber: 19
                                                }, this)
                                            ]
                                        }, void 0, true, {
                                            fileName: "src/components/IT20131456/admin/StudentGroup.js",
                                            lineNumber: 110,
                                            columnNumber: 17
                                        }, this),
                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("td", {
                                            children: [
                                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("tr", {
                                                    children: studentgroups.studentname1
                                                }, void 0, false, {
                                                    fileName: "src/components/IT20131456/admin/StudentGroup.js",
                                                    lineNumber: 127,
                                                    columnNumber: 19
                                                }, this),
                                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("tr", {
                                                    children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("td", {
                                                        children: [
                                                            studentgroups.studentname2,
                                                            " "
                                                        ]
                                                    }, void 0, true, {
                                                        fileName: "src/components/IT20131456/admin/StudentGroup.js",
                                                        lineNumber: 130,
                                                        columnNumber: 21
                                                    }, this)
                                                }, void 0, false, {
                                                    fileName: "src/components/IT20131456/admin/StudentGroup.js",
                                                    lineNumber: 129,
                                                    columnNumber: 19
                                                }, this),
                                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("tr", {
                                                    children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("td", {
                                                        children: [
                                                            studentgroups.studentname3,
                                                            " "
                                                        ]
                                                    }, void 0, true, {
                                                        fileName: "src/components/IT20131456/admin/StudentGroup.js",
                                                        lineNumber: 134,
                                                        columnNumber: 21
                                                    }, this)
                                                }, void 0, false, {
                                                    fileName: "src/components/IT20131456/admin/StudentGroup.js",
                                                    lineNumber: 133,
                                                    columnNumber: 19
                                                }, this),
                                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("tr", {
                                                    children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("td", {
                                                        children: [
                                                            studentgroups.studentname4,
                                                            " "
                                                        ]
                                                    }, void 0, true, {
                                                        fileName: "src/components/IT20131456/admin/StudentGroup.js",
                                                        lineNumber: 138,
                                                        columnNumber: 21
                                                    }, this)
                                                }, void 0, false, {
                                                    fileName: "src/components/IT20131456/admin/StudentGroup.js",
                                                    lineNumber: 137,
                                                    columnNumber: 19
                                                }, this)
                                            ]
                                        }, void 0, true, {
                                            fileName: "src/components/IT20131456/admin/StudentGroup.js",
                                            lineNumber: 126,
                                            columnNumber: 17
                                        }, this),
                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("td", {
                                            children: studentgroups.status
                                        }, void 0, false, {
                                            fileName: "src/components/IT20131456/admin/StudentGroup.js",
                                            lineNumber: 142,
                                            columnNumber: 17
                                        }, this),
                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("td", {
                                            children: [
                                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("a", {
                                                    className: "btn btn-outline-primary",
                                                    href: `/student/group/view/admin/${studentgroups._id}`,
                                                    children: [
                                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("i", {
                                                            className: "fa fa-eye"
                                                        }, void 0, false, {
                                                            fileName: "src/components/IT20131456/admin/StudentGroup.js",
                                                            lineNumber: 149,
                                                            columnNumber: 21
                                                        }, this),
                                                        "\xa0View"
                                                    ]
                                                }, void 0, true, {
                                                    fileName: "src/components/IT20131456/admin/StudentGroup.js",
                                                    lineNumber: 145,
                                                    columnNumber: 19
                                                }, this),
                                                "\xa0",
                                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("a", {
                                                    className: "btn btn-outline-success",
                                                    href: `/student/group/update/${studentgroups._id}`,
                                                    children: [
                                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("i", {
                                                            className: "fa fa-edit"
                                                        }, void 0, false, {
                                                            fileName: "src/components/IT20131456/admin/StudentGroup.js",
                                                            lineNumber: 156,
                                                            columnNumber: 21
                                                        }, this),
                                                        "\xa0Edit"
                                                    ]
                                                }, void 0, true, {
                                                    fileName: "src/components/IT20131456/admin/StudentGroup.js",
                                                    lineNumber: 152,
                                                    columnNumber: 19
                                                }, this),
                                                "\xa0",
                                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("a", {
                                                    className: "btn btn-outline-danger",
                                                    href: "#",
                                                    onClick: ()=>this.onDelete(studentgroups._id)
                                                    ,
                                                    children: [
                                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("i", {
                                                            className: "fa fa-trash"
                                                        }, void 0, false, {
                                                            fileName: "src/components/IT20131456/admin/StudentGroup.js",
                                                            lineNumber: 164,
                                                            columnNumber: 21
                                                        }, this),
                                                        "\xa0Delete"
                                                    ]
                                                }, void 0, true, {
                                                    fileName: "src/components/IT20131456/admin/StudentGroup.js",
                                                    lineNumber: 159,
                                                    columnNumber: 19
                                                }, this)
                                            ]
                                        }, void 0, true, {
                                            fileName: "src/components/IT20131456/admin/StudentGroup.js",
                                            lineNumber: 144,
                                            columnNumber: 17
                                        }, this)
                                    ]
                                }, void 0, true, {
                                    fileName: "src/components/IT20131456/admin/StudentGroup.js",
                                    lineNumber: 103,
                                    columnNumber: 15
                                }, this)
                            )
                        }, void 0, false, {
                            fileName: "src/components/IT20131456/admin/StudentGroup.js",
                            lineNumber: 101,
                            columnNumber: 11
                        }, this)
                    ]
                }, void 0, true, {
                    fileName: "src/components/IT20131456/admin/StudentGroup.js",
                    lineNumber: 88,
                    columnNumber: 9
                }, this)
            ]
        }, void 0, true, {
            fileName: "src/components/IT20131456/admin/StudentGroup.js",
            lineNumber: 64,
            columnNumber: 7
        }, this);
    }
}
exports.default = StudentGroup;

  $parcel$ReactRefreshHelpers$c341.postlude(module);
} finally {
  window.$RefreshReg$ = prevRefreshReg;
  window.$RefreshSig$ = prevRefreshSig;
}
},{"react/jsx-dev-runtime":"iTorj","react":"21dqq","axios":"jo6P5","../../IT20125202/admin/AdminNavBar":"kb2lK","@parcel/transformer-js/src/esmodule-helpers.js":"gkKU3","@parcel/transformer-react-refresh-wrap/lib/helpers/helpers.js":"km3Ru"}],"iOZTP":[function(require,module,exports) {
var $parcel$ReactRefreshHelpers$6cc0 = require("@parcel/transformer-react-refresh-wrap/lib/helpers/helpers.js");
var prevRefreshReg = window.$RefreshReg$;
var prevRefreshSig = window.$RefreshSig$;
$parcel$ReactRefreshHelpers$6cc0.prelude(module);

try {
var parcelHelpers = require("@parcel/transformer-js/src/esmodule-helpers.js");
parcelHelpers.defineInteropFlag(exports);
var _jsxDevRuntime = require("react/jsx-dev-runtime");
var _react = require("react");
var _reactDefault = parcelHelpers.interopDefault(_react);
var _axios = require("axios");
var _axiosDefault = parcelHelpers.interopDefault(_axios);
var _adminNavBar = require("../../IT20125202/admin/AdminNavBar");
var _adminNavBarDefault = parcelHelpers.interopDefault(_adminNavBar);
var _groupJpg = require("../../../images/group.jpg");
var _groupJpgDefault = parcelHelpers.interopDefault(_groupJpg);
class ViewStudentGroupAdmin extends _react.Component {
    constructor(props){
        super(props);
        this.state = {
            studentgroup: []
        };
    }
    componentDidMount() {
        const id = this.props.match.params.id;
        _axiosDefault.default.get(`http://localhost:5000/sgroup/${id}`).then((res)=>{
            if (res.data.success) {
                this.setState({
                    studentgroup: res.data.stugroup
                });
                console.log(this.state.studentgroup);
            }
        });
    }
    render() {
        const { groupid , groupname , studentid1 , studentname1 , studentid2 , studentname2 , studentid3 , studentname3 , studentid4 , studentname4 , supervisorname , cosupervisorname , panelmembername , status ,  } = this.state.studentgroup;
        return /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
            className: "container px-5 my-3",
            children: [
                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("br", {}, void 0, false, {
                    fileName: "src/components/IT20131456/admin/ViewStudentGroupAdmin.js",
                    lineNumber: 48,
                    columnNumber: 10
                }, this),
                /*#__PURE__*/ _jsxDevRuntime.jsxDEV(_adminNavBarDefault.default, {}, void 0, false, {
                    fileName: "src/components/IT20131456/admin/ViewStudentGroupAdmin.js",
                    lineNumber: 49,
                    columnNumber: 9
                }, this),
                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("br", {}, void 0, false, {
                    fileName: "src/components/IT20131456/admin/ViewStudentGroupAdmin.js",
                    lineNumber: 50,
                    columnNumber: 9
                }, this),
                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                    className: "float-left my-3",
                    children: [
                        "\xa0",
                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("h2", {
                            children: "View Student Group (Admin)"
                        }, void 0, false, {
                            fileName: "src/components/IT20131456/admin/ViewStudentGroupAdmin.js",
                            lineNumber: 54,
                            columnNumber: 11
                        }, this)
                    ]
                }, void 0, true, {
                    fileName: "src/components/IT20131456/admin/ViewStudentGroupAdmin.js",
                    lineNumber: 52,
                    columnNumber: 9
                }, this),
                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("hr", {}, void 0, false, {
                    fileName: "src/components/IT20131456/admin/ViewStudentGroupAdmin.js",
                    lineNumber: 56,
                    columnNumber: 9
                }, this),
                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                    class: "card mb-3 col-md-12 card border-dark my-3",
                    children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                        class: "row g-0",
                        children: [
                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                class: "col-md-4",
                                children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("img", {
                                    src: _groupJpgDefault.default,
                                    style: {
                                        height: "580px",
                                        width: "420px"
                                    },
                                    className: "img-fluid rounded-start mt-1",
                                    alt: "..."
                                }, void 0, false, {
                                    fileName: "src/components/IT20131456/admin/ViewStudentGroupAdmin.js",
                                    lineNumber: 61,
                                    columnNumber: 15
                                }, this)
                            }, void 0, false, {
                                fileName: "src/components/IT20131456/admin/ViewStudentGroupAdmin.js",
                                lineNumber: 60,
                                columnNumber: 13
                            }, this),
                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                class: "col-md-8",
                                children: [
                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                        className: "card-header text-center",
                                        children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("h3", {
                                            children: [
                                                groupname,
                                                " - ",
                                                groupid
                                            ]
                                        }, void 0, true, {
                                            fileName: "src/components/IT20131456/admin/ViewStudentGroupAdmin.js",
                                            lineNumber: 70,
                                            columnNumber: 17
                                        }, this)
                                    }, void 0, false, {
                                        fileName: "src/components/IT20131456/admin/ViewStudentGroupAdmin.js",
                                        lineNumber: 69,
                                        columnNumber: 15
                                    }, this),
                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                        class: "card-body mx-5 my-5",
                                        children: [
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("h5", {
                                                children: [
                                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("span", {
                                                        style: {
                                                            fontSize: "24px"
                                                        },
                                                        children: "Student_1 : "
                                                    }, void 0, false, {
                                                        fileName: "src/components/IT20131456/admin/ViewStudentGroupAdmin.js",
                                                        lineNumber: 76,
                                                        columnNumber: 19
                                                    }, this),
                                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("span", {
                                                        className: "text-primary",
                                                        style: {
                                                            fontSize: "19px"
                                                        },
                                                        children: [
                                                            studentid1,
                                                            " - ",
                                                            studentname1
                                                        ]
                                                    }, void 0, true, {
                                                        fileName: "src/components/IT20131456/admin/ViewStudentGroupAdmin.js",
                                                        lineNumber: 77,
                                                        columnNumber: 19
                                                    }, this)
                                                ]
                                            }, void 0, true, {
                                                fileName: "src/components/IT20131456/admin/ViewStudentGroupAdmin.js",
                                                lineNumber: 75,
                                                columnNumber: 17
                                            }, this),
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("h5", {
                                                children: [
                                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("span", {
                                                        style: {
                                                            fontSize: "24px"
                                                        },
                                                        children: "Student_2 : "
                                                    }, void 0, false, {
                                                        fileName: "src/components/IT20131456/admin/ViewStudentGroupAdmin.js",
                                                        lineNumber: 83,
                                                        columnNumber: 19
                                                    }, this),
                                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("span", {
                                                        className: "text-primary",
                                                        style: {
                                                            fontSize: "19px"
                                                        },
                                                        children: [
                                                            studentid2,
                                                            " - ",
                                                            studentname2
                                                        ]
                                                    }, void 0, true, {
                                                        fileName: "src/components/IT20131456/admin/ViewStudentGroupAdmin.js",
                                                        lineNumber: 84,
                                                        columnNumber: 19
                                                    }, this)
                                                ]
                                            }, void 0, true, {
                                                fileName: "src/components/IT20131456/admin/ViewStudentGroupAdmin.js",
                                                lineNumber: 82,
                                                columnNumber: 17
                                            }, this),
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("h5", {
                                                children: [
                                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("span", {
                                                        style: {
                                                            fontSize: "24px"
                                                        },
                                                        children: "Student_3 : "
                                                    }, void 0, false, {
                                                        fileName: "src/components/IT20131456/admin/ViewStudentGroupAdmin.js",
                                                        lineNumber: 91,
                                                        columnNumber: 19
                                                    }, this),
                                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("span", {
                                                        className: "text-primary",
                                                        style: {
                                                            fontSize: "19px"
                                                        },
                                                        children: [
                                                            studentid3,
                                                            " - ",
                                                            studentname3
                                                        ]
                                                    }, void 0, true, {
                                                        fileName: "src/components/IT20131456/admin/ViewStudentGroupAdmin.js",
                                                        lineNumber: 92,
                                                        columnNumber: 19
                                                    }, this)
                                                ]
                                            }, void 0, true, {
                                                fileName: "src/components/IT20131456/admin/ViewStudentGroupAdmin.js",
                                                lineNumber: 90,
                                                columnNumber: 17
                                            }, this),
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("h5", {
                                                children: [
                                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("span", {
                                                        style: {
                                                            fontSize: "24px"
                                                        },
                                                        children: "Student_4 : "
                                                    }, void 0, false, {
                                                        fileName: "src/components/IT20131456/admin/ViewStudentGroupAdmin.js",
                                                        lineNumber: 99,
                                                        columnNumber: 19
                                                    }, this),
                                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("span", {
                                                        className: "text-primary",
                                                        style: {
                                                            fontSize: "19px"
                                                        },
                                                        children: [
                                                            studentid4,
                                                            " - ",
                                                            studentname4
                                                        ]
                                                    }, void 0, true, {
                                                        fileName: "src/components/IT20131456/admin/ViewStudentGroupAdmin.js",
                                                        lineNumber: 100,
                                                        columnNumber: 19
                                                    }, this)
                                                ]
                                            }, void 0, true, {
                                                fileName: "src/components/IT20131456/admin/ViewStudentGroupAdmin.js",
                                                lineNumber: 98,
                                                columnNumber: 17
                                            }, this),
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("br", {}, void 0, false, {
                                                fileName: "src/components/IT20131456/admin/ViewStudentGroupAdmin.js",
                                                lineNumber: 105,
                                                columnNumber: 17
                                            }, this),
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("h5", {
                                                children: [
                                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("span", {
                                                        style: {
                                                            fontSize: "24px"
                                                        },
                                                        children: "Supervisor : "
                                                    }, void 0, false, {
                                                        fileName: "src/components/IT20131456/admin/ViewStudentGroupAdmin.js",
                                                        lineNumber: 107,
                                                        columnNumber: 19
                                                    }, this),
                                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("span", {
                                                        className: "text-primary",
                                                        style: {
                                                            fontSize: "19px"
                                                        },
                                                        children: supervisorname
                                                    }, void 0, false, {
                                                        fileName: "src/components/IT20131456/admin/ViewStudentGroupAdmin.js",
                                                        lineNumber: 108,
                                                        columnNumber: 19
                                                    }, this)
                                                ]
                                            }, void 0, true, {
                                                fileName: "src/components/IT20131456/admin/ViewStudentGroupAdmin.js",
                                                lineNumber: 106,
                                                columnNumber: 17
                                            }, this),
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("h5", {
                                                children: [
                                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("span", {
                                                        style: {
                                                            fontSize: "24px"
                                                        },
                                                        children: "Cosupervisor : "
                                                    }, void 0, false, {
                                                        fileName: "src/components/IT20131456/admin/ViewStudentGroupAdmin.js",
                                                        lineNumber: 115,
                                                        columnNumber: 19
                                                    }, this),
                                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("span", {
                                                        className: "text-primary",
                                                        style: {
                                                            fontSize: "19px"
                                                        },
                                                        children: cosupervisorname
                                                    }, void 0, false, {
                                                        fileName: "src/components/IT20131456/admin/ViewStudentGroupAdmin.js",
                                                        lineNumber: 116,
                                                        columnNumber: 19
                                                    }, this)
                                                ]
                                            }, void 0, true, {
                                                fileName: "src/components/IT20131456/admin/ViewStudentGroupAdmin.js",
                                                lineNumber: 114,
                                                columnNumber: 17
                                            }, this),
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("h5", {
                                                children: [
                                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("span", {
                                                        style: {
                                                            fontSize: "24px"
                                                        },
                                                        children: "Panelmember : "
                                                    }, void 0, false, {
                                                        fileName: "src/components/IT20131456/admin/ViewStudentGroupAdmin.js",
                                                        lineNumber: 123,
                                                        columnNumber: 19
                                                    }, this),
                                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("span", {
                                                        className: "text-primary",
                                                        style: {
                                                            fontSize: "19px"
                                                        },
                                                        children: panelmembername
                                                    }, void 0, false, {
                                                        fileName: "src/components/IT20131456/admin/ViewStudentGroupAdmin.js",
                                                        lineNumber: 124,
                                                        columnNumber: 19
                                                    }, this)
                                                ]
                                            }, void 0, true, {
                                                fileName: "src/components/IT20131456/admin/ViewStudentGroupAdmin.js",
                                                lineNumber: 122,
                                                columnNumber: 17
                                            }, this),
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("br", {}, void 0, false, {
                                                fileName: "src/components/IT20131456/admin/ViewStudentGroupAdmin.js",
                                                lineNumber: 129,
                                                columnNumber: 17
                                            }, this),
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("h4", {
                                                children: [
                                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("span", {
                                                        style: {
                                                            fontSize: "24px"
                                                        },
                                                        children: "Status : "
                                                    }, void 0, false, {
                                                        fileName: "src/components/IT20131456/admin/ViewStudentGroupAdmin.js",
                                                        lineNumber: 131,
                                                        columnNumber: 19
                                                    }, this),
                                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("span", {
                                                        className: "text-primary",
                                                        style: {
                                                            fontSize: "19px"
                                                        },
                                                        children: status
                                                    }, void 0, false, {
                                                        fileName: "src/components/IT20131456/admin/ViewStudentGroupAdmin.js",
                                                        lineNumber: 132,
                                                        columnNumber: 19
                                                    }, this)
                                                ]
                                            }, void 0, true, {
                                                fileName: "src/components/IT20131456/admin/ViewStudentGroupAdmin.js",
                                                lineNumber: 130,
                                                columnNumber: 17
                                            }, this)
                                        ]
                                    }, void 0, true, {
                                        fileName: "src/components/IT20131456/admin/ViewStudentGroupAdmin.js",
                                        lineNumber: 74,
                                        columnNumber: 15
                                    }, this),
                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                        className: "col-md-3 px-5",
                                        children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("a", {
                                            className: "btn btn-outline-primary",
                                            type: "submit",
                                            href: `/student/groups/view`,
                                            children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("i", {
                                                className: "fa fa-arrow-circle-left",
                                                children: " Back "
                                            }, void 0, false, {
                                                fileName: "src/components/IT20131456/admin/ViewStudentGroupAdmin.js",
                                                lineNumber: 140,
                                                columnNumber: 19
                                            }, this)
                                        }, void 0, false, {
                                            fileName: "src/components/IT20131456/admin/ViewStudentGroupAdmin.js",
                                            lineNumber: 139,
                                            columnNumber: 17
                                        }, this)
                                    }, void 0, false, {
                                        fileName: "src/components/IT20131456/admin/ViewStudentGroupAdmin.js",
                                        lineNumber: 138,
                                        columnNumber: 15
                                    }, this),
                                    "\xa0"
                                ]
                            }, void 0, true, {
                                fileName: "src/components/IT20131456/admin/ViewStudentGroupAdmin.js",
                                lineNumber: 68,
                                columnNumber: 13
                            }, this)
                        ]
                    }, void 0, true, {
                        fileName: "src/components/IT20131456/admin/ViewStudentGroupAdmin.js",
                        lineNumber: 59,
                        columnNumber: 11
                    }, this)
                }, void 0, false, {
                    fileName: "src/components/IT20131456/admin/ViewStudentGroupAdmin.js",
                    lineNumber: 58,
                    columnNumber: 9
                }, this)
            ]
        }, void 0, true, {
            fileName: "src/components/IT20131456/admin/ViewStudentGroupAdmin.js",
            lineNumber: 47,
            columnNumber: 7
        }, this);
    }
}
exports.default = ViewStudentGroupAdmin;

  $parcel$ReactRefreshHelpers$6cc0.postlude(module);
} finally {
  window.$RefreshReg$ = prevRefreshReg;
  window.$RefreshSig$ = prevRefreshSig;
}
},{"react/jsx-dev-runtime":"iTorj","react":"21dqq","axios":"jo6P5","../../IT20125202/admin/AdminNavBar":"kb2lK","../../../images/group.jpg":"imiRg","@parcel/transformer-js/src/esmodule-helpers.js":"gkKU3","@parcel/transformer-react-refresh-wrap/lib/helpers/helpers.js":"km3Ru"}],"jlglv":[function(require,module,exports) {
var $parcel$ReactRefreshHelpers$6582 = require("@parcel/transformer-react-refresh-wrap/lib/helpers/helpers.js");
var prevRefreshReg = window.$RefreshReg$;
var prevRefreshSig = window.$RefreshSig$;
$parcel$ReactRefreshHelpers$6582.prelude(module);

try {
var parcelHelpers = require("@parcel/transformer-js/src/esmodule-helpers.js");
parcelHelpers.defineInteropFlag(exports);
var _jsxDevRuntime = require("react/jsx-dev-runtime");
var _react = require("react");
var _reactDefault = parcelHelpers.interopDefault(_react);
var _axios = require("axios");
var _axiosDefault = parcelHelpers.interopDefault(_axios);
var _adminNavBar = require("../../IT20125202/admin/AdminNavBar");
var _adminNavBarDefault = parcelHelpers.interopDefault(_adminNavBar);
class EditStudentGroup extends _react.Component {
    constructor(props){
        super(props);
        this.state = {
            groupid: "",
            groupname: "",
            studentid1: "",
            studentname1: "",
            studentid2: "",
            studentname2: "",
            studentid3: "",
            studentname3: "",
            studentid4: "",
            studentname4: "",
            topic: "",
            supervisorname: "",
            cosupervisorname: "",
            panelmembername: "",
            status: ""
        };
    }
    handleInputChange = (e)=>{
        const { name , value  } = e.target;
        this.setState({
            ...this.state,
            [name]: value
        });
    };
    handleChange = (e)=>{
        const { name , value  } = e.target;
        this.setState({
            ...this.state,
            [name]: value
        });
    };
    onSubmit = (e)=>{
        e.preventDefault();
        const id = this.props.match.params.id;
        const { groupid , groupname , studentid1 , studentname1 , studentid2 , studentname2 , studentid3 , studentname3 , studentid4 , studentname4 , topic , supervisorname , cosupervisorname , panelmembername , status ,  } = this.state;
        const data = {
            groupid: groupid,
            groupname: groupname,
            studentid1: studentid1,
            studentname1: studentname1,
            studentid2: studentid2,
            studentname2: studentname2,
            studentid3: studentid3,
            studentname3: studentname3,
            studentid4: studentid4,
            studentname4: studentname4,
            topic: topic,
            supervisorname: supervisorname,
            cosupervisorname: cosupervisorname,
            panelmembername: panelmembername,
            status: status
        };
        console.log(data);
        _axiosDefault.default.put(`http://localhost:5000/sgroup/update/${id}`, data).then((res)=>{
            if (res.data.success) {
                swal("Details Updated Successfully", "", "success");
                this.setState({
                    groupid: "",
                    groupname: "",
                    studentid1: "",
                    studentname1: "",
                    studentid2: "",
                    studentname2: "",
                    studentid3: "",
                    studentname3: "",
                    studentid4: "",
                    studentname4: "",
                    topic: "",
                    supervisorname: "",
                    cosupervisorname: "",
                    panelmembername: "",
                    status: ""
                });
            }
        });
    };
    componentDidMount() {
        const id = this.props.match.params.id;
        _axiosDefault.default.get(`http://localhost:5000/sgroup/${id}`).then((res)=>{
            if (res.data.success) {
                this.setState({
                    groupid: res.data.stugroup.groupid,
                    groupname: res.data.stugroup.groupname,
                    studentid1: res.data.stugroup.studentid1,
                    studentname1: res.data.stugroup.studentname1,
                    studentid2: res.data.stugroup.studentid2,
                    studentname2: res.data.stugroup.studentname2,
                    studentid3: res.data.stugroup.studentid3,
                    studentname3: res.data.stugroup.studentname3,
                    studentid4: res.data.stugroup.studentid4,
                    studentname4: res.data.stugroup.studentname4,
                    topic: res.data.stugroup.topic,
                    supervisorname: res.data.stugroup.supervisorname,
                    cosupervisorname: res.data.stugroup.cosupervisorname,
                    panelmembername: res.data.stugroup.panelmembername,
                    status: res.data.stugroup.status
                });
                console.log(this.state.stugroup);
            }
        });
    }
    render() {
        return /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
            className: "container px-5 my-3",
            children: [
                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("br", {}, void 0, false, {
                    fileName: "src/components/IT20131456/admin/EditStudentGroup.js",
                    lineNumber: 141,
                    columnNumber: 9
                }, this),
                /*#__PURE__*/ _jsxDevRuntime.jsxDEV(_adminNavBarDefault.default, {}, void 0, false, {
                    fileName: "src/components/IT20131456/admin/EditStudentGroup.js",
                    lineNumber: 142,
                    columnNumber: 9
                }, this),
                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("br", {}, void 0, false, {
                    fileName: "src/components/IT20131456/admin/EditStudentGroup.js",
                    lineNumber: 143,
                    columnNumber: 9
                }, this),
                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("nav", {
                    children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                        class: "nav nav-tabs",
                        id: "nav-tab",
                        role: "tablist",
                        children: [
                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("button", {
                                class: "nav-link active",
                                id: "nav-home-tab",
                                "data-bs-toggle": "tab",
                                "data-bs-target": "#nav-home",
                                type: "button",
                                role: "tab",
                                "aria-controls": "nav-home",
                                "aria-selected": "true",
                                children: "Edit Student Group Details"
                            }, void 0, false, {
                                fileName: "src/components/IT20131456/admin/EditStudentGroup.js",
                                lineNumber: 147,
                                columnNumber: 13
                            }, this),
                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("button", {
                                class: "nav-link",
                                id: "nav-profile-tab",
                                "data-bs-toggle": "tab",
                                "data-bs-target": "#nav-profile",
                                type: "button",
                                role: "tab",
                                "aria-controls": "nav-profile",
                                "aria-selected": "false",
                                children: "Edit Evaluator Details"
                            }, void 0, false, {
                                fileName: "src/components/IT20131456/admin/EditStudentGroup.js",
                                lineNumber: 159,
                                columnNumber: 13
                            }, this)
                        ]
                    }, void 0, true, {
                        fileName: "src/components/IT20131456/admin/EditStudentGroup.js",
                        lineNumber: 146,
                        columnNumber: 11
                    }, this)
                }, void 0, false, {
                    fileName: "src/components/IT20131456/admin/EditStudentGroup.js",
                    lineNumber: 145,
                    columnNumber: 9
                }, this),
                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                    class: "tab-content",
                    id: "nav-tabContent",
                    children: [
                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                            class: "tab-pane fade show active",
                            id: "nav-home",
                            role: "tabpanel",
                            "aria-labelledby": "nav-home-tab",
                            children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                className: "container border border-dark bg-light mt-5 ",
                                children: [
                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                        className: "form-group row",
                                        children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                            className: "col-lg-12 margin-tb",
                                            children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                                className: "float-left",
                                                children: [
                                                    "\xa0",
                                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("h2", {
                                                        children: "Edit Student Group Details"
                                                    }, void 0, false, {
                                                        fileName: "src/components/IT20131456/admin/EditStudentGroup.js",
                                                        lineNumber: 185,
                                                        columnNumber: 21
                                                    }, this),
                                                    "\xa0"
                                                ]
                                            }, void 0, true, {
                                                fileName: "src/components/IT20131456/admin/EditStudentGroup.js",
                                                lineNumber: 183,
                                                columnNumber: 17
                                            }, this)
                                        }, void 0, false, {
                                            fileName: "src/components/IT20131456/admin/EditStudentGroup.js",
                                            lineNumber: 182,
                                            columnNumber: 17
                                        }, this)
                                    }, void 0, false, {
                                        fileName: "src/components/IT20131456/admin/EditStudentGroup.js",
                                        lineNumber: 181,
                                        columnNumber: 15
                                    }, this),
                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("form", {
                                        onSubmit: this.onSubmit,
                                        children: [
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                                className: "row ",
                                                children: [
                                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                                        className: "col-md-6",
                                                        children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                                            className: "form-group",
                                                            children: [
                                                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("strong", {
                                                                    children: "Group ID :"
                                                                }, void 0, false, {
                                                                    fileName: "src/components/IT20131456/admin/EditStudentGroup.js",
                                                                    lineNumber: 195,
                                                                    columnNumber: 23
                                                                }, this),
                                                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("input", {
                                                                    type: "text",
                                                                    className: "form-control",
                                                                    placeholder: "Enter Group id -Gxxx",
                                                                    name: "groupid",
                                                                    pattern: "G[0-9]{3}",
                                                                    title: "Group ID is Invalid",
                                                                    value: this.state.groupid,
                                                                    onChange: this.handleInputChange,
                                                                    readOnly: true
                                                                }, void 0, false, {
                                                                    fileName: "src/components/IT20131456/admin/EditStudentGroup.js",
                                                                    lineNumber: 196,
                                                                    columnNumber: 23
                                                                }, this)
                                                            ]
                                                        }, void 0, true, {
                                                            fileName: "src/components/IT20131456/admin/EditStudentGroup.js",
                                                            lineNumber: 194,
                                                            columnNumber: 21
                                                        }, this)
                                                    }, void 0, false, {
                                                        fileName: "src/components/IT20131456/admin/EditStudentGroup.js",
                                                        lineNumber: 193,
                                                        columnNumber: 19
                                                    }, this),
                                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                                        className: "col-md-6",
                                                        children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                                            className: "form-group",
                                                            children: [
                                                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("strong", {
                                                                    children: "Group Name :"
                                                                }, void 0, false, {
                                                                    fileName: "src/components/IT20131456/admin/EditStudentGroup.js",
                                                                    lineNumber: 211,
                                                                    columnNumber: 23
                                                                }, this),
                                                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("input", {
                                                                    type: "text",
                                                                    className: "form-control",
                                                                    placeholder: "Enter Group name",
                                                                    name: "groupname",
                                                                    value: this.state.groupname,
                                                                    onChange: this.handleInputChange,
                                                                    readOnly: true
                                                                }, void 0, false, {
                                                                    fileName: "src/components/IT20131456/admin/EditStudentGroup.js",
                                                                    lineNumber: 212,
                                                                    columnNumber: 23
                                                                }, this)
                                                            ]
                                                        }, void 0, true, {
                                                            fileName: "src/components/IT20131456/admin/EditStudentGroup.js",
                                                            lineNumber: 210,
                                                            columnNumber: 21
                                                        }, this)
                                                    }, void 0, false, {
                                                        fileName: "src/components/IT20131456/admin/EditStudentGroup.js",
                                                        lineNumber: 209,
                                                        columnNumber: 19
                                                    }, this)
                                                ]
                                            }, void 0, true, {
                                                fileName: "src/components/IT20131456/admin/EditStudentGroup.js",
                                                lineNumber: 192,
                                                columnNumber: 17
                                            }, this),
                                            "\xa0",
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                                className: "row",
                                                children: [
                                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                                        className: "col-md-6",
                                                        children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                                            className: "form-group",
                                                            children: [
                                                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("strong", {
                                                                    children: "Student_1 ID (group leader) :"
                                                                }, void 0, false, {
                                                                    fileName: "src/components/IT20131456/admin/EditStudentGroup.js",
                                                                    lineNumber: 228,
                                                                    columnNumber: 23
                                                                }, this),
                                                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("input", {
                                                                    type: "text",
                                                                    className: "form-control",
                                                                    placeholder: "Enter id -ITxxxx",
                                                                    name: "studentid1",
                                                                    pattern: "IT[0-9]{4}",
                                                                    title: "Student ID is Invalid",
                                                                    value: this.state.studentid1,
                                                                    onChange: this.handleInputChange,
                                                                    required: true
                                                                }, void 0, false, {
                                                                    fileName: "src/components/IT20131456/admin/EditStudentGroup.js",
                                                                    lineNumber: 229,
                                                                    columnNumber: 23
                                                                }, this)
                                                            ]
                                                        }, void 0, true, {
                                                            fileName: "src/components/IT20131456/admin/EditStudentGroup.js",
                                                            lineNumber: 227,
                                                            columnNumber: 21
                                                        }, this)
                                                    }, void 0, false, {
                                                        fileName: "src/components/IT20131456/admin/EditStudentGroup.js",
                                                        lineNumber: 226,
                                                        columnNumber: 19
                                                    }, this),
                                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                                        className: "col-md-6",
                                                        children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                                            className: "form-group",
                                                            children: [
                                                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("strong", {
                                                                    children: "Student_1 Name (group leader) :"
                                                                }, void 0, false, {
                                                                    fileName: "src/components/IT20131456/admin/EditStudentGroup.js",
                                                                    lineNumber: 244,
                                                                    columnNumber: 23
                                                                }, this),
                                                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("input", {
                                                                    type: "text",
                                                                    className: "form-control",
                                                                    placeholder: "Enter name",
                                                                    name: "studentname1",
                                                                    value: this.state.studentname1,
                                                                    onChange: this.handleInputChange,
                                                                    required: true
                                                                }, void 0, false, {
                                                                    fileName: "src/components/IT20131456/admin/EditStudentGroup.js",
                                                                    lineNumber: 245,
                                                                    columnNumber: 23
                                                                }, this)
                                                            ]
                                                        }, void 0, true, {
                                                            fileName: "src/components/IT20131456/admin/EditStudentGroup.js",
                                                            lineNumber: 243,
                                                            columnNumber: 21
                                                        }, this)
                                                    }, void 0, false, {
                                                        fileName: "src/components/IT20131456/admin/EditStudentGroup.js",
                                                        lineNumber: 242,
                                                        columnNumber: 19
                                                    }, this)
                                                ]
                                            }, void 0, true, {
                                                fileName: "src/components/IT20131456/admin/EditStudentGroup.js",
                                                lineNumber: 225,
                                                columnNumber: 17
                                            }, this),
                                            "\xa0",
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                                className: "row",
                                                children: [
                                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                                        className: "col-md-6",
                                                        children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                                            className: "form-group",
                                                            children: [
                                                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("strong", {
                                                                    children: "Student_2 ID :"
                                                                }, void 0, false, {
                                                                    fileName: "src/components/IT20131456/admin/EditStudentGroup.js",
                                                                    lineNumber: 261,
                                                                    columnNumber: 23
                                                                }, this),
                                                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("input", {
                                                                    type: "text",
                                                                    className: "form-control",
                                                                    placeholder: "Enter id",
                                                                    name: "studentid2",
                                                                    pattern: "IT[0-9]{4}",
                                                                    title: "Student ID is Invalid",
                                                                    value: this.state.studentid2,
                                                                    onChange: this.handleInputChange,
                                                                    required: true
                                                                }, void 0, false, {
                                                                    fileName: "src/components/IT20131456/admin/EditStudentGroup.js",
                                                                    lineNumber: 262,
                                                                    columnNumber: 23
                                                                }, this)
                                                            ]
                                                        }, void 0, true, {
                                                            fileName: "src/components/IT20131456/admin/EditStudentGroup.js",
                                                            lineNumber: 260,
                                                            columnNumber: 21
                                                        }, this)
                                                    }, void 0, false, {
                                                        fileName: "src/components/IT20131456/admin/EditStudentGroup.js",
                                                        lineNumber: 259,
                                                        columnNumber: 19
                                                    }, this),
                                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                                        className: "col-md-6",
                                                        children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                                            className: "form-group",
                                                            children: [
                                                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("strong", {
                                                                    children: "Student_2 Name :"
                                                                }, void 0, false, {
                                                                    fileName: "src/components/IT20131456/admin/EditStudentGroup.js",
                                                                    lineNumber: 277,
                                                                    columnNumber: 23
                                                                }, this),
                                                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("input", {
                                                                    type: "text",
                                                                    className: "form-control",
                                                                    placeholder: "Enter name",
                                                                    name: "studentname2",
                                                                    value: this.state.studentname2,
                                                                    onChange: this.handleInputChange,
                                                                    required: true
                                                                }, void 0, false, {
                                                                    fileName: "src/components/IT20131456/admin/EditStudentGroup.js",
                                                                    lineNumber: 278,
                                                                    columnNumber: 23
                                                                }, this)
                                                            ]
                                                        }, void 0, true, {
                                                            fileName: "src/components/IT20131456/admin/EditStudentGroup.js",
                                                            lineNumber: 276,
                                                            columnNumber: 21
                                                        }, this)
                                                    }, void 0, false, {
                                                        fileName: "src/components/IT20131456/admin/EditStudentGroup.js",
                                                        lineNumber: 275,
                                                        columnNumber: 19
                                                    }, this)
                                                ]
                                            }, void 0, true, {
                                                fileName: "src/components/IT20131456/admin/EditStudentGroup.js",
                                                lineNumber: 258,
                                                columnNumber: 17
                                            }, this),
                                            "\xa0",
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                                className: "row",
                                                children: [
                                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                                        className: "col-md-6",
                                                        children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                                            className: "form-group",
                                                            children: [
                                                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("strong", {
                                                                    children: "Student_3 ID :"
                                                                }, void 0, false, {
                                                                    fileName: "src/components/IT20131456/admin/EditStudentGroup.js",
                                                                    lineNumber: 294,
                                                                    columnNumber: 23
                                                                }, this),
                                                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("input", {
                                                                    type: "text",
                                                                    className: "form-control",
                                                                    placeholder: "Enter id",
                                                                    name: "studentid3",
                                                                    pattern: "IT[0-9]{4}",
                                                                    title: "Student ID is Invalid",
                                                                    value: this.state.studentid3,
                                                                    onChange: this.handleInputChange,
                                                                    required: true
                                                                }, void 0, false, {
                                                                    fileName: "src/components/IT20131456/admin/EditStudentGroup.js",
                                                                    lineNumber: 295,
                                                                    columnNumber: 23
                                                                }, this)
                                                            ]
                                                        }, void 0, true, {
                                                            fileName: "src/components/IT20131456/admin/EditStudentGroup.js",
                                                            lineNumber: 293,
                                                            columnNumber: 21
                                                        }, this)
                                                    }, void 0, false, {
                                                        fileName: "src/components/IT20131456/admin/EditStudentGroup.js",
                                                        lineNumber: 292,
                                                        columnNumber: 19
                                                    }, this),
                                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                                        className: "col-md-6",
                                                        children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                                            className: "form-group",
                                                            children: [
                                                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("strong", {
                                                                    children: "Student_3 Name :"
                                                                }, void 0, false, {
                                                                    fileName: "src/components/IT20131456/admin/EditStudentGroup.js",
                                                                    lineNumber: 310,
                                                                    columnNumber: 23
                                                                }, this),
                                                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("input", {
                                                                    type: "text",
                                                                    className: "form-control",
                                                                    placeholder: "Enter name",
                                                                    name: "studentname3",
                                                                    value: this.state.studentname3,
                                                                    onChange: this.handleInputChange,
                                                                    required: true
                                                                }, void 0, false, {
                                                                    fileName: "src/components/IT20131456/admin/EditStudentGroup.js",
                                                                    lineNumber: 311,
                                                                    columnNumber: 23
                                                                }, this)
                                                            ]
                                                        }, void 0, true, {
                                                            fileName: "src/components/IT20131456/admin/EditStudentGroup.js",
                                                            lineNumber: 309,
                                                            columnNumber: 21
                                                        }, this)
                                                    }, void 0, false, {
                                                        fileName: "src/components/IT20131456/admin/EditStudentGroup.js",
                                                        lineNumber: 308,
                                                        columnNumber: 19
                                                    }, this)
                                                ]
                                            }, void 0, true, {
                                                fileName: "src/components/IT20131456/admin/EditStudentGroup.js",
                                                lineNumber: 291,
                                                columnNumber: 17
                                            }, this),
                                            "\xa0",
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                                className: "row",
                                                children: [
                                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                                        className: "col-md-6",
                                                        children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                                            className: "form-group",
                                                            children: [
                                                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("strong", {
                                                                    children: "Student_4 ID :"
                                                                }, void 0, false, {
                                                                    fileName: "src/components/IT20131456/admin/EditStudentGroup.js",
                                                                    lineNumber: 327,
                                                                    columnNumber: 23
                                                                }, this),
                                                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("input", {
                                                                    type: "text",
                                                                    className: "form-control",
                                                                    placeholder: "Enter id",
                                                                    name: "studentid4",
                                                                    pattern: "IT[0-9]{4}",
                                                                    title: "Student ID is Invalid",
                                                                    value: this.state.studentid4,
                                                                    onChange: this.handleInputChange,
                                                                    required: true
                                                                }, void 0, false, {
                                                                    fileName: "src/components/IT20131456/admin/EditStudentGroup.js",
                                                                    lineNumber: 328,
                                                                    columnNumber: 23
                                                                }, this)
                                                            ]
                                                        }, void 0, true, {
                                                            fileName: "src/components/IT20131456/admin/EditStudentGroup.js",
                                                            lineNumber: 326,
                                                            columnNumber: 21
                                                        }, this)
                                                    }, void 0, false, {
                                                        fileName: "src/components/IT20131456/admin/EditStudentGroup.js",
                                                        lineNumber: 325,
                                                        columnNumber: 19
                                                    }, this),
                                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                                        className: "col-md-6",
                                                        children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                                            className: "form-group",
                                                            children: [
                                                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("strong", {
                                                                    children: "Student_4 Name :"
                                                                }, void 0, false, {
                                                                    fileName: "src/components/IT20131456/admin/EditStudentGroup.js",
                                                                    lineNumber: 343,
                                                                    columnNumber: 23
                                                                }, this),
                                                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("input", {
                                                                    type: "text",
                                                                    className: "form-control",
                                                                    placeholder: "Enter name",
                                                                    name: "studentname4",
                                                                    value: this.state.studentname4,
                                                                    onChange: this.handleInputChange,
                                                                    required: true
                                                                }, void 0, false, {
                                                                    fileName: "src/components/IT20131456/admin/EditStudentGroup.js",
                                                                    lineNumber: 344,
                                                                    columnNumber: 23
                                                                }, this)
                                                            ]
                                                        }, void 0, true, {
                                                            fileName: "src/components/IT20131456/admin/EditStudentGroup.js",
                                                            lineNumber: 342,
                                                            columnNumber: 21
                                                        }, this)
                                                    }, void 0, false, {
                                                        fileName: "src/components/IT20131456/admin/EditStudentGroup.js",
                                                        lineNumber: 341,
                                                        columnNumber: 19
                                                    }, this)
                                                ]
                                            }, void 0, true, {
                                                fileName: "src/components/IT20131456/admin/EditStudentGroup.js",
                                                lineNumber: 324,
                                                columnNumber: 17
                                            }, this),
                                            "\xa0",
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                                className: "col-md-12",
                                                children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                                    className: "form-group",
                                                    children: [
                                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("a", {
                                                            className: "btn btn-outline-primary",
                                                            type: "submit",
                                                            href: `/student/groups/view`,
                                                            children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("i", {
                                                                className: "fa fa-arrow-circle-left",
                                                                children: " Back "
                                                            }, void 0, false, {
                                                                fileName: "src/components/IT20131456/admin/EditStudentGroup.js",
                                                                lineNumber: 364,
                                                                columnNumber: 23
                                                            }, this)
                                                        }, void 0, false, {
                                                            fileName: "src/components/IT20131456/admin/EditStudentGroup.js",
                                                            lineNumber: 359,
                                                            columnNumber: 21
                                                        }, this),
                                                        "\xa0",
                                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("button", {
                                                            className: "btn btn-outline-success",
                                                            type: "submit",
                                                            children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("i", {
                                                                class: "fa fa-check-circle",
                                                                children: " Update "
                                                            }, void 0, false, {
                                                                fileName: "src/components/IT20131456/admin/EditStudentGroup.js",
                                                                lineNumber: 368,
                                                                columnNumber: 23
                                                            }, this)
                                                        }, void 0, false, {
                                                            fileName: "src/components/IT20131456/admin/EditStudentGroup.js",
                                                            lineNumber: 367,
                                                            columnNumber: 21
                                                        }, this)
                                                    ]
                                                }, void 0, true, {
                                                    fileName: "src/components/IT20131456/admin/EditStudentGroup.js",
                                                    lineNumber: 358,
                                                    columnNumber: 19
                                                }, this)
                                            }, void 0, false, {
                                                fileName: "src/components/IT20131456/admin/EditStudentGroup.js",
                                                lineNumber: 357,
                                                columnNumber: 17
                                            }, this),
                                            "\xa0"
                                        ]
                                    }, void 0, true, {
                                        fileName: "src/components/IT20131456/admin/EditStudentGroup.js",
                                        lineNumber: 191,
                                        columnNumber: 15
                                    }, this)
                                ]
                            }, void 0, true, {
                                fileName: "src/components/IT20131456/admin/EditStudentGroup.js",
                                lineNumber: 180,
                                columnNumber: 13
                            }, this)
                        }, void 0, false, {
                            fileName: "src/components/IT20131456/admin/EditStudentGroup.js",
                            lineNumber: 174,
                            columnNumber: 11
                        }, this),
                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                            class: "tab-pane fade",
                            id: "nav-profile",
                            role: "tabpanel",
                            "aria-labelledby": "nav-profile-tab",
                            children: [
                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                    className: "container border border-dark bg-light mt-5 ",
                                    children: [
                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                            className: "form-group row",
                                            children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                                className: "col-lg-12 margin-tb",
                                                children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                                    className: "float-left",
                                                    children: [
                                                        "\xa0",
                                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("h2", {
                                                            children: " Edit Evaluator Details"
                                                        }, void 0, false, {
                                                            fileName: "src/components/IT20131456/admin/EditStudentGroup.js",
                                                            lineNumber: 387,
                                                            columnNumber: 21
                                                        }, this),
                                                        "\xa0"
                                                    ]
                                                }, void 0, true, {
                                                    fileName: "src/components/IT20131456/admin/EditStudentGroup.js",
                                                    lineNumber: 385,
                                                    columnNumber: 19
                                                }, this)
                                            }, void 0, false, {
                                                fileName: "src/components/IT20131456/admin/EditStudentGroup.js",
                                                lineNumber: 384,
                                                columnNumber: 17
                                            }, this)
                                        }, void 0, false, {
                                            fileName: "src/components/IT20131456/admin/EditStudentGroup.js",
                                            lineNumber: 383,
                                            columnNumber: 15
                                        }, this),
                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("form", {
                                            onSubmit: this.onSubmit,
                                            children: [
                                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                                    className: "row ",
                                                    children: [
                                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                                            className: "col-md-6",
                                                            children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                                                className: "form-group",
                                                                children: [
                                                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("strong", {
                                                                        children: "Supervisor Name :"
                                                                    }, void 0, false, {
                                                                        fileName: "src/components/IT20131456/admin/EditStudentGroup.js",
                                                                        lineNumber: 397,
                                                                        columnNumber: 23
                                                                    }, this),
                                                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("input", {
                                                                        type: "text",
                                                                        className: "form-control",
                                                                        placeholder: "Enter Supervisor name",
                                                                        name: "supervisorname",
                                                                        value: this.state.supervisorname,
                                                                        onChange: this.handleInputChange
                                                                    }, void 0, false, {
                                                                        fileName: "src/components/IT20131456/admin/EditStudentGroup.js",
                                                                        lineNumber: 398,
                                                                        columnNumber: 23
                                                                    }, this)
                                                                ]
                                                            }, void 0, true, {
                                                                fileName: "src/components/IT20131456/admin/EditStudentGroup.js",
                                                                lineNumber: 396,
                                                                columnNumber: 21
                                                            }, this)
                                                        }, void 0, false, {
                                                            fileName: "src/components/IT20131456/admin/EditStudentGroup.js",
                                                            lineNumber: 395,
                                                            columnNumber: 19
                                                        }, this),
                                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                                            className: "col-md-6",
                                                            children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                                                className: "form-group",
                                                                children: [
                                                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("strong", {
                                                                        children: "Co Supervisor Name :"
                                                                    }, void 0, false, {
                                                                        fileName: "src/components/IT20131456/admin/EditStudentGroup.js",
                                                                        lineNumber: 410,
                                                                        columnNumber: 23
                                                                    }, this),
                                                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("input", {
                                                                        type: "text",
                                                                        className: "form-control",
                                                                        placeholder: "Enter Cosupervisor name",
                                                                        name: "cosupervisorname",
                                                                        value: this.state.cosupervisorname,
                                                                        onChange: this.handleInputChange
                                                                    }, void 0, false, {
                                                                        fileName: "src/components/IT20131456/admin/EditStudentGroup.js",
                                                                        lineNumber: 411,
                                                                        columnNumber: 23
                                                                    }, this)
                                                                ]
                                                            }, void 0, true, {
                                                                fileName: "src/components/IT20131456/admin/EditStudentGroup.js",
                                                                lineNumber: 409,
                                                                columnNumber: 21
                                                            }, this)
                                                        }, void 0, false, {
                                                            fileName: "src/components/IT20131456/admin/EditStudentGroup.js",
                                                            lineNumber: 408,
                                                            columnNumber: 19
                                                        }, this)
                                                    ]
                                                }, void 0, true, {
                                                    fileName: "src/components/IT20131456/admin/EditStudentGroup.js",
                                                    lineNumber: 394,
                                                    columnNumber: 17
                                                }, this),
                                                "\xa0",
                                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                                    className: "row",
                                                    children: [
                                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                                            className: "col-md-6",
                                                            children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                                                className: "form-group",
                                                                children: [
                                                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("strong", {
                                                                        children: "Panel Member Name :"
                                                                    }, void 0, false, {
                                                                        fileName: "src/components/IT20131456/admin/EditStudentGroup.js",
                                                                        lineNumber: 426,
                                                                        columnNumber: 23
                                                                    }, this),
                                                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("input", {
                                                                        type: "text",
                                                                        className: "form-control",
                                                                        placeholder: "Enter panel member name",
                                                                        name: "panelmembername",
                                                                        value: this.state.panelmembername,
                                                                        onChange: this.handleInputChange
                                                                    }, void 0, false, {
                                                                        fileName: "src/components/IT20131456/admin/EditStudentGroup.js",
                                                                        lineNumber: 427,
                                                                        columnNumber: 23
                                                                    }, this)
                                                                ]
                                                            }, void 0, true, {
                                                                fileName: "src/components/IT20131456/admin/EditStudentGroup.js",
                                                                lineNumber: 425,
                                                                columnNumber: 21
                                                            }, this)
                                                        }, void 0, false, {
                                                            fileName: "src/components/IT20131456/admin/EditStudentGroup.js",
                                                            lineNumber: 424,
                                                            columnNumber: 19
                                                        }, this),
                                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                                            className: "col-md-6",
                                                            children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                                                className: "form-group",
                                                                children: [
                                                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("strong", {
                                                                        children: "Status:"
                                                                    }, void 0, false, {
                                                                        fileName: "src/components/IT20131456/admin/EditStudentGroup.js",
                                                                        lineNumber: 439,
                                                                        columnNumber: 23
                                                                    }, this),
                                                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("select", {
                                                                        className: "form-control",
                                                                        name: "status",
                                                                        value: this.state.status,
                                                                        onChange: this.handleChange,
                                                                        children: [
                                                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("option", {
                                                                                value: "Not",
                                                                                children: "Not Selected"
                                                                            }, void 0, false, {
                                                                                fileName: "src/components/IT20131456/admin/EditStudentGroup.js",
                                                                                lineNumber: 446,
                                                                                columnNumber: 25
                                                                            }, this),
                                                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("option", {
                                                                                value: "Reviewed",
                                                                                children: "Reviewed "
                                                                            }, void 0, false, {
                                                                                fileName: "src/components/IT20131456/admin/EditStudentGroup.js",
                                                                                lineNumber: 447,
                                                                                columnNumber: 25
                                                                            }, this),
                                                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("option", {
                                                                                value: "Not Reviewed",
                                                                                children: "Not Reviewed "
                                                                            }, void 0, false, {
                                                                                fileName: "src/components/IT20131456/admin/EditStudentGroup.js",
                                                                                lineNumber: 448,
                                                                                columnNumber: 25
                                                                            }, this)
                                                                        ]
                                                                    }, void 0, true, {
                                                                        fileName: "src/components/IT20131456/admin/EditStudentGroup.js",
                                                                        lineNumber: 440,
                                                                        columnNumber: 23
                                                                    }, this)
                                                                ]
                                                            }, void 0, true, {
                                                                fileName: "src/components/IT20131456/admin/EditStudentGroup.js",
                                                                lineNumber: 438,
                                                                columnNumber: 21
                                                            }, this)
                                                        }, void 0, false, {
                                                            fileName: "src/components/IT20131456/admin/EditStudentGroup.js",
                                                            lineNumber: 437,
                                                            columnNumber: 19
                                                        }, this)
                                                    ]
                                                }, void 0, true, {
                                                    fileName: "src/components/IT20131456/admin/EditStudentGroup.js",
                                                    lineNumber: 423,
                                                    columnNumber: 17
                                                }, this),
                                                "\xa0",
                                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                                    className: "col-md-12",
                                                    children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                                        className: "form-group",
                                                        children: [
                                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("a", {
                                                                className: "btn btn-outline-primary",
                                                                type: "submit",
                                                                href: `/student/groups/view`,
                                                                children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("i", {
                                                                    className: "fa fa-arrow-circle-left",
                                                                    children: " Back "
                                                                }, void 0, false, {
                                                                    fileName: "src/components/IT20131456/admin/EditStudentGroup.js",
                                                                    lineNumber: 461,
                                                                    columnNumber: 23
                                                                }, this)
                                                            }, void 0, false, {
                                                                fileName: "src/components/IT20131456/admin/EditStudentGroup.js",
                                                                lineNumber: 456,
                                                                columnNumber: 21
                                                            }, this),
                                                            "\xa0",
                                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("button", {
                                                                className: "btn btn-outline-success",
                                                                type: "submit",
                                                                children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("i", {
                                                                    class: "fa fa-check-circle",
                                                                    children: " Update "
                                                                }, void 0, false, {
                                                                    fileName: "src/components/IT20131456/admin/EditStudentGroup.js",
                                                                    lineNumber: 465,
                                                                    columnNumber: 23
                                                                }, this)
                                                            }, void 0, false, {
                                                                fileName: "src/components/IT20131456/admin/EditStudentGroup.js",
                                                                lineNumber: 464,
                                                                columnNumber: 21
                                                            }, this)
                                                        ]
                                                    }, void 0, true, {
                                                        fileName: "src/components/IT20131456/admin/EditStudentGroup.js",
                                                        lineNumber: 455,
                                                        columnNumber: 19
                                                    }, this)
                                                }, void 0, false, {
                                                    fileName: "src/components/IT20131456/admin/EditStudentGroup.js",
                                                    lineNumber: 454,
                                                    columnNumber: 17
                                                }, this),
                                                "\xa0"
                                            ]
                                        }, void 0, true, {
                                            fileName: "src/components/IT20131456/admin/EditStudentGroup.js",
                                            lineNumber: 393,
                                            columnNumber: 15
                                        }, this)
                                    ]
                                }, void 0, true, {
                                    fileName: "src/components/IT20131456/admin/EditStudentGroup.js",
                                    lineNumber: 382,
                                    columnNumber: 13
                                }, this),
                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("br", {}, void 0, false, {
                                    fileName: "src/components/IT20131456/admin/EditStudentGroup.js",
                                    lineNumber: 472,
                                    columnNumber: 13
                                }, this),
                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("br", {}, void 0, false, {
                                    fileName: "src/components/IT20131456/admin/EditStudentGroup.js",
                                    lineNumber: 473,
                                    columnNumber: 13
                                }, this),
                                " ",
                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("br", {}, void 0, false, {
                                    fileName: "src/components/IT20131456/admin/EditStudentGroup.js",
                                    lineNumber: 473,
                                    columnNumber: 20
                                }, this),
                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("br", {}, void 0, false, {
                                    fileName: "src/components/IT20131456/admin/EditStudentGroup.js",
                                    lineNumber: 474,
                                    columnNumber: 13
                                }, this),
                                " ",
                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("br", {}, void 0, false, {
                                    fileName: "src/components/IT20131456/admin/EditStudentGroup.js",
                                    lineNumber: 474,
                                    columnNumber: 20
                                }, this),
                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("br", {}, void 0, false, {
                                    fileName: "src/components/IT20131456/admin/EditStudentGroup.js",
                                    lineNumber: 475,
                                    columnNumber: 13
                                }, this)
                            ]
                        }, void 0, true, {
                            fileName: "src/components/IT20131456/admin/EditStudentGroup.js",
                            lineNumber: 376,
                            columnNumber: 11
                        }, this)
                    ]
                }, void 0, true, {
                    fileName: "src/components/IT20131456/admin/EditStudentGroup.js",
                    lineNumber: 173,
                    columnNumber: 9
                }, this)
            ]
        }, void 0, true, {
            fileName: "src/components/IT20131456/admin/EditStudentGroup.js",
            lineNumber: 140,
            columnNumber: 7
        }, this);
    }
}
exports.default = EditStudentGroup;

  $parcel$ReactRefreshHelpers$6582.postlude(module);
} finally {
  window.$RefreshReg$ = prevRefreshReg;
  window.$RefreshSig$ = prevRefreshSig;
}
},{"react/jsx-dev-runtime":"iTorj","react":"21dqq","axios":"jo6P5","../../IT20125202/admin/AdminNavBar":"kb2lK","@parcel/transformer-js/src/esmodule-helpers.js":"gkKU3","@parcel/transformer-react-refresh-wrap/lib/helpers/helpers.js":"km3Ru"}],"bue2i":[function(require,module,exports) {
var $parcel$ReactRefreshHelpers$b52c = require("@parcel/transformer-react-refresh-wrap/lib/helpers/helpers.js");
var prevRefreshReg = window.$RefreshReg$;
var prevRefreshSig = window.$RefreshSig$;
$parcel$ReactRefreshHelpers$b52c.prelude(module);

try {
var parcelHelpers = require("@parcel/transformer-js/src/esmodule-helpers.js");
parcelHelpers.defineInteropFlag(exports);
var _jsxDevRuntime = require("react/jsx-dev-runtime");
var _react = require("react");
var _reactDefault = parcelHelpers.interopDefault(_react);
var _axios = require("axios");
var _axiosDefault = parcelHelpers.interopDefault(_axios);
var _adminNavBar = require("../../IT20125202/admin/AdminNavBar");
var _adminNavBarDefault = parcelHelpers.interopDefault(_adminNavBar);
class CreateSupervisorDetails extends _react.Component {
    constructor(props){
        super(props);
        this.state = {
            supervisorid: "",
            supervisorname: "",
            researchfield: "",
            email: ""
        };
    }
    handleInputChange = (e)=>{
        const { name , value  } = e.target;
        this.setState({
            ...this.state,
            [name]: value
        });
    };
    handleChange = (e)=>{
        const { name , value  } = e.target;
        this.setState({
            ...this.state,
            [name]: value
        });
    };
    onSubmit = (e)=>{
        e.preventDefault();
        const { supervisorid , supervisorname , researchfield , email ,  } = this.state;
        const data = {
            supervisorid: supervisorid,
            supervisorname: supervisorname,
            researchfield: researchfield,
            email: email
        };
        console.log(data);
        _axiosDefault.default.post("http://localhost:5000/supervisor/save", data).then((res)=>{
            if (res.data.success) {
                swal("Supervisor Details Added Successfully", "", "success").then((value)=>{
                    window.location = "/supervisor/view";
                });
                this.setState({
                    supervisorid: "",
                    supervisorname: "",
                    researchfield: "",
                    email: ""
                });
            }
        });
    };
    render() {
        return /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
            className: "container px-5 my-3",
            children: [
                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("br", {}, void 0, false, {
                    fileName: "src/components/IT20131456/admin/CreateSupervisorDetails.js",
                    lineNumber: 82,
                    columnNumber: 10
                }, this),
                /*#__PURE__*/ _jsxDevRuntime.jsxDEV(_adminNavBarDefault.default, {}, void 0, false, {
                    fileName: "src/components/IT20131456/admin/CreateSupervisorDetails.js",
                    lineNumber: 83,
                    columnNumber: 9
                }, this),
                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("br", {}, void 0, false, {
                    fileName: "src/components/IT20131456/admin/CreateSupervisorDetails.js",
                    lineNumber: 84,
                    columnNumber: 9
                }, this),
                "\xa0",
                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("h2", {
                    children: "Add Supervisor Details"
                }, void 0, false, {
                    fileName: "src/components/IT20131456/admin/CreateSupervisorDetails.js",
                    lineNumber: 86,
                    columnNumber: 9
                }, this),
                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("hr", {}, void 0, false, {
                    fileName: "src/components/IT20131456/admin/CreateSupervisorDetails.js",
                    lineNumber: 87,
                    columnNumber: 9
                }, this),
                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                    className: "container border border-dark bg-light mt-5 ",
                    children: [
                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                            className: "form-group row",
                            children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                className: "col-lg-12 margin-tb",
                                children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                    className: "float-left",
                                    children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("br", {}, void 0, false, {
                                        fileName: "src/components/IT20131456/admin/CreateSupervisorDetails.js",
                                        lineNumber: 92,
                                        columnNumber: 14
                                    }, this)
                                }, void 0, false, {
                                    fileName: "src/components/IT20131456/admin/CreateSupervisorDetails.js",
                                    lineNumber: 91,
                                    columnNumber: 13
                                }, this)
                            }, void 0, false, {
                                fileName: "src/components/IT20131456/admin/CreateSupervisorDetails.js",
                                lineNumber: 90,
                                columnNumber: 11
                            }, this)
                        }, void 0, false, {
                            fileName: "src/components/IT20131456/admin/CreateSupervisorDetails.js",
                            lineNumber: 89,
                            columnNumber: 9
                        }, this),
                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("form", {
                            onSubmit: this.onSubmit,
                            children: [
                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                    className: "row ",
                                    children: [
                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                            className: "col-md-6",
                                            children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                                className: "form-group",
                                                children: [
                                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("strong", {
                                                        children: "Supervisor ID :"
                                                    }, void 0, false, {
                                                        fileName: "src/components/IT20131456/admin/CreateSupervisorDetails.js",
                                                        lineNumber: 101,
                                                        columnNumber: 19
                                                    }, this),
                                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("input", {
                                                        type: "text",
                                                        className: "form-control",
                                                        placeholder: "Enter supervisor id -Sxx",
                                                        name: "supervisorid",
                                                        pattern: "[S]+[0-9]{2}",
                                                        title: "Supervisor ID is Invalid",
                                                        value: this.state.supervisorid,
                                                        onChange: this.handleInputChange,
                                                        required: true
                                                    }, void 0, false, {
                                                        fileName: "src/components/IT20131456/admin/CreateSupervisorDetails.js",
                                                        lineNumber: 102,
                                                        columnNumber: 19
                                                    }, this)
                                                ]
                                            }, void 0, true, {
                                                fileName: "src/components/IT20131456/admin/CreateSupervisorDetails.js",
                                                lineNumber: 100,
                                                columnNumber: 17
                                            }, this)
                                        }, void 0, false, {
                                            fileName: "src/components/IT20131456/admin/CreateSupervisorDetails.js",
                                            lineNumber: 99,
                                            columnNumber: 15
                                        }, this),
                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                            className: "col-md-6",
                                            children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                                className: "form-group",
                                                children: [
                                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("strong", {
                                                        children: "Supervisor Name :"
                                                    }, void 0, false, {
                                                        fileName: "src/components/IT20131456/admin/CreateSupervisorDetails.js",
                                                        lineNumber: 117,
                                                        columnNumber: 19
                                                    }, this),
                                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("input", {
                                                        type: "text",
                                                        className: "form-control",
                                                        placeholder: "Enter supervisor name",
                                                        name: "supervisorname",
                                                        value: this.state.supervisorname,
                                                        onChange: this.handleInputChange,
                                                        required: true
                                                    }, void 0, false, {
                                                        fileName: "src/components/IT20131456/admin/CreateSupervisorDetails.js",
                                                        lineNumber: 118,
                                                        columnNumber: 19
                                                    }, this)
                                                ]
                                            }, void 0, true, {
                                                fileName: "src/components/IT20131456/admin/CreateSupervisorDetails.js",
                                                lineNumber: 116,
                                                columnNumber: 17
                                            }, this)
                                        }, void 0, false, {
                                            fileName: "src/components/IT20131456/admin/CreateSupervisorDetails.js",
                                            lineNumber: 115,
                                            columnNumber: 15
                                        }, this)
                                    ]
                                }, void 0, true, {
                                    fileName: "src/components/IT20131456/admin/CreateSupervisorDetails.js",
                                    lineNumber: 98,
                                    columnNumber: 13
                                }, this),
                                "\xa0",
                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                    className: "row",
                                    children: [
                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                            className: "col-md-6",
                                            children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                                className: "form-group",
                                                children: [
                                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("strong", {
                                                        children: "Research Field :"
                                                    }, void 0, false, {
                                                        fileName: "src/components/IT20131456/admin/CreateSupervisorDetails.js",
                                                        lineNumber: 134,
                                                        columnNumber: 19
                                                    }, this),
                                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("select", {
                                                        className: "form-control",
                                                        name: "researchfield",
                                                        value: this.state.researchfield,
                                                        onChange: this.handleChange,
                                                        children: [
                                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("option", {
                                                                value: "Not",
                                                                children: "Not Selected"
                                                            }, void 0, false, {
                                                                fileName: "src/components/IT20131456/admin/CreateSupervisorDetails.js",
                                                                lineNumber: 141,
                                                                columnNumber: 25
                                                            }, this),
                                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("option", {
                                                                value: "Artificial Intelligence and Machine Learning",
                                                                children: "Artificial Intelligence and Machine Learning "
                                                            }, void 0, false, {
                                                                fileName: "src/components/IT20131456/admin/CreateSupervisorDetails.js",
                                                                lineNumber: 142,
                                                                columnNumber: 25
                                                            }, this),
                                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("option", {
                                                                value: "Elearning and Education",
                                                                children: "Elearning and Education"
                                                            }, void 0, false, {
                                                                fileName: "src/components/IT20131456/admin/CreateSupervisorDetails.js",
                                                                lineNumber: 143,
                                                                columnNumber: 25
                                                            }, this),
                                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("option", {
                                                                value: "Distributed and Parallel Computing",
                                                                children: "Distributed and Parallel Computing "
                                                            }, void 0, false, {
                                                                fileName: "src/components/IT20131456/admin/CreateSupervisorDetails.js",
                                                                lineNumber: 144,
                                                                columnNumber: 25
                                                            }, this),
                                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("option", {
                                                                value: "Information Security",
                                                                children: "Information Security "
                                                            }, void 0, false, {
                                                                fileName: "src/components/IT20131456/admin/CreateSupervisorDetails.js",
                                                                lineNumber: 145,
                                                                columnNumber: 25
                                                            }, this),
                                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("option", {
                                                                value: "Robotic and Intelligent Systems",
                                                                children: "Robotic and Intelligent Systems "
                                                            }, void 0, false, {
                                                                fileName: "src/components/IT20131456/admin/CreateSupervisorDetails.js",
                                                                lineNumber: 146,
                                                                columnNumber: 25
                                                            }, this)
                                                        ]
                                                    }, void 0, true, {
                                                        fileName: "src/components/IT20131456/admin/CreateSupervisorDetails.js",
                                                        lineNumber: 135,
                                                        columnNumber: 23
                                                    }, this)
                                                ]
                                            }, void 0, true, {
                                                fileName: "src/components/IT20131456/admin/CreateSupervisorDetails.js",
                                                lineNumber: 133,
                                                columnNumber: 17
                                            }, this)
                                        }, void 0, false, {
                                            fileName: "src/components/IT20131456/admin/CreateSupervisorDetails.js",
                                            lineNumber: 132,
                                            columnNumber: 15
                                        }, this),
                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                            className: "col-md-6",
                                            children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                                className: "form-group",
                                                children: [
                                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("strong", {
                                                        children: "Email :"
                                                    }, void 0, false, {
                                                        fileName: "src/components/IT20131456/admin/CreateSupervisorDetails.js",
                                                        lineNumber: 153,
                                                        columnNumber: 19
                                                    }, this),
                                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("input", {
                                                        type: "email",
                                                        className: "form-control",
                                                        placeholder: "Enter email ",
                                                        name: "email",
                                                        pattern: "[a-zA-Z0-9._%+-]+@[a-z0-9]+\\.[a-z]{2,3}",
                                                        value: this.state.email,
                                                        onChange: this.handleInputChange,
                                                        required: true
                                                    }, void 0, false, {
                                                        fileName: "src/components/IT20131456/admin/CreateSupervisorDetails.js",
                                                        lineNumber: 154,
                                                        columnNumber: 19
                                                    }, this)
                                                ]
                                            }, void 0, true, {
                                                fileName: "src/components/IT20131456/admin/CreateSupervisorDetails.js",
                                                lineNumber: 152,
                                                columnNumber: 17
                                            }, this)
                                        }, void 0, false, {
                                            fileName: "src/components/IT20131456/admin/CreateSupervisorDetails.js",
                                            lineNumber: 151,
                                            columnNumber: 15
                                        }, this)
                                    ]
                                }, void 0, true, {
                                    fileName: "src/components/IT20131456/admin/CreateSupervisorDetails.js",
                                    lineNumber: 131,
                                    columnNumber: 13
                                }, this),
                                "\xa0",
                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                    className: "col-md-12",
                                    children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                        className: "form-group",
                                        children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("button", {
                                            className: "btn btn-outline-primary",
                                            type: "submit",
                                            children: [
                                                "\xa0",
                                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("i", {
                                                    className: "fa fa-save",
                                                    children: " Save "
                                                }, void 0, false, {
                                                    fileName: "src/components/IT20131456/admin/CreateSupervisorDetails.js",
                                                    lineNumber: 174,
                                                    columnNumber: 25
                                                }, this)
                                            ]
                                        }, void 0, true, {
                                            fileName: "src/components/IT20131456/admin/CreateSupervisorDetails.js",
                                            lineNumber: 170,
                                            columnNumber: 17
                                        }, this)
                                    }, void 0, false, {
                                        fileName: "src/components/IT20131456/admin/CreateSupervisorDetails.js",
                                        lineNumber: 169,
                                        columnNumber: 15
                                    }, this)
                                }, void 0, false, {
                                    fileName: "src/components/IT20131456/admin/CreateSupervisorDetails.js",
                                    lineNumber: 168,
                                    columnNumber: 13
                                }, this),
                                "\xa0"
                            ]
                        }, void 0, true, {
                            fileName: "src/components/IT20131456/admin/CreateSupervisorDetails.js",
                            lineNumber: 97,
                            columnNumber: 9
                        }, this)
                    ]
                }, void 0, true, {
                    fileName: "src/components/IT20131456/admin/CreateSupervisorDetails.js",
                    lineNumber: 88,
                    columnNumber: 7
                }, this)
            ]
        }, void 0, true, {
            fileName: "src/components/IT20131456/admin/CreateSupervisorDetails.js",
            lineNumber: 81,
            columnNumber: 7
        }, this);
    }
}
exports.default = CreateSupervisorDetails;

  $parcel$ReactRefreshHelpers$b52c.postlude(module);
} finally {
  window.$RefreshReg$ = prevRefreshReg;
  window.$RefreshSig$ = prevRefreshSig;
}
},{"react/jsx-dev-runtime":"iTorj","react":"21dqq","axios":"jo6P5","../../IT20125202/admin/AdminNavBar":"kb2lK","@parcel/transformer-js/src/esmodule-helpers.js":"gkKU3","@parcel/transformer-react-refresh-wrap/lib/helpers/helpers.js":"km3Ru"}],"5yu2d":[function(require,module,exports) {
var $parcel$ReactRefreshHelpers$7e08 = require("@parcel/transformer-react-refresh-wrap/lib/helpers/helpers.js");
var prevRefreshReg = window.$RefreshReg$;
var prevRefreshSig = window.$RefreshSig$;
$parcel$ReactRefreshHelpers$7e08.prelude(module);

try {
var parcelHelpers = require("@parcel/transformer-js/src/esmodule-helpers.js");
parcelHelpers.defineInteropFlag(exports);
var _jsxDevRuntime = require("react/jsx-dev-runtime");
var _react = require("react");
var _reactDefault = parcelHelpers.interopDefault(_react);
var _axios = require("axios");
var _axiosDefault = parcelHelpers.interopDefault(_axios);
var _adminNavBar = require("../../IT20125202/admin/AdminNavBar");
var _adminNavBarDefault = parcelHelpers.interopDefault(_adminNavBar);
class EditSupervisorDetails extends _react.Component {
    constructor(props){
        super(props);
        this.state = {
            supervisorid: "",
            supervisorname: "",
            researchfield: "",
            email: ""
        };
    }
    handleInputChange = (e)=>{
        const { name , value  } = e.target;
        this.setState({
            ...this.state,
            [name]: value
        });
    };
    handleChange = (e)=>{
        const { name , value  } = e.target;
        this.setState({
            ...this.state,
            [name]: value
        });
    };
    onSubmit = (e)=>{
        e.preventDefault();
        const id = this.props.match.params.id;
        const { supervisorid , supervisorname , researchfield , email ,  } = this.state;
        const data = {
            supervisorid: supervisorid,
            supervisorname: supervisorname,
            researchfield: researchfield,
            email: email
        };
        console.log(data);
        _axiosDefault.default.put(`http://localhost:5000/supervisor/update/${id}`, data).then((res)=>{
            if (res.data.success) {
                swal("Details Updated Successfully", "", "success").then((value)=>{
                    window.location = "/supervisor/view";
                });
                this.setState({
                    supervisorid: "",
                    supervisorname: "",
                    researchfield: "",
                    email: ""
                });
            }
        });
    };
    componentDidMount() {
        const id = this.props.match.params.id;
        _axiosDefault.default.get(`http://localhost:5000/supervisor/${id}`).then((res)=>{
            if (res.data.success) {
                this.setState({
                    supervisorid: res.data.sdetail.supervisorid,
                    supervisorname: res.data.sdetail.supervisorname,
                    researchfield: res.data.sdetail.researchfield,
                    email: res.data.sdetail.email
                });
                console.log(this.state.sdetail);
            }
        });
    }
    render() {
        return /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
            className: "container px-5 my-3",
            children: [
                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("br", {}, void 0, false, {
                    fileName: "src/components/IT20131456/admin/EditSupervisorDetails.js",
                    lineNumber: 93,
                    columnNumber: 9
                }, this),
                /*#__PURE__*/ _jsxDevRuntime.jsxDEV(_adminNavBarDefault.default, {}, void 0, false, {
                    fileName: "src/components/IT20131456/admin/EditSupervisorDetails.js",
                    lineNumber: 94,
                    columnNumber: 9
                }, this),
                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("br", {}, void 0, false, {
                    fileName: "src/components/IT20131456/admin/EditSupervisorDetails.js",
                    lineNumber: 95,
                    columnNumber: 9
                }, this),
                "\xa0",
                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("h2", {
                    children: "Edit Supervisor Details"
                }, void 0, false, {
                    fileName: "src/components/IT20131456/admin/EditSupervisorDetails.js",
                    lineNumber: 97,
                    columnNumber: 9
                }, this),
                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("hr", {}, void 0, false, {
                    fileName: "src/components/IT20131456/admin/EditSupervisorDetails.js",
                    lineNumber: 98,
                    columnNumber: 9
                }, this),
                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                    className: "container border border-dark bg-light mt-5 ",
                    children: [
                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                            className: "form-group row",
                            children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                className: "col-lg-12 margin-tb",
                                children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                    className: "float-left",
                                    children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("br", {}, void 0, false, {
                                        fileName: "src/components/IT20131456/admin/EditSupervisorDetails.js",
                                        lineNumber: 103,
                                        columnNumber: 14
                                    }, this)
                                }, void 0, false, {
                                    fileName: "src/components/IT20131456/admin/EditSupervisorDetails.js",
                                    lineNumber: 102,
                                    columnNumber: 13
                                }, this)
                            }, void 0, false, {
                                fileName: "src/components/IT20131456/admin/EditSupervisorDetails.js",
                                lineNumber: 101,
                                columnNumber: 11
                            }, this)
                        }, void 0, false, {
                            fileName: "src/components/IT20131456/admin/EditSupervisorDetails.js",
                            lineNumber: 100,
                            columnNumber: 9
                        }, this),
                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("form", {
                            onSubmit: this.onSubmit,
                            children: [
                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                    className: "row ",
                                    children: [
                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                            className: "col-md-6",
                                            children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                                className: "form-group",
                                                children: [
                                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("strong", {
                                                        children: "Supervisor ID :"
                                                    }, void 0, false, {
                                                        fileName: "src/components/IT20131456/admin/EditSupervisorDetails.js",
                                                        lineNumber: 112,
                                                        columnNumber: 19
                                                    }, this),
                                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("input", {
                                                        type: "text",
                                                        className: "form-control",
                                                        placeholder: "Enter supervisor id -Sxx",
                                                        name: "supervisorid",
                                                        pattern: "[S]+[0-9]{2}",
                                                        title: "Supervisor ID is Invalid",
                                                        value: this.state.supervisorid,
                                                        onChange: this.handleInputChange,
                                                        readOnly: true
                                                    }, void 0, false, {
                                                        fileName: "src/components/IT20131456/admin/EditSupervisorDetails.js",
                                                        lineNumber: 113,
                                                        columnNumber: 19
                                                    }, this)
                                                ]
                                            }, void 0, true, {
                                                fileName: "src/components/IT20131456/admin/EditSupervisorDetails.js",
                                                lineNumber: 111,
                                                columnNumber: 17
                                            }, this)
                                        }, void 0, false, {
                                            fileName: "src/components/IT20131456/admin/EditSupervisorDetails.js",
                                            lineNumber: 110,
                                            columnNumber: 15
                                        }, this),
                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                            className: "col-md-6",
                                            children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                                className: "form-group",
                                                children: [
                                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("strong", {
                                                        children: "Supervisor Name :"
                                                    }, void 0, false, {
                                                        fileName: "src/components/IT20131456/admin/EditSupervisorDetails.js",
                                                        lineNumber: 128,
                                                        columnNumber: 19
                                                    }, this),
                                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("input", {
                                                        type: "text",
                                                        className: "form-control",
                                                        placeholder: "Enter supervisor name",
                                                        name: "supervisorname",
                                                        value: this.state.supervisorname,
                                                        onChange: this.handleInputChange,
                                                        readOnly: true
                                                    }, void 0, false, {
                                                        fileName: "src/components/IT20131456/admin/EditSupervisorDetails.js",
                                                        lineNumber: 129,
                                                        columnNumber: 19
                                                    }, this)
                                                ]
                                            }, void 0, true, {
                                                fileName: "src/components/IT20131456/admin/EditSupervisorDetails.js",
                                                lineNumber: 127,
                                                columnNumber: 17
                                            }, this)
                                        }, void 0, false, {
                                            fileName: "src/components/IT20131456/admin/EditSupervisorDetails.js",
                                            lineNumber: 126,
                                            columnNumber: 15
                                        }, this)
                                    ]
                                }, void 0, true, {
                                    fileName: "src/components/IT20131456/admin/EditSupervisorDetails.js",
                                    lineNumber: 109,
                                    columnNumber: 13
                                }, this),
                                "\xa0",
                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                    className: "row",
                                    children: [
                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                            className: "col-md-6",
                                            children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                                className: "form-group",
                                                children: [
                                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("strong", {
                                                        children: "Research Field :"
                                                    }, void 0, false, {
                                                        fileName: "src/components/IT20131456/admin/EditSupervisorDetails.js",
                                                        lineNumber: 145,
                                                        columnNumber: 19
                                                    }, this),
                                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("select", {
                                                        className: "form-control",
                                                        name: "researchfield",
                                                        value: this.state.researchfield,
                                                        onChange: this.handleChange,
                                                        children: [
                                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("option", {
                                                                value: "Not",
                                                                children: "Not Selected"
                                                            }, void 0, false, {
                                                                fileName: "src/components/IT20131456/admin/EditSupervisorDetails.js",
                                                                lineNumber: 152,
                                                                columnNumber: 25
                                                            }, this),
                                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("option", {
                                                                value: "Artificial Intelligence and Machine Learning",
                                                                children: "Artificial Intelligence and Machine Learning "
                                                            }, void 0, false, {
                                                                fileName: "src/components/IT20131456/admin/EditSupervisorDetails.js",
                                                                lineNumber: 153,
                                                                columnNumber: 25
                                                            }, this),
                                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("option", {
                                                                value: "Elearning and Education",
                                                                children: "Elearning and Education"
                                                            }, void 0, false, {
                                                                fileName: "src/components/IT20131456/admin/EditSupervisorDetails.js",
                                                                lineNumber: 154,
                                                                columnNumber: 25
                                                            }, this),
                                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("option", {
                                                                value: "Distributed and Parallel Computing",
                                                                children: "Distributed and Parallel Computing "
                                                            }, void 0, false, {
                                                                fileName: "src/components/IT20131456/admin/EditSupervisorDetails.js",
                                                                lineNumber: 155,
                                                                columnNumber: 25
                                                            }, this),
                                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("option", {
                                                                value: "Information Security",
                                                                children: "Information Security "
                                                            }, void 0, false, {
                                                                fileName: "src/components/IT20131456/admin/EditSupervisorDetails.js",
                                                                lineNumber: 156,
                                                                columnNumber: 25
                                                            }, this),
                                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("option", {
                                                                value: "Robotic and Intelligent Systems",
                                                                children: "Robotic and Intelligent Systems "
                                                            }, void 0, false, {
                                                                fileName: "src/components/IT20131456/admin/EditSupervisorDetails.js",
                                                                lineNumber: 157,
                                                                columnNumber: 25
                                                            }, this)
                                                        ]
                                                    }, void 0, true, {
                                                        fileName: "src/components/IT20131456/admin/EditSupervisorDetails.js",
                                                        lineNumber: 146,
                                                        columnNumber: 23
                                                    }, this)
                                                ]
                                            }, void 0, true, {
                                                fileName: "src/components/IT20131456/admin/EditSupervisorDetails.js",
                                                lineNumber: 144,
                                                columnNumber: 17
                                            }, this)
                                        }, void 0, false, {
                                            fileName: "src/components/IT20131456/admin/EditSupervisorDetails.js",
                                            lineNumber: 143,
                                            columnNumber: 15
                                        }, this),
                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                            className: "col-md-6",
                                            children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                                className: "form-group",
                                                children: [
                                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("strong", {
                                                        children: "Email :"
                                                    }, void 0, false, {
                                                        fileName: "src/components/IT20131456/admin/EditSupervisorDetails.js",
                                                        lineNumber: 164,
                                                        columnNumber: 19
                                                    }, this),
                                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("input", {
                                                        type: "email",
                                                        className: "form-control",
                                                        placeholder: "Enter email ",
                                                        name: "email",
                                                        pattern: "[a-zA-Z0-9._%+-]+@[a-z0-9]+\\.[a-z]{2,3}",
                                                        value: this.state.email,
                                                        onChange: this.handleInputChange,
                                                        required: true
                                                    }, void 0, false, {
                                                        fileName: "src/components/IT20131456/admin/EditSupervisorDetails.js",
                                                        lineNumber: 165,
                                                        columnNumber: 19
                                                    }, this)
                                                ]
                                            }, void 0, true, {
                                                fileName: "src/components/IT20131456/admin/EditSupervisorDetails.js",
                                                lineNumber: 163,
                                                columnNumber: 17
                                            }, this)
                                        }, void 0, false, {
                                            fileName: "src/components/IT20131456/admin/EditSupervisorDetails.js",
                                            lineNumber: 162,
                                            columnNumber: 15
                                        }, this)
                                    ]
                                }, void 0, true, {
                                    fileName: "src/components/IT20131456/admin/EditSupervisorDetails.js",
                                    lineNumber: 142,
                                    columnNumber: 13
                                }, this),
                                "\xa0",
                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                    className: "col-md-12",
                                    children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                        className: "form-group",
                                        children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("button", {
                                            className: "btn btn-outline-success",
                                            type: "submit",
                                            children: [
                                                "\xa0  ",
                                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("i", {
                                                    class: "fa fa-check-circle",
                                                    children: " Update "
                                                }, void 0, false, {
                                                    fileName: "src/components/IT20131456/admin/EditSupervisorDetails.js",
                                                    lineNumber: 185,
                                                    columnNumber: 26
                                                }, this)
                                            ]
                                        }, void 0, true, {
                                            fileName: "src/components/IT20131456/admin/EditSupervisorDetails.js",
                                            lineNumber: 181,
                                            columnNumber: 17
                                        }, this)
                                    }, void 0, false, {
                                        fileName: "src/components/IT20131456/admin/EditSupervisorDetails.js",
                                        lineNumber: 180,
                                        columnNumber: 15
                                    }, this)
                                }, void 0, false, {
                                    fileName: "src/components/IT20131456/admin/EditSupervisorDetails.js",
                                    lineNumber: 179,
                                    columnNumber: 13
                                }, this),
                                "\xa0"
                            ]
                        }, void 0, true, {
                            fileName: "src/components/IT20131456/admin/EditSupervisorDetails.js",
                            lineNumber: 108,
                            columnNumber: 9
                        }, this)
                    ]
                }, void 0, true, {
                    fileName: "src/components/IT20131456/admin/EditSupervisorDetails.js",
                    lineNumber: 99,
                    columnNumber: 7
                }, this)
            ]
        }, void 0, true, {
            fileName: "src/components/IT20131456/admin/EditSupervisorDetails.js",
            lineNumber: 92,
            columnNumber: 7
        }, this);
    }
}
exports.default = EditSupervisorDetails;

  $parcel$ReactRefreshHelpers$7e08.postlude(module);
} finally {
  window.$RefreshReg$ = prevRefreshReg;
  window.$RefreshSig$ = prevRefreshSig;
}
},{"react/jsx-dev-runtime":"iTorj","react":"21dqq","axios":"jo6P5","../../IT20125202/admin/AdminNavBar":"kb2lK","@parcel/transformer-js/src/esmodule-helpers.js":"gkKU3","@parcel/transformer-react-refresh-wrap/lib/helpers/helpers.js":"km3Ru"}],"hZryf":[function(require,module,exports) {
var $parcel$ReactRefreshHelpers$f893 = require("@parcel/transformer-react-refresh-wrap/lib/helpers/helpers.js");
var prevRefreshReg = window.$RefreshReg$;
var prevRefreshSig = window.$RefreshSig$;
$parcel$ReactRefreshHelpers$f893.prelude(module);

try {
var parcelHelpers = require("@parcel/transformer-js/src/esmodule-helpers.js");
parcelHelpers.defineInteropFlag(exports);
var _jsxDevRuntime = require("react/jsx-dev-runtime");
var _react = require("react");
var _reactDefault = parcelHelpers.interopDefault(_react);
var _axios = require("axios");
var _axiosDefault = parcelHelpers.interopDefault(_axios);
var _adminNavBar = require("../../IT20125202/admin/AdminNavBar");
var _adminNavBarDefault = parcelHelpers.interopDefault(_adminNavBar);
class SupervisorDetails extends _react.Component {
    constructor(props){
        super(props);
        this.state = {
            supervisordetails: []
        };
    }
    componentDidMount() {
        this.retrieveSupervisorDetails();
    }
    retrieveSupervisorDetails() {
        _axiosDefault.default.get("http://localhost:5000/supervisors").then((res)=>{
            if (res.data.success) {
                this.setState({
                    supervisordetails: res.data.existingsupervisordetails
                });
                console.log(this.state.supervisordetails);
            }
        });
    }
    onDelete = (id)=>{
        _axiosDefault.default.delete(`http://localhost:5000/supervisor/delete/${id}`).then((res)=>{
            swal("Are you sure to delete the Supervisor Details?", "", "warning");
            this.retrieveSupervisorDetails();
        });
    };
    filterData(supervisordetails, searchKey) {
        const result = supervisordetails.filter((supervisordetail)=>supervisordetail.supervisorid.toLowerCase().includes(searchKey) || supervisordetail.supervisorname.toLowerCase().includes(searchKey) || supervisordetail.researchfield.toLowerCase().includes(searchKey) || supervisordetail.email.toLowerCase().includes(searchKey) || supervisordetail.supervisorid.toUpperCase().includes(searchKey) || supervisordetail.supervisorname.toUpperCase().includes(searchKey) || supervisordetail.researchfield.toUpperCase().includes(searchKey) || supervisordetail.email.toUpperCase().includes(searchKey) || supervisordetail.supervisorid.includes(searchKey) || supervisordetail.supervisorname.includes(searchKey) || supervisordetail.researchfield.includes(searchKey) || supervisordetail.email.includes(searchKey)
        );
        this.setState({
            supervisordetails: result
        });
    }
    handleSearchArea = (e)=>{
        const searchKey = e.currentTarget.value;
        _axiosDefault.default.get("http://localhost:5000/supervisors").then((res)=>{
            if (res.data.success) this.filterData(res.data.existingsupervisordetails, searchKey);
        });
    };
    render() {
        return /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
            className: "container px-5 my-3",
            children: [
                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("br", {}, void 0, false, {
                    fileName: "src/components/IT20131456/admin/SupervisorDetails.js",
                    lineNumber: 72,
                    columnNumber: 10
                }, this),
                /*#__PURE__*/ _jsxDevRuntime.jsxDEV(_adminNavBarDefault.default, {}, void 0, false, {
                    fileName: "src/components/IT20131456/admin/SupervisorDetails.js",
                    lineNumber: 73,
                    columnNumber: 9
                }, this),
                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("br", {}, void 0, false, {
                    fileName: "src/components/IT20131456/admin/SupervisorDetails.js",
                    lineNumber: 74,
                    columnNumber: 9
                }, this),
                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                    className: "row",
                    children: [
                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                            className: "float-left col-lg-9 mt-2 mb-2",
                            children: [
                                "\xa0",
                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("h2", {
                                    children: "Supervisor Details"
                                }, void 0, false, {
                                    fileName: "src/components/IT20131456/admin/SupervisorDetails.js",
                                    lineNumber: 79,
                                    columnNumber: 13
                                }, this)
                            ]
                        }, void 0, true, {
                            fileName: "src/components/IT20131456/admin/SupervisorDetails.js",
                            lineNumber: 77,
                            columnNumber: 11
                        }, this),
                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                            className: "col-lg-3 mt-2 mb-2",
                            children: [
                                "\xa0",
                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("input", {
                                    className: "form-control border border-dark",
                                    type: "search",
                                    placeholder: "Search",
                                    name: "searchQuery",
                                    onChange: this.handleSearchArea
                                }, void 0, false, {
                                    fileName: "src/components/IT20131456/admin/SupervisorDetails.js",
                                    lineNumber: 84,
                                    columnNumber: 13
                                }, this)
                            ]
                        }, void 0, true, {
                            fileName: "src/components/IT20131456/admin/SupervisorDetails.js",
                            lineNumber: 82,
                            columnNumber: 11
                        }, this),
                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("hr", {}, void 0, false, {
                            fileName: "src/components/IT20131456/admin/SupervisorDetails.js",
                            lineNumber: 92,
                            columnNumber: 11
                        }, this)
                    ]
                }, void 0, true, {
                    fileName: "src/components/IT20131456/admin/SupervisorDetails.js",
                    lineNumber: 76,
                    columnNumber: 9
                }, this),
                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                    children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("a", {
                        className: "btn btn-outline-success",
                        href: `/supervisor/add`,
                        children: [
                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("i", {
                                className: "fa fa-edit"
                            }, void 0, false, {
                                fileName: "src/components/IT20131456/admin/SupervisorDetails.js",
                                lineNumber: 97,
                                columnNumber: 15
                            }, this),
                            "\xa0Add New Supervisor"
                        ]
                    }, void 0, true, {
                        fileName: "src/components/IT20131456/admin/SupervisorDetails.js",
                        lineNumber: 96,
                        columnNumber: 11
                    }, this)
                }, void 0, false, {
                    fileName: "src/components/IT20131456/admin/SupervisorDetails.js",
                    lineNumber: 95,
                    columnNumber: 9
                }, this),
                "\xa0",
                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("table", {
                    className: "table table-striped table-bordered",
                    children: [
                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("thead", {
                            className: " text-light",
                            style: {
                                background: "#000080"
                            },
                            children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("tr", {
                                children: [
                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("th", {
                                        scope: "col",
                                        children: "#"
                                    }, void 0, false, {
                                        fileName: "src/components/IT20131456/admin/SupervisorDetails.js",
                                        lineNumber: 105,
                                        columnNumber: 15
                                    }, this),
                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("th", {
                                        scope: "col",
                                        children: "Supervisor ID"
                                    }, void 0, false, {
                                        fileName: "src/components/IT20131456/admin/SupervisorDetails.js",
                                        lineNumber: 106,
                                        columnNumber: 15
                                    }, this),
                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("th", {
                                        scope: "col",
                                        children: "Supervisor Name"
                                    }, void 0, false, {
                                        fileName: "src/components/IT20131456/admin/SupervisorDetails.js",
                                        lineNumber: 107,
                                        columnNumber: 15
                                    }, this),
                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("th", {
                                        scope: "col",
                                        children: "Research Field"
                                    }, void 0, false, {
                                        fileName: "src/components/IT20131456/admin/SupervisorDetails.js",
                                        lineNumber: 108,
                                        columnNumber: 15
                                    }, this),
                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("th", {
                                        scope: "col",
                                        children: "Email"
                                    }, void 0, false, {
                                        fileName: "src/components/IT20131456/admin/SupervisorDetails.js",
                                        lineNumber: 109,
                                        columnNumber: 15
                                    }, this),
                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("th", {
                                        scope: "col",
                                        children: "Action"
                                    }, void 0, false, {
                                        fileName: "src/components/IT20131456/admin/SupervisorDetails.js",
                                        lineNumber: 110,
                                        columnNumber: 15
                                    }, this)
                                ]
                            }, void 0, true, {
                                fileName: "src/components/IT20131456/admin/SupervisorDetails.js",
                                lineNumber: 104,
                                columnNumber: 13
                            }, this)
                        }, void 0, false, {
                            fileName: "src/components/IT20131456/admin/SupervisorDetails.js",
                            lineNumber: 103,
                            columnNumber: 11
                        }, this),
                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("tbody", {
                            children: this.state.supervisordetails.map((supervisordetails, index)=>/*#__PURE__*/ _jsxDevRuntime.jsxDEV("tr", {
                                    children: [
                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("th", {
                                            children: index + 1
                                        }, void 0, false, {
                                            fileName: "src/components/IT20131456/admin/SupervisorDetails.js",
                                            lineNumber: 117,
                                            columnNumber: 17
                                        }, this),
                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("td", {
                                            children: supervisordetails.supervisorid
                                        }, void 0, false, {
                                            fileName: "src/components/IT20131456/admin/SupervisorDetails.js",
                                            lineNumber: 118,
                                            columnNumber: 17
                                        }, this),
                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("td", {
                                            children: supervisordetails.supervisorname
                                        }, void 0, false, {
                                            fileName: "src/components/IT20131456/admin/SupervisorDetails.js",
                                            lineNumber: 119,
                                            columnNumber: 17
                                        }, this),
                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("td", {
                                            children: supervisordetails.researchfield
                                        }, void 0, false, {
                                            fileName: "src/components/IT20131456/admin/SupervisorDetails.js",
                                            lineNumber: 120,
                                            columnNumber: 17
                                        }, this),
                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("td", {
                                            children: supervisordetails.email
                                        }, void 0, false, {
                                            fileName: "src/components/IT20131456/admin/SupervisorDetails.js",
                                            lineNumber: 121,
                                            columnNumber: 17
                                        }, this),
                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("td", {
                                            children: [
                                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("a", {
                                                    className: "btn btn-outline-success",
                                                    href: `/supervisor/update/${supervisordetails._id}`,
                                                    children: [
                                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("i", {
                                                            className: "fa fa-edit"
                                                        }, void 0, false, {
                                                            fileName: "src/components/IT20131456/admin/SupervisorDetails.js",
                                                            lineNumber: 130,
                                                            columnNumber: 21
                                                        }, this),
                                                        "\xa0Edit"
                                                    ]
                                                }, void 0, true, {
                                                    fileName: "src/components/IT20131456/admin/SupervisorDetails.js",
                                                    lineNumber: 126,
                                                    columnNumber: 19
                                                }, this),
                                                "\xa0",
                                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("a", {
                                                    className: "btn btn-outline-danger",
                                                    href: "#",
                                                    onClick: ()=>this.onDelete(supervisordetails._id)
                                                    ,
                                                    children: [
                                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("i", {
                                                            className: "fa fa-trash"
                                                        }, void 0, false, {
                                                            fileName: "src/components/IT20131456/admin/SupervisorDetails.js",
                                                            lineNumber: 138,
                                                            columnNumber: 21
                                                        }, this),
                                                        "\xa0Delete"
                                                    ]
                                                }, void 0, true, {
                                                    fileName: "src/components/IT20131456/admin/SupervisorDetails.js",
                                                    lineNumber: 133,
                                                    columnNumber: 19
                                                }, this)
                                            ]
                                        }, void 0, true, {
                                            fileName: "src/components/IT20131456/admin/SupervisorDetails.js",
                                            lineNumber: 125,
                                            columnNumber: 17
                                        }, this)
                                    ]
                                }, void 0, true, {
                                    fileName: "src/components/IT20131456/admin/SupervisorDetails.js",
                                    lineNumber: 116,
                                    columnNumber: 15
                                }, this)
                            )
                        }, void 0, false, {
                            fileName: "src/components/IT20131456/admin/SupervisorDetails.js",
                            lineNumber: 114,
                            columnNumber: 11
                        }, this)
                    ]
                }, void 0, true, {
                    fileName: "src/components/IT20131456/admin/SupervisorDetails.js",
                    lineNumber: 102,
                    columnNumber: 9
                }, this)
            ]
        }, void 0, true, {
            fileName: "src/components/IT20131456/admin/SupervisorDetails.js",
            lineNumber: 71,
            columnNumber: 7
        }, this);
    }
}
exports.default = SupervisorDetails;

  $parcel$ReactRefreshHelpers$f893.postlude(module);
} finally {
  window.$RefreshReg$ = prevRefreshReg;
  window.$RefreshSig$ = prevRefreshSig;
}
},{"react/jsx-dev-runtime":"iTorj","react":"21dqq","axios":"jo6P5","../../IT20125202/admin/AdminNavBar":"kb2lK","@parcel/transformer-js/src/esmodule-helpers.js":"gkKU3","@parcel/transformer-react-refresh-wrap/lib/helpers/helpers.js":"km3Ru"}],"fJiva":[function(require,module,exports) {
var $parcel$ReactRefreshHelpers$5436 = require("@parcel/transformer-react-refresh-wrap/lib/helpers/helpers.js");
var prevRefreshReg = window.$RefreshReg$;
var prevRefreshSig = window.$RefreshSig$;
$parcel$ReactRefreshHelpers$5436.prelude(module);

try {
var parcelHelpers = require("@parcel/transformer-js/src/esmodule-helpers.js");
parcelHelpers.defineInteropFlag(exports);
var _jsxDevRuntime = require("react/jsx-dev-runtime");
//panel members view the topic list  
var _react = require("react");
var _reactDefault = parcelHelpers.interopDefault(_react);
var _axios = require("axios");
var _axiosDefault = parcelHelpers.interopDefault(_axios);
var _sweetalert = require("sweetalert");
var _sweetalertDefault = parcelHelpers.interopDefault(_sweetalert);
class viewTopicList extends _react.Component {
    constructor(props){
        super(props);
        this.state = {
            topics: []
        };
    }
    componentDidMount() {
        document.title = "Research Topic Requests";
        // redirect to the login page if the user is not logged in
        if (!localStorage.userToken) _sweetalertDefault.default("Please login first", "", "warning").then((value)=>{
            if (value) {
                this.props.history.push(`/user/login`);
                window.location.reload();
            }
        });
        this.retrieveTopics();
    }
    //method for get request
    retrieveTopics() {
        _axiosDefault.default.get('http://localhost:5000/topics').then((res)=>{
            if (res.data.success) this.setState({
                topics: res.data.existingTopics
            });
        });
    }
    handleSearchArea = (e)=>{
        // console.log(e.currentTarget.value)
        const searchKey = e.currentTarget.value;
        _axiosDefault.default.get('/topics').then((res)=>{
            if (res.data.success) this.filterData(res.data.existingTopics, searchKey);
        });
    };
    filterData(topics, searchKey) {
        const searchResult = topics.filter((topic)=>topic.groupId.toLowerCase().includes(searchKey) || topic.topicR.toLowerCase().includes(searchKey) || topic.status.toLowerCase().includes(searchKey) || topic.groupId.toUpperCase().includes(searchKey) || topic.topicR.toUpperCase().includes(searchKey) || topic.status.toUpperCase().includes(searchKey) || topic.groupId.includes(searchKey) || topic.topicR.includes(searchKey) || topic.status.includes(searchKey)
        );
        this.setState({
            topics: searchResult
        });
    }
    render() {
        return /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
            className: "container",
            style: {
                padding: '50px 50px 50px 50px',
                background: 'white',
                minHeight: '100vh'
            },
            children: [
                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                    className: "row",
                    children: [
                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                            className: "col-lg-9 mt-2 mb-2",
                            children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("h1", {
                                children: "Topics"
                            }, void 0, false, {
                                fileName: "src/components/IT20125202/topic/ViewTopicList.js",
                                lineNumber: 87,
                                columnNumber: 13
                            }, this)
                        }, void 0, false, {
                            fileName: "src/components/IT20125202/topic/ViewTopicList.js",
                            lineNumber: 86,
                            columnNumber: 11
                        }, this),
                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                            className: "col-lg-3 mt-2 mb-2",
                            children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("input", {
                                className: "form-control",
                                type: "search",
                                placeholder: "Search",
                                name: "searchQuery",
                                onChange: this.handleSearchArea
                            }, void 0, false, {
                                fileName: "src/components/IT20125202/topic/ViewTopicList.js",
                                lineNumber: 90,
                                columnNumber: 13
                            }, this)
                        }, void 0, false, {
                            fileName: "src/components/IT20125202/topic/ViewTopicList.js",
                            lineNumber: 89,
                            columnNumber: 11
                        }, this),
                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("hr", {}, void 0, false, {
                            fileName: "src/components/IT20125202/topic/ViewTopicList.js",
                            lineNumber: 98,
                            columnNumber: 11
                        }, this),
                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("br", {}, void 0, false, {
                            fileName: "src/components/IT20125202/topic/ViewTopicList.js",
                            lineNumber: 98,
                            columnNumber: 17
                        }, this)
                    ]
                }, void 0, true, {
                    fileName: "src/components/IT20125202/topic/ViewTopicList.js",
                    lineNumber: 85,
                    columnNumber: 9
                }, this),
                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("table", {
                    className: "table",
                    children: [
                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("thead", {
                            children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("tr", {
                                children: [
                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("th", {
                                        scope: "col",
                                        children: " # "
                                    }, void 0, false, {
                                        fileName: "src/components/IT20125202/topic/ViewTopicList.js",
                                        lineNumber: 105,
                                        columnNumber: 15
                                    }, this),
                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("th", {
                                        scope: "col",
                                        children: " Group ID "
                                    }, void 0, false, {
                                        fileName: "src/components/IT20125202/topic/ViewTopicList.js",
                                        lineNumber: 106,
                                        columnNumber: 15
                                    }, this),
                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("th", {
                                        scope: "col",
                                        children: " Topic "
                                    }, void 0, false, {
                                        fileName: "src/components/IT20125202/topic/ViewTopicList.js",
                                        lineNumber: 107,
                                        columnNumber: 15
                                    }, this),
                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("th", {
                                        scope: "col",
                                        children: " Description "
                                    }, void 0, false, {
                                        fileName: "src/components/IT20125202/topic/ViewTopicList.js",
                                        lineNumber: 108,
                                        columnNumber: 15
                                    }, this),
                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("th", {
                                        scope: "col",
                                        children: " Status "
                                    }, void 0, false, {
                                        fileName: "src/components/IT20125202/topic/ViewTopicList.js",
                                        lineNumber: 109,
                                        columnNumber: 15
                                    }, this),
                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("th", {
                                        scope: "col",
                                        children: " Comments "
                                    }, void 0, false, {
                                        fileName: "src/components/IT20125202/topic/ViewTopicList.js",
                                        lineNumber: 110,
                                        columnNumber: 15
                                    }, this),
                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("th", {
                                        scope: "col"
                                    }, void 0, false, {
                                        fileName: "src/components/IT20125202/topic/ViewTopicList.js",
                                        lineNumber: 111,
                                        columnNumber: 15
                                    }, this)
                                ]
                            }, void 0, true, {
                                fileName: "src/components/IT20125202/topic/ViewTopicList.js",
                                lineNumber: 104,
                                columnNumber: 13
                            }, this)
                        }, void 0, false, {
                            fileName: "src/components/IT20125202/topic/ViewTopicList.js",
                            lineNumber: 103,
                            columnNumber: 11
                        }, this),
                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("tbody", {
                            children: this.state.topics.map((topics, index)=>/*#__PURE__*/ _jsxDevRuntime.jsxDEV("tr", {
                                    children: [
                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("th", {
                                            scope: "row",
                                            children: index + 1
                                        }, void 0, false, {
                                            fileName: "src/components/IT20125202/topic/ViewTopicList.js",
                                            lineNumber: 118,
                                            columnNumber: 17
                                        }, this),
                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("td", {
                                            children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("a", {
                                                href: `/panel/topic/details/${topics._id}`,
                                                style: {
                                                    textDecoration: 'none'
                                                },
                                                children: topics.groupId
                                            }, void 0, false, {
                                                fileName: "src/components/IT20125202/topic/ViewTopicList.js",
                                                lineNumber: 120,
                                                columnNumber: 19
                                            }, this)
                                        }, void 0, false, {
                                            fileName: "src/components/IT20125202/topic/ViewTopicList.js",
                                            lineNumber: 119,
                                            columnNumber: 17
                                        }, this),
                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("td", {
                                            children: topics.topicR
                                        }, void 0, false, {
                                            fileName: "src/components/IT20125202/topic/ViewTopicList.js",
                                            lineNumber: 124,
                                            columnNumber: 17
                                        }, this),
                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("td", {
                                            children: topics.description
                                        }, void 0, false, {
                                            fileName: "src/components/IT20125202/topic/ViewTopicList.js",
                                            lineNumber: 125,
                                            columnNumber: 17
                                        }, this),
                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("td", {
                                            children: topics.status
                                        }, void 0, false, {
                                            fileName: "src/components/IT20125202/topic/ViewTopicList.js",
                                            lineNumber: 126,
                                            columnNumber: 17
                                        }, this),
                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("td", {
                                            children: topics.comments
                                        }, void 0, false, {
                                            fileName: "src/components/IT20125202/topic/ViewTopicList.js",
                                            lineNumber: 127,
                                            columnNumber: 17
                                        }, this),
                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("td", {
                                            children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("a", {
                                                className: "btn btn-outline-success",
                                                href: `/panel/topic/update/${topics._id}`,
                                                children: [
                                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("i", {
                                                        className: "fas fa-edit"
                                                    }, void 0, false, {
                                                        fileName: "src/components/IT20125202/topic/ViewTopicList.js",
                                                        lineNumber: 130,
                                                        columnNumber: 21
                                                    }, this),
                                                    " \xa0Update"
                                                ]
                                            }, void 0, true, {
                                                fileName: "src/components/IT20125202/topic/ViewTopicList.js",
                                                lineNumber: 129,
                                                columnNumber: 19
                                            }, this)
                                        }, void 0, false, {
                                            fileName: "src/components/IT20125202/topic/ViewTopicList.js",
                                            lineNumber: 128,
                                            columnNumber: 17
                                        }, this)
                                    ]
                                }, index, true, {
                                    fileName: "src/components/IT20125202/topic/ViewTopicList.js",
                                    lineNumber: 117,
                                    columnNumber: 15
                                }, this)
                            )
                        }, void 0, false, {
                            fileName: "src/components/IT20125202/topic/ViewTopicList.js",
                            lineNumber: 115,
                            columnNumber: 11
                        }, this)
                    ]
                }, void 0, true, {
                    fileName: "src/components/IT20125202/topic/ViewTopicList.js",
                    lineNumber: 102,
                    columnNumber: 9
                }, this)
            ]
        }, void 0, true, {
            fileName: "src/components/IT20125202/topic/ViewTopicList.js",
            lineNumber: 84,
            columnNumber: 7
        }, this);
    }
}
exports.default = viewTopicList;

  $parcel$ReactRefreshHelpers$5436.postlude(module);
} finally {
  window.$RefreshReg$ = prevRefreshReg;
  window.$RefreshSig$ = prevRefreshSig;
}
},{"react/jsx-dev-runtime":"iTorj","react":"21dqq","axios":"jo6P5","sweetalert":"38a65","@parcel/transformer-js/src/esmodule-helpers.js":"gkKU3","@parcel/transformer-react-refresh-wrap/lib/helpers/helpers.js":"km3Ru"}],"92cwI":[function(require,module,exports) {
var $parcel$ReactRefreshHelpers$99c1 = require("@parcel/transformer-react-refresh-wrap/lib/helpers/helpers.js");
var prevRefreshReg = window.$RefreshReg$;
var prevRefreshSig = window.$RefreshSig$;
$parcel$ReactRefreshHelpers$99c1.prelude(module);

try {
var parcelHelpers = require("@parcel/transformer-js/src/esmodule-helpers.js");
parcelHelpers.defineInteropFlag(exports);
var _jsxDevRuntime = require("react/jsx-dev-runtime");
var _react = require("react");
var _reactDefault = parcelHelpers.interopDefault(_react);
var _axios = require("axios");
var _axiosDefault = parcelHelpers.interopDefault(_axios);
var _sweetalert = require("sweetalert");
var _sweetalertDefault = parcelHelpers.interopDefault(_sweetalert);
class ViewTopic extends _react.Component {
    constructor(props){
        super(props);
        this.state = {
            topic: {}
        };
    }
    componentDidMount() {
        document.title = "Topic Registration Details";
        // redirect to the login page if the user is not logged in
        if (!localStorage.userToken) _sweetalertDefault.default("Please login first", "", "warning").then((value)=>{
            if (value) {
                this.props.history.push(`/user/login`);
                window.location.reload();
            }
        });
        const id = this.props.match.params.id;
        _axiosDefault.default.get(`http://localhost:5000/topic/${id}`).then((res)=>{
            if (res.data.success) this.setState({
                topic: res.data.topic
            });
        });
    }
    render() {
        const { groupId , topicR , description , status , comments  } = this.state.topic;
        return /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
            className: "container",
            style: {
                padding: '50px 50px 50px 50px',
                background: 'white',
                minHeight: '100vh'
            },
            children: [
                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                    className: "col-lg-9 mt-2 mb-2",
                    children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("h1", {
                        children: "Topic Request Details"
                    }, void 0, false, {
                        fileName: "src/components/IT20125202/topic/ViewTopic.js",
                        lineNumber: 47,
                        columnNumber: 11
                    }, this)
                }, void 0, false, {
                    fileName: "src/components/IT20125202/topic/ViewTopic.js",
                    lineNumber: 46,
                    columnNumber: 9
                }, this),
                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("hr", {}, void 0, false, {
                    fileName: "src/components/IT20125202/topic/ViewTopic.js",
                    lineNumber: 49,
                    columnNumber: 9
                }, this),
                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("br", {}, void 0, false, {
                    fileName: "src/components/IT20125202/topic/ViewTopic.js",
                    lineNumber: 49,
                    columnNumber: 15
                }, this),
                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                    children: [
                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("dl", {
                            className: "row",
                            children: [
                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("dt", {
                                    className: "col-sm-3",
                                    children: "Group ID"
                                }, void 0, false, {
                                    fileName: "src/components/IT20125202/topic/ViewTopic.js",
                                    lineNumber: 53,
                                    columnNumber: 13
                                }, this),
                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("dd", {
                                    className: "col-sm-9",
                                    children: groupId
                                }, void 0, false, {
                                    fileName: "src/components/IT20125202/topic/ViewTopic.js",
                                    lineNumber: 54,
                                    columnNumber: 13
                                }, this),
                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("dt", {
                                    className: "col-sm-3",
                                    children: "Topic"
                                }, void 0, false, {
                                    fileName: "src/components/IT20125202/topic/ViewTopic.js",
                                    lineNumber: 56,
                                    columnNumber: 13
                                }, this),
                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("dd", {
                                    className: "col-sm-9",
                                    children: topicR
                                }, void 0, false, {
                                    fileName: "src/components/IT20125202/topic/ViewTopic.js",
                                    lineNumber: 57,
                                    columnNumber: 13
                                }, this),
                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("dt", {
                                    className: "col-sm-3",
                                    children: "Description"
                                }, void 0, false, {
                                    fileName: "src/components/IT20125202/topic/ViewTopic.js",
                                    lineNumber: 59,
                                    columnNumber: 13
                                }, this),
                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("dd", {
                                    className: "col-sm-9",
                                    children: description
                                }, void 0, false, {
                                    fileName: "src/components/IT20125202/topic/ViewTopic.js",
                                    lineNumber: 60,
                                    columnNumber: 13
                                }, this),
                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("dt", {
                                    className: "col-sm-3",
                                    children: "Status"
                                }, void 0, false, {
                                    fileName: "src/components/IT20125202/topic/ViewTopic.js",
                                    lineNumber: 62,
                                    columnNumber: 13
                                }, this),
                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("dd", {
                                    className: "col-sm-9",
                                    children: status
                                }, void 0, false, {
                                    fileName: "src/components/IT20125202/topic/ViewTopic.js",
                                    lineNumber: 63,
                                    columnNumber: 13
                                }, this),
                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("dt", {
                                    className: "col-sm-3",
                                    children: "Comments"
                                }, void 0, false, {
                                    fileName: "src/components/IT20125202/topic/ViewTopic.js",
                                    lineNumber: 65,
                                    columnNumber: 13
                                }, this),
                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("dd", {
                                    className: "col-sm-9",
                                    children: comments
                                }, void 0, false, {
                                    fileName: "src/components/IT20125202/topic/ViewTopic.js",
                                    lineNumber: 66,
                                    columnNumber: 13
                                }, this)
                            ]
                        }, void 0, true, {
                            fileName: "src/components/IT20125202/topic/ViewTopic.js",
                            lineNumber: 52,
                            columnNumber: 11
                        }, this),
                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("a", {
                            href: "/panel/topic/list",
                            class: "btn btn-outline-dark",
                            tabindex: "-1",
                            role: "button",
                            "aria-disabled": "true",
                            children: "Back"
                        }, void 0, false, {
                            fileName: "src/components/IT20125202/topic/ViewTopic.js",
                            lineNumber: 69,
                            columnNumber: 11
                        }, this)
                    ]
                }, void 0, true, {
                    fileName: "src/components/IT20125202/topic/ViewTopic.js",
                    lineNumber: 51,
                    columnNumber: 9
                }, this)
            ]
        }, void 0, true, {
            fileName: "src/components/IT20125202/topic/ViewTopic.js",
            lineNumber: 45,
            columnNumber: 7
        }, this);
    }
}
exports.default = ViewTopic;

  $parcel$ReactRefreshHelpers$99c1.postlude(module);
} finally {
  window.$RefreshReg$ = prevRefreshReg;
  window.$RefreshSig$ = prevRefreshSig;
}
},{"react/jsx-dev-runtime":"iTorj","react":"21dqq","axios":"jo6P5","sweetalert":"38a65","@parcel/transformer-js/src/esmodule-helpers.js":"gkKU3","@parcel/transformer-react-refresh-wrap/lib/helpers/helpers.js":"km3Ru"}],"2chAM":[function(require,module,exports) {
var $parcel$ReactRefreshHelpers$4d9d = require("@parcel/transformer-react-refresh-wrap/lib/helpers/helpers.js");
var prevRefreshReg = window.$RefreshReg$;
var prevRefreshSig = window.$RefreshSig$;
$parcel$ReactRefreshHelpers$4d9d.prelude(module);

try {
var parcelHelpers = require("@parcel/transformer-js/src/esmodule-helpers.js");
parcelHelpers.defineInteropFlag(exports);
var _jsxDevRuntime = require("react/jsx-dev-runtime");
//Staff update status and add comments
var _react = require("react");
var _reactDefault = parcelHelpers.interopDefault(_react);
var _axios = require("axios");
var _axiosDefault = parcelHelpers.interopDefault(_axios);
var _sweetalert = require("sweetalert");
var _sweetalertDefault = parcelHelpers.interopDefault(_sweetalert);
var _jwtDecode = require("jwt-decode");
var _jwtDecodeDefault = parcelHelpers.interopDefault(_jwtDecode);
class UpdateTopic extends _react.Component {
    constructor(props){
        super(props);
        this.state = {
            userType: "",
            groupId: "",
            topicR: "",
            description: "",
            status: "",
            comments: ""
        };
    }
    handleInputChange = (e)=>{
        const { name , value  } = e.target;
        this.setState({
            ...this.state,
            [name]: value
        });
    };
    onSubmit = (e)=>{
        e.preventDefault();
        const id = this.props.match.params.id;
        const { groupId , topicR , description , status , comments  } = this.state;
        const data = {
            groupId: groupId,
            topicR: topicR,
            description: description,
            status: status,
            comments: comments
        };
        // console.log(data)
        // validations--------------------------------------------------------
        let validated = true;
        if (this.state.userType === 'Student') {
            if (this.state.status === 'Rejected') {
                validated = false;
                _sweetalertDefault.default({
                    title: "You cannot edit this",
                    text: "Please create a new topic request",
                    icon: "warning"
                });
            } else if (this.state.topicR === '' || this.state.description === '') {
                validated = false;
                _sweetalertDefault.default({
                    title: "",
                    text: "Topic and Description cannot be empty",
                    icon: "warning"
                });
            }
        } else if ((this.state.status === 'Accepted' || this.state.status === 'Rejected') && this.state.comments === '') {
            validated = false;
            _sweetalertDefault.default({
                title: "",
                text: "Please give your comments",
                icon: "warning"
            });
        }
        // update validated data
        if (validated) _axiosDefault.default.put(`http://localhost:5000/topic/update/${id}`, data).then((res)=>{
            if (res.data.success) _sweetalertDefault.default("Topic updated successfully!", "", "success").then((value)=>{
                if (value) {
                    if (this.state.userType === 'Panel Member') {
                        this.props.history.push(`/panel/topic/list`);
                        window.location.reload();
                    } else {
                        this.props.history.push(`/student/topics`);
                        window.location.reload();
                    }
                }
            });
        });
    };
    componentDidMount() {
        document.title = "Update Topic";
        // redirect to the login page if the user is not logged in
        if (!localStorage.userToken) _sweetalertDefault.default("Please login first", "", "warning").then((value)=>{
            if (value) {
                this.props.history.push(`/user/login`);
                window.location.reload();
            }
        });
        const usertoken = localStorage.userToken;
        const decoded = _jwtDecodeDefault.default(usertoken);
        this.setState({
            userType: decoded.type
        });
        const id = this.props.match.params.id;
        // get the selected topic details
        _axiosDefault.default.get(`http://localhost:5000/topic/${id}`).then((res)=>{
            if (res.data.success) this.setState({
                groupId: res.data.topic.groupId,
                topicR: res.data.topic.topicR,
                description: res.data.topic.description,
                status: res.data.topic.status,
                comments: res.data.topic.comments
            });
        });
    }
    render() {
        return /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
            className: "container",
            style: {
                padding: '50px 50px 50px 50px',
                background: 'white',
                minHeight: '100vh'
            },
            children: [
                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                    className: "col-lg-9 mt-2 mb-2",
                    children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("h1", {
                        children: "Update Topic"
                    }, void 0, false, {
                        fileName: "src/components/IT20125202/topic/UpdateTopic.js",
                        lineNumber: 148,
                        columnNumber: 11
                    }, this)
                }, void 0, false, {
                    fileName: "src/components/IT20125202/topic/UpdateTopic.js",
                    lineNumber: 147,
                    columnNumber: 9
                }, this),
                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("hr", {}, void 0, false, {
                    fileName: "src/components/IT20125202/topic/UpdateTopic.js",
                    lineNumber: 150,
                    columnNumber: 9
                }, this),
                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("br", {}, void 0, false, {
                    fileName: "src/components/IT20125202/topic/UpdateTopic.js",
                    lineNumber: 150,
                    columnNumber: 15
                }, this),
                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                    className: "col-md-8 mt-4 mx-auto",
                    children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("form", {
                        className: "needs-validation",
                        noValidate: true,
                        children: [
                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                className: "form-group",
                                style: {
                                    marginBottom: '15px'
                                },
                                children: [
                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("label", {
                                        style: {
                                            marginBottom: '5px'
                                        },
                                        children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("b", {
                                            children: "Group ID"
                                        }, void 0, false, {
                                            fileName: "src/components/IT20125202/topic/UpdateTopic.js",
                                            lineNumber: 154,
                                            columnNumber: 54
                                        }, this)
                                    }, void 0, false, {
                                        fileName: "src/components/IT20125202/topic/UpdateTopic.js",
                                        lineNumber: 154,
                                        columnNumber: 15
                                    }, this),
                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("input", {
                                        type: "text",
                                        className: "form-control",
                                        name: "groupId",
                                        value: this.state.groupId,
                                        onChange: this.handleInputChange,
                                        required: true,
                                        readOnly: true
                                    }, void 0, false, {
                                        fileName: "src/components/IT20125202/topic/UpdateTopic.js",
                                        lineNumber: 155,
                                        columnNumber: 15
                                    }, this)
                                ]
                            }, void 0, true, {
                                fileName: "src/components/IT20125202/topic/UpdateTopic.js",
                                lineNumber: 153,
                                columnNumber: 13
                            }, this),
                            this.state.userType === 'Student' && /*#__PURE__*/ _jsxDevRuntime.jsxDEV("span", {
                                children: [
                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                        className: "form-group",
                                        style: {
                                            marginBottom: '15px'
                                        },
                                        children: [
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("label", {
                                                style: {
                                                    marginBottom: '5px'
                                                },
                                                children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("b", {
                                                    children: "Topic"
                                                }, void 0, false, {
                                                    fileName: "src/components/IT20125202/topic/UpdateTopic.js",
                                                    lineNumber: 170,
                                                    columnNumber: 58
                                                }, this)
                                            }, void 0, false, {
                                                fileName: "src/components/IT20125202/topic/UpdateTopic.js",
                                                lineNumber: 170,
                                                columnNumber: 19
                                            }, this),
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("input", {
                                                type: "text",
                                                className: "form-control",
                                                name: "topicR",
                                                value: this.state.topicR,
                                                onChange: this.handleInputChange,
                                                required: true
                                            }, void 0, false, {
                                                fileName: "src/components/IT20125202/topic/UpdateTopic.js",
                                                lineNumber: 171,
                                                columnNumber: 19
                                            }, this)
                                        ]
                                    }, void 0, true, {
                                        fileName: "src/components/IT20125202/topic/UpdateTopic.js",
                                        lineNumber: 169,
                                        columnNumber: 17
                                    }, this),
                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                        className: "form-group",
                                        style: {
                                            marginBottom: '15px'
                                        },
                                        children: [
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("label", {
                                                style: {
                                                    marginBottom: '5px'
                                                },
                                                children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("b", {
                                                    children: "Description"
                                                }, void 0, false, {
                                                    fileName: "src/components/IT20125202/topic/UpdateTopic.js",
                                                    lineNumber: 182,
                                                    columnNumber: 58
                                                }, this)
                                            }, void 0, false, {
                                                fileName: "src/components/IT20125202/topic/UpdateTopic.js",
                                                lineNumber: 182,
                                                columnNumber: 19
                                            }, this),
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("textarea", {
                                                type: "text",
                                                className: "form-control",
                                                name: "description",
                                                value: this.state.description,
                                                onChange: this.handleInputChange,
                                                required: true
                                            }, void 0, false, {
                                                fileName: "src/components/IT20125202/topic/UpdateTopic.js",
                                                lineNumber: 183,
                                                columnNumber: 19
                                            }, this)
                                        ]
                                    }, void 0, true, {
                                        fileName: "src/components/IT20125202/topic/UpdateTopic.js",
                                        lineNumber: 181,
                                        columnNumber: 17
                                    }, this),
                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                        className: "form-group",
                                        style: {
                                            marginBottom: '15px'
                                        },
                                        children: [
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("label", {
                                                style: {
                                                    marginBottom: '5px'
                                                },
                                                children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("b", {
                                                    children: "Status"
                                                }, void 0, false, {
                                                    fileName: "src/components/IT20125202/topic/UpdateTopic.js",
                                                    lineNumber: 194,
                                                    columnNumber: 58
                                                }, this)
                                            }, void 0, false, {
                                                fileName: "src/components/IT20125202/topic/UpdateTopic.js",
                                                lineNumber: 194,
                                                columnNumber: 19
                                            }, this),
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("textarea", {
                                                type: "text",
                                                className: "form-control",
                                                name: "status",
                                                value: this.state.status,
                                                onChange: this.handleInputChange,
                                                readOnly: true
                                            }, void 0, false, {
                                                fileName: "src/components/IT20125202/topic/UpdateTopic.js",
                                                lineNumber: 195,
                                                columnNumber: 19
                                            }, this)
                                        ]
                                    }, void 0, true, {
                                        fileName: "src/components/IT20125202/topic/UpdateTopic.js",
                                        lineNumber: 193,
                                        columnNumber: 17
                                    }, this),
                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                        className: "form-group",
                                        style: {
                                            marginBottom: '15px'
                                        },
                                        children: [
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("label", {
                                                style: {
                                                    marginBottom: '5px'
                                                },
                                                children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("b", {
                                                    children: "Comments"
                                                }, void 0, false, {
                                                    fileName: "src/components/IT20125202/topic/UpdateTopic.js",
                                                    lineNumber: 206,
                                                    columnNumber: 58
                                                }, this)
                                            }, void 0, false, {
                                                fileName: "src/components/IT20125202/topic/UpdateTopic.js",
                                                lineNumber: 206,
                                                columnNumber: 19
                                            }, this),
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("input", {
                                                type: "text",
                                                className: "form-control",
                                                name: "comments",
                                                placeholder: "",
                                                value: this.state.comments,
                                                onChange: this.handleInputChange,
                                                readOnly: true
                                            }, void 0, false, {
                                                fileName: "src/components/IT20125202/topic/UpdateTopic.js",
                                                lineNumber: 207,
                                                columnNumber: 19
                                            }, this)
                                        ]
                                    }, void 0, true, {
                                        fileName: "src/components/IT20125202/topic/UpdateTopic.js",
                                        lineNumber: 205,
                                        columnNumber: 17
                                    }, this)
                                ]
                            }, void 0, true, {
                                fileName: "src/components/IT20125202/topic/UpdateTopic.js",
                                lineNumber: 168,
                                columnNumber: 15
                            }, this),
                            this.state.userType === 'Panel Member' && /*#__PURE__*/ _jsxDevRuntime.jsxDEV("span", {
                                children: [
                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                        className: "form-group",
                                        style: {
                                            marginBottom: '15px'
                                        },
                                        children: [
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("label", {
                                                style: {
                                                    marginBottom: '5px'
                                                },
                                                children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("b", {
                                                    children: "Topic"
                                                }, void 0, false, {
                                                    fileName: "src/components/IT20125202/topic/UpdateTopic.js",
                                                    lineNumber: 223,
                                                    columnNumber: 58
                                                }, this)
                                            }, void 0, false, {
                                                fileName: "src/components/IT20125202/topic/UpdateTopic.js",
                                                lineNumber: 223,
                                                columnNumber: 19
                                            }, this),
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("input", {
                                                type: "text",
                                                className: "form-control",
                                                name: "topicR",
                                                placeholder: "Enter research topic",
                                                value: this.state.topicR,
                                                onChange: this.handleInputChange,
                                                readOnly: true
                                            }, void 0, false, {
                                                fileName: "src/components/IT20125202/topic/UpdateTopic.js",
                                                lineNumber: 224,
                                                columnNumber: 19
                                            }, this)
                                        ]
                                    }, void 0, true, {
                                        fileName: "src/components/IT20125202/topic/UpdateTopic.js",
                                        lineNumber: 222,
                                        columnNumber: 17
                                    }, this),
                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                        className: "form-group",
                                        style: {
                                            marginBottom: '15px'
                                        },
                                        children: [
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("label", {
                                                style: {
                                                    marginBottom: '5px'
                                                },
                                                children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("b", {
                                                    children: "Description"
                                                }, void 0, false, {
                                                    fileName: "src/components/IT20125202/topic/UpdateTopic.js",
                                                    lineNumber: 236,
                                                    columnNumber: 58
                                                }, this)
                                            }, void 0, false, {
                                                fileName: "src/components/IT20125202/topic/UpdateTopic.js",
                                                lineNumber: 236,
                                                columnNumber: 19
                                            }, this),
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("textarea", {
                                                type: "text",
                                                className: "form-control",
                                                name: "description",
                                                placeholder: "Enter a description about the topic",
                                                value: this.state.description,
                                                onChange: this.handleInputChange,
                                                readOnly: true
                                            }, void 0, false, {
                                                fileName: "src/components/IT20125202/topic/UpdateTopic.js",
                                                lineNumber: 237,
                                                columnNumber: 19
                                            }, this)
                                        ]
                                    }, void 0, true, {
                                        fileName: "src/components/IT20125202/topic/UpdateTopic.js",
                                        lineNumber: 235,
                                        columnNumber: 17
                                    }, this),
                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                        className: "form-group",
                                        style: {
                                            marginBottom: '15px'
                                        },
                                        children: [
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("label", {
                                                style: {
                                                    marginBottom: '5px'
                                                },
                                                children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("b", {
                                                    children: "Status"
                                                }, void 0, false, {
                                                    fileName: "src/components/IT20125202/topic/UpdateTopic.js",
                                                    lineNumber: 249,
                                                    columnNumber: 58
                                                }, this)
                                            }, void 0, false, {
                                                fileName: "src/components/IT20125202/topic/UpdateTopic.js",
                                                lineNumber: 249,
                                                columnNumber: 19
                                            }, this),
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("select", {
                                                name: "status",
                                                value: this.state.status,
                                                onChange: this.handleInputChange,
                                                className: "form-select",
                                                children: [
                                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("option", {
                                                        value: "Pending",
                                                        children: "Pending"
                                                    }, void 0, false, {
                                                        fileName: "src/components/IT20125202/topic/UpdateTopic.js",
                                                        lineNumber: 251,
                                                        columnNumber: 21
                                                    }, this),
                                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("option", {
                                                        value: "Accepted",
                                                        children: "Accepted"
                                                    }, void 0, false, {
                                                        fileName: "src/components/IT20125202/topic/UpdateTopic.js",
                                                        lineNumber: 252,
                                                        columnNumber: 21
                                                    }, this),
                                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("option", {
                                                        value: "Rejected",
                                                        children: "Rejected"
                                                    }, void 0, false, {
                                                        fileName: "src/components/IT20125202/topic/UpdateTopic.js",
                                                        lineNumber: 253,
                                                        columnNumber: 21
                                                    }, this)
                                                ]
                                            }, void 0, true, {
                                                fileName: "src/components/IT20125202/topic/UpdateTopic.js",
                                                lineNumber: 250,
                                                columnNumber: 19
                                            }, this)
                                        ]
                                    }, void 0, true, {
                                        fileName: "src/components/IT20125202/topic/UpdateTopic.js",
                                        lineNumber: 248,
                                        columnNumber: 17
                                    }, this),
                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                        className: "form-group",
                                        style: {
                                            marginBottom: '15px'
                                        },
                                        children: [
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("label", {
                                                style: {
                                                    marginBottom: '5px'
                                                },
                                                children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("b", {
                                                    children: "Comments"
                                                }, void 0, false, {
                                                    fileName: "src/components/IT20125202/topic/UpdateTopic.js",
                                                    lineNumber: 258,
                                                    columnNumber: 58
                                                }, this)
                                            }, void 0, false, {
                                                fileName: "src/components/IT20125202/topic/UpdateTopic.js",
                                                lineNumber: 258,
                                                columnNumber: 19
                                            }, this),
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("input", {
                                                type: "text",
                                                className: "form-control",
                                                name: "comments",
                                                placeholder: "",
                                                value: this.state.comments,
                                                onChange: this.handleInputChange,
                                                required: true
                                            }, void 0, false, {
                                                fileName: "src/components/IT20125202/topic/UpdateTopic.js",
                                                lineNumber: 259,
                                                columnNumber: 19
                                            }, this)
                                        ]
                                    }, void 0, true, {
                                        fileName: "src/components/IT20125202/topic/UpdateTopic.js",
                                        lineNumber: 257,
                                        columnNumber: 17
                                    }, this)
                                ]
                            }, void 0, true, {
                                fileName: "src/components/IT20125202/topic/UpdateTopic.js",
                                lineNumber: 221,
                                columnNumber: 15
                            }, this),
                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("button", {
                                className: "btn btn-success",
                                type: "submit",
                                style: {
                                    maeginTop: '15px'
                                },
                                onClick: this.onSubmit,
                                children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("b", {
                                    children: [
                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("i", {
                                            className: "far fa-check-square"
                                        }, void 0, false, {
                                            fileName: "src/components/IT20125202/topic/UpdateTopic.js",
                                            lineNumber: 272,
                                            columnNumber: 18
                                        }, this),
                                        "\xa0 Update"
                                    ]
                                }, void 0, true, {
                                    fileName: "src/components/IT20125202/topic/UpdateTopic.js",
                                    lineNumber: 272,
                                    columnNumber: 15
                                }, this)
                            }, void 0, false, {
                                fileName: "src/components/IT20125202/topic/UpdateTopic.js",
                                lineNumber: 271,
                                columnNumber: 13
                            }, this),
                            "\xa0\xa0",
                            this.state.userType === 'Student' && /*#__PURE__*/ _jsxDevRuntime.jsxDEV("span", {
                                children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("a", {
                                    href: "/student/topics",
                                    class: "btn btn-outline-success",
                                    tabindex: "-1",
                                    role: "button",
                                    "aria-disabled": "true",
                                    children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("b", {
                                        children: "Back"
                                    }, void 0, false, {
                                        fileName: "src/components/IT20125202/topic/UpdateTopic.js",
                                        lineNumber: 287,
                                        columnNumber: 19
                                    }, this)
                                }, void 0, false, {
                                    fileName: "src/components/IT20125202/topic/UpdateTopic.js",
                                    lineNumber: 281,
                                    columnNumber: 17
                                }, this)
                            }, void 0, false, {
                                fileName: "src/components/IT20125202/topic/UpdateTopic.js",
                                lineNumber: 280,
                                columnNumber: 15
                            }, this),
                            this.state.userType === 'Panel Member' && /*#__PURE__*/ _jsxDevRuntime.jsxDEV("span", {
                                children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("a", {
                                    href: "/panel/topic/list",
                                    class: "btn btn-outline-success",
                                    tabindex: "-1",
                                    role: "button",
                                    "aria-disabled": "true",
                                    children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("b", {
                                        children: "Back"
                                    }, void 0, false, {
                                        fileName: "src/components/IT20125202/topic/UpdateTopic.js",
                                        lineNumber: 300,
                                        columnNumber: 19
                                    }, this)
                                }, void 0, false, {
                                    fileName: "src/components/IT20125202/topic/UpdateTopic.js",
                                    lineNumber: 294,
                                    columnNumber: 17
                                }, this)
                            }, void 0, false, {
                                fileName: "src/components/IT20125202/topic/UpdateTopic.js",
                                lineNumber: 293,
                                columnNumber: 15
                            }, this)
                        ]
                    }, void 0, true, {
                        fileName: "src/components/IT20125202/topic/UpdateTopic.js",
                        lineNumber: 152,
                        columnNumber: 11
                    }, this)
                }, void 0, false, {
                    fileName: "src/components/IT20125202/topic/UpdateTopic.js",
                    lineNumber: 151,
                    columnNumber: 9
                }, this)
            ]
        }, void 0, true, {
            fileName: "src/components/IT20125202/topic/UpdateTopic.js",
            lineNumber: 146,
            columnNumber: 7
        }, this);
    }
}
exports.default = UpdateTopic;

  $parcel$ReactRefreshHelpers$4d9d.postlude(module);
} finally {
  window.$RefreshReg$ = prevRefreshReg;
  window.$RefreshSig$ = prevRefreshSig;
}
},{"react/jsx-dev-runtime":"iTorj","react":"21dqq","axios":"jo6P5","sweetalert":"38a65","jwt-decode":"ljM1w","@parcel/transformer-js/src/esmodule-helpers.js":"gkKU3","@parcel/transformer-react-refresh-wrap/lib/helpers/helpers.js":"km3Ru"}],"62LVF":[function(require,module,exports) {
var $parcel$ReactRefreshHelpers$c19b = require("@parcel/transformer-react-refresh-wrap/lib/helpers/helpers.js");
var prevRefreshReg = window.$RefreshReg$;
var prevRefreshSig = window.$RefreshSig$;
$parcel$ReactRefreshHelpers$c19b.prelude(module);

try {
var parcelHelpers = require("@parcel/transformer-js/src/esmodule-helpers.js");
parcelHelpers.defineInteropFlag(exports);
var _jsxDevRuntime = require("react/jsx-dev-runtime");
var _react = require("react");
var _reactDefault = parcelHelpers.interopDefault(_react);
var _axios = require("axios");
var _axiosDefault = parcelHelpers.interopDefault(_axios);
var _sweetalert = require("sweetalert");
var _sweetalertDefault = parcelHelpers.interopDefault(_sweetalert);
var _evaluationList = require("./EvaluationList");
var _evaluationListDefault = parcelHelpers.interopDefault(_evaluationList);
var _jwtDecode = require("jwt-decode");
var _jwtDecodeDefault = parcelHelpers.interopDefault(_jwtDecode);
class AddEvaluation extends _react.Component {
    constructor(props){
        super(props);
        this.state = {
            groupId: "",
            evaluationTopic: "",
            panel: "",
            date: "",
            from: "",
            to: "",
            link: "",
            subject: "About Your Evaluation Session.",
            text: "Your session has been added to the website. Please see the details and let your team members know. --Auto Generated Email--"
        };
    }
    componentDidMount() {
        if (localStorage.userToken) {
            const usertoken = localStorage.userToken;
            const decoded = _jwtDecodeDefault.default(usertoken);
            this.setState({
                panel: decoded.panel
            });
        }
    }
    handleInputChange = (e)=>{
        const { name , value  } = e.target;
        this.setState({
            ...this.state,
            [name]: value
        });
    };
    onSubmit = (e)=>{
        e.preventDefault();
        const { groupId , evaluationTopic , panel , date , from , to , link , subject , text  } = this.state;
        const data = {
            groupId: groupId,
            evaluationTopic: evaluationTopic,
            panel: panel,
            date: date,
            from: from,
            to: to,
            link: link
        };
        console.log(data);
        //save evaluations
        _axiosDefault.default.post("http://localhost:5000/evaluation/save", data).then((res)=>{
            if (res.data.success) {
                _sweetalertDefault.default("Added Evaluation Successfully !", "", "success").then((value)=>{
                    window.location = "/evaluation/add";
                });
                this.setState({
                    groupId: "",
                    evaluationTopic: "",
                    panel: "",
                    date: "",
                    from: "",
                    to: "",
                    link: ""
                });
            }
        });
        // Send data to Email Service
        const emailData = {
            groupId: groupId,
            subject: subject,
            text: text,
            type: evaluationTopic,
            panel: panel,
            date: date,
            from: from,
            to: to,
            link: link
        };
        //send email to group members
        _axiosDefault.default.post("http://localhost:5000/submitiont/email", emailData).then((res)=>{
            if (res.data.success) console.log("Sent Email Successfully");
            else console.log("Not Sent Email ");
        });
    };
    render() {
        return /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
            className: "container",
            children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                className: "row",
                children: [
                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                        className: "col-sm-7",
                        children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                            className: "ms-4",
                            children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                className: "row",
                                children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                    className: "col-sm-12",
                                    children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                        className: "col-sm-12 mt-4",
                                        children: [
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("h1", {
                                                className: "h3 mb-3 font-weight-normal",
                                                children: "Add New Evaluation Session"
                                            }, void 0, false, {
                                                fileName: "src/components/IT20128036/admin/AddEvaluation.js",
                                                lineNumber: 144,
                                                columnNumber: 19
                                            }, this),
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("hr", {}, void 0, false, {
                                                fileName: "src/components/IT20128036/admin/AddEvaluation.js",
                                                lineNumber: 147,
                                                columnNumber: 19
                                            }, this),
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("form", {
                                                className: "needs-validation",
                                                onSubmit: this.onSubmit,
                                                children: [
                                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                                        className: "row",
                                                        children: [
                                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                                                className: "col-sm-4",
                                                                children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                                                    className: "form-group",
                                                                    style: {
                                                                        marginBottom: "15px"
                                                                    },
                                                                    children: [
                                                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("label", {
                                                                            style: {
                                                                                marginBottom: "5px"
                                                                            },
                                                                            children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("strong", {
                                                                                children: "Group ID"
                                                                            }, void 0, false, {
                                                                                fileName: "src/components/IT20128036/admin/AddEvaluation.js",
                                                                                lineNumber: 158,
                                                                                columnNumber: 62
                                                                            }, this)
                                                                        }, void 0, false, {
                                                                            fileName: "src/components/IT20128036/admin/AddEvaluation.js",
                                                                            lineNumber: 158,
                                                                            columnNumber: 23
                                                                        }, this),
                                                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("input", {
                                                                            type: "text",
                                                                            className: "form-control",
                                                                            name: "groupId",
                                                                            placeholder: "Enter Group ID -Gxxx",
                                                                            pattern: "G[0-9]{3}",
                                                                            title: "Group ID is Invalid",
                                                                            value: this.state.groupId,
                                                                            onChange: this.handleInputChange,
                                                                            required: true
                                                                        }, void 0, false, {
                                                                            fileName: "src/components/IT20128036/admin/AddEvaluation.js",
                                                                            lineNumber: 159,
                                                                            columnNumber: 23
                                                                        }, this)
                                                                    ]
                                                                }, void 0, true, {
                                                                    fileName: "src/components/IT20128036/admin/AddEvaluation.js",
                                                                    lineNumber: 153,
                                                                    columnNumber: 22
                                                                }, this)
                                                            }, void 0, false, {
                                                                fileName: "src/components/IT20128036/admin/AddEvaluation.js",
                                                                lineNumber: 151,
                                                                columnNumber: 22
                                                            }, this),
                                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                                                className: "col-sm-8",
                                                                children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                                                    className: "form-group",
                                                                    style: {
                                                                        marginBottom: "15px"
                                                                    },
                                                                    children: [
                                                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("label", {
                                                                            style: {
                                                                                marginBottom: "5px"
                                                                            },
                                                                            children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("strong", {
                                                                                children: "Evaluation Type"
                                                                            }, void 0, false, {
                                                                                fileName: "src/components/IT20128036/admin/AddEvaluation.js",
                                                                                lineNumber: 187,
                                                                                columnNumber: 23
                                                                            }, this)
                                                                        }, void 0, false, {
                                                                            fileName: "src/components/IT20128036/admin/AddEvaluation.js",
                                                                            lineNumber: 186,
                                                                            columnNumber: 22
                                                                        }, this),
                                                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("select", {
                                                                            className: "form-select",
                                                                            name: "evaluationTopic",
                                                                            value: this.state.evaluationTopic,
                                                                            onChange: this.handleInputChange,
                                                                            required: true,
                                                                            children: [
                                                                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("option", {
                                                                                    evaluationTopic: "not selected yet",
                                                                                    selected: true,
                                                                                    children: "Select Type"
                                                                                }, void 0, false, {
                                                                                    fileName: "src/components/IT20128036/admin/AddEvaluation.js",
                                                                                    lineNumber: 197,
                                                                                    columnNumber: 24
                                                                                }, this),
                                                                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("option", {
                                                                                    evaluationTopic: "Topic Assessment Document",
                                                                                    children: "Topic Assessment Document"
                                                                                }, void 0, false, {
                                                                                    fileName: "src/components/IT20128036/admin/AddEvaluation.js",
                                                                                    lineNumber: 200,
                                                                                    columnNumber: 24
                                                                                }, this),
                                                                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("option", {
                                                                                    evaluationTopic: "Proposal Document",
                                                                                    children: "Proposal Document"
                                                                                }, void 0, false, {
                                                                                    fileName: "src/components/IT20128036/admin/AddEvaluation.js",
                                                                                    lineNumber: 203,
                                                                                    columnNumber: 24
                                                                                }, this),
                                                                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("option", {
                                                                                    evaluationTopic: "Presentation Slides",
                                                                                    children: "Presentation Slides"
                                                                                }, void 0, false, {
                                                                                    fileName: "src/components/IT20128036/admin/AddEvaluation.js",
                                                                                    lineNumber: 206,
                                                                                    columnNumber: 24
                                                                                }, this),
                                                                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("option", {
                                                                                    evaluationTopic: "Final Thesis",
                                                                                    children: "Final Thesis"
                                                                                }, void 0, false, {
                                                                                    fileName: "src/components/IT20128036/admin/AddEvaluation.js",
                                                                                    lineNumber: 209,
                                                                                    columnNumber: 24
                                                                                }, this)
                                                                            ]
                                                                        }, void 0, true, {
                                                                            fileName: "src/components/IT20128036/admin/AddEvaluation.js",
                                                                            lineNumber: 190,
                                                                            columnNumber: 22
                                                                        }, this)
                                                                    ]
                                                                }, void 0, true, {
                                                                    fileName: "src/components/IT20128036/admin/AddEvaluation.js",
                                                                    lineNumber: 182,
                                                                    columnNumber: 22
                                                                }, this)
                                                            }, void 0, false, {
                                                                fileName: "src/components/IT20128036/admin/AddEvaluation.js",
                                                                lineNumber: 175,
                                                                columnNumber: 22
                                                            }, this)
                                                        ]
                                                    }, void 0, true, {
                                                        fileName: "src/components/IT20128036/admin/AddEvaluation.js",
                                                        lineNumber: 150,
                                                        columnNumber: 20
                                                    }, this),
                                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                                        className: "form-group",
                                                        style: {
                                                            marginBottom: "15px"
                                                        },
                                                        children: [
                                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("label", {
                                                                style: {
                                                                    marginBottom: "5px"
                                                                },
                                                                children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("strong", {
                                                                    children: "Panel"
                                                                }, void 0, false, {
                                                                    fileName: "src/components/IT20128036/admin/AddEvaluation.js",
                                                                    lineNumber: 234,
                                                                    columnNumber: 62
                                                                }, this)
                                                            }, void 0, false, {
                                                                fileName: "src/components/IT20128036/admin/AddEvaluation.js",
                                                                lineNumber: 234,
                                                                columnNumber: 23
                                                            }, this),
                                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("input", {
                                                                type: "text",
                                                                className: "form-control",
                                                                name: "panel",
                                                                value: this.state.panel,
                                                                onChange: this.handleInputChange,
                                                                disabled: true
                                                            }, void 0, false, {
                                                                fileName: "src/components/IT20128036/admin/AddEvaluation.js",
                                                                lineNumber: 236,
                                                                columnNumber: 23
                                                            }, this)
                                                        ]
                                                    }, void 0, true, {
                                                        fileName: "src/components/IT20128036/admin/AddEvaluation.js",
                                                        lineNumber: 230,
                                                        columnNumber: 21
                                                    }, this),
                                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                                        className: "form-group",
                                                        style: {
                                                            marginBottom: "15px"
                                                        },
                                                        children: [
                                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("label", {
                                                                style: {
                                                                    marginBottom: "5px"
                                                                },
                                                                children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("strong", {
                                                                    children: "Date"
                                                                }, void 0, false, {
                                                                    fileName: "src/components/IT20128036/admin/AddEvaluation.js",
                                                                    lineNumber: 262,
                                                                    columnNumber: 62
                                                                }, this)
                                                            }, void 0, false, {
                                                                fileName: "src/components/IT20128036/admin/AddEvaluation.js",
                                                                lineNumber: 262,
                                                                columnNumber: 23
                                                            }, this),
                                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("input", {
                                                                type: "date",
                                                                className: "form-control",
                                                                name: "date",
                                                                value: this.state.date,
                                                                onChange: this.handleInputChange,
                                                                required: true
                                                            }, void 0, false, {
                                                                fileName: "src/components/IT20128036/admin/AddEvaluation.js",
                                                                lineNumber: 263,
                                                                columnNumber: 23
                                                            }, this)
                                                        ]
                                                    }, void 0, true, {
                                                        fileName: "src/components/IT20128036/admin/AddEvaluation.js",
                                                        lineNumber: 258,
                                                        columnNumber: 21
                                                    }, this),
                                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                                        className: "row",
                                                        children: [
                                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                                                className: "form-group col-6",
                                                                style: {
                                                                    marginBottom: "15px"
                                                                },
                                                                children: [
                                                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("label", {
                                                                        style: {
                                                                            marginBottom: "5px"
                                                                        },
                                                                        children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("strong", {
                                                                            children: "From"
                                                                        }, void 0, false, {
                                                                            fileName: "src/components/IT20128036/admin/AddEvaluation.js",
                                                                            lineNumber: 278,
                                                                            columnNumber: 64
                                                                        }, this)
                                                                    }, void 0, false, {
                                                                        fileName: "src/components/IT20128036/admin/AddEvaluation.js",
                                                                        lineNumber: 278,
                                                                        columnNumber: 25
                                                                    }, this),
                                                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("input", {
                                                                        type: "time",
                                                                        className: "form-control",
                                                                        name: "from",
                                                                        value: this.state.from,
                                                                        onChange: this.handleInputChange,
                                                                        required: true
                                                                    }, void 0, false, {
                                                                        fileName: "src/components/IT20128036/admin/AddEvaluation.js",
                                                                        lineNumber: 279,
                                                                        columnNumber: 25
                                                                    }, this)
                                                                ]
                                                            }, void 0, true, {
                                                                fileName: "src/components/IT20128036/admin/AddEvaluation.js",
                                                                lineNumber: 274,
                                                                columnNumber: 23
                                                            }, this),
                                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                                                className: "form-group col-6",
                                                                style: {
                                                                    marginBottom: "15px"
                                                                },
                                                                children: [
                                                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("label", {
                                                                        style: {
                                                                            marginBottom: "5px"
                                                                        },
                                                                        children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("strong", {
                                                                            children: "To"
                                                                        }, void 0, false, {
                                                                            fileName: "src/components/IT20128036/admin/AddEvaluation.js",
                                                                            lineNumber: 293,
                                                                            columnNumber: 64
                                                                        }, this)
                                                                    }, void 0, false, {
                                                                        fileName: "src/components/IT20128036/admin/AddEvaluation.js",
                                                                        lineNumber: 293,
                                                                        columnNumber: 25
                                                                    }, this),
                                                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("input", {
                                                                        type: "time",
                                                                        className: "form-control",
                                                                        name: "to",
                                                                        value: this.state.to,
                                                                        onChange: this.handleInputChange,
                                                                        required: true
                                                                    }, void 0, false, {
                                                                        fileName: "src/components/IT20128036/admin/AddEvaluation.js",
                                                                        lineNumber: 294,
                                                                        columnNumber: 25
                                                                    }, this)
                                                                ]
                                                            }, void 0, true, {
                                                                fileName: "src/components/IT20128036/admin/AddEvaluation.js",
                                                                lineNumber: 289,
                                                                columnNumber: 23
                                                            }, this)
                                                        ]
                                                    }, void 0, true, {
                                                        fileName: "src/components/IT20128036/admin/AddEvaluation.js",
                                                        lineNumber: 273,
                                                        columnNumber: 21
                                                    }, this),
                                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                                        className: "form-group",
                                                        style: {
                                                            marginBottom: "15px"
                                                        },
                                                        children: [
                                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("label", {
                                                                style: {
                                                                    marginBottom: "5px"
                                                                },
                                                                children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("strong", {
                                                                    children: "Link"
                                                                }, void 0, false, {
                                                                    fileName: "src/components/IT20128036/admin/AddEvaluation.js",
                                                                    lineNumber: 309,
                                                                    columnNumber: 62
                                                                }, this)
                                                            }, void 0, false, {
                                                                fileName: "src/components/IT20128036/admin/AddEvaluation.js",
                                                                lineNumber: 309,
                                                                columnNumber: 23
                                                            }, this),
                                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("input", {
                                                                type: "text",
                                                                className: "form-control",
                                                                name: "link",
                                                                placeholder: "Enter Session Link",
                                                                value: this.state.link,
                                                                onChange: this.handleInputChange,
                                                                required: true
                                                            }, void 0, false, {
                                                                fileName: "src/components/IT20128036/admin/AddEvaluation.js",
                                                                lineNumber: 310,
                                                                columnNumber: 23
                                                            }, this)
                                                        ]
                                                    }, void 0, true, {
                                                        fileName: "src/components/IT20128036/admin/AddEvaluation.js",
                                                        lineNumber: 305,
                                                        columnNumber: 21
                                                    }, this),
                                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("button", {
                                                        className: "btn btn-outline-success",
                                                        type: "submit",
                                                        style: {
                                                            margintop: "15px"
                                                        },
                                                        children: [
                                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("i", {
                                                                className: "far fa-check-square"
                                                            }, void 0, false, {
                                                                fileName: "src/components/IT20128036/admin/AddEvaluation.js",
                                                                lineNumber: 327,
                                                                columnNumber: 23
                                                            }, this),
                                                            "\xa0 Save"
                                                        ]
                                                    }, void 0, true, {
                                                        fileName: "src/components/IT20128036/admin/AddEvaluation.js",
                                                        lineNumber: 321,
                                                        columnNumber: 21
                                                    }, this)
                                                ]
                                            }, void 0, true, {
                                                fileName: "src/components/IT20128036/admin/AddEvaluation.js",
                                                lineNumber: 148,
                                                columnNumber: 19
                                            }, this)
                                        ]
                                    }, void 0, true, {
                                        fileName: "src/components/IT20128036/admin/AddEvaluation.js",
                                        lineNumber: 143,
                                        columnNumber: 17
                                    }, this)
                                }, void 0, false, {
                                    fileName: "src/components/IT20128036/admin/AddEvaluation.js",
                                    lineNumber: 142,
                                    columnNumber: 15
                                }, this)
                            }, void 0, false, {
                                fileName: "src/components/IT20128036/admin/AddEvaluation.js",
                                lineNumber: 141,
                                columnNumber: 13
                            }, this)
                        }, void 0, false, {
                            fileName: "src/components/IT20128036/admin/AddEvaluation.js",
                            lineNumber: 140,
                            columnNumber: 13
                        }, this)
                    }, void 0, false, {
                        fileName: "src/components/IT20128036/admin/AddEvaluation.js",
                        lineNumber: 139,
                        columnNumber: 11
                    }, this),
                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                        className: "col-sm-5",
                        children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                            className: "row",
                            children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                className: "col-12",
                                children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV(_evaluationListDefault.default, {}, void 0, false, {
                                    fileName: "src/components/IT20128036/admin/AddEvaluation.js",
                                    lineNumber: 340,
                                    columnNumber: 17
                                }, this)
                            }, void 0, false, {
                                fileName: "src/components/IT20128036/admin/AddEvaluation.js",
                                lineNumber: 339,
                                columnNumber: 15
                            }, this)
                        }, void 0, false, {
                            fileName: "src/components/IT20128036/admin/AddEvaluation.js",
                            lineNumber: 338,
                            columnNumber: 13
                        }, this)
                    }, void 0, false, {
                        fileName: "src/components/IT20128036/admin/AddEvaluation.js",
                        lineNumber: 337,
                        columnNumber: 11
                    }, this)
                ]
            }, void 0, true, {
                fileName: "src/components/IT20128036/admin/AddEvaluation.js",
                lineNumber: 138,
                columnNumber: 9
            }, this)
        }, void 0, false, {
            fileName: "src/components/IT20128036/admin/AddEvaluation.js",
            lineNumber: 137,
            columnNumber: 7
        }, this);
    }
}
exports.default = AddEvaluation;

  $parcel$ReactRefreshHelpers$c19b.postlude(module);
} finally {
  window.$RefreshReg$ = prevRefreshReg;
  window.$RefreshSig$ = prevRefreshSig;
}
},{"react/jsx-dev-runtime":"iTorj","react":"21dqq","axios":"jo6P5","sweetalert":"38a65","./EvaluationList":"g48St","jwt-decode":"ljM1w","@parcel/transformer-js/src/esmodule-helpers.js":"gkKU3","@parcel/transformer-react-refresh-wrap/lib/helpers/helpers.js":"km3Ru"}],"g48St":[function(require,module,exports) {
var $parcel$ReactRefreshHelpers$c14e = require("@parcel/transformer-react-refresh-wrap/lib/helpers/helpers.js");
var prevRefreshReg = window.$RefreshReg$;
var prevRefreshSig = window.$RefreshSig$;
$parcel$ReactRefreshHelpers$c14e.prelude(module);

try {
var parcelHelpers = require("@parcel/transformer-js/src/esmodule-helpers.js");
parcelHelpers.defineInteropFlag(exports);
var _jsxDevRuntime = require("react/jsx-dev-runtime");
var _react = require("react");
var _reactDefault = parcelHelpers.interopDefault(_react);
var _axios = require("axios");
var _axiosDefault = parcelHelpers.interopDefault(_axios);
var _addEvaluation = require("./AddEvaluation");
var _addEvaluationDefault = parcelHelpers.interopDefault(_addEvaluation);
var _sweetalert = require("sweetalert");
var _sweetalertDefault = parcelHelpers.interopDefault(_sweetalert);
var _jwtDecode = require("jwt-decode");
var _jwtDecodeDefault = parcelHelpers.interopDefault(_jwtDecode);
class EvaluationList extends _react.Component {
    constructor(props){
        super(props);
        this.state = {
            evaluations: [],
            panel: ""
        };
    }
    componentDidMount() {
        if (localStorage.userToken) {
            const usertoken = localStorage.userToken;
            const decoded = _jwtDecodeDefault.default(usertoken);
            this.setState({
                panel: decoded.panel
            });
        }
        setTimeout(()=>{
            this.retriveEvaluations();
        }, 1000);
    }
    //retrive evaluations
    retriveEvaluations() {
        const pnel = this.state.panel;
        _axiosDefault.default.get(`http://localhost:5000/evaluation/panel/${pnel}`).then((res)=>{
            if (res.data.success) {
                this.setState({
                    evaluations: res.data.existingEvaluations
                });
                console.log(this.state.evaluations);
            }
        });
    }
    //delete evaluation
    onDelete = (id)=>{
        _sweetalertDefault.default({
            title: "Are you sure?",
            text: "Once deleted, you will not be able to recover this file!",
            icon: "warning",
            buttons: true,
            dangerMode: true
        }).then((willDelete)=>{
            if (willDelete) {
                _axiosDefault.default.delete(`http://localhost:5000/evaluation/delete/${id}`).then((res)=>{
                    this.retriveEvaluations();
                });
                _sweetalertDefault.default("Evaluation Session  has been deleted!", "", "success");
            } else _sweetalertDefault.default("Your file is safe!");
        });
    };
    //search filter
    filterData(evaluations, searchKey) {
        const result = evaluations.filter((evaluation)=>evaluation.groupId.toLowerCase().includes(searchKey) || evaluation.evaluationTopic.toLowerCase().includes(searchKey) || evaluation.date.toLowerCase().includes(searchKey) || evaluation.groupId.includes(searchKey) || evaluation.evaluationTopic.includes(searchKey) || evaluation.date.includes(searchKey)
        );
        this.setState({
            evaluations: result
        });
    }
    //search evaluation
    handleSearchArea = (e)=>{
        const searchKey = e.currentTarget.value;
        const pnel = this.state.panel;
        _axiosDefault.default.get(`http://localhost:5000/evaluation/panel/${pnel}`).then((res)=>{
            if (res.data.success) this.filterData(res.data.existingEvaluations, searchKey);
        });
    };
    render() {
        return /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
            className: "me-4 mt-4 mb-2",
            children: [
                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                    className: "row",
                    children: [
                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                            className: "col-sm-6 mt-2 mb-2",
                            children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("h4", {
                                children: "All Evaluations"
                            }, void 0, false, {
                                fileName: "src/components/IT20128036/admin/EvaluationList.js",
                                lineNumber: 131,
                                columnNumber: 13
                            }, this)
                        }, void 0, false, {
                            fileName: "src/components/IT20128036/admin/EvaluationList.js",
                            lineNumber: 130,
                            columnNumber: 11
                        }, this),
                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                            className: "col-sm-6 mt-2 mb-2",
                            children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("input", {
                                className: "form-control",
                                type: "search",
                                placeholder: "Search",
                                name: "searchQuery",
                                onChange: this.handleSearchArea
                            }, void 0, false, {
                                fileName: "src/components/IT20128036/admin/EvaluationList.js",
                                lineNumber: 134,
                                columnNumber: 13
                            }, this)
                        }, void 0, false, {
                            fileName: "src/components/IT20128036/admin/EvaluationList.js",
                            lineNumber: 133,
                            columnNumber: 11
                        }, this)
                    ]
                }, void 0, true, {
                    fileName: "src/components/IT20128036/admin/EvaluationList.js",
                    lineNumber: 129,
                    columnNumber: 9
                }, this),
                this.state.evaluations.map((evaluations, index)=>/*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                        class: "row",
                        children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                            class: "col-sm-12",
                            children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                class: "card mt-2 mb-2",
                                children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                    class: "card-body",
                                    children: [
                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                            class: "card-header",
                                            children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("h5", {
                                                class: "card-title",
                                                children: [
                                                    " Session ",
                                                    index + 1,
                                                    " "
                                                ]
                                            }, void 0, true, {
                                                fileName: "src/components/IT20128036/admin/EvaluationList.js",
                                                lineNumber: 150,
                                                columnNumber: 21
                                            }, this)
                                        }, void 0, false, {
                                            fileName: "src/components/IT20128036/admin/EvaluationList.js",
                                            lineNumber: 149,
                                            columnNumber: 19
                                        }, this),
                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("p", {
                                            class: "card-text",
                                            children: [
                                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("strong", {
                                                    children: [
                                                        evaluations.groupId,
                                                        " "
                                                    ]
                                                }, void 0, true, {
                                                    fileName: "src/components/IT20128036/admin/EvaluationList.js",
                                                    lineNumber: 154,
                                                    columnNumber: 21
                                                }, this),
                                                "Students, your",
                                                " ",
                                                evaluations.evaluationTopic,
                                                " session will be held on",
                                                " ",
                                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("strong", {
                                                    children: evaluations.date
                                                }, void 0, false, {
                                                    fileName: "src/components/IT20128036/admin/EvaluationList.js",
                                                    lineNumber: 156,
                                                    columnNumber: 21
                                                }, this),
                                                " from",
                                                " ",
                                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("strong", {
                                                    children: evaluations.from
                                                }, void 0, false, {
                                                    fileName: "src/components/IT20128036/admin/EvaluationList.js",
                                                    lineNumber: 157,
                                                    columnNumber: 21
                                                }, this),
                                                " to",
                                                " ",
                                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("strong", {
                                                    children: evaluations.to
                                                }, void 0, false, {
                                                    fileName: "src/components/IT20128036/admin/EvaluationList.js",
                                                    lineNumber: 158,
                                                    columnNumber: 21
                                                }, this),
                                                " with the",
                                                " ",
                                                evaluations.panel,
                                                ". Your participation is mandatory. You can connect via the link below.",
                                                " "
                                            ]
                                        }, void 0, true, {
                                            fileName: "src/components/IT20128036/admin/EvaluationList.js",
                                            lineNumber: 153,
                                            columnNumber: 19
                                        }, this),
                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                            className: "row",
                                            children: [
                                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                                    className: "col-lg-4",
                                                    children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("a", {
                                                        href: evaluations.link,
                                                        class: "btn btn-outline-success",
                                                        children: [
                                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("i", {
                                                                class: "fa fa-link",
                                                                "aria-hidden": "true"
                                                            }, void 0, false, {
                                                                fileName: "src/components/IT20128036/admin/EvaluationList.js",
                                                                lineNumber: 170,
                                                                columnNumber: 25
                                                            }, this),
                                                            "Join"
                                                        ]
                                                    }, void 0, true, {
                                                        fileName: "src/components/IT20128036/admin/EvaluationList.js",
                                                        lineNumber: 165,
                                                        columnNumber: 23
                                                    }, this)
                                                }, void 0, false, {
                                                    fileName: "src/components/IT20128036/admin/EvaluationList.js",
                                                    lineNumber: 164,
                                                    columnNumber: 21
                                                }, this),
                                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                                    className: "col-lg-4",
                                                    children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("a", {
                                                        href: `/evaluation/edit/${evaluations._id}`,
                                                        class: "btn btn-outline-primary",
                                                        children: [
                                                            " ",
                                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("i", {
                                                                class: "fa fa-pencil-square-o",
                                                                "aria-hidden": "true"
                                                            }, void 0, false, {
                                                                fileName: "src/components/IT20128036/admin/EvaluationList.js",
                                                                lineNumber: 180,
                                                                columnNumber: 25
                                                            }, this),
                                                            "Update"
                                                        ]
                                                    }, void 0, true, {
                                                        fileName: "src/components/IT20128036/admin/EvaluationList.js",
                                                        lineNumber: 175,
                                                        columnNumber: 23
                                                    }, this)
                                                }, void 0, false, {
                                                    fileName: "src/components/IT20128036/admin/EvaluationList.js",
                                                    lineNumber: 174,
                                                    columnNumber: 21
                                                }, this),
                                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                                    className: "col-lg-4",
                                                    children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("a", {
                                                        href: "#",
                                                        class: "btn btn-outline-danger",
                                                        onClick: ()=>{
                                                            this.onDelete(evaluations._id);
                                                        },
                                                        children: [
                                                            " ",
                                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("i", {
                                                                class: "fa fa-trash",
                                                                "aria-hidden": "true"
                                                            }, void 0, false, {
                                                                fileName: "src/components/IT20128036/admin/EvaluationList.js",
                                                                lineNumber: 193,
                                                                columnNumber: 25
                                                            }, this),
                                                            "Delete"
                                                        ]
                                                    }, void 0, true, {
                                                        fileName: "src/components/IT20128036/admin/EvaluationList.js",
                                                        lineNumber: 185,
                                                        columnNumber: 23
                                                    }, this)
                                                }, void 0, false, {
                                                    fileName: "src/components/IT20128036/admin/EvaluationList.js",
                                                    lineNumber: 184,
                                                    columnNumber: 21
                                                }, this)
                                            ]
                                        }, void 0, true, {
                                            fileName: "src/components/IT20128036/admin/EvaluationList.js",
                                            lineNumber: 163,
                                            columnNumber: 19
                                        }, this)
                                    ]
                                }, void 0, true, {
                                    fileName: "src/components/IT20128036/admin/EvaluationList.js",
                                    lineNumber: 148,
                                    columnNumber: 17
                                }, this)
                            }, void 0, false, {
                                fileName: "src/components/IT20128036/admin/EvaluationList.js",
                                lineNumber: 147,
                                columnNumber: 15
                            }, this)
                        }, void 0, false, {
                            fileName: "src/components/IT20128036/admin/EvaluationList.js",
                            lineNumber: 146,
                            columnNumber: 13
                        }, this)
                    }, void 0, false, {
                        fileName: "src/components/IT20128036/admin/EvaluationList.js",
                        lineNumber: 145,
                        columnNumber: 11
                    }, this)
                )
            ]
        }, void 0, true, {
            fileName: "src/components/IT20128036/admin/EvaluationList.js",
            lineNumber: 128,
            columnNumber: 7
        }, this);
    }
}
exports.default = EvaluationList;

  $parcel$ReactRefreshHelpers$c14e.postlude(module);
} finally {
  window.$RefreshReg$ = prevRefreshReg;
  window.$RefreshSig$ = prevRefreshSig;
}
},{"react/jsx-dev-runtime":"iTorj","react":"21dqq","axios":"jo6P5","./AddEvaluation":"62LVF","sweetalert":"38a65","jwt-decode":"ljM1w","@parcel/transformer-js/src/esmodule-helpers.js":"gkKU3","@parcel/transformer-react-refresh-wrap/lib/helpers/helpers.js":"km3Ru"}],"e1Dqj":[function(require,module,exports) {
var $parcel$ReactRefreshHelpers$dda2 = require("@parcel/transformer-react-refresh-wrap/lib/helpers/helpers.js");
var prevRefreshReg = window.$RefreshReg$;
var prevRefreshSig = window.$RefreshSig$;
$parcel$ReactRefreshHelpers$dda2.prelude(module);

try {
var parcelHelpers = require("@parcel/transformer-js/src/esmodule-helpers.js");
parcelHelpers.defineInteropFlag(exports);
var _jsxDevRuntime = require("react/jsx-dev-runtime");
var _react = require("react");
var _reactDefault = parcelHelpers.interopDefault(_react);
var _axios = require("axios");
var _axiosDefault = parcelHelpers.interopDefault(_axios);
var _sweetalert = require("sweetalert");
var _sweetalertDefault = parcelHelpers.interopDefault(_sweetalert);
class EditEvaluation extends _react.Component {
    constructor(props){
        super(props);
        this.state = {
            groupId: "",
            evaluationTopic: "",
            panel: "",
            date: "",
            from: "",
            to: "",
            link: ""
        };
    }
    handleInputChange = (e)=>{
        const { name , value  } = e.target;
        this.setState({
            ...this.state,
            [name]: value
        });
    };
    onSubmit = (e)=>{
        e.preventDefault();
        const id = this.props.match.params.id;
        const { groupId , evaluationTopic , panel , date , from , to , link  } = this.state;
        const data = {
            groupId: groupId,
            evaluationTopic: evaluationTopic,
            panel: panel,
            date: date,
            from: from,
            to: to,
            link: link
        };
        console.log(data);
        //update evaluation
        _axiosDefault.default.put(`http://localhost:5000/evaluation/update/${id}`, data).then((res)=>{
            if (res.data.success) {
                _sweetalertDefault.default("Evaluation Updated Successfully !", "", "success").then((value)=>{
                    window.location = "/evaluation/add";
                });
                // window.location = "/evaluation/add"
                this.setState({
                    groupId: "",
                    evaluationTopic: "",
                    panel: "",
                    date: "",
                    from: "",
                    to: "",
                    link: ""
                });
            }
        });
    };
    componentDidMount() {
        // if(this.props.match && this.props.match.params.id){
        const id = this.props.match.params.id;
        //retrive evaluation to update 
        _axiosDefault.default.get(`http://localhost:5000/evaluation/${id}`).then((res)=>{
            if (res.data.success) {
                this.setState({
                    groupId: res.data.evaluation.groupId,
                    evaluationTopic: res.data.evaluation.evaluationTopic,
                    panel: res.data.evaluation.panel,
                    date: res.data.evaluation.date,
                    from: res.data.evaluation.from,
                    to: res.data.evaluation.to,
                    link: res.data.evaluation.link
                });
                console.log(this.state);
            }
        });
    // }
    }
    render() {
        return /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
            className: "container",
            children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                className: "col-md-12 mt-4",
                children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                    className: "me-4 ms-4",
                    children: [
                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("h1", {
                            className: "h3 mb-3 font-weight-normal",
                            children: "Edit Evaluation"
                        }, void 0, false, {
                            fileName: "src/components/IT20128036/admin/editEvaluation.js",
                            lineNumber: 120,
                            columnNumber: 8
                        }, this),
                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("form", {
                            className: "needs-validation",
                            noValidate: true,
                            children: [
                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                    className: "row",
                                    children: [
                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                            className: "col-sm-6",
                                            children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                                className: "form-group",
                                                style: {
                                                    marginBottom: '15px'
                                                },
                                                children: [
                                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("label", {
                                                        style: {
                                                            marginBottom: '5px'
                                                        },
                                                        children: "Group Id"
                                                    }, void 0, false, {
                                                        fileName: "src/components/IT20128036/admin/editEvaluation.js",
                                                        lineNumber: 130,
                                                        columnNumber: 1
                                                    }, this),
                                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("input", {
                                                        type: "text",
                                                        className: "form-control",
                                                        name: "groupId",
                                                        placeholder: "Edit Group Id",
                                                        value: this.state.groupId,
                                                        onChange: this.handleInputChange
                                                    }, void 0, false, {
                                                        fileName: "src/components/IT20128036/admin/editEvaluation.js",
                                                        lineNumber: 131,
                                                        columnNumber: 12
                                                    }, this)
                                                ]
                                            }, void 0, true, {
                                                fileName: "src/components/IT20128036/admin/editEvaluation.js",
                                                lineNumber: 125,
                                                columnNumber: 10
                                            }, this)
                                        }, void 0, false, {
                                            fileName: "src/components/IT20128036/admin/editEvaluation.js",
                                            lineNumber: 124,
                                            columnNumber: 10
                                        }, this),
                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                            className: "col-sm-6",
                                            children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                                className: "form-group",
                                                style: {
                                                    marginBottom: '15px'
                                                },
                                                children: [
                                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("label", {
                                                        style: {
                                                            marginBottom: '5px'
                                                        },
                                                        children: "Evaluation Topic"
                                                    }, void 0, false, {
                                                        fileName: "src/components/IT20128036/admin/editEvaluation.js",
                                                        lineNumber: 146,
                                                        columnNumber: 1
                                                    }, this),
                                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("input", {
                                                        type: "text",
                                                        className: "form-control",
                                                        name: "evaluationTopic",
                                                        placeholder: "Edit Evaluation Topic",
                                                        value: this.state.evaluationTopic,
                                                        onChange: this.handleInputChange
                                                    }, void 0, false, {
                                                        fileName: "src/components/IT20128036/admin/editEvaluation.js",
                                                        lineNumber: 147,
                                                        columnNumber: 12
                                                    }, this)
                                                ]
                                            }, void 0, true, {
                                                fileName: "src/components/IT20128036/admin/editEvaluation.js",
                                                lineNumber: 143,
                                                columnNumber: 10
                                            }, this)
                                        }, void 0, false, {
                                            fileName: "src/components/IT20128036/admin/editEvaluation.js",
                                            lineNumber: 142,
                                            columnNumber: 10
                                        }, this)
                                    ]
                                }, void 0, true, {
                                    fileName: "src/components/IT20128036/admin/editEvaluation.js",
                                    lineNumber: 123,
                                    columnNumber: 10
                                }, this),
                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                    className: "row",
                                    children: [
                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                            className: "col-sm-6",
                                            children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                                className: "form-group",
                                                style: {
                                                    marginBottom: '15px'
                                                },
                                                children: [
                                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("label", {
                                                        style: {
                                                            marginBottom: '5px'
                                                        },
                                                        children: "Panel"
                                                    }, void 0, false, {
                                                        fileName: "src/components/IT20128036/admin/editEvaluation.js",
                                                        lineNumber: 168,
                                                        columnNumber: 12
                                                    }, this),
                                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("input", {
                                                        type: "text",
                                                        className: "form-control",
                                                        name: "panel",
                                                        placeholder: "Edit Panel",
                                                        value: this.state.panel,
                                                        onChange: this.handleInputChange
                                                    }, void 0, false, {
                                                        fileName: "src/components/IT20128036/admin/editEvaluation.js",
                                                        lineNumber: 169,
                                                        columnNumber: 12
                                                    }, this)
                                                ]
                                            }, void 0, true, {
                                                fileName: "src/components/IT20128036/admin/editEvaluation.js",
                                                lineNumber: 167,
                                                columnNumber: 14
                                            }, this)
                                        }, void 0, false, {
                                            fileName: "src/components/IT20128036/admin/editEvaluation.js",
                                            lineNumber: 166,
                                            columnNumber: 14
                                        }, this),
                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                            className: "col-sm-6",
                                            children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                                className: "form-group",
                                                style: {
                                                    marginBottom: '15px'
                                                },
                                                children: [
                                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("label", {
                                                        style: {
                                                            marginBottom: '5px'
                                                        },
                                                        children: "Date"
                                                    }, void 0, false, {
                                                        fileName: "src/components/IT20128036/admin/editEvaluation.js",
                                                        lineNumber: 183,
                                                        columnNumber: 12
                                                    }, this),
                                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("input", {
                                                        type: "date",
                                                        className: "form-control",
                                                        name: "date",
                                                        value: this.state.date,
                                                        onChange: this.handleInputChange
                                                    }, void 0, false, {
                                                        fileName: "src/components/IT20128036/admin/editEvaluation.js",
                                                        lineNumber: 184,
                                                        columnNumber: 12
                                                    }, this)
                                                ]
                                            }, void 0, true, {
                                                fileName: "src/components/IT20128036/admin/editEvaluation.js",
                                                lineNumber: 182,
                                                columnNumber: 10
                                            }, this)
                                        }, void 0, false, {
                                            fileName: "src/components/IT20128036/admin/editEvaluation.js",
                                            lineNumber: 180,
                                            columnNumber: 14
                                        }, this)
                                    ]
                                }, void 0, true, {
                                    fileName: "src/components/IT20128036/admin/editEvaluation.js",
                                    lineNumber: 163,
                                    columnNumber: 12
                                }, this),
                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                    className: "row",
                                    children: [
                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                            className: "col-sm-6",
                                            children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                                className: "form-group",
                                                style: {
                                                    marginBottom: '15px'
                                                },
                                                children: [
                                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("label", {
                                                        style: {
                                                            marginBottom: '5px'
                                                        },
                                                        children: "From"
                                                    }, void 0, false, {
                                                        fileName: "src/components/IT20128036/admin/editEvaluation.js",
                                                        lineNumber: 203,
                                                        columnNumber: 12
                                                    }, this),
                                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("input", {
                                                        type: "time",
                                                        className: "form-control",
                                                        name: "from",
                                                        value: this.state.from,
                                                        onChange: this.handleInputChange
                                                    }, void 0, false, {
                                                        fileName: "src/components/IT20128036/admin/editEvaluation.js",
                                                        lineNumber: 204,
                                                        columnNumber: 12
                                                    }, this)
                                                ]
                                            }, void 0, true, {
                                                fileName: "src/components/IT20128036/admin/editEvaluation.js",
                                                lineNumber: 202,
                                                columnNumber: 10
                                            }, this)
                                        }, void 0, false, {
                                            fileName: "src/components/IT20128036/admin/editEvaluation.js",
                                            lineNumber: 201,
                                            columnNumber: 10
                                        }, this),
                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                            className: "col-sm-6",
                                            children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                                className: "form-group",
                                                style: {
                                                    marginBottom: '15px'
                                                },
                                                children: [
                                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("label", {
                                                        style: {
                                                            marginBottom: '5px'
                                                        },
                                                        children: "To"
                                                    }, void 0, false, {
                                                        fileName: "src/components/IT20128036/admin/editEvaluation.js",
                                                        lineNumber: 219,
                                                        columnNumber: 12
                                                    }, this),
                                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("input", {
                                                        type: "time",
                                                        className: "form-control",
                                                        name: "to",
                                                        value: this.state.to,
                                                        onChange: this.handleInputChange
                                                    }, void 0, false, {
                                                        fileName: "src/components/IT20128036/admin/editEvaluation.js",
                                                        lineNumber: 220,
                                                        columnNumber: 12
                                                    }, this)
                                                ]
                                            }, void 0, true, {
                                                fileName: "src/components/IT20128036/admin/editEvaluation.js",
                                                lineNumber: 218,
                                                columnNumber: 10
                                            }, this)
                                        }, void 0, false, {
                                            fileName: "src/components/IT20128036/admin/editEvaluation.js",
                                            lineNumber: 216,
                                            columnNumber: 10
                                        }, this)
                                    ]
                                }, void 0, true, {
                                    fileName: "src/components/IT20128036/admin/editEvaluation.js",
                                    lineNumber: 199,
                                    columnNumber: 8
                                }, this),
                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                    className: "form-group",
                                    style: {
                                        marginBottom: '15px'
                                    },
                                    children: [
                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("label", {
                                            style: {
                                                marginBottom: '5px'
                                            },
                                            children: "Link"
                                        }, void 0, false, {
                                            fileName: "src/components/IT20128036/admin/editEvaluation.js",
                                            lineNumber: 242,
                                            columnNumber: 12
                                        }, this),
                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("input", {
                                            type: "text",
                                            className: "form-control",
                                            name: "link",
                                            placeholder: "Edit Link",
                                            value: this.state.link,
                                            onChange: this.handleInputChange
                                        }, void 0, false, {
                                            fileName: "src/components/IT20128036/admin/editEvaluation.js",
                                            lineNumber: 243,
                                            columnNumber: 12
                                        }, this)
                                    ]
                                }, void 0, true, {
                                    fileName: "src/components/IT20128036/admin/editEvaluation.js",
                                    lineNumber: 241,
                                    columnNumber: 10
                                }, this),
                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                    className: "row mt-4",
                                    children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                        className: "col-2",
                                        children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("button", {
                                            className: "btn btn-outline-success",
                                            type: "submit",
                                            style: {
                                                margintop: '15px'
                                            },
                                            onClick: this.onSubmit,
                                            children: [
                                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("i", {
                                                    className: "far fa-check-square"
                                                }, void 0, false, {
                                                    fileName: "src/components/IT20128036/admin/editEvaluation.js",
                                                    lineNumber: 260,
                                                    columnNumber: 12
                                                }, this),
                                                "\xa0 Update"
                                            ]
                                        }, void 0, true, {
                                            fileName: "src/components/IT20128036/admin/editEvaluation.js",
                                            lineNumber: 259,
                                            columnNumber: 3
                                        }, this)
                                    }, void 0, false, {
                                        fileName: "src/components/IT20128036/admin/editEvaluation.js",
                                        lineNumber: 258,
                                        columnNumber: 3
                                    }, this)
                                }, void 0, false, {
                                    fileName: "src/components/IT20128036/admin/editEvaluation.js",
                                    lineNumber: 257,
                                    columnNumber: 1
                                }, this)
                            ]
                        }, void 0, true, {
                            fileName: "src/components/IT20128036/admin/editEvaluation.js",
                            lineNumber: 121,
                            columnNumber: 8
                        }, this)
                    ]
                }, void 0, true, {
                    fileName: "src/components/IT20128036/admin/editEvaluation.js",
                    lineNumber: 119,
                    columnNumber: 8
                }, this)
            }, void 0, false, {
                fileName: "src/components/IT20128036/admin/editEvaluation.js",
                lineNumber: 118,
                columnNumber: 6
            }, this)
        }, void 0, false, {
            fileName: "src/components/IT20128036/admin/editEvaluation.js",
            lineNumber: 117,
            columnNumber: 1
        }, this);
    }
}
exports.default = EditEvaluation;

  $parcel$ReactRefreshHelpers$dda2.postlude(module);
} finally {
  window.$RefreshReg$ = prevRefreshReg;
  window.$RefreshSig$ = prevRefreshSig;
}
},{"react/jsx-dev-runtime":"iTorj","react":"21dqq","axios":"jo6P5","sweetalert":"38a65","@parcel/transformer-js/src/esmodule-helpers.js":"gkKU3","@parcel/transformer-react-refresh-wrap/lib/helpers/helpers.js":"km3Ru"}],"hepmj":[function(require,module,exports) {
var $parcel$ReactRefreshHelpers$bebb = require("@parcel/transformer-react-refresh-wrap/lib/helpers/helpers.js");
var prevRefreshReg = window.$RefreshReg$;
var prevRefreshSig = window.$RefreshSig$;
$parcel$ReactRefreshHelpers$bebb.prelude(module);

try {
var parcelHelpers = require("@parcel/transformer-js/src/esmodule-helpers.js");
parcelHelpers.defineInteropFlag(exports);
var _jsxDevRuntime = require("react/jsx-dev-runtime");
var _groupChatCss = require("../css/GroupChat.css");
var _socketIoClient = require("socket.io-client");
var _socketIoClientDefault = parcelHelpers.interopDefault(_socketIoClient);
var _react = require("react");
var _groupChat = require("./GroupChat");
var _groupChatDefault = parcelHelpers.interopDefault(_groupChat);
var _axios = require("axios");
var _axiosDefault = parcelHelpers.interopDefault(_axios);
var _jwtDecode = require("jwt-decode");
var _jwtDecodeDefault = parcelHelpers.interopDefault(_jwtDecode);
var _reactScrollToBottom = require("react-scroll-to-bottom");
var _reactScrollToBottomDefault = parcelHelpers.interopDefault(_reactScrollToBottom);
var _s = $RefreshSig$();
const socket = _socketIoClientDefault.default.connect("http://localhost:3001");
function GroupChatAdmin() {
    _s();
    const [username, setUsername] = _react.useState("");
    const [room, setRoom] = _react.useState("");
    const [showChat, setShowChat] = _react.useState(false);
    const [studentGroups, setStudentGroups] = _react.useState([]);
    const joinRoom = ()=>{
        if (username !== "" && room !== "") {
            socket.emit("join_room", room);
            setShowChat(true);
        }
    };
    _react.useEffect(()=>{
        const usertoken = localStorage.userToken;
        const decoded = _jwtDecodeDefault.default(usertoken);
        setUsername(decoded.name);
        _axiosDefault.default.get("http://localhost:5000/sgroups").then((res)=>{
            setStudentGroups(res.data.existingstudentgroups);
        });
        console.log(studentGroups);
    }, []);
    //   const getStudentGroups = () => {
    //     axios.get("http://localhost:5000/sgroups")
    //     .then(res => {setStudentGroups(res.data.existingstudentgroups)});
    //     console.log(studentGroups);
    //   }
    return /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
        className: "container",
        children: [
            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("br", {}, void 0, false, {
                fileName: "src/components/IT20127046/user/GroupChat_Admin.js",
                lineNumber: 44,
                columnNumber: 7
            }, this),
            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                className: "col-lg-9 mt-0 mb-2",
                children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("h2", {
                    children: "Group Chat"
                }, void 0, false, {
                    fileName: "src/components/IT20127046/user/GroupChat_Admin.js",
                    lineNumber: 46,
                    columnNumber: 9
                }, this)
            }, void 0, false, {
                fileName: "src/components/IT20127046/user/GroupChat_Admin.js",
                lineNumber: 45,
                columnNumber: 7
            }, this),
            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("hr", {}, void 0, false, {
                fileName: "src/components/IT20127046/user/GroupChat_Admin.js",
                lineNumber: 48,
                columnNumber: 7
            }, this),
            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                className: "container chatBox",
                children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                    className: "row text-center groupSeletePanal",
                    children: [
                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                            className: "col-3 bg-dark text-white detailsPanel",
                            children: [
                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("br", {}, void 0, false, {
                                    fileName: "src/components/IT20127046/user/GroupChat_Admin.js",
                                    lineNumber: 52,
                                    columnNumber: 66
                                }, this),
                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("h5", {
                                    children: "Student Groups"
                                }, void 0, false, {
                                    fileName: "src/components/IT20127046/user/GroupChat_Admin.js",
                                    lineNumber: 53,
                                    columnNumber: 13
                                }, this),
                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("br", {}, void 0, false, {
                                    fileName: "src/components/IT20127046/user/GroupChat_Admin.js",
                                    lineNumber: 53,
                                    columnNumber: 36
                                }, this),
                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("input", {
                                    type: "text",
                                    className: "from-control",
                                    placeholder: "Search"
                                }, void 0, false, {
                                    fileName: "src/components/IT20127046/user/GroupChat_Admin.js",
                                    lineNumber: 55,
                                    columnNumber: 13
                                }, this),
                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("br", {}, void 0, false, {
                                    fileName: "src/components/IT20127046/user/GroupChat_Admin.js",
                                    lineNumber: 59,
                                    columnNumber: 15
                                }, this),
                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("br", {}, void 0, false, {
                                    fileName: "src/components/IT20127046/user/GroupChat_Admin.js",
                                    lineNumber: 59,
                                    columnNumber: 20
                                }, this),
                                !showChat ? /*#__PURE__*/ _jsxDevRuntime.jsxDEV("button", {
                                    className: "btn btn-danger",
                                    onClick: ()=>{
                                        setShowChat(false);
                                    },
                                    disabled: true,
                                    children: "Leave Chat"
                                }, void 0, false, {
                                    fileName: "src/components/IT20127046/user/GroupChat_Admin.js",
                                    lineNumber: 62,
                                    columnNumber: 15
                                }, this) : /*#__PURE__*/ _jsxDevRuntime.jsxDEV("button", {
                                    className: "btn btn-danger",
                                    onClick: ()=>{
                                        setShowChat(false);
                                    },
                                    children: "Leave Chat"
                                }, void 0, false, {
                                    fileName: "src/components/IT20127046/user/GroupChat_Admin.js",
                                    lineNumber: 64,
                                    columnNumber: 15
                                }, this),
                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV(_reactScrollToBottomDefault.default, {
                                    className: "message-container-groupList",
                                    children: studentGroups.map((data, index)=>{
                                        return /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                            className: "getGroupList",
                                            children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("button", {
                                                onClick: ()=>{
                                                    setRoom(data.groupid);
                                                },
                                                style: {
                                                    background: "none",
                                                    border: "none",
                                                    margin: "0px 5px 5px 5px",
                                                    color: "white"
                                                },
                                                children: data.groupid
                                            }, void 0, false, {
                                                fileName: "src/components/IT20127046/user/GroupChat_Admin.js",
                                                lineNumber: 72,
                                                columnNumber: 29
                                            }, this)
                                        }, index, false, {
                                            fileName: "src/components/IT20127046/user/GroupChat_Admin.js",
                                            lineNumber: 71,
                                            columnNumber: 25
                                        }, this);
                                    })
                                }, void 0, false, {
                                    fileName: "src/components/IT20127046/user/GroupChat_Admin.js",
                                    lineNumber: 68,
                                    columnNumber: 13
                                }, this)
                            ]
                        }, void 0, true, {
                            fileName: "src/components/IT20127046/user/GroupChat_Admin.js",
                            lineNumber: 52,
                            columnNumber: 11
                        }, this),
                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                            className: "col-9 p-3 mb-2 bg-light text-dark",
                            children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                className: "d-flex justify-content-center chatBoxAlign",
                                children: !showChat ? /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                    className: "chatInput",
                                    children: [
                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("br", {}, void 0, false, {
                                            fileName: "src/components/IT20127046/user/GroupChat_Admin.js",
                                            lineNumber: 84,
                                            columnNumber: 44
                                        }, this),
                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("br", {}, void 0, false, {
                                            fileName: "src/components/IT20127046/user/GroupChat_Admin.js",
                                            lineNumber: 84,
                                            columnNumber: 49
                                        }, this),
                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("br", {}, void 0, false, {
                                            fileName: "src/components/IT20127046/user/GroupChat_Admin.js",
                                            lineNumber: 84,
                                            columnNumber: 54
                                        }, this),
                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("h3", {
                                            children: "Join A Group Chat"
                                        }, void 0, false, {
                                            fileName: "src/components/IT20127046/user/GroupChat_Admin.js",
                                            lineNumber: 85,
                                            columnNumber: 19
                                        }, this),
                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("br", {}, void 0, false, {
                                            fileName: "src/components/IT20127046/user/GroupChat_Admin.js",
                                            lineNumber: 85,
                                            columnNumber: 45
                                        }, this),
                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("input", {
                                            type: "text",
                                            placeholder: "Name",
                                            className: "form-control",
                                            value: username,
                                            onChange: (event)=>{
                                                setUsername(event.target.value);
                                            }
                                        }, void 0, false, {
                                            fileName: "src/components/IT20127046/user/GroupChat_Admin.js",
                                            lineNumber: 86,
                                            columnNumber: 19
                                        }, this),
                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("br", {}, void 0, false, {
                                            fileName: "src/components/IT20127046/user/GroupChat_Admin.js",
                                            lineNumber: 94,
                                            columnNumber: 21
                                        }, this),
                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("input", {
                                            type: "text",
                                            placeholder: "Group ID",
                                            className: "form-control",
                                            value: room,
                                            onChange: (event)=>{
                                                setRoom(event.target.value);
                                            }
                                        }, void 0, false, {
                                            fileName: "src/components/IT20127046/user/GroupChat_Admin.js",
                                            lineNumber: 95,
                                            columnNumber: 19
                                        }, this),
                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("br", {}, void 0, false, {
                                            fileName: "src/components/IT20127046/user/GroupChat_Admin.js",
                                            lineNumber: 103,
                                            columnNumber: 21
                                        }, this),
                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("button", {
                                            onClick: joinRoom,
                                            className: "btn btn-outline-info",
                                            children: "Join with Group"
                                        }, void 0, false, {
                                            fileName: "src/components/IT20127046/user/GroupChat_Admin.js",
                                            lineNumber: 104,
                                            columnNumber: 19
                                        }, this)
                                    ]
                                }, void 0, true, {
                                    fileName: "src/components/IT20127046/user/GroupChat_Admin.js",
                                    lineNumber: 84,
                                    columnNumber: 17
                                }, this) : /*#__PURE__*/ _jsxDevRuntime.jsxDEV(_groupChatDefault.default, {
                                    socket: socket,
                                    username: username,
                                    room: room
                                }, void 0, false, {
                                    fileName: "src/components/IT20127046/user/GroupChat_Admin.js",
                                    lineNumber: 107,
                                    columnNumber: 17
                                }, this)
                            }, void 0, false, {
                                fileName: "src/components/IT20127046/user/GroupChat_Admin.js",
                                lineNumber: 82,
                                columnNumber: 13
                            }, this)
                        }, void 0, false, {
                            fileName: "src/components/IT20127046/user/GroupChat_Admin.js",
                            lineNumber: 81,
                            columnNumber: 11
                        }, this)
                    ]
                }, void 0, true, {
                    fileName: "src/components/IT20127046/user/GroupChat_Admin.js",
                    lineNumber: 51,
                    columnNumber: 9
                }, this)
            }, void 0, false, {
                fileName: "src/components/IT20127046/user/GroupChat_Admin.js",
                lineNumber: 50,
                columnNumber: 7
            }, this)
        ]
    }, void 0, true, {
        fileName: "src/components/IT20127046/user/GroupChat_Admin.js",
        lineNumber: 43,
        columnNumber: 5
    }, this);
}
exports.default = GroupChatAdmin;
_s(GroupChatAdmin, "kvVSrNwsFGDoWShK3HB35SNWtmE=");
_c = GroupChatAdmin;
var _c;
$RefreshReg$(_c, "GroupChatAdmin");

  $parcel$ReactRefreshHelpers$bebb.postlude(module);
} finally {
  window.$RefreshReg$ = prevRefreshReg;
  window.$RefreshSig$ = prevRefreshSig;
}
},{"react/jsx-dev-runtime":"iTorj","../css/GroupChat.css":"5wanh","socket.io-client":"8HBJR","react":"21dqq","./GroupChat":"8GZZG","axios":"jo6P5","jwt-decode":"ljM1w","react-scroll-to-bottom":"ht5tR","@parcel/transformer-js/src/esmodule-helpers.js":"gkKU3","@parcel/transformer-react-refresh-wrap/lib/helpers/helpers.js":"km3Ru"}],"5wanh":[function() {},{}],"fE618":[function(require,module,exports) {
var $parcel$ReactRefreshHelpers$8a24 = require("@parcel/transformer-react-refresh-wrap/lib/helpers/helpers.js");
var prevRefreshReg = window.$RefreshReg$;
var prevRefreshSig = window.$RefreshSig$;
$parcel$ReactRefreshHelpers$8a24.prelude(module);

try {
var parcelHelpers = require("@parcel/transformer-js/src/esmodule-helpers.js");
parcelHelpers.defineInteropFlag(exports);
var _jsxDevRuntime = require("react/jsx-dev-runtime");
var _react = require("react");
var _reactDefault = parcelHelpers.interopDefault(_react);
var _axios = require("axios");
var _axiosDefault = parcelHelpers.interopDefault(_axios);
var _adminNavBar = require("../../IT20125202/admin/AdminNavBar");
var _adminNavBarDefault = parcelHelpers.interopDefault(_adminNavBar);
var _sweetalert = require("sweetalert");
var _sweetalertDefault = parcelHelpers.interopDefault(_sweetalert);
class CreateMarkingSchem extends _react.Component {
    constructor(props){
        super(props);
        this.state = {
            moduleName: "",
            assignment: "",
            criteria: "",
            allocateMark: "",
            markingSchemTitleID: "",
            markingCriteria: [],
            addCriteriaBtn: true
        };
    }
    handleInputChange = (e)=>{
        const { name , value  } = e.target;
        this.setState({
            ...this.state,
            [name]: value
        });
    };
    handleChange = (e)=>{
        const { name , value  } = e.target;
        this.setState({
            ...this.state,
            [name]: value
        });
    };
    onSubmitTitle = (e)=>{
        e.preventDefault();
        const { moduleName , assignment  } = this.state;
        const data = {
            moduleName: moduleName,
            assignment: assignment
        };
        console.log(data);
        _axiosDefault.default.post("http://localhost:5000/add/markingTitle", data).then((res)=>{
            if (res.data.success) {
                console.log("Title Add Successfully" + res.data.saveTitle);
                this.setState({
                    markingSchemTitleID: res.data.saveTitle,
                    addCriteriaBtn: false
                });
            }
        });
    };
    onSubmit = (e)=>{
        e.preventDefault();
        const { markingSchemTitleID , criteria , allocateMark  } = this.state;
        const data = {
            markingSchemTitleID: markingSchemTitleID,
            criteria: criteria,
            allocateMark: allocateMark
        };
        console.log(data);
        _axiosDefault.default.post("http://localhost:5000/add/marking", data).then((res)=>{
            if (res.data.success) {
                alert("Hotel Created Successfully");
                this.setState({
                    criteria: "",
                    allocateMark: ""
                });
            }
        });
        setTimeout(()=>{
            this.getMarkingCriteria();
        }, 1000);
    };
    getMarkingCriteria() {
        const titleID = this.state.markingSchemTitleID;
        _axiosDefault.default.get(`http://localhost:5000/markings/get/${titleID}`).then((res)=>{
            if (res.data.success) this.setState({
                markingCriteria: res.data.existingMarkingCriteria
            });
        });
    }
    onDelete = (id)=>{
        _sweetalertDefault.default({
            title: "Are you sure?",
            text: "Delete this Marking Scheme Criteria",
            icon: "warning",
            buttons: true,
            dangerMode: true
        }).then((willDelete)=>{
            if (willDelete) _axiosDefault.default.delete(`http://localhost:5000/makingCriteria/delete/${id}`).then((res)=>{
                _sweetalertDefault.default("Deleted Successfull!", {
                    icon: "success"
                });
                setTimeout(()=>{
                    this.retrieveTitles();
                }, 1000);
            });
        });
    };
    // retriveCriteria() {
    //     axios.get("http://localhost:5000/get/markings").then((res) => {
    //         if (res.data.success) {
    //             this.setState({
    //                 markingCriteria: res.data.existingMarkingDetails,
    //             });
    //             console.log(this.state.markingCriteria);
    //         }
    //     });
    // }
    render() {
        return /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
            className: "container",
            children: [
                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("br", {}, void 0, false, {
                    fileName: "src/components/IT20127046/admin/CreateMarkingSchem.js",
                    lineNumber: 139,
                    columnNumber: 9
                }, this),
                /*#__PURE__*/ _jsxDevRuntime.jsxDEV(_adminNavBarDefault.default, {}, void 0, false, {
                    fileName: "src/components/IT20127046/admin/CreateMarkingSchem.js",
                    lineNumber: 140,
                    columnNumber: 9
                }, this),
                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("br", {}, void 0, false, {
                    fileName: "src/components/IT20127046/admin/CreateMarkingSchem.js",
                    lineNumber: 141,
                    columnNumber: 9
                }, this),
                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("h4", {
                    children: "Create Marking Schem"
                }, void 0, false, {
                    fileName: "src/components/IT20127046/admin/CreateMarkingSchem.js",
                    lineNumber: 142,
                    columnNumber: 9
                }, this),
                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("hr", {}, void 0, false, {
                    fileName: "src/components/IT20127046/admin/CreateMarkingSchem.js",
                    lineNumber: 143,
                    columnNumber: 9
                }, this),
                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                    className: "container p-3 mb-2 bg-light text-dark",
                    children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                        className: "row",
                        children: [
                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                className: "col",
                                children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                    className: "border border-secondary m-2 p-4 rounded",
                                    children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("form", {
                                        onSubmit: this.onSubmitTitle,
                                        children: [
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("h4", {
                                                children: "Set Marking Schem Title"
                                            }, void 0, false, {
                                                fileName: "src/components/IT20127046/admin/CreateMarkingSchem.js",
                                                lineNumber: 150,
                                                columnNumber: 19
                                            }, this),
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("label", {
                                                htmlFor: "moduleName",
                                                children: "Module"
                                            }, void 0, false, {
                                                fileName: "src/components/IT20127046/admin/CreateMarkingSchem.js",
                                                lineNumber: 152,
                                                columnNumber: 19
                                            }, this),
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("input", {
                                                style: {
                                                    width: "400px"
                                                },
                                                type: "text",
                                                className: "form-control",
                                                id: "moduleName",
                                                name: "moduleName",
                                                value: this.state.moduleName,
                                                onChange: this.handleInputChange
                                            }, void 0, false, {
                                                fileName: "src/components/IT20127046/admin/CreateMarkingSchem.js",
                                                lineNumber: 153,
                                                columnNumber: 19
                                            }, this),
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("label", {
                                                htmlFor: "assignment",
                                                children: "Assignment"
                                            }, void 0, false, {
                                                fileName: "src/components/IT20127046/admin/CreateMarkingSchem.js",
                                                lineNumber: 163,
                                                columnNumber: 19
                                            }, this),
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("input", {
                                                style: {
                                                    width: "400px"
                                                },
                                                type: "text",
                                                className: "form-control",
                                                id: "assignment",
                                                name: "assignment",
                                                value: this.state.assignment,
                                                onChange: this.handleInputChange
                                            }, void 0, false, {
                                                fileName: "src/components/IT20127046/admin/CreateMarkingSchem.js",
                                                lineNumber: 164,
                                                columnNumber: 19
                                            }, this),
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("button", {
                                                type: "submit",
                                                className: "btn btn-outline-primary mt-2 mb-2",
                                                children: [
                                                    "Set Title\xa0 ",
                                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("i", {
                                                        class: "fa fa-plus",
                                                        "aria-hidden": "true"
                                                    }, void 0, false, {
                                                        fileName: "src/components/IT20127046/admin/CreateMarkingSchem.js",
                                                        lineNumber: 175,
                                                        columnNumber: 37
                                                    }, this)
                                                ]
                                            }, void 0, true, {
                                                fileName: "src/components/IT20127046/admin/CreateMarkingSchem.js",
                                                lineNumber: 174,
                                                columnNumber: 19
                                            }, this)
                                        ]
                                    }, void 0, true, {
                                        fileName: "src/components/IT20127046/admin/CreateMarkingSchem.js",
                                        lineNumber: 149,
                                        columnNumber: 17
                                    }, this)
                                }, void 0, false, {
                                    fileName: "src/components/IT20127046/admin/CreateMarkingSchem.js",
                                    lineNumber: 148,
                                    columnNumber: 15
                                }, this)
                            }, void 0, false, {
                                fileName: "src/components/IT20127046/admin/CreateMarkingSchem.js",
                                lineNumber: 147,
                                columnNumber: 13
                            }, this),
                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                className: "col",
                                children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                    className: "border border-secondary m-2 p-4 rounded",
                                    children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("form", {
                                        onSubmit: this.onSubmit,
                                        children: [
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("h4", {
                                                children: "Set Marking Scheme Criteria"
                                            }, void 0, false, {
                                                fileName: "src/components/IT20127046/admin/CreateMarkingSchem.js",
                                                lineNumber: 183,
                                                columnNumber: 19
                                            }, this),
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("label", {
                                                htmlFor: "criteria",
                                                children: "Criteria"
                                            }, void 0, false, {
                                                fileName: "src/components/IT20127046/admin/CreateMarkingSchem.js",
                                                lineNumber: 185,
                                                columnNumber: 19
                                            }, this),
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("input", {
                                                style: {
                                                    width: "400px"
                                                },
                                                type: "text",
                                                className: "form-control",
                                                id: "criteria",
                                                name: "criteria",
                                                value: this.state.criteria,
                                                onChange: this.handleInputChange
                                            }, void 0, false, {
                                                fileName: "src/components/IT20127046/admin/CreateMarkingSchem.js",
                                                lineNumber: 186,
                                                columnNumber: 19
                                            }, this),
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("label", {
                                                htmlFor: "allocateMark",
                                                children: "Allocate Mark"
                                            }, void 0, false, {
                                                fileName: "src/components/IT20127046/admin/CreateMarkingSchem.js",
                                                lineNumber: 196,
                                                columnNumber: 19
                                            }, this),
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("input", {
                                                style: {
                                                    width: "400px"
                                                },
                                                type: "text",
                                                className: "form-control",
                                                id: "allocateMark",
                                                name: "allocateMark",
                                                value: this.state.allocateMark,
                                                onChange: this.handleInputChange
                                            }, void 0, false, {
                                                fileName: "src/components/IT20127046/admin/CreateMarkingSchem.js",
                                                lineNumber: 197,
                                                columnNumber: 19
                                            }, this),
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("button", {
                                                type: "submit",
                                                className: "btn btn-outline-primary mt-2 mb-2",
                                                disabled: this.state.addCriteriaBtn,
                                                children: [
                                                    "Add Criteria\xa0 ",
                                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("i", {
                                                        class: "fa fa-plus",
                                                        "aria-hidden": "true"
                                                    }, void 0, false, {
                                                        fileName: "src/components/IT20127046/admin/CreateMarkingSchem.js",
                                                        lineNumber: 208,
                                                        columnNumber: 40
                                                    }, this)
                                                ]
                                            }, void 0, true, {
                                                fileName: "src/components/IT20127046/admin/CreateMarkingSchem.js",
                                                lineNumber: 207,
                                                columnNumber: 19
                                            }, this)
                                        ]
                                    }, void 0, true, {
                                        fileName: "src/components/IT20127046/admin/CreateMarkingSchem.js",
                                        lineNumber: 182,
                                        columnNumber: 17
                                    }, this)
                                }, void 0, false, {
                                    fileName: "src/components/IT20127046/admin/CreateMarkingSchem.js",
                                    lineNumber: 181,
                                    columnNumber: 15
                                }, this)
                            }, void 0, false, {
                                fileName: "src/components/IT20127046/admin/CreateMarkingSchem.js",
                                lineNumber: 180,
                                columnNumber: 13
                            }, this)
                        ]
                    }, void 0, true, {
                        fileName: "src/components/IT20127046/admin/CreateMarkingSchem.js",
                        lineNumber: 146,
                        columnNumber: 11
                    }, this)
                }, void 0, false, {
                    fileName: "src/components/IT20127046/admin/CreateMarkingSchem.js",
                    lineNumber: 145,
                    columnNumber: 9
                }, this),
                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("center", {
                    children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("a", {
                        className: "btn btn-outline-success m-2",
                        href: "/view/marking",
                        children: "Submit Marking Scheme"
                    }, void 0, false, {
                        fileName: "src/components/IT20127046/admin/CreateMarkingSchem.js",
                        lineNumber: 217,
                        columnNumber: 11
                    }, this)
                }, void 0, false, {
                    fileName: "src/components/IT20127046/admin/CreateMarkingSchem.js",
                    lineNumber: 216,
                    columnNumber: 9
                }, this),
                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("br", {}, void 0, false, {
                    fileName: "src/components/IT20127046/admin/CreateMarkingSchem.js",
                    lineNumber: 220,
                    columnNumber: 9
                }, this),
                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                    className: "container border border-secondary",
                    children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                        className: "container p-4",
                        children: [
                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("center", {
                                children: [
                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("h5", {
                                        children: "Marking Schem"
                                    }, void 0, false, {
                                        fileName: "src/components/IT20127046/admin/CreateMarkingSchem.js",
                                        lineNumber: 224,
                                        columnNumber: 13
                                    }, this),
                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("h5", {
                                        children: this.state.moduleName
                                    }, void 0, false, {
                                        fileName: "src/components/IT20127046/admin/CreateMarkingSchem.js",
                                        lineNumber: 225,
                                        columnNumber: 13
                                    }, this),
                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("h6", {
                                        children: this.state.assignment
                                    }, void 0, false, {
                                        fileName: "src/components/IT20127046/admin/CreateMarkingSchem.js",
                                        lineNumber: 226,
                                        columnNumber: 13
                                    }, this)
                                ]
                            }, void 0, true, {
                                fileName: "src/components/IT20127046/admin/CreateMarkingSchem.js",
                                lineNumber: 223,
                                columnNumber: 11
                            }, this),
                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("table", {
                                className: "table",
                                children: [
                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("thead", {
                                        children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("tr", {
                                            className: "b ",
                                            children: [
                                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("th", {
                                                    scope: "col",
                                                    children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("center", {
                                                        children: "No"
                                                    }, void 0, false, {
                                                        fileName: "src/components/IT20127046/admin/CreateMarkingSchem.js",
                                                        lineNumber: 233,
                                                        columnNumber: 19
                                                    }, this)
                                                }, void 0, false, {
                                                    fileName: "src/components/IT20127046/admin/CreateMarkingSchem.js",
                                                    lineNumber: 232,
                                                    columnNumber: 17
                                                }, this),
                                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("th", {
                                                    scope: "col",
                                                    children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("center", {
                                                        children: "Criteris"
                                                    }, void 0, false, {
                                                        fileName: "src/components/IT20127046/admin/CreateMarkingSchem.js",
                                                        lineNumber: 236,
                                                        columnNumber: 19
                                                    }, this)
                                                }, void 0, false, {
                                                    fileName: "src/components/IT20127046/admin/CreateMarkingSchem.js",
                                                    lineNumber: 235,
                                                    columnNumber: 17
                                                }, this),
                                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("th", {
                                                    scope: "col",
                                                    children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("center", {
                                                        children: "Allocate Mark"
                                                    }, void 0, false, {
                                                        fileName: "src/components/IT20127046/admin/CreateMarkingSchem.js",
                                                        lineNumber: 239,
                                                        columnNumber: 19
                                                    }, this)
                                                }, void 0, false, {
                                                    fileName: "src/components/IT20127046/admin/CreateMarkingSchem.js",
                                                    lineNumber: 238,
                                                    columnNumber: 17
                                                }, this)
                                            ]
                                        }, void 0, true, {
                                            fileName: "src/components/IT20127046/admin/CreateMarkingSchem.js",
                                            lineNumber: 231,
                                            columnNumber: 15
                                        }, this)
                                    }, void 0, false, {
                                        fileName: "src/components/IT20127046/admin/CreateMarkingSchem.js",
                                        lineNumber: 230,
                                        columnNumber: 13
                                    }, this),
                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("tbody", {
                                        children: this.state.markingCriteria.map((data, index)=>/*#__PURE__*/ _jsxDevRuntime.jsxDEV("tr", {
                                                children: [
                                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("th", {
                                                        children: index + 1
                                                    }, void 0, false, {
                                                        fileName: "src/components/IT20127046/admin/CreateMarkingSchem.js",
                                                        lineNumber: 247,
                                                        columnNumber: 19
                                                    }, this),
                                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("td", {
                                                        children: data.criteria
                                                    }, void 0, false, {
                                                        fileName: "src/components/IT20127046/admin/CreateMarkingSchem.js",
                                                        lineNumber: 248,
                                                        columnNumber: 19
                                                    }, this),
                                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("td", {
                                                        children: [
                                                            data.allocateMark,
                                                            " "
                                                        ]
                                                    }, void 0, true, {
                                                        fileName: "src/components/IT20127046/admin/CreateMarkingSchem.js",
                                                        lineNumber: 249,
                                                        columnNumber: 19
                                                    }, this),
                                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("td", {
                                                        className: "text-center",
                                                        children: [
                                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("a", {
                                                                className: "btn btn-outline-success",
                                                                href: ``,
                                                                children: [
                                                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("i", {
                                                                        className: "fa fa-edit"
                                                                    }, void 0, false, {
                                                                        fileName: "src/components/IT20127046/admin/CreateMarkingSchem.js",
                                                                        lineNumber: 253,
                                                                        columnNumber: 23
                                                                    }, this),
                                                                    "\xa0Edit"
                                                                ]
                                                            }, void 0, true, {
                                                                fileName: "src/components/IT20127046/admin/CreateMarkingSchem.js",
                                                                lineNumber: 252,
                                                                columnNumber: 21
                                                            }, this),
                                                            "\xa0",
                                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("a", {
                                                                className: "btn btn-outline-danger",
                                                                href: "#",
                                                                onClick: ()=>this.onDelete(data._id)
                                                                ,
                                                                children: [
                                                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("i", {
                                                                        className: "fa fa-trash"
                                                                    }, void 0, false, {
                                                                        fileName: "src/components/IT20127046/admin/CreateMarkingSchem.js",
                                                                        lineNumber: 261,
                                                                        columnNumber: 23
                                                                    }, this),
                                                                    "\xa0Delete"
                                                                ]
                                                            }, void 0, true, {
                                                                fileName: "src/components/IT20127046/admin/CreateMarkingSchem.js",
                                                                lineNumber: 256,
                                                                columnNumber: 21
                                                            }, this)
                                                        ]
                                                    }, void 0, true, {
                                                        fileName: "src/components/IT20127046/admin/CreateMarkingSchem.js",
                                                        lineNumber: 251,
                                                        columnNumber: 19
                                                    }, this)
                                                ]
                                            }, index, true, {
                                                fileName: "src/components/IT20127046/admin/CreateMarkingSchem.js",
                                                lineNumber: 246,
                                                columnNumber: 17
                                            }, this)
                                        )
                                    }, void 0, false, {
                                        fileName: "src/components/IT20127046/admin/CreateMarkingSchem.js",
                                        lineNumber: 244,
                                        columnNumber: 13
                                    }, this)
                                ]
                            }, void 0, true, {
                                fileName: "src/components/IT20127046/admin/CreateMarkingSchem.js",
                                lineNumber: 229,
                                columnNumber: 11
                            }, this),
                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("br", {}, void 0, false, {
                                fileName: "src/components/IT20127046/admin/CreateMarkingSchem.js",
                                lineNumber: 268,
                                columnNumber: 11
                            }, this),
                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("br", {}, void 0, false, {
                                fileName: "src/components/IT20127046/admin/CreateMarkingSchem.js",
                                lineNumber: 269,
                                columnNumber: 11
                            }, this)
                        ]
                    }, void 0, true, {
                        fileName: "src/components/IT20127046/admin/CreateMarkingSchem.js",
                        lineNumber: 222,
                        columnNumber: 11
                    }, this)
                }, void 0, false, {
                    fileName: "src/components/IT20127046/admin/CreateMarkingSchem.js",
                    lineNumber: 221,
                    columnNumber: 9
                }, this)
            ]
        }, void 0, true, {
            fileName: "src/components/IT20127046/admin/CreateMarkingSchem.js",
            lineNumber: 138,
            columnNumber: 7
        }, this);
    }
}
exports.default = CreateMarkingSchem;

  $parcel$ReactRefreshHelpers$8a24.postlude(module);
} finally {
  window.$RefreshReg$ = prevRefreshReg;
  window.$RefreshSig$ = prevRefreshSig;
}
},{"react/jsx-dev-runtime":"iTorj","react":"21dqq","axios":"jo6P5","../../IT20125202/admin/AdminNavBar":"kb2lK","sweetalert":"38a65","@parcel/transformer-js/src/esmodule-helpers.js":"gkKU3","@parcel/transformer-react-refresh-wrap/lib/helpers/helpers.js":"km3Ru"}],"575e8":[function(require,module,exports) {
var $parcel$ReactRefreshHelpers$2324 = require("@parcel/transformer-react-refresh-wrap/lib/helpers/helpers.js");
var prevRefreshReg = window.$RefreshReg$;
var prevRefreshSig = window.$RefreshSig$;
$parcel$ReactRefreshHelpers$2324.prelude(module);

try {
var parcelHelpers = require("@parcel/transformer-js/src/esmodule-helpers.js");
parcelHelpers.defineInteropFlag(exports);
var _jsxDevRuntime = require("react/jsx-dev-runtime");
var _react = require("react");
var _reactDefault = parcelHelpers.interopDefault(_react);
var _axios = require("axios");
var _axiosDefault = parcelHelpers.interopDefault(_axios);
var _sweetalert = require("sweetalert");
var _sweetalertDefault = parcelHelpers.interopDefault(_sweetalert);
var _jsFileDownload = require("js-file-download");
var _jsFileDownloadDefault = parcelHelpers.interopDefault(_jsFileDownload);
class ViewSubmitions extends _react.Component {
    constructor(props){
        super(props);
        this.state = {
            submitions: []
        };
    }
    componentDidMount() {
        this.retrieveSubmitions();
    }
    // To get all submition data
    retrieveSubmitions() {
        _axiosDefault.default.get("http://localhost:5000/submition/all").then((res)=>{
            if (res.data.success) {
                this.setState({
                    submitions: res.data.exsitingSubmitions
                });
                console.log(this.state.submitions);
            }
        });
    }
    //filter submitions
    filterData(submitions, searchKey) {
        const result = submitions.filter((submition)=>submition.groupId.toLowerCase().includes(searchKey) || submition.type.toLowerCase().includes(searchKey) || submition.files.toLowerCase().includes(searchKey) || submition.groupId.includes(searchKey) || submition.type.includes(searchKey) || submition.files.includes(searchKey)
        );
        this.setState({
            submitions: result
        });
    }
    //search submitions    
    handleSearchArea = (e)=>{
        const searchKey = e.currentTarget.value;
        _axiosDefault.default.get("http://localhost:5000/submition/all").then((res)=>{
            if (res.data.success) this.filterData(res.data.exsitingSubmitions, searchKey);
        });
    };
    // Download File
    downloadFile = (fileName)=>{
        const data = {
            fileName: fileName
        };
        _axiosDefault.default({
            url: "http://localhost:5000/file/download",
            data,
            method: "POST",
            responseType: "blob"
        }).then((res)=>{
            console.log(res);
            _jsFileDownloadDefault.default(res.data, fileName);
        });
    };
    //delete submitions        
    onDelete = (id)=>{
        _sweetalertDefault.default({
            title: "Are you sure?",
            text: "Once deleted, you will not be able to recover this file!",
            icon: "warning",
            buttons: true,
            dangerMode: true
        }).then((willDelete)=>{
            if (willDelete) {
                _axiosDefault.default.delete(`http://localhost:5000/submition/delete/${id}`).then((res)=>{
                    this.retrieveSubmitions();
                });
                _sweetalertDefault.default("Deleted Successfully", "", "success");
            } else _sweetalertDefault.default("Your file is safe!");
        });
    };
    render() {
        return /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
            className: "container",
            children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                className: "ms-4 me-4 mt-2 mb-2",
                children: [
                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                        className: "row",
                        children: [
                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                className: "col-lg-4 mt-2 mb-2",
                                children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("a", {
                                    className: "btn btn-outline-dark",
                                    href: "/marks/view",
                                    children: [
                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("i", {
                                            className: "fas fa-eye"
                                        }, void 0, false, {
                                            fileName: "src/components/IT20128036/admin/viewSubmitions.js",
                                            lineNumber: 141,
                                            columnNumber: 18
                                        }, this),
                                        "\xa0View Marks"
                                    ]
                                }, void 0, true, {
                                    fileName: "src/components/IT20128036/admin/viewSubmitions.js",
                                    lineNumber: 140,
                                    columnNumber: 7
                                }, this)
                            }, void 0, false, {
                                fileName: "src/components/IT20128036/admin/viewSubmitions.js",
                                lineNumber: 139,
                                columnNumber: 7
                            }, this),
                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                className: "col-lg-4 mt-2 mb-2",
                                children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("h4", {
                                    children: "All Submitions"
                                }, void 0, false, {
                                    fileName: "src/components/IT20128036/admin/viewSubmitions.js",
                                    lineNumber: 145,
                                    columnNumber: 9
                                }, this)
                            }, void 0, false, {
                                fileName: "src/components/IT20128036/admin/viewSubmitions.js",
                                lineNumber: 144,
                                columnNumber: 7
                            }, this),
                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                className: "col-lg-4 mt-2 mb-2",
                                children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                    className: "row",
                                    children: [
                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                            className: "col-1",
                                            children: [
                                                " ",
                                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("i", {
                                                    class: "fa fa-search",
                                                    "aria-hidden": "true"
                                                }, void 0, false, {
                                                    fileName: "src/components/IT20128036/admin/viewSubmitions.js",
                                                    lineNumber: 149,
                                                    columnNumber: 27
                                                }, this)
                                            ]
                                        }, void 0, true, {
                                            fileName: "src/components/IT20128036/admin/viewSubmitions.js",
                                            lineNumber: 149,
                                            columnNumber: 3
                                        }, this),
                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                            className: "col-11",
                                            children: [
                                                "   ",
                                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("input", {
                                                    className: "form-control",
                                                    type: "search",
                                                    placeholder: "Search . . . . . .",
                                                    name: "searchQuery",
                                                    onChange: this.handleSearchArea
                                                }, void 0, false, {
                                                    fileName: "src/components/IT20128036/admin/viewSubmitions.js",
                                                    lineNumber: 150,
                                                    columnNumber: 30
                                                }, this)
                                            ]
                                        }, void 0, true, {
                                            fileName: "src/components/IT20128036/admin/viewSubmitions.js",
                                            lineNumber: 150,
                                            columnNumber: 3
                                        }, this)
                                    ]
                                }, void 0, true, {
                                    fileName: "src/components/IT20128036/admin/viewSubmitions.js",
                                    lineNumber: 148,
                                    columnNumber: 1
                                }, this)
                            }, void 0, false, {
                                fileName: "src/components/IT20128036/admin/viewSubmitions.js",
                                lineNumber: 147,
                                columnNumber: 7
                            }, this)
                        ]
                    }, void 0, true, {
                        fileName: "src/components/IT20128036/admin/viewSubmitions.js",
                        lineNumber: 138,
                        columnNumber: 1
                    }, this),
                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("table", {
                        className: "table",
                        children: [
                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("thead", {
                                children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("tr", {
                                    children: [
                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("th", {
                                            scope: "col",
                                            children: "#"
                                        }, void 0, false, {
                                            fileName: "src/components/IT20128036/admin/viewSubmitions.js",
                                            lineNumber: 170,
                                            columnNumber: 21
                                        }, this),
                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("th", {
                                            scope: "col",
                                            children: "Group ID"
                                        }, void 0, false, {
                                            fileName: "src/components/IT20128036/admin/viewSubmitions.js",
                                            lineNumber: 171,
                                            columnNumber: 21
                                        }, this),
                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("th", {
                                            scope: "col",
                                            children: "Type"
                                        }, void 0, false, {
                                            fileName: "src/components/IT20128036/admin/viewSubmitions.js",
                                            lineNumber: 172,
                                            columnNumber: 21
                                        }, this),
                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("th", {
                                            scope: "col",
                                            children: "Description"
                                        }, void 0, false, {
                                            fileName: "src/components/IT20128036/admin/viewSubmitions.js",
                                            lineNumber: 173,
                                            columnNumber: 21
                                        }, this),
                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("th", {
                                            scope: "col",
                                            children: "Submitted Date"
                                        }, void 0, false, {
                                            fileName: "src/components/IT20128036/admin/viewSubmitions.js",
                                            lineNumber: 174,
                                            columnNumber: 21
                                        }, this),
                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("th", {
                                            scope: "col",
                                            children: "Status"
                                        }, void 0, false, {
                                            fileName: "src/components/IT20128036/admin/viewSubmitions.js",
                                            lineNumber: 175,
                                            columnNumber: 21
                                        }, this),
                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("th", {
                                            scope: "col",
                                            children: "Preview"
                                        }, void 0, false, {
                                            fileName: "src/components/IT20128036/admin/viewSubmitions.js",
                                            lineNumber: 177,
                                            columnNumber: 21
                                        }, this),
                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("th", {
                                            scope: "col",
                                            children: "Delete"
                                        }, void 0, false, {
                                            fileName: "src/components/IT20128036/admin/viewSubmitions.js",
                                            lineNumber: 178,
                                            columnNumber: 21
                                        }, this),
                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("th", {
                                            scope: "col",
                                            children: "Add Marks"
                                        }, void 0, false, {
                                            fileName: "src/components/IT20128036/admin/viewSubmitions.js",
                                            lineNumber: 179,
                                            columnNumber: 21
                                        }, this)
                                    ]
                                }, void 0, true, {
                                    fileName: "src/components/IT20128036/admin/viewSubmitions.js",
                                    lineNumber: 169,
                                    columnNumber: 17
                                }, this)
                            }, void 0, false, {
                                fileName: "src/components/IT20128036/admin/viewSubmitions.js",
                                lineNumber: 168,
                                columnNumber: 13
                            }, this),
                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("tbody", {
                                children: this.state.submitions.map((submitions, index)=>/*#__PURE__*/ _jsxDevRuntime.jsxDEV("tr", {
                                        children: [
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("th", {
                                                scope: "row",
                                                children: index + 1
                                            }, void 0, false, {
                                                fileName: "src/components/IT20128036/admin/viewSubmitions.js",
                                                lineNumber: 185,
                                                columnNumber: 25
                                            }, this),
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("td", {
                                                children: submitions.groupId
                                            }, void 0, false, {
                                                fileName: "src/components/IT20128036/admin/viewSubmitions.js",
                                                lineNumber: 186,
                                                columnNumber: 25
                                            }, this),
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("td", {
                                                children: submitions.type
                                            }, void 0, false, {
                                                fileName: "src/components/IT20128036/admin/viewSubmitions.js",
                                                lineNumber: 187,
                                                columnNumber: 25
                                            }, this),
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("td", {
                                                children: submitions.description
                                            }, void 0, false, {
                                                fileName: "src/components/IT20128036/admin/viewSubmitions.js",
                                                lineNumber: 188,
                                                columnNumber: 25
                                            }, this),
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("td", {
                                                children: submitions.postDate
                                            }, void 0, false, {
                                                fileName: "src/components/IT20128036/admin/viewSubmitions.js",
                                                lineNumber: 189,
                                                columnNumber: 25
                                            }, this),
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("td", {
                                                children: submitions.status
                                            }, void 0, false, {
                                                fileName: "src/components/IT20128036/admin/viewSubmitions.js",
                                                lineNumber: 190,
                                                columnNumber: 25
                                            }, this),
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("td", {
                                                children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("a", {
                                                    className: "btn btn-outline-success",
                                                    onClick: ()=>this.downloadFile(submitions.files)
                                                    ,
                                                    children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("i", {
                                                        class: "fa fa-download",
                                                        "aria-hidden": "true",
                                                        children: [
                                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("br", {}, void 0, false, {
                                                                fileName: "src/components/IT20128036/admin/viewSubmitions.js",
                                                                lineNumber: 194,
                                                                columnNumber: 159
                                                            }, this),
                                                            "Download"
                                                        ]
                                                    }, void 0, true, {
                                                        fileName: "src/components/IT20128036/admin/viewSubmitions.js",
                                                        lineNumber: 194,
                                                        columnNumber: 114
                                                    }, this)
                                                }, void 0, false, {
                                                    fileName: "src/components/IT20128036/admin/viewSubmitions.js",
                                                    lineNumber: 194,
                                                    columnNumber: 23
                                                }, this)
                                            }, void 0, false, {
                                                fileName: "src/components/IT20128036/admin/viewSubmitions.js",
                                                lineNumber: 191,
                                                columnNumber: 25
                                            }, this),
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("td", {
                                                children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("a", {
                                                    className: "btn btn-outline-danger",
                                                    href: "#",
                                                    onClick: ()=>{
                                                        this.onDelete(submitions._id);
                                                    },
                                                    children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("i", {
                                                        className: "fas fa-trash-alt",
                                                        children: [
                                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("br", {}, void 0, false, {
                                                                fileName: "src/components/IT20128036/admin/viewSubmitions.js",
                                                                lineNumber: 204,
                                                                columnNumber: 51
                                                            }, this),
                                                            "Delete "
                                                        ]
                                                    }, void 0, true, {
                                                        fileName: "src/components/IT20128036/admin/viewSubmitions.js",
                                                        lineNumber: 204,
                                                        columnNumber: 19
                                                    }, this)
                                                }, void 0, false, {
                                                    fileName: "src/components/IT20128036/admin/viewSubmitions.js",
                                                    lineNumber: 201,
                                                    columnNumber: 25
                                                }, this)
                                            }, void 0, false, {
                                                fileName: "src/components/IT20128036/admin/viewSubmitions.js",
                                                lineNumber: 200,
                                                columnNumber: 25
                                            }, this),
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("td", {
                                                children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("a", {
                                                    className: "btn btn-outline-primary",
                                                    href: `/marks/add/${submitions._id}`,
                                                    children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("i", {
                                                        className: "fas fa-plus",
                                                        children: [
                                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("br", {}, void 0, false, {
                                                                fileName: "src/components/IT20128036/admin/viewSubmitions.js",
                                                                lineNumber: 213,
                                                                columnNumber: 45
                                                            }, this),
                                                            "Marks"
                                                        ]
                                                    }, void 0, true, {
                                                        fileName: "src/components/IT20128036/admin/viewSubmitions.js",
                                                        lineNumber: 213,
                                                        columnNumber: 18
                                                    }, this)
                                                }, void 0, false, {
                                                    fileName: "src/components/IT20128036/admin/viewSubmitions.js",
                                                    lineNumber: 212,
                                                    columnNumber: 25
                                                }, this)
                                            }, void 0, false, {
                                                fileName: "src/components/IT20128036/admin/viewSubmitions.js",
                                                lineNumber: 211,
                                                columnNumber: 25
                                            }, this)
                                        ]
                                    }, index, true, {
                                        fileName: "src/components/IT20128036/admin/viewSubmitions.js",
                                        lineNumber: 184,
                                        columnNumber: 21
                                    }, this)
                                )
                            }, void 0, false, {
                                fileName: "src/components/IT20128036/admin/viewSubmitions.js",
                                lineNumber: 182,
                                columnNumber: 13
                            }, this)
                        ]
                    }, void 0, true, {
                        fileName: "src/components/IT20128036/admin/viewSubmitions.js",
                        lineNumber: 167,
                        columnNumber: 11
                    }, this)
                ]
            }, void 0, true, {
                fileName: "src/components/IT20128036/admin/viewSubmitions.js",
                lineNumber: 137,
                columnNumber: 1
            }, this)
        }, void 0, false, {
            fileName: "src/components/IT20128036/admin/viewSubmitions.js",
            lineNumber: 136,
            columnNumber: 7
        }, this);
    }
}
exports.default = ViewSubmitions;

  $parcel$ReactRefreshHelpers$2324.postlude(module);
} finally {
  window.$RefreshReg$ = prevRefreshReg;
  window.$RefreshSig$ = prevRefreshSig;
}
},{"react/jsx-dev-runtime":"iTorj","react":"21dqq","axios":"jo6P5","sweetalert":"38a65","js-file-download":"cLODl","@parcel/transformer-js/src/esmodule-helpers.js":"gkKU3","@parcel/transformer-react-refresh-wrap/lib/helpers/helpers.js":"km3Ru"}],"dq82r":[function(require,module,exports) {
var $parcel$ReactRefreshHelpers$df93 = require("@parcel/transformer-react-refresh-wrap/lib/helpers/helpers.js");
var prevRefreshReg = window.$RefreshReg$;
var prevRefreshSig = window.$RefreshSig$;
$parcel$ReactRefreshHelpers$df93.prelude(module);

try {
var parcelHelpers = require("@parcel/transformer-js/src/esmodule-helpers.js");
parcelHelpers.defineInteropFlag(exports);
var _jsxDevRuntime = require("react/jsx-dev-runtime");
var _react = require("react");
var _reactDefault = parcelHelpers.interopDefault(_react);
var _axios = require("axios");
var _axiosDefault = parcelHelpers.interopDefault(_axios);
var _sweetalert = require("sweetalert");
var _sweetalertDefault = parcelHelpers.interopDefault(_sweetalert);
var _jwtDecode = require("jwt-decode");
var _jwtDecodeDefault = parcelHelpers.interopDefault(_jwtDecode);
class AddMarks extends _react.Component {
    constructor(props){
        super(props);
        this.state = {
            groupId: "",
            type: "",
            smarks: "",
            pmarks: "",
            marks: "",
            gradingStatus: "",
            status: "Marked",
            markedby: ""
        };
    }
    componentDidMount() {
        if (localStorage.userToken) {
            const usertoken = localStorage.userToken;
            const decoded = _jwtDecodeDefault.default(usertoken);
            this.setState({
                markedby: decoded.name
            });
        }
        setTimeout(()=>{
            this.retriveSubmitions();
        }, 1000);
    }
    handleInputChange = (e)=>{
        const { name , value  } = e.target;
        this.setState({
            ...this.state,
            [name]: value
        });
    };
    onSubmit = (e)=>{
        e.preventDefault();
        const id = this.props.match.params.id;
        const { groupId , type , smarks , pmarks , marks , gradingStatus , status , markedby  } = this.state;
        //calculate marks
        console.log(marks); //20
        console.log(smarks); //2
        console.log(pmarks); //6
        let ssm = parseInt(smarks);
        let ppm = parseInt(pmarks);
        let ssmp = marks * ssm / 100;
        let ppmp = marks * ppm / 100;
        let rmarks = ssmp + ppmp; //26
        console.log(rmarks);
        let totmarks = marks - rmarks;
        console.log(totmarks);
        let orgmarks = totmarks.toFixed(2);
        if (orgmarks < 0) orgmarks = 0;
        const data = {
            groupId: groupId,
            type: type,
            marks: orgmarks,
            gradingStatus: gradingStatus,
            markedby: markedby
        };
        console.log(data);
        //save marks
        _axiosDefault.default.post(`http://localhost:5000/mark/save`, data).then((res)=>{
            if (res.data.success) {
                _sweetalertDefault.default("Marks Added Successfully !", "", "success").then((value)=>{
                    window.location = "/submitions/view";
                });
                this.setState({
                    groupId: "",
                    type: "",
                    marks: "",
                    gradingStatus: "",
                    markedby: ""
                });
            }
        });
        const statusdata = {
            status: status
        };
        //update status 
        const ids = this.props.match.params.id;
        _axiosDefault.default.put(`http://localhost:5000/submition/update/${ids}`, statusdata).then((res)=>{
            if (res.data.success) console.log("Updated Successfully");
        });
    };
    retriveSubmitions() {
        // if(this.props.match && this.props.match.params.id){
        const id = this.props.match.params.id;
        //retrive submition data   
        _axiosDefault.default.get(`http://localhost:5000/submition/${id}`).then((res)=>{
            if (res.data.success) {
                this.setState({
                    groupId: res.data.submition.groupId,
                    type: res.data.submition.type
                });
                console.log(this.state);
            }
        });
    // }
    }
    render() {
        return /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
            className: "container",
            children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                className: "ms-4 me-4 mt-2 mb-2",
                children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                    className: "row",
                    children: [
                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                            className: "col-sm-8",
                            children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                className: "col-md-12 mt-4 mx-auto",
                                children: [
                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("h1", {
                                        className: "h3 mb-3 font-weight-normal",
                                        children: "Add Marks"
                                    }, void 0, false, {
                                        fileName: "src/components/IT20128036/admin/addMarks.js",
                                        lineNumber: 219,
                                        columnNumber: 8
                                    }, this),
                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("form", {
                                        className: "needs-validation",
                                        onSubmit: this.onSubmit,
                                        children: [
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                                className: "form-group",
                                                style: {
                                                    marginBottom: '15px'
                                                },
                                                children: [
                                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("label", {
                                                        style: {
                                                            marginBottom: '5px'
                                                        },
                                                        children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("strong", {
                                                            children: "Group ID"
                                                        }, void 0, false, {
                                                            fileName: "src/components/IT20128036/admin/addMarks.js",
                                                            lineNumber: 222,
                                                            columnNumber: 48
                                                        }, this)
                                                    }, void 0, false, {
                                                        fileName: "src/components/IT20128036/admin/addMarks.js",
                                                        lineNumber: 222,
                                                        columnNumber: 12
                                                    }, this),
                                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("input", {
                                                        type: "text",
                                                        className: "form-control",
                                                        name: "groupId",
                                                        placeholder: "Enter Group Id",
                                                        value: this.state.groupId,
                                                        onChange: this.handleInputChange,
                                                        disabled: true
                                                    }, void 0, false, {
                                                        fileName: "src/components/IT20128036/admin/addMarks.js",
                                                        lineNumber: 223,
                                                        columnNumber: 12
                                                    }, this)
                                                ]
                                            }, void 0, true, {
                                                fileName: "src/components/IT20128036/admin/addMarks.js",
                                                lineNumber: 221,
                                                columnNumber: 10
                                            }, this),
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                                className: "form-group",
                                                style: {
                                                    marginBottom: '15px'
                                                },
                                                children: [
                                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("label", {
                                                        style: {
                                                            marginBottom: '5px'
                                                        },
                                                        children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("strong", {
                                                            children: "Type"
                                                        }, void 0, false, {
                                                            fileName: "src/components/IT20128036/admin/addMarks.js",
                                                            lineNumber: 235,
                                                            columnNumber: 48
                                                        }, this)
                                                    }, void 0, false, {
                                                        fileName: "src/components/IT20128036/admin/addMarks.js",
                                                        lineNumber: 235,
                                                        columnNumber: 12
                                                    }, this),
                                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("input", {
                                                        type: "text",
                                                        className: "form-control",
                                                        name: "submitionType",
                                                        placeholder: "Enter Submition Type",
                                                        value: this.state.type,
                                                        onChange: this.handleInputChange,
                                                        disabled: true
                                                    }, void 0, false, {
                                                        fileName: "src/components/IT20128036/admin/addMarks.js",
                                                        lineNumber: 236,
                                                        columnNumber: 12
                                                    }, this)
                                                ]
                                            }, void 0, true, {
                                                fileName: "src/components/IT20128036/admin/addMarks.js",
                                                lineNumber: 234,
                                                columnNumber: 10
                                            }, this),
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                                className: "row",
                                                children: [
                                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                                        className: "col-sm-4",
                                                        children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                                            className: "form-group",
                                                            style: {
                                                                marginBottom: '15px'
                                                            },
                                                            children: [
                                                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("label", {
                                                                    style: {
                                                                        marginBottom: '5px'
                                                                    },
                                                                    children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("strong", {
                                                                        children: "Submission On Time (?/100)"
                                                                    }, void 0, false, {
                                                                        fileName: "src/components/IT20128036/admin/addMarks.js",
                                                                        lineNumber: 251,
                                                                        columnNumber: 48
                                                                    }, this)
                                                                }, void 0, false, {
                                                                    fileName: "src/components/IT20128036/admin/addMarks.js",
                                                                    lineNumber: 251,
                                                                    columnNumber: 12
                                                                }, this),
                                                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("select", {
                                                                    className: "form-select",
                                                                    name: "smarks",
                                                                    value: this.state.smarks,
                                                                    onChange: this.handleInputChange,
                                                                    required: true,
                                                                    children: [
                                                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("option", {
                                                                            smarks: "Select Minus Marks",
                                                                            selected: true,
                                                                            children: "Select Minus Percentage"
                                                                        }, void 0, false, {
                                                                            fileName: "src/components/IT20128036/admin/addMarks.js",
                                                                            lineNumber: 263,
                                                                            columnNumber: 25
                                                                        }, this),
                                                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("option", {
                                                                            smarks: "0",
                                                                            children: "0"
                                                                        }, void 0, false, {
                                                                            fileName: "src/components/IT20128036/admin/addMarks.js",
                                                                            lineNumber: 266,
                                                                            columnNumber: 25
                                                                        }, this),
                                                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("option", {
                                                                            smarks: "10",
                                                                            children: "10"
                                                                        }, void 0, false, {
                                                                            fileName: "src/components/IT20128036/admin/addMarks.js",
                                                                            lineNumber: 267,
                                                                            columnNumber: 25
                                                                        }, this),
                                                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("option", {
                                                                            smarks: "20",
                                                                            children: "20"
                                                                        }, void 0, false, {
                                                                            fileName: "src/components/IT20128036/admin/addMarks.js",
                                                                            lineNumber: 268,
                                                                            columnNumber: 25
                                                                        }, this),
                                                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("option", {
                                                                            smarks: "30",
                                                                            children: "30"
                                                                        }, void 0, false, {
                                                                            fileName: "src/components/IT20128036/admin/addMarks.js",
                                                                            lineNumber: 269,
                                                                            columnNumber: 25
                                                                        }, this),
                                                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("option", {
                                                                            smarks: "50",
                                                                            children: "50"
                                                                        }, void 0, false, {
                                                                            fileName: "src/components/IT20128036/admin/addMarks.js",
                                                                            lineNumber: 270,
                                                                            columnNumber: 25
                                                                        }, this)
                                                                    ]
                                                                }, void 0, true, {
                                                                    fileName: "src/components/IT20128036/admin/addMarks.js",
                                                                    lineNumber: 256,
                                                                    columnNumber: 23
                                                                }, this)
                                                            ]
                                                        }, void 0, true, {
                                                            fileName: "src/components/IT20128036/admin/addMarks.js",
                                                            lineNumber: 250,
                                                            columnNumber: 3
                                                        }, this)
                                                    }, void 0, false, {
                                                        fileName: "src/components/IT20128036/admin/addMarks.js",
                                                        lineNumber: 248,
                                                        columnNumber: 3
                                                    }, this),
                                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                                        className: "col-sm-4",
                                                        children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                                            className: "form-group",
                                                            style: {
                                                                marginBottom: '15px'
                                                            },
                                                            children: [
                                                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("label", {
                                                                    style: {
                                                                        marginBottom: '5px'
                                                                    },
                                                                    children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("strong", {
                                                                        children: "Checking Plagiarism (?/100)"
                                                                    }, void 0, false, {
                                                                        fileName: "src/components/IT20128036/admin/addMarks.js",
                                                                        lineNumber: 287,
                                                                        columnNumber: 48
                                                                    }, this)
                                                                }, void 0, false, {
                                                                    fileName: "src/components/IT20128036/admin/addMarks.js",
                                                                    lineNumber: 287,
                                                                    columnNumber: 12
                                                                }, this),
                                                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("select", {
                                                                    className: "form-select",
                                                                    name: "pmarks",
                                                                    value: this.state.pmarks,
                                                                    onChange: this.handleInputChange,
                                                                    required: true,
                                                                    children: [
                                                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("option", {
                                                                            pmarks: "Select Minus Marks",
                                                                            selected: true,
                                                                            children: "Select Minus Percentage"
                                                                        }, void 0, false, {
                                                                            fileName: "src/components/IT20128036/admin/addMarks.js",
                                                                            lineNumber: 300,
                                                                            columnNumber: 25
                                                                        }, this),
                                                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("option", {
                                                                            pmarks: "0",
                                                                            children: "0"
                                                                        }, void 0, false, {
                                                                            fileName: "src/components/IT20128036/admin/addMarks.js",
                                                                            lineNumber: 303,
                                                                            columnNumber: 25
                                                                        }, this),
                                                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("option", {
                                                                            pmarks: "10",
                                                                            children: "10"
                                                                        }, void 0, false, {
                                                                            fileName: "src/components/IT20128036/admin/addMarks.js",
                                                                            lineNumber: 304,
                                                                            columnNumber: 25
                                                                        }, this),
                                                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("option", {
                                                                            pmarks: "20",
                                                                            children: "20"
                                                                        }, void 0, false, {
                                                                            fileName: "src/components/IT20128036/admin/addMarks.js",
                                                                            lineNumber: 305,
                                                                            columnNumber: 25
                                                                        }, this),
                                                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("option", {
                                                                            pmarks: "30",
                                                                            children: "30"
                                                                        }, void 0, false, {
                                                                            fileName: "src/components/IT20128036/admin/addMarks.js",
                                                                            lineNumber: 306,
                                                                            columnNumber: 25
                                                                        }, this),
                                                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("option", {
                                                                            pmarks: "100",
                                                                            children: "100"
                                                                        }, void 0, false, {
                                                                            fileName: "src/components/IT20128036/admin/addMarks.js",
                                                                            lineNumber: 307,
                                                                            columnNumber: 25
                                                                        }, this)
                                                                    ]
                                                                }, void 0, true, {
                                                                    fileName: "src/components/IT20128036/admin/addMarks.js",
                                                                    lineNumber: 293,
                                                                    columnNumber: 22
                                                                }, this)
                                                            ]
                                                        }, void 0, true, {
                                                            fileName: "src/components/IT20128036/admin/addMarks.js",
                                                            lineNumber: 286,
                                                            columnNumber: 3
                                                        }, this)
                                                    }, void 0, false, {
                                                        fileName: "src/components/IT20128036/admin/addMarks.js",
                                                        lineNumber: 283,
                                                        columnNumber: 3
                                                    }, this),
                                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                                        className: "col-sm-4",
                                                        children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                                            className: "form-group",
                                                            style: {
                                                                marginBottom: '15px'
                                                            },
                                                            children: [
                                                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("label", {
                                                                    style: {
                                                                        marginBottom: '5px'
                                                                    },
                                                                    children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("strong", {
                                                                        children: "Marks (?/100)"
                                                                    }, void 0, false, {
                                                                        fileName: "src/components/IT20128036/admin/addMarks.js",
                                                                        lineNumber: 324,
                                                                        columnNumber: 48
                                                                    }, this)
                                                                }, void 0, false, {
                                                                    fileName: "src/components/IT20128036/admin/addMarks.js",
                                                                    lineNumber: 324,
                                                                    columnNumber: 12
                                                                }, this),
                                                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("input", {
                                                                    type: "text",
                                                                    className: "form-control",
                                                                    name: "marks",
                                                                    placeholder: "Enter Marks ?/100",
                                                                    value: this.state.marks,
                                                                    onChange: this.handleInputChange,
                                                                    required: true
                                                                }, void 0, false, {
                                                                    fileName: "src/components/IT20128036/admin/addMarks.js",
                                                                    lineNumber: 325,
                                                                    columnNumber: 12
                                                                }, this)
                                                            ]
                                                        }, void 0, true, {
                                                            fileName: "src/components/IT20128036/admin/addMarks.js",
                                                            lineNumber: 323,
                                                            columnNumber: 3
                                                        }, this)
                                                    }, void 0, false, {
                                                        fileName: "src/components/IT20128036/admin/addMarks.js",
                                                        lineNumber: 320,
                                                        columnNumber: 3
                                                    }, this)
                                                ]
                                            }, void 0, true, {
                                                fileName: "src/components/IT20128036/admin/addMarks.js",
                                                lineNumber: 247,
                                                columnNumber: 1
                                            }, this),
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                                className: "row",
                                                children: [
                                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                                        className: "col-8",
                                                        children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                                            className: "form-group",
                                                            style: {
                                                                marginBottom: '15px'
                                                            },
                                                            children: [
                                                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("label", {
                                                                    style: {
                                                                        marginBottom: '5px'
                                                                    },
                                                                    children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("strong", {
                                                                        children: "Grading Status"
                                                                    }, void 0, false, {
                                                                        fileName: "src/components/IT20128036/admin/addMarks.js",
                                                                        lineNumber: 352,
                                                                        columnNumber: 48
                                                                    }, this)
                                                                }, void 0, false, {
                                                                    fileName: "src/components/IT20128036/admin/addMarks.js",
                                                                    lineNumber: 352,
                                                                    columnNumber: 12
                                                                }, this),
                                                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("select", {
                                                                    className: "form-select",
                                                                    name: "gradingStatus",
                                                                    value: this.state.gradingStatus,
                                                                    onChange: this.handleInputChange,
                                                                    required: true,
                                                                    children: [
                                                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("option", {
                                                                            gradingStatus: "not selected yet",
                                                                            selected: true,
                                                                            children: "Select Status"
                                                                        }, void 0, false, {
                                                                            fileName: "src/components/IT20128036/admin/addMarks.js",
                                                                            lineNumber: 363,
                                                                            columnNumber: 25
                                                                        }, this),
                                                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("option", {
                                                                            gradingStatus: "Panel 01",
                                                                            children: "Bad"
                                                                        }, void 0, false, {
                                                                            fileName: "src/components/IT20128036/admin/addMarks.js",
                                                                            lineNumber: 366,
                                                                            columnNumber: 25
                                                                        }, this),
                                                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("option", {
                                                                            gradingStatus: "Panel 02",
                                                                            children: "Normal"
                                                                        }, void 0, false, {
                                                                            fileName: "src/components/IT20128036/admin/addMarks.js",
                                                                            lineNumber: 367,
                                                                            columnNumber: 25
                                                                        }, this),
                                                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("option", {
                                                                            gradingStatus: "Panel 03",
                                                                            children: "Nice Work"
                                                                        }, void 0, false, {
                                                                            fileName: "src/components/IT20128036/admin/addMarks.js",
                                                                            lineNumber: 368,
                                                                            columnNumber: 25
                                                                        }, this),
                                                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("option", {
                                                                            gradingStatus: "Panel 04",
                                                                            children: "Good"
                                                                        }, void 0, false, {
                                                                            fileName: "src/components/IT20128036/admin/addMarks.js",
                                                                            lineNumber: 369,
                                                                            columnNumber: 25
                                                                        }, this),
                                                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("option", {
                                                                            gradingStatus: "Panel 05",
                                                                            children: "excellent"
                                                                        }, void 0, false, {
                                                                            fileName: "src/components/IT20128036/admin/addMarks.js",
                                                                            lineNumber: 370,
                                                                            columnNumber: 25
                                                                        }, this)
                                                                    ]
                                                                }, void 0, true, {
                                                                    fileName: "src/components/IT20128036/admin/addMarks.js",
                                                                    lineNumber: 356,
                                                                    columnNumber: 22
                                                                }, this)
                                                            ]
                                                        }, void 0, true, {
                                                            fileName: "src/components/IT20128036/admin/addMarks.js",
                                                            lineNumber: 351,
                                                            columnNumber: 1
                                                        }, this)
                                                    }, void 0, false, {
                                                        fileName: "src/components/IT20128036/admin/addMarks.js",
                                                        lineNumber: 347,
                                                        columnNumber: 1
                                                    }, this),
                                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                                        className: "col-4",
                                                        children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                                            className: "form-group",
                                                            style: {
                                                                marginBottom: '15px'
                                                            },
                                                            children: [
                                                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("label", {
                                                                    style: {
                                                                        marginBottom: '5px'
                                                                    },
                                                                    children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("strong", {
                                                                        children: "Marked By"
                                                                    }, void 0, false, {
                                                                        fileName: "src/components/IT20128036/admin/addMarks.js",
                                                                        lineNumber: 394,
                                                                        columnNumber: 48
                                                                    }, this)
                                                                }, void 0, false, {
                                                                    fileName: "src/components/IT20128036/admin/addMarks.js",
                                                                    lineNumber: 394,
                                                                    columnNumber: 12
                                                                }, this),
                                                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("input", {
                                                                    type: "text",
                                                                    className: "form-control",
                                                                    name: "markedby",
                                                                    placeholder: "Marked By",
                                                                    value: this.state.markedby,
                                                                    onChange: this.handleInputChange,
                                                                    disabled: true
                                                                }, void 0, false, {
                                                                    fileName: "src/components/IT20128036/admin/addMarks.js",
                                                                    lineNumber: 395,
                                                                    columnNumber: 12
                                                                }, this)
                                                            ]
                                                        }, void 0, true, {
                                                            fileName: "src/components/IT20128036/admin/addMarks.js",
                                                            lineNumber: 393,
                                                            columnNumber: 1
                                                        }, this)
                                                    }, void 0, false, {
                                                        fileName: "src/components/IT20128036/admin/addMarks.js",
                                                        lineNumber: 388,
                                                        columnNumber: 1
                                                    }, this)
                                                ]
                                            }, void 0, true, {
                                                fileName: "src/components/IT20128036/admin/addMarks.js",
                                                lineNumber: 346,
                                                columnNumber: 1
                                            }, this),
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("button", {
                                                className: "btn btn-outline-success",
                                                type: "submit",
                                                style: {
                                                    margintop: '15px'
                                                },
                                                children: [
                                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("i", {
                                                        className: "far fa-check-square"
                                                    }, void 0, false, {
                                                        fileName: "src/components/IT20128036/admin/addMarks.js",
                                                        lineNumber: 422,
                                                        columnNumber: 12
                                                    }, this),
                                                    "\xa0 Add Marks"
                                                ]
                                            }, void 0, true, {
                                                fileName: "src/components/IT20128036/admin/addMarks.js",
                                                lineNumber: 421,
                                                columnNumber: 10
                                            }, this)
                                        ]
                                    }, void 0, true, {
                                        fileName: "src/components/IT20128036/admin/addMarks.js",
                                        lineNumber: 220,
                                        columnNumber: 8
                                    }, this)
                                ]
                            }, void 0, true, {
                                fileName: "src/components/IT20128036/admin/addMarks.js",
                                lineNumber: 218,
                                columnNumber: 6
                            }, this)
                        }, void 0, false, {
                            fileName: "src/components/IT20128036/admin/addMarks.js",
                            lineNumber: 215,
                            columnNumber: 11
                        }, this),
                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                            className: "col-sm-4",
                            children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                class: "card mt-5 mb-5",
                                children: [
                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("h5", {
                                        class: "card-header",
                                        children: "Guide To Add Marks"
                                    }, void 0, false, {
                                        fileName: "src/components/IT20128036/admin/addMarks.js",
                                        lineNumber: 436,
                                        columnNumber: 3
                                    }, this),
                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                        class: "card-body",
                                        children: [
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("h5", {
                                                class: "card-title",
                                                children: "About Minus Marks"
                                            }, void 0, false, {
                                                fileName: "src/components/IT20128036/admin/addMarks.js",
                                                lineNumber: 438,
                                                columnNumber: 5
                                            }, this),
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("p", {
                                                class: "card-text",
                                                children: [
                                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("strong", {
                                                        children: "Submission On Time"
                                                    }, void 0, false, {
                                                        fileName: "src/components/IT20128036/admin/addMarks.js",
                                                        lineNumber: 439,
                                                        columnNumber: 26
                                                    }, this),
                                                    " ",
                                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("br", {}, void 0, false, {
                                                        fileName: "src/components/IT20128036/admin/addMarks.js",
                                                        lineNumber: 439,
                                                        columnNumber: 62
                                                    }, this),
                                                    " 1 Day Late \xa0 \xa0 -- 10% ",
                                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("br", {}, void 0, false, {
                                                        fileName: "src/components/IT20128036/admin/addMarks.js",
                                                        lineNumber: 439,
                                                        columnNumber: 104
                                                    }, this),
                                                    " 2 Days Late \xa0  -- 20%",
                                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("br", {}, void 0, false, {
                                                        fileName: "src/components/IT20128036/admin/addMarks.js",
                                                        lineNumber: 440,
                                                        columnNumber: 5
                                                    }, this),
                                                    " 3 Days Late \xa0  -- 30% ",
                                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("br", {}, void 0, false, {
                                                        fileName: "src/components/IT20128036/admin/addMarks.js",
                                                        lineNumber: 440,
                                                        columnNumber: 42
                                                    }, this),
                                                    " More Than 3 -- 50%"
                                                ]
                                            }, void 0, true, {
                                                fileName: "src/components/IT20128036/admin/addMarks.js",
                                                lineNumber: 439,
                                                columnNumber: 5
                                            }, this),
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("p", {
                                                class: "card-text",
                                                children: [
                                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("strong", {
                                                        children: "Checking Plagiarism"
                                                    }, void 0, false, {
                                                        fileName: "src/components/IT20128036/admin/addMarks.js",
                                                        lineNumber: 442,
                                                        columnNumber: 26
                                                    }, this),
                                                    " ",
                                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("br", {}, void 0, false, {
                                                        fileName: "src/components/IT20128036/admin/addMarks.js",
                                                        lineNumber: 442,
                                                        columnNumber: 63
                                                    }, this),
                                                    "More than 20% Plagiarism -- 10% ",
                                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("br", {}, void 0, false, {
                                                        fileName: "src/components/IT20128036/admin/addMarks.js",
                                                        lineNumber: 443,
                                                        columnNumber: 33
                                                    }, this),
                                                    "More than 30% Plagiarism -- 20%",
                                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("br", {}, void 0, false, {
                                                        fileName: "src/components/IT20128036/admin/addMarks.js",
                                                        lineNumber: 445,
                                                        columnNumber: 5
                                                    }, this),
                                                    "More than 40% Plagiarism -- 30% ",
                                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("br", {}, void 0, false, {
                                                        fileName: "src/components/IT20128036/admin/addMarks.js",
                                                        lineNumber: 446,
                                                        columnNumber: 33
                                                    }, this),
                                                    "More than 50% Plagiarism -- 100%"
                                                ]
                                            }, void 0, true, {
                                                fileName: "src/components/IT20128036/admin/addMarks.js",
                                                lineNumber: 442,
                                                columnNumber: 5
                                            }, this)
                                        ]
                                    }, void 0, true, {
                                        fileName: "src/components/IT20128036/admin/addMarks.js",
                                        lineNumber: 437,
                                        columnNumber: 3
                                    }, this)
                                ]
                            }, void 0, true, {
                                fileName: "src/components/IT20128036/admin/addMarks.js",
                                lineNumber: 435,
                                columnNumber: 6
                            }, this)
                        }, void 0, false, {
                            fileName: "src/components/IT20128036/admin/addMarks.js",
                            lineNumber: 432,
                            columnNumber: 6
                        }, this)
                    ]
                }, void 0, true, {
                    fileName: "src/components/IT20128036/admin/addMarks.js",
                    lineNumber: 214,
                    columnNumber: 9
                }, this)
            }, void 0, false, {
                fileName: "src/components/IT20128036/admin/addMarks.js",
                lineNumber: 213,
                columnNumber: 9
            }, this)
        }, void 0, false, {
            fileName: "src/components/IT20128036/admin/addMarks.js",
            lineNumber: 212,
            columnNumber: 7
        }, this);
    }
}
exports.default = AddMarks;

  $parcel$ReactRefreshHelpers$df93.postlude(module);
} finally {
  window.$RefreshReg$ = prevRefreshReg;
  window.$RefreshSig$ = prevRefreshSig;
}
},{"react/jsx-dev-runtime":"iTorj","react":"21dqq","axios":"jo6P5","sweetalert":"38a65","jwt-decode":"ljM1w","@parcel/transformer-js/src/esmodule-helpers.js":"gkKU3","@parcel/transformer-react-refresh-wrap/lib/helpers/helpers.js":"km3Ru"}],"3s75B":[function(require,module,exports) {
var $parcel$ReactRefreshHelpers$57da = require("@parcel/transformer-react-refresh-wrap/lib/helpers/helpers.js");
var prevRefreshReg = window.$RefreshReg$;
var prevRefreshSig = window.$RefreshSig$;
$parcel$ReactRefreshHelpers$57da.prelude(module);

try {
var parcelHelpers = require("@parcel/transformer-js/src/esmodule-helpers.js");
parcelHelpers.defineInteropFlag(exports);
var _jsxDevRuntime = require("react/jsx-dev-runtime");
var _react = require("react");
var _reactDefault = parcelHelpers.interopDefault(_react);
var _axios = require("axios");
var _axiosDefault = parcelHelpers.interopDefault(_axios);
var _sweetalert = require("sweetalert");
var _sweetalertDefault = parcelHelpers.interopDefault(_sweetalert);
var _jwtDecode = require("jwt-decode");
var _jwtDecodeDefault = parcelHelpers.interopDefault(_jwtDecode);
class viewMarks extends _react.Component {
    constructor(props){
        super(props);
        this.state = {
            marks: [],
            markedby: ""
        };
    }
    componentDidMount() {
        if (localStorage.userToken) {
            const usertoken = localStorage.userToken;
            const decoded = _jwtDecodeDefault.default(usertoken);
            this.setState({
                markedby: decoded.name
            });
        }
        setTimeout(()=>{
            this.retriveMarks();
        }, 1000);
    }
    //retrive marks
    retriveMarks() {
        const name = this.state.markedby;
        _axiosDefault.default.get(`http://localhost:5000/marks/supervisor/${name}`).then((res)=>{
            if (res.data.success) {
                this.setState({
                    marks: res.data.existingMarks
                });
                console.log(this.state.marks);
            }
        });
    }
    //delete marks
    onDelete = (id)=>{
        _sweetalertDefault.default({
            title: "Are you sure?",
            text: "Once deleted, you will not be able to recover this file!",
            icon: "warning",
            buttons: true,
            dangerMode: true
        }).then((willDelete)=>{
            if (willDelete) {
                _axiosDefault.default.delete(`http://localhost:5000/mark/delete/${id}`).then((res)=>{
                    this.retriveMarks();
                });
                _sweetalertDefault.default("Deleted Successfully", "", "success");
            } else _sweetalertDefault.default("Your file is safe!");
        });
    };
    //filter marks
    filterData(marks, searchKey) {
        const result = marks.filter((mark)=>mark.groupId.toLowerCase().includes(searchKey) || mark.type.toLowerCase().includes(searchKey) || mark.gradingStatus.toLowerCase().includes(searchKey) || mark.groupId.includes(searchKey) || mark.type.includes(searchKey) || mark.gradingStatus.includes(searchKey)
        );
        this.setState({
            marks: result
        });
    }
    //search marks
    handleSearchArea = (e)=>{
        const searchKey = e.currentTarget.value;
        const name = this.state.markedby;
        _axiosDefault.default.get(`http://localhost:5000/marks/supervisor/${name}`).then((res)=>{
            if (res.data.success) this.filterData(res.data.existingMarks, searchKey);
        });
    };
    render() {
        return /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
            className: "container",
            children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                className: "ms-4 me-4 mt-2 mb-2",
                children: [
                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                        className: "row",
                        children: [
                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                className: "col-lg-9 mt-2 mb-2",
                                children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("h4", {
                                    children: "Marks"
                                }, void 0, false, {
                                    fileName: "src/components/IT20128036/admin/viewMarks.js",
                                    lineNumber: 135,
                                    columnNumber: 9
                                }, this)
                            }, void 0, false, {
                                fileName: "src/components/IT20128036/admin/viewMarks.js",
                                lineNumber: 134,
                                columnNumber: 7
                            }, this),
                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                className: "col-lg-3 mt-2 mb-2",
                                children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("input", {
                                    className: "form-control",
                                    type: "search",
                                    placeholder: "Search",
                                    name: "searchQuery",
                                    onChange: this.handleSearchArea
                                }, void 0, false, {
                                    fileName: "src/components/IT20128036/admin/viewMarks.js",
                                    lineNumber: 139,
                                    columnNumber: 9
                                }, this)
                            }, void 0, false, {
                                fileName: "src/components/IT20128036/admin/viewMarks.js",
                                lineNumber: 137,
                                columnNumber: 7
                            }, this)
                        ]
                    }, void 0, true, {
                        fileName: "src/components/IT20128036/admin/viewMarks.js",
                        lineNumber: 133,
                        columnNumber: 7
                    }, this),
                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("table", {
                        className: "table",
                        children: [
                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("thead", {
                                children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("tr", {
                                    children: [
                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("th", {
                                            scope: "col",
                                            children: "#"
                                        }, void 0, false, {
                                            fileName: "src/components/IT20128036/admin/viewMarks.js",
                                            lineNumber: 154,
                                            columnNumber: 12
                                        }, this),
                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("th", {
                                            scope: "col",
                                            children: "Group ID"
                                        }, void 0, false, {
                                            fileName: "src/components/IT20128036/admin/viewMarks.js",
                                            lineNumber: 155,
                                            columnNumber: 12
                                        }, this),
                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("th", {
                                            scope: "col",
                                            children: "Submition Type"
                                        }, void 0, false, {
                                            fileName: "src/components/IT20128036/admin/viewMarks.js",
                                            lineNumber: 156,
                                            columnNumber: 12
                                        }, this),
                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("th", {
                                            scope: "col",
                                            children: "Marks"
                                        }, void 0, false, {
                                            fileName: "src/components/IT20128036/admin/viewMarks.js",
                                            lineNumber: 157,
                                            columnNumber: 12
                                        }, this),
                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("th", {
                                            scope: "col",
                                            children: "Grading Status"
                                        }, void 0, false, {
                                            fileName: "src/components/IT20128036/admin/viewMarks.js",
                                            lineNumber: 158,
                                            columnNumber: 12
                                        }, this),
                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("th", {
                                            scope: "col",
                                            children: "Marked By"
                                        }, void 0, false, {
                                            fileName: "src/components/IT20128036/admin/viewMarks.js",
                                            lineNumber: 159,
                                            columnNumber: 12
                                        }, this)
                                    ]
                                }, void 0, true, {
                                    fileName: "src/components/IT20128036/admin/viewMarks.js",
                                    lineNumber: 153,
                                    columnNumber: 10
                                }, this)
                            }, void 0, false, {
                                fileName: "src/components/IT20128036/admin/viewMarks.js",
                                lineNumber: 152,
                                columnNumber: 8
                            }, this),
                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("tbody", {
                                children: this.state.marks.map((marks, index)=>/*#__PURE__*/ _jsxDevRuntime.jsxDEV("tr", {
                                        children: [
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("th", {
                                                scope: "row",
                                                children: index + 1
                                            }, void 0, false, {
                                                fileName: "src/components/IT20128036/admin/viewMarks.js",
                                                lineNumber: 166,
                                                columnNumber: 14
                                            }, this),
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("td", {
                                                children: marks.groupId
                                            }, void 0, false, {
                                                fileName: "src/components/IT20128036/admin/viewMarks.js",
                                                lineNumber: 169,
                                                columnNumber: 14
                                            }, this),
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("td", {
                                                children: marks.type
                                            }, void 0, false, {
                                                fileName: "src/components/IT20128036/admin/viewMarks.js",
                                                lineNumber: 175,
                                                columnNumber: 14
                                            }, this),
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("td", {
                                                children: marks.marks
                                            }, void 0, false, {
                                                fileName: "src/components/IT20128036/admin/viewMarks.js",
                                                lineNumber: 177,
                                                columnNumber: 14
                                            }, this),
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("td", {
                                                children: marks.gradingStatus
                                            }, void 0, false, {
                                                fileName: "src/components/IT20128036/admin/viewMarks.js",
                                                lineNumber: 178,
                                                columnNumber: 14
                                            }, this),
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("td", {
                                                children: marks.markedby
                                            }, void 0, false, {
                                                fileName: "src/components/IT20128036/admin/viewMarks.js",
                                                lineNumber: 179,
                                                columnNumber: 14
                                            }, this),
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("td", {
                                                children: [
                                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("a", {
                                                        className: "btn btn-outline-primary",
                                                        href: `/marks/edit/${marks._id}`,
                                                        children: [
                                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("i", {
                                                                className: "fas fa-edit"
                                                            }, void 0, false, {
                                                                fileName: "src/components/IT20128036/admin/viewMarks.js",
                                                                lineNumber: 184,
                                                                columnNumber: 18
                                                            }, this),
                                                            "\xa0Edit"
                                                        ]
                                                    }, void 0, true, {
                                                        fileName: "src/components/IT20128036/admin/viewMarks.js",
                                                        lineNumber: 183,
                                                        columnNumber: 16
                                                    }, this),
                                                    "\xa0",
                                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("a", {
                                                        className: "btn btn-outline-danger",
                                                        href: "#",
                                                        onClick: ()=>{
                                                            this.onDelete(marks._id);
                                                        },
                                                        children: [
                                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("i", {
                                                                className: "far fa-trash-alt"
                                                            }, void 0, false, {
                                                                fileName: "src/components/IT20128036/admin/viewMarks.js",
                                                                lineNumber: 192,
                                                                columnNumber: 18
                                                            }, this),
                                                            "\xa0Delete"
                                                        ]
                                                    }, void 0, true, {
                                                        fileName: "src/components/IT20128036/admin/viewMarks.js",
                                                        lineNumber: 189,
                                                        columnNumber: 16
                                                    }, this)
                                                ]
                                            }, void 0, true, {
                                                fileName: "src/components/IT20128036/admin/viewMarks.js",
                                                lineNumber: 182,
                                                columnNumber: 14
                                            }, this)
                                        ]
                                    }, index, true, {
                                        fileName: "src/components/IT20128036/admin/viewMarks.js",
                                        lineNumber: 165,
                                        columnNumber: 12
                                    }, this)
                                )
                            }, void 0, false, {
                                fileName: "src/components/IT20128036/admin/viewMarks.js",
                                lineNumber: 163,
                                columnNumber: 8
                            }, this)
                        ]
                    }, void 0, true, {
                        fileName: "src/components/IT20128036/admin/viewMarks.js",
                        lineNumber: 151,
                        columnNumber: 6
                    }, this),
                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("button", {
                        className: "btn btn-outline-success",
                        children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("a", {
                            href: "/submitions/view",
                            style: {
                                textDecoration: 'none',
                                color: 'black'
                            },
                            children: "Add Marks"
                        }, void 0, false, {
                            fileName: "src/components/IT20128036/admin/viewMarks.js",
                            lineNumber: 209,
                            columnNumber: 50
                        }, this)
                    }, void 0, false, {
                        fileName: "src/components/IT20128036/admin/viewMarks.js",
                        lineNumber: 209,
                        columnNumber: 6
                    }, this)
                ]
            }, void 0, true, {
                fileName: "src/components/IT20128036/admin/viewMarks.js",
                lineNumber: 131,
                columnNumber: 1
            }, this)
        }, void 0, false, {
            fileName: "src/components/IT20128036/admin/viewMarks.js",
            lineNumber: 130,
            columnNumber: 5
        }, this);
    }
}
exports.default = viewMarks;

  $parcel$ReactRefreshHelpers$57da.postlude(module);
} finally {
  window.$RefreshReg$ = prevRefreshReg;
  window.$RefreshSig$ = prevRefreshSig;
}
},{"react/jsx-dev-runtime":"iTorj","react":"21dqq","axios":"jo6P5","sweetalert":"38a65","jwt-decode":"ljM1w","@parcel/transformer-js/src/esmodule-helpers.js":"gkKU3","@parcel/transformer-react-refresh-wrap/lib/helpers/helpers.js":"km3Ru"}],"3m7HQ":[function(require,module,exports) {
var $parcel$ReactRefreshHelpers$ef77 = require("@parcel/transformer-react-refresh-wrap/lib/helpers/helpers.js");
var prevRefreshReg = window.$RefreshReg$;
var prevRefreshSig = window.$RefreshSig$;
$parcel$ReactRefreshHelpers$ef77.prelude(module);

try {
var parcelHelpers = require("@parcel/transformer-js/src/esmodule-helpers.js");
parcelHelpers.defineInteropFlag(exports);
var _jsxDevRuntime = require("react/jsx-dev-runtime");
var _react = require("react");
var _reactDefault = parcelHelpers.interopDefault(_react);
var _axios = require("axios");
var _axiosDefault = parcelHelpers.interopDefault(_axios);
var _sweetalert = require("sweetalert");
var _sweetalertDefault = parcelHelpers.interopDefault(_sweetalert);
class EditMarks extends _react.Component {
    constructor(props){
        super(props);
        this.state = {
            marks: "",
            gradingStatus: ""
        };
    }
    handleInputChange = (e)=>{
        const { name , value  } = e.target;
        this.setState({
            ...this.state,
            [name]: value
        });
    };
    onSubmit = (e)=>{
        e.preventDefault();
        const id = this.props.match.params.id;
        const { marks , gradingStatus  } = this.state;
        const data = {
            marks: marks,
            gradingStatus: gradingStatus
        };
        console.log(data);
        //update marks
        _axiosDefault.default.put(`http://localhost:5000/mark/update/${id}`, data).then((res)=>{
            if (res.data.success) {
                _sweetalertDefault.default(" Updated Successfully !", "", "success").then((value)=>{
                    window.location = "/marks/view";
                });
                this.setState({
                    marks: "",
                    gradingStatus: ""
                });
            }
        });
    };
    componentDidMount() {
        // if(this.props.match && this.props.match.params.id){
        const id = this.props.match.params.id;
        //retrive marks to update  
        _axiosDefault.default.get(`http://localhost:5000/mark/${id}`).then((res)=>{
            if (res.data.success) {
                this.setState({
                    marks: res.data.mark.marks,
                    gradingStatus: res.data.mark.gradingStatus
                });
                console.log(this.state);
            }
        });
    // }
    }
    render() {
        return /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
            className: "col-md-8 mt-4 mx-auto",
            children: [
                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("h1", {
                    className: "h3 mb-3 font-weight-normal",
                    children: "Edit Marks"
                }, void 0, false, {
                    fileName: "src/components/IT20128036/admin/editMarks.js",
                    lineNumber: 104,
                    columnNumber: 8
                }, this),
                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("form", {
                    className: "needs-validation",
                    noValidate: true,
                    children: [
                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                            className: "form-group",
                            style: {
                                marginBottom: '15px'
                            },
                            children: [
                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("label", {
                                    style: {
                                        marginBottom: '5px'
                                    },
                                    children: "Marks"
                                }, void 0, false, {
                                    fileName: "src/components/IT20128036/admin/editMarks.js",
                                    lineNumber: 107,
                                    columnNumber: 12
                                }, this),
                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("input", {
                                    type: "text",
                                    className: "form-control",
                                    name: "marks",
                                    placeholder: "Edit Marks",
                                    value: this.state.marks,
                                    onChange: this.handleInputChange
                                }, void 0, false, {
                                    fileName: "src/components/IT20128036/admin/editMarks.js",
                                    lineNumber: 108,
                                    columnNumber: 12
                                }, this)
                            ]
                        }, void 0, true, {
                            fileName: "src/components/IT20128036/admin/editMarks.js",
                            lineNumber: 106,
                            columnNumber: 10
                        }, this),
                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                            className: "form-group",
                            style: {
                                marginBottom: '15px'
                            },
                            children: [
                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("label", {
                                    style: {
                                        marginBottom: '5px'
                                    },
                                    children: "Grading Status"
                                }, void 0, false, {
                                    fileName: "src/components/IT20128036/admin/editMarks.js",
                                    lineNumber: 119,
                                    columnNumber: 12
                                }, this),
                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("input", {
                                    type: "text",
                                    className: "form-control",
                                    name: "gradingStatus",
                                    placeholder: "Edit Grading Status",
                                    value: this.state.gradingStatus,
                                    onChange: this.handleInputChange
                                }, void 0, false, {
                                    fileName: "src/components/IT20128036/admin/editMarks.js",
                                    lineNumber: 120,
                                    columnNumber: 12
                                }, this)
                            ]
                        }, void 0, true, {
                            fileName: "src/components/IT20128036/admin/editMarks.js",
                            lineNumber: 118,
                            columnNumber: 10
                        }, this),
                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("button", {
                            className: "btn btn-outline-success",
                            type: "submit",
                            style: {
                                margintop: '15px'
                            },
                            onClick: this.onSubmit,
                            children: [
                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("i", {
                                    className: "far fa-check-square"
                                }, void 0, false, {
                                    fileName: "src/components/IT20128036/admin/editMarks.js",
                                    lineNumber: 135,
                                    columnNumber: 12
                                }, this),
                                "\xa0 Update"
                            ]
                        }, void 0, true, {
                            fileName: "src/components/IT20128036/admin/editMarks.js",
                            lineNumber: 134,
                            columnNumber: 10
                        }, this)
                    ]
                }, void 0, true, {
                    fileName: "src/components/IT20128036/admin/editMarks.js",
                    lineNumber: 105,
                    columnNumber: 8
                }, this)
            ]
        }, void 0, true, {
            fileName: "src/components/IT20128036/admin/editMarks.js",
            lineNumber: 103,
            columnNumber: 6
        }, this);
    }
}
exports.default = EditMarks;

  $parcel$ReactRefreshHelpers$ef77.postlude(module);
} finally {
  window.$RefreshReg$ = prevRefreshReg;
  window.$RefreshSig$ = prevRefreshSig;
}
},{"react/jsx-dev-runtime":"iTorj","react":"21dqq","axios":"jo6P5","sweetalert":"38a65","@parcel/transformer-js/src/esmodule-helpers.js":"gkKU3","@parcel/transformer-react-refresh-wrap/lib/helpers/helpers.js":"km3Ru"}],"gaQiH":[function(require,module,exports) {
var $parcel$ReactRefreshHelpers$ad98 = require("@parcel/transformer-react-refresh-wrap/lib/helpers/helpers.js");
var prevRefreshReg = window.$RefreshReg$;
var prevRefreshSig = window.$RefreshSig$;
$parcel$ReactRefreshHelpers$ad98.prelude(module);

try {
var parcelHelpers = require("@parcel/transformer-js/src/esmodule-helpers.js");
parcelHelpers.defineInteropFlag(exports);
var _jsxDevRuntime = require("react/jsx-dev-runtime");
var _react = require("react");
var _reactDefault = parcelHelpers.interopDefault(_react);
var _axios = require("axios");
var _axiosDefault = parcelHelpers.interopDefault(_axios);
var _adminNavBar = require("../../IT20125202/admin/AdminNavBar");
var _adminNavBarDefault = parcelHelpers.interopDefault(_adminNavBar);
var _markingSchemUpdate = require("./MarkingSchemUpdate");
var _markingSchemUpdateDefault = parcelHelpers.interopDefault(_markingSchemUpdate);
var _sweetalert = require("sweetalert");
var _sweetalertDefault = parcelHelpers.interopDefault(_sweetalert);
class ViewMarkingSchem_Admin extends _react.Component {
    constructor(props){
        super(props);
        this.state = {
            moduleName: "",
            assignment: "",
            criteria: "",
            allocateMark: "",
            markingSchemTitleID: "",
            markingCriteria: [],
            markingSchemTitle: [],
            tableSection: true,
            headerSection: true,
            updateID: ""
        };
    }
    componentDidMount() {
        this.retrieveTitles();
    }
    // Get marking titles from db
    retrieveTitles() {
        _axiosDefault.default.get("http://localhost:5000/getAll/markingTitles").then((res)=>{
            if (res.data.success) {
                this.setState({
                    markingSchemTitle: res.data.existingMarkingTitles
                });
                console.log(this.state.markingSchemTitle);
            }
        });
    }
    // Get marking criteria bellowns to marking title
    onGetMarkingCriteria = (titleID, moduleName, assignment)=>{
        this.setState({
            moduleName: moduleName,
            assignment: assignment
        });
        this.getMarkingCriteria(titleID);
    };
    getMarkingCriteria(titleID) {
        _axiosDefault.default.get(`http://localhost:5000/markings/get/${titleID}`).then((res)=>{
            if (res.data.success) this.setState({
                markingCriteria: res.data.existingMarkingCriteria,
                tableSection: false,
                headerSection: true
            });
            console.log(this.state.markingCriteria);
        });
    }
    // After clcik marking titles update -> open update section
    onUpdate = (titleID)=>{
        this.setState({
            updateID: titleID,
            headerSection: false
        });
    };
    // This return from MarkingSchemeUpdate child componenet
    changeUpdateSection = (returnState)=>{
        if (returnState) {
            this.setState({
                headerSection: true,
                tableSection: true
            });
            setTimeout(()=>{
                this.retrieveTitles();
            //this.getMarkingCriteria(updateid);
            }, 1000);
        } else this.setState({
            headerSection: true
        });
    };
    // Delete specific marking scheme title
    onDeleteTitle = (titleID)=>{
        // Delete conformation alert
        _sweetalertDefault.default({
            title: "Are you sure?",
            text: "Delete this Marking Scheme",
            icon: "warning",
            buttons: true,
            dangerMode: true
        }).then((willDelete)=>{
            if (willDelete) _axiosDefault.default.delete(`http://localhost:5000/markingTitle/delete/${titleID}`).then((res)=>{
                _sweetalertDefault.default("Deleted Successfull!", {
                    icon: "success"
                });
                setTimeout(()=>{
                    this.retrieveTitles();
                }, 1000);
            });
        });
    };
    // Delete specific marking scheme criteria
    onDeleteCriteria = (criteriaID)=>{
        _sweetalertDefault.default({
            title: "Are you sure?",
            text: "Delete this Marking Scheme Criteria",
            icon: "warning",
            buttons: true,
            dangerMode: true
        }).then((willDelete)=>{
            if (willDelete) _axiosDefault.default.delete(`http://localhost:5000/makingCriteria/delete/${criteriaID}`).then((res)=>{
                _sweetalertDefault.default("Deleted Successfull!", {
                    icon: "success"
                });
                setTimeout(()=>{
                    this.retrieveTitles();
                }, 1000);
            });
        });
    };
    render() {
        return /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
            className: "container",
            children: [
                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("br", {}, void 0, false, {
                    fileName: "src/components/IT20127046/admin/ViewMarkingSchem_Admin.js",
                    lineNumber: 145,
                    columnNumber: 9
                }, this),
                /*#__PURE__*/ _jsxDevRuntime.jsxDEV(_adminNavBarDefault.default, {}, void 0, false, {
                    fileName: "src/components/IT20127046/admin/ViewMarkingSchem_Admin.js",
                    lineNumber: 146,
                    columnNumber: 9
                }, this),
                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("br", {}, void 0, false, {
                    fileName: "src/components/IT20127046/admin/ViewMarkingSchem_Admin.js",
                    lineNumber: 147,
                    columnNumber: 9
                }, this),
                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("h4", {
                    children: "Marking Schemes"
                }, void 0, false, {
                    fileName: "src/components/IT20127046/admin/ViewMarkingSchem_Admin.js",
                    lineNumber: 148,
                    columnNumber: 9
                }, this),
                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("hr", {}, void 0, false, {
                    fileName: "src/components/IT20127046/admin/ViewMarkingSchem_Admin.js",
                    lineNumber: 149,
                    columnNumber: 9
                }, this),
                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                    className: "container",
                    children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                        className: "row",
                        children: [
                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                className: "col-3",
                                children: [
                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("h6", {
                                        children: "View Marking Schemes"
                                    }, void 0, false, {
                                        fileName: "src/components/IT20127046/admin/ViewMarkingSchem_Admin.js",
                                        lineNumber: 153,
                                        columnNumber: 15
                                    }, this),
                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("center", {
                                        children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("a", {
                                            className: "btn btn-outline-success m-2",
                                            href: "/add/marking",
                                            children: "Create Marking Scheme"
                                        }, void 0, false, {
                                            fileName: "src/components/IT20127046/admin/ViewMarkingSchem_Admin.js",
                                            lineNumber: 156,
                                            columnNumber: 17
                                        }, this)
                                    }, void 0, false, {
                                        fileName: "src/components/IT20127046/admin/ViewMarkingSchem_Admin.js",
                                        lineNumber: 155,
                                        columnNumber: 15
                                    }, this),
                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                        className: "container p-3 mb-2 bg-light text-dark",
                                        children: [
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("hr", {}, void 0, false, {
                                                fileName: "src/components/IT20127046/admin/ViewMarkingSchem_Admin.js",
                                                lineNumber: 162,
                                                columnNumber: 17
                                            }, this),
                                            this.state.markingSchemTitle.map((data, index)=>/*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                                    children: [
                                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("p", {
                                                            children: data.moduleName
                                                        }, void 0, false, {
                                                            fileName: "src/components/IT20127046/admin/ViewMarkingSchem_Admin.js",
                                                            lineNumber: 165,
                                                            columnNumber: 21
                                                        }, this),
                                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("p", {
                                                            children: data.assignment
                                                        }, void 0, false, {
                                                            fileName: "src/components/IT20127046/admin/ViewMarkingSchem_Admin.js",
                                                            lineNumber: 166,
                                                            columnNumber: 21
                                                        }, this),
                                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("p", {
                                                            children: data.postDate
                                                        }, void 0, false, {
                                                            fileName: "src/components/IT20127046/admin/ViewMarkingSchem_Admin.js",
                                                            lineNumber: 167,
                                                            columnNumber: 21
                                                        }, this),
                                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("a", {
                                                            className: "btn btn-outline-primary m-2",
                                                            href: "#",
                                                            onClick: ()=>this.onGetMarkingCriteria(data._id, data.moduleName, data.assignment)
                                                            ,
                                                            children: "View"
                                                        }, void 0, false, {
                                                            fileName: "src/components/IT20127046/admin/ViewMarkingSchem_Admin.js",
                                                            lineNumber: 169,
                                                            columnNumber: 21
                                                        }, this),
                                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("a", {
                                                            className: "btn btn-outline-success m-2",
                                                            onClick: ()=>this.onUpdate(data._id)
                                                            ,
                                                            children: "Edit"
                                                        }, void 0, false, {
                                                            fileName: "src/components/IT20127046/admin/ViewMarkingSchem_Admin.js",
                                                            lineNumber: 182,
                                                            columnNumber: 21
                                                        }, this),
                                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("a", {
                                                            className: "btn btn-outline-danger m-2",
                                                            onClick: ()=>this.onDeleteTitle(data._id)
                                                            ,
                                                            children: "Delete"
                                                        }, void 0, false, {
                                                            fileName: "src/components/IT20127046/admin/ViewMarkingSchem_Admin.js",
                                                            lineNumber: 188,
                                                            columnNumber: 21
                                                        }, this),
                                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("hr", {}, void 0, false, {
                                                            fileName: "src/components/IT20127046/admin/ViewMarkingSchem_Admin.js",
                                                            lineNumber: 194,
                                                            columnNumber: 21
                                                        }, this)
                                                    ]
                                                }, index, true, {
                                                    fileName: "src/components/IT20127046/admin/ViewMarkingSchem_Admin.js",
                                                    lineNumber: 164,
                                                    columnNumber: 19
                                                }, this)
                                            )
                                        ]
                                    }, void 0, true, {
                                        fileName: "src/components/IT20127046/admin/ViewMarkingSchem_Admin.js",
                                        lineNumber: 161,
                                        columnNumber: 15
                                    }, this)
                                ]
                            }, void 0, true, {
                                fileName: "src/components/IT20127046/admin/ViewMarkingSchem_Admin.js",
                                lineNumber: 152,
                                columnNumber: 13
                            }, this),
                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                className: "col-9",
                                children: [
                                    this.state.headerSection ? /*#__PURE__*/ _jsxDevRuntime.jsxDEV("h6", {
                                        children: " View Marking Secheme Details"
                                    }, void 0, false, {
                                        fileName: "src/components/IT20127046/admin/ViewMarkingSchem_Admin.js",
                                        lineNumber: 202,
                                        columnNumber: 17
                                    }, this) : /*#__PURE__*/ _jsxDevRuntime.jsxDEV(_markingSchemUpdateDefault.default, {
                                        data: {
                                            updateID: this.state.updateID,
                                            chnageSection: this.changeUpdateSection
                                        }
                                    }, void 0, false, {
                                        fileName: "src/components/IT20127046/admin/ViewMarkingSchem_Admin.js",
                                        lineNumber: 204,
                                        columnNumber: 17
                                    }, this),
                                    this.state.tableSection ? /*#__PURE__*/ _jsxDevRuntime.jsxDEV("p", {
                                        children: "Select a Marking Scheme"
                                    }, void 0, false, {
                                        fileName: "src/components/IT20127046/admin/ViewMarkingSchem_Admin.js",
                                        lineNumber: 213,
                                        columnNumber: 17
                                    }, this) : /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                        className: "container border border-secondary",
                                        children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                            className: "container p-4",
                                            children: [
                                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("center", {
                                                    children: [
                                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("h5", {
                                                            children: "Marking Schem"
                                                        }, void 0, false, {
                                                            fileName: "src/components/IT20127046/admin/ViewMarkingSchem_Admin.js",
                                                            lineNumber: 218,
                                                            columnNumber: 23
                                                        }, this),
                                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("h5", {
                                                            children: this.state.moduleName
                                                        }, void 0, false, {
                                                            fileName: "src/components/IT20127046/admin/ViewMarkingSchem_Admin.js",
                                                            lineNumber: 219,
                                                            columnNumber: 23
                                                        }, this),
                                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("h6", {
                                                            children: this.state.assignment
                                                        }, void 0, false, {
                                                            fileName: "src/components/IT20127046/admin/ViewMarkingSchem_Admin.js",
                                                            lineNumber: 220,
                                                            columnNumber: 23
                                                        }, this)
                                                    ]
                                                }, void 0, true, {
                                                    fileName: "src/components/IT20127046/admin/ViewMarkingSchem_Admin.js",
                                                    lineNumber: 217,
                                                    columnNumber: 21
                                                }, this),
                                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("table", {
                                                    className: "table",
                                                    children: [
                                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("thead", {
                                                            children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("tr", {
                                                                className: "b ",
                                                                children: [
                                                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("th", {
                                                                        scope: "col",
                                                                        children: "No"
                                                                    }, void 0, false, {
                                                                        fileName: "src/components/IT20127046/admin/ViewMarkingSchem_Admin.js",
                                                                        lineNumber: 226,
                                                                        columnNumber: 27
                                                                    }, this),
                                                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("th", {
                                                                        scope: "col",
                                                                        children: "Criteria"
                                                                    }, void 0, false, {
                                                                        fileName: "src/components/IT20127046/admin/ViewMarkingSchem_Admin.js",
                                                                        lineNumber: 227,
                                                                        columnNumber: 27
                                                                    }, this),
                                                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("th", {
                                                                        scope: "col",
                                                                        children: "Allocate Mark"
                                                                    }, void 0, false, {
                                                                        fileName: "src/components/IT20127046/admin/ViewMarkingSchem_Admin.js",
                                                                        lineNumber: 228,
                                                                        columnNumber: 27
                                                                    }, this)
                                                                ]
                                                            }, void 0, true, {
                                                                fileName: "src/components/IT20127046/admin/ViewMarkingSchem_Admin.js",
                                                                lineNumber: 225,
                                                                columnNumber: 25
                                                            }, this)
                                                        }, void 0, false, {
                                                            fileName: "src/components/IT20127046/admin/ViewMarkingSchem_Admin.js",
                                                            lineNumber: 224,
                                                            columnNumber: 23
                                                        }, this),
                                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("tbody", {
                                                            children: this.state.markingCriteria.map((data, index)=>/*#__PURE__*/ _jsxDevRuntime.jsxDEV("tr", {
                                                                    children: [
                                                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("th", {
                                                                            children: index + 1
                                                                        }, void 0, false, {
                                                                            fileName: "src/components/IT20127046/admin/ViewMarkingSchem_Admin.js",
                                                                            lineNumber: 235,
                                                                            columnNumber: 29
                                                                        }, this),
                                                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("td", {
                                                                            children: data.criteria
                                                                        }, void 0, false, {
                                                                            fileName: "src/components/IT20127046/admin/ViewMarkingSchem_Admin.js",
                                                                            lineNumber: 236,
                                                                            columnNumber: 29
                                                                        }, this),
                                                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("td", {
                                                                            children: [
                                                                                data.allocateMark,
                                                                                " "
                                                                            ]
                                                                        }, void 0, true, {
                                                                            fileName: "src/components/IT20127046/admin/ViewMarkingSchem_Admin.js",
                                                                            lineNumber: 237,
                                                                            columnNumber: 29
                                                                        }, this),
                                                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("td", {
                                                                            className: "text-center",
                                                                            children: [
                                                                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("a", {
                                                                                    className: "btn btn-outline-success",
                                                                                    href: ``,
                                                                                    children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("i", {
                                                                                        className: "fa fa-edit"
                                                                                    }, void 0, false, {
                                                                                        fileName: "src/components/IT20127046/admin/ViewMarkingSchem_Admin.js",
                                                                                        lineNumber: 241,
                                                                                        columnNumber: 33
                                                                                    }, this)
                                                                                }, void 0, false, {
                                                                                    fileName: "src/components/IT20127046/admin/ViewMarkingSchem_Admin.js",
                                                                                    lineNumber: 240,
                                                                                    columnNumber: 31
                                                                                }, this),
                                                                                "\xa0",
                                                                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("a", {
                                                                                    className: "btn btn-outline-danger",
                                                                                    href: "#",
                                                                                    onClick: ()=>this.onDeleteCriteria(data._id)
                                                                                    ,
                                                                                    children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("i", {
                                                                                        className: "fa fa-trash"
                                                                                    }, void 0, false, {
                                                                                        fileName: "src/components/IT20127046/admin/ViewMarkingSchem_Admin.js",
                                                                                        lineNumber: 249,
                                                                                        columnNumber: 33
                                                                                    }, this)
                                                                                }, void 0, false, {
                                                                                    fileName: "src/components/IT20127046/admin/ViewMarkingSchem_Admin.js",
                                                                                    lineNumber: 244,
                                                                                    columnNumber: 31
                                                                                }, this)
                                                                            ]
                                                                        }, void 0, true, {
                                                                            fileName: "src/components/IT20127046/admin/ViewMarkingSchem_Admin.js",
                                                                            lineNumber: 239,
                                                                            columnNumber: 29
                                                                        }, this)
                                                                    ]
                                                                }, index, true, {
                                                                    fileName: "src/components/IT20127046/admin/ViewMarkingSchem_Admin.js",
                                                                    lineNumber: 234,
                                                                    columnNumber: 27
                                                                }, this)
                                                            )
                                                        }, void 0, false, {
                                                            fileName: "src/components/IT20127046/admin/ViewMarkingSchem_Admin.js",
                                                            lineNumber: 232,
                                                            columnNumber: 23
                                                        }, this)
                                                    ]
                                                }, void 0, true, {
                                                    fileName: "src/components/IT20127046/admin/ViewMarkingSchem_Admin.js",
                                                    lineNumber: 223,
                                                    columnNumber: 21
                                                }, this),
                                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("br", {}, void 0, false, {
                                                    fileName: "src/components/IT20127046/admin/ViewMarkingSchem_Admin.js",
                                                    lineNumber: 256,
                                                    columnNumber: 21
                                                }, this),
                                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("br", {}, void 0, false, {
                                                    fileName: "src/components/IT20127046/admin/ViewMarkingSchem_Admin.js",
                                                    lineNumber: 257,
                                                    columnNumber: 21
                                                }, this)
                                            ]
                                        }, void 0, true, {
                                            fileName: "src/components/IT20127046/admin/ViewMarkingSchem_Admin.js",
                                            lineNumber: 216,
                                            columnNumber: 19
                                        }, this)
                                    }, void 0, false, {
                                        fileName: "src/components/IT20127046/admin/ViewMarkingSchem_Admin.js",
                                        lineNumber: 215,
                                        columnNumber: 17
                                    }, this)
                                ]
                            }, void 0, true, {
                                fileName: "src/components/IT20127046/admin/ViewMarkingSchem_Admin.js",
                                lineNumber: 200,
                                columnNumber: 13
                            }, this)
                        ]
                    }, void 0, true, {
                        fileName: "src/components/IT20127046/admin/ViewMarkingSchem_Admin.js",
                        lineNumber: 151,
                        columnNumber: 11
                    }, this)
                }, void 0, false, {
                    fileName: "src/components/IT20127046/admin/ViewMarkingSchem_Admin.js",
                    lineNumber: 150,
                    columnNumber: 9
                }, this)
            ]
        }, void 0, true, {
            fileName: "src/components/IT20127046/admin/ViewMarkingSchem_Admin.js",
            lineNumber: 144,
            columnNumber: 7
        }, this);
    }
}
exports.default = ViewMarkingSchem_Admin;

  $parcel$ReactRefreshHelpers$ad98.postlude(module);
} finally {
  window.$RefreshReg$ = prevRefreshReg;
  window.$RefreshSig$ = prevRefreshSig;
}
},{"react/jsx-dev-runtime":"iTorj","react":"21dqq","axios":"jo6P5","../../IT20125202/admin/AdminNavBar":"kb2lK","./MarkingSchemUpdate":"8n0wr","sweetalert":"38a65","@parcel/transformer-js/src/esmodule-helpers.js":"gkKU3","@parcel/transformer-react-refresh-wrap/lib/helpers/helpers.js":"km3Ru"}],"8n0wr":[function(require,module,exports) {
var $parcel$ReactRefreshHelpers$8fdf = require("@parcel/transformer-react-refresh-wrap/lib/helpers/helpers.js");
var prevRefreshReg = window.$RefreshReg$;
var prevRefreshSig = window.$RefreshSig$;
$parcel$ReactRefreshHelpers$8fdf.prelude(module);

try {
var parcelHelpers = require("@parcel/transformer-js/src/esmodule-helpers.js");
parcelHelpers.defineInteropFlag(exports);
var _jsxDevRuntime = require("react/jsx-dev-runtime");
var _react = require("react");
var _reactDefault = parcelHelpers.interopDefault(_react);
var _axios = require("axios");
var _axiosDefault = parcelHelpers.interopDefault(_axios);
var _sweetalert = require("sweetalert");
var _sweetalertDefault = parcelHelpers.interopDefault(_sweetalert);
class MarkingSchemUpdate extends _react.Component {
    constructor(props){
        super(props);
        this.state = {
            updateID: "",
            moduleName: "",
            assignment: ""
        };
    }
    handleInputChange = (e)=>{
        const { name , value  } = e.target;
        this.setState({
            ...this.state,
            [name]: value
        });
    };
    handleChange = (e)=>{
        const { name , value  } = e.target;
        this.setState({
            ...this.state,
            [name]: value
        });
    };
    // Get id send from ViewMarkingScheme_Admin parent components and get marking title details using id
    componentDidMount() {
        const updateid = this.props.data.updateID;
        this.setState({
            updateID: updateid
        });
        _axiosDefault.default.get(`http://localhost:5000/markingTitle/get/${updateid}`).then((res)=>{
            if (res.data.success) this.setState({
                moduleName: res.data.schemeTitle.moduleName,
                assignment: res.data.schemeTitle.assignment
            });
        });
    }
    // After click update button -> update details and callback parent componenet
    onSubmitUpdate = (e)=>{
        e.preventDefault();
        const id = this.state.updateID;
        const { moduleName , assignment  } = this.state;
        const data = {
            moduleName: moduleName,
            assignment: assignment
        };
        _axiosDefault.default.put(`http://localhost:5000/markingTitle/update/${id}`, data).then((res)=>{
            if (res.data.success) _sweetalertDefault.default({
                title: "Update Successfull!",
                icon: "success"
            });
        });
        this.props.data.chnageSection(true);
    };
    render() {
        return /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
            className: "container",
            children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                className: "container p-3 mb-2 text-dark",
                style: {
                    backgroundColor: "#F0FFFF"
                },
                children: [
                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("a", {
                        className: "btn btn-outline-danger mb-2",
                        onClick: ()=>{
                            this.props.data.chnageSection(false);
                        },
                        children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("i", {
                            class: "fa fa-times",
                            "aria-hidden": "true"
                        }, void 0, false, {
                            fileName: "src/components/IT20127046/admin/MarkingSchemUpdate.js",
                            lineNumber: 84,
                            columnNumber: 13
                        }, this)
                    }, void 0, false, {
                        fileName: "src/components/IT20127046/admin/MarkingSchemUpdate.js",
                        lineNumber: 83,
                        columnNumber: 9
                    }, this),
                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("h6", {
                        children: "Update Marking Scheme"
                    }, void 0, false, {
                        fileName: "src/components/IT20127046/admin/MarkingSchemUpdate.js",
                        lineNumber: 87,
                        columnNumber: 11
                    }, this),
                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("form", {
                        onSubmit: this.onSubmitUpdate,
                        children: [
                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                className: "row",
                                children: [
                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                        className: "col",
                                        children: [
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("label", {
                                                htmlFor: "moduleName",
                                                children: "Module"
                                            }, void 0, false, {
                                                fileName: "src/components/IT20127046/admin/MarkingSchemUpdate.js",
                                                lineNumber: 92,
                                                columnNumber: 17
                                            }, this),
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("input", {
                                                style: {
                                                    width: "350px"
                                                },
                                                type: "text",
                                                className: "form-control",
                                                id: "moduleName",
                                                name: "moduleName",
                                                value: this.state.moduleName,
                                                onChange: this.handleInputChange
                                            }, void 0, false, {
                                                fileName: "src/components/IT20127046/admin/MarkingSchemUpdate.js",
                                                lineNumber: 93,
                                                columnNumber: 17
                                            }, this)
                                        ]
                                    }, void 0, true, {
                                        fileName: "src/components/IT20127046/admin/MarkingSchemUpdate.js",
                                        lineNumber: 91,
                                        columnNumber: 15
                                    }, this),
                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                        className: "col",
                                        children: [
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("label", {
                                                htmlFor: "assignment",
                                                children: "Assignment"
                                            }, void 0, false, {
                                                fileName: "src/components/IT20127046/admin/MarkingSchemUpdate.js",
                                                lineNumber: 104,
                                                columnNumber: 17
                                            }, this),
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("input", {
                                                style: {
                                                    width: "350px"
                                                },
                                                type: "text",
                                                className: "form-control",
                                                id: "assignment",
                                                name: "assignment",
                                                value: this.state.assignment,
                                                onChange: this.handleInputChange
                                            }, void 0, false, {
                                                fileName: "src/components/IT20127046/admin/MarkingSchemUpdate.js",
                                                lineNumber: 105,
                                                columnNumber: 17
                                            }, this)
                                        ]
                                    }, void 0, true, {
                                        fileName: "src/components/IT20127046/admin/MarkingSchemUpdate.js",
                                        lineNumber: 103,
                                        columnNumber: 15
                                    }, this)
                                ]
                            }, void 0, true, {
                                fileName: "src/components/IT20127046/admin/MarkingSchemUpdate.js",
                                lineNumber: 90,
                                columnNumber: 13
                            }, this),
                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("br", {}, void 0, false, {
                                fileName: "src/components/IT20127046/admin/MarkingSchemUpdate.js",
                                lineNumber: 116,
                                columnNumber: 13
                            }, this),
                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("button", {
                                type: "submit",
                                className: "btn btn-outline-primary mt-2 mb-2",
                                children: [
                                    "Update\xa0 ",
                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("i", {
                                        class: "fa fa-edit",
                                        "aria-hidden": "true"
                                    }, void 0, false, {
                                        fileName: "src/components/IT20127046/admin/MarkingSchemUpdate.js",
                                        lineNumber: 119,
                                        columnNumber: 28
                                    }, this)
                                ]
                            }, void 0, true, {
                                fileName: "src/components/IT20127046/admin/MarkingSchemUpdate.js",
                                lineNumber: 118,
                                columnNumber: 13
                            }, this)
                        ]
                    }, void 0, true, {
                        fileName: "src/components/IT20127046/admin/MarkingSchemUpdate.js",
                        lineNumber: 89,
                        columnNumber: 11
                    }, this)
                ]
            }, void 0, true, {
                fileName: "src/components/IT20127046/admin/MarkingSchemUpdate.js",
                lineNumber: 81,
                columnNumber: 9
            }, this)
        }, void 0, false, {
            fileName: "src/components/IT20127046/admin/MarkingSchemUpdate.js",
            lineNumber: 80,
            columnNumber: 7
        }, this);
    }
}
exports.default = MarkingSchemUpdate;

  $parcel$ReactRefreshHelpers$8fdf.postlude(module);
} finally {
  window.$RefreshReg$ = prevRefreshReg;
  window.$RefreshSig$ = prevRefreshSig;
}
},{"react/jsx-dev-runtime":"iTorj","react":"21dqq","axios":"jo6P5","sweetalert":"38a65","@parcel/transformer-js/src/esmodule-helpers.js":"gkKU3","@parcel/transformer-react-refresh-wrap/lib/helpers/helpers.js":"km3Ru"}],"gUCdL":[function(require,module,exports) {
var $parcel$ReactRefreshHelpers$14a3 = require("@parcel/transformer-react-refresh-wrap/lib/helpers/helpers.js");
var prevRefreshReg = window.$RefreshReg$;
var prevRefreshSig = window.$RefreshSig$;
$parcel$ReactRefreshHelpers$14a3.prelude(module);

try {
var parcelHelpers = require("@parcel/transformer-js/src/esmodule-helpers.js");
parcelHelpers.defineInteropFlag(exports);
var _jsxDevRuntime = require("react/jsx-dev-runtime");
var _react = require("react");
var _reactDefault = parcelHelpers.interopDefault(_react);
var _axios = require("axios");
var _axiosDefault = parcelHelpers.interopDefault(_axios);
var _jwtDecode = require("jwt-decode");
var _jwtDecodeDefault = parcelHelpers.interopDefault(_jwtDecode);
var _jsFileDownload = require("js-file-download");
var _jsFileDownloadDefault = parcelHelpers.interopDefault(_jsFileDownload);
var _rightSidePanel = require("../RightSidePanel");
var _rightSidePanelDefault = parcelHelpers.interopDefault(_rightSidePanel);
class ViewMarkingScheme extends _react.Component {
    constructor(){
        super();
        this.state = {
            userType: "",
            markingSchemTitle: []
        };
    }
    componentDidMount() {
        document.title = "User Profile";
        if (localStorage.userToken) {
            const usertoken = localStorage.userToken;
            const decoded = _jwtDecodeDefault.default(usertoken);
            this.setState({
                userType: decoded.type
            });
        }
        this.retrieveTitles();
    }
    // Get marking titles from db
    retrieveTitles() {
        _axiosDefault.default.get("http://localhost:5000/getAll/markingTitles").then((res)=>{
            if (res.data.success) {
                this.setState({
                    markingSchemTitle: res.data.existingMarkingTitles
                });
                console.log(this.state.markingSchemTitle);
            }
        });
    }
    render() {
        const loginRegLink = /*#__PURE__*/ _jsxDevRuntime.jsxDEV("ul", {
            className: "nav nav-tabs",
            children: [
                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("li", {
                    className: "nav-item",
                    children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("a", {
                        className: "nav-link",
                        "aria-current": "page",
                        href: "/user/login",
                        style: {
                            textDecoration: "none",
                            color: "white"
                        },
                        children: "Login"
                    }, void 0, false, {
                        fileName: "src/components/IT20127046/user/ViewMarkingScheme.js",
                        lineNumber: 45,
                        columnNumber: 11
                    }, this)
                }, void 0, false, {
                    fileName: "src/components/IT20127046/user/ViewMarkingScheme.js",
                    lineNumber: 44,
                    columnNumber: 9
                }, this),
                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("li", {
                    className: "nav-item",
                    children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("a", {
                        className: "nav-link",
                        "aria-current": "page",
                        href: "/user/registration",
                        style: {
                            textDecoration: "none",
                            color: "white"
                        },
                        children: "Register"
                    }, void 0, false, {
                        fileName: "src/components/IT20127046/user/ViewMarkingScheme.js",
                        lineNumber: 55,
                        columnNumber: 11
                    }, this)
                }, void 0, false, {
                    fileName: "src/components/IT20127046/user/ViewMarkingScheme.js",
                    lineNumber: 54,
                    columnNumber: 9
                }, this)
            ]
        }, void 0, true, {
            fileName: "src/components/IT20127046/user/ViewMarkingScheme.js",
            lineNumber: 43,
            columnNumber: 7
        }, this);
        // Internal CSS
        const headlineBar = {
            backgroundColor: "#DCDCDC",
            padding: "5px",
            marginBottom: "5px"
        };
        let userContent;
        if (this.state.userType === "Student") userContent = /*#__PURE__*/ _jsxDevRuntime.jsxDEV("p", {
            children: "Hello Student"
        }, void 0, false, {
            fileName: "src/components/IT20127046/user/ViewMarkingScheme.js",
            lineNumber: 77,
            columnNumber: 21
        }, this);
        else {
            this.state.userType === "Supervisor" || "Panel Member";
            userContent = /*#__PURE__*/ _jsxDevRuntime.jsxDEV("p", {
                children: "Hello Supervisor"
            }, void 0, false, {
                fileName: "src/components/IT20127046/user/ViewMarkingScheme.js",
                lineNumber: 79,
                columnNumber: 21
            }, this);
        }
        return /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
            className: "container",
            children: localStorage.userToken ? /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                children: [
                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("br", {}, void 0, false, {
                        fileName: "src/components/IT20127046/user/ViewMarkingScheme.js",
                        lineNumber: 86,
                        columnNumber: 13
                    }, this),
                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("h4", {
                        children: [
                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("i", {
                                class: "fa fa-table",
                                "aria-hidden": "true"
                            }, void 0, false, {
                                fileName: "src/components/IT20127046/user/ViewMarkingScheme.js",
                                lineNumber: 87,
                                columnNumber: 17
                            }, this),
                            "\xa0\xa0Marking Schemes"
                        ]
                    }, void 0, true, {
                        fileName: "src/components/IT20127046/user/ViewMarkingScheme.js",
                        lineNumber: 87,
                        columnNumber: 13
                    }, this),
                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("hr", {}, void 0, false, {
                        fileName: "src/components/IT20127046/user/ViewMarkingScheme.js",
                        lineNumber: 88,
                        columnNumber: 13
                    }, this),
                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                        className: "container",
                        children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                            className: "row",
                            children: [
                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                    className: "col-9",
                                    children: [
                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                            style: headlineBar,
                                            children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("h6", {
                                                children: "Note"
                                            }, void 0, false, {
                                                fileName: "src/components/IT20127046/user/ViewMarkingScheme.js",
                                                lineNumber: 93,
                                                columnNumber: 21
                                            }, this)
                                        }, void 0, false, {
                                            fileName: "src/components/IT20127046/user/ViewMarkingScheme.js",
                                            lineNumber: 92,
                                            columnNumber: 19
                                        }, this),
                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("p", {
                                            children: [
                                                "Document Template Document Template Document Template Document Template Document Template",
                                                " "
                                            ]
                                        }, void 0, true, {
                                            fileName: "src/components/IT20127046/user/ViewMarkingScheme.js",
                                            lineNumber: 95,
                                            columnNumber: 19
                                        }, this),
                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                            style: headlineBar,
                                            children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("h6", {
                                                children: "Marking Schemes"
                                            }, void 0, false, {
                                                fileName: "src/components/IT20127046/user/ViewMarkingScheme.js",
                                                lineNumber: 101,
                                                columnNumber: 21
                                            }, this)
                                        }, void 0, false, {
                                            fileName: "src/components/IT20127046/user/ViewMarkingScheme.js",
                                            lineNumber: 100,
                                            columnNumber: 19
                                        }, this),
                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                            className: "container",
                                            children: [
                                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("hr", {}, void 0, false, {
                                                    fileName: "src/components/IT20127046/user/ViewMarkingScheme.js",
                                                    lineNumber: 105,
                                                    columnNumber: 21
                                                }, this),
                                                this.state.markingSchemTitle.map((data, index)=>/*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                                        children: [
                                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("p", {
                                                                children: data.moduleName
                                                            }, void 0, false, {
                                                                fileName: "src/components/IT20127046/user/ViewMarkingScheme.js",
                                                                lineNumber: 108,
                                                                columnNumber: 25
                                                            }, this),
                                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("p", {
                                                                children: data.assignment
                                                            }, void 0, false, {
                                                                fileName: "src/components/IT20127046/user/ViewMarkingScheme.js",
                                                                lineNumber: 109,
                                                                columnNumber: 25
                                                            }, this),
                                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("a", {
                                                                className: "btn btn-outline-success m-2",
                                                                href: `/user/display/marking/${data._id}`,
                                                                children: "View"
                                                            }, void 0, false, {
                                                                fileName: "src/components/IT20127046/user/ViewMarkingScheme.js",
                                                                lineNumber: 111,
                                                                columnNumber: 25
                                                            }, this),
                                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("a", {
                                                                className: "btn btn-outline-primary m-2",
                                                                href: `/user/download/marking`,
                                                                children: "Download"
                                                            }, void 0, false, {
                                                                fileName: "src/components/IT20127046/user/ViewMarkingScheme.js",
                                                                lineNumber: 117,
                                                                columnNumber: 25
                                                            }, this),
                                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("hr", {}, void 0, false, {
                                                                fileName: "src/components/IT20127046/user/ViewMarkingScheme.js",
                                                                lineNumber: 123,
                                                                columnNumber: 25
                                                            }, this)
                                                        ]
                                                    }, index, true, {
                                                        fileName: "src/components/IT20127046/user/ViewMarkingScheme.js",
                                                        lineNumber: 107,
                                                        columnNumber: 23
                                                    }, this)
                                                )
                                            ]
                                        }, void 0, true, {
                                            fileName: "src/components/IT20127046/user/ViewMarkingScheme.js",
                                            lineNumber: 104,
                                            columnNumber: 19
                                        }, this)
                                    ]
                                }, void 0, true, {
                                    fileName: "src/components/IT20127046/user/ViewMarkingScheme.js",
                                    lineNumber: 91,
                                    columnNumber: 17
                                }, this),
                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                    className: "col-3",
                                    children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV(_rightSidePanelDefault.default, {}, void 0, false, {
                                        fileName: "src/components/IT20127046/user/ViewMarkingScheme.js",
                                        lineNumber: 129,
                                        columnNumber: 19
                                    }, this)
                                }, void 0, false, {
                                    fileName: "src/components/IT20127046/user/ViewMarkingScheme.js",
                                    lineNumber: 128,
                                    columnNumber: 17
                                }, this)
                            ]
                        }, void 0, true, {
                            fileName: "src/components/IT20127046/user/ViewMarkingScheme.js",
                            lineNumber: 90,
                            columnNumber: 15
                        }, this)
                    }, void 0, false, {
                        fileName: "src/components/IT20127046/user/ViewMarkingScheme.js",
                        lineNumber: 89,
                        columnNumber: 13
                    }, this)
                ]
            }, void 0, true, {
                fileName: "src/components/IT20127046/user/ViewMarkingScheme.js",
                lineNumber: 85,
                columnNumber: 11
            }, this) : loginRegLink
        }, void 0, false, {
            fileName: "src/components/IT20127046/user/ViewMarkingScheme.js",
            lineNumber: 83,
            columnNumber: 7
        }, this);
    }
}
exports.default = ViewMarkingScheme;

  $parcel$ReactRefreshHelpers$14a3.postlude(module);
} finally {
  window.$RefreshReg$ = prevRefreshReg;
  window.$RefreshSig$ = prevRefreshSig;
}
},{"react/jsx-dev-runtime":"iTorj","react":"21dqq","axios":"jo6P5","jwt-decode":"ljM1w","js-file-download":"cLODl","../RightSidePanel":"kymky","@parcel/transformer-js/src/esmodule-helpers.js":"gkKU3","@parcel/transformer-react-refresh-wrap/lib/helpers/helpers.js":"km3Ru"}],"2k3nQ":[function(require,module,exports) {
var $parcel$ReactRefreshHelpers$a519 = require("@parcel/transformer-react-refresh-wrap/lib/helpers/helpers.js");
var prevRefreshReg = window.$RefreshReg$;
var prevRefreshSig = window.$RefreshSig$;
$parcel$ReactRefreshHelpers$a519.prelude(module);

try {
var parcelHelpers = require("@parcel/transformer-js/src/esmodule-helpers.js");
parcelHelpers.defineInteropFlag(exports);
var _jsxDevRuntime = require("react/jsx-dev-runtime");
var _react = require("react");
var _reactDefault = parcelHelpers.interopDefault(_react);
var _axios = require("axios");
var _axiosDefault = parcelHelpers.interopDefault(_axios);
var _jwtDecode = require("jwt-decode");
var _jwtDecodeDefault = parcelHelpers.interopDefault(_jwtDecode);
var _jsFileDownload = require("js-file-download");
var _jsFileDownloadDefault = parcelHelpers.interopDefault(_jsFileDownload);
var _rightSidePanel = require("../RightSidePanel");
var _rightSidePanelDefault = parcelHelpers.interopDefault(_rightSidePanel);
class ViewDocumentTemplate extends _react.Component {
    constructor(){
        super();
        this.state = {
            userType: "",
            documentTemplate: []
        };
    }
    componentDidMount() {
        document.title = "Document Template";
        // Check user login
        if (!localStorage.userToken) swal({
            title: "Please login first",
            icon: "warning"
        }).then((value)=>{
            if (value) {
                this.props.history.push("/user/login");
                window.location.reload();
            }
        });
        else {
            const usertoken = localStorage.userToken;
            const decoded = _jwtDecodeDefault.default(usertoken);
            this.setState({
                userType: decoded.type
            });
        }
        this.retrieveDocumentTemp();
    }
    // To get all data
    retrieveDocumentTemp() {
        _axiosDefault.default.get("http://localhost:5000/template/getAll").then((res)=>{
            if (res.data.success) {
                this.setState({
                    documentTemplate: res.data.exsitingDocumentTemp
                });
                console.log(this.state.documentTemplate);
            }
        });
    }
    // Download File
    downloadFile = (fileName)=>{
        const data = {
            fileName: fileName
        };
        _axiosDefault.default({
            url: "http://localhost:5000/file/download",
            data,
            method: "POST",
            responseType: "blob"
        }).then((res)=>{
            console.log(res);
            _jsFileDownloadDefault.default(res.data, fileName);
        });
    };
    render() {
        // If user not logged
        const loginRegLink = /*#__PURE__*/ _jsxDevRuntime.jsxDEV("ul", {
            className: "nav nav-tabs",
            children: [
                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("li", {
                    className: "nav-item",
                    children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("a", {
                        className: "nav-link",
                        "aria-current": "page",
                        href: "/user/login",
                        style: {
                            textDecoration: "none",
                            color: "white"
                        },
                        children: "Login"
                    }, void 0, false, {
                        fileName: "src/components/IT20127046/user/ViewDocumentTemplate.js",
                        lineNumber: 75,
                        columnNumber: 11
                    }, this)
                }, void 0, false, {
                    fileName: "src/components/IT20127046/user/ViewDocumentTemplate.js",
                    lineNumber: 74,
                    columnNumber: 9
                }, this),
                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("li", {
                    className: "nav-item",
                    children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("a", {
                        className: "nav-link",
                        "aria-current": "page",
                        href: "/user/registration",
                        style: {
                            textDecoration: "none",
                            color: "white"
                        },
                        children: "Register"
                    }, void 0, false, {
                        fileName: "src/components/IT20127046/user/ViewDocumentTemplate.js",
                        lineNumber: 85,
                        columnNumber: 11
                    }, this)
                }, void 0, false, {
                    fileName: "src/components/IT20127046/user/ViewDocumentTemplate.js",
                    lineNumber: 84,
                    columnNumber: 9
                }, this)
            ]
        }, void 0, true, {
            fileName: "src/components/IT20127046/user/ViewDocumentTemplate.js",
            lineNumber: 73,
            columnNumber: 7
        }, this);
        // Internal CSS
        const headlineBar = {
            backgroundColor: "#DCDCDC",
            padding: "5px",
            marginBottom: "5px"
        };
        return /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
            className: "container",
            children: localStorage.userToken ? /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                children: [
                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("br", {}, void 0, false, {
                        fileName: "src/components/IT20127046/user/ViewDocumentTemplate.js",
                        lineNumber: 109,
                        columnNumber: 13
                    }, this),
                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("h4", {
                        children: [
                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("i", {
                                class: "fa fa-file-text",
                                "aria-hidden": "true"
                            }, void 0, false, {
                                fileName: "src/components/IT20127046/user/ViewDocumentTemplate.js",
                                lineNumber: 110,
                                columnNumber: 17
                            }, this),
                            "\xa0\xa0Document Template"
                        ]
                    }, void 0, true, {
                        fileName: "src/components/IT20127046/user/ViewDocumentTemplate.js",
                        lineNumber: 110,
                        columnNumber: 13
                    }, this),
                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("hr", {}, void 0, false, {
                        fileName: "src/components/IT20127046/user/ViewDocumentTemplate.js",
                        lineNumber: 111,
                        columnNumber: 13
                    }, this),
                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                        className: "container",
                        children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                            className: "row",
                            children: [
                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                    className: "col-9",
                                    children: [
                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                            style: headlineBar,
                                            children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("h6", {
                                                children: "Note"
                                            }, void 0, false, {
                                                fileName: "src/components/IT20127046/user/ViewDocumentTemplate.js",
                                                lineNumber: 116,
                                                columnNumber: 21
                                            }, this)
                                        }, void 0, false, {
                                            fileName: "src/components/IT20127046/user/ViewDocumentTemplate.js",
                                            lineNumber: 115,
                                            columnNumber: 19
                                        }, this),
                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("p", {
                                            children: [
                                                "Document Template Document Template Document Template Document Template Document Template",
                                                " "
                                            ]
                                        }, void 0, true, {
                                            fileName: "src/components/IT20127046/user/ViewDocumentTemplate.js",
                                            lineNumber: 118,
                                            columnNumber: 19
                                        }, this),
                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                            style: headlineBar,
                                            children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("h6", {
                                                children: "Proposal Document Template"
                                            }, void 0, false, {
                                                fileName: "src/components/IT20127046/user/ViewDocumentTemplate.js",
                                                lineNumber: 126,
                                                columnNumber: 21
                                            }, this)
                                        }, void 0, false, {
                                            fileName: "src/components/IT20127046/user/ViewDocumentTemplate.js",
                                            lineNumber: 125,
                                            columnNumber: 19
                                        }, this),
                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                            className: "container",
                                            children: this.state.documentTemplate.map((template, index)=>{
                                                if (template.documentType === "Proposal") return /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                                    className: "mb-2",
                                                    children: [
                                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("p", {
                                                            children: [
                                                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("i", {
                                                                    class: "fa fa-file-text-o",
                                                                    "aria-hidden": "true"
                                                                }, void 0, false, {
                                                                    fileName: "src/components/IT20127046/user/ViewDocumentTemplate.js",
                                                                    lineNumber: 133,
                                                                    columnNumber: 36
                                                                }, this),
                                                                "\xa0 ",
                                                                template.files,
                                                                " - ",
                                                                template.description
                                                            ]
                                                        }, void 0, true, {
                                                            fileName: "src/components/IT20127046/user/ViewDocumentTemplate.js",
                                                            lineNumber: 133,
                                                            columnNumber: 33
                                                        }, this),
                                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("a", {
                                                            className: "btn btn-sm btn-outline-primary m-2",
                                                            onClick: ()=>this.downloadFile(template.files)
                                                            ,
                                                            children: "Download"
                                                        }, void 0, false, {
                                                            fileName: "src/components/IT20127046/user/ViewDocumentTemplate.js",
                                                            lineNumber: 134,
                                                            columnNumber: 33
                                                        }, this)
                                                    ]
                                                }, index, true, {
                                                    fileName: "src/components/IT20127046/user/ViewDocumentTemplate.js",
                                                    lineNumber: 132,
                                                    columnNumber: 29
                                                }, this);
                                            })
                                        }, void 0, false, {
                                            fileName: "src/components/IT20127046/user/ViewDocumentTemplate.js",
                                            lineNumber: 128,
                                            columnNumber: 19
                                        }, this),
                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                            style: headlineBar,
                                            children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("h6", {
                                                children: "Presentation Template"
                                            }, void 0, false, {
                                                fileName: "src/components/IT20127046/user/ViewDocumentTemplate.js",
                                                lineNumber: 149,
                                                columnNumber: 21
                                            }, this)
                                        }, void 0, false, {
                                            fileName: "src/components/IT20127046/user/ViewDocumentTemplate.js",
                                            lineNumber: 148,
                                            columnNumber: 19
                                        }, this),
                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                            className: "container",
                                            children: this.state.documentTemplate.map((template, index)=>{
                                                if (template.documentType === "Presentation") return /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                                    className: "mb-2",
                                                    children: [
                                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("p", {
                                                            children: [
                                                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("i", {
                                                                    class: "fa fa-file-text-o",
                                                                    "aria-hidden": "true"
                                                                }, void 0, false, {
                                                                    fileName: "src/components/IT20127046/user/ViewDocumentTemplate.js",
                                                                    lineNumber: 156,
                                                                    columnNumber: 40
                                                                }, this),
                                                                "\xa0 ",
                                                                template.files,
                                                                " - ",
                                                                template.description
                                                            ]
                                                        }, void 0, true, {
                                                            fileName: "src/components/IT20127046/user/ViewDocumentTemplate.js",
                                                            lineNumber: 156,
                                                            columnNumber: 37
                                                        }, this),
                                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("a", {
                                                            className: "btn btn-sm btn-outline-primary m-2",
                                                            onClick: ()=>this.downloadFile(template.files)
                                                            ,
                                                            children: "Download"
                                                        }, void 0, false, {
                                                            fileName: "src/components/IT20127046/user/ViewDocumentTemplate.js",
                                                            lineNumber: 157,
                                                            columnNumber: 37
                                                        }, this)
                                                    ]
                                                }, index, true, {
                                                    fileName: "src/components/IT20127046/user/ViewDocumentTemplate.js",
                                                    lineNumber: 155,
                                                    columnNumber: 33
                                                }, this);
                                            })
                                        }, void 0, false, {
                                            fileName: "src/components/IT20127046/user/ViewDocumentTemplate.js",
                                            lineNumber: 151,
                                            columnNumber: 19
                                        }, this),
                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                            style: headlineBar,
                                            children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("h6", {
                                                children: "Final Thesis Template"
                                            }, void 0, false, {
                                                fileName: "src/components/IT20127046/user/ViewDocumentTemplate.js",
                                                lineNumber: 172,
                                                columnNumber: 21
                                            }, this)
                                        }, void 0, false, {
                                            fileName: "src/components/IT20127046/user/ViewDocumentTemplate.js",
                                            lineNumber: 171,
                                            columnNumber: 19
                                        }, this),
                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                            className: "container",
                                            children: this.state.documentTemplate.map((template, index)=>{
                                                if (template.documentType === "Final Thesis") return /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                                    className: "mb-2",
                                                    children: [
                                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("p", {
                                                            children: [
                                                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("i", {
                                                                    class: "fa fa-file-text-o",
                                                                    "aria-hidden": "true"
                                                                }, void 0, false, {
                                                                    fileName: "src/components/IT20127046/user/ViewDocumentTemplate.js",
                                                                    lineNumber: 179,
                                                                    columnNumber: 40
                                                                }, this),
                                                                "\xa0 ",
                                                                template.files,
                                                                " - ",
                                                                template.description
                                                            ]
                                                        }, void 0, true, {
                                                            fileName: "src/components/IT20127046/user/ViewDocumentTemplate.js",
                                                            lineNumber: 179,
                                                            columnNumber: 37
                                                        }, this),
                                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("a", {
                                                            className: "btn btn-sm btn-outline-primary m-2",
                                                            onClick: ()=>this.downloadFile(template.files)
                                                            ,
                                                            children: "Download"
                                                        }, void 0, false, {
                                                            fileName: "src/components/IT20127046/user/ViewDocumentTemplate.js",
                                                            lineNumber: 180,
                                                            columnNumber: 37
                                                        }, this)
                                                    ]
                                                }, index, true, {
                                                    fileName: "src/components/IT20127046/user/ViewDocumentTemplate.js",
                                                    lineNumber: 178,
                                                    columnNumber: 33
                                                }, this);
                                            })
                                        }, void 0, false, {
                                            fileName: "src/components/IT20127046/user/ViewDocumentTemplate.js",
                                            lineNumber: 174,
                                            columnNumber: 19
                                        }, this),
                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                            style: headlineBar,
                                            children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("h6", {
                                                children: "Other Template"
                                            }, void 0, false, {
                                                fileName: "src/components/IT20127046/user/ViewDocumentTemplate.js",
                                                lineNumber: 195,
                                                columnNumber: 21
                                            }, this)
                                        }, void 0, false, {
                                            fileName: "src/components/IT20127046/user/ViewDocumentTemplate.js",
                                            lineNumber: 194,
                                            columnNumber: 19
                                        }, this),
                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                            className: "container",
                                            children: this.state.documentTemplate.map((template, index)=>{
                                                if (!(template.documentType === "Proposal" || template.documentType === "Presentation" || template.documentType === "Final Thesis")) return /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                                    className: "mb-2",
                                                    children: [
                                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("p", {
                                                            children: [
                                                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("i", {
                                                                    class: "fa fa-file-text-o",
                                                                    "aria-hidden": "true"
                                                                }, void 0, false, {
                                                                    fileName: "src/components/IT20127046/user/ViewDocumentTemplate.js",
                                                                    lineNumber: 202,
                                                                    columnNumber: 40
                                                                }, this),
                                                                "\xa0 ",
                                                                template.files,
                                                                " - ",
                                                                template.description
                                                            ]
                                                        }, void 0, true, {
                                                            fileName: "src/components/IT20127046/user/ViewDocumentTemplate.js",
                                                            lineNumber: 202,
                                                            columnNumber: 37
                                                        }, this),
                                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("a", {
                                                            className: "btn btn-sm btn-outline-primary m-2",
                                                            onClick: ()=>this.downloadFile(template.files)
                                                            ,
                                                            children: "Download"
                                                        }, void 0, false, {
                                                            fileName: "src/components/IT20127046/user/ViewDocumentTemplate.js",
                                                            lineNumber: 203,
                                                            columnNumber: 37
                                                        }, this)
                                                    ]
                                                }, index, true, {
                                                    fileName: "src/components/IT20127046/user/ViewDocumentTemplate.js",
                                                    lineNumber: 201,
                                                    columnNumber: 33
                                                }, this);
                                            })
                                        }, void 0, false, {
                                            fileName: "src/components/IT20127046/user/ViewDocumentTemplate.js",
                                            lineNumber: 197,
                                            columnNumber: 19
                                        }, this)
                                    ]
                                }, void 0, true, {
                                    fileName: "src/components/IT20127046/user/ViewDocumentTemplate.js",
                                    lineNumber: 114,
                                    columnNumber: 17
                                }, this),
                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                    className: "col-3",
                                    children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV(_rightSidePanelDefault.default, {}, void 0, false, {
                                        fileName: "src/components/IT20127046/user/ViewDocumentTemplate.js",
                                        lineNumber: 218,
                                        columnNumber: 19
                                    }, this)
                                }, void 0, false, {
                                    fileName: "src/components/IT20127046/user/ViewDocumentTemplate.js",
                                    lineNumber: 217,
                                    columnNumber: 17
                                }, this)
                            ]
                        }, void 0, true, {
                            fileName: "src/components/IT20127046/user/ViewDocumentTemplate.js",
                            lineNumber: 113,
                            columnNumber: 15
                        }, this)
                    }, void 0, false, {
                        fileName: "src/components/IT20127046/user/ViewDocumentTemplate.js",
                        lineNumber: 112,
                        columnNumber: 13
                    }, this)
                ]
            }, void 0, true, {
                fileName: "src/components/IT20127046/user/ViewDocumentTemplate.js",
                lineNumber: 108,
                columnNumber: 11
            }, this) : loginRegLink
        }, void 0, false, {
            fileName: "src/components/IT20127046/user/ViewDocumentTemplate.js",
            lineNumber: 106,
            columnNumber: 7
        }, this);
    }
}
exports.default = ViewDocumentTemplate;

  $parcel$ReactRefreshHelpers$a519.postlude(module);
} finally {
  window.$RefreshReg$ = prevRefreshReg;
  window.$RefreshSig$ = prevRefreshSig;
}
},{"react/jsx-dev-runtime":"iTorj","react":"21dqq","axios":"jo6P5","jwt-decode":"ljM1w","js-file-download":"cLODl","../RightSidePanel":"kymky","@parcel/transformer-js/src/esmodule-helpers.js":"gkKU3","@parcel/transformer-react-refresh-wrap/lib/helpers/helpers.js":"km3Ru"}],"eqFYY":[function(require,module,exports) {
var $parcel$ReactRefreshHelpers$4489 = require("@parcel/transformer-react-refresh-wrap/lib/helpers/helpers.js");
var prevRefreshReg = window.$RefreshReg$;
var prevRefreshSig = window.$RefreshSig$;
$parcel$ReactRefreshHelpers$4489.prelude(module);

try {
var parcelHelpers = require("@parcel/transformer-js/src/esmodule-helpers.js");
parcelHelpers.defineInteropFlag(exports);
var _jsxDevRuntime = require("react/jsx-dev-runtime");
var _react = require("react");
var _reactDefault = parcelHelpers.interopDefault(_react);
var _axios = require("axios");
var _axiosDefault = parcelHelpers.interopDefault(_axios);
var _jwtDecode = require("jwt-decode");
var _jwtDecodeDefault = parcelHelpers.interopDefault(_jwtDecode);
var _jsFileDownload = require("js-file-download");
var _jsFileDownloadDefault = parcelHelpers.interopDefault(_jsFileDownload);
var _rightSidePanel = require("../RightSidePanel");
var _rightSidePanelDefault = parcelHelpers.interopDefault(_rightSidePanel);
class DisplayMarkingScheme extends _react.Component {
    constructor(){
        super();
        this.state = {
            userType: "",
            markingSchemTitle: [],
            markingCriteria: [],
            moduleName: "",
            assignment: ""
        };
    }
    componentDidMount() {
        document.title = "Marking Scheme";
        // Check user login
        if (!localStorage.userToken) swal({
            title: "Please login first",
            icon: "warning"
        }).then((value)=>{
            if (value) {
                this.props.history.push("/user/login");
                window.location.reload();
            }
        });
        else {
            const usertoken = localStorage.userToken;
            const decoded = _jwtDecodeDefault.default(usertoken);
            this.setState({
                userType: decoded.type
            });
        }
        this.getMarkingCriteria();
    }
    getMarkingCriteria() {
        const titleID = this.props.match.params.id;
        // Get marking scheme title details
        _axiosDefault.default.get(`http://localhost:5000/markingTitle/get/${titleID}`).then((res)=>{
            if (res.data.success) {
                this.setState({
                    markingSchemTitle: res.data.schemeTitle,
                    moduleName: res.data.schemeTitle.moduleName,
                    assignment: res.data.schemeTitle.assignment
                });
                console.log(this.state.markingSchemTitle);
            }
        });
        // Get marking scheme criterias
        _axiosDefault.default.get(`http://localhost:5000/markings/get/${titleID}`).then((res)=>{
            if (res.data.success) this.setState({
                markingCriteria: res.data.existingMarkingCriteria
            });
        //console.log(this.state.markingCriteria);
        });
    }
    render() {
        const loginRegLink = /*#__PURE__*/ _jsxDevRuntime.jsxDEV("ul", {
            className: "nav nav-tabs",
            children: [
                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("li", {
                    className: "nav-item",
                    children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("a", {
                        className: "nav-link",
                        "aria-current": "page",
                        href: "/user/login",
                        style: {
                            textDecoration: "none",
                            color: "white"
                        },
                        children: "Login"
                    }, void 0, false, {
                        fileName: "src/components/IT20127046/user/DisplayMarkingScheme.js",
                        lineNumber: 76,
                        columnNumber: 11
                    }, this)
                }, void 0, false, {
                    fileName: "src/components/IT20127046/user/DisplayMarkingScheme.js",
                    lineNumber: 75,
                    columnNumber: 9
                }, this),
                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("li", {
                    className: "nav-item",
                    children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("a", {
                        className: "nav-link",
                        "aria-current": "page",
                        href: "/user/registration",
                        style: {
                            textDecoration: "none",
                            color: "white"
                        },
                        children: "Register"
                    }, void 0, false, {
                        fileName: "src/components/IT20127046/user/DisplayMarkingScheme.js",
                        lineNumber: 86,
                        columnNumber: 11
                    }, this)
                }, void 0, false, {
                    fileName: "src/components/IT20127046/user/DisplayMarkingScheme.js",
                    lineNumber: 85,
                    columnNumber: 9
                }, this)
            ]
        }, void 0, true, {
            fileName: "src/components/IT20127046/user/DisplayMarkingScheme.js",
            lineNumber: 74,
            columnNumber: 7
        }, this);
        // Internal CSS
        const headlineBar = {
            backgroundColor: "#DCDCDC",
            padding: "5px",
            marginBottom: "5px"
        };
        return /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
            className: "container",
            children: localStorage.userToken ? /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                children: [
                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("br", {}, void 0, false, {
                        fileName: "src/components/IT20127046/user/DisplayMarkingScheme.js",
                        lineNumber: 109,
                        columnNumber: 13
                    }, this),
                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("h4", {
                        children: [
                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("i", {
                                class: "fa fa-table",
                                "aria-hidden": "true"
                            }, void 0, false, {
                                fileName: "src/components/IT20127046/user/DisplayMarkingScheme.js",
                                lineNumber: 110,
                                columnNumber: 17
                            }, this),
                            "\xa0\xa0Marking Schemes"
                        ]
                    }, void 0, true, {
                        fileName: "src/components/IT20127046/user/DisplayMarkingScheme.js",
                        lineNumber: 110,
                        columnNumber: 13
                    }, this),
                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("hr", {}, void 0, false, {
                        fileName: "src/components/IT20127046/user/DisplayMarkingScheme.js",
                        lineNumber: 111,
                        columnNumber: 13
                    }, this),
                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                        className: "container",
                        children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                            className: "row",
                            children: [
                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                    className: "col-9",
                                    children: [
                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                            style: headlineBar,
                                            children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("h6", {
                                                children: "Note"
                                            }, void 0, false, {
                                                fileName: "src/components/IT20127046/user/DisplayMarkingScheme.js",
                                                lineNumber: 116,
                                                columnNumber: 21
                                            }, this)
                                        }, void 0, false, {
                                            fileName: "src/components/IT20127046/user/DisplayMarkingScheme.js",
                                            lineNumber: 115,
                                            columnNumber: 19
                                        }, this),
                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("p", {
                                            children: [
                                                "Document Template Document Template Document Template Document Template Document Template",
                                                " "
                                            ]
                                        }, void 0, true, {
                                            fileName: "src/components/IT20127046/user/DisplayMarkingScheme.js",
                                            lineNumber: 118,
                                            columnNumber: 19
                                        }, this),
                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                            style: headlineBar,
                                            children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("h6", {
                                                children: "Marking Schemes"
                                            }, void 0, false, {
                                                fileName: "src/components/IT20127046/user/DisplayMarkingScheme.js",
                                                lineNumber: 124,
                                                columnNumber: 21
                                            }, this)
                                        }, void 0, false, {
                                            fileName: "src/components/IT20127046/user/DisplayMarkingScheme.js",
                                            lineNumber: 123,
                                            columnNumber: 19
                                        }, this),
                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("br", {}, void 0, false, {
                                            fileName: "src/components/IT20127046/user/DisplayMarkingScheme.js",
                                            lineNumber: 126,
                                            columnNumber: 19
                                        }, this),
                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                            className: "container",
                                            children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                                className: "container border border-secondary",
                                                children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                                    className: "container p-4",
                                                    children: [
                                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("center", {
                                                            children: [
                                                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("h5", {
                                                                    children: "Marking Schem"
                                                                }, void 0, false, {
                                                                    fileName: "src/components/IT20127046/user/DisplayMarkingScheme.js",
                                                                    lineNumber: 132,
                                                                    columnNumber: 27
                                                                }, this),
                                                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("h5", {
                                                                    children: this.state.moduleName
                                                                }, void 0, false, {
                                                                    fileName: "src/components/IT20127046/user/DisplayMarkingScheme.js",
                                                                    lineNumber: 133,
                                                                    columnNumber: 27
                                                                }, this),
                                                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("h6", {
                                                                    children: this.state.assignment
                                                                }, void 0, false, {
                                                                    fileName: "src/components/IT20127046/user/DisplayMarkingScheme.js",
                                                                    lineNumber: 134,
                                                                    columnNumber: 27
                                                                }, this)
                                                            ]
                                                        }, void 0, true, {
                                                            fileName: "src/components/IT20127046/user/DisplayMarkingScheme.js",
                                                            lineNumber: 131,
                                                            columnNumber: 25
                                                        }, this),
                                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("table", {
                                                            className: "table",
                                                            children: [
                                                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("thead", {
                                                                    children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("tr", {
                                                                        className: "b ",
                                                                        children: [
                                                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("th", {
                                                                                scope: "col",
                                                                                children: "No"
                                                                            }, void 0, false, {
                                                                                fileName: "src/components/IT20127046/user/DisplayMarkingScheme.js",
                                                                                lineNumber: 140,
                                                                                columnNumber: 31
                                                                            }, this),
                                                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("th", {
                                                                                scope: "col",
                                                                                children: "Criteria"
                                                                            }, void 0, false, {
                                                                                fileName: "src/components/IT20127046/user/DisplayMarkingScheme.js",
                                                                                lineNumber: 141,
                                                                                columnNumber: 31
                                                                            }, this),
                                                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("th", {
                                                                                scope: "col",
                                                                                children: "Allocate Mark"
                                                                            }, void 0, false, {
                                                                                fileName: "src/components/IT20127046/user/DisplayMarkingScheme.js",
                                                                                lineNumber: 142,
                                                                                columnNumber: 31
                                                                            }, this)
                                                                        ]
                                                                    }, void 0, true, {
                                                                        fileName: "src/components/IT20127046/user/DisplayMarkingScheme.js",
                                                                        lineNumber: 139,
                                                                        columnNumber: 29
                                                                    }, this)
                                                                }, void 0, false, {
                                                                    fileName: "src/components/IT20127046/user/DisplayMarkingScheme.js",
                                                                    lineNumber: 138,
                                                                    columnNumber: 27
                                                                }, this),
                                                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("tbody", {
                                                                    children: this.state.markingCriteria.map((data, index)=>/*#__PURE__*/ _jsxDevRuntime.jsxDEV("tr", {
                                                                            children: [
                                                                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("th", {
                                                                                    children: index + 1
                                                                                }, void 0, false, {
                                                                                    fileName: "src/components/IT20127046/user/DisplayMarkingScheme.js",
                                                                                    lineNumber: 149,
                                                                                    columnNumber: 33
                                                                                }, this),
                                                                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("td", {
                                                                                    children: data.criteria
                                                                                }, void 0, false, {
                                                                                    fileName: "src/components/IT20127046/user/DisplayMarkingScheme.js",
                                                                                    lineNumber: 150,
                                                                                    columnNumber: 33
                                                                                }, this),
                                                                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("td", {
                                                                                    children: [
                                                                                        data.allocateMark,
                                                                                        " "
                                                                                    ]
                                                                                }, void 0, true, {
                                                                                    fileName: "src/components/IT20127046/user/DisplayMarkingScheme.js",
                                                                                    lineNumber: 151,
                                                                                    columnNumber: 33
                                                                                }, this)
                                                                            ]
                                                                        }, index, true, {
                                                                            fileName: "src/components/IT20127046/user/DisplayMarkingScheme.js",
                                                                            lineNumber: 148,
                                                                            columnNumber: 31
                                                                        }, this)
                                                                    )
                                                                }, void 0, false, {
                                                                    fileName: "src/components/IT20127046/user/DisplayMarkingScheme.js",
                                                                    lineNumber: 146,
                                                                    columnNumber: 27
                                                                }, this)
                                                            ]
                                                        }, void 0, true, {
                                                            fileName: "src/components/IT20127046/user/DisplayMarkingScheme.js",
                                                            lineNumber: 137,
                                                            columnNumber: 25
                                                        }, this),
                                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("br", {}, void 0, false, {
                                                            fileName: "src/components/IT20127046/user/DisplayMarkingScheme.js",
                                                            lineNumber: 156,
                                                            columnNumber: 25
                                                        }, this),
                                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("br", {}, void 0, false, {
                                                            fileName: "src/components/IT20127046/user/DisplayMarkingScheme.js",
                                                            lineNumber: 157,
                                                            columnNumber: 25
                                                        }, this)
                                                    ]
                                                }, void 0, true, {
                                                    fileName: "src/components/IT20127046/user/DisplayMarkingScheme.js",
                                                    lineNumber: 130,
                                                    columnNumber: 23
                                                }, this)
                                            }, void 0, false, {
                                                fileName: "src/components/IT20127046/user/DisplayMarkingScheme.js",
                                                lineNumber: 129,
                                                columnNumber: 21
                                            }, this)
                                        }, void 0, false, {
                                            fileName: "src/components/IT20127046/user/DisplayMarkingScheme.js",
                                            lineNumber: 128,
                                            columnNumber: 19
                                        }, this)
                                    ]
                                }, void 0, true, {
                                    fileName: "src/components/IT20127046/user/DisplayMarkingScheme.js",
                                    lineNumber: 114,
                                    columnNumber: 17
                                }, this),
                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                    className: "col-3",
                                    children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV(_rightSidePanelDefault.default, {}, void 0, false, {
                                        fileName: "src/components/IT20127046/user/DisplayMarkingScheme.js",
                                        lineNumber: 163,
                                        columnNumber: 19
                                    }, this)
                                }, void 0, false, {
                                    fileName: "src/components/IT20127046/user/DisplayMarkingScheme.js",
                                    lineNumber: 162,
                                    columnNumber: 17
                                }, this)
                            ]
                        }, void 0, true, {
                            fileName: "src/components/IT20127046/user/DisplayMarkingScheme.js",
                            lineNumber: 113,
                            columnNumber: 15
                        }, this)
                    }, void 0, false, {
                        fileName: "src/components/IT20127046/user/DisplayMarkingScheme.js",
                        lineNumber: 112,
                        columnNumber: 13
                    }, this)
                ]
            }, void 0, true, {
                fileName: "src/components/IT20127046/user/DisplayMarkingScheme.js",
                lineNumber: 108,
                columnNumber: 11
            }, this) : loginRegLink
        }, void 0, false, {
            fileName: "src/components/IT20127046/user/DisplayMarkingScheme.js",
            lineNumber: 106,
            columnNumber: 7
        }, this);
    }
}
exports.default = DisplayMarkingScheme;

  $parcel$ReactRefreshHelpers$4489.postlude(module);
} finally {
  window.$RefreshReg$ = prevRefreshReg;
  window.$RefreshSig$ = prevRefreshSig;
}
},{"react/jsx-dev-runtime":"iTorj","react":"21dqq","axios":"jo6P5","jwt-decode":"ljM1w","js-file-download":"cLODl","../RightSidePanel":"kymky","@parcel/transformer-js/src/esmodule-helpers.js":"gkKU3","@parcel/transformer-react-refresh-wrap/lib/helpers/helpers.js":"km3Ru"}],"dnRrr":[function(require,module,exports) {
var $parcel$ReactRefreshHelpers$a39a = require("@parcel/transformer-react-refresh-wrap/lib/helpers/helpers.js");
var prevRefreshReg = window.$RefreshReg$;
var prevRefreshSig = window.$RefreshSig$;
$parcel$ReactRefreshHelpers$a39a.prelude(module);

try {
var parcelHelpers = require("@parcel/transformer-js/src/esmodule-helpers.js");
parcelHelpers.defineInteropFlag(exports);
var _jsxDevRuntime = require("react/jsx-dev-runtime");
var _react = require("react");
var _reactDefault = parcelHelpers.interopDefault(_react);
var _axios = require("axios");
var _axiosDefault = parcelHelpers.interopDefault(_axios);
var _jwtDecode = require("jwt-decode");
var _jwtDecodeDefault = parcelHelpers.interopDefault(_jwtDecode);
var _sweetalert = require("sweetalert");
var _sweetalertDefault = parcelHelpers.interopDefault(_sweetalert);
var _rightSidePanel = require("../RightSidePanel");
var _rightSidePanelDefault = parcelHelpers.interopDefault(_rightSidePanel);
class ContactUs extends _react.Component {
    constructor(){
        super();
        this.state = {
            userType: "",
            users: [],
            studentMsgs: [],
            userID: "",
            userName: "",
            panelMember: "",
            subject: "",
            message: "",
            replay: "",
            viewMyMsgSection: false
        };
    }
    handleInputChange = (e)=>{
        const { name , value  } = e.target;
        this.setState({
            ...this.state,
            [name]: value
        });
    };
    handleChange = (e)=>{
        const { name , value  } = e.target;
        this.setState({
            ...this.state,
            [name]: value
        });
    };
    componentDidMount() {
        document.title = "Contact Us";
        // Check user login
        if (!localStorage.userToken) _sweetalertDefault.default({
            title: "Please login first",
            icon: "warning"
        }).then((value)=>{
            if (value) {
                this.props.history.push("/user/login");
                window.location.reload();
            }
        });
        else {
            const usertoken = localStorage.userToken;
            const decoded = _jwtDecodeDefault.default(usertoken);
            this.setState({
                userType: decoded.type,
                userID: decoded.idNumber,
                userName: decoded.name
            });
        }
        this.getUserEmails();
        this.getStudentMsg();
    }
    // ***** Student Role Functions ***** //
    // Get user emails
    getUserEmails() {
        _axiosDefault.default.get("http://localhost:5000/users").then((res)=>{
            if (res.data.success) this.setState({
                users: res.data.existingUsers
            });
        //console.log(this.state.users);
        });
    }
    // Add student messages
    onSubmitMsg = (e)=>{
        e.preventDefault();
        const { userID , userName , panelMember , subject , message  } = this.state;
        const data = {
            userID: userID,
            userName: userName,
            panelMember: panelMember,
            subject: subject,
            message: message,
            replay: "Pending"
        };
        console.log(data);
        _axiosDefault.default.post("http://localhost:5000/supprtMsg/add", data).then((res)=>{
            if (res.data.success) _sweetalertDefault.default("Message Send Successful");
        });
    };
    // Display student messages section
    onViewMyMsg() {
        this.setState({
            viewMyMsgSection: true
        });
    }
    // ***** Panel Member and Supervisor Role Functions ***** //
    // Get Student Messages
    getStudentMsg() {
        _axiosDefault.default.get("http://localhost:5000/supprtMsg/getAll").then((res)=>{
            if (res.data.success) this.setState({
                studentMsgs: res.data.existingMessages
            });
        //console.log(this.state.users);
        });
    }
    // Send Replay
    onSendReplay(replayID) {
        _sweetalertDefault.default("Enter Replay:", {
            content: "input"
        }).then((replay)=>{
            const data = {
                replay: replay
            };
            _axiosDefault.default.put(`http://localhost:5000/supprtMsg/update/${replayID}`, data).then((res)=>{
                if (res.data.success) _sweetalertDefault.default("Replay Send Successful");
            });
        });
    }
    // Delete Student Message
    onDeleteMsg(MsgID) {
        _sweetalertDefault.default({
            title: "Are you sure?",
            text: "Delete this Message",
            icon: "warning",
            buttons: true,
            dangerMode: true
        }).then((willDelete)=>{
            if (willDelete) _axiosDefault.default.delete(`http://localhost:5000/supprtMsg/delete/${MsgID}`).then((res)=>{
                _sweetalertDefault.default("Deleted Successfull!", {
                    icon: "success"
                });
                setTimeout(()=>{
                    this.getStudentMsg();
                }, 1000);
            });
        });
    }
    render() {
        // Redirect to login page
        const loginRegLink = /*#__PURE__*/ _jsxDevRuntime.jsxDEV("ul", {
            className: "nav nav-tabs",
            children: [
                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("li", {
                    className: "nav-item",
                    children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("a", {
                        className: "nav-link",
                        "aria-current": "page",
                        href: "/user/login",
                        style: {
                            textDecoration: "none",
                            color: "white"
                        },
                        children: "Login"
                    }, void 0, false, {
                        fileName: "src/components/IT20127046/user/ContactUs.js",
                        lineNumber: 181,
                        columnNumber: 11
                    }, this)
                }, void 0, false, {
                    fileName: "src/components/IT20127046/user/ContactUs.js",
                    lineNumber: 180,
                    columnNumber: 9
                }, this),
                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("li", {
                    className: "nav-item",
                    children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("a", {
                        className: "nav-link",
                        "aria-current": "page",
                        href: "/user/registration",
                        style: {
                            textDecoration: "none",
                            color: "white"
                        },
                        children: "Register"
                    }, void 0, false, {
                        fileName: "src/components/IT20127046/user/ContactUs.js",
                        lineNumber: 191,
                        columnNumber: 11
                    }, this)
                }, void 0, false, {
                    fileName: "src/components/IT20127046/user/ContactUs.js",
                    lineNumber: 190,
                    columnNumber: 9
                }, this)
            ]
        }, void 0, true, {
            fileName: "src/components/IT20127046/user/ContactUs.js",
            lineNumber: 179,
            columnNumber: 7
        }, this);
        // Internal CSS
        const headlineBar = {
            backgroundColor: "#DCDCDC",
            padding: "5px",
            marginBottom: "5px"
        };
        // Check Student and Panel Member/Supervisor
        let groupChatLink; // Group chat link
        let supportMsg; // Support msg section -> Student (Send Msg) & Panel Member/Supervisor -> View Msgs
        let myMsgStudent; // Student -> View sent msgs
        if (this.state.userType === "Student") {
            groupChatLink = /*#__PURE__*/ _jsxDevRuntime.jsxDEV("a", {
                className: "btn btn-outline-success mb-4 p-4",
                href: "/chatAppStudent",
                children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("i", {
                    class: "fa fa-comments fa-2xl",
                    "aria-hidden": "true",
                    children: "\xa0Join Group Chat"
                }, void 0, false, {
                    fileName: "src/components/IT20127046/user/ContactUs.js",
                    lineNumber: 219,
                    columnNumber: 11
                }, this)
            }, void 0, false, {
                fileName: "src/components/IT20127046/user/ContactUs.js",
                lineNumber: 218,
                columnNumber: 9
            }, this);
            supportMsg = /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                className: "p-3 mb-2 bg-light text-dark",
                style: {
                    maxWidth: "500px"
                },
                children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("form", {
                    onSubmit: this.onSubmitMsg,
                    children: [
                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("label", {
                            className: "form-label",
                            children: "Panel Member or Supervisor"
                        }, void 0, false, {
                            fileName: "src/components/IT20127046/user/ContactUs.js",
                            lineNumber: 230,
                            columnNumber: 13
                        }, this),
                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("select", {
                            style: {
                                maxWidth: "400px"
                            },
                            className: "form-select",
                            "aria-label": "Default select example",
                            name: "panelMember",
                            value: this.state.panelMember,
                            onChange: this.handleInputChange,
                            children: [
                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("option", {
                                    defaultValue: true,
                                    children: "Select"
                                }, void 0, false, {
                                    fileName: "src/components/IT20127046/user/ContactUs.js",
                                    lineNumber: 239,
                                    columnNumber: 15
                                }, this),
                                this.state.users.map((user, index)=>{
                                    if (user.type === "Supervisor" || user.type === "Panel Member") return /*#__PURE__*/ _jsxDevRuntime.jsxDEV("option", {
                                        value: user.name,
                                        children: user.name
                                    }, void 0, false, {
                                        fileName: "src/components/IT20127046/user/ContactUs.js",
                                        lineNumber: 245,
                                        columnNumber: 26
                                    }, this);
                                })
                            ]
                        }, void 0, true, {
                            fileName: "src/components/IT20127046/user/ContactUs.js",
                            lineNumber: 231,
                            columnNumber: 13
                        }, this),
                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("label", {
                            htmlFor: "subject",
                            children: "Subject"
                        }, void 0, false, {
                            fileName: "src/components/IT20127046/user/ContactUs.js",
                            lineNumber: 249,
                            columnNumber: 13
                        }, this),
                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("input", {
                            style: {
                                maxWidth: "400px"
                            },
                            type: "text",
                            className: "form-control",
                            id: "subject",
                            name: "subject",
                            value: this.state.subject,
                            onChange: this.handleInputChange,
                            required: true
                        }, void 0, false, {
                            fileName: "src/components/IT20127046/user/ContactUs.js",
                            lineNumber: 250,
                            columnNumber: 13
                        }, this),
                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("label", {
                            htmlFor: "message",
                            children: "Message"
                        }, void 0, false, {
                            fileName: "src/components/IT20127046/user/ContactUs.js",
                            lineNumber: 260,
                            columnNumber: 13
                        }, this),
                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("textarea", {
                            style: {
                                maxWidth: "400px"
                            },
                            rows: "3",
                            type: "text",
                            className: "form-control",
                            id: "message",
                            name: "message",
                            value: this.state.message,
                            onChange: this.handleInputChange,
                            required: true
                        }, void 0, false, {
                            fileName: "src/components/IT20127046/user/ContactUs.js",
                            lineNumber: 261,
                            columnNumber: 13
                        }, this),
                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("button", {
                            type: "submit",
                            className: "btn btn-outline-success mt-2 mb-2",
                            children: "Send"
                        }, void 0, false, {
                            fileName: "src/components/IT20127046/user/ContactUs.js",
                            lineNumber: 273,
                            columnNumber: 13
                        }, this)
                    ]
                }, void 0, true, {
                    fileName: "src/components/IT20127046/user/ContactUs.js",
                    lineNumber: 229,
                    columnNumber: 11
                }, this)
            }, void 0, false, {
                fileName: "src/components/IT20127046/user/ContactUs.js",
                lineNumber: 225,
                columnNumber: 9
            }, this);
            myMsgStudent = /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                children: [
                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("a", {
                        className: "btn btn-outline-success",
                        onClick: ()=>this.onViewMyMsg()
                        ,
                        children: "My Messages"
                    }, void 0, false, {
                        fileName: "src/components/IT20127046/user/ContactUs.js",
                        lineNumber: 281,
                        columnNumber: 11
                    }, this),
                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("br", {}, void 0, false, {
                        fileName: "src/components/IT20127046/user/ContactUs.js",
                        lineNumber: 287,
                        columnNumber: 11
                    }, this),
                    !this.state.viewMyMsgSection ? /*#__PURE__*/ _jsxDevRuntime.jsxDEV("p", {}, void 0, false, {
                        fileName: "src/components/IT20127046/user/ContactUs.js",
                        lineNumber: 290,
                        columnNumber: 13
                    }, this) : /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                        className: "p-3 mb-2 bg-light text-dark",
                        children: [
                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("button", {
                                className: "btn btn-outline-danger",
                                onClick: ()=>{
                                    this.setState({
                                        viewMyMsgSection: false
                                    });
                                },
                                children: "X"
                            }, void 0, false, {
                                fileName: "src/components/IT20127046/user/ContactUs.js",
                                lineNumber: 293,
                                columnNumber: 15
                            }, this),
                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("table", {
                                className: "table",
                                children: [
                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("thead", {
                                        children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("tr", {
                                            className: "b ",
                                            children: [
                                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("th", {
                                                    scope: "col",
                                                    children: "No"
                                                }, void 0, false, {
                                                    fileName: "src/components/IT20127046/user/ContactUs.js",
                                                    lineNumber: 297,
                                                    columnNumber: 21
                                                }, this),
                                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("th", {
                                                    scope: "col",
                                                    children: "Panal Member"
                                                }, void 0, false, {
                                                    fileName: "src/components/IT20127046/user/ContactUs.js",
                                                    lineNumber: 298,
                                                    columnNumber: 21
                                                }, this),
                                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("th", {
                                                    scope: "col",
                                                    children: "Title"
                                                }, void 0, false, {
                                                    fileName: "src/components/IT20127046/user/ContactUs.js",
                                                    lineNumber: 299,
                                                    columnNumber: 21
                                                }, this),
                                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("th", {
                                                    scope: "col",
                                                    children: "Message"
                                                }, void 0, false, {
                                                    fileName: "src/components/IT20127046/user/ContactUs.js",
                                                    lineNumber: 300,
                                                    columnNumber: 21
                                                }, this),
                                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("th", {
                                                    scope: "col",
                                                    children: "Reply"
                                                }, void 0, false, {
                                                    fileName: "src/components/IT20127046/user/ContactUs.js",
                                                    lineNumber: 301,
                                                    columnNumber: 21
                                                }, this)
                                            ]
                                        }, void 0, true, {
                                            fileName: "src/components/IT20127046/user/ContactUs.js",
                                            lineNumber: 296,
                                            columnNumber: 19
                                        }, this)
                                    }, void 0, false, {
                                        fileName: "src/components/IT20127046/user/ContactUs.js",
                                        lineNumber: 295,
                                        columnNumber: 17
                                    }, this),
                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("tbody", {
                                        children: this.state.studentMsgs.map((msg, index)=>{
                                            if (msg.userID === this.state.userID) return /*#__PURE__*/ _jsxDevRuntime.jsxDEV("tr", {
                                                children: [
                                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("td", {
                                                        children: index + 1
                                                    }, void 0, false, {
                                                        fileName: "src/components/IT20127046/user/ContactUs.js",
                                                        lineNumber: 309,
                                                        columnNumber: 27
                                                    }, this),
                                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("td", {
                                                        children: msg.panelMember
                                                    }, void 0, false, {
                                                        fileName: "src/components/IT20127046/user/ContactUs.js",
                                                        lineNumber: 310,
                                                        columnNumber: 27
                                                    }, this),
                                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("td", {
                                                        children: msg.subject
                                                    }, void 0, false, {
                                                        fileName: "src/components/IT20127046/user/ContactUs.js",
                                                        lineNumber: 311,
                                                        columnNumber: 27
                                                    }, this),
                                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("td", {
                                                        children: msg.message
                                                    }, void 0, false, {
                                                        fileName: "src/components/IT20127046/user/ContactUs.js",
                                                        lineNumber: 312,
                                                        columnNumber: 27
                                                    }, this),
                                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("td", {
                                                        children: msg.replay
                                                    }, void 0, false, {
                                                        fileName: "src/components/IT20127046/user/ContactUs.js",
                                                        lineNumber: 313,
                                                        columnNumber: 27
                                                    }, this)
                                                ]
                                            }, index, true, {
                                                fileName: "src/components/IT20127046/user/ContactUs.js",
                                                lineNumber: 308,
                                                columnNumber: 25
                                            }, this);
                                        })
                                    }, void 0, false, {
                                        fileName: "src/components/IT20127046/user/ContactUs.js",
                                        lineNumber: 304,
                                        columnNumber: 17
                                    }, this)
                                ]
                            }, void 0, true, {
                                fileName: "src/components/IT20127046/user/ContactUs.js",
                                lineNumber: 294,
                                columnNumber: 15
                            }, this)
                        ]
                    }, void 0, true, {
                        fileName: "src/components/IT20127046/user/ContactUs.js",
                        lineNumber: 292,
                        columnNumber: 13
                    }, this)
                ]
            }, void 0, true, {
                fileName: "src/components/IT20127046/user/ContactUs.js",
                lineNumber: 280,
                columnNumber: 9
            }, this);
        } else if (this.state.userType === "Supervisor" || this.state.userType === "Panel Member") {
            groupChatLink = /*#__PURE__*/ _jsxDevRuntime.jsxDEV("a", {
                className: "btn btn-outline-success mb-4 p-4",
                href: "/chatAppAdmin",
                children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("i", {
                    class: "fa fa-comments fa-2xl",
                    "aria-hidden": "true",
                    children: "\xa0Join Group Chat"
                }, void 0, false, {
                    fileName: "src/components/IT20127046/user/ContactUs.js",
                    lineNumber: 330,
                    columnNumber: 11
                }, this)
            }, void 0, false, {
                fileName: "src/components/IT20127046/user/ContactUs.js",
                lineNumber: 329,
                columnNumber: 9
            }, this);
            supportMsg = /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                className: "p-3 mb-2 bg-light text-dark",
                children: [
                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("h6", {
                        children: "Student Messages"
                    }, void 0, false, {
                        fileName: "src/components/IT20127046/user/ContactUs.js",
                        lineNumber: 337,
                        columnNumber: 11
                    }, this),
                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("table", {
                        className: "table",
                        children: [
                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("thead", {
                                children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("tr", {
                                    className: "b ",
                                    children: [
                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("th", {
                                            scope: "col",
                                            children: "No"
                                        }, void 0, false, {
                                            fileName: "src/components/IT20127046/user/ContactUs.js",
                                            lineNumber: 342,
                                            columnNumber: 17
                                        }, this),
                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("th", {
                                            scope: "col",
                                            children: "Student ID"
                                        }, void 0, false, {
                                            fileName: "src/components/IT20127046/user/ContactUs.js",
                                            lineNumber: 343,
                                            columnNumber: 17
                                        }, this),
                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("th", {
                                            scope: "col",
                                            children: "Student Name"
                                        }, void 0, false, {
                                            fileName: "src/components/IT20127046/user/ContactUs.js",
                                            lineNumber: 344,
                                            columnNumber: 17
                                        }, this),
                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("th", {
                                            scope: "col",
                                            children: "Title"
                                        }, void 0, false, {
                                            fileName: "src/components/IT20127046/user/ContactUs.js",
                                            lineNumber: 345,
                                            columnNumber: 17
                                        }, this),
                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("th", {
                                            scope: "col",
                                            children: "Message"
                                        }, void 0, false, {
                                            fileName: "src/components/IT20127046/user/ContactUs.js",
                                            lineNumber: 346,
                                            columnNumber: 17
                                        }, this),
                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("th", {
                                            scope: "col",
                                            children: "Replay"
                                        }, void 0, false, {
                                            fileName: "src/components/IT20127046/user/ContactUs.js",
                                            lineNumber: 347,
                                            columnNumber: 17
                                        }, this)
                                    ]
                                }, void 0, true, {
                                    fileName: "src/components/IT20127046/user/ContactUs.js",
                                    lineNumber: 341,
                                    columnNumber: 15
                                }, this)
                            }, void 0, false, {
                                fileName: "src/components/IT20127046/user/ContactUs.js",
                                lineNumber: 340,
                                columnNumber: 13
                            }, this),
                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("tbody", {
                                children: this.state.studentMsgs.map((msg, index)=>/*#__PURE__*/ _jsxDevRuntime.jsxDEV("tr", {
                                        children: [
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("th", {
                                                children: index + 1
                                            }, void 0, false, {
                                                fileName: "src/components/IT20127046/user/ContactUs.js",
                                                lineNumber: 354,
                                                columnNumber: 19
                                            }, this),
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("td", {
                                                children: msg.userID
                                            }, void 0, false, {
                                                fileName: "src/components/IT20127046/user/ContactUs.js",
                                                lineNumber: 355,
                                                columnNumber: 19
                                            }, this),
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("td", {
                                                children: msg.userName
                                            }, void 0, false, {
                                                fileName: "src/components/IT20127046/user/ContactUs.js",
                                                lineNumber: 356,
                                                columnNumber: 19
                                            }, this),
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("td", {
                                                children: msg.subject
                                            }, void 0, false, {
                                                fileName: "src/components/IT20127046/user/ContactUs.js",
                                                lineNumber: 357,
                                                columnNumber: 19
                                            }, this),
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("td", {
                                                children: msg.message
                                            }, void 0, false, {
                                                fileName: "src/components/IT20127046/user/ContactUs.js",
                                                lineNumber: 358,
                                                columnNumber: 19
                                            }, this),
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("td", {
                                                children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("a", {
                                                    className: "btn btn-outline-success",
                                                    onClick: ()=>this.onSendReplay(msg._id)
                                                    ,
                                                    children: "Send Replay"
                                                }, void 0, false, {
                                                    fileName: "src/components/IT20127046/user/ContactUs.js",
                                                    lineNumber: 360,
                                                    columnNumber: 21
                                                }, this)
                                            }, void 0, false, {
                                                fileName: "src/components/IT20127046/user/ContactUs.js",
                                                lineNumber: 359,
                                                columnNumber: 19
                                            }, this),
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("td", {
                                                className: "text-center",
                                                children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("a", {
                                                    className: "btn btn-outline-danger",
                                                    onClick: ()=>this.onDeleteMsg(msg._id)
                                                    ,
                                                    children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("i", {
                                                        className: "fa fa-trash"
                                                    }, void 0, false, {
                                                        fileName: "src/components/IT20127046/user/ContactUs.js",
                                                        lineNumber: 373,
                                                        columnNumber: 23
                                                    }, this)
                                                }, void 0, false, {
                                                    fileName: "src/components/IT20127046/user/ContactUs.js",
                                                    lineNumber: 369,
                                                    columnNumber: 21
                                                }, this)
                                            }, void 0, false, {
                                                fileName: "src/components/IT20127046/user/ContactUs.js",
                                                lineNumber: 368,
                                                columnNumber: 19
                                            }, this)
                                        ]
                                    }, index, true, {
                                        fileName: "src/components/IT20127046/user/ContactUs.js",
                                        lineNumber: 353,
                                        columnNumber: 17
                                    }, this)
                                )
                            }, void 0, false, {
                                fileName: "src/components/IT20127046/user/ContactUs.js",
                                lineNumber: 351,
                                columnNumber: 13
                            }, this)
                        ]
                    }, void 0, true, {
                        fileName: "src/components/IT20127046/user/ContactUs.js",
                        lineNumber: 339,
                        columnNumber: 11
                    }, this)
                ]
            }, void 0, true, {
                fileName: "src/components/IT20127046/user/ContactUs.js",
                lineNumber: 336,
                columnNumber: 9
            }, this);
        }
        return /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
            className: "container",
            children: localStorage.userToken ? /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                children: [
                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("br", {}, void 0, false, {
                        fileName: "src/components/IT20127046/user/ContactUs.js",
                        lineNumber: 388,
                        columnNumber: 13
                    }, this),
                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("h4", {
                        children: "Contact Us"
                    }, void 0, false, {
                        fileName: "src/components/IT20127046/user/ContactUs.js",
                        lineNumber: 389,
                        columnNumber: 13
                    }, this),
                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("hr", {}, void 0, false, {
                        fileName: "src/components/IT20127046/user/ContactUs.js",
                        lineNumber: 390,
                        columnNumber: 13
                    }, this),
                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                        className: "container",
                        children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                            className: "row",
                            children: [
                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                    className: "col-9",
                                    children: [
                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                            style: headlineBar,
                                            children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("h6", {
                                                children: "Chat with Group"
                                            }, void 0, false, {
                                                fileName: "src/components/IT20127046/user/ContactUs.js",
                                                lineNumber: 395,
                                                columnNumber: 21
                                            }, this)
                                        }, void 0, false, {
                                            fileName: "src/components/IT20127046/user/ContactUs.js",
                                            lineNumber: 394,
                                            columnNumber: 19
                                        }, this),
                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                            className: "container",
                                            children: [
                                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("p", {
                                                    children: "Chat with your group"
                                                }, void 0, false, {
                                                    fileName: "src/components/IT20127046/user/ContactUs.js",
                                                    lineNumber: 398,
                                                    columnNumber: 21
                                                }, this),
                                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                                    className: "",
                                                    children: groupChatLink
                                                }, void 0, false, {
                                                    fileName: "src/components/IT20127046/user/ContactUs.js",
                                                    lineNumber: 399,
                                                    columnNumber: 21
                                                }, this)
                                            ]
                                        }, void 0, true, {
                                            fileName: "src/components/IT20127046/user/ContactUs.js",
                                            lineNumber: 397,
                                            columnNumber: 19
                                        }, this),
                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                            style: headlineBar,
                                            children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("h6", {
                                                children: "Support Message"
                                            }, void 0, false, {
                                                fileName: "src/components/IT20127046/user/ContactUs.js",
                                                lineNumber: 403,
                                                columnNumber: 21
                                            }, this)
                                        }, void 0, false, {
                                            fileName: "src/components/IT20127046/user/ContactUs.js",
                                            lineNumber: 402,
                                            columnNumber: 19
                                        }, this),
                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("br", {}, void 0, false, {
                                            fileName: "src/components/IT20127046/user/ContactUs.js",
                                            lineNumber: 405,
                                            columnNumber: 19
                                        }, this),
                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                            className: "container",
                                            children: supportMsg
                                        }, void 0, false, {
                                            fileName: "src/components/IT20127046/user/ContactUs.js",
                                            lineNumber: 407,
                                            columnNumber: 19
                                        }, this),
                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                            className: "container",
                                            children: myMsgStudent
                                        }, void 0, false, {
                                            fileName: "src/components/IT20127046/user/ContactUs.js",
                                            lineNumber: 408,
                                            columnNumber: 19
                                        }, this),
                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("br", {}, void 0, false, {
                                            fileName: "src/components/IT20127046/user/ContactUs.js",
                                            lineNumber: 410,
                                            columnNumber: 19
                                        }, this),
                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                            style: headlineBar,
                                            children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("h6", {
                                                children: "Contact Info"
                                            }, void 0, false, {
                                                fileName: "src/components/IT20127046/user/ContactUs.js",
                                                lineNumber: 413,
                                                columnNumber: 21
                                            }, this)
                                        }, void 0, false, {
                                            fileName: "src/components/IT20127046/user/ContactUs.js",
                                            lineNumber: 412,
                                            columnNumber: 19
                                        }, this),
                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("br", {}, void 0, false, {
                                            fileName: "src/components/IT20127046/user/ContactUs.js",
                                            lineNumber: 415,
                                            columnNumber: 19
                                        }, this),
                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                            className: "container",
                                            children: this.state.users.map((user, index)=>{
                                                if (user.type === "Supervisor" || user.type === "Panel Member") return /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                                    className: "p-3 mb-2 bg-light text-dark",
                                                    style: {
                                                        "max-width": "400px"
                                                    },
                                                    children: [
                                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("p", {
                                                            children: [
                                                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("i", {
                                                                    className: "fa fa-user",
                                                                    "aria-hidden": "true"
                                                                }, void 0, false, {
                                                                    fileName: "src/components/IT20127046/user/ContactUs.js",
                                                                    lineNumber: 429,
                                                                    columnNumber: 31
                                                                }, this),
                                                                "\xa0",
                                                                user.type,
                                                                " - ",
                                                                user.name
                                                            ]
                                                        }, void 0, true, {
                                                            fileName: "src/components/IT20127046/user/ContactUs.js",
                                                            lineNumber: 428,
                                                            columnNumber: 29
                                                        }, this),
                                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("p", {
                                                            children: [
                                                                " ",
                                                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("i", {
                                                                    className: "fa fa-envelope",
                                                                    "aria-hidden": "true"
                                                                }, void 0, false, {
                                                                    fileName: "src/components/IT20127046/user/ContactUs.js",
                                                                    lineNumber: 434,
                                                                    columnNumber: 31
                                                                }, this),
                                                                "\xa0Email - ",
                                                                user.email
                                                            ]
                                                        }, void 0, true, {
                                                            fileName: "src/components/IT20127046/user/ContactUs.js",
                                                            lineNumber: 432,
                                                            columnNumber: 29
                                                        }, this)
                                                    ]
                                                }, index, true, {
                                                    fileName: "src/components/IT20127046/user/ContactUs.js",
                                                    lineNumber: 423,
                                                    columnNumber: 27
                                                }, this);
                                            })
                                        }, void 0, false, {
                                            fileName: "src/components/IT20127046/user/ContactUs.js",
                                            lineNumber: 416,
                                            columnNumber: 19
                                        }, this),
                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("br", {}, void 0, false, {
                                            fileName: "src/components/IT20127046/user/ContactUs.js",
                                            lineNumber: 445,
                                            columnNumber: 19
                                        }, this)
                                    ]
                                }, void 0, true, {
                                    fileName: "src/components/IT20127046/user/ContactUs.js",
                                    lineNumber: 393,
                                    columnNumber: 17
                                }, this),
                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                    className: "col-3",
                                    children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV(_rightSidePanelDefault.default, {}, void 0, false, {
                                        fileName: "src/components/IT20127046/user/ContactUs.js",
                                        lineNumber: 448,
                                        columnNumber: 19
                                    }, this)
                                }, void 0, false, {
                                    fileName: "src/components/IT20127046/user/ContactUs.js",
                                    lineNumber: 447,
                                    columnNumber: 17
                                }, this)
                            ]
                        }, void 0, true, {
                            fileName: "src/components/IT20127046/user/ContactUs.js",
                            lineNumber: 392,
                            columnNumber: 15
                        }, this)
                    }, void 0, false, {
                        fileName: "src/components/IT20127046/user/ContactUs.js",
                        lineNumber: 391,
                        columnNumber: 13
                    }, this)
                ]
            }, void 0, true, {
                fileName: "src/components/IT20127046/user/ContactUs.js",
                lineNumber: 387,
                columnNumber: 11
            }, this) : loginRegLink
        }, void 0, false, {
            fileName: "src/components/IT20127046/user/ContactUs.js",
            lineNumber: 385,
            columnNumber: 7
        }, this);
    }
}
exports.default = ContactUs;

  $parcel$ReactRefreshHelpers$a39a.postlude(module);
} finally {
  window.$RefreshReg$ = prevRefreshReg;
  window.$RefreshSig$ = prevRefreshSig;
}
},{"react/jsx-dev-runtime":"iTorj","react":"21dqq","axios":"jo6P5","jwt-decode":"ljM1w","sweetalert":"38a65","../RightSidePanel":"kymky","@parcel/transformer-js/src/esmodule-helpers.js":"gkKU3","@parcel/transformer-react-refresh-wrap/lib/helpers/helpers.js":"km3Ru"}],"7I6As":[function(require,module,exports) {
var $parcel$ReactRefreshHelpers$40ec = require("@parcel/transformer-react-refresh-wrap/lib/helpers/helpers.js");
var prevRefreshReg = window.$RefreshReg$;
var prevRefreshSig = window.$RefreshSig$;
$parcel$ReactRefreshHelpers$40ec.prelude(module);

try {
var parcelHelpers = require("@parcel/transformer-js/src/esmodule-helpers.js");
parcelHelpers.defineInteropFlag(exports);
var _jsxDevRuntime = require("react/jsx-dev-runtime");
var _react = require("react");
var _reactDefault = parcelHelpers.interopDefault(_react);
var _axios = require("axios");
var _axiosDefault = parcelHelpers.interopDefault(_axios);
var _jwtDecode = require("jwt-decode");
var _jwtDecodeDefault = parcelHelpers.interopDefault(_jwtDecode);
var _rightSidePanel = require("../RightSidePanel");
var _rightSidePanelDefault = parcelHelpers.interopDefault(_rightSidePanel);
var _squadUnitJpeg = require("../../../../public/images/SquadUnit.jpeg");
var _squadUnitJpegDefault = parcelHelpers.interopDefault(_squadUnitJpeg);
var _blankProfilePng = require("../../../../public/images/BlankProfile.png");
var _blankProfilePngDefault = parcelHelpers.interopDefault(_blankProfilePng);
var _sliitresearchLogoPng = require("../../../../public/images/SLIITResearchLogo.png");
var _sliitresearchLogoPngDefault = parcelHelpers.interopDefault(_sliitresearchLogoPng);
class AboutUs extends _react.Component {
    constructor(){
        super();
        this.state = {
            userType: ""
        };
    }
    componentDidMount() {
        document.title = "About Us";
        // Check user login
        if (!localStorage.userToken) swal({
            title: "Please login first",
            icon: "warning"
        }).then((value)=>{
            if (value) {
                this.props.history.push("/user/login");
                window.location.reload();
            }
        });
        else {
            const usertoken = localStorage.userToken;
            const decoded = _jwtDecodeDefault.default(usertoken);
            this.setState({
                userType: decoded.type
            });
        }
    }
    render() {
        // Internal CSS
        const headlineBar = {
            backgroundColor: "#DCDCDC",
            padding: "5px",
            marginBottom: "5px"
        };
        return /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
            className: "container",
            children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                children: [
                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("br", {}, void 0, false, {
                        fileName: "src/components/IT20127046/user/AboutUs.js",
                        lineNumber: 51,
                        columnNumber: 11
                    }, this),
                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("h4", {
                        children: "About Us"
                    }, void 0, false, {
                        fileName: "src/components/IT20127046/user/AboutUs.js",
                        lineNumber: 52,
                        columnNumber: 11
                    }, this),
                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("hr", {}, void 0, false, {
                        fileName: "src/components/IT20127046/user/AboutUs.js",
                        lineNumber: 53,
                        columnNumber: 11
                    }, this),
                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                        className: "container",
                        children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                            className: "row",
                            children: [
                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                    className: "col-9",
                                    children: [
                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                            style: headlineBar,
                                            children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("h6", {
                                                children: "About the System"
                                            }, void 0, false, {
                                                fileName: "src/components/IT20127046/user/AboutUs.js",
                                                lineNumber: 58,
                                                columnNumber: 19
                                            }, this)
                                        }, void 0, false, {
                                            fileName: "src/components/IT20127046/user/AboutUs.js",
                                            lineNumber: 57,
                                            columnNumber: 17
                                        }, this),
                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                            className: "container",
                                            children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("center", {
                                                children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("img", {
                                                    src: _sliitresearchLogoPngDefault.default,
                                                    style: {
                                                        width: "220px",
                                                        height: "220px"
                                                    }
                                                }, void 0, false, {
                                                    fileName: "src/components/IT20127046/user/AboutUs.js",
                                                    lineNumber: 62,
                                                    columnNumber: 21
                                                }, this)
                                            }, void 0, false, {
                                                fileName: "src/components/IT20127046/user/AboutUs.js",
                                                lineNumber: 61,
                                                columnNumber: 19
                                            }, this)
                                        }, void 0, false, {
                                            fileName: "src/components/IT20127046/user/AboutUs.js",
                                            lineNumber: 60,
                                            columnNumber: 17
                                        }, this),
                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                            className: "container",
                                            children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("p", {
                                                children: "SLIIT Research project team is looking for a system, which has the capability of managing the Research project and automating certain tasks."
                                            }, void 0, false, {
                                                fileName: "src/components/IT20127046/user/AboutUs.js",
                                                lineNumber: 69,
                                                columnNumber: 19
                                            }, this)
                                        }, void 0, false, {
                                            fileName: "src/components/IT20127046/user/AboutUs.js",
                                            lineNumber: 68,
                                            columnNumber: 17
                                        }, this),
                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                            style: headlineBar,
                                            children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("h6", {
                                                children: "How to Use"
                                            }, void 0, false, {
                                                fileName: "src/components/IT20127046/user/AboutUs.js",
                                                lineNumber: 77,
                                                columnNumber: 19
                                            }, this)
                                        }, void 0, false, {
                                            fileName: "src/components/IT20127046/user/AboutUs.js",
                                            lineNumber: 76,
                                            columnNumber: 17
                                        }, this),
                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                            className: "container",
                                            children: [
                                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("p", {
                                                    children: "All SLIIT undergraduates must do a research project in their 4th year. This Research Project module is a 16 credit, two semesters long project. A student group must find a research topic in a specific research field and send it to a supervisor who has interests in the same research field. Once the supervisor accepted the topic, they must find a co-supervisor of the same research interest."
                                                }, void 0, false, {
                                                    fileName: "src/components/IT20127046/user/AboutUs.js",
                                                    lineNumber: 80,
                                                    columnNumber: 19
                                                }, this),
                                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("p", {
                                                    children: "After finalizing the supervisor student must send a document including the topic details of the Research. Then topic evaluation panel will evaluate the topic and send feedback to the student group. If the topic is accepted, they can continue to do the project. If rejected, they must find a new topic and submit it back."
                                                }, void 0, false, {
                                                    fileName: "src/components/IT20127046/user/AboutUs.js",
                                                    lineNumber: 89,
                                                    columnNumber: 19
                                                }, this),
                                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("p", {
                                                    children: "There are several evaluation stages. Document submissions are evaluated by the supervisor or the co-supervisor. Presentations will be evaluated by a separate panel. The final Thesis will be Double evaluated by the supervisor and a blind reviewer."
                                                }, void 0, false, {
                                                    fileName: "src/components/IT20127046/user/AboutUs.js",
                                                    lineNumber: 97,
                                                    columnNumber: 19
                                                }, this)
                                            ]
                                        }, void 0, true, {
                                            fileName: "src/components/IT20127046/user/AboutUs.js",
                                            lineNumber: 79,
                                            columnNumber: 17
                                        }, this),
                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                            style: headlineBar,
                                            children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("h6", {
                                                children: "Developed By"
                                            }, void 0, false, {
                                                fileName: "src/components/IT20127046/user/AboutUs.js",
                                                lineNumber: 107,
                                                columnNumber: 19
                                            }, this)
                                        }, void 0, false, {
                                            fileName: "src/components/IT20127046/user/AboutUs.js",
                                            lineNumber: 106,
                                            columnNumber: 17
                                        }, this),
                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("br", {}, void 0, false, {
                                            fileName: "src/components/IT20127046/user/AboutUs.js",
                                            lineNumber: 109,
                                            columnNumber: 17
                                        }, this),
                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                            className: "container",
                                            children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("center", {
                                                children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("img", {
                                                    src: _squadUnitJpegDefault.default,
                                                    style: {
                                                        width: "220px",
                                                        height: "180px"
                                                    }
                                                }, void 0, false, {
                                                    fileName: "src/components/IT20127046/user/AboutUs.js",
                                                    lineNumber: 112,
                                                    columnNumber: 21
                                                }, this)
                                            }, void 0, false, {
                                                fileName: "src/components/IT20127046/user/AboutUs.js",
                                                lineNumber: 111,
                                                columnNumber: 19
                                            }, this)
                                        }, void 0, false, {
                                            fileName: "src/components/IT20127046/user/AboutUs.js",
                                            lineNumber: 110,
                                            columnNumber: 17
                                        }, this),
                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("br", {}, void 0, false, {
                                            fileName: "src/components/IT20127046/user/AboutUs.js",
                                            lineNumber: 118,
                                            columnNumber: 17
                                        }, this),
                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                            className: "container",
                                            children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                                className: "row",
                                                children: [
                                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                                        className: "col p-3 mb-2 m-2 bg-light text-dark",
                                                        children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("center", {
                                                            children: [
                                                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("img", {
                                                                    src: _blankProfilePngDefault.default,
                                                                    style: {
                                                                        width: "150px",
                                                                        height: "150px",
                                                                        marginBottom: "5px"
                                                                    }
                                                                }, void 0, false, {
                                                                    fileName: "src/components/IT20127046/user/AboutUs.js",
                                                                    lineNumber: 123,
                                                                    columnNumber: 25
                                                                }, this),
                                                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("p", {
                                                                    children: "Samararathna L.H."
                                                                }, void 0, false, {
                                                                    fileName: "src/components/IT20127046/user/AboutUs.js",
                                                                    lineNumber: 131,
                                                                    columnNumber: 25
                                                                }, this),
                                                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("p", {
                                                                    children: "Undergraduate Software Engineering"
                                                                }, void 0, false, {
                                                                    fileName: "src/components/IT20127046/user/AboutUs.js",
                                                                    lineNumber: 132,
                                                                    columnNumber: 25
                                                                }, this),
                                                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("p", {
                                                                    children: "Sri Lanka Institute of Information Technology"
                                                                }, void 0, false, {
                                                                    fileName: "src/components/IT20127046/user/AboutUs.js",
                                                                    lineNumber: 133,
                                                                    columnNumber: 25
                                                                }, this),
                                                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("p", {
                                                                    children: "Sri Lanka"
                                                                }, void 0, false, {
                                                                    fileName: "src/components/IT20127046/user/AboutUs.js",
                                                                    lineNumber: 134,
                                                                    columnNumber: 25
                                                                }, this)
                                                            ]
                                                        }, void 0, true, {
                                                            fileName: "src/components/IT20127046/user/AboutUs.js",
                                                            lineNumber: 122,
                                                            columnNumber: 23
                                                        }, this)
                                                    }, void 0, false, {
                                                        fileName: "src/components/IT20127046/user/AboutUs.js",
                                                        lineNumber: 121,
                                                        columnNumber: 21
                                                    }, this),
                                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                                        className: "col p-3 mb-2 m-2 bg-light text-dark",
                                                        children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("center", {
                                                            children: [
                                                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("img", {
                                                                    src: _blankProfilePngDefault.default,
                                                                    style: {
                                                                        width: "150px",
                                                                        height: "150px",
                                                                        marginBottom: "5px"
                                                                    }
                                                                }, void 0, false, {
                                                                    fileName: "src/components/IT20127046/user/AboutUs.js",
                                                                    lineNumber: 139,
                                                                    columnNumber: 25
                                                                }, this),
                                                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("p", {
                                                                    children: "Kumarasinghe K.M.K.D."
                                                                }, void 0, false, {
                                                                    fileName: "src/components/IT20127046/user/AboutUs.js",
                                                                    lineNumber: 147,
                                                                    columnNumber: 25
                                                                }, this),
                                                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("p", {
                                                                    children: "Undergraduate Software Engineering"
                                                                }, void 0, false, {
                                                                    fileName: "src/components/IT20127046/user/AboutUs.js",
                                                                    lineNumber: 148,
                                                                    columnNumber: 25
                                                                }, this),
                                                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("p", {
                                                                    children: "Sri Lanka Institute of Information Technology"
                                                                }, void 0, false, {
                                                                    fileName: "src/components/IT20127046/user/AboutUs.js",
                                                                    lineNumber: 149,
                                                                    columnNumber: 25
                                                                }, this),
                                                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("p", {
                                                                    children: "Sri Lanka"
                                                                }, void 0, false, {
                                                                    fileName: "src/components/IT20127046/user/AboutUs.js",
                                                                    lineNumber: 150,
                                                                    columnNumber: 25
                                                                }, this)
                                                            ]
                                                        }, void 0, true, {
                                                            fileName: "src/components/IT20127046/user/AboutUs.js",
                                                            lineNumber: 138,
                                                            columnNumber: 23
                                                        }, this)
                                                    }, void 0, false, {
                                                        fileName: "src/components/IT20127046/user/AboutUs.js",
                                                        lineNumber: 137,
                                                        columnNumber: 21
                                                    }, this),
                                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                                        className: "col p-3 mb-2 m-2 bg-light text-dark",
                                                        children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("center", {
                                                            children: [
                                                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("img", {
                                                                    src: _blankProfilePngDefault.default,
                                                                    style: {
                                                                        width: "150px",
                                                                        height: "150px",
                                                                        marginBottom: "5px"
                                                                    }
                                                                }, void 0, false, {
                                                                    fileName: "src/components/IT20127046/user/AboutUs.js",
                                                                    lineNumber: 155,
                                                                    columnNumber: 25
                                                                }, this),
                                                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("p", {
                                                                    children: "Mendis T.C.U."
                                                                }, void 0, false, {
                                                                    fileName: "src/components/IT20127046/user/AboutUs.js",
                                                                    lineNumber: 163,
                                                                    columnNumber: 25
                                                                }, this),
                                                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("p", {
                                                                    children: "Undergraduate Software Engineering"
                                                                }, void 0, false, {
                                                                    fileName: "src/components/IT20127046/user/AboutUs.js",
                                                                    lineNumber: 164,
                                                                    columnNumber: 25
                                                                }, this),
                                                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("p", {
                                                                    children: "Sri Lanka Institute of Information Technology"
                                                                }, void 0, false, {
                                                                    fileName: "src/components/IT20127046/user/AboutUs.js",
                                                                    lineNumber: 165,
                                                                    columnNumber: 25
                                                                }, this),
                                                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("p", {
                                                                    children: "Sri Lanka"
                                                                }, void 0, false, {
                                                                    fileName: "src/components/IT20127046/user/AboutUs.js",
                                                                    lineNumber: 166,
                                                                    columnNumber: 25
                                                                }, this)
                                                            ]
                                                        }, void 0, true, {
                                                            fileName: "src/components/IT20127046/user/AboutUs.js",
                                                            lineNumber: 154,
                                                            columnNumber: 23
                                                        }, this)
                                                    }, void 0, false, {
                                                        fileName: "src/components/IT20127046/user/AboutUs.js",
                                                        lineNumber: 153,
                                                        columnNumber: 21
                                                    }, this),
                                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                                        className: "col p-3 mb-2 m-2 bg-light text-dark",
                                                        children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("center", {
                                                            children: [
                                                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("img", {
                                                                    src: _blankProfilePngDefault.default,
                                                                    style: {
                                                                        width: "150px",
                                                                        height: "150px",
                                                                        marginBottom: "5px"
                                                                    }
                                                                }, void 0, false, {
                                                                    fileName: "src/components/IT20127046/user/AboutUs.js",
                                                                    lineNumber: 171,
                                                                    columnNumber: 25
                                                                }, this),
                                                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("p", {
                                                                    children: "Herath G.G.T.K."
                                                                }, void 0, false, {
                                                                    fileName: "src/components/IT20127046/user/AboutUs.js",
                                                                    lineNumber: 179,
                                                                    columnNumber: 25
                                                                }, this),
                                                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("p", {
                                                                    children: "Undergraduate Software Engineering"
                                                                }, void 0, false, {
                                                                    fileName: "src/components/IT20127046/user/AboutUs.js",
                                                                    lineNumber: 180,
                                                                    columnNumber: 25
                                                                }, this),
                                                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("p", {
                                                                    children: "Sri Lanka Institute of Information Technology"
                                                                }, void 0, false, {
                                                                    fileName: "src/components/IT20127046/user/AboutUs.js",
                                                                    lineNumber: 181,
                                                                    columnNumber: 25
                                                                }, this),
                                                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("p", {
                                                                    children: "Sri Lanka"
                                                                }, void 0, false, {
                                                                    fileName: "src/components/IT20127046/user/AboutUs.js",
                                                                    lineNumber: 182,
                                                                    columnNumber: 25
                                                                }, this)
                                                            ]
                                                        }, void 0, true, {
                                                            fileName: "src/components/IT20127046/user/AboutUs.js",
                                                            lineNumber: 170,
                                                            columnNumber: 23
                                                        }, this)
                                                    }, void 0, false, {
                                                        fileName: "src/components/IT20127046/user/AboutUs.js",
                                                        lineNumber: 169,
                                                        columnNumber: 21
                                                    }, this)
                                                ]
                                            }, void 0, true, {
                                                fileName: "src/components/IT20127046/user/AboutUs.js",
                                                lineNumber: 120,
                                                columnNumber: 19
                                            }, this)
                                        }, void 0, false, {
                                            fileName: "src/components/IT20127046/user/AboutUs.js",
                                            lineNumber: 119,
                                            columnNumber: 17
                                        }, this),
                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("br", {}, void 0, false, {
                                            fileName: "src/components/IT20127046/user/AboutUs.js",
                                            lineNumber: 187,
                                            columnNumber: 17
                                        }, this),
                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                            style: headlineBar,
                                            children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("h6", {
                                                children: "Terms and Conditions"
                                            }, void 0, false, {
                                                fileName: "src/components/IT20127046/user/AboutUs.js",
                                                lineNumber: 190,
                                                columnNumber: 19
                                            }, this)
                                        }, void 0, false, {
                                            fileName: "src/components/IT20127046/user/AboutUs.js",
                                            lineNumber: 189,
                                            columnNumber: 17
                                        }, this),
                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                            className: "container",
                                            children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("p", {
                                                children: "Terms and Conditions"
                                            }, void 0, false, {
                                                fileName: "src/components/IT20127046/user/AboutUs.js",
                                                lineNumber: 193,
                                                columnNumber: 19
                                            }, this)
                                        }, void 0, false, {
                                            fileName: "src/components/IT20127046/user/AboutUs.js",
                                            lineNumber: 192,
                                            columnNumber: 17
                                        }, this)
                                    ]
                                }, void 0, true, {
                                    fileName: "src/components/IT20127046/user/AboutUs.js",
                                    lineNumber: 56,
                                    columnNumber: 15
                                }, this),
                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                    className: "col-3",
                                    children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV(_rightSidePanelDefault.default, {}, void 0, false, {
                                        fileName: "src/components/IT20127046/user/AboutUs.js",
                                        lineNumber: 197,
                                        columnNumber: 17
                                    }, this)
                                }, void 0, false, {
                                    fileName: "src/components/IT20127046/user/AboutUs.js",
                                    lineNumber: 196,
                                    columnNumber: 15
                                }, this)
                            ]
                        }, void 0, true, {
                            fileName: "src/components/IT20127046/user/AboutUs.js",
                            lineNumber: 55,
                            columnNumber: 13
                        }, this)
                    }, void 0, false, {
                        fileName: "src/components/IT20127046/user/AboutUs.js",
                        lineNumber: 54,
                        columnNumber: 11
                    }, this)
                ]
            }, void 0, true, {
                fileName: "src/components/IT20127046/user/AboutUs.js",
                lineNumber: 50,
                columnNumber: 9
            }, this)
        }, void 0, false, {
            fileName: "src/components/IT20127046/user/AboutUs.js",
            lineNumber: 49,
            columnNumber: 7
        }, this);
    }
}
exports.default = AboutUs;

  $parcel$ReactRefreshHelpers$40ec.postlude(module);
} finally {
  window.$RefreshReg$ = prevRefreshReg;
  window.$RefreshSig$ = prevRefreshSig;
}
},{"react/jsx-dev-runtime":"iTorj","react":"21dqq","axios":"jo6P5","jwt-decode":"ljM1w","../RightSidePanel":"kymky","../../../../public/images/SquadUnit.jpeg":"hfSp5","../../../../public/images/BlankProfile.png":"hVlFR","../../../../public/images/SLIITResearchLogo.png":"2vpUV","@parcel/transformer-js/src/esmodule-helpers.js":"gkKU3","@parcel/transformer-react-refresh-wrap/lib/helpers/helpers.js":"km3Ru"}],"hfSp5":[function(require,module,exports) {
module.exports = require('./helpers/bundle-url').getBundleURL('bLxZJ') + "SquadUnit.c74a4a2d.jpeg" + "?" + Date.now();

},{"./helpers/bundle-url":"lgJ39"}],"hVlFR":[function(require,module,exports) {
module.exports = require('./helpers/bundle-url').getBundleURL('bLxZJ') + "BlankProfile.6331d218.png" + "?" + Date.now();

},{"./helpers/bundle-url":"lgJ39"}],"2exFL":[function(require,module,exports) {
var $parcel$ReactRefreshHelpers$016f = require("@parcel/transformer-react-refresh-wrap/lib/helpers/helpers.js");
var prevRefreshReg = window.$RefreshReg$;
var prevRefreshSig = window.$RefreshSig$;
$parcel$ReactRefreshHelpers$016f.prelude(module);

try {
var parcelHelpers = require("@parcel/transformer-js/src/esmodule-helpers.js");
parcelHelpers.defineInteropFlag(exports);
var _jsxDevRuntime = require("react/jsx-dev-runtime");
var _react = require("react");
var _reactDefault = parcelHelpers.interopDefault(_react);
var _axios = require("axios");
var _axiosDefault = parcelHelpers.interopDefault(_axios);
class CreateResponse extends _react.Component {
    constructor(props){
        super(props);
        this.state = {
            groupid: "",
            name: "",
            position: "",
            feedback: "",
            status: ""
        };
    }
    handleInputChange = (e)=>{
        const { name , value  } = e.target;
        this.setState({
            ...this.state,
            [name]: value
        });
    };
    handleChange = (e)=>{
        const { name , value  } = e.target;
        this.setState({
            ...this.state,
            [name]: value
        });
    };
    onSubmit = (e)=>{
        e.preventDefault();
        const { groupid , name , position , feedback , status  } = this.state;
        const data = {
            groupid: groupid,
            name: name,
            position: position,
            feedback: feedback,
            status: status
        };
        console.log(data);
        _axiosDefault.default.post("http://localhost:5000/request/save", data).then((res)=>{
            if (res.data.success) {
                swal("Response Successful", "", "success");
                this.setState({
                    groupid: "",
                    name: "",
                    position: "",
                    feedback: "",
                    status: ""
                });
            }
        });
    };
    render() {
        return /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
            className: "container px-5 my-5",
            children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                className: "container border border-dark bg-light mt-5 col-lg-10",
                children: [
                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                        className: "form-group row",
                        children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                            className: "col-lg-12 margin-tb",
                            children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                className: "float-left",
                                children: [
                                    "\xa0",
                                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("h2", {
                                        children: "Provide Response for students"
                                    }, void 0, false, {
                                        fileName: "src/components/IT20131456/user/CreateResponse.js",
                                        lineNumber: 71,
                                        columnNumber: 17
                                    }, this),
                                    "\xa0"
                                ]
                            }, void 0, true, {
                                fileName: "src/components/IT20131456/user/CreateResponse.js",
                                lineNumber: 69,
                                columnNumber: 15
                            }, this)
                        }, void 0, false, {
                            fileName: "src/components/IT20131456/user/CreateResponse.js",
                            lineNumber: 68,
                            columnNumber: 13
                        }, this)
                    }, void 0, false, {
                        fileName: "src/components/IT20131456/user/CreateResponse.js",
                        lineNumber: 67,
                        columnNumber: 11
                    }, this),
                    /*#__PURE__*/ _jsxDevRuntime.jsxDEV("form", {
                        onSubmit: this.onSubmit,
                        children: [
                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                className: "col-md-12",
                                children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                    className: "form-group",
                                    children: [
                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("strong", {
                                            children: "Group ID :"
                                        }, void 0, false, {
                                            fileName: "src/components/IT20131456/user/CreateResponse.js",
                                            lineNumber: 81,
                                            columnNumber: 19
                                        }, this),
                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("input", {
                                            type: "text",
                                            className: "form-control",
                                            placeholder: "Enter group id",
                                            name: "groupid",
                                            pattern: "G[0-9]{3}",
                                            title: "Group ID is Invalid",
                                            value: this.state.groupid,
                                            onChange: this.handleInputChange,
                                            required: true
                                        }, void 0, false, {
                                            fileName: "src/components/IT20131456/user/CreateResponse.js",
                                            lineNumber: 82,
                                            columnNumber: 19
                                        }, this)
                                    ]
                                }, void 0, true, {
                                    fileName: "src/components/IT20131456/user/CreateResponse.js",
                                    lineNumber: 80,
                                    columnNumber: 17
                                }, this)
                            }, void 0, false, {
                                fileName: "src/components/IT20131456/user/CreateResponse.js",
                                lineNumber: 79,
                                columnNumber: 11
                            }, this),
                            "\xa0",
                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                className: "col-md-12",
                                children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                    className: "form-group",
                                    children: [
                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("strong", {
                                            children: "Name :"
                                        }, void 0, false, {
                                            fileName: "src/components/IT20131456/user/CreateResponse.js",
                                            lineNumber: 99,
                                            columnNumber: 19
                                        }, this),
                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("input", {
                                            type: "text",
                                            className: "form-control",
                                            placeholder: "Enter name",
                                            name: "name",
                                            value: this.state.name,
                                            onChange: this.handleInputChange,
                                            required: true
                                        }, void 0, false, {
                                            fileName: "src/components/IT20131456/user/CreateResponse.js",
                                            lineNumber: 100,
                                            columnNumber: 19
                                        }, this)
                                    ]
                                }, void 0, true, {
                                    fileName: "src/components/IT20131456/user/CreateResponse.js",
                                    lineNumber: 98,
                                    columnNumber: 17
                                }, this)
                            }, void 0, false, {
                                fileName: "src/components/IT20131456/user/CreateResponse.js",
                                lineNumber: 97,
                                columnNumber: 15
                            }, this),
                            "\xa0",
                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                className: "col-md-12",
                                children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                    className: "form-group",
                                    children: [
                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("strong", {
                                            children: "Position :"
                                        }, void 0, false, {
                                            fileName: "src/components/IT20131456/user/CreateResponse.js",
                                            lineNumber: 114,
                                            columnNumber: 19
                                        }, this),
                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("select", {
                                            className: "form-control",
                                            name: "position",
                                            value: this.state.position,
                                            onChange: this.handleChange,
                                            children: [
                                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("option", {
                                                    value: "Not",
                                                    children: "Not Selected"
                                                }, void 0, false, {
                                                    fileName: "src/components/IT20131456/user/CreateResponse.js",
                                                    lineNumber: 121,
                                                    columnNumber: 21
                                                }, this),
                                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("option", {
                                                    value: "Supervisor",
                                                    children: "Supervisor "
                                                }, void 0, false, {
                                                    fileName: "src/components/IT20131456/user/CreateResponse.js",
                                                    lineNumber: 122,
                                                    columnNumber: 21
                                                }, this),
                                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("option", {
                                                    value: "Co Supervisor",
                                                    children: "Co Supervisor "
                                                }, void 0, false, {
                                                    fileName: "src/components/IT20131456/user/CreateResponse.js",
                                                    lineNumber: 123,
                                                    columnNumber: 21
                                                }, this)
                                            ]
                                        }, void 0, true, {
                                            fileName: "src/components/IT20131456/user/CreateResponse.js",
                                            lineNumber: 115,
                                            columnNumber: 19
                                        }, this)
                                    ]
                                }, void 0, true, {
                                    fileName: "src/components/IT20131456/user/CreateResponse.js",
                                    lineNumber: 113,
                                    columnNumber: 17
                                }, this)
                            }, void 0, false, {
                                fileName: "src/components/IT20131456/user/CreateResponse.js",
                                lineNumber: 112,
                                columnNumber: 15
                            }, this),
                            "\xa0",
                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                className: "col-md-12",
                                children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                    className: "form-group",
                                    children: [
                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("strong", {
                                            children: "Feedback (word count-100) :"
                                        }, void 0, false, {
                                            fileName: "src/components/IT20131456/user/CreateResponse.js",
                                            lineNumber: 132,
                                            columnNumber: 19
                                        }, this),
                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("textarea", {
                                            type: "text",
                                            className: "form-control",
                                            rows: "3",
                                            maxLength: "100",
                                            name: "feedback",
                                            placeholder: "Feedback ",
                                            value: this.state.feedback,
                                            onChange: this.handleChange,
                                            required: true
                                        }, void 0, false, {
                                            fileName: "src/components/IT20131456/user/CreateResponse.js",
                                            lineNumber: 133,
                                            columnNumber: 19
                                        }, this)
                                    ]
                                }, void 0, true, {
                                    fileName: "src/components/IT20131456/user/CreateResponse.js",
                                    lineNumber: 131,
                                    columnNumber: 17
                                }, this)
                            }, void 0, false, {
                                fileName: "src/components/IT20131456/user/CreateResponse.js",
                                lineNumber: 130,
                                columnNumber: 15
                            }, this),
                            "\xa0",
                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                className: "col-md-12",
                                children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                    className: "form-group",
                                    children: [
                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("strong", {
                                            children: "Status :"
                                        }, void 0, false, {
                                            fileName: "src/components/IT20131456/user/CreateResponse.js",
                                            lineNumber: 149,
                                            columnNumber: 19
                                        }, this),
                                        /*#__PURE__*/ _jsxDevRuntime.jsxDEV("select", {
                                            className: "form-control",
                                            name: "status",
                                            value: this.state.status,
                                            onChange: this.handleChange,
                                            children: [
                                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("option", {
                                                    value: "Not",
                                                    children: "Not Selected"
                                                }, void 0, false, {
                                                    fileName: "src/components/IT20131456/user/CreateResponse.js",
                                                    lineNumber: 156,
                                                    columnNumber: 21
                                                }, this),
                                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("option", {
                                                    value: "Accept",
                                                    children: "Accept "
                                                }, void 0, false, {
                                                    fileName: "src/components/IT20131456/user/CreateResponse.js",
                                                    lineNumber: 157,
                                                    columnNumber: 21
                                                }, this),
                                                /*#__PURE__*/ _jsxDevRuntime.jsxDEV("option", {
                                                    value: "Decline",
                                                    children: "Decline "
                                                }, void 0, false, {
                                                    fileName: "src/components/IT20131456/user/CreateResponse.js",
                                                    lineNumber: 158,
                                                    columnNumber: 21
                                                }, this)
                                            ]
                                        }, void 0, true, {
                                            fileName: "src/components/IT20131456/user/CreateResponse.js",
                                            lineNumber: 150,
                                            columnNumber: 19
                                        }, this)
                                    ]
                                }, void 0, true, {
                                    fileName: "src/components/IT20131456/user/CreateResponse.js",
                                    lineNumber: 148,
                                    columnNumber: 17
                                }, this)
                            }, void 0, false, {
                                fileName: "src/components/IT20131456/user/CreateResponse.js",
                                lineNumber: 147,
                                columnNumber: 15
                            }, this),
                            "\xa0",
                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                className: "col-md-12",
                                children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("div", {
                                    className: "form-group",
                                    children: /*#__PURE__*/ _jsxDevRuntime.jsxDEV("button", {
                                        className: "btn btn-outline-primary",
                                        type: "submit",
                                        children: [
                                            "\xa0",
                                            /*#__PURE__*/ _jsxDevRuntime.jsxDEV("i", {
                                                className: "fa fa-paper-plane",
                                                children: " Send "
                                            }, void 0, false, {
                                                fileName: "src/components/IT20131456/user/CreateResponse.js",
                                                lineNumber: 167,
                                                columnNumber: 25
                                            }, this)
                                        ]
                                    }, void 0, true, {
                                        fileName: "src/components/IT20131456/user/CreateResponse.js",
                                        lineNumber: 166,
                                        columnNumber: 17
                                    }, this)
                                }, void 0, false, {
                                    fileName: "src/components/IT20131456/user/CreateResponse.js",
                                    lineNumber: 165,
                                    columnNumber: 15
                                }, this)
                            }, void 0, false, {
                                fileName: "src/components/IT20131456/user/CreateResponse.js",
                                lineNumber: 164,
                                columnNumber: 13
                            }, this),
                            "\xa0"
                        ]
                    }, void 0, true, {
                        fileName: "src/components/IT20131456/user/CreateResponse.js",
                        lineNumber: 77,
                        columnNumber: 11
                    }, this)
                ]
            }, void 0, true, {
                fileName: "src/components/IT20131456/user/CreateResponse.js",
                lineNumber: 66,
                columnNumber: 9
            }, this)
        }, void 0, false, {
            fileName: "src/components/IT20131456/user/CreateResponse.js",
            lineNumber: 65,
            columnNumber: 7
        }, this);
    }
}
exports.default = CreateResponse;

  $parcel$ReactRefreshHelpers$016f.postlude(module);
} finally {
  window.$RefreshReg$ = prevRefreshReg;
  window.$RefreshSig$ = prevRefreshSig;
}
},{"react/jsx-dev-runtime":"iTorj","react":"21dqq","axios":"jo6P5","@parcel/transformer-js/src/esmodule-helpers.js":"gkKU3","@parcel/transformer-react-refresh-wrap/lib/helpers/helpers.js":"km3Ru"}]},["kn9T2","7fmqN","8lqZg"], "8lqZg", "parcelRequire10c2")

//# sourceMappingURL=index.975ef6c8.js.map
